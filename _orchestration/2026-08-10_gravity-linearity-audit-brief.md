# Lane brief — the gravity-sector linearity audit (2026-08-10) — RUNS FIRST

**Goal (verify-not-derive):** trace every gravity-sector derivation that lands a
GR-exact result (lensing, redshift, the static force chain, perihelion, Shapiro) to
the exact line where LINEAR-in-ε₁₁ coupling enters, and name its license. The
tension of record: `graded-network-response.md` licenses δn ≈ +¼A² (QUADRATIC,
kernel-class) while the gravity chain runs ε₁₁ = 7GM/c²r LINEARLY to GR-exact
results; the #951 S^p lemma (pending verify) says no kernel-riding response can do
that. Either canon holds a license we haven't identified — which resolves the
linear-coupling fork from existing corpus — or the linearity is UNLICENSED and the
lemma exposed a standing inconsistency.

**Deliverable:** the license map — per derivation site: the file:line where
linearity enters; the verdict LICENSED(name the structure) / UNLICENSED /
AMBIGUOUS(both readings possible, quote both). Plus: whether the licenses (if
found) are ONE structure or several; and what the map implies for the four-arm
fork (which arms it moots or selects). Also fold: the routed eq_axiom_4.tex
:24-vs-:56-59 contradiction (ε₁₁ = 1 at two radii) — adjacent, verify and include.

**Fences:** verify-not-derive (no repairs, no new couplings); two-method receipts,
named engines; SVA §0; freeze the site list before classifying;
[DO-NOT-MERGE][REVIEW: pending-orchestrator]; own Tier-2. The #951 lemma is
pending orchestrator verify — consume it as PROVISIONAL input, tagged.

---
**DATED PRIOR-ART CONSUMPTION LIST (2026-08-10 sweep; two-method receipts in the
sweep record):** NOT-A-DUPLICATE — no prior linearity-license audit exists; the PPN
coherence audit (PR #91, research/2026-06-05_gravity-ppn-coherence-result.md)
settled COEFFICIENT coherence while ASSUMING the linear form — its S1-S4 site list
seeds this lane's frozen list. MANDATORY CONSUMES: the W1/W2 walk-backs as applied
at HEAD (PR #90 — do not re-litigate; classify post-relabel statements); the
graded-network quadratic row's provenance (b56ee5df 2026-06-22; the ASYM −¼A² form
older, vacuum-impedance-mirror:122); the ALREADY-FLAGGED linear-vs-quadratic
tension (research/2026-07-31_anisotropy-observable_scoping.md:893 — cite and
sharpen, do NOT present as newly discovered); the failed kernel-mechanism attempt
(PR #92 — the two-reactance factor-2 NOT-FORCED); trampoline-framework:113's
k_eff = k₀(1+βu₀) — a canonical site where linear coupling via an operating offset
is already licensed-by-analogy (Arm-1's structure, on the frozen site list); the
#951 S^p lemma + the TWO-CLOCKS finding (backreaction.md:128 kernel-clock vs
temporal-spatial-lattice-decomposition.md:28 linear lapse — PROVISIONAL until #951
clears; the audit's sharpest target: which of canon's own two clocks is the
node's).
