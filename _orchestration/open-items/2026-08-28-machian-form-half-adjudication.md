---
id: machian-form-half-adjudication
title: "The taxonomy layer promotes the xi termination as physics with an empty Gamma, while canon's own ilk-gravmb ruling books it VALUE-fitted; and H_infinity is booked two incompatible ways"
status: OPEN
owner: grant
opened: 2026-08-28
source: research/2026-08-28_machian-boundary-first-principles-audit_BLIND-AUDIT.md
anchor: "The record found a real defect and mis-described it"
---

**★ RE-POSED 2026-08-28 AFTER BLIND AUDIT.** The first version of this item
asked *"does the derived-FORM half survive?"* — **a malformed question**, and
the audit blocked the record that posed it. Canon's own `ilk-gravmb` ruling
already books the `xi` termination as the **VALUE-fitted** half. The
originating record is preserved unrewritten with a block banner; the audit
verdict is the `source` above.

**Not at issue.** The *value* of `G`. It is CODATA, canon states its own
back-solve unprompted in at least four shipped places, and GR derives `G` from
nothing either. **A calibrated coupling is peer, not defect.**

---

## DECISION 1 (re-posed) — the taxonomy layer contradicts the ruling

**This needs no physics call. It is documentary, and it is real.**

`manuscript/ave-kb/common/interlock-register.md:216` (`ilk-gravmb`, the exact
ruling `wall-taxonomy` row 7 cites), verbatim:

> **(i) Form-derived half — the "Achromatic-Lens".** Under SYM-class scaling
> eps(r) and mu(r) co-scale by the same n(r), so Z_local(r) = sqrt(mu/eps) = Z_0
> everywhere -> **Gamma = 0** -> reflectionless achromatic refraction (the
> matched-GRIN far-field); the 1/7 isotropic-impedance projection in a
> trace-reversed (nu=2/7) solid gives the /7 PPN coupling family. This half is
> substrate-derived (Ax 1 + Ax 4). **(ii) Value-fitted half — the xi
> termination.**

**Canon books the termination as VALUE-fitted (ii).** But two taxonomy leaves
promote it as physics:

- `wall-taxonomy.md:174` seats "Hubble termination `R_H`" as **row 7 of the
  wall table**, channel `"all"`, and says it *"is what makes `G` an **input
  impedance** rather than a free constant"* — **with a `Gamma` cell that
  restates the noun** (*"far-end termination of the cosmic line"*) where rows
  1-6 and 8 each carry a real reflection condition. Row 4 of the same table
  explicitly reads NOT "all channels". The row violates its own leaf's
  mandatory rules at `:155` (one channel per row) and `:160` (name the
  phase-state).
- `translation-circuit.md:134` — *"Machian G | Distributed transmission-line
  input impedance at Hubble-horizon termination"*. Its anchor
  (`omega-freeze-cosmic-grain-cascade.md`, 234 lines) returns **zero** on
  `terminat|input impedance|transmission line|R_H|reflection` by two
  independent methods, and its second pointer (`:450`) is stale.

**The decision:** does the taxonomy layer get corrected to match `ilk-gravmb`,
or does `ilk-gravmb` get revisited? **Do NOT hand-set row 7's `Gamma` to close
it** — `wall-taxonomy.md §10.3`: *"Computed, not chosen … Hand-setting a sign
is an import."*

## DECISION 2 — which `H_infinity`? UNCHANGED BY THE AUDIT; every number reproduced.

- **Today's `H`** — manifest P23 scores `69.32` against **TRGB 69.8**
  (`z ~ 0`), `error_percent: 0.7`, `public_in_readme: true`.
- **The de Sitter asymptote** — `vol3/claim-quality.md:984,:1004` attributes
  the `rho_Lambda` residual to *"de Sitter asymptote vs current epoch"*; the
  `1/Omega_Lambda = 1.4605` piece is the bulk of the advertised 1.54.

| Reading | `Gdot/G` | Cost |
|---|---|---|
| Live `H(t)` | `-3.257e-11 /yr` (`= -(1+q)H`) | **EXCLUDED 36x-3393x** — LLR 2021, BBN, pulsar timing, three independent probe classes |
| Fixed `H_infinity` | **0 exactly** | P23's `0.7%` becomes **+24.4%** (against `H_0 sqrt(Omega_Lambda) = 55.74`); a genuine asymptote of 69.32 demands today's `H_0 = 83.8`, outside every measurement in the tension |

Canon confronts this in `research/` and `_orchestration/` (2026-07-11
LIVE/FOSSIL/ATTRACTOR branches; *"This derivation is owed, not done"*; docket
BOOKED PENDING-GRANT). **It never reached `manuscript/`.** The escape that
satisfies both docket rows ("flatness-protected-live") is the item canon itself
logs as **underived**.

---

## ★ THE LIVE PHYSICS QUESTION THE AUDIT SURFACED

**Linear-in-`N` requires `Z_L = 0` exactly.** The blocked record claimed no
termination gives an impedance linear in cell count; that is false — a shorted
line gives `Z_in -> j*omega*L_p*l`, exactly linear, and the general-load
expansion `Z_L - j*l*omega*(C_p*Z_L^2 - L_p)` is linear **only** at `Z_L = 0`.

**So canon's `Gamma = -1` at `R_H` is strictly LOAD-BEARING**, not decorative.
The question is not whether a termination can do this work — it can, and only a
short can. **The question is whether that short is derivable.**

`manuscript/ave-kb/common/boundary-observables-m-q-j.md:112,:114` supplies a
candidate the blocked record never opened, though it cites that same file:

> `## We sit inside the cosmic $\Gamma = -1$ boundary`
> `...the cosmic horizon = parent-black-hole Schwarzschild radius per the
> generative cosmology`

**A Schwarzschild radius IS an Ax4 material boundary** — `eq_axiom_4.tex:24`
(*"BH event horizon eps_11(r)=1 matches Schwarzschild r_s exactly"*) and `:59`
(*"event horizon = dielectric rupture / perfect shear reflector"*). If that
identification holds, row 7's `Gamma` cell has real content and the whole
picture changes.

**It is itself an unaudited canon assertion**, and canon's own pointer for it
is bad (`op14:12` anchors `Gamma=-1` to `boundary-observables-m-q-j.md:21`,
which is the `J` winding row; the `Gamma=-1` language is at `:11` and
`:112-114`). **Auditing the parent-BH identification is the highest-value next
read** — it decides DECISION 1's physics half.

A second candidate the record also missed:
`backmatter/02_full_derivation_chain.tex:91` carries **"Bounding Limit 3 — The
Machian Boundary Impedance (G)"** as a named limit in the printed derivation
chain, with a `~10^40` interacting-lattice-link count. The impedance reading is
**not** confined to the translation layer, as the blocked record claimed.

---

## Also booked here, needing no physics decision

Plain corrections, all independently reproduced by the audit: the
`4 log 2/(7 xi) = 2.8e-44` arithmetic (computes to `4.86e-45`; the printed
value is exactly `16/(7 xi)`; **two sites**, `four-entropy-distinction.md:20`
and `:83`); the `T_max,g` symbol collision (two objects ~86 OOM apart, one
glyph, unregistered in `xi-topo-traceability.md`); the `C_loop` displayed
algebra off by `c^2` at `electron-unknot.md:28` and its verbatim mirror; and
the three surviving `Phi_A "derived in Chapter 4"` pointers that canon itself
ruled dead on 2026-08-02.
