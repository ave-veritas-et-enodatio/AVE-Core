---
id: kernel-normalization-homonym
title: The Ax4 kernel argument "A" is a two-normalization homonym (yield-zero vs snap-zero, √α apart) — context-graded reading RATIFIED; dilution mechanism candidate open
status: OPEN-IN-WALK
owner: lane
opened: 2026-08-24
source: manuscript/ave-kb/vol2/particle-physics/ch01-topological-matter/q-g18-schwinger-pair-wkb.md
anchor: "precisely the field at which $A = 1$ in the AVE saturation-kernel parameterization"
---

The 2026-08-24 pair-production adjudication surfaced the arc's FOURTH glyph collision (after
θ, Wind(∂Ω), γ_c), this one closest to the axioms: INVARIANT-S2 writes S(A) =
√(1−(A/A_yield)²) — kernel zero at YIELD — and the electron-wall canon needs exactly that
(Z_core = Z₀√S → 0 at ≈43.65 kV). But q-g18 parametrizes A = E/E_S — kernel zero at SNAP
(511 kV) — "by construction", and vol9 ch8 agrees. One universal kernel cannot zero at both;
the scales sit √α apart, and the axiom's own signature list mixes them ("V_snap, B_snap,
Regime IV, yield boundary").

**RATIFIED (Grant 2026-08-24, "yes id think context"): the CONTEXT-GRADED reading** — the
kernel form is universal; its denominator is each use's own rupture scale (confinement walls
normalize to yield; pair rupture to snap). Both canonical uses survive; the fix is
vocabulary (subscript the kernel argument per use: A_yield-normalized vs A_snap-normalized),
propagated by a lane pass over the kernel-citing leaves.

**OPEN — the mechanism candidate (Grant, same ruling, verbatim): "driven universally by
dilution? idk"** — the hypothesis that each context's rupture scale is the ONE absolute snap
scale diluted by a computable geometric/energetic filling factor, with the electron wall's
factor being √α. Testable shape: derive the per-context dilution factor and check it
reproduces V_yield = √α·V_snap rather than importing it. ⚠ α-echo hazard flagged: α's
identity-not-derivation status means any dilution derivation that consumes Q_tank = 1/α is
circular; the factor must come from geometry/energy accounting independent of the α
identity.

**Routes:** (1) vocabulary pass (subscripting) — lane-executable after the sweep lane
reports; (2) dilution-factor first-order derivation — physics lane, prereg-gated.
