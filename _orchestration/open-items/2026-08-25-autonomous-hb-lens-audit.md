---
id: autonomous-hb-lens-audit
title: "AUDIT RUN, VERDICT IN — the autonomous-HB lens: criterion DEAD as posed. Grant's call on whether anything survives into G2"
status: ROUTED-TO-GRANT
owner: grant
opened: 2026-08-25
source: research/2026-08-25_autonomous-harmonic-balance-lens_RECORD.md
anchor: "the fork does not get resolved"
---

**THE AUDIT HAS RUN.** Result:
[`research/2026-08-25_autonomous-hb-lens-audit_RESULT.md`](../../research/2026-08-25_autonomous-hb-lens-audit_RESULT.md)
(2026-08-26). Two phases — a six-lane adversarial review, then twelve refuter
votes plus a completeness-critic synthesis. All seven charter items (A1–A7) are
discharged or partially discharged; both required checks were run.

**Verdict: the existence criterion as posed is DEAD.** *"Does a nontrivial
source-free solution exist"* cannot return NO — existence is generic
(one-parameter families at `r_auto ~5e-15`, in every winding sector,
delocalized across 54–85 % of the lattice, running continuously to `A→0` where
they **are** the cold lattice's own linear eigenmodes). The lens is repairable
**only as a SELECTION test, never as an EXISTENCE test**, and only with four
additions named in the result doc's §6.

**★ THE BLOCKING RELATIONSHIP IS UNCHANGED AND STILL LIVE.** R58's **decision 1**
and the **(2,3) carrier fork** stay **LIVE and un-ruled**. This lens does not
supersede them; it was a candidate to make them moot, and the audit's finding is
that **it does not currently qualify to.** **Only Grant can rule that it does** —
and the audit does not pre-empt that ruling in either direction.

**What is on Grant's desk, and it is not "adopt or discard":**

1. **The (2,3) canon collision (charter A4).** Three canon sites place the (2,3)
   in three different places — Cosserat ω / K4 `V_inc·Φ_link` / K4
   `V_inc·V_ref` Clifford torus — against `master-equation.md`:33's *"share no
   `(V_inc,V_ref)` phasor"*. The A4 lane swept it by two methods and its verdict
   is that **canon does not currently decide it**. **This is a carve, not a
   sweep.** Finding F4 cannot be closed until it is decided, and §7's reframe
   cannot say which integer it is computing.
2. **Whether `A→1` is physics or the kernel clip.** Of the clip-directed runs
   the result doc reports, the three carrying an `A_max` all stopped on the
   **approach to** the saturation kernel's *declared* clip domain
   (`A_cap=0.99 / S_min=0.05`): `0.91819`, `0.950373` and `0.986728`, all short
   of `A_cap = 0.99`. **The result doc's §2.5 records a
   second converged continuum with no reported `A_max`, so whether the domain
   itself was entered is NOT ESTABLISHED rather than excluded** — RESULT §3/F8's
   scope box carries the enumeration, the method and its blind spots. That is
   where a fold-based or bracing-based selector would have to live, and no run
   in the round is reported reaching it.
3. **What survives into G2.** §3.2's Lagrange-constraint / fixed-charge
   formulation **survived the audit intact** and one lane calls it *"the
   strongest formulation in the record."* §3.1 consequence 3 is falsified by
   measurement (Rule-12 status note appended to the record, body untouched).

**Open follow-on tracked separately:**
[`2026-08-26-a2-prior-art-standing.md`](2026-08-26-a2-prior-art-standing.md) —
A2's external retrieval and clause-Q verdict were **run**, but by a single lane
that the verify phase never re-tested.

**A defect in the audit instrument, recorded so it is not repeated:** the verify
roll-up computed status as `refuters >= ceil(total/2)`, which on a two-lens panel
makes ONE refuter equal REFUTED. It stamped all six findings REFUTED when five
were splits, and in four of those the lane that had actually run the code voted
the other way. Result doc §1.
