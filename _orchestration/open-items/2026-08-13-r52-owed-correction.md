---
id: r52-owed-correction
title: R52 carries two defects the orchestrator flagged downstream but never fixed at source
status: OPEN
owner: lane
opened: 2026-08-13
source: _orchestration/docket-entries/2026-08-12-ruling-r52-k2g-operating-point.md
---

Owed by the orchestrator, on a merged ruling of its own authorship. Both were caught when the
same defects were flagged in downstream text; neither was corrected at the source.

1. `:112` — a mislabelled `(3K+G)/G` series.
2. `:114` — *"the kinematic inventory 7 stands on its own footing"*, which does not hold as
   written (the seven is exact at 1 of 4 tabulated points).

Rule 12: the frozen ruling text gets a **dated surface-note**, never a rewrite.

**Rides with R54's propagation** (~17 seven-counting sites) and R52's own mode-count propagation
(5+ sites) — the same lines, so one pass covers all three.
