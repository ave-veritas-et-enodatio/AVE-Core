---
id: x44-unblock
title: X44 bin-(iii) UNRECONCILED has been unowned for six weeks — the obstruction was DEFINITIONAL (a two-function glyph collision) and the frozen PASS bin was structurally unreachable; a replacement FROZEN prereg is pushed and needs a Grant read before the run
status: ROUTED-TO-GRANT
owner: grant
opened: 2026-08-27
source: research/2026-07-12_x44-komar-source_result.md
anchor: "the ruled Picard weight and the strain-energy ADM label are"
---

**Why this item exists at all.** X44 froze **bin (iii) UNRECONCILED** on 2026-07-12
with `η_mixed = +1.048`, named its own completion path (§7 option 5, "X44b"), and
then **nothing owned it for six weeks**. Verified this session: `grep -rl 'X44'`
returns 30 files across `_orchestration/` (8), `research/` (14),
`manuscript/ave-kb/common/` (2) and `src/` (6) — **and none of them is
`_orchestration/BOARD.md`, and none is under `_orchestration/open-items/`.** In a
program whose stated discipline is that board state is DERIVED, an arc with a frozen
negative and a named unbuilt follow-on sat off the board entirely. **This item closes
that gap.** It is filed as tracking + a routed decision, not as a physics claim.

## What the session established (three findings, all needing a Grant call)

**1 — The obstruction was DEFINITIONAL, not physics.** The glyph `√S` denotes two
different functions of `ε₁₁`, and X44's reconciliation bridged them **on the symbol**:

| side | site | form | leading deficit |
|---|---|---|---|
| engine | `src/ave/gravity/backreaction.py:250-252` → `src/ave/solvers/graded_vacuum_network.py:229-230` | `(1−A²)^{1/4}` | `ε₁₁²/4` — **QUADRATIC** |
| vol-3 canon (W2) | `manuscript/ave-kb/vol3/gravity/ch01-gravity-yield/temporal-spatial-lattice-decomposition.md:24,:28` | `√g₀₀ = √S ≈ 1 − GM/rc²` | `ε₁₁/7` — **LINEAR** |

So `η_mixed ≈ +1.05` measured the gap between a linear object and a quadratic object
that share a symbol. **The two sides were never comparable.** (A companion lane
enumerated **six** distinct functions of `ε₁₁` wearing this glyph family across three
orthogonal ambiguity axes; that enumeration is a claim about the sites it read, not a
corpus census.)

**2 — The frozen PASS bin was structurally unreachable, and worse than disclosed.**
X44's own post-review §4b (`research/2026-07-12_x44-komar-source_result.md:84-95`)
disclosed that reconcile required `f ~ 0.6`, *"~10× outside the frozen regime"* — but
the docket board row carries only the bare negative. **Measured this session**
(`research/drivers/x44_unblock_regime_map.py`, 96× amplitude scan at N=24): `f` is
**non-monotone**, peaks at **0.186** (λ=8, `max A = 0.783`, converged), then turns
over as the core pins against the yield cap and the Picard loop stops converging.
**`f ~ 0.6` is 3.2× above the reachable supremum and is unreachable at ANY amplitude.**
Bin (iii) could not have failed to fire.

**3 — Two flagged defects in canon, NOT fixed (FLAG-DON'T-FIX).**

- **Grant Ruling 1's code attribution is false by direct algebra.**
  `_orchestration/2026-07-10_rulings-docket.md:855` is titled *"√S (slope-1) IS the
  clock — CONFIRMED"* and `:858-860` certifies the live `komar_weight`
  (`backreaction.py:235-252`) as *"on the correct side."* That function returns
  `(1−ε₁₁²)^{1/4}`, whose expansion about `ε₁₁ = 0` has **zero linear term**. **The
  ruling's physics is correct and untouched** (`:869-871`'s per-cell in-place-reading
  argument is exactly right); only the code attribution and the `√g₀₀ = √S`
  glyph-bridge fail. **The ruled physics and the shipped code have disagreed silently
  since 2026-07-14, invisible because both sides write `√S`.**
- **The 2026-08-11 first-order-defect proposal is ALSO untracked, and carries a
  direction ambiguity.** `research/2026-08-11_gravity-linearity-audit_result.md:658`
  says *"the source should carry the **co-scaling factor**, not the kernel"*, and
  `:645` names that factor `m = 1 + ε₁₁/7`, which is **> 1** — taken literally the
  source would carry a mass **excess**, contradicting the SUBTRACT ruling. The physics
  needs `1/m`. Its own header tags §9 `⚑ UN-AUDITED`; `:661` says *"⚠ **Proposed, not
  applied.**"* A review brief exists
  (`_orchestration/2026-08-11_linearity-addendum-adversarial-review-brief.md`) but **no
  review result document does**, and the proposal has **no board row and no open item**
  — six weeks stale, same as X44.

## What has been done (this session, no run yet)

- `research/2026-08-27_x44-unblock_prereg_FROZEN.md` — **FROZEN**, replaces the
  2026-07-12 prereg (byte-untouched; git is the trail). It freezes the weight
  `w = 1/n_scalar = 1/(1 + ε₁₁/7)` by the **ponderomotive derivation**
  (`manuscript/ave-kb/vol3/gravity/ch03-macroscopic-relativity/ponderomotive-equivalence.md:14,:19`),
  never by the `η_mixed` it produces; **demotes `η_mixed` to a reported diagnostic and
  keys no bin on it**; adjudicates instead on `c ≡ Δ_clock/U_bind`, whose PASS bracket
  is **derived from the accessible band** and therefore lies inside it by construction.
- `research/drivers/x44_unblock_regime_map.py` — the reachability arithmetic,
  reproducible.
- **Zero canon edits.** No leaf, ruling, docket entry, or engine file was touched.

## ★ The decisions routed to Grant (nothing pre-selected)

1. **Run it, or re-scope it?** The prereg's own §9.3 pre-registers that **the engine as
   built cannot falsify a clock weight**: with a prescribed source the far-field flux
   is `Σ T₀₀^src` by Gauss (install-tautology) and the ray-trace register collapses
   onto it, so only two registers are independent and one of them is definitionally
   the thing installed. If that is right, the honest verdict on X44 is *"the question
   as posed is not decidable by the engine as built"* — and the run's value is the
   **structural** result plus the D-register and resolution receipts, not a
   reconciliation. **Is that worth the run, or should the effort go straight to the
   `T_ij` register?**
2. **The stress register.** `research/2026-07-14_tij-x44b_CHARTER.md` has a Grant GO
   from 2026-07-14. **Verified still unbuilt at HEAD by three methods**: no
   `def`/`class` matching `sigma_ij|stress_tensor|momentum_flux|force_density|T_ij|tolman`
   in the gravity path; no `*stress*`/`*tolman*`/`*tij*` filename there; **zero**
   `Tolman` or `3∫p` hits anywhere in `src/`. It is the only remaining candidate for
   the residual once the weight is corrected.
3. **Ruling 1's code attribution** — correct the docket, or leave it and carry the
   disagreement as a known flag? **Grant's call; the lane did not edit it.**
4. **The X44b expectation ladder is falsified as stated.** X44 §5b(ii)'s *"linear clock
   → η ≈ −1"* holds at `g_self = 1/7`, not at the frozen family's `g_self = 1.0`. From
   the derived weight's `+0.83` the stress term must supply **−0.83 — the opposite
   sign.** Does X44b get a corrected ladder before it is built?
5. **Home for the glyph collision.** `_orchestration/open-items/2026-08-24-collision-register-home.md`
   already carries four dependents (`a0-glyph-collision`,
   `gammac-gc-modulus-identity`, `axiom5-b-glyph`, `kernel-argument-normalization`).
   **This collision is not among them.** Mint it as a fifth dependent there, or keep
   it here?

## ⚑ Anti-fitting fence — carried into any follow-on

`η_mixed = 0 ⟺ c = 1` exactly, which requires a clock coefficient `k = 1/2`. The
substrate derives `k = 1/7`. Equivalently, `η → 0` is reachable at `g_self = 2/7`,
which is numerically `ν_vac` (`src/ave/gravity/backreaction.py:646`) — **a coincidence
with disjoint provenance, not a chord** (the reconciling `2/7` is 2-from-the-virial-
identity × 1/7-from-the-ponderomotive-projection; `ν_vac` is a Poisson ratio at
`K = 2G`). **Both are FORBIDDEN by the frozen prereg §12.4.** Any lane that reports a
reconciliation should be asked which knob it moved.
