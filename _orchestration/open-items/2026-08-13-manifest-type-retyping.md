---
id: manifest-type-retyping
title: The manifest `type` axis was never ruled — four rows carry derived_prediction while meaning consistency_check
status: ROUTED-TO-GRANT
owner: grant
opened: 2026-08-05
source: manuscript/consistency-manifest.yaml
anchor: "The `type` axis was not ruled."
---

Recorded in the P42 ruling's own OUT-OF-RULED-SCOPE block, verbatim:

> *"**The `type` axis was not ruled.** This row keeps type: derived_prediction, defined at line 20
> of this file as 'genuine forward prediction (Class D)', while consistency_check (line 23,
> 'reproduces a known result (Class C)') is the closer fit … MITIGATING: this is a **manifest-wide
> pattern**, not a P42 defect — all three other `consistency` rows (P47, P20_21, P44_45) likewise
> carry type: derived_prediction. **Re-typing is its own ruling across four rows, not a rider on
> this one.**"*

**Named rows:** P42, P47, P20_21, P44_45.

**Why it still matters after the 2026-08-13 forward/postdiction split.** The split moved these rows
into `manuscript/consistency-manifest.yaml`, so their *file* now states what they are. Their `type` field
still says `derived_prediction` — defined in the schema as *"genuine forward prediction (Class D)"*.
File and field now disagree, visibly, which is better than the previous state (both wrong) but is
not resolved. The split deliberately did **not** pre-empt this ruling.

★ Also flagged at the same site and unaddressed: the public tables print an unqualified ✅ for P42,
and *"No gate will ever catch this: the parity checks map by id and never read the claim text."*
