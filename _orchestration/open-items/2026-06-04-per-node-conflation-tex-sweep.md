---
id: per-node-conflation-tex-sweep
title: The .tex per-node-conflation sweep — the 13 files were never enumerated, and the premise moved
status: OPEN
owner: unassigned
opened: 2026-06-04
source: manuscript/ave-kb/claim-quality-closure-roadmap.md
anchor: "the rest flagged for a dedicated"
---

Carried as *"the broader `.tex` per-node-conflation echo sweep (13 files)"*. The source flag at
`claim-quality-closure-roadmap.md:104` reads: *"canonical KB `.md` corrected; `14_phase_diagrams.tex`
self-contradiction fixed; **the rest flagged for a dedicated `.tex` sweep**"* — **and never
enumerates the 13.**

Sweep findings at HEAD: `per-node conflation` / `per-node-conflation` returns **0 `.tex` hits** and
6 `.md` files. The *substance* is correct where it matters —
`vol_9_vacuum_datasheet/chapters/15_falsification_tests.tex:40` states it properly (*"the ratio
V_DC/V_yield is a per-node quantity for the quartz; the vacuum per-node amplitude at the same 30 kV
bias is A₀ ∼ 10⁻⁷"*).

★ **And the premise has shifted underneath it:** `divergence-test-substrate-map.md` now records
per-node-conflation as *"RETIRED as a live severity call (2026-08-01)"* on at least two rows.

**Likely disposition: retire, not execute** — but that needs the original 13-file list (probably in
the round-2 PONDER-05 adjudication or the round-2 synthesis doc) or an explicit decision to drop it.
Left UNDETERMINABLE by the 2026-08-13 sweep.
