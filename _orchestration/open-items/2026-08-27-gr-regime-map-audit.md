---
id: gr-regime-map-audit
title: "AUDIT REQUIRED — where GR is validated on the strain-regime ladder (walk-grade, three canon deltas)"
status: OPEN
owner: lane
opened: 2026-08-27
source: research/2026-08-27_gr-validity-regime-map_walk_RECORD.md
anchor: "Regime III (Yield) is a thin shell"
---

**A walk-grade record, UNAUDITED.** Grant, in chat 2026-08-27: *"regimes of
strain? GR is valid for linear?"* The lane was dispatched to sweep canon first
and document only the delta.

**Supersedes nothing. Only Grant rules.**

**The sweep result, which is the most useful line here:** canon already maps
GR's test bodies onto its own regime ladder with numbers
(`domain-catalog.md` §Gravitational and §Gravitational Waves), and already
states the recover-GR-where-established consequence for the black-hole rung
(`lattice-extreme-bh-rationality.md`:74). **The walk's headline item — clause G
is nonlinear via `D(A)` and linearizes at `D→1` — is canon verbatim in two
places** (`eq_axiom_5.tex`:75–77, `saturating-modulus-and-backreaction.md`:64)
and stands down.

**What is left over is three findings, none of which the walk proposed:**

1. **DELTA-1 `[ARITH]`** — composing canon's `ε₁₁ = 7GM/c²r` with its own
   `√(2α)` / `√3/2` / `1` cuts gives **radii**: Regime I ends at `28.97 r_s`,
   Regime III is a `~0.54 r_s` shell at `3.50–4.04 r_s`. No site the lane's
   method reached writes these down. **Carries its own validity caveat** — the
   inner rows use the linear profile where `D(A) ≠ 1`.
2. **DELTA-2 ⚑** — the gravity sector carries at least four distinct control
   parameters compared against one boundary, and `domain-catalog.md`:52 / :120
   put a neutron-star surface in **Regime IV** and **Regime I** respectively,
   with no reconciling sentence. **Surfaced, not fixed.**
3. **DELTA-3 ⚑** — `orbital-regime-table.md`:16 files Mercury precession as
   *"II (Yield)"*: wrong regime NAME against `four-regimes.md`:27–28, and wrong
   ASSIGNMENT by ~6 orders (`ε₁₁ = 1.785×10⁻⁷`, deep Regime I). This is the one
   GR-observable-to-regime table the lane found, and it mis-files the flagship
   solar-system GR test. **Surfaced, not fixed.**

**Why it needs an audit before anything propagates.** DELTA-1's novelty rests on
searches, not an end-to-end read; its inner rows are idealizations of an
equation the lane never relaxed; DELTA-2's enumeration is from the leaves read,
not a census; and the record's own §2 finding (that the walk mis-attributes a
prior lane's reading) could itself be wrong.

**The charter is §7 of the record — eight numbered items (A1–A8), each with
attack instructions and an explicit KILL CONDITION**, including:

- **A5** kills §5's program consequence outright if it reduces to *"AVE is
  untested"* — a reachable observable must be named or the kill recorded.
- **A7** requires **read-and-run** on `src/ave/gravity/backreaction.py` to get
  the graded `ε₁₁(r)`; if `28.97 r_s` does not survive, DELTA-1 fails entirely.
- **A8** is self-directed and can kill the record's own §2.
- **A4** routes one unsourced external figure (LIGO waveform precision) through
  the **gated retrieval pipeline** — the lane did not self-fetch.

Also required: the `ave-discrimination-check` (A5), the consensus-bias
symmetric standard (A2, A5), and the structural-null stencil lens on A7.

**Routing after the audit.** DELTA-2 and DELTA-3 are canon defects in leaves
this lane does not own. If the audit confirms them they go to those leaves'
owners and to Grant; **this lane edited nothing and proposes no repair.**

**Not touched, deliberately:** the `V_yield`/`V_snap` residue at
`four-regimes.md`:82–89, the R49(a) `4π` relay carve at `axiom-register.md`:361,
and the R40-B2a `D(A)` demotion. All three are pre-existing and cited where they
bear.
