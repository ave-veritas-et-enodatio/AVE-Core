---
id: clm-395gps-reband-owed
title: Print is ahead of the KB on the 18/49 ringdown — clm-395gps re-band is owed
status: OPEN
owner: grant
opened: 2026-09-06
source: manuscript/backmatter/07_universal_saturation_kernel.tex
anchor: "its pre-registered rider fired, falsifying"
---

**The ask (one line):** *print says a ROOT-CERTIFIED instrument falsified the step the KB still
books as a zero-free-parameter derivation — the KB card has to move, and only Grant can say how
far.*

**The inversion, both sides quoted at their sites.**

- **Print, `manuscript/backmatter/07_universal_saturation_kernel.tex:145`** (2026-08-05
  current-status note, re-issued): a **certified** instrument for the graded cavity's axial
  $\ell = 2$ toroidal shear pole exists — certification line ROOT-CERTIFIED, twelve of twelve
  gates pass — and *"its computed pole does not land at $18/49$; its pre-registered rider fired,
  falsifying $r_{eff} = r_{sat}/(1 + \nu_{vac})$ **as a derivation of the eigenfrequency**"*
  (citing `research/2026-08-03_coldq-pole-v2.4-root_result.md`).

- **KB, `manuscript/ave-kb/vol3/claim-quality.md:198`** (`clm-395gps`): still books that exact
  chain as derived — *"The Schwarzschild $\ell = 2$ fundamental QNM is derived from Axioms 1–4
  with **zero free parameters**: $r_{sat} = 7M_g$ (Axiom 4), $r_{eff} = r_{sat}/(1 + \nu_{vac})
  = 49M_g/9$ (Poisson), $\omega_R = \ell c/r_{eff}$ (mode)."* The card's grade is unmoved:
  `solidity: 0.55` at `:216`, and the `:217` rationale still reads *"closes end-to-end from three
  axiom-grounded steps ... with zero free parameters — a genuine 1.7%-error category-(iv) derived
  prediction."*

**★ THE BRIEF'S CITE WAS CORRECT — an earlier "CITE CORRECTION" in this item was
itself wrong and is withdrawn (2026-09-06, same day, caught by the sweep's own auditor).**
`manuscript/ave-kb/vol3/claim-quality.md:217` **does** carry the literal string
*"Solidity 0.55 UNCHANGED"*. Verified by reading the line: it is a **single line of 1396
characters**, and the string sits near its tail — which is why a windowed or truncated read
reports it absent. The pointer `:217` stands; nothing about the inversion changes.
**The general lesson, worth more than the fix:** in this corpus a "the cite is wrong"
finding must be established by reading the WHOLE line, because multi-hundred-character
single lines are common in the claim-quality files and every truncating tool lies about them
the same way.

**Status: OWED AND ROUTED. Nothing is adjudicated here.** This item registers the gap; it does
not propose a band, does not move `solidity`, and mints no id. Note in particular that the print
note falsifies the step **as a derivation of the eigenfrequency** — it does not retract the
$18/49$ eigenvalue itself, and this item takes no position on which of those the card should
follow.

**Do not duplicate the re-band proposal.** A separate auditor lane is preparing it; this row
exists only so the debt is visible on the board while that lane runs.

**Related, not the same item:**
- `_orchestration/open-items/2026-08-04-v3-ringdown-instrument.md` — the instrument-class
  successor (discrete near-wall chain). Registered, not scoped.
- `_orchestration/docket-entries/2026-07-30-b1-retraction-propagation.md` — Ruling B1, the
  spinning-remnant retraction that print `:143`–`:145` already carries.
