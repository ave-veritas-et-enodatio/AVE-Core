---
id: g2-freeze-decisions
title: "G2 freeze decisions — the four calls that must be frozen before the P2 existence solve runs"
status: OPEN
owner: grant
opened: 2026-08-25
source: research/2026-08-25_g2-boundary-and-gyromagnetic-walk_RECORD.md
anchor: "cannot come from spin-½ topology, because every spin-½ object has it"
---

The durable home for the G2 gate's decision set (the routing-gap repair: these
lived only in PR bodies until 2026-08-25, so a G2 author reading the epic or the
G1 walk record would never have seen them). Consumes the G1 walk record
(`research/2026-08-24_g1-ac-steady-state-walk_RECORD.md` §4) and the 2026-08-25
walk (`research/2026-08-25_g2-boundary-and-gyromagnetic-walk_RECORD.md`).

**Gate rule:** the P2 prereg does not freeze until all four are ruled, and every
ruling travels into the prereg verbatim.

## Decision 1 — the scaffold form  [Grant-pending]

Source-terminated boundary phasors vs injection-lock. **Orchestrator
recommendation, REVISED at the 2026-08-25 walk** (the earlier lean was
source-terminated alone): solve **source-terminated with the common mode
explicitly projected out**, injection-lock as the physics-honest cross-check,
and make the projection a receipt — *if projecting out the common mode changes
the solution, the scaffold was doing illegitimate work.* Basis: the winding IS a
differential boundary condition; a uniform specification is gauge (`clm-relcnc`)
and its common-mode part is the tube phase ϖ, which epic guard 8 forbids
imposing. Walk record §1.

## Decision 2 — the observable set  ★ RULED 2026-08-25

**Grant, verbatim: *"A2: include"***. The verdict set is S-profile railing at
the core **plus** the projected M/Q read on **two different imposition
representatives** — turning the ppt equivalence-class argument (impositions with
identical projections are behaviourally identical) from an argument into a
receipt.

## Decision 3 — the envelope normalization fork  [Grant-pending; HOLD]

DP-1's C-state projection vs DP-3's full-tank form, **exactly √2 apart in A**
(test-receipted on PR #1013). Two elements, both of which the ruling must see —
the second was surfaced by the clearing review after the fork was first routed
as a clean binary:

1. **the √2** (counting incident waves vs counting stored energy — walk §2);
2. **a per-node → per-bond re-basing** of canon's DP-1 C-state row, carried
   inside the same module change.

★ **GATING SUB-TASK before this is ruled (cheap, un-run):** test whether the √2
fork and the **storage-α vs response-α contour fork** (`A²=α` vs `A²=2α`;
`cvr-reflection-smith.md`:49-55, *"near-colliding, Δ = 1.4×10⁻⁵"*) are the SAME
fork seen twice — express both criteria in both envelope conventions and check
whether they map onto each other. If they collapse, one ruling settles both.
Walk record §2.

## Decision 4 — g as a frozen verdict observable  ★ NEW (orchestrator recommendation)

**Recommend: freeze g alongside S-railing as a verdict observable.** From the
solved state, μ and S are computable, so **g falls out as an OUTPUT rather than
a posit.** Three reasons (walk §6): (i) canon currently books g=2 as **POSITED,
not derived** (`translation-circuit.md`:637), and the attractive double-cover
derivation is **retracted with a decisive falfifier** — proton/neutron are
spin-½ with the same 4π cover and g≠2 (`electron-identification.md`:92); (ii) g
is a pure dimensionless ratio, hence **immune to every normalization fork on
this list** by `clm-relcnc` — the one criterion convention cannot move; (iii) it
is a kill either way, and *how* it misses says whether the identification, the
imposition, or the machinery failed. Cost: near-zero (read off the same
solution). Carries the uniqueness handle too: the electron is the ground state
of the Link=1 sector, so a solution FAMILY selects the lightest member.

## Not a G2 decision, recorded to prevent re-routing

The J/B/taper side-assignment (`2026-08-24-smith-annulus-tube-ratio-pin.md`) may
be **dissolved rather than answered** by the harmonic-balance reformulation —
do not fold it into G2; it has its own item and its own resolution path.
