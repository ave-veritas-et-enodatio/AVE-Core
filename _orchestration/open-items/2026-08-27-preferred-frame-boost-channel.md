---
id: preferred-frame-boost-channel
title: "The emergent-Lorentz defence covers rotations, not boosts -- and AVE cannot compute the PPN parameters that bound the difference"
status: ROUTED-TO-GRANT
owner: grant
opened: 2026-08-27
source: manuscript/ave-kb/vol1/dynamics/ch4-continuum-electrodynamics/preferred-frame-and-emergent-lorentz.md
anchor: "does not collide on either branch"
---

Surfaced by the `L2-preferred-frame` lane, independently re-grepped by the
reconciliation lane, cites re-verified verbatim at `a3f4fef7` by the
`two-knob-gravity-repair` lane. **Both the L2 lane and the reconciliation lane
judged this OUTRANKS the perihelion defect it was dispatched alongside.**

**Canon is candid at the axiom level** — `eq_axiom_3.tex`:27, verbatim:
*"**Lorentz invariance** follows only *in the continuum limit* — it is
**emergent**, not exact: the discrete K4 substrate is a preferred-frame medium."*
And `relational-cancellation-identity.md`:36 says the frame **is** detectable and
the CMB dipole is the detection. **This is not a hidden problem. What is missing
is arithmetic.**

**Three findings, in increasing order of how hard they are to escape.**

1. **The leaf's escape clause is wrong as kinematics.** `:112` states that on
   weak-C-fails *"the recovered O(k²) term is the *isotropic* 2nd moment, so the
   leading *anisotropic* term is STILL quartic and a Hughes-Drever / cavity
   anisotropy bound … does not collide on either branch."* An isotropic
   dispersive law makes `ω² − c²k² = a₂c²ℓ²k⁴ ≠ 0`, and `k⁴` **is not
   boost-invariant** — a term isotropic in one frame is anisotropic in every
   other. Doppler transport gives one-way `2a₂β(kℓ)²` and two-way
   `a₂β²(kℓ)²(5cos²θ−1)`: at `β_CMB`, `ℓ_node`, 633 nm, `a₂ = +0.056` that is
   `2.0e-15` and `6.3e-18` against the leaf's headline `(qℓ)⁴ = 2.2e-22` —
   **4.5 OOM larger, `ν²` not `ν⁴`, and AT the ~1e-17 optical rotating-cavity
   class.** *Brakes: the magnitude rides an `a₂` that lives inside a demoted
   bullet from an off-main driver, and the channel fires only on the
   weak-C-FAILS branch (gate `wejkhvnfb` OPEN). **The kinematic point does not
   depend on `a₂` at all.***
2. **A cubic point group protects rank-2 exactly and does NOT protect rank-4.**
   Verified by explicit averaging over all 24 proper rotations of point group
   432. At `ρ* = 9.7734` the shipped moduli give a transverse speed ratio
   `√A = 1.10885` — a **10.32% direction-dependent shear speed and 10.3%
   birefringence at ZEROTH order in k**, where no quartic argument reaches. And
   `port-register.md`:47 makes the photon the transverse-u branch, so the photon
   rides the same rank-4 tensor as the shear GW. The only thing making it vanish
   is `ρ_bond = 1`, a point where `K = −0.0589` (negative bulk modulus, cannot
   carry A1 mass) — and the derivation that was supposed to FORCE `ρ_bond = 1`
   from Axiom 3 is **DEMOTED** (2026-08-11, R40-B2a; stamped at `:54`, and per
   L2 **not** stamped on the two claim-quality rationale rows that assert the
   isotropy is "protected two independent ways").
3. **The invariance group has no boost generator at all.** L2's symbolic solve:
   a boost built to preserve one characteristic cone maps a second cone to a
   form with a cross term, and requiring conformal preservation of both returns
   the unique solution `{β = 0, k = 1}`. Canon supplies the two cones
   (`port-register.md`:47 photon at `c`, `:49` A1 bulk at `√(10/3)c ≈ 1.83c`).
   The cone ratio is **1.83, not 1 + 1e-22** — not a `(qℓ)⁴`-suppressible
   statement. `research/2026-08-06_lc1-one-speed_result.md`:181-197 shows the
   floor survives `K = 0` at `√(4/3)`. *(Caveat: the row carrying the specific
   values is `port-register.md`:**49**, which is DEMOTED **twice** — R40-B1 and
   R40-B2a. The cone EXISTENCE is derived independently at LC-1 §2.2.)*

**What canon cannot presently do.** Two independent search methods over the
worktree at `a3f4fef7` return **0 files** for `preferred-frame parameter`. The
PPN preferred-frame parameters are absent from the corpus as physics objects,
against bounds of `|α₁| < 3.4e-5`, `|α₂| < 1.6e-9` (externally retrieved,
tentative-standing). L2 **declined to assert an `α₂`** and said why, citing
`research/2026-08-06_lc1-one-speed_result.md`:340 row 5: *"the corpus does not
establish the limiting speed of a bound matter excitation in a medium with
v_L > v_T … NOT folded into the verdict, and no speed is guessed."*

**The one calculation that would settle affordability**, flagged UNDERDETERMINED
by L2 and NOT upgraded here: if a bound A1 excitation's kinematics track the A1
cone while its T2 emission tracks `c`, then `γ_T/γ_L − 1 = 0.35β² = 5.3e-7` at
`β_CMB` — Hughes-Drever / `α₂` shape, **four orders above the bound if the
coefficient is O(1)**. Its premise is exactly FLAG-LC1-D.

**Ruling sought.** (a) Is the `:112` escape clause to be walked back? (b) Does
the `ρ_bond = 1` demotion need propagating to the two un-stamped claim-quality
rows before anything else cites "doubly-protected isotropy"? (c) Is
FLAG-LC1-D — the limiting speed of a bound matter excitation on a two-cone
medium — worth an arc? It is the gate on every number in this item.
