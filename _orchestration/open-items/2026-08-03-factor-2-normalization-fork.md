---
id: factor-2-normalization-fork
title: The factor-2 normalization fork — leaf :47 vs :48 (a normalization-ledger lane awaits GO)
status: ROUTED-TO-GRANT
owner: grant
opened: 2026-08-03
source: _orchestration/2026-07-20_pending-rulings-and-frontier-queue.md:199
---

**The ask (one line):** *which prefactor on $\langle (S_d-S_q)(-\dot\Sigma_{near})\rangle$ is
substrate-derived — and is this a physics question at all, or a faceplate-convention bug?*

Two lines apart in the same display block, `q-g19a-petermann-saliency-closure.md:47` implies
$C_2 = \langle\cdot\rangle/(\pi\alpha)$ and `:48` asserts $2\langle\cdot\rangle/(\pi\alpha)$.
Inconsistent by exactly 2.

**★ The verdict does NOT ride on this.** Outcome C stands under every branch — the shortfall is
28× with the 2 and 331× without it. This is a **correctness** question about what the corpus
prints, not a rescue path (Rule 11).

**Cheap discriminator, not yet run:** the first-order-anchor test — run the same normalization
chain at Schwinger order where $A_1 = 1/2$ is independently pinned, and see which prefactor the
chain returns. Per substrate-adjudicates-forks, the anchor test picks the branch, not fiat.
