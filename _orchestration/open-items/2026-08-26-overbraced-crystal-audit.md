---
id: overbraced-crystal-audit
title: "AUDIT REQUIRED — the over-braced chiral crystal walk (the knot may be in the stress, not the material)"
status: OPEN
owner: lane
opened: 2026-08-26
source: research/2026-08-26_overbraced-chiral-crystal-walk_RECORD.md
anchor: "the knot cannot be in the material"
---

**A new lens, proposed 2026-08-26 and UNAUDITED.** It responds to the
autonomous-harmonic-balance lens
(`research/2026-08-25_autonomous-harmonic-balance-lens_RECORD.md`,
open item `2026-08-25-autonomous-hb-lens-audit.md`) after that lens's core
existence criterion was measured vacuous in review.

Provenance, Grant verbatim: *"for question two I want you to think through how
you can tie not in a spiderweb, except that spider is an over braced chiral
chrystal."* (typos transcribed as spoken; the reading is in §0/provenance of the
record).

**The proposal in one line:** a knot cannot be threaded through frozen
connectivity, so the candidate is that it lives in the **stress** — an
over-braced structure carries **states of self-stress** (load-free equilibria),
which chirality makes handed. That replaces a **dissipative** amplitude-selector
(gain compression, which Ax 3 forbids) with a **structural** one.

**★ READ THE VERDICT BEFORE COMMISSIONING WORK.** The adversarial VERIFY pass
landed while the record was being written and measured **both** load-bearing
mechanisms **vacuous on the shipped carrier** (record §4.2, §9.1):

- **KILL 1** — `M` is exactly real (`max|Im M| = 0.000e+00`) and the shipped
  `bloch_adjacency` satisfies `A(−k) = conj(A(k))` to `0.000e+00`, so spinless
  TRS holds exactly and **every band's Chern number vanishes identically at every
  amplitude**. Geometric chirality is **not** broken time-reversal.
- **KILL 2** — the `θ=0` and `θ=±π` eigenspaces are each **exactly
  34-dimensional** at `A=0`, at `A=√α`, and under a random graded field, so
  degeneracy-as-selector **fires on ~35% of the spectrum at every amplitude,
  including `A=0`**.

**So this is not a route to commission; it is a route that needs three builds
before it is testable at all** (record §9.1): a symmetry-adapted / Bloch-reduced
solve; a mechanism breaking the exact realness of `M` (twisted BCs / a complex
sector) **or** a Z₂ / non-abelian invariant surviving spinless TRS; and the
Cosserat/T2 channel — **R58's standing blocker S1**. None of the three exists.

**What the audit is actually for, then.** Three things, in this order:

1. **Run the two cheap tests first** — **B11** (scaffold ownership: is
   `Chern(A→0, cold empty lattice) ≠ Chern(A at the solution)`? one computation,
   decisive) and **B12** (the category bridge: does "load-free equilibrium" map
   to the `θ=0` eigenspace?).
2. **Run B1** — the exact Maxwell–Calladine rank on the **actual srs-z3
   connectivity** with Cosserat bonds, **under both bond conventions** (canon's
   3-row and the walk's 6-row; the answer's *sign* flips between them). The
   driver exists and is α-free by construction. This is the single most
   decision-relevant computation in the record.
3. **Adjudicate the canon collisions in record §6**, which are surfaced with
   file:line and verbatim content and deliberately left unresolved — in
   particular §6.4 (canon already assigns a handed load-free rest-twist to the
   **vacuum ground state**, so "the knot is a self-stress state" may not pick out
   a particle) and §6.5 (the Cosserat channel is **gapped**, a live obstruction
   to the `ω=0` branch).

**The charter is §8 of the record — twelve numbered claims (B1–B12) with attack
instructions**, plus the consensus-bias symmetric standard and a discrimination
check. B10 asks a lane to treat the walk as **fully redundant** with existing
corpus items and report what survives; *"re-motivation, no new content"* is
pre-declared an acceptable verdict. **Kill conditions K1–K6 are pre-registered in
§9.2** so a negative is cheap to reach.

**What already exists and must not be re-discovered** (record §6): "over-bracing
= couple-stress" is already the corpus identification; a Maxwell–Calladine count
was run 2026-06-15 (on the **de-canonized z=4 diamond**, landing the primary
micropolar lattice **exactly isostatic**); a Chern number is already computed on
the srs carrier and is **zero** (`clm-clvchn`, NULL-CONFIRMED-FINAL); and the
`ω=0` branch of Q2 may already be the routed static-existence candidate.

**Blocking relationship — explicit.** This walk **does NOT supersede R58's
decision 1 or the carrier fork**, and it does **NOT by itself retire the
autonomous-harmonic-balance lens**. All three remain **LIVE and un-ruled**.
**Only Grant rules that.** The open item `2026-08-25-autonomous-hb-lens-audit.md`
stays open on its own terms.

**Two open questions Grant has directed be settled by computation, not further
walking** (record §10): **Q1** isostatic vs over-braced on srs-z3; **Q2**
self-stress (`ω=0`) vs ringing (`ω≠0`). Q2 carries a three-way `ω` glyph
collision that must be disambiguated before anything is computed.
