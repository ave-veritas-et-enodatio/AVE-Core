# EPIC — Static Existence: does the lattice honor the railed core the electron IS? (2026-08-24)

**Grant's GO, verbatim:** *"ok, lets fully orchestrate/epic the static existence
arch firing all relevant akills and documentation/checks."* (Same message asked
and received the definitional answer recorded in §1.)

**Class:** orchestration epic — the phased plan, gates, skill plan, and guard
set for canon's own highest-flagged open physics. Mints no claims. Execution of
each phase fires on its own gate (§4); Phase 0 fires with this epic's landing.

---

## §1 — What "static" means (the definitional carve, load-bearing)

Three distinct questions about the railed core:

| question | asks | status |
|---|---|---|
| **Formation (dynamic)** | can the dynamics BUILD it from free radiation? | **CLOSED-NEGATIVE** — the energize-LOCK route (engine pumps H at dt→0; three escape hatches closed; `project_keystone` record) |
| **Response (probe)** | how does the medium REACT to an imposed grading? | **MEASURED 2026-08-24** — the Class-C means-test: the canonical core locus, no floor (research pair, PR pending) |
| **Existence (static — THIS EPIC)** | does a railed-core configuration EXIST as a self-consistent stationary state, given the topological constraint? | **OPEN** — the named test below |

"Static" = **fixed-point / eigenmode existence, not reachability**: impose the
(2,3) winding as a time-independent boundary condition, relax the lattice with
no drive and no pump, and ask whether the relaxed state rails S→0 at the
center. Canon's own vocabulary agrees: charge is the **STATIC imposed Link**
(the #416 two-natured ruling), and the frozen core is *"stable because it is
static."*

**The named test, verbatim**
([`saturation-rim-inversion.md:55`](../manuscript/ave-kb/common/saturation-rim-inversion.md)):
*"Impose the `(2,3)` winding as a **boundary condition**, relax the lattice,
and ask whether the relaxed core **rails `S → 0` at the center**. This **is**
the standing eigenmode-existence open item"* —
[`program-arc-map.md:119`](../manuscript/ave-kb/common/program-arc-map.md):
*"Closed: K=2G-as-derived. Opened: eigenmode-existence as the only remaining
open physics."*

## §2 — Open-goal framing (prove-or-disprove; no preferred outcome)

- **PROVE branch:** a relaxed configuration under the imposed winding rails
  S→0 at a localized core — the topological route to saturation
  (`clm-satnec`, OPEN) gains its first direct evidence, and the
  eigenmode-existence item moves.
- **DISPROVE branch:** the relaxed state stays sub-railed everywhere — the
  topological route is disfavored, `clm-satnec` demotes, and the dynamic
  route (self-biased Q-point, `resonant-lc-solitons.md:142`) carries the
  existence question alone.
- **NEITHER is a statement about formation** — both branches leave the
  energize-LOCK negative untouched. And neither is an EMERGENCE claim (§5
  guard 1).

## §3 — Phases

**P0 — Capability + transduction inventory (fires with this epic; read-only).**
(a) The T2/Cosserat channel: what in-tree machinery carries the transverse /
micro-rotation sector, can it be graded, and what are its scatter/step
primitives? (The Class-C run was scalar-channel; the electron's wall is the
**transverse** self-trap, and the transverse vertex scattering is unverified.)
(b) The **imposition map**: what lattice field configuration IS "the imposed
(2,3) winding"? The winding is a PHASE-SPACE object (the bond-pair tank's
Clifford-torus portrait — INVARIANT-N1: *not* a real-space knot), and the
Class-C transduction pull found canon provides **no map carrying phase-space
interior structure to real space** (only the edges project: M and Q). Candidate
imposition routes must be enumerated WITH their canon receipts — including
whatever #415/#417 and the fork-B/`saturated_core_strain_native` precedents
actually imposed — each tagged by what it assumes. (c) The graded-T2 response
map feasibility (the P1 measurement). **Deliverable:** a capability report +
the **ontology walk packet** for Grant (§4 gate G1).

**P1 — Graded-T2 response map (the transverse Class-C analog).** Measure the
T2 channel's own Γ(A) locus off a frozen transverse grading. ★ P0 CORRECTION
(2026-08-24) to this paragraph's first cut, which conflated the two T2
branches: canon holds BOTH — **magnetic-first** (μ_eff = μ₀S alone → Z→0,
Γ→−1, the Meissner/short branch) and **electric-first** (ε_eff→0 with μ cold →
Z→∞, Γ→+1, the rupture/open branch) — with OPPOSITE boundary phase
(`envelope-anatomy.md:94`, `master-equation.md:103-112`). **The P1 prereg must
declare WHICH constitutive parameter the frozen transverse grading loads**;
the transverse vertex behavior is unverified on either branch (the −1/3
counting fact is scalar-scoped, `srs-vertex-scattering.md:24`).
Own frozen prereg + result pair, full Class-C discipline. Independently
valuable whatever P2 does; also the null baseline for P2.

**P2 — The static-existence run.** Impose the winding texture per the
G1-walked map; relax (impose-and-relax ONLY); measure whether S rails at a
localized core, against P1's passive baseline. Frozen prereg carries every §5
guard. This run may also bear on the OPEN topological-vs-dynamic route
question — if so, the substrate adjudicates, not fiat.

**P3 — Adjudication + propagation (gated follow-on, per the
canonical-propagation discipline).** Eigenmode-existence item resolution;
K=2G residue update; `clm-satnec` re-grade; rim-inversion + `claim-quality`
notes; manuscript lockstep. Nothing propagates before adversarial review.

## §4 — Gates

- **G0 (this epic lands):** P0 fires.
- **G1 (before P1/P2 prereg): the ontology walk with Grant, in chat.** The
  imposition map is a load-bearing ontology ("what the imposed thing IS") —
  per the walk-before-execute and pre-test-physics-check trigger-8
  disciplines, it is walked with Grant BEFORE any expensive dispatch. P0's
  packet is the walk's input. **A wrong noun here wastes the whole arc.**
- **G2 (before the P2 run): the frozen prereg**, with the §5 guard set
  discharged in writing, author firewalled from results, deviations logged.
- **G3 (before P3): adversarial verify** (≥3 lenses) on the P2 result; the
  repairs-need-reaudit loop (repair → re-verify → repair) runs to
  convergence before anything is presented CLEARED.

## §5 — Guard set (each mandatory; the prereg discharges them by name)

1. **Existence-not-emergence carve** (`substrate-native-check` checkpoint 8):
   imposing the finished texture is legitimate ONLY because the claim is
   existence. No result may be framed as the engine "hosting" or "forming"
   anything. The forbidden-conclusions list travels in the prereg.
2. **Challenge-canonical-negative config-grep** — the rim-inversion guard
   verbatim
   ([`saturation-rim-inversion.md:57`](../manuscript/ave-kb/common/saturation-rim-inversion.md)):
   *"no free-precursor genesis, no `dt→0` pump ramp, impose-and-relax only"* —
   grep the CONFIG, not the conclusion. The Class-C prereg's grep is the
   template. Also #415 (real-space eigensolve bled the winding) and #417
   (phase-space carrier-lock) non-reconstruction, config-level.
3. **Phase-space-coordinate-check / the transduction hazard:** the (2,3) is a
   phase-space portrait; a naive real-space imposition is the exact
   conflation class the register's ambiguity flag and A46 police. The G1 walk
   exists because of this guard.
4. **Structural-null stencil-lens:** per-node-uniform grading cancels
   identically at the shunt junction
   (`vacuum_varactor_scatter.py:54` — the in-tree receipt); any null obtained
   through a per-node broadcast is an artifact, not a result. The transverse
   analog of this trap must be identified in P0.
5. **Sector-ownership (A1 ⊥ T2):** the winding is Cosserat/T2; the mass/S
   railing is A1-adjacent bookkeeping — the rim-inversion sector carve
   applies throughout; no cross-wiring of "the defect saturates" into "the
   defect is the mass."
6. **R40-B2a adjacency:** the longitudinal-TLM-port reading stamps on the
   Class-C machinery travel with any reuse; cite, never load-bear silently.
7. **Regime/phase-state declaration** atop every doc (house table); the
   relaxed state's regime is part of the RESULT, not the assumption.
8. **α-agnostic imposition** (from the 2026-08-24 frame-invariance walk,
   [`research/2026-08-24_frame-invariance-observer-walk_RECORD.md`](../research/2026-08-24_frame-invariance-observer-walk_RECORD.md)
   §2/§5): the tube phase α is NOT an invariant of the (2,3) winding — a
   uniform frame change preserves the ratio, and only the topological class
   is the winding. Imposing a specific α imposes MORE than the winding and
   smuggles un-owned structure into the relaxed state. The G1-walked
   imposition must be α-agnostic (the class, or the family).

## §6 — Skill-selection plan (pre-workstream, declared here)

`ave-prereg` (corpus inventory before each phase's doc); `ave-canonical-leaf-pull`
(P0 enumeration); `substrate-native-check` (checkpoints 8/9/10 explicitly);
`phase-space-coordinate-check` (G1 + every measurement doc);
`pre-test-physics-check` trigger 8 (the G1 walk itself);
`consistency-vs-emergence` (result classification; the PROVE branch is at most
consistency-with-`clm-satnec`, never emergence); `ave-assertion-gate` (every
chat relay of results); `ave-discrimination-check` (before any AVE-distinct
framing of the outcome); `ave-driver-script-honesty` + figure discipline
(white, Okabe-Ito, honest axes); `ave-walk-back` (if P3 demotes `clm-satnec`);
verify-before-cite throughout. Retro-pass before each phase's commit if the
applied set drifts.

## §7 — Consumers and receipts

- The eigenmode-existence open item (`program-arc-map.md:119`) — the epic's
  primary consumer.
- `clm-satnec` (rim-inversion claim 2, OPEN) and `clm-riminv` context.
- The K=2G provenance residue (*"only open physics = eigenmode existence"*).
- The Class-C machinery receipts (`vacuum_varactor_scatter` PR#305;
  `fork_b_saturation_tank.saturated_core_strain_native` impose-and-probe
  precedent; the pulse/probe plumbing) — P1 reuses; P0 inventories the T2
  analogs.
- The Class-C measured baseline (research pair, PR pending at authoring) —
  P2's passive null.

## §8 — What this epic is NOT

Not a formation study (the closed negative stands); not an emergence test
(guard 1); not a chord claim (any PROVE outcome is internal consistency of the
model's own claims — the AVE-vs-SM boundary is untouched by every branch);
not a license to re-open energize-LOCK.
