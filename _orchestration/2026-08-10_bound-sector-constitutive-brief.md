# Lane brief — the bound-sector constitutive law (R41, Grant GO) (2026-08-10)

**Goal (prove-or-disprove level):** fill the hole #935 exposed — the receipted axioms
underdetermine the bulk sector (a zero-stiffness inertial flat direction; neither the
imported wave NOR the constraint class derived). Deliver the two structures that gate
LC-1's frozen cell, then the two that complete the sector.

**SVA §0 header; freeze-first; frozen criteria verbatim or file:line;
[DO-NOT-MERGE][REVIEW: pending-orchestrator]; own Tier-2 before CLEARED. The #935
result is BANKED input — consume its knives and receipts, do not relitigate.**

## Frozen deliverables, in order

1. **(i) THE CONSTITUTIVE LAW:** derive — from the axioms, the K4/srs structure, or
   an honestly-TAGGED new axiom proposal (state which!) — the structure that
   determines the bound field u₀ = B/r² from its sources: the multiplier/Gauss-analog
   the controlled-source class requires. The flat direction leaves the dress
   under-determined; the law must pin it. If the ONLY route is a new axiom, deliver
   the minimal candidate with its full consequence audit (what else it forces) —
   that is a Grant-ratification object, not a lane verdict.
2. **(ii) THE NO-SIGNALLING THEOREM:** for a REAL, non-gauge bound field (G-SCALAR-REAL
   binds — no gauge escape), derive that the bound sector transports no
   energy/information superluminally: the retardation/constraint theorem #935 found
   missing. LC-1's cell un-fires ONLY on this theorem; state its status against
   LC-1's frozen wording explicitly.
3. **(iii) THE ENERGY FUNCTIONAL:** the bound sector's energy bookkeeping (mass = A1
   accounting hosted; the halo/near-field row) — consistent with (i).
4. **(iv) THE ν = 2/7 RECONCILIATION:** derive the static-response/frequency-gating
   the Tier-2 bias finding demands — WHY the mechanical response banks statically
   (the /7 couplings) yet does not radiate; the cut's placement must be derived, not
   sit where the pulsars require. This is the lane's honesty keystone.

## Verdict grammar (frozen)

Per-deliverable: DERIVED / DERIVED-VIA-NEW-AXIOM(candidate) / NOT-DERIVABLE(named
missing piece). The lane verdict is the four-tuple; no aggregation. LC-1's cell
re-adjudicates only on (i)+(ii) both DERIVED-class.

## Fences

Challenge-a-negative discipline (the #935 knives are banked — a derivation that
quietly reconstructs the flat-direction-as-constraint assertion dies at its own
gate); regime/phase declarations; sector-ownership (A1 accounting vs the driver
inventory); stencil-lens (the engine codes K — engine runs corroborate nothing);
Rule 11; two-method receipts, named engines; the rotational fence-reach STOP; the
create-on-append and measure-then-edit hygiene rules apply.
