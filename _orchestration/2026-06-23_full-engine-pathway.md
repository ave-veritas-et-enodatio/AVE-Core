# PATHWAY — the complete dynamic multi-soliton, all-modes vacuum engine (NATIVE-FIRST)

> ⚠ **SUPERSEDED 2026-06-24** by [`2026-06-24_engine-reroute-pathway.md`](2026-06-24_engine-reroute-pathway.md). This doc's **Stage 2 ("THE NATIVE CAGE") is 🔴 FALSIFIED** (MODE-III DISPERSE) and its **S0→S1→S2→S5→S6 critical path is DEAD**. The re-route changes the localization mechanism to BOUNDARY/TOPOLOGICAL (the (2,3) winding + `H_couple` + the Γ=−1 cavity); **mass = A1 (#260) is UNTOUCHED**. Read the re-route doc for the live path; this doc is retained as the audit trail.

**Created:** 2026-06-23 · orchestrator-tracked · **Re-architected 2026-06-24 (Grant: "drop the cartesian, draft the proper path").**
**Directive:** the full pathway to a fully dynamic multi-soliton, all-propagation-modes vacuum engine. Supersedes the cage⊗winding charter ([`2026-06-23_cage-winding-engine-charter.md`](2026-06-23_cage-winding-engine-charter.md)).

## End state
ONE engine, ONE **native K4 stencil**, the full **6-DOF micropolar node**: {2 transverse — photon c_EM=c0/S, shear/GW c_shear=c0√S; 1 longitudinal-bulk — A1 dilatation c_bulk=√2·c0 (K=2G); 3 micro-rotational — gapped Cosserat ω(k), charge=winding}, all four impedance channels coupled through **one conserved H_couple** (no pump; dual energy+|L| canary), each mode carrying a **CI-gated validate-on-known** at a pinned tolerance. Fully dynamic, multi-soliton, all interactions tracked.

## Architecture decision — NATIVE-FIRST (Grant 2026-06-24, "drop the cartesian")
**The engine is built on its own native K4 stencil (the tetrahedral graph-Laplacian, z=3) from the start. The Cartesian cubic grid is DROPPED as an operating grid — kept ONLY as a continuum cross-check reference.**

Why (grounded): everything is **already native except the c_eff(V) cage** — the transverse modes (`chiral_lattice_vector`) run on the **srs net**; the Cosserat winding (`cosserat_field_3d`) on the **K4 diamond**; the canonical eigenframe (`graded_vacuum_network`) on the **tetrahedral K4 stencil**. ONLY `master_equation_fdtd`'s c_eff(V) cage is Cartesian — and its own docstring (`:15-18`) says it is Cartesian *only because the native K4-TLM lacked c_eff(V) modulation* (it had Z(V), returned **Mode III**). **The K4 stencil was never tried WITH c_eff(V).** So the old "two-grid bridge" existed solely to reconcile that one foreign Cartesian component with the native rest — **make the cage native and the bridge dissolves.**

The make-or-break this surfaces: **does mass self-trap on the native K4 stencil with c_eff(V) — never tried?** A Cartesian self-trap can MASK a square-grid artifact: the L3 Γ=−1 wall "works" on the Cartesian 7-pt stencil but **clips to ≈−0.45, unreachable, on the native stencil** (the A.1/A.2 base-crack). Asking natively, now, surfaces a native failure EARLY instead of at a bridge after Cartesian scaffolding.

α-clean discipline (carried from Stage 0): `ringdown_Q` the sole Q-extractor; the guard triad; the literal scrubber; the landing-zone gate; **κ̃=6/5 not κ_chiral=α·κ̃**. The α-baked `CosseratField3D` Q-readout (golden-torus = α⁻¹ `:2422`) is excluded — its DOF are reused, its Q-readout is not.

## The 7 stages (native-first)

| # | Name | +DOF/mode | Validate-on-known | Cost | Status |
|---|---|---|---|---|---|
| **0** | α-clean spine + honest-Q + **native** eigenframe | guard triad, `ringdown_Q` sole extractor, tetrahedral eigenframe Q=∞ | cold lossless Q=∞ honest (NOT 137); guards fire; scrubber/landing-zone green | CHEAP | ✅ **DONE** #399 (merged) |
| **1** | 2 transverse modes (photon + shear) on the **native srs net** | 2 transverse: c_EM=c0/S (ε,μ — genuinely computed), c_shear=c0√S (G — √S *identity*, G-engine deferred to Stage 3) | T1.6 (drift<1e-8, dispersion<0.05, \|c\|/c_net±5%, transverse_dof==2) + driven split (c_EM↑, c_shear↓ off S=1) | CHEAP | ✅ **DONE** #401 (audited PASS) |
| **2** | **THE NATIVE CAGE** — make-or-break (absorbs the old self-trap + the entire two-grid bridge) | add c_eff(V) to the **native K4 graph-Laplacian (z=3)** — NEVER tried; establish the single native node set (align srs / diamond / tetrahedral, all z=3 native) | a seeded localized precursor (v14 Mode-I config: N=24, A=0.85, sech) **SELF-TRAPS at A→1 + persists (Mode I — POSITED persistence, NOT genesis)** ON the native stencil; the **Γ=−1 wall re-derived natively** (reaches −1, or clips like the Cartesian base-crack?); native `ringdown_Q`; **recovers the Cartesian v14 Mode-I in the continuum limit** (Cartesian = cross-check) | **EXPENSIVE — FIRST MAKE-OR-BREAK** | 🔴 **FALSIFIED** (2026-06-24) — make-or-break returned **MODE-III DISPERSE**, energy-conservation-certified. The seeded sech does NOT self-trap natively (WITH c_eff(V)); the bulk self-trap was a **Cartesian-grid artifact**. Source: [`research/2026-06-24_engine-stage2-native-cage_result.md`](../research/2026-06-24_engine-stage2-native-cage_result.md). Scope: **bulk self-trap RULED OUT; boundary/topological localization STANDS; mass = A1 (#260) UNTOUCHED** (only the localization mechanism changes — winding + H_couple pin the A1 core). 🔴 **SUPERSEDED (2026-06-24, S3+#417):** this "winding + H_couple pin the A1 core" successor was ITSELF then falsified — S3 DISPERSE (#411) + phase-space BREAK (#417); the localizer is the A1 cavity, the (2,3)=Link is a static no-work texture (both dynamical loci NEGATIVE). See `research/2026-06-24_engine-reroute-epic-summary.md`. |
| **3** | A1 longitudinal-bulk as a native grade | 1 longitudinal: c_bulk=√2·c0 (K=2G); mass = propagating dilatation, native bond-axial; gives the G-modulus engine the Stage-1 shear identity was deferred to | c_bulk=√2·c0 MEASURED (T1.7 longitudinal_dof flips); new CI gate; the √S shear now bites via G | MOD–EXP | — |
| **4** | Cosserat micro-rotation winding (charge) | 3 micro-rotational: gapped ω²=c²k²+m², m²=4G_c/I_ω; **c_R=√2** (clm-kmliqx — the curvature-γ mode, distinct from the bulk √2) | mass-gap period 0.35% as a SHA-pinned CI gate; gapless c_R; integer charge = closed Beltrami helicity | EXPENSIVE | — |
| **5** | conservative cross-sector coupling — ONE H_couple | none — couples A1/shear/EM/Cosserat via a single conserved H_couple | skew-Hermitian/rotation generator transfers norm-preserving; DUAL canary (\|dH/H\|<1e-8 + the \|L\| pump canary) | **EXPENSIVE** (most fragile) | — |
| **6** | **writhe-aware force kernel** + multi-soliton observables (the chord) | none — inter-soliton kernel: co-vs-anti-handed \|F\| (Observable-C, the AVE-distinct chord) | writhe-aware (RMF-bend/Bishop holonomy): signed equal-magnitude enantiomorph holonomy + EXACT achiral null | **EXPENSIVE** | — |

## Critical path
**Stage 0 ✅ → Stage 1 ✅ → Stage 2 (native cage) → Stage 5 (H_couple) → Stage 6 (writhe kernel).** The two-grid bridge is **GONE** — absorbed into the native cage (there is no Cartesian-to-native bridge because there is no Cartesian operating grid). The three expensive make-or-breaks: **the native cage** (does mass self-trap natively + does Γ=−1 reach −1?), the conservative coupling (the keystone-pump risk), the writhe kernel (the chord's false-zero trap). Cheap stages (3, 4) ride alongside.

## Next milestone — Stage 2 (the native cage) — 🔴 FALSIFIED 2026-06-24

> **🔴 DATED FINDING (2026-06-24) — the pre-stated IF-branch FIRED.** The make-or-break RAN and returned **MODE-III DISPERSE**, energy-conservation-certified (rel_drift −8.8e-6, Q_num 2.26e7; interior peak → 0.850 = seed at every dt; dt-converged + N-robust). Per the conditional written below — *reported early, as designed* — **the bulk self-trap is a Cartesian-grid artifact, NOT a substrate property.** The native K4 stencil WITH c_eff(V) STILL disperses, so this is the substrate's verdict, not a missing-modulation artifact. Scope-lock: **bulk self-trap RULED OUT; boundary/topological localization STANDS; mass = A1 (#260) UNTOUCHED** — only the localization MECHANISM changes (the (2,3) winding + H_couple + the Γ=−1 boundary cavity pin the A1 core, not an autonomous bulk self-focusing well). 🔴 **SUPERSEDED (2026-06-24, S3+#417):** this "winding+H_couple pin the A1 core" successor was ITSELF then falsified — S3 DISPERSE (#411) + phase-space BREAK (#417); the localizer is the A1 cavity, the (2,3)=Link is a static no-work texture (both dynamical loci NEGATIVE; see `research/2026-06-24_engine-reroute-epic-summary.md`). Source: [`research/2026-06-24_engine-stage2-native-cage_result.md`](../research/2026-06-24_engine-stage2-native-cage_result.md); ledger entry `stage2-native-cage`; supersedes the INCONCLUSIVE explicit run 3a4c3227. Original pre-run milestone prose preserved below (Rule 12).

Add c_eff(V) modulation to the native K4 graph-Laplacian and ask: does a seeded localized precursor (v14 Mode-I config) self-trap at A→1 and persist (Mode I) on the native stencil, with the Γ=−1 wall reaching −1? It must recover the Cartesian v14 Mode-I in the continuum limit (Cartesian = cross-check reference). **If it returns Mode III (fails to trap natively even WITH c_eff(V)) OR the wall clips short of −1 → that is the make-or-break finding, reported early** — the self-trap / wall would be a Cartesian artifact, not a substrate property. **POSITED persistence ONLY** (NOT genesis self-formation — the leaning-negative keystone-pump). **Regime:** near-yield/saturated (A→1 engages saturation), not cold — `ave-regime-phase-state-check`. Skills: VCA (the cage = the Γ=−1 impedance short), substrate-native-check, consistency-vs-emergence, ave-canonical-source.

## Open items
- **c_R = √2 RESOLVED** (clm-kmliqx, #397 merged): the read-AND-run adjudication VINDICATED √2 (m²=4 is locked to c_R=√2 by the asymmetric-½ convention) and disambiguated it from the bulk √2 + the shear-G c (the 3-speed table). Stage 4's c_R = √2.
- **Cartesian = continuum cross-check ONLY** (Grant 2026-06-24): the Cartesian v14 Mode-I is the known-good reference the native cage must recover in the continuum limit — NOT the engine's operating grid.
- **Stage-4 α-contaminant** (carry-forward, from the Stage-1 audit): `chiral_lattice_vector_sat.py:15` imports ALPHA — the saturated genesis engine, natural host for the near-yield c_shear G-dynamics; α-clean it before Stage 3/4 hosts the dynamical shear.
- **CI gap:** the Cosserat mass-gap validate (0.35%, T=π) is script/leaf-recorded — Stage 4 must SHA-pin it.
- **Native-grid alignment** (part of Stage 2, native + cheap, NOT a Cartesian bridge): confirm the srs net / K4-diamond / tetrahedral stencil are the SAME native node set (z=3) or align them.
