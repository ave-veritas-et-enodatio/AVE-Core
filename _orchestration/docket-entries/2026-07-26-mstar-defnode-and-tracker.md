### ENTRY 2026-07-26-mstar-defnode-and-tracker (2026-07-26): implementer — `m*` def-node (ruling 3) + pending-rulings items 13–20 (ruling 8)

- **Ruling 3 — the `m*` disposition (Grant verbatim `[sic]`: "correct, should not be a fitting parameter, a plus c should make sense") = option (a)+(c).** The corpus's strike of `m*` stays **byte-untouched**, and a disambiguating `def-` node is minted. Diagnosis: a **HOMONYM, not a physics conflict** — the surface symbol names two objects separated by Grant's discriminator **derived-not-fitted**. Minted `def-mstar1` in `manuscript/ave-kb/common/vocabulary-register.md` (status `ambiguous` — the ruling settles the *disposition*, the *surface form* stays overloaded), KEEP-BOTH with two senses: **Sense A** = the SM thermal-statistical FITTING PARAMETER, 🔴 struck and the strike STANDS (`vol_4_engineering/chapters/19_silicon_design_engine.tex`:45, `chapters/11_experimental_falsification.tex`:463, both verbatim-quoted, both byte-untouched; the softer `vol6/period-3/silicon/topological-area.md`:17 peer-frame recorded so the two stances are not conflated); **Sense B** = a DERIVED / substrate-read inertia, a distinct object, NOT struck (`m_eff = m_e√(1+k)` at `scale-separation.md`:66 + manuscript twin `:3698`; the emergent `M_eff` "emerges from the converged field's own integrated energy" at `saturating-modulus-and-backreaction.md`:21, `clm-w5ez6i`; the dressed-branch acoustic `ρ_eff` at `2026-07-21_beta-tracking-feasibility_scoping.md`:35 + `2026-07-09_x36-node-bottleneck_result.md`:25,51). `clm_cross_links`: `clm-w5ez6i` / `clm-m5swh9` / `clm-hu1jjw`.
- **Sense-B name minted PROPOSED-FOR-RATIFICATION (not canon): "dressed effective density"** — COINAGE-GREP two-method, **0 prior hits** across `manuscript/` `src/` `research/` `_orchestration/`. Alternative "curvature-read inertia" (also 0 hits) considered and **not** picked: the literal curvature read is exactly what the 2026-07-26 finding demotes. **⚑ Dimension caveat surfaced, not resolved (flag-don't-fix):** "density" is exact on the acoustic arm (`M L⁻³`) but wrong-dimension on the composite-resonator arm (`m_eff` is a mass, `M`); a one-name-spans-both option ("dressed effective inertia") is named but not picked. **Nothing renamed at any site by this entry.**
- **★Recorded finding — the literal band-bottom read is DECORATION for D1.** `m* = ħ²/(d²E/dk²)` evaluated at a band bottom is **sector-blind**: every gapped branch returns `m*v² = ħω₀ = E` identically (the gap divides out), so the read hands back *that branch's own* `c²` and cannot adjudicate which sector's `c²` divides `E_trapped` (`c_P = 0.519` / `c_S = 0.286` / `c_EM`). **The QUESTION's tracked anchor is `research/2026-07-21_continuum-radial-solver_CHARTER.md`:15 (D1) + :62 (I8). The FINDING ITSELF has NO tracked home** — surfaced in-chat 2026-07-26, no research/scoping doc landed it; per the citation-rot re-pin discipline (`def-1f6e34`: no `def-` node cites an untracked path) only the CHARTER is cited, and the finding is cited to `def-mstar1` until a scoping doc lands. D1 stays OPEN on its own axis (item 13).
- **`translation-circuit.md` §4.6.3 flag DISCHARGED, additive + dated.** The `:306` OPEN FLAG's evidence is preserved **byte-untouched** (Rule 12); two dated blocks appended — the ✅ RULED 2026-07-26 disposition (pointing at `def-mstar1`) and the ★consequence for the candidate-6 row (it is no longer carried as a candidate derivation route for D1; its "cross-ref only" status is unchanged).
- **Ruling 8 — the tracker gap (Grant verbatim `[sic]`: "do the rec").** `_orchestration/2026-07-20_pending-rulings-and-frontier-queue.md` §1 items 1–12 were all discharged, leaving the queue empty in-file while a fresh batch lived only in chat. Appended **items 13–20** (each with what-it-is / what-rides-on-it / status / Grant's verbatim `[sic]` words where ruled / executing lane or PR): **13** sector-of-storage — **OPEN-IN-WALK**, not ruled (Grant `[sic]`: "is this just ac vs dc analysis?"; the AC/DC carve reframes, the walk continues; charter D1 + C-load's premise + the Eötvös shared-ledger route ride on it); **14** the F6 attribution — **OPEN**, disposition pending (Grant `[sic]`: "what?"; the positive attribution is withdrawn, the driver string left byte-unchanged so the conflict stays visible); **15** OWED-1 SUBC/KUBC bracket — ✅ RULED → FIRED (`[sic]`: "yes, as above, so below, we should assume we have both boundaries right?"; **scope limit: brackets K, NOT ρ**); **16** OWED-2 the ρ extraction — ✅ RULED → FIRED (`[sic]`: "a with c as fallback works"; root cause on record: frozen `k·r_core ≪ 1` vs frozen box sizes mutually unsatisfiable); **17** charter D2/D3/D4/D5 — ✅ RULED (`[sic]`: "D2: disclosed, D3: follow rec, D4: do it, D5: do the rec"; D5 feeds the #796-measured grown profile, hoop `ε ≈ +0.139` / radial `ε ≈ −0.066`; **D1 remains HELD** pending item 13); **18** the `m*` disposition — ✅ RULED → EXECUTED (this PR); **19** AVE-Skills mirror push — ✅ RULED + EXECUTED (`[sic]`: "push"; commit `44dd336` on AVE-Skills `origin/main`); **20** the cannot-advect relabel — ✅ RULED (`[sic]`: "for the relabel, let's propagate/ (a)"), propagation lane running in a separate PR.
- **★STANDING DESIGN LESSONS recorded as binding on every future prereg** (from the #791/#796 arc, cross-lane and permanent, all three freeze-time obligations): (1) every outcome class needs a **REACHABLE bin** — band placement must not make a physically-likely outcome unbinnable (`2026-07-22_vessel-state-rve_result.md`:215,:218); (2) gates must be **DEMONSTRABLY FIREABLE on known-positives** before any verdict arm; (3) frozen requirements must be checked **MUTUALLY SATISFIABLE** before freeze, and the non-decisive **fallback pre-registered** (`:219`).
- **Verification.** Every quoted line two-method verified (`sed` line-range + `grep -n` on a distinct substring) at HEAD `c8ceacc3`. **No cite failed.** Two line-number self-corrections applied before commit (the executability quote is `:219` not `:220`; the ablation block is `:165-167` not `:165-170`). The `44dd336` receipt is the SHA + `git branch -r --contains` → `origin/main` + `rev-list --count origin/main..HEAD` → `0`; ⚑ its author/committer timestamp reads `2026-07-28` in this environment (materialization artifact — every tracked file carries a `Jul 28 00:0x` mtime), so the **SHA and branch-containment are the receipt, not the timestamp**.
- **⚑ Merge hazard recorded in-file.** `_orchestration/2026-07-20_pending-rulings-and-frontier-queue.md` falls under the blanket `*.md merge=ours` (per `_orchestration/docket-entries/README.md`), so a concurrent lane's status-flip can be **silently dropped** rather than conflicting. A warning block was added at the head of the §1-continued section telling any lane touching §1 to re-read after merge and re-apply.
- **Scope discipline.** `manuscript/ave-kb/common/claim-quality.md` **not touched** (concurrent lane owns it). No claim graded, no solidity moved, no leaf physics rewritten, no site renamed. `make refresh-kb-metadata` run (`def-mstar1` materialized into `claims.jsonl` as a `node_type: definition` record).

- *(2026-07-28 leak-audit fold, Grant [sic]: "fold": tracker item 21 appended — the anisotropy observable (direction-dependent long-wave P-speed of the single-crystal vacuum, from #802's measured Zener A = 1.605), OPEN frontier candidate, not fired.)*
