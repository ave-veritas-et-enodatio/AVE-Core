### ENTRY 2026-07-21-frozen-provenance-lint (2026-07-21): implementer — frozen-label provenance lint (Frozen-label byte-match gate)

- New gate `manuscript/ave-kb/tools/verify-frozen-provenance.py` closes the "a result doc ships a `Frozen:`-labeled criterion that does NOT match the lane's committed prereg" failure class — twice caught only by adversarial review in the 2026-07-20/21 window: **#770** (a hard-coded robustness string `"ROBUST across rail depth ... all RISING, pressure-tested"` surfaced as frozen, never in the prereg / never computed) and **#782** (the Lamé gate's absolute `|ext1−ext2| ≤ 0.10` mislabeled `"Frozen:"`, swapped for the prereg's relative `|Δ|/mean ≤ 0.25`).
- **Mechanism:** scan `research/**/*result.md` for `Frozen:`-labeled lines (a real label, not a quoted `"Frozen:"` mention); for each labeled quoted criterion, resolve the lane prereg (explicit `Prereg-file: <path>` pointer → header heuristic → naming fallback) and require the criterion **byte-identically** present in the prereg **CONTENT** (reconcile-don't-declare — never a self-declared echo elsewhere in the result doc). A gating doc with Frozen labels but no resolvable prereg hard-fails ("add a `Prereg-file:` line").
- **Gating:** date-cutoff. Hard-fail (exit 1) for docs dated ≥ `2026-07-22`; warn-only for earlier docs via the checked-in `frozen-provenance-grandfather.json` (holds the cutoff + the two incidents annotated caught-and-corrected + an explicit doc-exemption escape hatch). Wired HARD into `make verify` (no leading `-`, mirroring `verify-provenance-stamps`).
- **Regression demo (acceptance test):** four self-contained fixtures under `manuscript/ave-kb/tools/tests/fixtures/frozen-provenance/` reconstruct the pre-fix patterns — #770-class (fabricated string) and #782-class (swapped criterion) both FAIL with teeth (exit 1); the byte-identical control passes clean; the missing-pointer control hard-fails. Self-test `tools/tests/test_verify_frozen_provenance.py` (7 tests, stdlib-only).
- **Live corpus scan:** 278 result docs, 0 gating, 2 advisory (unquoted `Frozen-wall:` / `frozen-clean:` labels on pre-gate docs); the corrected #770/#782 docs produce zero findings (their disclosure `"Frozen:"` mentions are correctly excluded).
- **Scope honesty (in the docstring):** cannot catch a criterion frozen WRONG in the prereg itself (garbage-in), prose paraphrases / label-avoidance, or semantic-equivalence byte-diffs (`≤` vs `<=`) — those are surfaced-as-advisory or out of scope, never silently passed.
- **Sequencing:** the docket fragment relies on the news-fragments convention (`### ENTRY 2026-07-21-docket-fragments`, PR #783) for the `_orchestration/docket-entries/` dir + the key-lint that scans it; land #783 first.
- **Read-and-run audit repairs (2026-07-21):** REPAIR 1 closes the backdated-filename evasion (severity now keys on `max(filename, first-add date)`, untracked=new, one `git log` pass); REPAIR 2 surfaces the quoted-label smuggle `"Frozen:" \`<criterion>\`` as an advisory (proximity-gated off the #770/#782 disclosure lines); REPAIR 3 tries the named sibling before the header heuristic and hard-fails a gating doc that resolves ONLY via a cross-lane (different date-stem) header mention — corpus still 278 scanned / 0 gating; tests 7→12.
