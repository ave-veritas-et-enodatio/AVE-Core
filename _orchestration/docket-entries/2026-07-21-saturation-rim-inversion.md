### ENTRY 2026-07-21-saturation-rim-inversion (2026-07-21): implementer — saturation rim-inversion (phase-space walk) canonization

- **The saturated core's phase space is the BOUNDARY of the cold lattice's.** The 2026-07-21 phase-space-inversion walk (in the vessel-state thread) answered Grant's opening `[sic]` ("is the phase space of a saturated core inverted from the lattice baseline phase space?"). The orchestrator offered a mapping with three readings of "inverted"; Grant ratified the **interior→rim** reading `[sic]` ("i saw the phasor changing, but i think this mapping you provided makes more sense than what i was thinking, shoild we scope cannonizing it?") **over** his original sign-flip reading. Mapping: the Axiom-4 kernel `S(A)=√(1−(A/A_yield)²)` is a **circle constraint**; a cold node's per-mode phase space is the **disk interior** (amplitude-dynamic, phase-trivial, `π₁=0`), a saturated core sits **ON the rim** (amplitude-frozen, `S→0`, clock frozen, only phase free, `π₁(S¹)=ℤ`) — so **integers exist only on the rim**, which is why charge (the Cosserat `(2,3)` winding, a phase-space integer) quantizes exactly where saturation lives (the **quantization locus**).
- **New canon leaf (PROPOSED, Grant-ratification-at-merge):** `manuscript/ave-kb/common/saturation-rim-inversion.md` (name PROPOSED-FOR-RATIFICATION) — the sector carve (defect = Cosserat `(2,3)` winding ⊥ mass = A1 dilatation; defect ≠ mass-owner), the inversion statement + quantization locus, the topological necessity candidate + static-existence test + energize-LOCK guard, the channel-duality inversion + open μ-at-core detail, the parked sign-flip question, the vessel-state skirt connection.
- **Three claims minted, three separate grades (READING ≠ MECHANISM, #787 lesson):**
  - **`clm-riminv`** — the inversion mapping + quantization locus. **Consistency-class reading**, Grant-ratified mapping, pending adversarial review; **solidity 0.55** (use as input only). A near-theorem over already-canonical objects (kernel circle shape / frozen clock / phase-space winding), NOT a new derivation.
  - **`clm-satnec`** — the topological route to saturation (a `(2,3)` phase singularity forces the rail; the core = the lattice's resolution of it). **OPEN mechanism candidate**, `*pending*`; NOT Grant-ratified. The named static-existence test (impose winding as BC, relax, ask if the core rails `S→0`) IS the K=2G eigenmode-existence open item (`program-arc-map.md:118`). GUARD: distinct from the falsified energize-LOCK formation route — any prereg carries the challenge-canonical-negative config-grep.
  - **`clm-zdual1`** — the channel-duality inversion (A1 bond compliance `C_0/S→∞` shorts bulk `Γ_bulk=−1`; T2 permittivity `ε_0·S→0` opens EM `Z_wave→∞`; both `|Γ|=1`, opposite phase = the impedance dual). **Consistency-class reading over the Grant-ratified INVARIANT-S2 sector split** (`resonant-lc-solitons.md:35`, ratified 2026-06-15); **solidity 0.50**; ONE open sub-detail — what circulation `A_I` does AT the knot core (Route-C addresses static-B transparency, not the core interior; wall-fork Meissner `μ→0` is a second unreconciled reading). NOT Grant-ratified by the 2026-07-21 mapping.
- **Walk RECORD:** `research/2026-07-21_phase-space-inversion-walk_RECORD.md` (Grant's question `[sic]` → sector carve → the three-reading mapping → Grant's ratification `[sic]` → ratified/candidate/parked split → vessel-state skirt).
- **Verify-before-cite flag (surfaced, not resolved).** The naive "ε collapses ⇒ EM channel opens" is only half the picture: the `C_0/S`↑ (A1 compliance) vs `ε_0·S`↓ (T2 permittivity) inverse-monotonicity is a **sector split, NOT a sign error** — already Grant-ratified as **INVARIANT-S2** (2026-06-15). So `clm-zdual1` is well-grounded in ratified canon; the residual open piece is only the **μ-at-core direction** (cold vs Meissner-`μ→0` vs relativistic-inductor-rising). Recorded so the auditor lane reads it against the leaf.
- **Deferred / routed:** the `def-` vocabulary-register row for `saturation-rim-inversion` (name proposed-for-ratification) is **DEFERRED** to a post-#781 batch (register-row conflict-risk) — NOT minted in this PR. Two register rows now queued for that batch: relative-offset + this. The static-existence test is routed to the eigenmode-existence lane (un-fired). The sign-flip question is parked (no claim). A flag-don't-fix mini-sweep of disk/rim + phase-space + `S(A)`-circle sites is listed in the PR body as candidate cross-ref/revisit sites (no leaf outside the two new files touched).
