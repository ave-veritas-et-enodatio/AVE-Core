# Rulings — doc-lane calls F3/F5/F1 (2026-08-07)

### ENTRY 2026-08-07-rulings-f3-f5-f1

Core-orchestrator record. Grant's words on the #915 doc-lane decision package.

## R20 — F3: FULL REMOVAL. The quotation chain is re-anchored

**Grant, verbatim:** *"I think we might want the full term 'pre-geodesic plasma' fully
removed right?"* — ruled as the re-anchor route. Execution (doc lane): (1) apply the
R9 rename at the origin site `04_generative_cosmology.tex:491` (live print prose,
renameable); (2) re-issue the glossary anchor as a dated v2 entry quoting the NEW text,
the old quotation entry struck-with-pointer per Rule 12 (body preserved); (3) re-point
the four citing leaves to the v2 anchor (NOTE: the live anchor is `:75` — the `:73` in
the original routing was itself drift); (4) the retired term survives ONLY inside
Rule-12 dated supersession notes (history), never as live vocabulary. The
glossed-quotation carve from #915 is SUPERSEDED by this ruling.

## R21 — F5: R16 EXTENDS to the second clause (dated follow-on)

**Grant, verbatim:** *"follow rec."* Read at source (`trampoline-framework.md:220` at
the #915 branch): the two original clauses were mutually CONSISTENT — both put the 2 on
the field — and BOTH are backwards against the Grant-adjudicated A-008 resolution at
`:225-227` (the FRAME carries the 2; the standard SU(2)→SO(3) direction — 720° of
frame rotation returns the spinor). THE EXTENSION: the R16 strike covers the second
clause; corrected wording (frame↔field swap): *"This is the SU(2) → SO(3) double
cover: 720° of FRAME rotation = 360° of FIELD rotation = identity."* Same in-place
Rule-12 pattern as the first clause (strike-preserve, no line-count change — ~136
downstream line-cites). PROVENANCE CORRECTION carried with it: R16's stated rationale
("first clause vs its own second clause") originated in the A-008 propagation record's
FLAG-D table entry and was carried into R16 verbatim by the orchestrator — the actual
inconsistency was always both-clauses-vs-the-A-008-resolution. The ruling's SUBSTANCE
(the canonical direction) is unchanged. → doc lane lands the clause-2 correction.

## R22 — F1: the tag half routes back to the doc lane for a spec fragment

**Grant, verbatim:** *"let's route back."* The R1 treatment-(i) tag edit does not
execute until a repo-resident spec exists: the doc lane (whose costed proposal defined
"actionable") drafts a docket spec fragment — definition of actionable, the tag
format, the site-selection rule, census SHA pin — as its own small records PR;
merge-ratifies; then the tag edit runs against the frozen spec.

## Rider — the #915 excerpt-ratchet failure is a real catch

The non-required ratchet fired on 9 added line-cites lacking adjacent verbatim
excerpts. Routed to the doc lane's follow-up (fix by adding excerpts, not by
routing around the gate).
