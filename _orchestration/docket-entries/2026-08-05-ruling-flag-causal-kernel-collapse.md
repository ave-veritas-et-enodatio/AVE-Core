# Ruling — FLAG-CAUSAL resolved by kernel collapse (2026-08-05)

**Grant, verbatim [sic], the origin statement:** *"wasn't the abrupt transition just be when the
saturation is below certain limit so the energy can't transfer to the next node, i.e the mass
gap"* — followed, after the core walk integrated it, by the ratification: **"that lands."**

**The ruled content (walk-ratified, RULED-PENDING-RECEIPTS):**
1. **The wall self-terminates by kernel collapse.** Every transport (bond) coupling that rides
   the Ax4 kernel is graded to zero at the last bond (G_bond ∝ S → 0 across A → 1). Energy
   cannot transfer to the next node because the coupling itself vanishes — cold-lattice physics,
   no Regime-IV content required.
2. **Reflection is STATE-FORCED, not chosen.** Beyond the wall the region is stateless for every
   kernel-riding channel at every frequency (the "mass gap" statement generalized: all transport
   dead = one big gap). A wave arriving at a stateless region returns by unitarity. Linear
   cross-channel conversion dies with the same bond couplings.
3. **The mirror family holds in BOTH density branches.** A zero-stiffness spring into anything
   is a free end (stress node — the canonical short in the impedance convention), independent of
   the ρ profile. The Γ_L fork of the echo-v2 sweep collapses to the free-end member; the
   FLAG-W final-termination question is MOOT (profile physics lives in the APPROACH — the
   certified delay laws — not the end condition).
4. **Regime IV is fenced out of the echo observable.** The echo program is cold-lattice-complete.
5. The EM channel is the mapped exception (SYM-matched, Γ_EM = 0, transparent) — consistent
   with the canonical channel-split table.

**One load-bearing premise, routed for verification (the dispatched last-bond lane):** that every
shear/rotational transport coupling actually carries the kernel (G ratified; γ and G_c grading
provenance = the open placeholder territory per the two-band lane's VALUE-PROVENANCE verdict).
KILL CONDITION: a transport coupling that does not ride S reopens the conversion channel and
demotes this ruling to conditional. Bins: KERNEL-UNIVERSAL / COUPLING-EXCEPTION /
UNDERDETERMINED (canon lacks the grading provenance — routes a canon question, does not default).

**Consequences routed:** doc-lane Q2/Q3 inherit the derived form pending the lane (Γ_shear's wall
row becomes derived-from-the-kernel rather than RHO-A-conditional); the FLAG-CANON
(`vol3/claim-quality.md:122/:124`) repair form upgrades the same way; wall-taxonomy §10 and the
FLAG-W leaf blocks gain the kernel-collapse derivation on canonical propagation — ALL gated on
the lane's receipts + Grant. Cross-ref: the electron self-trap as the same mechanism pointed
inward (feeds ontology-note E1; adjudicates nothing).
