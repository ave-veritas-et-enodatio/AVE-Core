# Rulings — the GW-formation walk lands: mode carve, overlap lane, antenna candidate (2026-08-07)

### ENTRY 2026-08-07-rulings-r28-r30

## R28 — the mode carve: Grant-agreed at walk level; audit lane GO

**Grant, verbatim:** *"shear vs. bulk as diff vs common makes perfect sense to me"* +
*"let's run the audit lane."* The GW-formation walk's output (recorded here as the
citable summary; UN-AUDITED until the audit lands): from the lattice's frame, a binary
is a mechanically-commutated polyphase source; the GW is the DIFFERENTIAL-MODE wave on
the mechanical shear line (commutation-driven); the bulk channel is the COMMON-MODE
compression line (radial-AC-driven — the lumps' separation oscillating); the dark wake
is the reactive near-field trail of a moving biased patch (canon's surviving sense
only); LIGO is a differential pickup (balanced bridge); pulsar timing is a WATTMETER at
the transmitter (a source power-budget audit — no compression-line receiver exists).
Consequence carried from the walk: the #919 shear-anchor premise is DISSOLVED at
mechanism level (two source couplings, not one geometry-locked ratio) — its formal
disposition follows the audit + the overlap lane. The two-picture reconciliation
(echo-sector shear displacement = the first-order carrier; trampoline-sector impedance
modulation = what the EM port reads of it) is part of the audited set. → background
auditor verifies the full row set against canon before any translation-circuit landing.

## R29 — the overlap-integral lane: GO ("let's compute")

**Grant, verbatim:** *"let's compute?"* The pluck question, lattice-native: does the
rotating-quadrupole bias distribution of a circular binary have nonzero overlap with
the compression line's propagating common mode? Brief:
`_orchestration/2026-08-07_overlap-integral-brief.md` (this PR). Grant launches (the
sourcing satellite is the natural host). #913/#919/arc-scope/#770 remain held on its
verdict plus the audit.

## R30 — the compression-line antenna: EXPERIMENT CANDIDATE registered

**Grant, verbatim:** *"we should make sure to view the compression line antenna as a
possible experiment."* Registered as a bench candidate under the bench-documentation
pattern (Requirements derived-first, trade study after): a receiver coupled to the
longitudinal/bulk channel — the first proposed bulk-channel antenna; today's only
bulk constraint is the transmitter-side power audit (pulsar Ṗ_b). Design inputs when
taken up: the overlap lane's coupling structure (a receiver is the reciprocal of the
source pickup), the impedance-probe primitive's Ax3 READ-mode constraint, and the
channel-Z canon. Queued beside the cRIO bench for when the hardware program resumes;
NOT dispatched.
