# Rulings — the GW-formation walk lands: mode carve, overlap lane, antenna candidate (2026-08-07)

### ENTRY 2026-08-07-rulings-r28-r30

## R28 — the mode carve: Grant-agreed at walk level; audit lane GO

**Grant, verbatim:** *"shear vs. bulk as diff vs common makes perfect sense to me"* +
*"let's run the audit lane."* The GW-formation walk's output (recorded here as the
citable summary; UN-AUDITED until the audit lands): from the lattice's frame, a binary
is a mechanically-commutated polyphase source; the GW is the DIFFERENTIAL-MODE wave on
the mechanical shear line (commutation-driven); the bulk channel is the COMMON-MODE
compression line (radial-AC-driven — the lumps' separation oscillating); the dark wake
is the reactive near-field trail of a moving biased patch (canon's surviving sense
only); LIGO is a differential pickup (balanced bridge); pulsar timing is a WATTMETER at
the transmitter (a source power-budget audit — no compression-line receiver exists).
Consequence carried from the walk: the #919 shear-anchor premise is DISSOLVED at
mechanism level (two source couplings, not one geometry-locked ratio) — its formal
disposition follows the audit + the overlap lane. The two-picture reconciliation
(echo-sector shear displacement = the first-order carrier; trampoline-sector impedance
modulation = what the EM port reads of it) is part of the audited set. → background
auditor verifies the full row set against canon before any translation-circuit landing.

## R29 — the overlap-integral lane: GO ("let's compute")

**Grant, verbatim:** *"let's compute?"* The pluck question, lattice-native: does the
rotating-quadrupole bias distribution of a circular binary have nonzero overlap with
the compression line's propagating common mode? Brief:
`_orchestration/2026-08-07_overlap-integral-brief.md` (this PR). Grant launches (the
sourcing satellite is the natural host). #913/#919/arc-scope/#770 remain held on its
verdict plus the audit.

## R30 — the compression-line antenna: EXPERIMENT CANDIDATE registered

**Grant, verbatim:** *"we should make sure to view the compression line antenna as a
possible experiment."* Registered as a bench candidate under the bench-documentation
pattern (Requirements derived-first, trade study after): a receiver coupled to the
longitudinal/bulk channel — the first proposed bulk-channel antenna; today's only
bulk constraint is the transmitter-side power audit (pulsar Ṗ_b). Design inputs when
taken up: the overlap lane's coupling structure (a receiver is the reciprocal of the
source pickup), the impedance-probe primitive's Ax3 READ-mode constraint, and the
channel-Z canon. Queued beside the cRIO bench for when the hardware program resumes;
NOT dispatched.

---
## AUDIT OUTCOME (2026-08-07, pre-merge — the correspondence audit's verdicts, applied)

The background audit returned per-row verdicts; this dated section corrects the walk
record BEFORE first merge (the walk's verbatim content above is preserved unedited):

1. **The mode carve is PRIOR ART, canon-consistent:** `port-register.md:37` [canon]
   already decomposes the K4 4-port space as A1 = common-mode scalar/longitudinal,
   T2 = differential/traceless (the shear, the photon/GW). Rows land as EE
   re-expressions of the incumbent, channel-subscripted, KEEP-BOTH against the
   seismological analog at `translation-circuit.md:157` — never as new mints.
2. **The "radial-AC-only" drive restriction is STRUCK from the walk output — it
   reconstructs a closed path.** #761 §2
   (`research/2026-07-20_mechanical-commonmode-derivation_result.md:79`) names the
   exact inference ("circular binary ⇒ constant trace ⇒ no A1 radiation") and rules
   it FALSE: a longitudinal field radiates at quadrupole via the TRACELESS second
   moment, rotating at 2Ω — the derivation that fired the Q1 revert and the live
   pulsar exclusion. The surviving walk content: the #919 anchor's geometry-locked
   flux RATIO dissolves (two couplings; #761 §1.1 supports one-field-two-modes and
   its own §1.2 computes the coupling separately) — but dissolving the anchor does
   NOT restore bulk silence; #761 §1.2/§2 stand until the overlap lane re-derives
   them lattice-natively. #761 §2's argument is an IMPORTED continuum result
   ("standard acoustic multipole radiation") — the overlap lane is the legitimate
   lattice-native challenge, with the incumbent quoted verbatim in its brief.
3. **Row 4 (dark wake) is DROPPED — CONFLICT:** the orchestrator's "reactive
   near-field trail" inverted the canon tag (`dark-back-reaction-taxonomy.md:23`:
   the dark wake is the FAR-field radiated shear stress; the near-field reactive
   species is the dark RESONANCE). The object is EM-gradient-sourced and out-of-set
   for GW formation.
4. **R30's parenthetical corrected:** "no bulk-channel receiver exists" is
   CONTRADICTED — the LIGO/Virgo network polarization decomposition IS a
   receiver-side scalar-channel measurement (pure-scalar excluded at
   log10 B = +23.09 ± 0.08; mixed admixture only weakly bounded,
   `scalar-gw-bulk-channel_derivation.md:132/:141`). R30 re-scopes to the first
   proposed DEDICATED bulk-channel antenna.
5. **LIGO row wording:** canon noun "impedance antenna" retained; "differential
   pickup" is the added EE lens (KEEP-BOTH); it senses the channel-2 differential
   mechanical mode and reads out on the EM port — the mechanical→EM transducer leg
   is OPEN in canon and must not be asserted derived. "Balanced bridge" dropped
   (collides with the Mg 6-phase bridge).
6. **def-9a4f07 disposition input (audit check F):** REFINE, not retract — sharpen
   sense (a) to #761 §1.1 verbatim ("the longitudinal polarization of the vector
   displacement field ... NOT a separate scalar DOF"); same repair as #761 §1.3's
   own routed wording item. → doc/auditor batch, WITH the shear-Cosserat channel
   tag reconciliation (`device-circuit-models.md:150` + `node-2domain-nport.md:150`
   vs `port-register.md:48/:50`) and the `translation-circuit.md:157/:534`
   Reading-B→Reading-A relabel.
