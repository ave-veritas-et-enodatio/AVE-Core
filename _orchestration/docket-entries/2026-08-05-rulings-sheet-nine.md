# Ruling record — the nine-item sheet (2026-08-05)

**Grant, verbatim:** *"Ok, attack the sheet and let's walk the residue"* — ratifying the
fork-collapse sheet as presented (the pattern: the binary is the framework's, not the lattice's;
seven of ten queued calls collapsed to forced actions).

1. **(174°,−5°): STRIKE** with dated note (vacated-cite class — the referent never existed;
   re-pointing mints a fourth wrong target). → doc lane executes.
2. **A-008 propagation check: GO** — propagate the A-008 canonical frame/field convention
   (trampoline-framework.md:224-227) through cosserat-mass-gap.md + the two-band FLAG-1;
   escalates only if a residual choice survives. → core mini-lane dispatched.
3. **FLAG-3 scope-carve: BLESSED** — diamond-derived results valid where
   connectivity-independence is demonstrated (O(k²) proven, PR #884 G7b), OPEN where not
   (k⁴, band tops, zone edge, chirality); the srs-native static method piloting in the
   twist-coefficient lane. Carve text rides the upgrade wave.
4. **FT-DISC: WINDOW CLOSED** — the orchestrator adjudication stands (literal clause
   unsatisfiable by construction; fireability reading governs; CFG-B certified).
5. **R13: channel-agnostic form BLESSED as final** — dated addendum rides the upgrade wave.
6. **R3: the slew-tier denominator RATIFIED** (kind-selected: a slew rating's argument is a
   rate) — the theorem-thesaurus §6 fraction-bar edit rides the next core hygiene commit.
7. **Zener/avalanche split: EXECUTED** — translation-circuit §4 row updated in place; the
   carrier-sector flag converted to RULED.
8. **SVA amendment batch: EXECUTED** — leaf v0.2 (row 11 official + metric/regex sub-clauses,
   row-4 spectral gloss, row-5 BRACKETED, row-9 sub-clauses, fork-id convention); pilot-1
   retro-score still owed; skill-body mirror Grant-gated.
9. **VSC leaf: GO** — queued as the next core hygiene build (content = the chart-geometry
   framing note's verified layers + the five bench→vacuum modifications + §10 cross-refs).

Residue (walked separately, not sheet items): the twist-coefficient lane's bin (in flight);
the upgrade-wave text fork (LOCKSTEP-EXACT prose vs LOCKSTEP-APPROX discriminator handling —
selected by the lane's receipts + Tier-2 + Grant).
