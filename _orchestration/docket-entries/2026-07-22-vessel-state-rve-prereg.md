### ENTRY 2026-07-22-vessel-state-rve-prereg (2026-07-22): implementer — anisotropic vessel-state RVE prereg FROZEN alone (walk-1 bench)

- The "walk-1 bench" prereg, frozen + pushed ALONE (COMMIT 1) before this fragment: `research/2026-07-22_vessel-state-rve_prereg-FROZEN.md`. Extends the merged **#782** RVE aggregation bench — reuses its KUBC static-homogenization instrument, Lamé gate, STOP-gate, RVE-size/determinism gates — but SWAPS the verdict frame from #782's painted-pre-stress isotropic classes to a GROWN anisotropic vessel state read by small-signal linear response. PREREG ONLY — no driver, no results; the bench runs in a later lane after review + Grant merge. Engine byte-untouched; mints no `clm-`/`def-`; no leaf edit.
- **Shaped by three Grant rulings** (verbatim `[sic]`): (1) the **regime ruling** (walk 1, 2026-07-21: *"this all depends on regime of strain right?"*) → the small-signal-around-true-equilibrium verdict frame (`.OP` grow the vessel → `.AC` probe `≪` every nonlinear scale); (2) **pending-rulings item 10** (`_orchestration/2026-07-20_pending-rulings-and-frontier-queue.md:18`, ✅ APPROVED) → Protocol E `ρ_eff` measured ALONGSIDE `K_eff`; (3) his **"(b) yes"** folding C-V profiling into this prereg.
- **The honest carve (disclosed prominently, §1).** GRADE imposed (Eulerian; the engine cannot self-bind — β-scoping absence-3), STRESS GROWN (seed interior trapped energy → CG-relax to force balance → the hoop-tension/radial-compression pattern emerges self-consistently rather than painted). The #782 isotropic classes (cold/compressed/expanded) run as CONTROLS, not verdict arms. Grade-frame disclosure mandatory in every output. A `grown_equilibrium_exists` STOP-condition guards the whole `.OP` step (the keystone-energize-LOCK / buckling-runaway failure modes).
- **The three-outcome amplitude gate (§3, sign-resolved, frozen).** Scan `ε_probe ∈ {1e-5..1e-2}` both signs: (i) amplitude-independent both signs → clean linear → read verdict; (ii) amplitude-dependent SYMMETRIC (`A_sign ≤ 0.10`) → instrument artifact → bench VOID until fixed; (iii) amplitude-dependent SIGN-ASYMMETRIC (`A_sign > 0.10`, softens under squeeze) → PHYSICAL MARGINALITY (vessel at the buckling edge — a distinct bin, not an artifact). Frozen thresholds `(amp_spread=0.05, A_sign=0.10)`.
- **Frozen verdict bins (§6).** HARD (bridge/shell-carries: `K_ratio_lift ≥ 1.5 AND r_Z ≥ 0.5` → matched direction), SOFT (bottleneck: crash persists, `K_ratio_lift < 1.2 AND r_Z ≤ 0.5` → macro-cage direction), MARGINAL (outcome-iii fires → vessel-at-the-crack-threshold; neither banked). Each carries its kill condition. Percolation sub-check (does the grown hoop-tense network form a closed spanning shell or is it patchy — `hoop_percolates`). Anti-seduction fence FROZEN IN ADVANCE: the self-matching candidate (T and `ρ_eff` fed by the SAME seed reservoir) is NOT a bin — a matched-ish `r_Z` is a CANDIDATE only, gated on a seed-independence demonstration.
- **Protocol E (§4)** measures the STRUCTURAL `ρ` term only (`ρ_eff = K_eff/c_eff²`, L~32–48); the engine hosts no trapped-energy inertia (C-load open, `clm-m5swh9`); NO β claim. Empirical-driver discipline baked in: PML/window cell exclusion, density-peak (not centroid) sampling for the shell, reactance-pair (C-state + L-state) tracking.
- **C-V profiling (§5).** Sweep `ε_bias` both signs, small-signal `K(ε_bias)` at each bias reconstructs the shell (position/width/asymmetry) the way C-V profiling reconstructs doping. Cross-refs the parallel **semiconductor-tier-mapping** lane (cite-don't-duplicate). *Flag-don't-fix: that leaf path does not byte-resolve on `origin/main` at freeze — a parallel-lane forward reference; the result doc must re-verify the landed path before citing a file:line.*
- **Standard gates carried from #782 (§7):** Lamé exterior/interior `∇·u → 0` (`≤ 0.10`); determinism bit-compare; RVE-size gap (`≤ 0.15`); boundary/window exclusion; STOP-gate class check. **Grant prior: `no prior recorded`** (he declined a gut lean — stated explicitly).
- **Result-doc requirement stated in the prereg:** the eventual result must carry a machine-readable `Prereg-file: research/2026-07-22_vessel-state-rve_prereg-FROZEN.md` line, and every `Frozen:`-labeled criterion must byte-match a quoted token in the prereg (the LIVE frozen-provenance gate, gating date 2026-07-22).
- PR: `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. Branch `research/vessel-state-rve-prereg` off `origin/main` @ `b49b1558`. Feeds the continuum radial-solver charter's D5/R1 profile input (#789).

### ENTRY 2026-07-22-vessel-state-rve-prereg-rev2 (2026-07-22): implementer — REVISION 2 of the vessel-state RVE prereg (post-adversarial-review, F1–F9 repaired)

- **REVISION 2** of `research/2026-07-22_vessel-state-rve_prereg-FROZEN.md`, repairing the PR#791 adversarial review's **9 confirmed findings** (3 CRITICAL sharing one root; 1 review finding refuted). v1 was caught **PRE-RUN** — no results exist, so revision is legitimate under **Rule 11** (no-retune-after-FAIL governs results, of which there are none); v1 preserved BYTE-INTACT in git history (this file @ `35150ee0`, this fragment @ `c79d8d4c`), NOT carried inline (audit trail = git). Prereg re-frozen ALONE (COMMIT 1, frozen-first re-established) @ `275b0c91`; this docket note is the second commit.
- **★THE ROOT of the three CRITICALs (F2+F6): the frozen instrument was the exactly-linear #782 primitive** (fixed `Φ`, painted static `S(A)` ⇒ `K u = b` exactly linear) whose OWN result banked its amplitude check as `IDENTITY (not a fireable gate)` (`research/2026-07-21_rve-aggregation-bench_result.md:41`). On it the amplitude gate cannot fire, the `.OP` grow step is not implementable (unique equilibrium annihilates a displacement seed ⇒ dispersed-STOP fires trivially), and v1's C-V mechanism contradicted the static-Op14 freeze. **Repair = a STATE-DEPENDENT (Newton/self-consistent) instrument.**
- **F2** — live geometric-stiffness operator `k_shear,eff(u) = k_s + T(u)/ℓ` (`axiom-register.md:193`; `T(u)` re-evaluated from the solved state every outer iteration, inner Jacobi-CG on the tangent, §2); **the S(A(u)) decision decided-and-frozen** (imposed-`S` / live-`T`, scope-limit `kernel-knee physics not hosted; marginality carried by the T(u) geometric term per #779` — justified: a live `S(A(u))` = a self-binding co-moving grade, infeasible on the lossless engine per β-scoping absence-3; §0); the **★gate-fireability acceptance self-test** — three contrived configs firing (ii)/(iii)/(iv) at instrument-validation time (§3B).
- **F6** — vessel **grown from a frozen imposed interior source** (radiation-stress surrogate body-force `∝⟨A²⟩`, the #767 object per boundary-strain §3 R6), STRESS STATE emergent through the nonlinear balance; `grown_equilibrium_exists` now a real physical question (§1).
- **F1** — **BIN NULL** (no-vessel-effect, energize-LOCK-adjacent) + **BIN DISCORDANT** (stiffen-K with crash-ρ ⇒ ρ measurement verdict-controlling) added; **"straddling 0.5" operationalized** (`δ_rZ=0.05` band + exact tie rule); the **cell-walk exhaustiveness table** (12-row `(gate, lift, r_Z)` partition, §6).
- **F4** — **outcome (iv) stretch-asymmetric** (tensile marginality, distinct from (iii), → UNDETERMINED) + **all-legs-CG-converged precondition** + **residual-tightening robustness guard** (§3). **F3** — **second-difference `K_tan`** verdict formula replaces the contaminated energy quotient (§2). **F5** — operationalized C-V deliverables (POSITION/WIDTH/ASYMMETRY/normalizer/argmax-edge, §5). **F7** — **`yield_saturated`-native reservoir knob** (`A_yield_scale`) + candidate-disposition rule so the fence covers the HEADLINE arm (§1/§6). **F8** — **PAINTED-ANISOTROPIC** provenance-ablation control arm (grown-vs-painted separated from anisotropic-vs-isotropic, §8). **F9** — "bounded, non-secular residual energy" STRUCK (no time axis in a static solve), 3 numbered STOP criteria each with a threshold (§1).
- **Retained** (review did not fault): the `Prereg-file:` pointer requirement, the `seed_class` axis, the STOP routing to Grant, `Grant prior: no prior recorded`, the regime ruling, the A46 phase-space check, Protocol E structural-only scope, the #782-carried standard gates, the ledger, the owed-follow-ons fence.
- **Gates green:** `make verify` + docket lint + the LIVE frozen-provenance gate (0 gating; the prereg is the byte-match reference, not a scanned result doc). Engine byte-untouched; mints no `clm-`/`def-`; no leaf/port-register/ledger edit. PR stays `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; **no bench run in this lane**; Grant's design ratification happens at merge. Do not merge, do not retitle.
