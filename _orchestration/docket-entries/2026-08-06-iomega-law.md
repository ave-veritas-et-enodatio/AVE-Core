# Docket fragment — the I_ω(A) law (FLAG-IOMEGA) (2026-08-06, EXECUTION RECORD)

**Key:** `iomega-law` · **Branch:** `research/iomega-law` (PR #914, `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`) · **Written against `origin/main` = `d129e7ac`** (also the pinned scan surface).
**Dispatch:** [`_orchestration/2026-08-06_iomega-law-brief.md`](../2026-08-06_iomega-law-brief.md), ruled R6.
**Artifacts:** [`research/2026-08-06_iomega-law_prereg-FROZEN.md`](../../research/2026-08-06_iomega-law_prereg-FROZEN.md) (`aa62941f`, pushed **ALONE** with no code) · [`research/2026-08-06_iomega-law_result.md`](../../research/2026-08-06_iomega-law_result.md) · `research/drivers/iomega_law_scan.py` (`dab8aeaf`, committed before its JSON at `301f2a72`) · `research/drivers/iomega_law_check.py` (`ee5517f8`) · `research/drivers/iomega_law_number_check.py` (gating via `make verify`).

---

## Outcome, one screen

**`LAW-CERTIFIED`, delivered as a CONDITIONAL factorized over ONE named canon-internal fork.**
`b = 0` on the carve-out/disidentification arm; `b = −1` on the SYM-loading∧identification arm.
**The verdict DIRECTION is arm-invariant: no receipted-or-conditional reading of the two KEYED
channels reaches the `p = 2` knife.** A flip needs `b ≥ 2` at `a = 2`, reached only by the
un-receipted CH-R chain. The knife itself is untouched (Rule 11); `a = 2` is not promoted; nothing
propagates to KB/manuscript; FORK-3 is exactly as open as it was.

---

## Batch composition (measured, re-taken after the Tier-2 repair pass)

**`7` commits on `research/iomega-law` ahead of `origin/main`** — `git rev-list --count origin/main..HEAD`:
the six shipped by the lane (`aa62941f` prereg-alone → `dab8aeaf` scan driver → `301f2a72` scan
JSON → `ee5517f8` law driver → `d473778c` result + standing gate → `8f8427f4` JSON-restore fix)
plus the Tier-2 repair commit. **Reconciliation (Tier-2 review repair A12, 2026-08-06):** the
pre-repair fragment reported `7` while the branch measured `6`; the reported number was one ahead
of the tree. The count above is re-taken **after** the repair commit and is the measured one — if
this fragment is read at a different tip, re-take it rather than trust it.

---

## Flags routed (result §10)

| flag | routing | state |
|---|---|---|
| **FLAG-FORK-RESIDENCE** — the §2.1 fork (L-A grade-loading at `r_sat`; L-B μ↔`I_ω` identification, `ξ_topo²` bridge unstated) is the γ_c-residence question's territory | residence lane (E1), via the orchestrator | OPEN |
| **FLAG-R8-LIVE-ECHO** — `src/scripts/vol_1_foundations/r8_diag_a_cosserat_wave_speed.py`:257, a tracked non-archive driver, still prints the E-073-canceled prescription as a recommendation string | doc lane | OPEN |
| **FLAG-IMAX-DISCREPANCY** — `I_max ≈ 124.4 A` vs `I_max ≃ 116 A`, pre-existing corpus discrepancy (cite corrected to `pvlas-static-b-verdict.md`:33 by the Tier-2 review) | auditor lane | OPEN |
| **FLAG-BIN-BANK** — the §9 single-token banking decision + the three token-dependent items | orchestrator | OPEN |
| **FLAG-CEFF-CITE** *(new, Tier-2 review 2026-08-06)* — Op16 (`operators.md`:56) cites two Vol-2 Ch-7 locations as carrying `c_eff = c_0·S^{1/2}`; neither carries that formula at HEAD (`:991` gives `c_0√(1−A²)`; the `eq:c_shear` box at `:1032` gives `c_S ≡ c`). **Surfaced, not adjudicated; nothing in this lane rides on it** | auditor lane | OPEN |

---

## PENDING questions — routed by the Tier-2 review, NOT answered here

- **Q1 — the pitch/flywheel-size question. → GRANT.** The per-element PITCH channel (`g_c(ℓ)`,
  `j(ℓ)`) is the one `b > 0` route the lane's three keying reasons do not touch, and canon defines
  neither dependence (`P-I4`: zero hits). Plumber-physical form: *under a static radial squeeze,
  does the flywheel get smaller as well as more numerous — and if the rotor's moment goes with its
  size, which way?* The result doc now carries this as **ABSENCE-OF-COVERAGE, not closure**
  (headline; §2.2 antecedent (3); §3 bound conditionality; §4). **`PENDING`** — no lane may bank
  the `−1 ≤ b ≤ 0` bound as unconditional until this is ruled.
- **Q2 — the SYM-class of the `r_sat` approach. → RESIDENCE LANE (E1).** Is the static
  gravitational grade at the saturation wall a SYM-class both-sectors load (W6's Symmetric-Gravity
  voice, plus the `0.85`-solidity α-invariance leaf and the ten-site μ-grade enumerated at result
  §1.1), or is the rotational channel carved out at this wall (the ratified kernel-collapse
  carve-out, `wall-taxonomy.md`:499, plus the last-bond receipt)? This is link **L-A**, and it
  rides the OPEN cross-grade aggregation fork. **`PENDING`** — it decides which arm the lane's
  conditional collapses onto, and therefore whether the walk is falsified.

---

## What this lane does NOT license

Mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; propagates to no KB or manuscript leaf; changes no
solidity; edits no falsification ledger. Engine `src/ave` byte-untouched and never imported —
every engine line quoted in the result is a receipt about the CODE. The approach-leak knife's
frozen criterion is untouched. The `BIN-CONST-DERIVED` / `BIN-CONDITIONAL` co-fire is reported
with **both** tokens standing; the single-token banking is the orchestrator's call
(`FLAG-BIN-BANK`).
