### ENTRY 2026-08-03-imax-mechanical (2026-08-03): implementer — the `I_max` mechanical batch: a dead cite repaired, a one-legged "fixed two independent ways" corrected to DEFINITIONAL, and the three-sense glyph row landed. The A4 kernel fork stays OPEN and this lane rules NOTHING on it

- **Class: mechanical + naming.** No physics decision, no adjudication, **zero ids minted** (INVARIANT-S11) — no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; no `confidence`, `quality`, `solidity` or grade field on any claim moves. **No numeric value anywhere in the corpus changes**: `124.384 A` stays `124.384 A` and `124.4 A` stays `124.4 A` at every site this branch touches. Four items, all landed: **M1** dead-cite repair (**7 sites** — 3 dispatched, 2 in-file propagations found in the first pass, 2 more found by the audit and repaired in the repair pass), **M2** Rule-12 grade correction, **M3** the `theorem-thesaurus.md` §6 `I_max` row, **M4** this fragment.
- **★ REPAIR PASS 2026-08-03 (audit verdict CLEAR-WITH-REPAIRS; this entry has been edited in place, strikes quoted-and-dated).** Ten findings actioned. **Two arguments were STRUCK, not softened:** the *"cheap discriminator"* (a tautology — `V_YIELD_HAT` is the ratio's own reciprocal) and the *"cheatsheet native-1 tell"* (unsound — the cheatsheet's own rows are inconsistent by `4π`). **One counter-arm was RESTORED** to question 1 (Grant's recorded 2026-07-10 two-tier reading, which makes A4 adjacent to the `I_max` fork rather than dispositive of it). **One new corpus defect was LANDED as a separate flag-don't-fix finding** against `natural-units-cheatsheet.md` (not edited). Two flags were **narrowed** (sense 1 has no KB *leaf*, but does have a derivation; the `±15%` is conditional on a fixed numerator convention). ★**Net epistemic direction: this batch got WEAKER, deliberately.** Two of its three routed questions to Grant lost their supporting argument; what survives is the naming taxonomy, the exact `4π√α` ratio, the fork-shadow finding, and the dead-cite repairs.
- **★ WHAT THIS LANE DOES NOT DO.** It does **not** mint `ave.core.constants.I_MAX`. It does **not** pick which current ceiling the Axiom-4 μ-grade denominator carries. That is **Grant's A4 ruling and is explicitly still OPEN** — `research/2026-07-10_operator-typing-pass_result.md`:112, verbatim: *"**Grant's physical ruling — still OPEN.**"* Minting the symbol would have forced the ruling by fiat, which is why the repair re-points to the engine module instead.
- Base: `origin/main` **`66fc7e69`**. Branch `docs/imax-mechanical-0803`, executed in a throwaway worktree. Every anchor written in this batch was **two-method verified** (filesystem line-addressed read **plus** an independent `git show origin/main:<path>` line-addressed read, both required to match byte-for-byte) — **first pass: 33 anchors, 33 PASS, 0 FAIL. Repair pass (2026-08-03): 43 anchors re-run under the same two methods, 43 PASS, 0 FAIL** (4 of the 43 are lines this branch itself edits and so verify against `origin/main` only — the `ch18/index.md` header + `$I_{MAX}$` row and the `ave_electron_equivalent.lib` header + `I_MAX` row, i.e. exactly the pre-repair text quoted under Rule 12). ★**The repair-pass run caught one live defect in this very entry**: the `vca-mu` ledger row was cited at `:472`, its **branch** line after M2's insertion, while its `origin/main` line is `:410`. Both are now given. **A line-number cite that is only true post-merge is a dead cite on `main`** — the same class this batch exists to repair.
- **★ `verify-anchor-content` — HONEST TRIAGE, replacing the first pass's "exit 0; no drift" line (which was not evidence: the target's own docstring says it *"ALWAYS exits 0"*, so exit 0 is a property of the tool, not of this branch).** Run at `--top 100000` and filtered to this branch's files: **10 advisory hits — 8 on the new `theorem-thesaurus.md` §6 row (`:291` ×2, `:292` ×4, `:293` ×1, `:302` ×1) and 2 on the new `ch18/index.md` note (`:50` ×2). All 10 triage to the tool's OWN documented false-positive classes**, and each underlying anchor was re-verified two-method:
  - **7 hits = "table cross-row quote/cite mis-association"** (a named FP class in the tool's `--help`). In a dense 4-column §6 table cell the checker pairs a cite with the nearest backticked excerpt, which is not the excerpt belonging to that cite — e.g. `:291 → operators.md:145` is flagged for the excerpt `` I_max = E_c*l_node/Z_0 = 115.8703 A ``, which is the quote attached to the **`provenance.md:904`** cite in the same cell; `:292 → relativistic-inductor.md:15` is flagged for `` I_MAX_MU: float = XI_TOPO * C_0 ``, the quote attached to the **`fdtd_3d.py:69`** cite.
  - **3 hits = "generic short strings"** (also a named FP class): `:302 → node-up:124` flagged on the excerpt `` main `` (from the prose *"Nothing on `main` is wrong here"*), and both `ch18/index.md:50` hits flagged on the bare excerpt `` I_max ``.
  - **Underlying anchors, all PASS two-method:** `operators.md`:145, `universal-saturation-kernel-catalog.md`:171, `relativistic-inductor.md`:15, `topological-kinematics.md`:38, `node-up-small-large-signal.md`:124, `single-substrate-scale.md`:75, `resonant-lc-solitons.md`:17, `provenance.md`:904. **Zero real drift; the flags are the checker's known noise floor** (corpus-wide it reports 74.5% drift on checked cites, which is why it is advisory).

#### The finding in one line

**`I_max` is three objects wearing one glyph, and two of them are numerically different ceilings that sit in the DENOMINATOR of a QUADRATIC kernel.** The two ceilings differ by exactly `4π√α = 1.073476` — which is `+15.24%` / `−13.22%` once squared into `A_I²`.

| # | Sense | Value | Owner | Status |
|---|---|---|---|---|
| 1 | **Displacement ceiling** — `V_yield/Z_0`, the node's dielectric filled to its wall | **115.870 A** | transverse `T2` / ε-grade, read at `Z_0` | exact identity; ★**no ave-kb LEAF derives it** — a derivation DOES exist on `main` at `papers/2026_birefringence_letter/provenance.md`:1006 (narrowed 2026-08-03) |
| 2 | **Convection ceiling** — `ξ_topo·c`, a charge convected through the node at `c` | **124.384 A** | Ax2 TKI Row 2 at `v=c`; the μ-grade denominator | **DEFINITIONAL-from-Ax2** (corrected here — see M2) |
| 3 | **Peak amplitude** — where in the cycle a current is read | *no value* | a per-resonator operating point; no sector | a **modality**, not a third ceiling |

#### M1 — dead-cite repair: `ave.core.constants.I_MAX` DOES NOT EXIST (LANDED)

- **Premise verified, not trusted; it holds.** Two methods at `66fc7e69`: (A) `hasattr(ave.core.constants, "I_MAX")` → **`False`** (also `I_YMAX` → `False`, `I_MAX_MU` → `False`; the only `I_`-prefixed export is `I_SCALAR_1D`); (B) `grep -n "I_MAX" src/ave/core/constants.py` → **exit 1, no match**. The symbol has never existed there.
- **The live symbol** is `src/ave/core/fdtd_3d.py`:69 — `I_MAX_MU: float = XI_TOPO * C_0  # ≈ 124.384 A — μ-grade circulation threshold`, evaluating to `124.3840330668883`. **The μ-grade threshold lives in the ENGINE MODULE, not the constants module.** `V_SNAP`, `V_YIELD` and `Z_0` *are* `ave.core.constants` symbols — only the `I_MAX` row was dead, so every repair below is surgical and splits the attribution by symbol rather than rewriting the sentence.
- **The false attributions, quoted verbatim (Rule 12) before repair:**
  1. `src/ave/solvers/spice_models/ave_vacuum_cell.lib`:23 — `` *     I_MAX   = 124.384 A         (ave.core.constants.I_MAX) ``
  2. `manuscript/backmatter/06_spice_verification_manual.tex`:176–177 — *"$I_{max} = \xi_{topo}\,c = 124.384$ A are the canonical \texttt{ave.core.constants} values."*
  3. `manuscript/ave-kb/vol4/simulation/ch18-universal-vacuum-cell/spice-subcircuit.md`:125 — *"corrected here to the `ave.core.constants` canonical `V_YIELD = 43651.851844… V` (`≈ 43651.85`) and `I_MAX = 124.384 A`"*
- **★ TWO FURTHER SITES FOUND IN THE SAME FILES, NOT IN THE DISPATCH — same defect class, same edit, disclosed rather than absorbed silently:**
  4. `ave_vacuum_cell.lib`:12 — the block header `` * Constants (from ave.core.constants): `` **governs** the `I_MAX` row at `:15`, so repairing `:23` alone would have left the same false attribution standing three lines above it.
  5. `06_spice_verification_manual.tex`:187–190 — *"they are a maintained copy of the \texttt{constants.py} values"*, said of all three subckt defaults including `I_YMAX`. Split by symbol (`constants.py` for `V_SNAP`/`V_YLD`, `fdtd_3d.py` for `I_YMAX`).
- **★ TWO MORE SITES, FOUND BY THE AUDIT AND REPAIRED 2026-08-03 (same defect class; the original sweep missed them):**
  6. `manuscript/ave-kb/vol4/simulation/ch18-universal-vacuum-cell/index.md`:39 — the section header `` ### Parameters (from `ave.core.constants`) `` **governs** the `$I_{MAX}$` row at `:45` (`| $I_{MAX}$ | 124.4 A | $\xi_{topo} \times c$ |`). Repaired per-symbol; the header's prior text is preserved struck. ★**The `124.4` is a hand-rounded display of `124.3840330668883` — FLAGGED IN PLACE, NOT CHANGED**: it is the same hand-maintained-literal class FLAG-1 names at `spice-subcircuit.md`:125, and moving a number is outside a naming/attribution repair's scope.
  7. `src/ave/solvers/spice_models/ave_electron_equivalent.lib`:20 — the header `` * CONSTANTS (defaults from ave.core.constants; a driver SHOULD substitute the `` governing the `I_MAX` row at `:25`. ★**This file `.INCLUDE`s the lib repaired at item 1**, so before this pass the included header was honest and the *including* header still carried the false attribution. Comment-only, same as item 1.
- **All seven re-pointed to `ave.core.fdtd_3d.I_MAX_MU`**, each carrying (i) the explicit note that the threshold lives in the engine module and not in `constants`, and (ii) the open-fork caveat on the VALUE. The `.md` sites use the corpus's `~~struck~~`-plus-replacement KEEP-BOTH shape; the `.tex` carries the verbatim prior sentence in a `%` source comment plus a reader-facing `quote` caveat block; both `.lib`s carry a `DEAD-CITE REPAIR` comment block quoting the prior strings verbatim.
- **★ TWO-METHOD SWEEP for any REMAINING `from ave.core.constants … I_MAX` site (run 2026-08-03 after the two repairs above).** Method A: line-scoped `grep -rn` for the `I_MAX` glyph family filtered to lines also naming `ave.core.constants` / `constants.py`. Method B: an independent Python walk over the tree computing a **±6-line proximity window** around every `I_MAX`/`I_max`/`I_YMAX`/`i_max` hit and testing the window (not the line) for a `constants` attribution. ★**The two methods DISAGREE, and the disagreement is the point:** Method A **missed** `ave_electron_equivalent.lib`:25 entirely, because its false attribution is five lines above it in the block header — a same-line grep is structurally blind to a **governing header**, which is the exact defect shape M1 is about. Single-method sweeps on this defect class false-negative by construction.
- **★ SWEEP RESULT — FIVE FURTHER SITES SURVIVE, SURFACED NOT FIXED (flag-don't-fix; all outside this lane's dispatched scope):**
  - `manuscript/ave-kb/vol4/circuit-theory/ch1-vacuum-circuit-analysis/graded-network-response.md`:357–358 — *"constants from `src/ave/core/constants.py` ($Z_0$, $\ell_{node}$, $V_{yield}$, $I_{max}=124.4$ A)"*. **A KB leaf, same class**, inside a pre-reg-style blockquote — the strongest of the five.
  - `research/2026-06-25_vca-mu-circulation-observable-derivation.md` — the verification-ledger row `` | I_max = ξ_topo·c = 124.3840330668883 A | `constants.py:110,278,323-324` + live compute | VERIFIED | ``, at **`:410` on `origin/main` `66fc7e69`** and at **`:472` post-M2** (this branch's Rule-12 insertion pushes it down 62 lines; **both line numbers given because neither alone is currently true**). ★**This one is worse than a module mis-attribution: the LINE cites inside it are dead too.** At `66fc7e69`, `constants.py`:110 is `` C_0: float = 299_792_458.0 ``; `:278` is prose inside the α-drift comment block (`` # named-identification residual of the Class-B golden-torus α (both lift-routes — ``); `:323` is **blank**; `:324` is a Cosserat-length comment. **`XI_TOPO` is at `:356`** — cited nowhere in the row. And the row is stamped **VERIFIED**. Not repaired here: this lane's M2 edit to the same file is a Rule-12 grade correction, and silently repairing a `VERIFIED` stamp in the same commit would blur a naming repair into an audit finding.
  - `research/2026-07-05_em-saturation-keying-functional_RESULT.md`:129 — *"`I_max = ξ_topo·c = 124.384 A` (`constants.py`, …)"*.
  - `research/2026-07-05_em-keying-round2-worked-cell_prereg_FROZEN.md`:33 and `research/2026-07-05_em-saturation-keying-functional_prereg_FROZEN.md`:139 — both *"(`constants.py`)"*. ★**FROZEN pre-regs: MUST NOT be edited.** Recorded so the population count is honest, not as repair candidates.
  - **Honest nuance on all five, stated so the finding is not overstated:** `XI_TOPO` (`constants.py`:356) and `C_0` (`:110`) **are** `constants.py` symbols, so `I_max = ξ_topo·c` is *computable from* that module — what does not exist is a **symbol of that name**. The M1 defect (`ave.core.constants.I_MAX`, an attribute cite) is strictly worse than the loose *"constants from constants.py"* phrasing; both are surfaced, graded differently. **Correctly-attributed sites exist and were left alone** — e.g. `research/2026-07-07_electron-lock-2bS1_prereg_FROZEN.md`:150–151, *"from `ave.core.constants` (+ `fdtd_3d.I_MAX_MU`)"*.
- **No behaviour changes — proven, not asserted.** Both `.lib` edits are **comment-only**: `git diff -U0 origin/main -- <lib> | grep -E "^[-+]" | grep -v "^[-+][-+][-+]" | grep -vE "^[-+]\*"` returns **nothing** for `ave_vacuum_cell.lib` **and** for `ave_electron_equivalent.lib`, i.e. every changed line begins with the SPICE comment character `*`. Re-run as a **second, independent** check: `diff <(git show origin/main:<lib> | grep -vE "^\*") <(grep -vE "^\*" <lib>)` is **empty** for both — every non-comment line, `.param` / `.subckt` / `.INCLUDE` / element alike, is byte-identical to `origin/main`. `ave_vacuum_cell.lib`'s subckt `.param` literals `V_SNAP=510998.95 V_YLD=43651.85 I_YMAX=124.384` are byte-untouched, shifted only by the comment lines above them. The `.tex` and `.md` edits are prose.

#### M2 — the one-legged "fixed two independent ways" (LANDED, Rule 12)

- **Target:** `research/2026-06-25_vca-mu-circulation-observable-derivation.md`:113–117, written **2026-06-25**, under the heading *"The SCALAR threshold I_max is CLOSED (no free factor)"*. Quoted verbatim in the correction and **preserved unedited** beneath a 🔴 header per Rule 12.
- **The claim:** *"`I_max = ξ_topo·c` is fixed two independent ways: (1) canonically as the V→I, V_yield→I_max dual of the varactor under the single Axiom-4 kernel (`relativistic-inductor.md:15,:18`, clm-p5cf3t); (2) re-grounded by the rest-energy mapping `E_0 = ½ L_0 I_max² = ½(ξ_topo⁻² m_0)(ξ_topo c)² = ½ m_0 c²` (`relativistic-inductor.md:28`). No free factor lives in the constant itself."*
- **Leg (1) fails as a scale-fixer: it is FORM-ONLY.** The cited line says, verbatim (`relativistic-inductor.md`:18): *"This is structurally identical to the varactor equation, with the substitution $V \to I$ and $V_{yield} \to I_{max}$."* That statement is about the **shape** of the kernel. It puts *a* current in the denominator slot without saying **which**. ★And executed the way the medium executes it — through `Z_0` — it returns the **rival** number: `V_YIELD / Z_0 = 43651.851844321354 / 376.73031346177066 = 115.87029311022246 A`, with the inverse `Z_0 × 115.87029311022246 = 43651.851844321354 = V_YIELD` exact to the last printed digit.
- **Leg (2) fails as a scale-fixer: it is the tautology the canon already names.** `manuscript/ave-kb/vol1/axioms-and-lattice/ch1-fundamental-axioms/single-substrate-scale.md`:75, verbatim: *"**the $\xi_{topo}$ factors cancel identically**; face (3) is true for ANY value of $\xi_{topo}$ or $\ell_{node}$, so it carries **zero information** about the substrate scale."* A relation true for **any** `ξ_topo` cannot fix `ξ_topo·c`.
- **Corrected grade: DEFINITIONAL-from-Ax2 (Row 2 evaluated at `v=c`)** — `topological-kinematics.md`:38 (*"**Row 2: Current $\leftrightarrow$ Velocity.**"*) with the `v_max = c` evaluation at `:46`–`:50`. That is a perfectly good provenance; it is simply not *two independent ways*, and the difference is load-bearing because the one-legged claim was the standing reason to believe no rival sense could exist.
- **Explicitly unaffected:** the α-echo classification in the preserved paragraph **stands** (both senses carry `e`). The doc's own §"the per-cell MAP I_cell → I_max is OPEN" **stands and is strengthened**. No value in that doc moves.

#### M3 — the `theorem-thesaurus.md` §6 row (LANDED, naming-only)

- **Location:** `manuscript/ave-kb/common/theorem-thesaurus.md` §6, immediately **after** the `$Q$` glyph row's *"Adjacent, deliberately NOT folded in"* blockquote and **before** the Voigt/Reuss class-label row — i.e. **the second glyph row, after `Q`**. Header: `### $I_{max}$ — three senses: two distinct current CEILINGS + one MODALITY reading ★(the SECOND symbol row; the six above are surnames, $Q$ was the first glyph)`.
- **House format matched against the `Q` row**, element for element: `###` header with the `★(axis)` suffix; an italic parenthetical stating the addition date, the axis and the verification base ref; a two-axis "read the axis before the entry" sentence; the four-column table `Object | Sector / owner | Where | Note`; and a `> ★**The live hazard, stated plainly**` blockquote. §6 carries **three axes** — name (6 surname rows), glyph (`Q`, now `I_max`), class label (Voigt/Reuss) — and this row **declares its axis as `glyph`, the same as `Q`**; nothing in the §6 preamble is relaxed to admit it.
- **§6's admission bar re-verified against `:212`** (verbatim: *"an entry lands when one name or glyph demonstrably denotes **≥2 distinct corpus objects whose properties could be swapped**, and **every sense carries a verified on-`main` file:line**"*). **3 objects ✓** (two ceilings + one modality). **Swappable properties ✓** — a value, in a quadratic denominator. **On-`main` `file:line` per sense ✓** — sense 1 `operators.md`:145 / `universal-saturation-kernel-catalog.md`:171 / `provenance.md`:904; sense 2 `relativistic-inductor.md`:15,:18,:28 / `topological-kinematics.md`:38,:46–:50 / `fdtd_3d.py`:69; sense 3 `resonant-lc-solitons.md`:17. **The audit's pre-verification was re-checked here, not inherited**: a sample of 8 anchors was re-run two-method as dispatched, then the check was extended to **all 33** anchors written across the batch (33 PASS / 0 FAIL).
- **★ Sense 1's missing KB LEAF — flag NARROWED 2026-08-03 after the audit; still ROUTED, still not closed here.** The flag as first written said *"no derivation leaf on `main`"*, which reads as *"no derivation"* and **overstates**. Corrected: **no `manuscript/ave-kb/**` leaf derives it**, but a derivation **does** exist on `main` — `papers/2026_birefringence_letter/provenance.md`:1006 carries the **raw-CODATA independent route** (*"`ℓ_node = ħ/(m_ec)`, `Z_0 = μ₀c`, `I_max = E_c·ℓ_node/Z_0`. Result **115.870 A / 728.03 A** (2π loop), sharing no intermediate with the canonical chain"*), **cross-checked at `:920`** as a table row reading *"canonical chain **vs raw-CODATA recompute** (independent)"*, verdict **PASS**. Its other on-`main` sites are a `common/` catalog cell (`operators.md`:145), that cell's twin (`universal-saturation-kernel-catalog.md`:171), and the same ledger's arithmetic block (`provenance.md`:904). Sense 2 by contrast has two **KB leaves**. **The asymmetry is real and is part of the hazard** — the sense the KB never states in its own voice is the one a reader will assume does not exist — but the routed follow-on is now precisely **"a KB leaf is owed," not "a derivation is owed."** Routed to the owning lane, not taken here.
- **The row states, in its own text, that it RULES NOTHING**, mints nothing, and lands as `pending-orchestrator` per §7's Owner discipline (*"Owner: orchestration / auditor lane. Changes land by reviewed PR; no drive-by edits."*) — the PR review is the Owner gate.

##### The hazard box's content, and the one genuinely new observation in it

- **`4π√α` is EXACT, algebraically, not numerically.** `(ξ_topo·c)/(V_yield/Z_0) = e²Z_0/(√α·ħ) = 4πα/√α = 4π√α`, using `ℓ_node ≡ ħ/(m_e c)` and `α ≡ e²Z_0/(4πħ)`. Numerically `1.0734764686283056` vs `4π√α = 1.0734764685582188` — `6.5e-11` relative, the residual being **only** that `constants.py`:163 carries `ALPHA` as an independent CODATA literal (`7.2973525693e-3`) while the chain reconstructs `0.007297352569776441`.
- **The plumber's reading: `4π√α = e/Q_face`.** Multiplied by one node transit `τ = ℓ_node/c`, sense 2 moves **exactly one elementary charge** (`1.602176634e-19` C, reproducing `e_charge` bit-for-bit) and sense 1 moves `1.4925121144e-19` C `= e/(4π√α)`. The fork is *"one whole `e` per node"* vs *"the sub-`e` face charge a filled dielectric holds"*.
- **Kernel consequence:** `+15.24%` / `−13.22%` on `A_I²` (`(1.0734764686)² − 1 = +0.1523517287`; `(1/1.0734764686)² − 1 = −0.1322093983`). ★**CONDITIONAL, added 2026-08-03 after the audit:** these percentages are determinate **only AT A FIXED NUMERATOR CONVENTION.** The `4π√α` denominator change is **exactly absorbable** into the still-open per-cell circulation→`I_max` normalization — the very item the `xfail(strict=True)` at `test_vca_mu_circulation_keying.py`:208–213 stands as a tripwire for. A numerator normalization that is itself unpinned can swallow a constant factor with **no observable consequence**, so **the fork is currently UNOBSERVABLE IN THE ENGINE**; it becomes a `±15%` physical difference only once that convention pins. Never quote the percentages without the conditional.
- **The `2π` variant is a third number on the other side** — ★**receipt numerals corrected 2026-08-03 to full precision (the audit caught the printed operands not reproducing the printed quotient).** `provenance.md`:905 carries `728.0345 A`, a **display-rounded** print of `728.0345232087418`. Against sense 2 the ratio is `728.0345232087418 / 124.3840330668883 = 5.85311880679441` versus `1/(2√α) = 5.85311880717656`. **The rounded operands do NOT give that quotient**: `728.0345/124.3840 = 5.853120176228454` and `728.0345/124.3840330668883 = 5.853118620205013` — which is exactly why the receipt now carries full-precision operands rather than display-precision ones. The loop variant sits `1/(2√α)` **above** sense 2 where the bare variant is `1/(4π√α)` **below** it; whether a `2π` belongs depends on loop-vs-face, the same undecided question as the per-cell normalization.
- **★ THE FORK-SHADOW FINDING — the mismatch already sits on `main` INSIDE A SINGLE RESULTBOX, ACROSS THE FRACTION BAR.** `node-up-small-large-signal.md`:124 fixes the **denominator** as *"$I_{max}=\xi_{topo}\,c\approx 124.4$ A"* — a **convection-amplitude** quantity. Ten lines down, `:134`–`:138` fixes the **numerator's kind**: the μ-grade *"responds to the **circulating current** $I$ — a *rate/flux* variable, $I\propto \oint \mathbf H\cdot d\boldsymbol\ell$"* — a **rate/flux** quantity. **Nothing on `main` is wrong**: each half is correct about its own half. But `A_I` is assembled from a rate-kind numerator over an amplitude-kind denominator and **no leaf states the conversion that makes them commensurate**. ★That missing conversion has a name and a **strict test already failing by design**: `src/tests/test_vca_mu_circulation_keying.py`:208–213, `xfail(strict=True)`, reason *"…True grid-invariance awaits the **open per-cell circulation→I_max normalization factor**"*. **The denominator's identity and the numerator's normalization are one question, not two** — which is why closing A4 and closing the per-cell map are likely the same act.
- **★ WHY IT IS NOT URGENT — the flagship falsifier is DENOMINATOR-INDEPENDENT, with a receipt.** `pvlas-static-b-verdict.md`:53–54, verbatim: *"the kernel argument $A_I=I_{vac}/I_{max}$ is *identically zero* under a static $\mathbf B$, so $S_\mu=\sqrt{1-0^2}=1$ at **every** field strength"*; `:87` restates it as untouched *"whichever B-scale a downstream argument nominally references"*. `0/115.870 = 0/124.384`. **The fork is load-bearing for every LOADED μ-grade number and for nothing in the static-B null.**
- **Cite-currency note shipped inside the row — ★now carrying an explicit `UNMERGED-CLASS` marker (added 2026-08-03).** Open PR **#859** (`docs/mr-addenda-0803`, CLEARED, **unmerged at ship-time**) inserts net **+12** lines above the `node-up` anchors (`@@ -80,10 +80,22 @@`) leaving their bytes untouched, so on its merge `:124 → :136` and `:134–:138 → :146–:150`. Quoted phrases are given alongside the numbers so the row stays content-locatable across the shift. ★**The marker is the point:** this is the ONLY paragraph in the row whose content is **not** on `main` and therefore **could not** be two-method verified against `main` — it cites a branch. It is labelled by class so a later re-verification sweep finds it structurally rather than by memory, and so a reader does not mistake a branch prediction for a `main` anchor. If #859 lands differently, this is the paragraph that goes stale.
- **★ Status-judgment on `resonant-lc-solitons.md` — SURFACED HERE, NOT WITHDRAWN.** Sense 3's Note cell (`theorem-thesaurus.md`:293) says the leaf's identification of *"peak"* with *"the hardware velocity limit"* is *"a physical ASSUMPTION … an input, not a consequence."* The audit correctly observes that this is a **status judgment on a canonical leaf** made from a naming row. It **stands as written**, because the review gate is the mechanism: this row lands `pending-orchestrator` per §7 Owner discipline, and **pending-orchestrator review IS the gate** for exactly this class of judgment. Recorded here so the judgment is visible to the reviewing lane rather than buried in a table cell.

#### The three questions ROUTED TO GRANT — verbatim, unanswered, and NOT pre-empted by anything in this batch

1. **★ The structural one, RESTATED 2026-08-03 after the audit restored the counter-arm: A4 is ADJACENT to the `I_max` fork, not a free ruling on it.** A4 asks *"**Is the relativistic-inductor $L(I)$ literally the FPB slew kernel $S_B(A_I)$, or merely its dual?**"* (`research/2026-07-10_operator-typing-pass_result.md`:112; the row's status cell reads *"**A4** ⚠️ **still flagged (proposed reading recorded)**"*). **Sense 2 is the relativistic inductor's ceiling; sense 1 is the FPB slew rating** (`operators.md`:145, *"the FPB slew rating, $I_{max}\simeq116$ A"*).
   - **An A4 "one object, ONE TIER" ruling asserts `115.870 = 124.384` — false, so that arm is EXCLUDED by arithmetic alone.** That much of the original statement stands.
   - ★**But the SAME LINE `:112` carries a recorded counter-arm the first draft of this entry omitted** — Grant's 2026-07-10 walk reading, quoted verbatim: *"**Grant-walk proposed reading (2026-07-10, recorded not ratified):** the relativistic inductor is the **slew-μ kernel at the PARTICLE tier** (transport rate $v/c \leftrightarrow I/I_{max}$), i.e. the *same slew-μ cell two tiers* — "as above, so below" (constitutive-tier FPB slew rating ↔ particle-tier relativistic mass)."* This is a **one-object arm in which the ceilings differ BY CONSTRUCTION** — the two tiers are different tiers, so a single cell rated twice does not assert its two ratings are equal.
   - ★**Consequence, and it is the reason the restatement matters: the two LIVE arms — two-kernels vs one-cell-two-tiers — give IDENTICAL per-tier `I_max` assignments** (constitutive / FPB slew rating → sense 1; particle / relativistic inductor → sense 2). **So A4 does NOT force the `I_max` ruling for free on either live arm**; only the excluded one-tier arm would. Routed as a structural observation, **not** as an argument for either arm.
2. **★ The plumber sentence, which is the whole fork in one question:** is the μ-grade ceiling *"a field filling the node's own dielectric to its wall, or a charge shoved through at `c`?"* Sense 1 is the first; sense 2 is the second; in charge-per-node-transit units they are `e/(4π√α)` and `e` exactly.
3. **★ ~~The cheap discriminator~~ — STRUCK 2026-08-03 (pre-merge audit repair). THERE IS NO CHEAP DISCRIMINATOR; the solver number is a tautology and the cheatsheet tell is unsound.** The struck text, quoted verbatim before striking (Rule 12): *"★ The cheap discriminator, already computed and sitting in the solver: `src/ave/solvers/electron_lock_2bS1.py`:61–63 binds `I_MAX` to sense 2 and derives `V_S = Z_0 * I_MAX` and `V_YIELD_HAT = V_YIELD / V_S  # ≈ 0.931 — where the collapse cap knees`. Evaluated: **`V_YIELD_HAT = 0.9315527906054667`, and `1/(4π√α) = 0.9315527906662876` — the same number.** ★**Under sense 1 that knee would be exactly `1.0000`**, because `Z_0·(V_yield/Z_0) ≡ V_yield` identically … And the corpus independently says which current ought to be native-1: `natural-units-cheatsheet.md`:60 gives `V_YIELD` the native value **"$1.0$" (!)** … while `:61` gives `I_max = ξ_topo·c` the native value `√α`, **not 1**. In a system where `Z_0 = 1` and `V_YIELD = 1`, the native-1 current is `V_yield/Z_0`. ★Recorded as a TELL, not as a ruling …"*
   - **(a) The solver number is the SAME TAUTOLOGY CLASS M2 STRUCK, three items up in this entry.** `V_YIELD_HAT ≡ V_YIELD/(Z_0·I_MAX) ≡ (V_YIELD/Z_0)/I_MAX` — i.e. **sense 1 over whatever `I_MAX` is bound to**. Under sense 2 that is `1/(4π√α)` *by construction*; under sense 1 it is **exactly 1 for ANY `V_YIELD` and ANY `Z_0`**. That is precisely the objection `single-substrate-scale.md`:75 raises against leg (2) — *"true for ANY value … zero information"* — applied to a different pair of symbols. **Zero independent signal.**
   - **Retitled, per the audit's own wording:** *the ratio's reciprocal happens to be materialized in `electron_lock_2bS1.py`:63, which shows the solver is **BOUND to sense 2**, not which sense is right.* That is a useful fact about the engine's current binding; it is not evidence about the physics.
   - **(b) The cheatsheet "native-1" tell dies to its own units** — see the separate finding below. The `Z_0` native-`1` row it leans on is itself inconsistent with the same table's `e = √α` row by exactly `4π`, and under **either** self-consistent repair **neither** sense is native-1.
   - **What remains of item 3:** nothing as a discriminator. The live routing is questions **1** and **2**; item 3 is retained only as the record of a struck line of argument.

#### ★ NEW CORPUS DEFECT, SURFACED 2026-08-03 — `natural-units-cheatsheet.md` §2 is internally inconsistent by exactly `4π` (FLAG-DON'T-FIX; the file is NOT edited)

This fell out of striking the "native-1 tell" in question 3 above. It is a **separate finding against a separate canonical file**, recorded in full here and **routed for its own adjudication**. **`manuscript/ave-kb/common/natural-units-cheatsheet.md` IS NOT TOUCHED BY THIS BRANCH** — it is canonical, the fix is a convention choice with downstream consequences, and choosing one is not a naming lane's call.

- **The rows, quoted verbatim at `origin/main` `66fc7e69`:**
  - `:45` — `` | Action | $\hbar$ | J·s | $1$ | $1.054 \times 10^{-34}$ J·s | — | ``
  - `:56` — `` | Characteristic impedance | $Z_0 = \mu_0 c$ | Ω | $1$ | $376.73$ Ω | ``
  - `:57` — `` | Charge | $e$ | C | $\sqrt{\alpha} \approx 0.0854$ | $1.602 \times 10^{-19}$ C | ``
  - `:59` — `` | Snap voltage | $V_{SNAP} = m_e c^2 / e$ | V | $1/\sqrt{\alpha} \approx 11.71$ | $511{,}000$ V | ``
  - `:60` — `` | **Yield voltage** | $V_{YIELD} = \sqrt{\alpha} \cdot V_{SNAP}$ | V | **$1.0$** (!) | $43{,}652$ V | ``
  - `:61` — `` | Max current | $I_{max} = \xi_{topo} \cdot c$ | A | $\sqrt{\alpha}$ | $1.244 \times 10^{2}$ A | ``
- **The arithmetic.** Take the file's own definitional relation `α ≡ e²Z_0/(4πħ)` with `ħ` native `= 1` (its `:45` row).
  - **Branch A — keep `:57` (`e = √α`).** Then `α = α·Z_0/(4π)` ⇒ **`Z_0` native `= 4π ≈ 12.566`**, not the `$1$` printed at `:56`. `:58`–`:61` are all consistent with this branch (`ξ_topo = √α`, `V_SNAP = 1/√α ≈ 11.706`, `V_YIELD = 1.0`, `I_max = √α`). **`:56` is the single offending row, and it is off by exactly `4π`.**
  - **Branch B — force `:56` (`Z_0 = 1`).** Then `e = √(4πα) ≈ 0.30282`, which **breaks four other rows**: `:57` (`e` is no longer `√α`), `:58`, `:59` (`V_SNAP = 1/e ≈ 3.302`, not `11.706`), and **`:60` — the `$1.0$` `(!)` becomes `1/√(4π) ≈ 0.28209`**, killing the very emphasis the row exists to carry.
- **★ Why it mattered here, and why it is worth its own adjudication.** The struck "tell" argued that in a system where `Z_0 = 1` **and** `V_YIELD = 1`, the native-1 current is `V_yield/Z_0` (sense 1). **Those two conditions cannot hold at once.** Read self-consistently, **NEITHER sense is native-1** in **either** branch: Branch A gives sense 1 `= 1/(4π) ≈ 0.0796` and sense 2 `= √α ≈ 0.0854`; Branch B gives `1/√(4π) ≈ 0.2821` and `√(4πα) ≈ 0.3028`. (The `4π√α` **ratio** survives both — it is convention-free — which is why the `I_max` hazard is unaffected by this defect. Only the tell dies.)
- **Class and routing.** A **unit-convention inconsistency in a canonical reference table**, not a physics error and not a value error anywhere downstream — the SI column is right in every row. But the table is the corpus's answer to *"what is native-1?"*, and it currently answers inconsistently, which is exactly the question a reader brings to it. **Routed as a new corpus defect for the owning lane** (Gaussian-like `μ_0 = 4π` vs `Z_0 = 1` is a real fork with a real downstream tail — `:62`/`:63` already hedge with *"depends on $\mu_0$ choice"* / *"depends on $\varepsilon_0$ choice"*, which is likely where the inconsistency entered). **Not fixed here.**

#### Routed follow-ons (surfaced, not taken)

- **Sense 1 has no ave-kb LEAF (narrowed 2026-08-03 — a derivation exists; a KB leaf does not).** The derivation is on `main` at `papers/2026_birefringence_letter/provenance.md`:1006, raw-CODATA-independent and cross-checked **PASS** at `:920`. What is owed is a `manuscript/ave-kb/**` leaf that carries it — or an explicit statement that the FPB slew rating is *defined* as `V_yield/Z_0`. Routed to the owning lane.
- **The per-cell circulation→`I_max` normalization** remains open, with a `strict=True` xfail standing as its tripwire. It is **probably** the *same* item as A4, not a second one — stated as an observation for the adjudicating lane, **hedged deliberately** (no leaf establishes the identification), and the `theorem-thesaurus.md` row was aligned to this same hedge on 2026-08-03.
- **`natural-units-cheatsheet.md` §2's `4π` inconsistency** — the new corpus defect recorded in the section above. Owning lane's call; **not** this lane's, and the file is deliberately untouched.
- **Five surviving `constants.py`-attribution sites for `I_max`** (one KB leaf, one `VERIFIED`-stamped research ledger row with dead LINE cites, one result doc, two FROZEN pre-regs that must not be edited) — listed with their grades in M1's sweep result. Surfaced, not fixed.
- **The generated-`.lib` mechanism** (SPICE-charter design-(d)) is still unbuilt; this batch fixes the attribution but the literals remain a hand-maintained copy, now of **two** source modules rather than one — which makes the generator marginally more valuable, not less.
