# Walk-framing record — the 2026-08-24 opens walk (W1–W4), framings frozen PENDING-GRANT

### ENTRY 2026-08-24-opens-walk-framing

**Class:** framing record, NOT a ruling. Freezes the four decision framings as
put to Grant in chat on 2026-08-24, so satellite lanes and future sessions
consume the committed framing rather than chat memory (dispatch-quotes-frozen-
criteria discipline). Every recommendation below is the orchestrator's,
explicitly awaiting Grant's word; rulings will land as their own entries.

## W1 — Solver cross-check trades (T2 = the Phase-1 GO gate)

Decision space: `research/2026-08-24_solver-crosscheck-phase0_tradestudy.md`
T1–T6 (all OPEN, SELECTS NOTHING). Orchestrator recommendation as walked:
**T2 = (a) lossless TL element per bond**, with the ω_C-label rider resolved
as **R2 pinned as a TAGGED engineering convention for the exporter only** —
the corpus's R1-vs-R2 physics-level adjudication flag
(`srs-band-structure.md:157`) stays OPEN; the exporter declares its label and
machine-checks it (the Phase-1 brief mandates that test; the `bond_lc` (R1) vs
`ANALYTIC_NETWORK_FACTOR` (R2) symbol hazard is FL-1). T1 ngspice / T3
adjacency export / T4 `.AC` / T6 native-units + SI round-trip: lane calls,
ratify by exception. Consumer: `2026-08-24_solver-crosscheck-phase1-brief.md`
(launch-gated on the ratification entry).

## W2 — Place-vs-amount (the master ruling; gates the baryon lane)

Items: `2026-06-08-fork-45-value-axis.md` + `2026-06-08-proton-node-span.md`.
Framing as walked: 0.841 fm read as a PLACE is a sub-resolution level-set
(≈ ℓ_node/460) — by the R52 §0b logic, a continuum readout the lattice cannot
carry an opinion about; read as an AMOUNT it is a displacement amplitude with
the He-4 strain precedent (package numbers: He-4 1.933 fm ↔ 0.50 %, proton ↔
0.218 % — tagged package-level, re-verify at consumption), and the K2 floor's
73× ≈ (m_p/m_e)/8π circularity tell cuts against the place reading's
quantitative support. Options: (i) place, (ii) amount, (iii) KEEP-BOTH new
axis, N(1520) r_sat as discriminating consumer. Orchestrator lean: (ii) with
(iii) bookkeeping. Grant's ruling pending.

## W3 — γ_c vs G_c (the L² collision)

Item: `2026-08-24-gammac-gc-modulus-identity.md` (options (a) identity /
(b) relation with named ℓ / (c) two moduli). Orchestrator lean: (b) with
ℓ = ℓ_node — bond couple-stress vs node rotational stiffness related by bond
length — with the leaf's joint-falsification claim re-scoped under (b) or (c)
either way. Rider frozen with the framing: **θ route-4 stays un-built until
this rules** (its "γ_c primitive" may be the wrong modulus).

## W4 — the cheap batch

Per-item dispositions live in the items; orchestrator recommendations as
walked: **B(M)** → rename now (pre-consumers; glyph = Grant's pick);
**A₀** → disposition (a), rename to `a_0`/`BOHR_RADIUS` + consumer sweep;
**kernel-argument** → disposition (a), subscripted/named-ceiling `r = A/A_c`
form; **collision-register home** → def-nodes under `ave-vocab-discipline`
(already A6-confirmed; needs only the go). Consumer: Part B of
`2026-08-24_r55-polish-tail-brief.md`.

## W5 — added at Grant's direction after the walk opened

The Smith-annulus observation is deliberately NOT a W-item: it is documented
at full depth as its own open item
(`open-items/2026-08-24-smith-annulus-tube-ratio-pin.md`, WALK-grade,
un-audited) and gets its own dedicated walk. Recorded here only so the walk
inventory is complete.
