### ENTRY 2026-07-21-continuum-solver-charter (2026-07-21): implementer — continuum radial-solver lane CHARTER drafted (β import + Fork-R inheritance)

- Pending-rulings §1 **item 11** discharge-in-progress: the trapped-energy mass-loading β was routed to a continuum radial-solver lane (Grant-approved 2026-07-21, `[sic]`: *"approved the three words from my wue"*). This lane's CHARTER is drafted for Grant review BEFORE any build: `research/2026-07-21_continuum-radial-solver_CHARTER.md`. No solver code, no prereg, mints no `clm-`/`def-`, banks no verdict; engine byte-untouched.
- **Object.** A spherically-symmetric continuum radial-acoustic solver for a SINGLE cage/soliton, inheriting BOTH lattice-infeasible questions: (a) the deep-quasistatic Lloyd / k-scaling single-core exponent `p` (Fork R, `deep-rail-kscaling_derivation.md` §2 — the regime the lattice cannot reach, `k·r_core ≪ π`), and (b) the β magnitude (item 11), evaluated with both competing acoustic-ρ terms present.
- **Import ledger (the honesty core, §3).** I1–I10 tagged LATTICE-MEASURED (`K_eff(φ)` #782; `c_P/c_S` #779/#782) / DERIVED-FROM-AXIOMS (the two-term ρ decomposition FORM; the `r_Z`/Lloyd forms) / TAGGED IMPORT. **I5 = the E=mc² trapped-energy inertia law `ρ_contribution = E_trapped/c²·(participation)`, tagged import-NOT-derived**, split into I6 direction (walk-ratified, `clm-hu1jjw`) / I7 magnitude (OPEN, `clm-m5swh9`) / **I8 the sector-crossed c² (c_P/c_S/c_EM — OPEN, surfaced not picked; `A1⊥T2`)**.
- **Grant decision list (§0, blocks build):** D1 c² sector choice (the pre-test-physics-check plumber-physical question); D2 participation magnitude (β scan vs symbolic); D3 solver formulation (transfer-matrix / radial FDTD / matched-asymptotics); D4 stage-1 pre-walk-vs-wait; D5 pre-stress sign (Fork P, #779).
- **Surfaced tension (flag-don't-fix, §4):** `#782` result §8.2-4 phrases stage-2 as "star-scale Lloyd cancellation" (an ensemble outcome) while `#775` §2 restricts the radial solver to single-core and DENIES it can settle aggregation — reconciled only by splitting single-core `p` + given-medium star-boundary (owned) from the aggregation-coherence derivation (NOT owned; infeasible `N>1`). Routed to Grant, not reframed.
- PR: `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. Branch `research/continuum-radial-solver-charter` off `origin/main`.

- *(2026-07-21 review-repair note: I6 precisified to the #787 C-kin/C-load split — the UP loading is conditional on the D1/I8 sector-crossed inertia; D5 relabeled to the #779-R6 anisotropic remap-evaluation object, retiring the scalar both-signs framing; "settled the constitutive half" softened to "measured the constitutive input"; #787/#788 merge-order note added.)*
