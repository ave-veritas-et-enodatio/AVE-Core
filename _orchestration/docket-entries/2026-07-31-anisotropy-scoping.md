### ENTRY 2026-07-31-anisotropy-scoping (2026-07-31): implementer — the anisotropy observable, FEASIBILITY SCOPING on TWO axes (frontier item 21 + the Grant photoelasticity fold); scoping only, nothing minted

- **Scope fence, stated first.** **SCOPING, not derivation and not measurement.** No solver run, no engine file touched (`src/ave` byte-untouched), **zero corpus files modified**, no claim minted, no `clm-`/`def-`/`sup-` node created, no solidity changed, and the outcome bins are explicitly **NOT FROZEN**. Deliverable: [`research/2026-07-31_anisotropy-observable_scoping.md`](../../research/2026-07-31_anisotropy-observable_scoping.md).
- **Provenance.** Frontier tracker item **21** (`_orchestration/2026-07-20_pending-rulings-and-frontier-queue.md` §21, routed from merged **#802** §6.5 + the 2026-07-28 leak-audit fold, `ENTRY 2026-07-28-leak-audit-carves`). **Axis 2 is a Grant fold** (2026-07-31, verbatim `[sic]`: *"fold"*) from his question, verbatim `[sic]`: *"isn't gravity just macroscopic static strain including e and b field? so vacuum birefringence? if AVE is right?"*
- **★ THE LOAD-BEARING SCOPING OUTCOME (Axis 1): item 21 is NOT an independent frontier item — it collapses onto an already-flagged corpus fork.** Merged **#506** (`research/2026-07-04_srs-elastic-tensor_result.md`) **already ships** the direction-resolved long-wave answer — a per-direction Born-Huang acoustic-slope table at `:186`–`:199`, computed from the $k\to0$ acoustic branches of the same chiral srs-z3 net and cross-validated against an independent direct eigensolve (`:112`). And the answer **FORKS on the bond-stiffness operating point**: at $\rho_{bond}=k_a/k_s=1$ the Zener anisotropy is **`1.000` exactly** and *"all directions collapse to 0.17678"* (`:197`–`:198`) — zero observable — but $K=-0.0589$, mechanically unstable; at $\rho^\ast=9.7734$ the medium is stable ($\nu=2/7$, $K=2G$) and $A=1.229$. The corpus states the fork verbatim at `achromatic-impedance-matching.md:33`: the $\rho_{bond}=1$ match is *"a **lossless-reactive photon operating point** — $K<0$, mechanically **unstable** … **not** a stable static elastic solid; the matter sector sits at a different, mechanically-stable $\rho^\ast$."* **⇒ item 21's physics question is "which operating point does the gravitational-band channel ride?", which is walk-question W1 — surfaced, not picked, no default admissible.**
- **★ AND THE FEASIBILITY VERDICT INVERTS THE TRACKER'S IMPLIED PLAN.** `port-register.md:47` (G2 relabel, canon) makes the **photon the transverse-$u$ branch** — governed by the SAME rank-4 $C_{ijkl}$ as the shear GW, not by an independent rank-2 $\varepsilon\mu$ pair. If that stands (W6), then **existing optical-cavity anisotropy bounds ($\sim10^{-19}$–$10^{-20}$, `preferred-frame-and-emergent-lorentz.md:22`) already constrain the vacuum's Zener number ~18 OOM tighter than any GW measurement could.** The discriminating work is **not** "go measure a GW sky pattern" — it is "answer W1/W6", and existing optical data already bears heavily on it. **Lane recommendation (W8, labelled a recommendation not a decision): route the $\rho_{bond}$ two-operating-point fork as its own frontier item ABOVE item 21, and record item 21 as subsumed by it.**
- **Five Axis-1 findings, all flag-don't-fix, both file paths + verbatim content on the record, neither side reframed.** **F-A1** #506 already holds the long-wave answer; the item-21 flag did not consult it. **F-A2** the operating-point fork above. **F-A3** the canonical inter-channel speed ratios ($\sqrt2$, $\sqrt{10/3}$, and the $c_{shear}=c$ identification the Q1-revert bookkeeping rides) are *"[derived from `K = 2G`]"* (`deep-space-band-map_derivation.md:207`) — but $K,G$ are the **two** constants of an ISOTROPIC solid and this medium has **three**, so a Voigt/Reuss/Hill choice is buried in them (#506 `:147`: *"a single ν is a Voigt/Reuss/Hill averaging choice, not a bare lattice output"*) — while the very leak-audit ruling that generated item 21 declares a VRH average **inadmissible** here (`subc-kubc-bracket_result.md:327`: *"the vacuum is a **single crystal**; there are no grains to average over"*). **Two merged corpus statements in direct tension; surfaced, not resolved.** **F-A4** #802's Zener bracket `[1.330402, 1.605316]` does **not** contain #506's infinite-lattice Born `1.2293` — expected (a ratio of separately-bracketed moduli is bracketed by nothing; #802's own `:53` says the ratio is *"NOT theorem-grade"*), but it means **the #802 pair is the wrong instrument for an anisotropy magnitude and #506's Born value is the right one**. No walk-back owed (#802's block is already `SUPPLEMENTARY…NOT_FROZEN` and enters no frozen read); a future-consumer warning only. **F-A5** the "photon is rank-2 protected" premise is **not supported by the corpus's own EM measurement**: #515 measured the transverse-branch isotropy/degeneracy **at $\rho_{bond}=1$** (`lorentz-on-srs_result.md:84,:120`), where the rank-4 tensor is isotropic anyway — so the measurement cannot distinguish rank-2 protection from operating-point coincidence. **Every #515 number stands exactly as measured; only an interpretation is narrowed.**
- **★ AXIS 2 (the Grant fold) — the SYM claim HAS a lattice-level forcing chain, and it has a sharp residual that disputes its premise.** The forcing chain, from merged **#519** (`saturated-elastic-tensor_result.md:16`–`:18`): the DC-biased small-signal Cauchy tensor is *"the **COLD tensor with ρ → ρ_eff = ρ_cold·(S_axial/S_shear)**, exactly"* — so the entire bias-dependence enters through **one ratio**, and **a perfectly symmetric bias has $S_{axial}=S_{shear}$ ⇒ $\rho_{eff}=\rho_{cold}$ ⇒ the tensor is UNCHANGED at any bias depth ⇒ zero induced birefringence.** That upgrades the polarization-blindness of SYM lensing from asserted to mechanism-backed (CONSISTENCY-class, model-bounded). **The residual is whether the gravitational bias is actually symmetric — and the corpus says it is not.**
- **Four Axis-2 findings, all flag-don't-fix.** **F-B1** *(a verify-before-cite false negative IN the corpus)*: `research/2026-07-13_registers-walk_framing.md:147` records a *"Cite correction"* concluding the SYM/ASYM carve is *"**chat-record framing** … **pending a canonical carve leaf**"* because *"`CLAUDE.md:75` … is the repo's *Pure-AVE-corpus rule* text"*. **It resolved against the wrong file.** The carve **is canon**, at **`manuscript/ave-kb/CLAUDE.md:75`** (INVARIANT-S2, W6 clarification 2026-06-05) — and `graded-network-response.md:137` cites exactly that path. The carve is INVARIANT-level canon plus a canonical leaf, not chat-record. **Surfaced for the auditor lane; nothing edited.** **F-B2** the SAME scalar Axiom-4 kernel yields polarization-**BLIND** SYM lensing at `graded-network-response.md:271` (*"the kernel keys off $\lvert E\rvert$ so $\Gamma_\parallel=\Gamma_\perp$"*) and a polarization-**SPLITTING** uniaxial index tensor at `vacuum-birefringence-e4.md:95` (*"the AVE vacuum is **uniaxial** (optic axis $\parallel$ the pump) … $\varepsilon_{ij}=\varepsilon\delta_{ij}+2\varepsilon'E_{0i}E_{0j}$ (the exact differential of the scalar Axiom-4 kernel … DERIVED)"*) — the difference being a **magnitude-vs-vector** reading of the bias, and the two sites arguably describe different observables ($\Gamma$ at a boundary vs the propagation index). **Both cites on the record; W5.** **F-B3** the gravitational bias state is **intrinsically uniaxial at both scales the corpus measures** — $A=\varepsilon_{11}$ is a strain-TENSOR component in the far field (the corpus's own temporal-$2/7$ vs spatial-$9/7$ split, `vol3/claim-quality.md:42,:45`), and **#779 R6** is explicitly orientation-split at vessel scale (`boundary-strain-amplitude_result.md:96`: *"a **pressure vessel (hoop tension + radial compression)** and `k_{shear,eff}` is **anisotropic**: hoop bonds STIFFEN, radial bonds SOFTEN"*). Photoelastic response is **rank-4**, so cubic symmetry does **not** forbid a split (same carve as Axis 1). **In #519's own variables an orientation-split bias is exactly $S_{axial}\neq S_{shear}$ — the one thing that moves $\rho_{eff}$, and $\rho_{eff}$ is the Zener-controlling parameter.** The §2.2 forcing chain and #779 R6 pull opposite ways on merged numbers and nothing reconciles them. **W4.** **F-B4** the corpus has **NO photoelastic coupling anywhere** — `photoelast|photo-elast|stress-optic|elasto-optic|piezo-optic|Pockels tensor` returns **0 hits by two independent grep methods** over `*.md`/`*.tex`/`*.py`. The polarization-blindness of gravitational lensing is a standing forward statement with **no coupling calculation behind it in either direction**.
- **The $n_{eff}$ direction/sign overload ($\sqrt S$ EM vs $1/\sqrt S$ gravitational) is cited as an ALREADY-LIVE tracked item (13a)** (`engine-capability-map.md:130`; `vol9/ch17-engine-requirements/index.md:32`, *"LIVE (KB-owner decision)"*), **not re-minted as a new finding** — verify-before-cite caught that the apparent contradiction this lane spotted is the same object already on the books.
- **★ THE NON-CONFUSION STATEMENT (routed to be written into any pre-reg on either channel).** A **gravitational-lensing polarization NULL neither corroborates nor kills the flagship E-route falsifier**, and a **HIBEF null neither rescues nor kills the SYM carve**: gravity is **SYM** (both grades co-scale, $Z\equiv Z_0$, $\Gamma=0$, $\delta n\approx+\tfrac14A^2$, $A=1.486\times10^{-5}$ at the **solar limb**, $A^2=2.21\times10^{-10}$) while the flagship is **ASYM** ($\varepsilon$-only *because* $\partial\mathbf B/\partial t=0$ forces $S_\mu=1$; $Z$ varies, $\Gamma\neq0$, $\delta n_{bir}\approx-\tfrac12A^2$, $A=7.7\times10^{-4}$ / $A^2\sim5.9\times10^{-7}$ at HIBEF). **Different loading symmetry, different kernel argument, opposite index sign** — and amplitudes only **`~1.7` OOM apart in $A$ / `~3.4` OOM in $A^2$**, so the non-confusion argument rests on the STRUCTURAL carve, not on an amplitude chasm. **⊗ Amplitude self-correction (§2.5a of the deliverable):** the first version of both files carried $A\sim10^{-9}$ "at the solar limb" — that is the **Earth-surface** value (`appendix-experiments.md:17`, *"canonical Earth strain $\varepsilon_{11}=7GM_\oplus/(c^2R_\oplus)\approx4.87\times10^{-9}$"*), wrong by 4 OOM for the Sun; every site is corrected, the §2.8 reachability audit is **re-run** at the corrected $A^2$ (the *"honest and un-testable"* verdict on the quadratic residual bin is **withdrawn**; both residual bins now band on the photoelastic **coefficient**, verdict `[requires-external-retrieval]`), and **W8's "fire neither" recommendation is RE-OPENED** because it was reached on the wrong amplitude — not re-answered here; that is Grant's walk. **Also flagged, not fixed, routed to the auditor lane:** `domain-catalog.md:50` states *"Solar surface: $\varepsilon_{11}=2.1\times10^{-6}$"*, which is exactly $GM_\odot/(c^2R_\odot)$ — **factor-7 low against that same leaf's own `:45` definition** $\varepsilon_{11}=7GM/(c^2r)$, an apparent residual of the 2026-05-17 factor-7 cleanup (its neighbouring neutron-star and black-hole rows *do* carry the 7). The one thing that WOULD couple them is a demonstration that the gravitational bias is not exactly symmetric (F-B3) — which is the open physics. The SYM channel's own recorded kill-shape is unchanged: `research/2026-07-13_registers-walk_framing.md:160` / docket `2026-07-10_rulings-docket.md:463` — *"any confirmed **polarization-dependence** or **reflection component** in gravitational lensing kills the symmetric carve outright."*
- **Feasibility verdicts, both axes.** **Axis 1: the computation is essentially FREE and partly already done** — Appendix B reproduces #506's whole symmetry-direction slope table exactly (to input precision) with a `~30`-line Christoffel eigensolve, so *"the direction-resolved Christoffel treatment"* the merged bench-frame caveat names is not new work. **The lane is blocked entirely on physics inputs, none of them a compute question:** W1 (operating point), W3 (the lattice-axis sky orientation — 3 Euler angles never derived, though `preferred-frame-and-emergent-lorentz.md:178` already reports a null *against* cubic axes, so an orientation was assumed somewhere), and F-A3 (the $c_{shear}$ definition). **Axis 2: HIGH feasibility, and the first cut is a parameter sweep on already-merged machinery** — channel (i) spring-softening is BUILT (#519's `saturated_elastic_tensor.py`): feed an orientation-split $(S_{axial},S_{shear})$ instead of a symmetric one, re-run the existing Born-Huang extraction, read the splitting between the two transverse branches, **with a known-positive gate** (a symmetric bias must return exact zero by the $\rho_{eff}$ identity). Channel (ii) pre-stress ($k_{shear,eff}=k_s+T/\ell$, #779) is built in the vessel-state driver but not wired to any optical read; channel (iii) bias-induced geometry relaxation is **NAMED AND OPEN** in #519's own MODEL SCOPE (`:29`–`:30`: *"Both are **OUT OF SCOPE here and remain OPEN**"*).
- **Eight walk questions routed to Grant IN CHAT before either axis may fire** (`pre-test-physics-check`, asked BEFORE design per the Rule-16 strengthening; inline prose with bulleted options, none pre-picked): **W1** which $\rho_{bond}$ does the gravitational-band channel ride (the Axis-1 crux; option (b) closes item 21 negative-by-construction and promotes the $K<0$ instability instead); **W2** is $c_{shear}=c$ direction-resolved or VRH-averaged; **W3** where are the cubic axes on the sky (derivable, initial-condition, or domains — and domains would reintroduce grains and overturn the leak-audit ruling); **W4** is the gravitational strain hydrostatic or uniaxial (the Axis-2 crux — *"sitting inside one lattice cell a solar radius from the Sun, is the cell squeezed equally from all sides, or squeezed radially and stretched tangentially?"*); **W5** does the Ax4 kernel see the bias as a magnitude or a vector; **W6** is the photon really the transverse-$u$ branch (if yes, optical-cavity data already answers W1); **W7** does "SYM = both sectors driven" mean equal $S$ per FIELD sector ($\varepsilon$ vs $\mu$) or per BOND channel (axial vs shear) — the two are different statements and the whole Axis-2 forcing chain rides on which; **W8** priority, with the lane's recommendation stated as a recommendation.
- **Bins DRAFT — NOT FROZEN, with an explicit reachability audit per outcome class** (the standing DESIGN LESSON 1). Axis 1: A1-ISOTROPIC / A1-ANISOTROPIC-EXCLUDED / A1-ANISOTROPIC-LIVE / A1-UNDERDETERMINED / A1-DEFINITION-BLOCKED. Axis 2: A2-BLIND-FORCED / **A2-RESIDUAL-QUADRATIC** / **A2-RESIDUAL-LINEAR** / A2-KILLED / A2-COUPLING-BLOCKED / A2-DEGENERATE. **All eleven checked reachable before any freeze**; the A2-RESIDUAL-LINEAR bin is reachable precisely because photoelasticity is **linear in strain** while the kernel's index shift is **quadratic in amplitude** — two different expansions, which is why that bin is not a rescue. **⊗ The two residual bins were RE-BANDED and RENAMED in review** (`A2-RESIDUAL-BELOW-BOUND` → `A2-RESIDUAL-QUADRATIC`, `A2-RESIDUAL-TESTABLE` → `A2-RESIDUAL-LINEAR`): the old names asserted verdicts the corrected solar-limb amplitude does not support. Re-run at $A^2=2.21\times10^{-10}$, the accumulated differential retardance through the solar field is `~9e5` waves (quadratic) / `~2e11` waves (linear) at an $\mathcal{O}(1)$ coefficient, so **neither is "un-testable"** and the bins band on the **photoelastic coefficient**, which is exactly the object F-B4 shows the corpus has never computed. **`A2-KILLED` is materially more reachable than first stated.** No bound invented: the matched-class sensitivities are `[requires-external-retrieval]`.
- **Consistency-vs-emergence class tagged on EVERY candidate statement** (§1.12, §2.9). Two headline-discipline outcomes: (i) *"the anisotropy is a falsifiable AVE-distinct prediction"* **fails `ave-discrimination-check` as written** — any crystalline-vacuum model with a cubic substrate predicts the same structure, and the specific $A$ value is $K=2G$-imported (#506 `:159`: *"ρ* is K=2G RE-IMPORTED, not an independent crystalline determination"*); (ii) *"SYM ⇒ reflectionless achromatic lens"* is **not AVE-distinct as a mechanism-existence claim** and the corpus already says so (`graded-network-response.md:324`: *"a co-doped $\mu_r=\varepsilon_r$ metamaterial also gives $\Gamma=0$"*). The one genuinely AVE-distinct FORM available is a **nonzero gravitational photoelastic splitting** (GR predicts exactly zero at leading order — a presence/absence divergence of the same clean shape as `clm-fofwr1`'s parity chord), but its **magnitude would be an $A$-echo**, so only the FORM is headline-able.
- **No external bound invented.** Thirteen observational constraint classes enumerated (O1–O7 gravitational-band; P1–P6 polarization), each with its discriminating structure; **every magnitude is either a corpus read with a cite or tagged `[requires-external-retrieval]`**, per the standing rule at `research/2026-06-11_alpha-hand-of-god-framing.md:255` (*"any bound number must be WebFetch-verified before citing, NOT invented"*).
- **Two-method receipts (Appendix B).** (1) #506's symmetry-direction slope table reproduced exactly to input precision by an independent Christoffel eigensolve (max abs diff `1.03e-04` at 4-decimal $C_{ij}$ input); (2) the two Zener definitions agree (`1.229553` vs shipped `1.229`), with $C'=\rho c^2_{[110],T\perp}=0.20235$ as a third independent confirmation; (3) the F-B4 photoelasticity absence checked by `grep -rniE` **and** `git grep -niE`, both **0**; (4) every file:line re-read at ship time — three of this lane's own draft cites were wrong and were corrected before commit, and the same pass found F-B1; (5) the $\rho_{bond}=1$ / $A=1.000$ number cross-read at three sites (#506 table row, #506 prose, #516 minimiser).
- **Non-goals fenced.** No solver, no claim, no frozen bin, no contradiction resolved, no walk-back, no invented bound, no auditor manual entry, no KB/manuscript edit, no Axiom-5 candidate, no side picked on W1–W8. `ave-reproduction-gate` deliberately **NOT** fired and the omission **disclosed** — with the standing obligation recorded that any pre-reg firing downstream **must** run it on #506's $C_{ij}$ before banding anything on them.
- **Lane mechanics.** Self-isolated throwaway worktree off `origin/main` @ `512e1ef4`; branch `research/anisotropy-observable-scoping`; skeleton-first incremental commits (one section per commit); engine `src/ave` byte-untouched; **zero corpus files modified** — the only new files are the scoping doc and this fragment. Step-0 skill-selection plan + the retro-pass on applied-set drift (three unplanned skills fired, one planned skill deliberately not fired) are Appendix A of the scoping doc. PR opened `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.
