# Ruling batch — doc-lane figures + conventions (2026-08-04)

Lane: core orchestrator (rulings relayed to the documentation lane via
`_orchestration/2026-08-04_doc-lane-reconciliation-handoff.md`, which carries the full
operational expansion + pointers).

Grant, verbatim: **"4 rebuild white, 5 canonize circuits/, 6 carve-outs, 7/8/11 confirmed,
trailer per-session"** — replying to the doc lane's 2026-08-04 status (items numbered there).

One line each:

1. **R4** five latent vol6 figures (b11/be9/he4/li7/n14): `.tex` sources canonical, rebuild
   WHITE per house style; artwork change on five printed pages accepted; execution is
   generator-first (all five are generator-emitted).
2. **R5** `circuit_c12`: `circuits/` source (builds printed p.92) canonical;
   `figures/circuit_c12.*` retired strike-don't-delete.
3. **R6** retire-sweep carve-outs: be8_decay PLACE-NEW + dt_fusion KEEP
   (`research/2026-06-07_figure-audit-ledger.md:84`/`:76`) remain live; unexecuted June
   PLACE-NEWs get an "execute or re-rule" board row, never a silent retire.
4. **R7** Rule-12 banner form (A3) confirmed as wave-3 default + explicit corpus-sweep GO:
   rendered current-status notes stay; verbatim prior-wording `%` duplication drops (git is
   the trail).
5. **R8** "byte-identical" (A4) confirmed as a receipts-claim: only with an actually-run
   diff/md5, method cited inline.
6. **R9/R10** dedup-completion class + backmatter mirror-tag: no veto exercised; lane
   ratifications stand.
7. **R11** the `07` in-print seam: HOLD the fix; fires inside the ringdown wave
   (one-print-touch).
8. **Trailer**: per-session actual model; no rewrite of affected merged commits.

Also recorded (core-session finding, same date): the doc lane's Petermann factor-2 audit
duplicated frontier-queue row 22 — stale-handoff cost; handoffs now carry live-ledger pointers
("check the queue first").
