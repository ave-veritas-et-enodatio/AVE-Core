### ENTRY 2026-08-03-coldq-v2p4-root

- **Verdict.** `ROOT-CERTIFIED` — **twelve of twelve gates PASS, twelve of twelve self-tests FIRE.** The frozen precedence reaches the physics bins for the first time in this arc and adjudicates them: **`BIN-1-MISS`** (`ω_R M_g = 0.2648078872629827` vs GR `0.37367`, `D_omega = -0.2913322255921462`), **`BIN-2-MISS`** (`Q = 0.9201502744197102` vs `Q_GR = 2.1002135791366907`, `D_Q = -0.5618777615951112`; discriminator `BIN-2-CLOSER-CONVENTION`, FLAG-1 window checked and NOT ambiguous), **`BIN-3-MONOTONE`** (no interior energy maximum; both frozen measures at the window's outer edge `2.0000000000000004`), **`BIN-4` `N/A BY CONSTRUCTION`**. **The pre-registered `(1+ν_vac)` rider FIRED**: deviation from `18/49` is `-0.2791340846729915` against a `3 %` trip level, so `r_eff = r_sat/(1+ν_vac)` is **falsified as a derivation of the eigenfrequency** — recorded as the GOOD outcome the prereg said it would be.
- **Provenance + scope fence.** Grant's standing ruling of 2026-08-03 — *certify the located root, not the rectangle*. **FROZEN-FIRST honoured TWICE in this lane:** the v2.3 prereg was committed ALONE and pushed (`3e2c0c1c`), and the v2.4 prereg was committed ALONE and pushed (`36186006`) before any driver code and before any number existed. Written against `origin/main` = `184db4b6`. Deliverables: the two prereg files, [`research/2026-08-03_coldq-pole-v2.4-root_result.md`](../../research/2026-08-03_coldq-pole-v2.4-root_result.md), [`research/drivers/coldq_pole_v2p4_root.py`](../../research/drivers/coldq_pole_v2p4_root.py) + its results JSON + a gating number check. **Engine `src/ave` BYTE-UNTOUCHED; zero `manuscript/` edits; no `clm-`/`def-` minted; no solidity changed; no falsification-ledger edit — regardless of outcome, exactly as fenced.**
- **★ v2.3 SUPERSEDED PRE-MEASUREMENT, and its file was never edited.** The v2.3 lane froze `3e2c0c1c`, then audited its own frozen document against an adversarial review's findings and found it non-compliant on five of eight conditions. **Its driver was written but NEVER EXECUTED — zero numbers exist from that instrument** — so the supersede is clean pre-measurement, governed by the same rule that took v2 to v2.1: **the v2.3 file keeps its bytes, carries NO retraction header, and the whole supersession record lives in the v2.4 prereg's §S.2**, which reproduces the eight-condition compliance table as its rationale.
- **★ THE DEFECT THAT FORCED IT — a frozen criterion contradicted by a receipt quoted in the same frozen file.** v2.3 froze a gate literally named `GEOMETRIC-CONVERGENCE SHAPE`, testing a **constant ratio floor**, while quoting the declining ratios `1544.6 / 690.97 / 403.39` in its own text. A Chebyshev discretization of a problem with an endpoint singularity converges **ROOT-EXPONENTIALLY**, `E(n) = C·exp(−c·sqrt(n))`, under which the successive ratio declines by construction. **A vocabulary fence is now frozen: "geometric" describes no convergence law in this lane and appears only inside dated quotations of superseded text.**
- **★ THE LAW WAS DERIVED FROM AN IN-REPO BLOB, NOT FROM A RELAYED RECEIPT — and the provenance gap is on the record.** This lane was directed to cite a 12-rung sweep attributed to the PR #856 review. **It went looking and could not find it:** `gh pr view 856` returns no comments and no reviews, the API returns no inline review comments and no issue comments, and the branch tip was unchanged at `982c4c9b` — re-checked again after the battery ran. **Rather than cite an unverifiable receipt, the lane fitted the law itself** on `research/drivers/coldq_pole_v2p2_root_results.json` @ `982c4c9b`, whose diagnostics block carries the per-rung errors at full double precision: pairwise `c` = `5.775382`, `6.100131`, `6.354001` — near-constant, which is the root-exponential signature. Least squares on the **certification rungs only** `{48, 64, 80}` gives `c = 6.216374`, `lnC = 12.962558`, `max|residual| = 0.084849`. **The relayed band `5.4–6.6` contains all three pairwise values and is tagged `ORCHESTRATOR-RELAYED, UNVERIFIED` (I22) throughout; NO gate, tolerance, ladder rung or bin depends on it, and a halt clause fires if a receipt lands and contradicts the fit.** Routed to the orchestrator, not to physics: **if that sweep exists it belongs in the repository so a successor can cite it rather than relay it.**
- **★ THE LADDER IS PLACED BY AN OUT-OF-SAMPLE PREDICTION.** The fit never saw `n = 32`. Extrapolated out of sample it predicts `e(32) = 2.2779698805088156e-10` against a measured `1.2496816369074884e-10` — ratio `1.822840164432575`, **both above the frozen `1e-10`**. Corroborated from the coefficient side by the two-method-verified v2.1 receipt at `research/2026-08-03_coldq-pole-v2.1_prereg-FROZEN.md:489` @ `7d8fe484` (tail `5.3e-16` by `n = 40`). **G2's tolerance stayed `1e-10`, unchanged in value across five freezes, but is now DERIVED**: anchoring the law at the finest certification rung and extrapolating to `n = 48` at the band's upper edge gives a worst-case `1.309885e-12`, and `1e-10` sits `76.3426×` above it.
- **★ `n = 32` IS RETAINED, NOT HIDDEN.** It remains a **GATED** rung of G4(b), G5, FT-5(a) and FT-5(b) over the full five-rung ladder — all of which it passes — and it is shipped as a **mandatory non-gated G2 diagnostic row** carrying mp strings, every separation, the full-ladder max pairwise `1.2496816389659964e-10` (the quantity v2.2's G2 gated on, which still **fails** the tolerance), and the law's own out-of-sample prediction beside the measured value.
- **★ G2b DISCRIMINATES, and that is what it was added for.** On the certified root: `c = 6.216374478994577` in band `[4.4, 7.6]`, `max|resid| = 0.08484862390265135` against a `0.40` floor. **The identical routine pointed at the v2.1-banked pseudo-pole returns `c = 0.25002341694486013` — a factor `17.6` below the band's lower edge — and would NOT pass.** One routine, two objects, twenty-five-fold separation in the law's own parameter. **Honest limit, stated in the result doc:** fitted over the full ladder *including* `n = 32` the law still fits (`c = 6.043847309998414`, `max|resid| = 0.18870776957898983`), so **`n = 32` is pre-asymptotic in MAGNITUDE, not an outlier to the LAW** — the ladder rests on the magnitude prediction, not on G2b.
- **Five further inherited defects repaired, each named.** (3) the false *"and its §9 G0 row"* citation clause dropped — v2.2's §9 G0 row cites v2.1's C11 `8.9716e-16`, not the coefficient tail; the corrected digit-history site is `_orchestration/docket-entries/2026-08-03-coldq-pole-v2.md:20` @ `53bdd90f`, and `0a7dec1f` is never cited. (4) FT-8's non-vacuity arithmetic corrected to `-2.857143e-07` / `+5.714286e-07` (v2.2 and v2.3 both carried `±1.43e-06`). (5) **FT-7 rebuilt as a DIFFERENTLY-CODED EQUIVALENT spec** — closed-form CGL corner entry vs the negative-sum diagonal on the eigenvalue axis, `ell**2+ell-2` weight with reversed-association quadrature on the eigenfunction axis — measuring `1.5015831404915055e-46` and `4.440892098500626e-16`, **neither exactly zero**, where the inherited identical-code-path null returned `0.0` by construction and measured determinism rather than discriminator honesty. (6) mp strings shipped for every rung. (7) FLAG-10 restated: this lane claims no independence **and makes no claim about v2.2's**. (8) FLAG-12 restated: the Makefile contact is a **REAL two-line conflict**, not append-only.
- **★ ONE PRE-REGISTERED EXPECTATION WAS NOT MET, and it is reported as a defect in the expectation.** Expectation 6 predicted FT-7 would return `~1e-16 … 1e-13` on both axes. Axis (b) did (`4.440892098500626e-16`, one ulp of a double). **Axis (a) returned `1.5015831404915055e-46` — non-zero, but thirty orders below the stated range — because that path is built in 50-digit mp, not double, and my expectation was written from the wrong precision.** The gate's purpose is unaffected (both non-zero ⇒ the code paths did not collapse), and the deviation is disclosed rather than smoothed.
- **Checker, frozen pre-measurement, implementing the #854 routed items.** Minimum significant-digits floor of `3` **enforced at both ends** (a low-digit REGISTERED key is a hard configuration FAIL; a low-digit doc token can only be allow-listed); **per-site rather than global dedup**; and **list-valued registration** so the G5/FT-5 count vectors are machine-tied as vectors instead of dissolving into allow-listed single digits. Result: **71 sites (60 distinct tokens), 45 registered, 26 allow-listed, 0 unregistered.** **Scope deliberately NARROWED and stated as a choice:** the checker scans the **result doc only**, and no claim is made anywhere that the prereg is machine-checked.
- **🔴 CORRECTION 2026-08-03, POST-PR, UNDER ADVERSARIAL REVIEW — THE CHECKER DID NOT CHECK WHAT THE BULLET ABOVE SAYS IT CHECKED, and two shipped statements were FALSE when made.** The token pattern was `` `([^`]+)` ``; a negated character class matches newlines, so the §2.1 fenced block was consumed as one span that swallowed one of its own three closing back-ticks and **inverted back-tick pairing for the whole remainder of the document**. Measured on the as-shipped instrument against the as-shipped document: **111 newline-spanning phantom spans**, 71 sites reported where 151 exist, and **34 of the 72 keys registered in the checker's own file NEVER EXERCISED** — including `0.2648078872629827`, `0.9201502744197102`, `-0.2913322255921462`, `-0.5618777615951112`, G2b's fitted `c` `6.216374478994577`, and the run digest `6cec005e0155513a`. **The FALSE statements, quoted:** the frozen §4.5 clause *"every occurrence of a numeral is checked"* (the frozen text is right; the implementation did not meet it) and the result doc's own provenance sentence *"All numbers above are read from the shipped ... and are machine-verified against it"* (at ship, roughly half were not). **The repair is a TIGHTEN-TO-SPEC**: newline-excluding token class, a **completeness guard** (any registered key the document never exercises = hard configuration FAIL), digests classified as checkable, and eight registered numerals given bare-token sites in the result doc so the machine reads what the document claims. **No gate, tolerance, band, bin, threshold, comparator or measured number changed.** Rule 11 forbids loosening a frozen criterion, not making an implementation meet it.
- **★ MUTATION RECEIPTS for the repaired checker (M7, M8) — both mutations were INVISIBLE to the as-shipped instrument.** Isolated harness, fenced files never touched; the harness first reproduced the shipped counts exactly (71 sites / 60 distinct / 45 registered / 26 allow-listed / 0 unregistered) as a fidelity check.

  | run | checker | document | verdict |
  |---|---|---|---|
  | A | as-shipped | as-shipped, unmutated | exit `0` — 71 sites / 60 distinct / 45 registered / 26 allow-listed / 0 unregistered (**reproduces the shipped receipt exactly**) |
  | B | as-shipped | **M7** — G2b's fitted `c` drifted to `6.216374478994578` at its §2.2 site | **exit `0`, MISSED**; site counts *identical* to run A — the mutated token was never even seen |
  | C | as-shipped | **M8** — `D_omega` drifted to `-0.2913322255921463` at its §4.2 site | **exit `0`, MISSED**; site counts *identical* to run A |
  | E | **repaired** | **M7**, same mutation | **exit `1`, CAUGHT** — ``FAIL line 115 `6.216374478994578` UNREGISTERED - not in the shipped JSON and not allow-listed`` |
  | F | **repaired** | **M8**, same mutation | **exit `1`, CAUGHT** — ``FAIL line 183 `-0.2913322255921463` UNREGISTERED - not in the shipped JSON and not allow-listed`` |

  **Read plainly: the gate that was supposed to protect this document's numbers would have passed a corrupted `BIN-1` verdict and a corrupted G2b `c` without a murmur.** That is the whole content of the finding, and it is why the repair is blocking rather than cosmetic.
- **Battery.** `make verify` exit 0. **Determinism: two full driver runs produced identical digests `6cec005e0155513a`, byte-identical apart from `_runtime_sec`**, runtimes 262.19 s / 255.27 s, inside the frozen 3600 s. ruff clean; docket lint clean; frozen-provenance gate clean (three `Frozen:` labels were caught quoting the DRIVER rather than the prereg and were reconciled **to the prereg's bytes**, which is the gate working as designed). Pure-corpus.
- **★ THE PHYSICS READING, stated at the strength the evidence supports and no more.** Both `ω_R M_g` and `Q` are **zero-free-input FORM tests** — in units of `r_sat` the problem has no adjustable parameter at all — so a `29.1 %` and a `56.2 %` miss **cannot be attributed to a fitted parameter, because there is none**. What is falsified is **this chain**: canonical graded profile + Ax-4 kernel + Op16 projection + `Γ = −1` SHORT at `r_sat = 7GM/c²` + reflectionless Regime-I port ⇒ the `ℓ = 2` toroidal pole. **Any one of those inputs could be the thing that is wrong**, and two are flagged untested: **FLAG-3** (`I7`, the reflectionless port, assumed and divided out analytically — now the single biggest threat to the physics reading) and **FLAG-4** (#814 CF-7's unnamed `ρ` at `vol3/claim-quality.md:122`; FORK-3(b)'s `ρ_eff = ρ₀/S³` is fenced off by `X6` and **not run**, and it would move the eigenvalue). **And with `BIN-4` `N/A BY CONSTRUCTION` this lane sees ONE mode**: a miss on one pole says nothing about whether another mode of the same cavity sits at GR's value. **Not a chord; a clean negative on one link of a standing corpus chain, with its conditions named.**
- **Nothing propagated.** No `clm-`/`def-`, no KB leaf, no solidity change, no falsification-ledger row. **The auditor lane adjudicates what, if anything, propagates from the `(1+ν_vac)` rider firing.**
- **THE OPEN DECISIONS ARE GRANT'S, and are not resolved here:** (i) the **BIN-3 plumber question** — whether a leaky resonator's outward-growing `exp(|ω_I| r)` eigenfunction makes "where does the mode live" ill-posed, in which case the localization axis should be **retired** from this arc rather than re-measured (the window was **not** widened); (ii) a **substrate-derived low-frequency cutoff**, without which no completeness or overtone statement in this arc is honest (FLAG-5); (iii) whether **FLAG-3** (the far-field port) or **FORK-3(b)** (the inertia naming gap) is run before the `BIN-1`/`BIN-2` misses are read as a statement about the profile.
- **★ REBASE PROVENANCE + FLAG-12 CONFIRMED EMPIRICALLY.** The frozen mitigation required a rebase onto a fresh `origin/main` before the PR. **PR #854 merged mid-run and the Makefile conflicted on exactly the two lines FLAG-12 named** (`.PHONY`, the `verify:` prerequisite list) plus the `help` block — **a real textual conflict, not an append-only merge**, which is precisely the correction v2.3's characterization needed. Resolved as a **union**: both `verify-coldq-v2-number-check` (#854) and `verify-coldq-v24-number-check` (this lane) run under `make verify`; no recipe body is shared. `make verify` re-run to exit 0 on the rebased tree. **The rebase rewrote this lane's SHAs; both sets are recorded so freeze-before-code stays auditable** — v2.3 `3e2c0c1c` -> `8e9fb03b`, v2.4 `36186006` -> `d5b9978b`, with the prereg blobs byte-identical across them (`894be984a41010f6f9629967464300fe023a2f3d` and `33432ac7fd20eb5c4a348c4b2428a799b5f4dd4b`). The result doc's `Prereg-commit:` still names `36186006`, the commit actually pushed ALONE before any code existed.
