# Electron-identity program — calibration inputs are the minimum set

### ENTRY 2026-08-14-electron-identity-calibration-inputs

**Class:** orchestration sign. Mints no `clm-`/`def-`/`exp-`. Edits no KB leaf
or engine. Lands Grant's in-session 2026-08-14 word so Phase A/E do not
dispatch Layer-8 or a value-derivation of \(\{m_e,\alpha,G\}\).

**Grant (2026-08-14, this session):** \(m_e\), \(\alpha\), and \(G\) are
calibration inputs, and the minimum set. This program will not derive them.

**What this fragment records**

- Matches the existing count: `expected-independent-count: 3` at
  `manuscript/ave-kb/common/interlock-register.md` (`calibration-params:`
  \(m_e\), \(\alpha\), \(G\)).
- Layer-8 (smallest stable soliton with \(m_e\) nowhere in inputs) is **closed
  as a program target**, not a signed kill of a mechanism. The K4 *kill*
  remains \(Q=137\) as cage-emergent identity.
- \(\ell_{\mathrm{node}}\) as posit stands. Nyquist-binding stays CLOSED.
- G's MIXED `/7` FORM is **not** walked back. Chain B′ (forward G on the
  gravity register) is not an electron-identity lane.

**What it does not record:** a canon edit to `form-deriving-value-importing.md`
or a close of the gravity-register OPEN tag. Those are a separate word.
