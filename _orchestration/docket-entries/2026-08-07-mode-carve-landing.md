# Mode-carve correspondence landing — audit-verdicts execution (2026-08-07)

### ENTRY 2026-08-07-mode-carve-landing

**Lane:** implementer. **Source of authority:** the mode-carve correspondence
audit, recorded as the AUDIT OUTCOME section of
`_orchestration/docket-entries/2026-08-07-rulings-r28-r30.md` (merged #921).
**Nothing here is adjudicated by this lane** — every verdict executed is the
audit's, every open question is left open and named.

---

## Per-item outcome

| # | Item | Where it landed | State |
|---|---|---|---|
| 1 | Six landable rows G1–G6 | `manuscript/ave-kb/common/translation-tables/translation-circuit.md` **§12** (+ line-neutral tier pointer at `:159`) | LANDED — consistency-class, no `clm-` minted, KEEP-BOTH box explicit |
| 2 | `def-9a4f07` sense (a) sharpening | `manuscript/ave-kb/common/vocabulary-register.md`:594 | LANDED — REFINE-not-retract, additive, existing text byte-untouched |
| 3 | Stale "sharpens Q1 Reading B" label | `translation-circuit.md`:534 + `translation-elastodynamics.md`:19 | LANDED — one line each, own commit |
| 4 | Shear-vs-Cosserat channel-tag conflict | `device-circuit-models.md` (§CHANNEL-TAG FLAG + ⚑ at `:150`) + `research/2026-06-20_node-2domain-nport.md` (EOF flag + ⚑ at `:162`) | SURFACED ONLY — **PENDING-GRANT** sector-ownership call |
| 5 | FLAG-CEFF-CITE (routed from #914) | `manuscript/ave-kb/common/operators.md` §8 + ⚑ at `:56` | VACATED-CITE surface-note — **ROUTED, NOT RULED** |

---

## The six rows, and what each is fenced against

All six are **CONSISTENCY-class EE re-expressions of an already-canonical
decomposition** (`port-register.md`:37, $V_{\text{4-port}}=A_1\oplus T_2$ under
$T_d$). Audit verdict 1: the carve is **PRIOR ART**. No new claim-id; no
emergence claim; every row channel-subscripted (ch-1…ch-4).

- **G1** binary = a two-line driver; the load-bearing content is **two
  independent source couplings**, not one geometry-locked ratio. The $2\Omega$
  kinematics is tagged `[import]`.
- **G2** ch-2 ($T_2$ shear) = the **differential** mode; carries the observed GW.
- **G3** ch-3 ($A_1$ bulk) = the **common** mode. **MODE IDENTITY ONLY** — see
  the rider below.
- **G4** LIGO = a ch-2 **differential pickup** on the canon *"impedance antenna"*
  (KEEP-BOTH; canon noun retained). The mechanical→EM transducer leg is carried
  as **OPEN**.
- **G5** pulsar $\dot P_b$ = a **wattmeter at the transmitter**; total-power,
  channel-agnostic.
- **G6** two-picture reconciliation: displacement **carrier** vs impedance
  **readout** — one wave, not two channels. Transducer leg again **OPEN**.

**KEEP-BOTH, stated explicitly** (§12.1 box): this tier does **not** redefine,
supersede or absorb the seismological row at `translation-circuit.md`:157 / §6
means-test #28. That row is the **elastic-sibling VALUE-level** means-test
($E_S/E_P\approx23.4$); these rows are the **transmission-line MODE basis** at
role level. Two lenses on one decomposition; where they touch, the seismological
row stays the value-level authority.

## The G3 rider (§12.3) — the load-bearing honesty content

The walk's **"radial-AC-driven" drive restriction is STRUCK** (audit verdict 2 —
it reconstructs a closed path). #761 §2 is quoted **verbatim as a block**, not
paraphrased, from `research/2026-07-20_mechanical-commonmode-derivation_result.md`:79.

The rider states plainly that **dissolving the #919 anchor does NOT restore bulk
silence**: #761 §1.2/§2 **STAND**. The drive question is OPEN and adversarial
only because the incumbent's load-bearing step is an **imported continuum
result** (*"standard acoustic multipole radiation"*); the running overlap lane
(`_orchestration/2026-08-07_overlap-integral-brief.md`) is the legitimate
lattice-native challenge, and **both of its outcomes are pre-declared** in the
rider. Until it returns, **G3 is cite-able for the MODE IDENTITY only**; the
standing drive/port physics is `port-register.md`:93 — Q1 **Reading-A-live**.

## What was REFUSED (§12.4, flag-don't-fix)

- **Row 4 (dark wake) DROPPED as a CONFLICT, not an omission** — the walk's
  "reactive near-field trail" inverts `dark-back-reaction-taxonomy.md`:23, which
  makes the dark wake the **FAR-field radiated shear stress** (the near-field
  reactive species is the dark **RESONANCE**). Also EM-gradient-sourced ⇒
  out-of-set for GW formation.
- **"Balanced bridge" refused** for LIGO — collides with the canonical Mg
  6-phase balanced Wheatstone bridge (`vol6/period-3/magnesium/ee-equivalent.md`:10).
- **No transduction claim** on G4 or G6.
- **No drive claim** on the common-mode line, in either direction.
- **No §6 catalog row minted** (no validating instance exists).
- **R30's bench candidate not landed as a row** (bench-documentation pattern).

**R30 parenthetical corrected on G5** (audit verdict 4): *"no bulk-channel
receiver exists"* is **CONTRADICTED** — the LIGO/Virgo network polarization
decomposition **is** a receiver-side scalar-channel measurement (pure-scalar
excluded at $\log_{10}B=+23.09\pm0.08$, EM-sky-fixed; mixed admixture only
weakly bounded). What does not exist is a **DEDICATED** bulk-channel antenna.

---

## ★Findings surfaced by this lane (flag-don't-fix; none silently absorbed)

1. **The audit's proposed row TABLE is not in the repo.** What the cited docket
   section records is the six-item **verdict list** plus the R28 walk record.
   Row text here is derived from those two, verdict by verdict, and **every cite
   the audit carried was re-verified from scratch** rather than inherited.
2. **Dispatch pin `translation-circuit.md:157` was a false positive.** That line
   carries **no** Reading-B label — only *"Means-test PASS at value level (§6
   #28…)"* plus a pointer. It inherits the reading through the §6 #28 pointer, so
   correcting #28 corrects it. **Nothing edited at `:157`**; a "fix" there would
   have manufactured a label that was never written. The real second site is
   `translation-elastodynamics.md`:19 (the sibling seismology spoke), which
   carries the identical stale sentence verbatim — fixed.
3. **Still stale, NOT fixed (out of the one-line-each scope):**
   `translation-elastodynamics.md` Source footer still calls Q1 *"the OPEN Q1
   bulk-radiation row"* — pre-revert. Routed to the orchestrator.
4. **`def-9a4f07` disposition is a HALF discharge, not a full one.** The brief
   said the sharpening *"discharges"* #761 §1.3's routed wording item; the
   audit's own words are *"same repair as"*. The routed item has two halves: the
   **def-node** wording (discharged here) and a tightening of the
   `master-equation.md:20` **canon line** itself (NOT touched — Grant-ratified
   🔴 TWO-"3"s banner; `#761 §6.2` item 2 at `:181` books it as a
   *"Grant/auditor sector-ownership WORDING ruling"*). The def-node records the
   split so no later reader reads the item as fully closed.
5. **`master-equation.md:20` may be the ROOT of the channel-tag conflict** — the
   very line both conflicting sites cite as authority calls the Cosserat winding
   *"T2 couple-stress"*, carrying **both** labels on one object. Recorded, not
   acted on: it would be a Grant-level call on a ratified banner.
6. **The Op16 row is internally inconsistent** (found in this pass, not in the
   #914 routing): the same `:56` cell's gravitational analog uses
   $c\sqrt{1-A^2}$ — the *site-2* form — not the $c_0\sqrt S$ the row headlines.
7. **Doc-class check result:** `research/2026-06-20_node-2domain-nport.md` is
   **NOT frozen** (no freeze declaration, no `_prereg-FROZEN` suffix, no
   frozen-criteria companion; its three `prereg` string hits reference a
   *different* lane's prereg). It is a tracked hygiene/disambiguation artifact,
   so the flag lands there as an additive dated note — not device-leaf-only.

---

## ★Self-caught defect, recorded not rebased away

The first two content commits landed by **mid-file insertion**. A repo-wide
inbound-cite count re-taken after the edits showed **182 `path:NN` cites rotted**,
**nine of them inside `_prereg_FROZEN` docs** — and a frozen prereg cannot be
re-pinned, so those rots would have been permanent.

Repaired in commit `e193e406` with content unchanged and only placement moved:
the `def-9a4f07` sharpening merged into the existing line; the flags landed as
EOF appends plus one-token inline ⚑ markers; the mode-carve tier relocated from a
mid-file §4.8 to an EOF-appended **§12** (§4.8 could not become "§11" — this leaf
already **has** a §11; caught on a post-move grep). Placement is house-consistent:
this leaf already grows by EOF-append (§9 and §10 sit after §8).

**Count re-taken after repair: 0 inbound cites rotted by this branch, 0 in frozen
docs.**

## Receipts (two-method, engines named)

- **(a) Gating ratchet** — `make verify-new-cite-excerpts CITE_BASE=origin/main`
  → *"OK — every added load-bearing line-cite carries an adjacent excerpt."*
- **(b) Independent second method** — a purpose-built checker re-read every
  `(target, line, excerpt)` triple straight off disk against a ±10-line
  whitespace-normalized window, **sharing no code** with
  `verify-anchor-content.py`: **34 triples for items 1–3, 7 for item 2, 9 for
  item 5 — 0 ABSENT**. It caught a real defect the ratchet cannot see (an
  excerpt spanning a bold marker at `…commonmode-derivation_result.md`:46).
- `make verify` green at every commit; `make test` green.
- **★Instrument false-negative disclosed:** two early advisory-drift runs invoked
  `./.venv/bin/python`, which does not exist in a worktree (the venv belongs to
  the main checkout). They exited silently and read as clean. **They were no-ops,
  not receipts** — the two methods above are the real ones.

---

## ★Tier-2 review repairs (2026-08-07, second pass — NOT CONFIRMED → repaired on branch)

Tier-2 returned **3 must-fix, 4 should-fix, 1 nit**, every one **additive** — nothing landed in the first pass needed rewriting. The auditor also **upheld this lane's `:157` false-positive claim against its own finding**. All eight repaired in-branch; **31 new pins re-verified from scratch before use** (the auditor's pointers were the starting set, not the evidence).

| ID | Repair | Where |
|---|---|---|
| **D1** *(must)* | G3 gains the **FLAG-W termination rider** — new **§12.3(b)** | the compression line's termination is UNRESOLVED (`coldq-polar-family_result.md`:170, *"vent, or does it dead-end?"*, *"Three canonical leaves, two opposite answers, no repair made"*, ROUTED TO GRANT). Scope stated: mode-identity is termination-INdependent and stands; anything termination-dependent is NOT licensed. **R30 bench candidate inherits the flag** and cannot be specified past it |
| **D2** *(must)* | G5 gains the **LIVE Hulse-Taylor channel-attribution contradiction** | `port-register.md`:127 (*"a bulk+shear double-count pulsar timing forbids"*), plus `:87`, `:35`, `vocabulary-register.md`:1060. It is now stated as **the reason the wattmeter lens matters**; G5 explicitly must not read as closure over an open flag |
| **D3** *(must)* | **"Two-method" label on G2/G3 was WRONG and is withdrawn** | `:37` + `:48`/`:49` are two lines of ONE file — one source read twice. **Replaced with genuinely independent receipts:** (a) analytic irrep derivation `k4-port-irrep-decomposition.md`:11 vs (b) separately-run engine eigen-decomposition `src/ave/solvers/node_scattering_multiplicity.py`:81/:83 (*"the COMMON MODE"* / *"the DIFFERENTIAL modes"*), corroborated at `node-2domain-nport.md`:61,63. The withdrawal is stated **in-row** so the bad pairing cannot be cited as precedent |
| **D4** *(should)* | G4 cites **its own fence** | `device-circuit-models.md`:145 (*"do NOT collapse to one unit"*) + the ideal-transformer transducer `node-2domain-nport.md`:200/:201/:204 — with the explicit note that **a transducer object existing in canon does NOT make G4's leg derived**; it names what would have to be. Homodyne receipts `gw-detection-antenna.md`:31 + `fabry-perot-phase-shift.md`:15 |
| **D5** *(should)* | G6 **RE-GRADED to PRIOR-ART-EXISTS** | `gw-impedance-perturbation.md`:10 (readout) and `:18` (shear carrier) are **one leaf** — the reconciliation is already canon-resident. Prior "STRUCTURAL PASS as a de-confliction" was an over-grade; corrected in place. One site per text family added: `einstein-field-equation.md`:82,:84 (carrier) and `:76` (gradient-index) |
| **D6** *(should)* | G1 gains the **§10.4 noun-collision distinction** | "commutation" is already spent in **this same leaf** on the electron's self-commutation (`:832`, `:838` *"the rotor supplies its own commutation; no external commutator"*). G1's binary is the **inverted** case — externally commutated, no self-drive. Distinguished exactly as G4 distinguishes "balanced bridge". Plus `#761 §0`:28 (sector fence) and `def-satshr` (`vocabulary-register.md`:1054 — the trace/traceless split is already a def-node, so G1 mints no split) |
| **D7** *(should)* | G1 gains a **§12.3 rider pointer** | it asserts simultaneous ch-2/ch-3 drive, so the drive-scoping rider is now one hop away, as it already was from G3 |
| **NIT** | `dark-back-reaction-taxonomy.md` `:27 ff` → precise pins | **`:29`** (*"Species 2 — dark resonance"*) + **`:31`** (*"near-field reactive self-energy"*). ★Auditor suggested `:29`/`:33`; `:33` is the **symbol-mislabel** paragraph, so `:31` is the substantive second pin. Verified, then substituted — surfaced rather than silently followed |

**Placement discipline held:** every repair is either in-place on an existing line or inside §12, which is EOF. Diff hunks on `translation-circuit.md` are `:159` (in-place), `:534` (in-place), and `930+` (EOF). **Inbound-cite count re-taken after the repairs: 0 rotted, 0 in frozen docs.**

**Tier-2 receipts:** `verify-new-cite-excerpts` OK; the independent off-disk checker run over the **31 new pins → 0 ABSENT**; `make verify` green; `make test` **2966 passed, 3 skipped, 9 xfailed**.

## Open / routed after this lane

- **PENDING-GRANT:** the shear-vs-Cosserat sector-ownership call (does the
  CHARGE-"3" own ch-2, ch-4, or a projection across both?), including whether
  `master-equation.md:20`'s dual label is the root.
- **PENDING-GRANT:** the `master-equation.md:20` canon-line half of #761 §1.3's
  routed wording item.
- **ROUTED (derivation):** FLAG-CEFF-CITE — which of the four $c_{eff}$ forms is
  right. Not adjudicated here.
- **HELD ON THE OVERLAP LANE:** the G3 drive question.
- **PENDING-GRANT (added by Tier-2 D1):** **FLAG-W** — does the compression line **vent or dead-end** at $r_{sat}$? Three canonical leaves, two opposite answers, no repair made. **The R30 compression-line-antenna bench candidate inherits this and cannot be specified past it.**
- **LIVE against the framework (added by Tier-2 D2):** the `08_gravitational_waves.tex` Hulse-Taylor $\dot P_b$ bulk-vs-shear channel-attribution double-count (`port-register.md`:127) — resolves only with the Q1 ruling.
- **ORCHESTRATOR:** the stale *"OPEN Q1 bulk-radiation row"* footer in
  `translation-elastodynamics.md`.
