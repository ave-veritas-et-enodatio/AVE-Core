### ENTRY 2026-07-21-docket-fragments (2026-07-21): orchestrator — the news-fragments docket convention

- Root cause of the recurring PR conflicts diagnosed (shared-tail appends × GitHub's server-side merge ignoring `.gitattributes` merge drivers, including the built-in `union` × generated-index text-merges); the **docket instance** of the class retired: per-lane fragment files in `_orchestration/docket-entries/`, monolith frozen, lint extended to scan both, the regenerate-don't-merge rule for `manuscript/ave-kb/.index/*` documented. Grant [sic]: "seems like you could fix this routine issue easilh with a dofferent pr process" — correct, and done for the docket. Class-level remainder on record in the README: two `research/*` union-append docs unmigrated (winding-down batteries), and the pending-rulings queue's `merge=ours` silent-drop hazard routed as a separate follow-on. Dispatch-brief templates carry the new convention from this entry forward.
