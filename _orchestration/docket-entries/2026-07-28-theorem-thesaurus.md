### ENTRY 2026-07-28-theorem-thesaurus (2026-07-28): implementer — THEOREM THESAURUS (cross-framework theorem-identity register)

- **Grant's authorization (verbatim `[sic]`):** *"these need added to the vocab/kb somehow, we almost need a theorem thesaurus"*. Landed as a NO-CLAIM register leaf; the row formulations and the structure adjudication are lane/implementer-level.
- **★SWEEP FIRST (two-method at `origin/main` @ `c8ceacc3`), before any design.** The corpus already carries: (i) **`physics-lineage-map.md` T8** (:357–375) — already a theorem-identity record, and the SOURCE of the ceiling (*"an exact isomorphism is ontologically silent"*, :359; kill-certificate status STANDING, :366); (ii) **`translation-circuit.md` §6** means-test corpus (31 rows on `main`, mostly phenomenon-level; the theorem-identity one — **#32** + **§4.7** rows D/E/F/G — is **UNMERGED on PR #804**); (iii) **`ave-analytical-toolkit-index.md`** (9 problem-classes → which AVE tool: a routing axis, orthogonal); (iv) **`foundational-machinery-register.md`** (the VIEW over AVE's OWN 10 `clm-` machinery members); (v) `vocabulary-register.md` (`def-` terms + the `open_ambiguity` axis) and `historical-precedents.md`. **The gap:** nothing declared *one established theorem's several names to be one theorem*, with an exactness class and a ceiling attached.
- **★STRUCTURE ADJUDICATION — new leaf `manuscript/ave-kb/common/theorem-thesaurus.md`; both briefed alternatives rejected with reasons, and the path deviation SURFACED not taken silently.** (a) **Not a §9 of `physics-lineage-map.md`:** that leaf's own §1 contract is a *fork decision-tree of program history* (:16); its T-entries carry fork / WHY-class / kill-certificate / live-confrontation, exactly one of the fourteen is theorem-shaped, and the §7 Romance Guard does not apply to a theorem. (b) **Not `common/translation-tables/theorem-thesaurus.md`** (the briefed path): that subtree is governed by the Grant-ratified **HUB-AND-SPOKE RULE** (`README-architecture.md`:31 — *"never as a direct X↔Y row"*; §2 defines a spoke as one file per discipline). The thesaurus's core column IS a direct cross-discipline name-set, admissible only because its join is a **mathematical identity rather than a substrate identification** — i.e. it sits OUTSIDE that rule's scope, not inside it. Filing it in the subtree would need an edit to the ratified architecture leaf or leave a rule-violating-looking file in the family. **Deviation routed to the auditor lane** (the move, if wanted, is a `git mv` + a carve-out paragraph in `README-architecture.md` §3). **Why this is an EXTENSION, not a third parallel structure:** it is the **external-import counterpart** of `foundational-machinery-register.md` — that register views the theorems the AXIOMS forced (`clm-`-backed, solidity-bearing); this one indexes the theorems the program IMPORTS (no `clm-`, exactness-class + ceiling in place of solidity). Disjoint membership by construction, and every row CITES its lineage-map T-entry / translation-table row / driver docstring instead of restating it.
- **LANDED — 9 rows, all use-sites two-method verified.** **TH-1** Dirichlet's principle (min potential energy) = KUBC = voltage-clamped drive [EXACT]; **TH-2** Thomson's principle (min complementary energy / min dissipation) = SUBC = current-fed drive [EXACT]; **TH-3** the two-sided bracket — Hill/Huet ordering = **Voigt/Reuss** bounds = the Dirichlet–Thomson conductance bracket = Rayleigh short-cut/cut [EXACT]; **TH-4** Kron exact network representability + boundary Schur reduction (= domain decomposition = Kron reduction = TLM) [EXACT — ★the ceiling's source]; **TH-5** singular-Laplacian solvability = **KCL** = **self-equilibration** = the rigid-body null-space projection (Fredholm alternative) [EXACT]; **TH-6** Born(-Huang) long-wave + internal-strain (Kleinman) relaxation = the acoustic **Christoffel** eigenproblem = periodic homogenization's `k→0` limit [regime-scoped]; **TH-7** the standing-mode **virial/equipartition** identity = the LC-tank `½LI²_max = ½CV²_peak` [EXACT]; **TH-8** **Foster's reactance theorem** = the Caldeira–Leggett oscillator-comb = the Herglotz/positive-real class [EXACT — ★this row IS the regime fence on TH-1/2/3: for a general AC mixed-reactance network `B(ω)` is sign-indefinite and **both bounds FAIL**, so `static / DC / single-sign-susceptance` is a mandatory tag]; **TH-9** the locally-resonant `ρ_eff(ω) = ρ_0[1 + θ ω_0²/(ω_0²−ω²)]` — tagged **IMPORTED-FORM, NOT a theorem**, per #800's own tag (*"the form itself is imported"*) and the on-`main` x36 finding that it is *"**NOT forced** by shunt KCL + losslessness"*.
- **★DROPPED — the constraint lemma** (*"adding a linear constraint to a minimisation cannot lower the minimum"*), on two independent counts: (i) it has **one name in every discipline**, so a thesaurus row carries no thesaurus content; (ii) **no on-`main` use site** — two-method grep for `"cannot lower the minimum"` / `"minimum cannot"` / `"constraint to a minimi"` / `"kinematically admissible"` / `"statically admissible"` / `"admissible set"` returns **0 hits** at `c8ceacc3`; the only corpus statements are unmerged (#804 §4.7.3, #802 §1.3). **Landed instead as the shared proof mechanism named inside TH-1/2/3**, cited to those lanes. Also recorded in §5 with reasons: the `clm-v3port` matched-lossless-reciprocal-3-port bound (single-discipline), the Bogomol'nyi variational bound (single-name), FDT/Johnson–Nyquist (already a translation-table means-test row — lifting it would be the duplication failure mode), Rayleigh short-cut/cut (folded into TH-3), Hill's lemma (unmerged-only).
- **★§6 — the homonym watch-list, the sweep's own yield.** The theorem-level analog of the `def-` register's `open_ambiguity` axis: **5 Thomsons** (the variational principle / J.J. Thomson's corpuscle / the Thomson problem on `S²` / Kelvin = W. Thomson, the same man as #1 / Gibbs–Thomson, recorded at 0 corpus hits), **3 Hills** (the Voigt–Reuss–Hill **AVERAGE** `ν_Hill` — a heuristic mean, **not** a bound — vs the Hill 1963 **ordering theorem** vs **Hill's lemma**), **3 Dirichlets** (Dirichlet **energy** vs a Dirichlet **BC**, twice named as an *artifact* that bit two lanes, vs Dirichlet's **principle**), **4 Rayleighs**, **4 Borns**, **2 Fosters**. ★The live hazard named: `ν_Hill = 2/7` is **GR-imported** (`srs-band-structure.md`:116, PR #261) and is fed *into* TH-6; if "Hill" ever drifts from the average to the bound, an imported calibration input acquires the standing of a variational theorem. Nothing does this today — the row exists so nothing does it tomorrow.
- **★FLAG-DON'T-FIX (surfaced, NOT edited) — a precision slip at `mass-closure-theorem.md`:54.** The leaf's displayed equation (:52) is correct — `E_reactive = ½L_tube I_max² = ½C_tube V_peak²`, peak amplitudes — but the parenthetical gloss at :54 reads *"equipartition between magnetic and electric reactance, `½LI² = ½CV²` **at every instant**"*. In a resonant LC tank the two stores are in **phase quadrature**, so the instantaneous equality holds only at the **four** crossings per cycle (`sin²ωt = cos²ωt` at `ωt = π/4 + nπ/2`); the correct statements are the peak-amplitude equality (which the displayed line already writes) and the cycle-average equality. **Algebra right, gloss wrong.** Recorded in TH-7's regime fence with both line numbers; routed to the owning lane / Grant, not rewritten.
- **★Cross-lane hygiene observation — ~~PR #804's own docket fragment still carries the deleted *"the exact-mapping regime is the A1 bulk channel"* inference in its disanalogy-(ii) bullet~~ STRUCK.** *(struck 2026-07-28: the observation was pinned to #804 tip `4037e15e`, which is now an ancestor. Re-verified against the live ref before striking — at the current tip `a7785cab` the fragment's disanalogy-(ii) bullet reads *"The scalar network cannot carry the A1/T2 split, and that is exactly where the ordering failed"*, carries the inference struck through in the body, and its :16 tail note records the repair. Nothing left for that lane to fix.)* What still holds: **this thesaurus does NOT carry the deleted inference** — TH-1/2/3/5 cite #804's rows without it.
- **Conflict avoidance held.** `translation-circuit.md` **not touched** (owned by `kb/network-boundary-duality` / #804); the 2026-07-21 research docs + charter **not touched** (owned by `docs/leak-audit-carves` / #803); `research/2026-07-28_subc-kubc-bracket_*` and `research/2026-07-26_d1-sector-and-inertia-route_scoping.md` **not touched** (owned by #802 / #800) — all four cited **by path with an `★UNMERGED` flag and a PR number**, and §7 tripwire 2 requires re-pointing on merge or re-flagging on death.
- **Files:** NEW `manuscript/ave-kb/common/theorem-thesaurus.md` (no-claim leaf); `common/index.md` +1 row; `physics-lineage-map.md` +2 additive cross-ref pointers (a T8 read-as-a-theorem note + a §8 sibling-leaf line with its own update trigger) — **no T8 content changed**. **Mints ZERO ids** (no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; INVARIANT-S11 — the `clm-eemap1` precedent for means-tested consolidations); the `TH-n` labels are deliberately NOT spine-id-shaped and are materialised nowhere.
