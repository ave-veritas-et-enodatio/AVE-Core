### ENTRY 2026-08-01-d1-deprecate-ratio (2026-08-02): implementer — D1(ii) DEPRECATE `chord_magnitude_ratio()`, the superseded $7.5/\alpha^3$ birefringence ratio

- **Grant ruling D1 (2026-08-01), verbatim `[sic]`:** *"D1: (ii)"* — option **(ii) = DEPRECATE**, chosen over the two alternatives on the table (stamp-only; re-point to the corrected function). Discharges the ★FLAG-DON'T-FIX item routed by the 2026-07-31 verify-cite-corrections lane (`_orchestration/docket-entries/2026-07-31-verify-cite-corrections.md`:32), which surfaced the surface but explicitly declined the adjudication (*"the honest options (stamp-only vs deprecate vs re-point) are an adjudication this lane has no mandate to make"*).
- **The defect, restated from verified state (not from session memory).** `src/ave/qed/birefringence.py` `chord_magnitude_ratio()` returned the **v1 wrong-normalization** matched-differential ratio $7.5/\alpha^3 \approx 1.93\times10^7$ with a docstring presenting it as *the* magnitude ratio and **no supersession stamp**, while its bench-module sibling `src/ave/bench/birefringence.py` `coefficient_ratio_differential()` (**content-located at `:375-388`**, not the `:328` some cites carry — see the routed item below) has carried an explicit `==== SUPERSEDED (2026-07-03) … do NOT use its value in any prediction. ====` banner since 2026-07-03. Same quantity, two modules, one stamped and one not. The re-frozen **v3** (consistent instantaneous footing) value is $\delta n_{AVE}/\delta n_{QED} = 15\pi/(4\alpha^2) = 3.75\pi/\alpha^2 \approx 2.2\times10^5$, **boxed** at `manuscript/ave-kb/vol4/falsification/ch12-falsifiable-predictions/vacuum-birefringence-e4.md`:104; that leaf's `:106` records the $7.5/\alpha^3$ form as **v1 convention history**.
- **What (ii) means operationally — three changes, ZERO physics-value change.**
  1. **(a) Docstring banner** on `chord_magnitude_ratio()` mirroring the bench sibling's `==== SUPERSEDED … ====` shape, carrying the v3 value + its `vacuum-birefringence-e4.md`:104/:106 cite, the D1 ruling provenance verbatim, and the explicit *retained ONLY for provenance; do NOT use its value in any prediction*.
  2. **(b) `DeprecationWarning` on call** (`warnings.warn(..., DeprecationWarning, stacklevel=2)`) naming the superseded status, the v3 value + cite, and that the return value is preserved. ★**The return value is UNCHANGED** — the warning is the *only* behavior change. Deprecate-not-delete keeps the historical number reproducible (Rule-12-shaped: preserve the body, add the header).
  3. **(c) The pinning test re-scoped, not dropped.** `src/tests/test_grqed_stage2_qed_extension.py` still asserts $1.93\times10^7$, now wrapped in `pytest.warns(DeprecationWarning)` and carrying an in-test comment stating exactly what the assertion now is: *documentation of the preserved historical value of a deprecated function, NOT a defense of a live AVE claim*. A new `test_deprecation_warning_names_the_supersession` pins the warning itself. **The pin is kept deliberately** — a retained-for-provenance value that no test guards can drift silently.
- **★(d) LIVE-CALLER SWEEP — clean negative, THREE methods agreeing.** `git grep` over `src/` + `scripts/`; independent filesystem recursive `grep -rn`; and an **AST call-site scan** (`ast.walk` for `Call` nodes named `chord_magnitude_ratio` across every `.py` under `src/` and `scripts/`). All three return the **same 3 call sites, all inside the one test file** — `src/tests/test_grqed_stage2_qed_extension.py`:260/:270/:282 (post-edit line numbers). The only other references are the two export lines `src/ave/qed/__init__.py`:34,:52. **No runtime caller exists in `src/` or `scripts/`, so no caller was modified** and no caller follow-on question arises. (Three methods, not the grep-completeness-discipline minimum of two, because the brief's own line-number for the test call site named only one of the two pre-existing call sites — see the deviation below.)
- **★DEVIATION FROM THE BRIEF, surfaced not taken silently: the brief named ONE pinning call site; there are TWO.** The dispatch cited `test_grqed_stage2_qed_extension.py:250-253` (`test_chord_magnitude_equals_7p5_over_alpha_cubed`). Content-location found a **second** pre-existing call at the then-`:259` inside `test_magnitude_rides_alpha_inverse_cubed`. Both are wrapped in `pytest.warns`; wrapping only the briefed one would have leaked an uncaught `DeprecationWarning` into the suite. Recorded because the miss-shape (a brief's file:line under-counting call sites) is exactly what the AST cross-check is for.
- **★FLAG-DON'T-FIX #1 (NEW this lane, NOT edited) — the bench-side "corrected" replacement returns the KB-labeled v2, not the boxed v3.** `src/ave/bench/birefringence.py`:391-418 `coefficient_ratio_differential_pvlas()` is documented as the *"CORRECTED, arbiter-anchored"* replacement, and its default `geometry="propagating"` computes $[1/(2\alpha)]/[\alpha/(15\pi)] = 15\pi/(2\alpha^2) = 7.5\pi/\alpha^2$. **Numerically verified on this branch:** `pvlas(propagating) = 4.424666e+05`, `pvlas(static) = 8.849332e+05`, versus KB v3 $3.75\pi/\alpha^2 = 2.212333e+05$ — i.e. **exactly $2\times$ and $4\times$ the boxed v3**. But `vacuum-birefringence-e4.md`:106 names $7.5\pi/\alpha^2 \approx 4.42\times10^5$ as **v2**, *"QED-normalized but mixed-footing (instantaneous SVE over cycle-averaged $\alpha/15\pi$)"*, and states *"v3 is exactly half v2, the $\langle\cos^2\rangle=\tfrac12$ carrier average removed."* So the code surface advertised as corrected sits one re-freeze **behind** the corpus. **Not repaired here** — it is a different function, a different (live, not deprecated) value, and choosing between *re-freeze the code to v3* and *re-scope the KB's v3* is a physics-footing adjudication, not an implementer call. Consequence for this lane: the deprecation banner and warning cite the **KB leaf** as the v3 authority and deliberately do **not** name `coefficient_ratio_differential_pvlas()` as "the v3 replacement", which would have propagated the discrepancy.
- **★FLAG-DON'T-FIX #2 (NOT edited, per the brief's explicit `frozen=True` instruction) — `src/ave/bench/adopters.py`:185.** Inside a `frozen=True` `Prereg` block (`:179-…`), `prior_work_refs` carries `"birefringence.py:328 coefficient_ratio_differential"` — the function is content-located at `:375`. The same frozen block's `discriminating_outcomes` `:191` carries the **POSITIVE** bin *"ratio ~1.93e7, field-independent -> AVE-distinct tree-vs-loop"* and `:187-188` predicts *"AVE sits ~10^7x QED"* — i.e. a frozen pre-registration whose discriminating bin is stated in the value this ruling just deprecated. **Deliberately untouched** (frozen-provenance discipline; a frozen pre-reg records what was pre-registered, and silently re-freezing it would destroy the record). Remains **routed** to the orchestrator as the open question of how a frozen pre-reg bin is honestly annotated when its unit is superseded.
- **★FLAG-DON'T-FIX #3 (NOT edited — scope, surfaced for an orchestrator scope call) — two MODULE docstrings still state $7.5/\alpha^3$ as the live magnitude.** `src/ave/qed/__init__.py`:26 (*"the birefringence MAGNITUDE 7.5/α³ is an α-ECHO, not α-clean"*) and `src/ave/qed/birefringence.py`:32-35 (*"The **MAGNITUDE** δn_AVE/δn_QED = 7.5/α³ ≈ 1.93e7 is an **α-ECHO** at the value level"*). Both were flagged in the 2026-07-31 routing (`:32` of that fragment names the `__init__` module docstring `:26` explicitly). The D1(ii) execute-list enumerated the **function** docstring, the warning, the test, the caller sweep, and the adopters disclosure — **not** the module docstrings, so they are **left byte-untouched and disclosed** rather than folded in. ★**Consequence stated honestly:** `qed/birefringence.py` now contains a module docstring calling $7.5/\alpha^3$ the magnitude and, ~80 lines below, a function docstring stamping that same number SUPERSEDED. That in-file inconsistency is a **known, deliberate** artifact of holding scope, not an oversight; extending scope to the two module docstrings is a one-line-each orchestrator call.
- **Files:** `src/ave/qed/birefringence.py` (`import warnings`; function docstring + `warnings.warn`; **return expression byte-identical**), `src/tests/test_grqed_stage2_qed_extension.py` (class docstring re-scoped; +1 deprecation test; 2 call sites wrapped + commented), this docket fragment. **No other file touched.** `src/ave/bench/*` byte-untouched; `manuscript/` byte-untouched; **mints ZERO ids**; no claim-graph edge added or changed; no KB status flipped.
- **Battery:** `make verify` **exit 0**. Targeted pytest **63 passed** (`test_grqed_stage2_qed_extension` + `test_birefringence_v3_chain` + `test_grqed_stage1_gr_extension` + `test_grqed_stage3_backreaction`), with **zero uncaught** `chord_magnitude_ratio` warnings in the summary (all 3 call sites captured). `ruff check` on both touched files: **All checks passed** — and **net −1 violation vs baseline**: `origin/main` carried an unused-`pytest`-import `F401` in the test file, which using `pytest.warns` incidentally resolves. `ruff format --check` clean. `verify-docket-keys` no new duplicate keys. Numeric receipts banked live on-branch: `chord_magnitude_ratio() = 1.930035e+07` (1 `DeprecationWarning`), `coefficient_ratio_differential() = 1.930035e+07`, `pvlas(propagating) = 4.424666e+05`, `pvlas(static) = 8.849332e+05`, KB v3 $= 2.212333e+05$. Pure-corpus.
- **Class:** engine-code deprecation executing a Grant ruling; **consistency-vs-emergence tag = none newly asserted** — the ruling demotes a value's status, it derives nothing. No forward prediction changes: the **falsifier LOGIC is untouched** (the AVE-distinct content remains the **FORM** — a tree-level O(1) saturation vs an $\alpha^2$ loop; the magnitude was already an adjudicated **$\alpha$-echo** under v1 *and* v3, differing only in the power, $\alpha^{-3} \to \alpha^{-2}$).
