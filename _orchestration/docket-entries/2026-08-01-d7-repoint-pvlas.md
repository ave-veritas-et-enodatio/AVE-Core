### ENTRY 2026-08-01-d7-repoint-pvlas (2026-08-02): implementer — D7 re-point of the live PVLAS arbiter to v3, **GATE FIRED — NOT EXECUTED**, scoping-only

- **Grant ruling D7 (2026-08-01), verbatim `[sic]`:** *"D7: follow your rec"* — the rec being option **(i)** *re-point the live bench arbiter to v3*, **GATED on a consumer-and-letter sweep coming back clean.** The gate is the operative clause of the ruling. This lane ran the sweep; **the letter leg did not come back clean; the re-point was NOT executed.** Routing back is executing D7's own conditional, not declining it. Deliverable: [`research/2026-08-01_pvlas-arbiter-v3-repoint_scoping.md`](../../research/2026-08-01_pvlas-arbiter-v3-repoint_scoping.md).
- **Scope fence.** `src/` **byte-untouched** (no `.py` opened for write anywhere in the repo); `manuscript/` byte-untouched; `papers/` byte-untouched; **zero ids minted**, no solidity moved, no claim-graph edge, no frozen block edited, no KB status flipped. Two new files only: the scoping doc + this fragment. The dispatch's `src/ave/qed/*` fence (live lane on `src/deprecate-superseded-birefringence-ratio`) is honoured by construction.
- **GATE 1 — consumer sweep: CLEAN, three methods agreeing.** `git grep` (tracked) + independent filesystem `grep -rn` (sees untracked) + an **AST `Call`/`ImportFrom`/`Attribute` scan** over 1307 `.py` files. All three return the same set: **5 call sites in 3 driver files** — `birefringence_gap1_hibef_feasibility.py`:382,:385; `birefringence_hibef_scenario_predictions.py`:136,:140; `birefringence_prior_art_exposure_scan.py`:359 — plus the re-export at `src/ave/bench/__init__.py`:81,:159. **No call site inside `src/ave/`; none feeds a `P_flip`, a bin edge, or a verdict function.** Every one writes the value to JSON and prints it as context ⇒ **the re-point is behaviourally inert for every downstream prediction**, which is what the KB's own footing-invariance statement predicts. ⚠ A **zsh glob false-negative** was hit mid-sweep (`--include=*.py` expanded and aborted method 1's first run with `no matches found`) and is disclosed rather than silently re-run — a silent zero there reads as "no consumers".
- **★ GATE 1 sub-finding — D7's execute-step (c) has NO WORK IN IT, and acting on it as briefed would have destroyed a record.** The dispatch anticipated *"any test pinning 4.42e5 updated to the v3 value"*. **Zero tests pin the arbiter** (`git grep -c coefficient_ratio_differential_pvlas -- src/tests/` → no hits). The one `4.4247e5` pin, `src/tests/test_birefringence_v3_chain.py`:41, is on a **different** module — `scripts/vol_4_engineering/birefringence_coefficient_discriminator.py`'s `differential_ratio_v3_chain()`, which **already emits v3 as its headline** (`:39`,`:42`: `assert isclose(chain["v3"], 3.75*pi/ALPHA**2)`, `≈ 2.2123e5`; its docstring `:12` reads *"v3 = 3.75 pi/alpha^2 ~ 2.2e5 = 15 pi/(4 alpha^2) `<-- THE HEADLINE`"*). That assertion is a **chain-provenance** pin on the v1→v2→v3 arithmetic; re-pinning or deleting it would erase the record of the very $\langle\cos^2\rangle=\tfrac12$ carrier-average step D7 applies. **So a live, tested v3 implementation already exists in `src/`** — it simply is not the function the bench arbiter exposes.
- **★★ GATE 2 — THE LETTER CHECK: FIRES, and in the INVERSE direction the gate anticipated.** The stop condition is *"IF the letter rides v2 anywhere"*. It does — but **the letter's body and its authoritative public registration are already on v3, and it is the CODE that is one re-freeze behind.** `main.tex` carries v3 at **eight** sites (`:81-82`, `:275`, `:291`, `:750`, `:756`, `:794`, `:1083`, `:1136`); its only two v2 mentions (`:294`, `:759`) are **intentional convention history**, recorded as deliberately RETAINED at `provenance.md`:322-323. The **current** OTS chain is on v3 — `claim-prereg-ots/claims_by_hash.md`:12 (V4, `42c760c1`, which *"Supersedes the 2026-07-04 registration (f34e755998a9)"*), `:15` (V5), `:18` (V6, verbatim *"coefficient ratio 3.75\*pi/alpha^2 ~ 2.2e5 to the instantaneous Euler-Heisenberg coefficient"*). The v2-carrying `:9` (V1, `f34e7559`) is a **superseded frozen public artifact — never edit**. `make paper` is `latexmk` only (`Makefile`:269-272), so the **build consumes nothing**.
- **★ The trigger, both sides verbatim, neither reframed (flag-don't-fix).** `papers/2026_birefringence_letter/provenance.md`:121 is a **LIVE** row in §1 *"Number-by-number map (paper claim -> source)"*: *"`| Eq.(9), abstract, §III.B **(REV-2)** | Ratio `7.5pi/alpha^2 ~ 4.42e5` (propagating; `15pi/α²~8.85e5` static) | `ave.bench.coefficient_ratio_differential_pvlas(geometry="propagating")`; live `4.4247e5`. Was `7.5/α³~1.93e7`. |`"* — against `main.tex`:750 (equation body; `\label{eq:ratio}` at `:751`): *"`=\frac{15\pi}{4\alpha^2}=\frac{3.75\pi}{\alpha^2}\simeq\num{2.2e5}.`"* **Eq.(9) is v3; the traceability row says v2.** Both files sit at `19285c5d` and were last touched by the **same** commit `5109c961`. Cause located: the §9 Arm-2 re-freeze sweep is self-recorded at `provenance.md`:319 as covering *"abstract, §II.B honesty-item (iv), Eq.9 + surrounding text, §III conclusion, Table I caption"* — **`main.tex` sites only**; the §1 map is a `provenance.md` site and stayed at REV-2. `:168` (§4 discipline tags, *"headline the `4.42e5` magnitude (REV-2; was `1.93e7`)"*) is stale the same way. **A pre-existing corpus defect, surfaced not created, nothing edited.** Why it stops the lane: the re-point makes `:121`'s cited call return `2.212333e+05` — **repairing** the paper↔code link at Eq.(9) while **falsifying the row's own prose as written** — so D7 is not completable without editing a submission-gated artifact's provenance ledger, which is out of this lane's dispatch fence, is the Letter revision protocol's judgement (`provenance.md`:307, *"this v2 does NOT re-stamp"*), and is Grant's call.
- **★ BLOCKER-1 (the physics-shaped one) — "re-point to v3" is NOT a scalar swap; the arbiter has no v3 branch and adding one is a KEEP-BOTH design decision.** `src/ave/bench/birefringence.py`:411-418 is **denominator-parameterised, not footing-parameterised**: `geometry="propagating"` → $\alpha/(15\pi)$, `"static"` → $\alpha/(30\pi)$. **v3 needs $2\alpha/(15\pi)$ — the instantaneous coefficient — and NEITHER branch is it.** Neither is *wrong*: per `vacuum-birefringence-e4.md`:38-41 the chain is $\alpha/(30\pi) \xrightarrow{\times4} 2\alpha/(15\pi) \xrightarrow{\times\frac12} \alpha/(15\pi)$ (static duality → head-on crossing geometry → carrier average); all three are legitimate QED coefficients in their own footing, and what v2 got wrong was the **pairing**, not either coefficient. The parameter is doing two jobs: `geometry` names a **geometry**, while v2→v3 is a **temporal-footing** change orthogonal to it. Three admissible shapes: **(A)** redefine `"propagating"` in place (smallest diff; makes the keyword lie — the literature propagating one-loop headline **is** $\alpha/(15\pi)$ — and silently re-means all five call sites; **fails KEEP-BOTH**); **(B)** add an `"instantaneous"` keyword returning $15\pi/(4\alpha^2)$, make it the default, leave both legacy keywords returning exactly what they return today with a mixed-footing docstring note; **(C)** split into `geometry ∈ {static, propagating}` × `footing ∈ {instantaneous, cycle_averaged}`, which makes the v2 mixed state unrepresentable but changes the signature. **Recommendation stated as a recommendation, not a decision: (B).** ★Note that **(A) is the reading a fast pass at the word "re-point" most naturally supports, and it is the one shape that fails the corpus's own pattern** — which is precisely why the shape is routed rather than picked.
- **BLOCKER-2 — three JSON keys NAME the value they carry.** All three drivers write the return under the literal key `matched_differential_ratio_7.5pi_over_alpha2_propagating` (`:383`/`:138`/`:361`), plus `matched_differential_ratio_15pi_over_alpha2_static` (`:384`/`:139`). Re-point **without** a key rename yields a self-contradicting record (key says `7.5pi`, value is `3.75pi`); **with** a rename it changes the **output schema** of three drivers whose committed JSON the Letter cites as Table I's source (`provenance.md`:102-106). Committed values that would move: `442466.5835078048` in each of `_output/birefringence_{gap1_hibef_feasibility,hibef_scenario_predictions,prior_art_exposure_scan}.json`. Neither move is on D7's execute list and both touch letter-cited artifacts ⇒ folds into the same Gate-2 routing.
- **BLOCKER-3 — the KB register still teaches v2 as "the correction".** `manuscript/ave-kb/vol4/claim-quality.md`:455-466 carries a 🔴 header reading *"**Corrected matched-differential ratio: $\mathbf{7.5\pi/\alpha^2\approx4.42\times10^5}$** (propagating/LoI-matched headline)"* and names this exact function as the harness at `:464`, while the leaf D7 treats as truth-source is Option-B-consolidated to v3 (`vacuum-birefringence-e4.md`:51). **Not edited — KB is the auditor lane's.** Routed so the re-point is not landed underneath a register that still says v2 is the fix.
- **★ MID-LANE SUPPLEMENT (`adopters.py`) re-derived at head — two claims reproduce, the reachability claim DOES NOT.** The **verdict flip is REAL and reproduced**: `ratio_at(1e13)=ratio_at(1e14)=ratio_at(3e16)=1.930035e+07 → verdict_fn True`; KB v3 `2.212333e+05 → False`; and `v1/v3 = 87.2398 == 2/(pi*ALPHA) = 87.2398`, independently reproducing the KB's own stated understatement factor at `vacuum-birefringence-e4.md`:102 to 6 s.f. **But it is unreachable from D7's payload.** `adopters.py`:40-44 imports exactly `delta_n_ave_differential`, `delta_n_qed`, `vacuum_magnetic_birefringence_constant` — **neither ratio function** (the `coefficient_ratio_differential()` text at `:203`/`:221` is a provenance *string*, not a call); `ratio_at` (`:103-105`) recomputes the v1 pairing inline with `_A_EH_DIFFERENTIAL = 3.0/45.0` (`:72`). Empirical disproof, not an argument — monkeypatching the arbiter to v3 and re-running the full bench model: *"BASELINE verdict=BANKABLE_AS_DISCRIMINATOR | ratio_at(1e13)=1.930035e+07 … PVLAS->v3 verdict=BANKABLE_AS_DISCRIMINATOR | ratio_at(1e13)=1.930035e+07 … VERDICT MOVED? False"*. The banked verdict exists and is pinned (`src/tests/test_bench_adopters.py`:33) and is **invariant under the re-point** ⇒ **`adopters.py` is NOT a second STOP condition for D7**; the flip belongs to a different, un-fired change (re-pointing `ratio_at`'s QED leg). **And the "7 non-frozen sites are in-scope consumers for step (d)/(e)" instruction is pushed back with evidence, not silently absorbed:** those sites carry the **v1** magnitude $7.5/\alpha^3$, the return of the *already-deprecated* `coefficient_ratio_differential()` — one re-freeze further back. Editing them under D7 would re-freeze v1→v3 in a file D7 never names, **skipping the v2 rung**, on the exact surface the D1 lane deliberately routed rather than fixed. A genuine open surface; a **separate ruling's** surface.
- **★ Two divergences from the supplement's inventory, surfaced not absorbed.** (1) **The frozen count is 3, not 2.** Content-locating the block: `Prereg(` opens at `:179`, `frozen=True` at `:181`, closes `:201` — so **`:180`** (`ref="…(+ 7.5-trace a94672de)"`) is inside it by construction, alongside `:185` and `:191`. It must be disclose-only, not live-updatable. (2) **There is a THIRD stale cite** — `birefringence.py:320` at `adopters.py`:14, pointing at `coefficient_ratio` (content-located at `:356`) — beyond the two `:328` instances (`:185` frozen, `:221` not) the supplement named. Row totals differ only by pattern set and are not in conflict; the **frozen/non-frozen boundary** is the part that matters and it moves by one.
- **Numeric receipts, banked live on-branch.** `ALPHA = 0.0072973525693`; `(E_CRIT/E_YIELD)^2 = 137.035999083696 == 1/ALPHA` (identity holds); `7.5*pi/ALPHA**2 = 4.4246658351e+05` (v2); `15*pi/(4*ALPHA**2) = 3.75*pi/ALPHA**2 = (0.5*(E_CRIT/E_YIELD)**2)/(2*ALPHA/(15*pi)) = 2.2123329175e+05` (v3, **three independent routes agreeing to 1e-12**); `LIVE pvlas(propagating) = 4.4246658351e+05`, `LIVE pvlas(static) = 8.8493316702e+05`. **v2/v3 = 2.000000 exactly** — the KB's `:106` *"v3 is exactly half v2"* reproduced. So the arithmetic target of a re-point is unambiguous (`2.212333e+05`, derivable from `ave.core.constants` with no magic number); it is the **shape**, not the number, that is open.
- **Three dispatch cites verified-and-corrected (verify-before-cite).** **C1** the KB leaf is at `manuscript/ave-kb/vol4/**falsification/ch12-falsifiable-predictions**/`, not `vol4/circuit-theory/ch1-vacuum-circuit-analysis/` (that directory holds `node-up-small-large-signal.md`, which the leaf cites at `:85` — the likely transposition). **C2** the **`\boxed{}`** v3 is at `:34` (inside the 🔵 FOOTING RE-FREEZE note), not `:104`; `:104` carries the same equation unboxed as the Option-B body consolidation — content-correct, two sites agreeing. **C3** `origin/main` is **`19285c5d`**, not `e6de554b` (2 commits back). `:106` *"v3 is exactly half v2"* and the live `4.424666e+05` both **VERIFIED verbatim**. ⚠ **Numbering collision worth carrying:** the KB's value-level labels (`:61`: v1 $=7.5/\alpha^3$, v2 $=7.5\pi/\alpha^2$, v3 $=3.75\pi/\alpha^2$) collide with the Letter provenance's **document**-version labels, where Letter-"v1" carries value-v2 and Letter-"v2" carries value-v3. Two axes, opposite offsets.
- **Zero overlap, file sets diffed not assumed.** `src/deprecate-superseded-birefringence-ratio` = {its docket fragment, `src/ave/qed/__init__.py`, `src/ave/qed/birefringence.py`, `src/tests/test_grqed_stage2_qed_extension.py`} — **none**. `docs/factor7-and-782-basis-notes` = {its docket fragment, `domain-catalog.md`, `stellar-regime-classification.md`, 3 `.tex`, `research/2026-07-21_rve-aggregation-bench_result.md`} — **none**. `docs/rulings-d2-d3-d4` has **no `origin` ref** (unpushed or differently named) — cannot collide with an unpushed ref; re-check at merge time. This lane's set is **2 files**.
- **Battery.** `make verify` **exit 0** (*"[Verify] ALL PHYSICS PROTOCOLS PASSED."*). `verify-docket-keys` (both standalone and inside `make verify`): **85 entries / 83 unique keys / no new duplicate keys**; this fragment's `### ENTRY 2026-08-01-d7-repoint-pvlas` header parses. Targeted pytest, `test_ave_bench` + `test_bench_adopters` + `test_bench_model` + `test_birefringence_v3_chain`: **99 passed in 0.71s** — a *control*, not a validation of an edit, since no `.py` was modified. Pure-corpus grep over both new files: **zero hits**. ★**SELF-CORRECTION (verify-before-cite, on this lane's own output).** The first draft of this line asserted *"`ruff check src/ave/bench src/scripts/vol_9_device`: **All checks passed**"*. **That claim was written before the command was run, and it is FALSE.** Actually run: **`Found 86 errors.`** — and **verified PRE-EXISTING, not introduced**: the identical `Found 86 errors.` comes from the **untouched main checkout at the same SHA `19285c5d`**, and this branch's `git diff --stat origin/main...HEAD -- src/` is **empty**, so no `.py` byte changed. Two further honest corrections that the same check surfaced: **ruff is NOT part of `make verify`** — the Makefile has **no `lint:` target** and never invokes ruff, so "ruff clean" was never a gate this repo enforces; and the config is `select = ["E","F","W","I"]` at `line-length = 120` (`pyproject.toml`:37-42), under which `src/scripts/vol_9_device` carries a standing import-order/style backlog. **Deliberately NOT fixed** — pre-existing, unrelated to D7, and reformatting untouched driver bodies would be unrequested scope. The earlier "All checks passed" wording is **retracted**.
- **Class:** **scoping + gate verdict, executing a Grant ruling's own conditional.** Canonizes nothing, derives nothing, adjudicates no physics. **consistency-vs-emergence: nothing newly asserted** — every number printed is a re-computation of an already-adjudicated corpus value, banked as a receipt; `clm-pp3qwf` is untouched and remains CONSISTENCY-class with the magnitude an $\alpha$-echo and the FORM the chord. **Rule 11:** no criterion dropped to convert the gate's ❌ to ✅ — the gate fired, the ruling's conditional withheld execution, and the branch is reported as **gated, not done**.
- **Routing list for Grant / the orchestrator (nothing below is an implementer call).** (1) pick a BLOCKER-1 shape, (A)/(B)/(C); (2) rule on whether `papers/2026_birefringence_letter/provenance.md`:121,:168 may be updated, and by which lane; (3) rule on the BLOCKER-2 JSON key rename + driver-output regeneration (the same letter-artifact question in a different file); (4) route `claim-quality.md`:455-466 to the auditor lane; (5) **only then** the `src/ave/bench/birefringence.py` edit — ~8 lines, and by a wide margin the easiest part of D7. That asymmetry, a trivial code change fenced behind four adjudications, is the honest summary of why this lane stopped.
- **Lane mechanics.** Self-isolated throwaway worktree off `origin/main` @ `19285c5d`; branch `src/repoint-pvlas-v3` — **name retained as dispatched** despite carrying no `src/` change, so the orchestrator's tracking key does not move (disclosed rather than unilaterally renamed). Skeleton-first, one section per commit. **Every `file:line` re-read at ship time; six of this lane's own draft cites were wrong and were corrected before commit** (the three dispatch cites; `claims_by_hash.md` V5/V6 `:14`/`:16` → `:15`/`:18`; the KB decomposition-chain `:46-50` → `:38-41`; the provenance driver-list `:98-101` → `:102-106`), plus two more from re-deriving the supplement (the `adopters.py` import list and the frozen-block boundary). PR opened `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.
