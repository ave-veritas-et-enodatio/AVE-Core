### ENTRY 2026-07-28-network-duality-row (2026-07-28): implementer — NETWORK-ANALYSIS tier of the EE mapping (KUBC/SUBC ↔ resistor-network boundary duality, means-tested)

- **Grant's authorization (verbatim `[sic]`: *"and lets follow up on the constructive note"*)** — land ONE new EE-mapping row via the `ave-ee-first-mapping` Step-6 establishment procedure: the homogenization boundary-condition duality ↔ the resistor-network boundary duality. The specific row formulations are orchestrator / lane walk-level (the 2026-07-28 SUBC/KUBC bracket lane, OWED-1).
- **Sweep first (Step 6 discipline).** Two-method sweep BEFORE minting found the corpus already carries most of the algebra, and the row is landed as a CONNECTOR, not a re-mint: (i) **Kron T8** (`physics-lineage-map.md`:359) already states exact network representability of an *elastic solid* + Schur complements — **this row is an INSTANCE of Kron's theorem, not a new one**; (ii) `the-sourced-charge-no-go-cascade.md`:94 already runs the scalar weighted-graph-Laplacian Dirichlet energy `φᵀ L_w φ = Σ_edge ε_eff·(Δφ)²` with its 1-D constant nullspace, at THEOREM grade; (iii) `relative-offset-principle.md`:57 already states the `#782` K-side is a KUBC upper bound; (iv) §4.5(b) `.OP/.AC` is the sibling method-row class.
- **Minted (all under existing `clm-eemap1`, NO new claim-id — the §4.6.2 precedent): new `translation-circuit.md` §4.7 "Network-analysis tier" (rows D/E/F/G + an object dictionary) + §6 means-test corpus #32 + one §4.5(b) tool row (tally 31→32 rows, 10→11 ⚠).** Rows: (D) **KUBC ↔ voltage-clamped boundary** — drive the Op5 multiport ports with voltage sources, read `[Y]`; clamping shrinks the kinematically-admissible set ⇒ over-stiffens; (E) **SUBC ↔ current-fed boundary** — drive with current sources, read `[Z]=[Y]⁺`; prescribing the injection pattern shrinks the statically-admissible set ⇒ over-softens; (F) **Hill/Huet `C^SUBC ≤ C* ≤ C^KUBC` ↔ the Dirichlet/Thomson two-sided bound on effective conductance** (Rayleigh short-cut/cut = the special cases); (G) **PBC ↔ the periodic Born–von Kármán network cell** — STRUCTURAL only, **NOT RUN** in this lane and **NOT a bound** (frozen prereg §1.3 verbatim: `A periodic BC would give a third, intermediate estimate — informative but NOT a bound, and therefore not what OWED-1 asks for.`).
- **MEANS-TEST VERDICT = EXACT (rows D/E/F), CONSISTENCY-CLASS, NO CHORD.** Same theorem, not a vibe: Dirichlet's principle IS minimum potential energy, Thomson's IS minimum complementary energy, both being the Dirichlet-vs-Neumann partition of ONE PSD quadratic form on a graph — substrate = block (`Φ_b = k_a d̂⊗d̂ + k_s(I−d̂⊗d̂)`) Laplacian, network = scalar (`g_b`) Laplacian, same algebra with `g_b → Φ_b`. Frozen prereg §1.3 states the shared mechanism itself, verbatim: `Both principles are exact statements about the DISCRETE positive-semidefinite quadratic form U(u) = ½ uᵀKu under a Dirichlet vs a Neumann partition; nothing in the argument needs a continuum limit.` Every load-bearing object maps (stiffness ↔ boundary Schur complement = Op5 `[Y]`; strain energy ↔ stored/dissipated power; Hill's lemma ↔ the current-dipole identity; **self-equilibration ↔ KCL as the Fredholm solvability condition for a singular Laplacian**; translation projection ↔ floating-ground gauge fix; uncaged reference ↔ the uniform-conductance network; uncaged gap `g_0` ↔ contact/spreading resistance). **But the exactness is KRON's, so it inherits that lineage entry's own verdict verbatim — *"an exact isomorphism is ontologically silent"* — hence consistency-class, peer-with-standard-homogenization, no chord.**
- **★THREE DISANALOGIES, each of which BIT the bench (KEEP-BOTH disanalogy convention, per the §4.6.2 Row-B MOS precedent).** (i) **The scalar network has no rotational sector.** Scalar Neumann nullspace = 1 (the constant); vector = generically 6, but on the shipped Born bond model rotations COST energy so it is the 3 translations (`E(rigid rotation, 1e-3) = 1.984e-3 > 0`). A network reading would have said "project out the constant and you're done" and would have missed the absolute-frame rotational stiffness entirely — it cannot even pose the question. (ii) **The scalar network cannot carry the A1/T2 split, and that is exactly where the ordering failed.** No 4th-rank `K`/`C′`/`C44` decomposition, no hydrostatic-vs-deviatoric channel, no "which longitudinal modulus" question — while the medium is CUBIC (Zener `A = 1.330402` SUBC / `1.605316` KUBC, which is why `M = K + 4G/3` was WITHDRAWN there). Empirically: frozen G1 ratio-ordering violations = **`8` of `42` ratio-bearing rows, ALL T2 shear, `0` of `21` A1 hydro** ⇒ **~~the exact-mapping regime is the A1 bulk channel~~ *(inference DELETED per the 2026-07-28 audit — see the repair note below)*; the scalar analogy runs out at T2.** (iii) **Dissipated-vs-stored forces a regime fence.** Ax3-lossless makes the form STORED reactive energy, which is fine for one-sign susceptances but NOT for a general AC mixed-reactance network (`B(ω)` real-symmetric but not sign-definite — Foster territory; both bounds FAIL). **Regime tag mandatory at every cite: `static / DC / single-sign-susceptance` ONLY.**
- **Carried exposure (evidence the mapping is tight, not a disanalogy):** the bounds are on the ABSOLUTE modulus/conductance; a RATIO of two boundary-conditioned readings is NOT bound-ordered (`R_KUBC/R_SUBC = g_0^arm/g_0^uncaged`; shear `g_0 = 2.1600` vs bulk `1.2419`), while `absolute_theorem_grade_ordering_holds_everywhere = True` across all `48` walked rows. The network reading inherits this verbatim.
- **★REFUSED — the bracket-width corollary (flag-don't-fix, NOT landed).** The walk-level proposal "the bracket is WIDE (`0.15081` on `K_eff/K_0`) ⇒ the composite is far from self-averaging ⇒ boundary-layer-dominated, the network analog of contact-resistance dominance" **converts width into a claim about the MEDIUM**, which the lane's own frozen anti-seduction fence forbids, verbatim: `a wide bracket is a statement about the INSTRUMENT, not about the medium: the result doc may NOT convert bracket width into physical significance in either direction, and must report the uncaged gap g_0 alongside every width so the reader can see how much of it is finite-size boundary layer`. **Landed instead: only the INSTRUMENT half** the fence itself asks for — the `g_0` ↔ contact/spreading-resistance dictionary row, measured on the *uncaged* reference (no cages ⇒ no medium claim). Routed to Grant / the auditor lane if the width reading is ever wanted; it needs its own bench, not a table row.
- **Validating-instance status stated on the row, not glossed:** `research/2026-07-28_subc-kubc-bracket_{prereg-FROZEN,result}.md` are on **PR #802 (OPEN, not on `main`)** — cited by path — and that lane reports **BENCH STATUS = VOID AS FROZEN** on two criterion-side counts with its bracket **computed-not-banked**. The row therefore rests on the INSTRUMENT validation that did pass (bit-exact #782/#796 reproduction `0.2963682232` / `0.2982369863` at rel `0.00e+00`; G3 determinism byte-identical; G4 residual `9.99e-10`; G5 work identity `8.63e-16`), and on the fact that **the §4.7.3 exactness is algebraic (Kron), not empirical — it would stand if the bench had never run.**
- **Validation mark = ⚠, not ✓, with both reasons stated in the row:** (i) no consolidating canonical leaf derives the correspondence end-to-end; (ii) the only running instance is VOID-as-frozen / computed-not-banked. The Schur-complement→Op5-`[Y]` reduction is exact algebra, but per the §4.5(a) legend ✓ is reserved for a tool→operator identity or an end-to-end canonical derivation.
- **Mirror-check (skill Step 6 item 3): SKIPPED, both are stubs.** `vol2/appendices/app-a-translation-matrix/translation-circuit.md` (16 lines, `no-claim: routing forwarder`) and `vol4/circuit-theory/ch1-vacuum-circuit-analysis/translation-circuit.md` (12 lines, pointer) carry NO table — the skill says skip stubs/pointers. Verified by reading both.
- **Operator-citation provenance (`verify-before-cite`):** Op5 `[S] = (I + [Y]/Y_0)⁻¹·(I − [Y]/Y_0)` at `operators.md`:45 and Op1 `Z = √(μ/ε)` at `operators.md`:41 grep-verified at authoring time. Every verbatim quotation in §4.7 / §6 #32 was byte-matched against its source before landing.
- **Owed:** the `ave-ee-first-mapping` SKILL body (Step-2 table + means-test corpus) mirror was updated **locally only**; the **AVE-Skills repo commit/push of that mirror is OWED and Grant-hard-gated** (skills hard-gate) — exactly as the #792 lane recorded.

- *(2026-07-28 audit repair: the "exact-mapping regime = A1 bulk channel" inference DELETED at both sites — a non-sequitur converting a ratio-construction/free-surface instrument artifact into a sector-scope claim, the same move §4.7.4 refuses; absolutes hold 48/48 both sectors; sector-scoping routed to Grant. Rows D/E/F verdict cells now carry the Kron-class/consistency-class/no-chord ceiling + the static/DC/single-sign regime fence in-cell per the §4.6.2 precedent. M1 functional-vs-power label, M3 anchor range §0–§5.6, M4 design-time-pilot qualifier fixed. Grant's authorization quote paste-confirmed by the orchestrator: [sic] "and lets follow up on the constructive note".)*
