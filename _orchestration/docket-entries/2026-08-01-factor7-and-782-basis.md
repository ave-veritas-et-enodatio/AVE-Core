### ENTRY 2026-08-01-factor7-and-782-basis (2026-08-01): implementer — the domain-catalog solar factor-7 residual, and the routed #782 basis-correction notes

- **Class: mechanical propagation of already-established, audit-confirmed state. NOT adjudication.** Neither item re-opens a physics question, moves a grade, mints a `clm-`/`def-`, or re-bins anything. Both propagate corrections that a merged lane already established and ROUTED. Bodies preserved per Rule 12 — git is the trail. Everything that would have required a content decision is FLAGGED below, not fixed.

#### ITEM 1 — the `domain-catalog.md` solar-surface row was factor-7 low against its own definition

- **The defect.** [`domain-catalog.md`](../../manuscript/ave-kb/vol1/operators-and-regimes/ch7-regime-map/domain-catalog.md) `:45` defines Amplitude $A$ = principal radial strain $\varepsilon_{11} = 7GM/(c^2 r)$. Its solar-surface row read `2.1e-6`, which is exactly $GM_\odot/(c^2R_\odot) = 2.123\times10^{-6}$ — the **un-multiplied Newtonian potential**, a factor of 7 low against that same leaf's definition, while the neighbouring neutron-star and black-hole rows carried the 7. Flagged by two independent audits: the 2026-07-31 anisotropy-scoping lane ([`2026-07-31-anisotropy-scoping.md`](2026-07-31-anisotropy-scoping.md) `:11`, the `★ THE NON-CONFUSION STATEMENT` bullet, and `research/2026-07-31_anisotropy-observable_scoping.md:799-810`, both explicitly FLAG-DON'T-FIX) and #813's repair pass.
- **Recomputed, every catalogued body**, on canonical constants (`G`, `c`, `M_SUN` from `src/ave/core/constants.py`; $R_\odot = 6.957\times10^{8}$ m, IAU nominal):

| body | $GM/c^2R$ (no 7) | $7GM/c^2R$ (the definition) | printed | verdict |
|---|---|---|---|---|
| solar surface | `2.1231e-6` | `1.4862e-5` | `2.1e-6` | **factor-7 LOW** |
| red giant $5M_\odot$ / $100R_\odot$ | `1.0616e-7` | `7.4310e-7` | `7e-7` | carries the 7 |
| white dwarf (Sirius B) | `2.5314e-4` | `1.7720e-3` | `3e-4` | **routed, see below** |
| neutron star $1.4M_\odot$ / 10 km | `2.0679e-1` | `1.4475e0` | `1.46` | carries the 7 |
| black hole at $r_s$ | `5.0000e-1` | `3.5000e0` | `3.5` | carries the 7 |

- **Repaired to `1.486e-5`** — the value **already established in the merged corpus** at `2026-07-31-anisotropy-scoping.md` (`A=1.486\times10^{-5}` at the solar limb) and `research/2026-07-31_anisotropy-observable_scoping.md:803`. Not a new numeral.
- **Sites repaired** (primary + the 4 live consumers found by a two-method corpus sweep — `git grep` numeric-variant regex, and an independent python walk over all 4875 tracked files with a solar-context filter; both methods returned the same manuscript hit set):
  - `manuscript/ave-kb/vol1/operators-and-regimes/ch7-regime-map/domain-catalog.md:50` (+ dated note)
  - `manuscript/ave-kb/vol3/applied-physics/ch07-stellar-interiors/stellar-regime-classification.md:16` (+ dated note)
  - `manuscript/vol_1_foundations/chapters/07_regime_map.tex:160`
  - `manuscript/vol_3_macroscopic/chapters/07_stellar_interiors.tex:29`
  - `manuscript/vol_9_vacuum_datasheet/chapters/14_phase_diagrams.tex:183`

  Rule-12 dated notes land in the two KB leaves (KB is truth-source); the LaTeX mirrors take the value only.
- **Provenance — this is a residual of the 2026-05-17 factor-7 cleanup**, logged at [`claim-quality-closure-roadmap.md`](../../manuscript/ave-kb/claim-quality-closure-roadmap.md) `:82` (row `C11-MACH-ZEHNDER`, AVE-Core `d48f75d`). Verified two ways that the catalog was outside that pass: the roadmap row does not name `domain-catalog.md`, and `git show --stat d48f75d` lists 9 files, not including it. `d48f75d` is an ancestor of this branch (`git branch --contains`).
- **Dated sites DISCLOSED, not edited** (each is a correct-as-of-its-date record that explicitly declines to edit corpus): `_orchestration/docket-entries/2026-07-31-anisotropy-scoping.md`, `research/2026-07-31_anisotropy-observable_scoping.md:799-810`. Their verbatim quote of the old `:50` line is now superseded by this correction. Not a consumer at all: `research/2026-05-17_hulse-taylor-substrate-native-prereg.md:23` computes $\phi/c^2$ and then multiplies by 7 explicitly — correct as written, and a frozen prereg regardless.

##### ★ROUTED, deliberately NOT fixed (each needs a content decision, not a mechanical value fix)

1. **The white-dwarf row.** `domain-catalog.md:51` and the vol1/vol9 mirrors state **no $M$ or $R$**, so the row is not decidable from the file alone. Its vol3 companion table names the body as **Sirius B**, and on Sirius B parameters `3e-4` matches $GM/(c^2R) = 2.53\times10^{-4}$ — the **measured gravitational redshift**, which is what that table's own *Physical consequence* column cites (`Gravitational redshift ~10^-4`) — rather than $7GM/(c^2R) = 1.77\times10^{-3}$. i.e. it appears to carry the same factor-7 defect, but confirming that requires adopting WD parameters the vol1 rows never state. **Adjudication routed.**
2. **`domain-catalog.md:55`, the Buchdahl parenthetical — a SEPARATE arithmetic discrepancy, not a missing 7.** It reads: *"no static configuration can have $r < 9GM/4c^2$ (where $\varepsilon_{11} > 63/36 = 1.75$)"*. But $7GM/(c^2 \cdot 9GM/4c^2) = 28/9 = 3.111$, not `1.75` (`63/36` = `7/4`). Its own LaTeX source [`07_regime_map.tex`](../../manuscript/vol_1_foundations/chapters/07_regime_map.tex) `:167` **already carries the corrected arithmetic** (*"an object at the GR Buchdahl radius already sits at $\varepsilon_{11} = 7\cdot(4/9) = 28/9 \approx 3.11$"*) **and a materially different compactness statement** (the AVE limit is $R < 7GM/c^2$, stated as MORE restrictive than Buchdahl — the KB says the two coincide). So the KB leaf carries a **stale pre-correction paragraph**, and repairing it is a prose reconciliation against the LaTeX, not a numeral swap. **Routed to the auditor lane.**
3. **`src/ave/core/regime_map.py:523` — the whole gravity block is convention-divergent.** It ships `("Solar surface", 2.12e-6, 1.0, "strain"),` alongside `("Neutron star", 0.3, 1.0, "strain"),` and `("BH at r_s", 1.0, 1.0, "strain"),`, which match **neither** convention (canonical with-7 values are `1.4475` and `3.5`; without-7 they are `0.2068` and `0.5`). Correcting solar alone would leave the block more incoherent, not less.
4. **`src/scripts/vol_3_macroscopic/visualize_regime_map.py:59`** ships solar `2.1e-6` while its own `NS 1.46` / `BH 3.5` / `magnetar 5.29` entries carry the 7 — so this one IS a clean single-value consumer, but it is a figure driver: the edit implies figure regeneration, which is outside a docs lane.

#### ITEM 2 — the routed #782 basis-correction notes (dated notes appended; physics NOT rewritten)

- **What was routed.** Merged **#802** ([`research/2026-07-28_subc-kubc-bracket_result.md`](../../research/2026-07-28_subc-kubc-bracket_result.md) §8 consequence 7; docket [`2026-07-28-subc-kubc-bracket.md`](2026-07-28-subc-kubc-bracket.md) findings F5/F8) established, and explicitly declined to land, a basis correction to merged **#782** ([`research/2026-07-21_rve-aggregation-bench_result.md`](../../research/2026-07-21_rve-aggregation-bench_result.md)) under its own lane fence (*no edit to the merged #782 or #796 result docs regardless of outcome*). This entry lands it as **dated correction notes at the two consuming sites**, per Rule 12; #782's physics body is untouched.
- **Site (a) — `:124` (§7.1).** *"only `compressed r_Z = 0.466 < 0.5` is bound-robust."* That `0.466` is `√(R_KUBC_core)` — the **CORE estimator**, which carries **NO bound status**. #802 reproduces #782's `K_eff/K_0 = 0.2176075097402979` bit-for-bit and reads the same configuration on the bound-carrying **whole-cell** apparent modulus at **`r_Z = 0.6098`** — **above** `0.5`. Across #782's four `φ_sf` legs, **`0`** read macro-side on the whole-cell measure (`2` do on the core estimator), the core estimator sitting below the whole-cell value at every leg.
- **Site (b) — `:12` (trigger (iii)).** **BOTH** clauses evaporate on the bound-carrying measure. Second leg: the crash-min `r_Z≈0.40` is `0.4048630894644663` (`bulk_only_cold` route B, `r_cage = 1.7`, `s = 4.2`) — **again the core estimator**; #802 runs that exact configuration, reproduces it bit-for-bit, and reads **`0.7106`** whole-cell. Across #802's entire **21**-configuration hydrostatic set the **minimum** whole-cell KUBC `r_Z` is **`0.5850`** (`symmetric_cold_phi_sf`), with **`0`** configurations anywhere below `0.5`.
- **What survives, and in what form.** The `r_Z` straddle survives — as a **BOUNDARY-CONDITION ARTEFACT**, restored at **`2` of the `4`** legs (`bulk_only_compressed`, `symmetric_cold`) by #802's **SUBC LOWER** bound falling below `0.5`, i.e. by free-surface softening of the traction boundary condition, **not** by any class reading macro-side on an upper bound. Honest restatement: *"undetermined because the two admissible boundary conditions disagree"*, not *"undetermined because one pre-stress class flips to the macro side."*
- **★BIN-4's LABEL STANDS — stated explicitly at both sites.** Removing a bound-robust macro-side leg makes the `r_Z` axis **MORE** undetermined, not less; BIN-4 IS the undetermined bin. Separately, the `f_incl` BIN-2-vs-BIN-4 split #782 calls *"a verdict-controlling cross-class flip"* at `:114` is a **different object** — collapse-gate-driven, not `r_Z`-driven (`rve_aggregation_bench.py:831` `_bin_of` returns BIN-4 whenever the collapse gate fails, before `r_Z` is read) — and it persists unchanged. **Only the BASIS is corrected.**
- **This is the SECOND routed basis correction to #782**; the first was the ρ-flags audit ([`2026-07-21-relative-offset-principle.md`](2026-07-21-relative-offset-principle.md) `:6`, the Fork-ρ direction ruling's consequence flag). Both notes say so with the pointer.
- **Every quoted number verified two-method** against merged sources: the #802 result-doc prose AND the shipped JSON block `F5_782_bound_robustness_crosscheck_NOT_FROZEN` in `research/drivers/subc_kubc_bracket_results.json` (which reads #782's own artefact at run time rather than retyping it). No number in either note is retyped from a summary.

##### ★FLAGGED — a mis-attribution in the routing brief, corrected in the notes

The routing brief attributed the *"NO bound status"* criterion to **#782's** own frozen prereg §2.3. That is wrong, and the notes as written say so correctly: **§2.3 lives in #802's frozen prereg**, [`research/2026-07-28_subc-kubc-bracket_prereg-FROZEN.md`](../../research/2026-07-28_subc-kubc-bracket_prereg-FROZEN.md) `:97` (*"★THE SHIPPED ESTIMATOR — `R_KUBC_core`, which is NEITHER bound (the flag-don't-fix item)"*). **#782's frozen prereg has no §2.3 at all** — verified two ways: `grep -c "2\.3"` on `research/2026-07-21_rve-aggregation-bench_prereg-FROZEN.md` returns `0`, and its full section-header list runs §0–§6 with no §2.x subsections. All merged #802 sources say *"the frozen prereg §2.3"* meaning their own. Flagged rather than silently absorbed, since the brief's version would have planted a false cite in a merged doc.

##### ★LINE-DRIFT CONSEQUENCE, disclosed (not hidden in the diff)

Landing the `:12` note inserts 6 lines above the §7.1 bullet, which **moves it from `:124` to `:130`**. Incoming cites that name `research/2026-07-21_rve-aggregation-bench_result.md:124` therefore now point 6 lines high: merged #802's result doc `:44`, its docket fragment `:22`, and the shipped JSON string `F5_782_bound_robustness_crosscheck_NOT_FROZEN.claim_under_test_782_sec_7p1`. **The excerpts still name the real content** and all three still resolve inside the anchor-checker's `±10`-line window, so nothing is broken — but a cite-refresh on those three is available if the orchestrator wants the `:NN` exact. `:12` itself is UNCHANGED (the note is appended after it). Verified: all five pre-existing `:12`/`:124` anchor-advisory flags are present at HEAD **before** this edit — this lane creates none of them, and incidentally resolves one (`2026-07-28-subc-kubc-bracket.md:22`) by window overlap.

#### Battery

`make verify` exit `0`; `refresh-kb-metadata` idempotent (`0` subtree-claims, `0` solidity, `0` footers, `0` index files rewritten on the confirming re-run); `verify-md-links` gating `0`; `verify-frozen-provenance` `0` gating (the 2 advisory findings are pre-existing, on other lanes' docs, both pre-dating the `2026-07-22` gating date); `verify-lane-number-checks` PASS (its `DOCS` list is the three #802-lane files — none touched here). No `Frozen`-labelled text and no prereg was edited in either item. Pure-corpus.
