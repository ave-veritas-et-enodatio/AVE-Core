### ENTRY 2026-08-04-echo-delay-regulated-sum (2026-08-04): the LATTICE-REGULATED optical return delay to the `r_sat` wall — RHO-A `DELAY-CERTIFIED` with `BIN-DA-CLOSED` adjudicated; RHO-B `DELAY-NOT-CERTIFIED` on two freeze-time algebra errors this lane made and its own gates caught. Turning point NODE-governed, delay ACHROMATIC, no chirp. Zero ids minted, four flags routed. **SVA pilot case 2.**

- **Provenance.** Grant GO 2026-08-04 (core session), *"delay derivation first"*, following his catch on PR #876: the axial RHO-B lane's log-divergent `∫dr/c_shear` is a property of the **CONTINUUM LIMIT**; on the physical lattice the optical path is a **FINITE node sum cut at the last cell** — effectively infinite (log-enhanced), not infinite. This lane derives the regulated version and turns FORK-3(b) into a timing question.
- **Class.** DERIVATION lane · implementer-lane · NOT adversarially reviewed · mints **zero** `clm-`/`def-`/`exp-`/`sup-`/`ilk-` ids · moves **zero** solidities · edits **no** manuscript or KB file · adjudicates **no** fork · **engine `src/ave` byte-untouched**. Written against `origin/main` @ `2877eaa0`. Branch `research/echo-delay-regulated-sum`, PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.
- **Files.** `research/2026-08-04_echo-delay-regulated-sum_prereg-FROZEN.md` (COMMIT 1, pushed **ALONE**, `1da06a90`), `research/drivers/echo_delay_regulated_sum.py` + `..._results.json` (COMMIT 2), `research/2026-08-04_echo-delay-regulated-sum_result.md` + `..._number_check.py` + one appended `Makefile` target (COMMIT 3), this fragment (COMMIT 4). **No other file touched.**

- **★ WHAT IS ADJUDICATED — `BIN-DA-CLOSED`, on the one configuration entitled to adjudicate.** Under RHO-A the excess round-trip delay is a **pure number times `r_sat/c₀`**: `2𝒥_A` with `𝒥_A = 1 − √π Γ(3/4)/Γ(1/4) = 0.4009298826322039`, measured at `0.80185976526437822`, agreeing to `3.68818e-14`, **ACHROMATIC and REGULATOR-FREE** (six-variant spread `7.5252419456313015e-14`). SI values: `1.71 ms` at `62 M_⊙`, `∝ M`.
- **★ AND THE FINDING THAT CAME OUT OF THE PLANE DECLARATION.** wall-taxonomy §9 row 1 (*no signed Γ travels without its plane*) applied to a **delay** rather than a sign exposed that the 2026-06-17 forward prereg's **`3–10 ms` band was never an uncertainty in the substrate prediction** — it is the **free-flight term of an undeclared `r_out`**. Declaring `PLANE-∞` (excess over cold-lattice flight, convergent because the graded deviation is `O(1/r²)` with no `1/r` term) removes the knob and leaves a number. **The predecessor document is not edited and no edit is proposed.**

- **★ WHAT IS NOT ADJUDICATED, AND WHY IT IS THE DISCIPLINE WORKING.** `CFG-B` is **`DELAY-NOT-CERTIFIED`**: `G-DECADE` and `G-DISC` FAIL, so `BIN-DB`, `BIN-DISC` and CFG-B's `BIN-CUTOFF` / `BIN-EVAN` are `N/A` and every RHO-B number ships as a **NOT-ADJUDICATED DIAGNOSTIC**. The shipped JSON gates its own bins on that verdict. **No threshold is retuned.** Both failures are **this lane's own freeze-time algebra**, diagnosed exactly with the repair named:
  - **`G-DISC`** — prereg §2.8 derived the discrete-minus-continuum offset as `γ` **per one-way pass**; it is `γ` per **round trip**, the derivation having dropped the `1/2` in `v = c₀S² = 2c₀x/r_sat`. Measured `0.28860783245078786` vs the corrected `γ/2 = 0.2886078324507664` — **thirteen significant digits**; and `0.63518142273075926` vs the corrected `(γ + ln 2)/2` at `θ = 1/2`, also thirteen. **General corrected law derived in the result:** `K_disc(θ) = [ln θ − ψ(θ)]/2` per one-way pass. The constant was right; its **pass attribution** was wrong.
  - **`G-DECADE`** — the frozen `1e-6` was sized for the asymptotic log law while the sweep's shallowest rung sits at `S = 1e-2`, where the derived `O(S²)` correction is `1.07492e-5`. The five rungs fall by **exactly 100× each** (the `S²` signature) and the measured value matches the derived `S²/(4 ln 10)` to one per cent. **A sharper confirmation of the log law than a pass would have been — and still a failure as frozen.**

- **★ THE TURNING-POINT QUESTION, ANSWERED: NODE-GOVERNED, DELAY ACHROMATIC, NO CHIRP.** The lattice's local band edge collapses with the local speed (`ω_max(r) = β ω_C S^p`, Op14 local-clock modulation applied to a **band edge**), so a ringdown could in principle turn around at cutoff **before** running out of cells. The closed-form comparison `S_turn/S_last = √(Ω/(2θβ))` **contains no mass**: both cutoffs scale as `√(ℓ_node/r_sat)` because a ringdown sits at the object's own light-crossing frequency. Across both band-top bracket ends, both sub-cell placements and all four masses the ratio lies in `[0.2334174292085989, 0.58365895340449818]` — **always below 1, never by more than a factor of five.** The wave reaches the last intact cell; the chirped-echo branch (`BIN-DB-BAND`, `dT/d ln ω = −r_sat/c₀`) is derived, is demonstrated reachable every run by `FT-TURN`, and **does not fire**. **Hand-evaluated at freeze and disclosed in prereg §7.7, so it is reported as a CONFIRMED DERIVATION and not as a discovery.**
- **★ GROUP-vs-PHASE, DERIVED NOT ASSUMED — and the substrate-native check that changed the answer.** `srs-band-structure.md` §2 adjudicates the **arccos transmission-line** map as substrate-native and shows the graph-Laplacian map FAILS the `1/√3` velocity gate. On the `z = 2` radial cascade that map is **exactly linear**, so `v_group ≡ v_phase` up to the zone edge and **no group-velocity correction to the delay exists**. Defaulting to the tight-binding map would have manufactured a band-edge slowdown the substrate does not have. `FT-DISP` is the receipt: the rejected map moves the dispersion by `1.12595` where the adjudicated one is exact to `3.7711e-50`. **The rejected model is swept anyway (variant `D2`).**
- **★ A PARAMETER-FREE OUTER PLANE EXISTS OUTSIDE `r_sat`, ON BOTH BRANCHES.** Because the local speed vanishes at the wall, the effective barrier `𝒱 = v²U` vanishes at **both** ends and necessarily peaks between them: `1.3850982140393763 r_sat` (RHO-A) and `1.7482084718166324 r_sat` (RHO-B), i.e. `9.70` and `12.24` in `GM/c²`. The `U` collection is gated (`G-U`, `5.34553e-50`) against the raw `W = Ψ/(r√μ)` transformation.

- **★ FLAG-ECO — mandatory in the result headline BY FREEZE, and it is there.** The RHO-B log law `T ~ (r_sat/c₀) ln(r_sat/ℓ_node)` is **STRUCTURALLY DEGENERATE** with the standard ECO / near-horizon-firewall echo law `Δt ~ (2r_s/c)|ln(δ/r_s)|`. A detected log-form echo delay does **NOT** select AVE; the only discriminating content is that AVE's cutoff length is fixed where the ECO offset is a free knob. **Declared at freeze, before any number existed, precisely so it could not be discovered next to a favourable-looking ratio.**
- **★ `FLAG-ECO-COROLLARY` — routed to Grant and the auditor lane, repaired nowhere.** `research/2026-06-17_bh-shear-echo-forward-prereg.md:73` reads verbatim *"**AVE has no such knob:** its reflector is at the *fixed, parameter-free* radius `r_sat = 7GM/c²`, **outside** `r_s`, with **no log-divergence**"*, and the KB banner at `existing-experimental-signatures.md:42` repeats it. **Under RHO-B the AVE delay IS log-divergent in the continuum and log-enhanced on the lattice**, so that discriminator is **RHO-A-conditional**. Both lines verified two-method at freeze and again at result. **Neither repaired; neither branch preferred; no edit proposed.**
- **`FLAG-PLANE-GAP`** — `research/2026-06-17_bh-shear-echo-forward-prereg.md:65` states verbatim *"there is no parameter-free outer reflector *outside* `r_sat`"*, having surveyed only the imported GR photon sphere and the corpus ringdown-cavity dimension. The profile builds its own (above). **Surfaced with the line and the quote; that frozen document is not edited.**
- **`FLAG-CITE-SHIFT`** — `srs-band-structure.md:145` cites `constants.py:294` for `OMEGA_C`; at `2877eaa0` that symbol is at `constants.py:305`. Pure line-shift class, correct-when-written presumed. **NOT repaired** (KB edits outside this lane's scope fence). → **auditor**.
- **`FLAG-FREEZE-SIZING` — a lane-pattern flagged for the orchestrator, not resolved.** This is the **second consecutive lane in this arc** to land NOT-CERTIFIED on freeze-time sizing/derivation errors rather than on physics (the axial RHO-B lane logged four the same day; this lane logs two). **Both times the gates worked exactly as designed and the physics survived.** Whether the freeze-time derivation of gate constants needs its own check step is a **process question raised and not answered.**
- **Carried, not re-asked:** `FLAG-CAUSAL` (is an infinite-electrical-length lossless termination a legitimate Ax-3 port?) — this lane makes the electrical length **finite and calculable**, about `43` radians of `ln` at the reference mass, but whether that is a port or a mirror is still Grant's. `FLAG-PITCH` (does the lattice pitch inherit `ε₁₁`?) is **swept** as variant `R4`, moving the RHO-B delay by ~`2 %`, and **settled nowhere**. `FLAG-BRACKET` (the vector band top is a bracket pending Grant) is **swept at both ends**, with the verdict unchanged.

- **★ THE §0.2 PLUMBER QUESTION, ASKED AT FREEZE AND STILL OWED.** The node-vs-band margin is a factor of `2.3`–`4.3` in `S` — thin. *When an LC ladder is tapered until its last few sections are past cutoff at the drive frequency, does the pulse reflect off the cutoff (soft, dispersive, frequency-dependent) or tunnel the last few cells and reflect off the physical end (hard, achromatic)?* The sections are lossless either way and the evanescent skin is only a handful of cells thick — exactly the regime where tunnelling is not negligible. **This lane computes NO tunnelling amplitude (§1.3 Y8) and does not answer it.** → **Grant.**

- **Explicit non-claims, binding and honoured.** No adjudication of FORK-3(b) — *"a cleaner number on one branch is not evidence for that branch"*. **No echo TRAIN claim**: a barrier LOCATION is computed, a **reflectivity is not**, so no amplitude, no count, no detectability. **No observational claim**: no dataset analysed; the `~0.29 s` Abedi–Dykaar–Afshordi spacing appears **only** as an in-repo cited pointer in a diagnostic that was **frozen at §7.5b before any number existed** precisely so the foreseeable comparison could not be constructed post-hoc — and both branches **MISS it**, with FLAG-ECO applying in full. No FLAG-W, no sign-relativity leaf, no claim-quality repair, PR #876's branch untouched.

- **Battery.** `make verify` **exit 0** with the new gate wired in. **`G-NC` NEGATIVE CONTROL PASSES BY THREE ORDERS OF HEADROOM**: this lane's independently mpmath-derived RHO-A delay reproduces the frozen 2026-06-17 predecessor driver at **all four** of its own `r_out/r_sat` entries, worst `7.1262679104721422e-13` against a frozen `1e-10`. **All twelve fireability self-tests FIRE**, three of them (`FT-CUT`, `FT-EVAN`, `FT-TURN`) existing solely to demonstrate every run that a physics bin **can** fire. **Determinism**: two full runs, digest `a788ac6080af4073` twice, byte-identical apart from `_runtime_sec`. **Number check**: 299 sites, 144 registered, 155 allow-listed with reasons, **0 unregistered**, all 116 registered keys exercised; it implements the six accumulated checker lessons plus this lane's **seventh — a MUTATION RECEIPT** wired into the Makefile target, so the gate proves it can FAIL on every invocation, **and** it splits back-ticked spans on separators so numerals written inside expressions are checked rather than skipped (the completeness guard caught twelve such sites). **Pure-corpus.**
- **Numerical disclosure that is load-bearing rather than cosmetic.** At the innermost node `ℓ_node/r_sat = 6.02e-19`, so **any float64 `1 − A²` by subtraction returns exactly zero** and the whole calculation produces silent nonsense with no error. Every near-wall quantity is computed from `S² = x(2r_sat + x)/(r_sat + x)²` and every `S^{-p} − 1` from `expm1(−p/2 · log S²)`.
- **★ SVA PILOT CASE 2 — the per-row fill log is §9 of the result doc.** All ten rows **FILLED**, none un-fillable, the header written **before** the derivation existed. **Rows 3 (circuit statement), 4 (plane & projection) and 8 (discrimination class) each changed what this lane computed or how it reported it**: row 3 produced the band-edge turning-point question, row 4 produced three deliverables (the plane artefact finding, the plane-invariant excess definition, the derived barrier), row 8 produced FLAG-ECO **before** any number existed. Row 1 caught nothing on this problem class and that is recorded too. **Two gaps offered as pilot feedback, not proposed as changes:** (i) **there is no row for NUMERICAL CONDITIONING**, and the single largest failure mode here was the silent float64 cancellation above; (ii) **row 5 has no provenance tag for a BRACKET pending a ruling** — suggest `BRACKETED(pending-ruling)`. **The canonization decision is Grant's; this is input, not a vote.**
- **Routed follow-ons (named, NOT started).** (a) a successor lane with the two constants repaired (`K_disc(θ) = [ln θ − ψ(θ)]/2`; `G-DECADE` gated against the derived `S²/4` residual), which would certify CFG-B and let `BIN-DB` / `BIN-DISC` / CFG-B's `BIN-CUTOFF` be adjudicated rather than inherited as diagnostics; (b) the tunnelling amplitude through the few-cell evanescent skin; (c) a reflectivity at the derived barrier, without which no echo train can be claimed on either branch; (d) the auditor-lane disposition of `FLAG-ECO-COROLLARY`, `FLAG-PLANE-GAP` and `FLAG-CITE-SHIFT`.
