# R60 — the sibling definition-debt packaging: qualified "port family" only (R38/R40 fence mandatory), and "one 0₁ defect carries both A1 and T2 contents" is a DERIVATION TARGET, not a definition (2026-09-13)

### ENTRY 2026-09-13-ruling-r60-sibling-def-debt-o1-o2

*(Number provisional: R60 is the next free ruling number at `origin/main` `5619dd56` (R59 = `2026-09-13-ruling-r59-fired-riders-own-job.md`); no remote branch carries an R60/R61 fragment at writing. The Orchestrator confirms the number at merge.)*

**Grant, verbatim (the ruling, 2026-09-13, relayed through the AVE Orchestrator session against PR #1044 at `5095d4a7` — this fragment is its docket record; no primary artifact by Grant exists in the repo at the time of writing; the same text is banked in the ruling block of `research/2026-09-13_sibling-definition-debt_RECORD.md`, and a comment by Grant on #1044 quoting it would make this record self-verifying):**
- *"Lane A / PR #1044 — Grant ruling (via AVE Orchestrator), 2026-09-13. MATH agrees; Grant confirms."*
- *"O1 — NOUN: use qualified "port family" only. R38/R40 fence is mandatory on every use: A1 bound response is NOT a register PORT (Re Z>0). Never say unqualified "port family" for the A1 slot."*
- *"O2 — STATUS: "one defect (0₁) carries both A1 and T2 contents" is a DERIVATION TARGET, not a packaging definition. Do not assert co-ownership as definition; that would smuggle past canon "two objects, not one" (C1). Clause S stays mechanism-agnostic until derived."*
- *"Standing: grade remains WALK / DEFINITION-DEBT. No promote. No KB/manuscript leaf land before #1034 (H1) is reviewed. Update the RECORD / PR description to bank O1+O2 as this ruling; do not merge #1044 unless/until Orchestrator+Grant say so."*

**Same-day audit calls (the Orchestrator's audit of the Lane A status, 2026-09-13, relayed by Grant — "conditional pass"):**
- Call 1 — *"R38/R40 × #1034: Yes, the fence reaches. Unqualified "the A1 2-port" / unfenced G-PORTS is in O1 scope (A1 ≠ register PORT). Escalate those to blocks-merge until fenced or reworded. Other NEEDS-WORK items can stay needs-work."*
- Call 2 — *"#1044's "no KB leaf before #1034 reviewed" stays unsatisfied until the #1034 body matches head and you/I sign. NEEDS-WORK ≠ cleared."*
- *"Also owed: one-line O1/O2 docket (R59-style) before any KB land."* — this entry.

**Why this entry exists:** until now the O1/O2 ruling lived only inside the record it governs, so the record's own O1 pin was self-referential (Claims Gate packet §3, Steward note). Per the R59 precedent a ruling relayed through the orchestrator session gets a one-line docket receipt, so the pin has a non-self-referential home before any KB landing.

## §1 — THE RULING (R60), restated by the entry in one line

**In the sibling definition-debt packaging, "port family" is used only in the qualified def-portmp sector/grade sense with the R38/R40 fence stated on every use (the A1 bound response is NOT a register PORT); "one 0₁ defect carries both A1 and T2 contents" is a DERIVATION TARGET and is never asserted as a definition (clause S stays mechanism-agnostic); the record stays WALK / DEFINITION-DEBT, is not promoted, and lands no KB or manuscript leaf before #1034 (H1) is reviewed with its body matching its head and Grant/Orchestrator sign; the fence reaches #1034's "A1 2-port" / G-PORTS wording, which is blocks-merge until fenced or reworded.**

**Class:** ruling receipt (vocabulary + status + sequencing). Mints no `clm-`/`def-`; engine `src/ave` byte-untouched; no KB or manuscript leaf edited. Homes: `research/2026-09-13_sibling-definition-debt_RECORD.md` (banked text) · `research/2026-09-13_sibling-definition-debt_GATE-PACKET.md` (gate).
