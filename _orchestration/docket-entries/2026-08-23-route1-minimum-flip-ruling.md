### ENTRY 2026-08-23-route1-minimum-flip-ruling (2026-08-23): Grant — the D1 port-arithmetic flip: route 1 revised from embedding-obstruction to minimum-N mechanism

**Context.** Route 1's first-order pass (PR #999) returned B4 — question ill-posed as frozen —
with defect D1: a closed loop transiting a z=3 node consumes 2 of 3 ports, so "n loops at
one-per-port" is arithmetically impossible for n ≥ 2. The checker audit independently
reproduced every computed claim (exact-120° stars summing to zero; girth 10; the C₃ non-bond
axis).

**RULING (Grant, 2026-08-23, verbatim):** *"would the loop occupying 2 ports not be the
physical picture of why you need a minimum on N=3?"* — followed, on the orchestrator's walked
elaboration, by: *"proceed, word"*.

**The banked statement (WALK-level pending the adversarial pass this entry orders):** the
port-consumption defect FLIPS into leg 1 of a minimum-N argument, convergent across both fork
readings:

- *Wire reading:* (leg 1, DERIVED from z=3) two loops can never share a node (2+2 > 3) and
  cannot share a bond (disjoint embedded curves), so contact/galvanic binding is impossible —
  the only binding channel is topological linking. (Leg 2, CONDITIONAL = route 2's question,
  now LOAD-BEARING: if loop-loop linking mints charge, a neutral core requires all pairwise
  linkings zero.) (Leg 3, external mathematics: the minimal nontrivial pairwise-null linkage
  is 3-component — Borromean is the smallest Brunnian link.) ⇒ N=2 cannot bind; N_min = 3.
- *Saturation-locus reading (Grant's (c) candidate):* on the measured exactly-120° bond star,
  no 1- or 2-port loading can sum to zero (no two of the three directions are antipodal), so
  a zero-net "virtual neutral" core exists only with all three symmetric inputs engaged —
  minimum 3 by vector arithmetic on computed geometry, no conditional required.
- *Corroboration:* canon independently retired mesons-as-2-loop-links (pion = medium
  resonance); the mechanism explains that retirement — no 2-loop bound state exists.
- *NOT given:* the cap (N=3 exactly). Both arguments give N ≥ 3. WALK-level observations
  toward the cap: the measured C₃ axis is geometrically distinguished (wrong symmetry class
  for a 4-fold balance on this node); ground-state selection makes N ≥ 4 Brunnian structures
  candidate excited spectrum, a prediction-shaped statement.

**Ordered by this ruling:** (1) the open item's route 1 revised to this statement; (2) route 2
re-aimed as load-bearing (the leg-2 conditional); (3) the walk goes through a standard
adversarial pass before any claim-grade exceeds WALK. Held separately (not covered by this
ruling): the fork-routing decision (freeze (c) vs hold behind the baryon lane), the Δc=3
collision, and the NEW ropelength-convention question from the #999 audit (under the standard
convention BOTH electron-geometry arms are horn tori with no hole — canon threading
inexpressible on both; decides whether the neutron's threaded-electron picture is
geometrically expressible).
