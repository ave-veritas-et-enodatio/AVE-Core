
### ENTRY 2026-08-02-mr-no-kb-home-correction (2026-08-02): manuscript-reconciliation lane — Si-28 item (d) corrected twice over (#827 pilot + audit receipts)

- **Class: dated correction of `2026-08-02-mr-no-kb-home.md` item (d) (original untouched, Rule 12). Routed, not adjudicated.**
- **(d-corrected-1) A KB twin for R_bipyramid EXISTS — the routed question is a VALUE DISAGREEMENT, and the KB row is the staleness candidate.** `vol6/appendix/geometric-inevitability/platonic-progression.md:24` = `| 7 | Si-28 | Pentagonal Bipyramid | 83.0 | 0.0002% |`, column header `R ($d$)` — same quantity, same units as the print. The print (`vol_4 .../01_vacuum_circuit_analysis.tex`, R_bipyramid = 80.174d) AGREES with the engine (`src/scripts/vol_6_periodic_table/simulations/simulate_element.py:390`, `r_bipyr = 80.174370 * d`). Substrate-first: the engine computed the number; KB(83.0) vs {print, engine}(80.174370) reads as a stale KB row. **Request: rule which value is canonical (and re-run or cite the computing script), then the KB row and any dependents reconcile.** Note the leaf's `0.0002%` column is a geometric residual to assumed Platonic coordinates (its `:14` scope correction), not a mass-mapping error.
- **(d-corrected-2) The 378-count limb is WITHDRAWN — no divergence exists.** The KB carries BOTH counts, reconciled: `vol6/claim-quality.md:629` ("378 SPICE coupled inductors for Si-28's pentagonal bipyramid"), `vol6/framework/executive-abstract.md:52` (verbatim "exactly 378 coupled inductor nodes"), `vol6/period-3/silicon/ee-equivalent.md:10` ("378-parameter SPICE matrix decomposes into 21 inter-alpha links"); C(28,2)=378 nucleon pairs vs C(7,2)=21 inter-alpha links are two levels of the same model. Residual: a wording nit only (print "inductor nodes" vs claim-quality "coupled inductors"). The prior correction (in `2026-08-02-mr-no-kb-home.md`'s corrected item text landed via #825) had already fixed "channels"→"nodes" but retained a derivation-scope question; that question is hereby narrowed to the wording nit.
