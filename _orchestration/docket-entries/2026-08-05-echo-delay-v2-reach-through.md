### ENTRY 2026-08-05-echo-delay-v2-reach-through (2026-08-05): the regulated-return instrument v2 — **PART 1 fully re-certified on BOTH branches with `208` v1 numbers reproduced to exact string equality, and `BIN-DISC` ADJUDICATED: FORK-3(b) IS echo-discriminable by TIMING.** **PART 2 (Y8, the reach-through computation) `Y8-NOT-CERTIFIED` — the observable, as posed, is NOT WELL POSED, and the lane's own gate is what says so.** Zero ids minted, six flags routed (three new). **SVA pilot case 3.**

- **Provenance.** Grant GO 2026-08-05. Two deliverables in one lane: **(1)** the certification rerun of the v1 regulated-sum instrument with `G-DISC`'s LAW repaired and `G-DECADE`'s TOLERANCE resized — and no other threshold touched; **(2)** **Y8**, the reach-through computation, which v1 fenced out by name (v1 §1.3 Y8: *"does NOT compute a reflectivity, a tunnelling amplitude, or an evanescent-decay length through the cutoff region"*), in the **SEMICONDUCTOR** register with the three mandatory carves.
- **Class.** DERIVATION lane · implementer-lane · NOT adversarially reviewed · mints **zero** `clm-`/`def-`/`exp-`/`sup-`/`ilk-` ids · moves **zero** solidities · edits **no** manuscript or KB file · adjudicates **no** fork · **engine `src/ave` byte-untouched**. Written against `origin/main` @ `773fe007`. Branch `research/echo-delay-v2-reach-through`, PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.
- **Files.** `research/2026-08-05_echo-delay-v2-reach-through_prereg-FROZEN.md` (COMMIT 1, pushed **ALONE**, `db98550b`), `research/drivers/echo_delay_v2_reach_through.py` + `..._results.json` (COMMIT 2), `research/2026-08-05_echo-delay-v2-reach-through_result.md` + `research/drivers/echo_delay_v2_number_check.py` + one appended `Makefile` target (COMMIT 3), this fragment (COMMIT 4). **No other file touched.**
- **Supersede discipline.** v2 is a **versioned successor** per v1's own `BIN-CERT-FAIL` disposition. **v1's `DELAY-NOT-CERTIFIED` verdict on `CFG-B` stands as a historical fact and is neither edited, withdrawn nor converted.**

- **★ WHAT IS ADJUDICATED, AND THE CONTROL THAT MAKES IT CREDIBLE.** `CFG-A` and `CFG-B` are both **`DELAY-CERTIFIED`**. **`208` v1 numbers were recomputed and reproduced to EXACT STRING EQUALITY at v1's own 17-significant-digit rendering** — `37` on the certified CFG-A set (`G-NC-V1A`), `171` on the CFG-B diagnostics (`G-NC-V1B`) — **zero mismatches**, `BIN-STOP-V1` not fired. **v1's diagnosis is now a machine-checked receipt: the gates were wrong and the numbers were not.**
- **★ THE TWO REPAIRS, RE-DERIVED NOT IMPORTED.** `G-DISC` compares the **one-way** offset against `K_disc(θ) = [ln θ − ψ(θ)]/2`, evaluated from mpmath's digamma and re-derived in the prereg from `Σ1/(n−1+θ) = ψ(N+θ) − ψ(θ)` against `∫dx/x`: measured `7.42451e-14` relative against the **CARRIED-UNCHANGED** `1 %` — **twelve orders of headroom**. `G-DECADE` limb (a) `1.07492e-5` against the resized `1e-4` (`9.30×` stated headroom over the derived worst case), plus a **NEW strictly strengthening residual limb** (b) at `3.78771e-5` against `1e-3` which tests the derived `S²` **shape** rather than only its size. **A pass-count discipline is now enforced everywhere: `K_disc` is one-way, its round trip is `2K_disc`, and no bare Euler–Mascheroni constant appears without a pass count.**
- **★★ `BIN-DISC` FIRES AND IS ADJUDICATED — v1's one genuinely open question, answered.** `|T_B − T_A|` exceeds one substrate-native ringdown damping time at **every** mass on the frozen grid, by `38.6` to `43.2`, with the branch ratio running `48.8` to `54.5`. **The TIMING route to FORK-3(b) is OPEN.** `BIN-DA-CLOSED`, `BIN-DB-NODE`, `BIN-CUTOFF-ROBUST` on both configurations and `BIN-EVAN-CLEAR` at both bracket ends are likewise adjudicated. **No branch is preferred; `FLAG-ECO` applies in full.**

- **★ Y8, PART A: THE DEPLETION WIDTH IS ZERO EVERYWHERE, EXACTLY.** `W = max(0, ⌊Ω/(2β) + 1 − θ⌋)` — derived, **mass-free**, two-method-checked as exact integers (`G-DEP`). Across the frozen ringdown band (the **FWHM of v2.4's certified axial pole**, both endpoints read PROGRAMMATICALLY), both bracket ends, both `θ` and every variant, **`W = 0` cells**: the junction two-port is the `2×2` **identity to all digits** and `|T|²` through the depleted section is `1` **exactly**. The closest corner clears by `1.90×` in `Ω`. **Hand-evaluated at freeze and disclosed in prereg §7Y.5 — reported as a CONFIRMED DERIVATION, not a discovery.** Under RHO-A the edge is inside the last cell by `3.8e-29` in `S` and the question is **MOOT**, as v1's twenty-five-orders statement implied (cross-referenced, not restated).
- **★★ Y8, PART B — THE REAL RESULT, AND IT IS A NEGATIVE: `Γ_in` AT A NEAR-WALL PLANE IS NOT A WELL-POSED OBSERVABLE ON THE RHO-B TAPER.** `G-KWIN` measures a window spread of `0.641547` against a frozen `1e-3`. **The cause is structural, not numerical: `Z ∝ 1/S` and `S_n ∝ √n`, so the local reference impedance at the plane runs as `1/√K` without bound** (measured `1e-2`, `3.16e-3`, `1e-3` at `K = 10⁴, 10⁵, 10⁶`). Moving the plane changes what the reflection is measured against, so `Γ_in` cannot converge. **The freeze-time §2Y.6 argument covered the reflection GENERATED per cell, not the composite referenced to a moving plane.** `G-UNIT` also fails (`4.72977e-12` vs `1e-12`) on a second freeze-time error **of a different kind**: a Möbius map of the disc is non-expansive in the **hyperbolic** metric, not the Euclidean modulus the gate measures, and `G-PREC` (`2.02048e-15` against an mpmath `dps = 50` re-run) proves the arithmetic itself is sound. **Neither threshold retuned. No Y8 bin adjudicated. The `BIN-RT-INTERFERENCE` token in the shipped JSON is a classifier output on an uncertified configuration and may NOT be quoted as a verdict.**
- **★ Y8 CONTENT THAT SURVIVES THE ILL-POSED PLANE.** (i) **The transit delay is EXACTLY achromatic at the level of the reflection phase**: with the impedance steps off, `CHIRP-MEASURE = 7.7e-11` and the mirror group delay reproduces PART 1's node-sum round trip over the same `10⁶` cells to `1.5965e-13` (`G-XTIE`, the cross-part control). (ii) **All measured dispersion therefore comes from the impedance staircase**, which **relocates** v1 §2.7's chirp question rather than answering it — the band-edge mechanism is dead (`W = 0`), a discreteness-driven one is live and unquantified. (iii) **The near-wall two-port is MASS-FREE to `1.44774e-11`** across a `100×` mass lever (`G-MFREE`), so the machinery measures lattice structure, not mass. (iv) **The cost of the unresolved `FLAG-CAUSAL` fork is quantified**: the three-way `|Γ_in|` spread across `Γ_L ∈ {0, −1, +1}` reaches `0.992` on the primary readings — **a `W = 0` junction two-port, being the identity exactly, cannot screen the contact question at all.**

- **★ `FLAG-ECO`, EXTENDED BY Y8 AND MANDATORY IN THE RESULT HEADLINE BY FREEZE.** The ECO / near-horizon-firewall family carries a **free contact reflectivity**, so it reproduces **any** `Γ_L` and therefore **any** reach-through outcome. **Y8 adds a SECOND free knob on the ECO side rather than removing one.** A reach-through verdict, had one been issued, would have discriminated nothing.
- **★ `FLAG-PLANE-RT` — NEW, and it is the gating item for any successor.** A successor must either declare a **fixed** plane in advance — and then pay the `~1.2e18`-cell enumeration the no-WKB ruling demands — **or construct a plane-invariant reflection observable, the `Γ` analogue of what v1's `PLANE-∞` excess did for the delay.** → **the orchestrator, as a scoping decision BEFORE any Y8 successor is dispatched.**
- **★ `FLAG-PLACEMENT` — NEW.** Node-uniform vs bond-midpoint impedance placement differ only at the innermost cell and differ there by the full `√2` step — exactly where the reflection is generated. Swept as `Y-MID` (moves the band-centre value ~`2 %`); **neither preferred.** → **Grant / a successor.**
- **★ `FLAG-REGISTER` — NEW, vocabulary not physics.** On the **adjudicated** distributed cell model the band top is a **Bragg / half-wave resonance**, not an evanescence onset, so the semiconductor register's *depletion-edge* connotation does **not** carry over. **The ruled vocabulary was used and the connotation carved out explicitly; no vocabulary change is proposed.** → **auditor lane, if the register is reused.**
- **★ `FLAG-FT-WORDING` — NEW, this lane's own prereg.** `FT-DISC`'s frozen row's two clauses read on different quantities (introduced perturbation `0.02` vs resulting gate separation `0.0196`). **Both reported; the substantive fire condition — `G-DISC` FAILS under the mutation — is met by `1.96×`; the imprecision is SURFACED, not resolved in this lane's favour.**
- **★ `FLAG-FREEZE-SIZING`, THIRD INSTANCE — NOW WITH A DIAGNOSIS AND A CORRELATION.** ★ **PART 1, which had `208` predecessor numbers to control against, certified cleanly; PART 2, which had none, failed BOTH of its novel tolerances.** The mechanism is identifiable rather than merely recurrent: **a tolerance derived from an error MODEL fails when the model is wrong in kind (`G-UNIT`) or when the derivation covers a different quantity than the gate measures (`G-KWIN`).** → **the orchestrator.** Raised, not answered.
- **Carried BY POINTER ONLY, repaired nowhere:** `FLAG-CAUSAL` (still Grant's), `FLAG-ECO-COROLLARY`, `FLAG-PLANE-GAP`, `FLAG-CITE-SHIFT`, `FLAG-CANON`, `FLAG-PITCH`, `FLAG-BRACKET`.

- **Explicit non-claims, binding and honoured.** No adjudication of FORK-3(b) — **`BIN-DISC` says the branches are DISTINGUISHABLE, not which is right.** No adjudication of `FLAG-CAUSAL` — swept at all three readings, none preferred. **No echo TRAIN, amplitude, visibility or detectability claim**; no `Γ_in` at `PLANE-PEAK` or `PLANE-∞` (the no-WKB ruling makes the `~1.2e18`-cell product unenumerable, and this is DECLARED rather than approximated). No observational claim; the `~0.29 s` pointer appears only in the diagnostic v1 froze at §7.5b, and **both branches MISS it**. No 3D-srs correction, no pitch ruling, no band-top ruling, nothing Cosserat, nothing polar, no eigenvalue.

- **Battery.** `make verify` **exit 0** with the new gate wired in. **All twenty-one fireability self-tests FIRE**, including three exact synthetic ladders (`FT-RT-C/I/E`, impedance ratios `1.01`/`3`/`1000` giving `(1−f)/(1+f)` in closed form) that put one result **strictly inside each frozen `BIN-RT` band deterministically every run, exercising the real recursion and the real classifier**; `FT-W` demonstrates `BIN-W` reachable (`170` and `54` depleted cells at `ω × 10³`) **and** exercises the `G-ABCD` two-route comparison (`2.84e-16`) that `W = 0` makes N/A-by-outcome; `FT-V1` demonstrates the negative controls themselves are fireable. **Determinism**: two full runs, digest `4c7926fd9954dfc7` twice, byte-identical apart from `_runtime_sec`. **Number check**: `425` sites, `232` registered, `193` allow-listed with reasons, **`0` unregistered**, all `169` registered keys exercised, with the mutation receipt wired into the Makefile target. **Makefile conflict class DISCLOSED**: the `.PHONY` line and the `verify:` prerequisite line are shared with every other lane's number-check target — a **REAL two-line union conflict** with any concurrent lane. **Pure-corpus.**
- **★ SVA PILOT CASE 3 — and BOTH pilot-2 amendments were adopted voluntarily and BOTH earned their keep.** All eleven rows **FILLED**, header written before the derivation. **Rows 1, 3, 4, 8 and the adopted row 11 each changed what this lane computed or how it reported it, before any number existed.** ★ **Row 4 (plane & projection) is directly responsible for the lane's central negative being FOUND rather than shipped as a number**: it forced *"every `Γ` is referenced to the LOCAL impedance at its own plane"* into the freeze, which is what made the `1/√K` reference drift visible and gateable. ★ **Row 11 (NUMERICAL CONDITIONING) caught something and it changed the algorithm**: working the `1 + ρΓ` denominator before writing code surfaced that the obvious `ABCD` route **divides by zero on both `|Γ_L| = 1` terminations**, which carry half the result — so the Möbius formulation was chosen at design time rather than patched at integrator time. ★ **And row 11's named LIMITATION is the most useful pilot output**: it covers **expression** conditioning, not **error PROPAGATION over an iterated map**, which is exactly what `G-UNIT` failed on. **Candidate refinement offered, not proposed: require the error-propagation model to be named TOGETHER WITH THE METRIC IT CONTRACTS IN.** The **`BRACKETED(pending-ruling)`** tag localized the band-top bracket's blast radius to a single integer under the primary reading and made the `E2` bracket spread unambiguously a pending ruling rather than a physical uncertainty; **one gap: a fork a lane SWEEPS still has no `fork-` id (`Γ_L` had to be carried by a flag name).** **The canonization decision is Grant's; this is input, not a vote.**
- **Routed follow-ons (named, NOT started).** (a) **a plane-invariant reflection observable for the RHO-B taper — the gating item**, without which `FLAG-PLANE-RT` blocks any reach-through successor; (b) a `G-UNIT` tolerance derived from the correct hyperbolic contraction property, or an mpmath recursion at the primary window; (c) the **discreteness-driven dispersion mechanism** the steps-on `CHIRP-MEASURE` exposes — v1's chirp question has moved from the band edge to the impedance staircase; (d) a reflectivity at the derived barrier plane (`1.748 r_sat` under RHO-B), without which no echo train can be claimed on either branch; (e) the auditor-lane disposition of the four carried-forward flags.
