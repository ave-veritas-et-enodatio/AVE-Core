# Docket — the LAST BOND under kernel collapse (2026-08-05)

**Lane:** `research/last-bond-kernel-collapse` · **Prereg** `bf77d07a` (pushed ALONE) · **Driver** `8a27b1c9` (before any result) · written against `origin/main` = `c4a546dc`
**Provenance:** the ONE routed premise of Grant's 2026-08-05 FLAG-CAUSAL ruling (`2026-08-05-ruling-flag-causal-kernel-collapse.md`, PR #887), with its KILL CONDITION.
**Class:** DERIVATION. Research doc + this fragment ONLY. Mints no `clm-`/`def-`; propagates to no leaf; changes no solidity; edits no falsification ledger; `src/ave` byte-untouched and never imported.
**SVA pilot case 5.**

## Certification, first

| task | verdict | why |
|---|---|---|
| **1 — premise audit** | **`SCAN-NOT-CERTIFIED`; NO bin adjudicated** | `G-SCAN` FAILS, `FT-SCAN` does not fire — both on defects this lane built into its own frozen scan design. The per-coupling table ships as a NOT-ADJUDICATED DIAGNOSTIC. |
| **2 — the discrete row** | **`ROW-NOT-CERTIFIED`** | `G-RHO2` FAILS on an injection point sized wrong at freeze. Every other Task-2 gate passes; four pass at **exactly zero**. |
| **3 — continuum reconciliation** | **CERTIFIED; `BIN-C-DISJOINT` ADJUDICATED** | its gates (`G-PREC`, and Theorems 1–2 via `G-BOND`/`G-ROW`) all pass; it uses Theorem 3(b) nowhere. |

## What the lane returns

1. **The discrete free-end row is EXACT, and beyond-wall-independent EXACTLY.** Over `3360` swept
   rows the last-bond stiffness is `0.0`, `|Γ_LB + 1|` is `0.0`, the spread over the whole
   beyond-wall grid (density `1e-30`…`1e+30`, load `0`…`INFINITY`) is `0.0`, and the two density
   branches separate by `0.0`. The harmonic (series-spring) rule is what makes it exact; the
   arithmetic control fails to collapse and lets `Z_beyond` swing the row across the entire unit
   circle, which is what makes the exactness meaningful rather than tautological.
2. **★ THE PREMISE IS NOT ESTABLISHED, and the reason is already open in canon.** The thing that
   decides whether every transport coupling collapses at one surface is the **cross-grade
   aggregation rule**, and canon states it is UNDERDETERMINED
   (`research/2026-07-02_axiom4-combine-rule_result.md`:79; carried at `axiom-register.md`:190).
   On the **normalized-L2** member the premise holds. On the **L∞** member — *"the wall is whichever
   grade reaches `S→0` first"*, the member **the engine codes** — the `r_sat` wall is a STRAIN
   surface, the CURVATURE grade `S_κ` is untouched there, the couple-stress bond `γ·S_κ` does not
   collapse, and **the linear cross-channel conversion path reopens.** Reported at full strength;
   no grading law invented; no member chosen.
3. **★ Canon's `r_sat` channel table has three rows; the port register has four channels.**
   Two-method absence receipt: "Cosserat" / "couple-stress" / "micro-rotation" occur **zero** times
   in `bulk-impedance-at-saturation-boundary.md`. Canon has never stated what `r_sat` does to
   channel 4.
4. **★ The sign is plane-relative by the full short↔open inversion — measured.** `G-PLANE` = `2.0`:
   one node-mass outward, `Γ` runs from `-1` to `+1`. `|Γ| = 1` at both planes (`G-UNIT` = `0.0`),
   so the MIRROR conclusion is plane-invariant and the SIGN is not. Crossover `S_cross = √(ω/ω_C)`;
   **at physical parameters the inversion does NOT fire** (`ω m_0/Z_1 ≈ 0.083`, about three and a
   half decades of `S` away).
5. **`BIN-C-DISJOINT`, loss points named and demonstrated.** (i) the wall traction is `μ·∂ψ/∂r → 0·∞`,
   which the continuum resolves along a Frobenius branch to divergence and the lattice resolves as
   *exactly zero* times a bounded difference — the continuum never takes the last-bond difference
   quotient; (ii) the round-trip optical length diverges logarithmically at the derived rate
   `ln(10)/2` per decade, so the row survives the limit but its transport to an observer does not.
   **The RHO-A negative control is decisive** — the same span moves by a factor of `1.005` over nine
   decades, so the divergence is RHO-B-specific.

## Flags routed (surfaced, not repaired)

| flag | content | to |
|---|---|---|
| **FLAG-COMBINE** | the ruling's premise is degenerate with canon's own open cross-grade aggregation fork | **Grant** |
| **FLAG-CH4** | canon's `r_sat` channel table omits the Cosserat channel entirely | Grant / auditor |
| **FLAG-KFORK** | `K(A)` is a live un-adjudicated fork with a member (`BRANCH-STIFF`, `K → ∞`) that does NOT collapse | polar lane owns it |
| **FLAG-A2DEF** | the engine carries BOTH combine members in one file (`cosserat_field_3d.py`:761–762 vs `:486`) | engine lane; not repaired here |
| **FLAG-PLANE-N0** | the free-end sign inverts one node-mass outward under RHO-B | doc-lane Q2/Q3 + wall-taxonomy §10, both gated |
| **FLAG-SCANSELF** | this lane's frozen scan is self-referential and tree-state-dependent | successor version |
| **FLAG-UNIWB** | `\b` differs between `git grep -P` (ASCII `\w`) and Python `re` (Unicode `\w`); the corpus uses Unicode subscripts, so any two-method scan built on `\b` silently diverges | discipline lane |

## Consequences documented (no claims minted)

- **The `Γ_L` fork read back onto echo-v2** — the free-end member is selected by §3; echo-v2's own
  shipped `junction_two_port` is the **identity** with `W = 0` at all four entries, so at PLANE-JX
  `Γ_in` **is** `Γ_L` with no screening. Cited, not recomputed.
- **The EM channel** is the canonically-declared exception (`Γ_EM = 0`, SYM-matched, circulation-keyed
  `μ`) and is untouched. The Cosserat channel is the exception canon has NOT declared — item 3 above.
- **Electron-ontology E1** (`2026-08-04_electron-ontology-walk_framing-note.md`:91) is fed the same
  mechanism pointed inward, plus the plane caveat it will need. Adjudicates nothing.

## Gated follow-ons (NOT done here — all require Grant)

wall-taxonomy §10 · the FLAG-W leaf blocks · the FLAG-CANON `vol3/claim-quality.md`:122/:124 repair ·
doc-lane Q2/Q3 inheritance. **This lane edits none of them.**

## Receipts

Determinism digest `778f6ef9f0819a6f` twice, byte-identical apart from `_runtime_sec`; exact
reproduction = check out `8a27b1c9` and run the driver (FLAG-SCANSELF makes the tree state
load-bearing). Empty `git diff --stat` against `c4a546dc` for every predecessor named in prereg §1,
for `manuscript/` and for `src/ave/`. Number check wired into `make verify` with a mutation receipt;
the Makefile change touched the shared `.PHONY` and `verify:` lines — a real two-line union-conflict
class with any concurrent lane — plus an appended target block (the umbrella-glob proposal is
pending, so a per-lane target is still the shipping form).
