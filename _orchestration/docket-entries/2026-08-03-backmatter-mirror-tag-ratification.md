### ENTRY 2026-08-03-backmatter-mirror-tag-ratification (2026-08-03): orchestrator — the mirror-pair tag conflict RATIFIED (backmatter :132/:135/:196 crib-covered), with the reasoning and Grant's veto open

- **Class: orchestrator adjudication of a BOARD-INTERNAL inconsistency, not a physics ruling.** Surfaced by the #853 audit (finding A) as a breach of the board's own §1 rule ("route-to-core items get docket fragments only — no manuscript edits until the core session rules"). It is a real breach and the auditor was right to block on it. The adjudication below is mine as orchestrator; the lane had no authority to make it, and did not claim to.

**The inconsistency.** The board tags `backmatter/01_appendices.tex:{132,135,196}` **[route-to-core]** (board :695/:719/:769) while tagging their **byte-identical** vol0 twins **[ruling-needed]/[mechanical]** (board :70/:93). Same sentences, opposite gates, one board. The vol0 twins were executed in **#839 and MERGED**.

**RATIFIED: the crib-covered tag governs the mirror pair. The #853 commit stands.** Reasoning, in the order it actually decided the call:
1. **What landed is the settled axis, not the routed one.** The edits apply only CRIB-1's ratified label — K=2G is *form-derived, value GR-imported* (`common/form-deriving-value-importing.md:87`). The routed question (is K=2G a FOURTH import row, or does it ride G's import? — `2026-08-02-mr-k2g-kb-internal.md` item (a)) is untouched at both sites; the #853 audit's check-7 independently confirmed no side is taken.
2. **Reverting would MANUFACTURE the split state this epic exists to remove.** Backmatter would assert "Simulation confirms … emergent" while its byte-identical vol0 twin, already merged, withdraws it. A revert would make the corpus less honest, not more.
3. **Asymmetry with the #852 revert is deliberate, and the distinction is the point.** In #852 I reverted an executed finding because the verify pass had **REFUTED** it — no defect existed, so the edit added unnecessary content and a REFUTED verdict must actually stop work. Here the defect is **real**, the fix is **ratified-crib**, and the twin is **already merged**. Refuted-means-stop is not the same rule as routed-means-wait, and a routed tag that is demonstrably mis-assigned to one half of a byte-identical pair is a bookkeeping error, not a gate.

**Owed board correction (next board touch):** re-tag `backmatter/01_appendices.tex:{132,135,196}` crib-covered-mechanical with the twin-parity reason on the record, so the register stops contradicting itself. Until that lands, THIS fragment is the authority for the disposition.

**GRANT VETO OPEN.** If the ratification is refused, the remedy is to drop commit `87772d02` from #853 and re-land after the core session rules item (a) — the rest of the PR is independent of it.

**Related, same lane, ALSO landed (audit finding B):** `01_appendices.tex`'s "standard Cauchy elastic solid (K = −4/3 G)" sentence was skipped on a rationale the docket disclaims — item (d) was corrected on 2026-08-02 to EXCLUDE that site, and the board tags it **[mechanical]** (:764) with its verify note recording that the "KB does not decide it" framing is REFUTED (the KB decides it at `graph-architecture.md:24`, dated 2026-06-15). Repaired by mirroring the vol0 twin's already-merged wording byte-verbatim. Pure mirror-drift, no new claim.

- **Receipts:** board :5 (§1 route-to-core rule), :45 (§4 authority rule), :70/:93 (vol0 twin tags), :695/:719/:769 (backmatter tags), :764 (the :141 mechanical tag + REFUTED verify note); `form-deriving-value-importing.md:87`; `2026-08-02-mr-k2g-kb-internal.md` items (a) and (d); `vol_0_engineering_compendium/chapters/03_computational_graph.tex:79-80` (the pre-epic parity source); #839 MERGED.
