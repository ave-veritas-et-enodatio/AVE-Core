### ENTRY 2026-08-03-coldq-pole-v2 (2026-08-03): implementer — the cold-Q pole derivation v2/v2.1 (compactified hyperboloidal spectral successor to #845): the ENTRY TICKET IS PASSED by ~22 orders, and the lane is SOLVER-NOT-CERTIFIED because the INSTRUMENT DEGRADES on the frozen rectangle's low-Re-Omega edge — the gates that caught it WORKED

- 🔴 **TITLE CORRECTED 2026-08-03 (post-review, PR #854 BLOCKED). Original title tail, verbatim:** *"…and the lane is still SOLVER-NOT-CERTIFIED because three of its own gate SPECIFICATIONS were defective"*. **REFUTED BY MEASUREMENT for two of the three.** Review built this lane's own proposed C9 replacement and it fails identically; review swept C1's two unfrozen parameters and the failure has no passing setting. **One specification defect (FT-B), two real instrument defects (C9, C1-high-`ell`) of one shared mechanism.** Every corrected bullet below is quoted verbatim before its correction and dated. **Nothing in either frozen prereg and nothing in the driver was touched; no gate outcome, tolerance, verdict or bin moved.** Full record: [`research/2026-08-03_coldq-pole-v2.1_result.md`](../../research/2026-08-03_coldq-pole-v2.1_result.md) §3.2, §3.3, §3.4, §10.

- **Provenance + scope fence.** Grant's GO 2026-08-03, verbatim `[sic]`: *"Go on cold-Q"*. Successor to PR #845 (`research/coldq-pole-derivation`, unmerged, under concurrent adversarial audit), which returned `SOLVER-NOT-CERTIFIED` with the mechanism diagnosed as an asymptotic far-field series plus an `~exp(2|Im omega| R_match)` subdominant-coefficient extraction on the real axis. Written against `origin/main` = `583d43dd`. **FROZEN-FIRST honoured TWICE: the v2 prereg was committed ALONE and pushed (`00724432`) before any driver code, and the v2.1 prereg was committed ALONE and pushed (`7d8fe484`) before any number existed.** Deliverables: the two prereg files, [`research/2026-08-03_coldq-pole-v2.1_result.md`](../../research/2026-08-03_coldq-pole-v2.1_result.md), [`research/drivers/coldq_pole_v2.py`](../../research/drivers/coldq_pole_v2.py) + its results JSON + a gating number-check. **Engine `src/ave` BYTE-UNTOUCHED; zero `manuscript/` edits; no `clm-`/`def-` minted; no solidity changed; no falsification-ledger edit — regardless of outcome, exactly as fenced.**
- **★ PRE-MEASUREMENT VERSIONED SUPERSEDE (v2 → v2.1), and why it is NOT a retune.** The v2 prereg was frozen and pushed; review then found four gate-coverage holes; **no driver code had been written and no number produced**, so nothing was adjusted in light of a result. Rule 11 governs post-result retuning. v2 stays **byte-untouched as the record** (verified zero diff) and is superseded by a new version number with its own verification chain — the same move #845 → v2. v2.1 is **self-contained**, not incorporated by reference. Holes closed: a **derived left-edge bound** from the lane's own outflow-row mechanism (`rho_out = |C(1)|/|B(1)| -> l(l+1)/|Omega|`, giving `|Omega|_min >= 6e-38` at `dps=50` against the rectangle's actual `0.1402` — **cleared by 36 orders**, so the edge does NOT move and is certified by a new monitor gate instead); **graded-representation certification at the rectangle's corners** (new C9); **control reach extended** from `|Omega| = 7.2` to `17.67` to cover the rectangle's `15.65` with an exact closed-form reference; and a **count-vs-box-WIDTH scaling family** (into C4/FT-D). Plus new C11, an **operator-identity** gate. Gates went `C1..C8` → `C1..C11`, self-tests `FT-A..FT-G` → `FT-A..FT-J`; every carried tolerance identical to or stricter than v2's. — ⚑ **ADDED 2026-08-03 (post-review, WARN 10): THIS SUPERSESSION NOTE CITES NO AUDITABLE ARTIFACT.** It says *"review then found four gate-coverage holes"* without naming the review, the commit, the PR comment or the finding IDs, so a reader cannot check that the holes pre-dated the freeze rather than being reconstructed after it. **The claim happens to be true — both preregs are byte-verifiable and `7d8fe484` was pushed alone before any number — but the note does not carry its own evidence.** **Standing requirement for the successor: a v2.2 supersession note MUST name the finding, the commit and the PR comment it acts on.** Applied to this correction itself: it acts on the adversarial review of **PR #854 at head `bdcfa678`**, verdict **BLOCKED**, findings **1–10**, and the mapping from commits to findings is posted as a comment on that PR.
- **★ THE HEADLINE — THE ENTRY TICKET IS PASSED, DECISIVELY.** C1's low-`ell` rows are the **exact** control #845's FT-5 failed at `15.000`: same box, same closed-form reference. **v2.1 returns the closed-form counts `1`/`1`/`2` for `ell = 1,2,3`, an argument-principle winding of exactly `1.0`/`1.0`/`2.0` identically stable at 200/400/800 contour samplings, and locates the roots to `2.971e-44`, `3.538e-46`, `5.778e-42` against a frozen `1e-20` — inside the gate by ~22 orders of magnitude.** The compactified formulation removes #845's disease by construction (no matching radius, no asymptotic series, no shooting, no subdominant extraction) and the control now measures that.
- 🔴 **RETRACTED AND REPLACED 2026-08-03 (post-review). Original bullet, verbatim:** *"**★ AND THE LANE IS STILL `SOLVER-NOT-CERTIFIED` — 2 of 11 gates failed (C1, C9), 1 of 10 self-tests did not fire (FT-B) — with ALL THREE failures in this prereg's own gate SPECIFICATIONS, not in the instrument.** (i) **FT-B is structurally vacuous**: it mutates by applying the gauge `lambda` to the interior but not the wall-BC row, and the graded wall row is the exact SHORT `dpsi/deta = 0`, which carries **no `lambda` and no `Omega`** — nothing for the mutation to break (measured `1.83e-14`). (ii) **C9 probes an object the method never uses**: a *forced* solution `M(Omega) psi = b` is not guaranteed analytic and develops a boundary layer of width `~1/|Im Omega|` at the compactified infinity; the measured probe table degrades monotonically with `|Im Omega|` (`6.997e-14` at `|Im Omega| = 0.007` → `1.899e+01` at `7.0`), while the **homogeneous outgoing eigenfunction the method actually uses is analytic by construction and converges — C2 `3.327e-14`, C3 `8.332e-14` against `1e-12`.** (iii) **C1's high-`ell` rows depend on two parameters the prereg never froze** — the control Chebyshev order and the winding's precision; the *mp location* path stays perfect there (`5.177e-38`, `1.577e-38`) while the *double-precision* seeding and winding break over a box reaching `|Omega| ~ 22`. **Selecting those parameters now would be exactly the post-hoc choice Rule 11 forbids. Nothing was repaired; the negative is banked and routed to a v2.2 successor.**"*
- **★ THE CORRECTED READING — `SOLVER-NOT-CERTIFIED` because the INSTRUMENT DEGRADES, and the gates that caught it did their job.** 2 of 11 gates failed (C1, C9) and 1 of 10 self-tests did not fire (FT-B). **ONE specification defect and TWO real instrument defects.** (i) **FT-B — SPECIFICATION DEFECT, stands**: the graded wall row is the exact SHORT `dpsi/deta = 0` and carries no `lambda`, so there is nothing for the mutation to break. **Its reported number's provenance is corrected** — the driver zeroes `lambda` for the whole operator, so `1.8294e-14` is a plain `lambda = 0` vs `lambda = 0.25` comparison, and under the mutation *as frozen* it would read exactly `0.0`; **and FT-B as coded is the logical negation of C2**, a second independent vacuity. **C2 remains a LIVE gate** — a correctly-specified half-applied-gauge analog fires at `6.4e-02` against `1e-12`. (ii) **C9 — REAL INSTRUMENT DEGRADATION on the frozen rectangle's low-`Re Omega` edge.** Review built the homogeneous-outgoing replacement this lane proposed, at identical normalization/grid/probes: **it fails the same 6 of 9 probes at the same magnitudes**, with two left-edge probes non-convergent at `dps = 60` up to `n = 96` (`0.14 - 3.5035i`: `6.963e-01`; `0.14 - 7i`: `5.620e+00`). **Measured mechanism: a migrating pseudo-spectrum adjacent to `Re Omega ~ 0` — the in-box pencil eigenvalue count grows `2 -> 9` as `n` goes `32 -> 80`, and only `Omega = 1.853655 - 1.007257i` is `n`-stable.** **The monotonicity signature is retracted: it is FALSE in `|Im Omega|` and in `|Im Omega|/Re Omega` alike** (`0.14 - 0.007i` gives `1.286e-08` while `14 - 3.5035i` gives `1.006e-11`), and the `~1/|Im Omega|` layer width is **dimensionally inverted** (`e^{-2i Omega/A}` transitions at `A ~ 2|Im Omega|`, width `∝ |Im Omega|`). (iii) **C1's high-`ell` rows — REAL INSTRUMENT DEGRADATION.** The two-unfrozen-parameters statement is true but **not** the cause: review swept `n` from `32` to `128` and both precisions — **the winding never equals the closed-form count at any order, higher `n` is monotonically worse** (spurious in-box zeros `4 -> 32` at `ell = 6` while true roots drop `2 -> 1`), and **at `ell = 6`, `n = 40` the pencil finds `8` in-box eigenvalues against `3` closed-form roots and the winding correctly counts all `8`** — the argument principle is faithfully counting zeros of `det M_n`, five of them spurious; **mp reproduces the same zeros.** **Same phenomenon class as (ii): two gates, one instrument defect.** **Rule 11 untouched — nothing was retuned, no parameter selected post-hoc, and the negative stays banked.**
- **★ ALL FOUR PHYSICS BINS REPORT `N/A — not adjudicated`.** Frozen precedence `BIN-F-SOLVER` > `BIN-F-PROFILE` > `BIN-F-NOPOLE` > `BIN-1/2/3/4` fired at the first term. **No frozen criterion was dropped, widened or re-defined; no verdict language is used about any bin.** `BIN-F-PROFILE` did not fire (no canonical-input contradiction on the domain); `BIN-F-NOPOLE` did not fire (a pole was located).
- **⚑ THREE CODE-CORRECTNESS REPAIRS, DISCLOSED — and the verdict was already fixed BEFORE any of them.** (B1) the extended-precision polish was running mp arithmetic on a **double-precision operator**, so it converged to the exact root of the *wrong* operator (C1 floored at `2.73e-10`…`8.67e-08`, C3 at `3.96e-12`); (B2) C9's RHS was not scaled with the row equilibration, making the **boundary datum resolution-dependent** so the BVP changed with `n` (measured `5.65`, meaningless); (B3) the polished root and the closed-form reference were cast to Python `complex`, truncating at `~1e-16` and making the frozen `1e-20` **unreachable by construction**. All three are the code failing to do what the prereg SAYS; no frozen criterion was touched. **FT-B's vacuity is structural and precedes all numerics, so `SOLVER-NOT-CERTIFIED` was determined at first run — the repairs could not and did not rescue the certification.** They were made because a report conflating *"my code had a bug"* with *"the method cannot do this"* is worth nothing to the successor. Run-1 (pre-repair) is on the record: digest `8ed2738391046900`, failed `C1, C3, C9`. **C3 moved FAIL → PASS purely by the B1 repair.** — ⚑ **ADDED 2026-08-03 (post-review, WARN 9): THE RUN-1 RECORD IS SELF-REPORTED AND UNREPRODUCIBLE FROM ANY COMMITTED ARTIFACT.** No pre-repair driver, JSON or log was committed, so run-1's digest `8ed2738391046900`, its runtime `408.73` s, and its pre-repair gate values are prose disclosures that a reviewer **cannot** re-derive from this repository. **Review was able to independently reconstruct exactly one of them** — B2's `5.65`, reconstructed at `5.64963` by re-deriving the unequilibrated-RHS bug from the shipped code. **This is a standing limitation of the prose-disclosed repair-record pattern**, not a defect specific to this lane: the pattern discloses honestly but leaves the disclosure unauditable. **Surfaced, not repaired here; no artifact is back-dated and no history is rewritten.**
- 🔴 **CORRECTED 2026-08-03 (post-review, PR #854 WARN 7). Original text, verbatim:** *"**★ `nu_vac` CANCELLATION IS EXACT TO THE LAST BIT.** C5 measures `Q` spread and `Omega` spread of **`0.0`** across `x_sat in {5, 7, 11}` — not `1e-8`, not `1e-15`, **zero** — …"* **Corrected: the spread is BELOW THE DRIVER'S DOUBLE-PRECISION REPORTING FLOOR (complex cast at `research/drivers/coldq_pole_v2.py:612`), NOT identically zero.** Review re-measured at mp level, `dps = 50`: `Q` spread `4.466e-47`, `|Omega|` spread `1.381e-46` — **non-zero.** **The gate outcome is unaffected** (frozen tol `1e-9`; C5 passes either way), and FT-E fires at `6.014e-07`, proving the gate is live rather than dead. **#845 measured `1.1058e-08` here and failed its own `1e-9`.** The v2.1 prereg predicted this **in advance and structurally**: the compactified radial coordinate **is** the Axiom-4 amplitude `A = r_sat/r`, and the eigenvalue variable is `Omega = omega*r_sat`, so `r_sat` appears nowhere else in the operator. **The honesty upgrade was also declared in advance** — the prereg states that this makes C5 near-dead as a numerical gate and freezes FT-E to supply its fireability.
- **★ THE CROSS-LANE AGREEMENT, REPORTED AND EXPLICITLY NON-GATING.** Two instruments **different in kind** — #845's real-axis asymptotic matching with subdominant-coefficient extraction, and v2.1's compactified spectral with the outgoing wave divided out in closed form — agree on the scale-free eigenvalue to **`6.80e-07`** (`Omega = 1.8536552108408788 - 1.0072567831433188i` vs `1.8536565650028993 - 1.00725725871003i`) and on `Q` to **`-2.58e-07`** (`0.9201502744197103` vs `0.9201505121823758`). **Recorded as the single most useful pointer for the successor — and NOT a verdict, NOT a bin, NOT a certification: both lanes are `SOLVER-NOT-CERTIFIED`, and two uncertified instruments agreeing certifies neither.**
- **★ FLAG-2 IS VINDICATED BY MEASUREMENT.** The successor brief originally asked that #845's FT-6 `0.21729` spin-1 break be reproduced **as a gate**. The v2.1 prereg **declined**, freezing instead that `the #845 FT-6 value 0.21729 is NOT-ADJUDICATED prior-lane data produced by a SOLVER-NOT-CERTIFIED instrument and therefore may not gate this lane`, with the discrimination gated on its own thresholds (C6/FT-F) and `0.21729` reported as non-gating corroboration. **v2.1 measures `0.48017`. They do not agree — exactly as predicted in advance, because the two lanes evaluate the Rayleigh quotient on different objects. Had it been a gate it would have fired on a difference of setup rather than of physics.**
- **⚑ FLAG-1 — a review assertion FALSIFIED, surfaced not reframed.** The frozen `Q_GR = 2.1002135791366907` is the programmatic `KERR_QNM[0.00]` value (`research/2026-07-20_v1-spin-mapping-adjudication_rerun.py:51`), unchanged from v1/v2. A review claim that *"nothing in the corpus reads 2.0994"* is **false**: verified three independent ways, `research/2026-07-30_qlaw-derivation_scoping.md:401` reads `$Q_{GR}(0) = 2.0994$` verbatim, derived from the rounded prose pair at `:398`–`:402`. The gap is `0.04%`, two orders below BIN-2's tightest boundary, so it cannot flip any bin. **Routed to the auditor lane as a corpus-precision question; not repaired here.**
- **OTHER GATES.** C4 argument-principle consistency PASSES with winding `2.0/2.0/2.0` equal to the located count, **and the count-vs-box-width family returns `0, 0, 1, 2, 3, 3` against run-time closed-form contents `0, 0, 1, 2, 3, 3` — content SATURATES at `3` while the box width doubles from `8` to `16`, over a `32x` width span, decisively excluding the count-proportional-to-width artifact class**. C6 spin-2 Rayleigh residual `5.741e-12`; spin-1 weighting break `0.48017`; spin-1 wall condition shifts the fundamental by `0.28424`. C7 Ax-3: all `63` closed-cavity eigenvalues real to `1.785e-27`, FT-G detects a smuggled `Im(mu)/Re(mu) = 1e-3` at `5.000e-04`. C10 monitor: max `rho_out = 42.804`, margin over the derived bound `36.37` orders. C11 operator identity `8.972e-16`. — ⚑ **DE-WEIGHTED 2026-08-03 (post-review), TWO OF THESE PASSES.** **C4's PASS is an accident of the frozen order**: the winding over the frozen physics rectangle is `2.0/2.0/2.0` **only at `n <= 48`**; at `n = 56/64/80` it reads `3/5/3`, `3/4/14`, `12/4/11`. C4 required **sampling**-stability, not **`n`**-stability, so it passed at `N_PRIMARY = 48` and **would have failed at `56` and `64` — two of the three orders C3's own independence set uses.** A v2.2 C4 must require `n`-independence of the count (**not written here**). **C10 measured the wrong quantity's margin**: its derived left-edge bound `6e-38` (`research/2026-08-03_coldq-pole-v2.1_prereg-FROZEN.md:294`) is **~`37` orders off the binding constraint** — the operative left-edge mechanism is **spectral**, at `O(0.1)` in `|Omega|`, right at the rectangle's frozen `|Omega|_min = 0.1402`, **not arithmetic precision.** Both gates are banked as they stand and neither is re-adjudicated; **their margins are not evidence that the instrument's edges are sound.** The box-width artifact kill (`0, 0, 1, 2, 3, 3` over a `32x` width span) **stands** — it excludes one artifact class, not the one that bit this instrument.
- **NOT-ADJUDICATED DIAGNOSTICS (no bin, no claim, no solidity).** Least-damped physical root, zero free parameters: `Omega = 1.8536552108408788 - 1.0072567831433188i`, `omega_R M_g = 0.2648078872629827`, `omega_I M_g = 0.14389382616333127`, `Q = 0.9201502744197103`. Stated but **not adjudicated**: `-29.13%` vs the frozen GR cold comparator `0.37367`; `-27.91%` vs the corpus shortcut `18/49`; `-56.19%` vs `Q_GR`; `-53.99%` vs the Op21 convention `Q = ell = 2`, nearer the convention (`1.0798`) than GR (`1.1801`). **The advance identity held**: `k_0*r_sat = x_sat*omega_R M_g` identically, so the 9/7-above-cutoff test **IS** the `omega_R` vs `18/49` comparison re-expressed — both read `-27.91%`, **one result not two**, recorded before any number existed. **A second located root was EXCLUDED by the frozen physical-vs-artifact criterion** and banked as a discretization artifact; **no overtone ratio is reported.** Localization: both frozen measures agree exactly at the outer window edge (`u_energy = u_kinetic = 2.0000000000000004`, `interior_max = false`) — the reserved `BIN-3-MONOTONE` reading, dominated by the generic outward `exp(|omega_I| r)` growth of any QNM. `ell`-ladder tagged `DIAGNOSTIC - no bin, no verdict; FORK-12 is unanswered`; **unlike #845's, all four rows are monotone** (no seed-hit-noise signature).
- **FLAG-DON'T-FIX — routed, nothing repaired.** (1) 🔴 **CORRECTED 2026-08-03. Original text, verbatim:** *"the three defective gate specifications → **v2.2, new prereg, new version number, own verification chain (Rule 12: the slot is not refilled)**; candidate replacements are stated so the successor need not rediscover them and are **explicitly not adopted here**."* **Corrected: ONE defective gate specification (FT-B) and TWO instrument defects (C9, C1-high-`ell`).** **Two of the three stated candidate replacements are STRUCK as MEASURED-REFUTED:** *"a C9 replacement that probes the **homogeneous outgoing** solution"* — review built it and it reproduces the defect; and *"a frozen control order plus an mp winding at high `ell`"* — no order passes and mp reproduces the same zeros. **The FT-B candidate (a gauge mutation targeting a row that actually carries `lambda`) stands and was demonstrated to fire at `6.4e-02`.** **What v2.2 must decide is an instrument-SCOPE question, routed to Grant and NOT resolved here:** move the frozen rectangle's left edge **with a derivation**, or restrict the counting region — and separately, whether the successor certifies **the located root** or **the rectangle**. **Rule 12 still governs: the slot is not refilled, and no v2.2 prereg is written in this repair.** (2) FLAG-1's corpus-precision question. (3) **FLAG-3: I7 (the reflectionless Regime-I far-field port) is ASSUMED, not tested** — the method divides out the corresponding analytic factor, so if the substrate carries any far-field reflector every number here is wrong in the same direction. (4) FLAG-4: #814 CF-7's `rho` naming gap at `vol3/claim-quality.md:122`, untouched. (5) FLAG-5: the `15.000` characterisation was corrected **at source** in the v2.1 prereg to the **phase-rate** form (`d(arg N)/domega_R ~ 2R_match - a`, count `~ rate*delta-omega_R/2pi`, a **box-width coincidence, not a fixed signature**) — **cited from the concurrent #845 audit, not re-derived, and NO v2.1 gate depends on it**; the artifact class was excluded empirically by C4's width family instead. Correcting #845's own result-doc wording is that lane's move. (6) **the B1-ratified `Q = ell` anchor is untouched** — no bin was adjudicated, so there is not even a disagreement on the record to route; `qnm-quality-factor.md`, `op21-multi-mode-mode-counting.md` and `regime-eigenvalue-method.md` are byte-untouched.
- **⚑ ADDED 2026-08-03 (post-review) — THE RULE-11 OBSERVATION, RECORDED BECAUSE IT IS ADVERSE TO THIS LANE.** The frozen prereg pre-committed, at `research/2026-08-03_coldq-pole-v2.1_prereg-FROZEN.md:492` (§9 item 10), to reading a C9 failure one specific way: *"if the graded solve nonetheless fails C9, that is an honest SOLVER-NOT-CERTIFIED and routes to a successor — it will NOT be retuned."* **After the result was seen, the attribution moved — to "the gate is mis-specified; it tests an object the method never uses."** **The letter of Rule 11 was kept: the verdict was correctly banked as `SOLVER-NOT-CERTIFIED`, no tolerance was widened, no criterion was dropped, and no bin was adjudicated.** **What moved was the ATTRIBUTION, post-result, in the exculpatory direction** — from *the instrument is not certified* toward *the gate was wrong to ask*. **That is the failure mode Rule 11 exists to catch, arriving one level up from where it usually does: not in the numbers, in the story told about them.** **This repair restores the pre-committed reading.** Recorded so the pattern is visible to the next lane that is tempted by it.
- **★ WHAT STANDS AFTER THE CORRECTION.** `SOLVER-NOT-CERTIFIED` **stands**, now for honest reasons — the certification never moved, only the attribution, and it moved **toward** the instrument. The **entry-ticket pass stands**: counts `1`/`1`/`2`, windings `1.0`/`1.0`/`2.0` at all three samplings, locations to `2.971e-44`/`3.538e-46`/`5.778e-42` against a frozen `1e-20`, on the exact control #845's FT-5 failed at `15.000`. **C2, C3, C5, C6, C7, C8 and C11 stand as passes** (C5 with its arithmetic wording corrected; its gate outcome is unaffected). **The located pole `Omega = 1.853655 - 1.007257i` stands as an uncertified diagnostic** — stable to **12 digits across `n = 48 -> 80` (~10 digits from `n = 32`), the `n = 80` endpoint from the review's sweep** (🔴 **RESTATED 2026-08-03 as over-precise; original, verbatim:** *"stable to 12 digits across `n = 32 -> 80`"*. The shipped JSON's C3 sweep moves in the tenth significant digit between `n = 32` and `n = 64`, so `n = 32` carries ~10 digits and 12 holds only from `n = 48`; and the sweep stops at `n = 64`, so `80` is the review's pencil sweep, not this JSON. **The `n`-stability conclusion is unchanged.**) and the only in-box eigenvalue that is — as does the `6.80e-07` agreement with #845; **both lanes are `SOLVER-NOT-CERTIFIED` and two uncertified instruments agreeing certifies neither.** **C4 and C10 are banked with caveats.** **THE OPEN DECISIONS ARE GRANT'S, and are not resolved here:** (i) where the honest left edge of the frozen rectangle is, with a derivation; (ii) whether a successor certifies **the root** or **the rectangle** — the pole is `n`-stable while the rectangle's count is not, so these are different instruments with different burdens.
- **Battery.** `make verify` exit 0. **Determinism: two full driver runs produced byte-identical shipped objects apart from `_runtime_sec`, digest `e953f8882a4e675e`**, runtimes `445.26 s` / `450.74 s`, inside the frozen `3600 s`. The number-check registers every numeral in the result doc against the shipped JSON (at the shipped commit `bdcfa678`: 154 tokens, 131 registered or recomputed from the JSON, 31 allow-listed with reasons, **0 unregistered**; **after the 2026-08-03 post-review correction: 176 tokens, 143 registered, 47 allow-listed, 0 unregistered** — every added allow-list entry names the review as its source and states that the value is NOT from the shipped JSON) and is wired into `make verify` as its **own** target — deliberately not appended to `verify-lane-number-checks`, because **PR #845 has an open unmerged edit to that three-line recipe** and a second addition would collide. (⚑ **Dated note, 2026-08-03 post-review:** #845 has since **MERGED**, so the collision this avoided is moot. The own-target decision is left as made and as recorded; it is not re-litigated retroactively.) `_runtime_sec` is NON-REGISTRABLE (the #801 R3/WARN-4 lesson) and `main()` refuses any attempt to register or allow-list it. ruff clean; docket lint clean; pure-corpus.
- **⚑ ADDED 2026-08-03 (post-review, WARN 8) — A FALSE VALIDATION CLAIM IN THE PR BODY, CORRECTED THERE.** The PR body asserted *"zero overlap trial-diffed against all nine open branches"*. **That was false: `Makefile` was shared with PR #845**, which had an open edit to the same file. **The claim is now moot** — #845 has since MERGED — **but it was not true when it was made**, and a reader was entitled to the mitigation rather than a blanket denial of overlap. **Corrected in the PR body to the mitigation actually taken:** the number-check was wired as its **own** `make` target rather than appended to `verify-lane-number-checks`, precisely so the shared-file edit could not collide, and the branch merges clean. **Recorded here because PR-body text is not under version control and this docket fragment is.**
- **Lane mechanics.** Self-isolated throwaway worktree off `origin/main` @ `583d43dd`; branch `research/coldq-pole-v2`; **two frozen prereg commits, each ALONE and pushed before any number**; PR opened `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. **Not merged, not retitled.** The predecessor's worktree and branch were never touched.
