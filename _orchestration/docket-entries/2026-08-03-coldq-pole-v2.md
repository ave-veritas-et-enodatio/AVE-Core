### ENTRY 2026-08-03-coldq-pole-v2 (2026-08-03): implementer — the cold-Q pole derivation v2/v2.1 (compactified hyperboloidal spectral successor to #845): the ENTRY TICKET IS PASSED by ~22 orders, and the lane is still SOLVER-NOT-CERTIFIED because three of its own gate SPECIFICATIONS were defective

- **Provenance + scope fence.** Grant's GO 2026-08-03, verbatim `[sic]`: *"Go on cold-Q"*. Successor to PR #845 (`research/coldq-pole-derivation`, unmerged, under concurrent adversarial audit), which returned `SOLVER-NOT-CERTIFIED` with the mechanism diagnosed as an asymptotic far-field series plus an `~exp(2|Im omega| R_match)` subdominant-coefficient extraction on the real axis. Written against `origin/main` = `583d43dd`. **FROZEN-FIRST honoured TWICE: the v2 prereg was committed ALONE and pushed (`00724432`) before any driver code, and the v2.1 prereg was committed ALONE and pushed (`7d8fe484`) before any number existed.** Deliverables: the two prereg files, [`research/2026-08-03_coldq-pole-v2.1_result.md`](../../research/2026-08-03_coldq-pole-v2.1_result.md), [`research/drivers/coldq_pole_v2.py`](../../research/drivers/coldq_pole_v2.py) + its results JSON + a gating number-check. **Engine `src/ave` BYTE-UNTOUCHED; zero `manuscript/` edits; no `clm-`/`def-` minted; no solidity changed; no falsification-ledger edit — regardless of outcome, exactly as fenced.**
- **★ PRE-MEASUREMENT VERSIONED SUPERSEDE (v2 → v2.1), and why it is NOT a retune.** The v2 prereg was frozen and pushed; review then found four gate-coverage holes; **no driver code had been written and no number produced**, so nothing was adjusted in light of a result. Rule 11 governs post-result retuning. v2 stays **byte-untouched as the record** (verified zero diff) and is superseded by a new version number with its own verification chain — the same move #845 → v2. v2.1 is **self-contained**, not incorporated by reference. Holes closed: a **derived left-edge bound** from the lane's own outflow-row mechanism (`rho_out = |C(1)|/|B(1)| -> l(l+1)/|Omega|`, giving `|Omega|_min >= 6e-38` at `dps=50` against the rectangle's actual `0.1402` — **cleared by 36 orders**, so the edge does NOT move and is certified by a new monitor gate instead); **graded-representation certification at the rectangle's corners** (new C9); **control reach extended** from `|Omega| = 7.2` to `17.67` to cover the rectangle's `15.65` with an exact closed-form reference; and a **count-vs-box-WIDTH scaling family** (into C4/FT-D). Plus new C11, an **operator-identity** gate. Gates went `C1..C8` → `C1..C11`, self-tests `FT-A..FT-G` → `FT-A..FT-J`; every carried tolerance identical to or stricter than v2's.
- **★ THE HEADLINE — THE ENTRY TICKET IS PASSED, DECISIVELY.** C1's low-`ell` rows are the **exact** control #845's FT-5 failed at `15.000`: same box, same closed-form reference. **v2.1 returns the closed-form counts `1`/`1`/`2` for `ell = 1,2,3`, an argument-principle winding of exactly `1.0`/`1.0`/`2.0` identically stable at 200/400/800 contour samplings, and locates the roots to `2.971e-44`, `3.538e-46`, `5.778e-42` against a frozen `1e-20` — inside the gate by ~22 orders of magnitude.** The compactified formulation removes #845's disease by construction (no matching radius, no asymptotic series, no shooting, no subdominant extraction) and the control now measures that.
- **★ AND THE LANE IS STILL `SOLVER-NOT-CERTIFIED` — 2 of 11 gates failed (C1, C9), 1 of 10 self-tests did not fire (FT-B) — with ALL THREE failures in this prereg's own gate SPECIFICATIONS, not in the instrument.** (i) **FT-B is structurally vacuous**: it mutates by applying the gauge `lambda` to the interior but not the wall-BC row, and the graded wall row is the exact SHORT `dpsi/deta = 0`, which carries **no `lambda` and no `Omega`** — nothing for the mutation to break (measured `1.83e-14`). (ii) **C9 probes an object the method never uses**: a *forced* solution `M(Omega) psi = b` is not guaranteed analytic and develops a boundary layer of width `~1/|Im Omega|` at the compactified infinity; the measured probe table degrades monotonically with `|Im Omega|` (`6.997e-14` at `|Im Omega| = 0.007` → `1.899e+01` at `7.0`), while the **homogeneous outgoing eigenfunction the method actually uses is analytic by construction and converges — C2 `3.327e-14`, C3 `8.332e-14` against `1e-12`.** (iii) **C1's high-`ell` rows depend on two parameters the prereg never froze** — the control Chebyshev order and the winding's precision; the *mp location* path stays perfect there (`5.177e-38`, `1.577e-38`) while the *double-precision* seeding and winding break over a box reaching `|Omega| ~ 22`. **Selecting those parameters now would be exactly the post-hoc choice Rule 11 forbids. Nothing was repaired; the negative is banked and routed to a v2.2 successor.**
- **★ ALL FOUR PHYSICS BINS REPORT `N/A — not adjudicated`.** Frozen precedence `BIN-F-SOLVER` > `BIN-F-PROFILE` > `BIN-F-NOPOLE` > `BIN-1/2/3/4` fired at the first term. **No frozen criterion was dropped, widened or re-defined; no verdict language is used about any bin.** `BIN-F-PROFILE` did not fire (no canonical-input contradiction on the domain); `BIN-F-NOPOLE` did not fire (a pole was located).
- **⚑ THREE CODE-CORRECTNESS REPAIRS, DISCLOSED — and the verdict was already fixed BEFORE any of them.** (B1) the extended-precision polish was running mp arithmetic on a **double-precision operator**, so it converged to the exact root of the *wrong* operator (C1 floored at `2.73e-10`…`8.67e-08`, C3 at `3.96e-12`); (B2) C9's RHS was not scaled with the row equilibration, making the **boundary datum resolution-dependent** so the BVP changed with `n` (measured `5.65`, meaningless); (B3) the polished root and the closed-form reference were cast to Python `complex`, truncating at `~1e-16` and making the frozen `1e-20` **unreachable by construction**. All three are the code failing to do what the prereg SAYS; no frozen criterion was touched. **FT-B's vacuity is structural and precedes all numerics, so `SOLVER-NOT-CERTIFIED` was determined at first run — the repairs could not and did not rescue the certification.** They were made because a report conflating *"my code had a bug"* with *"the method cannot do this"* is worth nothing to the successor. Run-1 (pre-repair) is on the record: digest `8ed2738391046900`, failed `C1, C3, C9`. **C3 moved FAIL → PASS purely by the B1 repair.**
- **★ `nu_vac` CANCELLATION IS EXACT TO THE LAST BIT.** C5 measures `Q` spread and `Omega` spread of **`0.0`** across `x_sat in {5, 7, 11}` — not `1e-8`, not `1e-15`, **zero** — while FT-E fires at `6.014e-07`, proving the gate is live rather than dead. **#845 measured `1.1058e-08` here and failed its own `1e-9`.** The v2.1 prereg predicted this **in advance and structurally**: the compactified radial coordinate **is** the Axiom-4 amplitude `A = r_sat/r`, and the eigenvalue variable is `Omega = omega*r_sat`, so `r_sat` appears nowhere else in the operator. **The honesty upgrade was also declared in advance** — the prereg states that this makes C5 near-dead as a numerical gate and freezes FT-E to supply its fireability.
- **★ THE CROSS-LANE AGREEMENT, REPORTED AND EXPLICITLY NON-GATING.** Two instruments **different in kind** — #845's real-axis asymptotic matching with subdominant-coefficient extraction, and v2.1's compactified spectral with the outgoing wave divided out in closed form — agree on the scale-free eigenvalue to **`6.80e-07`** (`Omega = 1.8536552108408788 - 1.0072567831433188i` vs `1.8536565650028993 - 1.00725725871003i`) and on `Q` to **`-2.58e-07`** (`0.9201502744197103` vs `0.9201505121823758`). **Recorded as the single most useful pointer for the successor — and NOT a verdict, NOT a bin, NOT a certification: both lanes are `SOLVER-NOT-CERTIFIED`, and two uncertified instruments agreeing certifies neither.**
- **★ FLAG-2 IS VINDICATED BY MEASUREMENT.** The successor brief originally asked that #845's FT-6 `0.21729` spin-1 break be reproduced **as a gate**. The v2.1 prereg **declined**, freezing instead that `the #845 FT-6 value 0.21729 is NOT-ADJUDICATED prior-lane data produced by a SOLVER-NOT-CERTIFIED instrument and therefore may not gate this lane`, with the discrimination gated on its own thresholds (C6/FT-F) and `0.21729` reported as non-gating corroboration. **v2.1 measures `0.48017`. They do not agree — exactly as predicted in advance, because the two lanes evaluate the Rayleigh quotient on different objects. Had it been a gate it would have fired on a difference of setup rather than of physics.**
- **⚑ FLAG-1 — a review assertion FALSIFIED, surfaced not reframed.** The frozen `Q_GR = 2.1002135791366907` is the programmatic `KERR_QNM[0.00]` value (`research/2026-07-20_v1-spin-mapping-adjudication_rerun.py:51`), unchanged from v1/v2. A review claim that *"nothing in the corpus reads 2.0994"* is **false**: verified three independent ways, `research/2026-07-30_qlaw-derivation_scoping.md:401` reads `$Q_{GR}(0) = 2.0994$` verbatim, derived from the rounded prose pair at `:398`–`:402`. The gap is `0.04%`, two orders below BIN-2's tightest boundary, so it cannot flip any bin. **Routed to the auditor lane as a corpus-precision question; not repaired here.**
- **OTHER GATES.** C4 argument-principle consistency PASSES with winding `2.0/2.0/2.0` equal to the located count, **and the count-vs-box-width family returns `0, 0, 1, 2, 3, 3` against run-time closed-form contents `0, 0, 1, 2, 3, 3` — content SATURATES at `3` while the box width doubles from `8` to `16`, over a `32x` width span, decisively excluding the count-proportional-to-width artifact class**. C6 spin-2 Rayleigh residual `5.741e-12`; spin-1 weighting break `0.48017`; spin-1 wall condition shifts the fundamental by `0.28424`. C7 Ax-3: all `63` closed-cavity eigenvalues real to `1.785e-27`, FT-G detects a smuggled `Im(mu)/Re(mu) = 1e-3` at `5.000e-04`. C10 monitor: max `rho_out = 42.804`, margin over the derived bound `36.37` orders. C11 operator identity `8.972e-16`.
- **NOT-ADJUDICATED DIAGNOSTICS (no bin, no claim, no solidity).** Least-damped physical root, zero free parameters: `Omega = 1.8536552108408788 - 1.0072567831433188i`, `omega_R M_g = 0.2648078872629827`, `omega_I M_g = 0.14389382616333127`, `Q = 0.9201502744197103`. Stated but **not adjudicated**: `-29.13%` vs the frozen GR cold comparator `0.37367`; `-27.91%` vs the corpus shortcut `18/49`; `-56.19%` vs `Q_GR`; `-53.99%` vs the Op21 convention `Q = ell = 2`, nearer the convention (`1.0798`) than GR (`1.1801`). **The advance identity held**: `k_0*r_sat = x_sat*omega_R M_g` identically, so the 9/7-above-cutoff test **IS** the `omega_R` vs `18/49` comparison re-expressed — both read `-27.91%`, **one result not two**, recorded before any number existed. **A second located root was EXCLUDED by the frozen physical-vs-artifact criterion** and banked as a discretization artifact; **no overtone ratio is reported.** Localization: both frozen measures agree exactly at the outer window edge (`u_energy = u_kinetic = 2.0000000000000004`, `interior_max = false`) — the reserved `BIN-3-MONOTONE` reading, dominated by the generic outward `exp(|omega_I| r)` growth of any QNM. `ell`-ladder tagged `DIAGNOSTIC - no bin, no verdict; FORK-12 is unanswered`; **unlike #845's, all four rows are monotone** (no seed-hit-noise signature).
- **FLAG-DON'T-FIX — routed, nothing repaired.** (1) the three defective gate specifications → **v2.2, new prereg, new version number, own verification chain (Rule 12: the slot is not refilled)**; candidate replacements are stated so the successor need not rediscover them and are **explicitly not adopted here**. (2) FLAG-1's corpus-precision question. (3) **FLAG-3: I7 (the reflectionless Regime-I far-field port) is ASSUMED, not tested** — the method divides out the corresponding analytic factor, so if the substrate carries any far-field reflector every number here is wrong in the same direction. (4) FLAG-4: #814 CF-7's `rho` naming gap at `vol3/claim-quality.md:122`, untouched. (5) FLAG-5: the `15.000` characterisation was corrected **at source** in the v2.1 prereg to the **phase-rate** form (`d(arg N)/domega_R ~ 2R_match - a`, count `~ rate*delta-omega_R/2pi`, a **box-width coincidence, not a fixed signature**) — **cited from the concurrent #845 audit, not re-derived, and NO v2.1 gate depends on it**; the artifact class was excluded empirically by C4's width family instead. Correcting #845's own result-doc wording is that lane's move. (6) **the B1-ratified `Q = ell` anchor is untouched** — no bin was adjudicated, so there is not even a disagreement on the record to route; `qnm-quality-factor.md`, `op21-multi-mode-mode-counting.md` and `regime-eigenvalue-method.md` are byte-untouched.
- **Battery.** `make verify` exit 0. **Determinism: two full driver runs produced byte-identical shipped objects apart from `_runtime_sec`, digest `e953f8882a4e675e`**, runtimes `445.26 s` / `450.74 s`, inside the frozen `3600 s`. The number-check registers every numeral in the result doc against the shipped JSON (154 tokens: 131 registered or recomputed from the JSON, 31 allow-listed with reasons, **0 unregistered**) and is wired into `make verify` as its **own** target — deliberately not appended to `verify-lane-number-checks`, because **PR #845 has an open unmerged edit to that three-line recipe** and a second addition would collide. `_runtime_sec` is NON-REGISTRABLE (the #801 R3/WARN-4 lesson) and `main()` refuses any attempt to register or allow-list it. ruff clean; docket lint clean; pure-corpus.
- **Lane mechanics.** Self-isolated throwaway worktree off `origin/main` @ `583d43dd`; branch `research/coldq-pole-v2`; **two frozen prereg commits, each ALONE and pushed before any number**; PR opened `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. **Not merged, not retitled.** The predecessor's worktree and branch were never touched.
