### ENTRY 2026-07-31-engine-flag-overtone-fixes (2026-07-31): implementer — two mechanical dated corrections surfaced by the #814 compact audit: a STALE engine-FLAG referent, and the $\ell$-vs-overtone index cross-wire

- **Scope.** Two independent mechanical corrections, one commit each, both on already-merged files. **Nothing is adjudicated, canonized, retracted or re-scoped.** No `clm-`/`def-`/`exp-`/`sup-`/`ilk-` id is minted or moved; no eigenvalue, coefficient or claim content is altered. Provenance for both: the **#814 compact audit (2026-07-31)**.

- **★ITEM 1 — stale FLAG referent in the engine (`cosserat_field_3d.py`, COMMENT-ONLY).** The `_s11_density` Op14 impedance block carried a `⚑ FLAG (2nd-impedance conflict, task #12)` whose **third clause** cited *"`operators.md:54` (Z0/√S) vs `k4-tlm-lensing-validation.md:22` (Z0/S^{1/4})"*. That referent went stale on **2026-07-14**: `manuscript/ave-kb/vol3/gravity/ch02-general-relativity/k4-tlm-lensing-validation.md:22` now reads $Z_{\text{local}}(r) = Z_0/S^{1/2} = Z_0/\sqrt{S}$, corrected under the **KEEP-BOTH register-correction banner at `:25`–`:29`** (*"Previously stated as $Z_{\text{local}} = Z_0/S^{1/4}$ — the legacy register that let the Family-E $S^{1/4}=(1-A^2)^{1/8}$ exponent ride in, un-propagated"*). Both cites re-verified on-branch: `operators.md:54` is the Op14 row `$Z_{eff} = Z_0/\sqrt{S}$`; `k4…:22` is the corrected form. **The two now AGREE** — so the FLAG's third clause named a disagreement that no longer exists.
  - **Repair (comment-only):** the third clause is **struck and dated**; the rest of the FLAG is kept **intact**. ★**The FLAG is NOT declared resolved.** The first clause stands verbatim in substance — this OPEN $Z=Z_0/\sqrt{S}$ still coexists with the live-wall SHORT $Z=Z_0\cdot\sqrt{S_\mu/S_\varepsilon}$ **in this same file** — and the comment still says the exponent/sign reconciliation across Op14 forms *"is still a separate physics-review item."* The conflict is **NARROWER, not closed**; per the #814 audit it is not fully resolved. The new text also points at the k4 leaf's **own still-open downstream item** (`:30`–`:35`: with $Z_0/\sqrt{S}$ the leaf's $c_{\text{local}} = c_0\sqrt{Z_0/Z_{\text{local}}}$ yields $c_0 S^{1/4}$, which still differs from the canonical Op16 $c_{\text{shear}} = c_0\sqrt{S}$).
  - **Trailing-cite disambiguated (2026-07-31, #816 compact audit finding F15).** The comment's closing pointer read *"so it AGREES with `operators.md:54` (see `:30-35`)"* — a bare `:30-35` immediately after an `operators.md` cite, which reads as *operators.md*`:30-35`. It means the **k4 leaf's** own open downstream item, so it now reads *"(see `k4-tlm-lensing-validation.md:30-35`)"*. Adding the 28-char filename inside the **5-line / 2444-line** envelope required re-flowing the block; four purely non-substantive contractions absorbed it — *"and the exponent/sign"* → *"; the exponent/sign"*, *"(see the task-12 PR FLAGs)"* → *"(task-12 PR FLAGs)"*, *"The FLAG's THIRD clause"* → *"The THIRD clause"*, *"— it was corrected to"* → *"— corrected to"*. **No clause, cite, date or scope word is dropped**; the FLAG is still NOT declared resolved. Block max line length **119 ≤ 120** (`ruff` line-length), so **no new `E501`** — `ruff` on the file still reports exactly the one pre-existing `E741` at `:2006`.
  - **★AST-EQUALITY HELD (mandatory gate, machine-checked).** `ast.dump(ast.parse(...))` of the edited file is **byte-identical** to `ast.dump(ast.parse(git show origin/main:…))` — SHA-256 of both dumps `43424d24eca45008…`. The edit is comments only. ★Checked **tree-wide**, not just on the one file: every `.py` under `src/ave/` was AST-diffed against its `origin/main` blob — **0 divergent files**. This branch leaves `src/ave/` AST-identical to `origin/main` end-to-end.
  - **★RECIPROCAL-CITE PRESERVED (line-count discipline).** The k4 leaf's own banner at `:29` cites *"`cosserat_field_3d.py:425`, `Z_eff = 1/\sqrt{S}`"*. The replacement comment block was held to **exactly 5 lines** (419–423) so that **file line count is unchanged (2444 → 2444)** and **line 425 still reads `Z_eff = 1.0 / jnp.sqrt(S_safe)`** — the incoming cite does not break. Verified post-edit.
  - **Tests:** `src/tests/test_cosserat_field_3d.py`, `test_cosserat_master_equation_op14.py`, `test_cosserat.py` → **60 passed**. `ruff` on the file reports one pre-existing `E741` at `:2006` (`O = omega0[..., None]`), present **identically on `origin/main`** — untouched by this lane, not introduced here.

- **★ITEM 2 — the angular-index / overtone-index CROSS-WIRE (Rule 12 dated correction).** The Knot-Crossing ↔ Mode-Number isomorphism table's *"First excitation"* row glossed the BH-side cell as **"$\ell = 3$ (1st GW overtone)"**. That conflates two distinct indices:
  - **$\ell$ is the ANGULAR index.** The **overtone** index is the **radial node count** $n_r$. Canon, two cites re-verified this lane: `manuscript/ave-kb/vol2/quantum-orbitals/ch07-quantum-mechanics/radial-eigenvalue-solver.md:160` verbatim — *"where $n_r = n - l - 1$ is the number of radial nodes (Step 1(h)). This phase-matching condition IS Op6"*; and `manuscript/ave-kb/common/operators.md:189` verbatim — *"Op6 (eigenvalue target → radial nodes $n_r$), Op10 ($c=3$ invariant → angular nodes $l$)"*. (Note: the brief's *"Op6 at operators.md:189"* is the **§6 usage line**; the Op6 **table row** itself is at `operators.md:46`. Both verified; the `:189` line is the one that carries the $n_r$-vs-$l$ split, so it is the cite used.)
  - **$\ell = 3$ is a different angular multipole, not an overtone of $\ell = 2$.** The corpus already scopes the $Q = \ell$ claim to the **fundamental** on exactly this ground — `manuscript/ave-kb/vol3/claim-quality.md:205` verbatim: *"'$Q = \ell$' is the lattice-derived form; for higher modes ($\ell > 2$) this disagrees with GR overtone structure — the claim is the fundamental mode, not the full QNM spectrum."* So the gloss contradicted a scope the register already carries.
  - **Repair (re-cut 2026-07-31 under the #816 compact audit, finding F1).** The **first** cut wrote *"(angular multipole — **NOT** an overtone; the radial overtone index $n_r$ is a distinct object, Op6)"* into the **BH-side cell**. That killed the $\ell$-vs-overtone cross-wire but re-introduced a **milder cross-SECTOR** one: $n_r$/Op6 is an **ATOMIC-sector** object — *both* of its cites are atomic leaves (`radial-eigenvalue-solver.md:160`, `common/operators.md:189`) — a gravitational QNM overtone ($n\ge1$) is a **damping-ladder** label, not a bound-state radial node count, and the corpus's own ringdown lane says the gravitational sector supplies **no** overtone spectrum at all: `research/2026-07-20_ringdown-systematics_derivation.md:79` verbatim — *"the corpus derives `ω_R ∝ ℓ` and `Q=ℓ` only for the **fundamental** (n=0). It supplies **no** overtone spectrum."* **Current shape:** the in-cell parenthetical carries the **de-claim ONLY** — *"$\ell = 3$ (angular multipole — **NOT** an overtone)"* — and the $n_r$/Op6 identification lives in the **dated 🔴 note** behind an explicit **SECTOR TAG** ("in the ATOMIC sector the overtone-analog index is Op6's radial node count $n_r = n - l - 1$ … both cites are atomic-sector leaves, so $n_r$ is not an object of the BH column"), with the `:79` counter-cite quoted. ★**The rows' eigenvalue content is byte-untouched** — `$\ell = 2$`, `$\ell = 3$`, the $Q=\ell$ Stability row, the ladder row, `Trefoil $c=3$` / `Cinquefoil $c=5$`, and the existing B1 spin-scope and phase-space-qualifier banners are all unchanged. Only the parenthetical gloss moves.
  - **Sites repaired (3 — the complete mirror set for this gloss):** `manuscript/ave-kb/vol2/appendices/app-f-solver-toolchain/knot-mode-isomorphism.md:22`; `manuscript/ave-kb/common/solver-toolchain.md:440`; `manuscript/backmatter/05_universal_solver_toolchain.tex:535`.
  - ★**Disclosed deviation (tex mirror only, typographic).** The `l l l` tabular has no line-breaking column, so the whole table is one hbox. The first attempt — cell text *"(angular multipole, **not** an overtone)"* — pushed the tabular **5.98pt past `\textwidth`** (a NEW Overfull `\hbox` at lines 525–538, measured against an `origin/main` baseline build). Under the F1 re-cut the tex cell reads **"$\ell = 3$ (multipole, **not** an overtone)"**: the de-claim — the load-bearing half — is now *in the cell*, and only the adjective *"angular"* is dropped **in the tex mirror**, to stay inside `\textwidth`. Nothing is lost: the column header is *"BH (Cavity Mode)"*, the row two above reads *"Mode number $\ell$"*, and the `\noindent` note immediately after `\end{center}` spells out *"a **different angular multipole**"* plus the whole SECTOR TAG. **The two markdown mirrors carry the full "(angular multipole — NOT an overtone)".** **Verified after the re-cut:** `make vol0` **exit 0**, Overfull count **34 → 34** with an *identical* warning set (only two log line-numbers shift 577 → 588 from the longer note) — **zero new Overfull, zero LaTeX errors, zero undefined control sequences.** ★**Page count 136 → 138** — the SECTOR-TAG paragraph is genuinely new prose in the compendium; disclosed, not a regression.

- **★TWO-METHOD SWEEP (grep-completeness discipline; `git grep -n` **and** an independent `rg -n -i` filesystem pass, both at branch head).** Patterns: `overtone`, `1st GW`, `ell = 3`, `ℓ = 3`, `ℓ=3`.
  - `1st GW` → **exactly 3 hits**, all the isomorphism-table gloss, **all 3 repaired**. Both methods agree 3/3.
  - `ℓ = 3` / `ell = 3` → the 3 above plus **`manuscript/ave-kb/common/omega-freeze-cosmic-grain-cascade.md:127`**, which already reads *"$\ell = 3$ (octupole …) ↔ $\ell = 3$ QNM (subdominant)"* — **correct as written** (says *subdominant*, not overtone). **No edit.** Remaining hits are CMB-multipole and ringdown-research prose where $\ell=3$ is correctly the multipole.
  - `overtone` → 40 hits. All are either (a) correct usages of the **radial/QNM overtone** as a genuinely distinct object (the `research/2026-07-20_ringdown-systematics_*` set is explicit that *"the corpus derives $\omega_R \propto \ell$ and $Q=\ell$ only for the **fundamental** (n=0). It supplies **no** overtone spectrum"* — `_derivation.md:79`), (b) the Vol-9 A1-cavity spatial-FFT null (a different physical object), or (c) the **open** generation-as-overtone-vs-scaled-knot fork (`src/scripts/vol_2_subatomic/simulate_neutrino_oscillation.py:45-46`; fork recorded open at `research/2026-07-11_ringdown-nyquist-pi-register_framing.md:36`) — **out of scope, not touched.**

- **★FLAG-DON'T-FIX — a 4th mirror of the SAME cross-wire found in engine code, deliberately NOT edited.** `src/ave/axioms/scale_invariant.py:468` carries a docstring table row: `Overtone 1   3   3   First GW overtone` (in the `Domain / ℓ / Q / Physical meaning` table, between the `BH QNM  2  2  Fundamental GW ringdown (a* = 0 only)` row and the `Electron  n  n` row). This is the **identical index cross-wire** the three doc mirrors just corrected — the $\ell=3$ row is labelled an *overtone*. **Not repaired from this lane, for a stated reason:** the brief authorizes exactly one `src/ave` touch (ITEM 1) under a **mandatory AST-equality gate**, and a docstring edit is an AST change. Repairing it here would forfeit this branch's clean invariant that **`src/ave/` is AST-identical to `origin/main` end-to-end**. **Surfaced for orchestrator dispatch, not silently resolved.** Note the same file's `:452`–`:455` already scopes the cold $Q$ to the $a_*=0$ anchor and carries the `#808` open flag, so the fix is a one-row wording change in the same docstring, not a physics call.

- **Files:** `src/ave/topological/cosserat_field_3d.py` (comments only, AST-identical, line count unchanged); `manuscript/ave-kb/vol2/appendices/app-f-solver-toolchain/knot-mode-isomorphism.md`; `manuscript/ave-kb/common/solver-toolchain.md`; `manuscript/backmatter/05_universal_solver_toolchain.tex`; this docket fragment. **No other file touched. Mints ZERO ids.**

- **Lane-overlap check (coordination note, NOT a cite).** **Zero file overlap** with all three concurrent live branches, verified by `git diff --name-only origin/main...<branch>`: `research/qlaw-framing-walk` (2 files: `_orchestration/docket-entries/2026-07-31-qlaw-framing-walk.md`, `research/2026-07-31_qlaw-framing-challenge_walk.md`); `research/anisotropy-observable-scoping` (2 files: `_orchestration/docket-entries/2026-07-31-anisotropy-scoping.md`, `research/2026-07-31_anisotropy-observable_scoping.md`); `docs/verify-cite-false-negative-corrections` (no committed diff vs `origin/main`; its worktree carries one uncommitted modification, `research/2026-07-13_registers-walk_framing.md`). `manuscript/ave-kb/common/solver-toolchain.md` was last touched by **merged** PR #812 — no live branch touches it, so the `:440` edit lands on settled ground.

- **Battery:** `make verify` **exit 0**; `make verify-md-links` **gating errors 0** (kbleaf 1095 cites checked, gating 0, waived 1; warn-only 206 pre-existing); `make refresh-kb-metadata` **idempotent** (0 files rewritten); `verify-docket-keys` no new duplicate keys; **pure-corpus**; `pdflatex vol_0` clean (backmatter `05_universal_solver_toolchain.tex` is included via `manuscript/vol_0_engineering_compendium/chapters/_manifest.tex`); nearest pytest set 60 passed; AST-equality machine-verified.

- **Class:** mechanical dated corrections (stale cite referent + index cross-wire). Canonizes nothing; adjudicates nothing; adds no claim-graph edge. Provenance: #814 compact audit, 2026-07-31.
