# The coldQ status-note clause — RE-ISSUED (2026-08-05, execution record)

Lane: documentation / manuscript-reconciliation implementer. Branch `docs/coldq-clause-reissue`
off `origin/main` @ `d5a1b06b`. **One commit per file.** No number, verdict or ruling text is
transcribed into print from this record — every governing value was read at its source at write
time and cited by pointer.

## Why the clause is re-issued

The ringdown wave (PR #883) executed a ruled three-clause coldQ status note whose second clause
was staged as *"FORK-3(b) is owed an axial run."* That clause was already false when it printed:
PR **#876** merged the axial FORK-3(b) run into `main` at `773fe007` hours earlier. The wave
**printed truth-at-HEAD and surfaced the divergence** rather than folding it in silently
(`2026-08-05-ringdown-wave.md`, the staleness-flag section), and routed the question of whether
the ruled form should be formally re-issued. **Grant ruled: re-issue, with a pointer to the
kernel-collapse ruling record added.** This lane executes that ruling.

## Sites re-located by content (line numbers verified two-method at write time; not trusted from
any brief)

| # | Site | What the ringdown wave left there | What this lane wrote |
|---|---|---|---|
| 1 | `manuscript/vol_3_macroscopic/chapters/15_black_hole_orbital_resonance.tex` — the `Current status of the cold eigenvalue's derivation` block | the three clauses, already truth-at-HEAD on the axial run | `RE-ISSUED` stamp + the *why*; each clause restated **at its certification line**; clause (ii) rewritten to *not owed — it has HAD one*; kernel-collapse pointer paragraph with status carried |
| 2 | `manuscript/backmatter/07_universal_saturation_kernel.tex` — the `Current status of the 18/49 derivation` note | the compressed three clauses | same, compressed; clauses numbered (i)–(iii); kernel-collapse pointer with status carried; worked-in-full pointer to Vol 3 Ch 15 |
| 3 | `manuscript/ave-kb/common/wall-taxonomy.md` §10, the FLAG-W dissolution paragraph | the FORK-3(b) axial-run status clause | an additive `coldQ clause RE-ISSUED` block **inside §10** (no new heading — the §9/§10 collision history) + kernel-collapse pointer with status carried |
| 4 | `manuscript/ave-kb/vol4/falsification/ch11-experimental-bench-falsification/existing-experimental-signatures.md` — the `Fork status` bullet of the 2026-08-05 SCOPE CAVEAT | the one-line fork-status bullet | the re-issued clause with all three certification lines + a second bullet carrying the kernel-collapse pointer |

## The certification state read at write time

Read at `origin/main` = `d5a1b06b`, this session, from the result docs themselves:

- axial toroidal instrument — `research/2026-08-03_coldq-pole-v2.4-root_result.md`, headline
  certification line `ROOT-CERTIFIED`.
- axial FORK-3(b) — `research/2026-08-04_coldq-axial-rhob_result.md`, headline certification line
  `ROOT-NOT-CERTIFIED` on every configuration that has a root; thresholds not retuned; no physics
  bin adjudicated.
- polar family — `research/2026-08-03_coldq-polar-family_result.md`, headline certification line
  `SOLVER-NOT-CERTIFIED`; no physics bin adjudicated at any precedence level.
- kernel collapse — `research/2026-08-05_last-bond-kernel-collapse_result.md`: Task 1
  `SCAN-NOT-CERTIFIED` (no premise bin), Task 2 `ROW-NOT-CERTIFIED` (`G-RHO2` fails on an
  injection point sized wrong at freeze), Task 3 certified with its bin adjudicated; the three
  load-bearing disconnection theorems measured exactly zero; the sign a last-bond-plane
  statement.

**The queued `G-RHO2` certification rerun had NOT landed at write time.** Checked two ways: no
successor result doc under `research/` at this base, and no open PR or remote branch carrying
one (open PRs at write time: #896, #897 — both orchestration ruling records). **The pending
state is therefore what is printed**, per the standing print-certification rule.

**Kernel-collapse ruling status at write time.** The merged record
(`2026-08-05-ruling-flag-causal-kernel-collapse.md` + its status addendum) plus the merged
re-scope receipts (`2026-08-05-ruling-flag-causal-rescope-receipts.md`, PR #894) demote the
ruling from universal to channel-scoped and leave the final disposition word outstanding **on
the record at this base**. That is what the print sites say. (The disposition record is in
flight as an un-merged PR; it is deliberately **not cited** in print, because a print cite to a
path that does not exist at HEAD is a dead cite.)

## Two flags surfaced, not resolved in-lane

1. **The print-certification rule's own record is not on `main` at this base.** The rule applied
   here (print prose always carries certification status; compressed "confirmed" language is
   docket-register only, gate table adjacent) is ruled in
   `_orchestration/docket-entries/2026-08-05-rulings-upgrade-wave-five.md`, which exists only on
   the un-merged `orchestration/rulings-upgrade-wave-five` branch. **The rule is applied; the
   record is not cited in print** for the dead-cite reason above. When that record merges, the
   four sites above can gain a one-line provenance pointer in the propagation wave.
2. **Two sign-relativity leaves still carry `axial run in flight`, which is stale.**
   `manuscript/ave-kb/vol3/gravity/ch02-general-relativity/saturating-modulus-and-backreaction.md`
   and
   `manuscript/ave-kb/vol3/cosmology/ch15-black-hole-orbitals/bulk-impedance-at-saturation-boundary.md`
   each say the FORK-3(b) axial run is *in flight*; it landed on 2026-08-05. **Not touched here**
   — both sit inside the sign-relativity / Γ-conditional declaration blocks this lane is fenced
   out of. Routed to whichever lane holds that text.

## §10 pointer discipline applied

The kernel-collapse block is ruled to nest at `wall-taxonomy.md` **§10.2** (the ringdown wave's
§10.1 stays where it is). **That block has not landed**, so every pointer written here names
§10.2 as the *ruled, not-yet-landed* home and routes the reader to the merged ruling records
instead. No new heading was added to `wall-taxonomy.md` — the additive block nests inside §10
above §10.1, precisely because a §9/§10 numbering collision already bit this branch family once.

## Fenced out of this lane (untouched, verified untouched by the diff)

- the Γ_shear conditional-tagging text (HELD — a stronger profile-independent form supersedes it);
- the four #780 per-occurrence sites in Vol 3 Ch 15;
- the echo statements (they re-form under a separate ruling, wave-gated);
- `wall-taxonomy.md` §10.1 (the ringdown wave's register-scope row).
