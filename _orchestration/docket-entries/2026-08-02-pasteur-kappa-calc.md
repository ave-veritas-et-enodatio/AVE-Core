### ENTRY 2026-08-02-pasteur-kappa-calc (2026-08-02): implementer — the Pasteur-κ desk-calc, the round-2 MAGNITUDE follow-through (Grant go); FROZEN-FIRST; verdict bin (d) INCOMMENSURABLE, magnitude bin (a) reported non-adjudicating

- **Scope fence, stated first.** **CLASSICAL-EM BASELINE COMPUTATION. Makes no AVE claim.** No engine, no solver, no lattice; `src/ave` byte-untouched; **AVE-HOPF byte-untouched** (geometry read-only, vendored with sha256 + source commit). No claim minted, no `clm-`/`def-`/`sup-` node created, no solidity changed, no corpus KB/manuscript file edited. Consistency-vs-emergence class: **CLASSICAL-BASELINE** — not identity, not manifestation, not consistency, not emergence. Deliverables: prereg [`research/2026-08-02_pasteur-kappa-desk-calc_prereg-FROZEN.md`](../../research/2026-08-02_pasteur-kappa-desk-calc_prereg-FROZEN.md), result [`research/2026-08-02_pasteur-kappa-desk-calc_result.md`](../../research/2026-08-02_pasteur-kappa-desk-calc_result.md), driver `research/drivers/pasteur_kappa_desk_calc.py`, number-check `research/drivers/pasteur_kappa_desk_calc_number_check.py`.
- **Provenance.** Grant go 2026-08-02, verbatim `[sic]`: *"go"*. Fills the gap AVE-HOPF round-2 left: round-2 (`AVE-HOPF main:research/2026-06-04_hopf-round2-chiral-counterfactual-result.md`, `d240d70`) retired **C3** and **C4** as FORM-SHARED, and its own governing rule — `ave-discrimination-check` Step 2.5 (`SKILL.md:155`, verbatim: *"**Shares the FORM** (same functional shape / scaling exponent), **differs in SCALE** (prefactor, orders of magnitude) **→ MAGNITUDE discriminates.**"*) — routes a form-shared claim to the MAGNITUDE axis. That leg was never run.
- **FROZEN-FIRST discipline honoured.** The prereg was written, `make verify`-ed, committed **ALONE** (`3ae1f3de`) and **pushed before any number existed**. Bins, three estimators, seven gates, the four-point commensurability checklist, every tagged input and five bounded idealizations were fixed at that commit. **Mutual-satisfiability check run before freeze** (standing DESIGN LESSON 3): the three magnitude bins partition `(0,∞)` with no gap (the brief's "≳10×" / "≪" preserved as named STRONG sub-bands, not edges), no bin is excluded by construction, no two frozen requirements are unsatisfiable together, and bin (d)'s trigger is decidable from definitions **before** the magnitude is known — so it cannot be used to escape an unwelcome number.
- **★ THE COMPUTED CLASSICAL NUMBER, and its derivation chain.** Origin-independent chiral pseudoscalar $\chi=\boldsymbol\ell_e\cdot\mathbf A_e=$ `-5.951479e-07` m³ on the as-fabbed `k23_R` polyline (arc length `230.560` mm, bbox `34.581583`×`38.688032`×`2.6` mm) at the as-fabbed $f_0=$ `680` MHz; radiation resistance from the far-field integral of the same half-wave mode, $R_{\rm rad}=$ `0.7385` Ω; **chiral volume** $V_\chi=Z_0|\chi|/(3R_{\rm rad})=$ `1.012047e-04` m³, so $\kappa_{\rm cls}=N V_\chi$ for any inclusion density $N$. At the frozen close-packed reference density $N_{\rm ref}=$ `2.874784e+05` m⁻³ this gives $\kappa_{\rm cls}=$ `29.09` against $\kappa_{AVE,\text{eff}}=1.2\alpha=$ `8.756823e-03`, i.e. $\mathcal{R}=$ `3322.457263790723` → **magnitude bin (a) DISCRIMINATES-HIGH**, stable across every frozen sensitivity leg (`963.6431295970821` – `6481.485872877981`).
- **★ AND THE DENSITY-FREE STATEMENT, which is the one that carries the physics.** $\kappa_{\rm cls}$ is strictly ∝ $N$, so quoting it at one density invites an argument about the cell. Density-free: a classical composite of **these very as-fabbed copper knots** reaches the AVE number at $N^\star=$ `86.5258233657606` m⁻³ — **one knot per `0.011557243388171132` m³, about 11.6 litres** — which is **`11.4197`× MORE DILUTE than the conventional linear-mixing validity ceiling** (`988.0960542888662` m⁻³). So that statement is made **inside** the validity domain of the formula that makes it, whereas the close-packed `29.09` sits `290.942`× **outside** it and is reported as a formal linear-response extrapolation, not a physical prediction for a packed array. Both point the same way.
- **★ THE VERDICT IS BIN (d), NOT BIN (a).** The frozen four-point commensurability checklist returns **3 of 4 DIFFERENT**: **C-ii** the defining equation and its sign-keying differ ($k_\pm=k_0(n\pm\kappa)$ with ± keyed to the **wave's** circular polarisation, vs $k_{AVE}=k_0(1+\alpha pq/(p+q))$ with the sign keyed to the **structure's** handedness and no ± pair); **C-iii** object class differs (a **bulk effective-medium parameter strictly ∝ N** vs a **density-free per-topology number**); **C-iv** observable consequence differs (optical rotation of a wave **transmitted through a medium** vs $\Delta f/f$ of the knot's **own** 1-port resonance). Only C-i (dimension) is SAME. **One-sentence diagnosis: a knob and a constant are not the same kind of object, so comparing their numerical values adjudicates nothing** — the magnitude bin (a) is reported and explicitly labelled **NON-ADJUDICATING** per the prereg's own §4 rule.
- **★ THE §7 OBLIGATION DISCHARGED — the honest common observable, and it is not the one the magnitude axis was pointed at.** The observable both sides predict in matching coordinates is the **enantiomer split of the SCALAR self-resonance**, $(f_R-f_L)/f_0$, on the as-fabbed pair in an **achiral host** (air + FR-4) — what a 1-port differential $S_{11}$ actually reads. **Classical value: exactly `0`**, by parity-covariance of Maxwell. This lane makes that premise **checkable rather than assumed**: the as-fabbed L↔R map was extracted from the CSVs and is a **pure reflection $x\to-x$ with zero translation, max residual `0.0` mm** (the two competing coordinate mirrors miss by `47.1397` mm and `40.1328` mm), and the control wire lies exactly in that plane. Two independent corpus receipts agree — `AVE-HOPF docs/design/2026-05-05_hopf02_design_proposal.md:57` (*"the L↔R mirror is length-preserving (`f_R = f_L` to floating-point precision in NEC2)"*) and `docs/design/2026-05-05_hopf02_nec2_prediction.md:80`, whose `Δ_classical` column for (2,3) reads **`+0.000 MHz`**. **AVE value: `1.751365e-02` = `11.9093` MHz**, independently reproduced here from `ave.core.constants.ALPHA` and matching that same table's `−11.91 MHz` / `1.75%` to quoted precision. **FABRICATION** floor (round-2 §4 inherited S-8, ≈ `130` kHz, `hardened-prereg:264`, a `5000`-trial geometry MC) = `1.911765e-04` fractional → margin `91.6098`× — **that floor CLASS, and only that class** (audit F4, Rule 12 quote-and-correct: the `f9d5c86c` text said "Fab floor … → margin `91.6098`×" without the class qualifier). The **MEASUREMENT** floor $\sigma_{\rm repeat}\sqrt{2/N}$ is UNMEASURED and routed at **AVE-HOPF PR #`3`** (`docs/open_questions.md:116`: S-1's own $N\ge$ `10` implies $\sigma_{\rm repeat}\le$ `0.29` MHz); at that worst admitted value the combined floor is `183.630` kHz and the margin is `64.855`×. **The EXISTENCE claim is robust under both floors.** **Discriminating axis on this observable: EXISTENCE (nonzero vs exactly zero), NOT magnitude.**
- **★ FLAG-DON'T-FIX — a contradiction against LIVE merged corpus text, both sides verbatim, neither reframed.** **Side A**, round-2 §2.3 (`:84`–`:88`), verbatim: *"Then $\Delta k/k = \kappa/n \to -\kappa/n$: **opposite-sign shift for the mirror image.** The HOPF enantiomer prediction ($\Delta f_R = -\Delta f_L$) is, term for term, the classical enantiomer sign-flip of a Pasteur medium."* **Side B**, this lane: the quantity that reverses between classical enantiomers is $\Delta k/k=\kappa/n$, which **that same document's §2.1 defines** as $(k_+-k_-)/(2k_0n)$ — a **difference between two circular polarisations of a wave inside a medium** — whereas HOPF-02a measures a **scalar 1-port resonance of a single structure in an achiral host**, for which the classical prediction is **exactly zero**, as AVE-HOPF's own NEC2 table states. **The two sign-flips are not the same term:** one flips a polarisation *difference*, the other flips a *scalar frequency*, and a classical chiral medium delivers the second **only when the HOST is chiral** — sugar water does it, an air bath does not. `verify-before-cite` confirms the round-2 relabel is **LIVE, not draft**: `AVE-HOPF main:hardware/hopf_01_TEST_PROCEDURE.md:180` now carries *"| Opposite-sign Δf for L vs R enantiomer | **CONSISTENCY-CLASS** — reproducible by classical reciprocal Pasteur (chiral) media…"*.
- **Scope of what this lane does NOT say (guarded deliberately, because the finding runs FOR the framework and the `consensus-bias symmetric-standard` lens cuts both ways).** It does **not** adjudicate whether C4 returns — that is a framing call routed to Grant / the auditor lane. It does **not** disturb round-2's **C3** verdict: if the classical split is zero in every achiral host then *"same Δf/f in air, oil, vacuum"* is classically `0`=`0`=`0`, so C3 stays non-discriminating on its own and §2.2 of round-2 is untouched. It does **not** claim the magnitude `8.756823e-03` is AVE-distinct — that number is **α-injected** and round-1 already closed it (`AVE-HOPF docs/ave_crib_sheet.md:27`, verbatim: *"The form is **generic parallel-Γ algebra**: `(α·p · α·q)/(α·p + α·q) = α · pq/(p+q)`. Standard Kirchhoff parallel-impedance combination."*). What is at issue is a **presence/absence** divergence only. And it names the **residual obligation it does not discharge**: the parity theorem binds at the bench only if the **rest of the fixture** — board outline, feed position, hole pattern, connector — is also symmetric about $x=0$; that was not checked here.
- **Seven gates, all PASS — and one FIRED.** G1 origin-invariance `4.513421561571926e-13`; G2 mirror antisymmetry $\chi_L=-\chi_R$ exactly (`0.0`); G3 **known-negative** planar control $\chi=$ `0.0` exactly; G4 **known-positive with an exact analytic target** helix $\chi=\pi a^2h$, `1.5707963009561105e-07` vs `1.5707963267948966e-07` (rel `1.6449e-08`); G5 **known-positive** straight half-wave dipole $R_{\rm rad}=$ `73.07904449143211` Ω against the textbook `73.13` Ω (rel `6.9678e-04`), $|\boldsymbol\ell_e|$ rel `2.8558e-07`; G6 subwavelength `0.11784810733938897`; G7 reciprocity rel `1.977e-17`. **★ G7 FAILED on the first run, and the 2026-08-02 audit sized that catch correctly (F3, Rule 12 quote-and-correct; the `f9d5c86c` text read *"the bug was in the cross-polarizability, the one quantity the lane exists to compute. A gate that has never fired is a checklist; this one is a gate"*).** $\overleftrightarrow{\alpha}_{me}$ was built with a flipped sign — but **the defect lived in the gate's OWN reconstruction** of the dyad (driver `run_gates`, G7 block), and **no reported number consumes a signed $\overleftrightarrow{\alpha}_{em}$**: the production chain routes through `chiral_volume()`, which takes $|\chi|$. **The reported chain is sign-free, so the defect did not propagate.** The numerically load-bearing known-positives are **G4** (helix vs the exact $\pi a^2h$, rel `1.6449e-08`) and **G5** (dipole `73.07904449143211` Ω vs the textbook `73.13` Ω) — those compare a computed number against an independent target. Repair landed post-audit, KEEP-BOTH and NOT FROZEN: `post_audit_supplementary_NOT_FROZEN.S1_dyad_consumption` routes $V_\chi$ through **both** signed dyads ($-c\,\mathrm{tr}(\overleftrightarrow{\alpha}_{em})/3=+\mu_0c\,\mathrm{tr}(\overleftrightarrow{\alpha}_{me})/3=Z_0\chi/(3R_{\rm rad})=\mathrm{sign}(\chi)V_\chi$; agreement `0.0` and `1.3391198608554297e-16` relative), so a sign flip in either dyad now shows in a reported quantity. The seven frozen gates are byte-unchanged and the block is excluded from `ALL_PASS` by construction. A second (non-physics) defect was caught by running early per Rule 10: the far-field integral originally materialised the full direction×element phase matrix and spent six minutes in swap — chunked, arithmetic unchanged, and `quadrature_convergence` shows $\chi$ and $R_{\rm rad}$ drifting at most `1.04355e-06` / `2.57188e-06` across a 4x mesh change.
- **Idealizations, each with its frozen bounding leg reported whatever it showed.** I-1 current profile (γ `0.8`/`1.0`/`1.2` + uniform): $\kappa$ `31.784754192972528` / `29.094170460375217` / `27.229641103511227` / `56.75722510479334`. I-2 host index ($\varepsilon_{\rm eff}$ `1.0`/`1.5`/`2.0`): `29.094170460375217` / `14.222269603995263` / `8.438452401184271`. I-3 dipole truncation: G6, bbox diag/λ `0.11784810733938897` inside the frozen `0.25`. I-4 lossless copper: **direction of the bound was frozen, the number was NOT** — added at result time and labelled so, skin-effect $R_{\rm ohm}=$ `0.4826838832906189` Ω against $R_{\rm rad}=$ `0.7385` Ω ⇒ loss factor `0.6047315980017243`, which can only **shrink** the classical number. I-5 dilute mixing at close packing: **the one that bites**, and the frozen escape (K-3's $V_\chi$, $N^\star$) is what carries the result. Total spread `6.726023020148565`; **bin stable across every leg**.
- **No NEC2 run; disclosed at FREEZE time, not after.** PyNEC/necpp are absent from this environment (checked at design time and written into prereg §6.2), and K-2 needs none — it is a parity theorem plus two existing AVE-HOPF receipts. The classical chain is analytic + quadrature, validated against **two closed-form known-positives**. **No external experimental value used or invented**; nothing was tagged `[requires-external-retrieval]` because nothing needed to be.
- **★ AUDIT F6 ADJUDICATED — the "is this a strawman of round-2?" challenge, and it is not.** The 2026-08-02 audit lane re-read AVE-HOPF `main`:`research/2026-06-04_hopf-round2-chiral-counterfactual-result.md` §2.1–2.3 **at source** rather than through this lane's characterisation of it, and returned two findings. **(i) NOT a strawman.** This lane's distinction — that the classical enantiomer sign-flip flips a *polarisation difference* while the HOPF prediction flips a *scalar frequency* — is a **direct consequence of round-2's OWN definition** at `:45`, verbatim: *"$$ \frac{\Delta k}{k} = \frac{k_+ - k_-}{2 k_0 n} = \frac{\kappa}{n}. $$"* That is a difference between the two circular-polarisation branches, written by round-2 itself; the objection is internal to round-2, not imported. **(ii) C3's STATED BASIS is collateral to the same conflation, but C3's VERDICT survives.** Round-2's C3 argument rests at `:63`–`:64` on *"shift and the Pasteur shift carry the SAME $1/n$ dependence, and in the fractional resonance observable $\Delta f/f$ the carrier $n$ **cancels**"* — which is the same $\Delta k/k \to \Delta f/f$ step this lane declines to license. **C3's verdict nevertheless stands**, by the more conservative route already recorded above: if the classical split is exactly zero in *every* achiral host, then "same $\Delta f/f$ in air, oil and vacuum" is classically `0`=`0`=`0`, so C3 is non-discriminating regardless of whether the $1/n$-cancellation argument holds. **§2.2 of round-2 is untouched by this lane, and its retirement of C3 is not disturbed.**
- **★ ROUTED TO GRANT — three decisions, none taken at lane level (result §8.1).** **(1) The C4-return LABEL, not the physics.** The physics is settled and the substrate settled it: exact classical zero by parity covariance on the common observable, and round-2's retirement rested on an unlicensed $\Delta k/k\to\Delta f/f$ step. What remains is **which row C4 returns on** — relabel it **AVE-DISTINCT**, or add a **third row** carving the existing `hopf_01_TEST_PROCEDURE.md:180` KEEP-BOTH entry into *EXISTENCE-DISCRIMINATING* (nonzero vs exactly zero in an achiral host) alongside *MAGNITUDE-CONSISTENCY-CLASS* (the α-injected size, already closed by round-1). The second preserves the legacy row instead of redefining it in place. **Grant's call; both sides verbatim in result §6.2.1.** **(2) The fab-status contradiction** (result §6.2.2): `HANDOFF.md:14`/`:42` say the physical fab order is the next gate; round-2 `:193` calls it *"the fabbed, $123, enantiomer-pair board"*. Both live, both merged AVE-HOPF `main`. **The correction belongs to an AVE-HOPF lane**; nothing written to that repo from here. **(3) Weave orientation at order time** — if the glass weave is specified **aligned to the panel axes** in the fab order, the F1 weave systematic **vanishes identically** and the last parity hole of that class closes. One line in `hardware/hopf_02a_ORDERING.md`, free, added before the panel is cut; pair it with the singulation instruction. **AVE-HOPF ordering-guide candidate — routed, not executed.**
- **Lane mechanics.** Self-isolated throwaway worktree off `origin/main` @ `e72d18f6`; branch `research/pasteur-kappa-desk-calc`; skeleton-first incremental commits; prereg committed alone and pushed first. `make verify` exit 0 with the frozen-provenance gate and the new lane number-check green; `verify-lane-number-checks` extended with this lane's checker. PR opened `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.
