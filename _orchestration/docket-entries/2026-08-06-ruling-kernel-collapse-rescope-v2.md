# Ruling — kernel-collapse self-termination, CHANNEL-SCOPED, v2 (2026-08-06)

**Versioned re-issue of `2026-08-05-ruling-kernel-collapse-rescope.md`** (the v1 record is
preserved and gains a dated pointer to this file). Grant GO, 2026-08-06 ("5. agree" on the
posed re-issue; walk outcome in `2026-08-06-rulings-decision-batch.md` R6). Reason for the
re-issue: the doc-lane audit found the v1 ruled text carries six fences but omits the one
that decides it — the cross-grade combine member — and the upgrade wave demonstrated that
ruled text propagates byte-identically, so the fence must live in the citable record. This
v2 also corrects the engine-receipt statement that circulated with the wave.

**The ruled text, v2 (supersedes v1's as the citable form):**

> **Kernel-collapse self-termination, channel-scoped (2026-08-05; member-fenced
> 2026-08-06):** at any strain-saturation wall, every transport coupling riding the strain
> kernel — G, K (both fork branches), and the u↔φ coupling G_c — disconnects at the last
> bond: total reflection, phase computed from the branch-derived row at the declared
> plane, density-profile-independent (receipts: PR #888, #890, both Tier-2-verified). The
> rotational channel's transport (γ·S_κ) is carved out: unwalled at r_sat, its own wall
> being a κ-amplitude surface. **This carve-out is stated on the per-grade
> (L∞-across-grades) combine member. The cross-grade combine rule is open in canon
> (`axiom-register.md:190`, `:232`; see FLAG-COMBINE-SPLIT for the `:189` conflict); under
> a normalized-L2-across-grades member the rotational transport would ride the shared
> kernel and collapse with it.** Shear→rotation conversion cannot occur at the wall (the
> door closes with the strain kernel) and is confined to the graded approach where G_c·S_ε
> is finite — the leak is one computable number (the approach-leak lane, computed: PR
> #903/#904). Regime IV stays fenced out for every mirrored channel. The certified
> shear-echo physics — the delay laws, BIN-DISC's fork discriminability — is unaffected;
> the print form of the echo claim is the two-branch certified-timing statement with the
> mirror scoped to strain-kernel channels.

**The engine-receipt statement, corrected (supersedes the wave's "the member the engine
actually codes is L∞"):** the engine's live energy functional
(`cosserat_field_3d.py`, `_energy_density_saturated`) carries BOTH members and a third
amplitude definition — per-grade in the elastic block (`:767-768`: `(W_cauchy·G +
W_micropolar·G_c)·S_eps_sq + W_kappa·γ·S_kappa_sq`, different amplitude arguments), the
aggregated normalized-L2 kernel at unit weight in the same sum (`_reflection_density
:486-488`, `_s11_density:409-413`, `k_refl=1.0` default), and a sym-only-strain +
V²/V_SNAP² + chirality-biased definition (`:619/:681`). No test pins the split (`k_refl`
is zeroed in the tests). At κ=0 — the static-gravitational operating point — the members
are numerically degenerate, so the engine receipt for the carve-out is STRUCTURAL, not
numerical. FLAG-A2DEF is widened 2→3 accordingly. Vacated cite: the 2026-07-02
combine-rule result's `:411`-as-per-grade-evidence (that line is the aggregated member);
re-derive, do not patch.

**Fork status and routing:** the L2-vs-L∞ combine question is RECAST (2026-08-06 walk,
fork-collapse pattern) as a residence question — a saturation budget is a property of the
element that stores the energy, so the rule is forced once the storage residences are
adjudicated. Open items, routed to the Ax4 state-vector/residence lane
(`_orchestration/2026-08-06_ax4-state-vector-residence-brief.md`): (i) where twist-coupling
energy (γ_c) resides (bond vs through-material); (ii) whether the 2026-07-02 buckling
dissolution (the (A,S) load-response pair) reaches DP-3's grade-into-A aggregation at all
(the `axiom-register.md:189` vs `:190`/`:232` conflict = FLAG-COMBINE-SPLIT); (iii) at
which PHASE LEVEL the kernel's norm lives — the substance-monism walk (framing note §5)
admits a unified substance-level budget projecting as per-grade at crystal level.

**Carried forward unchanged from v1:** the walked filter-bank synthesis, including the
⚑ ROUTED walk-level frontier line ("the rotational channel behaves at r_sat like a
penetrating radiation") — still OUT OF PRINT, to be scoped with or after the residence
lane. Print sites carrying the wave's fence (12 files) should re-point their routing notes
to THIS record once merged (doc-lane follow-on brief, item 3).
