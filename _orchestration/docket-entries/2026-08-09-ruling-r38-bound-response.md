# Ruling — R38: the premise fork's Door 1 GO — the bulk slot as a BOUND RESPONSE field (2026-08-09)

### ENTRY 2026-08-09-ruling-r38-bound-response

**Grant, verbatim (the carve):** *"The material of the vacuum doesn't the back
reaction does?"* — read in context: the material does not compress; the back-reaction
does. **Grant, verbatim (the GO):** *"Let's maybe use a different term than 'slaved'
but let's derive."*

## The ruling

Door 1 of the premise fork is GO as a derivation lane, in its ontology-carve form:
**the substance has no bulk degree of freedom; the apparent compression sector is the
BACK-REACTION.** Term of record: **bound response field** (controlled-source class —
the circuit-theory dependent source: no autonomy, no independent flux, no port; bound
to the sectors that drive it). "Slaved" is not used.

## What the carve asserts (walk-level until the lane derives it)

The bulk slot = a bound response field: driven by the real DOFs, waving never. Its
energy bookkeeps in the driving sectors. Canon's own half-statement:
`port-register.md:35` (the halo = the bulk near-field, "stores/loads — NOT a port")
generalizes to: the bulk is NEVER a port. The static dress survives natively
(u = B/r² IS the incompressible exterior solution; mass = A1 survives as accounting —
how much back-reaction is sourced). What dies: the free-running P-branch — an
artifact of the coded compressibility (K = 2G, GR-imported per #261, minted the
phantom channel). The kill chain re-reads as the diagnosis: LC-1's superluminal
branch, the seismic-partition import in the residue, FLAG-W's contradictions, and
FLAG-CEFF's four wave-speed forms were all the costume slipping. The prediction
becomes SHEAR-ONLY radiation — what the pulsars measure.

## Fences of record

Walk-level until derived; the challenge-a-canonical-negative rule applies (the lane
must show the carve does not quietly reconstruct a closed path — removing an
identified import to resolve a derived internal contradiction is the legitimate
direction, but it must be shown, not narrated); Rule 11 clean (nothing frozen is
retuned — this is a versioned constitutive revision justified by the banked kill);
the engine implication (∇·u as constraint / A1 re-homing) is FLAGGED, not executed.
The manuscript demotion sweep from the #930 walk-back HOLDS until this lane returns —
one sweep, the complete arc, not two.

Brief: `_orchestration/2026-08-09_bound-response-brief.md` (this PR). Grant launches.
