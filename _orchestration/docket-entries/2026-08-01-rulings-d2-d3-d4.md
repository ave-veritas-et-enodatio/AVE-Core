### ENTRY 2026-08-01-rulings-d2-d3-d4 (2026-08-01): implementer — Grant rulings D2 / D3 / D4 executed (thesaurus placement carve-out, TH-3 two-tier restructure, identity-break scope tag)

- **Class: ruling execution, not adjudication.** Three Grant rulings, fired 2026-08-01, **verbatim `[sic]`: *"D2: follow rec / D3: rec / D4: follow rec"***. Each discharges a question a prior lane surfaced and deliberately did not take: **D2** and **D3** discharge the two items merged **#805**'s PR body listed as *"HELD for Grant, deliberately NOT done: the **F1 path carve-out** … and the **F5 TH-3 class restructure**"*; **D4** discharges the flag-don't-fix item merged **#812** audit F4 routed (*"routed to Grant: class-(a) tag here vs deferral to the #668 relabel lane"*). One commit per ruling. **Mints ZERO ids** (INVARIANT-S11) — no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; no solidity, confidence or grade field moves anywhere; `src/ave` byte-untouched.

#### D2 — the thesaurus placement carve-out (`#805` PR-body item F1)

- **Ruling `[sic]`: *"D2: follow rec"*.** The recommendation held open since 2026-07-28: `common/theorem-thesaurus.md` sits outside `common/translation-tables/`, and the reason belongs **in the architecture leaf**, not only in the thesaurus's own §1 self-defence. **The file is NOT moved** (no `git mv`); only the carve-out half lands.
- **`translation-tables/README-architecture.md` §3 — ONE added line**, placed between the rule's Rationale and its Corollary: the thesaurus is named a **deliberate OUTSIDE-SCOPE sibling** because its rows join theorem **names** across disciplines through a **proof** (a mathematical identity), never through a substrate identification — so the hub-and-spoke rule's **own rationale** (the unstated-substrate-identification / unauditable-X↔Y hazard, §3's rationale line) does not reach it, and its audit is the proof rather than a means-test. The thesaurus **§0 contract** is cited for the same boundary in its own voice (*"a statement about the **algebra**, not about the world"* + the mandatory Kron ontological-silence ceiling). The line states explicitly that **nothing is relaxed for any actual spoke**: a genuine discipline-X↔discipline-Y *substrate* correspondence still routes through the hub.
- **Both open-question notes in the thesaurus CLOSED, text preserved per Rule 12.** §1's *"Deviation surfaced, not taken silently … routed, not done here"* gains a dated `★RESOLVED 2026-08-01` stamp naming the ruling and the `#805` PR-body item it discharges; §7's *"Two open items surfaced by this pass, routed not taken"* bullet gains a `★BOTH NOW DISCHARGED` stamp. **Neither original sentence is struck or reworded.**
- **★Second §7 item found already-stale and stamped in the same pass (verified at HEAD, no new adjudication).** §7 item (ii) still read *"the **TH-7 precision flag** … flagged with both line numbers, **not** edited"*, but that flag was **resolved 2026-07-30** — TH-7's own row already carries the `★RESOLVED` note, and `vol2/…/mass-closure-theorem.md:54` now reads the peak-amplitude / cycle-average form with the quadrature caveat (landed via `#807`, read at HEAD). Recorded as a currency stamp on an existing discharge, not as a ruling.
- **★MIS-ATTRIBUTION IN THE BRIEF, CORRECTED RATHER THAN PROPAGATED (flag-don't-fix).** The dispatch named *"the #811-era PR-body question"* as the one D2 resolves. **It is not `#811`'s.** `#811`'s PR-body open question (audit finding 6) is a *different* placement question — *"is a §6 thesaurus row the right home for the `Q` content, or does the `Q` glyph want its own `def-Q` node in `vocabulary-register.md`?"* — which **D2 does not touch and which remains OPEN**. The question D2 discharges is **`#805`**'s *"F1 path carve-out"*. Both PR bodies were read at source before writing. `#811`'s `def-Q` question is **left open and unedited**.

#### D3 — the TH-3 two-tier restructure (`#805` PR-body item F5 / the standing ★AUDIT-FLAG)

- **Ruling `[sic]`: *"D3: rec"*.** Executes the recommendation the ★AUDIT-FLAG inserted 2026-07-28 (`#805` compact audit, repair R6 — *"FLAG ONLY, Grant ruling pending. The class cell is unchanged and the row is not split, per the hold"*) held open. **Flag text preserved verbatim per Rule 12; nothing is struck.** Six executed parts:
  1. **ONE row kept** — no TH-3a/TH-3b split. What the names share is the **mechanism** (constraint monotonicity: adding a constraint to a minimisation cannot lower the minimum); splitting would put one argument in two entries, which is the failure mode §5 already records for Rayleigh's short-cut/cut laws.
  2. **Canonical statement restated as the auditor's five-member hierarchy**, generically strict: $C^{\text{Reuss}} \le C^{\text{SUBC}} \le C^{*} \le C^{\text{KUBC}} \le C^{\text{Voigt}}$ — with the operational consequence stated (growing the specimen tightens the inner pair toward $C^{*}$ and does **not** move the outer pair at all).
  3. **Names cell split into two labelled tightness tiers.** **(a) trial-field / uniform-field OUTER bounds** — Voigt, Reuss, the elementary/first-order composite bounds — *scale-free: no BVP, no specimen, no boundary layer, no size dependence*, computed from phase moduli + volume fractions alone, never converging to $C^{*}$. **(b) boundary-condition APPARENT-MODULUS bounds** — Hill (1963) / Huet (1990) / Hazanov–Huet (1994) / KUBC–SUBC / Dirichlet–Thomson / Rayleigh short-cut + cut — *size-dependent solved BVPs on a finite specimen, each carrying a finite-cluster boundary layer, both converging to $C^{*}$*. Tier (b) sits strictly inside tier (a).
  4. **Class cell DEMOTED** `EXACT-same-theorem` → **`EXACT-same-mechanism-two-tightness-tiers`**. §3 **did** reserve headroom (it already carries `structural-analogy` as a declared-but-unused class *"reserved so a future row cannot be silently upgraded"*), so the new class is added there in §3's own bullet format and the header moves *"The four exactness classes"* → *"The five exactness classes"*. The class definition carries its own discipline clause: **every fence clause on such a row must name the tier it applies to.**
  5. **Regime fence rescoped per tier.** *Both tiers:* static/DC, sign-definite form (TH-8), bounds on the ABSOLUTE property. ***Tier (b) ONLY:*** the finite-cluster boundary layer (it widens the tier-(b) bracket, cannot invert it — `#802` §1.3) and the size dependence. Tier (a) **has no specimen and therefore no boundary layer**, and no size convergence — so a size-convergence argument transfers to tier (b) and never to tier (a). The previous wording (*"carried by **both** bounds"*) is quoted inside the rescope note so the change is legible rather than silent.
  6. **§6 homonym row added — *"Voigt/Reuss bounds vs the Hill/Huet ordering"*.** ★**Third §6 axis, declared additively** (the six surname rows collide on a *name*, the `Q` row on a *glyph*, this one on a **class label** — the bare words *"bound" / "the two-sided bracket" / "the Voigt–Reuss bracket"* are used for **both** tiers). Meets §6's own stated admission bar; every sense carries a verified on-`main` file:line (tier (a): `saturated-elastic-tensor_prereg_FROZEN.md`:204, `k2g-crystalline-provenance_prereg_FROZEN.md`:51, `prestress-tensor_prereg_FROZEN.md`:230 · tier (b): `translation-circuit.md`:334 row F + :467 row #32, `subc-kubc-bracket_prereg-FROZEN.md` §1.3 (`:59`), `relative-offset-principle.md`:57 + `common/claim-quality.md`:1541).
- **★The live confusion receipt, and it is named for exactly what it is.** Merged `#802` findings **F5 + F8** correcting merged `#782`: `#782` §7.1 (`research/2026-07-21_rve-aggregation-bench_result.md`:124) concluded *"only `compressed r_Z = 0.466 < 0.5` is **bound-robust**"*, but that `0.466` is $\sqrt{R_{\text{KUBC}}^{\text{core}}}$ — the **core** estimator, which `#802`'s frozen prereg §2.3 (`:97`) names in its own header as *"THE SHIPPED ESTIMATOR — `R_KUBC_core`, which is **NEITHER bound**"*. Reproduced bit-for-bit and re-read on the bound-carrying whole-cell modulus at `r_Z = 0.6098` (**above** `0.5`): *"not bound-robust; it is estimator-conditional"* (result `:55`); F8 adds the second leg — *"**BOTH** legs of trigger (iii) evaporate on the bound-carrying measure, not one"* (`:390`) — and re-identifies the surviving straddle as *"a **BOUNDARY-CONDITION ARTEFACT**, not … a cross-class physical flip"*. ★**Precision held, not smoothed:** that receipt lives on the **bound-STATUS** axis (a quantity with *no* bound status read as bound-robust), which is **one axis over** from the tier-(a)/tier-(b) swap the row fences — *same hazard class, different pair of objects*. The row says so in its own text rather than asserting a tier swap the receipt does not show. **`#782`'s BIN-4 label survives; its stated basis does not; the basis correction stays ROUTED** (`#802` §8 consequence 7) and is **not** adjudicated here.
- **★CITE-CURRENCY / MERGE-ORDER DEPENDENCY, disclosed for the orchestrator (verify-before-cite ship-time clause).** The `#782` result-doc cite `:124` is verified at `origin/main` **and is expected to move.** The live sibling lane `docs/factor7-and-782-basis-notes` (unmerged at this writing, 2 commits) lands the **same** `#802` F5/F8 correction as **inline Rule-12 notes inside `#782`'s own result doc** at `:12` and inside `§7.1`; its diff inserts 5 lines above `:124`, so **`:124` → `≈:130` on that merge.** Handled two ways rather than left to rot: the §6 row quotes the sentence **and** names the `§7.1` section label alongside the number, so the cite is content-locatable across the shift; and the dependency is stated in-row and here. ★**The two lanes are complementary and do NOT overlap by file** — that one corrects the corrected doc in place (`research/2026-07-21_rve-aggregation-bench_result.md`, which this branch does not touch), this one names the two objects in the thesaurus watch-list. **If `docs/factor7-and-782-basis-notes` merges first, bump this branch's one `:124` token; if this merges first, that lane is unaffected.**
- **Consistency pass (count / enum / tripwire references).** §4's summary-table TH-3 row updated (theorem cell now names both tiers; class cell; headline fence gains *"the boundary-layer clause is **tier (b) only**"*). §3 header 4 → 5 classes. `common/index.md`:78 updated three ways — the exactness-class enum `{…}` gains the fifth class, the TH-3 bullet in the 9-row list carries the tier split + the hierarchy, and the §6 enumeration tail is extended with the new row (the same additive pattern the `Q` row set on 2026-07-31). §4 still has **9 rows** (no row added or removed), so the §Sections line and the *"9 rows"* index text are unchanged and correct. TH-3's *"See §6"* pointer now names **both** bearing entries (the Hill surname row and the new class-label row).
- **★TRIPWIRE 2 HAS FIRED — surfaced, flag-don't-fix, sweep NOT done (out of ruling scope).** While verifying D3's cites: **`#800`, `#802` and `#804` are all on `main`**, and two of them already were when this leaf merged — `#800` and `#802` merged **2026-07-28**, `#804` merged **2026-07-30**, and `#805` merged **2026-07-30 after `#804`**. **Every `★UNMERGED` marker in the thesaurus's §4/§5/§6 is therefore STALE at HEAD** (content spot-verified live: `translation-circuit.md` §4.7 `:313`, row D `:332`, row F `:334`, §6 #32 `:467`; both `2026-07-28_subc-kubc-bracket_*` docs present in `research/`). A dated flag naming this is added **at tripwire 2 itself**, which is where that tripwire's own discipline lives. **Not swept**: re-pointing ~15 markers across four sections is the tripwire's deliverable for the owning lane, and doing it silently inside a ruling commit is the drive-by §7's Owner line forbids. **The new §6 row cites the `#802`/`#804` material as MERGED, by canonical on-`main` path** — it introduces no new stale marker. ★**Cascading second instance, likewise surfaced not edited:** `#804`-authored text now on `main` at `translation-circuit.md`:370 still reads *"PR #802, OPEN — cite by path, not yet on `main`"*, with the same staleness at `:196` and `:467`.
