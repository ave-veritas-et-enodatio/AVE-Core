# Ruling — the squeeze twists (2026-08-05)

**Grant, verbatim [sic]:** *"of course, the squeeze twists, until a point, the twist is how L
and C scale uniformly with a relative offset in the strain regime right?"* — followed, on the
core walk's two-part restatement, by: **"1 and 2, go"**.

**Ruled (1) — the squeeze-twist ruling:** the static A1 dilatation grading induces
micro-rotation (wryness) at r_sat through the chiral srs carrier. The DC bias is TWO-COMPONENT —
ε₁₁(r) plus a κ(r) profile riding it — so both kernel budgets load under a "pure" squeeze: the
geometry does the cross-grade coupling, no aggregation rule required. Consequences: the
2026-08-05 kernel-collapse ruling's premise is RESTORED AT LEADING ORDER; the residual leak
demotes from an open fork to an O(offset) computable (the lockstep's break near yield — "until a
point"); FLAG-COMBINE-SPLIT is REFRAMED, not contradicted — the axiom-register dissolution
("load-response bifurcation, no aggregation") is the WHAT, the chiral twist is the HOW; the
reconciliation text lands at propagation.

**Ruled (2) — the mechanism claim, GO'd for derivation (walk-level pending receipts):** the
twist is the L-channel's share of the squeeze — the proposed mechanism beneath canon's SYM
gravity class (ε and μ grading in lockstep ⇒ Γ_EM = 0). The srs conversion coefficient decides:
LOCKSTEP-EXACT ⇒ SYM is a theorem of the carrier's chirality; LOCKSTEP-APPROX ⇒ a residual
Γ_EM ≠ 0 in gravitational gradients exists with a derivable size — discriminator-class, to be
handled under mechanism-claims discipline, never headlined from a first lane.

**Dispatched:** `research/srs-twist-coefficient` (SVA pilot case 6) — static unit-cell lattice
mechanics on the ratified carrier (does not touch the rank-2 engine blocker); frozen bins
LOCKSTEP-EXACT / LOCKSTEP-APPROX (residual sized, fenced) / NO-TWIST (falsifies the walk
honestly) / ROLL-OFF-EARLY (leak size) / UNDERDETERMINED (canon gap enumerated). Bench anchor:
the chiral-mechanical-metamaterial compression-twist literature — retrieval-pointer class, no
imported values. Canonical propagation (wall-taxonomy §10, the SYM-mechanism cross-refs, the
FLAG-CANON repair, the ch15 fourth-channel row) is GATED on the lane + Tier-2 + Grant.
