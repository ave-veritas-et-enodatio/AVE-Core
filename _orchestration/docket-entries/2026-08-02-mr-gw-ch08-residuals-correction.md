
### ENTRY 2026-08-02-mr-gw-ch08-residuals-correction (2026-08-02): manuscript-reconciliation lane — item (b) upgraded to a DECIDABLE arithmetic question + two ch08 residuals added

- **Class: dated correction/extension of `2026-08-02-mr-gw-ch08-residuals.md` (Rule 12 — original untouched). Routed, not adjudicated.** Provenance: the #826 compact audit (independent verifier, read-and-run) contributed arithmetic that upgrades item (b).
- **(b-corrected) The strain-ratio divergence traces to ONE bad intermediate, so the routed question is decidable, not a convention split.** The leaf's own formula chain, evaluated on canonical constants (ℓ_node = ħ/(m_e c) = 3.8616e-13 m, `src/ave/core/constants.py:293`; h = 1e-21, f = 100 Hz, V_snap = 511 kV), gives V_GW ≈ 7.2739e-23 V → V_GW/V_snap ≈ 1.4235e-28 — reproducing the **1.4e-28** already carried by `gw-detection-antenna.md:46`, `ligo-gw-saturation-ratio.md:15`, `ch08-gravitational-waves/index.md:11`, and `vol2/.../computational-graph.md:28`. The two outliers both trace to `gw-propagation-lossless.md:34`'s intermediate "V_GW ~ 1e-19 V": the tex `:61` value 2e-25 = 1e-19/511e3 (right division, wrong input), and the KB's ~1e-24 (`temporal-saturation-regime-classifier.md:353` carries it too) is off by ~7.0e3 against the formula. **Request (sharpened): confirm the formula-chain value and rule 1.4e-28 canonical (or identify what the 1e-19 V intermediate was supposed to be); then the gated ch08 lane reconciles all sites to the ruled number.** Audit-contributed arithmetic — verify before ruling.
- **(d) NEW residual: `08_gravitational_waves.tex:65`** still prints the exact completeness sentence the KB struck 2026-08-02 ("without dissipation --- matching LIGO observations exactly, with zero free parameters") — the KB is now AHEAD of the tex on this sentence (#826). Rides the gated ch08 wave; recorded so it is not lost.
- **(e) NEW wording trip for the gated ch08 pass:** `08_gravitational_waves.tex:107` says the admixture radiates "at $O(1)$ coupling" while giving F_bulk/F_shear ≈ 0.03–0.12 — consistent only if O(1) describes the coupling constant, not the flux ratio. Inherited ruled-tex wording (not a #826 defect); one clarifying clause owed in the gated pass.
