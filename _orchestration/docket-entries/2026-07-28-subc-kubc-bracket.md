### ENTRY 2026-07-28-subc-kubc-bracket (2026-07-28): implementer — OWED-1 SUBC/KUBC bracket RUN (bracket built + reproduces bit-exactly; bench VOID as frozen on two CRITERION-side counts; routed)

- **Discharges OWED-1** as named co-equally by merged **#782** (`research/2026-07-21_rve-aggregation-bench_result.md` §8.2 item 2) and merged **#796** (`research/2026-07-22_vessel-state-rve_result.md` §9). Grant-fired 2026-07-28 [sic]: *"yes, as above, so below, we should assume we have both boundaries right?"* Every `K_eff` the corpus banked from this bench family was measured under KUBC — the rigorous UPPER-bound side. This lane adds the variational DUAL (SUBC, uniform traction, pure Neumann) so the corpus has a BRACKET rather than a one-sided bound. Prereg frozen + pushed ALONE at `1288e288`, **BYTE-UNTOUCHED** since (including across a mid-lane process death, disclosed). Driver `research/drivers/subc_kubc_bracket.py` (+ `_results.json`, white figure), result `research/2026-07-28_subc-kubc-bracket_result.md`. Engine `src/ave` byte-untouched; Rule-14 reuse of #782 KUBC + #796 grow; mints no `clm-`/`def-`; no leaf / ledger / port-register / matrix edit. Deterministic (`determinism_digest` in the JSON; wall-clock is machine-dependent, is excluded from that digest by the driver's own `determinism_digest()`, and is therefore not quoted here — D-13).
- **★SCOPE (required at every headline, frozen §7): this brackets `K` ONLY.** Every `r_Z` interval is a K-bracket around an **ASSUMED** `ρ_eff/ρ_0 ≡ 1`. **OWED-2 (the `ρ` half) is NOT discharged and stands entirely untouched.** ★And the modulus is the **BULK `K`** (both ends of the hydro mode are dev-free by frozen §1.2) — **NOT** the P-wave `M = K + 4G/3` a normal-incidence impedance carries, **NOT** Young's `E`.
- **G6 REPRODUCES THE MERGED CORPUS BIT-EXACTLY** — #796 isotropic control `0.2963682232` (rel `0.00e+00`), #796 painted-anisotropic `0.2982369863` (rel `0.00e+00`), #782 core ratio `0.2963682232` vs `0.296` (rel `1.24e-03`). The instrument IS the shipped instrument, so everything below is about boundary conditions, not a re-implementation drifting.
- **★THE BRACKET (computed + shipped in full; NOT BANKED — see the VOID below).** `bulk_only_cold` @ `φ_sf`: PRIMARY `K_eff/K_0 ∈ [0.26340, 0.41421]` (width `0.1508`) ⇒ `r_Z ∈ [0.51322, 0.64359]`; CONSERVATIVE (theorem-grade) `[0.21209, 0.51442]` ⇒ `r_Z ∈ [0.46053, 0.71723]`. #796 `grown_frozen_tangent`: PRIMARY `[0.26367, 0.41429]` ⇒ `r_Z ∈ [0.51348, 0.64365]`; CONSERVATIVE `[0.21231, 0.51451]` ⇒ `r_Z ∈ [0.46077, 0.71729]`. **Every banked corpus number sits INSIDE its bracket** (#782 `0.296`, #796 painted `0.29824`, #796 grown `0.29548`); `core_estimator_inside_primary_bracket = True` at both headline arms. **No banked number is contradicted.**
- **★THE VERDICT-RELEVANT SPLIT — and it is the whole result.** Against the #782 edge `T1: r_Z = 0.5`, the **PRIMARY** bracket **RESOLVES-HIGH** at both headline arms, but the **CONSERVATIVE (theorem-grade)** bracket **STRADDLES** it. The two frozen bracket definitions disagree about whether the corpus's `r_Z ≈ 0.54` is bound-robust, **and the one that disagrees is the rigorous one.** Both were frozen in advance precisely so this could not be picked after the fact. ⇒ frozen §5.5 item 1 applies: **BOUND-CONDITIONAL; #782 BIN-4 and #796 UNDETERMINED STAND UNCHANGED**; no bin minted, no side picked, no rescue (Rule 11/12).
- **★BENCH STATUS = VOID AS FROZEN, on TWO independent counts — both CRITERION-side, neither an extraction defect.**
  1. **`selftest_G2b_fires = False`** — the frozen §4B self-test cannot fire as written. Mechanism, and it is a real instrument finding: `g_0(L)` and `1/(Σ̄/σ)²` agree to `2.4/1.7/1.1 %` and are converging, i.e. **the uncaged KUBC/SUBC gap and the Hill-stress deficit are the SAME finite boundary layer**, so the nominal-σ mis-normalization multiplies `g_0` by nearly its own inverse and lands at `1.01–1.02` — just barely on the PASSING side. The frozen text anticipated the magnitude but drew the opposite conclusion from it (same-order **and same-sign** cancels the signal rather than probing it). Both normalizations shipped, as the frozen latitude requires. **The latitude sentence is ambiguous** ("accepted on the MONOTONICITY clause alone" = judged-on-it, or = accepted-provided-shipped); the VOIDing reading is carried **because it cannot be a rescue**. ROUTED.
  2. **Frozen G1 fires on `8` of the `42` ratio-bearing measurements** (`19 %`; the `48`-row figure is correct for the ABSOLUTE clause only — the `6` uncaged-reference rows carry `R_SUBC = R_KUBC = 1.0` by construction and CANNOT fail the ratio clause) — every one in the `T2` shear companion mode, **every one RATIO-ONLY**: `absolute_theorem_grade_ordering_holds_everywhere = True` (`48` of `48`, `G_SUBC = 0.290283 ≤ G_KUBC = 0.550409` at the headline arm). The hydrostatic A1 sector is clean: `0` ratio-clause violations of `21` ratio-bearing hydro rows. Mechanism is algebraic: `R_KUBC/R_SUBC = g_0^arm/g_0^uncaged`, and the shear channel's UNCAGED gap is huge (`2.1600` vs `1.2419` in bulk) because the under-coordinated free surface is far floppier in shear ⇒ the ratio inverts. **Frozen §2.1 already says the PRIMARY bracket is "NOT theorem-grade on the RATIO" — so G1 demands a bound-ordering from a quantity the same prereg declares not bound-ordered.** Same mutual-satisfiability class the prereg's own §6 audit caught for the core estimator and moved G1 off; the audit did not notice the whole-cell RATIO carries the identical exposure. **The frozen inference "a violation means the SUBC extraction is WRONG" is NOT supported by the data.** Reported AS FROZEN (VOID), criterion **ROUTED, not relabelled**. VOID *scoping* is also ambiguous ("VOID for that configuration"); both scopings shipped, the STRICT one (which voids both headline arms) carried.
- **Instrument validation otherwise all-PASS.** G3 determinism = the frozen criterion RUN, not a proxy (two full runs, separate processes/paths, `diff -q` **CLEAN** on the timing-stripped payloads, identical digest — the digest of the current artefact is quoted in the REPAIR NOTE below and in the result doc; earlier rounds carried earlier digests, which is expected and is why the re-run is the evidence rather than the numeral). G4 worst SUBC residual `9.99e-10` over 48 solves (cap `60000`, worst `6474`). G5 work identity `8.63e-16`. G8 `1.05e-15` (IDENTITY, labelled). G2a null exact (IDENTITY, labelled). G7a mirror holds under BOTH BCs (rail softens `0.263/0.414`; rigid stiffens `1.849/20.918`). SELFTEST-G1 FIRES (inverted `3.79654` violates as required); SELFTEST-PARTITION passes (32 tuples, all four classes, 0 unclassified). Born-model instrument fact CONFIRMED (translation exactly null, rotation costs `1.984e-3`) — the translation-only projection is correct.
- **★T4 lift is boundary-condition-robust:** `lift_under_SUBC = 1.00102`, `lift_under_KUBC = 1.00019`, and on the core convention `0.99521` (SUBC) / `1.00631` (KUBC) — all four within `0.0063` of unity. **The grown-vs-control ratio is `≈ 1` under BOTH boundary conditions on BOTH energy conventions, i.e. the near-null lift this bench family measures is not an artefact of clamping the outer skin.** ★Two narrowings (result-doc F9): this is **not** #796's banked `K_ratio_lift` read back — it is `(grown_frozen_tangent) / (isotropic_control)` recomputed *in this driver* under each BC — and the **frozen T4 band test cannot fire** here, since every variant is band `L1` by arithmetic at a `0.63 %` departure from unity, so `band_flips_across_boundary_condition = False` is not a discriminating statement and is not read as one. (Reported as a per-BC pair, not a bracket — a ratio of two ratios is not bound-ordered.)
- **SUPPLEMENTARY axes shipped ADDITIVELY (labelled NOT-FROZEN everywhere; nothing amended).** (a) **The `[111]` longitudinal modulus `K + 4·C44/3`** — a normal-incidence impedance carries a LONGITUDINAL modulus, and `√(ρK)` is right only for a shear-free medium, which this is not. ★**The label this axis originally shipped under — *"the P-wave modulus `M = K + 4G/3` (`= C11` for an isotropic average)"* — is WITHDRAWN (result-doc F2): the medium is CUBIC, so `K + 4·C44/3` is the `[111]` axis and the `[100]` `C11 = K + 4C′/3` is a different number.** At the headline arm `r_Z(M_[111])` PRIMARY `[0.62466, 0.70418]` vs `r_Z(K)` PRIMARY `[0.51322, 0.64359]` — **the axis choice moves the bin.** WHICH modulus `r_Z` should ride is a DEFINITION question, surfaced with data, **ROUTED to Grant, not settled here.** (b) **Pinned-shell confound (KUBC side only):** at the headline geometry **`51.3 %` of cage-shell nodes lie inside the KUBC Dirichlet layer** and are forced affine (route A `9.4/19.4/34.3/51.3 %`; route B `0.0/12.7/40.9/0.0 %`) — contradicting `rve_aggregation_bench.py`'s own `cubic_cage_centers` docstring ("so cages never touch the KUBC boundary shell"). `pinned_fraction_SUBC = 0.0` by construction. Reported **RAW, never subtracted**; strongly route-dependent so it is not a common-mode offset; **no attribution claimed** — routed. (c) **Realized vs intensive `φ`:** the three `L∈{12,16,20}` size points share one intensive label `0.4895` but realize `0.220/0.308/0.371` — three different composites. Bears on the merged RVE-size gate; **the auditor's to assess, not this lane's to re-open.** No bracket here depends on either convention (every config is fixed by the tuple `(L, r_cage, s, cage_w, wall, rail)`, never by a `φ` label).
- **Companion (frozen §3 D, explicitly NOT a bracket):** the fully-SUBC-grown arm RAN and CONVERGED well inside its frozen budget (`5` outer iterations vs the frozen cap of `40`, and well inside the wall-clock cap — the wall-clock numeral is machine-dependent and is not quoted, D-13), `min k_shear,eff = 0.29044 > 0`. Its tangent lands within `~0.6 %` of the KUBC-grown frozen-tangent arm. **Enters no bound-robustness claim.**
- **Deviations (§7, complete):** D-1 mid-lane process death + resume (prereg byte-untouched throughout; no verdict number existed pre-freeze or crossed the interruption); D-2/D-3 the two frozen-criterion defects above, **not repaired**; D-4 G7b passes but its size-convergence inference does NOT transfer (the composite changes with `L`); **D-5 the shipped load set does NOT satisfy a frozen DESIGN PROPERTY** (re-characterized by result-doc F4 — calling this "a pilot number that does not reproduce" understated it). Frozen §1.1 asserts that *"the constructed set is self-equilibrated AND moment-free to machine precision"* (`3.2e-14` at `L=12`); the shipped construction measures `4.238e-3` (`2.6e-6` of the load scale, eleven orders above machine precision). Harmless and result-irrelevant (net force IS machine-zero at `4.5e-12`; rotations are not null modes so a moment-carrying load is admissible; Hill's lemma symmetrizes it away) but **flagged, not fixed** — the prereg is frozen and is not amended; D-6 supplementary axes are a SUPERSET of the frozen config set, never a substitution; D-7 a driver bug caught pre-ship (companion self-consistency compared the operator at `u=0` with itself ⇒ false convergence in 1 outer step) — fixed, companion-only, no bracket number changed; D-8 NOT run: periodic-BC arm, surface-corrected estimator, any `ρ` measurement (★the *direct `C11` probe* half of this caveat is DISCHARGED at the RE-VERIFY repair — see the REPAIR NOTE, F1); **D-9 an externally-reported "3.19× KUBC over-stiffening" (PBC-vs-KUBC) was reported to this lane as root-caused-and-refuted — but that refutation is ORCHESTRATOR-RELAYED, NOT verified here** (result-doc F11): this lane did not run it, did not verify it, and could not locate it in any pushed branch at write time, so "REFUTED" should be read as unverified-by-this-lane until anchored to a landed artefact. **Nothing in this lane's design, execution or reading depends on it in either direction**, and it is recorded only so the non-dependence is explicit. This lane does NOT adjudicate PBC-vs-KUBC.
- **Ledger:** all ratios `[derived]`, **CONSISTENCY-class** (is an already-banked lattice number boundary-condition-robust?), not emergence; `ρ_eff/ρ_0 ≡ 1` `[assumption]` ⇒ every `r_Z` is **MIXED-provenance**, cite as such; `ρ* = 9.77337` `[import]`; probe amplitudes/tolerances `[engineering-choice]` and shown to cancel or be converged. `α`-CLEAN. No emergence-class claim headlined.
- **Consequences ROUTED to Grant / the auditor lane** (this lane surfaces and routes only): (1) the two frozen-criterion defects + the two ambiguous frozen sentences — including whether G1's ordering clause should be restated on the CONSERVATIVE pair where it IS a genuine theorem; (2) the modulus-identity definition question; (3) the pinned-shell confound on the KUBC side of this whole bench family; (4) the realized-vs-intensive `φ` labelling and its bearing on the merged size gate; (5) **OWED-2 stands untouched — a `K`-bracket does not dispose of the `ρ` question**; (6) standing verdicts NOT moved; frozen §5.5 item 3 is **NOT invoked at any headline arm on either bracket definition** (both audit lists empty) — though the artefact DOES carry item-3 disposition strings on `20` of `42` rows on the PRIMARY basis (`13` on CONSERVATIVE), every one of them VOIDed, non-headline or an identity row, so none discharges any standing caveat (result-doc F6/F7, §4.1); (7) **the #782 BASIS correction** — BOTH clauses of #782 `:12`'s trigger (iii) evaporate on the bound-carrying measure, and the surviving straddle is a boundary-condition artefact, not a cross-class flip (see the REPAIR NOTE, F5/F8/F3). Frozen-provenance gate: **0 gating** (14 `Frozen:` labels, all byte-matching). `make verify` + docket lint green.
- PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; branch `research/subc-kubc-bracket` off `origin/main` @ `c8ceacc3`. No bench merge in this lane.

#### REPAIR NOTE 2026-07-28 — PR #802 adversarial-review repairs landed (11 findings; frozen prereg BYTE-UNTOUCHED throughout)

- **★F5 (MAJOR) — the consequence that cuts AGAINST the status quo was computed, shipped in the JSON, and never stated. It is now stated at the top of the result doc, with numbers.** Merged #782 §7.1 (`research/2026-07-21_rve-aggregation-bench_result.md:124`) says `only compressed r_Z = 0.466 < 0.5 is bound-robust`, and `:12` carries that leg as the MACRO-side leg of the BIN-4 straddle. **That `0.466` is `√(R_KUBC_core)` — the CORE estimator, which the frozen prereg §2.3 says carries NO bound status.** This lane reproduces it BIT-FOR-BIT (`0.2176075097402979`, = #782's own shipped `leg4_verdict.by_class.bulk_only_compressed.K_eff_over_K0_sf`) and measures the same configuration on the bound-carrying WHOLE-CELL modulus: **`r_Z = 0.6098` — ABOVE `0.5`.** Across #782's four `φ_sf` legs, **`2` read macro-side on the core estimator and `0` do on the bound-carrying measure**, with the core estimator sitting BELOW the bound-carrying value at every leg. **⇒ #782's sole bound-robust macro-side reading is NOT bound-robust; it is estimator-conditional, and the `r_Z` MACRO-side/MATCHED-side split #782 `:12` grounds trigger (iii) on does not exist on the bound-carrying measure.** ★This sentence originally attributed a *"verdict-controlling cross-class flip"* to **#782 §5**; that attribution was WRONG and is corrected here — see the RE-VERIFY REPAIR NOTE below (F3). Re-examined honestly: **BIN-4's LABEL survives** (removing a macro-side leg makes the axis MORE undetermined, not less) **but #782's stated BASIS for it does not.** No merged doc edited, no bin minted — the **basis correction is ROUTED**, the second such to #782 after the ρ-flags audit. New JSON block `F5_782_bound_robustness_crosscheck_NOT_FROZEN` reads #782's own artefact at run time rather than retyping it.
- **★F2 (MAJOR) — the anisotropy error: `M = K + 4G/3` is NOT `C11`, because the medium is CUBIC.** MEASURED the missing constant rather than assuming it away: a supplementary `tetra` probe mode (`E = ε·diag(1,−1,0)` / `Σ = σ·diag(1,−1,0)`) run through this lane's OWN `subc_solve`/`kubc_solve` primitives returns `C′ = (C11−C12)/2` directly. Cold uncaged reference at `L=16`: **Zener `A = C44/C′ = 1.605316` (KUBC) / `1.330402` (SUBC)** — strongly cubic. So the shipped axis is the **`[111]`** longitudinal modulus, and it **overstates the true `C11` by `18.9 %` (KUBC) / `7.9 %` (SUBC)**: `M_[111] = 2.861883` vs `C11 = K + 4C′/3 = 2.406553`. The `= C11 for an isotropic average` label is **WITHDRAWN**; an anisotropy disclosure is added at the top of the doc and in the JSON `MODULUS_IDENTITY` so no downstream reader consumes `M_*_abs` as `C11`. **The `tetra` mode enters NO frozen gate, read or count.** ★**ROUTED TO GRANT:** a cubic medium's longitudinal modulus is **DIRECTION-DEPENDENT**, and **Protocol E launches along `[100]`, so a Protocol-E time-of-flight comparator is `C11 = K + 4C′/3`** — not the `K + 4C44/3` axis, not a VRH average. On the theorem-grade bracket at the headline arm the three candidates disagree at the controlling edge: `K` `[0.46053, 0.71723]` STRADDLES `T1 = 0.5`, `M_[111]` `[0.50778, 0.86626]` RESOLVES-HIGH, `C11_[100]` `[0.53990, 0.83129]` RESOLVES-HIGH. **Sharpened, not answered.**
- **F1/F8** — the CONSERVATIVE (theorem-grade) `M` ratio column is added (one divide from already-shipped absolutes; **no re-run of any solve**), the existing column is labelled PRIMARY, the frozen §2.1 non-theorem-grade caveat is lifted out of a driver comment into the prose, and the routed consequence is restated **threshold-by-threshold**: on CONSERVATIVE the bin **MOVES at `T1 = 0.5`** (`K` straddles / `M` resolves-high) and **does NOT move at `T2 = 0.55`** (both straddle). **This makes the routed consequence STRONGER** — it is the *theorem-grade* reading that is axis-sensitive, not merely the PRIMARY one.
- **F3** — honest ratio-clause denominators: `8` of **`42`** ratio-bearing measurements (`19 %`), not `8` of `48` (`17 %`); the `6` uncaged-reference rows carry `R_SUBC = R_KUBC = 1.0` by construction and CANNOT fail the ratio clause. Hydrostatic side: `0` of **`21`**, not "24 of 24". The `48` denominator is retained where it is correct — the ABSOLUTE clause.
- **F4** — D-5 re-characterized: frozen §1.1 asserts a **PROPERTY of the load-set construction** (*"self-equilibrated AND moment-free to machine precision"*) that the **shipped load set does not have** (`4.238e-3`), not merely "a pilot number that does not reproduce."
- **F6** — doc↔JSON reconciled: item 3 is NOT invoked **at any headline arm on either bracket definition** (both audit lists empty), but the artefact DOES carry item-3 disposition strings on **`20` of `42`** rows on the PRIMARY basis (`13` on CONSERVATIVE), `2` of them the identity null. Both counts shipped so doc and artefact cannot drift.
- **F7** — the **THIRD frozen ambiguity SURFACED**: frozen §5.5 never says which of the two frozen bracket definitions "a bracket" denotes, so items 1 and 3 fire together on **`14`** rows including both headline arms. And the VOID is now applied **consistently**: only item 4 survives a VOID; items 1/2/3 all presuppose a readable bracket. **No row's disposition changes** — every VOIDed row is item-4-governed and nothing moves either way.
- **F9** — the T4 sentence narrowed to what was measured: this is NOT #796's banked `K_ratio_lift` read back, and the frozen band test **cannot fire** at a `0.63 %` departure from unity. Claim retained: the grown-vs-control ratio is `≈ 1` under BOTH BCs on BOTH energy conventions.
- **F10** — the frozen prereg's provenance block cites a docket fragment that **never existed** (dead cite, invisible to the link gate inside an inline code span). **Prereg NOT amended**; the missing fragment `_orchestration/docket-entries/2026-07-28-subc-kubc-bracket-prereg.md` is supplied, dated honestly to its actual writing date with an explicit "this was NOT written at freeze time" statement, and the defect is disclosed as result-doc D-10.
- **F11** — D-9's flat "REFUTED" assertion is re-stated as an **orchestrator-relayed input** with that provenance (not verified by this lane, not locatable in any pushed branch at write time). The load-bearing point is unchanged: nothing here depends on it in either direction.
- **★NEW, found during the repair — `research/drivers/subc_kubc_bracket_number_check.py`** (the #801 pattern ported): every inline-code numeric token in the result doc must be the correctly-rounded value of a **NAMED** shipped-JSON leaf, hand-PINNED to one, or allow-listed with a reason; tokens below `3` significant digits are REFUSED auto-registration (a 1–2-digit match against `31164` enumerated forms is coincidence, not provenance). **Wall-clock is NON-REGISTRABLE and NON-ALLOW-LISTABLE** — the numeral is REMOVED from the doc, not laundered (the #801 R3 lesson), and the self-check that enforces this is STRUCTURAL, not value-based, so the checker's own verdict is machine-independent. **On its first run it caught FOUR retyped numbers in the doc the review had already passed** (`0.69588`→`0.69597`, `[0.21651, 0.60034]`→`[0.21652, 0.60037]`, `0.53275`→`0.53276`, `4.284e-3`→`4.238e-3`); all corrected and recorded as D-14. Counts at that round: **`404` tokens → `318` auto-verified + `17` PINNED-verified + `66` allow-listed + `3` digest-shaped + `0` UNACCOUNTED, PASS** — superseded by the RE-VERIFY round's counts below, which cover THREE documents instead of one.
- **WIP DISPOSITION (D-1b).** The repair lane itself died mid-F2; uncommitted driver+JSON work survived. It was **AUDITED, not trusted**: the driver was re-run from the surviving source and the regenerated JSON compared leaf-by-leaf against the surviving one; it was a complete, self-consistent, deterministic run of that source, so it was KEPT and the repair completed on top. ★The leaf-count figure this bullet originally quoted is **WITHDRAWN as unauditable** — see the RE-VERIFY REPAIR NOTE below (F5); the evidence is restated as the two re-runnable checks (G3 two-full-runs, and the `0e8d4e67`→HEAD leaf diff).
- **DISCIPLINE.** Frozen prereg **BYTE-UNTOUCHED**: md5 `9e467d603d5745c1e05094506234e5d2`, verified against the `1288e288` blob **before and after** the repair. Engine `src/ave` untouched. **Merged #782 and #796 NOT edited** — both corrections ROUTED. G3 determinism **re-run on the repaired code**: two independent processes, separate `--out`, identical digest `7bcebdb2d83092dd…`, `diff -q` CLEAN. Frozen-provenance gate **0 gating**. `make verify` + docket lint green. PR stays `[DO-NOT-MERGE]`, not retitled, not merged.

#### RE-VERIFY REPAIR NOTE 2026-07-28 — PR #802 re-verify audit repairs (8 findings, 2 GATING; frozen prereg BYTE-UNTOUCHED throughout)

★**Finding labels in this note are the RE-VERIFY audit's `F1`–`F8` and are a DIFFERENT numbering from the first review round's `F1`–`F11` above.** The re-verify audit independently reproduced both of this lane's MAJORs from primitives — F5's numbers exactly, F2's numbers exactly plus a cubic-identity probe this lane had not run — verified the number-check and the determinism on its own machine, found all hygiene green, and returned **CLEAR-WITH-REPAIRS**.

- **★F3 (GATING) — a MIS-CITATION that also mis-identified the object.** The result doc attributed the phrase *"verdict-controlling cross-class flip"* to **#782 §5**. **It is not there:** §5 is LEG 3 (`:76` through the §6 header at `:96`) and contains **zero** occurrences of `cross-class` in either case. The quoted phrase occurs exactly once, at **`:114`** — **§6 (LEG 4 / VERDICT)** — and the only other site, `:12`'s `CROSS-CLASS-FLIP`, names the SAME `f_incl` object. **Every cross-class phrase in #782 attaches to the `f_incl` collapse object, never to the `r_Z` one this lane re-measures.** And at `:114` it denotes a **different object** — the `f_incl` BIN-2-vs-BIN-4 split — which is **COLLAPSE-GATE-driven, not `r_Z`-driven**: `research/drivers/rve_aggregation_bench.py:831` `_bin_of` returns BIN-4 whenever `collapse_ok` is `False`, **before `r_Z` is consulted**, and `bulk_only_compressed` fails the `f_incl` collapse gate (`0.3280883375040938` vs the `0.30` threshold) while `bulk_only_cold` (`0.28045314594331805`) and `bulk_only_expanded` (`0.2687184264458418`) pass. **That flip therefore SURVIVES the change of measure unchanged, and this lane does not touch it.** The sentence is retargeted to the `:12` object it actually describes — the `r_Z` MACRO-side/MATCHED-side split (`compressed 0.466` vs `cold 0.544` / `expanded 0.568`) — cited to `:12`, and the `f_incl` flip is separately qualified as NOT touched. Every line number re-grepped before citing.
- **★F1 (GATING) — the axis-alignment evidence was a NON SEQUITUR, and is REPLACED BY MEASUREMENT.** §6.5 offered the realized SUBC `Σ̄ = diag(+0.9047…, −0.9047…, 0)` as evidence that "the cubic axes are the lattice axes". `hill_stress(traction_load(...))` **never sees `Phi`** — verified here: that tensor is **bit-identical across all `19` `L = 16` configurations**, including `rigid` (a `100×` stiffer shell) and `uniform_medium_null` (no cages at all). It is a property of the LOAD SET. **WITHDRAWN as evidence; the conclusion is TRUE and is now measured two ways that do see `Phi`:** (a) a **direct uniaxial KUBC probe** `E = ε·diag(1,0,0)` ⇒ `C11 = 2U/(Vε²)` — uncaged `2.406553079293439` vs assembled `K + 4C′/3 = 2.406553079062829` (rel `9.582601379065636e-11`), arm `1.2109017632998478` vs `1.210901763349438` (rel `−4.0953240798558e-11`) — a cubic IDENTITY that holds only under axis alignment (a `45`-degree-rotated medium would read `2.7480507557864744` vs `2.8618833146943565`, a `4 %` disagreement); and (b) the **KUBC reaction-stress structure** — tetragonal strain ⇒ purely tetragonal stress, uniaxial strain ⇒ `Σ₂₂ = Σ₃₃` to `5.160971547207703e-09`, the cubic `C12 = C13` signature. **This also DISCHARGES the "no direct `C11` probe" half of D-8.**
- **★F8 — this lane's own F5 was UNDERSTATED, in #782's favour.** #782 `:12` grounds trigger (iii) on **TWO** clauses; the first version addressed one. The second is the φ scan: **#782's crash-min `r_Z = 0.4048630894644663`** (`bulk_only_cold` route B `r_cage = 1.7`, `s = 4.2`, again the CORE estimator) — reproduced here **bit-for-bit** and read on the whole-cell measure at `0.7106391502114198`; and across this lane's **entire `21`-configuration hydrostatic set** the **minimum** whole-cell KUBC `r_Z` is `0.5850392860613116`, with **`0`** configurations anywhere below `0.5`. **⇒ BOTH legs of trigger (iii) evaporate on the bound-carrying measure, not one.** ★And the distinction that is the actual substance: the straddle **survives as a BOUNDARY-CONDITION ARTEFACT**, restored at `2` of the `4` legs by this lane's SUBC **LOWER** bound, **not** as a cross-class physical flip.
- **F4** — *"the actual upper bound"* applied a bound-ordering word to a RATIO the frozen §2.1 declares NOT bound-ordered — the same conflation §4 correctly diagnoses in G1. Column and JSON key relabelled to *whole-cell KUBC apparent modulus*, and the theorem-grade §2.2 `[R_lo, R_hi]` pair ADDED per leg (one divide, no re-run): compressed `[0.38080390724320373, 0.67960726963222229]` — still STRADDLES; all four legs STRADDLE, none RESOLVES-LOW.
- **F5** — D-1b's `8587`-of-`8587` leaf count is **WITHDRAWN as unauditable** (no committed state yields it under any convention; the right-hand side was an uncommitted file that no longer exists; the convention was never stated). Evidence restated as the two re-runnable checks, and the leaf diff is now reported with its convention and both commit refs (D-16).
- **F6** — the unanchored *"ρ-flags audit"* citation is anchored to `_orchestration/docket-entries/2026-07-21-relative-offset-principle.md:6` at both sites. Same defect class as the F11 repaired in the previous round.
- **F7** — the number-check's coverage was the result doc **alone**. Extended to a LIST including both lane docket fragments, and their bodies corrected in place so a body-only reader is not served text the appended notes had already superseded (wall-clock numerals, the pre-D-14 net moment, `8 of 48`, `24 of 24`, the withdrawn *"is REFUTED"* and *"P-wave `M = K + 4G/3`"* labels, and the pre-repair digest).
- **the auditor's margin note** — the routed modulus consequence is now stated on **`C11_[100]`**, which clears `T1` by `7.98 %` at the headline arm and holds `8.11 %` / `8.65 %` at `L = 12` / `L = 20`, rather than on `M_[111]`, which clears by `1.56 %` at `L = 16`, `0.702 %` at `L = 12` and `0.642 %` on the grown arm.
- **★TWO DEFECTS THIS ROUND FOUND IN ITS OWN ARTEFACTS, recorded rather than quietly fixed.** (i) The JSON string `F5_….WHAT_THIS_DOES_NOT_DO` claimed the `two_sided_class_vs_T1` column *"STRADDLES at every leg"* — **the column it cites contradicts it** (`2` STRADDLE, `2` RESOLVE-HIGH); corrected in place, with the count now shipped as its own leaf so prose and column cannot drift again. (ii) The number-check had a **false-provenance path in itself**: a bare-integer count (`1108`) auto-matched an unrelated CG iteration count and was reported as verified. Bare integers are now never auto-registrable — ALLOW-listed with a reason or PINNED to a named path. **The check got stricter.**
- **DISCIPLINE.** Frozen prereg **BYTE-UNTOUCHED**: md5 `9e467d603d5745c1e05094506234e5d2`, verified against the `1288e288` blob before and after this round. Engine `src/ave` untouched. **Merged #782 and #796 NOT edited** — every correction ROUTED. Driver re-run in full **twice** (separate processes, separate `--out`), `diff -q` CLEAN, digest `37b5fc81eb537902…`. **Leaf diff (D-16): `0` numeric leaves CHANGED in value against `0e8d4e67` (the first JSON this lane shipped) AND against `1b007cb2` (pre-RE-VERIFY HEAD); the only `5` removals are the F4 RENAME, same values, corrected labels.** **The white figure is UNCHANGED and is not re-committed:** the regenerated `.png` is BYTE-IDENTICAL to the committed one (the figure plots the frozen bracket, which did not move), and the regenerated `.pdf` differs from the committed one ONLY in its embedded `/CreationDate` — a wall-clock artefact of exactly the class D-13's policy keeps out of this lane's artefacts — so the committed `.pdf` is left in place rather than churned. Stated rather than done silently. Number-check across all three documents: **`471` tokens → `367` auto-verified + `19` PINNED-verified + `79` allow-listed + `6` digest-shaped + `0` UNACCOUNTED. PASS.** Frozen-provenance gate **0 gating**. `make verify` + docket lint green. PR stays `[DO-NOT-MERGE]`, not retitled, not merged.
