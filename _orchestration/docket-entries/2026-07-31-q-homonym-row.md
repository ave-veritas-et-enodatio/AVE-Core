### ENTRY 2026-07-31-q-homonym-row (2026-07-31): implementer — §6 HOMONYM ROW: the three senses of $Q$ (quality factor / winding charge / $Q_{TANK}$ echo)

- **Grant's authorization (verbatim `[sic]`):** *"do it"* (2026-07-31). Scope: **ONE row** added to `manuscript/ave-kb/common/theorem-thesaurus.md` §6 (the homonym watch-list). **NO-CLAIM leaf discipline held** — the row NAMES three already-adjudicated corpus objects with two-method-verified cites; it mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`, upgrades no status, and re-scopes nothing (INVARIANT-S11).
- **★WHY NOW — the drift is confirmed live, not hypothetical.** A 2026-07-31 orchestrator walk had to disambiguate the three $Q$ senses **mid-discussion** before the physics question could even be posed. §6's stated purpose is exactly this: *"recorded so a future cite cannot silently swap them."*
- **★THE ROW — three senses, three OWNERS, three ledgers** (this is the content; the ledger split is the fence):
  - **1. $Q$ = the QUALITY FACTOR** — per-resonator energy ratio (stored / lost per radian), an **impedance-ledger** object, **per-resonator ANY sector**, **continuous and strain-riding**. Cites: `op21-multi-mode-mode-counting.md`:10 (*"the **mode-counting identity** $Q = \ell$ per Nyquist-cell-resolved confined mode at the substrate's $\Gamma = -1$ saturation/TIR boundary"*); the Ruling-B1 ringdown Q-law $\omega_I=(\omega_R-m\Omega)/(2\ell)$ with flat $Q=\ell$ re-scoped to the $a_*=0$ cold anchor, `ave-merger-ringdown-eigenvalue.md`:91; the $\ge4$-value electron-scale sub-sense map `theorem-3-1-q-factor.md`:158–170.
  - **2. $Q$ = ELECTRIC CHARGE** — the Cosserat $(2,3)$ **winding number**, $Q=\mathrm{Link}(\partial\Omega,F)\in\mathbb{Z}$: a **topology-ledger** object, sector **Cosserat / rotational (T2)** ($A1 \perp T2$), **strain- and saturation-immune**. Cites: `electron-identification.md`:108 verbatim (*"the **quantization / integer-ness** is FORM-derived (Ax2 TKI integer winding number, $Q = \mathrm{Link}(\partial\Omega, F) \in \mathbb{Z}$ of the $0_1$ unknot)"*); `electron-bound-resonator-coverage.md`:54 verbatim (*"charge lives in (2,3) Cosserat micro-rotation; the scalar cage runs A1 only"*); `def-3638f2` sense (2) at `vocabulary-register.md`:272 (*"**invariant under any smooth deformation** (survives ionization)"* — **the failure mode IS the discriminator**); home cite `hollow-vortex-binding.md`:96.
  - **3. $Q_{\text{TANK}} = 1/\alpha$** — the **Theorem 3.1′ electron LC-tank identity**, numerically bridging sense 1 to the charge **COUPLING** $\alpha$ (never to sense 2's integer); an **echo-identity-register** object, **EM-sector**, electron-scoped. Cites: `theorem-3-1-q-factor.md`:15 (the boxed $\alpha^{-1}=Q_{\text{tank}}=4\pi^3+\pi^2+\pi$) + `:19` (the value-scoped **Class-B** status, *"a **definitional identity** that predicts no independent value"*); `physics-lineage-map.md`:390 verbatim (*"Q_tank = 1/α cited as identity NOT derivation"*, also :135); `interlock-register.md`:249 (the baked `Q_TANK = 1/α` ruled **CULPABLE** for the #41 circularity, then **CI-scrubbed**). Recorded per `consistency-vs-emergence`: **cite as an IDENTITY, never as a derivation.**
- **★THE HAZARD FENCED (verbatim in the row).** Any sentence letting one sense's property leak onto another. Canonical leak: *"charge $Q$ rides saturation"* — **TRUE** of sense 1 (a tank $Q$ does move with the Axiom-4 operating point) and **FALSE** of sense 2 (the winding integer is deformation-invariant), reachable **only** by sliding through the shared glyph. Mirror-image leak: reading $Q_{\text{TANK}}=1/\alpha$ as though it said something about the charge **INTEGER** promotes an adjudicated echo into a topological derivation — it speaks to the charge **COUPLING** and is silent on the winding.
- **★NOT a duplication of the T10 map (the structural call).** `theorem-3-1-q-factor.md`:158–170 already splits **sense 1 internally** into $\ge4$ electron-scale objects at distinct values (loaded $137$ / intrinsic $\to\infty$ / cold ring-down $\approx30.8$ / radiative floor $\approx29.98$ / per-mode $\ell$). That table lives one level **down**, inside the impedance ledger; this row sits one level **up, between ledgers** — it is the cross-owner fence the T10 map does not and cannot carry. The row says so explicitly and cross-refs rather than restating.
- **★DEVIATIONS SURFACED, not taken silently.** (i) The brief's phrase **"cosmic-tank identity"** has **ZERO corpus hits** (two-method: `git grep -n -I -i "cosmic tank\|cosmic-tank"` over `manuscript research _orchestration` → 0; independent filesystem recursive grep → 0). The row uses the leaf's own name — the Theorem 3.1′ **electron** LC-tank identity. (ii) The row carries a **4th "Sector / owner" column** the six surname rows lack, justified because these are physics objects with sector ownership rather than mathematicians' surnames. (iii) §6's preamble said *"the same **surname** for different objects"*; rather than redefine it in place, an **additive** scope clause was appended (*"§6 now also carries **symbol** collisions … The surname rows are untouched"*) and the row is headed **★the first SYMBOL row**.
- **★FLAG-DON'T-FIX (surfaced, NOT edited) — reciprocal update-trigger gap at `physics-lineage-map.md`:602.** That leaf's §8 sibling-leaf line enumerates which thesaurus rows depend on which lineage-map entries (*"currently T8 → TH-4, T14 :490 → TH-9, T6/F8 + T4/F3 → the §6 homonym rows"*) and does **not** list the $Q$ row, whose sense-3 cell cites `:390` + `:135`. Recorded in the thesaurus's own §7 tripwire 1 with the gap named; the lineage map is **not edited from this lane**.
- **★ADJACENT, deliberately NOT folded in as further senses** (different glyph, same word — recorded so neither is mistaken for a fourth object): the calligraphic **$\mathcal{Q}$** is the boundary observable (*"Boundary linking number … $\mathrm{Link}(\partial\Omega, \mathbf{F}_{\text{substrate}}) \in \mathbb{Z}$ … **1D line/loop**"*, `boundary-observables-m-q-j.md`:20) — the externally-measurable **face of sense 2**, not a separate object; and Axiom 2's bracket **$[Q] \equiv [L]$** is about the **dimension of the charged dislocation** the integer labels, not about the integer (`ave-kb/CLAUDE.md` INVARIANT-S2). The proposed sharpen of exactly that point at `vocabulary-register.md`:255 is cited **as corpus state** with its own *"awaiting Grant ratification; NOT yet canonical"* label intact — **not adopted**.
- **Files:** `manuscript/ave-kb/common/theorem-thesaurus.md` (§6 +1 row + hazard blockquote; §6 preamble +1 additive scope clause; §7 tripwire 1 +1 dependency + the surfaced reciprocal gap); `manuscript/ave-kb/common/index.md`:78 (§6 count updated — *"…, 2 Fosters, 3 $Q$s"*). **No other file touched**; `src/ave` byte-untouched. **Mints ZERO ids.**
- **Battery:** `make verify` **exit 0**; `verify-md-links` **gating errors 0** (kbleaf 1085 cites checked, gating 0); `verify-provenance-stamps` gating failures 0; `verify-docket-keys` no new duplicate keys; `make refresh-kb-metadata` **idempotent** (0 subtree-claims, 0 solidity, 0 leaf-references, 0 of 6 `.index/` files rewritten); pure-corpus. Every cite two-method verified at `cac12881` (`git grep` on the ref **and** an independent filesystem recursive grep — the grep-completeness discipline).
- **Class:** KB register maintenance (NO-CLAIM leaf, naming only); canonizes nothing new; no claim-graph edge added or changed.
