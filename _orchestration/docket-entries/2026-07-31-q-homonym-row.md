### ENTRY 2026-07-31-q-homonym-row (2026-07-31): implementer — §6 HOMONYM ROW: the three $Q$ entries (quality factor / winding charge / the $Q_{TANK}$ echo-STATUS reading)

- **Grant's authorization (verbatim `[sic]`):** *"do it"* (2026-07-31). Scope: **ONE row** added to `manuscript/ave-kb/common/theorem-thesaurus.md` §6 (the homonym watch-list). **NO-CLAIM leaf discipline held** — the row NAMES three already-adjudicated corpus objects with two-method-verified cites; it mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`, upgrades no status, and re-scopes nothing (INVARIANT-S11).
- **★WHY NOW — the drift is confirmed live, not hypothetical.** A 2026-07-31 orchestrator walk had to disambiguate the three $Q$ senses **mid-discussion** before the physics question could even be posed. §6's stated purpose is exactly this: *"recorded so a future cite cannot silently swap them."*
- **★THE ROW — TWO quantity-ledgers + ONE status entry over the first** (post-audit F2 restructure; the earlier *"three senses, three OWNERS, three ledgers"* spine is retired — see the audit-repair bullet below). Entries 1–2 are a **quantity** taxonomy; entry 3 is a **status** taxonomy:
  - **1. $Q$ = the QUALITY FACTOR** — per-resonator energy ratio (stored / lost per radian), an **impedance-ledger** object, **per-resonator ANY sector**. ★**Discriminator = the INVARIANCE GROUP, not integer-vs-continuous** (audit F1): the ledger owns continuous readings (loaded $Q$) *and* integer ones ($Q=\ell$) alike; the integers here are **mode-count** integers — `def-quant3` sense **(1)**, cavity-protected only (*"**Failure mode: ionization destroys them**"*, `vocabulary-register.md`:271) — **not** the deformation-invariant class, which is `def-quant3` sense (2) at `:272`. Cites: `op21-multi-mode-mode-counting.md`:10 (*"the **mode-counting identity** $Q = \ell$ per Nyquist-cell-resolved confined mode at the substrate's $\Gamma = -1$ saturation/TIR boundary"*); the Ruling-B1 ringdown Q-law $\omega_I=(\omega_R-m\Omega)/(2\ell)$ with flat $Q=\ell$ re-scoped to the $a_*=0$ cold anchor, `ave-merger-ringdown-eigenvalue.md`:91; the $\ge4$-value electron-scale sub-sense map `theorem-3-1-q-factor.md`:158–170. ★**Flag-don't-fix, NOT edited from this lane:** `ave-merger-ringdown-eigenvalue.md`:91 writes *"the topological $Q = \ell$"*. Under the register's own `def-quant3` taxonomy that object is **sense 1** — the adjective marks that the index $\ell$ does not move with spin, not that $Q$ joins the topology ledger (its protection class is the **cavity**; `:91` itself scopes the flat reading to the $a_*=0$ cold anchor and `:92` records the $-38\%$ catalog-spin failure). The thesaurus row says so; the ringdown leaf's own wording is **surfaced, not corrected** from here.
  - **2. $Q$ = ELECTRIC CHARGE** — ★post-audit F3 wording: **charge lives IN the Cosserat $(2,3)$ sector**, and the integer read out of it is the **linking number** $|\mathrm{Link}(\partial\Omega,F)| \in \mathbb{Z}$ — `hollow-vortex-binding.md`:96 keeps the two apart verbatim (*"the winding integer $n$ (the $(2,3)$: $w_{\text{tor}}=2$, $|\text{Link}|=1$)"*). The row does **NOT** fuse the $(2,3)$ with $\mathrm{Link}$ on the authority of the unratified sharpen at `vocabulary-register.md`:256, and it **carries** `def-3638f2`'s open ambiguity (status **ambiguous**, `:248`: real-space CROSSING number vs phase-space $(2,3)$ WINDING pattern, `:245`/`:251`) rather than resolving it. A **topology-ledger** object, sector **Cosserat / rotational (T2)** ($A1 \perp T2$), **strain- and saturation-immune**. Cites: `electron-identification.md`:108 verbatim (*"the **quantization / integer-ness** is FORM-derived (Ax2 TKI integer winding number, $Q = \mathrm{Link}(\partial\Omega, F) \in \mathbb{Z}$ of the $0_1$ unknot)"*); `electron-bound-resonator-coverage.md`:54 verbatim (*"charge lives in (2,3) Cosserat micro-rotation; the scalar cage runs A1 only"*); **`def-quant3` sense (2)** at `vocabulary-register.md`:272 (*"**invariant under any smooth deformation** (survives ionization)"* — **the failure mode IS the discriminator**), home cite `hollow-vortex-binding.md`:96. ★**Id-bind corrected (audit F4):** the pre-repair text bound `:272` to `def-3638f2` **sense (2)** — wrong on both counts. `def-3638f2`'s id comment is at `:242` and its senses are lettered **(a)/(b)** with canonical-home `ch8-alpha-golden-torus.md:29` + `electron-identification.md:77` (`:249`); `:272` sits inside **`def-quant3`** (id comment `:261`), whose senses are numbered **(1)/(2)/(3)** and whose sense-(2) home IS `hollow-vortex-binding.md`:96. `def-3638f2` is kept as an explicit cross-link.
  - **3. $Q_{\text{TANK}} = 1/\alpha$** — the **Theorem 3.1′ electron LC-tank identity**, bridging sense 1 to the charge **COUPLING** $\alpha$ (never to sense 2's integer). ★**NOT a third quantity (audit F2).** This *is* sense 1 read loaded at the electron's EM port — it **is** T10 row 1 (`theorem-3-1-q-factor.md`:164, *"**loaded / radiative $Q$** … $1/\alpha = 137.036$ … an α-baked **ECHO** at the value level"*). It is entered because its **VALUE carries an adjudicated status**, so the entry is a **STATUS register over sense 1**, not a co-equal third ledger, and its fence is **epistemic** (identity-not-derivation), not sectoral. The coined ledger name *"echo-identity register"* is retired with the restructure. Cites: `theorem-3-1-q-factor.md`:15 (the boxed $\alpha^{-1}=Q_{\text{tank}}=4\pi^3+\pi^2+\pi$) + `:19` (the value-scoped **Class-B** status, *"a **definitional identity** that predicts no independent value"*); `physics-lineage-map.md`:390 verbatim (*"Q_tank = 1/α cited as identity NOT derivation"*, also :135); `interlock-register.md`:249 (the baked `Q_TANK = 1/α` ruled **CULPABLE** for the #41 circularity, then **CI-scrubbed**). Recorded per `consistency-vs-emergence`: **cite as an IDENTITY, never as a derivation.**
- **★THE HAZARD FENCED (verbatim in the row).** Any sentence letting one sense's property leak onto another. Canonical leak (★reworded per audit F9 so no fast read of it scans as true, and the uncited Axiom-4 clause replaced by a cite): the predicate **"rides saturation"** attached to the *charge* $Q$ is **FALSE of sense 2** (invariant under any smooth deformation, `vocabulary-register.md`:272) and belongs to **sense 1**, where a tank $Q$ is a port-and-condition reading — *"all readings of the same electron LC tank's quality factor at different ports/conditions"*, `theorem-3-1-q-factor.md`:160, table `:164`–`:166`. The sentence *"charge $Q$ rides saturation"* is reachable **only** by sliding through the shared glyph. Mirror-image leak: reading $Q_{\text{TANK}}=1/\alpha$ as though it said something about the charge **INTEGER** promotes an adjudicated echo into a topological derivation — it speaks to the charge **COUPLING** and is silent on the winding.
- **★NOT a duplication of the T10 map (the structural call).** `theorem-3-1-q-factor.md`:158–170 already splits **sense 1 internally** into $\ge4$ electron-scale objects at distinct values (loaded $137$ / intrinsic $\to\infty$ / cold ring-down $\approx30.8$ / radiative floor $\approx29.98$ / per-mode $\ell$). That table lives one level **down**, inside the impedance ledger; this row sits one level **up, between ledgers** — it is the cross-owner fence the T10 map does not and cannot carry. The row says so explicitly and cross-refs rather than restating. ★**Reconciled with the F2 restructure:** entry 3 does not re-carry T10 row 1's *quantity* (that stays one level down, inside sense 1); it carries that value's **status**, which T10 states (*"α-baked ECHO at the value level"*, `:164`) but does not fence. The pre-repair text asserted both *"T10 lives inside sense 1"* and *"sense 3 is a co-equal third owner"*, which contradicted — that is audit F2, now repaired in-row.
- **★DEVIATIONS SURFACED, not taken silently.** (i) The brief's phrase **"cosmic-tank identity"** has **ZERO corpus hits** (two-method: `git grep -n -I -i "cosmic tank\|cosmic-tank"` over `manuscript research _orchestration` → 0; independent filesystem recursive grep → 0). The row uses the leaf's own name — the Theorem 3.1′ **electron** LC-tank identity. (ii) The row carries a **4th "Sector / owner" column** the six surname rows lack, justified because these are physics objects with sector ownership rather than mathematicians' surnames. (iii) §6's preamble said *"the same **surname** for different objects"*; rather than redefine it in place, an **additive** scope clause was appended (*"§6 now also carries **symbol** collisions … The surname rows are untouched"*) and the row is headed **★the first SYMBOL row**. (iv) ★**Coined organizing terms disclosed (audit F7).** The row's ledger names are **not** existing corpus vocabulary: **"impedance ledger" = ZERO corpus hits** and **"echo-identity register" = ZERO corpus hits** (two-method at `cac12881`: `git grep -n -I -i` on the ref over `manuscript research _orchestration src` → 0, independent filesystem recursive grep → 0, hyphenated variants included). **"topology ledger" has exactly ONE prior hit** — `research/2026-06-10_field-symbol-registry.md`:169, the $w_{tor},w_{pol}$ row, which files those winding integers under the column value *"topology ledger"* (both methods agree, 1/1). They are organizing labels for this row, not canonical registers, and none is a mint. **"echo-identity register" is now RETIRED** by the F2 restructure — entry 3 is a status entry over sense 1, not a ledger. (v) The §6 scope clause additionally states **§6's own admission bar** (audit F10) — ≥2 distinct objects under one name/glyph whose properties could be swapped, every sense with a verified on-`main` file:line — because §7's gate points at §0's four criteria, which govern §4 register rows and cannot govern a homonym list (§0(b) runs the opposite way). Additive; §0 and §7 untouched.
- **★FLAG-DON'T-FIX (surfaced, NOT edited) — reciprocal update-trigger gap at `physics-lineage-map.md`:602.** That leaf's §8 sibling-leaf line enumerates which thesaurus rows depend on which lineage-map entries (*"currently T8 → TH-4, T14 :490 → TH-9, T6/F8 + T4/F3 → the §6 homonym rows"*) and does **not** list the $Q$ row, whose entry-3 cell cites `:390` (with `:135` retained in-cell as a duplicate, but no longer as a tripwire dependency — see F8 below). Recorded in the thesaurus's own §7 tripwire 1 with the gap named; the lineage map is **not edited from this lane**. ★**Dependency narrowed (audit F8):** the tripwire fires on *lineage-map **T**-entries*, so the `:135` dependency is **DROPPED** — `:135` is an **F**-entry (F9, header `:127`) and duplicates `:390`'s wording; `:390` (inside **T9**, header `:379`) is the dependency of record. Narrowing the dependency was preferred to widening the trigger.
- **★ADJACENT, deliberately NOT folded in as further senses** (different glyph, same word — recorded so neither is mistaken for a fourth object): the calligraphic **$\mathcal{Q}$** is the boundary observable (*"Boundary linking number … $\mathrm{Link}(\partial\Omega, \mathbf{F}_{\text{substrate}}) \in \mathbb{Z}$ … **1D line/loop**"*, `boundary-observables-m-q-j.md`:20) — the externally-measurable **face of sense 2**, not a separate object; and Axiom 2's bracket **$[Q] \equiv [L]$**, which ★post-audit-F5 is stated **only** as `ave-kb/CLAUDE.md`:71 (INVARIANT-S2) literally carries it — *"charge as discrete geometric dislocation in the substrate; $[Q] \equiv [L]$; $\xi_{topo} = e/\ell_{node}$"* — a dimensional statement inside Axiom 2, distinct from sense 2's integer. The sharper *labels-vs-integer* contrast (*"the winding NUMBER is the conserved, dimensionless INTEGER … the dislocation that number LABELS carries $[Q]\equiv[L]$"*) is the payload of the C4 proposed sharpen at `vocabulary-register.md`:255 and now sits **entirely inside** the *"awaiting Grant ratification; NOT yet canonical … **not adopted**"* clause — the pre-repair text asserted that payload in the register's voice and disclaimed it in the next sentence.
- **★AUDIT + REPAIR PASS (2026-07-31, same day).** PR #811 was audited at head `57e95a89`; verdict **CLEAR-WITH-REPAIRS** — 2 FAILs + warns. All repairs landed on-branch, one commit per finding-class, **scope held to the same three files** (no new file, no id minted, surname rows byte-untouched): **F1** (FAIL) sense-1 discriminator re-based on the invariance group; **F2** (FAIL) entry 3 restructured as a status entry over T10 row 1; **F3** `def-3638f2`'s open ambiguity carried, not resolved by fiat; **F4** `:272` rebound `def-3638f2` → `def-quant3` sense (2); **F5** the labels-vs-integer contrast moved inside the not-adopted clause; **F7** coined ledger terms disclosed (and *"echo-identity register"* retired); **F8** tripwire dependency narrowed to the T-entry `:390`; **F9** port/condition cite added + the leak-shaped fast read inverted; **F10** §6's own admission bar stated additively. Plus one self-caught defect: the F3 edit had introduced unescaped `|` inside a table cell (11 pipes vs 5), fixed by `\|` escaping per the KB's existing convention.
- **★OPEN QUESTION ROUTED TO GRANT (audit finding 6 — PLACEMENT, deliberately NOT acted on here).** Is a §6 thesaurus row the right **home** for this content, or does the $Q$ glyph want its own `def-Q` node in `vocabulary-register.md` (as `def-quant3` / `def-3638f2` / `def-de17a0` have) with §6 carrying only a pointer? `theorem-3-1-q-factor.md`:160 explicitly notes *"No central `def-Q` exists; this row is the ownership map (an annotation, not a vocabulary-register mint)"*. **Nothing is minted and the row is not moved from this lane** — under NO-CLAIM discipline a `def-` mint needs Grant's go, and relocating the content is a corpus-state decision, not an implementer call. **Deferred, flagged, unresolved.**
- **Files:** `manuscript/ave-kb/common/theorem-thesaurus.md` (§6 +1 row + hazard blockquote; §6 preamble +1 additive scope clause incl. §6's own admission bar; §7 tripwire 1 +1 T-entry dependency + the surfaced reciprocal gap); `manuscript/ave-kb/common/index.md`:78 (§6 count + tail updated — *"…, 2 Fosters, 3 $Q$ entries"*, two quantity-ledgers plus a status entry); this docket fragment. **No other file touched**; `src/ave` byte-untouched. **Mints ZERO ids.**
- **Battery:** `make verify` **exit 0**; `verify-md-links` **gating errors 0** (kbleaf 1085 cites checked, gating 0); `verify-provenance-stamps` gating failures 0; `verify-docket-keys` no new duplicate keys; `make refresh-kb-metadata` **idempotent** (0 subtree-claims, 0 solidity, 0 leaf-references, 0 of 6 `.index/` files rewritten); pure-corpus. Every cite two-method verified at `cac12881` (`git grep` on the ref **and** an independent filesystem recursive grep — the grep-completeness discipline). ★**Re-run after the audit-repair pass, same results:** `make verify` **exit 0**; `verify-md-links` **gating 0** (kbleaf 1085 cites checked, gating 0, waived 1; warn-only 206); `verify-provenance-stamps` gating failures 0; `verify-frozen-provenance` 0 gating; `verify-docket-keys` 79 entries / 77 unique keys, **no new duplicate keys**; `make refresh-kb-metadata` **idempotent** (0 subtree-claims / 0 solidity / 0 leaf-references, **0 of 6** `.index/` files rewritten); pure-corpus; **zero ids minted** still true; surname rows still **byte-untouched**. Every cite touched by a repair was re-read at the repair head — the `def-` id-comment lines (`:242` / `:261` / `:280`), `theorem-3-1-q-factor.md`:160 + `:164`–`:168`, `physics-lineage-map.md`:390 (inside **T9**, header `:379`) and `:135` (inside **F9**, header `:127`), `ave-kb/CLAUDE.md`:71, `vocabulary-register.md`:245/:248/:251/:255/:256/:271/:272, `ave-merger-ringdown-eigenvalue.md`:91–:92, `op21-multi-mode-mode-counting.md`:10.
- **Lane-overlap check (coordination note, NOT a cite).** **Zero file overlap** with the concurrent ★UNMERGED sibling lane `docs/qell-scope-propagation` (local head `af5bca89`, not yet pushed): that lane touches `op21-multi-mode-mode-counting.md` / vol3-ch15 / app-f / `src`, this one touches only the three files above. Its op21 edit lands at `:31` and `:210`/`:229`, leaving this row's `op21`:10 cite unshifted (hunk `@@ -28,12 +28,12 @@` — no line-count change). ★**Independent convergence, worth recording.** That lane's head moved mid-repair (`af5bca89` → `a59bdcbc`, commit *"replace 'strain-immune topological integer' with the register-accurate mode-count wording"*) and its corrected op21 banner now reads *"$\ell$ is an integer **mode count** — cavity-protected only (def-quant3 sense (1), `common/vocabulary-register.md:261`), **not** a deformation-invariant winding"*. That is the **same repair** this lane's F1 makes, reached independently from the same `def-quant3` taxonomy — two lanes, one register, same answer. The F1 clause was aligned to the *"integer that can only jump discretely vs a ratio that moves smoothly"* framing; the sibling branch is **not cited** by the KB row (unmerged).
- **Class:** KB register maintenance (NO-CLAIM leaf, naming only); canonizes nothing new; no claim-graph edge added or changed.
