### ENTRY 2026-07-30-qlaw-scoping (2026-07-30): implementer — Q-law derivation SCOPING (Ruling B1 next-work); scoping only, no derivation, nothing minted

- **Scope fence, stated first.** This lane is **scoping, not derivation.** No solver was run, no engine file was touched, no claim was minted, no solidity was changed, no corpus file was modified, and the success bins are explicitly **NOT FROZEN** (freezing happens in a separate pre-reg at derivation-fire time, after the Grant walk). Deliverable: [`research/2026-07-30_qlaw-derivation_scoping.md`](../../research/2026-07-30_qlaw-derivation_scoping.md).
- **Provenance.** The Q-law is the **named next ringdown work** banked by **Ruling B1** (2026-07-21, ENTRY `2026-07-21-ruling-b1-foreword-walkback`), tracked as `_orchestration/2026-07-20_pending-rulings-and-frontier-queue.md` §2 item 5. Grant signalled frontier appetite 2026-07-30. The queued question as written: *"why the standing-v1 mΩ τ lands at −5.4%: the `r_Ω = r_ph·√(1+ν_vac)` / `ν_vac` factor / mapping"*.
- **★ The load-bearing scoping finding (F8) — the queue item is pointed at the wrong knob.** Arithmetic on already-banked corpus numbers only: at `a*=0` the standing model gives `Q = ℓ = 2` **exactly** (Ω and `r_Ω` drop out of the calculation entirely), against the **frozen generating table** the banked catalog numbers come from — `research/2026-07-20_v1-spin-mapping-adjudication_rerun.py:51`, `0.00: (0.37367, 0.08896)` → `Q_GR(0) = 2.10021`, i.e. a **cold deficit of −4.772%** versus the banked catalog deficit of **−5.44%** (the same dict's 0.64/0.67/0.74 rows reproduce `Q_Kerr = 3.071/3.176/3.487`, so cold anchor and catalog comparator are **one** table, not two sources; the KB prose values `0.3737`/`0.0890` at `vol3/claim-quality.md:199` / `qnm-quality-factor.md:18` are the rounded forms and agree to 3 s.f. at −4.74%). So **~88% of the tension is already present at ZERO spin**, where the named suspect is absent from the arithmetic; the mΩ law tracks the Kerr `Q` rise (3.07→3.49) to about a percentage point and the residual is an offset inherited from the **cold `Q = ℓ` anchor** — i.e. from the `1/ℓ` leak-fraction convention (F7), not from the spin machinery. Consequence — an **ATTRIBUTION** ceiling, not a numerical one: an `r_Ω`-only change *can* zero the catalog mean (`k = 1.0933` vs `√(9/7) = 1.1339`, i.e. `r_Ω(0.67): 2.3452 → 2.2612`, −3.6%, mean `D̄_Q = 0.00%`) but **cannot touch the cold −4.77%**, leaving **cold-broken/catalog-perfect** — an internal inconsistency and a **sharper falsifier**, not a resolution. (Reported as attribution measurement only; tuning the knob is what the Rule-11 fence forbids. Hardening receipt for F1: the exponent `p` in `(1+ν)^p` that zeroes the catalog mean is **0.355**, not `0.5` — so the square root is **neither derived nor best-fitting**.) **Class: arithmetic-consistency observation on banked numbers, offered as hypothesis H1 — NOT a derivation, NOT a claim.** Surfaced with receipts; the re-pointing of the queue item is routed to Grant/auditor, not executed here.
- **Nine findings surfaced flag-don't-fix (zero corpus files modified).** F1 the `√(1+ν_vac)` "same-correction-as-the-eigenfrequency" justification (`kerr-q-correction.md:26` ≡ `common/solver-toolchain.md:106`) is **not** a parallelism — the eigenfrequency correction *divides* a radius by `(1+ν)` (×0.778), the spin-evaluation correction *multiplies* by `√(1+ν)` (×1.134); no derivation produces the square root and none explains the sign flip. F2 **two different Ω formulas are both canonical** — Ch.2 `ω = 2Mar/(r²+a²)²` (clm-rd9cjm) vs app-F `Ω = 2a/(r_Ω³+a²r_Ω+2a²)` (clm-d9ivj1, the *exact* equatorial Kerr ZAMO); they differ by the `−a²Δsin²θ` term and that difference **is** the banked −5.44%/−4.57% variant split. F3 `ν_vac`'s **VALUE is GR-IMPORTED** via `K=2G` (`common/form-deriving-value-importing.md:87`, `:407`), capping every route's class. F4 the linear-ℓ vs `√(ℓ(ℓ+1))` mode-form fork is OPEN and sets **both** the `ω_R` numerator and the `2ℓ` damping denominator. F5 the three-way Q tension was *surfaced-not-resolved*; B1 discharged 2 of its 3 legs. **F6 honesty-lag:** the 2026-07-20 "sub-2% RETRACTED" banner did **not** propagate — as of `origin/main` @ `f7cc3e52`, **six** un-bannered sites still assert it: `vol2/appendices/app-f-solver-toolchain/kerr-q-correction.md:37`, `common/solver-toolchain.md:116`, `vol2/claim-quality.md:1097`, `common/claim-quality.md:136`, `backmatter/05_universal_solver_toolchain.tex:101` (0 banner hits in that file) and the code comment `src/ave/solvers/orbital_resonance.py:485` ("`Q error < 2% vs GR`" — a `sub-2%` grep cannot see it, which is why the first pass of this finding counted three), with the `Q_AVE` vs `Q_GR` table at `kerr-q-correction.md:39-45` computed against the **corrupt** Kerr reference; plus four companion "first-principles prediction of superradiance" sites (`kerr-q-correction.md:33` ≡ `common/solver-toolchain.md:113` ≡ `backmatter:98`, `vol2/claim-quality.md:1097`) riding the same grafted law. This contradicts the load-bearing leaf's own grade ("solidity 0.55 … disclosed-phenomenological … NOT a zero-free-parameter benchmark"). **Routing: reconciled with the concurrent lane, re-verified at ship time — that lane is now [PR #809]** (`docs/b1-retraction-propagation` @ `c451a305`, OPEN, DO-NOT-MERGE). **#809 discharges all six sites** with 🔴 Rule-12 banners, and its own entry records that two of them "were **NOT** in the four named to the lane — both found only by the exhaustive multi-pattern + two-method sweep (the single-pattern lesson, live again)": two lanes converged on the same six-site set by the same corrected method. **The one item that does NOT reconcile** is the four superradiance sites: #809 **deliberately retains** them ("Scope carve held … the superradiance limit at `ω_R = mΩ` [is] B1's standing **Q-LAW COMMITMENT** and [is] explicitly retained at every site"), while this lane flags "the first-principles prediction of superradiance from pure lattice geometry" as an underivation of the same shape as the retracted "zero free parameters" — the superradiance root is a root of the *grafted* Park-transform law. Both statements are on the record, neither reframed; **routed to Grant/auditor to adjudicate** (structure-survives vs needs-the-same-rider). F7 the `1/ℓ` leak is a **scaling** assertion with its constant set to exactly 1 by the `2π`-divides-out convention, never computed. F8 above. **F9 is a live canon INCONSISTENCY, not a gap** (repair-pass correction of an absence-claim false negative): canon **does** pin the amplitude — `A = ε₁₁/ε_yield` with `ε_yield = 1` at `vol3/gravity/ch02-general-relativity/saturating-modulus-and-backreaction.md:51` and `ε₁₁ = 7GM/(c²r)` at `ch01-gravity-yield/temporal-spatial-lattice-decomposition.md:14` (corroborated `common/vocabulary-register.md:309`: ε₁₁ "IS the Axiom-4 saturation amplitude A"), with the shear projection `c_shear = c₀(1−A²)^¼` riding the same `A` at `:60`. The **conflict** is with the temporal/EM-side convention `√g₀₀ = √S ≈ 1 − GM/c²r` at `temporal-spatial-lattice-decomposition.md:24`/`:28`, which puts `A → 1` near `r_s` and makes `A ∝ 1/√r` instead of `1/r`. Both are canonically stated — but `vol3/claim-quality.md:121` **already channel-splits** them (`r_s` = EM-transverse saturation limit; `r_sat = 3.5 r_s` = shear/bulk rupture boundary), and the ringdown is declared shear/T2, so **R2 is UNBLOCKED**; `vol4/circuit-theory/ch1-vacuum-circuit-analysis/q-g22-strain-convention.md:10` resolves only the electron/Coulomb `A` and is silent on the gravitational case.
- **Six routes enumerated, none pursued** (§2): R1 radiation-resistance leaky-cavity `Q` in the **shear** channel (the electron `Q = ωL/R_rad` template — with the sector-ownership warning that the template is an **EM**-channel calculation and the ringdown is a shear object); R2 graded-`Z_shear` WKB turning-point `Q` (**executable** — the canonical shear profile is `A = ε₁₁ = 7GM/c²r`; remaining prerequisites are narrow: confirm the channel-split reading and the `A` vs `A²` kernel-argument power, plus the open `√S`-vs-`S^¼` exponent flag at `vol3/gravity/ch02-general-relativity/k4-tlm-lensing-validation.md:25-35`, which IS R2's WKB integrand); R3 light-ring barrier-transmission cavity — **the only route that would *retire* F1 rather than re-justify it**, and whose by-product is an **echo delay** that could kill the corpus's own already-banked echo prediction against published LIGO limits; R4 Op21 mode-counting re-derived in the co-rotating frame (cleanest class ceiling **at the cold anchor only** — the mode-count is `ν_vac`-free but the co-rotating spin term still rides `Ω(r_Ω(ν_vac))` unless R4 re-derives the evaluation radius, which would also discharge F1); R5 Ω-form adjudication (hygiene, **not** a derivation, run first, with an explicit Rule-11 fence that picking the exact-ZAMO form *because* −4.57% is closer is retuning and inadmissible); R6 the mode-form fork (prerequisite, not a route). Plus **R0 — no route closes**, honestly enumerated and explicitly not a failure.
- **Class ceiling applied to every route (consistency-vs-emergence).** Because `ν_vac`'s value is GR-imported and both `r_sat = 7GM/c²` and `r_eff = 49M_g/9` ride it, **no route can produce a value-level emergence claim for an absolute `Q` or `ω`.** Absolutes cap at **FORM-emergence / VALUE-consistency**; only `ν_vac`-free dimensionless **ratios** are emergence-capable — and **dimensionlessness is necessary, not sufficient: `ν_vac`-CANCELLATION is the actual test, checked per candidate.** `ω_R(ℓ')/ω_R(ℓ)` **survives** (genuine `(1+ν_vac)` + `x_sat` cancellation; corroborated `research/2026-07-20_ringdown-systematics_derivation.md:45`). **`Q(a*)/Q(0)` is STRUCK from the list (repair-pass correction):** `Q = ℓ/(1 − mΩ/ω_R)` so the ratio is `1/(1 − mΩ/ω_R)`, and **both** factors carry `ν_vac` (`ω_R ∝ (1+ν_vac)`; `Ω` evaluated at `r_Ω = r_ph√(1+ν_vac)`) — recomputed on the frozen driver's own chain, at `a*=0.67` the ratio is **1.500** at `ν_vac=2/7` vs **2.492** at `ν_vac=0`, a **+66% swing**. R3's echo-delay-to-ringdown-period ratio remains a **candidate, UNTESTED** — the pre-reg must carry its explicit `ν_vac→0` sensitivity run, not a plausibility argument. Likewise R4's "`ν_vac`-free" is true **only of its cold half**: the co-rotating spin term still rides `Ω(r_Ω(ν_vac))` unless R4 re-derives the evaluation radius too. This is to be written into the derivation pre-reg header, not discovered afterwards.
- **Seven walk questions routed to Grant IN CHAT before the derivation may fire, plus one demoted confirm-note** (`pre-test-physics-check`, trigger 8, asked BEFORE design per the Rule-16 strengthening): Q1 the ontology one-liner — is the ringdown a **leaky cavity with a radiative port**, an **impedance-mismatch ring-down on a graded taper**, or a **mode-conversion drain**? (three mutually-exclusive pictures are all implied in canon; "both" would be a substantive correction to Op21's ratified **single-channel** classification at `op21-multi-mode-mode-counting.md:31,36`, not a detail). Q2 if `Γ_shear = −1` is a *perfect* reflector, the `Q` is set entirely by the **outer** radiation impedance and the inner mirror contributes nothing — making the current "Q from the Γ=−1 TIR boundary" attribution a mislabel of where the physics lives. **Q3 DEMOTED to a confirm-note** (its premise was the false F9 absence claim) — canon already answers "two surfaces, one per channel" via the `vol3/claim-quality.md:121` channel-split; the narrow residual left to confirm is the kernel-argument power, `A = ε₁₁` (what `:51` writes) vs `A² = ε₁₁` (what the first draft assumed), which moves the WKB integrand's weak-field falloff by one power of `r`. Q4 is Ω an imported GR ZAMO rate, an approximation of one, or a **substrate** impedance-convolution rate that merely coincides with frame-dragging weakly? Q5 is there a physical reason a *rotational* evaluation radius picks up `√(1+ν)` and **grows**, or should the factor be *replaced* by a computed turning point? Q6 is the `1/ℓ` leak a **counting** statement (exactly `1/ℓ`) or a **scaling** statement (`c₁/ℓ`)? Q7 do we accept the echo-prediction exposure in this lane? **Q8 is the B1-ratified cold `Q = ℓ` anchor in scope at all — this question decides whether the lane is worth firing**, because if it is out of scope the ceiling becomes an **attribution** ceiling: a spin-side-only route *can* zero the catalog mean but leaves the cold −4.77% untouched, so the reachable outcomes are bin (c) or a **cold-broken/catalog-matching** variant of bin (b).
- **Five DRAFT bins, NOT FROZEN, with a reachability audit** (§4): (a) RESOLVED — a route derives the mapping from inputs fixed pre-comparison AND reproduces `Q` within a pre-reg-stated tolerance (draft proposal `|D̄_Q| < 3%`, the band already frozen for `ω_R`) at the catalog spins **and** at the cold anchor; (b) DEEPENED — a route derives a **different** value → **tension deepens, banked honestly, THIS IS A GOOD OUTCOME** (mechanism named rather than unexplained); (c) NO ROUTE CLOSES → v1 stays disclosed-phenomenological at solidity 0.55, `build_status "use as input only"`, −5.4% banked deeper with the F8 decomposition attached; (d) SCOPE-BLOCKED by the Q8 answer → lane may be **declined before firing**; (e) CANON-INCONSISTENT → flag-don't-fix. All five reachable; (e) is **already partially triggered** (F2, F6 **and F9** are live canon inconsistencies today). **Rule 11 fence stated up front:** the −5.44% is a clean banked tension, this lane explains it or banks it deeper, it is **not** a rescue-debug, and **no retuning of v1 is admissible** — not the mapping, not the `√(1+ν_vac)` factor, not the Ω-variant choice, not the leak constant. Bins may be **tightened** before freezing; they may **not** be loosened after results are seen.
- **Non-goals fenced.** Does not re-open the v1↔v2 fork (B1 ruled it); does not revisit the frame/table corrections (#774 settled them); does not revisit `ν_vac`'s value provenance (GR-imported, #261 closed); does not land any manuscript or `COLLABORATION_NOTES` entry — F1–F9 are surfaced for the **auditor** lane to land, Q1–Q8 for **Grant**.
- **Lane mechanics.** Self-isolated throwaway worktree off `origin/main` @ `f7cc3e52`; branch `research/qlaw-derivation-scoping`; incremental commits (skeleton → one section per commit); PR opened `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. Step-0 skill-selection plan + retro-pass on applied-set drift are Appendix A of the scoping doc.
