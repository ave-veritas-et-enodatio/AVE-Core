# R52 — K/G = 2 is an OPERATING POINT, not an identity: the definitional ruling, the Q-point consolidation, and a verified convention collision (2026-08-12)

**Provenance, stated first.** This records a Grant walk held in chat on 2026-08-12, immediately
after R51 (`2026-08-12-ruling-r51-a1-two-objects-carve.md`, merged #958) and the boundary-response
lane (#959, whose headline was refuted at adversarial review). **It is chat-walk grade and
UN-AUDITED**, with one exception: §4's collision arithmetic and §6's derivation-status finding were
receipted by a read-only corpus sweep and are marked accordingly. **§1-§2 are DEFINITIONAL — a
vocabulary and question-shape ruling, not a physics claim.** §5 is a carried candidate, not a
finding. Nothing here mints, edits a leaf/register/axiom/ruling, or moves a solidity.

**Why it exists.** The corpus has asked "derive K = 2G" three times — PR #261 (provenance), PR #506
(Cauchy-grade srs elastic tensor), PR #508 (48x48 chiral micropolar Bloch) — and returned
NOT-FORCED three times. This record rules that the question was **malformed**, so the negatives
were correct answers to the wrong question, and re-poses the well-formed one.

---

## §0b — ⚑ SECTOR / REGIME DECLARATION: every moduli word here is a CONTINUUM-MODEL object

**Grant's catch, mid-draft (2026-08-12), and it scopes everything below.** The vocabulary this
entire question is posed in — **K, G, ν, λ, μ, Voigt–Reuss–Hill averaging, Born–Huang (the *method
of long waves*), Zener anisotropy, the elastic tensor `C_ij`, the Cauchy relation, internal-strain
relaxation, hydrostatic pressure** — is **continuum-elasticity vocabulary**. Not one of those
objects exists until the long-wave limit has been taken. The **substrate-native** objects are:
nodes, bonds, the bond spring constants `k_a` / `k_s` and their ratio, port amplitudes, `u₀`.

The corpus already ratified the scoping and it was not applied here: `def-c0ss3r`
(`common/vocabulary-register.md`) — the Cosserat continuum is grade **MODEL-OF, REGIME-SCOPED**,
the long-wave continuum limit, *"exact below the band edge **by construction of the continuum
limit**"*, and above the band edge *"the continuum description ceases to be co-equal."*

**Four consequences, and they sharpen every section below:**

1. **"The lattice does not force K = 2G" is partly a statement about the EXTRACTION PROCEDURE, not
   only about the lattice.** `K/G` is not a lattice quantity; it is a readout of a continuum limit
   taken *of* the lattice. The lattice has no opinion about a ratio it does not carry. This is why
   `vacuum-poisson-ratio.md`:18-22's *"an averaging choice, not a per-direction lattice output"* is
   the load-bearing sentence in the whole file.
2. **"K = 2G is GR-imported" is a CONTINUUM-TO-CONTINUUM import.** Both sides — the trace-reversal
   condition and the moduli ratio — live in the same model layer. That is a far weaker and less
   alarming statement than "the lattice failed to derive it," which is how it has been read for
   months.
3. **§4's collision is reclassified.** It is a disagreement between **two continuum-extraction
   methods** (a Session-13 discrete bond sum vs #506's relaxed Born–Huang long-wave extraction)
   applied to the same discrete parameter — not two lattice results contradicting each other. That
   does not dissolve it (K = 0 vs K/G = 2 is not an averaging nuance), but it names the axis the
   adjudication must run on: **which extraction, on which carrier, is the ratified one.**
4. **§5's kill-check must be posed NATIVELY.** A `Γ = 0 ⇒ K/G = 2` derivation carried out entirely
   in continuum moduli would reproduce the same import one layer down. The native form of the
   question is: **does the reflectionless condition select a BOND RATIO?** If reflectionlessness
   picks `k_a/k_s`, the selector is substrate-native and the continuum readout follows. If it only
   picks `K/G`, the selector is another continuum-layer statement and the import is untouched.

**Scope amendment to §1's ruling, in one line:** `K/G = 2` is the **continuum-model readout of a
discrete operating point**. The ruling stands; this is its regime declaration.

## §1 — THE DEFINITIONAL RULING

**`K/G = 2` is an OPERATING-POINT READOUT. It is not an identity, a relation, a lock, or a magic
angle.**

The substrate supplies a **one-parameter family**, not a point. Verbatim receipts:

- `research/2026-07-04_srs-elastic-tensor_result.md`:19 — the Cauchy-grade elastic tensor of the
  chiral srs-z3 net is *"a **ONE-PARAMETER FAMILY in ρ = k_a/k_s**"*; :21-24 — *"It does **NOT
  force ν = 2/7**. ν_Hill = 2/7 is reached **only** at an externally-supplied `ρ* ≈ 9.7734`"*.
- `research/2026-07-04_srs-chiral-micropolar_result.md`:24-29 — *"**But it does NOT reduce the
  Stage-1 ρ-family to a point** — ν_eff(ρ) with the coupling ON is still a one-parameter family
  (stable-branch span ν_eff∈[−0.124, +0.391]), and it does **NOT force ν=2/7**"*; :138-142 — *"**NO
  distinguished point emerges.** ... Where ν_eff ON crosses 2/7 (ρ≈4.46) is NOT a
  geometrically-distinguished point"*.

**A one-parameter family never forces a point.** Therefore "derive K = 2G" cannot be answered by any
lattice computation, in principle — not for want of a better solver. The three NOT-FORCED verdicts
are the family reporting that it is a family.

**K and G remain genuine mechanical moduli.** This ruling does NOT demote them, and the strong
reading floated during the walk (*"K = 2G is purely a projection parameter, not a modulus"*) is
**REJECTED** on the corpus's own stability arithmetic: `srs-elastic-tensor_result.md`:135-137 —
*"**K (bulk modulus) is NEGATIVE for ρ < 2** — mechanically unstable (the lattice would collapse
under hydrostatic pressure). **K = 0 EXACTLY at ρ = 2**"*. A projection coefficient cannot make a
lattice implode. K holds energy and carries units; only the **ratio** is the operating-point object.

**The well-formed questions**, replacing the malformed one:
1. **What selects the operating point?** (the live physics question — see §5)
2. **Where on the family is the vacuum biased?** (a measurement, currently read out as `K/G = 2` in
   moduli coordinates and as `u₀* ≈ 0.187` in over-bracing coordinates — see §3)

**The EE frame that makes this unambiguous** (and it is the corpus's own ratified vocabulary): a
device supplies a **load line**; a bias network selects the **Q-point**. One does not derive a
Q-point from the semiconductor. Axiom 5 clause Q already canonized *the DC operating point* for the
bias sector (R43); the same word governs here, one sector over.

## §2 — VOCABULARY (ruled)

- **RETIRED as load-bearing nouns for `K/G = 2`:** *identity*, *relation*, *lock*, *magic angle*.
  Each imports a different (and wrong) obligation — "identity" implies true-by-definition,
  "relation" implies constitutive-and-derivable, "lock" implies a holding mechanism, "magic angle"
  implies a distinguished geometry that `srs-chiral-micropolar_result.md`:138-142 explicitly denies.
  Existing occurrences are **preserved per Rule 12**; this ruling governs new text and any
  re-derivation of an old passage.
- **CANONICAL:** **the moduli Q-point** (or *the moduli operating point*). `K/G = 2` is its
  **readout in moduli coordinates**.
- **ν is a KINEMATIC / SHAPE ratio, not a stiffness.** A Poisson ratio is transverse strain per
  axial strain — dimensionless, a description of the *shape* of the response, which is why it is
  legitimately called a projection while K and G are not. Canon already says the operative half:
  `vacuum-poisson-ratio.md`:18-22 — *"ν_Hill = 2/7 is the **isotropic (Voigt–Reuss–Hill) average**
  scalar at K = 2G; the real srs/K4 lattice is materially **anisotropic** ... so 2/7 is an
  **averaging choice, not a per-direction lattice output**"*.
- **THE MODE-COUNT GLOSS IS THE IMPORT; THE KINEMATIC GLOSS IS CORRECT.** Reading ν = 2/7 as "2
  compliance modes over 7 total modes" is **not licensed**: PR #957's D3 established that the
  denominator `(3K+G)/G` **slides** (4, 7, 8, 10 at K = G, 2G, 7G/3, 3G), so the 7 is 7 *because*
  `K = 2G`, not because anything counted modes. That 7 is a different object from the per-cell
  kinematic inventory 7 (`vol9/ch3-pin-port-configuration/index.md`:17), which stands on its own
  footing. **Do not merge the two sevens.**

## §3 — THE IDENTITY COLLAPSE (three open items are one)

Applying the identity-collapse probe (`ave-ee-first-mapping` trigger 7 — two corpus structures
running parallel may be one bench object), these are **one question in three costumes**:

| costume | site | coordinate |
|---|---|---|
| "K = 2G **at u₀\* ≈ 0.187**" | `clm-iouqn9`; `q-g47-substrate-scale-cosserat-closure.md`:22-30 | moduli ratio + over-bracing parameter |
| "u₀\* ≈ 0.187 is asserted/back-fit, underdetermined, NOT forward-derived" | `claim-quality-closure-roadmap.md`:32 (FRAMING RETRACTED 2026-06-14) | over-bracing parameter |
| the three-number guard `u₀* = 0.187 / σ₀ = 0.176776695 / arc*` | `_orchestration/2026-08-10_pretension-brief.md` | pre-tension / quench mechanism |

**The single question: WHAT SETS THE VACUUM'S MODULI Q-POINT.** `K/G = 2` is that Q-point read out
in moduli coordinates; `u₀*` is the same Q-point read out in over-bracing coordinates; the
pre-tension thread is a **mechanism candidate for the selector**. They have been run as separate
lanes. **RULED: they are one arc.** Lane briefs and register rows should cite the arc, not
re-open the sub-questions independently.

## §4 — ⚑ A VERIFIED CONVENTION COLLISION (blocker for the collapsed arc)

**Receipted this walk (arithmetic re-derived; sites quoted at `origin/main`).** Two canonical
chains use the **same** family parameter `ρ = k_a/k_s` and disagree at the **same** value:

- **Q-G47 discrete chain** (`q-g47-substrate-scale-cosserat-closure.md`:58; `clm-bjceop`): at the
  stated *"K=2G operating point (`k_a = 2/7`, `k_s = 1/7`)"* — i.e. **ρ = 2** — the Session-13
  discrete formulas `K₀ = 4k_a + 8k_s` and `G₀ = 8k_s` give `K₀ = 16/7`, `G₀ = 8/7`, hence
  **`K₀/G₀ = 2`**.
- **PR #506 srs-z3 Born-Huang** (`srs-elastic-tensor_result.md`:19, :135-137): with the same
  `ρ = k_a/k_s`, **`K = 0` EXACTLY at ρ = 2** (and `K < 0` below); `K/G_Hill = 2` occurs only at
  **ρ* ≈ 9.7734**.

**Same parameter, same value, incompatible readings.** Candidate cause, **NOT adjudicated here**:
different carrier and method — a K4 Session-13 discrete bond sum versus an srs-z3 long-wave
Born-Huang with internal-strain relaxation. If the Session-13 discrete formulas are on a
superseded carrier, the chain they support is on that carrier too.

**Blast radius (flagged, nothing retracted):** `ξ_K1 = 8/3`, `ξ_K2 = 32`, and the cross-check
`ℓ_c/ℓ_node = √6` all descend from the `k_a = 2/7, k_s = 1/7` inputs
(`q-g47-substrate-scale-cosserat-closure.md`:58). `ℓ_c` is consumed downstream by #959's tank
frequency and by the R1a/R1b `γ_c` branch question. **This collision is the collapsed arc's FIRST
blocker** and must be adjudicated before any Q-point selector work is trusted.

## §5 — THE SELECTOR: the Γ = 0 candidate (CARRIED, NOT BANKED)

The best-supported candidate for *what selects the Q-point* is already in engine code, and it is
**not** the trace-reversal naming. `src/scripts/verify/k2g_constitutive_rho.py`:16-18, verbatim:

> `K=2G is the SYM (impedance-matched, Gamma=0 gravity-null) operating point where eps,mu co-scale
> -> Z_eff=Z0 -> the operating-point factor is INVARIANT (=1) all along the K=2G branch.`

and `:69` — `K=2G is undefined off-SYM (Z!=Z0)`.

**Read physically: `K/G = 2` may be the ratio at which grading the vacuum produces NO REFLECTION.**
Bias it, load it, put mass in it — and along that branch the medium stays impedance-matched to
itself, so gravity is pure re-tuning and never scattering. This is a **mechanical** statement
(impedance is mechanical), so it is consistent with §1's ruling that K stays a modulus.

**Three independent arrivals at the same structure**, none citing the others: the §9 co-scaling walk
on #955 (*hold the ratio, pump the product — reflectionless*); R51's ratified carve (`Γ = 0` while
the bonds hold); and this K=2G branch condition.

**Status: CANDIDATE.** It rests on one engine-code comment. **Kill-check (the lane's target):
derive that `Γ = 0` under ε–μ co-scaling requires `K/G = 2` exactly — or the naming collapses and
the selector is still unknown.** Unlike the trace-reversal route (§6), this is a computation rather
than a name.

## §6 — DERIVATION-STATUS FINDING (receipted)

**No derivation from GR trace-reversal to `K/G = 2` exists anywhere in the corpus.** Five sites
*assert* the identification — `q-g47-substrate-scale-cosserat-closure.md`:28,
`common/claim-quality.md`:1096, `secondary-scale-shared-b-node.md`:38,
`vol_3_macroscopic/chapters/01_gravity_and_yield.tex`:15,
`vol_9_vacuum_datasheet/chapters/19_calibration_parameter_justification.tex`:49 — and **none**
exhibits a step from `h̄_μν = h_μν − ½η_μν h` (or a TT projector, or de Donder) to `K/G = 2`. Canon
concedes it in its own words at `trampoline-analogy-primer.md`:439 — *"**Neither is a
first-principles K4-topology derivation.**"* — while :435 already calls `K = 2G` *"the 3D
macroscopic **projection**"*. `form-deriving-value-importing.md`:116-118 records this as the one row
where *"even the **FORM** is imported"*.

**Also NOT FOUND:** any corpus statement of what the "2" physically IS — no tensor-rank, spin-2,
two-polarization, matching-condition, or Cauchy-type account. (A tensor-rank/spin-2 reading was
floated in the walk by the orchestrator and is **NOT adopted**: unsupported in corpus and not
derivable, where §5's candidate is both better-supported and testable.)

## §7 — ROUTED: the task-list items this ruling re-points

Each currently poses the malformed question. **Flag-don't-fix — no edits ride this record;** the
doc lane executes, re-pointing each to §1's well-formed pair and to the §3 arc.

| # | site | current framing | re-point to |
|---|---|---|---|
| 1 | `clm-crbl60` strengthen-by (`vol1/claim-quality.md`:665) | *"Justify the Cauchy relation K_vac = 2G_vac ... STAYS OPEN"* | the Q-point selector (§5); note the relation-class framing is retired per §2 |
| 2 | `clm-iouqn9` strengthen-by (`common/claim-quality.md`:1113) | *"Exhibit K(u₀), G(u₀) and show the K=2G root lands at u₀*≈0.187"* | well-formed as a **load-line + Q-point-location** task; re-word, keep |
| 3 | `clm-bjceop` strengthen-by (`.index/strengthen-by.jsonl`:175) | *"Derive the K=2G operating-point spring constants k_a=2/7, k_s=1/7"* | **BLOCKED on §4's collision** — these are the collision's own inputs |
| 4 | `clm-satnec` rationale | *"the standing K=2G eigenmode-existence open item"* | the arc (§3); the eigenmode question survives, the K=2G framing does not |
| 5 | `program-arc-map.md`:118 | the standing K=2G eigenmode-existence item | same |
| 6 | `claim-quality-closure-roadmap.md`:32 | the u₀\* derivation row | fold into the §3 arc |
| 7 | R51 §3's three K-identity readings | constitutive / quench-IC / boundary-echo | supersede with §1: K is constitutive-class **and** the ratio is a Q-point; the three readings become candidate **selectors**, not candidate identities |
| 8 | the `K_discrete` separator lane (R51 §4 / #959 brief §5) | compute K4 longitudinal stiffness discretely | **RE-SCOPE**: (a) adjudicate §4's collision first, (b) then the §5 `Γ = 0` derivation |

**Additionally routed (independent of this ruling):** the **#935 demotion sweep is queued but not
executed**. `_orchestration/2026-08-10_inventory-review.md`:508 — *"The demotion sweep is verified
but not executed."* Live consumers with **no** in-file stamp include
`src/ave/topological/cosserat_field_3d.py`:721/:727/:756 (the coded `½K(∇·u)²` driving
`ρü = −∂W/∂u`), `master-equation.md`:26, and the **SOLID**-status definitions
`vocabulary-register.md`:870 (`def-l0ngdu`) and :885 (`def-uatk1s`). This gap is load-bearing right
now and belongs to the engine/doc lanes.

## §8 — CORRECTIONS TO THE WALK'S OWN RECORD (orchestrator, dated 2026-08-12)

Stated so the walk is not read as cleaner than it was:

1. **"Every mechanical use of K=2G is already stale post-#935" — FALSE.** The demotion is queued,
   not applied (§7). `vocabulary-register.md`:594 does not contain `K=2G` at all; the K=2G-bearing
   `½K(∇·u)²` sites in that file are :870 and :885, both SOLID.
2. **"2/7 may be off the ν_eff family" — FALSE.** It is ON the family: the coupling-ON curve crosses
   2/7 at ρ ≈ 4.46 (`srs-chiral-micropolar_result.md`:138-142). *"Moves away from 2/7"* was about
   the specific point ρ* = 9.7734, not about reachability.
3. **The strong projection reading — REJECTED** by §1's stability argument, which the walk's own
   receipt sweep surfaced against it.

## §9 — KILL-CHECKS AND FENCES

**Kill-checks this ruling ships:** (i) §4's collision — if the two chains reconcile trivially
(different normalizations), the blocker clears and only the wording changes; if they do not, a
canonical chain is on a superseded carrier and `ξ_K1`/`ξ_K2`/`ℓ_c` inherit it. (ii) §5's `Γ = 0`
derivation — if `Γ = 0` does not force `K/G = 2`, the selector is unknown and §5 is banked dead.
(iii) §3's collapse — if `u₀` and `ρ` are shown to parametrize **different** families, the three
items are not one arc and this consolidation is withdrawn.

**Fences:** §1-§2 are definitional and do not touch any physical claim's solidity. No leaf, axiom,
register, ledger, ruling, or engine file is edited; nothing is minted; no bin moves. The two
quaternionic-shaped structures stay distinct (R51 §7). Per the ratified mission posture (R51 §6),
an imported Q-point is **not** a defect — it is a boundary condition, and identifying its selector
is the program's target.
