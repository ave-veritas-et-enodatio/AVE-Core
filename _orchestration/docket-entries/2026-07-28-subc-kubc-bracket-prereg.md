### ENTRY 2026-07-28-subc-kubc-bracket-prereg (2026-07-28): implementer — OWED-1 SUBC/KUBC bracket prereg FROZEN alone (the missing companion fragment, written at PR#802 repair time)

- **★WHEN THIS WAS WRITTEN, stated first so nothing is backdated.** The prereg it documents was committed and pushed ALONE at `1288e288` on 2026-07-28 (COMMIT 1, the frozen-first discipline). This fragment was **NOT** written at that time — the lane process died immediately afterwards (result doc §7 D-1), and the fragment was never created. The frozen prereg's own provenance block nevertheless forward-cites it (`_orchestration/docket-entries/2026-07-28-subc-kubc-bracket-prereg.md`), so from `1288e288` until this commit that citation pointed at a file that did not exist — a **dead cite inside the frozen prereg**, invisible to the markdown link gate because it sits in an inline code span. The PR #802 adversarial review found it (finding F10). **The frozen prereg is NOT amended** (a frozen document with a defect gets disclosed and routed, never edited); instead the missing fragment is supplied here, dated honestly to its actual writing, and the defect is disclosed in the result doc's deviations (`research/2026-07-28_subc-kubc-bracket_result.md` §7 D-10).
- **What the prereg freezes** (`research/2026-07-28_subc-kubc-bracket_prereg-FROZEN.md`, md5 `9e467d603d5745c1e05094506234e5d2`, BYTE-UNTOUCHED since `1288e288`): the SUBC (STATIC uniform-traction, pure-Neumann) extraction — boundary condition, discrete load set, Hill-lemma macroscopic-stress identification, energy functional, active-node restriction and translation-only null-space handling — plus the variational argument that SUBC is the true LOWER-bound counterpart of the shipped KUBC (§1); the THREE ratio definitions with per-definition rigor status (§2 PRIMARY / CONSERVATIVE-theorem-grade / the shipped core ESTIMATOR which is neither); the exact bracketed-configuration list (§3); the frozen gates including the `SUBC ≤ KUBC` VOID condition and the §4B deliberately-broken-extraction fireability self-tests; the frozen READ with an exhaustive, reachability-proven outcome partition and a PRE-REGISTERED fallback (§5); the mutual-satisfiability audit + full design-time pilot disclosure (§6); the ★SCOPE FENCE that this brackets `K` ONLY and never `ρ` (§7); the undetermined-fork guard (§8); and the ledger + owed-follow-ons fence (§9).
- **Provenance.** Grant-fired 2026-07-28, verbatim `[sic]`: *"yes, as above, so below, we should assume we have both boundaries right?"* Discharges **OWED-1**, named co-equally by merged **#782** (`research/2026-07-21_rve-aggregation-bench_result.md` §8.2 item 2) and merged **#796** (`research/2026-07-22_vessel-state-rve_result.md` §9). Every `K_eff` the corpus banked from this bench family is KUBC-measured, i.e. the stiff / upper-bound-class side; with one side there is a BOUND, not a BRACKET.
- **Result-doc pointer requirement stated in the prereg:** the result must carry the machine-readable `Prereg-file: research/2026-07-28_subc-kubc-bracket_prereg-FROZEN.md` line, and every `Frozen:`-labelled criterion must byte-match a quoted token in the prereg (the LIVE frozen-provenance gate, gating date 2026-07-22).
- **Lane fences frozen with it:** engine `src/ave` BYTE-UNTOUCHED; no `manuscript/` or KB leaf edit; no port-register, falsification-ledger or matrix edit; **no edit to the merged #782 or #796 result docs regardless of outcome** — consequences ROUTED to Grant / the auditor lane only.
- Companion fragment for the RUN: `_orchestration/docket-entries/2026-07-28-subc-kubc-bracket.md`. Branch `research/subc-kubc-bracket` off `origin/main` @ `c8ceacc3`; PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.
