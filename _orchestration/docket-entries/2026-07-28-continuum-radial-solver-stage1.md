### ENTRY 2026-07-28-continuum-radial-solver-stage1 (2026-07-28): implementer — continuum radial-solver STAGE 1 BUILT + CERTIFIED (instrument only; D2–D5 executed, D1 held)

- **Grant ruled the charter's open decisions** (`research/2026-07-21_continuum-radial-solver_CHARTER.md`, merged #789) verbatim `[sic]`: *"D2: disclosed, D3: follow rec, D4: do it, D5: do the rec"*. **D1 (the sector-crossed `c²`) REMAINS HELD** — Grant is walking it. Stage 1 was therefore built and certified **D1-INDEPENDENT**: no gate consumes a `c²` and none is evaluated anywhere in the outputs.
- **Artifacts.** FROZEN prereg `research/2026-07-28_continuum-radial-solver-stage1_prereg-FROZEN.md` (committed ALONE and pushed before any solver code); driver `research/drivers/continuum_radial_solver.py` + `continuum_radial_solver_stage1_results.json`; result `research/2026-07-28_continuum-radial-solver-stage1_result.md`. Engine `src/ave` **BYTE-UNTOUCHED** (`git diff --stat origin/main -- src/ave` empty); mints no `clm-`/`def-`; no leaf / port-register / ledger / charter edit.
- **Formulation (D3 = the charter's recommendation, T1 (a)+(c)):** frequency-domain **transfer matrix PRIMARY** (exact analytic layer solutions, no CFL floor) + analytic **matched-asymptotics BACKSTOP**. The radial channel is derived for a spherically **ORTHOTROPIC** medium, so R1's hoop/radial split enters natively (isotropic is the `β²=2`, `ν=3/2` special case) — not a Cartesian or continuum-Helmholtz default.
- **★VERDICT: CLASS A — CERTIFIED. Instrument-class only; banks NO physics verdict.** All nine frozen gates PASS on **both source fittings** (prescribed displacement / traction) and **both profile arms** (isotropic baseline / D5-measured orthotropic vessel): Lamé exterior `∇·u=0` **`2.70e-14`** (frozen `≤1e-10`; shell agreement `6.07e-3` vs the inherited #782 `0.25`); uniform-medium NULL **`0.0` exact** (frozen `≤1e-12`); orthotropic→isotropic reduction **`0.0` exact**; TM↔matched-asymptotics **`4.86e-7`** (frozen `≤1e-6`); Ax3 energy balance **`1.41e-13`** (frozen `≤1e-10`, `Im T = 0`); layer refinement **`1.04e-4`** (frozen `≤1e-3`); amplitude independence **`2.96e-13`**; matching-radius independence **`5.01e-11`**; band conditioning **`5.02e10`** over `k·r_core ∈ [1e-8, 4]` (frozen `≤1e12`). Determinism: two independent full runs, identical digest `1012a94d…`. Runtime `1.11 s`.
- **★All four gate-fireability self-tests FIRE** (a gate that cannot fail is not a gate — `gate_fireability_selftest_pass = True`): FT-1 mis-specified exteriors (a residual grade past the contour; the cage's orthotropy leaking outward) → `lame_ratio` `4.65e-3` / `3.37e-3` (`≥1e-3`); FT-2 the **structural-null lens** — a `S_rail=0.99` contrast → `rho_S = 1.01e-4` (`≥1e-5`), proving the null observable is LIVE and not identically zero by construction; FT-3 an absorbing shell (`Im/Re = 1e-3`, the friction R7 forbids) → energy imbalance `2.92e-1` (`≥1e-2`); FT-4 the same TM↔MA comparison OUT of regime at `k·r_core = 3` → rel `6.29` (`≥1e-1`), proving G4's agreement is a real breakable agreement between two independent derivations, not two evaluations of one code path.
- **★FLAG-DON'T-FIX 1 (load-bearing, routed to Grant + the auditor lane; the charter is NOT edited).** The `n = 0` spherically-symmetric channel the charter §2 specifies **cannot host either object the charter's C1 and C2 name.** F2's `p = 2` is derived in `research/2026-07-20_deep-rail-kscaling_derivation.md` §1 for a source **DISPLACED from centre** — an `n = 1` **dipole** statement; and C2's structural added-mass is the `n = 1` effective-**density** coefficient, while the `n = 0` monopole coefficient carries effective **compressibility**. This is a property of the CHANNEL, not the implementation, and is the class of finding D4's "build now" exists to surface at integrator time (Rule 10). The `n = 0` centred-source exponent is **analytically** `0` (the `k⁴` monopole prefactor cancels in the caged/uncaged ratio) — measured `p ~ 5e-8`, i.e. the theorem reproduced to eight digits, **NOT** a test of F2. The `n = 1` (coupled P–S) extension inherits every gate certified here; whether stage 2 builds it, or the charter §2 object is amended, is Grant's / the auditor's call.
- **★FLAG-DON'T-FIX 2 (KEEP-BOTH, routed).** Charter R5(b) asks for `ρ_N → 0` in the uniform-medium null; #775's `ρ_N` is a caged/uncaged POWER RATIO whose no-scatterer value is `1` by construction. Both cannot hold of one symbol. Stage 1 redefines **neither in place**: it freezes and ships BOTH (`rho_N` = the #775 ratio; `rho_S ≡ |rho_N − 1|` = the charter's residual). Convention question routed.
- **D5 executed, with its gap disclosed.** The profile gains are computed at runtime from the shipped `research/drivers/vessel_state_rve_results.json` (`min_kse`, `peak_A`, `k_a_RHO_STAR`, `k_s_KS0`) — no vessel-state number retyped from prose: `radial_gain = 0.352994`, `hoop_gain = 2.363067` (`ε_radial = −0.0662009`, `ε_hoop = +0.1394675`). **But D5's instruction is currently under-supplied by the corpus:** #796 shipped scalar EXTREMA plus a C-V reconstruction its own result doc grades corroborative-only (`K(ε_bias)` flat to `0.059 %`) — there is no resolved radial `ε(r)`, so the radial SHAPE is tagged `[engineering-choice]` in every output. #796's `r_Z` is carried as **MIXED-provenance** exactly as its ledger correction requires.
- **D2 executed; the D1 lever quantified without picking.** The R2 two-term `ρ_eff` decomposition is kept SEPARABLE in every row (`columns_kept_separable = true`; the summed value exists only under an explicit `…_DOWNSTREAM_ONLY` key). Term (i) structural `= 1` with the honest flag `structural dipole term NOT BUILT (n=1 channel absent in stage 1)`; term (ii) `= β·φ_sf` over the disclosed sweep with the symbolic label `beta = (u_trapped · P) / (rho_0 · c_x^2) — c_x SYMBOLIC, D1 HELD; no c^2 evaluated`. `r_Z` family (`φ_sf = 0.489462`, `K` from the lattice input, never recomputed): `β=0 → 0.543577`, `β=1 → 0.663400`, `β=3 → 0.854019`. **The D1 lever is reported as a pure dimensionless candidate-swap ratio with NO candidate designated:** `(c_P/c_S)² = 3.2864` — i.e. the sector choice alone moves `β` by MORE than the full width of the disclosed `β ∈ {0,1,3}` family, which is the quantitative reason D1 cannot be defaulted. `c_EM` is carried as an unevaluated symbol (no lattice-measured value on the ledger).
- **The §9 mutual-satisfiability check (run BEFORE the freeze — the #796 Protocol-E lesson) paid off.** It found and EXCLUDED the analogous conflict in advance: a requirement that the TRANSFER MATRIX reach the physical constituent regime `k·r_core ~ 1e-25` is UNSATISFIABLE in float64 (basis dynamic range `~x^(−3) ~ 1e75`), so it was never frozen; the matched-asymptotics backstop is frozen as the instrument of record below the certified overlap band `[1e-8, 1e-3]` (five decades, confirmed by G4). **This is the concrete reason D3's ruling had to be (a)+(c) and not (a) alone.**
- **Other disclosures.** Tolerances feasibility-scouted on an uncommitted scratch prototype before the freeze (no physics observable scouted; nothing relaxed after any gate ran — every gate passed on its first shipped run). Layer allocation is log-uniform in the grade `S` (an `r`-uniform allocation converges only first-order here) — frozen before the run, tagged `[engineering-choice]`. No figure shipped. The lane exceeded the ~2 h wall-clock checkpoint during the driver build; disclosed. Lattice-implied `ν = 0.281313` vs canonical `ν_Hill = 2/7` (`−1.54 %`) recorded without a claim. `make verify` green; frozen-provenance gate **0 gating**; docket-key lint green.
- **Consequences ROUTED, not landed** (this lane surfaces): the `n = 1` channel gap; the `ρ_N` convention conflict; D5's missing radial profile (#796 OWED-2 territory); D1 still HELD with the lever number offered as walk input; the `ν` gap. No charter edit, no leaf edit, no rescue minted.
- PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; branch `research/continuum-radial-solver-stage1` off `origin/main` @ `c8ceacc3`. No merge in this lane.

**★REPAIR NOTE (2026-07-28) — PR #801 adversarial review: CLASS WITHDRAWN, REPAIRED, RE-EARNED.** The review returned 1 CRITICAL + 3 MAJOR (14 confirmed / 1 refuted). Two findings were the same defect and it is the one the prereg names as fatal: **G3 and G2 each divided two BIT-IDENTICAL computations, so neither gate could fail.** G3 (`F1`, CRITICAL) called `build_profile(cold, shell_ortho=(1.0,1.0))` against `build_profile(cold)` — `(1.0,1.0)` IS the default, and there was **no code path anywhere in the driver that built the graded shell from `iso_layer`**, so the gate had no isotropic arm at all; the reviewer's mutation (corrupt `ortho_layer` so unit gains no longer reduce to `iso_layer`) left G3 at exactly `0.0` with all nine gates passing and CLASS A standing. G2 (`F3`, MAJOR) divided the caged path at `s_rail = 1.0` by the same path at `uniform = True`.

- **★Order held, and it is the point.** The class was **WITHDRAWN to `C_NOT_CERTIFIED_VOID` in its own commit, before a single line of repair** (prereg §6 `A gate that cannot fail is not a gate`; §8 `A gate that cannot fail voids the certification exactly as hard as a gate that fails`), then re-earned by a **RE-RUN**. Had the repaired gates failed, the class would have stayed C and been banked as such.
- **REPAIRS.** New `build_profile(..., iso_shell=True)` construction (assembles the shell from `iso_layer`, never calls `ortho_layer`, refuses non-unit gains); G3 re-pointed at it and now run on BOTH source fittings (an axis the withdrawn G3 silently skipped). New `uniform_reference_profile` (one homogeneous region, built without `build_profile`) as G2's null arm, with `_K_compliance_ratio` and FT-2 re-pointed at it.
- **★FT-5 MINTED for G3** (a mis-normalized `ortho_layer`, `misnorm = 1e-3`, threshold declared pre-run at `1e6 ×` the gate tolerance) → fires `3.1829e-04`. Also **FT-6** (G6, the rejected `r`-uniform allocation) `4.9127e-02`; **FT-7** (G8, a graded exterior past the contour) `4.8706e-01`; **FT-8** (G9, conditioning below the band floor at `k·r_core = 1e-11`) `1.5875e+15`. FT-5..FT-8 are a **STRENGTHENING**: `gate_fireability_selftest_pass` keeps its frozen definition byte-exactly, and a repair-added test that fails to fire forces C. Nothing relaxed (Rule 11).
- **★NINE-GATE DISPOSITION AUDIT (the review's acceptance item).** G1←FT-1, G2←FT-2, G3←FT-5, G4←FT-4, G5/G5b←FT-3, G6←FT-6, **G7←CAVEAT** (frozen: structurally exact in a linear frequency-domain solver — no admissible input can break it, only a wiring error, which is what it certifies), G8←FT-7, G9←FT-8. **Eight of nine carry a breaking self-test; the ninth carries the disclosed caveat; zero undispositioned** (the withdrawn run had five gates with no disposition).
- **★RE-RUN OUTCOME: `A_CERTIFIED` re-earned.** G2 `1.8763e-13` (was `0.0` "exact"); G3 `0.0` exactly, now between two INDEPENDENT constructions; all other gates BIT-IDENTICAL to the withdrawn run, verified field-by-field. Determinism: two independent full runs, digest `3ad1f7429788022fe7b48f93f209fb50feb8e03645ed5945e691516aae3d7066`. Runtime `1.47 s`. **Mutation receipts:** the `ortho_layer` corruption now puts G3 at `1.9100e-04` (FAIL); a transfer-kernel corruption puts G2 at `5.2800e-07` (FAIL) where the pre-repair comparison returns `2.2204e-16` and would still have passed.
- **★F10 — #782 mis-attribution corrected.** The `0.25` shell-agreement tolerance was justified by "#782's own measured value was `0.036`". The truth is the opposite: **#782 measured `0.6261/0.5853/0.5680` against that tolerance and FAILED it on all three rows**; `0.036` is a different criterion (#782's frozen deliverable, ext/interior `∇·u` at tol `0.10`). Re-justified on its actual merits: the criterion is well-conditioned here because the exterior dilatation is analytic-modal, not differenced from a noisy lattice field — so the stage-1 PASS carries **no inherited evidence from #782**.
- **★F7 — the D1 lever direction was reported backwards, and the merged charter's I8 first clause is wrong on its own numbers.** `c_P = 0.51884 > c_S = 0.28621`, so `c_P²` is the **LARGEST** inertia divisor and gives the **SMALLEST** loading; **`c_S` loads `3.2864 ×` MORE than `c_P`**. Charter I8 says the reverse (*"`c_P` … gives the smallest inertia divisor and the largest loading"*) and this lane's own `3.2864` is what disproves it. Result doc corrected; **the charter is NOT edited (lane fence) — the error is ROUTED for a dated correction by the charter's owning lane, and must also be corrected to Grant directly, since it was relayed to him as an agreement.**
- **Remaining findings repaired in place.** F2 (the frozen §8 reachability argument claims `≥ 4` orders of margin on every gate; measured `3.7e3 ×` down to `2.1 ×` — disclosed in Deviations and routed, prereg BYTE-UNTOUCHED); F4 (FT-4 re-scoped to regime-boundedness, with code-path independence established by reading `basis_matrix`'s `omega == 0.0` branch instead); F5/F13/F14 (the three retyped-number defects); F6 (grade-SHAPE scope block shipped — `1.7063` orders of `rho_N` movement at fixed rail depth, and the review's "4–11 orders" figure **not reproduced**, flagged not relayed); F8 (`rho_S` now emitted in the liveness outputs, so KEEP-BOTH is genuinely "both everywhere"); F9 (the `n = 1` channel **inherits NO gate** — struck); F11 (S3's "`≥ 10 ×`" corrected to the actual `3.4 ×` floor); F12 (`1.7e11 ×` was the ratio to the MEASURED value, not the tolerance — corrected to `4.6e7 ×` / `3.4e7 ×`).
- **★Number-retyping made structurally impossible.** New `research/drivers/continuum_radial_solver_number_check.py`: every numeric token in the result doc must be registered against a JSON path (or allow-listed with a reason) and must be the correctly-rounded value at its own quoted precision. `171` distinct tokens — `115` registered-and-verified, `56` allow-listed, **0 unaccounted**. Both failure modes were live-tested (a retyped token → UNREGISTERED; a stale token vs a moved JSON value → MISMATCH).
- Frozen prereg **BYTE-UNTOUCHED**; engine `src/ave` untouched; no charter/leaf/ledger/port-register edit. `make verify` green; frozen-provenance **0 gating**; docket-key lint green. PR stays `[DO-NOT-MERGE]`, not retitled, not merged.

**★RE-VERIFY REPAIR NOTE (2026-07-28) — second, independent audit: CLEAR-WITH-REPAIRS, class NOT voided.** The re-verify audit independently proved the G3/G2 repair real by its own mutations (pre-repair G3 returns bit-exact `0.0` and holds `A_CERTIFIED` while the physics moves under corruption; repaired G3 FAILS), and prescribed six repairs. All six applied; the class survives them.

- **★R1 (the substantive one) — THE G7 CAVEAT WAS FALSIFIED BY MEASUREMENT, AND `FT-9` IS MINTED.** The map text asserted *"Amplitude enters only as a right-hand-side scale factor of a linear solve, so no admissible INPUT can make it fail; only a wiring error can."* **False.** G7 is **roundoff-limited, not structurally exact**, and its floor grows with the conditioning of the assembled system. Over the prereg's **own** frozen G8 sweep `R_match/r_core ∈ {2,4,8,16}` (prereg §5) the G7 amplitude spread runs `3.3556e-14 / 2.9550e-13 / 3.2399e-12 / 1.1932e-11` — the top two **BREACH** the frozen `1e-12` tolerance, at values that same frozen document calls admissible. **FT-9** (G7 at `R_match/r_core = 16`, threshold `1e-11` = the same mechanical `10 ×` rule FT-6/7/8 use, declared before the run) **FIRES at `1.1932e-11`** (`1.2 ×` its threshold; `11.9 ×` past the gate tolerance). The caveat is DROPPED: **nine of nine gates now carry a breaking self-test, zero caveats, zero undispositioned — honest by construction rather than by an argument.** Nothing relaxed: G7 is still evaluated at the frozen geometry `R_match = 4·r_core` where it passes at `3.4 ×`, and that `3.4 ×` — **the tightest margin in the battery relative to its achievable numerical floor** — is now stated plainly. **The verified half of the caveat is recorded too:** a genuine wiring error (amplitude dropped from the reference arm) puts G7 at `2.999999999997`, FAIL → class C; a naive common-mode mutation lands `5.665625e-13`, under tolerance, correctly not firing. **A SECOND frozen sentence is therefore defective** — the prereg's §5 G7 row / §7 (i) *"STRUCTURALLY exact"* — disclosed (§8.14) and **ROUTED** (§9.6), prereg BYTE-UNTOUCHED. Its conclusion (a G7 PASS is a wiring check, not evidence of physical linearity) is **not** disputed; only the premise that nothing admissible can break it.
- **R2 (WARN-1).** `build_profile(..., iso_shell=True, ortho_misnorm=1e-3)` was ACCEPTED and returned layers bit-identical to `ortho_misnorm=0.0` — the same defect shape as F1 itself, a parameter that appears to do something and does not. The guard covered `shell_ortho` only; it now **REFUSES** non-zero `ortho_misnorm` under `iso_shell=True` (verified: refused; `shell_ortho` refusal intact; `ortho_misnorm=0.0` still accepted).
- **R3 (WARN-4) — a self-defeating tool.** The number-check registered `"1.47" -> _runtime_sec`, which is machine-dependent and excluded from the determinism digest by the frozen §7 (vi) definition itself; the auditor's re-run produced a different runtime and the tool went MISMATCH → **FAIL**. **Disposition: DROP the token, not allow-list it** — an allow-listed numeral is a typed number verified against nothing, exactly the defect the tool exists to remove (F5/F13/F14). The doc now quotes **no** runtime numeral, citing `_runtime_sec` / `runtime_within_budget` instead; the tool carries a `NON_REGISTRABLE` block and a `self_check()` that **refuses any future attempt to register or allow-list that field**.
- **R4 (WARN-5).** The tool had **0** references in `Makefile` / `.github/` / `scripts/` — it protected the doc only at the moment it was run by hand. Now a prerequisite of `make verify` via a new **`verify-lane-number-checks`** target, so CI runs it on every PR. **GATING, not advisory,** per the Makefile's own split: the `-`-prefixed advisory tier (`verify-anchor-content`, which always exits 0; `verify-docket-keys`) is for heuristic drift detectors, the gating tier (`verify-md-links`, `verify-provenance-stamps`, `verify-frozen-provenance`) for correctness checks with a definite answer. A number-check failure means a stated number is not what its source ships. The tool is hermetic (stdlib only, three in-tree JSONs, one in-tree doc, no `ave` import, no network, no RNG, sub-second) — which is why R3 had to land first: wiring a machine-dependent check into CI would have broken every PR.
- **R5 (WARN-3).** `selftest_FT1` computed `lame_ratio` only, so G1's **second** frozen conjunct (`shell_agreement <= 0.25`) had no shipped coverage and the auditor had to recompute it. `_lame_ratio_only` → `_lame_measures`; FT-1 now emits `shell_agreement` = **`2.2530`** / **`2.1454`** against the frozen `0.25` — both breach, by `9.0 ×` / `8.6 ×`. The frozen `FIRES` criterion is UNCHANGED and still keys on `lame_ratio` alone.
- **R6 (WARN-2).** The `frozen_at_repair` vs `frozen` distinction is now stated **where the self-tests are presented** (§3), not only in the deviations list: `frozen` = byte-matches a pre-registration pushed before any solver code existed; `frozen_at_repair` = written down before *its* run in the same commit, git-witnessed at **commit granularity only** — a strictly weaker warrant. Disclosed for FT-9 specifically: its threshold is **by-rule, not blind** (the audit had already measured the configuration; the `10 ×` rule fixes the number and predates the measurement).
- **★RE-RUN OUTCOME: class RE-EARNED as `A_CERTIFIED`.** Every pre-existing gate and self-test value is **BIT-IDENTICAL** to the pre-repair run, verified field-by-field (only additions, the runtime and the digest moved). New digest `efea5059a98d0503b094737d9e7477b1d737dccba87bf9545916c6993cb25576`. **Force-C precedence re-verified BY MUTATION with FT-9 present:** with all nine gates PASSING and all four **frozen** self-tests FIRING (`gate_fireability_selftest_pass = true`), FT-9 alone failing to fire returns **`C_NOT_CERTIFIED_VOID`**. `gate_fireability_selftest_pass` keeps its FROZEN definition **byte-exactly** (verified against `HEAD` and against the prereg text); **no** `frozen`/`frozen_*` string anywhere in the results changed.
- Number-check after wiring: **`181` distinct tokens — `123` registered-and-verified, `58` allow-listed, `0` unaccounted, PASS.** Frozen prereg **BYTE-UNTOUCHED** (md5 `d99b1cd06788d9518ac2e35fa274b991`, blob `b44b54b5`, byte-identical to freeze commit `5f382989` before and after). Engine `src/ave`, `manuscript/` and the charter all **0 diff** — the charter I8 error stays **ROUTED**, not edited. `make verify` green; frozen-provenance **0 gating**; docket-key lint green. PR stays `[DO-NOT-MERGE]`, not retitled, not merged.
