### ENTRY 2026-08-03-oort-walkback-propagation (2026-08-03): implementer — propagate the merged 2026-07-19 MECHANISM-RETRACTED ruling into Vol 3; the Oort containment claim retracts, `r_sat` is relabeled to the solar Axiom-4 onset radius, and the T4 keying fork is routed

- **Class: propagation of an already-merged ruling. NOT new adjudication.** The 2026-07-19 deep-space reactive-bulk ruling declared the Asteroid-Belt / Oort-Cloud prediction **MECHANISM-RETRACTED**. Vol 1 (`04_continuum_electrodynamics.tex`:271-272 + KB twin `magnetic-saturation.md`:34) and Vol 4 (`11_experimental_falsification.tex`:358, `boundary-trapping-test.md`:11/:27, `advanced-protocols.md`:20, `vol4/claim-quality.md`:1133/:1135) have carried the banner since that date. **Vol 3 never received it** and went on printing the containment claim for 15 days. This lane closes that gap and nothing more.
- **Zero ids minted; zero grade fields moved.** No `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; no `confidence`, `solidity` or `quality` value changes anywhere in the diff. Re-banding `clm-3kmt3p` after the retraction is **flagged as plausibly owed and ROUTED, not decided** (re-banding is adjudication, which propagation does not own).
- **Two questions routed to Grant, both deliberately unresolved.** **(Q1) T4 keying** — does the Axiom-4 kernel `S(A)=√(1−A²)` key on the **internal** source field or the **total local** field? **(Q2) One object or two** — are the Vol-3 Axiom-4 onset radius and the Vol-4 yield/impedance-step "Oort transition zone" the same surface? Both are substrate questions; answering either by editorial choice would have been a silent physics ruling.

#### The disposition, item by item

**D1 — the containment retraction (print + KB twin).** `manuscript/vol_3_macroscopic/chapters/06_solar_system.tex`:62-63 and `manuscript/ave-kb/vol3/cosmology/ch06-solar-system/oort-cloud-saturation-boundary.md`:22 carried, verbatim: *"This falls within the observed Hills Cloud range (2,000–5,000 AU inner edge), confirming that the Oort Cloud is the solar system's **impedance boundary**."* **RETRACTED** via a dated header at each site; **body preserved byte-for-byte** per Rule 12; **slot NOT refilled** per A47 v11b (no replacement bracket, no successor claim). Three grounds recorded at both sites:
  - **(a) The mechanism is gone and the bridge with it.** `magnetic-saturation.md`:34, verbatim: *"any belt/Oort **position** derived **from** that kinetic-energy loss does not survive as a bulk-dissipation result"*; *"The Asteroid-Belt / Oort-Cloud prediction is **MECHANISM-RETRACTED**"* — the stall being Ax3-forbidden (`eq_axiom_3.tex`:24, *"never a bulk resistive one"*). **That dissipative stall was the only bridge in this corpus from a field isocline (`g_N = a₀`) to a population edge.** Without it `r_sat` constrains the solar field and says nothing about where comets are.
  - **(b) The chapter already contradicted itself.** `06_solar_system.tex`:205 at this branch's tip (`:84` at `origin/main`, before this lane's header insertion; merged, ratified #741): *"there is **no AVE-distinct content in the Kirkwood or Oort data**."* Twenty lines below the containment sentence in the pre-insertion file. Both could not stand. *(★ **Cite repaired in the 2026-08-03 repair pass, and it is the same F4 defect the drift table below catalogues.** This bullet shipped citing `:84` — the `origin/main` line — even though this lane's own drift table already recorded that anchor as moved. The table's post-edit value, `:202`, was correct **at commit `6623d051`**; the repair pass's own `.tex` edits then added three lines above it, so the current tip is `:205`. The **quoted content is unaffected**; only the offset moved. The print/KB **twin** carries the same finding with a different connector — see the note at* [`oort-cloud-saturation-boundary.md`](../../manuscript/ave-kb/vol3/cosmology/ch06-solar-system/oort-cloud-saturation-boundary.md) *ground (b): the KB leaf reads* **"Kirkwood / Oort"**, *the print chapter* **"Kirkwood or Oort"**. *The verbatim quote is the print chapter's and is now attributed to it alone.)*
  - **(c) The two numbers are not comparands.** `r_sat` = **instantaneous force-balance isocline**, computable from `GM_☉` and `a₀` alone, referencing no object. Hills inner edge = **4.6-Gyr emplacement-timescale boundary**, contingent on the full stellar-encounter history, torquing rate `∝ a^{7/2}`. **Agreement would be a coincidence, not a confirmation.**

**D2 — the "observed" falsity.** Recorded in every header: no object has ever been observed occupying the 2,000–5,000 AU band; the bracket is a **simulation-inferred modelling convention** with ~factor-10 author spread, and it is **uncited in this corpus**. Receipt, two methods, at `origin/main` `66fc7e69`: per-file `grep -ci "oort\|hills\|comet"` over all three tracked `.bib` files (`manuscript/bibliography.bib`, `manuscript/vol_6_periodic_table/bibliography.bib`, `papers/2026_birefringence_letter/refs.bib`) → **0, 0, 0**; independent Python line-scan of the same files on `oort|hills|comet|kuiper|sedna` → **0, 0, 0**. **No literature citation was added, deliberately** — sourcing the bracket sharpens the (c) comparand mismatch rather than relieving it, so the repair would be self-defeating.
  - **★ An ambiguity in the retracted sentence was found mid-pass and is surfaced, not adjudicated.** *"Falls within the observed Hills Cloud **range** (2,000–5,000 AU **inner edge**)"* reads two ways: **(A)** the bracket *is* the target range — then `7,438.9` is **~1.5× above its 5,000 AU upper end** and the sentence is **false**; **(B)** the bracket locates the inner edge and "range" means the cloud's extent outward — the reading `vol3/claim-quality.md`:355's *"within (not on)"* implies — then *"within"* holds **but the claim is vacuous**, its satisfied condition spanning **more than an order of magnitude**. An earlier draft of the `:355` note asserted only reading (A) as fact; that was **corrected before commit** — asserting the stronger reading would have replaced one unsupported claim with another. The claim fails under both readings and (c) disposes of it independently of both, so the ambiguity **needs no adjudication** and receives none.

**D3 — the `r_sat` relabel (arithmetic untouched).** `r_sat = √(GM_☉/a₀) = 7,438.9 AU` on canonical constants (`a₀ = c·H_∞/2π = 1.0719×10⁻¹⁰ m/s²`, `galactic_rotation.py`:56; the corpus' printed `≈ 7,400 AU` is that number rounded). Relabeled at leaf, print section, section **title**, figure caption, `saturation_radius_au()` docstring and module docstring to: **"the radius at which the Axiom-4 kernel turns on for the solar monopole under INTERNAL-FIELD keying — i.e. the outer edge of the Newtonian region."**
  - **Titles struck-and-replaced per Rule 12, originals quoted in the headers.** Print `\section{Oort Cloud as Saturation Boundary}` → `\section{Solar Axiom-4 Onset Radius (Internal-Field Keying)}`; KB `## Oort Cloud as Saturation Boundary` → `## Solar Axiom-4 Onset Radius (Internal-Field Keying)`. **`\label{sec:oort_cloud}`, the `<!-- label: sec:oort_cloud -->` and the leaf FILENAME are deliberately unchanged** — recorded in-header, since the filename now no longer describes its content.
    - **★ Retention rationale corrected 2026-08-03 (repair pass).** This bullet shipped saying all three were kept *"so every inbound link keeps resolving"*. **That is true of the FILENAME and false of the LABEL.** Two-method sweep: **(A)** `git grep -n "sec:oort_cloud"` → 4 hits, all of which are the `\label`/marker declarations themselves or notes *about* them; **(B)** independent Python regex over all tracked files for `\ref|\autoref|\cref|\nameref|\pageref{…oort…}` → **zero inbound references**. **No `\ref` was at stake.** The label is kept on the weaker, honest ground that renaming it buys nothing and could only break a future cross-reference. **The filename's case is the real one and it stands:** four inbound markdown links predate this lane (`vol3/claim-quality.md`:326, `ch06-solar-system/index.md`:31, `vol3/cosmology/index.md`:24, `vol3/index.md`:46), plus the generated `ave-kb/.index/cites.jsonl`:43 row.
  - **★ Existence GATED by the T4 keying fork, quoted verbatim** from `research/2026-07-10_collapse-target-registry.md`:271-290: *"**IF the kernel `S(A)=√(1−A²)` keys on the total local amplitude** (internal + external strain — the unadjudicated hinge, since an external field is *also* local field), **THEN an external-field pre-saturation of `η_eff` follows structurally**"*, with *"the Galactic external field at the solar neighborhood is `g_ext ≈ 2.1×10⁻¹⁰` — **≈ 2.0 a₀ against the AVE-derived a₀ = 1.07×10⁻¹⁰**"*. **Under total-local-field keying the Sun already sits at `g_ext ≈ 1.8–2.0 a₀`, so no saturation transition exists anywhere in the solar system and `r_sat` names nothing physical.** The registry itself calls this *"the unadjudicated hinge — flag-don't-fix, Grant's call."* **(Q1) ROUTED 2026-08-03.**
  - **🔴 RATIO CORRECTED 2026-08-03 (repair pass; Rule 12 — the wrong figure is quoted, not silently swapped).** The sentence above shipped at `6623d051` reading **`g_ext ≈ 2.0–2.1 a₀`**, and so did four other sites (`vol3/claim-quality.md`:343, `oort-cloud-saturation-boundary.md`:30, `06_solar_system.tex`:148, `solar_impedance.py`:345 + `:396`). **That was a unit-vs-ratio conflation:** it read the **mantissa** of `g_ext ≈ 2.1×10⁻¹⁰ m/s²` as if the `2.1` were already a multiple of `a₀`. The registry states both ratios explicitly, verbatim at `:282`–`:283`: *"**≈ 2.0 a₀ against the AVE-derived a₀ = 1.07×10⁻¹⁰** (`2.1/1.07 = 1.96`), or **≈ 1.8 against the empirical a₀ = 1.2×10⁻¹⁰** (`2.1/1.2 = 1.75`)"*. **Correct band: `≈1.8–2.0 a₀`** — which is also what **four already-merged corpus sites** print, so the shipped figure was in conflict with `main` as well as with its own source: `research/2026-07-11_keying-register-walk_framing.md`:81, `research/2026-07-11_astro-adjudicator-sweep_result.md`:66, `research/2026-07-11_astro-adjudicator-sweep_branch-signature-map_FROZEN.md`:40, `_orchestration/2026-07-11_astro-adjudicator-sweep-handoff.md`:49. **★ The physics direction is UNCHANGED and the T4 gate still bites:** the argument needs only `g_ext > a₀`, and the weaker of the two ratios is `1.75`. Under total-local-field keying the Sun is past the Axiom-4 onset either way, so no saturation transition exists anywhere in the solar system and `r_sat` still names nothing physical on that branch. **Nothing about (Q1) moves; only the number was wrong.** `advanced-protocols.md`:32 shipped correct (*"≈ 2.0 a₀"*, the AVE-`a₀` ratio) and is left alone.

**D4 — floor mis-sectoring (audit Finding 1).** `a₀ = c·H_∞/2π` is a **cosmic-Hubble** quantity with **zero galactic input** — no Milky Way mass, no Galactocentric radius, no local stellar density enters `galactic_rotation.py`:56, whose own comment reads *"Derived from the fundamental Topological Unknot Expansion (H_INFINITY) … NO empirical telescope parameter is used."* Calling it a "galactic background impedance floor" is a **sector error**, and not a harmless one: there **is** a real galactic field at the Sun (`g_ext ≈ 2.0 a₀`), so the mislabel **silently pre-answers Q1 in the total-field direction**.
  - **Sweep, two methods** at `origin/main` `66fc7e69`. **(A)** `git grep -in "galactic background impedance\|galactic baseline\|galactic background"` → 3 hits. **(B)** Python walk over all tracked files with `galactic\s+(background|baseline)|background\s+impedance\s+floor|galactic\s+.{0,20}floor` → 8 hits (a strict superset). Classification of the union:

| # | Site | Class | Disposition |
|---|---|---|---|
| 1 | `vol4/…/ch11-experimental-bench/advanced-protocols.md`:22 | **mis-sectored** — dispatched | **RELABELED** (additive note, see below) |
| 2 | `src/scripts/vol_3_macroscopic/simulate_oort_cloud_trap.py`:45 | **mis-sectored** — dispatched | **RELABELED** (comment) |
| 3 | `src/scripts/vol_3_macroscopic/simulate_oort_cloud_trap.py`:90 | **mis-sectored** — dispatched | **RELABELED** (plot legend) |
| 4 | `vol4/…/boundary-trapping-test.md`:17 | mis-sectored, but inside a **Rule-12-PRESERVED** body | **SECTOR-FLAGGED, body not edited** (additive note, added in the 2026-08-03 repair pass) |
| 5 | `vol_4_engineering/chapters/11_experimental_falsification.tex`:364 | mis-sectored, but inside a **Rule-12-PRESERVED** body | **SECTOR-FLAGGED, body not edited** (additive note, added in the 2026-08-03 repair pass) |
| 6 | `vol4/claim-quality.md`:1120 | mis-sectored, inside a **preserved claim block** — ★but at **different wording**: this site reads *"deep-space background impedance floor"*, not *"galactic"* | **SECTOR-FLAGGED, body not edited** (additive note stating **this site's own** defect, added in the 2026-08-03 repair pass) |
| 7 | `vol1/…/saturation-operator.md`:10 | **not a defect** — "galactic rotation floor" is a correct see-also label | untouched |
| 8 | `research/2026-07-17_regime-iv-dissipation-audit_items.json`:492 | **not a defect** — audit reasoning text | untouched |
  - **The dispatched in-place edit at site 1 was NOT available.** `advanced-protocols.md`:20 carries a merged 2026-07-19 banner asserting in print *"Protocol-10 text below **PRESERVED verbatim**"*. An in-place relabel at `:22` would have made that merged sentence **false in the same file, in the same pass**. Shape taken instead: the **additive dated note** — the Rule-12-legal instrument, same precedent as `2026-08-03-stale-refs-propagation.md` ITEM 1. **Constraint surfaced, not worked around.**  <!-- rule12-freeze: base=6623d051cad238a6489bc2c03321e453de3f3613 region=below offset=71 lines=12 bytes=2608 sha256=52960313c75dcf2e50f9b0785d19cacd5eac2351c2cd2cbde8690c5c45a0d9c2 -->
  - **★ Coordinate convention for the table above, stated because this lane uses two of them.** The table's line numbers are the **sweep coordinates at `origin/main` `66fc7e69`**, which is where the sweep ran — including rows 2 and 3, whose targets moved once the relabels were written. Post-edit, at this branch's tip, the two `simulate_oort_cloud_trap.py` sites are: the comment relabel at **`:45`**–`:52` (the relabeled constant lands at `:52`) and the plot-legend relabel at **`:97`**–`:103` (the relabeled legend string lands at `:103`). Row 3's `:90` is the `origin/main` legend line, not a tip-of-branch address. The **drift table further down uses tip coordinates**, which is why `:69 → :76` appears there and `:90` does not: mixing the two conventions in one fragment is legible only if the convention is named, so it is named here.
  - **Sites 4/5/6 are the same defect class under the same constraint** and are left byte-untouched for the same reason.
  - **🔴 FALSE RECEIPT, CORRECTED 2026-08-03 (repair pass; Rule 12 — the false sentence is quoted here rather than silently deleted).** This bullet shipped at commit `6623d051` reading, verbatim: *"**Sites 4/5/6 are the same defect under the same constraint** and are left byte-untouched for the same reason; **each is now covered by a flag in its own file.**"* **The clause after the semicolon was false when written.** What the three sites received on 2026-08-03 was the **D6 cross-volume number-conflict flag only**; **no D4 sector flag existed at any of the three**, so the D4 defect this table enumerates was recorded *only* in this fragment and at site 1. A reader checking the three files would have found no trace of the mis-sectoring. **Repair taken: the receipt was made TRUE rather than merely softened** — the D4 sector flag now exists at all three sites, in the same additive dated shape as site 1's, added in the repair pass: `boundary-trapping-test.md` (after the D6 block), `11_experimental_falsification.tex` (after the D6 block), `vol4/claim-quality.md` (after the D6 block). **★ One substantive finding came out of writing them, and is recorded rather than smoothed over:** site 6 does **not** carry the same string as sites 4/5. Sites 4 and 5 read *"static background impedance floor of **deep galactic space**"*; site 6 reads *"the **deep-space** background impedance floor"* — no galactic sector named. Its flag therefore states **its own** defect (booking `a₀` as a property a *region* has, when `a₀ = c·H_∞/2π` is locality-free) instead of copying the galactic wording across. The original table row is amended above to record the difference.
  - **Sweep completeness, stated honestly:** complete for this phrase family only. It would not catch a paraphrase avoiding both words (e.g. *"interstellar floor"*, *"deep-space baseline"*); no third method was run against that class.

**D5 — code honesty (audit Finding 7).** `src/ave/gravity/solar_impedance.py`:
  - The return dict's `"prediction": "Inner Oort Cloud coincides with g=a₀ transition"` **contradicted its own contents** — a population claim in a function that computes only a field radius. **Replaced** with the compressed D3 relabel, explicitly stating *"Field property only — NO Oort-population claim; containment retracted 2026-08-03. Existence gated by the unadjudicated internal-vs-total-field keying fork (T4)."*
  - **`r_hills_inner = 2000` / `r_hills_outer = 5000` DELETED**, together with their `r_hills_inner_au` / `r_hills_outer_au` dict keys — per the audit's either-or, and because D2 rules out the alternative (citing them). Both are **preserved verbatim in the function docstring** per Rule 12, alongside the retracted docstring and prediction string.
  - **⚑ One constant left standing and flagged, not deleted:** `r_oort_outer = 100000`. It is the **same defect class** (uncited empirical convention, ~50,000–200,000 AU author spread) but was **not in the dispatched deletion scope** and is **not a comparand** for anything the function computes. Retained with an inline flag marking it *"empirical reference value only, carries NO AVE claim"*; **deletion routed.** Surfaced rather than silently swept.
  - Module docstring key-prediction **#2** likewise relabeled (it read *"Oort Cloud inner edge ≈ radius where g_N ≈ a₀ (saturation onset)"* — the containment claim in the file's own header), original preserved inline.
  - **`src/tests/test_solar_impedance.py`:** class `TestOortCloud` → `TestSolarAxiom4OnsetRadius`; the structure test now **asserts the deleted keys are ABSENT** (`"r_hills_inner_au" not in p`) so they cannot be silently reintroduced, and asserts the prediction string does not re-assert containment. The `[1e3, 1e5]` order test **stays as a sanity floor**, as dispatched.
  - **★ New pin test at a STATED, DERIVED tolerance — two assertions, separately labelled, because they are different kinds of claim.** `test_saturation_radius_pin`: **(i) the physics band** — `a₀` is **10.7% below** the empirical MOND `a₀ = 1.2×10⁻¹⁰` (disclosed at `vol3/claim-quality.md`:259), and `r ∝ a₀^(-1/2)`, so the propagated band on `r_sat` is **-5.5%** (`7,438.9 → 7,030.7 AU` on the empirical `a₀`); asserted as `±5.5%` around `7438.9`, because **the number is not meaningful to any tighter tolerance and asserting tighter would overstate it**. **(ii) a chain pin** at `|r − 7438.9| < 0.1 AU`, explicitly labelled *"NOT an accuracy claim"* — it exists to fire if `C_0`, `H_INFINITY`, `G` or `M_SUN` drift, forcing a deliberate update instead of silent movement.
  - **★ TWO NAMES THE FIRST PASS LEFT CARRYING THE RETRACTED CLAIM — both fixed in the 2026-08-03 repair pass.**
    - **The section banner.** Four lines above the relabeled `saturation_radius_au()`, the file's own section divider still read, verbatim: `# Oort Cloud as impedance boundary`. Everything under it had been relabeled; the header announcing it had not. **RELABELED** to `# Solar Axiom-4 onset radius (internal-field keying)`, original quoted inline per Rule 12. Zero-cost, same file, same pass as the rest of D5 — there was no reason it was left.
    - **The function name.** `oort_cloud_prediction()` **named the retracted claim in the identifier itself** — it promised an Oort *prediction* from a function whose docstring, in the same commit, said it makes **no population claim at all**. **RENAMED** to `solar_axiom4_onset_summary()` (suffix per this module's existing convention for dict-returning roll-ups, cf. `oumuamua_summary()`), old name preserved verbatim in the docstring. **Renamed rather than flagged because it does not ripple:** two-method check — `git grep -n "oort_cloud_prediction"` and a Python scan of all tracked files — found the module declares **no `__all__`**, `ave/gravity/__init__.py` does **not** re-export it, and the only consumer in the tree is `src/tests/test_solar_impedance.py` (import + one call + one docstring mention). The "name retained for API stability" flag was the fallback and was not needed.
  - **Test receipt:** `PYTHONPATH=src python3 -m pytest src/tests/test_solar_impedance.py -q` ⇒ **`26 passed in 0.47s`** (was 24 tests pre-change; +2 net from the split pin test). Re-run after the repair pass: **26 passed**, unchanged.

**D6 — the Vol3-vs-Vol4 fence (audit Finding 8). FLAGGED AT BOTH POLES, NOT RECONCILED.** Identical flag text landed at **both** poles: *"⚑ Vol 3 books `r_sat ≈ 7,439 AU`; Vol 4 books `~15,000 AU` for the claimed same boundary; one-object-or-two routed to Grant 2026-08-03."*
  - **Vol 4 sites flagged (3):** `vol_4_engineering/chapters/11_experimental_falsification.tex`:370, `vol4/claim-quality.md`:1123 (which prints `~15,000` at **four** lines — `:1123`, `:1128`, `:1164`, `:1166` at this branch's tip), `boundary-trapping-test.md`:25. **Vol 3 sites flagged (2):** the retraction headers in `06_solar_system.tex` and `oort-cloud-saturation-boundary.md`.
  - **A third number is in the corpus and is named in every flag:** `src/scripts/vol_3_macroscopic/simulate_oort_cloud_trap.py`:76 hand-centers its Oort Gaussian at `10^4.2 ≈ 15,800 AU` — the script's own 2026-05-17 docstring already discloses the centers are *inputs, not predictions*.
  - **The gap is a factor of 2.0**, which is **outside the -5.5% `a₀`-provenance band**, so these are not one number at two precisions. **Vol 4's figure is round and empirical with no derivation anywhere in that volume; Vol 3's is derived.** Reconciling requires **(Q2)**: are the Axiom-4 onset radius and the yield/impedance-step boundary **one surface or two**? Picking either number would silently answer it. **ROUTED 2026-08-03.**

#### ⚑ Flagged, NOT fixed — out of dispatched scope, surfaced for the auditor lane

1. **KB index rows still asserting the retracted framing.** 🔴 **UNDERCOUNTED AND PARTLY MIS-DISPOSED; CORRECTED 2026-08-03 (repair pass; Rule 12 — the original item is quoted verbatim, not deleted).** As shipped at `6623d051` this item read: *"**Three KB index rows still assert the retracted framing**, and were not in the dispatched site list: `vol3/cosmology/ch06-solar-system/index.md`:31 (…the containment claim in compressed form, the most substantive of the three), `vol3/cosmology/index.md`:24 and `vol3/index.md`:46 (both render the leaf under its **old title**…). One-line fixes each; **routed rather than taken**, since expanding an enumerated-site dispatch unbidden is itself a scope breach."*
   - **Two things were wrong with it.** **(i) The count.** A **two-method re-enumeration** run in the repair pass — **(A)** `git grep -n "Oort" -- 'manuscript/ave-kb/**/index.md'`; **(B)** an independent Python walk over all tracked files flagging links to `oort-cloud-saturation-boundary.md`, the title phrase `Oort Cloud (as (a )?)?Saturation Boundary`, the phrase `Oort Cloud inner edge`, and any `Oort` inside a file named `index.md` — **converged on seven Vol-3 index sites, not three.** Both methods returned the identical seven; method (B) additionally returned the Vol-4 ch11 index row, which belongs to a different claim (`clm-h55fy1`) and is out of scope here. **(ii) The disposition.** Routing **all** of them was wrong for two of the seven: `:31` and `:11` are not stale *labels*, they are the **retracted claim still being asserted at the corpus' entry points**. `:31` asserts it in a form **stronger than the sentence that was retracted** — *"MOND threshold **defines** Oort Cloud inner edge"*, a bare causal claim, where the leaf sentence merely hedged *"falls within … consistent with"*. Rewriting an entry-point row to match an **already-merged** ruling mints no id, moves no grade and decides no fork; leaving it renders the retraction invisible to every reader who does not open the leaf. **Corpus integrity outranks scope discipline here, and the two rows are FIXED in the repair pass.**

| # | Site | What it says | Class | Disposition |
|---|---|---|---|---|
| 1 | `vol3/cosmology/ch06-solar-system/index.md`:31 | *"Resultbox: **MOND threshold defines Oort Cloud inner edge**"* | **BLOCKING** — the retracted containment claim, in a **stronger** form than the retracted sentence | **RELABELED** 2026-08-03 repair pass (Rule 12, original quoted in-file) |
| 2 | `vol3/cosmology/ch06-solar-system/index.md`:11 | *"the **Oort Cloud as a saturation boundary** ($r_{sat} \approx 7{,}400$ AU)"* — chapter prose abstract | **BLOCKING** — same containment framing at the chapter's entry point | **RELABELED** 2026-08-03 repair pass (Rule 12, original quoted in-file) |
| 3 | `vol3/cosmology/ch06-solar-system/index.md`:19 | *"\| Oort Cloud Saturation Boundary \| $r_{\text{sat}} = \sqrt{GM_\odot/a_0} \approx 7{,}400$ AU \|"* | **title debt** — stale label beside a **surviving** formula | **ROUTED** — batched relabel follow-on |
| 4 | `vol3/cosmology/index.md`:24 | *"\| Oort Cloud Saturation Boundary \| $r_{\text{sat}} = \sqrt{GM_\odot/a_0} \approx 7{,}400$ AU [Ch.6](…) \|"* | **title debt** | **ROUTED** — batched relabel follow-on |
| 5 | `vol3/cosmology/index.md`:39 | Ch.6 contents cell: *"…'Oumuamua acceleration (91% match), **Oort Cloud as saturation boundary**, Kirkwood gaps…"* | **title debt** | **ROUTED** — batched relabel follow-on |
| 6 | `vol3/index.md`:46 | *"\| Oort Cloud Saturation Boundary \| $r_{\text{sat}} \approx 7{,}400$ AU [Cosmo](…) \|"* | **title debt** | **ROUTED** — batched relabel follow-on |
| 7 | `vol3/index.md`:63 | *"solar system impedance (**Oort Cloud**, Kirkwood gaps, magnetospheres, Two Winds decoupling)"* | **not a defect** — names a *topic* in a chapter-contents list; asserts nothing about a boundary | untouched |

   - **The four routed rows are genuinely title debt, not false claims:** each sits beside `r_{sat} = \sqrt{GM_\odot/a_0}`, whose **arithmetic and derivation are untouched by the retraction** (D3 relabels what it *names*, not what it *computes*). They are stale labels. Batching them is a **relabel** follow-on and carries no adjudication.
   - **⚑ Method-honesty on the count.** Complete for this phrase family and for files named `index.md`. A paraphrase in a non-index entry point (a volume README, a backmatter summary) using neither the leaf link, the title phrase, nor the words *"Oort Cloud inner edge"* would not be caught; no third method was run against that class.
2. **The figure asset cannot be relabeled from here.** `assets/figures/solar_system_impedance.pdf` / `.png` almost certainly carries the old panel text on-figure; the **caption** was relabeled but the **artwork** was not, because there is **no tracked generator** — `git grep -l solar_system_impedance` returns only three prose files, and `research/2026-06-07_figure-audit-ledger.md` already books this figure in its **provenance-gap** section: `:28` heads that section *"provenance gap (content checklist-clean, but no generator found)"* and `:29` lists `solar_system_impedance` in its vol_3 cluster, closing *"Content current; reproducibility unverifiable."* **A caption/artwork mismatch now exists and is disclosed rather than left to a reader.**
   - **★ Two corrections to this item, made in the 2026-08-03 repair pass.** **(i) Quote attribution.** As shipped, this item read *"`…figure-audit-ledger.md`:29 already classifies this figure's cluster as **"orphan generator … reproducibility unverifiable."**"* — an **elision that joined two separate attributions**. At `:29`, *"orphan generator"* is attached to **`cross_scale_universality.pdf`** specifically; the vol_3 cluster containing `solar_system_impedance` is covered by the section heading at `:28` and by the closing *"Content current; reproducibility unverifiable."* The conclusion is unchanged — there is no tracked generator either way — but the quote is now stated exactly. **(ii) The mismatch is bigger than "the label".** The `.tex` caption comment shipped saying *"only its label changes"*; the caption's printed **value** also moved, `$\sim$7,400 AU` → `$\sim$7,439 AU` (the same quantity, unrounded). So the artwork/caption mismatch covers **label and value**, and the disclosure now says so at the caption itself.
3. **`clm-3kmt3p` re-banding may be owed.** Its `:355` rationale explicitly *"averaged down for the heterogeneity"* across three sub-results, one of which just retracted. **Grades left unmoved by design** (propagation ≠ adjudication); the question is routed.

#### ★ This lane's own edits SHIFT line numbers, and two separate drift findings came out of checking that

**(1) Self-inflicted drift — found and repaired before commit.** The `06_solar_system.tex` retraction header inserts ~118 lines *above* the rest of the chapter, and the two `claim-quality.md` blocks insert above their `### Quality` sections. **Every cite this lane wrote was re-resolved against the post-edit files**, and six were wrong on first write — repaired, not shipped:

| Cite written | Was | Now | Where it appears |
|---|---|---|---|
| `06_solar_system.tex` "no AVE-distinct content" | `:84` | **`:205`** *(shipped `:202`; the repair pass's own `.tex` edits added 3 lines above it)* | ch06 header (b), KB leaf header (b) |
| `vol3/claim-quality.md` "lands only within (not on)" | `:334` | **`:355`** | ch06 header (c-bis), KB leaf (c-bis), and that file's own header |
| `vol3/claim-quality.md` confidence / solidity | `:329`/`:333` | **`:350`/`:354`** | vol3 claim-quality header |
| `vol4/claim-quality.md` two of the four `~15,000` prints | `:1142`/`:1144` | **`:1164`/`:1166`** *(shipped `:1154`/`:1156`; the repair pass's sector-flag block added 10 lines above them)* | vol4 claim-quality flag, `boundary-trapping-test.md` flag |
| `simulate_oort_cloud_trap.py` Oort Gaussian center | `:69` | **`:76`** | all five D6 flags + this fragment |

  Anchors *below* an insertion point were unaffected and were re-verified rather than assumed: `vol3/claim-quality.md`:318, :324, :259, :319-:323; `vol4/claim-quality.md`:1120-:1135; `advanced-protocols.md`:20/:22/:24; `boundary-trapping-test.md`:25/:27; `11_experimental_falsification.tex`:370.

**(2) ⚑ PRE-EXISTING corpus drift discovered while doing (1) — FLAGGED, NOT FIXED.** **Six sites across the corpus cite `06_solar_system.tex:203` for the Regime-I "0 Watts / lossless induction" resolution of the Orbital-Friction Paradox. That cite was ALREADY STALE at `origin/main` `66fc7e69`, before this lane touched anything** — at the base, line 203 of that file is a **figure caption about the Uranus magnetopause**; the "0 Watts" sentence sits at **`:221`** (verified by `git show origin/main:…06_solar_system.tex | sed -n '197,205p'`). This lane's insertion moves it further, to **`:349`** (`:345` at commit `6623d051`; the repair pass's `.tex` edits added 4 more lines above it). The six sites:

| Site | Class |
|---|---|
| `vol_1_foundations/chapters/04_continuum_electrodynamics.tex`:272 | merged 2026-07-19 banner (other volume) |
| `vol1/dynamics/ch4-continuum-electrodynamics/magnetic-saturation.md`:34 | merged 2026-07-19 KB twin |
| `research/2026-07-17_regime-iv-dissipation-audit.md`:132 | merged audit record |
| `research/2026-07-19_deep-space-band-map_derivation.md`:28 | merged derivation record |
| `research/2026-07-19_deep-space-reactive-bulk-walk_RECORD.md`:15, :29, :96 | merged walk-record (`:96` cites `:199–203`) |
| `_orchestration/2026-07-10_rulings-docket.md`:1941, :1972 | frozen monolithic docket |

  **Not repaired here.** Every one is another lane's merged text, a research record, or the frozen docket; three of them are Rule-12 banner bodies. Repointing them is a cross-lane cite-sweep, not this dispatch's scope. **The correct target is `:349` at this branch's tip** — recorded so the sweep, when it runs, does not have to rediscover it. Note the *quoted content* at every one of those six sites is correct and unaffected; only the line numbers drift.

**(3) Two other-lane cites to `06_solar_system.tex:62` now land inside this lane's header** rather than on the containment sentence (which moved to `:183` at this branch's tip; `:180` at commit `6623d051`): `_orchestration/2026-08-02_manuscript-reconciliation-board.md`:325 and `_orchestration/docket-entries/2026-08-02-mr-astro-singletons.md`:4. Both are other lanes' files (the board is the manuscript-reconciliation epic's live working file; the fragment is another lane's, and `docket-entries/README.md`:7 forbids editing it). **Flagged, not edited.** New target: **`:183`**.

#### ★ This discharges a question another lane routed to Grant — by making it moot, not by answering it

`_orchestration/docket-entries/2026-08-02-mr-astro-singletons.md`:4 (merged) already flagged this exact sentence, verbatim: *"**(a) Oort-cloud non-sequitur.** `06_solar_system.tex:62`: printed r_sat ≈ 7,400 AU is asserted to 'fall within' the 2,000–5,000 AU Hills-cloud inner-edge range. **7,400 is not inside 2,000–5,000.** Either the saturation radius, the observational bracket, or the containment claim must move — **which one is physics, not editing**."* That lane offered Grant a three-way choice and correctly declined to pick.

**This lane does not pick either.** It observes that the choice is **moot**: the 2026-07-19 ruling had *already* retracted the mechanism that connects the two sides, so the containment claim fails **regardless of which of the three items moves**, and no number needs adjudicating to retract it. That fragment's reading (**7,400 is not inside 2,000–5,000**) is reading (A) above; this lane records that the sentence is **ambiguous** and fails under (B) as well, so even a reader who rejects (A) reaches the same disposition. **The routed question is discharged as moot, not overridden** — and the two questions this lane routes (T4 keying; one-object-or-two) are genuinely new, not restatements of it.

#### Verification receipts

- **Base:** branched from `origin/main` `66fc7e69` (local `main` was **18 commits behind** at session start and was **not** used as the base).
- **Collision fence, run at fresh tips against every branch touched on 2026-08-02/03** — `docs/mr-handoff-mechanical-0803`, `docs/rulings-mr-batch-0803` (the two named in the dispatch), plus `kb/petermann-artifact-record`, `research/coldq-pole-v2p3`, `research/coldq-pole-v2p2`, `kb/wall-taxonomy`, `docs/mr-addenda-0803`, `docs/mr-board-corrections-0803`, `docs/mr-epic-closeout`: **zero file overlap on all 10 paths this branch touches.** The two dispatch-named branches do not own any Oort site, as the dispatch predicted.
- **Two-method site verification** was run **before every edit**; two cited paths in the dispatch needed correction and are recorded so the cites reconcile: the MECHANISM-RETRACTED ruling is at **`vol_1_foundations`**`/chapters/04_continuum_electrodynamics.tex`:272 (not `vol_3`), and `boundary-trapping-test.md` lives under **`ch11-experimental-bench-falsification/`** (not `ch11-experimental-bench/`). The dispatch's `simulate_oort_cloud_trap.py:44` is **`:45`** (`:44` is `strain_solar = 100.0 / r`). All other cited line numbers verified exact.
- **★ Cite-count claim: RULE NOW STATED, because without one the number is not checkable.** The PR body shipped *"Every `path:line` cite in the diff re-resolved post-edit (**67** distinct)"* with **no counting rule given**, and an independent audit counting by its own method got **59**. Neither number was wrong so much as **unfalsifiable** — a bare count of "cites" varies by a factor of two depending on whether you count occurrences or distinct targets, whether repeated targets across files collapse, and whether unresolvable tokens are dropped. **Rule adopted, and the three counts it distinguishes, measured at this branch's tip against `origin/main`:**
  - **(A) occurrences** — every `file.ext:NNN` token appearing in a line *added* by the diff, counted once per appearance: **132**.
  - **(B) distinct targets** — the same tokens collapsed to unique `(basename, line)` pairs: **62**.
  - **(C) distinct resolving targets** — (B) restricted to basenames that exist in `git ls-files`: **61**. *(The one dropped is a path-form token that does not name a tracked file.)*
  - **The claim this fragment makes is (C): 61 distinct resolving `path:line` targets, each content-verified at the tip** by an explicit expectation table (path, line, expected substring) rather than by range-checking. **Range-checking alone is not verification** — a line number inside a file's length passes it while pointing at the wrong sentence, which is exactly the failure mode `:334`/`:1154`/`:202`/`:345`/`:180` all were.
- **Pure-corpus:** no external-context terms anywhere in the diff or commit message.
