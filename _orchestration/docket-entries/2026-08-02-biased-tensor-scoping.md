### ENTRY 2026-08-02-biased-tensor-scoping (2026-08-02): implementer — the DC-biased small-signal tensor AT the iso-bond point (the W1-resolution path); SCOPING + PILOT, nothing minted

- **Scope fence, stated first.** **SCOPING + PILOT**, not a derivation and not a claim. **Zero corpus files modified** (`manuscript/` untouched, `src/ave` **byte-untouched**). No claim minted, no `clm-`/`def-`/`sup-` node created, no solidity changed, no manual landed. Outcome bins are explicitly **NOT FROZEN** — the follow-on prereg freezes them. Deliverables: [`research/2026-08-02_biased-tensor-scoping.md`](../../research/2026-08-02_biased-tensor-scoping.md) · pilot driver `src/scripts/vol_1_foundations/biased_tensor_iso_bond_pilot.py` · tracked output `research/2026-08-02_biased-tensor-scoping_pilot.json`.
- **Provenance.** Grant GO 2026-08-02, verbatim `[sic]`: *"7, GO"*, on the W1-resolution path ratified in the walk — the vacuum sits at the energetics-derived iso-bond point $\rho_{bond}=1$ (knob-free, #516), whose bare $K<0$ is stabilised by a DC operating-point pre-stress; the pressurized-vessel picture, Grant verbatim `[sic]`: *"I agree smells like DC op point"*. Resolves the two-axis fork surfaced by merged **#813** (`research/2026-07-31_anisotropy-observable_scoping.md` W1).
- **★ Pilot headline (three results, all on the merged #506 pipeline imported unmodified).** (1) **A symmetric DC bias provably cannot flip sign($K$)** — $K_{ac}=S\cdot K_{cold}<0$ for every $S>0$ (#519 degree-1 homogeneity) and Zener stays $1.000$ to $1.6\times10^{-15}$ over the whole sweep $A_0\in[0,0.999]$. (2) **An asymmetric bias can, and the pilot prices it:** $A_{Zener}-1\simeq(\rho_{eff}-1)/8$, transverse fractional speed spread $\simeq(\rho_{eff}-1)/16$; buying $K_{acoustic}>0$ costs $\rho_{eff}\ge2$ ⇒ Zener $=1.0889$, $4.24\%$ shear split. **Within the swapped-springs model, isotropy and $K_{acoustic}>0$ are mutually exclusive** — the tension the brief asked to map, exactly quantified. (3) **★ The object being stabilised is not what the merged docs say it is.** Reading the Born $k_s$ as the bond pre-tension $\tau=k_s\ell$ (rotational invariance; pilot P1: a rigid rotation costs $4.000$, exactly $\tfrac12k_s\sum|\Omega r|^2$), the reference state carries an isotropic tension $\sigma_0=(1/3V)\sum k_s\ell^2=0.176776695$, and at $\rho=1$ **the entire acoustic tensor is that pre-stress**: $C_{11}=C_{44}=-C_{12}=\sigma_0$ (dev $1.2\times10^{-9}$), second-order elastic tensor **identically zero**. The Birch↔Brugger split gives the exact closed form $K_{thermo}=(\rho_{bond}-1)\sigma_0/3$ (max err $1.1\times10^{-8}$ over $\rho\in[0.5,10]$) ⇒ **at the iso-bond point the intrinsic bulk modulus is ZERO, not negative**; the published $-0.0589$ is exactly $-\sigma_0/3$, the initial-stress offset. And the phonon spectrum has **no negative eigenvalue at any wavevector**: $D(q)=L(q)\otimes I_3$, a **positive-SEMI-definite** graph Laplacian, so $\min\lambda(D(q))\ge0$ **everywhere**, $\to0$ as $q^2$ at the zone centre (as acoustic branches must). ⚠ **Two propagation guards, stated so downstream readers do not over-read the two headline numbers.** (i) The sampled $\min\lambda=+5.183\times10^{-3}$ over 2000 seeded $q$ is a **sampling artifact** — 2000 random $q$-draws never land on $q=0$ — **not** a spectral gap and **not** a strict positive-definiteness result; the load-bearing statement is the structural $\lambda\ge0$, never the strict inequality. (ii) The $\lVert D(q)-L(q)\otimes I_3\rVert/\lVert D(q)\rVert = 0.00$ **bit-exact** figure is the **algebraic identity** $\Phi_b = k(\hat d\hat d + I - \hat d\hat d) = kI$ at $\rho_{bond}=1$ appearing in floating point (disclosed in the deliverable's §2 P4 derivation paragraph) — a verification that the pipeline implements the algebra, **not an empirical measurement** and not independent evidence.
- **New structural result (P4).** At $\rho_{bond}=1$ the vector elasticity problem **collapses to a scalar graph-Laplacian problem**: all three polarisations are degenerate at **every** $q$ (worst intra-triple spread $2.4\times10^{-15}$; control at $\rho=3$: $0.279$ — the instrument sees the difference), and cubic symmetry then forces the rank-2 long-wave coefficient isotropic — so **Zener $A=1$ at the iso-bond point is a symmetry theorem, not a numerical coincidence**. This upgrades #516's knob-free minimiser and gives item 21 a stronger negative than #813 had. Also: $c_L=c_T$ exactly there ($c_L/c_T=1.000000000$). *(The degeneracy and control numbers ARE measurements; the $0.00$ factorisation figure is not — see the guard above.)*
- **Stabilization-candidate map (six, enumerated honestly).** S1 symmetric bias — **CLOSED** (no-op on sign, isotropy survives exactly, only depressurises: $\sigma_0\to S\sigma_0$). S2 asymmetric — **works, priced in isotropy**. S3 the pre-stress already in the model — **the load-bearing candidate** (= #519's OMITTED channel (a)). S4 mode condensation — **not computable in a harmonic model** (no quadratic instability to condense; needs $U'''$). S5 external/cosmological loading — **the residual, and the real frontier object** (because $K_{th}=0$, the boundary condition *is* the stability criterion). S6 bond-length shift — **OPEN and most derivable** (= #519's channel (b); $\rho_{bond}=\ell U''/U' = d\ln U'/d\ln\ell$, and $\rho=1\iff U\propto\ell^2$, the zero-rest-length spring).
- **Four flag-don't-fix items (surfaced with verbatim, both paths, nothing edited).** **FLAG-1** — the port-register channel speeds $\sqrt2\,c$ / $\sqrt{10/3}\,c$ (`port-register.md`:49) have **no operating point at $\rho_{bond}=1$**, where all three acoustic branches are exactly degenerate; framed as the same GR-import surfacing (`srs-band-structure.md`:116 already grades $\sqrt{10/3}$ *"NOT lattice-emergent"*), with two honest paths and **neither picked**. ⊗ **And canon already fences half of it:** `srs-band-structure.md`:**117** grades the $\sqrt2$ `V_LONG` as *"a **scalar-sector** object **imported from `constants.py:770`**, **NOT a Bloch branch** of this translational problem"* — i.e. option (γ) for the $\sqrt2$ is **already written down**, so what V6 must actually decide is the $\sqrt{10/3}$ VRH row (graded but **not** sector-fenced) plus `port-register.md`:49 shipping both speeds **unfenced**. **FLAG-2** — "mechanically unstable" is a three-way conflation (dynamical stability / intrinsic bulk stiffness / dead-load Birch combination) with three different answers. **FLAG-3** — an isolated arithmetic residual in the merged #506 table: the $\rho^\ast$ row (`:132`) prints $K=0.4308$, but that row's own $C_{11},C_{12}$ give $(0.7279+0.6464)/3 = \mathbf{0.4581}$, the full-precision re-run gives $\mathbf{0.458052}$, and the row's own $K/G_{Hill}=2.0000$ requires $0.458052$ ($G_{Hill}=\mathbf{0.229025}$); **no #506 conclusion rides on it**, every other row checks out. **This flag is FORK-INDEPENDENT** (wrong on every reading, every $\rho$-convention and every bond model) — **route-to-auditor-now vs hold-for-V1 is recorded as Grant's call**, not taken by this lane. **FLAG-4** — the corpus **already measured** the Born-model rotation cost (#802 `:70`, verbatim: *"`E(rigid rotation, 1e-3) = 1.984e-3 > 0`"*) and canonizes the **opposite** reading (`translation-circuit.md`:360: *"the Born model's absolute-frame rotational stiffness"*); the two readings are different physics and **Grant adjudicates**.
- **Consequences map.** Item 21 closes negative-by-construction under S1/S3/S5 for a stronger reason than #813 had (BZ-wide degeneracy, not long-wave Zener); becomes live with a derived transfer function under S2/S6; and **S5 opens a new axis #813 did not consider** — a non-hydrostatic cosmological boundary load would *supply* the sky-axis orientation #813 W3 flags as never-derived. For the $\nu=2/7$/$K=2G$ chain: at $\rho_{bond}=1$, $K_{th}=G_{th}=0$ and $K_{ac}/G_{ac}=-1/3$, so **$K=2G$ has no operating point AT THE RATIFIED $\rho_{bond}=1$** — sharpening #506's "GR-imported" grade to "imported AND not located at the vacuum's iso-bond operating point". ⚠ **Scope guard (do not drop the qualifier):** the stronger form *"no operating point in the ratified vacuum"* additionally assumes $\rho_{bond}=1$ is the vacuum's **only** operating point, which silently closes FLAG-1 path (β) (#813 W1 option (c): photon-sector at $\rho=1$, bulk channel elsewhere) — a path this lane explicitly leaves open and routes to Grant at V6. **PPN numbers UNCHANGED** (#506's own fallout row: calibrated independently at the deflection-integral level).
- **Sector keying (W7).** $k_a=U''(\ell)$ and $k_s=U'(\ell)/\ell$ are the **curvature and slope of one bond potential at one operating point**, not two free knobs — so #519's $\rho_{eff}=S_{ax}/S_{sh}$ is a reparametrisation of *where on the bond law the DC operating point sits*. The pilot's W7 answer: **field-sector SYM ($S_\varepsilon=S_\mu$) and bond-channel SYM ($S_{ax}=S_{sh}$) are distinct**, and the missing map is sharper than "field sectors → bond channels" — it is *"does the Ax4 kernel rescale the bond law, or move the operating point along it?"* (walk question V4). For #813's W4, the pilot supplies the transfer function: $\lvert\delta c_T/c_T\rvert\simeq A^2/32$ under maximal channel asymmetry ⇒ $6.9\times10^{-12}$ at the solar limb ($\approx3.0\times10^4$ waves of accumulated retardance at 500 nm), a ~4–5 OOM over-shoot of any plausible polarimetric null — which **argues for, but does not prove,** W4 option (a). Cross-check clean against #813's independent $\mathcal{O}(1)$-coefficient band ($8.8\times10^5/32=2.8\times10^4$). **No observational bound invented**; the one external comparison is tagged `[requires-external-retrieval]`.
- **Seven walk questions for Grant (V1–V7)**, asked BEFORE design per the Rule-16 strengthening, inline prose with bulleted options, none pre-picked. V1 (rigid-rotation cost vs pre-tension — the fork everything hangs from), V2 (what holds the vessel out), V3 (which boundary condition), V4 (kernel rescales vs moves the operating point), V5 (does gravity load the channels equally — #813 W4 with a transfer function attached), V6 (where do $\sqrt2$/$\sqrt{10/3}$ live now), V7 (does the prereg test the model or the physics).
- **Two self-corrections made mid-draft by `verify-before-cite`, both disclosed in-place.** (i) FLAG-4's first draft claimed the corpus never addresses rotational invariance of $\Phi_b$ — **false**, and the corrected finding is stronger. (ii) #526's $T$-sign fork was drafted as open; it is **Grant-ratified closed** (`axiom-register.md` §"LOAD-RESPONSE SIGN RULE", `[SIGN-RULE-DERIVED]`) — the row and V1 were rewritten so the pilot's input sits **upstream** of that rule rather than re-opening it.
- **ρ-convention guard.** Every $\rho$ in the deliverable is the **srs swapped-spring** $\rho=k_a/k_s$ ($K_{ac}=0$ at $\rho=2$, $K=2G$ at $\rho^\ast\approx9.7734$), **not** the moduli-model/buckling $\rho$ — per `axiom-register.md` §"ρ-CONVENTION DISAMBIGUATION".
- **Class.** CONSISTENCY (P0/P2/P3/P4) + IDENTITY/MECHANISM (P1). **No emergence claim, no value derived** — $\sigma_0$ is an engine-unit geometry sum; $\rho^\ast$, $2/7$, $2$, $\sqrt{10/3}$ are read-off comparison constants the sweeps never fit to. `ave-discrimination-check`: *"the vacuum is a pre-tensioned membrane"* is an **AVE-internal model statement, NOT an AVE-distinct prediction** (the Birch initial-stress split is standard continuum elasticity) — stated so nobody headlines it as a chord.
- **★ Review-repair pass (2026-08-02, post-audit; CLEAR-WITH-REPAIRS, 14 findings, none overturning a headline).** All 14 executed as **precision + scoping**, every one dated and quote-and-corrected in place rather than silently rewritten. The load-bearing ones: **(1/13)** the three-way stability carve is **conditional on FLAG-4** — rows (ii)/(iii) hold under reading (a); under the corpus's canonized reading (b) the acoustic $K_{ac}=-\sigma_0/3$ **is** the physical bulk modulus and the merged *"unstable"* wording is right as written; row (i) is reading-independent, and S4's *"NOT COMPUTABLE"* verdict survives either way. **(2)** the §1.1 *"one alternative and only one"* binary was **false and self-contradicting** (V1 enumerates **four**, including the standard Keating-1966 never-rotationally-complete position) — retracted, re-pointed at V1. **(4)** the P0 reference literals were **mis-attributed**: the K literal `-0.058926` has **zero corpus hits**; re-attributed per-cell to #519 `:57`/`:117` (`-0.05893`, 5 s.f.), gate value **unchanged**, max deviation $3.30\times10^{-6}$ → $4.43\times10^{-6}$, still PASS. **(11)** *"$K=2G$ has no operating point in the ratified vacuum"* → **"at the ratified $\rho_{bond}=1$"** (the stronger form closes FLAG-1 (β) by assumption). **(12)** the $K_{thermo}$ threshold gate sits ~3 OOM **below** the pilot's own noise floor — **disclosed, KEEP-BOTH, gate retained byte-unchanged** (Rule 11: no post-result retune), with a HALT-asserted **analytic** form $K_{th}=(\rho_{eff}-1)\sigma_0/3>0 \iff \rho_{eff}>1$ added alongside. **(7)** FLAG-1 under-cited canon's own fence — `srs-band-structure.md`:117 already grades the $\sqrt2$ out of the translational Bloch problem. **(8)** the #813 retardance cross-check is **consistent by construction**, not independent (same $\pi bA_b^2$ integral; the pilot supplies only $\kappa=1/32$; the residual $7\%$ is $550$-vs-$500$ nm). **(3/5/6/9/10)** measured-vs-target slope $0.0625$ → $\mathbf{0.062342}$ (the $1/16$ is the affine/analytic limit; the $0.25\%$ gap is internal relaxation, real physics); three stale cites repaired **by content** (#519 `:134` → `:131`, #813 `:1181` → `:419`, #506 `:217–218` → `:216–217`); FLAG-3's own arithmetic $0.45805$ → $\mathbf{0.4581}$ and $G_{Hill}$ $0.229031$ → $\mathbf{0.229025}$ (conclusion stands); the *"53 hits, concentrated"* receipt count re-run to **41 across 31 files, max 3/file — diffuse, not concentrated**; a Receipt-7 parenthetical citing a **closed** flag retracted and re-pointed at the still-open white-dwarf row (#830) and `regime_map.py`:523. **(14)** V1's briefing now records that **PR #838 (in flight) is adding further reading-(b) language to canon**, so canon's weight on (b) is increasing while V1 sits open — and that #838's same hunk supplies concrete material for V1 option (d) (`micropolar_bloch.py`:41, where the rigid rotation **is** a genuine zero mode). **No headline moved; no verdict changed; every physics number in the shipped JSON is byte-identical to the frozen run.**
- **Battery.** `make verify` exit 0 · `make refresh-kb-metadata` idempotent · `verify-md-links` gating errors 0 · `verify-docket-keys` no new duplicate keys · pure-corpus clean (three methods) · engine `src/ave` byte-untouched · nine two-method receipts (receipt 4 is the one that corrected the author; receipts 1, 5, 6 and 7 were themselves corrected in the 2026-08-02 review pass).
