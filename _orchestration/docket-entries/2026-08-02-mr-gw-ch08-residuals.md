### ENTRY 2026-08-02-mr-gw-ch08-residuals (2026-08-02): manuscript-reconciliation lane — ch08 GW-sector residuals routed to the core session

- **Class: routed adjudication request (KB unsettled / mid-churn), not adjudication.** Provenance: the 2026-08-02 full manuscript-vs-KB sweep ([board](../2026-08-02_manuscript-reconciliation-board.md) §5 vol3); every item two-method verified + independently re-verified.
- **(a) `08_gravitational_waves.tex:364` prints "Gravitational waves propagate \textbf{exclusively} as lossless, trace-free, transverse impedance modulations"** — contradicted by the chapter's own Grant-F4→(a) reconciliation box (:242-258, landed #771): item-3 now carries the Reading-A bulk admixture at O(1) coupling. The 'exclusively' sentence is the pre-Reading-A voice surviving in the same chapter. The manuscript fix is GATED (ringdown-wave, per the epic sequencing); what is ROUTED here is the underlying physics question the chapter still leaves live: the Q1-REVERT bulk/shear double-count contradiction (`common/port-register.md:5` carries the REVERTED-to-Reading-A-live status; the double-count text is at `:127`). No rewrite of :364 should land until the core session settles whether Reading-A's admixture survives the port-register Q1 disposition.
- **(b) ch08 strain-saturation ratio — two distinct values across three sites**: `:11` teaches ~28 orders of magnitude below snap, while `:61` and `:355` both teach ~2e-25 (= 10^-24.7 — the same value in two forms, conflicting with :11). The KB does not pin one number. Request: core session pins the canonical V_GW/V_snap statement, so the gated ch08 lane can reconcile all three sites to it.
- **(c) KB-ward inversion (EXECUTING, disclosure only):** `gw-propagation-lossless.md` lags the ruled Reading-A admixture (no banner, still "matching LIGO observations exactly"); #771's own body owes the KB-leaf propagation. A KB-ward lane is executing that already-ruled propagation now (it is discharged-decision propagation, not adjudication). Recorded here so the core session sees the leaf move.
