### ENTRY 2026-07-31-qell-scope-propagation (2026-07-31): implementer — Ruling B1's $Q = \ell$ cold-anchor SCOPING propagated to the sites teaching it as universal (honesty-lag)

- **Class: discharged-decision propagation, not adjudication.** Grant Ruling B1 (2026-07-21) pinned the **spin scope** of $Q = \ell$: the flat topological reading is the **cold $a_* = 0$ anchor** — the $\Omega \to 0$ limit of the m$\Omega$ law $\omega_I = (\omega_R - m\Omega)/(2\ell)$ — and at catalog spins it would **fail at $\bar D_Q = -38\%$** (corrected-Kerr $Q$ rises $3.07 \to 3.49$). The ruling landed at `manuscript/ave-kb/vol3/claim-quality.md:204` (clm-395gps, the model banner), at `…/ave-merger-ringdown-eigenvalue.md § GRANT RULING B1` (the **Q-LAW COMMITMENT**), and at `…/qnm-quality-factor.md:20-31`. **It never propagated.** No new physics, no grade moved, no re-adjudication. Sibling lane: this is the **scoping** class (#808), distinct from the **accuracy** class (the sub-2% retraction) propagated by #809/#810.
- **This is a DIFFERENT question from the sub-2% retraction, and the carve is held at every site.** #809 retracted an *accuracy figure*. This lane adds a *scope* — nothing is struck anywhere in it. The Op21 mode-counting identity, the m$\Omega$ law, and the superradiance limit at $\omega_R = m\Omega$ are all left standing; B1 scoped the **BH instance**, not the identity.
- **Physical rationale carried in every banner, explicitly labelled orchestrator-walk provenance 2026-07-31 (NOT canon):** $\ell$ is an integer **mode count** — cavity-protected only (`def-quant3` sense (1), `common/vocabulary-register.md:261`, *"Failure mode: ionization destroys them"*), **not** a deformation-invariant winding — so it can only jump discretely, while the physical $Q = \omega_R/2\omega_I$ is an impedance ratio that moves *continuously* with the strain profile. An integer that can only jump cannot track a ratio that moves smoothly: the equality is a **zero-spin coincidence, not a law**, and the banked $-38\%$ at catalog spins is what that looks like.
- **Attribution carve applied at every banner (#812 audit F2, 2026-07-31).** A second, *un-ruled* claim was shipped underneath the Grant-ruling header: *"even the cold value is convention — the $1/\ell$-per-cycle leak is a scaling assertion, never a computed radiated power."* B1 is **discharged**; #808 **F7/F8 is an open unadjudicated flag**, and Grant never ruled on it. Content is unchanged and stays everywhere it was; at every site it now sits in its own sentence under its own header — **[#808 scoping F7/F8 — OPEN FLAG, not a ruling; B1 did not adjudicate this]** — so a reader cannot mistake it for ruled corpus. Sites re-attributed: `common/solver-toolchain.md` (SPIN-SCOPE banner + knot-mode bracket), `regime-eigenvalue-method.md`, `knot-mode-isomorphism.md`, `axiom-coverage-audit.md`, `lattice-extreme-bh-rationality.md`, `vol2/claim-quality.md` (clm-d9ivj1 body + strengthen-by), `op21-multi-mode-mode-counting.md`, `cross-scale-isomorphism-table.md` (pointer form), `backmatter/05_…tex`, `scale_invariant.py`, `universal_operators.py` — **11 sites in 11 files**, plus this bullet. The three **count-vs-loss** convention flags (`common/solver-toolchain.md`, `backmatter/05_…tex`, `protein-eigenvalue.md`) already carried their own non-B1 header and were left as written.
- **★ Register correction applied mid-lane (orchestrator-relayed, #811-audit provenance, 2026-07-31).** The first draft of the banners said *"$\ell$ is a strain-immune topological integer."* That borrows **def-quant3 sense (2)** protection (winding: *"invariant under any smooth deformation"*) for a **sense (1)** object (mode count: cavity-protected, *"ionization destroys them"*). Corrected at all 18 banner sites before push (commit `qell-register-correction`); the argument survives and sharpens — the discriminator is jump-vs-move, not immunity. **★ SECOND correction (#812 audit F1, 2026-07-31): the first correction moved the register error to the particle side.** The corrected knot-mode banners then read *"$c$ is a deformation-invariant **winding** (def-quant3 sense (2))"* and drew a causal conclusion from it (*"that asymmetry is itself the reason the gravitational side is spin-scoped"*). Both over-reach: `def-3638f2` holds the particle-side classification **OPEN** (status *ambiguous* — real-space knot **CROSSING** number vs the phase-space $(2,3)$ Clifford-torus **WINDING** pattern), and `def-kn0t01` locks the *phase-space* qualifier before any $(2,q)$ label attached to a particle name. Re-framed at all three sites (`knot-mode-isomorphism.md`, `common/solver-toolchain.md`, `backmatter/05_…tex`) to the **integrality** argument alone — both sides are integers, neither can drift continuously — plus an explicit open-ambiguity pointer: *whether the particle-side integer is the deformation-invariant winding class is register-OPEN (`def-3638f2`, status ambiguous) — surfaced, not adjudicated.* The causal claim is likewise demoted to observation: the gravitational side **is** spin-scoped and the particle side is not; **why** is not settled by a sense-class asymmetry this lane does not have.
- **Per-site classification (a/b/c/d/e), 27 sites edited across 21 files.**
  - **(a) BH-ringdown-application assertions → full 🟩 SPIN-SCOPE banner or short `(cold a*=0 anchor — B1)` qualifier:** `common/solver-toolchain.md` :23/:79/:390/:400/:407/:413; `vol2/appendices/app-f-solver-toolchain/regime-eigenvalue-method.md` :18/:68; `.../knot-mode-isomorphism.md` :11/:18; `.../cross-scale-isomorphism-table.md` :18/:29; `.../index.md` :19; `vol2/appendices/index.md` :24; `vol3/cosmology/ch15-black-hole-orbitals/axiom-coverage-audit.md` :17/:28/:43; `.../lattice-extreme-bh-rationality.md` :46/:92; `.../ch15/index.md` :11/:24/:43/:44; `vol3/cosmology/index.md` :27/:41; `vol3/index.md` :49; `vol2/claim-quality.md` :1091/:1095/:1117; `common/claim-quality.md` :132; `manuscript/backmatter/05_universal_solver_toolchain.tex` :18/:66/:438/:453/:462/:472; `src/ave/axioms/scale_invariant.py` (the `BH QNM 2 2 Fundamental GW ringdown` row) + `src/ave/core/universal_operators.py` (Op21) + `src/scripts/vol_3_macroscopic/simulate_black_hole_core.py:164`.
  - **(b) generic Op21 counting-identity statements citing BH ringdown as an instance → the BH INSTANCE mention tagged, the identity NOT:** `vol4/circuit-theory/ch1-vacuum-circuit-analysis/op21-multi-mode-mode-counting.md` §1 bullet / §1 table BH row / §5 fire-list item 2 / §6.1 anchor; `common/operators.md:61`; `common/ave-analytical-toolkit-index.md:75`. Pure-identity mentions with no BH instance left untagged and recorded: `common/translation-tables/translation-circuit.md` :126/:202/:439/:525, `vol9/ch3-pin-port-configuration/index.md` :21/:22/:37, `vol4/.../theorem-3-1-q-factor.md` :109/:168, `manuscript/vol_9_vacuum_datasheet/chapters/{03,05,16}`.
  - **(c) protein $Q = \ell = 7$ rows (the ambiguous class #809 flagged) → NO $a_*=0$ tag (wrong domain).** All three mirrors assert **Q-as-loss** ($\Delta f = f_0/Q$ vs a measured $\sim3.3$ THz linewidth, $-6\%$), so each gets the **count-vs-loss** cross-ref to #808 finding F7 instead: `common/solver-toolchain.md:259`, `backmatter/05_…tex:252`, and the third mirror `vol2/appendices/app-f-solver-toolchain/protein-eigenvalue.md:65` (found by sweep, not on the inherited list).
  - **(d) already-scoped → left:** `…/qnm-quality-factor.md:20-31` (the canonical scope banner, cited as the pointer from the new banners), `vol2/claim-quality.md:1097`/`:1101` (#809/#810), `vol2/appendices/app-f-solver-toolchain/kerr-q-correction.md:59`, `src/ave/solvers/orbital_resonance.py:493`.
  - **(e) frozen/dated → disclosed, not edited:** `research/2026-07-20_v1-spin-mapping-adjudication_prereg-FROZEN.md`, `research/2026-07-13_t1-atom-q-cascade-gate_prereg_FROZEN.md`, all `research/` result/derivation/framing docs, `research/_archive/`, and `_orchestration/` handoffs + docket fragments. `manuscript/vol_3_macroscopic/chapters/15_black_hole_orbital_resonance.tex` is **already bannered** at :31 with the B1 $a_*=0$ scope stated verbatim and per-occurrence reconciliation routed to the manuscript-reconciliation program (#780) — left as routed, per the same disposition #809 recorded.
- **Sites found beyond the inherited list.** The inherited enumeration (from the #809 audit) named 6 files; the exhaustive multi-pattern sweep (`Q = \ell`, `Q=ℓ`, `Q = ell`, `Q_\ell`, `quality factor.*= *[27]`, two-method git-tracked + filesystem walk over `manuscript/`, `research/`, `src/`, `_orchestration/`) found the **three App-F KB mirrors** (`regime-eigenvalue-method.md`, `knot-mode-isomorphism.md`, `cross-scale-isomorphism-table.md`) that carry the identical Step-5 / knot-mode / cross-scale text, the **third protein mirror** (`protein-eigenvalue.md`), the **five index rows** (`vol2/appendices/index.md`, `app-f-.../index.md`, `ch15/index.md`, `vol3/cosmology/index.md`, `vol3/index.md`), the **Op21 canonical leaf** BH rows, and **`simulate_black_hole_core.py`**, which applies flat $Q = \ell$ to every LIGO event at $a_* = 0.6$–$0.7$. Fourth consecutive week in which an inherited site list undercounted.
- **The reader-visible face of the $-38\%$, surfaced not fixed.** `axiom-coverage-audit.md:37-43` prints a LIGO $\tau$ table with $Q = 2$ for GW150914/GW170104/GW190521 (all at $a_* = 0.64$–$0.72$) and errors of **43% / 38%**, followed by *"the Kerr correction to $Q$ … is not yet included."* That is the cold anchor read outside its scope, and the "not yet included" clause is a **scoping lag** — under B1 the m$\Omega$ law IS computed and banked at $-5.44\%$ / $-4.57\%$. The table and its numbers are **preserved per Rule 12**; a banner discloses both facts. Nothing re-adjudicated.
- **Cite-shift sweep (the #809 R1 lesson, run up-front).** Eleven files changed line count. A programmatic two-method scan over `git ls-files` (filename`[:#]`NNN forms, comma/range forms, and the bare-prose `line NNN` form) found **11 live occurrences in 9 files**, each repaired only after verifying old-line-content == new-line-content: `lattice-extreme-bh-rationality.md:75→:87` (×4, incl. `src/ave/gravity/gw_propagation.py:49`), `common/solver-toolchain.md:521→:538` (×2), `common/solver-toolchain.md` prose *"line 274"*→*"line 287"* (×1 — invisible to a `filename:NNN` grep), `backmatter/05_…tex:156-172→:169-185` (×1), `universal_operators.py:969→:982` (×3). Cites into `regime-eigenvalue-method.md` (:18/:61/:63/:68) and `knot-mode-isomorphism.md:24` sit ABOVE the insertion points and are unshifted — verified, not assumed.
- **★ Disclosed deviation, isolated for revert.** `vol3/claim-quality.md` is a model file this lane was told to leave untouched, but its `:160` strengthen-by bullet cites `lattice-extreme-bh-rationality.md:75`, which THIS BRANCH shifted to `:87`. Commit `e13c1fa8` bumps that one token and nothing else (the clm-395gps card and every grade field are untouched); it is isolated so the orchestrator can revert it alone if "untouched" is read literally, at the cost of shipping a stale cite this lane created.
- **Flag-don't-fix residue routed to the auditor lane (surfaced, not edited):** (i) `vol2/claim-quality.md:1115` rationale still reads *"the leaf states $Q=\ell$ is consistent with observed quality factors but not shown to be the unique resolution"* — the clause it paraphrases was **STRUCK at the claim body by #810 Ruling C**; a live grade-vs-body disagreement. (ii) `common/claim-quality.md:152` rationale carries the same five-step $Q = \ell$ chain unscoped. (iii) `src/ave/core/universal_operators.py:969` (now `:982`) was **already mis-targeted at origin/main** — it resolves to `n_topology: Topological crossing number …`, not to the two-speeds content its three citers describe; shifted faithfully rather than silently re-pointed. (iv) White-dwarf $Q = \ell$ sites (`vol3/gravity/ch20-white-dwarf-predictions/white-dwarf-gravitational-predictions.md:83`, `vol3/claim-quality.md:1171/:1174`, `manuscript/vol_3_macroscopic/chapters/20_white_dwarf_predictions.tex:156`, `src/scripts/vol_4_engineering/wd_shear_eigenfrequency.py`) are **out of B1's Kerr-BH scope** and got no tag — but WDs rotate, so whether an analogous scope applies is an open question this lane has no mandate to answer.
- **Battery:** `make verify` exit 0 (baseline on `origin/main` also 0); `make refresh-kb-metadata` reports **0 solidity lines**, 0 subtree-claims, 0 depends-on annotations, 0 leaf-reference footers — one derived record in `.index/strengthen-by.jsonl` per strengthen-by-text change, idempotent on re-run; `verify-md-links` gating 0; `verify-docket-keys` no new duplicate keys; `pdflatex -draftmode -halt-on-error vol_0_engineering_compendium/main.tex` (the only volume whose manifest includes `backmatter/05`) RC=0 with zero `^!` errors; `pytest src/tests/test_cross_domain_operators.py src/tests/engine_acceptance/test_operators.py` → 26 passed; all five touched `src` files **AST-EQUAL** to HEAD with docstrings normalised out (zero executable delta).
