### ENTRY 2026-07-31-qell-scope-propagation (2026-07-31): implementer — Ruling B1's $Q = \ell$ cold-anchor SCOPING propagated to the sites teaching it as universal (honesty-lag)

- **Class: discharged-decision propagation, not adjudication.** Grant Ruling B1 (2026-07-21) pinned the **spin scope** of $Q = \ell$: the flat topological reading is the **cold $a_* = 0$ anchor** — the $\Omega \to 0$ limit of the m$\Omega$ law $\omega_I = (\omega_R - m\Omega)/(2\ell)$ — and at catalog spins it would **fail at $\bar D_Q = -38\%$** (corrected-Kerr $Q$ rises $3.07 \to 3.49$). The ruling landed at `manuscript/ave-kb/vol3/claim-quality.md:204` (clm-395gps, the model banner), at `…/ave-merger-ringdown-eigenvalue.md § GRANT RULING B1` (the **Q-LAW COMMITMENT**), and at `…/qnm-quality-factor.md:20-31`. **It never propagated.** No new physics, no grade moved, no re-adjudication. Sibling lane: this is the **scoping** class (#808), distinct from the **accuracy** class (the sub-2% retraction) propagated by #809/#810.
- **This is a DIFFERENT question from the sub-2% retraction, and the carve is held at every site.** #809 retracted an *accuracy figure*. This lane adds a *scope* — nothing is struck anywhere in it. The Op21 mode-counting identity, the m$\Omega$ law, and the superradiance limit at $\omega_R = m\Omega$ are all left standing; B1 scoped the **BH instance**, not the identity.
- **Physical rationale carried in every banner, explicitly labelled orchestrator-walk provenance 2026-07-31 (NOT canon):** $\ell$ is an integer **mode count** — cavity-deformable (`common/vocabulary-register.md:261` (def-quant3), sense (1) at `:271`, *"Failure mode: ionization destroys them"*), **not** a deformation-invariant winding. **The discriminator is its INTEGRALITY, not its sense class** (#812 audit F5 — discreteness follows from being an integer, not from the sense assignment): an integer can only jump discretely, while the physical $Q = \omega_R/2\omega_I$ is an impedance ratio that moves *continuously* with the strain profile. An integer that can only jump cannot track a ratio that moves smoothly: the equality is a **zero-spin coincidence, not a law**, and the banked $-38\%$ at catalog spins is what that looks like.
- **Discriminator normalised + register vocabulary adopted (#812 audit F5, 2026-07-31).** Three banner variants inferred discreteness FROM the sense assignment — *"**not** a deformation-invariant winding — **so** it cannot drift continuously"*. That is a non-sequitur: an integer cannot drift continuously because it is an **integer**, not because of which of `def-quant3`'s three senses it belongs to. (The knot-mode variant already had it right: *"neither can drift continuously; both can only jump discretely."*) All variants — long-banner, short-bracket and src-docstring — now state the discriminator explicitly: **the discriminator is INTEGRALITY, not sense class**. The sense-(1) classification of $\ell$ is retained (it is register-accurate and is what scopes the claim); only the false inference is broken. Separately, the coinage *"cavity-protected only"* is replaced by the register's own word **"cavity-deformable"** (`common/vocabulary-register.md:274`: *"sense (1) is cavity-deformable, sense (2) is topology-protected, sense (3) is a Class-B sampling floor"*) at **all 18 instances** — 0 residual.
- **Register cite repaired, off by ten (#812 audit F6, 2026-07-31).** Every banner cited `common/vocabulary-register.md:261` as the home of the quoted *"Failure mode: ionization destroys them"*. `:261` is the `<!-- id: def-quant3 -->` comment; the sense-(1) bullet carrying that verbatim string is at **`:271`** (verified at HEAD). All **17** cite sites now read `:261` (def-quant3), sense (1) at `:271` — the id anchor and the quoted line, both navigable.
- **Attribution carve applied at every banner (#812 audit F2, 2026-07-31).** A second, *un-ruled* claim was shipped underneath the Grant-ruling header: *"even the cold value is convention — the $1/\ell$-per-cycle leak is a scaling assertion, never a computed radiated power."* B1 is **discharged**; #808 **F7/F8 is an open unadjudicated flag**, and Grant never ruled on it. Content is unchanged and stays everywhere it was; at every site it now sits in its own sentence under its own header — **[#808 scoping F7/F8 — OPEN FLAG, not a ruling; B1 did not adjudicate this]** — so a reader cannot mistake it for ruled corpus. Sites re-attributed: `common/solver-toolchain.md` (SPIN-SCOPE banner + knot-mode bracket), `regime-eigenvalue-method.md`, `knot-mode-isomorphism.md`, `axiom-coverage-audit.md`, `lattice-extreme-bh-rationality.md`, `vol2/claim-quality.md` (clm-d9ivj1 body + strengthen-by), `op21-multi-mode-mode-counting.md`, `cross-scale-isomorphism-table.md` (pointer form), `backmatter/05_…tex`, `scale_invariant.py`, `universal_operators.py` — **11 sites in 11 files**, plus this bullet. The three **count-vs-loss** convention flags (`common/solver-toolchain.md`, `backmatter/05_…tex`, `protein-eigenvalue.md`) already carried their own non-B1 header and were left as written.
- **★ Register correction applied mid-lane (orchestrator-relayed, #811-audit provenance, 2026-07-31).** The first draft of the banners said *"$\ell$ is a strain-immune topological integer."* That borrows **def-quant3 sense (2)** protection (winding: *"invariant under any smooth deformation"*) for a **sense (1)** object (mode count: cavity-deformable, *"ionization destroys them"*). Corrected at all 18 banner sites before push (commit `qell-register-correction`); the argument survives and sharpens — the discriminator is jump-vs-move, not immunity. **★ SECOND correction (#812 audit F1, 2026-07-31): the first correction moved the register error to the particle side.** The corrected knot-mode banners then read *"$c$ is a deformation-invariant **winding** (def-quant3 sense (2))"* and drew a causal conclusion from it (*"that asymmetry is itself the reason the gravitational side is spin-scoped"*). Both over-reach: `def-3638f2` holds the particle-side classification **OPEN** (status *ambiguous* — real-space knot **CROSSING** number vs the phase-space $(2,3)$ Clifford-torus **WINDING** pattern), and `def-kn0t01` locks the *phase-space* qualifier before any $(2,q)$ label attached to a particle name. Re-framed at all three sites (`knot-mode-isomorphism.md`, `common/solver-toolchain.md`, `backmatter/05_…tex`) to the **integrality** argument alone — both sides are integers, neither can drift continuously — plus an explicit open-ambiguity pointer: *whether the particle-side integer is the deformation-invariant winding class is register-OPEN (`def-3638f2`, status ambiguous) — surfaced, not adjudicated.* The causal claim is likewise demoted to observation: the gravitational side **is** spin-scoped and the particle side is not; **why** is not settled by a sense-class asymmetry this lane does not have.
- **Per-site classification (a/b/c/d/e). ★ COUNT CORRECTED (#812 audit F3): 50 scope-tag/flag sites across 21 files, not "27 across 21" — the shipped header undercounted its own enumeration by ~half. **Counting rule (stated so it is auditable):** one *site* = one distinct edited location — a table row, a numbered-list item, one contiguous banner block (however many lines), or one docstring/comment block. Two adjacent table rows are two sites; a 20-line banner is one. Under that rule: **(a) 41 + (b) 6 + (c) 3 = 50 scope-tag/flag sites in 21 files**, plus the 2 sites added by #812 F4 in `common/identity-break-test-design.md` → **(a) 43, total 52 in 22 files**. Separately this branch also touched **12 mechanical line-cite repairs in 10 files** and **2 derived `.index/strengthen-by.jsonl` records** — **66 edit locations across 33 corpus files** in total (34 with this fragment). **Every line number below is regenerated against the branch HEAD after the #812 repairs**, not the pre-repair state.
  - **(a) BH-ringdown-application assertions → full 🟩 SPIN-SCOPE banner or short `(cold a*=0 anchor — B1)` qualifier — 41 sites in 17 files:** `common/solver-toolchain.md` :23/:90/:411/:426/:436/:444 (6); `vol2/appendices/app-f-solver-toolchain/regime-eigenvalue-method.md` :18/:79 (2); `.../knot-mode-isomorphism.md` :18/:28 (2); `.../cross-scale-isomorphism-table.md` :18/:34 (2); `.../index.md` :19 (1); `vol2/appendices/index.md` :24 (1); `vol3/cosmology/ch15-black-hole-orbitals/axiom-coverage-audit.md` :17/:33/:66 (3); `.../lattice-extreme-bh-rationality.md` :46/:52/:112 (3); `.../ch15-black-hole-orbitals/index.md` :11/:24/:43/:44 (4); `vol3/cosmology/index.md` :27/:41 (2); `vol3/index.md` :49 (1); `vol2/claim-quality.md` :1091/:1095/:1117 (3); `common/claim-quality.md` :132 (1); `manuscript/backmatter/05_universal_solver_toolchain.tex` :18/:77/:468/:488/:504/:531 (6); `src/ave/axioms/scale_invariant.py` :437 (the docstring SPIN-SCOPE block) + :467 (the `BH QNM 2 2 Fundamental GW ringdown` table row) (2); `src/ave/core/universal_operators.py` :942 (Op21 docstring) (1); `src/scripts/vol_3_macroscopic/simulate_black_hole_core.py` :164 (1).
  - **(b) generic Op21 counting-identity statements citing BH ringdown as an instance → the BH INSTANCE mention tagged, the identity NOT — 6 sites in 3 files:** `vol4/circuit-theory/ch1-vacuum-circuit-analysis/op21-multi-mode-mode-counting.md` :31 (§1 single-channel bullet) / :36 (§1 table BH-ringdown row) / :210 (§5 fire-list item 2) / :229 (§6.1 anchor) (4); `common/operators.md` :61 (1); `common/ave-analytical-toolkit-index.md` :75 (1). Pure-identity mentions with no BH instance left untagged and recorded (verified at HEAD, unedited and unshifted): `common/translation-tables/translation-circuit.md` :126/:202/:439/:525, `vol9/ch3-pin-port-configuration/index.md` :21/:22/:37, `vol4/circuit-theory/ch1-vacuum-circuit-analysis/theorem-3-1-q-factor.md` :109/:168, and `manuscript/vol_9_vacuum_datasheet/chapters/{03_pin_port_configuration,05_ac_electrical_characteristics,16_cross_volume_reference}.tex`.
  - **(c) protein $Q = \ell = 7$ rows (the ambiguous class #809 flagged) → NO $a_*=0$ tag (wrong domain) — 3 sites in 3 files.** All three mirrors assert **Q-as-loss** ($\Delta f = f_0/Q$ vs a measured $\sim3.3$ THz linewidth, $-6\%$), so each gets the **count-vs-loss** cross-ref to #808 finding F7 instead — under its own non-B1 header, so the #812 F2 attribution carve does not apply to these: `common/solver-toolchain.md` :281, `manuscript/backmatter/05_universal_solver_toolchain.tex` :278, and the third mirror `vol2/appendices/app-f-solver-toolchain/protein-eigenvalue.md` :68 (found by sweep, not on the inherited list).
  - **★ (#812 audit F4) THREE SITES THE SWEEP MISSED — fifth consecutive undercount.** The multi-pattern sweep that produced the (a)/(b)/(c) lists above still missed three, all found by the #812 audit:
    - **`common/identity-break-test-design.md` :55 and :65-77 → CLASS (a), now tagged.** Both are BH-ringdown $Q = \ell$ assertions taught without the spin scope (`:55` *"between the electron's loaded `α⁻¹` and the black hole's `Q = ℓ`"*; `:65-66` *"The black-hole endpoint stays canon and **distinct** (`Q = ℓ`, Op21 at the at/above-rupture `Γ = −1` boundary)"*). `:55` gets the short `(cold a*=0 anchor — B1)` qualifier; a short bracket at `:68-77` scopes both mentions and states explicitly that the section's *endpoint-vs-atom contrast is unaffected* — it turns on `Γ = −1` being a structural cutoff, not on the value of $Q$.
    - **`common/identity-break-test-design.md:162` → FLAG-DON'T-FIX, ROUTED TO GRANT (row NOT edited).** The THREE-VALUE-GENERATORS table files BH `Q = ℓ` under the **"Topology | the integers"** generator — which is the exact sense-conflation this lane corrects: the BH-side $\ell$ is a `def-quant3` sense-(1) **mode count**, not a topological winding integer, so "Topology owns it" is the over-read. This site **also** sits on the **#668 T1 routed Q-ladder relabel** (the atom rung), which is an auditor-lane deliverable, not this lane's. **Disposition — routed to Grant: class-(a) tag here vs deferral to the #668 relabel lane.** Not edited either way; surfaced, not adjudicated.
    - **`src/ave/core/regime_map.py:495` → CLASS (d), out of scope, no tag.** `"(Q = ℓ_min = 2)"` is **not** the BH-ringdown identity and not the same $Q$: it labels the regime II→III boundary $r_2 = \sqrt3/2$ where the *response ratio* $Q(r) = 1/S(r)$ reaches 2, with $\ell_{min} = 2$ named as "the lowest radiating multipole" (`regime_map.py:26`, `:60`). The ringdown $Q = \omega_R/2\omega_I$ never appears. B1's Kerr-BH spin scope does not reach it; no BH context, so no comment-tag. **Surfaced observation, not a fix:** the two objects share the glyph $Q$ and the integer 2 and nothing else — a homonym worth a register node, which this lane has no mandate to mint.
    - **`src/ave/core/constants.py:1163` → CLASS (d), out of scope, no tag.** `"Step 5: Q = ℓ = 1"` is the **nuclear** domain fire of the same five-step method, so B1's Kerr-BH spin scope does not apply (same disposition as the protein rows). It also does **not** earn the class-(c) count-vs-loss flag: unlike the three protein mirrors there is no $\Delta f = f_0/Q$ reading here, and the value is **inert** — no `Q_NUCLEAR` symbol exists and neither `D_NN_EIGENVALUE` (`:1176`) nor `B_DEUTERON_PREDICTED` (`:1183`) consumes it. Recorded, unedited.
  - **(d) already-scoped → left (re-verified at HEAD 2026-07-31; none of these files was touched by this branch, so the line numbers are unshifted):** `…/qnm-quality-factor.md:20-31` (the canonical scope banner, cited as the pointer from the new banners), `vol2/claim-quality.md:1097`/`:1101` (#809/#810), `vol2/appendices/app-f-solver-toolchain/kerr-q-correction.md:59`, `src/ave/solvers/orbital_resonance.py:493`.
  - **(e) frozen/dated → disclosed, not edited:** `research/2026-07-20_v1-spin-mapping-adjudication_prereg-FROZEN.md`, `research/2026-07-13_t1-atom-q-cascade-gate_prereg_FROZEN.md`, all `research/` result/derivation/framing docs, `research/_archive/`, and `_orchestration/` handoffs + docket fragments. `manuscript/vol_3_macroscopic/chapters/15_black_hole_orbital_resonance.tex` is **already bannered** at :31 with the B1 $a_*=0$ scope stated verbatim and per-occurrence reconciliation routed to the manuscript-reconciliation program (#780) — left as routed, per the same disposition #809 recorded.
- **Sites found beyond the inherited list.** The inherited enumeration (from the #809 audit) named 6 files; the exhaustive multi-pattern sweep (`Q = \ell`, `Q=ℓ`, `Q = ell`, `Q_\ell`, `quality factor.*= *[27]`, two-method git-tracked + filesystem walk over `manuscript/`, `research/`, `src/`, `_orchestration/`) found the **three App-F KB mirrors** (`regime-eigenvalue-method.md`, `knot-mode-isomorphism.md`, `cross-scale-isomorphism-table.md`) that carry the identical Step-5 / knot-mode / cross-scale text, the **third protein mirror** (`protein-eigenvalue.md`), the **five index rows** (`vol2/appendices/index.md`, `app-f-.../index.md`, `ch15/index.md`, `vol3/cosmology/index.md`, `vol3/index.md`), the **Op21 canonical leaf** BH rows, and **`simulate_black_hole_core.py`**, which applies flat $Q = \ell$ to every LIGO event at $a_* = 0.64$–$0.74$ (`LIGO_EVENTS` = 0.67 / 0.64 / 0.74; the range in the shipped comment read "0.6-0.7", corrected per #812 audit F7 to the canonical catalog-spin range at `qnm-quality-factor.md:28`). **Fifth** consecutive week in which an inherited site list undercounted — and this lane's own exhaustive sweep then undercounted again by three (see the #812 F4 bullet below). The sweep's blind spot both times was the same: it keyed on `Q = \ell` *formula* forms and missed prose/table mentions in leaves that are not about the BH (`identity-break-test-design.md`, `regime_map.py`, `constants.py`).
- **The reader-visible face of the $-38\%$, surfaced not fixed.** `axiom-coverage-audit.md:58-62` prints a LIGO $\tau$ table with $Q = 2$ for GW150914/GW170104/GW190521 (all at $a_* = 0.64$–$0.72$) and errors of **43% / 38%**, followed at `:64` by *"the Kerr correction to $Q$ … is not yet included."* (HEAD line numbers, regenerated per #812 F3; the banner disclosing both facts is at `:66-78`.) That is the cold anchor read outside its scope, and the "not yet included" clause is a **scoping lag** — under B1 the m$\Omega$ law IS computed and banked at $-5.44\%$ / $-4.57\%$. The table and its numbers are **preserved per Rule 12**; a banner discloses both facts. Nothing re-adjudicated.
- **Cite-shift sweep (the #809 R1 lesson) — run twice, and the second run mattered.** Twelve files changed line count on this branch. A programmatic two-method scan over `git ls-files` (filename`[:#]`NNN forms, comma/range forms, markdown-link `](path):NNN` forms, and the bare-prose `line NNN` form) found **12 live occurrences in 10 files**, each repaired only after mapping the ORIGINAL `origin/main` line through a `difflib` opcode map to its HEAD position and confirming the content matches. **★ The #812 repair commits themselves shifted every one of those targets again** (they add lines above the cite targets in `lattice-extreme-bh-rationality.md`, `common/solver-toolchain.md`, `backmatter/05_…tex` and `universal_operators.py`), so the sweep was re-run at the repaired HEAD and the cites re-bumped. **Final, HEAD-accurate values** (origin/main line → HEAD line): `lattice-extreme-bh-rationality.md:75 → :95` (×4 — `vol3/gravity/ch02-general-relativity/einstein-field-equation.md:72`, `vol4/claim-quality.md:138` and `:1142`, `src/ave/gravity/gw_propagation.py:49`; a fifth, `vol3/claim-quality.md:160`, is the disclosed deviation below); `common/solver-toolchain.md:521 → :548` (×2 — `vol5/common/translation-protein-solver.md:27`, `vol5/common/translation-protein.md:34`); `common/solver-toolchain.md` prose *"line 274" → "line 295"* (×1, `vol2/particle-physics/ch02-baryon-sector/proton-identification.md:95` — invisible to a `filename:NNN` grep); `backmatter/05_universal_solver_toolchain.tex:156-172 → :179-195` (×1, `common/translation-tables/translation-circuit.md:762`); `src/ave/core/universal_operators.py:969 → :994` (×3 — `vol4/circuit-theory/ch1-vacuum-circuit-analysis/cvr-dc-operating-point.md:35`, `src/scripts/vol_9_device/cvr_ee_sweep/cvr_model.py:13` and `:111`). The intermediate values shipped in the pre-repair fragment (`:87`, `:538`, *"line 287"*, `:169-185`, `:982`) were correct for the pre-repair head and are **superseded**, not re-asserted. Cites into `regime-eigenvalue-method.md` (:18/:43/:61/:63/:68) and `knot-mode-isomorphism.md:24` sit ABOVE every insertion point and remain correct — re-verified by content at the repaired HEAD, not assumed. (`regime-eigenvalue-method.md:18` is an in-place content edit at the same line number, which is the intended target.)
- **★ Disclosed deviation, isolated for revert.** `vol3/claim-quality.md` is a model file this lane was told to leave untouched, but its `:160` strengthen-by bullet cites `lattice-extreme-bh-rationality.md:75`, which THIS BRANCH shifted to `:87`. Commit `e13c1fa8` bumps that one token and nothing else (the clm-395gps card and every grade field are untouched); it is isolated so the orchestrator can revert it alone if "untouched" is read literally, at the cost of shipping a stale cite this lane created.
- **Flag-don't-fix residue routed to the auditor lane (surfaced, not edited):** (i) `vol2/claim-quality.md:1115` rationale still reads *"the leaf states $Q=\ell$ is consistent with observed quality factors but not shown to be the unique resolution"* — the clause it paraphrases was **STRUCK at the claim body by #810 Ruling C**; a live grade-vs-body disagreement. (ii) `common/claim-quality.md:152` rationale carries the same five-step $Q = \ell$ chain unscoped. (iii) `src/ave/core/universal_operators.py:969` (now `:994` at the repaired HEAD) was **already mis-targeted at origin/main** — it resolves to `n_topology: Topological crossing number …`, not to the two-speeds content its three citers describe; shifted faithfully rather than silently re-pointed. (iv) White-dwarf $Q = \ell$ sites (`vol3/gravity/ch20-white-dwarf-predictions/white-dwarf-gravitational-predictions.md:83`, `vol3/claim-quality.md:1171/:1174`, `manuscript/vol_3_macroscopic/chapters/20_white_dwarf_predictions.tex:156`, `src/scripts/vol_4_engineering/wd_shear_eigenfrequency.py`) are **out of B1's Kerr-BH scope** and got no tag — but WDs rotate, so whether an analogous scope applies is an open question this lane has no mandate to answer.
- **Battery:** `make verify` exit 0 (baseline on `origin/main` also 0); `make refresh-kb-metadata` reports **0 solidity lines**, 0 subtree-claims, 0 depends-on annotations, 0 leaf-reference footers — one derived record in `.index/strengthen-by.jsonl` per strengthen-by-text change, idempotent on re-run; `verify-md-links` gating 0; `verify-docket-keys` no new duplicate keys; `pdflatex -draftmode -halt-on-error vol_0_engineering_compendium/main.tex` (the only volume whose manifest includes `backmatter/05`) RC=0 with zero `^!` errors; `pytest src/tests/test_cross_domain_operators.py src/tests/engine_acceptance/test_operators.py` → 26 passed; all five touched `src` files **AST-EQUAL** to HEAD with docstrings normalised out (zero executable delta).
