### ENTRY 2026-07-22-register-defrow-batch (2026-07-22): implementer — vocabulary-register def-rows for relative-offset + rim-inversion (post-merge batch)

- **Deferred def-rows discharged.** The two 2026-07-21 walk leaves each deferred their `def-` vocabulary-register row to a "post-#781 batch" (register-row conflict-risk) while their NAMES were still `PROPOSED-FOR-RATIFICATION`. Both names are now **Grant-ratified-at-merge** — PR **#787** (`relative-offset-principle`, merged 2026-07-21) and PR **#790** (`saturation-rim-inversion`, merged 2026-07-21) — so the two rows are minted into [`manuscript/ave-kb/common/vocabulary-register.md`](../../manuscript/ave-kb/common/vocabulary-register.md) following the register's exact `def-` conventions (12-field block, INVARIANT-S12).
- **`def-r0ffst` — the relative-offset principle.** Status **SOLID for the NAME/term** (Grant-ratified-at-merge, PR #787), locked to [`relative-offset-principle.md`](../../manuscript/ave-kb/common/relative-offset-principle.md). The `def-` grade locks the TERM only, not a physics grade (a `def-` node carries no solidity): the physics is DIRECTION-not-MAGNITUDE — `clm-hu1jjw` (principle + Fork-ρ direction, **0.50**, walk-ratified/pending review) + `clm-m5swh9` (loaded-inertia MAGNITUDE / β / sector-crossed c², **OPEN `*pending*`**). axis `other` (a phase-only frame/position principle, not a substrate noun → no `ontology-grade`). open-ambiguity **no** (single-sense compound), with read-guards recorded vs `def-cc2196` "node" (graph-vertex sense, NOT the SOLID spatial-Brillouin cell) and `def-u0star` "offset/bias" (the frozen DC operating point).
- **`def-satrim` — saturation rim-inversion.** Status **SOLID for the NAME/term** (Grant-ratified-at-merge at the MAPPING level, PR #790), locked to [`saturation-rim-inversion.md`](../../manuscript/ave-kb/common/saturation-rim-inversion.md). TERM-only grade: the MAPPING is `clm-riminv` (**0.55**, consistency-class), with two lower-grade riders — `clm-satnec` (topological-necessity CANDIDATE, **OPEN `*pending*`**, = the K=2G eigenmode-existence open item, NOT Grant-ratified) + `clm-zdual1` (channel-duality re-reading over INVARIANT-S2, **0.50**). axis `phase-carrier` with the A46 coordinate-discipline flag (a per-mode PHASE-SPACE reading, NOT a real-space radial profile); not a substrate noun → no `ontology-grade`.
- **Sense-collision guard (the reason for the deferral).** The surface word **"inversion"** is overloaded in the register, so `def-satrim` carries **open-ambiguity YES** disambiguating four senses + one cross-ref: (a) THIS node's phase-space CHARACTER-inversion; (b) the parity-meter "inversion-symmetry" / even-order bias (`def-u0star` Sense B, `clm-invmtr`, `universal-saturation-kernel-catalog.md:203`); (c) the TKI "INVERTIBLE dictionary" (`def-tk1xfm`, `translation-circuit.md:17`); (d) Grant's PARKED sign-flip "phasor changing" (`saturation-rim-inversion.md:74`, no-claim); plus a **KEEP-BOTH cross-ref (same object, not a competing sense)** to the **MOS inversion-layer** translation-circuit row (`translation-circuit.md:299` §4.6.2 Row B / `:400` row 30, `clm-riminv`). A "rim" read-guard separates the phase-space unit-circle boundary from the real-space envelope wall/knee (A46). `def-cc2196` "node" senses do not collide with either new term (both compound terms are unique surface forms; the "node" usage is internal to the relative-offset principle and read-guarded).
- **Pipeline.** `make refresh-kb-metadata` materialized both as `node_type: "definition"` records (KB now 60 definitions); `def-r0ffst` open_ambiguity=false, `def-satrim` open_ambiguity=true with 4 conflicting_sites. `make verify-kb-metadata` + `make verify` PASS. `.index/*` regenerated by refresh, never hand-edited.
- **RETURN.** def-node ids: **`def-r0ffst`** (status SOLID for the name), **`def-satrim`** (status SOLID for the name, open-ambiguity YES). Branch `docs/register-defrow-batch`; PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.
