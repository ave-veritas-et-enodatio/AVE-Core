### ENTRY 2026-08-03-stale-refs-propagation (2026-08-03): implementer — propagate the merged #830 predicted-vs-measured relabel and the merged #822 R4 cite mapping to their flagged sites; the K2G "off-by-one" returns NOT-A-DEFECT

- **Class: mechanical propagation of already-ruled fixes.** No physics decision, no adjudication, **zero ids minted** (INVARIANT-S11) — no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; no `confidence`, `quality`, `solidity` or any grade field moves. Three dispatched items: **ITEM 1 landed** (shape adapted to a Rule-12 constraint the dispatch did not carry — surfaced, not worked around), **ITEM 2 landed** (after its collision fence fired and then cleared when #822 merged mid-session), **ITEM 3 executed as a correction in the opposite direction** — the dispatched defect was verified **not to exist**, so the mistaken disclosure is what gets corrected. The `FROZEN` prereg was **not touched**; no other lane's docket fragment was edited.
- **★ Base moved mid-session; every finding re-verified against the new base.** Work started at `origin/main` `2fcde4db`; `origin/main` advanced to **`583d43dd`** partway through (merging **#822**, #834, #835). The branch was **rebased clean** onto `583d43dd`, ITEM 1's diff re-checked additive-only against it, both ITEM-1 defect sites re-read at the new base and confirmed **still defective**, the corpus sweep **re-run** at the new base (same 7 hits, unchanged), and the collision fence **re-run** against the refreshed open set (#833/#838/#841/#842/#843/#844/#845/#846/#847 — **zero overlap on the four files that existed when it ran**). ⚑ **This PR ships FIVE files, not four:** the fifth is the `-correction` fragment ITEM 3 minted *after* the fence had already run, so it was never in the fence's input set; the five-file fence has since been re-run and re-confirmed clean (receipts under ITEM 2). Nothing in this entry rests on the stale base.

#### ITEM 1 — the #830 predicted-vs-measured relabel, propagated to both flagged upstream sites (LANDED)

- **What merged #830 (`4abc407c`) established.** The un-7'd $GM/(c^2R) = 2.59\times10^{-4}$ at the Sirius B parameters ($M = 1.018\,M_\odot$, $R = 5800$ km) is the **GR-PREDICTED** redshift, **not** the measured one. Receipt re-run here rather than restated: $c\,GM/(c^2R) = 77.72$ km/s, exact-Schwarzschild $z$ at the same $M$, $R$ = $77.75$ km/s; the **measured** Sirius B line shift is $80.65 \pm 0.77$ km/s $= 2.69\times10^{-4}$ — $3.7\%$ higher, and $(80.65 - 77.75)/0.77 = \mathbf{3.8\sigma}$ on the quoted uncertainty. Predicted and measured are **not interchangeable at the printed precision**.
- **The two sites #830 flagged and left byte-untouched, both verified still defective at `origin/main` (`2fcde4db`) before editing:**
  1. [`stellar-regime-classification.md`](../../manuscript/ave-kb/vol3/applied-physics/ch07-stellar-interiors/stellar-regime-classification.md)`:26` — verbatim *"$3\times10^{-4}$ matches $GM/(c^2R) = 2.59\times10^{-4}$ **(the measured gravitational redshift its own consequence-column cites)**"*. Line number matches the dispatch; **not** fixed by any intervening merge.
  2. [`domain-catalog.md`](../../manuscript/ave-kb/vol1/operators-and-regimes/ch7-regime-map/domain-catalog.md)`:55` — verbatim *"which is also the **measured gravitational redshift**, the observable its vol3 companion table's *Physical consequence* column cites"*. Line number matches the dispatch; **not** fixed by any intervening merge.
- **★ THE DISPATCH'S SHAPE WAS NOT AVAILABLE — RULE-12 CONSTRAINT SURFACED, NOT WORKED AROUND.** The dispatch said *"apply the same relabel"*, i.e. an in-place edit. **Both sites sit inside Rule-12-PRESERVED blocks whose preservation is asserted, in print, by the merged resolution immediately beneath each one:**
  - `domain-catalog.md`:57 — *"whose text is **preserved unedited per Rule 12**"*
  - `stellar-regime-classification.md`:28 — *"that text is **preserved unedited per Rule 12**"*

  An in-place relabel would have made **both of those merged sentences false**, in the same files, in the same pass. #830's own commit message records the identical reason for stopping — *"`:55` is the Rule-12-preserved 2026-08-01 note this entry's own resolution sits beneath (**editing it would break the preservation this PR relies on**)"* — a half of the rationale the dispatch did not carry. The dispatch's premise (*"#830 left them byte-untouched because they were another lane's text"*) is **incomplete, not wrong**: the other-lane reason is real, the Rule-12 reason is the binding one, and it survives this lane taking ownership of the file set.
- **Shape actually taken: the additive dated disclosed-correction — the same pattern the dispatch itself specifies for a frozen target.** At each site, a `**Correction note (2026-08-03 …)**` block appended **after** the existing resolution (so the audit trail reads forward), which (i) **quotes the defective clause verbatim**, (ii) states the correct label with the full arithmetic receipt above, (iii) states explicitly that the preserved text is left byte-untouched **and why**, (iv) records that the clause's *other* assertion — redshift-not-strain is what the consequence column cites — is correct and unaffected. **Diff is additive-only: 6 insertions, 0 deletions, 2 files** (`git diff --stat`; `git diff -U0 | grep "^-"` returns only the two `--- a/` file headers). The preserved 2026-08-01 text at both `:26` and `:55` is byte-identical to `origin/main`.
- **No physics moves.** The displaced $3\times10^{-4}$ remains the un-7'd potential and remains a different *quantity* from the strain; the row value $1.81\times10^{-3}$, the *Physical consequence* cells, and the `regime_map.py:524` code-site routing are all untouched.

##### ★ The corpus sweep — the inherited site list was SHORT, as expected (two methods, honestly reported)

Two independent methods, run at `origin/main`: **(A)** `git grep -n "measured gravitational redshift"` plus the broader family `-iE "measured[^.]{0,40}redshift|redshift[^.]{0,30}measured"`; **(B)** a Python walk over all **4897** tracked files with `re.compile(r"measured[- ]?(gravitational )?redshift", re.I)`. **Both returned the same 7 hits** — method (A)'s broader family added exactly one extra, `src/scripts/vol_4_engineering/white_dwarf_saturation_redshift.py:16` (*"WD redshift measured to ~0.1%"*), which is a statement about **measurement precision**, not the predicted-vs-measured mislabel, and is **not** a defect. Classification of the 7:

| # | Site | Class | Disposition |
|---|---|---|---|
| 1 | `stellar-regime-classification.md`:26 | **DEFECT** — dispatched | **CORRECTED** (additive note) |
| 2 | `domain-catalog.md`:55 | **DEFECT** — dispatched | **CORRECTED** (additive note) |
| 3 | `docket-entries/2026-08-02-rulings-d8-ell-split.md`:46 | not a defect — #830's own correct *description* of the defect | untouched |
| 4 | `docket-entries/2026-08-02-rulings-d8-ell-split.md`:51 | not a defect — #830's own correct routing note | untouched |
| 5 | `_orchestration/2026-08-02_manuscript-reconciliation-board.md`:340 | **DEFECT — NOT DISPATCHED, FLAGGED NOT FIXED** | see below |
| 6 | `docket-entries/2026-08-01-factor7-and-782-basis.md`:32 | **DEFECT — NOT DISPATCHED, FLAGGED NOT FIXED** | see below |
| 7 | `docket-entries/2026-08-02-mr-astro-singletons.md`:5 | **DEFECT — NOT DISPATCHED, FLAGGED NOT FIXED** | see below |

- **Site 5** — `_orchestration/2026-08-02_manuscript-reconciliation-board.md`:340 carries *"(the measured gravitational redshift its own consequence-column cites)"* **and a second, independent defect: it prints the with-7 value as $1.77\times10^{-3}$**, where every other corpus site prints $1.81\times10^{-3}$ (recomputed here: $7GM/(c^2R) = 1.8148\times10^{-3}$ at $M = 1.018\,M_\odot$, $R = 5800$ km).
  - **★ ATTRIBUTION CORRECTED — the value defect is NOT this lane's discovery.** An earlier phrasing of this bullet called it *"a second, independent defect the dispatch did not know about"* and cited #830's fragment at its `:51`. `:51` is only the *"why not fixed here"* note; **the discovery, with the full counterfactual arithmetic, is at [`2026-08-02-rulings-d8-ell-split.md`](2026-08-02-rulings-d8-ell-split.md)`:50`**, verbatim: *"on canonical constants $7GM/(c^2R) = 1.77\times10^{-3}$ needs $R = 5947$ km at $M = 1.018\,M_\odot$, or $M = 0.993\,M_\odot$ at $R = 5800$ km — **neither pair is tabulated anywhere in the corpus**"*. Merged #830 had already found it, computed it, and routed it. What this lane contributes is an **independent re-confirmation at a moved base** (`583d43dd`) reached by a corpus sweep that did not start from #830's finding — corroboration, not a find. The dispatch did not carry the defect; #830 did, and the entry now points at the line that carries it.
  - **Line drift on the live board, noted so the two cites reconcile:** #830 located these fields at the board's `:337-338`; they now sit at `:339` (`PRINTED:`) and `:340` (`KB TRUTH:`) under a header at `:338`, a **+2 shift with byte-identical content** — the board is a live working file and has grown above the row. Same defect, same row, different line number; neither cite is wrong for its own base.
  - **Not fixed:** this is the manuscript-reconciliation epic's **live working board**, and #830 stated the reason at its `:51` (*"editing it would open exactly the cross-lane conflict the fragment convention exists to prevent"*). **Both defects flagged with path + verbatim content; neither silently reconciled.** Routed to the manuscript-reconciliation lane.
- **Sites 6 and 7** — both are **other lanes' merged docket fragments** (#820's own, and the mr-astro-singletons lane's). [`README.md`](README.md):7 is binding: *"One lane, one file. **Never edit another lane's fragment** (Rule 12 corrections = a dated `-correction` suffix file referencing the original)."* Site 6 reads *"which is also the **measured-redshift observable** its own *Physical consequence* column cites"*; site 7 reads *"prints 3e-4 (matches GM/c²R, **the measured redshift**)"*. Both are the same predicted-vs-measured defect. **Flagged, not fixed** — a `-correction` fragment per lane is the convention-legal route and is **routed, not taken here** (this lane's dispatch scoped the two KB sites only, and minting two cross-lane correction fragments unbidden is a scope breach).
- **Honest statement of sweep completeness:** the sweep is complete **for this phrase family**. It would not catch a paraphrase that avoids both words (e.g. *"the observed shift"*, *"spectroscopic z"*), and no third method was run against that class.

#### ITEM 2 — `adopters.py:125` stale `E_YIELD` cite `:471` → `:516` (LANDED, after the fence cleared mid-session)

- **★ THE FENCE FIRED, THEN CLEARED — recorded because the sequence is the receipt, not a formality.** At session start `gh pr view 822` returned **`state: OPEN`** (`mergedAt: null`), and PR #822's file set contains `src/ave/bench/adopters.py` — a **direct file-level collision** with the dispatched edit, on a PR the dispatch's own fence list omitted precisely because it believed #822 already merged. ITEM 2 was accordingly **stopped**. Mid-session, `git fetch` showed `origin/main` had advanced `2fcde4db` → **`583d43dd`**, landing **#822 (`7343b243`, merged `2026-08-03T02:46:21Z`)** together with #834 and #835. **The collision is a merge, not a conflict**, so the branch was rebased onto `583d43dd` (clean; ITEM 1 re-verified additive-only against the new base) and the fence **re-run against the refreshed open set** — #833/#838/#841/#842/#843/#844/#845/#846/#847. **All four of this lane's then-existing files return zero overlap**, `adopters.py` included. ITEM 2 then executed.
- **★ THE FENCE COUNT WAS FOUR; THE PR SHIPS FIVE — the fence ran BEFORE the fifth file existed.** The fence's input set was this lane's file list at the time it ran: `adopters.py`, `domain-catalog.md`, `stellar-regime-classification.md`, and this fragment. The fifth file, [`2026-08-03-paperclip-canonization-correction.md`](2026-08-03-paperclip-canonization-correction.md), was **minted afterwards** by ITEM 3 (below) and therefore could not have been fenced — a **newly created path**, which no open PR can be editing, but the count of "all four" was left standing in this entry after the file set grew. Corrected here rather than left to a reader's arithmetic against `git diff --stat`. The reviewing audit **re-ran the fence at five files against all 13 open PRs and re-confirmed zero overlap**; this repair pass re-ran it a third time against the open set at the time of writing — **#833/#838/#841/#842/#843/#844/#845/#846/#847/#848/#849/#850/#852/#853, 14 PRs, zero overlap on all five paths.**
- **★ #822's R4 FOUND AND READ — it lives in the PR's audit COMMENT, not in the diff.** An earlier pass of this lane reported the dispatch's *"the merged #822 R4 established the mapping `:471`→`:516`"* as unfindable, because `git diff origin/main...src/repoint-pvlas-v3 -- src/ave/bench/adopters.py` contains **no `constants.py` cite change at all** (its only two cite edits are `birefringence.py:320`→`:356` and `:328`→`:375`). **That was a search-surface error, now corrected:** R4 is an audit finding in the #822 review thread, tagged *"frozen-cite disclosure 1 → 4 sites (**disclose-only**)"* — the mappings were **established and disclosed but deliberately not applied**, which is exactly why the diff is silent. Its table has **four** rows, reproduced here in full (byte-checked against the #822 review thread; transposed from the source table's four columns to inline prose, drift in parentheses — the *sentence* that follows the table is quoted verbatim):
  - `adopters.py:185` `birefringence.py:328` → **`:375`** (+47)
  - `adopters.py:197` `constants.py:154` (`ALPHA`) → **`:163`** (+9)
  - `adopters.py:198` `constants.py:465` (`E_CRIT`) → **`:510`** (+45)
  - `adopters.py:199` `constants.py:471` (`E_YIELD`) → **`:516`** (+45)

  all *"inside the `frozen=True` `Prereg` (`:179-201`). **Frozen block byte-untouched.** Symbols are imported live at `:66` ⇒ **no VALUE is stale, only the line numbers.**"* ⚑ **An earlier pass of this entry printed only rows 2–4 under the word "verbatim", dropping row 1** (`:185`, the sole `birefringence.py` row). Row 1 is restored above and its consequence is carried into the scope finding below; the word "verbatim" is narrowed to the clause it actually covers. **The dispatch's premise is confirmed correct; the lane's earlier "premise FALSE" reading is withdrawn** (it was true of the diff, false of the finding).
- **R4 ALSO NAMED THIS EXACT SITE AND ROUTED IT — the dispatch is discharging R4's own routing.** Verbatim from the same audit: *"⚑ **Surfaced, NOT fixed, NOT one of the four:** `adopters.py`:125 (a `LedgerRow` citing *"E_YIELD constants.py:471"*) carries the same stale `:471` but is **outside** the frozen block, so under the rule applied at `:14`/`:221` it *would* be fixable. Missed by the first pass; left untouched rather than absorbed into a docs-only repair that touches no `.py`. **Routed.**"
- **Target re-verified by reading `constants.py`, not by trusting `:516`** (dispatch explicitly required this; re-run again after the rebase onto `583d43dd`, and `#822` is confirmed not to have touched `constants.py`):
  - `constants.py:516` → `E_YIELD: float = V_YIELD / L_NODE  # ≈ 1.13e17 V/m …` ✅ **correct target.**
  - `constants.py:471` → `EE_TO_TOPO_CAPACITANCE: float = 1.0 / (XI_TOPO**2)` — the stale cite resolves to a **different constant entirely**, so this was a wrong-**symbol** cite, not merely a drifted line number.
- **Frozen-block boundary confirmed before editing, exactly as the dispatch specified.** `prereg=Prereg(` opens at `:179`, `frozen=True` at `:181`, block closes at `:201`; `:125` is **well outside**. Post-edit re-check: the `Prereg` span and `frozen=True` line are **unmoved at `:179`/`:181`** (the edit is same-length in-place, inserting no lines).
- **★ ZERO-EXECUTABLE-CHANGE PROVEN, NOT ASSERTED.** `ast.parse` on the pre- and post-edit files, compared via `ast.dump`: the trees differ by **exactly one node** — `Constant(value='… E_YIELD constants.py:471')` → `Constant(value='… E_YIELD constants.py:516')`. No structural node, no call, no signature, no default, no control flow differs. Diff is **1 line changed, 0 added, 0 removed, 1 file**.
- **Test receipts (both required batteries, run at the post-edit branch tip):**
  - Arbiter pin test — `pytest src/tests/test_birefringence_pvlas_arbiter.py -q` ⇒ **`7 passed in 0.24s`**.
  - The #822 lane's own named selector (R5 named the 8 files after the prior bare "164" proved unreproducible) — `test_ave_bench`, `test_bench_adopters`, `test_bench_model`, `test_birefringence_pvlas_arbiter`, `test_birefringence_v3_chain`, `test_grqed_stage1_gr_extension`, `test_grqed_stage2_qed_extension`, `test_grqed_stage3_backreaction` ⇒ **`164 passed, 4 warnings in 84.47s`** — the pass-count and warning-count **byte-match #822 R5's receipt** (*"164 passed, 4 warnings in 84.51s"*), so the edit is measured to move nothing. (R5's shorthand `test_grqed_stage{1,2,3}` expands to the `_gr_extension` / `_qed_extension` / `_backreaction` filenames; the bare stems do not exist.)
- **★ SCOPE FINDING CARRIED FORWARD — `:125` WAS ONE OF FOUR `constants.py` CITES IN THIS FILE (a *different* four from R4's four frozen sites), AND FOUR FROZEN SITES REMAIN STALE BY RULING.**
  - **★ Two four-sets are in play and they are NOT the same set** — an earlier phrasing of this heading (*"`:125` WAS ONE OF FOUR"*) collided with R4's own *"NOT one of the four"* quoted above, which reads as a contradiction until the two sets are named:
    - **R4's four** = the four **frozen sites** {`:185`, `:197`, `:198`, `:199`} — a mixed set spanning both `birefringence.py` and `constants.py` targets. `:125` is explicitly **not** in it (R4: *"NOT one of the four"*), because it is outside the frozen block.
    - **This lane's four** = the four **`constants.py` cites in this file** {`:125`, `:197`, `:198`, `:199`} — the set `git grep -n "constants\.py:" src/ave/bench/adopters.py` returns. `:125` **is** in it, and is the only member outside the frozen block, which is why it is the only one this edit touches.
  - **After this edit the frozen block still carries FOUR stale cites**, all left byte-untouched here per R4's disclose-only disposition: `:185` `birefringence.py:328` (true `:375`; the stale line `:328` is a **blank line inside the docstring of `optical_activity_rotation_deg`**, an unrelated function defined at `:324` — read at `src/ave/bench/birefringence.py`, where `def coefficient_ratio_differential` sits at `:375`), `:197` `constants.py:154` (true `:163` — the stale line is a prose comment, *"nuclear-scale Pd-D externally-driven"*), `:198` `constants.py:465` (true `:510`; stale line is `EE_TO_TOPO_VOLTAGE`), `:199` `constants.py:471` (true `:516`; stale line is `EE_TO_TOPO_CAPACITANCE`). Three of the four (`:185`, `:198`, `:199`) are wrong-**symbol** cites, not merely drifted line numbers; the fourth (`:197`) lands on a prose comment, i.e. on no symbol at all.
  - **TWO consequences to state plainly rather than leave for discovery — the split-cite condition holds at two symbols, not one:**
    1. **`E_YIELD` is now cited at two different line numbers** — `:516` at `:125` (corrected here, outside the block) and `:471` at `:199` (stale, inside it). Opened by this edit.
    2. **`coefficient_ratio_differential` was ALREADY cited at two different line numbers before this lane touched the file** — `birefringence.py:375` at `:232` (correct, outside the block) and `:328` at `:185` (stale, inside it — R4's row 1). Opened by **#822's own diff** (`f898e353`), which repointed the out-of-block `analytic_provenance` cite `:328` → `:375` while R4 left the in-block `prior_work_refs` twin at `:185` deliberately unfixed. **Not introduced here, and not previously stated in this entry** — it follows directly from the R4 row this entry had dropped.
  - Both splits are R4's ruling operating as designed, and **no VALUE is affected in either case** (the `constants.py` symbols import live at `:66`; the `birefringence.py` cite is a `prior_work_refs` documentation string). Whether the frozen block's cite strings should ever be re-pinned is a standing question for the frozen-prereg convention — **flagged, not decided.**

#### ITEM 3 — the K2G frozen-prereg "off-by-one": **VERIFIED NOT TO EXIST. The frozen file is byte-untouched; the DISCLOSURE is what gets corrected.**

- **Dispatch premise, verified rather than trusted — and it returns FALSE.** The dispatch describes *"`…prereg_FROZEN.md`:16 cites `electron-plumbing-primer.md:26` for microrotation, but microrotation sits at `:27`"*, inherited from merged **#832**'s repair-pass disclosure at [`2026-08-02-paperclip-canonization.md`](2026-08-02-paperclip-canonization.md)`:153`. **The prereg does not cite `:26` for microrotation.** It cites `:26` for **stretch**, and gives the microrotation half a *different* cite. The sentence wraps across three lines, putting the cite at the head of `:16` while it grammatically closes the clause ending `:15` — which is how it came to be read as attached to the clause that follows it.
- **The receipt** (`prereg_FROZEN.md`:15-17, verbatim): clause 1 *"stretch ↔ capacitive ↔ E-sector"* → `` (`electron-plumbing-primer.md:26`) ``; clause 2 *"twist/microrotation ↔ couple-stress ↔ inductive/μ-sector"* → `` (`translation-circuit.md:99-104`) ``. Both targets read and both are **exactly on point**: `primer.md:26` is *"**E-field = translational stretch** … capacitive (C) sector"*, and [`manuscript/ave-kb/common/translation-tables/translation-circuit.md`](../../manuscript/ave-kb/common/translation-tables/translation-circuit.md)`:100/:101/:104` carry microrotational→inductive flywheel, couple-stress→mutual-inductance gradient, and bond-twisting→couple-stress respectively. **No off-by-one, nothing to fix.** *(Full path given in this lane's own prose because **three** tracked files share the basename `translation-circuit.md` — `common/translation-tables/`, `vol4/circuit-theory/ch1-vacuum-circuit-analysis/`, `vol2/appendices/app-a-translation-matrix/`. The prereg's bare form is quoted as-written above and **not** corrected; a `FROZEN` file is quoted, not repaired.)*
- **Both dates checked, as the dispatch required** (merged #832 added Step 3.5 to the primer, so a shift was plausible): at the freeze-date blob **`bfa94beb`** (2026-06-13 — the primer's creating commit and the only one at or before the 2026-06-15 freeze) and at `origin/main` **`583d43dd`**, primer `:26` and `:27` are **byte-identical**. #832's insertions land *below* the Step-1 bullet pair. **The correct target is `:26` at the freeze date AND today** — so no re-pin is owed even in principle, and the two answers the dispatch asked for are the same answer.
- **What IS true in #832's disclosure, and is not corrected:** primer `:27` *is* the microrotation bullet and `:26` *is* the E-field/stretch bullet. The sole error is **which clause the prereg attaches `:26` to**.
- **Where the correction landed, and why not where the dispatch guessed.** The dispatch routed the note to *"the K2G arc's non-frozen companion"*. That companion exists ([`2026-06-15_k2g-constitutive-provenance_result.md`](../../research/2026-06-15_k2g-constitutive-provenance_result.md)) but **needs no correction — the K2G arc is not what is wrong.** The mistaken claim lives in another lane's merged docket fragment, and [`README.md`](README.md)`:7` names exactly one legal instrument for that: *"Rule 12 corrections = a dated `-correction` suffix file referencing the original"* — the same shape #830's lane used for #821's fragment. Minted accordingly: [`2026-08-03-paperclip-canonization-correction.md`](2026-08-03-paperclip-canonization-correction.md), which references the original by path **and** by PR #832. **`2026-08-02-paperclip-canonization.md` is byte-untouched**, as are the frozen prereg, the primer, and the K2G result. **Zero ids minted; no grade or ruling moves.** If the orchestrator prefers the correction confined to this fragment, the `-correction` file is a clean single-file drop.
- **Durable lesson (the reason this is worth a fragment at all):** a cite-audit that reads a `file:line` claim without reading the *sentence* around it will mis-attach a trailing parenthetical to the following clause whenever the sentence wraps. The catching check is the one `verify-before-cite` already prescribes: open the target, read what it says, and confirm it matches **the clause the cite actually closes** — not the nearest clause sharing its line. This lane's ITEM 1 and ITEM 2 premises were also both re-verified from source rather than inherited, and **all three dispatched premises needed correction** (ITEM 1 incomplete, ITEM 2 correct-but-unfindable-in-the-diff, ITEM 3 false).
