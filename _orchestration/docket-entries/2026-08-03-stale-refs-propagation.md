### ENTRY 2026-08-03-stale-refs-propagation (2026-08-03): implementer — propagate the merged #830 predicted-vs-measured relabel and the merged #822 R4 cite mapping to their flagged sites; the K2G "off-by-one" returns NOT-A-DEFECT

- **Class: mechanical propagation of already-ruled fixes.** No physics decision, no adjudication, **zero ids minted** (INVARIANT-S11) — no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; no `confidence`, `quality`, `solidity` or any grade field moves. Three dispatched items: **ITEM 1 landed** (shape adapted to a Rule-12 constraint the dispatch did not carry — surfaced, not worked around), **ITEM 2 landed** (after its collision fence fired and then cleared when #822 merged mid-session), **ITEM 3 executed as a correction in the opposite direction** — the dispatched defect was verified **not to exist**, so the mistaken disclosure is what gets corrected. The `FROZEN` prereg was **not touched**; no other lane's docket fragment was edited.
- **★ Base moved mid-session; every finding re-verified against the new base.** Work started at `origin/main` `2fcde4db`; `origin/main` advanced to **`583d43dd`** partway through (merging **#822**, #834, #835). The branch was **rebased clean** onto `583d43dd`, ITEM 1's diff re-checked additive-only against it, both ITEM-1 defect sites re-read at the new base and confirmed **still defective**, the corpus sweep **re-run** at the new base (same 7 hits, unchanged), and the collision fence **re-run** against the refreshed open set (#833/#838/#841/#842/#843/#844/#845/#846/#847 — **zero overlap on all four of this lane's files**). Nothing in this entry rests on the stale base.

#### ITEM 1 — the #830 predicted-vs-measured relabel, propagated to both flagged upstream sites (LANDED)

- **What merged #830 (`4abc407c`) established.** The un-7'd $GM/(c^2R) = 2.59\times10^{-4}$ at the Sirius B parameters ($M = 1.018\,M_\odot$, $R = 5800$ km) is the **GR-PREDICTED** redshift, **not** the measured one. Receipt re-run here rather than restated: $c\,GM/(c^2R) = 77.72$ km/s, exact-Schwarzschild $z$ at the same $M$, $R$ = $77.75$ km/s; the **measured** Sirius B line shift is $80.65 \pm 0.77$ km/s $= 2.69\times10^{-4}$ — $3.7\%$ higher, and $(80.65 - 77.75)/0.77 = \mathbf{3.8\sigma}$ on the quoted uncertainty. Predicted and measured are **not interchangeable at the printed precision**.
- **The two sites #830 flagged and left byte-untouched, both verified still defective at `origin/main` (`2fcde4db`) before editing:**
  1. [`stellar-regime-classification.md`](../../manuscript/ave-kb/vol3/applied-physics/ch07-stellar-interiors/stellar-regime-classification.md)`:26` — verbatim *"$3\times10^{-4}$ matches $GM/(c^2R) = 2.59\times10^{-4}$ **(the measured gravitational redshift its own consequence-column cites)**"*. Line number matches the dispatch; **not** fixed by any intervening merge.
  2. [`domain-catalog.md`](../../manuscript/ave-kb/vol1/operators-and-regimes/ch7-regime-map/domain-catalog.md)`:55` — verbatim *"which is also the **measured gravitational redshift**, the observable its vol3 companion table's *Physical consequence* column cites"*. Line number matches the dispatch; **not** fixed by any intervening merge.
- **★ THE DISPATCH'S SHAPE WAS NOT AVAILABLE — RULE-12 CONSTRAINT SURFACED, NOT WORKED AROUND.** The dispatch said *"apply the same relabel"*, i.e. an in-place edit. **Both sites sit inside Rule-12-PRESERVED blocks whose preservation is asserted, in print, by the merged resolution immediately beneath each one:**
  - `domain-catalog.md`:57 — *"whose text is **preserved unedited per Rule 12**"*
  - `stellar-regime-classification.md`:28 — *"that text is **preserved unedited per Rule 12**"*

  An in-place relabel would have made **both of those merged sentences false**, in the same files, in the same pass. #830's own commit message records the identical reason for stopping — *"`:55` is the Rule-12-preserved 2026-08-01 note this entry's own resolution sits beneath (**editing it would break the preservation this PR relies on**)"* — a half of the rationale the dispatch did not carry. The dispatch's premise (*"#830 left them byte-untouched because they were another lane's text"*) is **incomplete, not wrong**: the other-lane reason is real, the Rule-12 reason is the binding one, and it survives this lane taking ownership of the file set.
- **Shape actually taken: the additive dated disclosed-correction — the same pattern the dispatch itself specifies for a frozen target.** At each site, a `**Correction note (2026-08-03 …)**` block appended **after** the existing resolution (so the audit trail reads forward), which (i) **quotes the defective clause verbatim**, (ii) states the correct label with the full arithmetic receipt above, (iii) states explicitly that the preserved text is left byte-untouched **and why**, (iv) records that the clause's *other* assertion — redshift-not-strain is what the consequence column cites — is correct and unaffected. **Diff is additive-only: 6 insertions, 0 deletions, 2 files** (`git diff --stat`; `git diff -U0 | grep "^-"` returns only the two `--- a/` file headers). The preserved 2026-08-01 text at both `:26` and `:55` is byte-identical to `origin/main`.
- **No physics moves.** The displaced $3\times10^{-4}$ remains the un-7'd potential and remains a different *quantity* from the strain; the row value $1.81\times10^{-3}$, the *Physical consequence* cells, and the `regime_map.py:524` code-site routing are all untouched.

##### ★ The corpus sweep — the inherited site list was SHORT, as expected (two methods, honestly reported)

Two independent methods, run at `origin/main`: **(A)** `git grep -n "measured gravitational redshift"` plus the broader family `-iE "measured[^.]{0,40}redshift|redshift[^.]{0,30}measured"`; **(B)** a Python walk over all **4897** tracked files with `re.compile(r"measured[- ]?(gravitational )?redshift", re.I)`. **Both returned the same 7 hits** — method (A)'s broader family added exactly one extra, `src/scripts/vol_4_engineering/white_dwarf_saturation_redshift.py:16` (*"WD redshift measured to ~0.1%"*), which is a statement about **measurement precision**, not the predicted-vs-measured mislabel, and is **not** a defect. Classification of the 7:

| # | Site | Class | Disposition |
|---|---|---|---|
| 1 | `stellar-regime-classification.md`:26 | **DEFECT** — dispatched | **CORRECTED** (additive note) |
| 2 | `domain-catalog.md`:55 | **DEFECT** — dispatched | **CORRECTED** (additive note) |
| 3 | `docket-entries/2026-08-02-rulings-d8-ell-split.md`:46 | not a defect — #830's own correct *description* of the defect | untouched |
| 4 | `docket-entries/2026-08-02-rulings-d8-ell-split.md`:51 | not a defect — #830's own correct routing note | untouched |
| 5 | `_orchestration/2026-08-02_manuscript-reconciliation-board.md`:340 | **DEFECT — NOT DISPATCHED, FLAGGED NOT FIXED** | see below |
| 6 | `docket-entries/2026-08-01-factor7-and-782-basis.md`:32 | **DEFECT — NOT DISPATCHED, FLAGGED NOT FIXED** | see below |
| 7 | `docket-entries/2026-08-02-mr-astro-singletons.md`:5 | **DEFECT — NOT DISPATCHED, FLAGGED NOT FIXED** | see below |

- **Site 5** — `_orchestration/2026-08-02_manuscript-reconciliation-board.md`:340 carries *"(the measured gravitational redshift its own consequence-column cites)"* **and a second, independent defect the dispatch did not know about: it prints the with-7 value as $1.77\times10^{-3}$**, where every other corpus site prints $1.81\times10^{-3}$ (recomputed here: $7GM/(c^2R) = 1.8148\times10^{-3}$ at $M = 1.018\,M_\odot$, $R = 5800$ km). **Not fixed:** this is the manuscript-reconciliation epic's **live working board**, and #830 already flagged it at its `:51` for exactly this reason (*"editing it would open exactly the cross-lane conflict the fragment convention exists to prevent"*). **Both defects flagged with path + verbatim content; neither silently reconciled.** Routed to the manuscript-reconciliation lane.
- **Sites 6 and 7** — both are **other lanes' merged docket fragments** (#820's own, and the mr-astro-singletons lane's). [`README.md`](README.md):7 is binding: *"One lane, one file. **Never edit another lane's fragment** (Rule 12 corrections = a dated `-correction` suffix file referencing the original)."* Site 6 reads *"which is also the **measured-redshift observable** its own *Physical consequence* column cites"*; site 7 reads *"prints 3e-4 (matches GM/c²R, **the measured redshift**)"*. Both are the same predicted-vs-measured defect. **Flagged, not fixed** — a `-correction` fragment per lane is the convention-legal route and is **routed, not taken here** (this lane's dispatch scoped the two KB sites only, and minting two cross-lane correction fragments unbidden is a scope breach).
- **Honest statement of sweep completeness:** the sweep is complete **for this phrase family**. It would not catch a paraphrase that avoids both words (e.g. *"the observed shift"*, *"spectroscopic z"*), and no third method was run against that class.

#### ITEM 2 — `adopters.py:125` stale `E_YIELD` cite `:471` → `:516` (LANDED, after the fence cleared mid-session)

- **★ THE FENCE FIRED, THEN CLEARED — recorded because the sequence is the receipt, not a formality.** At session start `gh pr view 822` returned **`state: OPEN`** (`mergedAt: null`), and PR #822's file set contains `src/ave/bench/adopters.py` — a **direct file-level collision** with the dispatched edit, on a PR the dispatch's own fence list omitted precisely because it believed #822 already merged. ITEM 2 was accordingly **stopped**. Mid-session, `git fetch` showed `origin/main` had advanced `2fcde4db` → **`583d43dd`**, landing **#822 (`7343b243`, merged `2026-08-03T02:46:21Z`)** together with #834 and #835. **The collision is a merge, not a conflict**, so the branch was rebased onto `583d43dd` (clean; ITEM 1 re-verified additive-only against the new base) and the fence **re-run against the refreshed open set** — #833/#838/#841/#842/#843/#844/#845/#846/#847. **All four of this lane's files return zero overlap**, `adopters.py` included. ITEM 2 then executed.
- **★ #822's R4 FOUND AND READ — it lives in the PR's audit COMMENT, not in the diff.** An earlier pass of this lane reported the dispatch's *"the merged #822 R4 established the mapping `:471`→`:516`"* as unfindable, because `git diff origin/main...src/repoint-pvlas-v3 -- src/ave/bench/adopters.py` contains **no `constants.py` cite change at all** (its only two cite edits are `birefringence.py:320`→`:356` and `:328`→`:375`). **That was a search-surface error, now corrected:** R4 is an audit finding in the #822 review thread, tagged *"frozen-cite disclosure 1 → 4 sites (**disclose-only**)"* — the mappings were **established and disclosed but deliberately not applied**, which is exactly why the diff is silent. Its table, verbatim: `adopters.py:197` `constants.py:154` (`ALPHA`) → **`:163`**; `:198` `constants.py:465` (`E_CRIT`) → **`:510`**; `:199` `constants.py:471` (`E_YIELD`) → **`:516`**; all *"inside the `frozen=True` `Prereg` (`:179-201`). **Frozen block byte-untouched.** Symbols are imported live at `:66` ⇒ **no VALUE is stale, only the line numbers.**"* **The dispatch's premise is confirmed correct; the lane's earlier "premise FALSE" reading is withdrawn** (it was true of the diff, false of the finding).
- **R4 ALSO NAMED THIS EXACT SITE AND ROUTED IT — the dispatch is discharging R4's own routing.** Verbatim from the same audit: *"⚑ **Surfaced, NOT fixed, NOT one of the four:** `adopters.py`:125 (a `LedgerRow` citing *"E_YIELD constants.py:471"*) carries the same stale `:471` but is **outside** the frozen block, so under the rule applied at `:14`/`:221` it *would* be fixable. Missed by the first pass; left untouched rather than absorbed into a docs-only repair that touches no `.py`. **Routed.**"
- **Target re-verified by reading `constants.py`, not by trusting `:516`** (dispatch explicitly required this; re-run again after the rebase onto `583d43dd`, and `#822` is confirmed not to have touched `constants.py`):
  - `constants.py:516` → `E_YIELD: float = V_YIELD / L_NODE  # ≈ 1.13e17 V/m …` ✅ **correct target.**
  - `constants.py:471` → `EE_TO_TOPO_CAPACITANCE: float = 1.0 / (XI_TOPO**2)` — the stale cite resolves to a **different constant entirely**, so this was a wrong-**symbol** cite, not merely a drifted line number.
- **Frozen-block boundary confirmed before editing, exactly as the dispatch specified.** `prereg=Prereg(` opens at `:179`, `frozen=True` at `:181`, block closes at `:201`; `:125` is **well outside**. Post-edit re-check: the `Prereg` span and `frozen=True` line are **unmoved at `:179`/`:181`** (the edit is same-length in-place, inserting no lines).
- **★ ZERO-EXECUTABLE-CHANGE PROVEN, NOT ASSERTED.** `ast.parse` on the pre- and post-edit files, compared via `ast.dump`: the trees differ by **exactly one node** — `Constant(value='… E_YIELD constants.py:471')` → `Constant(value='… E_YIELD constants.py:516')`. No structural node, no call, no signature, no default, no control flow differs. Diff is **1 line changed, 0 added, 0 removed, 1 file**.
- **Test receipts (both required batteries, run at the post-edit branch tip):**
  - Arbiter pin test — `pytest src/tests/test_birefringence_pvlas_arbiter.py -q` ⇒ **`7 passed in 0.24s`**.
  - The #822 lane's own named selector (R5 named the 8 files after the prior bare "164" proved unreproducible) — `test_ave_bench`, `test_bench_adopters`, `test_bench_model`, `test_birefringence_pvlas_arbiter`, `test_birefringence_v3_chain`, `test_grqed_stage1_gr_extension`, `test_grqed_stage2_qed_extension`, `test_grqed_stage3_backreaction` ⇒ **`164 passed, 4 warnings in 84.47s`** — the pass-count and warning-count **byte-match #822 R5's receipt** (*"164 passed, 4 warnings in 84.51s"*), so the edit is measured to move nothing. (R5's shorthand `test_grqed_stage{1,2,3}` expands to the `_gr_extension` / `_qed_extension` / `_backreaction` filenames; the bare stems do not exist.)
- **★ SCOPE FINDING CARRIED FORWARD — `:125` WAS ONE OF FOUR, AND THE OTHER THREE REMAIN STALE BY RULING.** After this edit, `git grep -n "constants\.py:" src/ave/bench/adopters.py` still returns three stale cites, all **inside** the frozen block and all left byte-untouched here per R4's disclose-only disposition: `:197` `constants.py:154` (true `:163` — the stale line is a prose comment, *"nuclear-scale Pd-D externally-driven"*), `:198` `constants.py:465` (true `:510`; stale line is `EE_TO_TOPO_VOLTAGE`), `:199` `constants.py:471` (true `:516`; stale line is `EE_TO_TOPO_CAPACITANCE`). **Consequence to state plainly rather than leave for discovery:** the file now cites the **same symbol `E_YIELD` at two different line numbers** — `:516` at `:125` (corrected, outside the block) and `:471` at `:199` (stale, inside it). That is R4's ruling operating as designed, not a defect introduced here, and **no VALUE is affected** (symbols import live at `:66`). Whether the frozen block's cite strings should ever be re-pinned is a standing question for the frozen-prereg convention — **flagged, not decided.**
