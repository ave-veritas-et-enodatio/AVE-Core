### ENTRY 2026-08-04-mechanical-rulings-batch-executed (2026-08-04): implementer — the mechanical-rulings batch (Phase-2 step 1): R10 canonized, R5 executed, R12/R13/R7/R3 landed, R8 found ALREADY-EXECUTED; ★ six of eight items had NO committed ruling record and the provenance gap is the headline finding

> **★ READ THIS FIRST — the dispatch's Rule Zero could not be satisfied for most of the batch, and that is reported rather than papered over.** The dispatch said *"every ruling's operational content lives in the docket records — read them there … trust NOTHING transcribed in this brief."* **Executed as instructed, that check FAILS for six of the eight items:** no committed `_orchestration/` record carries the ruling text for R3, R4, R5, R7, R8, R12 or R13. What the dockets carry is the **routed question** in each case, usually explicitly marked OPEN / *"Grant's call"* / *"routed, not resolved here"*. Only **R10** has a full ruling chain in committed corpus (docket §Ruling 3(c) as corrected + the merged SVA row 6 + the ringdown-batch caption ruling).
>
> **How each item was therefore handled** — the rule applied was: *execute only where the docket already records the CONSEQUENCE of the ruled arm, so that the ruling supplies the arm and the corpus supplies the content.*
>
> | item | ruling text in a committed docket? | consequence pre-recorded in canon? | disposition |
> |---|---|---|---|
> | **R10** transfer-cost | **YES** (3 independent records) | n/a | **CANONIZED** |
> | **R5/R4** Oort + total-field | no | **YES** — the leaf's own `:30` gate states the total-field consequence verbatim | **EXECUTED** (gate fired) |
> | **R12** acoustic drag | no (routed as *"a physics ruling, not a relabel"*) | **YES** — mechanism identified in the 2026-07-17 audit record | **EXECUTED** |
> | **R13** two-ruler | no | partial — the two rulers and the $10^8$ disagreement are receipted; the per-channel labels are not | **EXECUTED as labels only**, contradiction dissolved, no ruler edited |
> | **R7** walls | no | **YES** — the docket routes exactly one placement question to Grant | **EXECUTED narrowly** (placement only) |
> | **R3** $I_{max}$ | no | **YES for the tier assignments** (both live A4 arms give them, receipted in-row); **NO for the fraction-bar resolution direction** | **SPLIT** — assignments landed, kind-requirement landed, sense NOT picked |
> | **R8** varactor | no | n/a | **NO EDIT — already executed on `main` 2026-08-02/03**; see item 4 |
> | **EFE total-field** | no | partial | **EXECUTED on the KB side**, print flagged wave-held |

- **Class: ruling execution + one canonization. Adjudicates nothing.** Mints **exactly one** id (`clm-xfrcst`, the R10 theorem — the dispatch's only canon-creating item). No other `clm-`/`def-`/`exp-`/`sup-`/`ilk-` minted, retired or re-pointed; **no `confidence` / `solidity` / `quality` / `build_status` / `real_or_fitted` field moves anywhere except the one new card's own authored `confidence: 0.60`** (derived `solidity: 0.55` written by `refresh-kb-metadata`, not by hand).
- **Line-pin hygiene, applied per file before every insert.** Incoming `filename:NN` pins were greppped repo-wide and every insertion was placed **below the highest** pin, or end-appended: `common/index.md` (highest incoming `:78` → appended at `:80`), `common/claim-quality.md` (`:1541` → end), `retention-transition-split.md` (`:61` → end), `substrate-native-terminology.md` (`:65` → end), `theorem-thesaurus.md` (`:293` → after the row at `:309`), `network-analytics.md` (`:16` → in-place, no line added above it), `ligo-gw-saturation-ratio.md` (`:15` → appended below), `einstein-field-equation.md` (`:92` → end), `oort-cloud-saturation-boundary.md` (`:22`, `:30` → inserted below both).
- **Fences honoured.** `vol3 ch08/ch15` print `.tex`, `backmatter/07` and every ringdown-wave-held site are **byte-untouched** (verified in the battery). `bulk-impedance-at-saturation-boundary.md` and `saturating-modulus-and-backreaction.md` (PR #871's files) are **byte-untouched** — the R10 leaf required no cross-ref into either. `predictions.yaml` **byte-untouched** (doc lane's).

---

## Item 8 — R10: the transfer-cost theorem CANONIZED (the only canon-creating item)

- **Ruling chain, three committed records, all verified at execution (not transcribed from the dispatch):**
  1. `_orchestration/docket-entries/2026-08-03-rulings-mr-batch.md` §Ruling 3(c) — Grant verbatim *"there's always a cost when transferring energy between regimes/states/channels right? seems like a theorem or law territory"*, plus the review-finding-7 correction of *within-CHANNEL* → **within-SYSTEM** and the four open edges (i)–(iv).
  2. `_orchestration/docket-entries/2026-08-04-rulings-ringdown-wave-batch.md` item 6 — *"R10 transfer-cost canonization rides the mechanical-rulings batch"* (the resequencing), and item 3 — the ruled caption wording *"radiated power DELIVERED to the far-field port, not 'dissipated'"*.
  3. **`manuscript/ave-kb/common/standard-vacuum-analysis.md` §1 row 6 (MERGED, PR #872, 2026-08-04)** — the ruled statement **already in canon**, including the three delivery modes by name. ★ **The leaf quotes this row rather than paraphrasing it**, so the canonical statement has exactly one wording in the corpus.
- **Site:** `manuscript/ave-kb/common/transfer-cost-theorem.md` (NEW leaf), `clm-xfrcst` in `common/claim-quality.md`, one row in `common/index.md`, cross-refs in `standard-vacuum-analysis.md` (row 6 Home pointer), `retention-transition-split.md`, `substrate-native-terminology.md`.
- **Edit form:** new leaf + one minted card + three additive cross-refs. **No cited leaf's body, row or grade was edited.**
- **The three delivery modes, mapped rather than invented:** far-field radiation / matter detector / topology change ↔ the three ruled loss channels at `substrate-native-terminology.md:33` (radiative port / boundary-Joule extraction / Regime-IV rupture), verified verbatim at that line.
- **R_rad-as-delivery resolves the docket's open edge (iv)** — the edge said the candidate's *"counting done by the detector"* gloss was *"a reframe, not an instance"* because `retention-transition-split.md:60` books the SYSTEM-loss row with a genuine `Re(Z)`. Ruled reading landed: **a real port at the boundary is a DELIVERY terminal; delivery is not dissipation**; the *"never paid to a resistor"* clause is exact as *"no resistor INSIDE the system."* Receipted by the two merged records above, **not** by this lane's judgment.
- **The Op3 negative control is load-bearing and is stated as such** (RULING 21, `retention-transition-split.md:47`; `k4_tlm.py:396-398` unitary): a channel crossing with no system crossing and correspondingly no arrow. It is also the only argument on the leaf that the theorem is not a tautology — recorded as an argument, not a proof.
- **Three edges left OPEN on the card's non-claims:** definition-vs-claim (the tautology edge), the near-yield fork (out of scope, not banked), and altitude-vs-PRODUCT/TRANSITION (the honest null).
- **Verified how:** every quoted line read at execution tip by line-addressed read (`substrate-native-terminology.md:33`, `retention-transition-split.md:47/:57/:59/:60/:61`, `f6-tier1-two-reservoir-ledger_CHARTER.md:256`, SVA §1 row 6 / §2); `clm-xfrcst` uniqueness by repo-wide grep (0 pre-existing hits); `refresh-kb-metadata` wrote the derived solidity and leaf-references footer; `make verify` PASSED; `verify-md-links` gating 0.

## Item 2 — R5/R4: the Oort containment dissolution under the ruled TOTAL-field keying

- **What made this executable without adjudicating.** The consequence of the total-field arm is **already written into the canon this ruling lands on**, verbatim at `oort-cloud-saturation-boundary.md:30`: *"**Under total-local-field keying the Sun already sits at `g_ext ≈ 1.8–2.0 a₀`, so no saturation transition exists anywhere in the solar system** and `r_sat` names nothing physical."* The ruling supplies the arm; the leaf supplies the content. **No new physics was decided in this lane.**
- **Numbers re-derived from the canonical chain at execution, not transcribed:** `A0_LATTICE = 1.0719120949508851e-10` m/s²; `√(GM_☉/a₀) = 7438.906056841474` AU (via `ave.gravity.solar_impedance.saturation_radius_au`); ambient ratios `2.1e-10/1.0719e-10 = 1.959` and `2.1e-10/1.2e-10 = 1.750` — **both > 1**, so the dissolution does not depend on which `a₀` normalization is used. That is the same both-ways receipt the 2026-08-03 repair pass established.
- **Sites (strike-don't-delete, dated headers, body byte-preserved at every one):**
  - `manuscript/ave-kb/vol3/cosmology/ch06-solar-system/oort-cloud-saturation-boundary.md` — dated 🔴 block inserted **below** the existing header and **above** the preserved body (below both incoming pins `:22`/`:30`).
  - `manuscript/vol_3_macroscopic/chapters/06_solar_system.tex` — rendered dated paragraph in the same position (print/KB lockstep), plus the figure caption's gate clause updated. **Rendered, not `%`-only, per the doc-lane R7 ruling** (*"rendered current-status notes stay in print; verbatim prior-wording `%` duplication drops"*).
  - `src/ave/gravity/solar_impedance.py` — module docstring key-prediction #2, the section banner, `saturation_radius_au()`'s gate paragraph (prior status preserved beneath the new one per Rule 12), `solar_axiom4_onset_summary()`'s docstring, and the returned `"prediction"` string. **Arithmetic, dict keys and test pins unchanged** — `src/tests/test_solar_impedance.py` 26 passed, including the `"coincide"`-absence and Hills-key-absence assertions.
  - Index rows: `vol3/index.md:46`, `vol3/cosmology/index.md:24`, `vol3/cosmology/ch06-solar-system/index.md:31` — all three were still presenting the object as a Key Result. Edited **in place, one line each**, so no downstream `:NN` offset moves.
- **BOTH Oort numbers retire as boundaries** — Vol 3's `7,438.9 AU` and Vol 4's `~15,000 AU` *"Oort Cloud transition zone"*. **Consequence recorded honestly: the cross-volume "one object or two" question (Q2) is MOOT, not answered** — there is no object for the two numbers to be one or two of.
- **What survives is strictly less than a boundary:** `√(GM_☉/a₀)` is retained as an **internal-field counterfactual isocline** (the solar monopole taken in isolation), explicitly not quotable as a location. The section title's *"(Internal-Field Keying)"* parenthetical is deliberately kept — it now records which keying the surviving arithmetic assumes.
- **⚑ OWED, NOT EXECUTED (outside the dispatch's named scope, flagged per flag-don't-fix): the Vol-4 side.** `vol_4_engineering/chapters/11_experimental_falsification.tex:370`, `vol4/claim-quality.md:1123`/`:1128`/`:1164`/`:1166`, `vol4/falsification/ch11-experimental-bench-falsification/boundary-trapping-test.md:25`, `advanced-protocols.md:24`, and the illustrative driver `src/scripts/vol_3_macroscopic/simulate_oort_cloud_trap.py:76` (hand-centered Gaussian at `10^4.2 ≈ 15,800 AU`). Four of those are **claim-card** lines, so the propagation is grade-adjacent and belongs to a lane with that mandate.
- **⚑ ALSO FLAGGED, NOT EDITED — two chapter-level Vol-3 prose carriers that predate this ruling and survived the 2026-08-03 retraction untouched:** `06_solar_system.tex:10` (chapter objective — *"Map solar system boundaries (heliopause, Oort cloud) to macroscopic acoustic impedance limits"*) and `:367` (chapter summary — *"Solar system boundaries, including the heliopause and the Oort cloud, map **precisely** to macroscopic acoustic impedance limits"*). The summary's *"precisely"* is now false twice over. Surfaced rather than swept in with a dissolution note.

## Item 5 — R12: the acoustic-drag relabel, with the mechanism IDENTIFIED (not chosen)

- **Sites:** `manuscript/ave-kb/vol6/framework/computational-mass-defect/network-analytics.md` (the sentence at `:16`, edited **in place with no line added above it**, so both incoming pins keep their offset) + the print twin `manuscript/vol_6_periodic_table/chapters/01_computational.tex` (the `:75`-area sentence, the site the docket names).
- **Edit form:** in-place sentence replacement + a **rendered** current-status note. **No `%`-duplication of the prior wording** — per the doc-lane **R7 (A3)** ruling (*"rendered current-status notes stay in print; verbatim prior-wording `%` duplication drops (git is the trail)"*), which is the ruled Rule-12 shape for prose repairs.
- **The mechanism, and where it came from.** *"acoustic drag (vacuum friction)"* → **boundary/aperture radiative delivery** — energy leaving the defect *through its geometric perimeter* into the surrounding lattice, an **Ax3-legal port**, not a bulk friction. **Identified, not invented:** the 2026-07-17 Regime-IV dissipation audit graded this exact leaf **RADIATIVE-PORT**, `requires_R` = **port-only**, and its recommendation is verbatim *"relabel the loss term as boundary/aperture radiative loading to match the computation"* (`research/2026-07-17_regime-iv-dissipation-audit_items.json`). Under the R10 theorem this is **delivery mode 1**, and delivery is not dissipation.
- **★ The audit's own counter-caveat is carried, not dropped.** The tabulated $Q$ is $U_{stored}/R_{eff}$ — **no dissipated-power term at all** — so the same evidence that refutes bulk friction also declines to certify a *computed* port: the port is named in prose. Both sites now say the $Q$ column ranks **confinement/compactness** (He-4 19.19 tight / Li-7 2.85 leaky) and must not be read as $\omega U/P$. Shipping the relabel without this would have upgraded an AMBIGUOUS audit verdict to a clean one.
- **Vacated cites (recorded, nothing rewritten) — per the 2026-08-04 ratified vacated-cite pattern.** Two sites quote the pre-relabel sentence at `network-analytics.md:16`: the frozen audit artifact `research/2026-07-17_regime-iv-dissipation-audit_items.json` and the board row `_orchestration/2026-08-02_manuscript-reconciliation-board.md:590`. Both now describe a **superseded state**. Frozen text gets a dated surface-note, never a rewrite — **neither is edited here**.
- **⚑ SIBLING SITES FLAGGED, NOT SWEPT (outside the docket's named scope; the neon precedent — fix the named pair, flag the rest):** `05_lithium.tex:13` + `vol6/period-2/lithium/structure-isotope-stability.md:10` (*"polarizes the knot's **acoustic drag** perimeter"*), `10_oxygen.tex:29` + `vol6/period-2/oxygen/vacuum-density-flux.md:14` (*"minimizing its localized **acoustic drag**"*), and the code comment `src/tests/test_q_factor_s_params.py:37` (*"'dissipation' is the geometric strain radiation (acoustic drag)"* — already the least-wrong of the five). The corpus is now internally split on this phrase; the sweep is owed.

## Item 6 — R13: the two-ruler DISSOLUTION (and the labels flagged, not picked)

- **Sites (KB only):** `manuscript/ave-kb/vol3/gravity/ch08-gravitational-waves/ligo-gw-saturation-ratio.md` (appended below the `:15`/`:20` pins) and `manuscript/ave-kb/vol3/gravity/ch02-general-relativity/einstein-field-equation.md` (appended at end, below the `:92` pin). **⚑ The vol3 ch08/ch15 PRINT twins carry the same two rulers and are WAVE-HELD — byte-untouched here.**
- **What was dissolved.** Routed follow-on **#8** of the 2026-08-03 batch booked a **corpus-wide debt**: the EMF ruler ($V_{GW}/V_{snap}$, saturation at $h=10^{7}$) and the strain ruler ($h/h_{yield}$, yield at $h=\sqrt\alpha$) place the yield point **$1.17\times10^{8}$ apart**, and the regime table's near-merger row is simultaneously *past yield* on one and $10^{-8}$ *of saturation* on the other. **RULED: they are PER-CHANNEL rulers, each against its own channel's wall** — a voltage-over-voltage-wall ratio and a strain-over-strain-wall ratio are different quantity **kinds**, so the $10^8$ is not a contradiction and needs no reconciliation. **Neither ruler is edited, deprecated or rescaled; no number moves; the Regime column is left exactly as printed.**
- **The duty the dissolution creates, landed at both sites:** *"Regime I" is not a property of a source; it is a property of a source-on-a-channel* — a regime verdict must name which ruler it is a verdict on. This is where the near-merger row's two verdicts stop contradicting: both true, of different channels.
- **★ STOPPED-AND-FLAGGED — the channel LABELS.** The dispatch transcribes *"$V_{GW}/V_{snap}$ = T2-electric channel vs its wall; $h/h_{yield}$ = shear channel vs its wall."* **That is not obviously consistent with a ratified line already in print**, quoted verbatim at both sites: `manuscript/vol_4_engineering/chapters/01_vacuum_circuit_analysis.tex` §*"Sector re-keying correction"* (2026-08-02; `def-vyvsn1` grade-fork **RESOLVED = T2**, Grant 2026-06-30) — *"$V_{yield} = \sqrt{\alpha}\,V_{snap} \approx 43.65$~kV is the **transverse Cosserat ($T_2$) self-trap wall** … **not** the A1 compliance bound. The longitudinal-A1 compliance diverges at the higher $V_{snap} = m_e c^2/e \approx 511$~kV."* Canon attaches **$V_{yield}$** to $T_2$ and uses **$V_{snap}$** as the **A1** compliance normalizer; the staged ch15 text derives $h_{yield}=\sqrt\alpha$ from *"the same Axiom 4 saturation physics that defines $V_{yield}$"*. **Two readings survive** — (a) canon's A1 statement is scoped to *which voltage normalizes the compliance kernel* rather than to sector ownership, so the dispatched labels hold; (b) the labels are crossed. **NEITHER PICKED.** The dissolution is landed in **channel-agnostic** form (it turns on quantity KIND, not on sector ownership) and holds under both readings. → **auditor / Grant**, with both quotes.

## Item 7 — R4 total-field: the EFE regime-table propagation

- **Site:** the regime table's leaf, `einstein-field-equation.md` (same appended block as item 6, part (2)).
- **Landed as a SCOPE CAVEAT, not a re-classification.** The kernel argument $A$ is the **total local amplitude (internal + external)**, so every entry in the table is computed from **the source's own field alone** and is therefore a **lower bound** on the total local amplitude; each regime label is conditional on an ambient contribution the table does not check.
- **⚑ EXPLICITLY NOT COMPUTED (routed).** The same ruling dissolved the solar boundary *because* the ambient term dominated there. Whether an ambient term likewise dominates for a laboratory GW detector is **a computation nobody has run**, and re-classifying rows on an un-run computation would be fiat. **No row re-classified.** This is the honest stopping point for a mechanical lane; the computation is a real routed follow-on.

## Item 3 — R7: the walls confirmation (narrow)

- **Sites:** `manuscript/ave-kb/common/wall-taxonomy.md` §1.2 (dated 🟢 block; the pre-existing bullets **preserved unedited**), the leaf's two status lines (`:26` header bullet, §9 closing bullet), and the `common/index.md` row marker.
- **What is answered:** exactly ONE of the leaf's three routed questions — *"was 'Should the wall table go into KB?' a GO?"* — **YES**. Grant verbatim `[sic]`: ***"I like our walls"***. The `⚑ PLACEMENT PENDING GRANT` marker retires.
- **What is NOT answered, said at every site:** *"one lattice cutoff or four?"* (§5 row 4) and *"should cold below-cutoff terminations be in a table called walls at all?"* (§2.2) stay **OPEN**. **Nothing is upgraded** — every walk-level label in §§1.2–4, §6–§9 keeps it; the §1.3 attribution split stands; zero rows, anchors, flags, grades or ids move. **A confirmed home is not a ratified content**, and the note says so.
- **Provenance flag:** the confirmation was relayed by dispatch; **no committed docket record carries it.** Flagged on the leaf itself — the same discipline §1.2 applies to the original sentence.

## Item 1 — R3: $I_{max}$ tier assignments + the fraction-bar requirement (A4 untouched)

- **Sites:** `manuscript/ave-kb/common/theorem-thesaurus.md` §6 (dated block inserted after the $I_{max}$ row at `:309`, **below** the highest incoming pin `:293`) and `node-up-small-large-signal.md` (end-appended cross-ref, below the highest incoming pin `:370`).
- **(a) TIER ASSIGNMENTS ADOPTED:** constitutive tier (FPB slew rating) = **sense 1**, $115.870$ A; particle tier (relativistic inductor / TKI Row 2 at $v=c$) = **sense 2**, $124.384$ A. **A quoted $I_{max}$ must now name its tier.**
- **★ Why this adjudicates nothing about A4** — and the argument is the row's own: **both live A4 arms give identical per-tier assignments** (two-kernels by construction; one-cell-two-tiers because the tiers differ), and only the **excluded** one-tier arm conflicts — it asserts $115.870 = 124.384$ and dies to arithmetic. **Arm-independent ⇒ A4 stays OPEN**, exactly as `research/2026-07-10_operator-typing-pass_result.md:112` has it. Corroborated on `main` by [`operators.md`](../../manuscript/ave-kb/common/operators.md)`:145`, which already carries *"**Particle-tier costume:** the relativistic inductor … — **pending A4**."*
- **(b) THE FRACTION-BAR ITEM — SPLIT, and the split is the finding.** The **requirement** is landed: $A_I$'s numerator and denominator must be ceilings of the **same kind**, and both kinds must be declared. **The SENSE is NOT picked.** ★ Recorded and deliberately not converted into a ruling: `operators.md:145` states sense 1 as a **slew** rating whose own argument is a **rate** (*"L keyed on $V$ = flux-rate / circulation $A_I=\dot E/(E_c\omega_0)$"*), while sense 2 is a convection **amplitude** — so kind-matching is **not neutral** between the senses. **Picking the denominator by that kind argument would be an A4 ruling by fiat**, which is precisely what the naming row exists to prevent. → **A4's owner**, with the requirement attached. *(The row's standing hedge is unchanged: the denominator's identity and the open per-cell circulation→$I_{max}$ normalization are probably one question.)*
- **(c) CITE REPAIR (mechanical; the row instructed it).** **PR #859 has MERGED**, so the row's `UNMERGED-CLASS` paragraph is **DISCHARGED** and its prediction is verified at this tip: `node-up-small-large-signal.md` `:124` → **`:136`**, `:134`–`:138` → **`:146`–`:150`**; quoted phrases byte-unchanged.
- **Values re-derived at execution (canonical chain, nothing transcribed):** `V_YIELD/Z_0 = 115.87029311022246` A · `XI_TOPO*C_0 = 124.3840330668883` A (bit-identical to `ave.core.fdtd_3d.I_MAX_MU`) · ratio `1.0734764686283056` vs `4π√α = 1.0734764685582188`. **No number moves anywhere.**

## Item 4 — R8: the varactor re-key — NO EDIT. It is ALREADY EXECUTED on `main`, and the flag it names has no record

- **Finding, verified by content (the docket's own line-cites are stale — `:720`/`:756` now hold Γ=−1 / Pauli prose):** the re-key the item asks for **already landed on `main`** in two rendered notes inside `manuscript/vol_4_engineering/chapters/01_vacuum_circuit_analysis.tex`:
  - §*"The Vacuum Varactor"* — **Sector re-keying correction (2026-08-02)**, `def-vyvsn1` grade-fork **RESOLVED = T2** (Grant 2026-06-30): the diverging $C_0/S$ is the **longitudinal-A1** bond compliance, $V_{yield}$ is the **transverse-$T_2$** self-trap wall, and every row is read with $V_{yield}\to V_{snap}$. Scope: *"this note covers this subsection only."*
  - §IMD — **Sector re-keying correction (2026-08-03)**, the un-propagated remainder, including the one absolute value that must be carried: $V_{IP3}=\sqrt{4/3}\,V_{snap}$.
- **Values re-derived from the canonical chain before reporting (as dispatched):** `V_SNAP = 510998.9499961642` V · `V_YIELD = 43651.851844321354` V (`= √α·V_SNAP`, reproduced exactly) · `√(4/3)·V_SNAP = 590050.762671803` V → the printed **590,051 V** is that number rounded to the volt ✓ · `√(4/3)·V_YIELD = 50404.81682588919` V → the printed **50.4 kV** cross-check ✓ · `1/√α = 11.70623761435312` → the printed **11.70624** ✓. **The KB twin `intermodulation-distortion.md` carries the same value substitution at its `:67`/`:74`.** Nothing needed writing anywhere.
- **★ STOPPED-AND-FLAGGED — "the T2-override flag, exactly as the docket records it" does not exist in any docket.** The phrase `T2-override` returns **zero hits** repo-wide. What the docket actually records (`2026-08-02-mr-varactor-keying-correction.md`) is a **question back to the core session**: *"Core session: confirm whether the `:720`/`:756` request is now narrowed (mechanical `V_snap` re-key per the ratified line) or still open."* If R8 is that confirmation, it is **already discharged by the two merged notes** and this lane has nothing to write. If R8 additionally rules a *new* flag, **its content is not recoverable from the corpus** and inventing it would be adjudication.
- **The one site that genuinely remains is NOT mechanical.** `manuscript/vol_4_engineering/chapters/15_autoresonant_breakdown_spice.tex:34` still prints *"$\sim 43.65$ kV point-yield, or the $60$ kV bulk-avalanche limit"*, and its KB twin `vol4/simulation/ch15-autoresonant-breakdown/theory.md` carries a standing **⛔ INVALIDATED — RECOMPUTATION REQUIRED** banner. The docket's own held-back list says the same. **A re-key here would launder a number the corpus has already declared invalid** — it needs the recomputation, not a relabel. **Left untouched; routed.**
- **⚑ Adjacent, surfaced not swept:** other `V_{yield}`-keyed sites in the same chapter sit outside both notes' declared scopes — the kernel statement at `:244`, and the drag/slipstream + relaxation-time + skin-depth blocks (`:349`–`:356`, `:384`, `:419`–`:424`). Whether those are the same defect or a different (A1-yield) context is a **sector question**, not a transcription one. Not touched.

---

## Battery

- **`make verify` — exit 0, run before EVERY commit (4 runs + a final tip run).** Final: `[Verify] ALL PHYSICS PROTOCOLS PASSED.`
- **`make verify-md-links` — gating errors 0** (`warn-only 209`, `broken inter 42`, inter-repo mode warn); `kbleaf: 1234 cites checked, gating 0, waived 1`. ★ One gating error appeared mid-batch — the R5 leaf's forward link to *this* fragment before it existed — and was cleared by writing the fragment, not by deleting the link.
- **`make refresh-kb-metadata` — idempotent** (second and third runs: `0 file(s)` written, `6 unchanged`, clean tree). The new card's derived fields were written **by the tool, not by hand**: `solidity: 0.55 [= min(0.60, 0.55)]` (capped by `clm-acdc07`) and the `Leaf references` footer.
- **`verify-docket-keys` — 127 entries / 125 unique keys**, no new duplicate keys; grandfathered numeric dups `22`/`32` unchanged. This branch mints **one** key: `2026-08-04-mechanical-rulings-batch-executed`.
- **Engine tests where the batch touched `src/`:** `src/tests/test_solar_impedance.py` — **26 passed**, including the `"coincide"`-absence and Hills-key-absence assertions and the `r_sat` chain pin (`|r − 7438.9| < 0.1` AU). **No engine behaviour changed anywhere in the batch** — the only `src/` diff is docstrings/comments plus one returned status string.
- **Id churn: exactly ONE mint, as dispatched.** `clm-xfrcst`. No other `clm-`/`def-`/`exp-`/`sup-`/`ilk-` minted, retired or re-pointed; no `confidence`/`solidity`/`quality`/`build_status`/`status`/`real_or_fitted` field moves outside that one new card.
- **Fence check:** `git diff --name-only` contains **no** `vol_3_macroscopic/chapters/08_*`, **no** `vol_3_macroscopic/chapters/15_*`, **no** `manuscript/backmatter/07*`, **no** `predictions.yaml`, **no** `bulk-impedance-at-saturation-boundary.md`, **no** `saturating-modulus-and-backreaction.md`.
- **Pure-corpus scan** over the full branch diff plus this fragment: **0 hits** (case-insensitive `grep -E` over the combined diff for the fenced external-context pattern set).

## Routed follow-ons opened by this branch (none taken here)

1. **★ THE PROVENANCE GAP — six of eight items had no committed ruling record.** R3, R4, R5, R7, R8, R12, R13. Every execution above states, at its own site, that the ruling was relayed by dispatch and its docket record is **OWED**. **The orchestrator lands those records**; this lane cannot write them without becoming the source of its own authority.
2. **The Vol-4 side of the Oort dissolution** — 6 sites, 4 of them claim-card lines (item 2).
3. **`06_solar_system.tex:10` / `:367`** — chapter objective + summary still assert the boundary mapping *"precisely"* (item 2).
4. **The `acoustic drag` sibling sweep** — 5 remaining sites, corpus now internally split (item 5).
5. **★ The R13 channel-label conflict** — dispatched labels vs the ratified `def-vyvsn1` line; both verbatim on the leaf (item 6).
6. **The ambient-vs-source total-field computation for lab GW detectors** — un-run; no row re-classified until it exists (item 7).
7. **A4 with the kind requirement attached** — the fraction-bar sense stays unpicked (item 1).
8. **The ch15 autoresonant recomputation** — the last varactor site, invalid-not-mislabelled (item 4).
9. **The SVA leaf has no `common/index.md` row** (noticed while appending the transfer-cost row) — every other `common/` leaf has one. Cosmetic, but the index is a bootstrap surface.

> **⚑ Disclosed housekeeping.** `assets/sim_outputs/visualize_topological_bounds.png` is regenerated as a side effect of `make verify` (a gate driver re-plots it, and the bytes differ run-to-run). It was swept into this branch's first commit by `git add -A` and has been **reverted to `origin/main`** so the branch diff carries no unrelated binary. **The same artifact rode the immediately-preceding merged commit** (`8e3f4761`, the SVA leaf) — i.e. this is a standing trap for every lane that runs the gate before staging, not a one-off. Surfaced rather than silently reverted.
