### ENTRY 2026-08-02-coldq-pole (2026-08-02): implementer — the cold-Q pole derivation (zero-free-input spin-2 ringdown eigenvalue): SOLVER-NOT-CERTIFIED, all four physics bins report N/A, and the failures are properties of the frozen INSTRUMENT

- **Provenance + scope fence.** Grant's GO 2026-08-02, verbatim `[sic]`: *"6, GO"*, after the framing-challenge walk merged as PR #814. Upstream: [`research/2026-07-30_qlaw-derivation_scoping.md`](../../research/2026-07-30_qlaw-derivation_scoping.md) (#808) and [`research/2026-07-31_qlaw-framing-challenge_walk.md`](../../research/2026-07-31_qlaw-framing-challenge_walk.md) (#814). Written against `origin/main` = `ac165cf2`. **FROZEN-FIRST honoured: the pre-registration was committed ALONE and pushed before a single line of driver code existed** (commit `db29d5a3`). Deliverables: [`research/2026-08-02_coldq-pole-derivation_prereg-FROZEN.md`](../../research/2026-08-02_coldq-pole-derivation_prereg-FROZEN.md), [`research/2026-08-02_coldq-pole-derivation_result.md`](../../research/2026-08-02_coldq-pole-derivation_result.md), [`research/drivers/coldq_pole_derivation.py`](../../research/drivers/coldq_pole_derivation.py) + its results JSON + a gating number-check. **Engine `src/ave` BYTE-UNTOUCHED; zero `manuscript/` or `manuscript/ave-kb/` edits; no `clm-`/`def-` minted; no solidity changed; no falsification-ledger edit; no `translation-circuit.md` row landed — regardless of outcome, exactly as fenced.**
- **★ THE HEADLINE — `SOLVER-NOT-CERTIFIED`, and the frozen precedence therefore reports ALL FOUR physics bins as `N/A — not adjudicated`.** Four of nine gates failed at their frozen tolerances (**G4** `1.2377e-04` vs `1e-8`; **G5** `1.9488e-05` vs `1e-8`; **G7** winding `28` vs `34` located; **G8** `Q` spread `1.1058e-08` vs `1e-9`) and one of six self-tests did not fire (**FT-5**). The prereg's frozen precedence is `BIN-F-SOLVER` > `BIN-F-PROFILE` > `BIN-F-NOPOLE` > `BIN-1/2/3/4` with the instruction that later bins are reported as `N/A — not adjudicated` **and no verdict language is used about them**. That is what the result doc does. **No frozen criterion was dropped, widened or re-defined to convert a FAIL into a PASS — the tolerances are reported exactly as frozen.**
- **★ TWO NAMED MECHANISMS explain all five failures, and both are properties of the frozen INSTRUMENT (the prereg §4.2 real-`sigma` far-field method) — not of the physics, and NOT of the controls, which did their job by firing.** **M1** is the asymptotic-series truncation floor, which FALLS as `R_match` rises; **M2** is the `exp(2|Im omega| R_match)` conditioning, which RISES as `R_match` rises. They run in opposite directions, which is why no single frozen `R_match` set can satisfy both. **Per-gate: G4/G5 are M1; G7/FT-5 are M2; G8's residual spread is the non-cancelling remainder of M1.** **A THIRD, INDEPENDENT contamination source was found by the PR #845 audit AFTER this entry first landed** — the same asymptotic recursion diverges at the rectangle's LEFT edge (`omega_R M_g` = `0.02`) at ANY `|omega_I|`: `|c_20|/R^20` = `5.06e+07` there at `R_match` = `40`, and moving the contour's left edge to `0.20` moves the winding from `3.0000` to `0.0000` (audit receipts, external to the shipped JSON). **It changes no gate verdict — every gate already failed — but it changes what a successor must fix.** Result doc §2.4. (M1) **The far-field matching is an ASYMPTOTIC (divergent) series**, whose error is minimised near an `R`-dependent optimal truncation and grows on BOTH sides; the shipped `instrument_accuracy_map` measures a floor of `6.5814e-05` at `R_match = 25` (diverging to `23`% wrong by `N = 36`), `4.3437e-07` at `R = 40`, and `1.2588e-08` at `R = 60`. G4's frozen set contains `R = 25`, whose floor is four orders above `1e-8`; G5's contains `N = 12`, far from optimal truncation at `R = 40`. **The controls were written for a CONVERGENT expansion, where "more terms is better" and "any R in the set is equivalent" — both false for an asymptotic one.** (M2) **The subdominant ingoing-coefficient extraction is exponentially ill-conditioned as `exp(2|Im omega| R_match)`**, so the frozen rectangle's `|omega_I| M_g` reach of `1.00` at `R = 40` (`exp(80)`) is pure roundoff. **G3 independently proves the integrator is NOT the limit** (`n_steps` 16000→64000 moves the pole by `3.5006e-10`; a separate 8000→128000 sweep agrees to `1e-11`).
- **★ FT-5 not firing is the most informative line in the battery, and it is a self-test working exactly as designed.** On the zero-grade problem the traction-free condition reduces in closed form to a polynomial of degree exactly `ell+1`, so the root count in the test box is known without computing a root: `1`/`1`/`2` for `ell = 1,2,3`. The measured winding is `15.000` for **all three**. **What `15` identifies is a phase RATE, not a fixed signature (PR #845 audit R4):** on the deep edge the objective's phase advances at `d(arg N)/d(omega_R)` ≈ `2*R_match − a` (better than `4` percent across `R_match` in `{6, 8, 10, 12}`), and the COUNT is `rate * Delta(omega_R)/(2*pi)` — it equalled `2*R_match − a` only because the shipped box has `Delta(omega_R)` ≈ `2*pi`. Audit receipts: widening the box at `R_match` = `8` walks the winding through `8`, `12`, `15`, `20`; at `R_match` = `6` the winding is `10`, not `11`. **A successor must not treat `15` as a fixed signature.** The physical content is unchanged: **on the noise-dominated stretch of the contour the objective's phase tracks the cancelled large terms, not the physical ingoing amplitude.** The pole-free control box returned `-2.5621e-16` (correct). G7's failure is the same fact from the other side. **A fireability self-test detected that its target gate's instrument is untrustworthy; that is precisely what it exists for.**
- **★ THE SPIN-2 PREREQUISITE (#814 R7) IS DISCHARGED *AND MEASURED*, not merely asserted.** The radial system is **derived** for the **toroidal (odd-parity) branch**, which is exactly divergence-free — so the bulk/Lamé modulus drops out **identically** rather than by assumption, and there is no compressional partner for linear P↔SV conversion at a spherically symmetric interface (a structural answer to #814's A2-open-half / CF-15 **for this branch only**; the spheroidal branch is fenced as an owed follow-on). Receipts: **G1** — the derived far-field reproduces the exact spherical Hankel log-derivative to `1.7688e-14` over 27 points (so the radial functions DO coincide, as #814's F10 caution says they must); **G2** — the spin-2 `(l-1)(l+2)` stored-energy weighting reproduces the shot closed-cavity eigenvalue to `4.9220e-13` via an independent Rayleigh quotient; **FT-6** (ADDED at implementation time as a *strengthening*, never a relaxation) — substituting the **spin-1** `l(l+1)` weighting **breaks that agreement by `0.21729`**. **Scope, tightened by the PR #845 audit (R8d):** `0.21729` is the relative break in the **recovered closed-cavity eigenfrequency** that G2's Rayleigh quotient reproduces; it is **not** a measured `Q` shift on the open (radiating) problem, and this battery computed no such shift. **Scoped that way the claim stands: substituting the spin-1 weighting moves the eigenfrequency `Q` is built from by 22%. It is load-bearing, not bookkeeping.**
- **★ `Q`'s `nu_vac`-FREEDOM CARRIES A UNIT-COVARIANCE RECEIPT TO EIGHT SIGNIFICANT FIGURES.** **DOWNGRADED by the PR #845 audit (R5)** from *"the strongest form of the #808 §2.1 requirement produced in this arc"*: under `scaled_geometry()` the three `x_sat` runs are **the same problem in scaled units by construction**, so the agreement is a unit-covariance receipt for a cancellation the prereg establishes **analytically** — not an independent measurement of it. The prereg froze, BEFORE any number existed, that in units of `r_sat` the problem has **no free parameter at all**, so `Omega = omega*r_sat/c_0` is a pure number and `Q = Re(Omega)/(2|Im(Omega)|)` contains no `r_sat` scale — the GR-imported `7` cancels **exactly**, while `omega_R*M_g` does not and stays VALUE-CONSISTENCY class. G8 measured it across `x_sat` in {5, 7, 11} at matched geometry in units of `r_sat`: `Omega` = `1.8536565651917778` / `1.8536565650028993` / `1.8536565678872325`, `Q` = `0.9201505146751939` / `0.9201505121823758` / `0.9201505045003425`. **The gate FAILS at its frozen `1e-9` (spread `1.1058e-08`).** **Floor mis-citation, corrected (audit R5):** all three rows run at the SAME scaled matching radius `R_match/r_sat` = `5.714` (the `R_match` = `40` map row, floor `4.3437e-07`), not the `1e-8`-at-`R_match` = `60` figure originally cited; because the geometry is identical in scaled units the floor is **common-mode and largely cancels in the spread**, and what survives is M1's non-cancelling remainder. #808 §2.1's rule — *"dimensionlessness is cheap; cancellation is the actual requirement"* — is discharged **as a unit-covariance receipt**, which is weaker than the rule taken at full strength, and this entry no longer claims otherwise.
- **⚑ A REAL BUG THIS LANE FOUND IN ITS OWN FIRST BATTERY RUN, recorded rather than quietly fixed.** The first run returned G8 `Q` spreads of `1.74` (a `174`% violation). The cause was **the implementer's, not the physics'**: `R_match` and the search rectangle were held fixed in units of `M_g` while `x_sat` varied, which puts the far-field match *inside the grade* at `x_sat = 11` (`R/r_sat = 3.64`) and breaks the NUMERICS' scale invariance even though the PHYSICS' scale invariance is exact. Repaired by scaling both with `x_sat` (`scaled_geometry()`, `coldq_pole_derivation.py:389-401`), and re-run. **Disclosure the PR #845 audit is entitled to (R5): the prereg did NOT freeze how the geometry follows `x_sat`, so `scaled_geometry()`'s convention is an UNFROZEN implementation choice settled AFTER seeing G8 fail.** **Rule 11 is preserved not because the choice is defensible in the abstract, but because the frozen G8 criterion string is byte-untouched and the gate STILL FAILS at it — the continued failure, not the repair, is what keeps this clean.** The first-run numbers (`1.74`, `174`, `3.64`) are on the record in the result doc so the repair is auditable, and are marked there as **as-run and NOT re-derivable** (the pre-repair driver was never committed).
- **NOT-ADJUDICATED DIAGNOSTICS (no bin, no claim, no solidity — recorded so nothing is lost by the honest closure).** Least-damped pole of the primary branch (`rho = rho_0`, `c_shear = c_0*sqrt(S)`, traction-free SHORT at `r_sat`, outgoing at infinity, `ell = 2`, **zero free parameters**): `omega M_g = 0.2648080807146999 - 0.14389389410143283i`, `Omega = 1.8536565650028993`, `Q = 0.9201505121823758`. Stated but **not adjudicated**: `-29.13`% against the frozen GR cold comparator `0.37367`; `-27.91`% against the standing corpus shortcut `18/49`; `-56.19`% against `Q_GR = 2.1002135791366907`; `-54.0`% against the Op21 `2*pi`-convention `Q = 2`; distances `1.0798` (convention) and `1.1801` (GR) recorded, **with WHICH of the two is nearer left UNEVALUATED — that comparison is BIN-2's frozen discriminator and `BIN-F-SOLVER` fired (PR #845 audit R2; the comparative sentence that stood here is withdrawn).** **Localization:** both frozen measures agree exactly and both put the maximum at the OUTER window edge (`u_energy = u_kinetic = 1.9997126071429716`, `interior_max = false`, wall energy `0.04058976258552422` of peak) — the prereg reserved a `BIN-3-MONOTONE` sub-bin for *"localization is not a well-posed observable for this mode"*, and **whether it applies is NOT adjudicated (PR #845 audit R1)**; the reading is in any case dominated by the generic outward `exp(|omega_I| r)` growth of any QNM rather than by substrate physics. **Overtones:** one credible pole; the next located pole sits inside the band the battery's own self-test shows is roundoff-dominated, so **no overtone ratio is reported.**
- **★ THE IDENTITY THE PREREG FROZE IN ADVANCE, AND IT HELD.** **Attribution corrected (PR #845 audit R8c):** the prereg's frozen string (prereg:284) is the **literal-`7`** form `k_0 r_sat = 7 * omega_R M_g`; the `x_sat`-generalized `k_0*r_sat = x_sat * omega_R*M_g` is the **result doc's trivial generalization of it**, not a frozen string. Either form holds **identically**, so the "9/7-above-cutoff" test **IS** the `omega_R` vs `18/49` comparison re-expressed — **not an independent axis**. Both read `-27.91`%. This was recorded in the prereg *before any number existed* precisely so it could not afterwards be presented as two corroborating results. **It is one result, and the doc says so.**
- **FORKS.** **FORK-10 and FORK-11 DISSOLVED by construction** — the lane computes the complex pole directly, so `no port-Q is computed and no port-to-pole transfer is performed`, and neither disputed spin-1 estimator was used; the `50`% estimator spread recorded in #814 §1.3 never entered and is **neither adopted nor resolved**, merely bypassed, and the doc says so rather than presenting it as an adjudication. **FORK-2 KEEP-BOTH** — `sqrt(S)` primary, the Family-E `S^{1/4}` counterfactual reported as a sensitivity (`Omega = 0.45204846339024657`, `Q = 0.40761649426415175`, **low confidence**, no bin). **FORK-3(b) NOT RUN**, with the reason stated: it contradicts a frozen canonical input (the SHORT at `vol3/claim-quality.md:123`); its shape is nevertheless on the record via FT-2's clamped-wall run, which shifts the pole by `0.28430` — **the canonical `Gamma_shear = -1` free surface is load-bearing in the answer, which is #814's CF-13 with a number attached for the first time**. **FORK-12 NOT ADJUDICATED** — Grant has not answered it, so the `ell`-ladder ships tagged `DIAGNOSTIC - no bin, no verdict`, and its `ell = 4`/`ell = 5` rows are flagged **not credible** (they break the monotone rise in `Omega` that `ell = 2, 3` show — a seed-selection-hit-noise signature). **FORK-1 is NOT ADJUDICATED; the diagnostic that would have fed it is recorded.** (Corrected by the PR #845 audit R1. This entry previously read, verbatim: **FORK-1 was handed to the substrate and the substrate returned "ill-posed for this mode"** — which states a `BIN-3` outcome the frozen precedence forbids stating once `BIN-F-SOLVER` has fired.)
- **FLAG-DON'T-FIX — routed, nothing repaired.** (1) **The frozen controls G4/G5/G7 and the frozen rectangle are mis-specified for this problem class.** **This is NOT a request to loosen them.** The correct successor controls are *different in kind, not looser*: (i) optimal-truncation agreement rather than fixed-order agreement; (ii) a measured conditioned band rather than an assumed rectangle; (iii) a winding contour that does not traverse the noise floor; and **(iv) ADDED by the PR #845 audit (R3c): bound the rectangle's LEFT edge away from `omega_R -> 0`, or adopt a representation with no low-frequency divergence at all** — the `1/(omega r)` recursion is unusable at the frozen left edge `0.02` regardless of `|omega_I|`, so no tolerance choice can certify a rectangle that reaches it. **Two structural requirements on the battery come with it:** certification must cover the **full** search rectangle **including its corners** (G1 certified down to `omega_R` = `0.4` while the scan ran to `0.02` — a factor of twenty of uncovered range), and feasibility checks must exercise the **solution** representation, not an input one (prereg §9 item 4 measured the convergent `mu(z)` Taylor series and never touched the divergent solution series that actually failed). **All four controls and both requirements were transmitted to the in-flight successor (`v2`) lane by the orchestrator on 2026-08-03 — a routing act, not an adoption: this lane adjudicates nothing about `v2` and `v2` inherits no certification from here.** **Per Rule 12 the slot is NOT refilled — a successor needs a NEW prereg with a new version number and its own verification chain, not an edit to this one.** (2) **A method upgrade that would reach deeper was deliberately NOT taken:** complex-rotating the radial contour inverts the dominance so the quasinormal condition lands on the *dominant* solution. It was not used **because the prereg froze the real-`sigma` method, and changing method mid-lane after seeing failures is the move the discipline exists to prevent.** Routed as the leading successor candidate. (3) **#814 CF-7's naming gap stands untouched** — `vol3/claim-quality.md:122` writes `Z_shear = rho c_shear` and never names which `rho`; this lane consumed the leading reading as a frozen input and did not repair the leaf. (4) **The B1-ratified `Q = ell` anchor is untouched** — since no bin was adjudicated there is not even a disagreement on the record to route; `qnm-quality-factor.md`, `op21-multi-mode-mode-counting.md` and `regime-eigenvalue-method.md` are byte-untouched.
- **Battery.** `make verify` exit 0 (all gates including frozen-provenance and the new gating number-check); refresh idempotent; docket lint clean; pure-corpus. **Determinism: two independent full driver runs produced identical shipped objects apart from `_runtime_sec`, digest `24bff544f53727ea`.** The number-check registers every **backticked** numeral in the result doc against the shipped JSON (105 backticked numeric tokens after the repair pass: 69 registered, 36 allow-listed with reasons, 0 unregistered; the pre-repair figures were 88 / 69 / 19 / 0). **Coverage wording corrected by the PR #845 audit (R8b): un-backticked prose numerals are OUT OF SCOPE and were never covered — the tool now says so on stdout, and the house convention that makes it sufficient is that any load-bearing numeral must be backticked** and is wired into `make verify-lane-number-checks`; `_runtime_sec` is explicitly NON-REGISTRABLE (the #801 R3/WARN-4 lesson) and `main()` refuses any attempt to register or allow-list it. Runtime exceeded the frozen `900` s budget and that is **disclosed, not silently accepted**, per the frozen wording.
- **Lane mechanics.** Self-isolated throwaway worktree off `origin/main` @ `ac165cf2`; branch `research/coldq-pole-derivation`; prereg committed ALONE and pushed first; PR opened `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. **Not merged, not retitled.**
- **⚑ COMMIT-TRAILER DEVIATION, RECORDED AND DELIBERATELY NOT REPAIRED (2026-08-03, PR #845 audit F7/R7).** Commits `db29d5a3` (the frozen prereg) and `43001467` (the result battery) **lack the `Co-Authored-By:` trailer** that every other commit in the window carries. The audit found these to be **the sole deviations in 60 `main` commits**, and the repair lane re-verified it independently: scanning the last 60 non-merge commits on `origin/main` returns **zero** missing trailers. **They are NOT repaired.** Repairing them means rewriting history, which would **destroy the frozen-first SHA evidence chain** — the push timestamps and the GitHub `PushEvent` anchored on `db29d5a3` are what make *"the prereg was committed ALONE and pushed before a single line of driver code existed"* independently checkable by a third party. **A provenance chain is worth more than a trailer.** The deviation is therefore recorded here rather than erased, and **every subsequent commit on this branch carries the trailer.**
- **REPAIR PASS (2026-08-03, PR #845 audit CLEAR-WITH-REPAIRS).** Items F1-F8 executed on this branch. **Nothing in the repair changes a gate verdict, a bin, a certification class, or a solidity** — the verdict was and remains `SOLVER-NOT-CERTIFIED` with all four physics bins `N/A — not adjudicated`. **HARD FENCES HELD: `research/2026-08-02_coldq-pole-derivation_prereg-FROZEN.md` is BYTE-UNTOUCHED** (`git diff` empty against `43001467`), **`research/drivers/coldq_pole_derivation.py` is BYTE-UNTOUCHED**, the shipped results JSON is BYTE-UNTOUCHED, and **no git history was rewritten** (`db29d5a3` and `43001467` keep their SHAs). The only executable edited is the number check's allow-list and its coverage wording. Corrections are logged in the result doc's **§8**, with the withdrawn text preserved verbatim under 🔴 headers per Rule 12.
