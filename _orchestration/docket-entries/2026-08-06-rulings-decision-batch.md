# Rulings — post-merge decision batch + the combine-fork walk (2026-08-06)

Core-orchestrator session record. Context: all wall-arc PRs (#899–#904) merged; main =
`dcc11323`; post-merge gate GREEN (make verify + make test in a clean worktree, all four
lane checkers with mutation receipts — workflow receipt `wf_9a5784a1`, session-local).
The doc lane's upgrade-wave final report was relayed by Grant and its two load-bearing
claims were independently re-verified before these rulings were posed (findings in R6).

**Grant, verbatim:** *"1. agree / 2. agree / 3. agree / 4. agree / 5. agree"* — on the
five-item decision list as posed. Then the combine-fork walk (R6/R7), his framings quoted
there.

## R1 — census fork: treatment (i)

Tag the **216 actionable sites / 60 files**. Treatment (ii) (subscripting ~2,998 sites) is
REJECTED on the posed ground: `wall-taxonomy.md` §10.1 records the `Γ_shear` sign as
unresolved, so mass-subscripting the ~2,230 judgment sites would hand-set a channel
attribution canon has not adjudicated. Census numbers cited in any brief must pin the SHA
(the 80%-figure drifted by 16 sites in ten commits). → doc lane executes (their proposal).

## R2 — SHA-pinned cites: explicit marker convention

The line-scoped proximity heuristic is unfixable (measured 7%–68% corroboration spread —
no line-scoped regex can say which cite a row-level SHA pins). Forward convention: an
**author-declared machine-readable pin marker** adjacent to the SHA; the existing 96
heuristic-exempt cites are grandfathered; the gate's exemption re-keys to marker-only once
migrated. → doc/tooling lane designs the token + migration.

## R3 — branch protection on main: verify job required

Create protection requiring only the **`make verify + make test`** check, applied to
admins, force-pushes off. The excerpt ratchet stays non-required (3/20 back-test blocks
post-widening). Enactment note: the orchestrator's API call was blocked by session
permissions; the exact command was handed to Grant to run — **status: HANDED-OFF, not yet
confirmed enacted.**

## R4 — G-RHO2 supersession: LOCKSTEP route

The frozen v1 kernel-collapse result doc gets a **dated supersession note** (after its
headline verdict at `:24`, plus a §1.3 cross-reference near `:78`) pointing at the #902
ROW-CERTIFIED rerun — shipped in ONE commit with the `approach_leak_v2` NC-BYTES re-pin
(the v1 doc is in that driver's READ_ONLY_ARTIFACTS roster; an un-re-pinned edit turns
`make verify` red through G-DET-V2, not through either G-RHO2 checker — probed
empirically). Constraints carried from the probe: the note may back-tick no numeric-leading
token (`902`, dates FAIL the numeral registry; `#902`, `b06cbeb1`, `ROW-CERTIFIED` are
safe). Same PR: `wall-taxonomy.md:566-575` gets its own dated surface-note (its premises —
#902 open, v2 doc absent — are vacated at HEAD; its Makefile cites drifted
:111/:250→:115/:275). → core implementer lane, Tier-2 verify before CLEARED.

## R5 — #897 re-issue: GO, as v2 record

The rescope ruling is re-issued as a versioned v2 carrying the cross-grade combine fence
and the corrected engine statement. Ruled text lives in
`2026-08-06-ruling-kernel-collapse-rescope-v2.md` (this batch); the 2026-08-05 record is
preserved and gains a dated pointer. Ground: the wave demonstrated ruled text propagates
byte-identically, so the fence must live in the citable record, not only at print sites.

## R6 — the combine-fork walk: L2-vs-L∞ recast as a RESIDENCE question

Pre-walk verification findings (Tier-2, session workflow `wf_9a5784a1`), which the walk
consumed: the wave's "the engine codes L∞" headline is OVER-BROAD — the elastic block is
per-grade (`cosserat_field_3d.py:767-768`: γ·W_κ rides only S_κ — the carve-out's
structural receipt STANDS), but the aggregated normalized-L2 kernel is live at unit weight
in the SAME energy sum (`_reflection_density:486-488`, `_s11_density:409-413`,
`k_refl=1.0` default), a THIRD amplitude definition exists (`:619/:681`: sym-only strain
+ V²/V_SNAP² + chirality bias — FLAG-A2DEF undercounts 2→3), no test pins the split
(`k_refl` zeroed in tests), and at κ=0 the members are numerically degenerate. One vacated
cite: the 2026-07-02 combine-rule doc cites `:411` as per-grade evidence; `:411` is the
aggregated line. Canon is internally split: `axiom-register.md:189` (dissolved) vs
`:190`/`:232` + `trampoline-framework.md:255` (open) = FLAG-COMBINE-SPLIT, and the
dissolution argues the (A,S) load-response pair while DP-3 posed grade-into-A aggregation
— possibly two questions under one fork name.

**The walk (Grant-driven, fork-collapse pattern).** Opening move, verbatim: *"well, are we
talking lattice local mechanical strain, or its projected strain fields? and what type of
strain?"* Established: all kernel arguments are LOCAL per-cell finite differences (the fork
is constitutive, not projective), and "strain" bundles three distinct local measures —
bond stretch (ε_sym), slip (ε_antisym, the G_c grade), twist gradient (κ = ∇ω) — which the
engine's three definitions inventory differently. Grant then caught a register leak
(*"why are you saying when one cell dies?"*): saturation is Ax3-lossless-reactive
detuning, not failure — the corrected question is **whether the softening is
cross-coupled** (does stored twist reduce the incremental stiffness stretch sees), which a
materials "yield/failure" frame mis-poses. His reframe, verbatim: *"is the rotational
inductive sector the actual material, and the capacitive compliance is solely the bonds
meaning the flywheel rotational action is the inherent material of the universe and in the
crystallized form, we have the compliance, but then we don't want it train becomes too
much like within a black hole?"*

**Outcome:** the L2-vs-L∞ "norm choice" DISSOLVES into a residence question — a saturation
budget is a property of the element that stores the energy, so the combine rule is forced
once the storage residences are adjudicated. The one open residence: **where does the
twist-coupling energy (γ_c) live — in the bond, or through the material?** The ontology
walk itself (rotation = substance, compliance = bonds) is **UN-AUDITED walk-level**
(framing note `research/2026-08-06_rotation-substance-ontology_framing-note.md`, with
counter-arms); it is NOT ruled. It generates one falsifiable commitment: I_ω independent
of strain amplitude (FLAG-IOMEGA's missing law). → two derivation lanes briefed
(`2026-08-06_ax4-state-vector-residence-brief.md`, `2026-08-06_iomega-law-brief.md`);
substrate adjudicates, not fiat.

## R7 — Ax1 phase-scoping: GO

**Grant, verbatim:** *"all right, let's do it but intrinsic LC would be the crystallized
form of the vacuum so really that ax needs to understand each phase of the vacuum material
right?"* Ruled at concept level: Axiom 1's intrinsic-LC description is the constitutive
law of the vacuum material's **crystalline phase**, not of the material as such; phases
are states of one substance, not new axioms. PROPOSED header text (wording
merge-ratifies with this record; doc lane executes into the axiom register):

> **Domain of validity (phase-scoped, 2026-08-06).** This axiom states the constitutive
> law of the vacuum material's crystalline phase (cold, sub-yield, lossless-reactive).
> It does not assert the substance-level law across the material's other phase-states
> (saturation boundary; ruptured/plasma above V_snap; pre-freeze). Any use across a
> phase boundary is an extrapolation and must be declared as one.

Vocabulary fence: the phase inventory must be substrate-named ("amorphous" is retired;
STZ/plastic fails Ax3 — no borrowed condensed-matter phase words). The substance-level
("melt") law is hereby the program's named deepest open object.

## R8 — the phase-inventory walk (continuation; leans and routings, NOT rulings)

**Grant, verbatim:** *"we were calling it a pre-geodesic plasma but I think pre-geodesic
state or pre-bond state or something similar might make the most sense, but we also have
to thing through the full thermal dynamic process of the vacuum. What would our
evaporation or gaseous state be? would that just be free electric field or bonds being? I
don't know what is Hawking radiation under this"*

Walk outcome (all UN-AUDITED walk-level; details + counter-arms in
`research/2026-08-06_rotation-substance-ontology_framing-note.md` §4):

- **Naming (Grant LEAN, not GO):** "plasma" is a vocabulary leak of the same class as the
  retired "amorphous" — it imports free-charge/Debye theorems underived here. Candidate
  primary name **pre-bond state** (constitutive), gloss *pre-geodesic* (no bonds → no
  impedance network → no wave paths → no metric). History distinction: BH interior =
  de-bonded (released), primordial pre-freeze = pre-bond (never bonded); one phase, two
  histories. → rename PROPOSAL routed to the doc lane; no corpus sweep without a GO.
- **The gaseous state cannot be free E-field — the carve itself forbids it:** under
  compliance-is-bonds, E is a bond-sector DOF, so the de-bonded phase carries only free
  rotational excitations — **dark by construction** (stores energy, no EM coupling).
- **Thermodynamic arc:** pre-bond material (unconfined) → omega-freeze AS the
  crystallization event (seed orientation = the frozen chirality) → crystal → local
  re-melts (BH interiors) as droplets confined by the crystal itself → annealing.
- **Hawking radiation = interface annealing (walk-level):** a confined melt droplet
  re-crystallizes at its interface, releasing bond-formation latent heat into crystal
  modes; Gibbs-Thomson/Kelvin curvature-driven evaporation gives hotter-when-smaller
  ∝ 1/r — the Hawking T_H ∝ 1/M scaling at the structural level. **Falsifiable target
  (routed, frontier-queue candidate, NOT dispatched — arc-serial):** derive the
  coefficient from interface thermodynamics and test against ħc³/(8πGM k_B). A
  forward-prediction-class object if the number lands.
