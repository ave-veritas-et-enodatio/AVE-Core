### ENTRY 2026-07-26-cannot-advect-relabel-propagation (2026-07-26): Grant ruling — PROPAGATE the "cannot advect" re-label; strengthen-by leg DISCHARGED

- **The ruling.** Grant ruled (verbatim `[sic]`: *"for the relabel, let's propagate/ (a)"*) on the adjudication routed by the `#787` adversarial review of `clm-hu1jjw`. Corpus statements that read as **absolute real-space station-keeping** are more narrowly **(i)** a **no-SELF-transport / no-re-hosting-DOF** result for the saturated core *at those formulations* — which **SUPPORTS** index-pinning (Corollary A of the relative-offset principle), since a core with no re-hosting DOF is exactly what index-pinning describes — **plus (ii) formulation-anchor nulls** (bond strains boost-invariant under a uniform material shift in the small-displacement model; the `#788` structural absences, `research/2026-07-21_beta-tracking-feasibility_scoping.md` §3). They are **not** a *"can't-surf-a-drive-at-phase-velocity ⇒ can't-track-material-displacement"* result.
- **SCOPE ONLY — no verdict overturned.** Every June/July verdict stands byte-untouched: `GENUINE-FALSIFICATION` (motion-stability-bemf-cosserat), `PIN-even-longitudinal` (06-04), `UNRESOLVED` (06-11), `PROPAGATION_WITH_TIR_AT_WALL` (06-08), `NULL-mobility-banked` (07-09). Only the SCOPE of their READING is carved. Rule 12 throughout — additive dated blocks, placed at doc TAILs so every banked line-number citation resolves unchanged.
- **Evidence the carve rests on (verified two-method this session).** (a) The ledger's two motion-stability runs boosted the **V-SECTOR winding phasor** `(ox,oy)= (V0+V1, V2+V3)` — a moving-solution SELF-transport ansatz (`src/scripts/vol_1_foundations/motion_stability_bemf_longitudinal_probe.py:10-12`, naming `059ae318` @ ledger `:134-139` and `c6613c26` @ ledger `:141-146`). (b) The ledger's own named mechanism — `S=0 ⇒ c_eff→0`, *"stable **because it is static**"* (`genesis-chord-falsification-ledger.md:145`) — **is itself a no-re-hosting statement**, i.e. the same fact as index-pinning. (c) The 2026-06-04 longitudinal companion drove the Cosserat material field with the **K4→Cosserat FORCE channel DISABLED** (`disable_cosserat_lc_force=True`; probe `:40-41`, `:108-109`, `:115`), leaving only the `z_local` path, which that doc measures as **screened at the core** (`:181-182`, `:210`) — a formulation-anchor null. *(★ the "formulation-anchor null" label on this leg is **WITHDRAWN 2026-07-28** — see the dated repair note at the end of this entry; the `z_local` path was **LIVE**, not dead.)*
- **Landed carves.** Canon: `manuscript/ave-kb/common/genesis-chord-falsification-ledger.md` (★SCOPE CARVE bullet on the motion-stability section — the carve's canon home). Research: `research/2026-06-04_motion-stability-bemf-longitudinal-result.md`; `research/2026-06-11_annihilation-evaporation_result.md`. **Sweep-missed sites found by a two-method re-grep and carved, each disclosed in-doc:** `research/2026-06-08_native-electron-propagation-adjudication.md:19`; `research/2026-07-09_superband-carrier-fork_result.md:94`; `research/figures/photon_vs_electron_cells.py` (the "stays put" caption — comment-only; the caption string is left verbatim and the figure is **not** re-rendered, its strip output being recorded LEAVE-STALE at `research/2026-06-07_figure-audit-ledger.md:44,:58`); `src/scripts/vol_1_foundations/moving_defect_transport_gate.py` (the "frozen-clock object that cannot translate" ENGINE-GAP header).
- **Judged NOT to need a carve (disclosed for audit).** `research/2026-06-23_mass-sector-two-body-scattering_prereg.md:113` — `The single blob's centroid should stay put` — is a **zero-drive O0 control expectation**: one blob, no neighbour, zero initial velocity (`mass_sector_two_body_scattering.py:161-172`), defining the radiation-jitter floor `N_FORCE` that the two-body drift must exceed. It asserts no transport physics and needs **no** carve. Likewise `research/2026-06-11_moving-defect-doubleslit_prereg.md:100` — `cannot translate when boosted transversally` — is self-scoped; `manuscript/ave-kb/common/figures/engine_capability_matrix.yaml` `boost:` rows (engine **capability-absence** rows = precisely the no-re-hosting-DOF reading the ruling affirms); `src/scripts/vol_1_foundations/mass_sector_two_body_scattering.py:23-25` — `the imprinted KE radiates instead of convecting the trap` — names the self-transport ansatz explicitly; `research/2026-06-23_mass-sector-two-body-scattering_result.md:166` — `two-body force readout that does not require either blob to physically convect` — is a readout-design statement.
- **Adjudication discharged.** `manuscript/ave-kb/common/claim-quality.md` — `clm-hu1jjw` strengthen-by leg 1 (*"adversarial review of the corollary set against the corpus 'cannot advect …' sites — reconcile material-tracking vs phase-velocity-surfing or flag a genuine tension"*) marked **DISCHARGED 2026-07-26**, in the in-place #790-F8 style: the reconciliation is **material-tracking, NO genuine tension**. The `★STATUS NOTE`'s stale clause (*"strengthen-by leg 1 below STANDS"*) carries a dated addendum. **No re-grade** (`clm-hu1jjw` HELD at **0.50** — the discharge re-labels sites, it adds no new evidence for the DIRECTION) and **no retitle** (anchor-slug stability). `manuscript/ave-kb/common/relative-offset-principle.md` — the `:59` revisit-sites bullet's closing *"this leaf reframes none of them"* is **SUPERSEDED** by a new dated bullet at `:60`; `:59` itself is byte-untouched so existing citations resolve.
- **Audit of the two pre-existing carve commits (this lane, before building on them).** Both sound in substance — no verdict overturned, all load-bearing evidence re-verified. **Two citation defects repaired in this pass:** the 06-04 carve cited *"the selection table at `:72`"* (a blank line; the table is `:68-71`, chosen row `:71`) and named the results-JSON keys as `drive_variant`/`drive_form` (actual keys: `variant`/`form`) — corrected in-place with a dated repair note, no substantive change (the run did use Variant **B** / `form="displacement"` on every arm). The figure-caption note cited *"the car-on-the-road reading stated at line 51"* — a self-invalidating reference, since inserting the note itself pushed that text to `:59`; replaced with a content-anchored reference. *(★ **2026-07-28 correction**: the JSON-key half of "REPAIR 1" was a **FALSE POSITIVE** — `drive_variant`/`drive_form` ARE the results-JSON `config`-block keys, so the original citation was correct. Only the `:72` → `:68-71` half was a real defect. See the repair note at the end of this entry.)*

**★ REPAIR NOTE (2026-07-28) — adversarial review of this lane's propagation work; six findings applied.** Rule 12 throughout: every banked body is byte-untouched (origin/main's 06-04 result, 313 lines, sha `a4fc5b9a…`; the ledger's `:1-146`, sha `80bbbf66…`), the ledger carve bullet stays a single line at `:147`, no re-grade (`clm-hu1jjw` HELD **0.50**), no retitle.

- **F1 (MAJOR — the over-carve, repaired in CANON and in the 06-04 result).** The 2026-07-26 carve softened a banked falsification: it cited only the **screened** half of the 06-04 doc's coupling control (`:210`) and omitted the **live** half — *"**Coupling is LIVE** (mean moves 0.973→0.051 at 100× — the drive's strain reaches the kernel)"* (`:209`) and *"**SCREENED-PIN confirmed — the falsification is real, not a no-op.**"* (`:212-213`) — and on that half-cite re-classified the run as a **`#788`-family formulation-anchor null**, a class `#788:31` itself calls *"ARTIFACT, not a falsification"*. **That label is WITHDRAWN.** `#788`'s family is defined by **uniform-shift invariance** — bond strains invariant under a uniform material shift, `max|Δdu| = 5.6e-17` (`research/2026-07-21_beta-tracking-feasibility_scoping.md:28`) — which **provably does not obtain** for this run: Variant **B** is a *spatially varying* `x`-planar displacement pulse (probe `:177`, `:198`) with **measured nonzero core strain** (`ε_sym²/ε_yield² ≈ 1.90e-2`, 06-04 result `:176-178`) and a **live** kernel response. `#788` is also a `Class: SCOPING (pre-lane)` doc that banks no verdict (`research/2026-07-21_beta-tracking-feasibility_scoping.md:3`) whose June clause is an explicit *candidate*. Residual artifact language is now scoped to the **material-tracking axis ONLY**. **What survives and is kept:** the **disabled-FORCE-channel** disclosure (`disable_cosserat_lc_force=True`; probe `:40-41`, `:108-109`, `:115`) — a genuine, previously-undisclosed configuration-scope gap (grep of origin/main's 06-04 body returns no such mention) — and the narrow **no-SELF-transport** scope. **`PIN-even-longitudinal` STANDS**, now restated explicitly in the canon bullet.
- **F5 (claim strength).** Six carve sites read **"SUPPORTS Corollary A"**; the ruling licensed only the weaker supporting-rather-than-contradicting reading. All six are harmonized to **NOT COUNTEREVIDENCE**, with the material-tracking axis stated as **evidentially NULL** — a null cannot double as support. The sharpest case was `research/2026-07-09_superband-carrier-fork_result.md`, which declared the run *"carries no evidence either way"* and then claimed SUPPORTS four lines later. Governance sites (`relative-offset-principle.md:60`, `claim-quality.md` `clm-hu1jjw` leg 1) carry an additive dated clause; the ruling's own recorded wording is preserved, not rewritten.
- **F3 / F4 (under-carves).** `src/scripts/vol_1_foundations/motion_stability_bemf_longitudinal_probe.py` carries the `PIN-even-longitudinal` verdict sentence verbatim and was missed — now carved above its VERDICT block (emitted string byte-untouched; probe cites `≥650` shift, the single affected corpus cite `:759 → :781` is updated). DEV-6's `the imprinted KE radiates instead of convecting the trap` (`src/scripts/vol_1_foundations/annihilation_evaporation_run.py:46-50`) is the **upstream source** of two sites this lane did handle and had zero mentions on the branch — now carved at the origin, appended at the end of the module docstring so `:46-50` is byte-stable.
- **F2 (false repair, corrected in place).** See the inline correction above: `drive_variant`/`drive_form` are the results-JSON `config`-block keys; the original citation was right.
- **F6 (gate claim made honest).** The earlier claim *"anchor-content drift: no new drift on any touched file"* **did not reproduce**. Measured (`make verify-anchor-content`, `--top 100000`, branch vs `origin/main` @ `c8ceacc3`): main **923** drift rows (179 moved + 744 absent) → pre-repair branch **931** (179 + 752) = **+9 new rows / −1 = net +8**, all nine sourced in files this lane touched. Fixed at source (verbatim excerpts re-anchored next to their cites, per the tool's nearest-span association) rather than waived. Post-repair numbers are recorded in the repair commit.
