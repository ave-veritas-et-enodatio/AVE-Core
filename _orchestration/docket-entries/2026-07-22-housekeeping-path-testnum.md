### ENTRY 2026-07-22-housekeeping-path-testnum (2026-07-22): implementer — two audit-surfaced housekeeping fixes (charter pending-rulings path + #417-vs-#59 test-number reconciliation)

- **Two small audit-surfaced fixes, batched.** Branch `docs/housekeeping-charter-path-testnum` off `origin/main` @ `f050e33b`, worktree-isolated. Engine byte-untouched; mints no `clm-`/`def-`; **no KB leaf edited** (the canon leaf is CORRECT — see fix 2).
- **FIX 1 — wrong pending-rulings path in the continuum-radial-solver charter.** `research/2026-07-21_continuum-radial-solver_CHARTER.md` cited `research/2026-07-20_pending-rulings-and-frontier-queue.md` at **:3 and :119** — the file actually lives at `_orchestration/2026-07-20_pending-rulings-and-frontier-queue.md`. **2 occurrences fixed, both in the charter.** Two-method corpus sweep (`git grep` + `rg`) confirms the wrong-path string appeared **nowhere else** — no other doc copied it (hit count = 2, all in the charter). Path-only correction, no other bytes changed.
- **FIX 2 — #417-vs-#59 test-number discrepancy, ADJUDICATED BY RUN RECORD.** `the-abandoned-interior.md:111` cites the both-loci negative as `#415 + #417`; `research/2026-06-24_engine-reroute-epic-summary.md:5,20,24` cited `#415 + #59`. **Verdict: the KB leaf (`#417`) is CORRECT; the epic summary (`#59`) is the typo.** Evidence: `gh pr view 417` = *"Phase-space coupling-winding test — BREAK …"*, head branch `analysis/engine-phase-space-winding` — the **exact branch the epic summary's own header (:3) grep-verified against** — merged 2026-06-24; `gh pr view 415` = the coupled-eigensolve real-space rescope, merged 2026-06-24. By contrast **PR #59** = the unrelated **2026-05-31 Q-EMBED-SEL-1** investigation (`_orchestration/index.md:628`), and archive doc `59_memristive_yield_crossing_derivation.md` is the memristive-yield derivation — **no identifier scheme (PR# or archive-doc#) denotes the phase-space test by #59.** So the "two valid schemes" possibility is REFUTED; the `#59/#417` double-cite at two downstream sites is a hedge on the same typo. `#415`/`#417` are a consistent PR-number pair.
- **What was FIXED vs FLAGGED.** FIXED: the epic-summary root — inline `#59 → #417` at :5/:20/:24 (line numbers preserved so downstream `:5,20,24` cites still resolve) + a dated Rule-12 correction note appended at the tail (banked body preserved, evidence + propagation list recorded). LEFT UNTOUCHED: `the-abandoned-interior.md:111` (already correct, canon leaf). FLAGGED (routed to auditor lane — KB canon / claim-DAG, out of housekeeping scope): the `#59` mis-number propagated from the epic-summary root into `common/claim-quality.md` (clm-riminv :1587/:1597/:1607) → `.index/claims.jsonl:252` (derived — needs `make refresh-kb-metadata`); `vol4/…/unified-engine-design-doctrine.md:168` (`#59/#417`); `research/2026-07-03_engine-verdict-exposure-sweep_result.md:49` (`#59/#417`); `research/2026-07-21_phase-space-inversion-walk_RECORD.md:6`; `_orchestration/docket-entries/2026-07-21-saturation-rim-inversion.md:13`. All cite the epic summary `:5,20,24` and inherit the correction.
- **Note on the task's implied premise (flag-don't-fix).** The audit brief's Rule-12 parenthetical anticipated the abandoned-interior LEAF might be the wrong site; the run-record evidence REVERSED that — the leaf is correct, the research-doc epic summary is the typo. Fixed the actually-wrong site rather than reframe a correct canon leaf to match an error.
- **Gates:** `make verify` PASS; docket lint clean (no new dup keys). PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. Do not merge, do not retitle.
