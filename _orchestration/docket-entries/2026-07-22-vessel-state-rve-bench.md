### ENTRY 2026-07-22-vessel-state-rve-bench (2026-07-22): implementer — vessel-state RVE bench RUN (walk-1 verdict = UNDETERMINED)

- The walk-1 bench BUILT + RUN per the merged, Grant-ratified REVISION-3 frozen prereg (`research/2026-07-22_vessel-state-rve_prereg-FROZEN.md`); driver `research/drivers/vessel_state_rve.py` (+ `_results.json`, white figure `vessel_state_rve_figure.py`), result doc `research/2026-07-22_vessel-state-rve_result.md`. Rule-14 reuse of the #782 KUBC scaffold; the NEW piece = the STATE-DEPENDENT operator `k_shear,eff(u) = k_s + k_a·ε_axial(u)` (live geometric remap, `axiom-register.md:193`) + the frozen Newton/self-consistent solve. Engine byte-untouched; mints no `clm-`/`def-`; no leaf/ledger/port-register edit. Deterministic, 756 s.
- **LEG 0 (the mandatory VOID gate) PASSES.** `gate_fireability_selftest_pass = True`: SELFTEST-(ii) fires (ii) at the frozen loose tol 1e-3 (`amp_spread± = 649/649` > 0.05, `A_sign = 0.030` symmetric + non-converged; tight 1e-10 → `0.006/0.004` clean); SELFTEST-(iii) fires (iii) (`outcome iii, A_sign = 569, K_tan_minus 7484 < K_tan_plus 4.4e6`, converged+robust). Instrument validation all-pass (uniform-null `K/K0 = 1.0000`, determinism bit-identical, Lamé `0.036`, size gap `0.007`, STOP-gate rail `0.296` < 1 < rigid `1.583`). The bench is **VALIDATED, not VOID.** (★DISCLOSED §3B calibration: the ≥50%-shell target met via the "justified equivalent" latitude — a near-buckling core cluster of 21 bonds producing the same aggregate crossing; a convergent broad ≥50% shell sits at the buckling knife-edge.)
- **STOP criteria on the grown state (`fixed_budget p_ref = 0.040`):** `grown_equilibrium_exists = True` — CG converged, `max_r|T| = 0.647 ≥ 0.489` (tension nonzero), `min k_shear,eff = 0.353 > 0` (bonds positive). Hoop-tension ε up to +0.139, radial-compression to −0.066.
- **HEADLINE BIN = UNDETERMINED** (cell `(i) × L1 × Z_str`). `K_ratio_lift = 0.997` (L1 < 1.2) — the grown hoop-stiffen and radial-soften CANCEL in the small-signal tangent (`K_tan/K_0 = 0.2955` grown vs control `0.2964`); amplitude gate **outcome (i) CLEAN** (`A_sign = 0.002`, converged+robust); **Protocol E MEASURED `ρ_eff/ρ_0 = 0.916` → `r_Z = 0.520`** (straddles 0.5, Z_str). The grown anisotropic vessel reads like its isotropic control — the SAME landing as merged #782's painted bench (BIN-4/UNDETERMINED at the `r_Z` edge), now with a MEASURED ρ.
- **yield_saturated has NO stable equilibrium** — it BUCKLES (peak-strain ceiling ≈ 0.21) before peak strain reaches `A_yield = 1.0`, across all `A_yield_scale ∈ {0.9,1.0,1.1}`. The energize-LOCK-adjacent NULL (the pre-test-physics-check answered NEGATIVE). Headline falls to `fixed_budget` per §9 Fork SEED.
- **Corroborators:** percolation `hoop_percolates = False` (patchy tension, corroborates SOFT/NULL); PAINTED-ANISOTROPIC `0.298` ≈ GROWN `0.296` (the near-null effect is anisotropy-driven, not provenance-driven); seed-independence stable (lift rel-spread `0.002`, r_Z `0.001`, no flip → not a seed-controlled UNDETERMINED); C-V `K(ε_bias)` nearly FLAT (`0.059 %` total variation, `|ASYMMETRY| = 0.40 ≥ 0.15` corroborative-only per the demoted clause); anti-seduction fence NOT triggered (`r_Z = 0.520`, not matched-ish).
- **Deviations (§8):** T = remap term `k_a·ε_axial`; SELFTEST-(iii) justified-equivalent core cluster; Protocol E at `L = 32` only (frozen `{32,48}`, runtime-budget scoping, not verdict-controlling); yield_saturated fail-fast capped scan (Rule-10 runtime fix, no physics change); `p_ref = 0.040` cage-arm knob. Frozen-provenance gate: 0 gating on the result doc. `make verify` green.
- **Consequence routed to Grant** (this lane surfaces + routes only): UNDETERMINED at the `r_Z` straddle → the owed resolver is the feasible SUBC/periodic lower-bound bracket (#782 Fork B), NOT run here; the yield_saturated NULL banked (no rescue, Rule 12). Feeds the continuum radial-solver charter D5/R1 (#789).
- PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; branch `research/vessel-state-rve-bench` off `origin/main` @ `f050e33b`. No bench merge in this lane.
