### ENTRY 2026-07-22-vessel-state-rve-bench (2026-07-22): implementer — vessel-state RVE bench RUN (walk-1 verdict = UNDETERMINED)

- The walk-1 bench BUILT + RUN per the merged, Grant-ratified REVISION-3 frozen prereg (`research/2026-07-22_vessel-state-rve_prereg-FROZEN.md`); driver `research/drivers/vessel_state_rve.py` (+ `_results.json`, white figure `vessel_state_rve_figure.py`), result doc `research/2026-07-22_vessel-state-rve_result.md`. Rule-14 reuse of the #782 KUBC scaffold; the NEW piece = the STATE-DEPENDENT operator `k_shear,eff(u) = k_s + k_a·ε_axial(u)` (live geometric remap, `axiom-register.md:193`) + the frozen Newton/self-consistent solve. Engine byte-untouched; mints no `clm-`/`def-`; no leaf/ledger/port-register edit. Deterministic, 756 s.
- **LEG 0 (the mandatory VOID gate) PASSES.** `gate_fireability_selftest_pass = True`: SELFTEST-(ii) fires (ii) at the frozen loose tol 1e-3 (`amp_spread± = 649/649` > 0.05, `A_sign = 0.030` symmetric + non-converged; tight 1e-10 → `0.006/0.004` clean); SELFTEST-(iii) fires (iii) (`outcome iii, A_sign = 569, K_tan_minus 7484 < K_tan_plus 4.4e6`, converged+robust). Instrument validation all-pass (uniform-null `K/K0 = 1.0000`, determinism bit-identical, Lamé `0.036`, size gap `0.007`, STOP-gate rail `0.296` < 1 < rigid `1.583`). The bench is **VALIDATED, not VOID.** (★DISCLOSED §3B calibration: the ≥50%-shell target met via the "justified equivalent" latitude — a near-buckling core cluster of 21 bonds producing the same aggregate crossing; a convergent broad ≥50% shell sits at the buckling knife-edge.)
- **STOP criteria on the grown state (`fixed_budget p_ref = 0.040`):** `grown_equilibrium_exists = True` — CG converged, `max_r|T| = 0.647 ≥ 0.489` (tension nonzero), `min k_shear,eff = 0.353 > 0` (bonds positive). Hoop-tension ε up to +0.139, radial-compression to −0.066.
- 🔴 **RETRACTED IN PART (see the 2026-07-26 repair note at the foot of this entry) — the bin label survives, two of its stated bases do not:** the "hoop-stiffen ⊕ radial-soften CANCEL" mechanism is REFUTED by ablation, and "Protocol E MEASURED `ρ_eff/ρ_0 = 0.916` → `r_Z = 0.520` … now with a MEASURED ρ" is RETRACTED (`ρ` is NOT measured; the shipped `r_Z = 0.5436` is the structural anchor at `ρ ≡ 1`). Original text: **HEADLINE BIN = UNDETERMINED** (cell `(i) × L1 × Z_str`). `K_ratio_lift = 0.997` (L1 < 1.2) — the grown hoop-stiffen and radial-soften CANCEL in the small-signal tangent (`K_tan/K_0 = 0.2955` grown vs control `0.2964`); amplitude gate **outcome (i) CLEAN** (`A_sign = 0.002`, converged+robust); **Protocol E MEASURED `ρ_eff/ρ_0 = 0.916` → `r_Z = 0.520`** (straddles 0.5, Z_str). The grown anisotropic vessel reads like its isotropic control — the SAME landing as merged #782's painted bench (BIN-4/UNDETERMINED at the `r_Z` edge), now with a MEASURED ρ.
- **yield_saturated has NO stable equilibrium** — it BUCKLES (peak-strain ceiling ≈ 0.21) before peak strain reaches `A_yield = 1.0`, across all `A_yield_scale ∈ {0.9,1.0,1.1}`. The energize-LOCK-adjacent NULL (the pre-test-physics-check answered NEGATIVE). Headline falls to `fixed_budget` per §9 Fork SEED.
- **Corroborators:** percolation `hoop_percolates = False` (patchy tension, corroborates SOFT/NULL); PAINTED-ANISOTROPIC `0.298` ≈ GROWN `0.296` (the near-null effect is anisotropy-driven, not provenance-driven); seed-independence stable (lift rel-spread `0.002`, r_Z `0.001`, no flip → not a seed-controlled UNDETERMINED); C-V `K(ε_bias)` nearly FLAT (`0.059 %` total variation, `|ASYMMETRY| = 0.40 ≥ 0.15` corroborative-only per the demoted clause); anti-seduction fence NOT triggered (`r_Z = 0.520` 🔴 superseded → `0.5436`, not matched-ish either way).
- **Deviations (§8):** T = remap term `k_a·ε_axial`; SELFTEST-(iii) justified-equivalent core cluster; Protocol E at `L = 32` only (frozen `{32,48}`, runtime-budget scoping, not verdict-controlling); yield_saturated fail-fast capped scan (Rule-10 runtime fix, no physics change); `p_ref = 0.040` cage-arm knob. Frozen-provenance gate: 0 gating on the result doc. `make verify` green.
- **Consequence routed to Grant** (this lane surfaces + routes only): UNDETERMINED at the `r_Z` straddle → the owed resolver is the feasible SUBC/periodic lower-bound bracket (#782 Fork B), NOT run here; the yield_saturated NULL banked (no rescue, Rule 12). Feeds the continuum radial-solver charter D5/R1 (#789).
- PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; branch `research/vessel-state-rve-bench` off `origin/main` @ `f050e33b`. No bench merge in this lane.

### REVISION (2026-07-22): PR#796 adversarial-review repairs (15 confirmed findings; PRE-MERGE, disclosed)

Repairs applied in the driver/result/JSON only — the frozen prereg is BYTE-UNTOUCHED. Frozen-provenance gate 0-gating; `make verify` + docket lint green. Grouped commits: compute-repairs / prose-repairs / bookkeeping.

- **F2 — Protocol E re-implemented per the frozen §4 (PML/window exclusion, density-peak top-K sampling, the FROZEN reflection-free window `t_end ≤ 0.9·(L/2−r_meas)/c_P`, a FROZEN two-monitor cross-correlation arrival criterion, a REAL reactance-pair flag) and RUN at BOTH `L∈{32,48}`.** FINDING: the ToF is **method-indeterminate on the grown scatterer** — the cold reference is Bloch-validated (rel `1.1 %` at L=32) but the grown cage-array returns an UNPHYSICAL `c_eff > c_0` (softer medium apparently faster) and the self-consistent iteration DIVERGES (`0.519→0.701→1.499`). The shipped clean measured `r_Z = 0.520` is **RETRACTED**; the bin rests on the structural anchor `r_Z = √K_ratio = 0.5436` (Z_str). **HEADLINE BIN LABEL UNCHANGED (UNDETERMINED); the RESULT is weaker — the ρ half is UNRESOLVED, not measured** (see the 2026-07-26 repair note below for the method band and the fallback grading). ToF-medium grow uses a disclosed capped solve (identical grown OP verified).
- **F6 — the "hoop-vs-radial CANCELLATION" mechanism RETRACTED.** The ablation decomposition (JSON-shipped): control `0.29637`, hoop_only `0.29696` (`+0.20 %`), radial_only `0.29764` (`+0.43 %`) — the radial-soften pattern RAISES the core tangent; both named components STIFFEN; `cancellation_mechanism_supported = False`. Replacement: the null lift is provenance/live-operator-driven (grown ≈ painted ≈ control).
- **F11/F3/F8 — yield_saturated re-graded from extrapolated to OBSERVED.** Extended scan past the p0=0.06 ceiling; OBSERVED buckling onset `p0 = 0.062`, `min k_shear,eff = −0.0028 ≤ 0` (physical `buckled`). Instrument-scoped (this σ, remap surrogate, cage geometry); yield UNREACHED with observed onset.
- **F9 — §8.4 corrected:** the fail-fast cap (outer≤8/inner≤800) applies UNCONDITIONALLY to every yield-scan solve; converged=False onset → (ii)-artifact per §2; capped-vs-uncapped confirmation at the onset (2× cap, `classification_unchanged = True`).
- **F1 — Leg-0 (iii) claim narrowed to fireability-in-extremis;** prose-only calibration numbers deleted / JSON-backed.
- **F4/F15 — calibration_disclosure regenerated to computed truth** (the frozen 1e-3 fired the artifact UNCHANGED, amp_spread 649; `%%` bug fixed).
- **F5/F13 — determinism upgraded to two-full-runs:** `determinism_digest` (SHA-256 over results minus timing); the in-process bit-compare disclosed as a proxy. **(Note: at checkpoint `16618974` the second full run had NOT yet been executed — this bullet was written ahead of the evidence. It was RUN on 2026-07-26; see the repair note below.)**
- **F10/F14 — VERDICT_SRC_SIGMA ledgered + a σ-variant arm (σ=2.5) run:** no verdict-relevant flip (bin UNDETERMINED both). Bloch cross-check (cold P/S ToF vs run_c2_speeds) now runs (rel 0.011/0.188). Grown-K_tan single-size caveat disclosed. Deviations §8 complete.
- **F7 — design-lesson recorded in result §9 (carry-forward, NO post-hoc re-banding):** the band placement made a non-UNDETERMINED bin unreachable for a null-lift vessel whose structural `r_Z` already sits inside the straddle; the follow-on SUBC-bracket prereg must band the SUBC/KUBC bounds as the discriminator so every outcome class has a reachable bin.
- Repaired result: `research/2026-07-22_vessel-state-rve_result.md` (dated REVISION header); regenerated JSON + white figure; runtime `1049 s`.

### REPAIR NOTE (2026-07-26): the repair lane's prose pass FINISHED — the ρ-side headline is retracted, not repaired

The PR#796 repair lane was interrupted mid-pass (usage limit) after its COMPUTE completed and was checkpointed at `16618974`; the result doc in that checkpoint was mid-edit and explicitly not the lane's final statement. This note closes the pass. The frozen prereg remained **BYTE-UNTOUCHED** throughout; the JSON was regenerated by the driver only (never hand-edited); every number in the result doc is read from the shipped `vessel_state_rve_results.json`.

- **★THE HEADLINE PHYSICS CHANGED, and the change is a DEMOTION.** The first-run claim that this bench delivers a MEASURED structural `ρ` — the bench's stated advance over merged #782 — **does not survive.** Protocol E, re-implemented per the frozen §4 and run at both `L∈{32,48}`, is **METHOD-UNSTABLE**: `r_Z` spans `[0.1005, 0.5436]` at `L=32` and `[0.1975, 0.5436]` at `L=48`, each band spanning `["Z_lo","Z_str"]`; `bins_by_L_from_launch_cP = {"32":"SOFT","48":"SOFT"}` while the structural anchor reads `Z_str`; `method_indeterminate_any_L = True`, `size_flip_L32_vs_L48 = False`. The shipped `r_Z = 0.5436` is the STRUCTURAL ANCHOR with `ρ_eff/ρ_0 ≡ 1` **assumed, not measured**. ⚠ The two bands are NOT like-for-like — the SC-coherent arm runs at the first box size only (`do_sc=(i == 0)`, `vessel_state_rve.py:1482`), so `L=48` is narrower because it contains one fewer method, NOT because the instrument stabilizes with size; disclosed in result §6 so no size-convergence trend is inferred that was never measured.
- **⇒ The walk-1 verdict is `UNDETERMINED` with its ρ half UNRESOLVED.** One of the two Z factors is measured (`K_tan/K_0 = 0.29548`); the other is not. **🔴 RETRACTED: "Protocol E MEASURED `ρ_eff/ρ_0 = 0.916` → `r_Z = 0.520`" and every "both Z factors measured" / "a REAL structural ρ term" framing** — in this entry, in the result doc, wherever it appears. On the ρ axis this bench delivers exactly #782's `r_Z(β=0) = √K_ratio` and nothing more.
- **The fallback is graded as a CHOICE, not as a measurement.** Shipped: walk the frozen bin table on the structural anchor ⇒ UNDETERMINED. Not shipped: bank SOFT from the launch@`c_P` arm alone — refused because that arm's own output falsifies it as a group speed (`c_eff = 0.7005 > c_0 = 0.5133`, and the SC continuation diverges `0.519→0.701→1.499`), and because SOFT is the outcome this lane has the most incentive to bank (it would re-open the Reading-B route). A disclosed assumption beats an unvalidated measurement; the data decides neither. Because the prereg pre-registered no fallback, the choice is post-hoc and is labelled as such.
- **Root cause = a FROZEN-PRECONDITION conflict, caught only at integrator time (Rule 10).** The frozen §4 requires `k·r_core ≪ 1`; the realizable pulse on `L∈{32,48}` at `φ_sf = 0.489` sits at `k·r_core ≈ 2–4`. The frozen box sizes and the frozen long-λ precondition are mutually unsatisfiable on a cage-array of this packing. **Prereg-design miss, not a coding defect in the repair; NOT repaired post-hoc.**
- **★OWED-2 routed to Grant, co-equal with the SUBC bracket (OWED-1).** Whether a decidable `ρ_eff` is obtainable at all on a grown cage-array (bigger box at fixed `r_core`? a static/quasi-static ρ estimator instead of a ToF? or retire the ρ-measurement and adopt `ρ ≡ 1` as the standing structural assumption?) is Grant's call, not this lane's. **A SUBC/KUBC bracket brackets `K`, not `ρ` — OWED-1 does not dispose of OWED-2.**
- **F6 (cancellation) — mechanism prose replaced everywhere by the ablation-backed statement, which is a NEGATIVE.** Control `0.29637`, full-painted `0.29824` (`+0.63 %`), hoop_only `0.29696` (`+0.20 %`), radial_only `0.29764` (`+0.43 %`); `radial_only_softens_core_tangent = False`, `cancellation_mechanism_supported = False`. **BOTH named components STIFFEN ⇒ the hoop-vs-radial CANCELLATION is REFUTED.**
- **⚠ NEW CONTRADICTION SURFACED (flag-don't-fix, needs Grant/auditor adjudication).** The driver's positive REPLACEMENT attribution conflicts with its own painted-ablation decision rule, both shipped in the same JSON: `ablation_decomposition_fixed_budget.attribution` says the null lift is *"provenance/live-operator-driven"*, while `painted_anisotropic_fixed_budget.note` states the rule *"GROWN≈PAINTED ⇒ anisotropy-driven"* — and the shipped numbers give grown `0.29548` vs painted `0.29824`, `Δ ≈ 0.93 %`, i.e. GROWN≈PAINTED. **The positive attribution is WITHDRAWN from the result doc's claims** (the computed flag `cancellation_mechanism_supported = False` is unaffected); the driver string is left byte-unchanged so the conflict stays visible in the data. What the bench supports: **not a cancellation; positive mechanism UNATTRIBUTED.**
- **F3/F8/F11 (yield NULL) — re-graded to the OBSERVED terminal state and instrument-scoped.** Highest stable `p0 = 0.06` (`peak_A_ceiling = 0.2087`); OBSERVED onset `p0 = 0.062` with `min k_shear,eff = −0.00278`, `peak_A = 0.2156`, `kind = "buckled"`; `yield_reached_before_buckling = False` at all three `A_yield_scale ∈ {0.9,1.0,1.1}`; capped-vs-uncapped at `2×` cap leaves `classification_unchanged = True`. **Scoped to THIS source family (`σ = 1.6`), THIS affine radiation-stress remap surrogate, THIS cage geometry (`φ_sf = 0.489`) — NOT an engine-level unreachability claim, and not a claim about the Op14 `S(A)` knee (not hosted here).**
- **F1** — the SELFTEST-(iii) validation claim is narrowed to **fireability-in-extremis** (a localized stretch-side curvature divergence at a near-buckling core cluster, dynamic range `0.002 ↔ 568` with nothing between); it does NOT establish threshold-scale detectability of graded compressive marginality. The prose-only calibration figures the driver never emitted are **deleted**, not re-derived.
- **F4/F15** — `calibration_disclosure` is generated by the driver from the same dict it describes (`vessel_state_rve.py:670–683`), so it cannot drift from computed truth; regenerate by re-running the driver, never hand-edit. The `%%` literal defect is gone.
- **F9/F14** — §8.4 cap mis-description corrected (the fail-fast cap applies UNCONDITIONALLY to every yield-scan solve); deviations §8 completed: Bloch cross-check RAN (`rel_P = 0.0105`, `rel_S = 0.1880`, both `≤ 0.20`; `rel_S` clears by a small margin only), `VERDICT_SRC_SIGMA = 1.6` on the engineering-choice ledger with a σ=2.5 variant arm returning `verdict_relevant_flip = False`, and the grown-`K_tan` **single-size** caveat (grown lift read at `L=16` only; the cold `L∈{12,16,20}` size gate does not transfer) disclosed.
- **F5/F13 (determinism) — the FROZEN criterion was RUN, not substituted.** The checkpoint had only a `determinism_digest` from a single run; the frozen criterion is *two independent full driver runs, `diff -q` clean*. The driver was therefore executed a SECOND time end-to-end (`--legs all`, separate process, separate `--out`; `1048.6 s` vs `1089.5 s`). **`diff -q` CLEAN**: timing-stripped payloads byte-identical (18 222 B each), `determinism_digest` identical (`69968227e455137f…`). Disclosed scope: the comparison excludes `_runtime_sec` (wall clock is not a result); a raw byte-diff of the two files would differ in that key alone. Side benefit — this re-verifies that the SHIPPED JSON reproduces exactly, so every number in the result doc is reproducible rather than one-shot.
- **Ledger tag correction:** the frozen ledger tagged `ρ_eff/ρ_0` and `r_Z` `[derived]` on the assumption Protocol E would measure `ρ`. As shipped, `ρ_eff/ρ_0 ≡ 1` is an **`[assumption]`** and `r_Z = 0.5436` is **MIXED-provenance** (`[derived]` in its `K` factor, `[assumption]` in its `ρ` factor). Downstream consumers — notably the continuum-radial-solver lane (#789 D5/R1) — must cite it as mixed, not as derived.
- **F7 design-lesson extended** with an EXECUTABILITY requirement for the follow-on SUBC prereg: freeze a precondition-satisfiability check, and **pre-register the fallback for a non-decisive measurement** — the gap that forced a post-hoc choice here. No post-hoc re-banding of this run.
- Scope of this note: driver/result/JSON/docket only. **Frozen prereg BYTE-UNTOUCHED; engine byte-untouched; no `clm-`/`def-` minted; no KB/tex leaf, ledger, port-register or falsification-ledger edit.** `make verify` + docket lint + frozen-provenance gate green. PR remains `[DO-NOT-MERGE]`, not retitled, not merged.
