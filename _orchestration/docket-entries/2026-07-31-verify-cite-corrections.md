### ENTRY 2026-07-31-verify-cite-corrections (2026-07-31): implementer — two dated corrections on merged docs: the registers-walk verify-cite FALSE NEGATIVE, and the graded-network v3 stamp

- **Class: honesty-lag propagation, NOT adjudication.** Both items propagate corpus state that was already established elsewhere; neither re-opens a physics question, moves a grade, or mints a claim. Both were surfaced by the #813 scoping pass and independently confirmed by that PR's 2026-07-31 compact audit. Original text is preserved at both sites per Rule 12 — git is the trail.

#### ITEM 1 — the 2026-07-13 verify-before-cite check was a FALSE NEGATIVE (wrong file), and its downgrade is WITHDRAWN

- **What the merged note said.** `research/2026-07-13_registers-walk_framing.md:147` (§6 SYM-GRAVITY WALK) carries: *"⚠ **Cite correction (verify-before-cite).** The walk pointed this carve at `CLAUDE.md:75`; that line did **not** verify — `CLAUDE.md:75` is the repo's *Pure-AVE-corpus rule* text, not the SYM/ASYM physics carve. So the carve is recorded here as **chat-record framing** … **pending a canonical carve leaf.**"*
- **Why it was wrong — two files named `CLAUDE.md`.** The 2026-07-13 check resolved the pointer against the **repo-root** `CLAUDE.md`, whose `:75`-region is the skill-registry / *Pure-AVE-corpus rule* area (that heading sits at `:78` at HEAD 512e1ef4, so even the region has drifted). The walk's pointer meant the KB's own conventions file, **`manuscript/ave-kb/CLAUDE.md:75`**.
- **The carve IS canon (verified verbatim at HEAD, not trusted).** `manuscript/ave-kb/CLAUDE.md:75` sits under **INVARIANT-S2** (*AVE Axiom numbering*, heading at `:66`) as the *"Operating-point state and small-signal modulation"* paragraph, and states the carve under the **W6 clarification 2026-06-05** (basis `research/2026-06-05_gravity-sign-frequency-modulation-result.md`), quoted with the source's own emphasis intact: *"this* symmetric *both-sectors-scale form ($S_\varepsilon = S_\mu = S$, so $Z = Z_0\sqrt{\mu_0 S/\varepsilon_0 S} = Z_0$ invariant → reflectionless) is the **SYMMETRIC-loading operating point**"* … *"A **static-E-only drive is ASYMMETRIC**: a static field has no $\partial\mathbf{B}/\partial t$ to load the $\mu$ / microrotational (Cosserat-B) sector, so it loads the $\varepsilon$ / capacitive sector only ($S_\varepsilon < 1$, $S_\mu = 1$)"* … *"$Z$ **changes**, so the boundary reflects ($\Gamma \neq 0$)"*. Programmatically string-matched against line 75 of that file, not eyeballed.
- **Second, independent consumer of the same path.** `manuscript/ave-kb/vol4/circuit-theory/ch1-vacuum-circuit-analysis/graded-network-response.md:137-138` cites *"INVARIANT-S2 operating-point, `manuscript/ave-kb/CLAUDE.md`:75"* (wrapped across the two source lines), and that leaf's derived-vs-asserted ledger at `:337` grades the SYM row **DERIVED** on the basis *"INVARIANT-S2 W6"*. A KB leaf was already building on the carve as canon while the research note called it chat-record.
- **Repair (Rule 12, appended after `:147`, original ⚠ note untouched).** A dated `✅ CORRECTION TO THE CORRECTION (2026-07-31)` block states the wrong-file diagnosis, quotes the canonical text, names the second consumer, and **WITHDRAWS** the *"chat-record framing"* downgrade and the *"pending a canonical carve leaf"* qualifier. **Explicitly unaffected and said so in the block:** (i) the note's FRAMING-NOT-DERIVATION quarantine header still governs — this restores a *cite*, it does not canonize the §6 walk; (ii) the ⚠ note's two supporting anchors (`src/ave/core/constants.py:385` for the $\nu = 2/7$ both-moduli grade; `vacuum-birefringence-e4.md:108` for the common-mode blindness) still verify and remain corroborating, not load-bearing.
- **Sweep for other consumers of the false negative — two-method (`rg` + `git grep`), corpus-wide: exactly ONE site, the note itself.** Patterns `chat-record framing` and `pending a canonical carve` each return `research/2026-07-13_registers-walk_framing.md:147` and nothing else. No downstream doc adopted the downgrade, so there is no propagation debt beyond this repair. (The §6 heading string *"chat-record landing here"* is a different phrase and is not the downgrade.)
- **Cite-shift repair caused by this insert (the #809 R1 lesson).** The block adds 8 lines above `:147` (285 → 293). A two-method scan for cites into this file found **two live tokens above the insertion point**, both at `research/2026-07-14_tij-x44b_CHARTER.md:250` (Flag F5): `:191 → :199` and `:211 → :219`, remapped through a `difflib` opcode map from `origin/main` and content-confirmed at the new positions. All other cites into this file (`:7`, `:60-67`, `:82`, `:120-134`, `:129`, `:132`) sit below `:147` and are unshifted — re-verified, not assumed.
- **★ Disclosed, not fixed: `:211`/`:219` was ALREADY mis-targeted at `origin/main`.** The charter cites it for Grant's balance≡yield conjecture, but `origin/main:211` resolves to *"- **photon = envelope on the linear carrier** — pure **real** power, **no stored potential**"* (the carrier-wave frame), not the conjecture. The conjecture is at `:191`/`:199` (continuation item a.4). Shifted **faithfully** rather than silently re-pointed; surfaced here for the orchestrator to route.

#### ITEM 2 — the two headline birefringence-coefficient rows in `graded-network-response.md` stamped to v3

- **The three cites verified at HEAD first, none trusted from the brief.** `manuscript/ave-kb/vol4/falsification/ch12-falsifiable-predictions/vacuum-birefringence-e4.md:104` carries the boxed v3 ratio $\frac{\delta n_{AVE}}{\delta n_{QED}} = \frac{15\pi}{4\alpha^2} = \frac{3.75\pi}{\alpha^2} \approx 2.2\times10^5$ on the consistent instantaneous footing; `:106` is the convention-history italic — *"v1 $=7.5/\alpha^3\approx1.93\times10^7$ paired the instantaneous SVE kernel against the AVE-side differenced $(3/45)\alpha^2$; v2 $=7.5\pi/\alpha^2\approx4.42\times10^5$ … v3 is exactly half v2"*; `:112` is the chord-vs-echo split — *"the **MAGNITUDE** $2.2\times10^5=3.75\pi/\alpha^2$ (v3; the v1 $1.93\times10^7=7.5/\alpha^3$ is convention history) is an **$\alpha$-echo** … Do not headline the magnitude as a chord."*
- **What was stale.** Two **headline-class table rows** in the merged leaf still carried the v1 form inline with no per-row version stamp: `graded-network-response.md:326` (§6 network-prediction summary, row 6, *"Birefringence COEFFICIENT $\delta n_{AVE}/\delta n_{QED}=7.5/\alpha^3\approx1.93\times10^7$ | **MIXED**"*) and `:341` (§7 derived-vs-asserted ledger, *"Birefringence-coefficient MAGNITUDE $1.93\times10^7=7.5/\alpha^3$ | **$\alpha$-ECHO** (value)"*).
- **Repair (Rule 12, in place, no line shift).** Both rows keep the v1 form **struck, not deleted** (`~~…~~`), gain the bold v3 value and a dated `v3 stamp 2026-07-31`, and point at `vacuum-birefringence-e4.md:104` / `:106` / `:112` via the leaf's own relative link form. **No grade moves:** row 6 stays **MIXED**, the ledger row stays **$\alpha$-ECHO** — the magnitude simply rides $\alpha^{-2}$ instead of $\alpha^{-3}$, which is the point the ECHO grade was always making. File line count unchanged (362 → 362), so this item shifts no cites anywhere.
- **★ MATERIAL QUALIFIER, surfaced not smoothed over: the leaf was NOT undisclosed.** `graded-network-response.md:8-17` already carries a file-level Rule-12 banner whose scope sentence reads *"Every "$7.5/\alpha^3\approx1.93\times10^7$" birefringence-ratio below carries an understated QED denominator … **Corrected (v3 headline, single instantaneous footing — OPTION-B re-freeze 2026-07-07): $3.75\pi/\alpha^2\approx2.2\times10^5$**"*. So the rows were **banner-covered at file level and un-stamped only at row level.** This item closes the row-level gap because summary and ledger table rows are the excerpt-prone surface a reader lands on directly; it does **not** assert the leaf was hiding the correction, and the brief's "no v3 stamp" characterization is true only at row granularity.
- **Corpus sweep, two-method (`rg` over the worktree + `git grep` over tracked files), patterns `7.5/alpha^3` / `7.5/α³` / `1.93e7` / `1.93\times10^7` / `frac{7.5}{\alpha^3}`, data + results + `.json(l)` excluded: 209 hits in 54 files.** Classified:

| Class | Files / hits | Disposition |
|---|---|---|
| **manuscript/ (live canon)** | 16 / 58 | **All 16 already carry a file-level 🔴 QED-NORMALIZATION CORRECTION banner (or are the canonical v3 leaf / already-v3 `.tex`).** Zero unbannered manuscript files. Only the two row-level headline gaps in `graded-network-response.md` (`:326`, `:341`) were un-stamped inline — corrected here. Body-prose instance `:223` left as banner-covered per Rule 12 (it is an in-argument sentence, not a headline row). |
| **research/ (dated records)** | 18 / 92 | **Disclosed, not edited** — dated result / prereg / ruling / note docs are frozen records of what was believed on their date; several already carry their own dated banners (`2026-07-03_birefringence-*`, `2026-06-21_…bankable-falsifier`, `2026-06-24_forward-prediction-register`). |
| **_orchestration/ (dated arc docs)** | 3 / 6 | **Disclosed, not edited** — `2026-06-22_birefringence-vca-bench-arc.md:15,55`, `2026-06-23_testing-infra-gate-charter.md:3,62,82`, `2026-07-04_p1-lint-sweep-findings.md:13`. |
| **papers/** | 1 / 3 | **Already correct** — `2026_birefringence_letter/provenance.md:121,168` states the current value and marks the v1 as *"Was `7.5/α³~1.93e7`"*. |
| **src/ (code + tests)** | 16 / 50 | **Mixed; NOT touched by this lane.** One live un-stamped API surface flagged below. |

- **★ FLAG-DON'T-FIX — the one live, un-stamped, exported code surface (routed, not repaired here).** `src/ave/bench/birefringence.py:375-388` does the right thing: `coefficient_ratio_differential()` carries *"==== SUPERSEDED (2026-07-03) — this returns the WRONG-NORMALIZATION 7.5/alpha^3 … do NOT use its value in any prediction. ===="*. Its sibling in the QED module does **not**: `src/ave/qed/birefringence.py:112-132` `chord_magnitude_ratio()` returns the same superseded v1 quantity with a docstring that presents it as *the* magnitude ratio — *"= \frac{45/6}{\alpha^3} = \frac{7.5}{\alpha^3} \approx 1.93\times10^7"* (`:120`), *"Returns: The field-independent ratio 7.5/α³ ≈ 1.93e7."* (`:130`) — **no supersession stamp**, and it is re-exported in the public API (`src/ave/qed/__init__.py:34,52`, module docstring `:26`) and pinned by `src/tests/test_grqed_stage2_qed_extension.py:250-253` (`assert np.isclose(ratio, 1.93e7, rtol=2e-2)`). Same quantity, two modules, one stamped and one not. **Not repaired in this lane** — touching a public API docstring plus its pinning assertions is a src-lane change with test consequences, outside a two-doc honesty-lag correction, and the honest options (stamp-only vs deprecate vs re-point) are an adjudication this lane has no mandate to make. Routed to the orchestrator.
- **Second, smaller src flag (disclosed, not fixed):** `src/ave/bench/adopters.py:185` names its spec source as `"birefringence.py:328 coefficient_ratio_differential"`, but that function is at `:375` at HEAD — a drifted line-cite inside a `frozen=True` adopter record (`:181`), whose `discriminating_outcomes` POSITIVE bin at `:191` still reads `"ratio ~1.93e7"`. Frozen-record class: disclosed by the sweep, left as frozen.
- **★ Pre-existing stale cite found in the rows being stamped — disclosed, deliberately NOT repaired.** Both `:326` and `:341` cite `claim-quality.md:399,405` for the self-adjudicated ECHO grade (and `:215`/`:339` in the same leaf cite `:391`/`:431`). Verified at HEAD: `manuscript/ave-kb/vol4/claim-quality.md:391` and `:405` are **blank lines**, `:399` is a YBCO phased-array lift bullet, `:431` is an unrelated non-claim caveat. The content those cites want is the `clm-pp3qwf` block at `:452+`, with the chord-vs-echo sentence at `:486`. This is a `origin/main` cite-drift cluster this lane did not create; per flag-don't-fix it is surfaced with the target rather than silently re-pointed inside an edit whose mandate was the v3 stamp.

#### Battery (2026-07-31, at branch HEAD)

- `make verify` exit **0** (baseline on `origin/main` before any edit: also **0**) — *"[Verify] ALL PHYSICS PROTOCOLS PASSED."*
- `make refresh-kb-metadata` **idempotent** — two consecutive runs write 0 files and leave a clean tree: 0 subtree-claims, 0 solidity lines, 0 depends-on annotations, 0 leaf-reference footers, `0 file(s) written under manuscript/ave-kb/.index/ (6 unchanged)`.
- `verify-md-links` **gating errors: 0** (206 warn-only, 42 broken-inter in warn mode; kbleaf 1092 cites checked, gating 0, 1 waived) — unchanged from baseline.
- `verify-docket-keys.py` exit **0** — 81 entries / 79 unique keys (80 / 78 at baseline: this fragment's key `2026-07-31-verify-cite-corrections` is new and unique), *"no new duplicate keys"*.
- **Pure-corpus scan** over the full branch diff + this fragment + all commit messages: **0 hits**.
- **Zero file overlap** with both open PRs at check time — `#813 research/anisotropy-observable-scoping` and `#814 research/qlaw-framing-walk` — verified by `comm -12` on the changed-file sets (both empty). This branch's set is 4 files: `research/2026-07-13_registers-walk_framing.md`, `research/2026-07-14_tij-x44b_CHARTER.md`, `manuscript/ave-kb/vol4/circuit-theory/ch1-vacuum-circuit-analysis/graded-network-response.md`, and this fragment.
- **No engine/driver code touched**, so no re-run of any numeric receipt is owed by this lane; the one src finding is routed, not acted on.
