### ENTRY 2026-07-30-b1-retraction-propagation (2026-07-30): implementer — B1 sub-2% Kerr-Q retraction propagated to the lagging sites (honesty-lag)

- **Class: discharged-decision propagation, not adjudication.** Grant Ruling B1 (2026-07-21) retracted the "sub-2%" Kerr-$Q$ match — it was computed against the corrupt $\omega_R$/$\omega_I$ Kerr reference tables + source-frame masses (#774), the same MATCH-ARTIFACT behind the retracted $-0.45\%$/$-0.47\%$ ringdown figures. The ruling landed at `manuscript/ave-kb/vol3/claim-quality.md:204` (clm-395gps, the model banner) and in the load-bearing leaf `…/ave-merger-ringdown-eigenvalue.md § GRANT RULING B1` (solidity 0.55, `use as input only`, disclosed-phenomenological, *"NOT a zero-free-parameter benchmark"*). **Six sites never received it.** No new physics; no grade moved.
- **Corrected picture carried to every repaired site:** the topological **flat $Q = \ell = 2$ is scoped to the $a_* = 0$ cold anchor** (the $\Omega \to 0$ limit of the m$\Omega$ law) and would fail at $\bar D_Q = -38\%$ at catalog spins (corrected-Kerr $Q$ rises $3.07 \to 3.49$); the **spin-refined m$\Omega$ law** under the standing v1 mapping lands at $-5.44\%$ (Resultbox) / $-4.57\%$ (ZAMO) — an **OPEN near-miss tension**, the named next ringdown work (the #808 scoping lane).
- **Sites repaired (6, Rule 12 strike-don't-delete, vol3:204 model shape):** `vol2/appendices/app-f-solver-toolchain/kerr-q-correction.md:37` + its $Q_{AVE}$/$Q_{GR}$ table `:39-45`; `common/solver-toolchain.md:116` + table `:118-124`; `vol2/claim-quality.md:1097` (clm-d9ivj1); `common/claim-quality.md:136` (clm-m7qd0w); `manuscript/backmatter/05_universal_solver_toolchain.tex:101` + table `:107-115`; `src/ave/solvers/orbital_resonance.py:485`. The three Q-tables are **preserved, bannered as computed-against-the-corrupt-reference** — including explicit inheritance disclosure for the per-row errors and the "$\sim\!40\%$ at $a_* = 0.99$" divergence sentences derived from them.
- **Two sites were NOT in the four named to the lane** — both found only by the exhaustive multi-pattern + two-method sweep (the single-pattern lesson, live again): the printed backmatter `.tex:101` (third member of the KB mirror trio — repairing only the two KB sides would have manufactured the KB↔manuscript divergence the honesty-lag program closes) and the engine comment `orbital_resonance.py:485`, which writes the claim as *"Q error < 2%"* and therefore does not match a `sub-2` grep at all.
- **Scope carve held (no over-retraction):** only the ACCURACY claim is retracted. The m$\Omega$ law $\omega_I = (\omega_R - m\Omega)/(2\ell)$ and the superradiance limit at $\omega_R = m\Omega$ are B1's standing **Q-LAW COMMITMENT** and are explicitly retained at every site. Zero executable change in `src/` (comment-only).
- **Verified-not-touched:** `vol3/claim-quality.md:204` (the model), `…/ave-merger-ringdown-eigenvalue.md:127` (already struck 2026-07-20), the #808 scoping branch, and the frozen prereg `research/2026-07-20_v1-spin-mapping-adjudication_prereg-FROZEN.md`. `manuscript/vol_3_macroscopic/chapters/15_black_hole_orbital_resonance.tex` (`:27/:271/:292/:387`) is **already bannered** by its section-head *"Number correction (2026-07-21 … Grant Ruling B1)"* note, with per-occurrence reconciliation explicitly routed to the manuscript-reconciliation program at `:270`/`:289` — left as routed.
- **Flag-don't-fix residue routed to the auditor lane (surfaced, not edited):** (i) the grade-field text of both claim cards still quotes the corrupt-table range — `vol2/claim-quality.md` rationale + strengthen-by ("$\sim\!40\%$ extremal Kerr"), `common/claim-quality.md` rationale; that is grade-owner territory. (ii) **Q=ℓ scoping-lag**, a DIFFERENT question from the sub-2% retraction and adjacent to B1's Q-LAW COMMITMENT: `vol3/cosmology/ch15-black-hole-orbitals/lattice-extreme-bh-rationality.md:46` rows *"QNM quality factor | $Q = \ell$ | GR QNM | match | CONSISTENCY"* with no $a_*=0$ scoping, and `src/ave/axioms/scale_invariant.py:422-444` + `src/ave/core/universal_operators.py:934-946` present $Q = \ell$ as universal with a "BH QNM 2 2 Fundamental GW ringdown" row, likewise unscoped.
- **Battery:** `make verify` exit 0; `make refresh-kb-metadata` **zero derived-field drift** (0 solidity lines, 0 `.index/` files rewritten — confirming prose-level propagation, not re-grading) and idempotent on re-run; `verify-docket-keys` no new duplicate keys; `pdflatex -draftmode vol_0_engineering_compendium/main.tex` RC=0 with zero `^!` errors; `pytest src/tests/test_cross_domain_operators.py` 15 passed.
