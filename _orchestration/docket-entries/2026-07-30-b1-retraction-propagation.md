### ENTRY 2026-07-30-b1-retraction-propagation (2026-07-30): implementer — B1 sub-2% Kerr-Q retraction propagated to the lagging sites (honesty-lag)

- **Class: discharged-decision propagation, not adjudication.** Grant Ruling B1 (2026-07-21) retracted the "sub-2%" Kerr-$Q$ match — it was computed against the corrupt $\omega_R$/$\omega_I$ Kerr reference tables + source-frame masses (#774), the same MATCH-ARTIFACT behind the retracted $-0.45\%$/$-0.47\%$ ringdown figures. The ruling landed at `manuscript/ave-kb/vol3/claim-quality.md:204` (clm-395gps, the model banner) and in the load-bearing leaf `…/ave-merger-ringdown-eigenvalue.md § GRANT RULING B1` (solidity 0.55, `use as input only`, disclosed-phenomenological, *"NOT a zero-free-parameter benchmark"*). **Six sites never received it.** No new physics; no grade moved.
- **Corrected picture carried to every repaired site:** the topological **flat $Q = \ell = 2$ is scoped to the $a_* = 0$ cold anchor** (the $\Omega \to 0$ limit of the m$\Omega$ law) and would fail at $\bar D_Q = -38\%$ at catalog spins (corrected-Kerr $Q$ rises $3.07 \to 3.49$); the **spin-refined m$\Omega$ law** under the standing v1 mapping lands at $-5.44\%$ (Resultbox) / $-4.57\%$ (ZAMO) — an **OPEN near-miss tension**, the named next ringdown work (the #808 scoping lane).
- **Sites repaired (6, Rule 12 strike-don't-delete, vol3:204 model shape):** `vol2/appendices/app-f-solver-toolchain/kerr-q-correction.md:37` + its $Q_{AVE}$/$Q_{GR}$ table `:39-45`; `common/solver-toolchain.md:116` + table `:118-124`; `vol2/claim-quality.md:1097` (clm-d9ivj1); `common/claim-quality.md:136` (clm-m7qd0w); `manuscript/backmatter/05_universal_solver_toolchain.tex:101` + table `:107-115`; `src/ave/solvers/orbital_resonance.py:485`. The three Q-tables are **preserved, bannered as computed-against-the-corrupt-reference** — including explicit inheritance disclosure for the per-row errors and the "$\sim\!40\%$ at $a_* = 0.99$" divergence sentences derived from them.
- **Two sites were NOT in the four named to the lane** — both found only by the exhaustive multi-pattern + two-method sweep (the single-pattern lesson, live again): the printed backmatter `.tex:101` (third member of the KB mirror trio — repairing only the two KB sides would have manufactured the KB↔manuscript divergence the honesty-lag program closes) and the engine comment `orbital_resonance.py:485`, which writes the claim as *"Q error < 2%"* and therefore does not match a `sub-2` grep at all.
- **Scope carve held (no over-retraction):** only the ACCURACY claim is retracted. The m$\Omega$ law $\omega_I = (\omega_R - m\Omega)/(2\ell)$ and the superradiance limit at $\omega_R = m\Omega$ are B1's standing **Q-LAW COMMITMENT** and are explicitly retained at every site. Zero executable change in `src/` (comment-only).
- **Verified-not-touched:** `vol3/claim-quality.md:204` (the model), `…/ave-merger-ringdown-eigenvalue.md:127` (already struck 2026-07-20), the #808 scoping branch, and the frozen prereg `research/2026-07-20_v1-spin-mapping-adjudication_prereg-FROZEN.md`. `manuscript/vol_3_macroscopic/chapters/15_black_hole_orbital_resonance.tex` (`:27/:271/:292/:387`) is **already bannered** by its section-head *"Number correction (2026-07-21 … Grant Ruling B1)"* note, with per-occurrence reconciliation explicitly routed to the manuscript-reconciliation program at `:270`/`:289` — left as routed.
- **Flag-don't-fix residue routed to the auditor lane (surfaced, not edited):** (i) the grade-field text of both claim cards still quotes the corrupt-table range — `vol2/claim-quality.md` rationale + strengthen-by ("$\sim\!40\%$ extremal Kerr"), `common/claim-quality.md` rationale; that is grade-owner territory. (ii) **Q=ℓ scoping-lag**, a DIFFERENT question from the sub-2% retraction and adjacent to B1's Q-LAW COMMITMENT: `vol3/cosmology/ch15-black-hole-orbitals/lattice-extreme-bh-rationality.md:46` rows *"QNM quality factor | $Q = \ell$ | GR QNM | match | CONSISTENCY"* with no $a_*=0$ scoping, and `src/ave/axioms/scale_invariant.py:422-444` + `src/ave/core/universal_operators.py:934-946` present $Q = \ell$ as universal with a "BH QNM 2 2 Fundamental GW ringdown" row, likewise unscoped.
- **Battery:** `make verify` exit 0; `make refresh-kb-metadata` **zero derived-field drift** (0 solidity lines, 0 `.index/` files rewritten — confirming prose-level propagation, not re-grading) and idempotent on re-run; `verify-docket-keys` no new duplicate keys; `pdflatex -draftmode vol_0_engineering_compendium/main.tex` RC=0 with zero `^!` errors; `pytest src/tests/test_cross_domain_operators.py` 15 passed.

#### RULINGS ADDENDUM 2026-07-30 — three Grant rulings executed (A / B / C); entry body above PRESERVED per Rule 12  <!-- rule12-freeze: base=e3804bdece2870f9cc61df0907f4b237a92768e8 region=above offset=0 lines=11 bytes=4653 sha256=741e21e7ca48d45aadec7b73d50ccedbfb0cb2266070683b2b7aac7dca49cd4f -->

- **Provenance of this addendum.** Grant delivered three in-chat rulings on 2026-07-30 discharging the three items this lane and the #808 scoping lane had left open. Rulings quoted **verbatim [sic]** below. Executed as three commits on the follow-on branch `docs/b1-grant-rulings-2026-07-30`. **⚠ Premise correction:** the dispatch assumed PR #809 was still OPEN at `[REVIEW: CLEARED]` with the branch live at `24e58508`. It was not — **#809 MERGED 2026-07-31T05:04:41Z** (merge commit `afcd5617`) and the remote branch `docs/b1-retraction-propagation` was **deleted** (`git ls-remote origin 'refs/heads/docs/b1*'` empty). Follow-on commits cannot attach to a merged PR, so the work was branched off `origin/main` — which already contains all of #809 — and #809 was neither retitled nor re-opened.

- **RULING A — Grant verbatim [sic]: "soften".** The four "first-principles prediction of superradiance from pure lattice geometry" sites. This was the ONE item #809 and #808 did **not** reconcile: #809 deliberately RETAINED them under its scope carve; the #808 scoping doc flagged them (§1.7) as an underivation of the same shape as the retracted "zero free parameters" and routed the two readings — (i) structure survives, (ii) needs the same rider — to Grant. **Grant picked (ii): a provenance rider on four sites, not a retraction.** Sites (grep-located at head): `vol2/appendices/app-f-solver-toolchain/kerr-q-correction.md:33` · `common/solver-toolchain.md:113` · `manuscript/backmatter/05_universal_solver_toolchain.tex:98` (tex-native `%`-comment preservation per the file's `:101` precedent) · `vol2/claim-quality.md:1097` (the clause #809 left deliberately unstruck). **Retained, not weakened:** the $\omega_R = m\Omega$ threshold, the $\omega_I \to 0$ / $Q \to \infty$ limit, and the m$\Omega$ law are B1's standing **Q-LAW COMMITMENT**. New wording: the threshold is a **structural consequence of the adopted m$\Omega$ law**, which is disclosed-phenomenological, solidity 0.55 (`ave-merger-ringdown-eigenvalue.md:72-76`) — **not** a first-principles lattice derivation; the law is grafted on as a Park-transform decomposition, *analogised, not derived* (#808 scoping §2.5 / Route R4). Commit `af492da9`.

- **RULING B — Grant verbatim [sic]: "follow rec".** Option (i) on #809's open (b)-row call: extend the existing dated Vol 3 Ch 15 chapter-head note **now** rather than hold the block. `manuscript/vol_3_macroscopic/chapters/15_black_hole_orbital_resonance.tex:31` extended with two printed sentences naming the "matches GR sub-2% for $a_* = 0.3$–$0.8$" validation-scope retraction (Ruling B1, 2026-07-21; corrupt reference tables, #774; truth-source `vol3/claim-quality.md:204`), and stating that the four printed occurrences (`:26-27`, `:271`, `:292`, `:387`) are **left standing per Rule 12, pending per-occurrence reconciliation — #780 routing stands**. Interim printed flag only; the reconciliation program still owns the rewrites. Commit `8326dfe3`.

- **🔴 RULE-12 CORRECTION to this entry's own body (above).** The "Verified-not-touched" bullet asserts that `15_black_hole_orbital_resonance.tex` (`:27/:271/:292/:387`) "is **already bannered** by its section-head *Number correction* note, with per-occurrence reconciliation explicitly routed … at `:270`/`:289` — left as routed." **That coverage claim was FALSE and is corrected here** (it was already corrected in #809's PR body as audit Finding 2, but never in this fragment). Verified at head before Ruling B: the `:31` note contained **no** `sub-2%` string and **no** `$a_* = 0.3$–$0.8$` spin-range text — it covered only the $-0.45\%$/$-0.47\%$ MATCH-ARTIFACT and the v1↔v2 re-selection; and `:270`/`:289` both begin with `%`, i.e. **non-printing LaTeX comments**, so their pointer never reached the page. The compiled Vol 3 was still asserting the retracted validation scope. Ruling B is the fix. Original bullet preserved above per Rule 12.  <!-- rule12-freeze: base=e3804bdece2870f9cc61df0907f4b237a92768e8 region=above offset=0 lines=7 bytes=3000 sha256=eacca83cb9c492c9718aa94b805198f502926f5f9b58c4dd76120641a2cb06fd -->

- **RULING C — Grant verbatim [sic]: "follow rec"; bucket = this retraction.** Discharges this entry's own flag-don't-fix item *"⚠ Needs Grant's bucket ruling — `vol2/claim-quality.md:1101`"*. The clause *", only that it is consistent with the observed quality factors"* (clm-d9ivj1, Specific Non-Claims bullet 2) is an **accuracy assertion** that B1's $\bar D_Q = -38\%$ at catalog spins directly contradicts. Struck per Rule 12 (original preserved inside the strike), sentence continued *"— and the consistency-with-observed-$Q$ reading holds only at the $a_* = 0$ cold anchor"*, dated 🔴 banner added. **Bucket ruled: accuracy-class (this retraction), NOT the #808 $Q = \ell$ scoping class.** The ~22-site unscoped-$Q = \ell$ CLASS remains #808's lane, untouched. Commit `4590a6e5`.

- **Cite-shift sweep re-run (the #809 R1 lesson).** Ruling A's banners shifted three files: `kerr-q-correction.md` **+17** past `:33`, `common/solver-toolchain.md` **+17** past `:113`, `backmatter/05` **+11** past `:98`. `vol2/claim-quality.md` and the Vol 3 chapter are **delta 0** (inline edits). Two methods, eight LIVE cites repaired, every anchor verified **both directions**: `translation-circuit.md:762` bm05 `:145-161`→`:156-172` · `translation-circuit.md:765` `:426`→`:437` · `proton-identification.md:95` solver-toolchain `line 257`→`line 274` · `vol5/common/translation-protein-solver.md:27` + `vol5/common/translation-protein.md:34` `:504`→`:521` · `src/scripts/vol_1_foundations/r10_path_alpha_v6_unknot_chair_ring.py:11` bm05 `:327`→`:338` · `…v7_helical_beltrami_chair_ring.py:{41,42}` `:173-181`→`:184-192`, `:306-327`→`:317-338`. `src/` change is **comment-only**, zero executable delta. Unaffected (target precedes insertion): `substrate-native-terminology.md:47`→`solver-toolchain.md:72`; `ave-bh-horizon-area-theorem.md:77`→`solver-toolchain.md line 51`. Dated-record class, **not** rewritten per the #809 precedent: `research/2026-07-30_qlaw-derivation_scoping.md:{135,139}`, `_orchestration/2026-05-28_parameter-count-framing-walkback.md:382`, `_orchestration/2026-06-07_electron-synthesis-epic.md:153`, the `research/_archive/L3_electron_soliton/` set, and `src/scripts/_archive/r10_path_alpha_v{6,7}*.py` (the frozen-driver class-boundary item #809 flagged, still open).

- **Cite-drift surfaced, not fixed (verify-before-cite).** #809's PR body quotes the Ruling-C clause as *"**consistent with the observed quality factors**"* (bold). At head the clause carried **no** bold markers. Words right, emphasis markup not in the file — flagged for the orchestrator; no file changed on that account.

- **Battery (all three commits):** `make verify` exit 0 · `make refresh-kb-metadata` **zero derived-field drift** (0 subtree-claims / 0 solidity lines / 0 depends-on / 0 leaf-reference footers / 0 `.index/` files) and **idempotent** on re-run · `verify-md-links` gating **0** (kbleaf 1085 cites checked, gating 0, 1 pre-existing waiver) · `verify-anchor-content` 1279 checked / 939 drift — **byte-identical to the HEAD baseline**, zero new entries naming any edited file · `verify-docket-keys` no new duplicate keys · `pdflatex -draftmode` **vol_0** RC=0 zero `^!` errors AND **vol_3** clean two-pass RC=0 zero `^!` errors (Over/Underfull 164→168, warn-class reflow only) · pure-corpus clean. **Model files still zero-diff vs `origin/main`:** `vol3/claim-quality.md`, `ave-merger-ringdown-eigenvalue.md`.
