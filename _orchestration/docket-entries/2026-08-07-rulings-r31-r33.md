# Rulings — signed-Γ batch: spelling, the magnitude class, the instrument (2026-08-07)

### ENTRY 2026-08-07-rulings-r31-r33

Core-orchestrator record; Grant's words on the #923 decision package.

## R31 — §3 call-site spelling: RATIFIED as `\gammaundeclared{}`

**Grant, verbatim:** *"Ratify."* §3 froze the macro NAME; the call-site spelling is
corrected by a DATED SPEC-CORRECTION FRAGMENT (the frozen spec body untouched)
carrying: the pdflatex measurement (bare form gobbles the following space at 136/205
sites, worst case `Γ = −1wall`; the executed `{}` form is glyph-position-identical,
argument-free, grep-countable), the two measured-and-rejected alternatives
(`\gammaundeclared\ ` injects a spurious space at the 69 punctuation-adjacent sites;
`xspace` adds a package dependency), and the §3 counting fix — `git grep -c` counts
LINES (191), the discharge workstream inherits the `-o` occurrence form (205).

## R32 — the 15 `|Γ| = 1` sites: FOURTH FINDING CLASS, no channel debt

**Grant, verbatim:** *"Agree."* Unsigned-magnitude assertions are channel-independent
by construction — Ax3 forces |Γ| = 1; only SIGNED Γ requires the
plane/projection/profile declarations — so the signed-channel-declaration debt does
NOT attach. The 15 sites land as their own named class (MAGNITUDE-ASSERTION) with
the site list, in the census record.

## R33 — `classify_sign`: CENSUS-SCRIPT FIX

**Grant, verbatim:** *"Agree."* The spec's §1 adjacency definition stands; the
instrument diverges (`_VALUE_RE.search` takes the first value anywhere later on the
line — one site read its "adjacent" value from 410 characters away). Fix the script
with a MUTATION RECEIPT: the repaired classifier reproduces all 33 findings AND a
planted far-value case is demonstrably no longer mis-adjacent (force the fix off →
the receipt goes MISSED). The fix is documented in the same dated correction
fragment as R31.
