### ENTRY 2026-07-26-d1-sector-scoping-bank (2026-07-26): implementer — the D1 sector-and-inertia scoping banked (homeless orchestration findings given a corpus home)

- **The gap closed.** The 2026-07-21/26 D1 / effective-mass scoping analysis lived **only in an orchestration conversation** — tracked in no file — while the live sector-of-storage walk and charter decision **D1** both depend on it. Banked as `research/2026-07-26_d1-sector-and-inertia-route_scoping.md`, **class SCOPING (pre-lane), ORCHESTRATION-LANE output, NOT adversarially reviewed, prototype/analytic-grade**. Mints no `clm-`/`def-`, banks no verdict, ratifies nothing, engine byte-untouched. Same class + disclaimer set as `research/2026-07-21_beta-tracking-feasibility_scoping.md`.
- **§2 — the `m*` route is DECORATION (negative).** The semiconductor `m* = ħ²/(d²E/dk²)` is INTRA-band (dresses the same carrier whose dispersion is read); D1's loading is INTER-object (a bound cage loading a *different* propagating carrier). Correct home = the locally-resonant metamaterial `ρ_eff(ω) = ρ_0[1 + θ·ω_0²/(ω_0²−ω²)]`, whose `ω→0` limit **is** the corpus's `ρ_eff/ρ_0 = 1 + β·φ`. At `k→0` the resonant factor → 1, `ω_0` divides out, and `ρ_eff(0) = ρ_0(1+θ)` carries **only the bare-mass `θ` — no sector information**. So the literal curvature read **cannot** answer D1 and must not be headlined as if it could (it would read the structural term while being presented as the trapped-energy one). Does **not** resolve the standing `m*` OPEN FLAG at `translation-tables/translation-circuit.md`:306.
- **§3 — where the sector signature lives.** The avoided-crossing `ω_0 ≈ π·c_sector/r_core`. At `r_core ≈ 1.6`: `ω_0^A1 ≈ 1.02` vs `ω_0^S ≈ 0.56`, separation `c_P/c_S = 1.81` — cleanly resolvable. Observable is the **dimensionless** `ω_0·r_core/c_P` (α-clean; regime-portable since sector identity does not run with `r_core`). Feasible as an `L = 24–32` near-field BOUND-mode read (evades the `#775` sponge-vs-wavelength gap — no far-field radiation, no giant sponge) or, cheapest, an **eigensolve of the caged sub-block**. ★Honest fence: it reads which sector hosts the cage stiffness mode, **NOT** whether `E_trapped` converts to inertia at that rate (β-scoping §3 absence-1). Corroborating structural input, not a magnitude.
- **§4 — the virial consistency lever (the shape change).** For a standing cavity mode, `E_trapped/c_sector² = ρ_0·k_mode²·⟨u²⟩·V` — a **speed-independent geometric added mass**. Requiring the I5 import to equal the added mass the dressed dispersion independently fixes (`ρ_0·θ·V`) cancels `c_sector²` against the imported `c²` **iff `c² = c_sector²`**; any other pick leaves a dangling `(c_sector/c_chosen)²`. **⇒ D1 collapses from a free 3-way pick to one physical question: WHICH SECTOR STORES `E_trapped`.** Two caveats stated: (i) it **consumes** the I5 import, does not certify it; (ii) it is **vacuous** if `participation` is allowed to absorb a speed ratio rather than being the pure kinematic C-kin fraction (`clm-hu1jjw`'s unconditional half).
- **§5 — Eötvös/MICROSCOPE class-eliminator.** Sector-split inertia (`m_i = Σ E_sector/c_sector²`) is composition-dependent; the AVE spread is large (`c_P²/c_S² = 3.29`, 229%). Order-of-magnitude `Δ(m_g/m_i) ~ 2.29·Δf` → `~2e-3` (nuclear) / `~2e-5` (EM) vs the corpus-carried bound `~1e-15` (`translation-circuit.md`:156) — **10–12 orders over**. **The branch "sector-split inertia WITH sector-blind gravity" is DECISIVELY DEAD.** Two survivors: (a) universality, (b) a fully shared per-sector ledger — and the corpus **already derives (b)** (`ponderomotive-equivalence.md`:30 `clm-rd9cjm`, `m_i` cancels in `F=ma`; `2026-07-11_nordtvedt-eta_result.md`:28–29 single-`T₀₀^total` η=0 entailment, gravitating charge = A1-dilatation :71–73), read with that doc's own install-tautology caveat (:229–231). Consensus-bias lens applied explicitly: GR passes Eötvös by construction via the same identity, so a shared-ledger cancellation is legitimate physics, not an AVE excuse. Eötvös eliminates a class; it does **not** discriminate between sector assignments.
- **★§6 — the upstream contradiction surfaced (flag-don't-fix, NOT reconciled).** `master-equation.md`:20 (canon, **Grant-ratified**, 2026-06-10) — the A1 breather, *"`mₑc²` = trapped acoustic compression energy"* — vs `relative-offset-principle.md`:49 (`clm-m5swh9`, leaf status **PROPOSED**) — *"The trapped energy is `T2`/swing-class energy sitting inside the `A1` compression carrier's budget"*. **These cannot both name the store.** Provenance recorded: the T2 label entered via the orchestrator's Fork-ρ walk framing (`2026-07-21_fork-rho-walk_RECORD.md`); the A1 statement is older ratified canon — and the charter's I8 row currently propagates the walk's label. **Status OPEN — Grant is walking it.** Grant's in-chat framing recorded verbatim `[sic]`: *"is this just ac vs dc analysis?"*; the orchestration-lane response (*2026-07-28 fold: restated substrate-natively in §6 — the store is the oscillating DOF per canon's own assignments (master-equation:20 A1 breather + the LC-tank Compton-frequency identification), with `ℏω` demoted from premise to consistency note;* the original chain read: `mₑc² = ℏω_Compton` ⇒ rest-energy store is AC by construction, while the `(2,3)` winding is a STATIC Link texture = DC per the #416 two-natured ruling ⇒ oscillation store in A1 ⇒ `c² = c_P²` consistency-forced via §4) is labelled **ORCHESTRATION-LANE, NOT RATIFIED, NOT REVIEWED — the walk's current position, not a result**. Honest remainder also recorded: a static Cosserat twist **does** carry elastic energy, so the honest form may be a **DECOMPOSITION** (AC oscillation store ⊕ static-texture self-energy), measurable in principle by the §3 eigensolve.
- **§7 — routing.** (a) What CLOSES D1: nothing on the lattice — §4 closes its **structure**, reducing it to sector-of-storage, whose answer is a **corpus adjudication (Grant)**. (b) What CONSTRAINS it: §5 (class-eliminator) + the §3 eigensolve (corroborating structural read). (c) DECORATION: the literal `m*` curvature read. Best-plausible and honest-most-likely outcomes stated **separately** (the latter: a two-term AC⊕DC decomposition that sharpens rather than closes the question). Routed: the §6 contradiction → **Grant** (the next unlanded `2026-07-20_pending-rulings-and-frontier-queue.md` §1 slot); the §3 eigensolve → **fireable on his word** (needs its own prereg); charter **D1 HELD**, build stays blocked.
- **★Cite-verification receipt (two-method at `c8ceacc3`, `git grep` on `origin/main` + `grep -n` on the worktree file).** All corpus cites resolved and are tabulated in the doc's §8 — **except two, recorded as FAILED and deliberately not repeated as anchors**: (1) **`def-mstar1` is NOT on `main`** (0 hits both methods; `vocabulary-register.md` carries no `m*` row) — cited by path with no line number, with the live `translation-circuit.md`:306 flag pointed at instead; (2) **"pending-rulings item 13" is NOT on `main`** — §1 of that doc ends at item 12 — recorded as "the next unlanded §1 slot", never as a citable anchor. §8 also separates every number into **re-derived-here** (`c_P/c_S = 1.8147`; `ω_0^A1 = 1.0191`; `ω_0^S = 0.5616`; `c_P²/c_S² = 3.2931`; 229.3% spread; 12.4 / 10.4 orders over the WEP bound) vs **carried-from-the-orchestration-lane** (`r_core ≈ 1.6` within the cited 1.6–2.2 band; the external `Δf_sector ~ 1e-3` / `~1e-5` binding fractions — the §5 argument's softest input; the `L=24–32` sizing and eigensolve cost extrapolated from the β-scoping cost table, not timed; the metamaterial `ρ_eff(ω)` form itself, an external import).

- *(2026-07-26 repair pass, per the #800 audit: the §6 provenance flag re-worded to the observable (the cite is bound to the orthogonality clause; the storage assignment is not engaged) and STRENGTHENED — a third instance of the same cite pattern found at `relative-offset-principle.md`:41, plus leaf:49's own internal juxtaposition ("trapped energy is T2" and "mass is A1 dilatation" in one bullet, the second clause added at the leaf and absent from RECORD:55) as self-evidencing support. The dichotomy now engages `master-equation.md`:20's own closing clause — "two objects, not one" — which is canon's own seed of the decomposition reading. Propagation re-described near-verbatim; charter link pointed at `:15` (D1) rather than the `:54` I8 row; external/extrapolated inputs tagged inline at point of use; the `c_P/c_S` 1.8147-vs-source-1.813 discrepancy footnoted.)*
