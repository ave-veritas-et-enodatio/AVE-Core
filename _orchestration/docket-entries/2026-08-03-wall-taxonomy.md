### ENTRY 2026-08-03-wall-taxonomy (2026-08-03): canonization — the WALL TAXONOMY leaf ($\Gamma=\pm1$ level-sets of DC operating-point fields, indexed by phase-space axis); zero ids minted, two open flags carried, one new flag surfaced

- **Provenance.** Grant GO 2026-08-03, verbatim `[sic]`: *"Should the wall table go into KB?"* — fired on the orchestrator's canonization proposal and treated as ratification of placement on the **paperclip precedent** (`electron-plumbing-primer.md` Step 3.5, 2026-08-02). Three Grant framings carried **verbatim `[sic]` under his attribution** in §1: (1) *"what are all the "walls" AVE predicts? and what do they tell us? (phase space boundary conditions right?"*; (2) *"is this just the DC op point? literally a "standing wave" wall? so no energy can interact with it?"*; (3) *"There can still be a gradient toward the wall vs the wall itself"*. Everything else in §§2–4 and §7 is labelled **orchestrator walk-level refinement, NOT canon**, in separately-headed sections — the Step-3.5 attribution split copied exactly.
- **Class.** **ORGANIZING TAXONOMY · CONSISTENCY / PEDAGOGY-CLASS · NO-CLAIM leaf** · implementer-lane · NOT adversarially reviewed · mints **zero** `clm-`/`def-`/`exp-`/`sup-`/`ilk-` ids · moves **zero** solidities · ratifies nothing · **engine byte-untouched** (no solver, no driver, no numeric produced anywhere in the lane). Per `ave-discrimination-check`: **no chord claim appears anywhere**, and the scope note says so at the top of the leaf. Base `origin/main` @ `66fc7e69`; all cites verified at ship tip, not at brief time.
- **Files.** `manuscript/ave-kb/common/wall-taxonomy.md` (new leaf), `manuscript/ave-kb/common/index.md` (one index row), this docket fragment. **No other file touched** — zero edits to any cited leaf, zero `claim-quality.md` edits, zero `.index/` edits, zero manuscript-TeX edits.
- **Placement + naming rationale.** `manuscript/ave-kb/common/` because the rows span vol1 (regimes, axioms), vol2 (electroweak, nuclear), vol3 (gravity, cosmology, dark sector) and vol4 (circuit theory) — no single volume owns it. Name `wall-taxonomy.md`: (i) the `-taxonomy` suffix is house-native (`dark-back-reaction-taxonomy.md`, `dual-reactance-storage-taxonomy.md`); (ii) **"wall" is the corpus's own ratified word** for the $\lvert\Gamma\rvert=1$ surface (`envelope-anatomy.md`:32; `vocabulary-register.md`:404, Grant Ruling 8); (iii) **"boundary" was deliberately rejected** — it is already owned by `boundary-observables-m-q-j.md` and by that leaf's four **boundary-REGISTER** discipline, and using it here would invite precisely the conflation canon forbids.
- **★ THE REGISTER FENCE — the single most important thing in the leaf, and it cuts against the walk's own framing.** The walk's organizing line was *"every AVE wall = the same law in a different coordinate."* **Canon has already ruled against the strong reading of that**, verbatim (`boundary-observables-m-q-j.md`:63): *"the electron TIR wall (amplitude level-set) and the Schwarzschild horizon (coordinate radius) are the **same observability object** … sitting on **different boundary registers**; do not read 'same $\Gamma = -1$ saturation surface' as 'same boundary-setting mechanism.'"* The leaf therefore licenses the unification **at the $\Gamma$-FORM level only** and states in §2.2, in the leaf body, that the axis column is a routing aid layered on canon's register column, which is *"the one with the physics in it."* The closing synthesis (§7) re-applies the fence to itself.
- **★ NEW FLAG SURFACED (flag-don't-fix; routed, NOT adjudicated).** Canon's **four** boundary registers (amplitude level-set / pressure-balance locus / coordinate radius / balance locus, `boundary-observables-m-q-j.md`:57–63) were enumerated over a table of **spatial** boundaries. **Rows 4–6 of this taxonomy — the lattice band edge, the Cosserat mass gap, the evanescent $\ell_c$ cutoff — are cutoff-class conditions on the FREQUENCY and LENGTH-SCALE axes and fit none of the four.** Either the register list needs a fifth (cutoff / dispersion) register, or those rows are outside its intended scope. Both readings surfaced; **not picked.** Corroborating datum quoted in the leaf: canon's own P5 row tags the band-edge case **"emergent-configurational (band position)"** rather than as any of the four (`port-register.md`:73). → **auditor / Grant.**
- **The two pre-existing open flags, carried prominently in §6 rather than footnoted.**
  1. **Row 1's wave-horizon / singular-gradient-skin rider** — marked **WALK-LEVEL, provenance 2026-08-03**. The coordinate-free argument (the ODE's singular point sits where $G_{shear}(A)=0$, a **substrate** condition, so it cannot be transformed away) is recorded as **the lean, explicitly flagged not adjudicated**. **The compactification-swap discriminator has NOT been run** and coordinate-artifact-vs-physical is **not excluded**. Decisive status receipt quoted: `research/2026-08-03_coldq-pole-v2.1_result.md`:17, verbatim — *"**Certification: `SOLVER-NOT-CERTIFIED`. Frozen precedence therefore fires `BIN-F-SOLVER`, and NO physics bin is adjudicated.**"* **Two-method ABSENCE receipt included in the leaf:** the walk's *"root-exponential"* vocabulary has **zero hits on `origin/main`** and resolves only on the **unmerged** branch `research/coldq-pole-v2p3`, in a **FROZEN PRE-REGISTRATION** (`2026-08-03_coldq-pole-v2.4-root_prereg-FROZEN.md`) — i.e. a lane that has not measured yet. It is cited **nowhere** in the leaf body.
  2. **Row 6's two-$\ell_c$ collision** — carried with the in-code verbatim (`src/ave/core/constants.py`:331–337, *"two-objects-one-symbol — flag-don't-fix … ~6 orders of magnitude apart and a different physical referent. **Surfaced for auditor adjudication** … not silently merged"*), a side-by-side referent table (lattice $\sqrt6\,\ell_{node}\approx9.46\times10^{-13}$ m at `constants.py`:338 vs weak-scale $\approx10^{-18}$ m at `gauge-boson-masses.md`:39), and **why it is ledger-load-bearing rather than naming-only**: $\gamma_c$ does double duty for the weak range **and** the B-mode mass gap under canon's own simultaneous-falsification clause (`delta-strain-cosmic-tcc.md`:47) — which wires **rows 5 and 6 of this very table to the same primitive**. Row 6 uses the weak-scale referent because that is what its anchor uses, and says so rather than picking a winner. Convergence noted with the twist-ledger arc's flag 3, which reached the same load-bearing reading independently.
- **★ FOUR WALK-MEMORY CORRECTIONS — corpus followed, walk corrected, recorded in the leaf's §8.2 rather than silently applied.**
  1. *"Crossing $V_{yield}$ = Miller avalanche"* → **Miller multiplication $M = 1/S^2$ is the Regime-III APPROACH; the wall at $r_3=1$ is Regime IV, $M\to\infty$, topology destroyed** (`four-regimes.md`:58). Avalanche is the ramp; the wall is the asymptote. Row 3 states it that way. *(The row gained a genuinely strong anchor in the process: `four-regimes.md`:60 identifies $r_3 = 1.0$ **with** $V_{yield}=\sqrt\alpha V_{snap}\approx43.65$ kV and states Grant's two-sided reading in canon's own words — sub-threshold reflects at $\Gamma=-1$ into matter, super-threshold ruptures topology, *"Same Axiom 4 saturation, two operational faces."*)*
  2. *"Cite the **ratified** cold-Q circuit map site"* for the $0.41\lambda$ taper → **there is no ratified site.** It lives only in `research/2026-07-31_qlaw-framing-challenge_walk.md`, which self-tags *"🟡 WALK MATERIAL — this document exists to be walked with Grant, **not to be believed**"* (`:5`) and *"**NOT a claim.** No claim-id is minted"* (`:15`). **Row 8 is re-tagged 🟡 NOT CANON in the row itself.**
  3. The *"$Q \approx 2$ instead of $\infty$"* figure → **omitted, not quoted.** The ringdown $Q$ is precisely what the uncertified lane was to determine and it determined nothing. Per the no-magnitude-beyond-cited-canon fence.
  4. `V_YIELD` at `constants.py:382` → **actual home is `:505`**; `:382` is inside the native-units preamble. ⚑ **Surfaced, NOT fixed (other lane's file):** `manuscript/ave-kb/common/dual-reactance-storage-taxonomy.md` §"the collision" carries **two stale line-anchors** — row 1 cites `constants.py:382` for `V_yield` (actual `:505`) and row 2 cites `:373` for `V_snap` (actual `:496`). → **auditor**, as a correction-PR item; this lane edited nothing.
- **Grant's gradient-vs-wall split — and the finding that canon already carried it.** §3 shows Grant's *"gradient toward the wall vs the wall itself"* is the general form of a carve the corpus had already made for the single particle: `vocabulary-register.md`:404's **three radial surfaces** (wall / balance shell / knee-dress edge), with the reflectivity contrast stated outright at `envelope-anatomy.md`:40 — *"The reflection off the knee is **small** ($\lvert\Gamma\rvert \ll 1$, the near-matched sub-yield regime — distinct from the wall's $\lvert\Gamma\rvert = 1$)."* **Recorded as convergence, not as new structure.** The walk-level part (shoaling via Op16 $c=c_0\sqrt S$; skin-as-reactive-stub; skin-as-where-nonlinearity-concentrates) is fenced in §3.2 against citation as evidence.
- **Interaction ledger at a $\Gamma=-1$ wall (answering Grant's *"so no energy can interact with it?"*).** Zero transmission, **zero absorption**, **MAXIMAL** reflection contact — a wall is maximally interacting and completely impenetrable at once. ⚑ The zero-absorption row is explicitly disclosed as sitting on **one side of a STANDING OPEN FORK**: the same canonical site supplying *"ideal saturation dissipates nothing — it is a **lossless refusal**"* (`envelope-anatomy.md`:101) records in the same paragraph that the standing manuscript models the near-yield channel as **dissipative/thixotropic/memristive**, and tags it *"a standing open fork … Grant leans reversible; the fork stays open."* **The leaf does not pick the branch and gains nothing from either outcome.**
- **Battery.** `make verify` **exit 0**. `verify-md-links` **0 gating hits** on both new files. `make refresh-kb-metadata` **idempotent** (clean `git status` on `manuscript/ave-kb/` after the run). `verify-docket-keys` exit 0. **Cite receipts: 48 distinct anchors, 48 PASS, 0 FAIL**, byte-compared worktree-vs-`origin/main` — and the sweep was run **by machine over the rendered leaf** (regex-extracted, not hand-listed) specifically so the receipt count cannot drift from the text. One **two-method ABSENCE** receipt with its command shown (`root-exponential`, zero hits on `origin/main`). **Pure-corpus.**
- **Collision fence.** Checked against open branches at fresh tips: the cold-Q v2.4 lane runs on `research/coldq-pole-v2p3` (and `research/coldq-pole-v2p2`); **zero file overlap** — this lane touches only `manuscript/ave-kb/common/wall-taxonomy.md` (new), `manuscript/ave-kb/common/index.md` (one added row), and this docket fragment.
- **Recorded post-merge follow-ons (NOT performed here — other lanes' files).** (a) back-references **from** `envelope-anatomy.md` and `boundary-observables-m-q-j.md` to this leaf; (b) back-reference from the cold-Q arc documents; (c) the auditor's disposition of the new fifth-register flag; (d) the auditor's correction of the two stale `dual-reactance-storage-taxonomy.md` line-anchors.
