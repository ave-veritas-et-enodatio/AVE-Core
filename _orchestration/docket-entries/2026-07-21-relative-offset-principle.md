### ENTRY 2026-07-21-relative-offset-principle (2026-07-21): implementer — relative-offset principle (Fork-ρ direction closure) + walk RECORD

- **Fork ρ closed at the DIRECTION level.** The 2026-07-21 walk (following the `#782` RVE-aggregation-bench landing) ruled Fork ρ (*is trapped-energy mass acoustic inertia, or gravitating-only?*) toward **Reading A (MATCHED)** via the **relative-offset principle**: in a phase-only substrate there is no spatial frame beneath the node graph, so a localized excitation is a pattern on nodes that cannot hold station against the material displacement of its host nodes (the anti-advection reductio) — trapped-energy patterns participate in a carrier's oscillatory material motion, hence load `ρ_eff`. Grant-ratified at the direction level in-chat (verbatim `[sic]`: "yes the relative offset makes sense, and seemsmlike a major concept for the cannon to adjudicate and define"). The "surf" reading was retracted (material velocity, not phase velocity; sway, not transport).
- **New canon leaf (PROPOSED, Grant-ratification-at-merge):** `manuscript/ave-kb/common/relative-offset-principle.md` — the principle statement, three corollaries (A: discrete pinning delivers material tracking not station-keeping; B: the mechanism-side of no-aether-drag; C: the Fork-ρ direction), the anti-advection impossibility argument, honest DIRECTION-not-MAGNITUDE scope. Claims minted: **`clm-hu1jjw`** (principle + Fork-ρ direction — walk-ratified-direction, pending adversarial review, solidity 0.50) and **`clm-m5swh9`** (the loaded-inertia MAGNITUDE `β`/`M`/sector-crossed `c²` — OPEN, `*pending*`, gated on the β feasibility lane).
- **Walk RECORD:** `research/2026-07-21_fork-rho-walk_RECORD.md` (the sequence: fork statement → surf-retraction → Grant's frame question `[sic]` → the relative-offset argument → Grant's ruling `[sic]` → direction-closure/open-magnitude split → β-lane routing → the `r_Z`-floor consequence flag).
- **Consequence flag (flag-don't-fix):** the `#782`-banked `r_Z` straddle (`0.40–0.57`, bare-node ρ) becomes a **FLOOR** under the direction ruling — `r_Z` rises with `ρ_eff` loading toward the `[0.39, 0.89]` band; the bench's `K_eff` numbers and the BIN-4 verdict are untouched (BIN-4 is `β`-invariant). The #782 verdict is not re-opened.
- **Deferred / routed:** the `def-` vocabulary-register row for "the relative offset" (Grant's handle, proposed-for-ratification) is **DEFERRED** to a post-#781 batch (register-row conflict-risk) — NOT minted in this PR. The MAGNITUDE is routed to the **β feasibility lane** (un-fired). A flag-don't-fix mini-sweep surfaced candidate revisit sites where corpus prose reads as absolute real-space station-keeping (`genesis-chord-falsification-ledger.md:144`; `motion-stability-bemf-longitudinal`; `annihilation-evaporation` TRANSPORT-ABSENT) — routed to auditor/Grant, no leaf touched.
- **Docket-fragment note:** written as a per-lane fragment (news-fragments convention) rather than a monolith append; the convention's README lives on the unmerged `infra/docket-fragments` branch, so this PR introduces the `docket-entries/` directory with this single fragment (no README — that is the convention branch's file).
