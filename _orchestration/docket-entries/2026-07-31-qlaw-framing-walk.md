### ENTRY 2026-07-31-qlaw-framing-walk (2026-07-31): implementer — Q-law ADVERSARIAL FRAMING CHALLENGE + vacuum-circuit mapping (pre-derivation walk material); nothing minted, nothing landed

- **Scope fence, stated first.** This lane is **framing-challenge + circuit-mapping walk material, not derivation.** No solver was run, no `Q` was computed, no engine file was touched, no claim was minted, no solidity was changed, **no corpus file was modified**, no bin was frozen, and **no row was landed in `translation-circuit.md`**. Deliverable: [`research/2026-07-31_qlaw-framing-challenge_walk.md`](../../research/2026-07-31_qlaw-framing-challenge_walk.md). **Provenance:** Grant's conditional yes on the cold-Q derivation, 2026-07-31, verbatim `[sic]`: *"Yes but challenge all framing, assumptions, and map to the vacuum circuits and physical lattice walk first"*. Written against `origin/main` = `512e1ef4` (#810–#812 merged). Upstream: [`research/2026-07-30_qlaw-derivation_scoping.md`](../../research/2026-07-30_qlaw-derivation_scoping.md) (#808) — **challenged, not extended**; its F1–F9 stand as written and this lane's findings use a separate CF-1…CF-16 numbering space so the two never collide.
- **★ THE LOAD-BEARING RESULT (CF-5/CF-6) — the standing `√S`-vs-`S^¼` flag is a CHANNEL fork, not an exponent fork.** *(Headline trimmed 2026-07-31, F3: the original continued "…**and the shear channel has no exponent freedom left**" — that clause is **withdrawn**; see the over-count correction below. The channel-fork reclassification stands.)* Fired `ave-ee-first-mapping` and went to the constitutive pair: a transmission line is specified by `L` and `C` per unit length **and nothing else**; `Z = √(L/C)` and `c = 1/√(LC)` are **outputs**. Canon states `Z(S)`, `c(S)` **and** a `Z↔c` relation — **three statements for two degrees of freedom**, and that over-determination *is* the flag. **⚠ OVER-COUNT CORRECTED 2026-07-31 (repair pass, PR #814 audit F3):** `Z = ρc` is an **algebraic identity of the chosen mechanical dictionary** — zero constraints, the means-test PASS that licenses the mapping, and a means-test pass cannot double as evidence of over-determination; and `C_eff = C₀/S` is **disowned by this document's own residual box** (`nonlinear-vacuum-capacitance.md:14` calls it the *longitudinal-A1* bond compliance, *"NOT the transverse dielectric capacitance"*). Honest shear ledger: **ONE canonical constraint** (`c_shear = c₀√S`, Op16 `operators.md:56`) **+ one dictionary identity + one open DOF (which ρ — FORK-3)**. One constraint pins the product `LC` and leaves the ratio `L/C` free, so *"zero freedom left over"* is **withdrawn**, as is *"every statement lands in exactly three branches"*. A **fourth (LENSING) branch** is added — `k4-tlm-lensing-validation.md:22` (`Z₀/√S`) with the canonical lensing slow-down `c₀√S` ⇒ `L₀/S`, `C₀` — and it shares ASYM's `Z` while differing in `c`, so `Z(S)` alone does not identify a branch. **Canon already owns the two-parameter family:** `operators.md:54` verbatim *"Asymmetric Meissner case: `Z_eff = Z₀·√(S_μ/S_ε)` … different observable when `S_μ ≠ S_ε`"* — the branches are **points in a 2-DOF family canon carries**, so the `(L,C)` move is a re-notation, not a new DOF (weaker as discovery, stronger as receipt). **CF-5 and CF-7 are in unacknowledged tension and it is now stated:** FORK-3(b)'s `ρ_eff = ρ₀/S³` gives `Z ∝ S^(−5/2) → ∞` **with the same `c = c₀√S`**, i.e. it satisfies the one constraint — CF-5 and CF-7 are two halves of one under-determination. *(The original text of this bullet follows, preserved per Rule 12; read it against the correction above — "exactly three branches" is now **four**, and they are points in a family, not a partition.)* Declaring `(L,C)` instead, every canonical impedance/speed statement in the corpus falls into exactly **three branches, each internally exact**: **SYM** (`L₀S`, `C₀S` ⇒ `Z=Z₀` invariant, `c=c₀/S`) per `manuscript/ave-kb/claim-quality.md:111` — the branch canon explicitly assigns to *"gravity, BH interior, particle confinement"*; **ASYM** (`L₀`, `C₀S` ⇒ `Z=Z₀/√S → ∞` open, `c=c₀/√S`) per `claim-quality.md:112` — **this is Op14's `Z_eff = Z₀/√S`, `common/operators.md:54`**; and **SHEAR/T2** (`L₀ ↔ ρ`, `C₀/S ↔ 1/G_shear` ⇒ `Z=Z_sh,0√S → 0` short, `c=c₀√S`) per `vol3/claim-quality.md:122` + `operators.md` Op16 + `saturating-modulus-and-backreaction.md:60`. **`S^¼` appears in none of the three.** It arises only via `c_local = c₀√(Z₀/Z_local)` (`k4-tlm-lensing-validation.md:30-33`, an already-open auditor flag), and solving for the pair that relation demands gives **`L = L₀S^(−3/4)` (inductance RISING) and `C = C₀S^(1/4)` (capacitance FALLING)** — quarter- and three-quarter powers found nowhere in canon *(**leg 1**, the real deliverable, unchanged)*, with `C` moving **opposite** to the canonical Ax-4 varactor `C_eff = C₀/S` *(**leg 2**, **hedged 2026-07-31 (F3)** — it compares against the longitudinal-A1 compliance this document disowns for the shear channel, so it is a directional observation, not an argument; leg 1 does not depend on it)*. In both fixed-`L` branches the correct relation is **`c/c₀ = Z/Z₀` exactly** (SYM does not satisfy it, correctly, because SYM moves `L` too — a useful branch signature). **Class: arithmetic-consistency observation on canonical constitutive statements. Flag-don't-fix — surfaced with both paths and verbatim text, ZERO files modified, routed to Grant for ratification. It is NOT canon until he rules**, and the derivation lane is told to carry `√S` primary **with the `S^¼` counterfactual recorded** (KEEP-BOTH), not to pick silently. **CF-16 — ⚠ DOWNGRADED 2026-07-31 (F4) from "independent corroboration" to "a THIRD INSTANCE of the same drift":** `op14-local-clock-modulation.md:13`'s own chain **consumes the same Op16 input** (it derives `ω_local` from `c_shear = c₀√S` via *"`ω_local ∝ c_eff`"*), so agreeing with Op16 after substituting Op16 is not a second route. Still worth carrying as a third instance of the failure class. Canon already made this correction elsewhere — `op14-local-clock-modulation.md:13` (2026-06-22) verbatim *"The earlier `(1−A²)^(1/2)` exponent … was off by a factor of 2 in the exponent and is now corrected"*, and the corrected `ω_local ∝ √S` is **exactly** what the SHEAR branch's `ω = 1/√(LC)` gives. **That correction did not propagate:** `op14-cosmic-horizon-profile.md:22,:43` still carry the superseded `√(1−A²)` while `:41` cites the very line that retracts it — and `:47` of the same leaf carries the **corrected** form. **One leaf, both exponents, four lines apart.** Not repaired here (cosmology leaf, out of this lane's scope); routed to the auditor lane. **Third receipt, from the engine itself — ⚠ and one of its three clauses is now STALE (F4; span also corrected `:419-424` → `:419-423`, since `:424` is code not comment):** the FLAG contrasts *"`operators.md:54` (Z0/√S) vs `k4-tlm-lensing-validation.md:22` (Z0/S^{1/4})"*, **but `k4-tlm:22` was corrected 2026-07-14 and now reads `Z₀/√S`** (KEEP-BOTH banner at `:25-29`, which even cites `cosserat_field_3d.py:425` as the authority) — so code and leaf now agree and the comment still calls them conflicting. The FLAG's *first* clause (in-file OPEN-vs-SHORT coexistence) remains live. **Engine source-comment repair routed to a separate lane; out of this branch's two-file scope.** `src/ave/topological/cosserat_field_3d.py:419-423` carries a verbatim FLAG — *"⚑ FLAG (2nd-impedance conflict, task #12, NOT resolved here): this OPEN Z=Z0/√S coexists with the live-wall SHORT Z=Z0·√(S_μ/S_ε) in this SAME file, and with operators.md:54 (Z0/√S) vs k4-tlm-lensing-validation.md:22 (Z0/S^{1/4}). The exponent/sign reconciliation across Op14 forms is a separate physics-review item"*.
- **★ CF-1/CF-2/CF-3/CF-4 — the canonical five-step chain reads `ω_R` and `Q` off TWO DIFFERENT CIRCLES, and computes the rim frequency with the cold far-field speed at the radius where canon puts the shear speed at exactly zero.** Verbatim, same file, adjacent steps (`vol2/appendices/app-f-solver-toolchain/regime-eigenvalue-method.md`): `:52` `r_eff = r_sat/(1+ν_vac) = 49/9 M_g ≈ 5.444 M_g`; `:55` *"The ℓ = 2 tangential standing wave **at r_eff**"*; `:63` *"The mode orbits tangentially **at r_sat** with ℓ wavelengths fitting around the circumference … the curvature radiation loss per cycle scales as 1/ℓ"*. **`ω_R` comes from the 5.444 M_g circle; `Q = ℓ` comes from the mode-counting picture on the 7 M_g circle.** If ℓ wavelengths genuinely fit around `2πr_sat`, `ω_R M_g = 2/7 = 0.2857`, not `18/49 = 0.3673` (−22%, and `(1+ν_vac)` disappears). **CF-2:** `r_eff = 5.444 M_g` sits **22% inside** `r_sat = 7 M_g`, i.e. inside Regime IV where `electron-bh-isomorphism.md` says verbatim GWs *"**cannot propagate**"*. **CF-3 (the diagnosis that unifies A1/A5/A6):** Step 4 uses `c = c₀`, the **unsaturated** speed, while `saturating-modulus…:60` gives `c_shear = c₀√S → 0` and `op14-local-clock-modulation.md:13` gives `ω_local → 0` at the same place — **evaluated with canonical LOCAL quantities the rim mode's frequency is zero, not 18/49. The standing eigenvalue is a cold-cavity hard-wall far-field-speed calculation wearing saturated-wall language**; every piece of Op14/Ax-4 content the corpus owns is absent from both `18/49` and `Q = ℓ`. **🔴 CF-4 — RETRACTED 2026-07-31 (repair pass, PR #814 audit F1); original preserved in the deliverable per Rule 12, slot NOT refilled.** CF-4 originally read: *"taking Step 5's geometry and Step 4's frequency together gives a tangential phase speed `v_φ = c₀(1+ν_vac) = 1.286 c₀` — and a passive line cannot exceed `1/√(LC)` in its own medium."* **That is a uniform-TEM statement applied to the AZIMUTHAL phase velocity of a spherical multipole, which exceeds `c₀` above cutoff by definition** (no energy moves superluminally; the radial group velocity carries the power). `k·r_eff = ℓ` **is** the ℓ-th mode's radiation cutoff (CF-8), so `k·r_sat = ℓ(1+ν_vac) = 2.571` and `v_φ(r_sat) = (9/7)c₀`, while `v_φ(r_eff) = c₀` **exactly**. **The 1.286 is not a defect — it IS `r_sat/r_eff`, the factor by which the physical wall sits above the mode's own cutoff.** Canon itself carries superluminal phase velocities at `manuscript/ave-kb/claim-quality.md:111-112` (`c_EM,sym = c₀/S → ∞`, "EM phase velocity rises"; `c_EM,asym = c₀/√S → ∞`) — quoted in this document's own CF-5 table, so the objection deleted two of its own three branches. **CF-1/CF-2 correspondingly DOWNGRADED** from "inconsistency" to *"the chain never states which reading — cutoff radius or physical wall — it intends"*; under the multipole reading the two radii are **one mode described twice**, and both steelmen stand unchanged. **FORK-4 re-posed** (below). **Decision-relevant arithmetic (AUDITOR-ARITHMETIC provenance, spin-1 vector-multipole estimator):** Yaghjian–Best `Q_Z(ℓ=2)` = **1.962** at `ka=2` (cutoff) vs **0.965** at `ka=2.571` (wall) vs **1.100** at `ka=2.449` (the A3 fork) — **the cutoff-vs-wall choice moves `Q` by ~2×**. *(Implementer re-computation of the same estimator gives 1.863/0.839/0.973 and the exact Chu/Collin–Rothschild stored-energy `Q` gives 2.812/1.679/1.837; all three agree the cutoff→wall move is 40–55% and disagree on the absolute by up to 50% — that spread IS CF-14, surfaced not resolved.)* **Class: arithmetic-consistency on verbatim canonical lines. Not a claim.**
- **★ CF-8 — `ka = ℓ` is an IDENTITY, and it names what `Q = ℓ` actually is.** The chain sets `ω_R = ℓc/r_eff`, so the mode's **electrical size** is `k·r_eff = ℓ` **exactly** (`= 2.000` at `ℓ=2`) — a one-line re-expression of two canonical lines, **CLASS: IDENTITY**, not emergence. In spherical-mode/antenna theory `ka = ℓ` is precisely the **radiation cutoff of the ℓ-th spherical mode** (the reactive/evanescent ↔ freely-radiating boundary). ⇒ **the cold `Q = ℓ` is a claim about the external (radiation) `Q` of a spherical multipole evaluated at its own cutoff** — a **closed-form standard-EE object** (spherical-Hankel wave impedance `Z_ℓ(x) = jη h_ℓ′(x)/h_ℓ(x)`; Chu / Collin–Rothschild), computable with zero free parameters in the shear channel from `η = Z_sh,∞`. **That is the computation upstream F7 says has never been done**, and it is what R1's "computed radiation resistance" should actually compute. Fired as `ave-ee-first-mapping` **trigger-7 IDENTITY-COLLAPSE** (mode-count `ℓ` and electrical size `ka` are one bench object). **Proposed as candidate route R7; NOT executed here — the Chu/Collin–Rothschild value at `ka=ℓ=2` was deliberately NOT computed.**
- **★ CF-9 — upstream Route R3's geometry is INVERTED for the shear channel.** R3 proposes a cavity *"bounded **inside** by the Γ_shear = −1 mirror at r_sat and **outside** by the light-ring potential barrier."* Canon says the light ring is **inside** the mirror: `regime-eigenvalue-method.md:47` verbatim *"The photon sphere at `r_ph = 3M_g` lies *inside* the saturated region"*, and the Kerr prograde branch runs `3 M_g → 1 M_g` as `a*: 0 → 1`, **always** inside `r_sat = 7 M_g`. There is no annulus; the barrier is behind the mirror, unreachable by shear. **What the substrate has instead:** the graded profile's own turning point, **outside** the wall — stationary point of `ℓ(ℓ+1)c_shear²(r)/r²` at `A² = 2/3`, `r★ ≈ 8.573 M_g` (canonical `c ∝ √S`) or `A² = 4/5`, `r★ ≈ 7.826 M_g` (Family-E `S^¼`). **Both fenced as illustrative locators on canonical inputs — NOT a derivation, NOT a claim, NOT pre-registered**, gradient terms neglected, and the ~10% spread between them is itself the CF-5 point. **Consequence: R3 should be re-scoped to "the substrate's own graded-shear turning point", which is R2's object — R3 and R2 are one route, not two.** And this **relocates the corpus's own banked echo prediction** (`vol3/claim-quality.md:123`): the second mirror is the taper, not the light ring, so the echo delay is a **short** transit through **slow** medium (`2∫dr/c_shear` from `r_sat` to `≈8.6 M_g`, integrand `∝(r−r_sat)^(−1/4)`, integrable), **not** the long light-ring delay published LIGO echo searches target. **The upstream Q7 exposure changes shape: the risk is not "AVE's echo is excluded" but "AVE's echo is at a delay nobody has searched."**
- **Nine further findings, all flag-don't-fix.** **CF-7** the `ρ` in `Z_shear = ρ c_shear` is unspecified and the two canonical candidates flip the termination: `ρ₀` ⇒ free surface, `Γ=−1`; `ρ_eff = ρ₀/S³` (`interior-singularity-resolution.md:14-21`) ⇒ `Z ∝ S^(−5/2) → ∞`, clamped surface, `Γ=+1`, taper reversed (steelman recorded: that leaf says *"collapsing **matter**"*, plausibly a different object — but canon never says which `ρ` enters the wave impedance). **CF-10** a pure interface mode is **Scholte**-class (solid/fluid), not Stoneley, and a flat-interface Scholte wave is non-leaky ⇒ `Q → ∞` ⇒ **a finite ringdown Q rules out a pure interface mode**; what survives is the curvature-leaking whispering-gallery reading — which *strengthens* the corpus's own phrase *"curvature radiation"* while renaming the mode. **CF-11** canon already says the wall is a **perfect** reflector and attributes the loss to outward curvature radiation ⇒ **the Γ=−1 mirror contributes exactly zero to Q**, which makes the canonical leaf titles (*"Op21 … at the Γ = −1 Saturation/TIR Boundary"*; `qnm-quality-factor.md`'s *"axiom-manifestation of Ax 3 + Ax 4 at the Γ = −1 saturation/TIR boundary"*) a **mis-attribution of where the physics lives** — and it is why no radiated power has ever been computed. **CF-12** the ratified vessel-state R6 result (`research/2026-07-21_boundary-strain-amplitude_result.md:96`, verbatim *"pressure vessel (hoop tension + radial compression)"*, `k_shear,eff` *"anisotropic: hoop bonds STIFFEN, radial bonds SOFTEN toward buckling"*) is **not carried anywhere in the ringdown chain**; if it transplants, the rim has two branches and `Q⁻¹` is a sum — **honest fence: that result is core-local, the transplant is untested**. **CF-13** the wall's `Γ` sign is **Q-neutral in the loss ledger** (`|Γ|=1` both ways, #260 B3-DEGENERATE) but **Q-relevant through the frequency** (node vs antinode = quarter-wave shift in effective length) — **and the standing chain is insensitive to it only because it has no radial structure at all**; any graded route (R2/R7) becomes sensitive and the #260 degeneracy stops protecting the answer. **CF-14** port-`Q` (`X/R`) and pole-`Q` (`ω_R/2ω_I`) coincide only for an **isolated single pole**; a taper contributes a **branch cut**, so the pre-reg must declare which object it computes and how it maps to the frozen C-τ comparator (an A46-class item, not a formality). **CF-15** off the exact `A=1` point `|Γ| < 1` and some shear crosses the wall — but the interior has `G_shear = 0` so shear **cannot propagate there**: the transmitted energy must (a) mode-convert to bulk/A1 (**a second loss channel ⇒ breaks Op21's single-channel classification**), (b) reflect off the compact shell (a delay, feeding the echo), or (c) be absorbed (**a bulk `Re{Z}` ⇒ Ax-3 violation unless port-licensed**). **Nobody has checked which; this fork is not in the upstream route list.** **CF-16** the clock-exponent honesty-lag (above).
- **FORCED / CHOICE / OPEN-FORK verdicts on the nine challenged assumptions.** **A1** (resonator = the rim) **OPEN-FORK** — four candidates, canon explored one and explored it with two radii and a cold speed; candidate (c) interface-mode is **closed by its own physics** (CF-10); the live fork is *rim ring* vs *curvature-leaking whispering-gallery mode of the graded shell*. **A2** (sector) **FORCED** at linear order — A1 is the DC bias, T2 the AC signal, so any A1 admixture lands at `2ω` or DC, **not** at `ω` (a stronger reason for Op21's single-channel classification than the corpus currently gives); **sub-fork OPEN** on vessel anisotropy. **A3** **OPEN-FORK** on `ℓ` vs `√(ℓ(ℓ+1))` (upstream F4, unchanged) — but CF-8 makes it cheaper than it looked (it moves you along one universal curve from `ka=2` to `ka=2.449`, it does not change what kind of object `Q` is); **plus a separate GAP, not a fork — 🔴 CORRECTED 2026-07-31 (repair pass, PR #814 audit F2): the original *"the radial-overtone index `n` has NO AVE object at all"* is FALSE.** Canon owns `n_r` and Op6 owns the condition: `radial-eigenvalue-solver.md:160` verbatim *"where `n_r = n − l − 1` is the number of radial nodes … **This phase-matching condition IS Op6**"* (the condition being `∫k_in dr + ∫k_out dr + φ_Γ = n_r·π`, a radial phase integral **with an explicit reflection phase**), plus `de-broglie-standing-wave.md:226` (*"| Radial | Phase per bounce between turning points | `n_r` | Radial quantum number |"*) and `operators.md:189` (*"Op6 (eigenvalue target → radial nodes `n_r`), Op10 (`c=3` invariant → angular nodes `l`)"*). **The narrow true form: the BH-ringdown chain does not INSTANTIATE `n_r`** — and canon already scopes the claim accordingly at `vol3/claim-quality.md:205` (*"the claim is the fundamental mode, not the full QNM spectrum"*). Chain-coverage gap, not a vocabulary gap — a smaller finding but a sharper one, since the closing object already exists. **FORK-9 re-posed** accordingly. **Routed corpus error found while verifying (flag-don't-fix, NOT repaired in this lane):** `knot-mode-isomorphism.md:22` and `common/solver-toolchain.md:440` carry the identical row *"| First excitation | Cinquefoil `c = 5` (proton) | `ℓ = 3` **(1st GW overtone)** |"* — **cross-wiring the ANGULAR index with the OVERTONE index** (`ℓ=3` is the octupole, a different multipole; the GW overtone index is the radial `n`). Surfaced with both paths + verbatim row; a separate lane owns the repair. **A4** **FORCED by canon** (outward radiation only; wall lossless) **+ OPEN** off the exact yield point (CF-15). **A5 RECLASSIFIED** (above). **A6** ((1+ν)) **CHOICE** — FORM unexceptional, the specific `r_sat/(1+ν)` **asserted not derived** (the entire justification is one sentence), VALUE GR-imported (#261); its arithmetic content is that the wall sits `9/7` **above the ℓ-th mode's own radiation cutoff** (`k·r_sat = 2.571`), a *result* not a defect (the `VF > 1` reading is retracted, F1). **A7** **FORCED** to the physical `ω_R/2ω_I`, with CF-14 as a new caveat. **A8** **FORCED** at `|Γ|=1`, sign degenerate, Q-neutral for loss / Q-relevant for frequency (CF-13). **A9** **OPEN-FORK** — the taper is **≈0.41 wavelengths** long (`λ_∞ = 2πM_g/(18/49) = 17.10 M_g` vs grade scale `~7 M_g`), i.e. **borderline between adiabatic and lumped**, exactly the regime where partial reflection is guaranteed and `Q` comes out `O(1)` — **`Q ≈ 2` is what a borderline taper gives**, a structural consistency the corpus had never noticed and a genuine point in the standing picture's favour. Re-reflection feeds back into `Q` **by definition** (it *is* the taper's input impedance; there is no separate "echo correction").
- **PART 2 — the full vacuum-circuit mapping (15 elements, E1–E15), each tied to a lattice primitive and a canonical leaf.** The ringdown maps to: a **ring resonator** (E8, `ℓ` wavelengths, electrical size `ka = ℓ`) on a **short-terminated** boundary (E5, lossless, phase-setting only), coupled outward through a **non-uniform tapered line** (E6, `Z(r) = Z_sh,0√(S(7M_g/r))`, `≈0.41λ` long) into a **matched load** (E7, the cold Regime-I lattice). **`Q = X_rim/Re{Z_in(taper)}`** ⇒ three consequences: the wall is **not in the formula**; upstream F7's uncomputed constant `c₁` **is** the taper's input resistance over the rim reactance (a computable number, not a convention); and in the taper-free limit `Z_in` is exactly the ℓ-th spherical-mode wave impedance at `x = ka = ℓ`, so the graded profile becomes a **correction on top of a known cold answer**. **Dual-convention declared once and explicitly** (mechanical impedance analogy: inertia→`L`, compliance→`C`, force↔voltage — the dual of `translation-circuit.md` §4's EM rows; mixing them is the open/short trap that leaf's `:119` row already documents). **Failure-mode probes** (elements with **no** clean mapping) — **three after the 2026-07-31 repair pass**: ~~**P1** `(1+ν)` as a velocity factor `> 1`~~ **WITHDRAWN (F1)** — E11 maps cleanly as the wall-above-cutoff ratio `r_sat/r_eff`; **P2** `ρ_eff` as a line inductance (flips the termination); **P3** spin as **Doppler-offset vs circulator-bias** — the corpus's `ω → ω − mΩ` Park reading and a gyrotropic ferrite-circulator bias are **different EE devices making different Q predictions**, and the corpus has only ever used the first; **P4** the missing radial-overtone object. **What the circuit shows that the elastic picture hides:** only `(L,C)` are free (⇒ CF-5); taper length **in wavelengths** is the question (⇒ 0.41λ ⇒ `Q = O(1)`); the wall drops out of the loss ledger; `ka = ℓ` is an identity; ~~`VF > 1` is impossible~~ **(retracted F1)** — replaced by *`(1+ν) = k·r_sat/ℓ` is a wall-above-cutoff ratio*; a resonator has exactly **one** electrical length (⇒ CF-1). **What the elastic picture shows that the circuit hides** (recorded so the mapping is not over-read): polarisation and the A1⊥T2 grade split (the **same** disanalogy already banked at `translation-circuit.md` §4.7.3 (ii), live again here); angular structure (`ℓ`,`m`) — a 1-D line has no `ℓ`; P↔SV **mode conversion** at a solid/fluid interface; and what the interior can actually carry (the circuit says "short"; only the elastic picture poses CF-15).
- **Two NEW substrate↔EE correspondences identified and DELIBERATELY NOT LANDED** (`ave-ee-first-mapping` Step 6 withheld, per `ave-mechanism-claims-discipline`): (a) *Op21 `1/ℓ` per-cycle leak at a curved saturation boundary ↔ external (radiation) `Q` of the ℓ-th spherical mode at `ka = ℓ` (Chu / Collin–Rothschild)*; (b) *frame-dragging `Ω` at a spinning saturation wall ↔ gyrotropic (ferrite-circulator) bias on a ring resonator*. **Step 6 lands *established* correspondences; these are hypotheses whose means-test has not been run (running it IS the derivation).** Landing an unvalidated row would put a hypothesis in the canonical catalog. **Routed to the auditor lane to land if and when the derivation validates them.**
- **PART 3 — the lattice walk** (sit-inside-the-cell, hop-by-hop from the wall outward, convention declared): the free-surface node at `A=1` that **takes no energy from the wave**; the stiffness ramp `G/G₀ = S(7M_g/r)` tabulated at `r/r_sat = 1.00 → 5.00` (`c/c₀`: `0 → 0.375 → 0.645 → 0.760 → 0.931 → 0.990`) showing the ramp is **steepest at the wall** and **93% done by `2 r_sat`**; the turning point at `1.22 r_sat`; and the matched cold line past `3 r_sat` that is **the only real loss**. Each PART-1 fork is marked **inline at the hop where it changes the story**. One-sentence version: *"the bell is not the wall — the wall just refuses to absorb; the bell is the ramp, and `Q` is asking how leaky a half-wavelength horn is when the thing radiating into it is exactly at its own cutoff."*
- **§4 — TEN forks routed to Grant, each as one plumber question with options** (`pre-test-physics-check`, asked BEFORE design per the Rule-16 strengthening; seven of the ten were **not askable** before the circuit mapping existed): **FORK-1** *is this a bell (mass at a radius) or a horn (a taper that resonates)?* **FORK-2** *at the wall, does the vacuum go **soft** (`C→∞`, short) or **stiff-and-opaque** (`Z→∞`, open)? You can't have both in one channel.* **FORK-3** *is the frozen infalling matter part of the pipe wall, or the water in the pipe?* **FORK-4** *(re-posed 2026-07-31, F1)* *the standing chain puts the physical wall exactly `9/7` above the mode's own cutoff radius (`k·r_eff = ℓ`, `k·r_sat = 2.571`) — is that the `(1+ν)` factor's actual job, or is the coincidence accidental?* **FORK-5** *a hair off the wall the mirror isn't perfect and shear crosses; shear can't propagate in there — where does it go?* **FORK-6** *where's the second mirror?* **FORK-7** *is the wall a pressure vessel — does the rim ring in two notes?* **FORK-8** *is frame dragging a Doppler shift or a magnetic bias on a circulator?* **FORK-9** *a horn has more than one resonance — does this one?* **FORK-10** *are we measuring how lossy the port is, or where the pole sits? For a taper those aren't the same number.* **Plus the standing scope gate, unchanged and still unanswered (upstream Q8): is the B1-ratified cold `Q = ℓ` anchor in scope at all** — everything here says the tension lives there (upstream F8: 88% of the deficit is present at zero spin), and if it is out of scope the reachable outcome is a *cold-broken / catalog-matching* inconsistency, a **sharper falsifier**, not a resolution.
- **Class discipline.** Every number is tagged inline: **IDENTITY** (`ka = ℓ`; `λ_∞ = 17.10 M_g`; `0.41λ`; the hop table) or **arithmetic-consistency observation on canonical inputs** (`r★ ≈ 8.573 M_g`; `k·r_sat = 2.571`; `v_φ(r_sat) = 1.286 c₀`, `v_φ(r_eff) = c₀` exactly). **No emergence-class language appears anywhere**, and the upstream `ν_vac`-GR-imported class ceiling is inherited unchanged and restated in §0. **Explicitly NOT computed, stated so it cannot be mistaken for computed:** the Chu/Collin–Rothschild external-`Q` at `ka = ℓ = 2`; the R6 vessel-state transplant; any `ν_vac → 0` cancellation sensitivity (still a pre-reg requirement per upstream §2.1).
- **Lane mechanics.** Self-isolated throwaway worktree off `origin/main` @ `512e1ef4`; branch `research/qlaw-framing-walk`; incremental commits (skeleton → one section per commit); PR opened `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. Step-0 skill-selection plan + retro-pass (three drift items: `ave-cavity-class-identification` fired **by analogy** — its "name the cavity class before asserting a shared operator mechanism" discipline **is** A1, and Op21 asserts BH↔electron mechanism-sharing while the BH cavity class is unidentified; `ave-ee-first-mapping` **trigger 7** unplanned → CF-8; `ave-mechanism-claims-discipline` unplanned → the §2.7 non-landing) are Appendix A. A **20-row two-method `verify-before-cite` battery** plus 8 numerical receipts and an explicit **NOT-verified** list are Appendix B; the second method changed an answer twice (a `claim-quality.md` path error, and the discovery of the engine's own verbatim impedance-conflict FLAG).
