### ENTRY 2026-08-03-coldq-v2p2-root (2026-08-03): implementer — cold-Q v2.2 ROOT certification under Grant's "certify the root, not the rectangle" ruling; `ROOT-NOT-CERTIFIED` on one gate, and the isolation gate the lane was built for PASSED

- **Class: DERIVATION result.** **Zero ids minted** (INVARIANT-S11) — no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; no `confidence`, `quality`, `solidity` or any grade field moves; no KB or manuscript file is touched; the falsification ledger is untouched; engine `src/ave` is byte-untouched. Written against `origin/main` = `184db4b6`.
- **Provenance.** Grant's ruling of 2026-08-03 on the PR #854 audit's Q2 — **certify the located root, not the rectangle.** Prereg `research/2026-08-03_coldq-pole-v2.2-root_prereg-FROZEN.md` frozen and pushed **ALONE** at `f15a6e4d`, before any driver code and before any number produced by this instrument existed. **Zero pre-freeze computation:** no prototype was written and no operator was assembled; the only arithmetic before the freeze is elementary arithmetic on already-published prior-lane values, reproduced by the driver and machine-checked.
- **VERDICT: `ROOT-NOT-CERTIFIED`.** Frozen precedence fires `BIN-F-ROOT`; **BIN-1/2/3 are `N/A — not adjudicated` and BIN-4 is `N/A BY CONSTRUCTION`.** Ten of eleven gates PASS and **all eleven self-tests FIRE**. The single failure is **G2** (`n`-independence over the frozen ladder `n ∈ {32, 48, 64, 80, 96}`): measured `1.2496816388248957e-10` against the frozen `1e-10`, a factor of `1.25`, **entirely the `n = 32` rung**.
- **★ The failure is in this lane's OWN gate specification and was knowable at freeze from the frozen document against itself.** The prereg's **§6** — the FT-2 non-vacuity cell at `research/2026-08-03_coldq-pole-v2.2-root_prereg-FROZEN.md:337` — cites v2.1's measured Chebyshev coefficient tail as reaching `5.3e-16` only by `n = 40`, and the prereg then froze an independence ladder starting **below** that at a `1e-10` tolerance. (⚑ **CORRECTED 2026-08-03, post-review:** this bullet, the result doc at two sites, the number check's `5.3e-16` reason string and the PR body all said **§9**. Original text, verbatim: *"The prereg's §9 cites v2.1's measured Chebyshev coefficient tail as reaching `5.3e-16` only by `n = 40`"*. §9 is the tolerance-derivation table and its G0 row cites an unrelated v2.1 measurement, C11's `8.9716e-16`; the receipt is in §6, and the inner citation there is to **v2.1's** §9 item 7. **The argument does not move — only the pointer.**) **That is the #854 audit's WARN-4 lesson landing on this lane, and it is written in the result doc rather than left for an auditor.** Per the frozen Rule-11 fence nothing was retuned; per Rule 12 the slot is **not refilled** — the obvious successor ladder is stated as explicitly-not-adopted and routed to a v2.3 with its own verification chain.
- **★ The attribution is MEASURED, not asserted** — a `NOT-ADJUDICATED DIAGNOSTIC` added after the outcome was seen, changing no criterion and no verdict, tests whether the spread is under-resolution or the audit's migrating pseudo-spectrum. Error against `n = 96` falls `1.2497e-10` → `8.0906e-14` → `1.1709e-16` → `2.9026e-19`, ratios `1544.6` / `690.97` / `403.39` per `16`-rung step in `n`: **clean root-exponential (sub-geometric) convergence,** `E(n) ≈ C·exp(−c√n)`. A migrating pseudo-pole does not do that — and FT-5 measures what one actually does. (⚑ **CORRECTED 2026-08-03, PR #856 review finding 4.** Original text, verbatim: *"ratios `1544.6` / `690.97` / `403.39` per 16 orders: **clean geometric convergence.**"* **Geometric convergence requires CONSTANT ratios at fixed rung spacing and these fall by ~2× per step, so this bullet's own numbers refuted its own adjective.** The correct law is root-exponential — the expected Chebyshev rate for an **endpoint-singular** operator, and the Ax-4 kernel `S = η√(2 − η²)` vanishes at the wall `η = 0`. **The mechanism conclusion is unchanged and better supported:** an endpoint singularity is a property of the continuous operator, and it predicts the observed law with no free choices.)
- **⚑ ADDED 2026-08-03 (PR #856 review, finding 4) — THE TWELVE-RUNG CONVERGENCE EXTENSION. POST-RESULT REPRODUCTION; NOT IN THE SHIPPED JSON; ADJUDICATES NOTHING.** Produced by re-running this lane's own `root()` unmodified on `n ∈ {24, 32, 40, 48, 56, 64, 72, 80, 88, 96, 104, 112}` against an `n = 128` reference. `E(n)` and the local rate `c = −d ln E/d√n`: `7.903217e-09` (`24`) · `1.249682e-10` (`32`, `c = 5.4718`) · `2.801717e-12` (`40`, `5.6879`) · `8.090600e-14` (`48`, `5.8721`) · `2.843058e-15` (`56`, `6.0320`) · `1.170909e-16` (`64`, `6.1734`) · `5.503738e-18` (`72`, `6.3005`) · `2.895094e-19` (`80`, `6.4162`) · `1.678933e-20` (`88`, `6.5225`) · `1.060881e-21` (`96`, `6.6206`) · `7.234526e-23` (`104`, `6.7122`) · `5.262357e-24` (`112`, `6.8081`). **Monotone at every rung, thirteen orders of decay from `n = 24` to `n = 96`, `n = 104` and `n = 112` continuing the same law — no plateau anywhere.** **And it is nowhere near an instrument floor:** the polish's own termination is `1e-38` relative (`POLISH_TOL_EXP = 38` in the driver), which sits **nineteen orders** below the shipped table's deepest rung (`2.9026e-19` at `n = 80`) and **fifteen orders** below the extension's deepest (`5.262357e-24` at `n = 112`). **Cross-check that the reproduction is the same instrument:** the extension's own max pairwise separation over the frozen ladder `{32, 48, 64, 80, 96}` is `1.2496816388248957e-10`, bit-identical to the shipped `G2` measured value.
- **★ G5, the gate the ruling created, PASSED — and it localizes the audit's own mechanism.** Isolation count within the derived `R_iso = 0.5`: **`[1, 1, 1, 1, 1]`** at every ladder order around the certified root. The **same routine, same radius, same ladder** returns `[2, 1, 2, 3, 0]` with a `0.16191` relative drift when centred on the v2.1-banked discretization artifact, and `[1, 1, 0, 0, 0]` when centred on v2.1's own contaminated-edge C9 probe. **The PR #854 audit's migrating pseudo-spectrum is reproduced at the pseudo-poles and absent at the root, by one measurement.** `R_iso` is derived, not picked: bounded above by the GR `ℓ=2` fundamental-to-first-overtone spacing `1.3083542634814167` (so a genuine overtone would not trip it) and by the `2.127881506829584` distance to the nearest other prior-lane root; bounded below at `237006.65113790636` × the dedupe radius.
- **Other load-bearing passes.** G6 two-instrument agreement `6.8032e-07` against the v1 shipped root, with FT-6 showing the same gate reads `5.8722e-04` under a `1e-3` coefficient corruption (it catches transcription error, it does not rubber-stamp). G8 `ν_vac` cancellation `1.8619e-46` measured **in mp end-to-end** — the audit's WARN-7 `complex`-cast defect closed at source rather than inherited. G10 Ax-3: operator reality `0.0` exactly and conjugate-mirror symmetry `9.2731e-47`, both broken by a smuggled `Im(μ)/Re(μ) = 1e-3`. G1 eigenfunction residual `4.7268e-50`. G7 spin-2-vs-spin-1 at the root `0.28424` (eigenvalue) and `0.19697` (eigenfunction), with a REVERSE self-test returning `0.0` on identical specifications.
- **NO winding, NO contour, NO region count anywhere in the lane** — the pole-counting instrument the audit impeached is not used, not repaired and not relied on. **The frozen non-claim:** *this lane asserts the existence and location of THIS root; it asserts NOTHING about the absence or presence of other modes.*
- **Two NEW flags, both surfaced not fixed, both running against this lane's convenience.**
  - **FLAG-9 (result doc §3.4).** v1's `0.28430` is a **clamped**-wall mutation (`W(r_sat) = 0`, `Γ = +1`, verbatim at `research/2026-08-02_coldq-pole-derivation_prereg-FROZEN.md:229`), **not** the spin-1 condition. v2.1's result-doc §5.4 cross-lane table places the clamped and spin-1 shifts in one row labelled *"spin-1 / clamped wall shift"* and reports their `2.1e-04` agreement. **That row compares two different mutations.** Not resolved here; both file paths and both verbatim descriptions surfaced; neither side reframed.
  - **FLAG-8 (result doc §7) — CROSS-LANE CONVERGENCE, not an open question.** The characterization this lane's G2 tolerance was derived from — *"n-stable to 12 digits over `n = 32 → 80`"* — is **not reproduced at `n = 32`** by this reimplementation (`1.2497e-10`, ~10 digits); ~13 digits **is** reproduced over `n ∈ {48, 64, 80}`. **The concurrent #854 lane reached the identical conclusion from its own shipped data and landed the correction at `cb2012af` (06:36:08), twelve minutes AFTER this lane's prereg was frozen at `f15a6e4d` (06:23:52).** Two implementations sharing no code, same fact. The frozen fence binds regardless of when the underlying evidence moved: **G2 stays banked as FAIL and nothing is retuned.**
- **FLAG-5 is the arc's biggest open item and this lane makes it harder to forget.** `BIN-4` is `N/A BY CONSTRUCTION` **at every precedence level, including a full pass** — the completeness question needs a **substrate-derived low-frequency cutoff**, which is routed to Grant (prereg §0's plumber question) and **not attempted, not sketched, not assumed** here.
- **⚑ ADDED 2026-08-03 (PR #856 review, finding 1) — G2'S TOLERANCE WAS DERIVED FROM A MIS-ATTRIBUTED PREMISE THAT WAS ALREADY STALE AT FREEZE, AND UNDER THE CORRECTED PREMISE THE GATE PASSES. Disclosed, NOT applied — the verdict does not move.** The frozen prereg at `:476` derives `G2`'s `1e-10` from *"the PR #854 audit measured this root stable to **12 digits** over `n = 32 → 80`"*, citing `0a7dec1f`. **`0a7dec1f` contains no digit count at all** — the review searched the whole commit, message and diff, and the string `digit` does not occur in it; the commit says only that the root is the sole `n`-stable eigenvalue in the box. The *"12 digits"* text was the **#854 lane's own result-doc characterization**, and that lane had already 🔴-restated it at `cb2012af` to *"12 digits across `n = 48 → 80` (~10 digits from `n = 32`)"*. **Consequence, by the prereg's OWN "two orders looser" rule:** the corrected `~10 digits from n = 32` premise returns `1e-8` — which this battery's `1.2496816388248957e-10` passes by `80.02`× — and a ladder starting at `n = 48` returns the frozen `1e-10` itself, which the `{48, 64, 80, 96}` spread passes by `1236`×. **So the FAIL is manufactured entirely by the stale premise.** **Operational receipt, reproduced post-result in the review lane by re-running this lane's own `root()` on shifted ladders (NOT in the shipped JSON, adjudicates nothing):** max pairwise separation over `{n_low, 48, 64, 80, 96}` is `1.249682e-10` at `n_low = 32` (**FAIL**, `0.80`× the tolerance), `4.701667e-11` at `34` (**PASS**, `2.13`×), `1.801673e-11` at `36` (`5.55`×), `7.042647e-12` at `38` (`14.20`×) and `2.882464e-12` at `40` (`34.69`×). **The crossing is between `n = 32` and `n = 34`** — the review reported it as *"between `n = 32` and `n = 36`"* from a stride-`4` sweep, and the tighter bracket measured here is consistent with and sharper than that. **The boundary sits immediately below the `n = 40` resolution receipt the prereg itself cites in §6.** **The frozen prereg is byte-untouched, `G2` stays banked as FAIL at the tolerance it was frozen at, `ROOT-NOT-CERTIFIED` stands, and the correction is routed into the v2.3 prereg's tolerance derivation — which must cite `cb2012af`, not `0a7dec1f`, for the digit count.**
- **⚑ ADDED 2026-08-03 (PR #856 review, finding 6) — "INDEPENDENT" MEANS NO SHARED CODE, NOT AN INDEPENDENT DERIVATION, AND THE PR BODY OVERSOLD IT.** The PR body called this an *"independent transcription sharing no line of code"*. **No line of code is shared; the ALGEBRA is v2.1's, transcribed with attribution comments at every site.** The arithmetic shows it: FT-2's `4.4038e-04`, FT-8's `6.0137e-07` and G7(a)'s `0.28424` are identical to v2.1's measurements of the identical mutations at the precision both docs quote — which the frozen prereg predicted in advance at `:337` and `:343`. **Two encodings of the same transcribed algebra agreeing is a re-encoding check, not an independent-derivation check.** What it catches is coding and transcription error — which is real, and which FT-6 demonstrates at `5.8722e-04` — but it is not evidence that the algebra is right. **The one comparison in this lane that IS different in kind is `G6` against v1's `6.8032e-07`.** Corrected in the PR body and in the result doc's §3.3 and headline.
- **⚑ ADDED 2026-08-03 (PR #856 review, finding 7) — THE MAKEFILE CONTACT WITH PR #854 IS A REAL MERGE CONFLICT, NOT AN APPEND-ONLY AUTO-MERGE. Corrected in the PR body; recorded here because PR-body text is not under version control.** The bullet below describes the contact as *"two single-line **append-only** list edits"*. **Measured: `git merge-tree --write-tree origin/research/coldq-pole-v2p2 origin/research/coldq-pole-v2` exits `1` with `CONFLICT (content): Merge conflict in Makefile`.** A three-way merge from the true base `583d43dd` produces **three** conflict hunks, not two: (i) the `.PHONY:` line — both branches rewrite the same line with different text; (ii) the `help:` echo block — both branches INSERT a line at the same point, which the disclosure below does not mention at all; (iii) the `verify:` prerequisite line — same-line rewrite again. **The own-target placement did what it was for: the two recipe BODIES auto-merge cleanly and no `research/` or `_orchestration/` file conflicts.** But *"append-only"* is the wrong word for a same-line edit, and a reader was entitled to the conflict. **Whoever merges second rebases;** both resolutions are mechanical (keep both target names on each list, keep both echo lines). Re-measured against #854's tip at the time of this repair, `71c76644`.
- **⚑ ADDED 2026-08-03 (PR #856 review, finding 8) — G2's LOWER-RUNG DIAGNOSTICS ARE NOT REPRODUCIBLE FROM THE SHIPPED JSON.** The `diagnostics.spectral_convergence` rows report `rel_vs_n_ref` down to `2.9026e-19`, but the per-rung roots the JSON ships are **double-precision** (`gates.G2.rows[*].Omega`), and doubles carry `~1e-16` relative. **Every rung below the `n = 48` row is therefore an mp-internal quantity that a reader cannot recompute from the shipped object** — the `2.9026e-19` figure in particular is unreachable from anything in the file. The values are correct (this lane's post-result extension reproduces the ladder and returns the shipped `G2` measured value bit-identically); the defect is **auditability**, not accuracy. **Routed to v2.3: ship an mp decimal STRING per rung alongside the double, so the convergence diagnostic is checkable at the precision it is reported at.**
- **⚑ ADDED 2026-08-03 (PR #856 review, finding 10 + task-2 note) — TWO OBSERVATIONS ON G5 AND FT-5, BOTH RUNNING IN THIS LANE'S FAVOUR AND BOTH RECORDED ANYWAY.** (a) **`isolation_row`'s centre-fallback is a conservatism that NEVER TRIGGERS IN `G5` PROPER.** When the polish fails or lands outside `R_iso` the routine centres on the raw seed instead (`research/drivers/coldq_pole_v2p2_root.py:670-672`); all five `G5` rows carry a non-null `polished`, so `[1, 1, 1, 1, 1]` is measured around polished roots at every order and never around an unpolished seed. It DOES trigger in FT-5, at the artifact's `n = 96` row and the edge probe's `n = 64/80/96` rows — where it is the conservative direction, since centring on the raw seed is the configuration most likely to still find something. (b) **The artifact's `0.16191` drift is the `n = 48`-vs-`n = 80` pair, and at `n = 80` the polished centre's real part collapses onto the imaginary axis** — `1.4142791927586542e-35`, at `self_tests.FT_5.artifact_rows[3].polished`. **The artifact does not merely wander; at that order it stops being a resonance at all.** That strengthens the pseudo-spectrum mechanism claim of §3.2 rather than softening it, and is stated in the result doc.
- **⚑ ADDED 2026-08-03 (PR #856 review, finding 9) — THE FROZEN PREREG'S FT-8 NON-VACUITY ARITHMETIC IS WRONG, AND THE PREREG'S OWN "MACHINE-CHECKED" CLAIM DOES NOT REACH IT.** Prereg `:343` says the mutation `A → A·(1 + 1e-6·(x_sat − 7)/7)` is *"`±1.43e-06` relative at `x_sat = 5, 11`"*. **The true perturbations are `-2.857143e-07` and `5.714286e-07` — asymmetric, and about `5×` / `2.5×` smaller than quoted.** The self-test's conclusion is unaffected and conservative: the mutation vanishes at `x_sat = 7` and not elsewhere, and FT-8 fired at `6.0137e-07` against `1e-9` on the *true* perturbation. **Why it survived:** the gating number check scans the **result doc only**, never the prereg — so §9's disclosure that the pre-freeze arithmetic *"is reproduced by the driver, and is machine-checked by the number check"* holds for §4.3 and §7.3, which the driver recomputes, and **not** for §6's non-vacuity algebra, which nothing recomputes. **Frozen prereg byte-untouched; disclosed, not edited.**
- **⚑ ADDED 2026-08-03 (PR #856 review, finding 5) — FT-7's TWO ZEROS ARE A LITERAL CODE-PATH IDENTITY, WEAKER THAN THE FROZEN CHARACTERIZATION, AND IT GATES DETERMINISM RATHER THAN G7's DISCRIMINATOR.** The prereg's §6 FT-7 row calls the null mutations *"exact identities [that] must return `0` up to arithmetic"*. **They are the same call.** `spin1_wall=False` is the **default** of `graded_matrices` / `graded_matrices_mp` (`research/drivers/coldq_pole_v2p2_root.py:254`, `:298`), so FT-7(a)'s `root(N_PRIMARY, spin1_wall=False)` at `:878` is the call that produced `Ω_*`; and FT-7(b) at `:879` divides `strain_to_kinetic(N_PRIMARY, om_c, psi, w2)` by `R2`, the value of that identical call at `:874`. **No arithmetic differs, so `0.0` is forced — the two zeros could not have been anything else.** What fires is **determinism and call-path purity, which is `G9`'s job**, not a demonstration that `G7`'s discriminator returns small on a *differently-coded* null. **Consequence for the frozen reachability audit:** the prereg's `:430` claim that *"FT-0…FT-10 each drive an actual gate into its failing state"* is **over-stated for FT-7**, which drives nothing and re-evaluates the unmutated configuration. **Frozen prereg byte-untouched; disclosed, not edited. `G7` itself is unaffected** — `0.28424` and `0.19697` come from genuinely different specifications. **The differently-coded-null fix is routed to v2.3 (confirmed dispatched).**
- **⚑ ADDED 2026-08-03 (PR #856 review, finding 3) — THE #854 LANE ROUTED TWO CHECKER FIXES TO THIS LANE BY NAME, THIS LANE'S CHECKER DOES NOT CARRY EITHER, AND THE MISS WAS NOT DISCLOSED. Recorded here; the fixes are ROUTED ONWARD, NOT MADE.** At `53bdd90f` (`2026-08-03T06:40:11-07:00`) the #854 lane wrote, verbatim: *"**ROUTED TO THE v2.2 CHECKER AS A NAMED SUCCESSOR ITEM, NOT CHANGED HERE:** a **minimum significant-digit floor (≈`3`) below which a token must be allow-listed rather than matched**, and **per-site rather than global dedup**."* **This lane's own checker landed at `982c4c9b` (`2026-08-03T07:00:07-07:00`) — nineteen minutes and fifty-six seconds later — carrying neither, and saying nothing about either.** The routing existed and was missed.
  - **THE MUTATION-TEST WITNESS, reproduced in the review lane and again here on a scratch copy of the tree.** Corrupt the shipped JSON at `self_tests.FT_7.measured_a` → `123456.789`, `self_tests.FT_7.measured_b` → `-987654.321`, and every `gates.G5` count → `42`, then re-run the checker unmodified. It prints, verbatim: `tokens 86 | registered 62 | allow-listed 24 | unregistered 0` and `[coldq-v22-number-check] OK`, **exit `0`.** **The two most load-bearing rows in the whole result — the isolation counts the lane was built to produce, and the reverse self-test that is supposed to prove G7 can return small — can be replaced by garbage without the gating check noticing.**
  - **The three specific gaps.** (i) **`G5`'s `[1, 1, 1, 1, 1]` is not a numeric token at all** (the whole bracketed string fails the numeral regex), and the per-rung `1`s in the §3.1 table are covered only by the ALLOW-LIST entry for a bare `1` — *"structural integer / the frozen G5 count / an isolation count"* — which matches any `1` anywhere. **No G5 count is checked against the JSON.** (ii) **The dedup is global**: `seen` in `main()` admits each distinct numeral string once, at its FIRST occurrence in the document. The token `0.0` is REGISTERED against `gates.G10.measured_a`, but its first occurrence is at the result doc's `:25`, where it denotes **v2.1's** artifact value — so the one check the token gets is spent at a site that means something else, and the G10 row, both FT-7 zeros and the convergence table's reference zero are all unchecked. (iii) **The bare-integer branch of `matches()` accepts anything within `±0.5`**, so a registered integer token is verified to roughly one significant figure.
  - **WHAT DOES HOLD, stated so the disclosure is not read as worse than it is.** This lane's `REGISTERED` table maps each token to **one explicit JSON path or one explicit formula**, where v2.1's checker matched a token against **any value anywhere in the JSON**. **The two #854 witnesses — `11` "registering" against `10.996902431077316` and `9` against `-9.222435328580387` — cannot recur here**, and no sign-insensitive comparison exists in this checker. The defect is coverage, not false attribution.
  - **ROUTED, NOT FIXED HERE.** The minimum-significant-digit floor and per-site dedup land in the **v2.3 checker** (confirmed dispatched). **They are not made in this lane because this is gating logic and the result is already banked** — changing a gate's own instrument after its verdict is the post-result move Rule 11 forbids, which is the same reason the #854 lane declined to make them. The allow-list and comment strings edited by this repair are **not** gating logic: no key was removed, no `REGISTERED` entry added or altered, and `matches()` is byte-untouched.
- **⚑ ADDED 2026-08-03 (PR #856 review) — TWO PHYSICS QUESTIONS ROUTED TO GRANT. OPEN; NOT ADJUDICATED HERE BY ANYONE.**
  1. **Is the root-exponential convergence telling us the field is genuinely non-smooth AT THE WALL, or is it an artifact of the coordinate?** `E(n) ≈ C·exp(−c√n)` is the Chebyshev signature of a **branch point sitting exactly at a domain endpoint**, and the endpoint here is the soft inner terminus: the Ax-4 kernel `S = η√(2 − η²)` vanishes there, so the local wave speed `c₀√S` goes to zero and the coefficient functions lose analyticity at `η = 0`. **Two readings, and this lane cannot separate them.** (i) **Physical** — the substrate really does carry a non-smooth field at the terminus, in which case the singularity is a property of the graded cavity and every spectral method will see it, and it may be the numerically visible fingerprint of whatever the terminus actually is. (ii) **Coordinate** — the compactification `A = 1 − η²` puts the singular point at the endpoint by construction, and a different radial variable would move or soften it, in which case the rate is an artifact of the chart and says nothing about the medium. **A cheap discriminator exists and is not run here:** re-derive the operator in one alternative radial variable and re-measure `c`. **Routed to Grant as a physics question, and to a successor lane as a test.**
  2. **The FLAG-9 `2.1e-04` closeness between the clamped-wall and spin-1-wall shifts — coincidence, or a real insensitivity of the wall row?** FLAG-9 (result doc §3.4) establishes that v1's `0.28430` and v2.1's/this lane's `0.28424` are **two different mutations**: `ψ(0) = 0` versus `[1 + iΩ(λ−1)]ψ(0) − ψ_ηη(0)/2 = 0`. **They are not the same row, and yet they move the fundamental by the same amount to four significant figures.** Two readings. (i) **Coincidence** at this `ℓ` and this profile, in which case nothing follows. (ii) **The eigenvalue is insensitive to the FORM of the wall row at fixed tensor rank** — i.e. what moves the root by `0.284` is the change of angular weighting that comes with the mutation, not the boundary condition itself. **If (ii), then `G7(a)` is not measuring what its name says it measures**, and the spin-2-vs-spin-1 discrimination would need a wall-row-only mutation at fixed rank to be a discriminator at all. **Routed to Grant; not adjudicated here, and no verdict in this lane depends on the answer** — `G7` passed on both axes and `G7(b)`, the eigenfunction axis, is a rank change with no wall row involved.
- **Discipline receipts.** `make verify` exit 0 · gating number check **at the shipped commit `982c4c9b`**: `86` tokens / `62` registered / `24` allow-listed / **`0` unregistered**; **after the 2026-08-03 post-review repair: `96` tokens / `62` registered / `34` allow-listed / `0` unregistered** — the ten added allow-list entries are counterfactuals, prior-lane numerals and prereg-arithmetic corrections, each naming its formula or its source and each stating that it is **not** a battery output; **no `REGISTERED` entry was added or altered and `matches()` is byte-untouched** · `ruff check` clean on both new Python files · docket keys clean · pure-corpus · **two full runs digest-identical** (`a10f8aa906198605`, byte-identical apart from `_runtime_sec`) · runtime inside the frozen `3600` s budget · **all three predecessor preregs byte-untouched** · engine `src/ave` byte-untouched · zero `manuscript/` edits · **the frozen prereg (`f15a6e4d`), the driver and the shipped JSON are blob-identical before and after the repair** (`b7a209bd` / `908d9b99` / `796ffc1e`).
- **⚑ ONE disclosed file contact with the concurrent PR #854 repair lane, and it is not a research file.** (🔴 **CORRECTED 2026-08-03, post-review — see the finding-7 bullet above: this contact is a real `git merge-tree` CONFLICT in three hunks, not an append-only auto-merge. Original text of the two clauses that follow, verbatim:** *"two single-line **append-only** list edits (`.PHONY` and the `verify:` prerequisite list) touch lines #854 also appends to"* and *"**Append-only textual contact on two lines**"*.) The gating number check is wired as its **own** Makefile target placed away from #854's two insertion points; **three same-point edits** (`.PHONY`, the `help:` echo block and the `verify:` prerequisite list) collide with edits #854 makes at the same points. Disclosed before either lane merges; for the orchestrator to sequence — and stated as the mitigation rather than as a blanket denial of overlap, per that lane's own `e1d236dd` finding that a blanket "zero file overlap" claim had been false. **Zero overlap on every `research/` and `_orchestration/` file, verified against the #854 branch re-fetched at its CURRENT tip `53bdd90f` (it advanced from `21981789` to `53bdd90f` during this lane's run; every commit this doc cites is still an ancestor, and both frozen preregs are still blob-identical at `0e34ff98` / `4fc4faf9`).**
