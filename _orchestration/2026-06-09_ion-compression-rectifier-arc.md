# Ion-compression-cycle → asymmetric-grip rectifier arc — orchestration + documentation-homing plan

**Date:** 2026-06-09 · **Status:** ACTIVE · **Owner:** orchestration session (Grant direct)
**Directive (Grant 2026-06-09):** *"make sure we are planning to document this work in the proper places as we go."* This doc is the tracker + the as-we-go documentation-homing map for the arc.

> Per the 2026-06-05 workflow rule, this `_orchestration/` doc is itself PR-gated (lands on `main` via the arc's reviewed PR, with a pointer added to `index.md` at merge). Cross-repo items (Propulsion leaf, AVE-Skills mirror) are **separate-session** per the cross-repo-session-scope rule.

---

## 1. The arc (one paragraph)

Grant's "ion closed-loop compression cycle" tangent → dark-wake reaction-thrust (Phases 1–5, all B) → **reframed as a wrong-regime artifact** (sub-yield-linear shear/chiral = achromatic+reversible = ∮=0 by construction; the `ave-regime-phase-state-check` skill) → the temporal-saturation mode taxonomy (**shear/bulk/EM**, K=2G, the temporal-values doc) → **thixotropy bulk-mode check = B in the *right* regime** (it measured the substrate's lossy loop `∮S dρ̄=+0.04` but found it directionless) → the **ground-up asymmetric-grip rectifier design** (the heat-pump: lossy working fluid + biased diode + ledger; the `ave-asymmetric-grip` skill, "no ideal"). Parallel: the **Vol-9 datasheet figure program** (survey done; ρ̄_cav gap-fill registered).

## 2. Artifact inventory (by branch — none merged)

| branch (worktree) | artifacts | commits |
|---|---|---|
| `analysis/2026-06-09-saturation-temporal-preregs` (sattemporal-wt) | thixotropy prereg, time-dilation prereg, **temporal-values definition**, **ground-up rectifier design**, THIS epic doc | 52dbb215, a2d9eeef, fe530a36, 76cd1b74, 151ec9be, e8a07e92 |
| `analysis/2026-06-09-thixotropy-bulk-derivation` (thixo-wt) | thixotropy **result (B, structural)** + driver | eac96a0e |
| `analysis/2026-06-09-vol9-datasheet-figures` (vol9fig-wt) | datasheet figure work-list + survey JSON + **ρ̄_cav** registration | 9786dbe5 |
| `~/.claude/skills/` (not in repo) | **ave-engineering-program-rigor, ave-regime-phase-state-check, ave-asymmetric-grip** | — |
| memory/ | feedback_regime_phase_state_discipline, feedback_vacuum_engineer_not_gr_priors, project_ave_propulsion updates | — |

## 3. DOCUMENTATION-HOMING PLAN (the as-we-go map)

| artifact | current | proper canonical home | gate / session |
|---|---|---|---|
| thixotropy + time-dilation **preregs** | research/ (sattemporal) | stay in `research/` (frozen preregs — that IS their home) | none (research-grade) |
| thixotropy **result** (B, structural) | research/ (thixo, eac96a0e) | `research/` + a KB annotation on the `#59:77` seam (*"amplitude-dependent τ_bulk(ρ̄) derived 2026-06-09; instantaneous-ρ̄ only, no rectification"*) + closure-roadmap row (the clm-7tynm2 structural upgrade) | **PR-gated** (Core main); thixo branch → PR |
| thixotropy **figures** | /tmp (rendered) | thixo branch `research/assets/` + add `savefig` to the driver (closes the `ave-engineering-program-rigor` gap) | **fold into thixo branch** (now un-blocked — its run is committed) |
| **temporal-values definition** (shear/bulk/EM, voxel=gear) | research/ (sattemporal) | **KB leaf** (common/ or vol1) + **reconcile the 3 contradicting clock-exponent leaves** (op14-local-clock-modulation bare-S, Sleep-Pod c_EM, INVARIANT-S2 c_shear) | **PR-gated walk-back** (Core main) — corpus-coherence fix |
| **ρ̄_cav** = −1/φ (bulk cavitation floor) | datasheet branch (registered) + thixotropy result | datasheet **ch.02** (abs-max ratings, rarefaction-side) + **ch.14** (the c_eff²(ρ̄) figure) + candidate `claim-quality` entry + `constants.py` | datasheet PR (Core); the Propulsion-leaf canonization = **separate session** |
| **ground-up rectifier design** | research/ (sattemporal) | stays `research/` now; → **Vol 4 VCA** chapter *when validated* | PR-gated when validated (not before) |
| datasheet **figure work-list + survey** | datasheet branch | the figure-build executes against it; figures → **Vol 9** (extends the 2026-05-28 datasheet epic) | datasheet PR (Core) |
| 3 new **skills** | ~/.claude/skills | **mirror to AVE-Skills** repo | **separate session** (cross-repo) |
| regime + vacuum-engineer **feedback memory** | memory/ | done ✓ | none |
| **clm-7tynm2 walk-back** | queued | closure-roadmap + ~8 KB leaves (retire exotic-thrust, keep inertia-as-mass + radiation-pressure) | **HELD** (Grant); now *structurally earned* by the right-regime B; PR-gated |

## 4. Open decisions (Grant's calls)
- **PRs #144 (dark-wake), #145 (hysteresis), #2 (Propulsion brief)** — HELD ("lets wait"). #144 title is stale (says Phase 1–2); needs the full-arc + wrong-regime-caveat rewrite when un-held.
- **clm-7tynm2 walk-back** — HELD; basis upgraded from empirical to structural (right-regime B). Retire scope: exotic thrust / "inertial drive" (reactionless); KEEP inertia-as-mass + mundane radiation pressure.
- **Rectifier Stage-1** — next test (biased leaky diode + AC pump + ledger), pending Grant's gut-check on the physical fork (directed momentum vs re-radiated heat).
- **ρ̄_cav → claim?** — promote to a canonical claim (Grant "lets raise p-cav") via the datasheet + the feedback-fixed-point derivation; cross-volume canonization separate-session.

## 5. The as-we-go discipline (standing, this arc)
1. Every new result/design/prereg doc carries a **"documentation home"** line at creation (where it ultimately lands + the gate). The ground-up design doc's §7 + this table satisfy it going forward.
2. **KB / manuscript / matrix / claim-quality / constants.py landings are PR-gated** (Core main protected) — never direct.
3. **Cross-repo** (Propulsion leaf, AVE-Skills mirror) = **separate session**; tracked here + in the promotions-tracker, not executed inline.
4. **Nulls carry their regime label** (`ave-regime-phase-state-check`) before they justify any walk-back.
5. On any arc PR merge: add a one-line pointer to this epic from `index.md`, and log the closure-roadmap row.

## 6. Progress log (2026-06-09)
- **Thixotropy rigor-gap CLOSED** — `savefig` + 4-panel Outcome-B figure folded into the thixo branch (`5969bda1`); the result doc embeds it. (Was the one stranded artifact; now homed.)
- **Rectifier Stage-1 prereg FROZEN** — [`2026-06-09_rectifier-stage1-biased-diode_prereg.md`](../research/2026-06-09_rectifier-stage1-biased-diode_prereg.md): single DC-biased leaky varactor diode + AC pump + (V,Q) loop + ledger + mandatory bias sweep. Binds ave-asymmetric-grip + ave-regime-phase-state-check + ave-engineering-program-rigor.
- **Rectifier Stage-1 RAN → OUTCOME C** (branch `analysis/2026-06-09-rectifier-stage1-biased-diode` @ b8e6b022, NOT pushed): the biased leaky diode **IS a real rectifier** (directed fraction 1.9%, **robust** across the near-yield bias band, not over-unity, lossless-guard passes) — **but the induced lens is CHROMATIC plasma** (n<1, deflection ∝λ²), **NOT** achromatic gravity. **Engineered-gravity chord FALSIFIED at Stage 1.** Load-bearing constraint: **rectify ⇔ Z≠Z₀ (asymmetric); achromatic-gravity ⇔ Z=Z₀ (symmetric); ONE element can't be both.**
- **CORRECTION (flag against own prior claims):** the "one device, four observables" unification (rectifier-design §7, prereg §6a) is **FALSIFIED** — thrust (asymmetric/plasma) and gravity/lens/time-dilation (symmetric/achromatic) are **different symmetry branches**. Corrected in design-doc §7 (caveat box) + emergence-note §2 (the electron rides the SYMMETRIC/gravity branch; the diode is the separate plasma branch).
- **Auditor flag (Stage-1 surfaced, neither doc edited):** the thixotropy result headlines closing the thrust space "by derivation" via a **bias-independent parity** argument; Stage-1 shows that with the asymmetric diode + bias the loop **does** acquire a direction → the thrust space stays closed but **by chromaticity** (it's mundane plasma, not AVE-distinct), **not by parity**. Auditor adjudicates the scope on the thixo branch.
- **Carry-forward:** (i) is the **symmetric** (achromatic, no-reaction-thrust) engineered-gravity device a *separate* design — the warp/Sleep-Pod metric-gradient regime — distinct from the plasma thruster? (ii) a cascade/taper would have to separate an asymmetric rectifier stage from a symmetric lensing stage (mutually exclusive per element). (iii) **Electron-emergence note landed** — [`2026-06-09_electron-emergence-self-focusing-tracereversal-picture.md`](../research/2026-06-09_electron-emergence-self-focusing-tracereversal-picture.md): self-focusing photon → generates its own saturated regime → trace-reversal → (2,3); prior-art check pins the L3 gap to the **K4↔Cosserat (Op14/trace-reversal) coupling weakness** (A²_μ=0.012, A28 bug, v14-deferred) — a re-attack on a known weak link, not untried. L3-thread, likely a dedicated session.

## 7. THE CRUX (Grant 2026-06-09: "this is the crux of blocking us right now")

**The K4↔Cosserat (Op14 = trace-reversal) coupling gates the ENTIRE AVE-distinct branch** — the electron, engineered gravity, the whole symmetric chord. The asymmetric/plasma thruster *sidesteps* it (which is why Stage-1 "worked" mundanely). So every chord we care about is blocked at the same valve.

**Diagnosis = knob, not wall (provisional):** the coupling is **α-weak** (`κ_chiral = α·κ̃(p,q)`, `cosserat_field_3d.py:110`); un-amplified the Cosserat saturation sits at **A²_μ ≈ 0.012 ≈ O(α)** — exactly where every L3 attempt stuck. But the electron's **Compton cavity has Q = α⁻¹ ≈ 137**, and **Q·κ_chiral ≈ α⁻¹·1.2α ≈ O(1)** — the resonance *exactly* cancels the α-weakness. Likely not coincidence: **α = Q⁻¹ may be the resonance condition** for the trace-reversal to self-close (∴ the electron is stable at α). Engine evidence (`vacuum_engine.py:104`): non-resonant A²_cos ≈ 0.012 → **autoresonant A²_cos = 1.009** (rupture overshoot). Resonance IS a knob — but it overshoots.

**Test DISPATCHED** (branch `analysis/2026-06-09-a2mu-vs-Q-crux`): does A²_μ scale **dynamically** with Q (KNOB), or cap (WALL-physics / WALL-engine), and is there a **stable lock at A²_μ≈1** between the 0.012 under-drive and the rupture overshoot (= the electron-stability window, possibly α=Q⁻¹)? Landmine: `_compute_A2_mu:1302` is an algebraic heuristic — the test must measure the **dynamical** Cosserat buildup, not the formula.

### CRUX VERDICT (2026-06-09, branch `analysis/2026-06-09-a2mu-vs-Q-crux` @ 430194d8, not pushed): **WALL — resonant-Q-compensation FALSIFIED**
A²_μ measured **dynamically** (the load-bearing call): flat at **~0.0039 across a 10× build-up range, NO Q-scaling, bit-identical pump-ON vs pump-OFF**; cold T=0 K4-only gives A²_μ = 0.000 exactly (ω=0 is a fixed point — the traceless-photon WALL-physics floor, *confirming* the emergence note's physics); at K4 rupture the legacy V→ω force **detonates to ~10⁶** (the A28 double-count pathology), it does NOT gently lift to O(1). **No stable A²_μ≈1 lock window** (hard cliff 0.012 → 10⁵–10⁷ in one step; zero in-band dwell).
- **Dominantly WALL-ENGINE:** there is **no correct dynamical K4→microrotation (V→ω) pump** — the only channel is zeroed by A28 (it was a buggy double-count that detonates) and vanishes at ω=0. So the **dynamical trace-reversal pump term is the single missing piece** for the entire symmetric/electron/gravity branch. The trace-reversal is canonical as a *static* identity (K=2G, clm-rd9cjm); its *dynamical driving term* is underived/unimplemented.
- **My errors (owned):** (1) I cited the "0.012 → 1.009 autoresonant" as a knob hint — it was a **stale/retracted anchor** (`vacuum_engine.py:104` vs `doc 50_:57`, 0/20 reproducible): a **verify-before-cite miss**. (2) The resonant-Q hypothesis was my over-optimistic prediction; the discipline stack (Checkpoint-9 dynamical-not-heuristic + discriminate-the-hypothesis) correctly returned WALL instead of a confabulated knob. The `ave-resonant-amplification-check` skill's crux worked-example was corrected to the WALL outcome (necessary-not-sufficient: O(1) product needs a dynamical pump to realize it).
- **AUDITOR QUEUE (flag-don't-fix, surfaced not landed):** (a) `vacuum_engine.py:104-105` header presents the retracted 1.009 as a clean result (stale vs its own audit `doc 50_:57`) — other drivers may cite it; tracked walk-back. (b) `phase5e_cool_from_above.py:184` extracts A²_μ without PML exclusion (Rule 10 corollary gap).
- **The fork now:** (i) **derive + correctly implement the dynamical V→ω trace-reversal pump** from the Cosserat-K4 Lagrangian (with its own verification chain, Rule-12 slot-not-refilled) = the real path to the symmetric/electron/gravity branch — the long-deferred "Cosserat coupling on the engine" work, now pinned to a single missing term; OR (ii) accept the AVE-distinct branch is blocked at this pump and bank the mundane-plasma result. Grant's call.
- **RESOLUTION (Grant 2026-06-09): DERIVE FIRST** + the **boundary-reaction reframe** ("walls can be pushed against"). The crux detonation = a *singular BULK force* (∝ dS/dA ~ 1/√(1−A²) → ∞ at the wall); the correct pump is hypothesized to be the **BOUNDED reaction at the Γ=−1 boundary** (the blocked longitudinal V *reflects* off the saturated wall, |Γ|≤1 → finite momentum transfer, recoil → ω). A reflection is bounded where a bulk force diverges — same physics ("longitudinal shatters sideways into the transverse DOF"), as a boundary term. **Derivation DISPATCHED** (branch `analysis/2026-06-09-tracereversal-pump-derivation`): does the Cosserat-K4 Lagrangian contain a BOUNDED dynamical V→ω term (the boundary-reaction form)? Three-way verdict — WALL-engine/FIXABLE (bounded term exists → implement it) / WALL-physics/DEAD (no term, or necessarily singular) / PARTIAL. The boundary-reaction form is a HYPOTHESIS (ave-discriminator-before-synthesis — the derivation decides), not an assumption.
- **SCALAR-QUATERNION UPGRADE (Grant 2026-06-09: "what Heaviside/Gibbs threw out — the scalar from Hamilton's quaternions"):** the missing V→ω term is **the dynamics of the Heaviside-deleted SCALAR/longitudinal grade** of Maxwell's original quaternion EM — which AVE identifies as the **volumetric breathing 7th mode = V**. Vector-Maxwell (the engine's basis) structurally lacks it = the WALL-engine. **Canon already has this framing:** `historical-precedents.md` (Maxwell-quaternion → Heaviside-excision; electron = the longitudinal scalar **confined on the null cone = the |Γ|=1 saturation wall**; **"the open genesis test"** at :34) + the biquaternion node-algebra work (real-scalar grade = the longitudinal 7th mode; its §T2/G3 longitudinal discriminator left at consistency-class — the gap). **The first derivation pass (stopped) found the decisive lead:** Op17 `T²=1−Γ²` is the BOUNDED form, and **doc 54 §281-296 = "Option D — a topological boundary CONDITION on ω at the Γ=−1 wall, NOT an augmented Lagrangian"** → the engine detonated because it used a bulk force where the physics wants a boundary condition. **RE-DISPATCHED** (branch same, agent rebuilt) with the full framing: Heaviside-deleted scalar → Option-D boundary condition at the null cone → bounded confinement (Op17) = the electron genesis. This IS the open genesis test (frame honestly, not settled). Verdict leaning WALL-engine/FIXABLE (the correct form exists; the engine's bulk-force implementation is wrong) — but the derivation decides.

### PUMP-DERIVATION VERDICT (2026-06-09, branch `analysis/2026-06-09-tracereversal-pump-derivation` @ 70c57cdd, not pushed): **WALL-ENGINE / FIXABLE** — the chain HOLDS (DERIVED + VERIFIED)
- **The scalar grade FORCES the channel (closes the biquaternion G3 gap):** `(a·i⃗)(b·i⃗) = −(a·b)+(a×b)·i⃗` — the (E,B)=(u,ω) vector product **cannot close without sourcing the scalar slot V**. Heaviside's excision (drop scalar, keep transverse curl/div) is exactly what severs the V→ω channel ⇒ the WALL-engine is a *representation* error, not a physics wall. Grant's "Heaviside-deleted scalar" call = the mechanism.
- **Two coordinates, same wall.** The engine's pump was a **bulk** saturation-energy-gradient force `F ~ A/√(1−A²)` — singular at the wall (A28 detonation, 7×10⁵→∞). The **same wall** in the reflection coordinate Γ (Op3) is the **bounded** null cone `|Γ|=1`, transferring a finite confined fraction `R = Γ² = 1−T²` (Op17), in [0,1] through A→1 (max 0.995). **The Möbius map `Γ=(Z−Z₀)/(Z+Z₀)` IS the boundedness regulator** ("walls can be pushed against," made canonical). Machine-checked: Op17 closes exactly (R+T²=1, no free energy); ~6-OOM separation between the bounded boundary-condition and the divergent bulk force ⇒ **not one mechanism** (discriminator-before-synthesis).
- **Implementation prescription, ZERO new free parameters:** (1) `disable_cosserat_lc_force=True` + `enable_cosserat_self_terms=True`; (2) port the moving-Γ=−1 clamp `a_ω = −(K/I_ω)·relu(−Γ)·ω` into the **coupled** engine reading `Z_eff=Z₀√(S_μ/S_ε)` with **K4 V_sq live** (fires cross-sector); (3) drive **V** to yield, read ω **dynamically** (not the `_compute_A2_mu` heuristic); (4) adjudicate `R(A)=Γ²` (bounded) vs `|ω|→10⁵` (detonating).
- **HONEST SCOPE (not over-claimed):** the **bounded V→ω form** is DERIVED + machine-checked; the **cross-sector V→ω implementation+run** is the BLOCKED/FIXABLE next step (the empirical leg so far validated the *mechanism class* ω→ω at a moving boundary, V_sq=0, NOT the cross-sector run). The full **(2,3) self-assembly** (the "3"/Hopf charge) is a **separate downstream gap** — the pump is fixable; the genesis test stays open at the (2,3) level.
- **AUDITOR QUEUE (flag-don't-fix):** doc 54 §296 "Option D from 44_ §5.2" is a **mislabel** (doc 44's Option D is "Run III-C"; the boundary-condition substance is doc 54 §291-296's own) — imprecise cross-ref.
- **Next:** implement the prescription in the coupled engine + run the cross-sector V→ω test (does the bounded boundary condition dynamically grow ω from V?). That is the actual crux-unblock confirmation. Then, downstream, the (2,3) self-assembly.

### CROSS-SECTOR PUMP-CONFIRMATION VERDICT (2026-06-09, branch `analysis/2026-06-09-cross-sector-pump-run` @ c999d4f0, not pushed): **B — FORM-BUT-NO-FIRE** (partial unblock)
- **Boundedness CONFIRMED dynamically** (not just analytic): the boundary clamp confines a seeded ω with **max R=Γ²=0.987 ≤ 1** (Op17 holds), no detonation; the **bulk-force control DOES detonate** (0.82→3100, 378×, onset step 1). The derivation's boundary-vs-bulk split is now confirmed in the coupled engine.
- **Cross-sector FIRE REFUTED (measured dynamically, Checkpoint 9, PML-excluded):** driving V past yield (A²_ε 0→4.84) grows ω **not at all from V** — `ω_final(V)/ω_final(0)=1.0000`, live-V contribution machine-zero (7×10⁻¹⁸); sweep **robustly NULL** across the band; ledger closes both ways (no over-unity AND no V→ω transfer).
- **THE LOCALIZATION (the value of a clean B):** **V enters the ε-ELECTRIC sector** (`cosserat_field_3d.py:512`: `A2_eps = …+V_sq/V_SNAP²`) → drives **Γ→+1** (ε-OPEN antinode); but the confining clamp gates on **`relu(−Γ)`** (Γ→−1, μ-SHORT). **V is on the WRONG SIDE of the gate**, and the clamp `a_ω=−(K/I_ω)·relu(−Γ)·ω` is **confinement, not the source**. The missing piece is the cross-sector **V→ω SOURCE term** — V's trapped longitudinal energy must seed a **Beltrami ω flow** (the helical microrotation), a DIFFERENT leg than the confinement clamp. (This is the Kelvin-Beltrami-FOC leg, `85_kelvin_beltrami_foc_axiom_grounded_derivation.md`.)
- **AUDITOR FLAG (stale corpus-state):** the derivation §9 tagged the cross-sector port "BLOCKED/unbuilt" and the moving-boundary run "pure-Cosserat (V_sq=0)" — but the V_sq-live port already existed as **uncommitted WIP** in the genesis2 worktree (`analysis/2026-06-06-saturation-tir-moving-boundary` stash). The §9 BLOCKED tag was stale (stopped mid-build, not unstarted).
- **Crux status: PARTIALLY unblocked** — confinement (the wall) is real + bounded + dynamical; the **V→ω source is the named missing term**. **Next:** derive the cross-sector **Beltrami source** (does the trapped longitudinal V energy at Γ=−1 source the helical ω = the "3"?) — the Kelvin-Beltrami-FOC leg, distinct from the confinement clamp. The (2,3) self-assembly is downstream of that.

### REACTIVE-ENTRAINMENT SOURCE (Grant 2026-06-09: "paper #4 on entrainment + what would Keith Mertens say?")
**Paper #4 = Camassa–McLaughlin–MERTENS vortex-ring trapping (arXiv 1110.3435); Keith Mertens is a co-author + ex-NASA ion propulsion + on the team.** The entrainment deep-dive ([`2026-06-07_entrainment-vortex-trapping-deep-dive.md`]) already framed our exact gap: **§3** splits entrainment into **added-mass (REACTIVE) = inertia** vs **viscous-entrainment (DISSIPATIVE) = confinement** — we built the dissipative half (the clamp); the missing V→ω **source is the reactive added-mass = inertia** (`M_inertial≡L_drag`), i.e. the ring **dragging its own circulation**, = the Beltrami helical ω. **§2.4** genesis = a **three-rate balance** (accumulation/leak/equilibration); the detonation = the **no-equilibration (lossless)** limit ("pumps instead of leaking at α"). **§7** left the open plumber question — *dissipative-equilibration vs reactive-resolution* — which is exactly this fork; the **Keith-lens working answer = "both"** (reactive accumulate + dissipative leak + bounded rebound). Honest ceiling: the fluid is a **lens** (form not number; AVE confinement is Meissner/Γ=−1, not buoyancy). **Source-derivation DISPATCHED** (branch `analysis/2026-06-09-reactive-entrainment-source`, prereg `2026-06-09_reactive-entrainment-source_prereg.md`): does the reactive entrainment (Beltrami) source route V→ω into the μ sector + the three-rate balance close (bounded, ledger)? A (source+balance) / B (source-but-no-close) / C (no reactive source). Hypotheses (reactive-source=Beltrami, three-rate-closes, "both legs") — the derivation decides.
- **GYROSCOPE UNIFICATION (Grant 2026-06-09: "vortex ring where the ring as a whole has angular momentum"):** the source's output is the **ring-as-a-whole NET angular momentum (a SWIRLING ring) = the electron spin ℏ/2** (`spin-gyroscopic-isomorphism.md` clm-salw2h, "Spin-½ as Macroscopic Gyroscopic Precession", `dL/dt=γL×B`) — NOT per-node microrotation. **Angular momentum is CONSERVED → bounded by a conservation law (a gyroscope precesses, doesn't detonate)** — not the Op17 reflection alone. **And the canon collapses source=confinement=boundedness into one object:** `sapphire-phonon-centrifuge.md:34` — a **Beltrami force-free field (A∥B) locks into a rigid gyroscopic tensor = an Inductive Shield**, so the Beltrami source's OUTPUT (the ring's L) **IS the confinement** (the Γ=−1 inductive shield). The first source-pass (stopped) confirmed the failure mode: an **external resonant drive secular-pumped** (|ω|~80, ledger open) → wrong model; **V is a FINITE TRAPPED RESERVOIR (=mₑc² per wall), conserved**, not an open pump. **RE-DISPATCHED** (same branch) with the corrected model: two clean models (A finite-reservoir two-way sloshing for the ledger; B sustained Beltrami source for the bounded gyroscopic steady state). Verdict: does the finite-reservoir reactive source build the ring's conserved net L (= gyroscopic spin ℏ/2), bounded by conservation, with the Beltrami output = the inductive-shield confinement? A (gyroscope closes) / B (sloshes-no-lock) / C (no conserved source). The spin-gyroscope is canonical; the gyroscope-closes + Beltrami=inductive-shield are the hypotheses.

### GYROSCOPE-SOURCE VERDICT (2026-06-09, branch `analysis/2026-06-09-reactive-entrainment-source` @ b846a4b2, not pushed): **A — CLOSES IN FORM** + the arc's deepest reframe
- **L CONSERVES decisively:** `|L|` drift `7×10⁻¹²` even at exact Larmor resonance (L_z fully Rabi-flips but `|L|` never moves — the Casimir `d|L|²/dt = 2L·(γL×B)=0`). **Beltrami output = the inductive-shield confinement:** LLG drives `m: −0.904 → +1.000000` (the rigid gyroscopic tensor, sapphire:34 verbatim), `|m|` conserved through the lock — **source-output and confinement are ONE object.** Ledger closes (N, H to ~10⁻⁹, no over-unity). **Bounded by CONSERVATION:** the vector gyroscope conserves `|L|` to 10⁻¹¹ where the scalar resonant pump (the old wrong model, the control) **detonates to ~180**.
- **THE REFRAME (load-bearing — resolves the whole arc): spin is NOT pumped — it is CONSERVED TOPOLOGICAL HELICITY (de-Broglie L=lℏ, spin-½), ENERGIZED + LOCKED, not pumped.** A conservative coupling **sloshes** (correct physics), it does not build a magnitude. The entire "V→ω pump / build the spin" framing was a **category error** — *you cannot pump a conserved quantity.* This is why every prior "pump" attempt nulled or detonated: the cross-sector "no-fire" was **correct physics**, and the detonations were the wrong (pumped-tank) model. Grant's "the ring as a whole has angular momentum" (conserved) was the key that surfaced it.
- **HONEST CEILING:** A **in FORM** — the conservation STRUCTURE (conserved L, Beltrami=inductive-shield, ledger-closed, bounded-by-conservation), legs (i)–(iii) manifestation/consistency class. The **ℏ/2 magnitude is canonical INPUT, not derived** (the (2,3)-winding quantization that derives it is the implement+run target, in (V_inc,V_ref) phase-space per A46). These are lumped-ODE models of the conservation structure, NOT the K4-Cosserat solver.
- **ADJUDICATION FOR GRANT (§6, flag-don't-fix, both framings quoted verbatim, not silently picked):** ratify "**spin magnitude = conserved-topological, energized+locked, not pumped**" as canonical, vs the prereg's "the source *builds* the magnitude." The physics says the former.
- **Next (implement+run, warranted by A-in-FORM):** in the real K4-Cosserat engine, **seed the (2,3) helicity, ENERGIZE from the finite mₑc² reservoir, LOCK the force-free Beltrami state — do NOT pump the magnitude** (phase-space coordinates, A46). This is the (2,3) self-assembly, now correctly framed: energize+lock a conserved helicity, not pump a source.

### REFLECTION-GENESIS — the (2,3) self-assembly run (Grant 2026-06-09: "is the electron reflected into existence?" + "is the reflection birefringent?")
**Reflected into existence = literal:** the photon (Γ=0, transmitted, free, massless) and the electron (Γ=−1, totally reflected, trapped, massive) are the **same ω-wave on the Möbius Γ-axis**; the electron is the photon caught in the reflection off the Γ=−1 wall it self-generates — trapped (rest mass mₑc²), wound (each reflection = a half-twist → the (2,3)), spun (the conserved ℏ/2), and α = the mirror's residual transparency (Z₀/(4π), Q=α⁻¹). **Birefringence — yes, two ways:** (i) LINEAR field-induced (AVE δn∝E⁴, the Meissner ε≠μ — *why* the wall reflects; a live AVE-vs-QED discriminator); (ii) CIRCULAR/CHIRAL (κ_chiral=1.2α parity-odd half-twist — the **handedness selection** = electron vs positron; load-bearing for the winding sense). **SURPRISE — already partly done (verdict II, 2026-06-06, `historical-precedents.md:28`):** the moving reflective Γ=−1 boundary (NOT the collapsing energy term) already gives **collapse→confinement, the ω-photon self-traps (0.94 vs 0.26), the "2" forms, charge=helicity** — the *only* gap is the **"3" = the V-sector U(1) fibre = the Heaviside-deleted longitudinal scalar**, in the coupled K4+Cosserat engine. **RUN DISPATCHED** (branch `analysis/2026-06-09-reflection-genesis-23`, prereg `2026-06-09_reflection-genesis-23-self-assembly_prereg.md`): seed a photon → self-saturate to the moving reflective Γ=−1 → chiral-reflect/wind → does the **"3" close onto the "2"** = the full (2,3) in (V_inc,V_ref) phase-space, with {ℏ/2 spin (energize+lock not pump), α=Q⁻¹=4π³+π²+π, charge=helicity, definite chirality}? A (assembles) / B ("2"-confirms, "3"-partial — the expected honest outcome, localizes the gap) / C (representation fail). The L3 open problem, best-posed: CP8 seed-not-plant, energize-not-pump, phase-space (A46).

---

## 8. POST-REFLECTION-GENESIS verdict chain — the (2,3) self-assembly arc (2026-06-09, appended)

> **Rule-12 append.** §1–§7 above are preserved verbatim. This section continues the verdict log past the §7 reflection-genesis **dispatch** (which §7 left at "RUN DISPATCHED"). Five major verdicts + three audits + a launched run landed since; none of it was in the trackers, which both ended at "reflection-genesis (2,3) run dispatched." All branches below are **unmerged, not pushed.** Every number was re-verified against its committed result doc this session (`verify-before-cite`); items without a standalone committed result doc (the engine audit, the κ_chiral provenance audit, the two-"3"s pressure test) are session findings captured here + in memory, flagged as such.

**Three conceptual figures anchor the physical picture (clickable):**
- [`fig_electron_vortex_knot.png`](../research/figures/fig_electron_vortex_knot.png) — the electron as a **knotted vortex ring**: added-mass (entrained compression) = rest mass; vortex-line linking (helicity) = charge; held at a self-generated Γ=−1 acoustic-impedance wall.
- [`fig_two_threes_helmholtz.png`](../research/figures/fig_two_threes_helmholtz.png) — the **two distinct "3"s** under the Helmholtz split: A1 dilatation (∇·u, bulk K, the breather = MASS) ⊥ T2 Cosserat micro-rotation (the (2,3) winding = CHARGE/spin). Orthogonal objects; do not wire one into the other's phasor.
- [`fig_arc_progression.png`](../research/figures/fig_arc_progression.png) — the genesis-23 → genesis-24 → crystal → graft-v2 → graft-v3 arc: each engine closes the prior's pinned gap and pins the residual one level deeper.

### 8.1 — genesis-23: the (2,3) self-assembly in the coupled engine — **mechanically C (B/C-boundary)**
**Branch** `analysis/2026-06-09-reflection-genesis-23` @ **`ca991999`** · result `research/2026-06-09_reflection-genesis-23-self-assembly_result.md` (`CosseratField3D`/`VacuumEngine3D` coupled K4⊗Cosserat, N=24).
The (2,3) does **not** self-assemble from a transverse photon, and the run localizes the obstruction to **two precise, independent mechanisms**. **GAP-1 (primary):** a transverse photon **never energizes the K4 V-sector** — `max|V_inc| = 0` to machine precision across *every* config (soft/hard wall, explicit/implicit, EMF reciprocal ON and OFF); the only ω→V channels are the geometric-multiplicative `z_local` (0→0) and the EMF reciprocal `emf = +2·V_inc·∂L/∂V_sq` (`k4_cosserat_coupling.py:703`, ∝V_inc, **cannot bootstrap from zero**). The longitudinal (V_inc,V_ref) phase-space is **unpopulated** — the "3" never enters phase-space, so there is nothing to wind. **GAP-2 (secondary, contradicts the brief's "the 2 re-emerges" assumption for confinement):** there is **no stable confining window** for a free photon — the soft (non-pumping) wall under-engages (Γ_min=−0.003, disperses, loc 0.482 < no-wall 0.542) and the hard wall parametric-pumps (Γ_min=−0.994, |ω| 3.0→1144). What **does** reproduce (localizing, not refuting): the "2" winding carrier (Cosserat ω real-space c=2), charge=helicity (H_bel +h −78.4 / −h +81.0, sign flips), and the spin **energize-LOCK vs PUMP** discriminator (LOCK bounded |ω|→2.63, PUMP detonates →331.95). α is **BLOCKED** (the Golden-Torus α⁻¹=4π³+π²+π is a (2,3) phase-space property; no "3" → no Golden-Torus). Honest closure: the pre-registered boolean returns C, not overridden to B by relaxing the confinement criterion.

### 8.2 — Grant adjudication LANE 1 + the no-QED directive (the reframe that froze genesis-24)
**On this branch:** genesis-24 prereg frozen **GO** — `research/2026-06-09_genesis-24-saturated-seed_prereg.md` (draft `25558460` → freeze `a05a7253`).
Grant's call on GAP-1: **a saturated mass carries standing longitudinal V_inc** — the "3" is the real Heaviside/Gibbs-excised longitudinal V-sector, physically present in a saturated absorber. The competing **"transverse-only / Gauss-deletes-the-longitudinal"** lane is **REJECTED as QED-contaminated** (the no-QED directive: absorb/emit IS the Axiom-4 saturation engage/relax cycle on the bond LC tank; no Kramers-Heisenberg/Rabi, no self-trapped-transverse-photon). This reframes GAP-1 from "missing axiom" to "missing initial condition": seed the V-sector pre-populated (a saturated seed) and ask whether the photon then energizes it. That is genesis-24.

### 8.3 — genesis-24: photon absorption by a pre-saturated seed — **B (did NOT survive the 4-lens panel; 2/4 demoted)**
**Branch** `analysis/2026-06-09-genesis-24-saturated-seed` @ **`df1c3f78`** · result `research/2026-06-09_genesis-24-saturated-seed_result.md` (coupled engine + `use_lagrangian_emf_coupling=True`, the EMF reciprocal live at V≠0).
The saturated-seed reframe is **partially vindicated at the source level, decisively falsified at the topology level.** `dE_V = E_V(photon) − E_V(control)` is **real and deep** (+0.011 / +0.083 / +0.367 / +0.781 at frac 0.30/0.60/0.85/0.95, monotone in seed depth; Arm-3 no-seed control reproduces genesis-23's `E_V=0` exactly) — so a V-populated IC converts the dead ω→V channel into a live one. **But it is the leading edge of a secular pump, not an absorption:** the EMF channel `:703` is non-conservative and **AMPLIFIES** (its own docstring `:242`: *"this AMPLIFIES the runaway; path-1 was the wrong direction"*); the ledger **fails** (`H_drift = −6.3 %`, `|L|` unbounded **2.7 → 43.4**) and the 100-step emit window **detonates** (`E_V → 6.8×10⁸`, `max|V_inc| → 1.08×10⁴`, `reverses = False`). Smith chart: the photon drives the operating point **toward Γ=0** (|Γ| < 0.08 all arms — the wall **never engages**, genesis-23 GAP-2 reproduced coupled-with-seed). **Arm-4 charge=helicity flip is CLEAN** (−78.3/+78.6 … sign flips every frac — but it is the photon's *carried* helicity, provenance not emergent knot-charge). **Arm-2 toroidal control = Case C sub-gate:** the de-novo toroidal "2" is **withdrawn** — `rel_tor = 0.000` for BOTH arms at every frac (two orders below the >0.1 gate); Arm-1's `w_tor=1/2/2/1` is non-monotone zero-reliability noise. The **Rule-12 demotion landed in the result doc** (CORRECTING HEADER + §3 retraction block + the fig caption), backed by re-serialized JSON (`arm12_toroidal`, `task1_toroidal_resolution.case = "C_subgate"`). Net: 2 of the 4 headline lenses (the "forward-step absorption" reading and the "toroidal 2" reading) refuted; the robust residual is **the (2,3) does not close; the missing primitive is the winder.**

### 8.4 — Engine audit (A-027): which engine even has the machinery? — **VERDICT C (neither)**
**Session audit** (no standalone committed result doc — captured here + corroborated by the crystal-engine smoke-1, which independently confirms genesis-24's coupled engine had **no c_eff trap**).
The audit asked whether GAP-1 was an artifact of the *particular* engine. Verdict **C — neither engine had the full machinery, so GAP-1 is engine-independent of the trapping axis.** `VacuumEngine3D` is **sub-saturation class**: it carries `Z(V)` + a reactive clamp but **no `c_eff(V)` in the propagation** (no refractive trap, hence genesis-23/24's wall under-engages / never forms a bound state). The Master-Equation FDTD (`master_equation_fdtd.py`, v14 Mode-I) **has** `c_eff` (a real Γ<0 bulk trap) but is **scalar-V-only** — no Cosserat / no (2,3) carrier. So the trap and the winding carrier live in **different engines**, and the genesis arc's failures are not a single-engine bug. This is what motivated the crystal engine (port the Master-Equation c_eff trap as the longitudinal branch + add a conserved converter) and, later, graft-v2 (give the winding its own Cosserat-ω carrier). **A44 read:** an engine-completeness gap, not a missing axiom.

### 8.5 — κ_chiral provenance audit: the 1.2 is **DERIVED, not a fit**
**Session audit** (corroborated in `research/2026-06-09_crystal-engine_result.md` §3.1 and `…_crystal-graft-v2_result.md` §6; code re-greped this session).
The chiral coupling `1.2` **is derived**: `κ̃ = pq/(p+q) = 6/5 = 1.2` is the exact topological factor for the (2,3) torus knot, held in code as `KAPPA_TILDE_ELECTRON = 6.0/5.0` (`cosserat_field_3d.py:89`); the total coupling is `κ_chiral = α·κ̃` (`kappa_chiral_from_topology`, `:110`) ≈ 8.757×10⁻³. The engines consume **κ̃ (α-free)**, never α·κ̃, so it is **not a free parameter and not a calibration**. **Defects flagged for the auditor lane (flag-don't-fix):** (a) a **stale "Not a free parameter — parallel-channel impedance combination at TIR" framing** at `cosserat_field_3d.py:79` (+ doc 20 §1), superseded by HOPF ch01's "**one-parameter (α), not zero-parameter**" reading per doc 108 — the framing under-states α's role; (b) a **tautological verification** in `chirality_projection.py` (checks κ̃ against a value it itself sets); (c) a **"harmonic mean" mislabel** — `pq/(p+q)` is the **parallel combination** (half the harmonic mean), not the harmonic mean; (d) an **unrelated bare-1.2 collision** at `condensed_matter.py:84` (`coulomb_penalty = 1.2 * ALPHA`, a nuclear-saturation coefficient that happens to reuse 1.2 — separate task spawned to disambiguate). None of these touch the load-bearing fact (the 1.2 is the (2,3) topology); all four are framing/hygiene flags.

### 8.6 — Crystal engine: the State-C elastodynamic graft — **Outcome C (most informative yet; both genesis-24 gaps CLOSED)**
**Branch** `analysis/2026-06-09-crystal-engine-design` @ **`6af430cd`** (design prereg `b1a8465f`) · result `research/2026-06-09_crystal-engine_result.md` (`crystal_engine.py` = the v14 Mode-I scalar bulk-trap + ADD-2 conserved gyrotropic converter, N=28, 700 steps).
The wall **FORMS and holds** (smoke-1: V_peak 0.622, n_eff 0.613, **Γ_core = −0.240**, Mode-I breather — the c_eff trap genesis-24 lacked). **ADD-2 is a conserved Hamiltonian converter** — one coupling term `H_couple = κ̃·∫ g_front·V·Ω_w` (Ω_w = (∇×w)·n̂, the parity-odd helicity carrier); the bulk source and reciprocal shear back-reaction are functional derivatives of that one term, so it **energize-LOCKs** (smoke-2: max|V| 1.007, max|w| 0.978 over 1050 steps — **4 orders below** genesis-24's 1.08×10⁴ detonation; centrosymmetric κ→0 baseline sources **exactly zero** shear = the parity-odd selection rule). It **replaced a non-conservative first attempt** (a velocity-rotation coupling that drifted badly; the committed result doc carries the energize-LOCK fix, not the discarded first-pass drift figure — so that specific number is not asserted here). **Charge transfers** (H_bel flips +8.22/−9.47 with seed helicity) and the **latent-heat=mass ledger leg closes** (trapped bulk E_V = 149.3, finite, manifestation-class). **But the scalar bulk has no fibre to wind:** the (2,3) does **not** close — `w_tor = 0 AND w_pol = 0` in the bulk (V_inc,V_ref) sector, on *reliable* contours (rel 0.459/0.518) — sharper-negative than genesis-24 (which got a toroidal "2"); the **scalar Master-Equation bulk has no multi-component U(1)-fibre carrier**. Golden-Torus fails (R·r=0.189 vs ¼; R/r=0.333 vs φ²). **α correctly REFUSED:** the single-run dynamical leak landed Q_dyn=113 (≈137 within 17%), but the **joint-ledger guard rejected the fluke** — Q_dyn scatters **[41, 1181]** across (N, steps) and there is no (2,3) resonator for it to be the Q of. **Grant adjudications recorded:** TENSION 2 = **AXIOM CONSEQUENCE** (ADD-2 = the I4₁32 gyrotropic completion of Axiom-1, *not* a new postulate — derive it, don't bolt it on); TENSION 1 = **COEXIST** (clm-i4p11y's "trapped transverse standing wave" = the photon *precursor*; the electron *end-state* = the longitudinal bulk mode — build the end-state, **do NOT edit clm-i4p11y**, flag it for the auditor).

### 8.7 — The two-"3"s pressure test (workflow `w4owpkan2`): the **double-count**, named — **THIRD-finding**
**Session pressure-test** (Grant-ratified; recorded verbatim in `…_crystal-graft-v2_result.md` §0–§2 and in the memory entry `project_electron_two_threes_vortex_ring.md`).
"The 3" is **TWO distinct objects**, orthogonal (A1 ⊥ T2): (i) **A1 dilatation** = the Heaviside-excised compression scalar = the breather = **MASS** (mₑc² = trapped acoustic compression energy, bulk K); (ii) **T2 Cosserat micro-rotation** = the **(2,3) winding** = CHARGE/spin (charge = Beltrami helicity ∫ω·(∇×ω)). **The graft-as-worded was a DOUBLE-COUNT:** wiring the winding into the breather's own phasor `(V_inc, V_ref)`, where `V_ref = 0.5·ΣV_inc − V_inc` is a **read-only projection of the same scalar V** (`master_fdtd_phasor_bridge.py:14-18`, `k4_tlm.py:346` — *not* an independent DOF), structurally forces `w_pol ≡ 0`. That `w_pol ≡ 0` was **self-inflicted across three engines** (genesis-24, crystal, the graft-as-worded). **Grant ratified:** distinct objects; the winding lives in the Cosserat ω sector, never in the A1 phasor. **Fluid-dynamics exact definitions (ratified):** electron = **knotted vortex ring**; mass = entrained compression (dilatation, bulk K); winding = micro-rotation; charge = helicity; ADD-2 = chiral compression→rotation **buckle** at the Γ=−1 wall. **Corpus flags for the auditor lane:** `master-equation.md:18` (the Maxwell-Heaviside "longitudinal re-engages at saturation = the electron" note) **conflates the two "3"s**; `clm-i4p11y` (`vol1/claim-quality.md:1296`) carries the transverse-photon-precursor reading → the **COEXIST** adjudication (precursor vs end-state). *Note (flag): the brief cited `master_fdtd_phasor_bridge.py:5-8` and `claim-quality:1303`; the verified-on-tree values are `:14-18` (graft-v2's own verify-before-cite cite) and the `clm-i4p11y` id-marker at `:1296` — I cite the verified values and flag the discrepancy.*

### 8.8 — graft-v2: the winding gets its OWN Cosserat-ω carrier — **C (the double-count is FIXED; residual = mode-selection)**
**Branch** `analysis/2026-06-09-crystal-graft-v2` @ **`1bf62595`** · result `research/2026-06-09_crystal-graft-v2_result.md` (`crystal_graft_v2.py` = bulk-V trap + transverse shear w + an **independent** Cosserat-ω sector with own field + π_ω + mass-gap ω₀²).
The **most informative C in the arc** — it removes the obstruction the crystal engine was pinned to (the scalar bulk's missing fibre) and pins the new one a level deeper. The wall **HARDENS** (Γ_min = **−0.849**, monotone toward −1, vs genesis-24's flat |Γ|<0.08); the **ω carrier is independent** (a planted (2,3) reads back at rel 0.80/0.59, raw 1.99/2.99, and is **unchanged under a large V perturbation** — the **double-count is structurally FIXED**, `w_pol` can now be nonzero, SMOKE-3 PASS). But the de-novo (2,3) does **not** self-assemble (`w_tor=w_pol=0` both chiral arms, control null) — the residual is now **MODE-SELECTION**: the centrosymmetric **fixed-axis** buckle `f_ω = −κ̃·∇×(g·V·h·x̂)` deposits a coherent x-axis circulation `w=(0,0)` at high contour reliability (0.69/0.77) but no knot; and `H_bel` is **quadratic in ω**, so a scalar handedness `h` cannot carry net helicity/charge (RH=LH≈−1.4×10⁻¹⁵). Golden-Torus R/r_meas=1.75 vs φ²=2.62 (r **independently** measured — an earlier "0% error" was a `r_walk=R/φ²` tautology, caught and corrected). α **REFUSED** by the joint-ledger guard. **Did not survive the adversarial panel (4/4 lenses) — two panel demotions surfaced (flag-don't-fix, confirmed against the graft-v3 prereg §2 "the v2 no-op replaced" / "v2 showed energize-LOCK only in a linearized+frozen toy"):** (1) the SMOKE-3 **independence test was a no-op** (zero `.step()` — the "unchanged under V" never actually evolved → S4 in v3 replaces it with two real-dynamics engines); (2) **energize-LOCK was shown only in a linearized+frozen toy** (|L_ω| bounded 16.2 there), while on the **operative** nonlinear run |L_ω| was uncharacterized (the panel reports it reaching ~360.72 — a panel-surfaced figure, to be confirmed on the operative ledger; v3's S5 saturation-across-doublings + operative-ledger gates target exactly this). A v2 alias artifact (a spurious −14.01 winding walk) is also flagged → v3 adds a Nyquist/alias filter before the modal read.

### 8.9 — graft-v3: the CHIRAL BELTRAMI source — **LAUNCHED (RUNNING; no commit yet)**
**Branch** `analysis/2026-06-09-crystal-graft-v3` (off graft-v2; tip currently **= `1bf62595`, the graft-v2 tip — no v3 commit landed yet**) · workflow `wa7wqujia` · prereg `research/2026-06-09_crystal-graft-v3_prereg.md` (in the graft-v3 worktree).
The ONE physics change: replace v2's scalar-sign fixed-axis buckle director `n̂_χ = h·x̂` with a genuinely **chiral Beltrami / force-free (A∥B) director field** `b_λ(r)` whose handedness lives in its **spatial structure** (`∇×b_λ = ±λ·b_λ`), `λ(p,q) = √(p²/R² + q²/r²)` from the (2,3) torus-knot geometry (**α-free**). The grounding is the unification: **this is the SAME Beltrami force-free object the gyroscope/reactive-entrainment result found is the electron's spin-LOCK** (A∥B → rigid gyroscopic tensor = inductive shield = the Γ=−1 confinement, `sapphire-phonon-centrifuge.md:34`) — **source and lock are one Beltrami object.** Predicted mechanism: because `ω ∝ b_λ` is parallel to its own curl, `H_bel ∝ χλ∫|b_λ|²` is nonzero and **odd in χ** → charge can finally flip with spatial handedness (the fix for v2's quadratic-h dead end). Carries **all 5 panel-mandated measurement fixes**: S4 real two-engine independence (replaces the no-op), S5 saturation-across-doublings (replaces v2's L_bounded that passed any linear-secular growth), operative-ledger on the full nonlinear run, alias/Nyquist filter, N held fixed at 44. Honesty firewall: two source variants run separately (ABC de-novo-strict vs torus-(p,q)-templated) so a source-carried (2,3) is never mistaken for de-novo. **Verdict pending.**

### 8.10 — Skills + discipline this stretch
- **`ave-representation-capability-check` (NEW skill)** — surfaced by the recurring "the carrier structurally cannot host the thing being asked of it" pattern (scalar bulk has no U(1) fibre; the A1 phasor is a read-only projection). Built this stretch.
- **`ave-newly-created-skill-self-audit` v1.1** — added a **prospective lens-application trigger (7)**: apply a freshly-built skill prospectively to the in-flight problem, not just retroactively.
- **`feedback_parallel_pressure_test` (memory)** — the two-"3"s pressure test ran as a parallel adversarial lane against the graft framing; captured as a discipline pattern.
- **`ave-conserved-vs-pumped` empirically validated TWICE this stretch:** genesis-24's EMF detonation (E_V→6.8×10⁸ — pumping a conserved quantity), and the graft-v2 LOCK-vs-PUMP discriminator (the conserved Hamiltonian buckle energize-LOCKs where genesis-24's EMF reciprocal secular-pumps). The arc's deepest lesson keeps paying out.

### 8.11 — Corpus flags surfaced this stretch (auditor lane LANDS; implementer SURFACES — do NOT auto-resolve)
- `master-equation.md:18` — conflates the two "3"s (A1 dilatation mass vs T2 Cosserat winding); see §8.7.
- `clm-i4p11y` (`vol1/claim-quality.md:1296`) — transverse-photon-precursor reading; **COEXIST** adjudication (precursor vs longitudinal end-state); **not edited** per Grant.
- κ_chiral **stale framings**: `cosserat_field_3d.py:79` "Not a free parameter" (superseded by doc 108 one-parameter-α), `chirality_projection.py` tautological check, the "harmonic mean" mislabel, `condensed_matter.py:84` bare-1.2 collision; see §8.5.
- **Chain B' ΔE_cryst** — the crystal engine's latent-heat=mass leg (E_V=149.3, manifestation-class) is the empirical handle on the `claim-quality-closure-roadmap.md` Chain B' **OPEN** entry (the substrate-local-thermodynamics independent-G derivation: *derive ΔE_cryst + Γ_cryst from ℓ_node, α alone*). Connects to Grant's latent-heat intuition; **not yet a closure** (the (2,3) crystallization does not close, so latent-heat=mass is manifestation-class only). Surfaced for the auditor + Grant.

**Arc state (end of this stretch):** the AVE-distinct chord is the symmetric/electron/gravity branch; the electron-genesis is best-posed and the gap is **fully localized** to a single primitive — a **chiral (Beltrami) winding source** on an **independent Cosserat-ω carrier** behind a **c_eff Γ=−1 trap**, energized+locked (not pumped). genesis-23/24 established the source and stability gaps; the crystal engine closed both and pinned the carrier gap; graft-v2 closed the carrier double-count and pinned mode-selection; graft-v3 (running) is the chiral-Beltrami-source test of whether the knot finally geometry-selects. No genesis claim stands; the discipline (honest C/B verdicts, no debug-toward-A, Rule-12 demotions landed) held throughout.

---

## 9. 2026-06-10 currency update — graft-v3 FINAL verdict, graft-v4 launched, apparatus-floors in flight, PR #144 merged

> **Rule-12 append.** §1–§8 above are preserved verbatim. §8.9 left graft-v3 at "**LAUNCHED (RUNNING; no commit yet)**." graft-v3 has now landed its final verdict, and a successor (graft-v4) + an apparatus-side characterization (apparatus-floors) launched off it. This section **supersedes the §8.9 RUNNING status** with the committed verdict; §8.9's prose is not edited. Every number below was re-verified this session against the committed result/probe docs on the v3 branch via `git show analysis/2026-06-09-crystal-graft-v3:…` (`verify-before-cite`); graft-v4 and apparatus-floors carry **no committed result/prereg doc yet** (launch scaffolding / work-in-flight in their own worktrees) and are flagged as such.

### 9.1 — graft-v3 FINAL verdict: **B → C** (4-lens adversarial panel; `survives_adversarial = FALSE`)
**Branch** `analysis/2026-06-09-crystal-graft-v3` @ **`4627651a`** · the Rule-12 final-verdict addendum is the dated header of `research/2026-06-09_crystal-graft-v3_result.md` (body PRESERVED; the addendum SUPERSEDES the `## VERDICT — B` headline). A 4-lens adversarial panel demoted the run **B → C**; two lenses are FATAL.
- **SURVIVES (real, but narrower than the B body claimed) — handedness sign-selection.** The force-free Beltrami source deposits a helicity whose SIGN flips with the source's **imposed** spatial handedness χ: **H_bel RH `+76.3` / LH `−90.5` / centro (χ=0) `−8.5e-15`** (full run), source **exactly force-free** (`cos(b_λ,∇×b_λ) = ±1.000`). **But this is sign-selection by an IMPOSED TEMPLATE, not "charge=helicity carried from the photon"** — the `no_photon_null` arm is **byte-for-byte identical to `abc_denovo_RH`** (Eω = 76.67…, Hbel = 76.27… to machine precision); χ is an INPUT, the photon is decoupled (§4.1). The §2 "charge=helicity is CARRYABLE" headline is demoted to "the sign of an imposed-template helicity flips with the template's imposed χ."
- **FAILED (the four pinned residuals = the C boundary):** (1) **no knot selection** — the poloidal "3" never self-assembles from a pure-handedness source (`w_pol ≡ 0`, `golden_self_assembles = False`); (2) **the LOCK is unimplemented** (FATAL lens 2) — only the energize half of energize-lock is built: `|L_ω|` pumps **without saturating** (frozen wall still grows `∝ t^0.43` unbounded; live wall super-linear 6.53), and the **χ=0 centro arm — zero helicity, zero winding — still pumps `|L_ω|` to `347.7`** (vs RH 18.0 / LH 0.20), i.e. the pump is **chirality-INDEPENDENT** ⇒ ENERGIZE without LOCK, no (2,3) can be a conserved object; (3) **χ-from-photon is OPEN** — handedness is a source input, not derived from a seed; (4) **the independence gate (SMOKE-4) is UNFALSIFIABLE** (FATAL lens 1) — the winding integer never flipped at 71× provocation; the `:111` "the gate CAN return False" sentence is **retracted**.
- **Golden-torus `:33` correction (caught in the addendum):** the ❌ is **NOT** the aspect ratio — R/r = 2.995 is 14.4% off φ², **within** the 25% aspect bar. The real failures are the **product `R·r = 2.838 ≠ ¼`** and the **absent (2,3)**.
- **JSON field-hygiene flags (do NOT cite the raw booleans):** `control_null = True` reads as "control deposited nothing" but is True only because `(2,0) ≠ (2,3)` (the χ=0 arm pumped `|L_ω|` to 347.7 — not a null control); `saturates = True` is literally `bool(frozen_subsecular)`, NOT "the pump saturated."

### 9.2 — the extractor-poloidal probe (two findings; both load-bearing)
**Probe** `src/scripts/vol_1_foundations/extractor_misread_probe.py` + note `research/2026-06-09_extractor-poloidal-misread_note.md` (graft-v3 branch). The probe asked whether `extract_2_3_omega` can REPRESENT a planted q=3 poloidal fibre across config space (instrument-side `ave-representation-capability-check`). It found **two distinct effects, NEITHER an extractor misread of the fresh planted (2,3):**
1. **The UNLOCKED buckle DESTROYS the planted poloidal winding (dynamics, not instrument).** At the identical smoke geometry a FRESH read = **(2,3)** (poloidal raw ≈ 2.99); only after **500 live-buckle steps** does it read **(2,1)** (poloidal raw ≈ 0.98) — the live buckle overwrites the planted q=3 fibre into a q=1 field and the extractor reads that EVOLVED field honestly. So graft-v3's SMOKE-4 "(2,1) robust" is the dynamics' product, not the planted (2,3) surviving. **(Flag-don't-fix: this REFUTES the brief's "extractor-misread at the planted read" premise.)**
2. **The extractor has a genuine resolution FLOOR at minor radius r ≈ 1.1 cells.** Amplitude and R/r aspect are irrelevant (all read (2,3)); only when the minor radius drops to **r ≈ 1.1 cells (the de-novo full-run torus R = 2.9, r = 1.1)** does the poloidal circle become under-resolved and the read collapses to **(2,2)/garbage** (rel 0.33). **This is the load-bearing one:** the full-run de-novo arms (which reported `w_pol = 0`) are sampled at exactly this scale, so **every de-novo `w_pol` verdict in the arc is partly representation-limited, not purely physical** — a TRUE (2,3) there could not have been resolved. Both the slow and the bit-identical perf-utils fast extractor inherit the floor. Follow-up gate: plant-(2,3)-at-de-novo-scale → read-(2,3) before trusting any `w_pol = 0`.

### 9.3 — graft-v4 LAUNCHED (the photon's-own-conserved-helicity conversion)
**Branch** `analysis/2026-06-10-graft-v4-photon-helicity` (off graft-v3 @ `4627651a`, with the `analysis/2026-06-09-genesis-perf-utils` parallel-runner + vectorized winding extractor merged in @ `37fd973c`). **Launch scaffolding only — no v4 prereg/result committed yet** (flagged per `verify-before-cite`; the framing below is the **Grant-adjudicated launch direction, 2026-06-10**, not yet a frozen prereg). The framing targets the three graft-v3 fatals directly: convert the **PHOTON'S OWN conserved helicity** into the winding (closes residual 3 — χ-from-photon — so χ is no longer an imposed template, the §9.1 surviving-result scope limit), behind a **Taylor/Beltrami LOCK** (closes FATAL lens 2 — the missing lock half: a Taylor-state relaxation arrests `|L_ω|` instead of letting the buckle pump it), with a **resolution gate** (closes probe-finding-2: plant-at-de-novo-scale → read-(2,3) before any `w_pol = 0` is trusted). graft-v4 is the photon-sourced + locked + resolution-gated successor to the imposed-template + unlocked + floor-limited graft-v3.

### 9.4 — apparatus-floors characterization IN FLIGHT
**Branch** `analysis/2026-06-10-apparatus-floors` (off graft-v3 @ `4627651a`; **no commits yet** — work-in-flight in its own worktree, flagged). Scope: separate **physics-nulls from apparatus/representation floors** across the graft arc — **S_min / A_cap wall attribution** (is the wall behavior a real Γ→−1 trap or a clamp-floor artifact?), the **H_bel ledger floor** (what helicity magnitude is below the integrator/grid noise floor — the centro `−8.5e-15` is at this floor), and a **wall-sign audit**. It continues the §9.2 probe-finding-2 thread instrument-side: characterize which arc verdicts (`w_pol = 0`, the centro nulls) are apparatus-floor-limited vs genuinely physical, so the floors are attributed before a verdict leans on them.

### 9.5 — two new skills (both carry `trigger_patterns` → 25 skills now do)
- **`ave-representation-capability-check`** — the carrier/instrument structurally cannot host OR resolve the thing being asked of it (scalar bulk has no U(1) fibre; the A1 phasor is a read-only projection; **and now the extractor's q=3-representation is scale-bounded at r ≳ 3 cells**). Built across the arc; §9.2 finding-2 is its instrument-side worked example.
- **`ave-apparatus-floor-attribution` (NEW)** — before a null/zero/below-threshold reading justifies a verdict, attribute it: is it a **physics null** or an **apparatus floor** (resolution floor, ledger/noise floor, clamp-floor, PML artifact)? The apparatus-floors characterization (§9.4) is its launching worked example; §9.2's "every de-novo `w_pol` verdict is partly representation-limited" is the lesson that motivated it.

### 9.6 — PR #144 MERGED (the dark-wake regime-rescope landed)
**PR #144** merged **2026-06-10** (merge commit `7ee998f5`), title *"Dark-wake thruster R_rad,L: Phases 1-5 — antenna framing survives, 2/7 walk-back; Phase 2-5 rectification nulls REGIME-RESCOPED (research)."* The §4 / §6 "Held PRs (#144)" item is now resolved: the **Phase 2-5 rectification nulls carry their regime-rescope headers** (sub-yield-linear shear/chiral = achromatic + reversible = ∮ = 0 by construction = a wrong-regime artifact, NOT a falsification — the `ave-regime-phase-state-check` lesson), and the antenna framing survives with the 2/7-coupling walk-back. (Separately: the thixotropy bulk derivation at `5969bda1` remains unmerged — it closed bulk rate-asymmetry NEGATIVE by derivation, `τ_bulk` instantaneous in ρ̄ with no `sign(dρ̄/dt)` memory.)

**Arc state (2026-06-10):** graft-v3's chiral-Beltrami-source test landed **C** — the handedness sign-selection works (force-free, flips with χ) but as an **imposed template, not from the photon**; the **knot does not self-assemble** and the **LOCK is unimplemented** (chirality-independent `|L_ω|` pump). The probe added a load-bearing caveat: **every de-novo `w_pol = 0` verdict in the arc is partly representation-limited** (extractor floor at r ≈ 1.1 cells). The residual is now **three named primitives** — photon-sourced χ + the Taylor/Beltrami lock + a resolution gate — which is **exactly graft-v4** (launched). apparatus-floors (in flight) attributes the arc's nulls to physics-vs-apparatus before they're load-bearing. No genesis claim stands; the discipline held (B→C demotion landed via Rule 12, the brief's extractor-misread premise refuted via flag-don't-fix).
