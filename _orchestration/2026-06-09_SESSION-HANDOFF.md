# Session handoff — ion-compression → reflection-genesis arc (2026-06-09)

**Detailed per-step tracker:** [`2026-06-09_ion-compression-rectifier-arc.md`](2026-06-09_ion-compression-rectifier-arc.md) (the epic; §7 = THE CRUX onward carries every verdict verbatim). This doc is the consolidated pre-compaction snapshot + the skill-gap analysis.

---

## 1. The arc in one paragraph
Grant's "ion closed-loop compression cycle" tangent ran all the way to the electron-genesis. Trajectory: ion thrust → **mundane plasma** (real but not AVE-distinct) → the AVE-distinct **symmetric/gravity/electron branch** → the **trace-reversal (K4↔Cosserat) coupling** as the single crux → it's the **Heaviside-deleted scalar grade** (the longitudinal V the vector-Maxwell engine lacks) → the confinement is a **bounded Möbius/Op17 boundary condition** (FIXABLE) → cross-sector run: **confinement works, the V→ω source is missing** → the source is **reactive entrainment = added-mass = inertia** (Keith Mertens' vortex-ring paper #4) → the gyroscope closes, and **the deepest reframe: spin is CONSERVED, not pumped** (energize+lock, the whole "pump" hunt was a category error) → photon and electron are the **same ω-wave** (Γ=0 free / Γ=−1 reflected) → the electron is **reflected into existence** (the photon caught in the reflection off the Γ=−1 wall it self-generates; mass=trapped energy, spin=the winding's conserved L, α=the mirror's leak) → the reflection is **chirally birefringent** (selects e⁻ vs e⁺) → the **(2,3) self-assembly run** (genesis-23) landed **C** (the "3" never energizes; no stable confining window) → Grant's **LANE 1 / no-QED** reframe (a saturated mass carries standing longitudinal V_inc) → **genesis-24** (saturated seed: source fires but as a secular **pump**, not a lock; B) → the **crystal engine** (c_eff trap + conserved converter close both genesis-24 gaps; C — scalar bulk has no winding carrier) → **graft-v2** (independent Cosserat-ω carrier fixes the double-count; C — residual is mode-selection) → **graft-v3 RUNNING** (the chiral Beltrami source = the same A∥B object as the spin-lock; the test of whether the knot finally geometry-selects). *(This §1 paragraph was the pre-compaction snapshot; the post-genesis-23 chain is documented per-verdict in epic §8.)*

## 2. Load-bearing reframes (the durable physics — adjudicate/canonize separately)
- **Spin is conserved, not pumped** (Casimir `d|L|²/dt=0`); the electron is **energize-and-lock**, not pump. The whole "V→ω pump / build the spin" framing was a category error. *(Adjudication pending — §4.)*
- **The electron is reflected into existence** (literal): photon (Γ=0, transmitted, free, massless) and electron (Γ=−1, totally reflected, trapped, massive) are the **same ω-wave on the Möbius Γ-axis**. Mass = trapped energy (mₑc²); spin = the winding's conserved L; **α = the Γ=−1 mirror's residual transparency** (Z₀/(4π), Q=α⁻¹, Golden-Torus α⁻¹=4π³+π²+π).
- **The missing V→ω term = the Heaviside/Gibbs-deleted SCALAR grade** = the V-sector U(1) fibre = the "3" (needs the coupled K4+Cosserat engine).
- **Confinement = a bounded BOUNDARY CONDITION** (Möbius `Γ=(Z−Z₀)/(Z+Z₀)`, Op17 `R=Γ²≤1`), NOT a bulk energy/force term (which detonates — the A28 bug).
- **Thrust ⊥ gravity/electron:** rectify needs Z≠Z₀ (asymmetric → chromatic plasma); achromatic gravity/electron needs Z=Z₀ (symmetric). One element can't be both. ("Four-observables-one-device" was falsified.)
- **The two "3"s are DISTINCT, orthogonal objects** (Grant-ratified, the two-"3"s pressure test): **A1 dilatation** (∇·u, bulk K, the breather = **MASS**, mₑc² = trapped acoustic compression) ⊥ **T2 Cosserat micro-rotation** (the (2,3) **winding** = charge/spin, charge = Beltrami helicity). Wiring the winding into the breather's own phasor `(V_inc,V_ref)` — where `V_ref` is a *read-only projection* of the same scalar V — is the **double-count** that self-inflicted `w_pol≡0` across three engines. The winding needs its OWN Cosserat-ω carrier. **Electron = a knotted vortex ring** (added-mass = mass, linking = charge); ADD-2 = chiral compression→rotation buckle at the Γ=−1 wall.
- **Keith-frame one-liner:** *"a self-trapping swirling vortex ring on a medium that supplies its own stratification — the trapped ring is a particle, its angular momentum the spin."*

## 3. Branch + verdict inventory (all unmerged, not pushed)

> **Updated 2026-06-09 (post-compaction):** the table previously ended at `reflection-genesis-23 RUNNING`. genesis-23 landed C, and **five more branches + three audits** followed (genesis-24, the A-027 engine audit, the κ_chiral provenance audit, the crystal engine, the two-"3"s pressure test, graft-v2, graft-v3). Full per-verdict prose in the epic §8 (`2026-06-09_ion-compression-rectifier-arc.md`).
>
> **Updated 2026-06-10 (currency refresh):** graft-v3 landed its FINAL verdict (**B → C**, 4-lens panel) and **two successors launched off it** (graft-v4, apparatus-floors); **PR #144 merged**. The graft-v3 row below is updated from `RUNNING` to `C`; graft-v4 + apparatus-floors rows added. Full per-item prose in **§7 (this doc, the 2026-06-10 currency section)** and the epic **§9**.

| branch | verdict | tip | what |
|---|---|---|---|
| `analysis/2026-06-09-saturation-temporal-preregs` (sattemporal-wt) | hub | a05a7253 | preregs (thixotropy, time-dilation, rectifier-stage1, reactive-entrainment, reflection-genesis, **genesis-24 FROZEN/GO**), temporal-values def, ground-up rectifier design, electron-emergence note, **the 3 conceptual figures**, THIS handoff + the epic (epic §8 = the post-genesis-23 verdict chain) |
| `analysis/2026-06-09-thixotropy-bulk-derivation` | **B** | 5969bda1 | bulk sat/desat has no rate-asymmetry; closes by derivation. +figure |
| `analysis/2026-06-09-rectifier-stage1-biased-diode` | **C** | b8e6b022 | real rectifier but CHROMATIC plasma, not achromatic gravity. +3 figs |
| `analysis/2026-06-09-a2mu-vs-Q-crux` | **WALL** | 430194d8 | resonant-Q falsified; no dynamical V→ω pump (WALL-engine). +fig |
| `analysis/2026-06-09-tracereversal-pump-derivation` | **FIXABLE** | 70c57cdd | the pump = Heaviside scalar as Option-D bounded boundary condition. +fig |
| `analysis/2026-06-09-cross-sector-pump-run` | **B** | c999d4f0 | confinement works (bounded), V→ω source missing (V on wrong side of gate). +5 figs |
| `analysis/2026-06-09-reactive-entrainment-source` | **A (in form)** | b846a4b2 | gyroscope closes; spin conserved-not-pumped; Beltrami=inductive-shield. +5 figs |
| `analysis/2026-06-09-reflection-genesis-23` | **C (B/C-boundary)** | **ca991999** | (2,3) self-assembly: GAP-1 photon never energizes V-sector (max\|V_inc\|=0); GAP-2 no stable confining window. "2"/charge=helicity/spin-lock reproduce. +5 figs |
| `analysis/2026-06-09-genesis-24-saturated-seed` | **B** (4-lens panel, 2/4 demoted) | **df1c3f78** | saturated-seed: dE_V real+deep but = secular-pump onset (ledger fails, emit detonates E_V→6.8e8); Smith→Γ≈0; Arm-4 charge=helicity CLEAN; toroidal "2" withdrawn (Case C, Rule-12 demotion landed). +5 figs |
| `analysis/2026-06-09-crystal-engine-design` | **C** (most informative) | **6af430cd** | scalar c_eff trap + conserved ADD-2 converter: wall FORMS (Γ_core=−0.24), charge transfers, **both genesis-24 gaps CLOSED**; (2,3) absent (scalar bulk has no U(1) fibre, w_tor=w_pol=0); α refused (Q_dyn [41,1181]) |
| `analysis/2026-06-09-crystal-graft-v2` | **C** (double-count FIXED) | **1bf62595** | independent Cosserat-ω carrier: wall hardens Γ_min=−0.849, planted (2,3) reads back (double-count fixed, SMOKE-3); de-novo (2,3) absent → residual = MODE-SELECTION; **did not survive 4-lens panel** (independence no-op; energize-LOCK toy-only) |
| `analysis/2026-06-09-crystal-graft-v3` | **C** (B→C, 4-lens panel) | **4627651a** | chiral Beltrami source: handedness sign-SELECTS (H_bel RH +76.3 / LH −90.5 / centro −8.5e-15, force-free) but as an IMPOSED template (no_photon arm byte-identical to signal), NOT from the photon; no knot (w_pol≡0); **LOCK unimplemented** (χ=0 arm pumps \|L_ω\|→347.7, chirality-independent); gate unfalsifiable. +extractor-probe: unlocked buckle DESTROYS planted (2,3) over 500 steps + resolution floor at r≈1.1 → every de-novo w_pol verdict partly representation-limited |
| `analysis/2026-06-10-graft-v4-photon-helicity` | **LAUNCHED** (scaffolding only) | 37fd973c (= v3 + perf-utils) | photon's-OWN-conserved-helicity conversion + Taylor/Beltrami LOCK + resolution gate — targets the 3 v3 fatals (imposed-χ, missing lock, extractor floor). No v4 prereg committed yet (Grant-adjudicated launch direction 2026-06-10) |
| `analysis/2026-06-10-apparatus-floors` | **IN FLIGHT** (no commit) | (= 4627651a) | apparatus-vs-physics attribution across the arc: S_min/A_cap wall attribution + H_bel ledger floor + wall-sign audit. Work-in-flight in its own worktree |
| `analysis/2026-06-09-vol9-datasheet-figures` | tracked | 9786dbe5 | datasheet survey (323 curves) + ρ̄_cav gap-fill registered |

**Session audits (no standalone committed result doc — captured in epic §8.4/§8.5):** **A-027 engine audit → C** (`VacuumEngine3D` sub-saturation, no c_eff in propagation; Master-Equation FDTD has c_eff but scalar-V-only; neither engine had the full machinery → GAP-1 is engine-independent). **κ_chiral provenance → 1.2 is DERIVED** (κ̃=6/5=pq/(p+q) for the (2,3); 4 framing/hygiene defects flagged). **two-"3"s pressure test (`w4owpkan2`) → the double-count, named** (A1 dilatation mass ⊥ T2 Cosserat winding; `V_ref` is a read-only projection; w_pol≡0 was self-inflicted across 3 engines).

## 4. Adjudications RESOLVED this arc (Grant calls, durable physics — canonize separately)
- **LANE 1 (Grant) — a saturated mass carries standing longitudinal V_inc.** The "3" is the real Heaviside/Gibbs-excised longitudinal V-sector, physically present in a saturated absorber. Reframed GAP-1 from missing-axiom to missing-IC → genesis-24.
- **The no-QED directive (Grant).** The "transverse-only / Gauss-deletes-the-longitudinal" lane is **REJECTED as QED-contaminated**; absorb/emit IS the Axiom-4 saturation engage/relax cycle on the bond LC tank (no Kramers-Heisenberg/Rabi/self-trapped-transverse-photon). The longitudinal V-sector is real.
- **TENSION 1 = COEXIST (Grant).** clm-i4p11y's trapped transverse standing wave = the photon *precursor*; the electron *end-state* = the longitudinal bulk mode. Build the end-state; **do NOT edit clm-i4p11y** (flag for the auditor).
- **TENSION 2 = AXIOM CONSEQUENCE (Grant).** ADD-2 (the shear→bulk gyrotropic converter) = the I4₁32 chirality's transverse↔longitudinal coupling = **engine-completeness of Axiom-1**, NOT a new postulate. Derive it; don't bolt it on.
- **The two "3"s are DISTINCT objects (Grant-ratified).** A1 dilatation (mass, Heaviside scalar, bulk K) ⊥ T2 Cosserat micro-rotation (the (2,3) winding, charge/spin). Never wire the winding into the A1 phasor (`V_ref` is a read-only projection) — that was the double-count. Electron = knotted vortex ring; mass = entrained compression; charge = helicity.
- **(carried from earlier this session) Spin is CONSERVED, not pumped** — energize+lock, not pump (`d|L|²/dt=0`); the whole "V→ω pump / build the spin" framing was a category error. *(Grant ratification of "conserved-topological, energize+lock" as canonical vs the prereg's "builds the magnitude" is the one still-pending adjudication — see §4b.)*

## 4b. Still-OPEN (priority order)
1. ~~**graft-v3 verdict** (RUNNING)~~ — **RESOLVED 2026-06-10: C** (B→C, 4-lens panel; epic §9.1, this doc §7). The Beltrami source's handedness sign-selection LANDS (force-free, flips with χ) but as an IMPOSED template (not from the photon); the (2,3) knot does NOT geometry-select and the LOCK is unimplemented. **Did not debug toward A.** *(Original prediction text preserved:)* A = the chord (de-novo (2,3) closes α-free + charge flips with spatial handedness + Golden-Torus self-assembles); B = charge=helicity becomes carryable (H_bel≠0, odd in χ) — the predicted honest win; C = the Beltrami source still doesn't geometry-select → name the missing ingredient. **The named missing ingredients (3) → graft-v4 (launched, §7).**
2. **Adjudications for Grant:** (a) ratify "**spin = conserved-topological, energize+lock, NOT pumped**" as canonical (vs the prereg's "builds the magnitude") — the physics is clear; (b) ρ̄_cav=−1/φ → canonical claim or datasheet value? (c) the thixotropy-scope flag (the thrust space closes by **chromaticity** not **parity** once the asymmetric diode + bias are present).
3. **Held PRs** (Grant "lets wait"): ~~#144 (dark-wake; title stale — say Phase 1-2)~~ → **#144 MERGED 2026-06-10** (`7ee998f5`; title rewritten to the full Phases 1-5 + 2/7-walk-back + **Phase 2-5 rectification nulls REGIME-RESCOPED**; epic §9.6, this doc §7), #145 (hysteresis, CI-clean), #2 (Propulsion brief; reframe to clean-dead-end+byproducts). **PR #146** (skill-trigger-detect self-maintaining hook) **awaiting review** — activation deferred until the main checkout returns to `main` (the running hook = main checkout's working copy, currently parked on `analysis/2026-06-07-two-node-alpha-projection`).
4. **clm-7tynm2 walk-back** — HELD. The thrust was wrong-regime (sub-yield-linear shear/chiral, plasma), NOT a clean refutation. Retire the *exotic/inertial-drive* (reactionless) framing; keep inertia-as-mass + radiation-pressure; the symmetric branch is the real chord.
5. **Cross-repo (separate session):** mirror to AVE-Skills — **now 8 new skills** (`ave-engineering-program-rigor`, `ave-regime-phase-state-check`, `ave-asymmetric-grip`, `ave-resonant-amplification-check`, `ave-discriminator-before-synthesis`, `ave-conserved-vs-pumped`, `ave-representation-capability-check`, **`ave-apparatus-floor-attribution`** (added 2026-06-10)) **+ 5 amendments** (`substrate-native-check` CP9+CP10, `pre-test-physics-check` T8, `verify-before-cite` T7-ext, `ave-discipline-translate` fluid-vortex lens, `ave-newly-created-skill-self-audit` v1.1 trigger-7). Also ρ̄_cav Propulsion-leaf canonization; constants.py landing.
6. **Corpus flags (flag-don't-fix, surfaced not landed — auditor lane LANDS):**
   - `master-equation.md:18` conflates the two "3"s (A1 dilatation mass vs T2 Cosserat winding).
   - `clm-i4p11y` (`vol1/claim-quality.md:1296`) transverse-photon-precursor reading → COEXIST adjudication; **not edited** per Grant.
   - **κ_chiral stale framings:** `cosserat_field_3d.py:79` "Not a free parameter" (superseded by doc 108 one-parameter-α), `chirality_projection.py` tautological check, "harmonic mean" mislabel (it's the *parallel combination*), `condensed_matter.py:84` bare-1.2 collision.
   - (carried) `vacuum_engine.py:104-105` stale 1.009 anchor (retracted by doc 50_:57); `phase5e_cool_from_above.py:184` PML-exclusion gap; doc 54 §296 "Option D from 44_ §5.2" mislabel.
7. **Chain B' ΔE_cryst connection** — the crystal engine's latent-heat=mass leg (E_V=149.3, manifestation-class) is the empirical handle on `claim-quality-closure-roadmap.md` Chain B' (the OPEN substrate-local-thermodynamics independent-G derivation: *derive ΔE_cryst + Γ_cryst from ℓ_node, α alone*). Connects to Grant's latent-heat intuition; **not yet a closure** (the (2,3) crystallization does not close → manifestation-class only). Surfaced for the auditor + Grant.
8. **Documentation-homing** (per epic §3): temporal-values def → KB leaf + reconcile the 3 contradicting clock-exponent leaves (PR-gated walk-back); datasheet figure-gen (Phase A: ρ̄_cav + ch.01/07).

## 5. Skill ecosystem + IDENTIFIED GAPS

> **Currency note (2026-06-09 post-genesis-23):** the two gaps ranked #1–#2 below were **both built/applied this arc** — `ave-conserved-vs-pumped` (now empirically validated twice: genesis-24 detonation + graft-v2 LOCK-vs-PUMP) and the `substrate-native-check` **CP10** boundary-vs-bulk-energy amendment. Also added since: `ave-representation-capability-check` (the carrier-can't-host-the-thing pattern) and `ave-newly-created-skill-self-audit` v1.1 (prospective lens trigger-7). **Total now pending mirror to AVE-Skills: 7 new + 5 amendments** (see §4b item 5).
>
> **Currency note (2026-06-10):** one more new skill — **`ave-apparatus-floor-attribution`** (separate a physics-null from an apparatus floor — resolution/ledger/clamp/PML — before a null justifies a verdict; motivated by the graft-v3 probe's "every de-novo `w_pol`=0 verdict is partly representation-limited"; the apparatus-floors branch is its worked example). And `ave-representation-capability-check` extended **instrument-side** (the extractor's q=3-representation is scale-bounded at r ≳ 3 cells). **Both now carry `trigger_patterns` (verified on disk → 25 skills total now do).** Pending-mirror count → **8 new + 5 amendments** (§4b item 5 updated).

**Added this session (5 + 1 amendment):** `ave-engineering-program-rigor`, `ave-regime-phase-state-check`, `ave-asymmetric-grip`, `ave-resonant-amplification-check`, `ave-discriminator-before-synthesis` + `substrate-native-check` Checkpoint 9 (heuristic-vs-dynamical). All 5 pending mirror to AVE-Skills.

**GAPS IDENTIFIED (Grant directive 2026-06-09) — ranked:**

1. **`ave-conserved-vs-pumped` (NEW SKILL — strongest; the arc's deepest lesson).** *Before testing whether a substrate quantity can be "built / pumped / grown / amplified," classify it: is it a CONSERVED topological/Noether invariant (spin, charge, helicity, angular momentum) — which you ENERGIZE + LOCK, not pump (it sloshes, doesn't build; pumping it secular-pumps/detonates) — or a genuinely accumulable quantity?* The entire "V→ω pump / build the spin" arc (thixotropy B, crux WALL, cross-sector B, the secular-pump detonations) was this category error; the gyroscope result (`d|L|²/dt=0`, energize+lock) is the resolution. Distinct from `ave-power-category-check` (real-vs-reactive *power*) — this is about the *quantity's* conservation class. **Recommend building.**

2. **`substrate-native-check` Checkpoint 10 (AMENDMENT) — boundary-condition vs bulk-energy-term rendering.** *When rendering saturation / confinement / reflection in the engine, render it as a BOUNDARY CONDITION (Γ, Op17-bounded, reflective) — NOT a bulk energy/force term (dS/dA-gradient), which is singular at the wall and detonates.* Recurred 3×: the pump derivation (Option-D boundary vs bulk force, ~6-OOM split), the gyroscope (conservation vs pumped tank), verdict-II (moving reflective boundary vs collapsing energy term → collapse→confinement). The bulk renderings detonate; the boundary conditions are the physical ones.

3. **`pre-test-physics-check` strengthening — dispatch-time ontology gut-check.** *Before dispatching an expensive derivation in a multi-step chain, surface the load-bearing FRAMING/ONTOLOGY to Grant for a one-line gut-check (what IS the thing — a pump? a conserved gyroscope? a reflection? a vortex ring?).* I dispatched-then-Grant-corrected-then-killed-and-re-dispatched **twice** (scalar-quaternion; ring-angular-momentum). Each correction was load-bearing and grounded in canon the moment we checked — so the gut-check belongs BEFORE the dispatch. Trigger-7 (2+ negatives → reframe) is adjacent but fires too late.

4. **(Minor) `verify-before-cite` note:** applies to ENGINE code headers/comments, not just manuscript docs — they can be stale vs their own audit docs (the `vacuum_engine.py:104` 1.009 anchor was retracted by `doc 50_:57`; I cited it as a live hint).

5. **(Minor) `ave-discipline-translate` += fluid-dynamics lens:** vortex-ring / entrainment / stratification (Camassa–McLaughlin–Mertens) is a load-bearing lens for *genesis/confinement* problems (paper #4 named the reactive-entrainment source). Add it alongside EE / chemistry / stat-mech. Also: **consult the team's native domains** (Keith=fluid-dynamics/ion-propulsion; Benn=viz; the molecular biologist=protein folding) when stuck — "what would Keith say?" produced the source mechanism.

## 6. Where the chord stands (updated 2026-06-09, post-genesis-23→graft-v2)
The AVE-distinct chord is the **symmetric/electron/gravity branch** (the thrust line was mundane plasma — bank it). The electron-genesis is the deepest test, and across the genesis-23→24→crystal→graft-v2 chain the gap has been **fully localized to a single primitive.** The arc is a clean cascade of informative negatives, each closing the prior's pinned gap:
- **genesis-23 (C):** the "3" never energizes from a transverse photon (dead ω→V source); no stable confining window.
- **genesis-24 (B):** a saturated seed *does* energize the V-sector — but the source channel is a **secular pump** (detonates), not an energize-LOCK; toroidal "2" withdrawn as sub-gate noise.
- **crystal engine (C):** a c_eff trap + a **conserved** ADD-2 converter close **both** genesis-24 gaps (the source now bootstraps AND energize-LOCKs) — but the **scalar bulk has no U(1)-fibre carrier** for the winding.
- **graft-v2 (C):** an **independent Cosserat-ω carrier** fixes the double-count (a planted (2,3) reads back; `w_pol` can be nonzero) — but the centrosymmetric fixed-axis buckle can't **geometry-select** the knot (mode-selection residual; scalar handedness can't carry charge, H_bel quadratic in ω).

**The residual is now one named primitive:** a **chiral (Beltrami) winding source** on an independent Cosserat-ω carrier behind a c_eff Γ=−1 trap, energized+locked (not pumped). That is exactly **graft-v3** (running): the A∥B force-free Beltrami source — which is the **same object** the gyroscope result found is the electron's spin-LOCK (source and lock are one Beltrami). If the knot geometry-selects there, the chord closes; if not, the next missing ingredient gets named. **No genesis claim stands.** The discipline held throughout — honest C/B verdicts, no debug-toward-A, Rule-12 demotions landed in the result docs (genesis-24's toroidal "2", the crystal α-fluke, graft-v2's R/r tautology). The two-"3"s clarity (mass ⊥ winding) and the conserved-not-pumped reframe are the durable physics this arc earned.

---

## 7. Currency update — 2026-06-10 (graft-v3 final verdict + graft-v4/apparatus-floors launched + PR #144 merged)

> **Rule-12 append.** §1–§6 are preserved verbatim. §3/§4b/§6 ended at "**graft-v3 RUNNING**." This section records what landed since; the §3 table + §4b items 1/3/5 + §5 carry dated in-place annotations pointing here. Numbers re-verified this session against the committed v3 result/probe docs (`git show analysis/2026-06-09-crystal-graft-v3:…`); graft-v4 + apparatus-floors carry **no committed result/prereg yet** (launch scaffolding / work-in-flight) and are flagged as such. Full per-item prose in the epic **§9**.

- **graft-v3 → C (B→C, 4-lens panel; `survives_adversarial = FALSE`)** — branch `analysis/2026-06-09-crystal-graft-v3` @ **`4627651a`**, Rule-12 addendum atop `research/2026-06-09_crystal-graft-v3_result.md`.
  - **SURVIVES (narrowed):** the force-free Beltrami source's helicity **sign-selects with the imposed handedness χ** — H_bel RH `+76.3` / LH `−90.5` / centro `−8.5e-15` (full run), `cos(b_λ,∇×b_λ)=±1.000` (exactly force-free). **But it is sign-selection by an IMPOSED TEMPLATE, not from the photon** — the `no_photon_null` arm is **byte-identical** to `abc_denovo_RH` (χ is an input; the photon is decoupled). The "charge=helicity carryable" headline is demoted.
  - **FAILED (the C boundary, 4 residuals):** (1) **no knot** (`w_pol≡0`, no de-novo (2,3)); (2) **the LOCK is unimplemented** (FATAL) — `|L_ω|` pumps without saturating (frozen `∝t^0.43`; the **χ=0 centro arm pumps `|L_ω|`→347.7 with zero helicity/winding** — chirality-INDEPENDENT ⇒ energize without lock); (3) **χ-from-photon OPEN**; (4) the **independence gate is UNFALSIFIABLE** (FATAL — never flipped at 71×; `:111` retracted). Golden-torus ❌ corrected to `R·r=2.838≠¼` + absent (2,3) (NOT the 14.4% aspect, which is within the bar).
  - **Extractor-poloidal probe (two findings):** (a) the **unlocked buckle DESTROYS the planted poloidal winding** — fresh read = (2,3) (pol raw ≈2.99), after 500 live steps = (2,1) (pol raw ≈0.98); SMOKE-4 "(2,1) robust" is the dynamics' product, not the planted (2,3) surviving (**refutes the brief's extractor-misread premise**, flag-don't-fix). (b) **genuine resolution floor at minor radius r≈1.1 cells** (the de-novo full-run torus R=2.9/r=1.1 reads (2,2)/garbage) ⇒ **every de-novo `w_pol`=0 verdict in the arc is partly representation-limited, not purely physical.** Both slow + fast extractor inherit the floor.
- **graft-v4 LAUNCHED** — branch `analysis/2026-06-10-graft-v4-photon-helicity` (off v3 @ `4627651a` + the perf-utils parallel-runner/vectorized extractor @ `37fd973c`; **no v4 prereg committed yet**). **Grant-adjudicated launch direction (2026-06-10):** convert the **photon's OWN conserved helicity** (kills the imposed-template χ scope-limit) + a **Taylor/Beltrami LOCK** (the missing lock half) + a **resolution gate** (plant-at-de-novo-scale → read-(2,3) before trusting `w_pol`=0). Targets the three graft-v3 fatals.
- **apparatus-floors IN FLIGHT** — branch `analysis/2026-06-10-apparatus-floors` (off v3 @ `4627651a`; **no commits yet**, own worktree). Physics-vs-apparatus attribution across the arc: **S_min/A_cap wall attribution + H_bel ledger floor + wall-sign audit** (continues the probe's representation-limited caveat instrument-side).
- **Two new skills (verified on disk, both carry `trigger_patterns` → 25 skills total):** `ave-apparatus-floor-attribution` (NEW — separate physics-null from apparatus floor; apparatus-floors is its worked example) + `ave-representation-capability-check` extended instrument-side (extractor q=3-representation scale-bounded at r ≳ 3 cells).
- **PR #144 MERGED 2026-06-10** (`7ee998f5`) — *"Dark-wake thruster R_rad,L: Phases 1-5 — antenna framing survives, 2/7 walk-back; Phase 2-5 rectification nulls REGIME-RESCOPED (research)."* The held #144 is resolved; the Phase 2-5 rectification nulls carry their **wrong-regime-artifact (∮=0 by construction)** headers, not falsification.

**Where the chord stands (2026-06-10):** graft-v3 closed the chiral-Beltrami-source test at **C** — handedness sign-selection works (force-free) but as an imposed template, the knot does not self-assemble, the lock is unbuilt, and a representation-floor caveat now attaches to every de-novo `w_pol`=0. The residual is **three named primitives** (photon-sourced χ + Taylor/Beltrami lock + resolution gate) = **graft-v4** (launched). apparatus-floors (in flight) will attribute the arc's nulls to physics-vs-apparatus before they are load-bearing. No genesis claim stands; the discipline held (the B→C demotion landed via Rule 12; the brief's extractor-misread premise refuted via flag-don't-fix).

## 8. Currency update #2 — 2026-06-10 (graft-v4 FINAL LOCK-FAIL + apparatus-floors verdicts + wall-sign resolved + vapor-lock framing doc)

> **Rule-12 append.** §7 left graft-v4 "LAUNCHED" and apparatus-floors "IN FLIGHT." Both landed verdicts. This section **supersedes those two statuses**; §7 prose is not edited. Full per-item prose + the KB queue in the arc **§10** (`2026-06-09_ion-compression-rectifier-arc.md`). Numbers re-verified by branch+commit this session.

- **graft-v4 → C → LOCK-FAIL** (4-lens panel, addendum `f9447421`; result `2026-06-10_graft-v4-photon-helicity_result.md`). The named dichotomy (`ave-conserved-vs-pumped`): the **conserve-and-transfer** coupling (`f_w=−κ̃gV(∇×ω)`) is **indefinite ⇒ pumps/detonates**; the **stable LOCK** drops `f_w` ⇒ **transfers ~no helicity** (`−6` of `−291`). **Conservation UNTESTED** — the §2 ledger closure is tautological (`radiated := input−trapped−residual`), not a measured flux. **Lock-isolation = INERT / NOT-DEMONSTRATED** (`3d055de3`): the `δ=0.4` v3-strength seed destroys the knot in BOTH lock-ON/OFF arms (lock `|drift|`~4e-16), `δ=0.00775` survives in both — the seed sets the fate, **re-attributing** v3's knot-destruction to source strength, not a lockable wall.
- **apparatus-floors verdicts LANDED** (`a0d7dbb8`, `2026-06-10_apparatus-floors_note.md`): wall = **APPARATUS** (corr 1.0000 on the clip, no physical plateau); **dynamical cap −0.37** at standard `A_cap=0.999` (the `−0.849` was a non-binding-clip static seed); **n-exponent defect** (`S^0.25` vs physical `S^0.5`, deepens −0.37→−0.65, doesn't flip; flag-don't-fix); **leak = dispersion** (CV 1.5%, knob-independent ⇒ **α-from-leak DEAD at config**); **H_bel floor ±6.5%** (trust sign + >5% ratios only); **wall-sign SECTOR-RESOLVED** via PR #150 (`|Γ|=1` both branches; −1 short/μ, +1 open/ε).
- **3 KB annotations LANDED** (`dccdc63e`): `master-equation.md:18` two-"3"s, `clm-i4p11y` COEXIST (claim preserved), `op14:17,31` stale-exponent — all pre-adjudicated, Rule 12. **Vapor-lock framing doc** `research/2026-06-10_matter-as-vapor-locked-pump_framing.md` (`24cf3aa4`) — NEW-ASSEMBLY hypothesis-class.
- **PR batch:** #144-#149 MERGED 2026-06-10; **#150 OPEN** (wall-sign relabel, review-gated); #143 OPEN.
- **Skills:** 8 new + 5 amendments; 25 carry `trigger_patterns`; mirror to AVE-Skills pending (separate session).
- **No genesis claim stands.** The arc has now localized the residual to a stated coupling-family gap (bounded helicity-transfer doesn't exist in the director-buckle family) — an engine gap surfaced for Grant, **not a missing axiom** (A44 / Rule 16).
