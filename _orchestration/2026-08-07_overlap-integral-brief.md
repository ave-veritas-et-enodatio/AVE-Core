# Lane brief — the compression-line overlap integral (2026-08-07, Grant GO)

**Goal (prove-or-disprove level):** derive, on the receipted lattice, whether the
propagating longitudinal (common-mode/bulk) channel couples to a rotating-quadrupole
bias distribution with constant magnitudes (a circular binary's far-field boundary
condition) — the pluck question posed as a MODE-OVERLAP INTEGRAL. This decides the
#919 sourcing question at mechanism level and gates #913/#919/arc-scope/#770.

**SVA §0 header mandatory; freeze-first; frozen criteria verbatim or file:line;
`[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; Tier-2 at adjudication.**

## The frozen question

For the lattice's longitudinal propagating mode (the common mode of the bond network),
compute the source-coupling overlap for two boundary-condition classes at the port
surface: (a) RADIAL-AC — net displacement current through the surface oscillating
(eccentric-orbit class); (b) COMMUTATION — constant-magnitude bias pattern rotating
(circular-orbit class; the l=2 spatial harmonic of a scalar profile, time-varying at
2Ω at fixed points). Does (b)'s overlap with the propagating common mode vanish by the
network's structure (the mode functions' orthogonality / the Green function), or not?

## Verdict grammar (frozen)

- **OVERLAP-ZERO** — (b) is projected out by named structure: circular systems are
  silent on the compression line; the #919 floor re-scopes to radial-AC (eccentric)
  drive only; derive the eccentric coupling order next.
- **OVERLAP-NONZERO(n)** — (b) couples at derived order n: compute the flux and
  compare BOTH frozen comparators (below); the scalar-tensor door is open and the
  two-pulsar pair adjudicates.
- **NOT-DERIVABLE** — a named piece is missing (enumerate; the likely gap is the
  matter-to-lattice source term — if so, state exactly what a pluck derivation must
  deliver for this integral to close).

## Frozen comparators (re-retrieve from source at freeze; pointers-not-values)

The two-pulsar eccentricity pair, both already banked by the #919 prereg from arXiv
abstracts: Hulse-Taylor (e ≈ 0.62 — hard radial AC) and the double pulsar (e ≈ 0.09 —
almost none), both Ṗ_b consistent with GR's differential-channel-only prediction.
Any nonzero coupling must survive BOTH at their respective drive levels. The verdict
sentence states the eccentricity SCALING explicitly — it is the bankable
forward-prediction shape if nonzero.

## Mandatory inputs

The R28 walk record + the audit lane's row verdicts (consume, re-derive quotes); the
#919 result as re-cut (R23) — the order result (quadrupole lowest) stands, the
amplitude was NOT-DERIVABLE, this lane supplies the missing coupling structure; the
kernel-collapse v2 record (member fence — declare cold-linear kernel-insensitivity);
LC-1's spectrum receipts (do not re-derive the speeds). Fences: regime/phase
declarations; sector-ownership (A1 owns the bias content); stencil-lens (derivation
adjudicates, engine corroborates); rotational fence-reach STOP; two-method receipts,
named engines.
