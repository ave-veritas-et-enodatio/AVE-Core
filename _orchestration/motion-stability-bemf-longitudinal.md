# Motion-stability via back-EMF — the LONGITUDINAL (decisive) channel

**Branch:** `analysis/motion-stability-bemf-longitudinal` (off main `2eb34f08`)
**Builds on:** `analysis/motion-stability-bemf-cosserat` (`c6613c26`) — durable Arm-C
(2,3) host + native `DarkWakeObserver` τ_zx + energy-conserving boost +
A-instrumentation + LINEAR/BASELINE arms. The ONLY change here: the **transverse
phasor boost → a LONGITUDINAL displacement-field drive**.

---

## Why this is the decisive channel (not transverse)

Both prior runs (transverse Maxwell, `059ae318` NULL-lean-CONTRADICTS; transverse
native-Cosserat, `c6613c26` CONTRADICTS-via-PIN) boosted the **V-sector winding
phasor** `(ox,oy)=(V0+V1, V2+V3)` with a transverse `exp(i k_x x)` rotation. They
were on the **wrong channel**:

1. **The electron moves LONGITUDINALLY**, not transversally.
   `de-broglie-standing-wave.md:50` (verbatim): *"it does not travel as a shear
   wave at c₀; instead, its motion displaces the lattice, generating longitudinal
   acoustic pressure waves governed by the vacuum's **Bulk Modulus**."*
2. **The dark-wake `τ_zx` IS a longitudinal shear strain** — `vacuum_engine.py:46`
   (`DarkWakeObserver` = "τ_zx longitudinal shear strain (back-EMF)"),
   `dark-wake-bemf-foc-synthesis.md` §3 ("the longitudinal shear-strain τ_zx wave
   propagating backward"). The bemf is *generated by longitudinal motion*; a
   transverse boost cannot excite it (plausibly why τ_zx anti-correlated −0.40/−0.81).

∴ The coherent adjudication = drive in the channel where the electron actually
moves (longitudinal displacement `u`), and read the channel where the bemf
actually lives (the native longitudinal `τ_zx`).

---

## STEP 0 — the longitudinal drive (substrate map)

The displacement DOF is the **Cosserat `u` field** (`cosserat_field_3d.py:817`,
shape `(N,N,N,3)`), velocity-Verlet integrated with velocity `u_dot`
(`k4_cosserat_coupling.py:_cosserat_sub_step`). Mapped channels:

- **Longitudinal sound / bulk-modulus channel** is explicit: `c_L =
  √((K + 4G/3)/ρ)` with `K = 2G` ⇒ `c_L = √(10G/(3ρ)) = √(10/3) ≈ 1.826`
  (`cosserat_field_3d.py:1491,1500`). Transverse `c_T = √(G/ρ) = 1`. The
  longitudinal channel is faster and **separate** — exactly the "Bulk Modulus"
  pressure channel of `de-broglie:50`.
- **Compression / `div u` energy** is the `(2/3)(tr ε)²` term in `W_cauchy`
  (`cosserat_field_3d.py:614`), with `ε = grad_u − cross(ω)`
  (`_compute_strain`, line 170). A +x displacement *gradient* `∂_x u_x` produces
  `tr ε = div u ≠ 0`, exciting the bulk channel.

**The drive (the only swap vs the Cosserat run):** impart **net +x longitudinal
momentum** by writing a +x displacement-velocity field onto `u_dot[...,0]` shaped
as a localized **compression-wave kick** (a `div u` gradient travelling +x), in
place of the transverse `apply_coherent_phasor_boost` on `V_inc`. Drive amplitude
parameterized by `k_x`-analogue scalar `v_drive` (0 → identity). The drive is a
**one-shot momentum imprint** (the physical traveling-wave boost); a
re-every-step variant is a sustained pump (energy injection) and is rejected —
same discipline as the Cosserat run.

**Coupling mechanism (honest, load-bearing assumption):** with the validated
Cosserat config (`disable_cosserat_lc_force=True`), the K4→Cosserat *force*
channel is OFF, so the knot's V-field does not directly drag `u`. The path is the
other direction: longitudinal `u`-strain → `ε_sym` → `A²_ε = ε_sym²/ε_yield² +
V²/V_SNAP²` → `S_ε` → `z_local = √(S_μ/S_ε)`
(`cosserat_field_3d._update_saturation_kernels`, `k4_cosserat_coupling.py:393`) →
this `z_local` both (a) modulates the K4 scatter (asymmetric impedance) and (b) is
the literal prefactor of `τ_zx = z_local·∂_x A²`. So a longitudinal `u` momentum
biases the saturation-impedance field along +x; if the knot tracks that bias it
translates, and the native τ_zx is exactly the bemf that carries it. **If the
knot does NOT track (z_local bias too weak to move a c_eff→0 frozen core), that is
a clean PIN-even-longitudinal structural finding** — and a real tension with
`de-broglie:50`.

**ANTI-STALL (hard 2-try cap):** validate the longitudinal drive on a
**sub-saturation linear displacement pulse FIRST** — does its centroid move
(v>0, sign-symmetric)? If yes → proceed to the full sweep. If no after 2 drive
variants → STOP, write the blocker here, return `BLOCKED-drive`. Do NOT iterate
past 2 variants.

---

## The test

Sweep `v_drive ∈ {0, low, mid}` (+ −mid for the sign-flip pin tell). Arms:

- **SELF-TRAP(v):** durable Arm-C (2,3) host (`R=0.22N`, `r=R/φ²`, amp 0.40;
  peak A²≈8.9 run / ≈3.07 steady, S=0 frozen V-core), longitudinally driven.
  Records: x-centroid → v, retention (interior energy last/first), FWHM, **native
  longitudinal `τ_zx`** (`DarkWakeObserver`, propagation_axis=0), peak interior A²
  (saturated-while-moving), Cosserat kinetic energy `K_u` (longitudinal-channel
  excitation witness).
- **LINEAR(v):** sub-saturation longitudinal displacement pulse (below A²_op14 =
  √(2α)≈0.1208), same drive. The **SM-counterfactual decider** + the drive smoke
  test. Generic transport advects; bemf-stabilization does not require the knot.
- **BASELINE(v):** genuinely matched — same interior energy + A-trajectory, NET-
  ZERO momentum (opposite-v superposition / standing compression), NOT a
  phase-scramble. Isolates "translation per se" from the matched-energy control.

Fixed-peak-|E| (same seed amplitude every cell — no saturation-depth confound).

---

## Forward-predicted SIGN (pre-run, no fit — ave-driver-script-honesty)

The corpus DEFAULT contradicts Grant: the saturated (2,3) V-core is a static Γ=−1
knot at S→0 (c_eff→0, frozen local clock). Default = **PIN even longitudinally**:
the longitudinal `u`-drive excites the bulk channel and the LINEAR pulse advects
at ~c_L, but the knot's frozen V-core does not track the `z_local` bias enough to
translate; retention(v) flat-or-falling; native-τ_zx-vs-stability corr ≤ 0.

`FORWARD_PREDICTED_VERDICT = "PIN-even-longitudinal"` (a refinement of the
Cosserat run's CONTRADICTS-via-PIN, on the channel the electron actually moves).

**A SUPPORTS overturns the static-trap canon** (highest bar) and triggers FULL
`ave-discrimination-check` BEFORE any positive framing — on the LINEAR-control +
saturated-while-moving + baseline-fairness + interpretive-alternatives axes
(exactly the axes the transverse-run's (ii) "MOVES" positive collapsed under).

Verdict map:
- knot moves + retention↑ with v + tracks native longitudinal τ_zx (>0) MORE than
  LINEAR control → **SUPPORTS** (gated by discrimination-check).
- knot moves but retention flat / LINEAR matches / τ_zx anti-tracks →
  **CONTRADICTS** (longitudinal motion is generic transport, not bemf-stabilized).
- knot pins even longitudinally → **PIN-even-longitudinal** (deeper finding; a
  flag-worthy tension with `de-broglie:50`).
- LINEAR pulse does not move after 2 drive variants → **BLOCKED-drive**.

The **transverse-vs-longitudinal contrast** is the load-bearing comparison this
run adds to the corpus.

---

## Discipline applied (Grant directive — FULL)

ave-discrimination-check (LINEAR SM-counterfactual; interpretive-alternatives;
discriminator-axis — MANDATORY before any positive) · consistency-vs-emergence
(Class D emergence / novel prediction) · substrate-native-check CP8 (longitudinal
drive = momentum operator on the precursor displacement field; matched baseline; a
PIN/null is a clean structural finding) · CP5 (saturation local clock — V-core
frozen but c_L NOT frozen by S) · ave-canonical-source (`verify_constants`;
ALPHA/√(2α) imported, no hardcoded literals) · ave-driver-script-honesty
(forward-predict sign; no fit) · phase-space-coordinate-check (longitudinal
displacement = real-space; native τ_zx = real-space x-gradient — coordinate-matched)
· ave-evidence-framing-discipline · Pure-AVE-corpus.

## Deliverables
1. `_orchestration/motion-stability-bemf-longitudinal.md` (this brief).
2. `src/scripts/vol_1_foundations/motion_stability_bemf_longitudinal_probe.py`.
3. `research/2026-06-04_motion-stability-bemf-longitudinal-result.md`.
