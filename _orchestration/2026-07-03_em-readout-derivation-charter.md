# Charter — the EM-readout derivation: how the boundary-charge integer sources the Coulomb field

**Epic class:** Axiom-2's last underived leg. **Commissioned:** Grant, 2026-07-03. **Ontology ruling (Grant, verbatim):** *"let the derivation find it, come to me if you get stuck"* — no mechanism is pre-imposed; Stage-0's deliverable IS the mechanism; framing-level stuck-points route to Grant (pre-test-physics-check discipline).

---

## 1. The question and why it is now THE target

**How does the boundary-charge integer 𝓠 = Link(∂Ω, F) ∈ ℤ source the massless EM channel's static, exact-1/r exterior field?**

Three independent 2026-07 arcs converged on this single gap:
- **Cleave closure** (clm-clvchn): the displacement→charge pump computed to zero; ξ_topo confirmed a unit-bridge, not a mechanism.
- **Impedance-probe Phase-A**: no invertible electrical→winding coupling row exists (the deflationary symmetry).
- **Compositeness engine leg** (PR #472, ENGINE-BLOCKED): no engine implements a winding→EM source path; the FDTD host is curl-only (a planted Coulomb field is not even a fixed point — no Gauss sector, no charge source); the corpus's own `claim-quality.md:1311` flags the exterior-field shape open. **The code-block and the canon-gap are the same gap** — all three are the `def-tk1xfm` ceiling ("identity-by-translation, NOT a derivation") made concrete.

Axiom-2 leg-map after the 2026-07 arcs: dictionary ✓ · pair-interaction ✓ (gapped ω channel, clm-wcoul2) · pump ✗ (null) · **EM-readout = this epic.**

**Standing context (leading hypothesis, UNDECIDED-BY-ENGINE):** Grant's sector-conflation ruling — `translation-circuit.md:541` ("~ℓ_node/r Coulomb leak") describes the massless matched EM channel; `substrate-perspective-electron.md:109` (exponentially-suppressed hedgehog, 1/r² gravitational survivor) describes the gapped ω sector (Yukawa screening measured directly, clm-wcoul2, ξ≈0.548 cells). Both plausibly true of different sectors; the EM half is exactly what this epic derives. Recorded in the compositeness engine-leg prereg addendum; leaf sector-header corrections GATED on this epic's outcome.

## 2. The anchor and the stakes (pre-registered, asymmetric)

**Atoms exist ⇒ the empirically-required answer is exact 1/r.** The question is whether AVE can *derive* it.
- **1/r DERIVED** → Axiom 2 closes its last leg; the compositeness defense completes end-to-end (F₁ ≡ 1 earned, not posited); the framework's deepest internal result to date.
- **SCREENED / ABSENT derived** → a framework-level negative at the charge story's readout (charge-as-winding cannot reach the far zone; collides with the existence of electrostatics). Booked honestly, no rescue — the highest-stakes negative branch of any arc yet.
- **UNDERIVABLE (stuck at grade)** → recorded as a permanent FORM-level ceiling on charge-as-winding unless/until new machinery: the honest "posited forever" state, named.

## 3. The trap — un-riggability constraints (non-negotiable, all stages)

Adding Gauss's law with ρ defined from the winding **inserts the answer** (Gauss forces 1/r by construction) — the circular code-convenience coupling refused three times this month. Therefore:
1. **The coupling must trace to Axiom 1's own node structure** — each node carries translational (ε₀/E) ⊥ rotational (μ₀/B) DOFs, LC-coupled. That intra-node coupling is the only axiom-native place a rotational winding can push the translational sector. Every term in any derivation or update equation carries a per-term ledger row: AXIOM-DERIVED (cite) / ENGINEERING-CHOICE (rationale) / FORBIDDEN-INSERTION (reject).
2. **Gauss as diagnostic, never constraint** — measure ∇·E of what emerges; do not enforce it.
3. **Auditor gate on all update equations BEFORE any Stage-2 run** (the #384 unriggable-gate pattern): no term may reference the winding as a charge source by declaration.
4. **Validate-on-known = Maxwell-recovery**: the completed dynamics' linear limit must reproduce known electrostatics for the engine's native charge representation before any knot readout counts.

## 4. Staged structure

**STAGE 0 — analytic (this dispatch).** Derive, from Axiom 1's 6-DOF node structure + Axiom 3 (lossless) + the canonical winding definition, the static translational-sector exterior field of a persistent (2,3) winding. Candidate mechanism classes to INVESTIGATE, not pre-commit (Grant's ruling): (a) **dynamic pumping** — the node LC rotation↔translation coupling driven by the winding's persistent circulation (must then explain why the result is STATIC); (b) **static strain / DC-offset** — the winding shifts neighboring nodes' LC operating points (a quiescent-point displacement field equilibrating outward; derive its falloff); (c) **boundary/topological** — the linking integer as a boundary condition on the translational sector's solution space (source-free exterior with a topologically-forced 1/r harmonic); (d) other, derived. Deliverable: the mechanism with its predicted exterior profile and the F₁(q²) consequence — OR a sharply-named stuck-point surfaced to Grant. Frozen prereg with bins before deriving.
**STAGE 1 — engine (gated on Stage-0):** complete the 6-DOF per-node dynamics in the unified srs host (photon+A1+ω already co-carried; the static/longitudinal translational sector + the axiom-native rotation↔translation coupling are the missing DOFs), with the Maxwell-recovery validation suite and the §3 constraints.
**STAGE 2 — the readout (gated on Stage-1):** seed the 0₁+(2,3), measure exterior E(r); bins TAIL-EXACT-COULOMB / TAIL-DEPARTS(+bounds table ⇒ pre-registered exposure path) / GAMMA-EM-NONZERO / BLOCKED. Both Grant's ruling and the engine verdict recorded regardless of agreement (standing instruction).

## 5. Cross-references

`research/2026-07-03_compositeness-defense-gate0_{prereg,result}.md` + `_engine-leg_{prereg,result}.md` (the bounds table, the frozen bins, the host audit) · clm-wcoul2 (the gapped-channel interaction + channel-coherence) · clm-clvchn (the pump null; non-conflation guard applies — this epic is the READOUT, not the pump) · `unified_engine.py` P0/P1 docs (the 6-DOF host) · `translation-circuit.md:541` / `substrate-perspective-electron.md:109` (the sector-conflation pair, corrections gated) · `common/boundary-observables-m-q-j.md` (q²-conditioned no-hair, wall channel) · the axiom-register derived-legs table (Ax2 row updates on this epic's outcomes).
