# x42 implementer brief — the atomic eigencavity: hydrogen as an Op6 phase-closure problem (task #11)

**Date:** 2026-07-10 · **Lane:** implementer (worktree `/private/tmp/x42-wt`, branch `analysis/x42-atomic-eigencavity` off `origin/main` @ `7fedf5c3`) · **Orchestrator dispatch.**
**PR title (exact):** `[DO-NOT-MERGE][REVIEW: pending-orchestrator] x42: the atomic eigencavity — hydrogen as an Op6 phase-closure problem (task #11)`

---

## 0. The walked formulation (Grant-ratified as the shape — this is the picture; the gate is yours)

The atom = a bound state exhibiting **both halves of the impedance carve** — the nucleus's Coulomb well is **off-line source-slaved reactive dress** which, seen by the electron's wave, is a **graded impedance/mismatch profile Z(r)**; the orbital = **the electron's wave trapped between its own reflections**; quantization = **round-trip phase closure 2πn**; the spectrum = **an Op6 problem** (`λ_min(S†S)→0`, eigenmode-finding — its CANONICAL scope; cite the eigenvalue-target leaf `clm-gdd70j`).

Source walk: `research/2026-07-10_impedance-register-walks_framing.md` walk (c) (:49-68) + walk (a) four-bin taxonomy (:11-24). The walk is FRAMING; this lane runs the gate.

## 1. Deliverables (all five; the RESULT doc carries them)

1. **Z(r) from the Coulomb dress, substrate-native.** The off-line dress (bin 3 of walk (a): source-slaved, `|Γ|=1` at ω→0, radiates nothing) seen by the probe's de Broglie dispersion as a graded impedance/mismatch profile. Build ON canon — do not reinvent: `de-broglie-n.md` already has the atom as a **spherical radial transmission line** with `n(r,ξ)=√(2Z_eff(r)a₀/r − ξ)`; your added content is the **port-language derivation path** (dress → Z(r) → reflections), stated in the impedance-carve register.
2. **The phase-closure spectrum on the graded line.** Round-trip phase closure 2πn (equivalently the WKB/ABCD condition canon already uses) → show **E_n ∝ 1/n²** and **a₀** fall out. Numerically: prefer REUSE of `src/ave/solvers/radial_eigenvalue.py` (ABCD cascade, `B_total(E)=0`, Op6 at step 5, zero hardcoded values) or a thin driver over it; a new solver needs `substrate-native-check` first.
3. **The muonic case = same network, heavier probe scale.** State what changes (ONLY the probe's mass/Compton scale → dispersion scale; the network is unchanged) and reproduce the known scaling: `a_μ = a₀·(m_e/m_r)`, `E_n(μ) = E_n·(m_r/m_e)` with the reduced mass `m_r = m_μ m_p/(m_μ+m_p)`. **Constants:** `M_MUON` is ABSENT from `src/ave/core/constants.py` — ADD it (CODATA 2018, tagged experimental anchor, alongside `M_PROTON` at :122); never hard-code it in the driver. Flag the constants.py addition explicitly in the PR body (engine lane owns constants.py — coordinate note per constants.py:228).
4. **The two-register guard, stated formally.** Mode-count integers (n, l, m — embedding register, cavity-deformable) vs winding integers (charge, the (2,3) — graph register, topologically protected). **"Ionization kills the mode, not the knot"** — the two integer families have different failure modes; that is the operational proof they are different objects. State it formally in the RESULT doc. Do **NOT** mint the `def-` node pair — walk (c):68 flags it as a vocabulary-register CANDIDATE for a future terminology pass; keep it flagged-not-minted.
5. **The K1/K2 caveat, stated conditionally.** The well's kernel-transparency (does the held longitudinal Coulomb bias the Ax-4 kernel?) is GOVERNED by the X41 fork — quote its verdict class VERBATIM: **"[UNDERDETERMINED — K1 ∧ K2, with the transverse-reactive near-zone as the named discriminator]"** (frozen tie; `research/2026-07-10_x41-radiative-scoping-why_RESULT.md`). Both keys predict the held Coulomb transparent-to-T2, each conditioned on its own unpaid premise; the standing canonical reading (`manuscript/ave-kb/CLAUDE.md`:75) has a held static E LOADING S_ε. **DO NOT resolve the fork here.** Adjudicators are Grant + the CVR held-DC-E bench + the unbuilt near-zone probe.

## 2. Canon anchors (all grep-verified by the orchestrator this session — re-verify at your branch tip before citing)

- **Op6 canonical:** `manuscript/ave-kb/vol1/operators-and-regimes/ch6-universal-operators/eigenvalue-target.md` (`clm-gdd70j`) — `λ_min(S†S)→0`, ground state = one mode perfectly absorbed.
- **Op6 HONEST SCOPE (binding):** `src/ave/core/constants.py:212-228` + `manuscript/ave-kb/vol2/particle-physics/ch01-topological-matter/electron-identification.md:77` — **"Op6 is eigenmode-finding for a GIVEN network, never geometry-selection"** (the S₁₁ landscape is FLAT in R·r; S₁₁-min did NOT select the trefoil). For x42 this fits cleanly: the atom's Z(r) is **GIVEN** (source-slaved dress cast by the nuclear charge); Op6 finds its modes. Any sentence crediting Op6 with *selecting* a₀ or the 1/r geometry is a scope breach — a₀ falls out as the eigenmode scale OF the given profile.
- **The canon spectrum recovery (your consistency baseline — build on, never duplicate):** `manuscript/ave-kb/vol2/quantum-orbitals/ch07-quantum-mechanics/` — `de-broglie-standing-wave.md` (`clm-oltvwy`,`clm-qde5gn`: Helmholtz–Schrödinger isomorphism, c_eff(r)); `de-broglie-n.md` (`clm-oltvwy`: radial TL, n(r,ξ), S₁₁ dips = Op6, ξ₀=Z² verified <2%); `radial-eigenvalue-solver.md` (`clm-w6kk5y`: turning points); `helium-symmetric-cavity.md` (mutual cavity loading — out of x42 scope, cite only if needed).
- **Contamination hazards (binding, vol2/claim-quality.md:330-342):** `E = Z_eff²Ry/n²` as the IE formula is Bohr/Schrödinger, NOT AVE — the AVE route is the ABCD cascade with `B_total(E)=0` via Op6. `Ry = α²m_ec²/2` is emergent from `r_sat = a₀ = ℓ_node/α` (not a Bohr postulate); `r_n = n²a₀/Z` is the standing-wave condition (not imported). Your phase-closure derivation must GO THROUGH the closure/ABCD route and only COMPARE against the closed forms.
- **The 1/r tail is asserted-not-derived (flag, don't hide):** `manuscript/ave-kb/vol4/claim-quality.md:1311` — WHY topological strain = `ℓ_node/r` (vs `α·ℓ_node/r`) from first principles is an OPEN multi-week item; the clean 1/r far-field atoms require is asserted-not-derived (`clm-4r4jiy`, 0.70). Your Z(r) derivation IMPORTS the Coulomb dress form exactly as canon does and cites this open item. No claim to derive the tail.
- **X41 fork + #547 muon config-fact:** `research/2026-07-10_x41-radiative-scoping-why_RESULT.md` (verdict class above; #547 fed the full |E| into the V_yield/T2 key with no Helmholtz split — muonic-H Lamb overshoot 1.52×10⁶ µeV is why radiative-scoping exists as a postulate). Before writing ANYTHING about muonic Lamb shift / finite-size, read `manuscript/ave-kb/vol2/quantum-orbitals/ch07-quantum-mechanics/q-g20a-lamb-shift-structural-closure.md` — the Lamb shift's imported status is prior art; do not contradict it.
- **Figures:** WHITE via `ave.viz.style.apply` (`src/ave/viz/style.py:213`), Okabe-Ito, honest axes+units, legend outside data, no on-figure title.

## 3. THE MUONIC OPERATING-POINT CHECK (named check — do it, from constants)

Compute the per-node kernel argument at the muonic Bohr radius from canonical constants: `A = E_Coulomb(a_μ)/E_yield` (orchestrator's rough estimate ≈ 0.16 — NOT deep-linear, unlike ordinary hydrogen's ~1e-4 per `de-broglie-n.md`). Consequence for the doc: the SPECTRUM reproduction (reduced-mass scaling) rides the well acting as pure GIVEN geometry; whether the near-nucleus field ADDITIONALLY biases the lattice at the muonic scale is exactly the X41 frozen tie (standing canon: loads → a computable ε-shift, the #547-class overshoot; K1/K2: transparent). State this CONDITIONALLY in the sector header + deliverable 5; do not resolve; do not turn it into a new spectral-correction claim.

## 4. Prereg (FREEZE-BY-PUSH — the prereg is commit #1, PUSHED to origin BEFORE any derivation/code commit)

`research/2026-07-10_x42-atomic-eigencavity_prereg_FROZEN.md`, then `..._RESULT.md`. Frozen analytic expectations = **REPRODUCTION MARKS (consistency-class, not chords)**:

- **M1:** phase-closure spectrum on the graded line reproduces `E_n ∝ 1/n²`, `E_1 = −RY_EV` (state the reduced-mass convention you adopt — infinite-mass vs `m_r = m_e/(1+m_e/m_p)` — BEFORE running).
- **M2:** the ground-state closure scale reproduces `a₀ = A_0` (constants.py; canon identity `a₀ = ℓ_node/α`).
- **M3:** muonic scaling: `a_μ = a₀(m_e/m_r,μ)`, `E_n(μH) = E_n(H)·(m_r,μ/m_r,H)` — numeric marks computed from constants at prereg time and frozen (≈ −2.53 keV ground state, ≈ 285 fm; freeze the exact values your constants give).
- **M4 (inherited consistency check):** Z²-scaling per `de-broglie-n.md` if the driver sweeps Z.
- **Entailed-branch check (P10):** enumerate the outcome branches — (i) closure reproduces marks within stated tolerance; (ii) closure reproduces the 1/n² FORM but with an O(1) closure-constant offset (record honestly, do NOT tune — the Maslov/turning-point phase is where this lives; pre-state how turning-point phase is handled); (iii) fails → negative record, no rescue in-lane. Tolerances frozen BEFORE code.
- **Sabotage test (P11) on every numerical gate:** plant (a) a wrong-exponent profile (e.g. 1/r² dress) and (b) a detuned closure integer — show each gate FIRES on the plant and passes on the real profile. A gate that cannot fire is a checklist.

## 5. Classification (consistency-vs-emergence — binding)

Canon already recovers E_n and a₀ as consistency checks (`clm-oltvwy` etc. — that is the CANONICAL CEILING). Your added content is the **port-language derivation path**. Classify honestly: **re-derivation / consistency demonstration** UNLESS a genuinely new primitive appears — run the explicit new-primitive scan and expect NONE. Explicit derivation-path tracing for every "AVE reproduces X" sentence. No emergence headline; no promotion past the canonical ceiling; the two-register guard is a FORMALIZATION of walk (c), not a new claim.

## 6. Discipline riders (binding)

- **Sector header FIRST** in prereg + RESULT: MODE (derivation-from-canon + numerical consistency driver; NOT engine-fire), REGIME (hydrogen: cold/linear, per-node A~1e-4; muonic: state the §3 computed A and its conditional status), SECTOR (the probe's de Broglie/matter-wave channel on the Z₀ line; the dress is longitudinal source-slaved — A1⊥T2 ownership respected; charge = Cosserat winding, untouched by any of this).
- **Worktree paths:** first call `git rev-parse --show-toplevel`; ALL reads/edits via `/private/tmp/x42-wt/...` absolute paths from the first call onward. Ad-hoc python: `PYTHONPATH=/private/tmp/x42-wt/src`.
- **Canonical source:** every constant from `ave.core.constants` (ALPHA, A_0, RY_EV, M_E, M_PROTON, + your new M_MUON). Zero hard-coded values; verify_constants-style cross-check before plots.
- **Incremental writes:** skeleton-first, then one section per commit (single large Writes hit socket-timeout). Commit trailer: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- **Pure corpus. Verify-before-cite** (re-grep every cite at your branch tip; NO stitched quotes — the X41 R3 fabricated-quote catch is the standing warning). **Flag-don't-fix** any canon contradiction you hit (surface verbatim in RESULT §surfaced; Grant adjudicates). **KEEP-BOTH** on any pre-reg axis inconsistency (add an axis, never redefine in-place). **No KB/canon edits from this lane** — research/ + src/ only; KB canonization is a gated follow-on.
- **Skill-selection plan (orchestrator, 60-sec, applied set):** ave-prereg (freeze-by-push) · ave-analytical-tool-selection (Mode+Boundary+Network classes — consult the toolkit index before deriving) · ave-canonical-leaf-pull (energy-quantum class — §2 anchors are the pull; extend if your derivation touches Q-factor/coupling) · ave-canonical-source · consistency-vs-emergence (§5) · substrate-native-check (only if new solver code) · ave-driver-script-honesty + P11 sabotage · phase-space-coordinate-check (compare in ξ=E/Ry matched coordinates) · ave-regime-phase-state-check (§3/§6 headers) · verify-before-cite · ave-figure-discipline. Retro-pass before final commit if the applied set drifts.

## 7. Endgame

Push branch; open the PR with the exact title above; PR body: deliverable map, the M1-M4 mark outcomes, the constants.py M_MUON flag, the §3 muonic operating-point number, the classification statement, and the freeze-by-push ordering proof (prereg commit hash + push timestamp vs first code commit). Do NOT merge. Report back: branch tip hash, PR number, mark outcomes, any flags surfaced.
