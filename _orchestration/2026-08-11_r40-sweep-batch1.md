# R40 demotion sweep — BATCH 1 (the DIES class, the R42 CHORD tag, the drifted anchors)

### ENTRY 2026-08-11-r40-sweep-batch1

**Class:** demotion execution. Mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`, **moves no solidity
number**, adjudicates no channel, opens no fork. It changes the STATUS of already-written claims and
preserves every byte of their text (honesty-lag pattern, Rule 12).
**Authority:** R40, [`docket-entries/2026-08-10-rulings-r40-r42.md`](docket-entries/2026-08-10-rulings-r40-r42.md),
Grant verbatim *"1. go / 2. agreed / 3. agreed"*; the fourth clause of the header form is the
RATIFIED resolution per [`docket-entries/2026-08-10-ruling-r43-ratification.md`](docket-entries/2026-08-10-ruling-r43-ratification.md)
as reconciled by [`docket-entries/2026-08-10-ruling-r44-r43-reconciliation.md`](docket-entries/2026-08-10-ruling-r44-r43-reconciliation.md).
**Vocabulary:** R50, [`docket-entries/2026-08-10-ruling-r50-vocab.md`](docket-entries/2026-08-10-ruling-r50-vocab.md)
(dress / the bias / grade-to-informal / halo-as-astronomical-only, and the THE BIAS PROPAGATION
THEOREM rename) **and R49(b)**,
[`docket-entries/2026-08-10-rulings-r48-r49.md`](docket-entries/2026-08-10-rulings-r48-r49.md)
(the *retardation* retirement → **propagation delay / finite propagation speed**).
**Worklist consumed:** [`../research/drivers/r40_sweep_worklist_verified.json`](../research/drivers/r40_sweep_worklist_verified.json)
(banked by batch 0, PR #938); method record
[`2026-08-10_r40-sweep-scope-verification.md`](2026-08-10_r40-sweep-scope-verification.md).

**Base:** `origin/main` @ `ad376e9a`, fresh worktree, `git status --porcelain` = 0 before any edit.

**Scope, and nothing else:** (1) the 59 DIES-WITH-THE-PHANTOM table rows; (2) the 1 supplement DIES
row (beyond-floor — reported SEPARATELY throughout, never folded into the 59); (3) the R42 CHORD-tag
disposition; (4) the 5 DRIFTED-NEEDS-REPIN anchor re-pins. The 185 NEEDS-RE-DERIVATION rows, the 105
SURVIVES-AS-RESPONSE re-scopes and the two-method straggler sweep are **BATCH 2** and were not
started.

---

## 1. The header form as executed — with its fourth clause RATIFIED

Every note carries the arc's complete story, per R40. The first three clauses are R40's own text; the
fourth is the change since R40 was written — Grant unheld this batch with the instruction to cite the
RATIFIED resolution, **not** "pending-R41". The string `pending-R41` appears nowhere in this batch.

1. **The kill fired** (#930) — the walk-back that closed the bulk radiative-port reading.
2. **The premise localized to the #261 K = 2G import** (G-RECON, unchallenged): the compressible
   far-field branch was minted by a GR-imported elastic modulus, not forced by the axioms.
3. **The axioms underdetermine the bulk sector** — the #935 flat-direction finding: the written
   action conserves the Gauss function pointwise and never fixes its value.
4. **The replacement is the RATIFIED bound-sector law — Axiom 5, Substrate DC Bias** (BC-SRC clauses
   **S** / **G** / **Q**), ratified per the R43-final record, as reconciled by R44 (that full-scope
   record is FINAL and authoritative; the partial
   [`docket-entries/2026-08-10-ruling-r43-sg-ratified.md`](docket-entries/2026-08-10-ruling-r43-sg-ratified.md)
   is SUPERSEDED and is never cited as the resolution). Under the ratified law the A1 / bulk slot is
   a **bound response** — mechanism gloss **back-reaction** — with no independent propagating branch,
   no port, and zero longitudinal characteristic speed. A bulk *wave speed*, a bulk *radiative port*,
   a bulk *band-branch* and a bulk *transit clock* therefore have no referent.

**The honesty rider, carried in every note.** The ratified axiom does **not** discharge everything.
**THE BIAS PROPAGATION THEOREM** is Axiom 5's standing named-open entry — clause G's elliptic law is
the *static abstraction* of underived finite-speed bias dynamics
([`2026-08-10_bias-propagation-brief.md`](2026-08-10_bias-propagation-brief.md)). Where a demoted
claim's replacement depends on finite-speed bias dynamics, the resolution is the ratified axiom
**with that debt open**, not a closed replacement.

**Vocabulary compliance — R50 AND R49(b), attributed correctly.** No note authored in this batch
uses *dress*, *grade* as the canonical noun for ε₁₁, *halo* for the physics, or *retardation*.
Existing corpus text carrying those words is quoted byte-exact where it is quoted and is otherwise
untouched.

> ⚑ **CORRECTION OF RECORD (2026-08-11, at review).** The first cut of this batch attributed the
> *retardation* retirement to **R50** in all 42 notes and in this paragraph. That is **wrong**. The
> retardation retirement is **R49(b)** —
> [`docket-entries/2026-08-10-rulings-r48-r49.md`](docket-entries/2026-08-10-rulings-r48-r49.md),
> verbatim: *"'retardation' is RETIRED from this role. The canonical term is **propagation delay /
> finite propagation speed**"*. **R50** rules *dress* / *the bias* / *grade*-to-informal /
> *halo*-as-astronomical-only, **and the theorem RENAME** (THE BIAS PROPAGATION THEOREM supersedes
> R49(b)'s "grade propagation theorem" label). Two methods on the R50 blob confirm it carries no
> retardation clause: shell `grep -c -i retard` → 0, and an independent Python `re.findall` over the
> same bytes → 0. All 42 notes now cite **both** records with the right item against each; the R50
> pointer is retained for the items R50 actually rules.

---

## 2. Method — zero-line-shift, and why it was mandatory

**Line-count first (the mandated pre-edit measurement).** Before any edit, a Python `os.walk` + `re`
enumeration of every `path.ext:NN` cite pointing INTO the 44 candidate files was run over 4111 tracked
text files: **3997 incoming line-cites** resolve into these files (`constants.py` 1003,
`translation-circuit.md` 461, `cosserat_field_3d.py` 421, `vol4/claim-quality.md` 311,
`vocabulary-register.md` 264, `port-register.md` 141, …). Inserting a note *in body* would have moved
several thousand of them.

So the batch is **append-only at the line level**:

- **Same-line status stamp** at each live-canon site — appended to the end of the line, or inserted
  before the row's final `|` (markdown table cells), or before the closing brace / `\\` (LaTeX
  captions and table cells), or before the closing `"""` (a Python docstring terminator). The claim
  text is preserved byte-for-byte inside the stamped line.
- **The full dated note at EOF** — one per file, carrying the four-clause header form, the honesty
  rider, the per-site verbatim quote + the audited rationale verbatim, and the records.
- **No line is inserted or deleted above EOF in any file.**

Where the worklist anchor is mid-sentence, the stamp is placed at the end of that sentence or block
and the note records **both** the anchor line and the stamped line. Where the anchor is display math
(`04_continuum_electrodynamics.tex:348`, `mond-hoop-stress.md:40`) the math line is left byte-untouched
and the stamp goes on the resultbox's own title or scope line.

**R39 BYTE-FENCE.** **THREE** DIES sites sit inside Rule-12 preserved spans. None is stamped; all
three are routed to the governing leaf's own EOF ledger with a pointer, span bytes untouched — see §4.

> 🔴 **CORRECTION OF RECORD (2026-08-11, at review) — one R39 BREACH occurred and is REVERSED.**
> The first cut of this batch said *"Two DIES sites … Neither was stamped"*. That census was
> **affirmatively wrong**: the row anchored at
> `manuscript/vol_3_macroscopic/chapters/08_gravitational_waves.tex:198` was stamped at `:208`, which
> is **inside** the Rule-12 preserved *Q1 REVERT* warningbox (`:188-242`) — a span that declares
> itself preserved at `:240-241` (*"This box is preserved verbatim (Rule~12)"*) and is declared
> preserved again from outside at `:252` (*"both prior boxes are preserved verbatim (Rule~12)"*).
> The stamp is **REMOVED** and the span restored: `:128-176` and `:188-242` are now byte-identical to
> `origin/main` (verified line-by-line against `git show origin/main:<file>`). The row's note is
> routed to the chapter's EOF ledger with a pointer, per R39
> ([`docket-entries/2026-08-09-ruling-r39-sixtags.md`](docket-entries/2026-08-09-ruling-r39-sixtags.md)
> — *"Rule-12 preserved spans are byte-fenced against ALL later passes, mechanical included; their
> audit findings live in ledgers, not in-span"*). **Why the first pass missed it, and the method
> change:** the original span-detector keyed on **banner prose in a ±25-line window**; this span's
> preservation declaration sits **32 lines below** the stamp, at the box's closing paragraph. The
> detector is now **container-aware, not window-aware** — see §2.1.

**Truth-break rule.** A stamp that would make the surrounding sentence false goes to the
Blocked/Findings section unnoted. **Zero rows hit that rule in this batch** (§6 lists the findings
that were surfaced instead of fixed).

## 2.1 The preserved-span census, RE-DERIVED with a detector that catches the missed class

The first pass's detector was a **±25-line banner-prose window**. It catches a preservation banner
printed near the stamp; it cannot catch a span whose declaration sits at the far end of a
**structural container**. That is exactly the `08_gravitational_waves.tex:208` class — the box's
declaration is 32 lines below the stamp.

**The method change — container-aware, not window-aware.** Widened on three axes:

- **(A) Containers, not windows.** For `.tex`, EVERY `\begin{X}` … `\end{X}` span enclosing the
  stamp is computed, with **no environment allow-list** — the tcolorbox family (`warningbox`,
  `resultbox`, `simbox`, `examplebox`, `summarybox`, `exercisebox`, `circuitbox`, `codebox`,
  `objectivebox`) plus `figure`, `table`, `tabular`, `itemize`, `enumerate`, `equation`, everything.
  For `.md`, the enclosing blockquote run, the enclosing `^#{1,6}` section, and the enclosing bullet
  block. For `.py`, the module docstring, the contiguous comment run, and the enclosing `def`/`class`.
- **(B) The WHOLE container is scanned at any distance** — the declaration may sit anywhere inside
  it, including its closing paragraph.
- **(C) Widened declaration vocabulary** — adds the container-naming forms the first pass had no
  pattern for: *"This box is preserved verbatim"*, *"both prior boxes are preserved verbatim"*,
  *"quoted verbatim and not paraphrased"*, *"verbatim [sic]"*, *"do NOT delete"*, `byte-fence(d)`,
  `KEEP-BOTH`, `SUPERSEDED … preserv…`, alongside the original banner prose.

**Result — 61 stamped live-canon lines scanned, 16 flagged for adjudication, every one hand-read:**

| verdict | n | sites |
|---|---|---|
| **GENUINE BREACH — reversed** | **1** | `08_gravitational_waves.tex:208` (inside preserved warningbox `:188-242`) |
| FALSE POSITIVE — declaration governs an **inline** superseded quote on a *different* line | 4 | `port-register.md:93` (decl at `:87`, intra-line), `gauge-boson-masses.md:48` (decl at `:46`, prior form of that note), `gw-detection-antenna.md:51` / `ligo-gw-saturation-ratio.md:20` (decl = the `~~struck~~` clause at `:49` / `:18`) |
| FALSE POSITIVE — declaration governs a `%`-comment block, stamp is on live prose | 3 | `08_gravitational_waves.tex:107` (comments `:80-106`), `09_mechanical_characteristics.tex:82` (comments `:63-80`), `04_continuum_electrodynamics.tex` family |
| FALSE POSITIVE — `KEEP-BOTH` matched, which is a both-objects-retained note, **not** a Rule-12 preservation | 5 | `port-register.md:74`, `translation-circuit.md:157` / `:958`, `constants.py:780`, `vacuum_engine.py:1653` |
| FALSE POSITIVE — the container is the CURRENT/live box that declares the OTHER boxes preserved | 1 | `08_gravitational_waves.tex:297` (box `:249-312`, whose `:252` declares the two prior boxes preserved) |
| FALSE POSITIVE — declaration governs a value re-pin / a live 2026-08-02 note elsewhere in the section | 1 | `lc-electrodynamics.md:22`, `gw-propagation-lossless.md:48` |
| FALSE POSITIVE — self-match on **this batch's own EOF note** | 1 | `bulk_rarefaction_sector.py:129` (decl at `:314`, inside the note appended by this pass) |

**Corrected byte-fence inventory: THREE routed sites** — `genesis-chord-falsification-ledger.md:104`,
`translation-circuit.md:974`, and now `08_gravitational_waves.tex:198`. The count agreed
independently with the review lane's own container-parse sweep, which returned the same single
genuine breach.

---

## 3. The reconciliation identity

Batch-1's row set, stated as a sum and reconciled against the banked worklist:

```
   56   table DIES rows      — stamped in place + dated EOF note
 +  3   table DIES rows      — R39 BYTE-FENCE ROUTED to the file's EOF ledger (no in-body stamp)
 = 59   table DIES rows      ✓ reconciles with bin_counts_rederived["DIES-WITH-THE-PHANTOM"] = 59

 +  1   supplement DIES row  — stamped in place + dated EOF note  (beyond-floor; reported
                               SEPARATELY, never folded into the 59)
 +  2   R42 CHORD-tag sites  — ruled label landed AS RULED, stamped in place
 +  5   DRIFTED-NEEDS-REPIN  — re-derived; 0 anchors moved (§5)
 -----
 = 67   batch-1 row-actions
```

Cross-checks:

- **frozen-doc surface notes = 0.** No DIES site lands in a dated `research/*_result.md`, a
  `prereg-FROZEN`, or a preserved-historical document — the 44 files are manuscript volumes, KB
  leaves/registers, and engine modules. Nothing in this batch needed the surface-note-only path.
- **Blocked / truth-break rows = 0.**
- **Stamps applied = 60** ≠ 60 demoted rows, by two offsetting accounted deltas:
  `05_ac_electrical_characteristics.tex:542` is one worklist row whose claim is realised on **two**
  table rows (`:553` the bulk wave sector, `:571` the bulk transit clock), so it carries two stamps
  (+1); and three rows are byte-fence-routed and carry **no** stamp (−3).
  60 = (59 table − 3 routed) + 1 supplement + 1 second stamp + 2 R42 = 60. ✓
  *(Was 61 in the first cut; the R39 breach reversal at `08_gravitational_waves.tex:208` removes one.)*
- **Files carrying a dated EOF demotion note = 42** (every file holding at least one DIES row,
  including all three byte-fence-routed cases, whose notes ARE the routed ledger entries).

---

## 4. Per-class disposition

| Class | Rows | Disposition | Where |
|---|---|---|---|
| DIES — table | **56** | same-line status stamp + dated EOF note carrying the four-clause header, the rider, the verbatim quote and the audited rationale | 40 files across `manuscript/ave-kb/`, `manuscript/vol_*`, `src/ave/` |
| DIES — table, R39 byte-fenced | **3** | **no in-body stamp**; note routed to the governing leaf's EOF ledger with a pointer | `genesis-chord-falsification-ledger.md:104`, `translation-circuit.md:974`, `08_gravitational_waves.tex:198` (**breach reversed 2026-08-11**) |
| DIES — supplement (beyond-floor) | **1** | same-line stamp + dated EOF note; **reported separately, not folded into the 59** | `vol2/appendices/app-f-solver-toolchain/lattice-phase-transition.md:18` |
| R42 CHORD tag | **2** | ruled label `engine-artifact-pending-constitutive-law` landed AS RULED, stamped in place, dated, citing the R42 record; **no solidity number moves** (quality was `*pending*`) | `vol9/ch17-engine-requirements/engine-acceptance-suite.md:174`, `vol9/claim-quality.md:574` |
| DRIFTED-NEEDS-REPIN | **5** | re-derived on three engines; **all five anchors are CORRECT** — the batch-0 DRIFTED verdict is a probe artifact (§5). Zero re-pins. **No demotion note added to the four SURVIVES rows** — a re-pin is not a demotion | `research/drivers/r40_sweep_worklist_verified.json` (dated block; banked verdicts preserved) |

**The three byte-fence routings, stated exactly:**

- `manuscript/ave-kb/common/genesis-chord-falsification-ledger.md:104` — the `q1-reading-A-radiative-bulk-port`
  entry's **Verdict** bullet sits inside the span that the entry's own `:109` status line declares
  *"prior entry text above PRESERVED per Rule 12"*. `:104` is byte-untouched; `:109` (live writing —
  the appended dated status change) **is** stamped, per R39's seventh-containment reading (*live prose
  follows the pass; preserved bodies do not*).
- `manuscript/ave-kb/common/translation-tables/translation-circuit.md:974` — sits inside the verbatim
  #761 block-quotation at `:971-979`, introduced at `:969` as *"The incumbent, quoted verbatim and not
  paraphrased"*. Quotation class per R39. Bytes untouched; the file's EOF ledger carries the note.
- `manuscript/vol_3_macroscopic/chapters/08_gravitational_waves.tex:198` — **added at review, 2026-08-11;
  this one was a BREACH in the first cut and is reversed.** The anchor sits inside the Rule-12
  preserved *Q1 REVERT* warningbox `:188-242` (self-declared preserved at `:240-241`, declared again
  from outside at `:252`). The first cut stamped `:208` inside that box; the stamp is removed and the
  box restored byte-identical to `origin/main`. The chapter's EOF ledger carries the routed note and
  records the reversal in the open.

---

## 5. The 5 DRIFTED-NEEDS-REPIN rows — re-derived, and the verdict CORRECTED

The brief said to re-derive these rather than trust any summary. Re-derived they are — and **none of
them is drifted**. All five quoted contents sit at the originally cited line.

**Engines (named, independent):**

- **ENGINE 1 — hard-reduction character scan.** Quote and target both reduced to `[a-z0-9]` only
  (LaTeX command names, `$ \ { } _ ^`, Greek, arrows, punctuation and all whitespace removed); the
  file is concatenated with a per-character line map; the reduced quote is located as a substring and
  the line of its first character is reported. This is the batch-0 v3 reduction, blind to markup on
  **both** sides.
- **ENGINE 2 — token-containment sliding window.** A different algorithm with different failure
  modes: both sides reduced to alphanumeric word tokens; every k-line window (k = 1…6) scored by
  `|quote ∩ window| / |quote|`; the first line of the best window reported. Order-insensitive,
  gap-tolerant.
- **ENGINE 3 (confirmation) — targeted single-transform probe.** Remove *only* `\gammaundeclared{}`
  from the target (the four `.tex` rows) or render the source line (the one `.md` row), then require
  an **exact raw substring** match.

| row | bin | line BEFORE | line AFTER | moved? | why the batch-0 probe missed it |
|---|---|---|---|---|---|
| `manuscript/vol_9_vacuum_datasheet/chapters/03a_device_circuit_models.tex`:148 | NEEDS-RE-DERIVATION | 148 | **148** | no | `\gammaundeclared{}` inserted mid-quote **plus** the captured quote normalised `$\Gamma{=}-1$` from the source's `$\Gamma=-1$` |
| `manuscript/ave-kb/vol9/ch1-general-description/index.md`:22 | SURVIVES-AS-RESPONSE | 22 | **22** | no | rendered prose (`V_snap=m_ec²/e≈511 kV`, `def-vyvsn1` unbackticked) vs LaTeX-in-markdown source (`$V_{snap}=m_ec^2/e\approx511$ kV`, `` `def-vyvsn1` ``) — the v2-class artifact batch 0 itself named |
| `manuscript/vol_3_macroscopic/chapters/02_general_relativity_and_gravity.tex`:43 | SURVIVES-AS-RESPONSE | 43 | **43** | no | `\gammaundeclared{}` inserted mid-quote |
| `manuscript/vol_3_macroscopic/chapters/03_macroscopic_relativity.tex`:169 | SURVIVES-AS-RESPONSE | 169 | **169** | no | `\gammaundeclared{}` inserted mid-quote |
| `manuscript/vol_9_vacuum_datasheet/chapters/05_ac_electrical_characteristics.tex`:121 | SURVIVES-AS-RESPONSE | 121 | **121** | no | `\gammaundeclared{}` inserted mid-quote |

Engines 1 and 2 agree on the line for all five; engine 3 returns an exact raw hit at the cited line
for all five after removing exactly one known transform. The banked table's reading — *"Three of the
five are same-passage off-by-a-few"* — is **withdrawn**: the content is on the cited line itself, not
a few lines away.

**Diagnosis.** The dominant cause is a **sixth probe generation** the batch-0 method finding did not
have: the signed-Γ tag macro `\gammaundeclared{}` (declared in `manuscript/structure/commands.tex`,
landed by the signed-Γ census / gamma-tag-spec family) is inserted *inside* the quoted span, after the
audit quotes were captured. Batch 0's v3 reduction strips `\command` names — but the hand-read of the
11 residuals happened against the raw source, which is where the miss occurred. **The batch-0 lesson
strengthens rather than weakens: a single probe on this table has a ~20× false-drift rate, and even
the hand-read residual set was not drift-free.**

**Handling (flag-don't-fix, ave-reproduction-gate).** The banked per-row `site_verdict` is **not**
overwritten. The correction rides in new dated fields
(`site_verdict_repin_2026_08_11`, `repin_line_2026_08_11`) plus a top-level `repin_2026_08_11` block
naming the engines and the cause. Both readings are on the record.

**Consequence for batch 2:** the DRIFTED bin is empty. The site-verdict tally re-states as
**343 VERIFIED / 11 VERIFIED-BY-HAND / 0 DRIFTED** over the 349 + 5 = 354 known rows (was 338 / 6 / 5),
so no batch-2 note needs to be written against a stale anchor for these five.

---

## 6. Findings surfaced, not fixed

**F1 — R42's ruled label has a referent-drift, and it is landed AS RULED.** R42 ruled the vol9 T3.1 /
`sup-3kq9w7` tag demotes to **`engine-artifact-pending-constitutive-law`**. That label was written
while the bound-sector constitutive law was the open R41 lane. The constitutive law is **now RATIFIED**
(Axiom 5, BC-SRC S/G/Q, per the R43-final record as reconciled by R44), so as of this batch the label's
"pending-constitutive-law" clause names a debt that is closed. The debt that is genuinely outstanding
is **THE BIAS PROPAGATION THEOREM**. The ruled string is landed verbatim at both sites and is **not**
silently re-labelled. **Recommendation (Grant's adjudication, not this lane's):** relabel to
`engine-artifact-pending-bias-propagation`, or keep the ruled string and attach a dated referent note.
**Mitigation, stated precisely (corrected 2026-08-11 at review — the first cut overstated it).** The
mitigation is *not* the mere existence of the EOF notes: it is that each tag's own bullet is itself
stamped dead 9 lines later at `engine-acceptance-suite.md:183` / 8 lines later at
`vol9/claim-quality.md:582`, and that stamp points the reader to the dated EOF note whose clause 4
carries the ratified resolution. Nothing at `:174` / `:574` annotates the label itself as stale — that
annotation is remedy 2, and it waits on Grant.

**F2 — the batch-0 DRIFTED bin is a probe artifact, and the upstream correction is ROUTED, NOT
EXECUTED.** §5. This corrects a merged artifact (PR #938). **⚑ Forward pointer, landed in the corpus
(2026-08-11, at review; previously this request existed only in the PR body, which is not tracked
corpus):** the batch-0 prose record
[`2026-08-10_r40-sweep-scope-verification.md`](2026-08-10_r40-sweep-scope-verification.md), **merged
on `main`**, still asserts at `:61` *"| **DRIFTED-NEEDS-REPIN** | **5** | the cited line does **not**
hold the quoted content |"* and at `:79` *"Three of the five are **same-passage off-by-a-few**"*.
Batch 1 **does not edit that merged artifact** — it owes a **dated forward/supersession note** in the
corpus's own convention (cf. the `SUPERSEDED, 2026-08-10 (R44)` banner on
[`docket-entries/2026-08-10-ruling-r43-sg-ratified.md`](docket-entries/2026-08-10-ruling-r43-sg-ratified.md)).
Until that lands, a grep for `DRIFTED-NEEDS-REPIN` can reach the stale doc first; the correction
lives here (§5) and in the worklist JSON's `repin_2026_08_11.upstream_debt_2026_08_11` field. **This
is a named open debt of batch 1, listed in §8.** The other #938 artifact — the worklist JSON — IS
amended in-branch, dated, with banked verdicts preserved un-overwritten;
both readings are banked, nothing is overwritten.

**F3 — `03a_device_circuit_models.tex:113` carries the banked row's own `uncertain` flag.** The
rationale itself says *"Uncertain: the speed-reference note already fences it as re-expression,
arguable NEEDS."* It is noted at its **banked bin (DIES)**, and the chapter note says so in the open.
If batch 2's NEEDS pass re-reads it, this row is the one to re-bin.

**F4 — two `def-` nodes are demoted by HALF, and their `status:` fields were deliberately NOT moved.**
`def-satshr` (`status: SOLID`) and `def-pndenv` (`status: proposed`) in
`manuscript/ave-kb/common/vocabulary-register.md`: the audited rows demote only the
**compression-wave** half of the first and the **P-branch coupling coefficient** of the second; the
trace/traceless split (accounting) and the ponderomotive mass-quadrupole source structure survive, and
the observed-GW-=-shear identification is Reading-independent. Flipping either `status:` would
overstate the demotion. The stamp therefore sits on the `adjudicated-meaning` line and the EOF note
carries the exact half that dies. `make refresh-kb-metadata` propagated the two changed
`adjudicated_meaning` strings into the derived `.index/claims.jsonl` (2 records; that file is a
derived artifact and is committed regenerated, not hand-edited).

**F5 — no solidity number moved anywhere in this batch.** Receipt: `make refresh-kb-metadata` reports
*"Rewrote solidity in 0 claim-quality.md file(s) (0 solidity line(s), 0 depends-on annotation(s)
changed)"* and *"Rewrote leaf-references footer in 0 claim-quality.md file(s)"*. `sup-3kq9w7`'s
`quality` stays `*pending*` exactly as R42 requires.

**F7 — TWO MIXED-BIN LINES carry both a DIES row and a co-resident NEEDS-RE-DERIVATION row; both are
now disclosed in-canon (added 2026-08-11 at review).** Grouping all 354 banked rows by `site` returns
exactly two sites hosting a DIES row **and** a non-DIES row:
`manuscript/ave-kb/vol9/ch9-mechanical-characteristics/index.md:11` and
`manuscript/ave-kb/common/port-register.md:49`. At both, the co-resident row is
**NEEDS-RE-DERIVATION** (the `v_bulk = √2 c_0` port-mode speed formula; the `Z_bulk = ρ c_bulk` /
gapless-band-edge cells) — **no SURVIVES-AS-RESPONSE row is touched at either site**, and the
*printed form* on those lines is what the worklist kills in both bins. So this is a **disclosure
defect, not an over-demotion**: the line-terminal / final-cell stamp reads as blanket while the EOF
note scopes it. **Fix applied:** one carve sentence added to each of the two EOF notes naming the
co-resident batch-2 NEEDS row verbatim (quote + rationale), and stating exactly what the stamp does
and does not demote. **No stamp moved, no preserved byte touched, no bin re-scored.** Batch 2 must
expect an already-stamped line at both sites.

**F8 — one R39 BYTE-FENCE BREACH occurred and is REVERSED; the census is corrected from two to
three.** `08_gravitational_waves.tex:208`, inside the preserved Q1-REVERT warningbox. Full statement,
cause, and the detector method-change at §2 / §2.1; corrected inventory at §2.1 and §4. This is the
second breach of this convention in the arc — the first-cut detector was window-based and could not
see a declaration 32 lines below the stamp. The replacement detector is container-based.

**F9 — the auto-generated "Also covered by this demotion" line was a POLARITY-BLIND regex scrape;
11 instances corrected.** The generator emitted every `:NN` occurring in the audited rationale
without reading whether the rationale named it as *covered* or as *surviving / untouched /
not-adjudicated / cited-as-evidence*, and without subtracting lines this pass itself stamped. Of 29
emitted lists, **18 were correct** (the rationale genuinely says "also covers" / "covers" / "recurs
at" / "same claim repeats at") and **11 were wrong**. Every emitted target was re-intersected against
the banked bins and against the stamped set. **One swept a banked SURVIVES-AS-RESPONSE row** —
`biquaternion-complex-coupled-network-equations.md:159`, whose own rationale carves it verbatim
(*"the mass-store-at-Γ=-1-wall column survives separately (see :159 row)"*). Two were **cross-file
referents rendered as same-file `:N`** (`lattice-phase-transition.md:39 → :214`, actually
`solver-toolchain.md:214`, out of range in a 23-line file; `translation-tables/index.md:51 → :19`,
actually `translation-elastodynamics.md:19`, in range and therefore resolving *silently* to an
unrelated navigation line). Four declared "not separately stamped" lines that this pass **did** stamp
(`vol9/claim-quality.md:574`, `04_continuum_electrodynamics.tex:350`,
`05_ac_electrical_characteristics.tex:553` and `:571`). Four swept sites the rationale names as
surviving or not-adjudicated (`lc-electrodynamics.md:72` — the TRANSVERSE `v_T = c` identity;
`translation-circuit.md:983` — a routed re-derivation lane; `ligo-gw-saturation-ratio.md:32` — an
A1-vs-T2 sector-ownership flag explicitly *"not adjudicated here"*; `gw-propagation-lossless.md:50` —
re-scoped, not killed). In every corrected instance the carve sentence now travels **verbatim from
the banked rationale**. **No live-canon line was ever stamped by this defect** — verified: none of
the swept targets carries an in-place stamp — so the corruption was confined to the EOF ledgers, but
those ledgers are what batch 2 reads.

**F6 — the engine edits are documentation-only.** `git diff -U0 -- src/` adds 442 lines and removes
11; every added line is a `#` comment or docstring prose, and the 11 removed lines are the 11 stamped
lines re-emitted with their stamp. No code path, constant, default, flag or numeric changed.
`python -m compileall` clean on all 8 modules. Any *live* re-scope of the coded bulk sector
(`BulkRarefactionSector`, `bulk_density_on`, the `c_bulk` EOS, the PML bulk vent) is **not** in this
batch — it belongs to the engine lane, and the notes say so in their own text.

---

## 7. Counts re-taken AFTER the edits, at the batch SHA, two methods

Tree state: branch `docs/2026-08-11-r40-batch1`, **receipt measured at the committed fix-pass SHA
`2d178335`**, base `origin/main` @ `ad376e9a`.

> ⚑ **RE-RUN AT THE FIX-PASS TIP (2026-08-11, at review) — the first cut's receipt was STALE, and it
> is replaced, not re-declared.** The prior wording claimed *"61 … byte-identical to the original
> after the stamp string is removed … 0 violations"*, but the cite-excerpt fix commit had already
> appended authored prose **beyond the stamp** to one of those lines
> (`gw-propagation-lossless.md:48`), so under the receipt's own stated criterion that figure was 1,
> not 0. The receipt below is a **fresh run at the fix-pass tip**, and it counts that line honestly in
> its own row. **A receipt that describes a prior tree is not a receipt** — this one names the tree it
> was measured on. The stamp count also moves 61 → 60 because the R39 breach reversal removes a stamp.

**The load-bearing receipt — no pre-existing line number moved. Re-run at the fix-pass tip:**

- **METHOD 1 — `git diff -U0` hunk-shape analysis vs `origin/main`** (engine: `git` 2.x + Python
  `re`; tree = fix-pass tip). Over the 42 demotion-edited files: **59 in-place hunks**
  (old-count == new-count ⇒ no shift), **42 EOF-append hunks** (append past the original EOF, or the
  original last line modified plus an append), **0 SHIFTING hunks**.
- **METHOD 2 — independent line-by-line comparison against `git show origin/main:<file>`** (engine:
  Python, no git plumbing beyond the blob read; tree = fix-pass tip). **23255 original lines
  checked**, **60 modified original lines**, **0 violations**, **0 truncations**, split honestly:
  - **59** are **stamp-only** — byte-identical to the original after the stamp string is removed
    (2 of those differ by one additional inserted space, from the insert-before-needle path);
  - **1** — `manuscript/ave-kb/vol3/gravity/ch08-gravitational-waves/gw-propagation-lossless.md:48` —
    additionally carries the **gate-required verbatim cite excerpt** appended after the stamp
    (required by `verify-new-cite-excerpts`; the excerpt is byte-exact against
    `08_gravitational_waves.tex:107`). Its original text is a strict prefix of the new line, so the
    incoming cite still resolves to a **superset** of its original content.

⇒ **Every one of the 3997 incoming `path:NN` cites into these files still resolves to its original
content.** The 60 modified original lines reconcile with the identity's stamp count of 60 (§3).

**Scope note on this receipt's currency.** The receipt is measured over the **42 corpus files** at
`2d178335`. Any commit after it that touches only `_orchestration/` leaves those 42 files
byte-untouched and so does not stale it — check with `git diff --name-only 2d178335 <tip>`; **if that
list contains anything outside `_orchestration/`, the receipt must be re-run before it may be
cited.** This scope rule is the fix for the defect that produced the first cut's stale receipt.

**Gates, on the edited tree:**

| Gate | Result |
|---|---|
| `make verify` | **PASS**, exit 0 (`[Verify] ALL PHYSICS PROTOCOLS PASSED`) after `make refresh-kb-metadata` regenerated the derived `.index/claims.jsonl` |
| `make verify-new-cite-excerpts CITE_BASE=origin/main` | **PASS** — *"every added load-bearing line-cite carries an adjacent excerpt"* (see the gate note below) |
| `make test` | **PASS** (see §7 note) |
| `python -m compileall` on the 8 edited engine modules | clean |

**Advisory counters (`verify-anchor-content`, WARN-CLASS, non-gating), before → after:**

| counter | baseline @ `ad376e9a` | after the fix pass @ `2d178335` | Δ |
|---|---|---|---|
| cites scanned | 16364 | 16433 | +69 |
| checked & anchored (OK) | 899 | 904 | +5 |
| DRIFT — excerpt moved | 433 | 437 | +4 |
| DRIFT — excerpt absent | 2048 | 2049 | +1 |
| **checked cites** | **3380** | **3390** | **+10** |
| **drift total** | 2481 (73.4%) | 2486 (73.3%) | +5 |

*(Superseded first-cut row, kept for the trail: at the pre-review tip the table read
16366 / 898 / 435 / 2047, checked 3380, drift 2482. The fix pass adds the three file-qualified
cross-file cites and their excerpts plus the record's own new cites, which is why `scanned` and
`checked` rise.)*

**Gate note worth carrying forward (a real trap in this repo's tooling).**
`verify-new-cite-excerpts` diffs against `CITE_BASE`, so **it only runs for real once the pass is
COMMITTED** — run against an uncommitted working tree it sees zero added cites and passes
*trivially*. Run against the landed commits it flagged three added cites with no adjacent verbatim
excerpt: `translation-tables/index.md:49 → translation-elastodynamics.md:19`,
`lattice-phase-transition.md:37 → solver-toolchain.md:214` (both riding inside audited-row rationales
quoted verbatim in EOF notes), and `gw-propagation-lossless.md:48 →
08_gravitational_waves.tex:107` — the last being a **pre-existing** leaf cite the gate re-classified
as "added" because the R40 stamp modified its line. All three now carry a backticked verbatim
excerpt. **Anyone running this gate on a work-in-progress tree and reporting green has not run it.**

The +2 scanned are two `path.ext:NN` cites that ride inside audited rationales quoted verbatim in EOF
notes (`solver-toolchain.md:214` inside `lattice-phase-transition.md`'s note;
`translation-elastodynamics.md:19` inside `translation-tables/index.md`'s note). The +1 net drift is
an artifact of the notes themselves: each EOF note reproduces its demoted quote verbatim **in the same
file**, so the ±10-line window checker can now find a previously-absent excerpt "elsewhere" and
re-bins it as *moved*. No `:NN` anchor was invalidated — §7's METHOD 1 + METHOD 2 receipt is the
primary evidence for that, not this advisory.

---

## 8. What batch 1 did NOT do

- The **185 NEEDS-RE-DERIVATION** rows and the **105 SURVIVES-AS-RESPONSE** re-scopes: untouched.
- The **two-method straggler sweep** (the PATTERN-BOUNDED floor's remainder): not run.
- **No live re-scope of engine behaviour**, no flag default changed, no channel enumeration rewritten
  (e.g. `biquaternion-…-network-equations.md`'s "three propagating wave channels" is demoted, not
  re-written to "2 propagating + 1 bound" — that re-derivation is a batch-2/NEEDS action).
- **No `status:` field of any `def-` node moved**, no solidity moved, no `clm-`/`sup-` minted or
  retired.
- **The R42 referent-drift is not adjudicated** — F1 is a finding with a recommendation, routed to
  Grant.
- **★ NAMED OPEN DEBT — the #938 supersession note is OWED and NOT written.** The merged batch-0
  prose record [`2026-08-10_r40-sweep-scope-verification.md`](2026-08-10_r40-sweep-scope-verification.md)
  still asserts 5 DRIFTED rows at `:61` / `:79`; §5 of this record and the worklist JSON's
  `repin_2026_08_11` block carry the correction, but that merged artifact is deliberately **not
  edited by this batch**. It owes a dated forward/supersession note (corpus convention: the
  `SUPERSEDED, 2026-08-10 (R44)` banner pattern). Routed to the orchestrator. See F2.
- **The two mixed-bin lines are DISCLOSED, not re-scoped** — F7. The co-resident
  NEEDS-RE-DERIVATION rows at `vol9/ch9-mechanical-characteristics/index.md:11` and
  `port-register.md:49` remain batch-2 work; batch 1 only carves the stamp's scope in the notes.
