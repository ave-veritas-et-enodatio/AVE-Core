# PENDING GRANT RULINGS + FRONTIER QUEUE — state as of 2026-07-20 (post-#761-review)

**Class: orchestration state doc.** Repo-tracked per the multi-agent ruling (2026-07-20: anything multi-agent-usable must be repo-tracked; model-local memory is not a shared surface). Supersede by dated edit or successor doc; do not silently rewrite.

## §1 — Rulings Grant owes (each fires a routed lane on his word; nothing executes without it)

1. **Merge #761 — ✅ DISCHARGED 2026-07-20** (Grant merged; main @ `caa51c17`). The NONE-DERIVES derivation is banked. Merging did NOT execute the consequence — ruling 2 below remains open.
2. **THE REVERT RULING — ✅ DISCHARGED 2026-07-20 (this PR — `docs/q1-revert-execution`).** Per `port-register.md:87` (verbatim): *"if NONE-DERIVES this ruling REVERTS and the banked Reading-A exclusion (falsification ledger; 9–110σ pulsar) goes live."* Executing it = a routed auditor lane that (a) flips the Q1 row RULED-CONDITIONAL → REVERTED/Reading-A-live, (b) promotes the exclusion entry in `genesis-chord-falsification-ledger.md` from banked-conditional to live, (c) stamps both with the #761 receipt, (d) flags/stamps `manuscript/vol_3_macroscopic/chapters/08_gravitational_waves.tex` (the q1-hardening §4 double-count tension: its Hulse-Taylor macro-LC reproduction rides the shear formula while step-3 calls the decay bulk — the manuscript must not lag the KB, per the honesty-lag discipline). Rule 11: the clause fires as written; the envelope lane (§2) is the forward path that could later re-open it with a DERIVED coupling — re-open is a separate future ruling, not a reason to soften this one. **DISCHARGE:** all four arms landed on `docs/q1-revert-execution` — (a) port-register Q1 row flipped REVERTED → Reading-A-live (RULED-CONDITIONAL text preserved verbatim per Rule 12) + §3 verdict + §4 tally + frontmatter; (b) ledger entry `q1-reading-A-radiative-bulk-port` promoted to LIVE closed-negative; (c) #761 receipt (`caa51c17`) two-method-verified on both; (d) a new dated ch08 warningbox flags the LIVE bulk/shear double-count contradiction (physics prose NOT rewritten; the earlier RESOLVED box NOT edited — its "no double-count" *basis* superseded by the revert, flag-don't-fix / Rule 12) routed to the manuscript-reconciliation program. Docket: ENTRY 2026-07-20-q1-revert-execution.
3. **AVE-Skills push call — ✅ DISCHARGED 2026-07-20 (reported-by-orchestrator).** The three minted skills + verify-before-cite v1.6, local-only commit `cfb2b5f`, were **pushed to `origin/main` of the AVE-Skills repo** (`cfb2b5f` + a one-word precision fix, per the orchestrator) — now multi-agent-reachable beyond this machine. *(Prior state, preserved: committed as local-only `cfb2b5f` (unpushed); the orchestrator would not push a remote without the word.)*
4. **`master-equation.md:20` sector-ownership wording tightening — ✅ DISCHARGED 2026-07-20 (this PR — `docs/masterq-tag-vocab-defs`; Grant-ratified verbatim [sic]: "ratify master equation").** The Grant-ratified **sector-dynamics tag** landed at `master-equation.md:26` (FROZEN wording, zero edits; additive after the 2026-07-03 Rotation-flavor tag banner, prior lines byte-untouched per Rule 12) — *"∇·u propagates; ∇·A is gauge"*: the "Heaviside-excised" line is **historical/notational** for the *transverse EM* sector, NOT a dynamical gauge-kill of the DYNAMICAL A1 compression scalar (bulk restoring force ½K(∇·u)², K=2G, gapless P-branch). Two **category-(a)** vocab def-nodes minted in `vocabulary-register.md`: `def-l0ngdu` (SOLID — the longitudinal ∇·u/∇·A split) / `def-uatk1s` (proposed — the u/A counterpart-sector mapping, inherits `def-tk1xfm`). Docket: ENTRY 2026-07-20-masterq-tag-vocab-defs.
   - **Queued follow-on — ✅ DISCHARGED 2026-07-21 (this PR — `docs/envelope-vocab-defs`).** category-(b) envelope-sector vocab: FOUR def-nodes minted in `vocabulary-register.md` — `def-envcar` (envelope vs carrier, the two-level decomposition of the A1 breather; **SOLID for the DECOMPOSITION only** — the #767 coupling verdict is BIN-1-CONDITIONAL, not hardened) / `def-satshr` (saturation-shear (traceless, observed GW) vs saturation-compression (trace, Q1 Reading-A LIVE bulk port); **SOLID for the definitional SPLIT**) / `def-ncsatw` (non-captured (free) vs captured (bound) saturation wave; **proposed, WALK-RATIFIED DIRECTION** — the RECORD canonizes nothing) / `def-pndenv` (ponderomotive envelope coupling; **proposed** — source structure banked per #767, coefficient q1-inherited/ASSUMED). Receipts verify-before-cite two-method at HEAD `3d07ceeb`: `master-equation.md:20` (breather, canon), #767 merged `5287ef32` (the reduction, BIN-1-CONDITIONAL), #768 merged `c12da9f5` (Abbott second-source stamp), `port-register.md:87` (Q1 Reading-A LIVE), RECORD §4–§5. Cross-ref'd from `substrate-native-terminology.md` (Cross-references block — natural home, no leaf restructure). `make refresh-kb-metadata` run (4 `definition` records into `claims.jsonl`). No walk-level or coupling claim hardened into canon (def-uatk1s/#769 format precedent). Docket: ENTRY 2026-07-21-envelope-vocab-defs. *(Original routed text, preserved per Rule 12 KEEP-BOTH:)* **Queued follow-on:** category-(b) envelope-sector vocab (envelope/carrier, ponderomotive, saturation-shear vs saturation-compression, non-captured saturation wave) lands with the #767 canonical-propagation follow-on after its review clears.
   - *(Original routed text, preserved per Rule 12 KEEP-BOTH:)* **`master-equation.md:20` sector-ownership wording tightening** (Grant-level, routed from #761 result §6.2-2): the canon line's dual phrasing (name + physical content of the ONE A1-breather object) invited the gauge misreading the #761 review had to close at corpus level; a one-sentence tightening prevents re-litigation.
5. **Standing riders:** genesis-24 receipt-refresh call; Abbott PRL 123,011102 log₁₀-Bayes figures manual eyeball (single-sourced import).
6. **Bond ontology-grade ratification (post-walk) — ✅ RULED 2026-07-21 (KEEP-BOTH).** Grant ratified the bond grade mid-flight (verbatim `[sic]`: *"yes rhat grade matches my perspective"*): the **bond** noun = **IDENTITY-at-structure-level** (three orientation-resolved channels $k_a/k_s/\gamma_c$, unitary node scatter, finite per-cell delay); **"TL segment"** = **MODEL-OF, REGIME-SCOPED** (per-channel projection, exact long-wave / EM-native; at band-edge scales the DISTRIBUTED arccos-TL description stays exact while the LUMPED mass-spring approximation breaks down — `clm-bnd5rq`; the two descriptions book the delay differently, distributed-in-bond vs emergent-from-chain; co-equal only below the band edge). Landed as `def-b0nd01` on `docs/substrate-noun-ontology`. **★Correction (2026-07-21, #781 review):** the original inverted label (arccos paired with the *lumped* description) came from the orchestrator walk and was caught by this review; the ratified GRADE (IDENTITY + MODEL-OF regime-scoped) is unaffected — the reviewer's carve. The prior DRAFTED-PENDING-GRANT status is preserved-in-substance per Rule 12 KEEP-BOTH (the walk was delivered AND ratified the same day). Docket: ENTRY 2026-07-21-substrate-noun-ontology.
7. **`def-tk1xfm` ratification package — ✅ RULED + EXECUTED 2026-07-21 (Grant ratified, verbatim `[sic]`: *"ratify def-tk1xfm"*).** With the impedance-analogy convention pinned (`def-1mpanl`) and the band-edge scope walked + ratified (`def-b0nd01`, ruling 6), the `def-tk1xfm` (TKI-transformer, PROPOSED since PR #265) ratification package was **complete** and Grant ratified it 2026-07-21. **EXECUTED on `docs/substrate-noun-ontology` (#781 repair pass):** `def-tk1xfm` flipped proposed→SOLID (register status + header + adjudicated-meaning + verification, Rule-12 preserving the prior proposed text). ★**Framed one-sentence ratification (as put to Grant, ratified verbatim; preserved per Rule 12):** *"Ratify `def-tk1xfm` proposed→SOLID: the Topo-Kinematic Isomorphism (Axiom 2) is an EXACT, lossless, gain-1, pole-less, invertible **co-equality of the mechanical and electrical descriptions BELOW the band edge** ($\omega\tau\ll1$ / long-wave regime), with **per-sector carriers at lattice scale**; the co-equality is REGIME-SCOPED — at band-edge scales the distributed-in-bond (arccos TL) and lumped mass-spring descriptions book the delay differently and are no longer co-equal (per the ratified bond scope, `def-b0nd01` / `clm-bnd5rq`)."* Ratifying flipped `def-tk1xfm` proposed→SOLID and lifted the scopes that inherited its ceiling: `def-uatk1s` **upgraded** proposed→SOLID (sole limiter was the tk1xfm inheritance; its transverse identity-collapse stays ROUTED-OPEN on its own axis); the row-mapping half of `def-1mpanl` was already SOLID; **`def-portmp` stays proposed** with a dated note (its limiter was NOT tk1xfm — gated independently on auditor+Grant review + the $V_{yield}$-fork three-channel roles). ★**One clause of the framed sentence — "per-sector carriers at lattice scale" — rode the inverted orchestrator walk and is corrected → item 8 (PENDING-GRANT-CONFIRM).** Docket: ENTRY 2026-07-21-substrate-noun-ontology + -781-review-repairs.
8. **`def-tk1xfm` framed-sentence carrier clause — ✅ CONFIRMED 2026-07-21 (Grant, verbatim `[sic]`: *"confirm"*, item 3 of the 2026-07-21 adjudication queue).** The ratified item-7 sentence's CORE (proposed→SOLID; exact co-equality below the band edge) STANDS. One clause — *"per-sector carriers at lattice scale"* — rode the same inverted walk R1 corrected and is adjusted to the corpus-adjudicated form: *the distributed/TL description is the adjudicated band-edge carrier in BOTH sectors (`clm-bnd5rq`); the lumped mass-spring is the long-wave approximation; the co-equality statement is between the mechanical and electrical descriptions (the TKI proper), exact below the band edge where lumped↔distributed also coincide.* **Grant confirmed the corrected carrier wording 2026-07-21** (the SOLID flip never waited on this — only the one clause's phrasing did). `def-tk1xfm`'s status field (`vocabulary-register.md`) updated PENDING-GRANT-CONFIRM → GRANT-CONFIRMED. Docket: ENTRY 2026-07-21-substrate-noun-ontology-781-review-repairs + 2026-07-21-tk1xfm-carrier-clause-confirm.
9. *(renumbered 2026-07-21: was a second "6." — duplicate list number, content unchanged)* **Ringdown v1↔v2 spin-mapping fork RULING + LIGO-ringdown foreword benchmark walk-back — ✅ RULED + EXECUTED 2026-07-21 (KEEP-BOTH).** Grant, verbatim [sic]: *"ruling 1 proceed with your rec, ruling 2 walk back now."* **Ruling 1 = option B1** on the reopened fork (surfaced by the #774 MATCH-ARTIFACT / § FORK-REOPEN, evidence-brief #776): the mΩ spin-refined Q-law committed; **Q=ℓ re-scoped to the a\*=0 cold anchor**; Phase-5's v1/v2-invariance hypothesis **RETRACTED**; **v1 re-selected** as the standing ω_R spin-mapping phenomenology at its honest **0.55 disclosed-phenomenological** grade (+2.63% mean primary), with the **τ −5.44% near-miss banked as an OPEN tension** and the Q-derivation named as next work. **Ruling 2 = the foreword ringdown-benchmark walk-back**, executed with the B1 outcome filling the slot. **EXECUTED** on `docs/ruling-b1-foreword-walkback` — 7 arms: (a) eigenvalue-leaf B1 re-bank banner; (b) qnm-quality-factor Q-scope banner; (c) design-doc §10 Phase-5 retraction; (d) 3 register corrections (:202/:204/:217, solidity 0.55 unchanged); (e) 54/49 near-extremal forward-prediction promotion (first-principles-predictions.md); (f) foreword scope-correction block + ch15 chapter number-correction note; (g) this bookkeeping + docket ENTRY 2026-07-21-ruling-b1-foreword-walkback. Docket entry carries the per-arm detail. *(Prior state preserved per KEEP-BOTH: the fork ruling + foreword walk-back were carried as FLAG-DON'T-FIX walk-back candidates in the #774/#776 lanes, routed to Grant; that routing is now DISCHARGED by this ruling.)*
10. **Protocol E folded into the vessel-state RVE prereg — ✅ APPROVED 2026-07-21 (Grant, verbatim `[sic]`: *"approved the three words from my wue"*, covering all three of the day's β-feasibility words).** The anisotropic vessel-state RVE bench must measure the composite's **acoustic effective density** ρ_eff (long-wavelength time-of-flight, β-feasibility "Protocol E": L~32–48, `ρ_eff = K_eff/c_eff²`) **alongside** K_eff, so `r_Z = Z_eff/Z_0` is **measured, not assumed** — the ρ side carries two competing terms (structural soft-inclusion added-mass, possibly DOWN/bubble-like; trapped-energy loading, UP per the relative-offset direction ruling `clm-hu1jjw`), and only the lattice-accessible structural term is measurable on the current engine. Scoping basis: `research/2026-07-21_beta-tracking-feasibility_scoping.md`. The prereg itself stays GATED on the vessel-state walk (bias-network picture; bridge-vs-bottleneck kill conditions both armed; grown-not-painted vessel state).
11. **β-proper routed to the continuum radial-solver lane — ✅ APPROVED 2026-07-21 (same verbatim word).** The trapped-energy mass-loading β is **not representable on the current lattice engine** — three structural absences (no mass-energy equivalence: uniform node mass; no advective pattern transport: bond strains boost-invariant in the small-displacement formulation; imposed-grade-sets-the-answer: a hand-painted Eulerian grade forces β=0, a hand-co-moved grade forces β=1). Per regime discipline, any lattice β null is an **ARTIFACT, not a falsification** of the relative-offset direction ruling. β enters the continuum radial-solver lane as an explicit **E=mc² IMPORT** (tagged import, not derived) scoped by `clm-hu1jjw`'s direction; the lane charter is being drafted for Grant review before build. Scoping basis: the same scoping doc.
12. **`leg4_moving_cage` quarantine — ✅ APPROVED 2026-07-21 (same verbatim word) + CONDITION MET + EXECUTED (this PR).** The `constituent_cage_ensemble.py:525` `carry_fraction`/`energy_centroid_displacement` outputs are artifact-dominated (initial zero-velocity sample subtraction; committed-JSON reads near-identical ~1.4% across cage classes — the same division-by-drift artifact class the β scoping doc names). The gating corpus sweep completed 2026-07-21: **LOAD-BEARING-NOWHERE** (two-method; #770 banks rest on Legs 3/5/6; #775/#782 never consume Leg 4; one latent unexercised Fork-C clause names the Leg-4 *far-field* only — NOT the quarantined outputs — and stays live as frozen). Quarantine executed as a dated in-driver docstring note scoped to the two artifact outputs; far-field outputs untouched. Basis: `research/2026-07-21_beta-tracking-feasibility_scoping.md` §8.

---

### §1 continued — NEW opens + rulings, appended 2026-07-26 (items 13–20)

> **Why appended (the tracker gap).** Items 1–12 above are all discharged, so the queue had gone empty in-file while a fresh batch of opens and rulings lived only in chat. Grant `[sic]`: *"do the rec"* — the recommendation being that every open and every ruling gets a repo-tracked row, so the queue survives outside the session. Each row: **what it is / what rides on it / current status / Grant's verbatim `[sic]` words where ruled / the executing lane or PR.**
>
> **⚑ Merge hazard on this file (pre-existing, recorded at `_orchestration/docket-entries/README.md`):** this doc falls under the blanket `*.md merge=ours`, so a concurrent lane's status-flip can be **silently dropped** rather than conflicting. Any lane touching §1 should re-read the merged file after merge and re-apply if a row went missing.

13. **Sector-of-storage — WHERE the compression store lives (A1 canon vs the T2/swing label). ⚑ OPEN-IN-WALK — Grant is walking it; NOT ruled.**
    - **What it is.** `master-equation.md`:20 is canon that the **A1 dilatation** carries the compression store (the mass "3": the Heaviside-excised longitudinal scalar $V$; $m_ec^2$ = trapped acoustic compression energy). The charter/`clm-m5swh9` line labels the trapped energy as **T2/swing-class energy sitting IN the A1 carrier's inertia budget** (`research/2026-07-21_continuum-radial-solver_CHARTER.md`:15,:23). `A1 ⊥ T2` forbids silently assigning it either way — that is the whole content of the sector-ownership cross-wiring watch.
    - **Grant's question in chat, verbatim `[sic]`:** *"is this just ac vs dc analysis?"* — **the orchestrator's answer:** the **AC/DC carve** (`clm-acdc07`) reframes it rather than dissolving it: the **oscillating** store (the AC half — what swings, what a small-signal probe sees) and the **static confining texture** (the DC half — the frozen bias the swing rides on) are different bookkeeping slots, and the open question is which slot the trapped energy is booked in. **The walk continues; nothing is ruled.**
    - **What rides on it.** (i) **Charter D1** — the sector-crossed $c^2$ that divides $E_{trapped}$ ($c_P=0.519$ / $c_S=0.286$ / $c_{EM}$; CHARTER:15,:62 = I8). (ii) **C-load's premise** — the UP acoustic loading arm of the relative-offset principle is CONDITIONAL on a nonzero sector-crossed acoustic inertia, i.e. on exactly this (`clm-hu1jjw` C-kin unconditional / C-load conditional; magnitude OPEN at `clm-m5swh9`). (iii) the **Eötvös shared-ledger route** — whether trapped-swing energy and rest mass draw on ONE inertia ledger is the equivalence-principle-facing consequence.
    - **Note (2026-07-26):** the `m^*` band-curvature route is **NOT** a way around this — see item 18 / `def-mstar1`: the literal $\hbar^2/(d^2E/dk^2)$ read is sector-blind and presupposes the answer. D1 is not answerable by dispersion decoration; it needs this walk.
    - **Status:** **OPEN-IN-WALK.** No lane fired; no default admissible (per the CHARTER, *"no default (e.g. the SM habit of `c_light`) is admissible"*).
    - **Pointer (2026-08-02) — the walk produced a canonized PICTURE, not a ruling.** The paperclip twist analogy is canonized as a **walk-ratified physical analogy** at [`manuscript/ave-kb/common/electron-plumbing-primer.md`](../manuscript/ave-kb/common/electron-plumbing-primer.md) Step 3.5, with this item as its **explicit fence**: whether the base twist's spring energy sits INSIDE $m_ec^2$ is stated there as OPEN-under-item-13 and is **not** pre-empted (the section is pedagogically live under either outcome, and says so). **Nothing here is ruled by that commit** — item 13's status is UNCHANGED at OPEN-IN-WALK. Class: pedagogical/consistency, **no `clm-` minted, no solidity assigned**, and per `ave-discrimination-check` **not** logged as a discriminating connection.

14. **The F6 attribution — the #796 bench's two contradicting mechanism strings. ⚑ OPEN; disposition pending.**
    - **What it is.** The vessel-state RVE bench (#796) refuted the headlined **hoop-stiffen ⊕ radial-soften CANCELLATION** mechanism by direct ablation (`radial_only` **raises** the core tangent `+0.429 %` vs control instead of softening it; **both** named components stiffen; `cancellation_mechanism_supported = False` — `research/2026-07-22_vessel-state-rve_result.md`:165-167). The bench then ships **two positive mechanism strings that contradict each other on its own numbers**: `verdict.ablation_decomposition_fixed_budget.attribution` says the null lift is *"provenance/live-operator-driven"* (`vessel_state_rve.py:1227–1230`), while `verdict.painted_anisotropic_fixed_budget.note` states the decision rule *"GROWN≈PAINTED ⇒ anisotropy-driven"* — and the shipped numbers are grown `0.29548` vs painted `0.29824`, i.e. `Δ ≈ 0.93 %` ⇒ **GROWN ≈ PAINTED**, which by the bench's own rule says the provenance channel is **not** carrying it. **The two strings cannot both be right.**
    - **Current disposition (already landed, not the open part).** The **positive** attribution is **WITHDRAWN** from the doc's claims; the computed flag `cancellation_mechanism_supported = False` is unaffected; the driver's `attribution` string is left **byte-unchanged** so the conflict stays visible in the data (flag-don't-fix). The honest banked statement: **the null lift is NOT a hoop-vs-radial cancellation, and its positive mechanism is UNATTRIBUTED.**
    - **The open part.** Grant asked for a clearer statement in chat, verbatim `[sic]`: *"what?"* — a re-explanation was delivered; **the disposition of the contradiction is still pending.** The choices on the table (none picked): fix the driver's `attribution` string to match its own rule; keep both and route the mechanism question to a follow-on ablation; or bank the negative and retire the positive-attribution slot entirely (Rule 12 — do **not** refill it with a new unverified mechanism).
    - **What rides on it.** The #796 bin (UNDETERMINED) does **not** move either way — this is about whether a positive mechanism is bankable at all, and about the driver shipping a self-inconsistent string.

15. **OWED-1 — the SUBC/KUBC bracket. ✅ RULED 2026-07-26 → FIRED.**
    - **Grant, verbatim `[sic]`:** *"yes, as above, so below, we should assume we have both boundaries right?"*
    - **What it is.** #796's verdict rides a **one-sided** bound: `r_Z = 0.5436` is the structural anchor built on a **KUBC upper bound** for $K$, and it **straddles** the `0.5` decision boundary, so bridge-vs-bottleneck cannot be decided (`research/2026-07-22_vessel-state-rve_result.md`:210 — *"The owed resolver is the feasible SUBC/periodic lower-bound bracket"*, same cost class, `L = 16`, #782 Fork B). Building the **SUBC** arm brackets $K$ from below as well, so the discriminator becomes a **bracket** rather than a single point against a straddle.
    - **Status:** **FIRED** — lane building the SUBC arm.
    - **★Scope limit (do not over-read):** the bracket **brackets $K$, NOT $\rho$**. Per `:211` verbatim: *"a SUBC/KUBC bracket brackets `K`, not `ρ`"* — OWED-1 does **not** dispose of OWED-2 (item 16).

16. **OWED-2 — the $\rho$ extraction (the Protocol E method-instability). ✅ RULED 2026-07-26 → FIRED (option a, with c as declared fallback).**
    - **Grant, verbatim `[sic]`:** *"a with c as fallback works"*
    - **What it is.** #796's repaired Protocol E is **method-unstable**: the grown cage-array is a locally-resonant scatterer, the ToF returns an **unphysical `c_eff > c_0`**, the self-consistent continuation **DIVERGES** (`0.519 → 0.701 → 1.499`), and the `r_Z` band crosses the SOFT/UNDETERMINED boundary at **both** box sizes — so **no decidable `ρ_eff` is extractable** and the shipped `r_Z` rides `ρ ≡ 1` **assumed, not measured** (the first-run *"`ρ_eff/ρ_0 = 0.916` MEASURED"* headline is 🔴 RETRACTED).
    - **The ruling.** **(a) quasi-static $\rho$ feasibility scoping FIRED** — a static/quasi-static $\rho$ estimator instead of a ToF; **(c) the continuum-solver route is the DECLARED FALLBACK** if (a) does not yield a decidable $\rho_{eff}$.
    - **★Root cause on record (this is the transferable part).** The **frozen** Protocol E requirement `k·r_core ≪ 1` and the **frozen** box sizes `L ∈ {32,48}` at `φ_sf = 0.489` are **mutually unsatisfiable**, *"and nothing in the freeze caught it"* (`:219`). Not a data fault — a freeze-time fault. See the DESIGN LESSONS block below.
    - **What rides on it.** Whether the vessel-state bench family can measure the $\rho$ side **at all**, or whether `ρ ≡ 1` becomes a disclosed standing structural assumption and the $\rho$ measurement is retired from this family.

17. **Charter D2 / D3 / D4 / D5 — the continuum radial-solver build gates. ✅ RULED 2026-07-26 (all four).**
    - **Grant, verbatim `[sic]`:** *"D2: disclosed, D3: follow rec, D4: do it, D5: do the rec"*
    - **D2 — the participation magnitude β:** **disclosed β sweep.** The `#782` placeholder scan `β ∈ {0,1,3}` (`ρ_eff/ρ_0 = 1 + β·φ`) is imported as an **explicitly disclosed sweep**, not held fully symbolic (CHARTER:16). Rule-12 note: this is a disclosed *scan*, not a derived value — the slot is not refilled with an unverified number.
    - **D3 — the solver method:** **transfer-matrix primary + analytic matched-asymptotics backstop** (two independent routes, so a single-method artifact cannot carry the result).
    - **D4 — staging:** **build stage-1 now**, gated on **profile-independent validation gates** (the gates must fire on known-positives before any verdict arm — see the DESIGN LESSONS block).
    - **D5 — the profile input:** **feed the #796-measured GROWN profile** — hoop-tension axial strain to `ε ≈ +0.139` at peak `A`, radial-compression to `ε ≈ −0.066` (`min k_shear,eff = 0.353`), verbatim from `research/2026-07-22_vessel-state-rve_result.md`:82. (The #796 result doc names itself the D5/R1 profile input.)
    - **★D1 remains HELD** pending item 13 (sector-of-storage). D1 is the one gate the build **cannot** proceed past without a physics input; D2–D5 set *when* and *with what* the build runs.

18. **The `m^*` disposition. ✅ RULED 2026-07-26 → EXECUTED (this PR).**
    - **Grant, verbatim `[sic]`:** *"correct, should not be a fitting parameter, a plus c should make sense"* = option **(a)+(c)**.
    - **What it is.** `translation-circuit.md`:306 carried an OPEN flag: the corpus **strikes** $m^*$ as an SM fitting parameter (`19_silicon_design_engine.tex`:45, `11_experimental_falsification.tex`:463) **while** candidate-6 proposed $m^*=\hbar^2/(d^2E/dk^2)$ as a substrate-native inertia **to derive** — and vols 2/3 already USE an emergent effective mass. Same symbol, opposite disposition.
    - **The ruling.** A **HOMONYM**, not a physics conflict. **(a)** the strike stays **byte-untouched** — Sense A (the SM thermal-statistical **fitting parameter**) stays struck; **(c)** a `def-` node is minted disambiguating Sense A from **Sense B**, a **derived / substrate-read inertia** ($m_{eff}=m_e\sqrt{1+k}$, the emergent $M_{eff}$, the dressed-branch acoustic $\rho_{eff}$) — a **different object, not struck**. Grant's discriminator: **derived-not-fitted**.
    - **Executed:** `def-mstar1` in `manuscript/ave-kb/common/vocabulary-register.md` (status `ambiguous` — the symbol stays overloaded, the *disposition* is ruled), with a Sense-B name **"dressed effective density"** minted **proposed-for-ratification** (0 prior corpus hits; dimension caveat surfaced). `translation-circuit.md` §4.6.3 flag marked **RULED 2026-07-26**, evidence preserved. **★Also recorded:** the literal band-bottom curvature read is **DECORATION** for D1 (sector-blind; the gap divides out) — so item 13, not dispersion, is what answers D1.
    - **Lane/PR:** this PR (`docs/mstar-defnode-tracker`).

19. **AVE-Skills mirror push. ✅ RULED + EXECUTED 2026-07-26.**
    - **Grant, verbatim `[sic]`:** *"push"*
    - **What it is.** The semiconductor-tier rows landed in AVE-Core **#792** (merged) had no mirror in the AVE-Skills repo, so a skill body and its canonical leaf would have drifted.
    - **Executed:** commit **`44dd336`** — *"mirror: semiconductor-analysis tier rows (AVE-Core #792, merged)"* — on AVE-Skills `main`. **Receipt verified:** `git branch -r --contains 44dd336` → `origin/main`, and `git rev-list --count origin/main..HEAD` → `0` (pushed, nothing local-only). ⚑ *Recorded honestly:* the commit's author/committer timestamp in this environment reads `2026-07-28 00:04:06 -0500`, an environment-materialization artifact (every tracked file here carries a `Jul 28 00:0x` mtime) — **the SHA + on-`origin/main` check is the receipt, not the timestamp.**

20. **The cannot-advect relabel. ✅ RULED 2026-07-26 → propagation lane running (separate PR).**
    - **Grant, verbatim `[sic]`:** *"for the relabel, let's propagate/ (a)"*
    - **What it is.** The corpus's real-space *"does not move … cannot advect … pinned by its own frozen clock"* statements (`genesis-chord-falsification-ledger.md`:144) read as absolute station-keeping and sat in apparent tension with the relative-offset principle. The #787 review **RE-LABELED** them: they are more narrowly **no-SELF-transport / no-re-hosting-DOF** results at those formulations (which *supports* Corollary A index-pinning) **plus formulation-anchor nulls** (bond strains boost-invariant in the small-displacement model) — **not** a "can't-surf-a-drive ⇒ can't-track-material-displacement" result. The leaf explicitly did **not** reframe them (`relative-offset-principle.md`:59 — *"this leaf reframes none of them"*).
    - **The ruling: (a) = propagate the relabel** to the sites, rather than leaving it stated once at the principle leaf.
    - **What rides on it.** `clm-hu1jjw`'s strengthen-by leg 1 (the owed adversarial reconciliation of the corollary set against those sites) and the grade currently HELD at 0.50 pending a formal re-grade.
    - **Status:** propagation lane **running** in a **separate PR** (not this one).

### 21. The anisotropy observable — direction-dependent long-wave P-speed of a single-crystal vacuum (⚑ OPEN frontier candidate, 2026-07-28; NOT fired)

- **What it is.** The #802 SUBC/KUBC lane MEASURED the vacuum lattice as cubic-anisotropic at lattice scale: Zener `A = C44/C′ = 1.605` (KUBC) / `1.330` (SUBC) at `L=16`, cubic axes = lattice axes (verified by direct uniaxial probe, `C11 = K + 4C′/3` to `1e-10`). A cubic **single crystal** has a **direction-dependent longitudinal wave speed** — and the vacuum's lattice axes are cosmically fixed, while physical compression-wave directions are source-set. Surfaced by the 2026-07-28 orchestrator leak-audit (the same check that caught the VRH/polycrystal import: there are no grains, so "grain average" and "propagation-direction average" are different objects).
- **The question.** Does the direction dependence survive to the long-wavelength limit of the P-branch (the mechanical/gravitational band), at what magnitude, and is it already constrained observationally (gravitational-band propagation anisotropy relative to fixed axes)? Note the known corpus constraint class: emergent isotropy/Lorentz statements exist for the EM sector; whether the P-branch carries an analogous isotropization mechanism or genuinely inherits the lattice anisotropy is the open physics.
- **What rides on it.** Potentially a falsifiable prediction class (anisotropy in GW propagation vs a fixed cosmic frame) OR a consistency requirement (a mechanism that isotropizes the long-wave P-branch). Also bears on the item-13-adjacent modulus question: the physical discriminator's comparator is direction-resolved `C(q̂)`, not any polycrystal average.
- **Status:** OPEN, candidate for a scoping lane (feasibility-first per the standing pattern); NOT fired; no claim minted. Provenance: #802 §6.5 measurement + the 2026-07-28 leak-audit fold (docket ENTRY 2026-07-28-leak-audit-carves).

#### ★ STANDING DESIGN LESSONS — now binding on every future prereg (from the #791/#796 arc)

Recorded here because they are **cross-lane and permanent**, not #796 bookkeeping. All three are freeze-time obligations: they are cheap before a freeze and unrecoverable after one (the #796 verdict could not be re-banded post-hoc, and correctly was not).

1. **Every outcome class needs a REACHABLE bin.** Band placement must not make a physically-likely outcome unbinnable. #796's frozen bands put the `[canon, #782]` baseline `r_Z(β=0) ≈ 0.544` **inside** the `Z_str` straddle, so a null-lift vessel could **only** read UNDETERMINED unless a decisive $\rho$ measurement moved it out — *"the whole verdict was loaded onto the one measurement that this instrument cannot make. That is the design fault, and it is a band-placement fault, not a data fault"* (`research/2026-07-22_vessel-state-rve_result.md`:215). **Obligation:** at freeze, enumerate HARD / SOFT / NULL / UNDETERMINED and show each has a reachable bin — e.g. band a **lower bound against an upper bound** as the discriminator, not a single point against a straddle the baseline already occupies (`:218`).
2. **Gates must be DEMONSTRABLY FIREABLE on known-positives before any verdict arm.** A gate that has never fired is a checklist, not a gate (cf. the standing reconcile-don't-declare discipline). #796 ran LEG-0 gate-fireability self-tests that **force** outcomes (ii)/(iii) before the verdict arms were allowed to read. **Obligation:** ship the known-positive fireability demonstration *with* the freeze, and scope honestly what "fireable" means (#796 scoped (iii) to **fireability-in-extremis**).
3. **Frozen requirements must be checked MUTUALLY SATISFIABLE before freeze.** The #796 miss: `k·r_core ≪ 1` **and** `L ∈ {32,48}` at `φ_sf = 0.489` are **mutually unsatisfiable**, *"and nothing in the freeze caught it"* (`:219`). **Obligation (both halves):** (i) freeze a **precondition-consistency check** — every frozen requirement checked against every other frozen parameter at freeze time; (ii) freeze **what the verdict falls back to** when a measurement returns non-decisive, *"so the fallback is pre-registered rather than chosen after seeing the numbers, as it was here"* (`:219`).


---

### §1 continued — the 2026-08-01/02 ruling batch, enumerated (items D1–D8 + three side rulings), appended 2026-08-02

> **Why this block exists — the D1-label collision hazard, and it is not hypothetical.** Grant fired eleven
> rulings across 2026-08-01/02 under a **`D<n>` shorthand** that this corpus **already uses for at least five
> unrelated things**. Verified at HEAD, two-method (`git grep` + filesystem recursive grep):
>
> | Older `D1` sense | Receipt |
> |---|---|
> | **D1-CHSH** — the Tier-D divergence-map row ($\|S\|_{max}=2\sqrt2$ from the K4 Möbius half-angle) | `manuscript/ave-kb/common/divergence-test-substrate-map.md`:380 (section), :542 (Matrix-1 row) |
> | **D1 = the sector-of-storage question** (A1 canon vs the T2/swing label) | **item 13 of this very file**, `:30`; scoping doc `research/2026-07-26_d1-sector-and-inertia-route_scoping.md`; `manuscript/ave-kb/common/vocabulary-register.md`:1150 (*"This does not touch D1 itself, which stays OPEN"*) |
> | **D1 = the field-definition lane** (a lane name, not a question) | `_orchestration/2026-06-15_k2g-crystalline-provenance.md`:142 (*"HAND-OFF → D1 (field-definition lane)"*) |
> | **D1 = "Canonize the projective-ℂP¹ Smith-chart ontology?"** — a standing-decisions audit item | `_orchestration/2026-06-16_standing-decisions-audit-lane.md`:32 |
> | **D1 = a decision-item label inside a frozen astro sweep** | `research/2026-07-11_astro-adjudicator-sweep_branch-signature-map_FROZEN.md`:17 |
>
> ★**Honest count note:** the dispatch for this block said *"four older unrelated `D1` uses"*; the sweep found
> **five**. Recorded as found rather than as briefed. ★**And a verify-before-cite correction on the same
> sentence:** the dispatch attributed the collision finding to *"the #819 audit"*. **That attribution does not
> verify** — PR #819's body and its reviews carry no D1-label-collision item (checked via `gh pr view 819` and
> the reviews API). The collision is real; its provenance is **this block's own sweep**, not #819. The receipts
> above are what it rests on.
>
> **The disambiguator is the DATE, not the letter.** Every row below is `D<n>` **of the 2026-08-01/02 batch**.
> A bare "D1" in this corpus does not resolve; `D1 (2026-08-01)` does. Cite the date or cite the executing PR.

| # | Ruling, verbatim `[sic]` | What it decided | Executing PR / lane | State |
|---|---|---|---|---|
| **D1** | *"D1: (ii)"* | Option **(ii) = DEPRECATE** `chord_magnitude_ratio()`, the superseded v1 $7.5/\alpha^3 \approx 1.93\times10^7$ birefringence ratio (over stamp-only and re-point). Return value preserved; `DeprecationWarning` added. | **#819 — MERGED** (`fcf2c96b`, 2026-08-02) | ✅ executed |
| **D2** | *"D2: follow rec"* | `common/theorem-thesaurus.md` **stays in `common/`** (no `git mv`); the carve-out lands in `translation-tables/README-architecture.md` §3 as a deliberate OUTSIDE-SCOPE sibling. | **#821 — MERGED** (`276916d3`) | ✅ executed |
| **D3** | *"D3: rec"* | TH-3 kept as **ONE row** with **two labelled tightness tiers**; class demoted to `EXACT-same-mechanism-two-tightness-tiers`; boundary-layer clause rescoped to tier (b); the homonym lands in §6 as a **class-label** collision row. | **#821 — MERGED** | ✅ executed |
| **D4** | *"D4: follow rec"* | Discharges the one row merged `#812`'s F4 repair left flagged-not-fixed (class-(a) tag vs deferral to the `#668` relabel). | **#821 — MERGED** | ✅ executed |
| **D5** | *"wait"* | **NO ACTION.** The `WD` / $Q=\ell$ tag question is **held**, deliberately — the cold-$Q$ derivation **supersedes any tag** that would be applied now, so tagging first would bank a label the derivation is about to move. Not a deferral for capacity reasons; a sequencing decision. | — (no lane fires) | ⏸ held by ruling |
| **D6** | *"D6: recs"* | The `Q` glyph content **stays a §6 thesaurus row**; **no `def-Q` node is minted**; revisit only on a structural change to `vocabulary-register.md`. | recorded in **#821 — MERGED** (`theorem-thesaurus.md` §6 placement stamp) | ✅ executed |
| **D7** | *"D7: follow your rec"* then *"go"* | (i) Re-point the live bench arbiter to **v3** — **gated** on a clean consumer-and-letter sweep. The gate **fired negative** (letter leg dirty), Grant adjudicated, and the second word (*"go"*) authorized the five-piece plan with **shape (B)** for BLOCKER-1. | **#822** — `src/repoint-pvlas-v3`, **execution in flight** | 🔄 in flight |
| **D8** | *"correct plus note"* | Correct the item **and** carry a note (rather than correct silently or note-only). | `docs/rulings-d8-and-ell-split` — **in flight** | 🔄 in flight |
| **side** | *"go"* — the κ desk-calc | Fire the Pasteur-κ desk calculation lane. | `research/pasteur-kappa-desk-calc` — **in flight** | 🔄 in flight |
| **side** | *"adopt and split"* — the ℓ handshake | Adopt the handshake **and** split it (both, not either). | `docs/rulings-d8-and-ell-split` — **in flight** | 🔄 in flight |
| **side** | *"4, follow rec"* — the divergence-map priority block | Point, don't restate: the map's "Priority order for action" is reduced to **one pointer** at `_orchestration/2026-07-15_hardware-ratings-map.md` §2/§3; only map-row-scoped ordering stays local. Rationale: **KB leaves catalog physics; orchestration owns process ordering.** | **this PR** (`docs/mechanical-closeout-batch`) | ✅ executed |
| **side** | *"5, yes"* — this enumeration | Enumerate the batch here, with verbatim words + executing-PR pointers. | **this PR** — this block | ✅ executed |

**Receipt discipline on the verbatims above — which are re-quoted from the repo and which are first-recorded here.**
Six carry a pre-existing repo receipt and were re-read from it rather than from session context:
**D1** `_orchestration/docket-entries/2026-08-01-d1-deprecate-ratio.md`:3 · **D2/D3/D4** `_orchestration/docket-entries/2026-08-01-rulings-d2-d3-d4.md`:3 · **D6** same fragment `:12` · **D7** `_orchestration/docket-entries/2026-08-01-d7-repoint-pvlas.md`:3 and `:26` (read from `origin/src/repoint-pvlas-v3`; that branch owns the file and **it was not edited**).
Five are **chat-only and are first-recorded here** — **D5**, **D8**, the two side rulings (**κ desk-calc**, **ℓ handshake**), and the two this PR executes — which is precisely the tracker gap this block closes. They carry no independent receipt yet; the lanes that execute them own landing one.

> **⚑ Merge hazard, restated because this block is a fresh mid-file insert into the file that has it:** this doc
> falls under the blanket `*.md merge=ours` (see this file's own 2026-07-26 preamble note at `:28` and `_orchestration/docket-entries/README.md`:19),
> so a concurrent lane's edit to §1 can be **silently dropped** rather than surfacing as a conflict. If a
> D5/D8/κ/ℓ lane lands its own status flip here while this block is open, re-read the file before assuming
> either edit survived.

---

## §2 — Frontier queue (item-3 standing authorization: derivation-class fires with judgment; build-class parked)

| # | Lane | Status |
|---|------|--------|
| 1 | **Envelope-sector reduction** (`research/envelope-sector-reduction`) — slow-modulation dynamics of the saturable lattice; the gravitational-band coupling κ vs the frozen pulsar kill-line; consistency gate = observed shear GW must survive | **FIRED + DELIVERED + MERGED 2026-07-20 (#767 @ `5287ef32`)** (Grant: "fire the lane" `[sic]`); max-stakes wrapper review landed → verdict **BIN-1-CONDITIONAL** (ENVELOPE-RADIATES on the shipped analysis; constituent-cage-ensemble fork OPEN, owed driver §6.2-1). Result: `research/2026-07-20_envelope-sector-reduction_result.md`. |
| 1b | **u/A transverse identity-collapse candidate** (trigger-7 class: are the EM and mechanical-transverse sectors two meters on one bench object?) — ★frozen observational gate: GW170817 two-distinct-signals, a **speed/arrival kill** (bulk radiates at √(10/3)·c ≈ 1.83c vs |Δv|/c ≲ 10⁻¹⁵ coincidence bound; #766 review F7 arithmetic) — COMPLEMENTARY to, not duplicating, the banked pulsar **energy-budget** kill | candidate, sequenced AFTER the envelope lane (its Leg B channel-ownership output is a direct input); does NOT displace ringdown's queued-next rec |
| 2 | **Ringdown-systematics candidate** (mode-ratio locking + ω ∝ √C_44 critical-slowing as beyond-Kerr-deviation organizers; dimensionless; existing LIGO catalog) | **FIRED + DELIVERED 2026-07-20** (Grant `[sic]`: "word"; branch `research/ringdown-systematics`, frozen prereg `f22d0b2d` pushed ALONE first; both organizers DERIVED-AND-TESTABLE + CONSISTENT-UNTESTED, no bankable negative; docket ENTRY 2026-07-20-ringdown-systematics). PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]` |
| 3 | J(ω) coupling fork (needs engine constitutive tensor; shares machinery with the gravitational-arc lanes) | queued |
| 4 | D1 compact-object rider · N=3 · m_p/m_e | queued |
| 5 | **Q-law derivation** (why the standing-v1 mΩ τ lands at −5.4%: the `r_Ω = r_ph·√(1+ν_vac)` / `ν_vac` factor / mapping — the named next ringdown work banked by Ruling B1) | **queued (derivation candidate, Ruling B1 2026-07-21)** — resolves the OPEN τ near-miss tension; NOT a rescue-debug (Rule 11: the −5.4% is a clean banked tension, the derivation would explain it or bank it deeper) |
| — | **★ WALK-CLASS PARKED** (Grant [sic]: *"lets not lose sight of the later walks"*): the hoop-stiff / radially-soft **vessel-state shell** → `γ_c` couple-stress → (2,3) winding thread — the **wall-asymmetry carrier candidate** | **PARKED** — walk-first with Grant (walk-the-picture-first) BEFORE any lane; not a fired derivation |
| — | BUILD-class (quantum-seeded floor instrument, arrow instruments, CVR bench) | **PARKED** pending explicit word |

**Routed question (#766 review F4) — ✅ RULED 2026-07-20 → option (a)** (Grant verbatim [sic]: *"oh a for sure lets revisit that scope"*). The manuscript-reconciliation program's scope now **INCLUDES re-examining ch08 item-3's shear-only rewording** ("…as transverse shear", the Part-1 fix) under Reading-A — the original radial/bulk prose is arguably closer to the live admixture picture; the Part-1 identification of the **OBSERVED** decay channel as shear **survives untouched** (Reading-independent). Routed to the manuscript-reconciliation program.

*(Original routed question, preserved per Rule 12 KEEP-BOTH — no rewrite:)* **Routed question (#766 review F4, one-line Grant ruling owed):** under Reading-A the ch08 item-3 *rewording* ("…as transverse shear", the Part-1 fix) is itself now arguably incomplete — the original radial/bulk prose is closer to Reading-A's admixture picture. Scope the reconciliation program to revisit the item-3 rewording, or hold item-3 frozen and flag only the chapter-level double-count? (The #766 warningbox is honest either way; both options preserve the Part-1 step-3=shear ruling for the *observed* decay channel, which is Reading-independent.)

## §3 — Open PRs + running lanes (snapshot; the board is re-derived live at each session, this is orientation only)

- **#761** MERGED 2026-07-20 (main @ `caa51c17`) — ruling §1.2 (the revert word) **EXECUTED 2026-07-20** via `docs/q1-revert-execution` (this PR; all four arms landed — Q1 row flipped, ledger promoted LIVE, ch08 double-count flagged, receipts stamped).
- **#764** (skills batch + hub-and-spoke architecture) — review CLEAR-WITH-REPAIRS; repair pass in flight (merge-forward, #762-merged currency flips, AVE-Skills wording soften).
- **Envelope lane** — DELIVERED + MERGED 2026-07-20 (#767 @ `5287ef32`); verdict BIN-1-CONDITIONAL (§2 item-1); constituent-cage-ensemble driver owed (§6.2-1).
- Walk record: `research/2026-07-20_envelope-boundary-walk_RECORD.md` (this PR).

## §4 — Process notes new this window (already repo-tracked via #764; listed for orientation)

- Skills minted: `ave-reproduction-gate`, `ave-mechanism-claims-discipline`, `ave-board-assembly-check`, `ave-cross-discipline-mapping` (+ `verify-before-cite` v1.6). Home: AVE-Skills repo (see §1.3 push call).
- Five review moves baked into `ave-adversarial-pr-review.js` standing discipline (additive-only, verified by read-and-run).
- Candidate 5th instance for the mechanism-claims history (routed to the AVE-Skills doc, not edited here): the orchestrator's mid-review provisional relay on #761 ("UNDETERMINED-on-the-fork, crisis doesn't fire") reversed by the verify pass — hedged as provisional at the time, still a relay of pre-verify lens findings; corrected to Grant explicitly.
