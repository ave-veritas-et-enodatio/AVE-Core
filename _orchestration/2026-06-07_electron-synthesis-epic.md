# Electron-structure synthesis — workstream epic (2026-06-07)

Stages the follow-ups from the 2026-06-07 deep co-thinking session so the big ones survive compaction. The session built a unified electron picture; this tracks deliverables, the emergence-test candidates, the α-closure thread, and the demo/viz candidates.

## §0 The synthesis (one paragraph)
Electron = a frictionless B-rotor loop orbiting ONE host K4 node (host = the loop's axis), body interstitial (Meissner-expelled, can't touch nodes — maglev), tensioned by its chiral neighbors. **Strain** (not E/B) is fundamental; `E↔node-shunt-C`, `B↔bond-series-L`. Mass `=½LI²` (rotor reactive energy `=ℏω_C`). Spin-½ = chiral neighbor tension imparting a ½-twist (holonomy π) per orbit (the double-cover). `(2,3)` = the real-space **trefoil projection** of the twisted orbit (the phase-space winding's shadow). Charge = the E terminating on the host node. Confinement = the self-dug Meissner cage. Coherence (qubit) = the frictionless rotor; decoherence = the cage leak (**candidate: α**).

## §1 Running deliverables (3 background docs)
- **rotor-synthesis** (`a7f46b8c`) — structure + rotor=zitter & spin-stabilizes proofs. **On land: fold in the fine-structure-tidal forward-prediction (§7).**
- **coherence-Reynolds** (`a0b1b8a8`) — qubit/Cooper/classical `Re_q` map + CFD/Vacuum-Fluid-Dynamics. **On land: reframe §5 — VFD = Vacuum Fluid Dynamics; variable-freq-drive demoted to motor sub-lens.**
- **chiral-holonomy diagnostic** (`a7467a52`) — the ½-twist test (is holonomy π?) + viz #1/#6.
Each → fold-ins → **reviewed PR** (no self-merge, no leaf without Grant).

## §2 Emergence-test candidates (the "does it have teeth?" list)
The synthesis is mostly consistency-class re-description. The genuine emergence candidates (testable *numbers*, not re-descriptions):
1. **Holonomy = π — VERDICT (II)** (`a7467a52`, DONE; PR pending) — chirality is **NECESSARY** (0/400 achiral vs 127/400 chiral; π exact `Z₂`, step-robust) but **PATH-DEPENDENT** (encircling-conditional Berry phase, not a topological spin-½-for-every-orbit). CONSISTENCY-class, not the strong emergence. Real-space path ≠ `(2,3)` (winding ~2,1.3) — non-contradicting, the canonical `(2,3)` is **phase-space** not real-space. Fills the corpus gap `k4-rotation-group.md:119-123` + `finkelstein-misner §8` (open discrete-lattice spin-½). **Does NOT touch the canonical FM/gyroscopic spin-½** (real-space rotation of the extended unknot — different mechanism, untouched).
   - **2nd-shell screw test DONE → VERDICT (II)** (`a8501311`, PR #111): the TRUE reflection-odd `I4₁32` 4₁ screw gives the SAME path-dependent class — `f≈18% (72/400)`, π exact when present but **nonzero ONLY in a commensurate-pitch window `κ≈[½π,7⁄12π]`, 0% else → a RESONANCE, not a sign-of-κ Z₂ plateau.** So orbital-encircling = **PROJECTED HELICITY** (path/pitch-dependent Berry phase), **NOT intrinsic spin-½.**
   - **THE INTRINSIC spin-½ is a DIFFERENT mechanism — Finkelstein–Misner.** A screw axis is *translational* (frame ∝ displacement along it); intrinsic spin-½ comes from **rotating the extended `0₁` unknot RELATIVE TO the crystal (a disclination — the unknot SPINS ON ITS AXIS, not orbits a host)** — `finkelstein-misner-spin-half-derivation.md §8`, explicitly-open. **NEXT decisive test:** the FM extended-defect self-rotation, NOT orbital encircling. (Grant's loop-orbiting-host picture = the *projected* chirality, exactly as the intrinsic-vs-projected split predicted; intrinsic spin = the self-spin.)
   - **FLAG A (load-bearing geometry — Grant/auditor):** bare engine `k4_tlm.py` positions are **degree-4 ACHIRAL diamond-class** (0/400 controls confirm); `I4₁32` chirality is a Cosserat decoration. But the **canonical chiral "K4 crystal" is degree-3 srs / (10,3)-a Laves** — a real net-geometry tension. *Is the engine on the right lattice?* Resolve before the FM run.
   - **FLAG B (verify-before-cite):** PR #110's headline `127/400` is **NOT in its committed driver** (JSON has only `n_minus_I_total=33`); the screw run reconstructed an equivalent battery. Audit item.
   - **1b. g = 2 — STILL BLOCKED:** needs the FM extended-defect run (projected helicity supplies no intrinsic spin). NOT the orbital. [dipole+double-cover fix to coherence-doc §5 still stands.]
   - **FLAG 1 (handedness):** NOT resolved here — the screw sign selects a *projected-helicity* sign, not intrinsic e⁻ chirality (that's the FM unknot's ±½ twist orientation).
2. **Fine structure from tidal spin-orbit** (queued, rotor-doc §7) — rotor-resolved atom generates `L·S` splitting with no term inserted.
3. **`Re_q` → decoherence rate** (coherence-doc §5) — does the quantum Reynolds number predict a coherence-time number?
4. **α-closure** (§3) — the big one.
5. **Varactor knee = V_yield?** (QUEUED, Grant's panel-B insight; gated on the screw verdict — builds on its geometry) — panel B of the holonomy viz is a varactor `C-V` knee in *orientation* space; the orientation-degeneracy where the holonomy **flips (= the chirality/spin-½ being stamped)** may coincide with the **strain yield `A=A_yield`** (the dielectric `V_yield`). **TEST:** evaluate the local strain `A` at the degeneracy locus (the tilt/radius where the holonomy snaps) — does it sit at `A_yield=√α` (V_yield) or `A=1` (V_SNAP)? **If YES →** spin-½, chirality, AND `α` (via `V_yield=√α·V_SNAP`) are CO-SET at one yield threshold — chirality is stamped at the pair-production yield and `α` sits right there (ties straight to §3 α-closure); and it reframes the (II) path-dependence as **yield-dependence** (spin-½ set only when the orbit crosses the yield, not every path — not a bug). **If NO →** the varactor resemblance is structural, not the same threshold. Extends the holonomy/screw driver with a strain readout at the degeneracy. **This is the cleanest bridge yet from the spin test to α.**

## §3 α-closure thread (BIG follow-up — STAGED, not dispatched)
**Q (Grant): does the rotor-cage-leak picture close or justify α?**
- **Justifies — yes:** α = electron rotor's intrinsic **leak rate into its Meissner cage** = `1/Q` = London-depth coupling = decoherence rate. Grounded in `Q=1/α` (theorem-3-1-q-factor).
- **Closes — candidate path, NOT solved:** α is currently a CALIBRATION INPUT; the corpus `p_c=8πα` chain is **CIRCULAR** (`p_c` defined via α). New path: derive `Q=1/α=137` from the **chiral-cage geometry** (holonomy + London depth + packing), *independent of α*. If geometry → 137, α closed non-circularly (first time).
- **First steps (when adjudicated):** (a) **VERIFY** whether `Q=1/α` is DERIVED (geometric, α-independent) or ASSERTED (`Q≡1/α` definition → circular) in theorem-3-1 — gates viability; (b) extend the holonomy diagnostic to compute the cage **leak** (London-depth coupling / the Q) from geometry — does it give ~137?
- **Honesty (`ave-evidence-framing`):** deriving 137 has eluded everyone. The picture makes the closure WELL-POSED (compute the cage leak), it does NOT solve it. High-value, high-risk. Do NOT overclaim in any doc.

## §4 Demo + visualization candidates
- Viz candidates (9, ADJ/POST): [`2026-06-07_visualization-candidates.md`](2026-06-07_visualization-candidates.md).
- **Physical lab demo (Grant):** RGB LED strip on a loop, color-cycling as it rotates — demonstrates the rotor + the **720° double-cover** (color returns only after *two* rotations = the spinor) + the trefoil path. Save for a lab demo. Added to viz file as #10.

## §5 Open forks + next
- Genesis forks A–D (genesis-scope §9) still open.
- New physics forks: does neighbor coordination **select the ground-state knot** (trefoil=electron, higher-crossing=heavier)? Is the holonomy **topologically** π? Does the cage leak **= α**?
- **Next:** land the 3 docs → fold-ins → PRs. **Watch the holonomy verdict first** (cleanest emergence test). Then adjudicate whether α-closure earns a dedicated derivation push (gated on the `Q=1/α` derived-vs-asserted check).

## §6 Bulk→electron projection thread (how cosmic strain sets one electron's spin/precession)
Synthesis on canonical pieces (NOT a single leaf): how the bulk strain of ALL nodes projects to a SINGLE electron's phase-space precession through its neighbor nodes.
- **Operating-point bias → precession clock** (Op14): bulk → smooth `A₀(r)` → local `ω_local=ω_C·√S(A₀)` + `Z_eff=Z₀/√S` sets the precession RATE.
- **1/7 trace-reversal projection** (`K=2G`, `ν=2/7`, `gravity/index.md:13`): the 3D bulk strain projects onto the electron's 1D loop scaled by **1/7**. `K=2G` IS the bulk↔local projection operator (Grant's "factor of 2").
- **Gradient torque → precession**: `∇A₀` (neighbors at differing operating points) torques the `(V_inc,V_ref)` axis → the trefoil-axis sweep; rate ∝ projected gradient (the GR-precession mechanism, cosmic-strain-sourced).
- **Ω_freeze → the local screw** (`water-anomaly-lc-partition.md:44`): cosmic K4 crystallization under cosmic angular momentum froze the `I4₁32` screw = the local 2nd-shell chirality. **THE BULK SOURCES THE LOCAL CHIRALITY THAT MAKES SPIN-½.**
- **Machian anchor** (`claim-quality:14`): `ξ=4π(R_H/ℓ_node)α⁻²` embeds the cosmic horizon → the precession's scale/inertia.
- **Neighbors = messengers:** the electron sees its 3–4 neighbors carrying the local sample of `{A₀, ∇A₀, the screw}`; the tension/holonomy path IS that encoding.
- **Host-node balance:** the host = the **capacitive (C) half** of the electron's LC tank (anchors the E/charge) + the **inner Meissner wall** (expels the B/loop). Balance = reactive equipartition `½CV²=½LI²` at `ω_C` + the Meissner cavity (loop floats between host-inner + neighbors-outer).
- **Intrinsic vs projected chirality (the screw-test discriminator):** spin CARRIES intrinsic chirality (rest, frame-independent, `Ω_freeze`-sourced); forward momentum PROJECTS it (helicity `S·p̂`, path-dependent). Mass = the rotor's ability to spin without translating. So **path-independent π = intrinsic spin-½ (I); path-dependent π = projected helicity only (II).**
- **One-line:** precession = the **SLIP between the electron's rotor (d-q) frame and the bulk strain frame** (Machian + FOC + trace-reversal, one statement). Co-rotate with the bulk → no precession.
- **PAYOFF:** the 2nd-shell screw test (running, `a8501311`) tests whether the cosmically-sourced (`Ω_freeze`) handedness makes the spin **intrinsic**. The bulk→electron thread and the spin-½ test are the SAME physics — *watch the bulk hand the electron its spin.*

## §7 External research deep-dives — fluid-dynamics enrichment (QUEUED 2026-06-07, Grant-supplied; not urgent)
The Camassa–McLaughlin (UNC) **entrainment / stratification / vortex-trapping** line. **Why it's relevant:** a **vortex ring trapped in a stratified fluid by entrainment** is the fluid-mechanical twin of the **electron loop trapped in the lattice strain gradient** — i.e. the genesis confinement question. The papers may carry: the quantitative **trap-vs-escape criterion** (= confinement vs the MODE-III "escape"/dissolution), the **entrainment → buoyancy-change** law (= the dark-wake / entrained-mass / inertia — "can't be entrained without motion"), and the **stratification = bulk-strain-gradient** mapping (epic §6).
- **[PRIORITY · fetchable] arXiv 1110.3435** — Camassa, McLaughlin et al., *"Trapping and escape of buoyant **vortex rings** in sharply stratified fluids."* THE direct analog (vortex ring = electron loop; stratification = strain gradient; trap/escape = genesis/MODE-III). **Deep-dive this first.**
- Zhi Lin et al., *"Subsurface Trapping of Oil Plumes in Stratification"* (researchgate — likely gated) — entrainment-trapping of plumes at a neutral level ↔ soliton trapping at the impedance-matched/yield level.
- PhysRevFluids 1, 050503 (2016) — the **"Liquid Chandeliers by Entrainment"** (Grant's earlier thread) — entrainment self-assembly.
- IOP Comput. Sci. Discov. 6:014001 (2013) — the computational method for these stratified-flow/vortex problems (possible numerical-method import for an AVE soliton-in-gradient sim).
- **Deep-dive scope:** extract {trap-vs-escape criterion · entrainment-buoyancy law · stratification parameterization}; map to {genesis confinement (Forks A–D) · dark-wake inertia · bulk-strain-gradient (§6)}; flag reusable math/method. **Caveat:** non-arXiv PDFs likely gated → arXiv 1110.3435 is the feasible start; others may need Grant's PDFs or abstract-level engagement. **Connects to:** coherence-Reynolds doc (CFD/VFD lens) + the genesis confinement mechanism.
- **RESULT (`a3f60de4` DONE → PR #112):** No closed dimensionless criterion in the readable sources (arXiv 1110.3435 is a 1-page Gallery-of-Fluid-Motion **video abstract**; PRF + Zhi-Lin **gated** — honest access ledger in-doc). Two real findings:
  1. **Fork A ↔ Fork D LINKED.** The genesis's double-sided failure (1× dissolves / 4× **parametric-pumps `10⁴–10⁷×`**) has NO fluid counterpart **because the fluid trap is DISSIPATIVE** (viscous mixing settles the over-amplitude to neutral buoyancy) **while the modeled genesis ramp is LOSSLESS/reactive.** → the 4× pump is a **MISSING EQUILIBRATION CHANNEL**, and **C3 (Fork D) IS that channel** → predicts the already-queued **C3-gate test converts the 4× pump into a bounded trap.** Fork A (amplitude-gating) and Fork D (C3) are the same physics.
  2. **THE PLUMBER-PHYSICAL QUESTION (Grant):** is genesis a **CAST** (irreversible real-power settle that bleeds entropy to lock the over-amplitude state — like viscous mixing / the cosmological crystallization latent-heat) or a **TUNE** (lossless reactive C3 winding-closure)? The 4× pump smells like a missing damping path.
  - **Inertia bifurcation:** reactive **added-mass** (`m_add↔L_eff`) → inertia; **viscous** entrainment → confinement (clean split; entrainment is dissipative, wrong polarity for reactive inertia).
  - **Falsifiable-number path:** derive **`A²_crit`** (where `τ_acc=τ_leak` AND `τ_eq=τ_pump`) from `A²_μ=1` + α-leak + Duffing-pump-rate → either a testable bounded-`(2,3)` amplitude OR (no `A²_crit` at lattice scale) a substrate-native **falsification of lattice-scale genesis** (forces Fork C, sub-`ℓ_node`). Lens-only until derived. **The α appears again** (the leak rate `1/(α ω_C)` is the `τ_leak` in the balance — ties to §3).
  - **★ CONVERGENCE (Grant 2026-06-07) — the missing equilibration channel IS the dark-wake/back-EMF, OBSERVED-NOT-FED-BACK.** `DarkWakeObserver` (`vacuum_engine.py:1457`) measures the lattice's mutual-inductance back-EMF (`M_inertial≡L_drag`, `:1478`) but is **read-only — `τ_zx`/back-EMF is absent from the EOM** (`cosserat_field_3d`/`k4_cosserat_coupling` grep empty). So the genesis runs with the soliton's **back-reaction switched off** → the over-amplitude is never opposed → the 4× pump. **FORK-A FIX CANDIDATE (gated on the lattice audit `a4472bc5`): feed the dark-wake back-EMF into the EOM and re-run genesis.** Pump bounds → CAST confirmed, Fork A solved by back-reaction, Fork D's C3 = same mechanism. Pump only slows → need a separate dissipative channel. **The dark-wake's nature (radiated wake = dissipative/CAST vs bound added-mass = reactive/TUNE) ANSWERS the CAST-vs-TUNE question by experiment.** Most concrete Fork-A fix to date — converges the deep-dive + the engine state + Grant's question.
  - **★ FISSION (Grant re-read of 1110.3435, 2026-06-07) — the THIRD outcome + the pump's counterpart.** The deep-dive's "fluid has only one failure mode" was **WRONG**: the abstract names *"fissioning into a cascade of smaller vortices."* Genesis has THREE outcomes: **trap → one electron · escape → MODE-III · FISSION → a cascade of smaller solitons (a shower).** The 4× pump is the **lossless artifact** of an engine with no fragmentation channel; restore it and the over-amplitude **fissions** (= the missing equilibration, dispersing the excess) instead of pumping. Physical: **one soliton → many = decay / pair-or-multi-production.** Trap-vs-fission line = **make-ONE-electron vs make-a-SHOWER = laminar (low `Re_q`) vs turbulent-cascade (high `Re_q`)** — ties the entrainment + coherence-Reynolds threads. Folded into PR #112 (§2.3/§2.2/§1.1).

## §8 ★★ LATTICE AUDIT (`a4472bc5` DONE) — FLAG A confirmed + a BIGGER unflagged corpus contradiction
**The engine builds a degree-4 ACHIRAL diamond-cubic (`Fd-3m`) net** — BOTH engines (`k4_tlm.py:10,105,109-115` "bipartite Diamond lattice, exactly 4 neighbors"; `cosserat_field_3d.py:129-145` same 4 diamond vectors, "consistent on the diamond lattice"). The canonical *name* ("chiral Laves K4 / SRS Net / `I4₁32`") is the **degree-3 chiral srs net** — topologically DIFFERENT. Chirality exists ONLY as **Cosserat/field decorations** (κ_chiral keyed to the soliton's (p,q), zero under linear drive; `k_χ` is in a *verify script*, **0 occurrences in `src/ave/`**), **never in node positions.** FLAG A confirmed on every clause.
- **THE CORPUS IS INTERNALLY CONTRADICTORY (previously unflagged):** Axiom 1 (`eq_axiom_1.tex:20`) says BOTH "`I4₁32` chiral Laves K4" (degree-3) AND "**4-fold** connectivity" (degree-4) **in one sentence**. A DOZEN load-bearing leaves derive on **z=3** (neutrino δ_CP=π/3 "1/3 per bond *because* 3-connected"; trefoil c=3 ↔ connectivity-3; macroscopic moduli z=3). The engine is **z=4**. **Coordination is a topological invariant — you CANNOT average 3↔4.** The 2026-05-17 "false positive" resolution (`closure-roadmap:191`) fixed only the space-group *label*, never the coordination contradiction (and its "engine confirms via k_χ" is imprecise — k_χ is a verify-script constant, geometry is diamond for all k_χ).
- **THE SPIN (II) TESTS PROBED THE WRONG SECTOR.** The holonomy ran on **node positions** (achiral diamond → *correctly* returns achiral / path-dependent). Per Axiom 1, spin/chirality lives in the **Cosserat ω microrotation sector** — so the meaningful spin-½ probe is the **ω-field helicity** (`_beltrami_helicity`), NOT node coordinates. **The (II) results are consistent with the achiral engine; intrinsic spin-½ REOPENS in the Cosserat sector** (re-scopes the FM test to the ω-helicity, not node-position rotation).
- **IMPACT (tiered):** **AT-RISK** (need discrete z=3 / node-position chirality — NOT realized): neutrino δ_CP=π/3 (→ π/4 at z=4), trefoil-c=3↔connectivity identity, baryon asymmetry from lattice handedness, node-position spin. **ROBUST:** α (load-bearer is λ_G=4/21, **chirality-independent for all k_χ** — a diamond-base property; "chiral srs z=3" language is decorative), the (2,3) (phase-space + Cosserat κ_chiral), the genesis A-B node-pair (diamond sublattices + achiral saturation).
- **★ TWO FOUNDATIONAL QUESTIONS FOR GRANT (audit surfaced, did NOT pre-pick):**
  1. **Is the vacuum net 3-wire (WYE/three-phase junction, degree-3 srs) or 4-wire (tetrahedral, degree-4 diamond)?** Neutrino/CP math wires it 3; both engines wire it 4; can't average 3↔4 — one set must move.
  2. **Is chirality a property of the COLD empty lattice or only of an EXCITED soliton?** Engine: excited-soliton (κ_chiral keyed to (p,q), zero under linear drive — matches "spin IS the rotational DOF when excited"); the name: cold-lattice handedness.
- **Walk-backs pending Grant's net call** (auditor surfaces, implementor lands): 🔴 on the losing side (z=3 leaves OR engine+Axiom-1-"4-fold"); `closure-roadmap:191` correction; holonomy-test re-scope to the Cosserat ω-sector.

### §8.1 AUDIT-OF-AUDIT (`aea3cc7d` DONE) — geometry CONFIRMED, but the headline + the spin-½→3-WIRE argument were OVER-STATED; the net effectively decides **z=4**
All 5 geometric claims re-grep-CONFIRMED (engine=z=4 diamond both engines; `Fd-3m` centrosymmetric; the z=3/z=4 split real; chirality a Cosserat decoration, `k_χ` absent from `src/ave/`; α robust). Two scope-corrections (NOT reversals):
- **The contradiction splits two ways.** (A) **Chirality axis** (`I4₁32` vs `Fd-3m`) is **ALREADY ADJUDICATED** — `closure-roadmap:191` Foundation Item 10 (2026-05-17): `Fd-3m` is the *supergroup* of `I4₁32`; `k_χ=0→Fd-3m`, `k_χ>0→I4₁32` = **diamond + chiral order-parameter = the engine + the decoration.** SUPPORTS the audit; should've been cited, not re-flagged. (B) **Coordination axis** (z=3 vs z=4) is the genuinely-open one (symmetry-lowering can't change coordination; srs is a *different* net, not diamond's chiral child).
- **"Engine on the WRONG net" is OVER-STATED — and so was my "spin-½ → 3-WIRE."** The COMPUTATIONAL weight is **entirely z=4** (the λ_G=4/21→α chain, the foreword Lorentz, the trampoline docs — all on z=4; **no z=3 computation exists**). The z=3 leaves are **unbacked numerology**. Burden inverted: the z=3 neutrino/molecular leaves diverge from the substrate, NOT the engine. **Substrate effectively decided z=4.**
- **★ The spin-½ "3" is the 3 COSSERAT SECTORS, not the lattice bonds.** SU(2)'s 3 generators = the 3 microrotational DOF (`ω_x,ω_y,ω_z`) per node — **intrinsic to every 6-DOF node, present on z=4 diamond.** So spin-½ does NOT force degree-3. Grant's 3-phase intuition is RIGHT for the **spin** (the 3 Cosserat sectors = the rotating microrotation field) but it's the **node's internal spin**, decoupled from the lattice coordination (4 bonds). 3-fold (spin) and 4-fold (lattice) are both real, *different* structures. The z=3 neutrino leaves' error: attributing the spin-3 to the lattice BONDS ("1/3 per bond").
- **NARROW OPEN WORK:** re-ground the neutrino "3" on the **3 Cosserat sectors / trefoil crossings** (both survive z=4). δ_CP survives z=4 anyway (1.27π vs 1.36π, within ±0.2); the sharper exposure is `Δc_crit=3→4` (θ₁₃ screening flip), which may also re-ground on the 3 sectors. **Re-grounds → keep z=4, walk back the bond-wording. Doesn't → walk back the neutrino leaves.** Rebuilding the engine on z=3 srs would INVALIDATE the α + Lorentz chains — NOT recommended.
- **NET: adopt z=4 (the engine is the grounded choice).** Open work = the neutrino-3 per-bond→per-sector substitution check.

## §9 Late-session insights (Grant, 2026-06-07) — parametric-oscillator reframe + trefoil-4π spin

### §9.1 ★★ The genesis forks are 1-D cuts of an un-swept (A², ω) response surface — and "amplitude" IS strain
- **Amplitude = strain, VERIFIED canonical.** `cosserat_field_3d.py:332,383` literally computes `A² = |ε|²/ε_yield² + |κ|²/ω_yield²` (translational E-strain + microrotational ω-strain, normed to yield); `eq_axiom_4.tex:10`: "A is the local strain normalized to A_yield." So "rest-energy pair (A²≈0.23) vs short-its-own-bond (A²→1)" is **ONE strain axis, not two ontologies.** Fork C (scale) is mostly the same axis side-on (strain = field×ℓ_node → smaller structure concentrates strain higher).
- **The actual hole — never swept `Z(ω,A²)` / `Γ(ω,A²)`.** The forks are argued from the STATIC kernel `S(A)=√(1−A²)` (DC) + two stray operating points (`A²≈0.23→Γ≈−0.03`; `A²→1→Γ_min≈−0.994` but pumps `10⁴–10⁷×`). The 4× pump is a **PARAMETRIC instability** (modulating a saturating varactor reactance — even-in-ω coupling = a parametric pump); parametric gain is **2-D (A², ω)**. The "[1×,4×] no-stable-window" sweep was a **1-D amplitude cut at fixed drive — exactly the cut that can't see a stable window if one exists.**
- **GREP CORRECTION — "nothing exists" is OVER-STATED.** The parametric-coupling kernel is **already derived** (`research/2026-05-17_parametric-coupling-kernel-derivation-steps-{1-3,4-9}.md`, Dynamical-Casimir-analog operator chain); an impedance-spectroscopy driver exists and **already references strain/A²** (`r10_v8_foundation_audit_t3_impedance_spectroscopy.py`); node-resonance + coupled-eigenmode drivers exist. **Narrow gap: never assembled into a strain-swept `Z(ω,A²)` surface + connected to the genesis forks.** Cheaper than build-from-scratch — assemble + strain-sweep + apply.
- **★ GRANT'S PHYSICAL INSIGHT — THE ELECTRON IS A PARAMETRIC OSCILLATOR AT THRESHOLD.** Saturating reactance (`C_eff=C₀/S(A)`) = the parametric **GAIN**; the dark-wake back-reaction (§7 convergence) = the **LOSS**; at **threshold (gain=loss)** it self-oscillates as a stable clock = the Compton clock `ω_C` / zitterbewegung. **Fork A's "stable window" = the threshold locus in (A², ω):** above → pumps (4× runaway), below → dissolves; the electron sits *exactly* at threshold (marginal stability = coherent self-oscillation). **α-CLOSURE BONUS:** threshold ⇒ gain = loss = `1/Q`; if `Q=1/α`, the threshold parametric gain **= α** → characterizing the threshold **DERIVES α** (`Q(A²)` at the operating point = `1/α`). The `Z(ω,A²)` sweep serves BOTH the genesis (Fork A = threshold) AND α (= threshold Q). α stops being a stray unused constant — it's the threshold Q of the parametric oscillator.
- **Two caveats (mine):** (a) the sweep MUST include the dark-wake **loss** — a lossless parametric oscillator has unbounded gain / no stable region, so the bare engine's tongue is artificially unbounded → **dark-wake feedback + strain-sweep are the SAME fix.** (b) The sweep dissolves the **amplitude/resonance** fork (A) but the `(2,3)` **TOPOLOGICAL closure** (C3, phase-space, not real-space resonance) is a SEPARATE axis — **necessary, not sufficient.** Fork C keeps its "can the grid even represent a sub-ℓ_node structure" remainder (substrate-native).
- **NEXT DRIVER (the EE-correct step that should've preceded the genesis attempts):** swept `Γ(ω, A²)` around `ω_C` **with the dark-wake back-reaction fed in** — map the parametric tongue, find the threshold locus (Fork A), read `Q(A²)→α`.

### §9.2 Trefoil painted around the tetrahedral node, closing in 4π (the 3-vs-4 reconciliation)
- A torus around the 4-vortex (tetrahedral/K4) node, hole aligned to a 3-fold vertex axis: wind **3× around the hole** (the node's 3-fold axis — a tetrahedron carries threefold axes through all 4 vertices) × **2× around the tube** (spinor double-cover) = the `(2,3)` trefoil, closing only after **4π**.
- **Group theory (the corpus's own K4→A4→2T chain):** A4 (tetrahedral, order 12) → 2T (binary tetrahedral, order 24) ⊂ SU(2); the 120° C3 has `U³=−1, U⁶=+1` → **order 6** → closes after `6×120°=720°=4π`. The trefoil **can't close in 2π** (three thirds = −1, the spinor flip) — *that impossibility IS spin-½.*
- **`(2,3)` reads as (2 = spinor double-cover / 2 tube-turns, 3 = tetrahedral 3-fold axis)** — dissolves the 3-vs-4: the trefoil's 3 is the threefold axis the 4-vertex node already carries; the 4π stitches it closed. CAVEAT: canonically `(2,3)` is phase-space; this is a faithful picture of the *projection* — the teeth are in the K4→A4→2T closure.
- **VIZ candidate:** trefoil on the tetrahedral node, **2 full turns**, spinor-phase color-cycle (RGB-LED demo) returning to start only after lap 2.

## §10 ★ RE-SEQUENCE — cosmology BEFORE the electron (Grant, 2026-06-07)
**The cosmic operating point is the GAUGE for the electron's local strain.** `A` is gauge-relative (CLAUDE.md INVARIANT-S2: *"only spatial GRADIENTS of `A` are physically observable, not absolute per-node values"*). The electron is a **local gradient of `A` on the cosmic background** — its "amplitude → `A→1`" only *means* something relative to where the background sits on `S(A)`. So **the cosmology (the dilution trajectory §9.1 + the cosmic-dilution scope `ac56eb95`) fixes the reference frame for the electron's `A`** — working the electron first measures a gradient with no defined zero. The genesis = the early-universe high-`A` regime re-created locally; the cosmic `A`-history IS the boundary condition.
- **PRIORITY (re-ordered):** the two `A`-characterizations are the FOUNDATIONS — (i) **swept-Γ** (`ac4b8e9a`, *local* A-response / parametric threshold), (ii) **cosmic-dilution** (`ac56eb95`, *global* A-trajectory). The electron **GENESIS (nucleation) is DOWNSTREAM of both.**
- **They combine:** swept-Γ threshold + cosmic `A`-trajectory → the **genesis EPOCH** (the cosmic background `A` at which nucleation was automatic). Work the cosmic `A`-history (A vs redshift, the genesis-knee crossing, the α-drift) → *then* the electron sits on it.
- **Gauge consequence for swept-Γ:** its absolute `A²` sweep is gauge-relative; the physical content is the *excursion above the cosmic background* — so the cosmology supplies the "zero" the swept-Γ threshold is measured from.

## §11 ★ COSMIC-DILUTION SCOPE VERDICT (`ac56eb95` DONE → PR #117) — dilution REFUTED by corpus homeostasis
The §9.1 dilution-trajectory (slide down `S(A)`) is **in tension with the corpus on its load-bearing assumption — refuted on three independent grounds:**
1. **`u₀*` is FIXED at the magic angle, MAINTAINED by homeostatic crystallization** (process dynamic, value pinned): `u₀*≈0.187` = over-bracing at `K(u₀*)=2G(u₀*)`, *"survives forever as the cosmological initial condition"* (`omega-freeze-cosmic-grain-cascade.md:34,47`).
2. **Strain is INTENSIVE, not extensive:** `u₀=(L₀−d)/d=ρΩ_freeze²r²/2K₀` is a per-bond rest-length excess set at LOCAL freeze-in (`trampoline-framework.md:103,109`); `S(A)` acts per-bond. Dilution needs `A` extensive (load÷N).
3. **Crystallization holds `∂_t ρ_n=0` (CONSTANT density)** — opposite of dilution (`lattice-genesis-hubble-tension.md:12`).
- **α can't slide with `u₀*` anyway:** `α_cold=4π³+π²+π` is golden-torus GEOMETRY (not a function of `A`); a slide would break `K=2G`, magic-angle-forbidden.
- **SILVER LININGS:** (a) **α-drift vs SYM-invariance = DIFFERENT AXES, confirmed verbatim** (SYM-invariance = gravitational-potential axis; α-drift = cosmic-temperature axis via ASYM δ_strain at FIXED `u₀*`) → **cosmic-α-variation SURVIVES, via the thermal δ_strain NOT the dilution.** (b) weaker substrate-native hook (`Ω_freeze` spin-down → later bonds freeze at lower `u₀`) — not in corpus, weaker than a global slide.
- **CASUALTIES:** "genesis-was-early because A was high" + the **chirality-dilution-lock (§9.1, the 2026-06-07 cosmology turn) are REFUTED.** Operating point was always at the magic angle → genesis-early reverts to the *thermal* explanation (early hot bath drives local `A→yield` on the FIXED background); chirality reverts to a frozen order-parameter on the maintained lattice (back toward frozen-relic).
- **GAUGE RE-SEQUENCE (§10) HOLDS but SIMPLIFIES:** cosmology IS the gauge (A gauge-relative), but the background is a **FIXED magic-angle `u₀*≈0.187`**, NOT a sliding A-history. The electron sits on a fixed reference — simpler than the dilution curve, and the swept-Γ "zero" is just `u₀*`.
- **CLASSIFICATION:** lens-only, consistency-class, NOT AVE-distinct. No promotion (δ_strain magnitude already closed-negative FT-1).
- **THE FORK (Grant, Rule-16):** crystallization front lays cells **SLACK** (dilution/slide) or **PRE-LOADED at `u₀*`** (intensive/homeostasis)? Corpus answers PRE-LOADED in 3 places; overturning it is load-bearing (ripples through Class-E / dark-energy / Hubble-tension). Grant's call.
