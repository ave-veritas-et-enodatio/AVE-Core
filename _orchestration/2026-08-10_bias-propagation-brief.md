# Lane brief — the bias propagation theorem (2026-08-10)

**Goal (prove-or-disprove level):** derive the CIRCUIT CLASS and frequency response
of the bias sector's dynamics — the hyperbolic/diffusive/dispersive completion of
clause G's elliptic abstraction — and the coupling law of fast-layer objects to the
bias. This discharges Axiom 5's named-open entry (the bias propagation debt) and
adjudicates the walk-level mechanism seeds (framing note §10, UN-AUDITED).

**SVA §0; freeze-first; frozen criteria verbatim; [DO-NOT-MERGE]; own Tier-2.
Vocabulary per R50 (bias / bound response / back-reaction; no dress, no grade-noun,
no retardation).**

## Frozen deliverables

1. **THE ENVELOPE REDUCTION:** derive the bias field's equation of motion FROM the
   fast layer (the bias = the lossless oscillator network's operating-point
   envelope; no new hardware permitted). Classify: RC-DIFFUSIVE (must exhibit where
   the ledger closes — Ax3 forbids dissipation) / LC-HYPERBOLIC (must exhibit the
   bias inertia's hardware origin) / DISPERSIVE-UNITARY (the generic lossless
   envelope). Deliver the dispersion relation / frequency response explicitly — the
   delay's frequency structure IS the deliverable (R49/R50 naming).
2. **THE COUPLING LAW:** derive the force on a trapped resonance from the bias
   gradient. The frozen check (framing §10 C9): F = −ħ(∂ω/∂ε)∇ε against g = GM/r²
   on the canon bias profile — REPRODUCES-WITH-ZERO-NEW-CONSTANTS / REPRODUCES-WITH
   -IMPORT(name it) / FAILS. This adjudicates the redshift-lensing-attraction
   unification claim: derived mechanism vs echo.
3. **THE SETTLING SIGNATURE:** whatever class lands, state the observable — the
   settling constant / dispersion signature a moving source imprints on its bound
   response — and its magnitude for the two-pulsar systems (re-retrieve comparators
   at freeze). This is the R30 bench candidate's design input (lag experiment, not
   wave receiver).

## Fences

The ratified Ax5 text travels verbatim (post-doc-lane-pass form); the fast layer is
the ONLY hardware (stencil-lens: the engine codes what it codes); regime/phase
declarations per R45's comprehensive-map doctrine; the seductive-structural flag —
if deliverable 1 lands DISPERSIVE-UNITARY (Schrödinger-class), the reach-beyond-
gravity implication is ROUTED to Grant, never asserted (frozen criteria only);
two-method receipts, named engines; Rule 11; the walk seeds are UN-AUDITED input,
not receipts.
