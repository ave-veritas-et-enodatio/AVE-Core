# Lane brief — Hawking coefficient from interface thermodynamics (2026-08-06)

**Goal (prove-or-disprove level):** derive the evaporation temperature of a confined
de-bonded droplet (a BH interior) from the substrate's own interface thermodynamics, and
test whether the Hawking coefficient — T_H = ħc³/(8πGM k_B) — is FORCED, merely
scaling-matched, or contradicted. Walk-level origin + counter-arms:
`research/2026-08-06_rotation-substance-ontology_framing-note.md` §4 (UN-AUDITED — this
lane is what audits it) and ruling record `2026-08-06-rulings-go-prebond-hawking.md` R10.

**SVA §0 header (leaf v0.2, 11 rows) MANDATORY on the prereg. Freeze-first: the prereg —
including the frozen comparison target and the verdict grammar below — commits ALONE
before any derivation lands. PR opens `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.**

## The physical picture to derive against (not assume)

A de-bonded droplet confined by the surrounding crystal, evaporating by
re-crystallization at its interface, releasing bond-formation latent heat into crystal
modes. The candidate law class is Gibbs-Thomson/Kelvin curvature-driven interface
thermodynamics (hotter-when-smaller ∝ 1/r). The scaling match to T_H ∝ 1/M is ALREADY
KNOWN and is worth nothing by itself (counter-arm C5: scaling matches are the cheapest
coincidence class). The lane's entire content is the COEFFICIENT and the FORM.

## Required derivation chain, every link substrate-native or honestly TAGGED

1. **The latent heat of a bond.** From the crystal's own energy scales (the yield
   energies, the A-008-pinned gap, the moduli) — NOT from a fitted constant. If a scale
   must be imported, TAG it engineering-choice and carry it symbolically to the end.
2. **The interface relation.** Curvature-driven evaporation for THIS interface: a
   crystal/de-bonded boundary is a saturation wall (r_sat physics — the kernel-collapse
   v2 record governs which channels are mirrored there). What carries the outbound
   energy? Declare the channel and its wall status from the ruled filter bank; do not
   hand-wave "radiation".
3. **The temperature.** Assemble T(r) and extract the coefficient against the frozen
   target. Report as: FORCED (form + coefficient) / FORM-ONLY (coefficient off by a
   stated factor) / CONTRADICTED (wrong r-dependence or sign).

## Fences

- **Regime/phase declarations mandatory** at every step (crystal phase, wall,
  de-bonded interior — three regimes meet at this interface; a claim made in the wrong
  one is an artifact, not a result).
- The rotational-penetration frontier line remains OUT OF PRINT (v2 rescope record); if
  the outbound channel in step 2 turns out to BE the rotational channel, that is the
  frontier line becoming load-bearing — STOP at that finding and route to Grant before
  proceeding (walk-before-execute on fence-reach).
- G is MIXED-provenance in canon and K=2G is GR-imported — if the derivation consumes
  either, cite the provenance records and carry the import tag into the verdict
  sentence.
- No comparison-target tuning (Rule 11): the target is frozen in the prereg; a near-miss
  is a near-miss.
- Two-method receipts, named engines, on any corpus-absence claim.
