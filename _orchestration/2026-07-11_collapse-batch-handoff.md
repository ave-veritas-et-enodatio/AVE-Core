# Handoff brief — FIRE-READY COLLAPSE BATCH satellite (2026-07-11)

**Launch:** Grant launches this himself and picks the model + effort. **Cost read:** the batch is
mostly **S/M-cost register work** (def-node mints, single-leaf annotations, KEEP-BOTH cross-links, a
stale-cite fix). A **mid-tier model at moderate effort** is likely sufficient for the janitorial bulk;
the two places that want a stronger hand are the **per-target adjudication summaries** (receipt-fidelity
prose) and the **T6 / T12 riders** (sector-ownership reasoning). Nothing here is engine or derivation work.

**Standing stack (applies to every step below):** pure-AVE-corpus · only-Grant-merges ·
`[DO-NOT-MERGE][REVIEW: pending-orchestrator]` PR title · KEEP-BOTH (add-alongside, never redefine-in-place) ·
incremental commits (≥ one per target) · self-isolate in a throwaway worktree (do not touch the main checkout) ·
verify-before-cite on every corpus cite (LaTeX-aware greps, two methods where a completeness claim is made) ·
backtick-pointer cites (`path:line`), not markdown links · trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

## MISSION

Execute the **fire-ready CLEAN subset** of the merged collapse-target registry
`research/2026-07-10_collapse-target-registry.md` (merged **#631**, receipt corrections **#637** — the
registry's receipts are trustworthy **as corrected**; **consume the post-#637 state**, which is already
folded into the file at HEAD in its "Post-review repairs (2026-07-10, orchestrator-dispatched)" table).

**The registry executes no collapse.** It is the ranked input; this batch is the "which fire" execution the
core planning session adjudicated. **Mint physics ONLY where the registry says the mint is staged** (T1 triad,
T12 def-node). Everything else is register hygiene, cross-links, and flag-don't-fix annotations.

**Golden rule for every target below:** pull the target's **full spec, receipts, and kill-test from the
registry section itself** and **verify each load-bearing receipt fires** (open the file, confirm the line,
confirm the verbatim string) **before acting on it**. The pointers in this brief are navigation aids, not a
substitute for reading the registry section. If a receipt does not fire, **HALT that target and surface**
(flag-don't-fix) — do not repair the receipt silently.

---

## TARGETS

### T1 — quantization TRIAD def-node · MINT STAGED · S
Registry `research/2026-07-10_collapse-target-registry.md` §T1. P6 homonym SPLIT: **mode-count ⊥ winding ⊥
Nyquist-sampling** — three integer eigenvalue-labels of the same bound electron, each with a distinct
invariance group (ionization kills mode-count; smooth deformation preserves winding; only `ℓ_node` change
moves the Nyquist floor). **The strongest single mint; the def-node pair is already staged by the corpus
itself** (the framing note staged the mode/winding pair "not minted here" — this target supplies the vol2
receipt AND the missed **third** Nyquist sense). Land the triad def-node in `vocabulary-register.md`,
cross-linked to `def-3638f2` (winding) + `def-kn0t01` (phase-vs-real knot); optionally the one-leaf relabel
of `de-broglie-standing-wave.md:181/223/236` (mode-count, not "winding"). Verify the three receipts fire
first (the vol2 `:223` "Winding #" column header is the smoking-gun overload).

### T2 — slew ↔ hysteresis-§1 cross-link · KEEP-BOTH · M
Registry §T2. P1 MERGE. The kernel-catalog RATING-TYPE block calls the slew family "un-catalogued/empty"
while `substrate-hysteresis-index.md §1` has already assembled the rate-keyed family on the same object, with
**no cross-link**. **Land the cross-reference rows (KEEP-BOTH), do not rewrite either register.** Two receipt
disciplines: (a) the "slew" **label** stays **A4-gated** — X35's A4 (does `L_eff(I)` key on amplitude `I` or
on the conjugate rate?) is OPEN; write the cross-link as "same object; the *label* slew is A4-contingent",
not as a settled slew catalog. (b) **the family is SIX members, not five** (per the #637 correction —
`clm-p2tp9i` op14-cross-sector-trading was the omitted sixth: relativistic inductor, dark-wake BEMF,
Newtonian-inertia-as-Lenz, Op14 local-clock, Op14 cross-sector-trading, geodynamo). Confirm the six `clm-`
ids resolve before writing the count.

### T5 — θ ↔ tan δ de-orthogonalization (NARROWED) · S
Registry §T5, **as narrowed by the #631 review**. The **fireable** claim is the phasor identity
`tan δ = cot θ` (the EM loss-tangent axis and the power-factor angle are complementary angles of one LC-tank
phasor), which kills the `temporal-saturation-regime-classifier.md:331` "all three axes are orthogonal"
over-claim on its real leg. **Do NOT write "tan δ = cot θ ties θ to δ_AVE"** — that stretches the identity onto
the `δ_AVE` **duty ratio**; keep `δ_AVE ↔ θ` as the **`:310` taxonomic bridge** ("does NOT yet derive their
numerical values from S(A)"), a taxonomic-not-derivational tie, NOT a phasor identity. Grep-confirm the `θ`-owner
leaf (`orbital-friction-paradox.md`, out of the common lane) before landing.

### T7 — "Cauchy relation" mislabel on K=2G · S · reinforces a standing negative
Registry §T7. P8/P6. The genuine classical **Cauchy relation** (central-force lattice, `c₁₂=c₄₄`) forces
`λ=μ ⇒ ν=1/4, K=5G/3` — a **different point** from AVE's `K=2G` (`λ=4μ/3 ⇒ ν=2/7`). So naming AVE's `ν=2/7`
lock "the Cauchy relation" mis-cashes the namesake AND papers over the very GR-import the echo-register already
flags. Land the 3-way "Cauchy" homonym def-node/row (Cauchy elastic solid / "Cauchy relation K=2G" mislabel /
true central-force `λ=μ ⇒ ν=1/4`) + the one-line correction on `vol1/claim-quality.md:644` (it is the GR
trace-reversal lock, already correctly named at `ch8-alpha-golden-torus.md:13`). **Values unchanged.** This
**reinforces the standing GR-import negative** (PR #261/#506/#508) — the low-seduction direction.

### T8 — four-bin taxonomy → 3rd axis on lattice-model-register · GATED · execute ungated part only
Registry §T8. P1 MERGE-into-register deflation. Bins 1/2/4 (traveling + standing + winding) are already the
`lattice-model-register.md` Axis-A columns; **only bin 3 (reactive-static / off-line) is new, and bin 3 is the
X41 K1-vs-K2 open fork (PENDING-GRANT, #627).** **Execute ONLY the ungated part:** annotate
`lattice-model-register` with the kernel-coupling axis (loads / loads-at-full / transparent / blind) for bins
1/2/4 as the third axis (KEEP-BOTH with the framing note); **leave the bin-3 row GATED and log the gate**
(cite X41 / #627). Do not pre-resolve "really four bins or three-plus-a-fork" — that is the corpus's open gate.

### T10 — Q-glyph ownership row + stale-cite fix · S · janitorial
Registry §T10. P6 SPLIT / ownership row. The glyph "Q" carries ≥4 electron-scale objects at distinct values
(loaded/radiative `Q=1/α=137.036` α-baked ECHO · intrinsic `Q→∞` · cold-cage ringdown `Q_ringdown≈30.8` ·
structural `Z_RADIATION≈29.98`) with **no central `def-Q`**. Land the ownership row. **Two cite disciplines:**
(a) use the **corrected** anti-coincidence test cite `test_graded_vacuum_network_isolation.py:141-146`
(`test_anti_coincidence_Q_is_not_Z_radiation`) — verified live at HEAD; (b) **fix the flagged stale canon cite:
`vacuum-varactor-scatter-operator.md:188` still points at the pre-migration `:119-124` range** for that same
test — this is in-scope register hygiene, so **correct it to `:141-146` as part of this target** (quote the
superseded `:119-124` string in the KEEP-BOTH commit log). Confirm the live test lines before rewriting the cite.

### T12 — u₀* def-node · MINT STAGED · S · ★WITH THE X40 HOMONYM RIDER
Registry §T12. P6 SPLIT / def-node — the **deflation** of the §b triple-convergence (Sense A = SYM magic-angle
operating point `u₀*≈0.187`, `K(u₀*)=2G(u₀*)`, back-fit; Sense B = the parity-meter frozen T-breaking bias,
PENDING the D-II bridge). `u₀*` is currently un-registered in `vocabulary-register.md`. Land the def-node
(Sense A / Sense B) so the two senses stop cross-authenticating.

**★ RIDER (Grant 2026-07-11) — the def-node ALSO carries the X40 HOMONYM FLAG.** Beyond the SYM-vs-bias split,
`u₀*` may cover **two orthogonal fossils** (an independent P6 axis — the graph-theoretic Helmholtz split, not
the SYM/ASYM fence):

> **SECTOR / REGIME / PHASE-STATE:** frozen-formation residual of the K4 / srs `I4_132` chiral vacuum — a COLD
> static register, split by **time-reversal parity**. **cut-space** (gradient-like, T-even) vs **cycle-space**
> (divergence-free circulation, T-odd); orthogonal complements, cannot mix.

- **strain-`u₀*`** — the cut-space, **T-even** projection, read by the statics windows (α / G / Lamb);
- **flux-`u₀*`** — the cycle-space, **T-odd** projection, readable ONLY by non-reciprocal observables.

**X40 returned BOTH components nonzero WITHIN the ratified matched-bath model** (trapped cycle-fraction 1/10 =
T-odd loop current; radiated 9/10 = T-even bond strain; the cut-space fate is itself model-dependent —
KEEP-BOTH — `research/2026-07-10_x40-ring-closure-transient_result.md:328-354`). Because both projections are
nonzero **only within the model** (model-conditional; the split is owed only if the finite cycle-fraction is
model-independent), **Grant DEFERRED the register split.** So the rider is: **land the homonym as a FLAG on the
def-node, NOT a split.** Cite `research/2026-07-10_rb-fossil-walk_framing.md` §3 (the u₀* HOMONYM FLAG,
P6-class) + the X40 result. **The rider stays a FLAG — do not split the two fossils, do not mint two u₀*
def-nodes.** Verify the §3 flag text and the X40 both-nonzero result fire before writing.

### T14 — "strain" register row · S
Registry §T14. P6 SPLIT. The un-guarded dimensionless collision is `δ_strain` (ε-sector α-echo residual) ↔
`ε₁₁` (A1-dilatation saturation amplitude `A`). The `δ`-glyph node `def-de17a0` guards `δ_strain` against the
loss-angle family but NOT against the amplitude-`A`/`ε₁₁` family. Land the one-line ownership row extending the
δ-glyph row to name the amplitude-`A` sibling (or a `strain`-word node → q-g22 + `def-de17a0` + M-integrand).
Medium confidence — modifiers manage most sites; the flag is the one genuinely un-guarded collision.

### T16 — "register" hygiene note · S · low value
Registry §T16. P6 clean SPLIT (a ledger-index and an impedance-content-bin are unrelated objects sharing a
word) — **the sole ranked target with no fireable kill-test** (pure-terminology; the registry flags this in
situ). Land a short register-of-registers hygiene note so "off-line register" is never parsed as a tracked
index. Low value; land it as the light hygiene item it is.

### T19 — vol5 conjugate-impedance table reconcile · S · low value
Registry §T19. P6/P1 drift reconcile. `translation-protein.md:28` and `translation-protein-solver.md:20-21`
attach the "Conjugate impedance match" label to **different** residue pairs (salt bridge vs hydrophobic core),
while the solver ground truth `solver-toolchain.md:477` uses one `Y_shunt ∝ Re(Z_iZ_j*)/d_ij²` term for both.
Reconcile the two translation tables to the solver term. **Note the kill-test caveat:** if the production
`dc_analysis()` implements two distinct terms, only the label string is the (minor) homonym — not verifiable
from this checkout, so **flag rather than assert** if you cannot see the solver internals.

---

## ★ T6 — the M-row demotion (NEWLY UNGATED — Grant ruling 2026-07-11) · S–M

**SECTOR header:** this target is a sector-ownership disambiguation — **A1 (mass / dilatation, `Z_bulk`) vs
T2 / `X_L` (spin / micro-rotation)**. It is a direct instance of the A1⊥T2 cross-wiring watch. Read the sector
before the EE word "inductance."

Registry §T6 was Grant-gated (mass→inductance sector contradiction). **Grant ruling 2026-07-11: "follow the
most robust description according to the claims."** The Grant-ratified **A1-dilatation mass ownership** governs
and is the more robust claim:

- `boundary-observables-m-q-j.md:19` (`clm-ze4clw`) — the `𝓜` (mass) row projects **EE = "inductance $L$"**
  (verified: M-row at :19, claim at :15; ME-projection = "inertia (kg)", QFT = "rest energy $mc^2$").
- Grant-ratified mass ownership (verify **all**): `master-equation.md:20` ("A1 dilatation-MASS", Rule-12,
  Grant-ratified) · `constants.py:72` (mass block, `m_e = ℏ/(ℓ_node·c)`, A1) · `dual-reactance-storage-taxonomy.md:221`
  ("🔴 `X_L` = the FLYWHEEL … NOT the rest-mass store … The **rest mass is the A1 longitudinal DILATATION**",
  Grant-ratified 2026-06-20) · `def-portmp` (vocabulary-register; the id line resolves at `:420`, cite by
  node-id — "**MASS (A1) ⊥ CHARGE (T2)** … never one phasor", explicit DOMAIN guard against wiring EM/inductive
  params onto the mechanical sector).

**Execute as:** annotate the M-row as a **TKI translation-image (a dictionary entry)** — the `L = ξ_topo⁻²·m`
kinematic image, "identity-by-translation, NOT a derivation" per `def-tk1xfm` — and state it is **explicitly
NOT a sector-ownership claim.** The physical reactance sector `X_L` = micro-rotation/spin flywheel is a
different object; the M-row's bare "inductance L" is the translation-image, not the flywheel.

**Flag-don't-fix corollary:** if the sweep finds any site treating the M-row as **load-bearing sector physics**
(i.e. concluding "rest mass IS inductance in the `X_L` sector"), **FLAG it** with both file paths + verbatim
content for Grant — **do not fix it.** The task is to annotate the dictionary entry, not to re-wire any
downstream leaf.

---

## ★ δ_strain PRECISION RIDER (under the now-RATIFIED precision house rule) · S–M

**SECTOR header:** `δ_strain` is the **ε-sector** dielectric thermal-running residual (the α-echo). This is a
**reporting-precision** reconcile, not a physics change — no value moves; the α keystone stays a Class-B
calibration identity, cited identity-not-derivation.

The computed `DELTA_STRAIN` at `constants.py:279`
(`DELTA_STRAIN = 1.0 - (1.0 / ALPHA) / ALPHA_COLD_INV`) evaluates to **2.2234 × 10⁻⁶** against the
**CODATA-2018** α pin, and **2.2228 × 10⁻⁶** against CODATA-2022. The prose string **"2.225 × 10⁻⁶"** carries
a **4th significant figure the number does not have** (wrong at the 2018 pin; the digit shifts with vintage).

**Prose sites to reconcile** (board Continuation-2 §5, `_orchestration/2026-07-10_orchestration-board.md:185-197`;
re-grep `rg -n "2\.225"` before acting — the corpus list is broader than the named subset):
- `constants.py:179` (the prose comment; the definition itself at `:279` is fine — it computes). The same file
  also carries prose "2.225" at `:263` and `:275` (note: board named `:179`; `:263`/`:275` are the extra
  in-file sites — reconcile all three prose comments, leave the `:279` computed def untouched).
- `foundational-machinery-register.md:307` ("two independent paths … agree to δ_strain=2.225×10⁻⁶" — asserts
  agreement to a precision the number lacks).
- `mathematical-closure.md:107` (status bullet) and `:165` (gap-table row).
- `omega-freeze-cosmic-grain-cascade.md:30`.
- `divergence-test-substrate-map.md:699`.
- **+ any others `rg -n "2\.225"` finds** (the board flags `docs/framing_and_presentation.md:117`;
  `vol1/ch8-alpha-golden-torus.md:207`; the canonical leaf `delta-strain-cosmic-tcc.md:90`; several Vol-9
  datasheet `.tex` sites; multiple `research/` docs). **Sweep the full grep, do not stop at the named list.**

**Per the ratified rule, pick ONE convention and apply it UNIFORMLY:** either (i) full supported digits +
vintage at each site (e.g. `2.2234 × 10⁻⁶` [CODATA 2018]), **or** (ii) demote prose to **"≈ 2.22 × 10⁻⁶"** with
`constants.py` carrying the digits. Do not mix conventions across sites. **Quote each superseded "2.225" string
in the KEEP-BOTH log** (per-site, so the audit trail carries what each said before).

---

## ★ LAND THE PRECISION HOUSE RULE AS RATIFIED · S

The precision house rule was **PROPOSED-IN-CHAT 2026-07-11** at
`_orchestration/2026-07-10_orchestration-board.md:199-209` (Continuation-2 §6) and is now **RATIFIED (Grant
2026-07-11)**. Land it as ratified via **this batch** (per the rulings-docket ruling, item 15 — the house rule
lands via the D1 batch; the board §6 site is the PROPOSED origin to cross-reference). A short methods note or
register entry stating the **five clauses**, with the ratification date, cross-ref to the board §6 PROPOSED site:

- **(a)** internal numerics = full double from `constants.py`, never retyped by hand;
- **(b)** measured values quoted at full CODATA digits + (uncertainty) + vintage
  (e.g. `137.035999084(21)` [CODATA 2018]);
- **(c)** exact expressions labeled EXACT, evaluated to the comparator's uncertainty + 2 guard digits;
- **(d)** every comparison stated as Δ ± propagated σ — the word "matches" without a σ is banned;
- **(e)** residuals quoted only to experiment-supported digits, with sign.

**Cite-verification note (git-confirmed this session — the two PRs do different jobs):** **#640** (merge
`43e5c636`, commit `f44939dc` "docs(board): Continuation-2 part 2 — delta_strain hygiene rider + proposed
precision house rule") landed the precision house rule into the board's Continuation-2 §6 **as PROPOSED /
PENDING-GRANT** — that is what "landed via #640" in the D1 spec refers to (the *proposal*, not the
ratification). The board's own PR *table* stops at #639, which is why the proposal is easy to mis-read as
un-landed; git shows #640 (and #641) merged after that table was written. **This batch lands the
RATIFICATION** — per rulings-docket item 15 the ratification vehicle is **this batch's own PR** (not #640,
which only carried the proposal). Cross-ref the board §6 PROPOSED origin and note the PROPOSED→RATIFIED
transition dates to Grant 2026-07-11.

---

## DELIVERABLES

- **Per-target commits** (≥ one per target; T6 rider, T12 rider, δ_strain rider, and the precision-house-rule
  landing each get their own commit).
- **A batch result doc** summarizing each target as **executed / gated / flagged** with its receipts (T8 bin-3
  logged GATED on X41; T6 flag-count if any load-bearing M-row site is found; T12 rider recorded as FLAG-not-split;
  T19 flagged if the solver internals are not visible).
- **NO physics claims minted beyond the registry's pre-verified specs.** Def-nodes minted **ONLY** where the
  registry says the mint is staged — **T1** (triad) and **T12** (u₀* SYM/bias, with the X40 flag as a rider, not
  a second node). Everything else is annotation / cross-link / hygiene.

## REVIEW

Adversarial review (M4 wrapper, `.claude/workflows/ave-adversarial-pr-review.js`) **before CLEARED**. Lenses:
- **receipt-fidelity** — every cited line/string fires verbatim at the stated location (two-method greps where a
  completeness claim is made); the #637-corrected receipts (T2 six-member, T10 `:141-146`) are the ones to check.
- **mint-scope** — no def-node exceeds its registry spec; the **T12 rider stays a FLAG, not a split**; T6 stays a
  dictionary-entry annotation, not a sector re-wire; no new forward-prediction claims (T4 is Grant-gated and NOT
  in this batch).

## GATES (before push)

`make verify` · `make verify-md-links` · `make verify-provenance-stamps`. Avoid the provenance-stamp token set
(`driver-confirmed`, `test-locked`, `sympy-verified`, `sympy-confirmed`, `engine-confirmed`, `engine-verified`,
`FEM-verified`) in any `research/` result doc; use "verified" / "grep-confirmed" / "git-confirmed" instead.

---

*Cross-refs: the registry `research/2026-07-10_collapse-target-registry.md` (post-#637 state, the sole physics
source for every target); the rulings docket `_orchestration/2026-07-10_rulings-docket.md` (Continuation
2026-07-11, items 7/8/12/15 — T6, u₀* split-wait, collapse-batch GO, precision-rule RATIFIED); the R-B framing
note `research/2026-07-10_rb-fossil-walk_framing.md` §3 (the u₀* homonym flag); the X40 result
`research/2026-07-10_x40-ring-closure-transient_result.md` (both cut/cycle components nonzero within the model);
the orchestration board `_orchestration/2026-07-10_orchestration-board.md` §5/§9 (registry rows) + Continuation-2
§5/§6 (δ_strain rider + precision house rule PROPOSED origin). This brief mints nothing and canonizes nothing;
it hands the registry's pre-verified specs to a satellite for execution under the standing stack.*
