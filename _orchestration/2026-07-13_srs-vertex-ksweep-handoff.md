# Handoff brief — srs vertex k-sweep backscatter driver (carrier-wave frame)

**Date:** 2026-07-13 · **Grant lean:** 2026-07-13 (homogenization-split, carrier-wave picture)
· **Class:** satellite-session driver run (self-contained). **Role:** adjudicate the srs
per-vertex Γ=−1/3 ontology fork that **A-class cascade drainage load-bears on**.

**Sector header (mandatory).** MODE: linear wavepacket propagation on the srs lattice — a
**carrier / homogenization property of the network**, NOT a saturation test. REGIME: cold
lattice, **KERNEL OFF** (no Op14 saturation; the fork is about whether the per-vertex tee is
physically expressed, which is a linear-network question). PHASE-STATE: freely propagating
Bloch/wavepacket content, sub-yield throughout.

---

## Mission

Adjudicate the **srs per-vertex Γ = −1/3 ontology fork**:

- **real-per-vertex** — every z=3 vertex physically expresses its mismatched-tee reflection;
- **lumped-idealization** — the −1/3 is a bookkeeping floor that never physically resolves;
- **homogenization-split** — the per-vertex event is invisible to in-band collective modes and
  only resolves near the band edge (Grant's leaned frame).

**Method:** launch wavepackets on the srs lattice across a **k-sweep**; measure **backscatter
fraction vs k·ℓ_node**. **KERNEL OFF** (linear lattice).

---

## The arithmetic (per-vertex tee — state in the brief)

Each **z = 3 vertex is a three-way tee**. A wave down one bond sees the **other two bonds in
parallel** (Z₀/2), so the bare junction reflects **Γ = (2 − z)/z = −1/3** — a **COUNTING fact**,
one bond feeding two, immune to symmetric transformation. Power split:
**|Γ|² = 1/9 reflected**, **4/9 + 4/9 transmitted** into the two onward bonds. This is
**LOSSLESS reactive back-scatter / redistribution** — **Axiom 3 is never threatened** (reactive,
not loss; the matched-lossless-reciprocal-3-port theorem, `|S₁₁| ≥ 1/3` floor). The **fork is
whether the per-vertex event is physically expressed**, not whether it costs energy — it never
costs energy. (Canonical: `manuscript/ave-kb/common/translation-tables/translation-circuit.md:180`.)

---

## Frozen bins

- **(i) HOMOGENIZATION-SPLIT** (Grant's leaned frame; carrier-wave picture). Backscatter
  **falls toward zero with a power law at long wavelength** (k·ℓ_node → 0) and **rises toward
  the per-vertex scale near the band edge**. Physical reading: **in-band collective Bloch modes
  never see a tee** — the periodic lattice's eigenmodes already diagonalize the vertex scattering,
  so a mode that fits the band does not backscatter; only content that cannot be carried
  collectively (near the band edge) resolves the individual junction.
- **(ii) REAL-AT-ALL-K.** Localized packets **scatter at the per-vertex scale (|Γ|² ≈ 1/9) at
  every k** — the tee is physically expressed independent of wavelength.
- **(iii) NULL-EVERYWHERE.** Backscatter **consistent with zero at all k** — the lumped junction
  **never physically resolves** (the −1/3 is a bookkeeping idealization only).

---

## Stakes (and the sharp edge)

**A-class cascade drainage's "primitive unit" language load-bears on this.** The srs vertex
−1/3 is the candidate **primitive unit of the A (continuous entropic drainage) coupling class**;
whether that unit is physically real vs a distributed-merge idealization is the **PENDING-GRANT
ontology fork** (docket T4 / the srs 1/9 real-vs-idealization fork).

**Band-edge expression is the candidate mechanism for the muon's finite Q.** If backscatter only
resolves near the band edge (bin i), then a bound mode whose content **straddles the edge** would
pay a finite backscatter price — a candidate origin for the muon's finite loss-Q (Q_μ ≈
3.5×10¹⁷, finite where the electron's is infinite). **[Cite-flag — walk-estimate, no direct
corpus receipt.]** The specific figure "**muon content ~0.74-cell radial**" is **Grant's leaned
estimate (2026-07-13)** and has **no corpus receipt** (searched research/ + ave-kb/; the nearest
anchors are the muon's SUB-PITCH band `[r_turn, ℓ_node]` dominating the overshoot,
`research/2026-07-08_p4-forward-voltage-threshold_RESULT.md:115`, and the muon ∇A gradient scale
~1–2 ℓ_node, `research/2026-07-06_em-keying-round3-eps-dc-mechanism_RESULT.md:374`). Carry the
0.74 figure as a lean, not a fact.

**The SHARP EDGE the resolution must survive (state as a named consistency check, NOT a
result).** The **electron is also node-scale** — it binds at **R\* ≈ 1.6 ℓ_node**
(`manuscript/ave-kb/vol2/particle-physics/ch01-topological-matter/hollow-vortex-binding.md:133`,
dimensionally-forced / CANDIDATE-class consistency check;
`research/2026-07-01_hollow-vortex-sigma-gate_result.md:312`) — **yet its Q → ∞**. So proximity
to node scale cannot itself price the channel. Resolution: **proximity PRICES the channel only
where an onward mode exists to carry the drained power**; **topology GATES it** — the electron
sits at a **terminal pole** (no onward mode; nothing to drain into ⇒ Q→∞), whereas the muon's
content straddles a region with an onward channel. **This is a named consistency check the
adjudication must survive, not a result to be assumed.**

---

## Rails (binding)

- **Platform firewall: loop-gap / srs platform ONLY.** No `NativeCageIMEX` / cage work — this is
  a linear srs-network property; keep the carrier off the cage platform entirely.
- **KERNEL OFF** — linear lattice; this is not a saturation test, and turning the kernel on
  would confound the carrier/homogenization read with saturation dynamics.
- **Freeze-by-push** — prereg (with the frozen backscatter-vs-k·ℓ_node bin thresholds) is its own
  commit, PUSHED BEFORE the driver runs.
- **Sabotage plants act on EVOLVED observables, not arithmetic-only** — a plant must corrupt the
  evolved backscatter field measurement, not a post-hoc reduction.
- **Adversarial review wrapper** — ALWAYS use a `scriptPath` wrapper that inlines ARGS and calls
  `workflow({scriptPath: '.claude/workflows/ave-adversarial-pr-review.js'}, ARGS)`; the
  named-workflow args path silently drops args.
- **DO-NOT-MERGE** — PR opens `[DO-NOT-MERGE]`; only Grant merges.

---

## References (grep-verified anchors — 2026-07-13, at this PR's base d0037d8f)

- `manuscript/ave-kb/common/translation-tables/translation-circuit.md:180` — srs z=3 vertex =
  intrinsically mismatched reciprocal 3-port; Γ=(2−z)/z=−1/3 counting fact; |Γ|²=1/9 reactive
  back-scatter (Axiom 3 — reactive, not loss); |S₁₁|≥1/3 floor; circulator = non-reciprocal
  matched alternative, T-breaking PENDING-GRANT.
- `manuscript/ave-kb/vol2/particle-physics/ch01-topological-matter/hollow-vortex-binding.md:133`
  + `research/2026-07-01_hollow-vortex-sigma-gate_result.md:312` — electron binds at R*≈1.6
  ℓ_node (dimensionally-forced / CANDIDATE-class consistency check).
- `research/2026-07-08_p4-forward-voltage-threshold_RESULT.md:115` (muon sub-pitch band
  `[r_turn, ℓ_node]`) + `research/2026-07-06_em-keying-round3-eps-dc-mechanism_RESULT.md:374`
  (muon ∇A gradient scale ~1–2 ℓ_node) — nearest anchors for the muon radial extent; **the
  "~0.74-cell" figure itself is Grant's lean, unreceipted.**
