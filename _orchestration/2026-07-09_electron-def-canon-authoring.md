# AUTHORING BRIEF — Task #27: canonical electron-definition propagation (all decisions ruled)

**Date:** 2026-07-09 · **Branch:** `analysis/electron-def-canon-27` · **Mode:** parallel lane-authors (disjoint files) → single committer → adversarial verify → fix. **No self-merge; PR is DO-NOT-MERGE.**
**MAP-phase provenance:** four read-only auditor punch-lists (2026-07-09, session task outputs), consolidated here. Every file:line below was auditor-verified against post-#589 main; re-verify on read (the worktree is now at post-#595 main).

## THE TARGET FRAMING (updated for the five rulings)

**One line:** the electron is a stable, lossless, chiral topological defect in the K4/srs Cosserat vacuum lattice — a persistent quantized circulation whose measurable properties are the far-field projection of a core that carries the particle's identity as topological boundary data.

- Medium: lossless saturable K4/srs Cosserat LC network; ℓ_node=ƛ_C≈3.86×10⁻¹³ m; ω₀=m_ec²/ħ; Z₀=377Ω. Sectors A1 (mass) ⊥ T2 (charge/spin) ⊥ longitudinal scalar. Never cross-wire.
- **Core/far-field (D3 = COEXIST, with justification):** TWO statements at different radii, both kept. (i) IDENTITY: the core is a topological singularity — charge sign/windings are boundary data (Burgers/Frank-analog INPUTS to the surrounding elasticity); mapping spatial structure INTO it to store identity is a category error. (ii) ENVELOPE: the ~ℓ_node near-field region (hollow-vortex R*≈1.6ℓ_node cavity; saturated/slowed tube-core values) is the REAL yield-envelope of the medium — where saturation and the Γ→−1 mismatch wall live (cavitation-bubble sense: where the medium gave up, not the inside of the propeller). The cavity describes the medium's response region; the singularity states that no interior map stores the identity. Different questions, no contradiction. EVERY edit touching a spatial-core passage adds this two-question justification rather than a demotion banner.
- **Identity labels (D2 = OUT):** 0₁ unknot (real space) ⊥ (2,3) torus-knot winding (PHASE space, d–q plane; ℤ² family; selection imported) ⊥ A1 dilatation (mass). The lattice's 2T disclination catalog (order 24, 7 Frank classes) is a LATTICE-level derived fact — cross-link it, but it does NOT appear on the electron's identity card; whether the electron occupies a Frank sector is open/underived. Do not add it anywhere as an electron label.
- Observables (tags MUST match): charge quantization DERIVED (Link∈ℤ) / value imported; spin STRUCTURE derived / SELECTION imported ([SPIN-HALF-POSITED], #584/#585/#588, clm-rkisb8/clm-salw2h); **spin-statistics DERIVED at PEER-ahead (D1 resolved: PR #315 MERGED — FR two-loop braid exchange = −I, σ²=+I, operator `src/ave/topological/fr_braid_exchange.py`, result doc `research/2026-06-20_fr-braid-spin-statistics_result.md`; ceiling = PEER-ahead because the −I is GENERIC-FR (non-A4 control also passes), NOT an AVE-distinct chord; NOT "A4-only")** — scope leaf edits to exactly what #315 PASSed (exchange holonomy); if a leaf claims Pauli EXCLUSION as a theorem, check #315's scope and represent honestly (exchange derived; exclusion corollary status = whatever the result doc supports, no more); mass finite-by-ℓ_node-cutoff / value calibration-anchored; g=2 mechanism-AVE / value peer-with-Dirac; g−2 = α-echo.
- Dynamics: fluxoid (persistent supercurrent, topologically held; lock-arc closed negative). Q=1/α cited as identity, not derivation.
- FORM/VALUE law: forms derived / values-selections imported, at every level; imports are STRUCTURAL (boundary data) not a gap.
- **Honest-peer caveat (leaf-level, the #1 protect):** the interior is a finite/mechanistic/honest PEER re-representation; the AVE-distinct chord lives ONLY in the forward predictions. No edit may tip a tally, title, or one-liner into an interior-chord claim.

## RULED DECISIONS (verbatim application rules)
- **D1:** spin-statistics = derived, PEER-ahead, generic-FR (#315). Reconcile the two stale PROVISIONAL sites to it.
- **D2:** disclination OUT of the electron identity ("for now"). Cross-link only, with the open-question sentence.
- **D3:** COEXIST **with justification** — the two-question note (identity-vs-envelope) at every spatial-core site; no demotion banners; no silent supersede.
- **D4:** full HANDEDNESS REGISTER (Lane E): enumerate every handedness-like attribute, define what each physically describes, assign owning sector, map leaf-by-leaf, THEN reconcile the e⁻ RH-vs-LH conflict against the map. Any genuine convention choice → surface in the PR body as a one-liner for Grant, do NOT silently pick.
- **D5:** clm-8c3yhs "(2,3) forced" → selection-imported/PEER tag + a **paths-to-derived survey** appendix (four routes: energetic minimality [needs a mass functional over the (p,q) family]; stability/decay selection [open]; topological exclusion [odd-q / no-(2,4) exclusions EXIST → partial]; genesis dynamics [open; genesis arcs closed-negative to date]) — each with status + what would close it. Survey, not derivation.

## LANES (disjoint files — authors NEVER touch another lane's files, NEVER run git/make)

**LANE A — core candidates:** `manuscript/ave-kb/vol2/particle-physics/ch01-topological-matter/electron-identification.md` (+ its `index.md`) and `manuscript/ave-kb/common/form-deriving-value-importing.md`.
1. electron-identification §1/§2: integrate the consolidated definition (above) into the EXISTING definitional structure — its §4 forbids a 9th independent definition, so this is an upgrade-in-place. Add: core/far-field D3 two-question framing; the three-identity-labels list (D2 OUT + the cross-link sentence); the fluxoid dynamics line; the FORM/VALUE law; the leaf-level honest-peer caveat.
2. Fix `:80` "charge e Fully derived (FORM+VALUE)" → quantization-derived / value-imported (align to coverage-matrix "VALUE=DEFINITIONAL").
3. Guard the tallies (`:73` 8/8, `:74` 5/8, `:85` "honestly axiom-derived"): keep the numbers, put the per-observable import tags immediately adjacent so no quote can strip them.
4. Spin-statistics row → derived-PEER-ahead per #315 (cite result doc).
5. index.md `:34`: 6/8→5/8; `:13` α-running inherits the "sketch/open problem" hedge from electron-unknot:63.
6. form-deriving-value-importing.md: add the ELECTRON row (charge-quant-derived/value-imported · spin-structure-derived/selection-imported · spin-statistics-derived-PEER-ahead(#315) · m_e definitional · (2,3)-selection imported · g=2 mechanism/value split) + the "imports are structural = topological boundary data (Burgers/Frank-analog)" mechanism paragraph, built INSIDE its existing guard machinery (L312-347 ethos) — an electron row must NOT read as a chord.

**LANE B — spin/topology leaves (all in ch01 + one appendix):** `finkelstein-misner-spin-half-derivation.md`, `electron-unknot.md`, `electron-unknot-cosserat-seeder.md`, `spin-gyroscopic-isomorphism.md`, `vol2/appendices/app-b-paradoxes/spin-half-paradox.md`, `torus-knot-uniqueness.md`, `torus-knot-ladder.md`, `mathematical-topology-of-mass.md`, `mass-closure-theorem.md`.
1. FM leaf: retitle to a double-cover-STRUCTURE derivation; rescope `:63` to structure-only (align with its own `:16`); §5 IMPORTED list (`:99-103`) gains the fermionic SELECTION; add the π₁-admits-both sentence + clm-rkisb8 crossref. PRESERVE `:16`, `:22-24`, `:178`.
2. electron-unknot `:13`: g=2 "generated"/spin "classically derivable" → the structure/selection + g=2-value-posited banner its siblings carry. PRESERVE `:41-51` (finiteness), `:63` (α-running hedge).
3. seeder: `:42/:56` spin split; `:59` α-formula → cite-as-identity echo-tag; `:22/:63/:67` "bounded interior"/"fits inside one cell" → D3 two-question language (envelope, not identity-storage). PRESERVE `:84/:94-95` engine-status honesty.
4. spin-gyroscopic: `:51/:60` "macroscopic" → ~ℓ_node-scale; add one-line selection-imported pointer + the symmetric-standard note (gyro≡Bloch equivalence is standard physics, peer not chord); collapse the duplicated frontmatter (`:4-7`/`:9-12`).
5. spin-half-paradox `:12`: "macroscopic" → extended (~ℓ_node core). PRESERVE `:14` verbatim.
6. torus-knot-uniqueness: align `:93/:125` "MUST be (2,3)" with `:127`'s honest split using the D5 lexicon (structure derived / selection imported); `:107` muon-mass formula gets an echo/import tag (defers to its real source leaf). PRESERVE `:15`, `:127`, §7 FI-13.
7. torus-knot-ladder: one-line imported-assignment note on the winding→particle column. PRESERVE `:21`, `:25`.
8. mathematical-topology-of-mass: `:20` add the disambiguation (general Faddeev-Hopf functional language; the ELECTRON's real-space topology is 0₁ — cross-ref); add ℓ_node-finiteness + peer tags. PRESERVE `:28`.
9. mass-closure-theorem: explicit "m_e VALUE is calibration-anchored, not predicted" caveat; add real/phase-space disambiguation to the `:42-46` body. PRESERVE `:24`, `:76`, `:78`. Leave the existing Rule-12 banners alone.

**LANE C — interior/coexist + coverage + l3:** `substrate-perspective-electron.md`, `electron-bound-resonator-coverage.md`, `hollow-vortex-binding.md`, `common/the-abandoned-interior.md`, `l3-electron-soliton-synthesis.md`.
1. substrate-perspective `:24`: "spin-½ (forced)" → structure-forced/selection-posited (the #588 miss). §2 `:80-91` tube-core table: add the D3 two-question justification note (these values describe the yield-ENVELOPE; identity = boundary data; category-error guard). `:119-123`: modernize the *why* (topological, not just geometric). PRESERVE `:105-117` (build on it), `:11/:167/:198/:209`.
2. coverage: `:27` one-liner — remove "g=2 ratio" from the natively-derived FORMs list (align to its own `:55`); `:31` + `:85` spin-statistics PROVISIONAL → derived-PEER-ahead per #315 (READ the #315 result doc first; scope the edit to the exchange-holonomy PASS; represent the exclusion-corollary status exactly as that doc supports); ℓ_node `:34/:71/:91` 2.4×10⁻¹² → 3.86×10⁻¹³ m + fix the dependent "~8 OOM"→~7 arithmetic. PRESERVE `:70/:90`, `:71/:91` compositeness-gap, `:72-73` echo-register.
3. hollow-vortex-binding: add the D3 coexist note (cavity = Class-C near-core yield-envelope description; identity = boundary data; two questions, two radii) — INTO the existing fence structure, preserving §3.3 verbatim.
4. abandoned-interior: add the core/far-field reframe as the modern lens; Thread C re-scoped as a question about the ENVELOPE's constitution, not about identity-storage. PRESERVE `:112`, `:45-48`.
5. l3: §1 `:30/:45` + §2 `:53-55` add the real-space/phase-space + three-distinct-"2"s disambiguation cross-refs (bipartite-lobe vs SU(2)-cover vs d-axis winding are DIFFERENT objects); §6.2 add the g=2/μ mechanism-vs-value banner; `:166` Pauli/spin-stat → #315-scoped update. **PROTECT `:160` (χ=1.2α forward prediction) and §8 verbatim.**

**LANE D — claim-quality registries:** `vol1/claim-quality.md`, `vol2/claim-quality.md`.
1. clm-unk0bd (vol1:18): add the selection-imported caveat (align body with its clm-salw2h dependency); optional stale-emphasis note at :20 (topological-protection is the settled framing).
2. **clm-i4p11y (vol1:1331-1345): EDIT-LOCKED — append-only.** Do NOT touch the body. Append a dated 🟡 flag-note (match the existing note pattern) carrying the #584/#585 refinement (π₁ admits both → selection imported → PEER) + pointer to clm-rkisb8's current scope.
3. clm-rkisb8 title (vol1:991): "Substrate-Native Spin-½" → "Substrate-Native Spin-½ STRUCTURE (double cover)" or equivalent — title must not outrun the body.
4. clm-8c3yhs (vol2:1350): "forced" → selection-imported/PEER + the D5 paths-to-derived survey appendix (four routes w/ status, per the ruling above).
5. clm-stgx1i (vol2:189): add the α-echo tag (g−2 rides α; symmetric-standard: peer-with-Schwinger).
6. clm-uatcql: `:1152` keep the 5/8 tally but put per-observable import tags adjacent (this RESOLVES the "5/8-demote?" residual as KEEP-WITH-GUARDS); `:1147/:1150` add a note that the transverse-precursor framing is the DISCLOSED precursor picture (COEXIST flag clm-i4p11y:1335) and the precursor-vs-end-state sub-fork stays OPEN (D3 did not resolve it — do not silently resolve).
7. Grep both registries for any spin-statistics claim rows; reconcile to #315 if present.

**LANE E — handedness register (D4):** `chirality-and-antimatter.md` (+ read-only consult of `pair-production-axiom-derivation.md` — flag, don't edit it unless the map REQUIRES a relabel, in which case add a crossref note only).
Build the register IN chirality-and-antimatter.md (expand the 26-line leaf): for EACH handedness-like attribute — (1) srs lattice handedness (geometry column; fixed background); (2) real-space twist/writhe of the 0₁ carrier; (3) Beltrami/helical flux chirality; (4) Cosserat micro-rotation sense (node rotor sign); (5) phase-space winding sign ((2,3) vs (2,−3), d–q orientation); (6) μ-sign selector (degenerate wall-branch selector, #260); (7) charge-conjugation: which attributes flip for e⁺ — define what it physically describes, assign the owning sector, and map which leaf uses which term. THEN adjudicate `:10` ("e⁻ right-handed unknot") vs pair-production `:27/:79` ("e⁻ LH chirality"): same attribute (→ convention conflict: surface to Grant in the PR body, one line, do NOT pick) or different attributes (→ label each with its register entry). Fix `:16` "Hopfion" → 0₁ unknot. Add derived/imported tags; 2γ/1.022 MeV tagged peer.

## DISCIPLINES (all lanes)
Read `manuscript/ave-kb/CLAUDE.md` before any KB edit. verify-before-cite every line number in the CURRENT worktree (post-#595 — lines may have drifted from the audit). ave-worktree-paths: absolute worktree paths from the first call. NO git commands, NO make, NO file outside your lane. Preserve everything the punch-lists mark PRESERVE — verbatim. Honest-peer register throughout. If a punch-list item conflicts with what you find on read, FLAG it in your return instead of forcing the edit.
