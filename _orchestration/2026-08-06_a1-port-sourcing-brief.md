# Lane brief — A1-port sourcing adjudication (2026-08-06, Grant GO)

**The most load-bearing open object on the board: a live kill hangs on it.** LC-1
(PR #913) established that the longitudinal bulk branch is gapless, energy-carrying,
and structurally superluminal (v_L² / v_T² = 4/3 + K/G ≥ 4/3 for all K ≥ 0; the
level-repulsion cancellation is unavailable to it; direction-resolution on the real srs
tensor WIDENS the margin, min ratio 2.13). Whether that kills the framework hangs
entirely on SOURCING — this lane adjudicates it. The LC arc (LC-2..5) is HELD on this
lane's verdict plus Grant's arc-scope ruling.

**SVA §0 header (leaf v0.2, 11 rows) MANDATORY on the prereg. Freeze-first: prereg
commits ALONE and pushes before any derivation. Frozen criteria travel VERBATIM or as
file:line pointers — never paraphrased (the LC-1 lesson). PR opens
`[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. Tier-2 at adjudication.**

## The frozen question

Does any coupling in the receipted structure convert a gravitating source's A1 content
into TIME-VARYING far-field bulk strain — and at what multipole order? Sharpened form
(the static-mass argument does not cover binaries): a static mass is DC bias and cannot
radiate; a BINARY is a time-varying A1 configuration, so the drive exists. Derive the
lowest non-vanishing multipole order of bulk-channel emission for a binary A1
configuration: monopole should vanish by total-mass conservation, dipole by momentum
conservation — does the QUADRUPOLE coupling integral vanish (and if so, by what
structure — there is no gauge to hide the trace behind in an elastic medium), or not?
If non-vanishing: compute the flux order and compare against the pulsar-timing budget
residual (re-retrieve the banked exclusion from source at lane time; the Hulse-Taylor
class budget is matched by the transverse channel to ~fraction-of-a-percent — state the
residual room honestly).

## Verdict grammar (frozen)

- **RADIATIVE(n)** — a non-vanishing coupling at multipole order n, flux derived;
  compare pulsar-timing residual; feeds Grant's arc-scope ruling as the kill-arm
  confirmation.
- **SOURCELESS** — every receipted coupling vanishes to the derived order, by named
  structure (a conservation law, a coupling-integral identity), not by assumption;
  LC-1 re-banks as structural compliance and the lineage-map line is vindicated.
- **NOT-DERIVABLE** — canon lacks a named piece (most likely the source coupling: what
  physically plucks the bond in matter, `axiom-register.md:193`'s standing open);
  deliver the enumerated missing-piece list. An honest open with a name, not a shrug.

## Mandatory inputs (re-derive, don't trust; verbatim quotes at freeze)

1. **FLAG-LC1-B, the live canon contradiction this lane resolves:**
   `physics-lineage-map.md:63` ("the A1 grade is non-radiative in free space") vs the
   port-register Q1 row (RULED, REVERTED 2026-07-20 to Reading-A-live). Read BOTH at
   HEAD and quote verbatim. Per the challenge-the-canonical-negative rule: the map line
   may be a fossil of a true structural claim the reversion never re-examined — grep
   the CONFIGS/derivations behind both, not just the conclusions.
2. **#761's falsified candidates.** The reversion rests on #761 falsifying three
   candidate non-radiativity answers. Read #761; enumerate what the three candidates
   WERE; state whether the multipole/coupling-order question this lane poses is among
   them or orthogonal to them. If #761 already killed the quadrupole-vanishing route,
   say so with the receipt.
3. **LC-1's result (PR #913)** — the channel's existence/speed receipts and its §6.4
   arc-scope package. This lane adjudicates the PREMISE that result inherited; do not
   re-derive the spectrum.
4. **The pluck question** (`axiom-register.md:193`) — if the source coupling is
   underived, route through NOT-DERIVABLE rather than inventing one.
5. **Walk-level context (UN-AUDITED, licenses nothing):** the rotation-substance
   framing note (`research/2026-08-06_rotation-substance-ontology_framing-note.md`)
   incl. §9's static-bias walk — the quaternion bulk-scalar reading of the V-sector and
   the nodes-fixed/waves-compress lean. Context for the walk, not receipts.

## Fences

- Regime/phase declarations at every step (cold crystalline far field; the source
  region's state declared separately).
- Sector-ownership discipline: A1 owns the dilatation content; do not cross-wire the
  rotational or EM sectors into the source coupling without a receipt.
- No comparator tuning (Rule 11): the pulsar-timing bound is re-retrieved and frozen in
  the prereg before any flux number exists.
- Walk-before-execute on fence-reach: if the derivation finds the outbound coupling
  runs through the ROTATIONAL channel (the #897-fenced frontier line), STOP and route
  to Grant before proceeding.
- Two-method receipts, named engines, on every corpus-absence claim.
- The engine codes what it codes (stencil-lens): an engine run demonstrating
  radiation/silence inherits the coded coupling — derivation adjudicates, engine runs
  corroborate at most.
