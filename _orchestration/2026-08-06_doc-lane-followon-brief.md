# Doc-lane follow-on brief — post-upgrade-wave batch (2026-08-06)

Rulings behind every item: `_orchestration/docket-entries/2026-08-06-rulings-decision-
batch.md` (R1/R2/R6/R7/R8) + the v2 rescope record. Re-derive states at HEAD; do not
trust this brief's counts (pointers-not-values). PRs open `[DO-NOT-MERGE][REVIEW:
pending-orchestrator]`.

1. **Census treatment (i) — GO (R1).** Your proposal as costed: the 216 actionable sites
   / 60 files. Pin the SHA on every census number you print.
2. **SHA pin-marker convention (R2).** Design the author-declared machine-readable marker
   (token adjacent to the SHA), grandfather the existing heuristic-exempt set, spec the
   exemption re-key to marker-only post-migration. Convention text to CONVENTIONS.md;
   gate change is a separate follow-on.
3. **Fence re-point (after the v2 record merges).** The wave's fence sites (12 files at
   last count — re-derive) re-point their routing notes to
   `2026-08-06-ruling-kernel-collapse-rescope-v2.md`; the two sites naming no #897 (vol3
   ch15 index; backmatter/07) get the pointer added. The "omission at #897" language is
   RESOLVED by the v2 — notes should say the fence is now carried at the ruling.
4. **Wave-report corrections (R6 verification findings).** (a) Qualify the wave's
   "the member the engine actually codes" headline to its own §2.2 form ("the engine
   carries both readings"); (b) FLAG-A2DEF 2→3 (the `:619/:681` sym-only+V+chirality
   definition) + the S_eps_sq ≠ S_eps² naming hazard; (c) add the κ=0 numerical-degeneracy
   note where the engine receipt is cited.
5. **ROW-NOT-CERTIFIED currency sweep (after the core G-RHO2 lockstep PR merges).**
   Every payload sentence names WHICH run it quotes (v1 FAIL record vs #902 ROW-CERTIFIED
   rerun). Sweep case-INSENSITIVELY — lowercase `\textsc{row-not-certified}` sites in the
   .tex escape a case-sensitive grep (measured; ≥5 sites at last count — re-derive).
6. **Ax1 phase-scope header (R7 — execute after the decision-batch record merges).** Land
   the PROPOSED domain-of-validity header (text in R7) at the axiom register's Ax1 entry,
   as an addition, not a rewrite. Vocabulary fence applies: substrate-named phases only.
7. **Pre-bond rename — PROPOSAL ONLY (R8, Grant lean, NOT a GO).** Draft the rename
   proposal ("pre-geodesic plasma" → "pre-bond state", gloss *pre-geodesic*; de-bonded vs
   pre-bond history split) with a costed site census. NO corpus sweep without Grant's GO.
8. **Standing fences.** The #897-frontier line (rotational penetration) stays out of
   print — unchanged by the v2. The trampoline K=2G bridge flag (its correction's routing
   note) stays open — do not adjudicate. The un-audited framing note
   (`research/2026-08-06_rotation-substance-ontology_framing-note.md`) licenses NOTHING
   in print.

---
**DATED UPDATE 2026-08-06 PM (Tier-2 on #905, findings A3/A4/A5/A11):** item 3 widens —
`wall-taxonomy.md` §10.2 quotes the SUPERSEDED v1 ruled text byte-identically and its
"the ruled text above does NOT carry it" fence sentence is now false of the current
ruling: replace the quote with the MERGED v2 blockquote VERBATIM (the correction fragment
alters no ruled text — C1 declares an existing delta, C2 corrects prose OUTSIDE the
blockquote) and repair the fence sentence. Item 4(a) gains its site list: five "engine actually
codes" sites (`git grep -Fn "engine actually codes"`), of which `wall-taxonomy.md:528`
is a live canon table row (edit) and `research/2026-08-05_last-bond-kernel-collapse_
result.md:29` + `research/2026-08-05_approach-leak_result.md:495` are FROZEN result docs
— dated surface-note pattern, never a rewrite. New item 9: docket-fragment hygiene — new
fragments carry `### ENTRY <YYYY-MM-DD>-<lane-slug>` headers per the docket README (the
keys gate is warn-only and currently vacuous for ~20 recent fragments; sweep optional,
forward hygiene mandatory). Consume the corrected engine residence map from
`2026-08-06-ruling-kernel-collapse-rescope-v2-correction.md` C2 wherever item 4 lands.
