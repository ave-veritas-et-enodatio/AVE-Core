# Adversarial echo-reanalysis — workstream brief

**Status:** DRAFT, landed for tracking. Fires only on Grant's explicit go (highest rescue-risk of the queued workstreams). **Queue position: 3rd** — after `waem9tk6r` (single-scale unification) lands and after the frame/gravity Phase-0 gate.

**Origin:** Grant's 511 keV insight (2026-06-22) — `ℏω_C = m_e c²` is a value-echo but the *cutoff ≡ rest-mass identity* is a one-substrate structural unification the SM lacks (electron mass = free Yukawa, unrelated to any vacuum/EM scale). This adds a **third bin** to the form-deriving / value-importing meta-finding.

## Purpose

Re-tag the corpus's existing **echo**-tagged constants/relations against a sharper distinction:
- **pure echo** — an imported number with no structural content; OR
- **structural-unification echo** — the *value* is imported, but the one-substrate ontology *forces* a relationship the SM treats as independent free parameters.

Outcome is **explanatory coherence only — ZERO new falsifiers.** Re-tagging never upgrades an echo to a prediction; that still requires deriving the value. This is the standing task #45 (model-dependence re-interpretation register), sharpened.

## The two-gate test (BOTH required to earn a structural-unification re-tag)

1. The SM genuinely treats the quantities as **independent free parameters** — not already unified by a symmetry the SM has (gauge unification, anomaly cancellation, dimensional transmutation are the SM's nearest analogs and DEFEAT distinctness if they apply).
2. AVE genuinely **forces** the relationship — not merely *defines* it. **Circularity trap:** if AVE sets the quantities via each other (a chain of definitions), that is import-economy, not forced structure.

## Discipline (non-negotiable — this is the most rescue-prone pass we run)

- **Adversarial-by-default:** one refute-by-default skeptic per candidate. Default verdict = **stays pure-echo**; the structural tag must be *earned*, read off the actual `constants.py` definitions + the actual SM treatment, not the appealing narrative.
- **The tripwire (kill-criterion):** if the pass re-tags *most* echoes as structural-chords, that is a RED FLAG — the lens is too permissive → **throw out the lens, do not celebrate.** A meta-audit lane (adversary-of-the-adversary) checks specifically for over-rescue. Healthy result = the large majority stay pure-echo, a few genuinely pass both gates.
- **Symmetric-standard:** the SM also has structural relations it doesn't predict numerically; "AVE has structure the SM lacks" must be *specific to the item*, never generic (part of gate 1).
- **Explanatory ≠ predictive:** flag every survivor as explanatory-only; no falsifier is created.

## Candidates (refute-by-default each)

- α — the `Q = 1/α` tank-Q + the `R·r = ¼` web
- K = 2G — GR-imported
- m_p/m_e — the one-residual consistency fit
- charge quantization — `Q = Link(∂Ω,F) ∈ ℤ`
- the vacuum impedance / saturation scales (Z_vac, B_SNAP family)
- the 511 keV identity = the already-run **pilot, which FAILED both gates** (frame-check 2026-06-22, `claim_survives=false`; canonicalized as `clm-sw5oao`, solidity 0.25 — one-import-economy + explanatory-ontology, NOT a chord). This is *good* calibration: the two-gate test correctly rejected an import-tautology rather than rubber-stamping it. Even the TKI/inductor backbone deflated (the `E₀=½L₀I_max²=½m₀c²` identity has `ξ_topo` cancel algebraically), and gate (a) failed outright — QED ties an EM scale to `m_e` too (the Compton length, the Schwinger cutoff = AVE's own `E_CRIT`).

## Structure when it runs

N parallel skeptics (one per candidate) → synthesis → **meta-audit for over-rescue** → a re-tag register (per item: both-gate verdict + verbatim `constants.py`/SM evidence + final tag). **Only if ≥1 item beyond the pilot genuinely passes both gates** → a scoped update to the form-value meta-finding adding the third category. Canonicalize (KB + manuscript) only what survives — per the standing rule (refute-by-default audit gates all documentation).

## Open for Grant

- Approve / adjust the candidate list + the tripwire threshold.
- The TKI angle did NOT rescue the 511 keV: the inductor/TKI identity is algebraically trivial (`ξ_topo` cancels), and the frame-check found the SM is not distinct here (QED's Compton length + Schwinger cutoff tie an EM scale to `m_e`). TKI-as-Axiom-2-isomorphism stands separately, but it does not make a value-import scale-coincidence a forced prediction. **Lesson carried into the candidate gates:** dimensional-transmutation / the QED-scale-anomaly are the SM's nearest analog and DEFEAT gate (a) if they apply — run that test against every candidate.
