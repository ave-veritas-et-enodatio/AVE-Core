# Lorentz-compliance arc — brief (2026-08-04, Grant GO)

**Status: ARC BRIEF — framing preserved + five derivation-only kill tests, all UNSTARTED.**
Sequencing (arc-serial): after the axial RHO-B lane returns and the mechanical-rulings batch
lands. Every lane in this arc opens with the SVA §0 header
(`manuscript/ave-kb/common/standard-vacuum-analysis.md`, `ave-prereg` ≥ v1.8). **Rule zero:
pointers-not-values — every published bound named below is re-retrieved from source at lane time
(external-retrieval pipeline); nothing numeric in this brief is load-bearing.**

## Framing (preserved from the 2026-08-04 walk; walk-level until each lane freezes it)

1. **The three-premise skeleton (Grant).** (i) The vacuum carries a material property (Z₀): the
   consensus concedes the medium (running permittivity, condensates, a calculable breakdown
   scale); what it refuses is a REST FRAME. (ii) Continuum math applied to the universe is
   unresolvable at the limits: the Wilsonian effective-theory consensus in different words — and
   its universality corollary EXPLAINS the corpus's FORM-deriving/VALUE-importing meta-finding
   while locating the only unscreened sectors: **topology** (integers survive coarse-graining)
   and **breakdown nonlinearity** (infrared protection lapses near yield). (iii) The electron is
   the lightest charged stable excitation, its stability protected by a conserved quantity; the
   incumbent ontology is a symmetry classification (Wigner), not an answer to "what is it."
2. **Emergent-Lorentz-invariance mapping (walked, Grant-confirmed).** Uniform saturation is
   unobservable from inside (self-cancellation — the phase-only doctrine IS the emergence
   mechanism). SYM gradients are pure geometry (the equivalence principle as impedance matching,
   Γ_EM = 0). ASYM responses are the invariance-violating observable class. **Carve:** the
   birefringence bench is FIELD-KEYED response-structure discrimination (Euler–Heisenberg is also
   field-keyed and fully Lorentz-invariant); the FRAME-KEYED surface is THIS arc — complementary,
   not duplicate.
3. **Method — the consensus-prior partition ("scream map," Grant 2026-08-04).** Consensus
   objections split by evidence class: metaphysics-backed objections ("twelve digits settles the
   ontology" — universality makes twelve-digit infrared agreement expected for ANY substrate
   flowing to the same IR) are charged at directly; **measurement-backed objections are not bias —
   they are free, pre-built falsification benches**, and this arc is their harvest. The
   orchestrator's consensus prior is treated as an instrumented systematic (see the
   consensus-bias lens; drift audits at arc retros).

## The five kill tests

Each: claim under test / published comparator (re-retrieve at lane time) / derivation task /
kill condition. All Tier-2 at adjudication (canon-touching verdicts), derivation-only (no
hardware, no simulation campaign).

| # | Test | Comparator class | Derivation task | Kill condition |
|---|---|---|---|---|
| **LC-1 ★ leads** | One-speed (multi-messenger) | GW170817 timing, \|c_GW/c−1\| ≲ 1e-15 class | (a) provenance of cold c_shear = c (is G_vac = ρc² derived or a matching condition?); (b) bulk √(10/3)c P-wave observability — gapped, confined, or sourceless? | An energy-carrying inter-event channel at ≠ c ⇒ arc-level kill |
| LC-2 | Anisotropy (lattice axes) | Modern Michelson–Morley / SME resonator bounds (~1e-18 class) + sidereal | Derive the K4 anisotropy order; the O((ℓ_node/λ)²) suppression expectation is UN-AUDITED and the shear-sector suppression is NOT guaranteed — derive both channels | Predicted anisotropy above bounds |
| LC-3 | Dispersion (discreteness) | GRB time-of-flight (Fermi-LAT class) | Derive ω(k) curvature from the band structure; the linear term should vanish by inversion symmetry — derive, don't assume; re-opens the GRB orphan-chord file with the correct carrier | Predicted dispersion above bounds |
| LC-4 | Cosmological birefringence | Astrophysical polarization (~1e-37 class) | Compliance derivation: cold-vacuum exact transparency + field-local, non-accumulating response (Route C circulation keying) | Any accumulating cold-vacuum birefringence |
| LC-5 | Electron form factor | LEP compositeness (< 1e-19 m class) | Does the winding/tank structure predict ANY form-factor deviation, or zero-by-topology (point-coupled through the winding)? | Predicted deviation above bounds; zero-by-topology = the scream answered structurally |

**Why this arc is truth-per-token optimal:** each test is adjudicated by data already collected
to thirteen-plus digits at someone else's expense; every outcome — pass or kill — is bankable;
and the arc attacks the consensus's two strongest refusals (the frame problem, multi-speed
consistency) head-on with receipts instead of rhetoric.

## Discipline

Frozen SVA-headed prereg per lane, pushed alone before any derivation; comparator bounds
re-retrieved and cited from source at lane time; outcomes propagate only per each prereg's
adjudication routing; LC-1 runs first and its kill condition is arc-terminating.

## Related routed items

- **V2 isometry check** (chart-metric ↔ optical-metric, the rim↔horizon rhyme): queued BEHIND
  this arc — see `research/2026-08-04_vacuum-smith-chart-geometry_framing-note.md`.
- **V3 literature retrieval** for the same note's `[L]` tags: may run alongside this arc's
  retrieval lane (shared external-retrieval dispatch).

---
**DATED CURRENCY UPDATE 2026-08-06 EOD (pre-launch check; ruling record
`2026-08-06-rulings-final-batch.md` R15):** the sequencing clause above is SUPERSEDED —
this arc now LEADS (with the I_ω lane; residence runs alongside as canon-closure).
New on `main` since this brief froze, mandatory lane inputs (re-derive, don't trust):
(1) **the two-band k·p result** (#884 + its KB landing, `clm-2bkp7v`) — photon-at-c
derived via (G+G_c)/ρ minus the G_c/ρ level-repulsion, and the gapped-carrier
v-mismatch v²_⊥−v²_∥ = G_c/ρ (moduli-independent) — DIRECT input to LC-1(a); its
scale-separation and gap-identification conditionals carry into any LC-1 verdict.
(2) **A-008** (#895): the rotational gap = 2m_ec² pinned — input to LC-1(b)
(gapped-channel observability) and LC-3's carrier bookkeeping. (3) **kernel-collapse
v2 + correction** (#905/#907): the cross-grade combine fence — LC tests run cold-linear
where all kernels ≈ 1, so verdicts should be member-INSENSITIVE, but each prereg
declares this explicitly rather than assuming it. (4) **NO-TWIST** (#890): srs 432
kills homogeneous strain→rotation coupling — input to LC-2's rotational-channel
suppression derivation (which the brief correctly lists as NOT guaranteed — the O(q²)
gradient-order survivor is the thing to derive). SVA leaf is v0.2 on `main`; the
prereg-header mandate stands as written.
