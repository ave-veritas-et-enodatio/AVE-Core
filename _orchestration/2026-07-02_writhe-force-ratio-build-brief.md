# Build brief — the writhe co-vs-anti-handed |F| ratio (Observable-C)

**For:** a dedicated derivation session (implementor lane, AVE-Core).
**From:** Cleave-01 closure orchestration, 2026-07-02. **Status:** commissioned by Grant same day ("draft the writhe build brief"). Self-contained.
**Ask:** build the corpus's highest-value unbuilt forward prediction — the dimensionless co-handed-vs-anti-handed pairwise force ratio between winding solitons — to a DERIVED verdict: is the ratio **forced** (a pure lattice number ≠ 1), **import-riding**, or **ill-defined**? Bench concepts are GATED on that verdict. Derivation first, hardware never before the number.

---

## 1. The object (canonical, register §2.4 — quote-anchored, re-verify lines at HEAD)

From `research/2026-06-24_forward-prediction-register.md` §2.4 (verbatim): *"A pairwise force whose magnitude depends on the **RELATIVE handedness** of two winding solitons — a **dimensionless** co-handed-vs-anti-handed |F| ratio. Because it is a ratio, it dodges the m_e/α-echo magnitude trap entirely."* SM/QED counterfactual, per the register: *"A handedness-dependent force ratio ≠ 1; QED has no parity-odd vacuum force between like objects."* Falsifier once built: *"a co-vs-anti |F| ratio of exactly 1 falsifies the winding-force chord."*

**Why it is the highest-value target on the board (2026-07-02 state):** it is the *only* candidate that could bank as an echo-free **number** — every other live prediction is either a form-only chord (field-free OA, clm-fofwr1), a facility-gated coefficient whose magnitude is a symmetric α-echo (E-route birefringence, clm-pp3qwf), or demoted/conditional ((q·ℓ)⁴). And it **survives the Cleave closure untouched**: it rides S1's winding-DOF conservation (PASSED 2026-06-24), not the displacement-pump coupling that just closed NULL-CONFIRMED-FINAL (PR #463, clm-clvchn).

**Prerequisites the register itself names:** *"a `T⁰ⁱ` momentum-flux observable, near-yield regime, a writhe-aware kernel."*

## 2. What exists to build on (verified this session)

- **S1 PASS-WITH-FLAGS** (`research/2026-06-24_engine-s1-winding-dof_result.md`): the (2,3) winding hosts as a separately-conserved DOF with its own wave equation and momentum; `compute_Q_link` reads Q_link=3 / w_tor=2 on the seeded canonical (2,3), 0 on the null. Gate code: `src/ave/core/s1_winding_conservation_gate.py` (+9 passing acceptance tests). Scope flags: real-space ω, single-knot — the PAIR extension is exactly this brief.
- **A T⁰ⁱ momentum-flux observable already exists for the mass sector**: `research/2026-06-23_mass-sector-two-body-scattering_T0i_prereg.md` + `_result.md` — two-body scattering with momentum-flux readout. The writhe build extends that machinery to winding-carrying pairs; do not reinvent it.
- **Writhe machinery is live**: the chiral-vector-TLM arc computes signed writhe (±0.04087, enantiomorph-odd, diamond-null exact) — `research/2026-06-23_chiral-vector-tlm-phase1_result.md` GATE-1.
- **The BenchModel spine** (`src/ave/bench/model.py`, 8-gate bankability record) is canonical — when/if this reaches bench-concept stage it gets modeled through the spine, SM leg co-computed, no strawman.
- Related-reading only (do not chase): `vol3/.../dama-matched-lc-coupling.md` mentions co-vs-anti in the dark-sector context.

## 3. The derivation task (in order; each step can kill or reshape the next)

0. **GATE-0 — PAIR FEASIBILITY (staged, runs FIRST, cheap-decisive).** S1's scope was single-knot; the engine has never hosted TWO winding solitons at controlled separation, and dispersal is the engine's demonstrated default failure mode (Stage-2 native cage DISPERSES; S3 DISPERSE-FALSIFIED). Before any T⁰ⁱ campaign: seed a knot pair (start RR) at separation d, evolve, and gate on {both survive their individual S1 conservation checks ∧ separation trackable ∧ no merger/dispersal over the measurement window}. Also answer near-yield-of-WHAT — **the requirement's provenance is now traced (2026-07-03 yield-map)**: the register:199 "near-yield" prerequisite descends, uncited, from the cage-winding charter's risk item 4 ("handedness is saturation-only ⇒ DRIVEN/near-yield"), which is grounded in the engine's coupling construction — κ_chiral enters ONLY as a multiplicative bias on saturation strain (`cosserat_field_3d.py` `_reflection_density_asymmetric`, A²_μ/ε = (1 ± κ_chiral·h_local)·A²_base), so parity-odd dynamics vanish cold BY CONSTRUCTION. Consequences for this gate: (a) the knots' own T2 walls are AT V_yield by definition (Grant sectoral ruling fc680254 — the Γ=−1 self-trap IS the yield wall), so the saturation the parity-odd term needs is **wall-supplied by the solitons themselves**; (b) the A1 cores are deeply sub-saturated (A=√α, S≈0.996) — "cores are near-yield" is true only for the T2 sector; (c) the cold inter-knot medium is parity-EVEN by construction. **The actual un-derived gap is TRANSMISSION, not drive: can a parity-even linear medium carry the wall-generated handedness-dependent stress to the mid-plane T⁰ⁱ, or must the inter-knot region itself be driven near-yield?** Gate-0 must derive/test the transmission question — do NOT assume the medium needs driving a priori, and do NOT book a cold-medium null as wrong-regime without first checking whether wall-supplied saturation suffices. **Frozen mini-bins: [pairs stable → proceed to steps 1–5] / [pairs unstable at all tested d → the pair-force observable is ILL-DEFINED at current engine capability — named blocker, arc stops at ~10–15% of cost, register §2.4 gets the honest status] / [stable only in a d-window → the window IS the measurement domain; record it].** This gate is the Cleave lesson applied forward: the cheapest decisive computation runs before the expensive campaign. (2026-07-02 sanity-check amendment; also note the Ax4 arc #456–#464 landed the buckling-kernel result — the saturation kernel's √-shape is now an α-free geometric theorem given inextensibility — cite the current Ax4 state, not the pre-#455 one.)

1. **Sector header first** (per discipline): the force is a T2/Cosserat-winding-sector interaction read through momentum flux. Declare which medium DOF carries it, cold-vs-near-yield, and what "force" means substrate-natively (T⁰ⁱ flux through a plane between the pair — reuse the mass-sector construction).
2. **Regime declaration — the register says NEAR-YIELD.** Work out *why* (presumably the winding-winding coupling is second-order in the linear regime and only the saturation nonlinearity makes it leading). If the effect exists only near-yield, that constrains every bench concept and (per regime discipline) a linear-regime null is an artifact, not a falsification. If a linear-regime version exists, derive its suppression factor explicitly.
3. **The four-configuration computation:** seeded pairs RR, LL, RL, LR (+ null controls: unknot-unknot, knot-unknot), measure |F| via T⁰ⁱ at matched separation/geometry. The observable is the ratio R = |F|_co / |F|_anti. Enantiomorph consistency: R(RR/RL) must equal R(LL/LR) — a same-handedness-of-asymmetry failure is a red flag (the Chern arc's guard, reused).
4. **Chord-vs-echo per axis, pre-declared:** the RATIO is the chord candidate; the absolute |F| will ride imports (m_e/scale) — report it, never headline it. If R itself comes out expressible only through α or a tuned knob, that is an ECHO verdict — book it honestly.
5. **Separation scaling:** measure R(d) — a genuinely topological pair force should have a characteristic range/scaling (contact-like? power-law? screened?). This is load-bearing for both the SM-counterfactual and the prior-art scan (§4).

## 4. Guardrails — every one is a paid-for Cleave lesson

- **G-A (the big one): coupling at derivation grade BEFORE any bench.** Cleave-01 spent a full re-spec + hardware program on a unit-bridge that its own engine later computed to zero. Sequence here is inverted by design: if a topological invariant or an uncomputed kernel sits at the heart of the claimed force, **compute it first** — the frozen-prereg + validate-on-known + committed-bins pattern from the Chern arc (PRs #454/#463) is the template. No |F|-ratio claim without the engine number.
- **G-B: prior-art / discovery-context counterfactual EARLY, not after design.** Macroscopic parity-/handedness-dependent forces are a heavily-instrumented territory: torsion-balance searches for exotic spin-dependent forces (Moody–Wilczek-class), polarized-mass Eöt-Wash experiments, equivalence-principle tests with chiral materials. As soon as the derivation produces a magnitude+range, run the "would existing instruments have seen it?" scan against real published bounds — the same scan whose absence cost Cleave an audit cycle. If the derived effect needs winding-CARRYING matter (electrons/nuclei with net (2,3) content?) vs merely *chiral-shaped* matter (sugar crystals), say which explicitly — it decides whether prior art applies at all.
- **G-C: stay distinct from the FALSIFIED chiral-thrust.** The register's own ⚠: the dark-wake rectifier is dead (rectification ratios 0.62/0.31, common-mode radiation pressure, closed-by-derivation). This object is a *static relative-handedness pair force*, not a cyclic-drive rectifier. Any construction that sneaks a cyclic drive back in must be checked against the τ_bulk no-sign-memory closure before proceeding.
- **G-D: ontology declared at dispatch** (pre-test-physics-check): what physically carries the force — exchanged Cosserat shear? overlap of static winding textures? near-field reactive coupling (an impedance/mutual-inductance read per the EE mapping)? One wrong noun wastes the run; surface the candidate ontology to Grant with the prereg.
- **G-E: honest classification** (consistency-vs-emergence): S1 established the winding as a conserved DOF — consistency-class. The pair-force ratio ≠ 1 would be an EMERGENCE-class claim. Tag every intermediate accordingly; no promotion past what the run shows.
- **G-F: dimensionless ≠ automatically forced** (the VCA-R19 knife): a ratio produced only after tuning a free coupling knob is ACCOMMODATED, not FORCED. Count the free parameters in the writhe-aware kernel before claiming the ratio is parameter-free; knob-sweep invariance (<5% spread) + α→2α invariance are the tests.

## 5. Deliverable shape + outcome bins (freeze in the prereg, not here)

Research doc + frozen prereg + driver + result per the Chern-arc pattern. Indicative bins the prereg must sharpen: **[R ≠ 1, forced, knob-invariant, enantiomorph-consistent → mint the clm-, upgrade register §2.4 UNBUILT → DERIVED-CHORD, then and only then open a bench-concept gate through the BenchModel spine]** / **[R ≡ 1 within resolution → the winding-force chord falsifies; register row books the negative; the corpus keeps the S1 conservation result untouched]** / **[R ≠ 1 but knob-dependent or α-riding → ECHO, demote honestly]** / **[ill-defined (e.g. near-yield pair states don't stabilize) → named blocker, no verdict]**.

**Cost note (token/compute):** this is an engine campaign of the S1/Chern class — expect a multi-run arc (pair seeding + T⁰ⁱ + sweeps), heavier than the Chern computation, lighter than the eigensolve epics. Worth one dedicated implementor arc with the frozen-prereg discipline; not a side quest.

## 6. What success and failure both buy

If R ≠ 1 and forced: AVE's first **echo-free quantitative** forward prediction — a pure number QED cannot produce, in principle bench-reachable (torsion-balance class), and the register's headline changes. If R ≡ 1: the winding-force chord dies cleanly by the framework's own hand, the register thins further, and the bankable frontier concentrates fully on the EM-photon channel — which is itself decision-relevant for where the facility effort goes. Either outcome moves the program; only not-running-it doesn't.
