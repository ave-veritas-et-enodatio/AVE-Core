# Lane brief — the pre-tension derivation (Arm 1's charter) — HELD until the linearity audit returns

**Goal (prove-or-disprove level):** derive the vacuum's standing scalar operating
offset A₀ from the srs rest-geometry, and its consequences. Charter (framing note
§11, Grant's leans verbatim there):

1. **PINNING:** derive A₀ from the lattice's own geometry (the screw structure /
   bond bow) — the built-in-potential analog. Frozen hypothesis: the orphaned
   u₀* ≈ 0.187 (magic angle) and arc* (standing bow) and A₀ are ONE geometric
   fact. Symmetry gate: 432 permits only scalar/pseudoscalar offsets (NO-TWIST) —
   any directional built-in is a derivation error.
2. **THE LINEAR COUPLING:** show the small-signal slope at A₀ supplies the gravity
   sector's linear-in-ε₁₁ coupling with the RIGHT magnitude — the imported /7
   coefficients re-read as measurements of A₀. Zero-new-constants grammar.
3. **ORIGIN + EVOLUTION (falsifier-class):** the quench story (residual stress at
   the freeze; self-equilibrating; reconcile with vol6 — never parallel-mint) and
   the creep rate: derive the relaxation rate from A₀ + lattice constants and
   compare H₀ (re-retrieve at freeze). REPRODUCES / WRONG-SCALE(state it) /
   NOT-DERIVABLE.
4. **THE WAVE LEDGER:** adiabatic stretch bookkeeping (redshift energy into the
   tension books; the ρ_Λ row's mechanism candidate — stated, not asserted).

**Fences:** NO SSB machinery (potential-minimization, order parameters, Goldstone
narratives = the import tells — the walk's C-fence); NO polarization/piezo
machinery (blocked by NO-TWIST); cosmology-seduction fence (C10-C11: frozen
criteria before any number; vol6 reconciliation mandatory); the orphan-marries-
orphan guard (C12: u₀*/arc* equality must come FROM the geometry derivation, not
from mutual back-fit); SVA §0; freeze-first; two-method receipts;
[DO-NOT-MERGE]; own Tier-2.

---
**DATED PRIOR-ART CONSUMPTION LIST + BRIEF CORRECTIONS (2026-08-10 sweep):**

**The headline: Arm 1 is the corpus's oldest unfinished thread, not a new idea.**
The quench origin is Grant's own 2026-05-15 hypothesis, ALREADY MECHANIZED in canon
(trampoline-framework:95-125 — rotating-frame freeze-in, u₀ = ρΩ²_freeze r²/(2K₀));
and canon ALREADY derives G from the pre-tension (T_EM = n·k₀·d·u₀·(K4);
G = c⁴/(7ξT_EM)) — charter item 2's "the /7 coefficients as measurements of A₀"
has a canonical precursor. Charter item 3's origin half therefore NARROWS to the
delta: the tempered-glass SELF-EQUILIBRATING-FIELD framing + the Prince-Rupert
stability/V_snap link (new) — cite the freeze-in, derive the delta.

**MANDATORY CONSUMES:** (1) Q-G47 sessions 1-19 (AVE-QED/docs/analysis/) —
SUPERSEDED-ROUTE, method reusable; session 6 already ran
pre-stress-modifies-stiffness (compressive T₀ = −k·d·u₀) aimed at the dead K=2G
target; note its COMPRESSIVE sign vs the #527 channel-keyed rule. (2) The u₀*
retraction of record (research/2026-06-14_magic-angle-provenance...md — ECHO,
do-not-refill; constants.py:22-25) — AND the corpus's queued closure route
(claim-quality.md:499, an Ω_freeze LANDAU MINIMIZATION) is EXACTLY what this
brief's SSB fence bans: that queued path is hereby EXPLICITLY VACATED by this
brief, not silently ignored. (3) The buckling arc (#459/#460): the √ projection is
DERIVED — do not re-derive; inextensibility is K=2G-FORBIDDEN as a free premise.
(4) The #518→#527 pre-stress tensor arc: the k_s → k_s + T/ℓ remap is bit-exact
machinery — consume it; open items this lane could close: the #521 residual + the
δ_y normalization. (5) **The biased-tensor scoping (research/2026-08-02, GRANT-
RATIFIED "smells like DC op point") — the nearest living ancestor**: bare K<0 at
ρ_bond=1 stabilized by DC operating-point pre-stress, with a DERIVED
σ₀ = 0.176776695. ⚠ THE THREE-NUMBER GUARD (C12 sharpened): u₀* = 0.187
(asserted/back-fit), σ₀ = 0.1768 (pilot-derived), arc* (form-derived) — three
ADJACENT numbers, NO derived identity among them; conflation without derivation is
the orphan-marries-orphan failure. (6) The residual-stress row ruled CLEAN
2026-07-20 (translation-materials.md:26 — elastic pre-stress is Ax3-native; the
PLASTIC/STZ side is the standing rejection: cite the carve, stay elastic). (7)
NO-TWIST is a theorem (research/2026-08-05_srs-twist — cite, don't re-prove; the
O(q²) gradient channel is the quantified loophole). (8) The sector-ownership
PENDING-GRANT question (claim-quality.md:893 — which channel u₀* enters through)
— deliverable 2 implicitly answers it: SURFACE it, don't silently resolve.

**CORRECTIONS TO THIS BRIEF:** (a) the cosmology pointer is vol3 (ch04/ch05) +
vol9 ch12 — NOT vol6 (orchestrator error). (b) The H₀-creep deliverable gains two
reconciliation obligations: canon's expansion mechanism is node-CRYSTALLIZATION
(new nodes; a discrete LC network cannot stretch) — creep-of-existing-bonds vs
creation-of-new-nodes are different mechanisms wearing one "relaxation" word,
reconcile-or-compete explicitly; and canon HAS a derived H∞ =
28π·m_e³·c·G/(ħ²α²) ≈ 69.32 km/s/Mpc tied to the u₀* operating point — the creep
rate must REPRODUCE it or state the conflict. (c) The wave ledger competes with
the mechanism of record (dark energy = latent heat of crystallization;
translation-cosmology.md:17 already attributes the phantom EOS to "lattice
pre-stress") — reconcile-or-compete, per C10. (d) "Creep" dynamics inherit the
#951 D1 hole (the bias has no derived equation of motion): scope creep as
quasi-static or inherit the debt knowingly. (e) NO PRIOR REJECTION of elastic
pre-tension exists (4 candidate hits inspected, none a rejection) — no
challenge-a-negative posture needed; the corpus trend is repeatedly-proposed,
ruled-CLEAN, never-derived-to-a-value. The lane's job is to finish the thread.
