# Lane brief — the I_ω(A) law (FLAG-IOMEGA) (2026-08-06)

**Goal (prove-or-disprove level):** derive, or bound, the amplitude dependence of the
rotational microinertia I_ω — the unwritten law flagged as FLAG-IOMEGA in the
approach-leak lane (see the #903/#904 result docs' flag blocks), on which the p=2
knife-edge verdict (FORK-3(b)'s rotational shadow) hinges.

**SVA §0 header (leaf v0.2, 11 rows) MANDATORY on the prereg. Freeze-first. PR opens
`[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.**

## The question

Does I_ω depend on the local strain amplitude A_ε, on the rotational amplitude A_κ, on
neither, or on both — and with what functional form? Derive from the substrate's own
structure (the srs control net, Wyckoff 8a, the A-008-pinned gap identity G_c/I_ω = 1),
not by convention import.

## Stakes, pre-registered

- **The p=2 knife:** the leak lane's mass-cancellation verdict flips on the I_ω(A) form
  (mass enters as x^(1/p−1/2), cancels iff p=2). Rule 11 applies — the knife's frozen
  criterion is untouched by this lane; you deliver the law, the knife's verdict follows
  mechanically.
- **The ontology commitment (context, NOT a target):** the un-audited rotation-substance
  walk (`research/2026-08-06_rotation-substance-ontology_framing-note.md` §3) predicts
  I_ω independent of strain amplitude. Do not steer toward it; if the derivation lands
  I_ω(A_ε) ≠ const, that is a walk-falsifier and gets reported as one — the lab
  falsifies its founder's leans on equal terms.

## Fences

- Mode/regime/phase-state declared before any claim (cold crystalline, sub-yield, unless
  the derivation itself crosses a boundary — then declare the crossing).
- A-008's G_c/I_ω = 1 is PINNED by ruling (#895 record) — consume it as a constraint,
  cite the record; if the derivation contradicts it, STOP and route (do not re-tune).
- Two-method receipts, named engines, on any corpus-absence claim ("no I_ω(A) law
  exists" is itself an absence claim — receipt it first).
- Engine reads are receipts about the CODE, not about the substrate (the engine carries
  I_ω as a constant parameter — that is the gap being filled, not evidence of constancy).
