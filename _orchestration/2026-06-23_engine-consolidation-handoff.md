# ORCHESTRATION HANDOFF — Engine-Consolidation Lane

**Created:** 2026-06-23 · **Role:** separate orchestration lane · **Posture:** refute-by-default, deflate-then-document
**Merge model:** you produce **reviewed PRs**; you do **NOT** self-merge. The originating orchestrator (a separate Claude session) adversarially audits each diff; **Grant merges.** Surface every framing-level physics question as a Grant-call inline — do not decide it.

You have **no memory of the originating conversation.** This brief is self-contained. Verify every corpus pointer with grep against current `origin/main` before relying on it.

## 0. What this lane is
You run the engine-consolidation follow-ons that a 6-agent "engine-map + derived-vs-constrained ranking" workflow opened (2026-06-23, verify HIGH, no value-over-reach found). **Five deliverables (§3)** — D1-D4 are reviewed PRs; **D5 is a refute-by-default research inspection** (the quaternion / left-right-derivative cross-check). The ledger in §2 is your baseline — **re-verify it, do not trust it.**

## 1. Context
AVE = vacuum modeled as a saturable K4-TLM lattice with Cosserat-Beltrami flux. Standing meta-finding: **AVE forces FORMS, imports VALUES**; the calibration set is {m_e, α, G} = 3, CI-gated (`manuscript/ave-kb/common/interlock-register.md:11-13,47`). The recurring failure mode the project keeps catching is **clean-structural over-claims** (ranking something derived whose *value* is actually echoed/fitted/imported) — guard against it hard.

## 2. The ledger (engine-map result — your baseline; re-verify each line)
Seven tiers, most-derived → least:
- **T1 AXIOM-FORCED (forms only; axioms posited ≈ SM positing its gauge group):** the 4 axioms; S(A)=√(1−(A/A_yield)²) kernel (clm-8nkvwy 0.85); Op5 K4 scattering S_ij=0.5−δ_ij (exact unitary); Op3 reflection Γ; tetrahedral-gradient stencil FORM; K4 scatter/connect linear update.
- **T2 DERIVED, α-FREE:** κ̃=pq/(p+q)=6/5 (topological); ξ_topo=e/ℓ_node (clm-rppigm 0.90, "no fitting"); Z₀-as-LC-impedance (clm-kezk9z 0.90, value SI-definitional); spin-½/g=2 (0.70); ξ_K2/ξ_K1=12, ℓ_c/ℓ_node=√6.
- **T3 DERIVED-CONDITIONAL (forced GIVEN a posit/import):** exact charge Q=Link in ℤ (**C.3 two-integral reconciliation OPEN — Beltrami helicity returns ~18% of p·q**); sin²θ_W=2/9; α_s=α^(3/7); proton m_p/m_e (clm-mnb3lt 0.63, rides imported m_e+α + chosen Skyrme ansatz + Gaussian flux-tube); baryon ladder (~2% forward); **finite-by-construction/no-renormalization (genuinely T1-strong, via the Axiom-4 UV regulator).**
- **T4 CONSISTENCY-CHECK (reproduces, not forward):** BCS B_c(T); A-034 cross-scale catalog (0.62); EW masses/CKM/PMNS (0.5–5% vs data); H_∞ (self-consistency identity); p_c=8πα; three-impedance network + per-DOF node circuit ("predict no α/m_e value").
- **T5 IMPORTED/MIXED:** m_e (definitional input scale, ℓ_node=ℏ/m_e c circular); K=2G / ν_vac=2/7 (GR-imported trace-reversal, PR#261 closed the crystalline route negative); G (MIXED — Achromatic-Lens /7 form derived, ξ back-solved from CODATA G, circular); V_yield/E_yield (existence=chord, √α value=echo).
- **T6 ECHO (substrate doesn't select the value):** **α three-route α⁻¹=4π³+π²+π (clm-0ktpcn 0.63 — 22 dependents, the single highest-leverage shaky claim, all 3 lift-routes closed-negative)**; δ_strain magnitude (0.55).
- **T7 FITTED/ASSERTED/REFUTED (floor):** nuclear mass-defect (headline 0.0000% are fitting tolerances); optical-activity magnitude (demoted #374/#376); Sagnac=proton-eigenvalue (0.20, "do not build"); spin-statistics THEOREM + Pauli (ASSERTED/provisional — distinct from the derived spin-½ VALUE); chirality-as-phase-polarity (0.10, REFUTED).

**SPINE** (survives any reframing): the four axioms + the operators they force exactly + the two α-free derived results (κ̃=6/5, ξ_topo) + finite-by-construction. **NOT the Master Equation** — it is clm-efo113 solidity 0.50, EFT-leading-order, "use as input only" (it drops the ∇ε·∇V gradient term; saturated-regime confinement rides that being negligible-by-symmetry, asserted).
**SOFT EDGES:** every value that anchors a magnitude. Critically, **K=2G is GR-imported, so most "forced dimensionless ratios" (Weinberg, α_s, CKM, PMNS) are downstream of an imported value.** The AVE-distinct CHORD lives ONLY in the untested forward predictions.

## 3. Deliverables (each a reviewed PR; refute-by-default before landing)
**D1 — CANONICALIZE THE LEDGER.** Write a "state-of-engine derived-vs-constrained" ledger doc — the cross-cutting tier view that no single leaf has (it complements `claim-quality.md`'s per-claim solidity with the spine/edges landscape). Include the 7 tiers + spine/soft-edges + the form-vs-value framing, AND the **two operators the map omitted but that load-bear ranked results: Op21 universal_quality_factor Q=ℓ** (`universal_operators.py:932` — load-bears the α Q_tank mode-counting, the canonical α close-route) **and Op20 universal_regime_eigenvalue ω=ℓ·c_wave/r_eff** (`universal_operators.py:911` — generates the baryon ladder + BH ring-down). Propose the home (a KB `common/` leaf vs `_orchestration/`); placement is a Grant-call. Refute-by-default before landing; do NOT promote anything above its value-evidence tier.

**D2 — LAND THE THREE ADJUDICATION FLAGS.**
- (a) **A-034 count 19-vs-26:** `universal_operators.py:36,41,84` say 19 saturation instances; `full-derivation-chain.md:927,942,951` + `CLAUDE.md:73` say 26. Reconcile to the true count, fix the inconsistent sites.
- (b) **√(3/7) "torsion-shear" label:** algebraically √(3/7)=√(1−2ν) at ν=2/7 is the **BULK/dilatational** signature, NOT torsion-shear. `_SIN_THETA_W_PAT` (the engine constant) is unflagged but the **KB leaves carry the OPEN-FLAG** (`lepton-spectrum.md:29`, `q-g27-muon-cosserat-saliency.md:34`, `spontaneous-symmetry-breaking.md:38`). This is a **physics-labeling Grant-call (flag-don't-fix)** — surface the label-vs-physics resolution, do not unilaterally relabel.
- (c) **predictions.yaml congruence:** it tags 24/36 entries `type: derived_prediction` INCLUDING P01 α (an adjudicated ECHO) and P07 proton (rides fitted m_e+α). The "type" field is NOT congruent with the interlock chord/echo axis — an "AVE derives 24 values" headline would over-claim. Reconcile (re-tag the echoes/fits, or add an explicit chord/echo column cross-linked to the interlock register).

**D3 — STALE-IMPORT SWEEP (maintenance).** `simulate_geodynamo_vca.py` was **silently broken at HEAD** — it imported the renamed `GRAVITATIONAL_CONSTANT` (canonical name is `G`) and, being in no verify gate, rotted unnoticed (already repaired in PR #377). Sweep `src/scripts/` + `src/ave/` for renamed-constant imports (`GRAVITATIONAL_CONSTANT` and any other drift from `constants.py`); fix the broken ones; recommend adding orphan scripts to a smoke/verify gate so they can't silently rot.

**D4 — SCOPE (do NOT solve) the owed derivations.** The four tracked closure-gaps (`mathematical-closure.md` Outstanding Rigour Gaps): (1) m_e/ℓ_node Nyquist-independence circularity; (2) flux-tube Gaussian-ansatz (profile posited, not derived); (3) H_∞/G Chain B′ (derive G independent of R_H — 0 closed-form candidates); (4) δ_strain magnitude (closed-negative). Produce a **tracked scoping** (what would close each + the difficulty), NOT a research attempt. These are research-hard; flag, don't try to solve in this lane.

**D5 — QUATERNION / LEFT-RIGHT-DERIVATIVE INSPECTION (refute-by-default; a cross-check, NOT a new chord; research doc, not a code rewrite).** AVE already carries the quaternion ALGEBRA (grep: Clifford 111 files, Pauli 94, quaternion 19, biquaternion 7) but does NOT use the **non-commutative left/right quaternion DERIVATIVE operator** (grep `left.*derivative` = **0 files**) — the one tool from the Maxwell-quaternion-revival lineage that AVE skipped. Lineage: Maxwell's original quaternion *Treatise* (he wanted to "leaven" it with quaternions, died first) → Conway/Silberstein biquaternion-Maxwell → **P.M. Jack** (arXiv math-ph/0307038): the non-commutative product gives DISTINCT left- and right-multiplied derivatives; standard Maxwell silently picks ONE ordering, which is exactly what gauges the scalar away. Keeping BOTH orderings forces a **seventh scalar — the "Temporal Field" T = ∇·A + (1/c²)∂φ/∂t** (the Lorenz-gauge quantity normally set to zero) — to appear dynamically. → Dunning-Davies/Norman (JMP 2020) and **Stephen J. Kennedy, UMass Dartmouth** (viXra 2312.0075; SciRP/JHEPGC 2025) extend it (Kennedy: Lorenz-scalar ∝ gravitational potential → a gravity-EM unification ansatz).

INSPECT, refute-by-default:
- **(i) Does the operator FORCE the V-sector?** Recast AVE's EM / master-equation in (bi)quaternion / Pauli-quaternion form and keep BOTH derivative orderings. Does the longitudinal scalar — AVE's **V-sector / "the 3" / the Gibbs-Heaviside-excised grade** — fall out FORCED, and does that scalar match the **substrate-dilatation V the engine already carries**? If yes → an independent operator-algebra route to the V-sector = a derivation-tightening / cross-check (could raise the V-sector's solidity from posited-physical to operator-forced). If it merely reproduces what AVE already posits → say so honestly (no new content). [Jack's T is the EM-continuum analog of the V-sector; the question is whether AVE's substrate route and the quaternion-operator route AGREE.]
- **(ii) Is left≠right the natural CHIRALITY operator?** Non-commutativity is intrinsically a handedness — assess whether the left/right derivative is a cleaner operator language for AVE's chiral sector (writhe / optical activity) than the local-bond Bloch stencil that proved writhe-blind in Phase-1. **HONEST GUARD:** this would NOT change the Phase-1 optical-activity verdict (the bulk g₀ is buried ~40 OOM, continuum unsettled — formalism doesn't move that); its value is a cleaner chiral operator + a cross-check, NOT a bankability fix. Do not over-claim.
- **(iii) RESEARCH the standard tools** this sits inside, and which map onto AVE's open needs: **Fueter regularity** (the rigorous left/right-regular quaternion-function calculus = the formal home of Jack's left/right derivative); **biquaternion = SL(2,C)** (the spinor / Lorentz structure — relevant to AVE's carrier/spin sector); **Clifford geometric calculus** (the geometric derivative + grade systematics — AVE uses Clifford the *algebra* but not the grade-*calculus*); the **Riemann-Silberstein vector** F = E + iB.

**CRANK-CHECK (essential, both directions).** The Kennedy/Jack lineage wraps this operator in over-unity "clean energy," superluminal signaling, and reactionless-propulsion conclusions (viXra/SciRP venues, Thunder Energies Corp). **TAKE THE OPERATOR, REJECT THE CONCLUSIONS** — AVE's V-sector is lossless-reactive (Axiom-3, no energy extraction), its longitudinal mode is a medium-stiffness phase property (NOT an FTL *signal*), and it keeps EM and gravity as SEPARATE impedance channels (NOT one unified quaternion field, which is Kennedy's ansatz). Symmetric-standard: equally, do NOT dismiss the operator as crank — it is algebraically sound and Maxwell's own intent; the crank is the over-claim layer bolted on top, not the scalar-grade operator underneath.

**Deliverable:** a refute-by-default research doc with the verdict — (i) V-sector-forced / reproduces-posit / no; (ii) chiral-operator-cleaner / not; (iii) which standard tools are worth importing — plus a **gated canon recommendation ONLY if** it genuinely tightens the V-sector derivation or yields a cleaner canonical chiral operator (otherwise document the honest "cross-check, no new content"). This is an inspection + finding, NOT an engine code rewrite.

## 4. Standing gate
Every claim/landing passes a **refute-by-default** adversarial audit, not a verify-pass. Deflate-then-document. Symmetric-standard both ways (peer-with-SM honesty; don't over- or under-claim). Over-reach watch (above) applies to D1 ranking especially.

## 5. Constraints (standing, non-negotiable)
- `main` PROTECTED; **NO self-merge.** Self-isolate git-mutating work in a `/tmp` worktree off `origin/main` (workspace root is not a git repo; AVE-Core is one level down).
- **NEVER put the substring `build` in a worktree/branch name** (trips `predictions_manifest_validator.py:136`).
- verify-before-cite (re-confirm every file:line against current HEAD; pins drift as files grow). Run the local verify/precommit gate before committing.
- PURE-AVE-CORPUS (physics only — no investors/funds/external context). audit-trail-in-git: do NOT edit `_archive`, `*_FROZEN` preregs, SESSION_STATE, or result/walk-back docs.
- `ave-canonical-source` (import constants from `src/ave/core/constants.py`, never hard-code). Flag-don't-fix for physics-labeling judgments (D2b).

## 6. Check-in / audit interface
Produce reviewed PRs (one per deliverable, or coherently bundled), each marked "for review — orchestrator audit + Grant merge pending." Surface all Grant-calls in the PR bodies. The originating orchestrator will adversarially audit each diff before Grant merges — write so that audit is easy (clear per-fix provenance, honest self-flagging of anything ambiguous).

## 7. Reference anchors (re-verify against HEAD)
- `manuscript/ave-kb/common/claim-quality.md` (per-claim solidity), `interlock-register.md` (calibration count + chord/echo axis), `mathematical-closure.md` + `full-derivation-chain.md` (axiom→derivation chain + the four closure-gaps).
- `src/ave/core/universal_operators.py` (Op20 :911, Op21 :932, the operator catalog), `src/ave/core/constants.py`, `predictions.yaml` (the 24-derived count), the A-034 catalog (Backmatter Ch.7).
- `src/scripts/vol_3_macroscopic/simulate_geodynamo_vca.py` (the repaired stale-import exemplar).
- **D5 anchors:** grep `Clifford` / `Pauli` / `quaternion` / `biquaternion` (already-present algebra) vs `left.*derivative` (= 0, the gap); the V-sector / "the 3" leaves (master-equation.md, the no-QED-longitudinal-scalar material); P.M. Jack arXiv math-ph/0307038 (Temporal Field T); Kennedy viXra 2312.0075 + SciRP/JHEPGC 2025 (external, fringe-adjacent — cite the *operator*, not the conclusions). Math-research targets: Fueter regularity, biquaternion=SL(2,C), Clifford geometric calculus, Riemann-Silberstein.
