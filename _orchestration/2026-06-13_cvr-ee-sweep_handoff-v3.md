# Documentation orchestrator handoff v3 (CURRENT) — 2026-06-13

> Supersedes v2 on the FOUNDATION and on WS-T (see `_orchestration/2026-06-13_cvr-ee-sweep_auditor-handoff-v2.md`,
> review section A+B — now RESOLVED). WS-1…WS-5 unchanged (shipped, audit-clean). Persisted per auditor request.
> **Orchestrator: executing WS-T (additive-only) on the (c)=two-"3"s footing Grant ratified.**

## Foundation correction (Grant-ratified) — the electron is TWO orthogonal "3"s, not "a branch"
- Two-"3"s ([master-equation.md:20](manuscript/ave-kb/vol1/dynamics/ch4-continuum-electrodynamics/master-equation.md:20),
  verified verbatim): the **A1 dilatation-MASS "3"** (translational/E sector; Heaviside-excised longitudinal
  compression scalar; `mₑc²` = trapped acoustic compression energy) **carrying** the **Cosserat (2,3) WINDING-charge
  "3"** (microrotational/B sector; T2 couple-stress; charge = Beltrami helicity `H_bel=∫ω·(∇×ω)`). "TWO DISTINCT
  objects, orthogonal (A1 ⊥ T2)."
- **SECTOR (physical, gauge-invariant)** = which "3"/DOF: mass-dilatation (E) ⊥ charge-winding (B); observable via
  *which sector loads* (CLAUDE.md:60). **GAUGE (NOT observable, §6.1)** = for a given wall, Z→0-vs-Z→∞ and the
  Γ-sign = which side you measure (Möbius Z↔1/Z; gauge-invariant content |Γ|=1, Z_eff→∞; "legacy Γ=−1" = inside).
- Mapping: trampoline-sheet / stretch / **CVR H(s) = the mass-dilatation "3"**; window-blind / bow / microrotation
  = the **charge-winding "3"**. "One spring, two DOF" = the two "3"s, NOT two impedance branches.
- 🔴 RULE: never wire the winding into the breather's phasor (V_inc, V_ref) — V_ref is a read-only projection of
  the same scalar V; wiring = the genesis-24/crystal w_pol=0 double-count. **Window-blind leaf stays SEPARATE from
  the CVR H(s)/Smith leaf.**

## WS-1…WS-5 — done, audit-clean (no rework). Recommend push for PR.
Used the inside-convention Γ=−1 (Z₀√S→0), which §6.1 confirms canonical; carried the sector as an open flag (the
conservative survivor). The foundation correction forces NO rework. At merge, the one physics item = the
clm-ir8h78 channel-split walk-back (auditor recommends ratify; orthogonal to gauge, §6.1 strengthens it).

## WS-T — revised scope (additive-only, ave-walk-back + Rule-12; ONE commit)
1. **Window-blind figure + leaf** = the Cosserat charge-winding "3" (cite master-equation.md:20 + Axiom 1
   CLAUDE.md:55). Bow DOF of the spring, orthogonal to stretch (sheet/mass). "Two DOF" = the two orthogonal "3"s,
   NOT "two branches / Z→0-vs-∞."
2. ~~Correct §3.2~~ **DROPPED** — §3.2's Z₀/√S→∞ is the gauge/outside reading, not a bug (§6.1 harmonizes). Leave untouched.
3. ~~Edit clm-i4p11y~~ **DROPPED** — standing Grant "do NOT edit". Propagate via referencing leaves only.
4. **Cite §6.1 gauge-duality** as the reconciliation of the Z→0/∞/Γ-sign spread (gauge, not branch).
5. **Cross-link** sheet (mass-"3") ⟷ blind (charge-"3") as two orthogonal objects, explicitly NOT wired (no-phasor rule).
6. **Optional WS-1 touch-up** (non-blocking): cvr-reflection-smith.md — §6.1 cite (Γ=−1 inside-gauge, Z→0/∞ Möbius-dual,
   not a branch) + recontextualize FLAG-2 (μ-vs-C partly the two gauge sides; physical sector = the two-"3"s).

## Audit gates (carry-forward + new)
prior gates + (a) no leaf treats Z→0/∞ or Γ-sign as a physical branch (gauge, §6.1); (b) two-"3"s orthogonality —
winding kept out of the CVR phasor; (c) window-blind leaf structurally separate from CVR H(s)/Smith; (d) §3.2 +
clm-i4p11y untouched.

## Branch
Continue on `analysis/2026-06-13-cvr-ee-sweep-doc` (WS-T = 7th commit); main protected; chirality needs K4-TLM/srs
(cubic averages it out).
