# Lane brief — the 𝒜_g derivation (R46 DERIVE-FIRST) (2026-08-10)

**Goal (prove-or-disprove level):** adjudicate the frozen hypothesis
**𝒜_g = c·ℓ_node²** (c a pure number) for Axiom 5's grade-coupling area
(u₀ = −𝒜_g∇ε₁₁), BEFORE the constant enters any register. If derived, 𝒜_g is
core-derived (ℓ_node = ħ/m_ec) and is never an import.

**SVA §0 header; freeze-first (the hypothesis + the consumer set freeze BEFORE any
number is computed); frozen criteria verbatim; [DO-NOT-MERGE]; own Tier-2.**

## The frozen test (the over-determination port, executed)

From the ratified clause G and the internal relation B = 7·𝒜_g·GM/c², compute 𝒜_g
independently from EACH receipted consumer: (1) the dress normalization (the canon
ε₁₁ = 7GM/c²r profile + the clause-G bridge against the canon dress amplitude);
(2) the halo added-mass row (the bulk near-field store, port-register:35 family);
(3) the backreaction.py solve's normalization (the κ chain). THREE independent
𝒜_g values. Verdicts: **DERIVED(c)** — all three agree at a pure number × ℓ_node²
(state c exactly and its geometric origin candidate); **CONSISTENT-UNVALUED** —
they agree with each other but not at ℓ_node² scale (𝒜_g is real, one value, not
core-derived → the interlock/count adjudication fires); **OVER-DETERMINATION-FAILS**
— the consumers DISAGREE: BC-SRC's internal falsifier has fired; route to Grant
immediately (this is the axiom's own test port doing its job).

## Fences

The ratified axiom text travels verbatim; regime/phase declarations (R45's
comprehensive-map doctrine applies — state which phase each consumer reads);
two-method receipts, named engines; no comparator tuning; the engine codes the
solve (stencil-lens — backreaction.py's normalization is a CONSUMER here, not an
adjudicator); create-on-append + measure-then-edit hygiene.
