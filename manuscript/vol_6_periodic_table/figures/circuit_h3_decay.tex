\documentclass[tikz,border=10pt]{standalone}
\usepackage{circuitikz}
\usepackage{xcolor}
\usepackage{amsmath}
\usetikzlibrary{arrows, decorations.pathmorphing, shapes.symbols}

% [2026-08-02 KNOT DISAMBIGUATION per manuscript/ave-kb/CLAUDE.md INVARIANT-N1
%  (knot / trefoil disambiguation clause), verbatim: "the electron's **real-space body** is the
%  $0_1$ **unknot** ... Rolfsen names ($3_1$ trefoil, $5_1$ cinquefoil, ...) and '$(2,q)$ torus
%  knot' labels refer to **phase-space winding portraits** on the bond-pair LC tank (Clifford
%  torus), **not** real-space body knots. Never write 'trefoil electron' without an explicit
%  *phase-space* qualifier". The annotation below attached $3_1$ to an electron STORED WITHIN and
%  EJECTED FROM a node -- a real-space body statement, the exact form the invariant forbids.
%  Prior wording (git trail): "The secondary $3_1$ (electron) topology stored within the Neutron
%  node is ejected as transient energy dissipation, converting the node to a stable Proton."
%  Same rewrite pattern as the ratified vol4 pilot (#827; at HEAD the three compliant vol4 sites
%  are manuscript/vol_4_engineering/chapters/14_particle_decay_spice.tex:53, :75 and :133).
%  UNTOUCHED and CORRECT: the $6^3_2$ labels on the Proton / Neutron nodes -- INVARIANT-N1 names
%  the $6^3_2$ Borromean linkage as the proton's REAL-SPACE body. No decay claim, threshold,
%  coupling or geometry is changed.]
%
% [2026-08-02 HOUSE-STYLE RESTYLE, dark -> WHITE, under the standing ratified figure-style
%  ruling. Single source of truth: src/ave/viz/README.md:1-3 -- "# `ave.viz` -- the shared AVE
%  house figure style ... The single source of truth for manuscript / Vol-9 datasheet figure
%  appearance" -- and :15, "style.apply()  # white-background 'print' profile (the default)";
%  :36, '"print" = white bg / black text (manuscript default)'; :37, the semantic Okabe-Ito
%  colourblind-safe palette. This file previously carried \definecolor{darkbg}{RGB}{15,15,15}
%  with a \fill over the whole canvas plus a saturated neon palette (cyan / spring-green /
%  red / magenta / yellow), i.e. the dark screen profile shipping into a print manuscript --
%  exactly the drift README.md:46-49 records ("the corpus had drifted to 30x dark_background
%  rasters shipping into a print manuscript").
%  WHAT CHANGED: background fill removed (white), all text black, and the five neon colours
%  replaced by the COLORS dict of src/ave/viz/style.py:54-59 verbatim. CONTENT IS UNCHANGED --
%  identical nodes, identical coordinates, identical edges, identical labels, identical decay
%  physics. Colour is not load-bearing here (every node carries a text label), per the
%  ave-figure-discipline "never rely on colour alone" rule.
%  SCOPE: this file only -- correct at the time, and now superseded; see the 2026-08-05 note
%  below for the sweep that followed and for a correction to the two counts once stated here.
%  NOTE: this standalone is currently an ORPHAN -- no chapter \includegraphics its PDF
%  (`grep -rn circuit_h3_decay manuscript/` returns only this file), so the restyle produces no
%  page or Overfull delta in `make vol6`. The tracked companion PDF is regenerated alongside.]
%
% [2026-08-05 COUNT CORRECTION + SWEEP STATUS. The scope sentence above originally read
%  "23 of the volume's 29 hand-authored TikZ standalones (22 figures/*.tex + 7 circuits/*.tex)
%  still carry darkbg". BOTH load-bearing numbers were wrong, and the parenthetical was
%  ambiguous. Re-derived 2026-08-05 against the pre-sweep tree, two methods:
%
%  (1) 22, NOT 23. The true pre-sweep count was 22 of 29 -- 21 of the 22 figures/*.tex and 1
%      of the 7 circuits/*.tex (that one being circuits/circuit_c12.tex). The "23" came from a
%      BARE SUBSTRING grep, which counts THIS FILE: the 2026-08-02 restyle note above quotes
%      the string \definecolor{darkbg}{RGB}{15,15,15} inside a comment while describing its own
%      removal, so the file reports itself as dark. The grep was self-confirming. Any recount
%      MUST strip LaTeX comments first (drop from the first unescaped %); a bare
%      `grep -l darkbg` and even `grep -l 'definecolor{darkbg}'` both return 23 and are both
%      wrong. Comment-stripped: 22. The parenthetical "(22 figures + 7 circuits)" was the
%      breakdown of the 29 TOTAL, not of the darkbg subset, but sat where it read as the latter.
%
%  (2) NOT "hand-authored" -- overwhelmingly MACHINE-GENERATED. Of the 22 dark standalones, 19
%      are emitted by src/scripts/vol_6_periodic_table/circuits/generate_all_semiconductor_circuits.py
%      and only 3 are not (circuits/circuit_c12.tex, figures/circuit_be8_decay.tex,
%      figures/circuit_dt_fusion.tex). Method: md5 of each file's first 21 lines against the
%      generator's emitted PREAMBLE, from a sandbox run of the generator -- 19/19 match. Within
%      figures/*.tex specifically the split is 19 machine-generated of 21 dark. This mattered
%      operationally: restyling the .tex without the generator would have been reverted by the
%      next regenerate, so the 2026-08-05 sweep went generator-first.
%
%  SWEEP OUTCOME (2026-08-05). The generator was whitened and its output regenerated; the 13
%  print-visible generator-emitted figures and the canonical circuits/circuit_c12.tex were
%  restyled to the white house style. 8 of the 29 standalones still carry darkbg in code, and
%  every one is deliberate: 2 are the standing R6 carve-outs (circuit_be8_decay = PLACE-NEW,
%  circuit_dt_fusion = KEEP, per research/2026-06-07_figure-audit-ledger.md); 1 is
%  figures/circuit_c12.tex, retired strike-don't-delete under R5 and intentionally left dark
%  because it is not a build source; and 5 (b11, be9, he4, li7, n14) are STOPPED pending a
%  provenance ruling -- their shipped PDFs are LIGHT and are builds of the circuits/ tree, so
%  rebuilding them from these dark figures/ sources would swap the printed artwork for a
%  different figure entirely, not recolour it.]
\begin{document}
\begin{tikzpicture}[>=latex']

% --- AVE house figure style: WHITE print profile, Okabe-Ito colourblind-safe palette.
%     Single source of truth: src/ave/viz/README.md:1-3 ("the shared AVE house figure style ...
%     The single source of truth for manuscript / Vol-9 datasheet figure appearance") and :15
%     ("style.apply()  # white-background 'print' profile (the default)"). Hex values are the
%     COLORS dict of src/ave/viz/style.py:54-59 verbatim -- ave #0072B2, comparison #D55E00,
%     data #000000, accent #009E73, muted #7F7F7F.
\definecolor{aveblue}{HTML}{0072B2}      % COLORS["ave"]        -- the AVE object under study
\definecolor{avevermillion}{HTML}{D55E00} % COLORS["comparison"] -- rupture / ejection channel
\definecolor{aveaccent}{HTML}{009E73}    % COLORS["accent"]     -- intact reactive couplings
\definecolor{avemuted}{HTML}{7F7F7F}     % COLORS["muted"]      -- annotation frame

% Background: white (house print profile). The prior dark fill is removed rather than recoloured.

% Title
\node[text=black, font=\bfseries\Large] at (0, 4) {Tritium ($\beta^-$-Decay Transient Failure)};

\tikzset{
    proton block/.style={
        draw=black, thick, fill=white,
        circle, inner sep=2pt,
        minimum size=1.5cm,
        text=black, font=\bfseries, align=center
    },
    neutron block/.style={
        draw=aveblue, thick, fill=white,
        circle, inner sep=2pt,
        minimum size=1.5cm,
        text=black, font=\bfseries, align=center
    },
    electron/.style={
        draw=avevermillion, thick, fill=white,
        circle, inner sep=1pt,
        minimum size=0.8cm,
        text=black, font=\bfseries, align=center
    },
    bus/.style={
        draw=aveaccent, thick, dashed
    },
    rupture/.style={
        draw=avevermillion, ultra thick, decoration={zigzag, segment length=4pt, amplitude=2pt}, decorate
    }
}

% Tritium Nucleons (1 Proton, 2 Neutrons in an unstable triad)
\node[proton block] (P1) at (0, 2) {Proton\\$6^3_2$};
\node[neutron block] (N1) at (-2, -1.5) {Neutron\\$6^3_2 + e^-$};
\node[neutron block] (N2) at (2, -1.5) {Neutron\\$6^3_2 + \text{?}$};

% Intact Couplings
\draw[bus] (P1) -- (N1) node[midway, left=5pt, text=aveaccent] {$M_{pn}$ (Stable)};
\draw[bus] (N1) -- (N2) node[midway, below=5pt, text=aveaccent] {$M_{nn}$ (Tension)};

% Rupturing Coupling (Beta Decay specific)
\draw[rupture] (P1) -- (N2);

% Rupture Explosion Star on N2
\node[starburst, fill=white, draw=avevermillion, line width=1pt, inner sep=2pt, font=\bfseries\tiny, text=black] at (1.5, 0.5) {OVERLOAD};

% Ejection Path
\node[electron] (E) at (4, 1.5) {$e^-$};
\draw[->, avevermillion, thick, dashed] (N2) -- (E) node[midway, sloped, above, text=avevermillion, font=\small] {Dielectric Rupture / Ejection};

% Beta Decay Anti-neutrino representation
\coordinate (Nu) at (5, -1);
\draw[->, aveblue, thick, dashed] (N2) -- (Nu) node[right, text=aveblue] {$\bar{\nu}_e$ (Reactive Back-EMF)};

% Annotations
% text width widened 10cm -> 13.5cm and the anchor lowered -4.5 -> -5.2 so the (longer)
% INVARIANT-N1-compliant sentence does not collide with the $M_{nn}$ bus label. Layout only.
\node[text=black, text width=13.5cm, align=center, draw=avemuted, dashed, inner sep=8pt] at (0, -5.2) {
    \textbf{Tritium Beta-Minus ($^3\text{H} \rightarrow ^3\text{He} + e^- + \bar{\nu}_e$)}\\
    The geometric asymmetry of 1 Proton + 2 Neutrons creates an unstable LC loop.\\
    Capacitive tension in the overarching node cluster exceeds the structural $V_{yield}$ limit.\\
    The secondary electron topology stored within the Neutron node---a $0_1$ unknot body carrying the $(2,3)$ Clifford-torus winding in \emph{phase space} (the $3_1$ trefoil is that phase-space portrait, not a real-space body knot)---is ejected as transient energy dissipation, converting the node to a stable Proton.
};

\end{tikzpicture}
\end{document}
