# Pass fixture — PREREG (FROZEN)

## §5 discriminator (frozen)

The frozen discriminator is `ρ_N → 1 (roughly constant <1)` = coarse-grained
texture vs per-core cage survival.

## §2 impedance bins (frozen)

Frozen band edges on `r_Z`: `r_Z ≤ 0.5 (short) / 0.5 < r_Z < 2.0 (matched)` /
`r_Z ≥ 2.0 (open)`. Both labeled criteria in the result doc are byte-present
here verbatim.
