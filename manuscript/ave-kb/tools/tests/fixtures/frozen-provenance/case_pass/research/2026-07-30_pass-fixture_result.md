# Pass fixture — RESULT (byte-identical frozen criterion; must NOT fire)

Prereg-file: research/2026-07-30_pass-fixture_prereg-FROZEN.md

A gating-dated (2026-07-30) result doc whose every `Frozen:`-labeled criterion
appears byte-identically in the prereg. The gate must return ZERO gating
findings on this doc — the negative control proving the lint is not a
false-positive machine.

> **Discriminator — Frozen:** `ρ_N → 1 (roughly constant <1)`
> **Impedance edge — Frozen-band:** `r_Z ≤ 0.5 (short) / 0.5 < r_Z < 2.0 (matched)`
