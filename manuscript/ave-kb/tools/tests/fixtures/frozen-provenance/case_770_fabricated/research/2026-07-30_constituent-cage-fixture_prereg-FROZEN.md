# Constituent-cage fixture — PREREG (FROZEN)

## §5 — Leg 5 discriminator (frozen)

The frozen Leg-5 discriminator is: `ρ_N → 1 (coarse-grained texture, BIN-1) vs bounded <1/falling (per-core cage, BIN-2)`.

`S_RAIL` is evaluated at `0.03` only in this scoping prereg; no rail-depth
ladder or robustness-across-depth claim is frozen here. (This absence is the
whole point of the PR#770 regression: the result doc's "ROBUST across rail
depth ..." string has no byte-preimage in this file.)
