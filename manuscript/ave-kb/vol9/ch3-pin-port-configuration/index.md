[↑ Vol 9: The Vacuum Datasheet](../index.md)

<!-- kb-frontmatter
kind: index
subtree-claims: []
subtree-experiments: []
-->

# Ch.3 Pin/Port Configuration

Chapter 3 of the Vol 9 datasheet documents the substrate's external-interface specification — how the substrate presents at its internal impedance discontinuities. Every interior boundary between two regions of distinct local $Z$ is a substrate "port" with reflection coefficient $\Gamma = (Z_2 - Z_1)/(Z_2 + Z_1)$ (Op3). Axiom 3 (Minimum Reflection Principle) is the substrate-native action principle: the substrate minimizes $|\Gamma|^2$ at every internal impedance boundary. Engineering observables — $S_{11}$, return loss, VSWR, peak-power-transfer matching — are the EE-projection of the same minimization (per `clm-eemap1` EE-as-substrate-native META framework). The substrate is natural; engineering characterizes; AVE substrate-physics derives.

The chapter content is **Class B/C synthesis** per `consistency-vs-emergence` v1.3 — no new substrate-physics primitives are introduced; the content consolidates the canonical Op3 / Op17 / Op21 leaves + CLAUDE.md INVARIANT-S2 Axiom 3 + the SYM-class vs ASYM-class scaling distinction (`clm-8nkvwy` / `clm-3zz0f6` / `clm-hp7nlm`) into datasheet Pin/Port-Configuration format.

## Synthesis content

- **Per-cell DOF/mode inventory (the cell's internal mode content).** The seven per-cell kinematic modes — 3 translational $\mathbf{u}$ ($\to \mathbf{E}$), 3 microrotational $\boldsymbol{\omega}$ ($\to \mathbf{B}$, $T_2$), 1 volumetric $A_1$ breathing — with the Pythagorean total $A^2 = \varepsilon^2 + \kappa^2 + V^2$ (canonical at `common/trampoline-framework.md`:198, :239–241, :332). K4 four-port valence $V_{4\text{-port}} = A_1 \oplus T_2$ (photon $= T_2$ alone; electron $= E \otimes T_2$ hosting the $(2,3)$ knot). Per-port wave content: ONE independent wave per bond-port ($V_{inc}$; $V_{ref}$ **derived** by the TLM split, `master_fdtd_phasor_bridge.py`:16–17 implements it — **adjudicated as A36** ($V_{ref}$ not independent state; the joint $(u,\omega,V_{inc},V_{ref})$ Hessian was retracted as an A36 Rule 6 violation because $V_{ref}$ is fully determined by $V_{inc}$ once the TLM operator is fixed, `research/2026-05-18_abcd-handoff-prereg-outcome-corpus-state-retrofit.md`:62,68) — and **not** "Op5" which is Y-to-S, `operators.md`:45). Carries three explicit disambiguations: (i) the $A_1$ breathing **mode** $\neq$ the saturation **state** $A$ (`07_saturation_characteristics.tex` §Operating-Point State: "not a seventh spatial DOF"); (ii) the K4 **bond-port** $\neq$ the $\Gamma$-**port** (connectivity element vs impedance-boundary figure-of-merit); (iii) the $A_1$ dilatation "3" (mass) $\neq$ the $T_2$ winding "3" (the $(2,3)$ poloidal number) — orthogonal irreps.
- **Sector-to-sector coupling matrix** over $\{\mathbf{E}(u), \mathbf{B}(\omega), V(A_1), A(\text{sat})\}$. Conservative $\mathbf{E}\leftrightarrow\mathbf{B}$ Faraday/Cosserat $\sigma^A$ coupling (`trampoline-framework.md`:208); $\kappa_{chiral} = \alpha\cdot(6/5)$ chiral bias (6/5 **derived** from the $(2,3)$ winding `cosserat_field_3d.py`:89; $\alpha$ **calibration-input** per the one-parameter framing, PR #147); the **structural zeros** (no $\omega\to V$ source — genesis GAP-1, measured $\max|V_{inc}|=0$); the saturation back-reaction row ($\varepsilon_{eff}=\varepsilon_0 S$, $\mu_{eff}=\mu_0 S$, $c_{eff}$ via Op14/Op16). The chiral Beltrami **shear$\leftrightarrow$bulk** cell now carries the **`graft-v3` verdict** (2026-06-09 adversarial panel, demoted B→C; `research/2026-06-09_crystal-graft-v3_result.md` Rule-12 addendum @ `4627651a`): handedness / **sign-selection DEMONSTRATED** (exactly force-free, $\cos(b_\lambda,\nabla\times b_\lambda)=\pm1.000$; $H_{bel}$ RH $+76.3$ / LH $-90.5$ / centro $-8.5\times10^{-15}$, flips with the imposed $\chi$, clean centro null — but $\chi$ is a source INPUT, not carried from a photon); **topology-selection REFUTED** (no $(2,3)$ self-assembles, $w_{pol}\equiv0$; the geometry-templated variant **detonates**, $E_\omega=3.15\times10^{27}$, $\cos=0.19$); the conserved **LOCK UNIMPLEMENTED** ($|\mathbf{L}_\omega|$ pumps without saturating, **chirality-independent** — the $\chi=0$ centro arm pumps $|\mathbf{L}_\omega|$ to $347.7$ vs RH $18.0$ / LH $0.20$); **$\chi$-from-photon OPEN** (in flight on `analysis/2026-06-10-graft-v4-photon-helicity`). Do NOT wire it as an adjudicated coupling. The flag-gated Lagrangian-EMF reciprocal (the EMF assignment in `_compute_emf_per_port`, `k4_cosserat_coupling.py`, flag `use_lagrangian_emf_coupling=False` by default) is **off-by-default**. 🔴 **UPDATE (2026-06-21, Rule 12 — the NON-CONSERVATIVE tag below was the preserved record; this is the adjudicated successor):** the prior NON-CONSERVATIVE tag rested on the $E_V\to6.8\times10^8$, `reverses=False` detonation — but that was a **sign-wiring artifact**: the source was wired $+2\,V_{inc}\,\partial L_c/\partial V_{sq}$, opposite to the derived/docstring $-2$ Lenz back-EMF (doc 67_ §13.6). With the corrected $-2$ (the reaction opposes the drive) the deep-saturated genesis-24 seed has its **$V$-sector energy bounded and `reverses=True`** ($E_V$ peaks near $12$ and unwinds to $\approx5$; $v_{secular}<1$; the smoke dE_V is now negative ⇒ verdict C1). So the **$V$-sector runaway is a sign artifact, NOT intrinsic non-conservation**. (Scope: the $-2$ removes the $V$-sector detonation; the full three-part ledger does not *fully* close on that run — $|\mathbf{L}|$ still transiently spikes to $\approx43$ before relaxing rather than staying pinned, $H$ drifts $\approx-7\%$ — so this is a $V$-sector-energy fix, not a demonstrated full energize-LOCK.) The flag nonetheless stays off-by-default for an **independent, sign-INDEPENDENT** reason: on small-amplitude mixed-mode it double-counts the Op14 varactor $C_{eff}(V)$ (doc 67_ §14.1–§14.4), where **both** signs diverge. (Preserved prior record: tagged NON-CONSERVATIVE / off-by-default on the $E_V\to6.8\times10^8$, $|\mathbf{L}|$-unbounded, `reverses=False` evidence under the $+2$ wiring; the in-code comment then describing the positive sign as oscillatory was the bug, now corrected in the `_compute_emf_per_port` assignment to the $-2$ result.) 🔴 **UPDATE (2026-06-20, Rule 12 — the graft-v3 verdict above is the preserved empirical record; this notes the successor result):** the conserved **LOCK** graft-v3 left UNIMPLEMENTED is **now realized by a skew-Hermitian circulator GENERATOR (PARTIAL)** — a *rotation* not a *potential*, so norm-preserving by construction (escapes the $|\mathbf{L}_\omega|$ pump). It conserves AND transfers (100 %, on the winding); the residual is the 2-port skew is RECIPROCAL and genuine chiral non-reciprocity needs the 3-port loop with an IMPOSED magnitude (echo). See the 🔴 UPDATE at [`device-circuit-models.md`](device-circuit-models.md):201 + PR #321. ($\chi$-from-photon remains OPEN; the circulator imports $\chi$ as the lattice handedness, it does not derive the non-reciprocity magnitude.)
- **Substrate boundary semantics.** Op3 $\Gamma = (Z_2 - Z_1)/(Z_2 + Z_1)$ as the substrate's native single-port figure-of-merit (scale-invariant across 14 orders of magnitude per Vol 1 Ch 6 §6.3). Axiom 3 Minimum Reflection as the substrate-native action principle that engineering practice has measured for over a century under different EE names ($S_{11}$ minimization, return-loss maximization, VSWR $\to 1$, peak-power-transfer matching).
- **Port class taxonomy.** SYM-class (gravity-class realization; $Z_0$ invariant; α exactly invariant per `clm-3zz0f6`) vs. three ASYM subclasses: ASYM-N(ε), ASYM-N(μ), and **Cosserat-Curie thermal-mode-population ASYM** (`clm-hp7nlm`, landed 2026-05-28; the substrate-mechanism class for $\delta_{strain} \approx 2.225 \times 10^{-6}$ at $T_{CMB}$).
- **Boundary-saturation conditions table.** $\Gamma = -1$ (saturation TIR / Pauli exclusion / particle confinement; Op21 $Q = \ell$ endpoint), $\Gamma = 0$ (matched impedance; Op17 $T^2 \to 1$ endpoint), $\Gamma = +1$ (open-circuit / less common at substrate primitives).
- **Operator-level port behavior.** Op17 ($T^2 = 1 - \Gamma^2$, Vol 1 Ch 6 §1.16) and Op21 ($Q = \ell$ per Nyquist-cell-resolved confined mode at $\Gamma = -1$) as the two endpoints of substrate $\Gamma$-space — substrate-mechanically complementary (open-boundary energy transfer vs closed-boundary energy quantization).
- **Engineering translation table.** Standard EE port quantities ($S_{11}$, $|S_{11}|^2$, return loss, VSWR, $Z_0$, port admittance $Y$) mapped to substrate-mechanism inputs of Axiom 3 + Op3 + Op17 + Op21 per the `clm-eemap1` EE-as-substrate-native identity statements (not approximations).
- **Device circuit models (§Device Circuit Models, 2026-06-11).** Canonical leaf: [`device-circuit-models.md`](device-circuit-models.md) — `AVE_VACUUM_CELL`, electron $\Gamma_{\mathrm{bulk}}=-1$ LC tank, three-channel boundary port, cascaded cell string, operating-point coefficient table. LaTeX figures are **renders only** (`manuscript/vol_9_vacuum_datasheet/figures/circuit_*.tex`).
- **Per-DOF node-constitutive layer (2026-06-19).** Canonical leaf: [`per-dof-vacuum-node-circuit.md`](per-dof-vacuum-node-circuit.md) — the reactive pair $(L_i,C_i)$ made a **constitutive TENSOR** (one per translation DOF), the node layer BENEATH the scalar `AVE_VACUUM_CELL` (§1) and the graded vacuum impedance network (device-circuit-models.md §6). One node-constitutive structure derives THREE behaviors: isotropic saturation → achromatic+isotropic ($Z=Z_0$, $\Gamma=0$); deviatoric strain → birefringence FORM; high-$k$ → the matter $(q\ell)^2$ zone-edge AND photon $(q\ell)^4$ cubic-symmetry anisotropy **[🔴 photon-$(q\ell)^4$ DEMOTED→CONDITIONAL on the unproven weak-C theorem, 2026-06-25: the P1b genuine chiral-srs Bloch eigensolve gives the photon band-edge anisotropy at slope-2, not 4 — see per-dof-vacuum-node-circuit.md §3.3 + clm-k4d4ph]**. Class-C CONSISTENCY re-expression; predicts NO $\alpha/m_e$. CODE: `src/ave/core/vacuum_node_circuit.py`.
- **Two-domain N-port + Fork-A coupling resolution (2026-06-20).** The node is a **two-domain N-port**: an EM (electrical, $\Omega$) sub-network that is the substrate's **SOLE external** radiative port ($\Gamma_{\mathrm{EM}}=0$, $Z_{\mathrm{EM}}\equiv Z_0\approx376.73\,\Omega$) + a **mechanical** (Rayl) 2-port — shear (charge-"3", Cosserat $(2,3)$) and bulk (mass-"3", A1 dilatation, $Z_{\mathrm{bulk}}=\sqrt2\rho_b c_0$ at $K=2G$), both confined ($\Gamma\to-1$) and coupled through the conserved $H_{\mathrm{couple}}$. The EM ($\Omega$) and mechanical (Rayl) domains carry different units, so they are joined by a **TRANSDUCER**, not a direct wire: an **ideal transformer** (turns-ratio change-of-reference, **NOT a gyrator**). $\alpha$ **localizes to exactly the transducer turns-ratio** — the $p_c=8\pi\alpha$ packing-fraction residual, isolated and visible at the transformer, not smeared across the network (driver [`node_2domain_nport.py`](../../../../src/scripts/vol_9_device/node_2domain_nport.py) header lines 84–89; $p_c=8\pi\alpha$ canonical at [`topological-packing-fraction.md`](../../vol3/gravity/ch01-gravity-yield/topological-packing-fraction.md)). The transformer node is `def-tk1xfm`, status **proposed** (gated, flagged not asserted). **Fork-A resolution** (driver [`node_circulator_coupling.py`](../../../../src/scripts/vol_9_device/node_circulator_coupling.py), verdict PARTIAL): a conservative shear$\leftrightarrow$bulk skew coupling EXISTS (no pump / not isolation / not inert; 100 % helicity transfer), but the **2-port skew is RECIPROCAL** = the optical-activity **gyrator** (`def-0pt1ac`), NOT a one-way router; genuine chiral non-reciprocity needs the **3-PORT RING** (EM/photon as the 3rd leg, loop phase $3\chi\theta_\chi$ flips with $\chi$); the non-reciprocity **magnitude is IMPOSED** (cubic-FDTD averages chirality out) ⇒ **ECHO at the magnitude**; directional-motion$\to$mass is **NEGATIVE**. $\alpha$-free throughout ($\tilde\kappa=6/5$, $\theta_\chi=2\pi\nu_{\mathrm{vac}}$). Full content: [`device-circuit-models.md`](device-circuit-models.md) §6.1, §6.5. LaTeX renders the N-port netlist + Fork-A 3-port ring as circuitikz/TikZ figures (`fig:vol9_node_2domain_nport`, `fig:vol9_node_circulator_ring`); the drivers emit JSON only.
- **Vacuum-node IM3 / harmonic-distortion datasheet (2026-06-24).** Canonical leaf: [`vacuum-node-im3-distortion.md`](vacuum-node-im3-distortion.md) — the node's **nonlinear-reactance spectral signature** datasheet (the χ⁽³⁾ kernel-Taylor source $C(V)/C_0=1+\tfrac12A^2+\tfrac38A^4$, the cubic-in-drive IM3 law, the field-dependent index shift δn(E)) beneath the multi-port LC node. **SCALING RESOLVED: δn is $E^2$-leading, NOT $E^4$** — the historical "$E^4$" was a $\sqrt\varepsilon$ conflation (permittivity DEPTH vs index $n=\sqrt S$), RETRACTED clm-pp3qwf. The $E^2$ exponent and cubic IM3 are **SHARED with QED** (non-falsifiers; symmetric standard). The bankable δn table (AVE $-\tfrac12A^2$ vs QED $+\tfrac{3}{45}\alpha^2$, ratio $7.5/\alpha^3\approx1.93\times10^7$, sign-flip) is an **α-ECHO** at the value level. The one parameter-free AVE-distinct content — the **E-vs-B keying asymmetry** (static-B exactly transparent, $S_\mu=1$) — is surfaced as a **chord CANDIDATE with a clean falsifier** (the static-B null PVLAS already returns discriminates nothing until ~$10^{-23}$), NOT a confirmed/bankable chord. Class-C CONSISTENCY characterization; predicts NO new value. CODE: `src/scripts/vol_9_device/im3_vacuum_harmonic_distortion.py` (validate-on-known, HALT-on-mismatch). Provenance: `research/2026-06-24_e4-im3-vacuum-distortion.md`.
- **Node-scatter multiplicity bedrock (2026-06-20).** Canonical leaf: [`node-scattering-multiplicity.md`](node-scattering-multiplicity.md) — the valence-distinct node scatter $S_n=(2/n)J-I$ assembled on the **REAL** srs/diamond CONNECT map (srs deg-3 = **192 DOF** vs diamond deg-4 = **64 DOF** — genuinely distinct, fixing the dense-`TETRA_OFFSETS`-cube collapse `graded_vacuum_network.py` hardwires; first `build_srs_net` solver call-site). Bare spectrum $\{+1$ common, $-1\times(n-1)$ differential$\}$; four validate-on-known anchors ($n=3$ differential mult $=2=$ photon transverse polarizations; winding $\mathcal{Q}=3$; $\alpha$-free). **Honest scope:** the Fork-A "longitudinal needs the 3rd differential mode" prediction was **REFUTED** — but it is a projector-algebra **sector-orthogonality FACT** (the A1 scalar IS the $+1$ common mode by construction, scramble-invariant), NOT a discriminating chord; the 2-vs-3 transverse count is an **ECHO** of DOF-counting (peer-to-SM rep-theory). Class-C; predicts NO $\alpha/m_e$. CODE: `src/ave/solvers/node_scattering_multiplicity.py`. Result: `research/2026-06-20_node-scattering-containment-gate_result.md` (PR #304).

## Primary canonical sources

| Source | Content |
|---|---|
| CLAUDE.md INVARIANT-S2 (Axiom 3) | Minimum Reflection Principle: substrate-native action; the substrate minimizes $|\Gamma|^2$ at every internal boundary |
| `common/operators.md` (`clm-sysqaf` / `clm-gdd70j`) | Universal 22-operator catalogue; Op3 reflection-coefficient row; Op17 power-transmission row; Op21 quality-factor row |
| `vol1/operators-and-regimes/ch6-universal-operators/reflection-coefficient.md` (`clm-gdd70j`) | Op3 canonical-leaf; $\Gamma$ scale-invariance across 14 orders of magnitude |
| `vol4/circuit-theory/ch1-vacuum-circuit-analysis/op21-multi-mode-mode-counting.md` (no-claim leaf; strengthens `clm-0ktpcn`, `clm-rtdmsn`) | Op21 substrate-foundational form $Q = \ell$ at $\Gamma = -1$; single-channel vs substrate-orthogonal-channel mode multiplicity |
| `vol3/cosmology/ch05-dark-sector/delta-strain-cosmic-tcc.md` (`clm-hp7nlm`) | Cosserat-Curie thermal-mode-population ASYM subclass; the third ASYM substrate mechanism beyond canonical SYM/ASYM |
| `vol3/gravity/ch01-gravity-yield/alpha-invariance-symmetric-gravity.md` (`clm-3zz0f6`) | SYM-class α-invariance proof — load-bearing for the SYM-class port classification |
| `common/translation-tables/translation-circuit.md` §2 (`clm-eemap1`) | EE-as-substrate-native META framework; engineering-translation table is identity, not approximation |

## Canonical leaves (this chapter)

| Leaf | Role |
|---|---|
| [`device-circuit-models.md`](device-circuit-models.md) | Device equivalent circuits + port semantics (**source of truth** for §Device Circuit Models) |
| [`per-dof-vacuum-node-circuit.md`](per-dof-vacuum-node-circuit.md) | Per-DOF node-constitutive layer — reactive tensor $(L_i,C_i)$ per translation DOF BENEATH the scalar `AVE_VACUUM_CELL`; one circuit → three behaviors (isotropic achromatic / deviatoric birefringence FORM / high-$k$ $(q\ell)^4$ cubic anisotropy — **photon-$(q\ell)^4$ horn DEMOTED→conditional-on-weak-C, P1b eigensolve gives slope-2**). Class-C CONSISTENCY re-expression (**source of truth** for the per-DOF node layer) |
| [`node-scattering-multiplicity.md`](node-scattering-multiplicity.md) | Valence-distinct node scatter $S_n=(2/n)J-I$ on the REAL srs/diamond CONNECT map (192 vs 64 DOF; fixes the `graded_vacuum_network` `TETRA_OFFSETS` collapse); bare spectrum $\{+1, -1\times(n-1)\}$ + four validate-on-known anchors. Fork-A longitudinal-confinement prediction REFUTED (a sector-orthogonality FACT, not a chord); 2-vs-3 transverse count = ECHO of DOF-counting. Class-C (**source of truth** for the node-scatter multiplicity bedrock) |
| [`vacuum-node-im3-distortion.md`](vacuum-node-im3-distortion.md) | Vacuum-node IM3 / harmonic-distortion / δn(E) datasheet — the χ⁽³⁾ kernel-Taylor source, cubic IM3 law, and field-index shift. δn is $E^2$-leading (the "$E^4$" was a √ε conflation, RETRACTED clm-pp3qwf); $E^2$ + cubic IM3 are QED-SHARED non-falsifiers; bankable δn ratio $7.5/\alpha^3$ is an α-echo (sign-flip vs QED); the E-vs-B keying asymmetry is a **chord CANDIDATE** (clean falsifier, not confirmed). Class-C CONSISTENCY characterization (**source of truth** for the node IM3/δn datasheet) |

## Manuscript counterpart (render)

`manuscript/vol_9_vacuum_datasheet/chapters/03_pin_port_configuration.tex` + `chapters/03a_device_circuit_models.tex` (PDF figures; cites KB leaves via `\kbleaf{}`). 03a is `\input` once from `chapters/_manifest.tex` (rendered after Ch 3); 03a §2-domain N-port + §Fork-A coupling resolution wire `figures/node_2domain_nport.tex` + `figures/node_circulator_ring.tex`.

---
