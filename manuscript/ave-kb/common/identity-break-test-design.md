[↑ Common Resources](index.md)

<!-- kb-frontmatter
kind: leaf
no-claim: "Methodology / review-instrument leaf (Grant-ratified 2026-07-13 as a corpus process input): the shape-of-the-mathematics synthesis — the corpus's boundary math has two fixed points (Γ=−1 / Γ→0), its interior collapses to identities, its nonlinearity is even (second-order-only), and its numbers come from exactly three closed value-generators. The operative content is a DESIGN RULE (a discriminating test breaks an identity, never measures a magnitude) and its reviewer corollary (the vacuous-detector knife: audit every PASS criterion for being an identity in disguise). Mints NO physics claim; every structural statement is a POINTER to a merged result / charter carrying its own claim-ids and receipts. A map over the accumulated FORM-derives/VALUE-imports record, not a new derivation."
path-stable: "the canonical identity-break test-design methodology leaf; the review-instrument companion to form-deriving-value-importing.md (that leaf states the FORM/VALUE organizing principle; this leaf turns it into a test-design + review discipline)"
-->

# Identity-Break Test-Design — the Shape of the Corpus's Mathematics as a Review Instrument

> **What this leaf is.** A **methodology / no-claim** leaf. It records the 2026-07-13
> *shape-of-the-mathematics* synthesis, **Grant-ratified as a process input** for reviewing
> and auditing the engine, so it lives durably in the corpus rather than only in session
> memory. It mints **no new physics claim**: every structural statement below is a **pointer**
> to a merged result, charter, or canonical leaf that carries its own claim-ids and receipts.
> It is the review-instrument companion to
> [`form-deriving-value-importing.md`](form-deriving-value-importing.md) — that leaf states the
> FORM-derives / VALUE-imports **organizing principle**; this leaf turns the same observation
> into a **test-design rule and a reviewer's knife**.
>
> **Honesty scope (read first).** This is the shape of the mathematics **as the corpus has it**
> — form-deriving, value-importing. Whether it is the shape of *the vacuum* or of the *corpus's
> echo* is exactly what identity-breaking **forward** tests adjudicate (see the coda). Do not
> read the five sections as a proof that the vacuum is so structured; read them as the audited
> shape of the artifact we have built, and as the reason a good AVE test looks the way it does.

---

## The one-line synthesis

> **The corpus's boundary mathematics has two fixed points, its interior collapses to
> identities, its nonlinearity is even, and its numbers come from three closed generators.
> All discriminating content therefore lives at IDENTITY-BREAKS — and every PASS criterion
> must be audited for being an identity in disguise.**

Five observations, each receipted at HEAD. Sections 1–4 are the *shape*; section 5 is the
*operative rule* that shape implies.

---

## 1. TWO FIXED POINTS, NO LADDER

The framework's boundary mathematics has exactly **two** behaviors, not a graded ladder of
reflection coefficients:

- **Perfect mismatch — `Γ = −1`.** Topologically gated, intrinsic, **`Q → ∞`**. The
  `Γ = −1` wall is a phase-transition / topological boundary (the ruptured `Γ = −1` surface,
  the electron's terminal pole).
- **Perfect match — `Γ → 0`.** Absorbing / radiating, **`Q ~ few`** (finite, coupling-set).

**Apparent intermediate structure resolves to these two, projected through sector ports.** The
sharpest receipt is the **T1 atom-Q adjudication** (merged **#668**,
[`../../../research/2026-07-13_t1-atom-q-cascade-gate_RESULT.md`](../../../research/2026-07-13_t1-atom-q-cascade-gate_RESULT.md)):
the atom's observed `~10⁷` "excited-state Q" *looked* like a distinct intermediate cascade-filter
cutoff sitting between the electron's loaded `α⁻¹` and the black hole's `Q = ℓ` (cold `a* = 0`
anchor — B1). It is not. The
result splits it across the two fixed points seen through **different sector ports**:

- the **wall channel** (longitudinal `Z_bulk` trapping walls of the graded Coulomb dress) returns 🔴 **[DEMOTED 2026-08-11 — R40-B2a: NEEDS RE-DERIVATION, not dead; dated note at the end of this file]**
  the **intrinsic endpoint** `Q_wall → ∞`, **α-free** (H(1s), H(2s), He⁺, reduced-mass leg all
  diverge; Z-/coupling-/mass-invariant to `rel < 1e-9`) — the `Γ = −1` fixed point;
- the number `~10⁷` lives only in the **transverse-EM (`Z_EM`) radiative port** — a *different*
  sector the longitudinal Hermitian eigencavity does not express — and it is the **α-echo**
  `Q_rad = 4 α⁻³` exactly (the QM value `≈ 9.6 α⁻³ ≈ 2.5×10⁷` is the measured linewidth rung).

The black-hole endpoint stays canon and **distinct** (`Q = ℓ`, Op21 at the at/above-rupture
`Γ = −1` boundary — a genuine structural cutoff, the contrast that sharpens the atom's collapse).
**[Spin-scope, Grant Ruling B1 2026-07-21 (propagated 2026-07-31; nothing struck, scope added): both
`Q = ℓ` mentions above are the BH-ringdown fire, where flat `Q = ℓ` is the **cold `a* = 0` anchor**
only — at catalog spins the flat reading fails at `D̄_Q = −38%` (corrected-Kerr Q rises 3.07 → 3.49)
and the mΩ law `ω_I = (ω_R − mΩ)/(2ℓ)` carries the spin dependence. The *endpoint-vs-atom* contrast
this section draws is unaffected: it turns on `Γ = −1` being a structural cutoff, not on the value of
`Q`. Scope banner: [`qnm-quality-factor.md`](../vol3/cosmology/ch15-black-hole-orbitals/qnm-quality-factor.md);
model banner [`vol3/claim-quality.md`](../vol3/claim-quality.md) `:204`.]**
So the "cascade filter" is the homogeneous vacuum line **relabeled** on this rung — a vocabulary
echo, not a third fixed point. **There is no ladder; there are two fixed points read through
ports.**

---

## 2. IDENTITY-DOMINATED INTERIOR

In-band / sub-yield / bulk / physical-history mathematics **collapses to identities**. The
discriminating content is not in the interior — it is at the interior's **breaks**.

**Receipts (each a merged result / charter at HEAD):**

- **Closed-box `E ≡ 1` conservation identity** (merged **#670**,
  [`../../../research/2026-07-13_genesis-npersist-n14-battery_RESULT.md`](../../../research/2026-07-13_genesis-npersist-n14-battery_RESULT.md)):
  under a reflecting boundary `E_persist ≡ 1.0000` exactly — *"a reflecting box conserves total
  energy identically, independent of localization. Not a persistence read — a conservation
  identity."* An energy-retention detector reads unity by conservation regardless of whether
  anything is localized.
- **In-band Bloch cancellation** (merged **#669**,
  [`../../../research/2026-07-13_srs-vertex-ksweep-backscatter_RESULT.md`](../../../research/2026-07-13_srs-vertex-ksweep-backscatter_RESULT.md)):
  the srs per-vertex `Γ = −1/3` tee (`|Γ|² = 1/9`, real at a single vertex) is **invisible to
  in-band collective carriers** — the periodic lattice diagonalizes the per-vertex reflection into
  its band structure, so `R_LW/R_dis = 0.12` (strongly suppressed). The junction **resolves only
  near the band edge** (`R_BE/R_LW = 3.14`). Interior carriers ride an identity (the homogenized
  effective medium, `v_φ/c = 1/√3`); the reactive event surfaces only at the edge.
- **Exact `H ↔ n_matter` degeneracy** (F6 tier-1 charter §4.7,
  [`../../../research/2026-07-13_f6-tier1-two-reservoir-ledger_CHARTER.md`](../../../research/2026-07-13_f6-tier1-two-reservoir-ledger_CHARTER.md)):
  on **physical** (FRW-locked) input histories the DE-drainage form is degenerate with the
  frontier default, and §4.7 pins the mechanism **precisely** — it is *"not soft co-monotonicity
  but the exact `3H` continuity-operator identity: `dρ/dt = −3Hρ ⇒ ρ_DE ∝ a⁻³`, the `w=0`
  matter-density form itself."* The physical run is expected degenerate; the chord can fire
  **only** on the **decorrelated** run that breaks the `a(t)` lock.
- **Install-tautology** (X36 result,
  [`../../../research/2026-07-09_x36-node-bottleneck_result.md`](../../../research/2026-07-09_x36-node-bottleneck_result.md)):
  *"The ceiling is wherever the tank is installed"* — the coupled ceiling equals the installed
  anchor `ω_r`; the engine *"returns whatever the node model installs — it does not derive the
  anchor."* A ceiling that lands at `ω_C` is **calibration-in, calibration-out (identity-class),
  NOT an emergence-class prediction.**

**Discriminating content exists ONLY at identity-BREAKS:** band edges (#669), yield surfaces,
periodicity **defects = envelopes**, **sector ports** (#668), **decorrelated inputs** (F6 §4.7).

**Correspondence with the phase-only epistemology.** This is the same fact the framework's
phase-only stance states from the other side: the bulk **self-cancels** (no direct observable in
a physical-history interior), and the signal lives only where the cancellation **breaks**. The
identity-dominated interior *is* the substrate's bulk self-cancellation seen in the mathematics;
the identity-break *is* the residual that survives the cancellation. A test that samples the
interior samples the cancellation and reads the identity back.

---

## 3. EVEN NONLINEARITY ⇒ SECOND-ORDER-ONLY SIGNATURES

The saturation kernel is **even in the amplitude**: `S(A) = √(1 − A²)`, `S(−A) = S(A)`. An even
nonlinearity **cannot rectify** — there is no first-order (odd) observable, no DC-offset from a
symmetric drive, no directed ratchet. Every signature is **A²-class** (second order). It follows
that a discriminator must be **FORM-class** — a *sign*, a *distance-power*, a *frequency
flatness*, a *parameter-free zero*, an *exponent* — and **never magnitude-class**, because the
magnitude is `(A/A_yield)²`-suppressed to unreachability at any bench field.

**Receipts:**

- **CVR requirements** (merged **#667**,
  [`../../../research/2026-07-13_cvr-requirements_DERIVED.md`](../../../research/2026-07-13_cvr-requirements_DERIVED.md)).
  `CVR-REQ-FRAME` makes the response `E²`-even and the magnitude route **dead three ways**
  (unreachable by `~20` OOM; the bench bound is `~10` OOM looser than the lattice coefficient it
  would need to detect). `CVR-REQ-BIAS` is therefore **classifier-driven, not magnitude-driven**.
  Because the response is even, the CLEAVE-01 polarity-reversal (odd) trick is **unavailable** —
  the even sag is polarity-degenerate with every even fixture systematic. The discriminator is the
  **triple form-conjunction** `{negative-sign ∧ d⁻² ∧ flat-in-f}` (chord slope `−1/2`, tangent
  slope `−3/2`, `1/√2` NDC point), no one of which any single classical mechanism survives — a
  parameter-free FORM object, not a magnitude.
- **The diode / rectifier "four deaths"** (F6 charter §(iv),
  [`../../../research/2026-07-13_f6-tier1-two-reservoir-ledger_CHARTER.md`](../../../research/2026-07-13_f6-tier1-two-reservoir-ledger_CHARTER.md)):
  a one-way valve is dead four ways because the even kernel cannot rectify — (1) *"`S(A)=√(1−A²)`
  is instantaneous, even, memoryless — identical 2nd-order momentum for symmetric and asymmetric
  drive"*; (2) any true rectifying loop is Level-2 memristive `= dissipative` (contradicts
  losslessness); (3) the diode threshold `V_f` is **free, not forced** (no substrate scale gives a
  forward dead-zone); (4) chirality-ratchet-as-arrow is **refuted** ("chirality is a parity
  selector, not the arrow"). The arrow, where it exists, must come from **where the energy goes**
  (mode-count), never from a valve.

**Design consequence:** headlining an even-kernel result on a *magnitude* is a category error — it
is `A²`-suppressed and unreachable. Headline the **form**.

---

## 4. THREE VALUE-GENERATORS, CLOSED

Every number the corpus produces traces to exactly **three** generators. No fourth has ever
appeared.

| generator | owns | worked instances (receipts) |
|---|---|---|
| **Topology** | the integers | `z = 3 ⇒ Γ = (2−z)/z = −1/3`, `|Γ|² = 1/9` (#669; `translation-circuit.md:180`); the confinement counts `c = 3, 5, 7`; `Q = ℓ` (BH endpoint, Op21, #668 — cold `a* = 0` anchor, B1; ledger scope in the banner below) |
| **The kernel's algebra** | the parameter-free forms | `1/√2` tangent/NDC zero; `−3/2` vs `−1/2` slopes (tangent vs chord, #667); `√S` vs `S` (small- vs large-signal); `√(1−A²)` even shape |
| **The import basis `{m_e, α, G}`** | all the scales | with **`α`-POWERS as the sector-port ladder**: `α⁻¹` = loaded electron tank; `α⁻³` = radiative atom (#668). The powers are set by *which port* (loaded reactive vs transverse radiative), not by a new constant |

**[Spin-scope + ledger scope on the Topology row's `Q = ℓ`, Grant ruling D4 2026-08-01 (verbatim [sic]: *"D4: follow rec"*). Nothing struck, scope added; the row is TAGGED, not restructured.** This discharges the flag-don't-fix item merged **#812** audit F4 routed to Grant (*"routed to Grant: class-(a) tag here vs deferral to the #668 relabel lane"*), applying the same class-(a) treatment that sweep applied at its other sites. **(i) Spin scope (Ruling B1, 2026-07-21).** The `Q = ℓ` here is the **BH-ringdown fire**, where flat `Q = ℓ` is the **cold `a* = 0` anchor** only — at catalog spins the flat reading fails at `D̄_Q = −38%` (corrected-Kerr `Q` rises 3.07 → 3.49) and the m$\Omega$ law `ω_I = (ω_R − mΩ)/(2ℓ)` carries the spin dependence. **(ii) Ledger scope — why the filing is a weld.** `Q` is an **impedance-ledger** object (a stored-per-lost-per-radian ratio read at a port); `ℓ` is a **cavity-deformable mode count** — `def-quant3` sense **(1)** ([`vocabulary-register.md`](vocabulary-register.md)`:261` (def-quant3), sense (1) at `:271`, *"Failure mode: ionization destroys them"*) — and **not** a deformation-invariant winding, which is sense **(2)** at `:272`. So what Topology hands this row is the **integer `ℓ`**; the **ratio `Q`** belongs to the impedance ledger, and `Q = ℓ` is the Op21 mode-counting identity read at the `Γ = −1` boundary **at zero spin**. Filing the whole equality under *"Topology | the integers"* reflects the **pre-correction weld** of the two — it is tagged here, not rewritten. **The discriminator is INTEGRALITY, not sense class** (#812 audit F5): an integer can only jump discretely, while the physical `Q = ω_R/2ω_I` moves continuously with the strain profile — an integer that can only jump cannot track a ratio that moves smoothly, so the equality is a zero-spin coincidence, not a law. **(iii) THE THREE-WAY `ℓ`-HANDSHAKE — Grant ruling 3 (2026-08-02), verbatim [sic]: *"adopt and split"*; ADOPTED, and it replaces the prior (iii) (quoted verbatim below, struck nowhere, per Rule 12).** `ℓ` has **three owners, not one**, and each earlier reading held exactly one third of it. **TOPOLOGY hands the INTEGRALITY:** global **REGULARITY** of the mode on the closed 2-sphere forces `ℓ ∈ ℤ` — the associated-Legendre solutions `P_ℓ^m(cos θ)` are regular at **both poles** only for integer `ℓ`; at non-integer `ℓ` the solution regular at `θ = 0` diverges logarithmically at `θ = π`, so it is **boundedness on a closed manifold**, not phase bookkeeping, that pins the index. Nothing the cavity does can make it a non-integer. **★MECHANISM CORRECTION (2026-08-02 repair pass, Rule 12 — the clause this replaces is quoted, not struck).** This clause first read *"single-valuedness of the field on the closed 2-sphere forces `ℓ ∈ ℤ`"* — **wrong condition, right conclusion**, and the wrong condition is one the corpus uses elsewhere for a *different* integer, so the slip is not cosmetic. Single-valuedness alone forces only the **azimuthal** index `m ∈ ℤ` (`e^{imφ}` must close under `φ → φ + 2π`); it constrains `ℓ` not at all. The distinction is load-bearing **because the single-valuedness / phase-holonomy argument (`∮∇θ = 2πq`) is this corpus's WINDING-integer machinery** — [`form-deriving-value-importing.md`](form-deriving-value-importing.md)`:154` (*"single-valuedness pins the winding **integer** (`∮∇θ = 2πq`, derived)"*) and [`physics-lineage-map.md`](physics-lineage-map.md)`:92` (*"phase holonomy (∮∇θ = 2πq, LC-tank phasor single-valuedness) forces the integer FORM"*) — i.e. `def-quant3` sense **(2)**, the deformation-invariant winding. **That machinery must NOT be imported for the mode index:** doing so would smuggle sense (2) into a sense-(1) object and re-weld precisely what (ii) above unwelds, and it would let the winding row's *derived* status leak onto a row that has not earned it. `ℓ` here stays sense **(1)**. *This is why the generator table's filing was never crazy:* the `Topology | the integers` row is right about the part topology actually owns. **The CAVITY hands the VALUE:** *which* `ℓ` rings, and whether it survives, is a property of the resonator and not of the topology — mode counts are **cavity-deformable**, `def-quant3` sense **(1)** ([`vocabulary-register.md`](vocabulary-register.md)`:271`, *"Failure mode: ionization destroys them"*); deform the cavity and the spectrum moves, ionize it and the mode is gone, while the integrality survives both. **The impedance LEDGER hands `Q`:** the ratio `Q = ω_R/2ω_I` is a stored-per-lost-per-radian reading at a port — neither topology's nor the cavity's to hand. **Supersession — this SHARPENS BOTH prior readings, each of which was right about a different third.** *(a)* **#812 audit F4** — *"'Topology owns it' is the over-read"* — is right about the **VALUE** (topology does not pick *which* `ℓ` rings) and over-reaches on the **INTEGRALITY** (topology does force `ℓ ∈ ℤ`). *(b)* **This banner's own earlier (iii)** — preserved verbatim per Rule 12: *"**(iii) Section 4's own thesis is unaffected.** The row's claim is that the corpus's* integers *come from topology, and `ℓ` **is** such an integer; only the welding of the* ratio *to the integer is scoped. The generator set stays closed and stays three."* — is right about the **INTEGRALITY** and **silent on the VALUE**, the third the cavity owns. Part **(ii)** above carries the same wording (*"what Topology hands this row is the **integer `ℓ`**"*) and is sharpened identically; **nothing in (ii) is struck, moved or reworded.** **The reconciling instrument is #812 audit F5** — *"the discriminator is INTEGRALITY, not sense class"* — which is precisely what lets both readings stand: it puts the discrete-vs-continuous discriminator on the **integrality** axis (topology's third) and leaves the sense-(1) cavity-deformability (the cavity's third) doing no work in the discriminator. **§4's thesis is still unaffected, now for a sharper reason:** the corpus's *integers* do come from topology — that **is** the integrality third — so the generator set stays closed and stays three, and `Q = ℓ` stays filed under the Topology generator with the ratio scoped out, exactly as (ii) and (iv) leave it. **(iv) Scope split — what this tag does NOT do.** This site also sits on the **#668-routed Q-ladder relabel** follow-on (an auditor-lane deliverable, from the T1 atom-Q adjudication). **This tag discharges the SCOPE half only**; any deeper relabel — including whether `Q = ℓ` should be re-filed off the Topology generator altogether — is **owned by the #668 lane and is not taken here**. *(Ledger reading = orchestrator-walk provenance 2026-07-31, not canon; the sense classification is register-accurate.* ***The 2026-08-02 topology-integrality rationale in (iii) carries the SAME provenance label — orchestrator-walk, not canon.*** ***★And the topology third is itself ROUTED TO GRANT, not closed here:*** *a **box cavity** has integer mode indices `(n_x, n_y, n_z)` with **no 2-sphere anywhere** — its integrality comes from fitting half-wavelengths between walls, which is the **cavity's** doing, not topology's. So whether topology hands this row **anything** for `ℓ`, or whether the "topology" third collapses into the cavity's, is **his walk**. The three-way split as ruled is left standing and **deliberately NOT restructured** pending it.)* Scope banner: [`qnm-quality-factor.md`](../vol3/cosmology/ch15-black-hole-orbitals/qnm-quality-factor.md); model banner [`vol3/claim-quality.md`](../vol3/claim-quality.md) `:204`; the §6 `Q`-homonym row in [`theorem-thesaurus.md`](theorem-thesaurus.md) carries the same two-ledger split.**]**

The generators are **closed**: topology hands integers, the kernel hands forms, the import basis
hands scales, and `α`-powers index the port. A result that appears to need a *fourth* generator —
a new scale, a new form, a magic number outside `{integers} × {kernel forms} × {m_e, α, G}^n` — is
the tell that either an import went unlabeled or an identity is being read as a derivation. This is
the numerical face of FORM-derives / VALUE-imports (see
[`form-deriving-value-importing.md`](form-deriving-value-importing.md)).

---

## 5. THE DESIGN RULE (and its reviewer corollary)

The four observations above have one operative consequence.

> **A discriminating AVE test never measures a magnitude — it BREAKS AN IDENTITY.**

The five identity-break levers, each with a live receipt:

1. **Decorrelate imported histories** — break a co-monotone lock so the response reveals *which*
   input it tracks (F6 §4.7 decorrelated arm: the only tier-1 chord-carrier;
   [`../../../research/2026-07-13_f6-tier1-two-reservoir-ledger_CHARTER.md`](../../../research/2026-07-13_f6-tier1-two-reservoir-ledger_CHARTER.md)).
   **Update (F6 tier-1 executed, PR #674, ruled 2026-07-13 — KEEP-BOTH).** The lever's
   identity-break example **stands** — the decorrelation arms isolated *which* input `Γ` responds to
   (matter-occupancy `n_B`), i.e. decorrelated histories **did break the degeneracy** the lever is
   built to break. What is **superseded** is "*the only* tier-1 chord-carrier": the driver found the
   chord separable from the frontier default on **physical** histories too (different powers of `a`),
   and Grant's §5.4 ruling (2026-07-13, BOTH) closed the homogeneous ledger as the **wrong instrument**
   at late epochs (chord → Λ past `τ₀≈300`). The F6 CHORD therefore **relocates to the spatial
   cross-correlation channel** (DESI/Euclid); the decorrelation *design principle* stays a live
   identity-break lever regardless (`research/2026-07-13_f6-tier1-ledger-driver_result.md`).
2. **Sweep a geometric power** — fit an exponent, not a level (CVR `d⁻²` vs electrode `d⁻³`,
   `Δs = 1` at `σ_s ≤ 0.1`; #667).
3. **Cross a band edge** — move from the homogenized interior identity to the resolving edge
   (#669).
4. **Flip a boundary condition** — compare `Γ = −1` reflecting vs `Γ → 0` absorbing on the *same*
   seed (the #670 PML-vs-closed-box axis, which is *how* the E-channel vacuity was exposed).
5. **Close/open a port with a fireability control** — a positive control that *can* fire the
   distinct bin, so the null is a verdict and not an unfireable checklist (#668 Leg B returned an
   in-window `Q_control = 1.02×10⁶`, proving the `Q_wall → ∞` was physics, not an instrument that
   cannot fire).

### Reviewer corollary — the VACUOUS-DETECTOR KNIFE

> **For every PASS criterion in a PR under review, audit whether it is an identity in disguise —
> a conservation law, a calibration return, or a co-monotone form. A detector that cannot fail on
> physical inputs is a checklist, not a gate.**

Three kinds of disguised identity, each caught in the record:

- **Conservation-law-in-disguise:** the closed-box `E`-channel discriminator caught vacuous in
  **#670**'s own review — *"the boundary-axis E-channel discriminator is vacuous"* (finding #1/#3,
  `E ≡ 1.0000` mode-independent, including the structure-dead `photon_lock` seed). A retention
  gate that a conservation law satisfies for free is no gate.
- **Reach-limited equality-audit:** the F6 **IMPOSED-LEAK** conservation/destination audit owns
  its own limit in the charter — it catches only a **non-conserving, energy-destroying** transfer;
  *"an honestly-booked conserving dissipative term (`dρ=−γρ`, `dE_T2=+γρ`) is NOT caught"* because
  entropic-vs-dissipative is inexpressible in two scalars. The gate is real but bounded, and the
  bound is disclosed, not hidden.
- **Calibration-return-in-disguise:** the X36 **install-tautology** — a ceiling that equals the
  installed anchor is *calibration-in, calibration-out (identity-class), NOT emergence*
  ([`../../../research/2026-07-09_x36-node-bottleneck_result.md`](../../../research/2026-07-09_x36-node-bottleneck_result.md)).

The knife is the same one the standing review instrument already carries as
*"gates must be able to fire"* — this leaf gives it its physics grounding (the interior is
identities, so a gate sampling the interior reads an identity back) and its worked receipts.

---

## Honest coda — corpus-shape or vacuum-shape?

Everything above is the shape of the mathematics **as the corpus has it**: it **derives forms and
imports values**. The two fixed points, the identity interior, the even kernel, and the three
closed generators are audited facts about the artifact we have built. They do **not** by themselves
establish that the *vacuum* is so shaped — a value-importing echo would exhibit exactly this
signature (a self-consistent form-machine fed calibrated scales). **Which it is** — the shape of
the vacuum, or the shape of the corpus's echo — is precisely what **identity-breaking forward
tests** adjudicate: a test that breaks an identity on *decorrelated / swept / edge-crossing /
port-toggled* inputs and returns a **parameter-free FORM the import basis cannot supply** is the
only thing that distinguishes a chord from an echo. Until such a forward break lands, this leaf is
a **review instrument**, not a claim: it tells you where the discriminating content *can* live and
which PASS criteria are identities wearing a gate's coat.

---

## Cross-references — where the neighboring disciplines live

- **The vacuous-gate / reconcile-don't-declare gate discipline** — its operative home is the
  standing review instrument's `STANDING DISCIPLINE` block,
  [`.claude/workflows/ave-adversarial-pr-review.js`](../../../.claude/workflows/ave-adversarial-pr-review.js)
  (*"Gates must be able to fire: any control/reconcile that consumes its own defining identity,
  uses a vacuous tolerance, or gates a proxy instead of the consumed observable is a finding"*),
  which now carries the identity-audit knife as a standing item citing this leaf. Its
  bench-instrument twin is the **mandatory `INCONCLUSIVE` bin** of the bench-model spine
  (`src/ave/bench/model.py`, `GateStatus` / `OutcomeKind.INCONCLUSIVE`). This leaf supplies the
  *why* (an interior detector reads an identity back) behind the *"reconcile every label vs
  COMPUTED truth"* rule.
- **The regime / phase-state discipline** — canonical home
  [`substrate-native-terminology.md`](substrate-native-terminology.md) §"declare the regime first"
  (the `ave-regime-phase-state-check` discipline) over the four-regimes map
  [`../vol1/operators-and-regimes/ch7-regime-map/four-regimes.md`](../vol1/operators-and-regimes/ch7-regime-map/four-regimes.md).
  It is upstream of every section here: *which* fixed point, *which* identity, and *whether* an
  even-kernel signature can even exist all depend on the declared MODE / REGIME / PHASE-STATE — a
  null where the effect cannot exist in the declared regime is an artifact, not an identity-break.
- **The organizing principle this instrument operationalizes** —
  [`form-deriving-value-importing.md`](form-deriving-value-importing.md) (FORM = chord, VALUE =
  echo) and its AC/DC epistemological mechanism (`clm-acdc07`).

---

## R40 batch-2a — NEEDS-RE-DERIVATION status note (2026-08-11)

**Class:** status demotion under **R40**. This note mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`,
**moves no solidity number**, adjudicates no channel and opens no fork. Every byte of each demoted
claim is preserved; the stamped line gains a status marker only (honesty-lag pattern, Rule 12).

**The arc, in four clauses (R40's header form; clause 4 points at the LANDED artifact, not at a
ruling record).**

1. **The kill fired** — the walk-back that closed the bulk radiative-port reading.
2. **The premise localized to the imported `K = 2G` elastic modulus** — the compressible far-field
   branch was minted by a GR-imported modulus, not forced by the axioms.
3. **The axioms underdetermine the bulk sector** — the flat-direction finding: the written action
   conserves the Gauss function pointwise and never fixes its value.
4. **The replacement is the LANDED ratified bound-sector law — Axiom 5, Substrate DC Bias**, clauses
   **S** (deposit), **G** (bias coupling / bridge) and **Q** (quiescence), canonical at
   [`eq_axiom_5.tex`](../../common_equations/eq_axiom_5.tex) with its register entry in
   [`axiom-register.md`](../common/axiom-register.md) (§ *Axiom 5 — Substrate DC Bias*). Under
   clause **G** the A1 / bulk slot is a **bound response** — $\mathbf{u}_0 =
   -\mathcal{A}_g\nabla\varepsilon_{11}$, mechanism gloss **back-reaction** — with **no independent
   propagating branch, no port and zero longitudinal characteristic speed**. A bulk *wave speed*, a
   bulk *radiative port*, a bulk *band-branch* and a bulk *transit clock* therefore have **no
   referent**, and each row below owes its re-derivation on that footing.
   $\mathcal{A}_g$ (the **bias-coupling area**) is an `UNVALUED-RATIFIED-CONSTANT` per **R48**
   ([`interlock-register.md`](../common/interlock-register.md), § *𝒜_g — the bias-coupling
   area*): it is **not valued here or anywhere**, and **the calibration count stays 3**.

**Standing named-open debt — the honesty rider.** The ratified axiom does **not** discharge
everything. **THE BIAS PROPAGATION THEOREM is Axiom 5's standing named-open debt**, stated by the
axiom's own phase-structure paragraph, clause **(c1)**: clause G's elliptic law is the *static
abstraction of underived finite-speed bias dynamics*, and the $(u,\pi)$ no-signalling theorem does
**not** cover the bias read — the bias's finite propagation speed is *owed, not held*. Every row
tagged **⚑ BIAS-DEBT** below re-derives against the ratified axiom **with that debt standing**, never
against a closed replacement.

**Vocabulary.** Canonical nouns authored here: **the bound response** ($\mathbf{u}_0$), **the bias**
($\varepsilon_{11}$), the **DC operating point / quiescent point (Q-point)**; **back-reaction** is
the mechanism gloss. *"dress"*, *"grade"* as $\varepsilon_{11}$'s canonical noun, and *"halo"* for
the physics (the physics noun is the **near-field store / added-mass**) are RETIRED by **R50**;
*"retardation"* is retired by **R49(b)** in favour of **propagation delay / finite propagation
speed**. Corpus text quoted below is byte-exact and is never reworded.

**Rows carried in this file.**

- **`:59`** — stamped at `:59`. *(family: atom-Q-wall-channel; banked `uncertain`)*  ⚑ **BIAS-DEBT**
  Quoted claim, byte-exact at HEAD:
  ```text
  the **wall channel** (longitudinal `Z_bulk` trapping walls of the graded Coulomb dress)
  ```
  Audited rationale, verbatim from the banked worklist:
  ```text
  The RESULT (Q_wall→∞ α-free, radiation only in the transverse port) is a no-port receipt FOR the carve; the 'longitudinal eigencavity trapping' mechanism label consumes a cold-region longitudinal wave and is owed re-homing.
  ```

  **Resolution.** The demoted carrier is the propagating A1 / bulk branch; under Axiom 5 clause G that slot is the **bound response**, so the re-derivation must be re-posed on the bound-sector constitutive law (bias $\varepsilon_{11}$, bound response $\mathbf{u}_0$, mechanism gloss back-reaction) rather than on a compression wave. **⚑ BIAS-DEBT:** this row's re-derivation turns on finite-speed bias dynamics, so the resolution is the ratified axiom **with THE BIAS PROPAGATION THEOREM standing** (clause (c1)) — the replacement is *owed, not held*.

**Records.** Ruling **R40** (the demotion sweep) · the banked worklist
[`r40_sweep_worklist_verified.json`](../../../research/drivers/r40_sweep_worklist_verified.json) · batch-0
scope verification and batch-1 execution records in `_orchestration/` · this batch's record
`_orchestration/2026-08-12_r40-sweep-batch2a.md`.

