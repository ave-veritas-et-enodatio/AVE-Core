# AVE Forward-Prediction Manifest — untested by construction
#
# Structured claim graph for Tier-2 rigor. Every row of the public README and
# LIVING_REFERENCE Master Prediction Table maps to an entry in EITHER this file
# or manuscript/consistency-manifest.yaml; the validator's parity checks read the UNION.
#
# SCOPE (split 2026-08-13, Grant: "the predictions yaml should be forward only,
# we should make a postdiction yaml"). This file holds claims that predict
# something NOT YET MEASURED. Claims reproduced against an already-known value
# moved to manuscript/consistency-manifest.yaml -- that was 35 of the previous 36 rows.
#
# READ THE HARD FACT FIRST. Every clm- node in this corpus carries
# experimental_solidity: null. "Forward prediction" here means UNTESTED BY
# CONSTRUCTION -- a derived divergence no experiment has yet confronted. It does
# NOT mean "AVE is empirically ahead". Solidity figures are derivation rigor.
#
# Per README.md:41 the armed forward falsifier lives on the falsification surface
# OUTSIDE the 47-slot table, so entries here carry public_in_readme: false and
# public_in_living_ref: false. That is what keeps the ratified 47<->36 accounting
# (_orchestration/2026-07_repo-conventions.md sec.(f)) intact across the split.
#
# The split does NOT key on `calibration_role`: Grant ruled 2026-08-05 that that
# axis is value-provenance and "orthogonal to `type`". Membership is by FILE.
# Entries link back to:
#   - manuscript labels (\label{} targets) where the derivation lives
#   - constants.py symbols that carry the predicted numeric value
#   - observed values with their source
#
# src/scripts/predictions_manifest_validator.py verifies (8 checks; the script was
# renamed from claim_graph_validator.py and this header lagged it):
#   1. schema      — required fields, allowed `type`, id shape, no duplicate ids
#   2. label       — derivation_label resolves to a \label{} in manuscript/**/*.tex
#   3. engine      — constants_py_symbol resolves and matches predicted_value (rtol 1e-5)
#   4. bridge      — every entry bridges into the KB claim DAG via clm:/exp: (INVARIANT-S11)
#   5. axioms      — axioms_used equals the recomputed transitive axiom cone of the bridge
#   6. calibration_role — declared role reconciled against the bridged claim card's prose
#                    (GATING at severity=critical since 2026-08-05)
#   7. parity      — every README table row maps to an entry in EITHER manifest
#   8. lr_parity   — same for LIVING_REFERENCE
#
# ─── Entry schema ────────────────────────────────────────────────────────────
# id:                    stable identifier (P01, P02, ...) — never re-use
# name:                  short human-readable name
# type:                  classification (A1 rule, mirrors `consistency-vs-emergence` skill v1.1)
#                          derived_prediction        — genuine forward prediction (Class D)
#                          axiom_manifestation       — axiom expressed at a new scale (Class B)
#                          identity                  — definitional; 0% is trivially true (Class A)
#                          consistency_check         — reproduces a known result (Class C)
#                          operating_point_projection — Class E: observable is one of N projections
#                                                       of a single substrate operating-point;
#                                                       N observables joint-constrained, failure of
#                                                       any one falsifies the operating-point
#                          engineering_limit         — theoretical bound
# calibration_role:    (optional) value-provenance axis, orthogonal to `type`:
#                        chord  — a FORM/ratio/selection-rule AVE genuinely forces
#                        echo   — dimensionful VALUE imported/calibrated (rides {m_e,α,G})
#                        mixed  — form-derived but value rides echoes ± a fitted scalar
#                        fitted — magnitude tuned per-system to match
#                        consistency — reproduces a known result / definitional residual
#                        forward-prediction — untested, divergent-from-SM, AVE-distinct
# derivation_label:      \label{} target in manuscript/**/*.tex
# constants_py_symbol:   (optional) name in src/ave/core/constants.py
# predicted_value:       (optional) numeric value for validator to cross-check
# predicted_unit:        (optional) unit annotation
# observed_value:        (optional) measured value
# observed_source:       (optional) e.g. "CODATA 2022", "PDG 2022"
# error_percent:         (optional) |pred - obs| / obs × 100
# axioms_used:           list of axiom numbers (1-4) the derivation depends on
# public_in_readme:      true if appears in README master table
# public_in_living_ref:  true if appears in LIVING_REFERENCE master table
# notes:                 free-form explanation
# ────────────────────────────────────────────────────────────────────────────

version: 1

predictions:
  - id: P_A034_solar_flare
    clm: clm-cltls0
    name: "Solar flare LED-avalanche kernel (forward prediction; NOAA GOES 40-yr validation pending live fetch)"
    type: axiom_manifestation
    derivation_label: "ch:orbital_mechanics"
    axioms_used: [1, 4]
    public_in_readme: false
    public_in_living_ref: false
    notes: |
      [2026-06-15 LF-03 scope correction]: this entry is an A-034 FORWARD
      PREDICTION (the 0.46-yr FWHM), NOT a validated empirical anchor. Per KB
      leaf solar-flares-led-avalanche.md the NOAA GOES comparison is a
      synthesized illustrative timeline (a Monte-Carlo placeholder generated in
      fetch_and_plot_noaa_goes_flares.py), not a live catalog fetch; promotion
      to a validated anchor is pending the live NOAA GOES fetch + falsification.

      A-034 catalog stellar-scale instance (SYM symmetry class).
      Solar CME / flare onset governed by S(A) = √(1-A²) when the Parker
      spiral's accumulated topological strain crosses the critical shear
      threshold (S → 0 locally) and the topology snaps via magnetic
      reconnection. NOAA GOES 40-yr validation PENDING a live catalog fetch
      (LF-03); the 0.46-yr FWHM danger window per 11-yr solar cycle is the AVE
      forward prediction. Same universal kernel as BCS
      (P43), BH ring-down (P_A034_bh_ringdown), and cosmic K4 crystallization.
      Derivation: Vol 3 Ch 14 §"Stellar Magnetic Topology and Solar Flares"
      and Backmatter Ch 7 universal_saturation_kernel catalog.

  - id: P_biref_coefficient
    clm: clm-pp3qwf
    name: "E-route vacuum birefringence COEFFICIENT (tree-level saturation vs QED's alpha^2 loop)"
    type: derived_prediction
    calibration_role: mixed
    pre_registered: true
    research_doc: "research/2026-06-21_birefringence-coefficient-bankable-falsifier.md"
    test_file: "src/tests/test_birefringence_v3_chain.py"
    axioms_used: [4]
    public_in_readme: false
    public_in_living_ref: false
    notes: |
      THE ARMED FORWARD FALSIFIER. Carried in prose at README.md:41 as "the 1 armed
      forward falsifier (the birefringence Letter) ... outside the 47"; this row is
      the machine-readable counterpart, minted 2026-08-13 with the forward/postdiction
      split. Kill criterion pre-committed before data.

      FORM vs VALUE, per the claim card (vol4/claim-quality.md, clm-pp3qwf):
        - the CHORD is that the vacuum saturates AT TREE LEVEL -- an O(1) existence
          claim -- against QED's alpha^2-loop birefringence. Structural, two-sided,
          field-independent, reachable at HIBEF-class fields.
        - the MAGNITUDE is NOT the bankable content, and it has been re-normalized
          TWICE. The CURRENT canonical ratio is v3, on the consistent instantaneous
          footing (vacuum-birefringence-e4.md, the "delta n_AVE / delta n_QED" display):
              15*pi/(4*alpha^2) = 3.75*pi/alpha^2 ~ 2.2e5
          using E_crit = alpha^(-1/2) * E_yield, so (E_crit/E_yield)^2 = 1/alpha.
          Convention history, from that leaf: v1 = 7.5/alpha^3 ~ 1.93e7 (instantaneous
          SVE kernel against the AVE-side differenced (3/45)alpha^2); v2 = 7.5*pi/alpha^2
          ~ 4.42e5 (QED-normalized but MIXED footing); v3 is exactly half v2, the
          <cos^2> = 1/2 carrier average removed.
          ⚠ An earlier draft of this row quoted the v1 figure as "the card states it
          verbatim". That sentence is real but sits inside the card's PRESERVED
          historical note, not its live headline (the 2026-07-07 OPTION-B FOOTING
          CONSOLIDATION boxes v3). Quoting preserved history as current is the
          vacated-cite pattern; corrected here rather than carried.

      calibration_role: mixed -- NOT chord. Form derived, value rides an echo, which
      is def-fmv001's shape exactly. Declaring `chord` would re-open the axis Grant
      closed 2026-08-05, when both rows that carried it were CONTRADICTED by the
      reconciler and relabelled (P04 chord->mixed, P42 forward-prediction->consistency)
      before check_calibration_role was flipped to severity="critical".

      experimental_solidity is null, like every clm- node in the corpus. Nothing here
      has been measured. This row exists so the framework's one AVE-distinct forward
      claim is tracked by the same gate as everything else -- not to assert it.
