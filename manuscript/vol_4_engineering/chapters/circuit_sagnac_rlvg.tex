\documentclass[border=10pt]{standalone}
\usepackage{tikz}
\usepackage{circuitikz}
\usepackage{amsmath}

\begin{document}
\begin{circuitikz}[american, thick, scale=1.2]
    
    % Source (The Injection Laser)
    \draw (0, 0) node[circ] (BeamSplitter) {} 
          node[left=10pt] {\textbf{Beam Splitter}};
          
    \draw (-2, 0) to[sV, l=\text{Coherent Source ($E_0 \sin(\omega t)$)}] (0,0);
    
    % Clockwise Path (With the Earth's Rotation)
    \draw (0,0) -- (0, 2)
          to[L, l=\text{Metric Drag Delay}] (3, 2);
    
    \node[above, align=center, text=blue] at (1.5, 2.5) {Clockwise (CW) Path\\$v_{prop} = c - v_{drift}$};
    
    \draw (3,2) to[vL, l=$\mathbf{L_{\text{vac}}(+v)}$] (6, 2)
          to[short] (6,0);
          
    % Counter-Clockwise Path (Against the Earth's Rotation)
    \draw (0,0) -- (0, -2)
          to[L, l=\text{Metric Drag Advance}] (3, -2);
          
    \node[below, align=center, text=red] at (1.5, -2.5) {Counter-Clockwise (CCW) Path\\$v_{prop} = c + v_{drift}$};
          
    \draw (3,-2) to[vL, l=$\mathbf{L_{\text{vac}}(-v)}$] (6, -2)
          to[short] (6,0);
          
    % Interferometer Recombination Node
    \draw (6,0) node[circ] (Detector) {}
          node[right=10pt, align=center] {\textbf{Interferometric Detector}\\$\Delta \Phi = \oint \mathbf{v}_{\text{drift}} \cdot d\mathbf{l}$};
          
    % Rotational Indicator
    \draw[->, dashed, color=gray, line width=1.5pt] (3, 1) arc(90:-90:1);
    \node[text=gray, right] at (4, 0) {Macroscopic Entrainment Ring ($\Omega$)};

\end{circuitikz}
\end{document}

\begin{summarybox}
\begin{itemize}
    \item This chapter demonstrates the direct engineering application of continuous Sagnac Rlvg networks functioning natively within the AVE transmission line.
    \item No empirical fitting variables or Standard Model mass parameters are required; hardware geometry traces explicitly to the four macroscopic yielding limits.
    \item Hardware operation is verified by predicting exact reflection coefficients ($\Gamma$) and local lattice resonance boundaries in purely geometric terms.
\end{itemize}
\end{summarybox}

\begin{exercisebox}
\begin{enumerate}
    \item Derive the hardware fracture scale matching the rigorous mathematical limits of Sagnac Rlvg when local strain exceeds the Axiom 4 topological dielectric threshold ($\varepsilon > 1.0$).
    \item Construct a native LC structural tensor illustrating how macroscopic power cascading forces the spatial substrate into localized saturation during active Sagnac Rlvg operation.
\end{enumerate}
\end{exercisebox}
