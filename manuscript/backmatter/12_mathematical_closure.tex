\chapter{System Verification Trace}
\label{app:verification}

\textbf{Gating-clause resolution (2026-05-31, Q-EMBED-SEL-1 Phase 1+2+3).} The 2026-05-28 gating clause that made the closure contingent on ropelength-minimality embedding-selection is \textbf{RESOLVED} by the Q-EMBED-SEL-1 substrate-mechanism work (commits \texttt{66d63503} $+$ \texttt{b509767a} $+$ \texttt{ecfe9c13}; audit tag \texttt{\seqsplit{audit/2026-05-31\_q-embed-sel-1-substrate-mechanism}}; merged via PR \#59 at \texttt{7529f7ce}). The substrate-mechanism for $R \cdot r = 1/4$ now closes at \textbf{Class B substrate-mechanism manifestation} via Axiom 4 self-saturation + Op14 Meissner-asymmetric + named phasor-area-equals-Nyquist-cell-area identification (canonical at \texttt{research/2026-05-31\_Q-EMBED-SEL-1\_step\_c\_result.md} \S2.3); cross-particle universal at Phase 2; cross-domain universal at Phase 3. \textbf{All ``zero free parameters'' / ``structurally zero-parameter'' / ``absolutely zero-parameter'' framing below --- including the Layer-8 back-edges \S, Outstanding Rigour Gaps table, and the A-034 empirical-anchors \S Methodological significance --- now reads as Class B substrate-mechanism manifestation level per Q-EMBED-SEL-1, with the named substrate-canonical identification as input (not Class 2 axiom-emergence from K4 $+$ Cosserat primitives alone).} \textbf{Honest-$\alpha$ relabel (2026-06-02):} sharpened further --- $\alpha$'s value ($\alpha^{-1} = 4\pi^3 + \pi^2 + \pi$) is closed-form geometry whose \emph{scale} ($\sim 1/137$) is forced by the Compton-resonance trapping condition, but whose \emph{exact value} rests on ONE substrate-geometric identification per route ($R \cdot r = 1/4$ Golden-Torus route; $z_0 \leftarrow 1.187 \leftarrow p_c = 8\pi\alpha$ rigidity-percolation route) which the substrate does NOT independently select (four dynamic engine tests + doc-34 $S_{11}$-landscape-flatness + $z_0$ $\alpha$-circularity). It closes at Class B substrate-mechanism manifestation via a named identification that is not independently derived --- not a ``derivation'' and not a ``Zero-Parameter Closure.'' The $\delta_{strain}$ magnitude is a \textbf{definitional residual} ($1-$CODATA$/\alpha_{cold}$): the candidate magnitude derivation (Q-DELTA-MAP-1-quant, E-mode dispersion + Bose-Einstein occupation) was \textbf{attempted and closed NEGATIVE} (FT-1, 2026-05-31: $\sim$31 OOM undershoot, generic-thermal not AVE-distinct), so only the SIGN of $\alpha$-vs-$T$ running is substrate-set. All three NAMED $\alpha$-routes are now closed-negative: the two golden-torus Class 2 lift-routes (dynamical selection $+$ the phasor$\leftrightarrow$real-space area bijection per Phase 1 result \S7.3) both closed Class B 2026-06-04, and the separate $z_0$-from-K4 (rigidity-percolation) route closed-NEGATIVE 2026-06-15 via the exact $z_{\text{eff}} \to 6$ Maxwell--Calladine count (\texttt{alpha\_free\_map\_to\_137\_exists = False}). %% Rule-12 2026-06-19 z0-route propagation: prior wording "leaving only the separate z_0-from-K4 route open" superseded --- closed-NEGATIVE per research/2026-06-15_alpha-crystal-mc-count_result.md.
\emph{Scoped-echo register (Grant ratified 2026-06-18):} the $\alpha$-value is a \textbf{standing echo} --- closed-negative on all three named routes, flip-condition live ($R \cdot r = 1/4$ forced without $\alpha$-circularity would flip it to a chord), the route-space not provably exhausted. Canonical anchor: KB leaf \texttt{vol1/ch8-alpha-golden-torus.md} \S``Substrate-mechanism provenance of regime (c)'' $+$ \S``Class B caveat (honest-$\alpha$ relabel).''

The following verification log was aggregated from the AVE computational validation suite. It certifies that the fundamental limits, constants, and parameters derived in this text are calculated exclusively using exact Chiral LC continuum mechanics and rigid solid-state thermodynamic boundaries, constrained by exactly three empirical parameters.

\section*{Automated Verification Output}

\begin{verbatim}
==========================================================
AVE UNIVERSAL DIAGNOSTIC & VERIFICATION ENGINE
Dynamic Output -- Generated from src/ave/core/constants.py
(constants only; lines tagged [STATIC] below are NOT generated)
==========================================================

[SECTOR 1: INITIAL HARDWARE CALIBRATION]
> Parameter 1: Lattice Pitch (l_node):  3.8616e-13 m
> Parameter 2: Dielectric Limit (alpha):    1/137.036
> Parameter 3: Macroscopic Gravity (G):  6.6743e-11 m^3/kg*s^2
> Topo-Conversion Constant (xi_topo):     4.1490e-07 C/m
> QED Geometric Packing Fraction (p_c):  0.1834
> Impedance of Free Space (Z_0):         376.73 Ohm

[SECTOR 2: BARYON SECTOR & STRONG FORCE]
> Faddeev-Skyrme Coupling (kappa_cold):  8*pi = 25.1327
> Thermal Correction (delta_th):         1/(14*pi^2) = 0.007237
> Effective Coupling (kappa_eff):        24.9508
> Dynamic I_scalar:                      1162.0 m_e
> Toroidal Halo Volume (V_halo):         2.0
> Theoretical Proton Eigenvalue:         1836.12 m_e
> Empirical CODATA Target:              1836.15268 m_e
> Deviation:                             0.0019%
> Torus Knot Ladder Spectrum:
>   (2,5)  -> 938 MeV vs Proton (938)      0.00%
>   (2,7)  -> 1261 MeV vs Delta(1232)      2.35%
>   (2,9)  -> 1582 MeV vs Delta(1600)      1.11%
>   (2,11) -> 1895 MeV vs Delta(1900)      0.27%
>   (2,13) -> 2195 MeV vs N(2190)          0.21%
>   (2,15) -> 2478 MeV vs Delta(2420)      2.40%
> Derived Confinement Force:             160,037 N (0.999 GeV/fm)
> Baseline Lattice Tension (T_EM):       0.2120 N
> Dielectric Snap Voltage (V_snap):      511.0 kV

[SECTOR 3: COSMOLOGY & DARK SECTOR]
> Asymptotic Hubble Limit (H_inf):       69.32 km/s/Mpc
> Asymptotic Hubble Time (1/H_inf):      14.105 Billion Years
> Hubble Radius (R_H):                   1.334e+26 m
> MOND Acceleration (a_0 = cH/2pi):      1.07e-10 m/s^2
> Bulk Mass Density (rho_bulk):          7.910e+06 kg/m^3

[SECTOR 4: LATTICE IMPEDANCE & MODULI]
> Poisson Ratio (nu_vac = 2/7):          0.285714
> Trace-Reversal (K = 2G):               Exact by construction
> Weak Mixing Angle (sqrt(7)/3):         0.8819

[SECTOR 5: FDTD ENGINE STATUS]
> 3D Non-Linear FDTD:                   Axiom 4 eps_eff per cell per timestep
> Linear Mode:                           Available (linear_only=True)
> Mur ABC:                               1st-Order (6 faces)
> Total Test Suite:                       62/62 PASSED   [STATIC 2026-04-13]

==========================================================
VERIFICATION COMPLETE: STRICT GEOMETRIC CLOSURE
168/168 framework files -- zero Standard Model parameters.  [STATIC 2026-04-13]
==========================================================
\end{verbatim}

\noindent\textbf{Scope of the ``Dynamic Output'' label (2026-09-06).} Every value in the block above except the two lines tagged \texttt{[STATIC]} is emitted by \texttt{src/scripts/vol\_2\_subatomic/generate\_verification\_trace.py}, which imports them from \texttt{src/ave/core/constants.py} and substitutes them as format placeholders. The two tagged figures --- the test-suite tally and the framework-file count --- are \emph{literals inside that generator's format string}, not computed by it. Both date from the initial release (2026-04-13) and have never been regenerated; the repository has grown substantially since. They are \textbf{deliberately not refreshed here}: re-minting a new number under a label that promises it is generated would reproduce the defect rather than repair it. Retiring the promise for these two lines, and leaving the figures as a dated snapshot, is the honest state until a generator actually computes them.

\section{The Directed Acyclic Graph (DAG) Proof}
To definitively establish that the Applied Vacuum Engineering (AVE) framework possesses strict mathematical closure without phenomenological curve-fitting, the framework maps the Directed Acyclic Graph (DAG) of its derivations.

The entirety of the framework's predictive power is derived by bridging \textbf{Three Initial Hardware Parameters} with \textbf{Four Topological Axioms}.
\begin{enumerate}
    \item \textbf{Parameter 1 (The Spatial Cutoff):} The effective macroscopic spatial scale of the lattice ($\ell_{node}$). The electron mass is derived as the unknot ground-state energy: $m_e = T_{EM} \cdot \ell_{node} / c^2$.
    \item \textbf{Parameter 2 (The Dielectric Bound):} The absolute structural self-impedance of the macroscopic lattice is rigidly governed by the fine-structure constant ($\alpha$).
    \item \textbf{Parameter 3 (The Machian Boundary):} Macroscopic Gravity ($G$) acts as the structural impedance parameter defining the causal limits of the manifold.
    \item \textbf{Axiom 1 (Substrate Topology / Chiral Laves K4 Cosserat Crystal):} The physical vacuum IS a \textbf{chiral Laves K4 Cosserat crystal} --- a 3D crystallized substrate of micropolar nodes at pitch $\ell_{node}$, governed by the right-handed $I4_1 32$ chiral space group with 3-fold ($z=3$) chiral srs (Sunada-K4 / Laves) nearest-neighbor connectivity. Each node carries six intrinsic DOFs (three translational $\leftrightarrow$ E, three microrotational $\leftrightarrow$ B; the Cosserat rotational DOF IS the substrate-native origin of intrinsic spin). In the macroscopic continuum: a Trace-Reversed Chiral LC Network (Cosserat micropolar continuum). Legacy short name: \emph{LC Network} / \emph{Chiral Laves K4 Crystal}.
    \item \textbf{Axiom 2 (Topo-Kinematic Isomorphism):} Charge is a discrete geometric dislocation in the substrate with Burgers vector $\ell_{node}$, so $[Q] \equiv [L]$ and $\xi_{topo} \equiv e/\ell_{node}$. Charge quantization, sign-as-handedness, and fractional quark charges (Witten effect on $\mathbb{Z}_3$ Borromean topology) all follow.
    \item \textbf{Axiom 3 (Minimum Reflection Principle):} The substrate minimizes the boundary reflection $|\Gamma|^2$ at every internal impedance boundary (boundary form, substrate-native, names the externally-observable quantity); equivalently, it extremizes $S_{AVE} = \int \mathcal{L}_{node}\, d^4x$ (variational form --- the \emph{temporal-gauge} (Weyl, $A_0$-free) Maxwell Lagrangian in vector-potential form; standard Maxwell dynamics are recovered on the Gauss-constrained initial-data surface, the constraint supplied by Axiom 5 (Substrate DC Bias; clause attribution left open, riding the deferred FORK-1), not by an equation of motion of this action --- rescoped 2026-08-10, R43). Legacy name: \emph{Effective Action Principle}.
    \item \textbf{Axiom 4 (Universal Saturation Kernel):} The substrate's bulk response to local strain $A$ is the universal quarter-arc kernel $S(A) = \sqrt{1 - (A/A_{yield})^2}$, dialect-independent (governs voltage, thermal, gravitational, rotational strain identically). A-034: same kernel at 26 cross-scale instances spanning 21 orders of magnitude, with three empirical anchors at zero free parameters (BCS 0.00\%, BH event horizon Schwarzschild exact, BH ring-down $18/49$ at 1.7\% from GR); the stellar solar-flare instance is a forward prediction, with NOAA GOES 40-yr validation pending a live catalog fetch (LF-03). % [2026-06-15 LF-03 -- prior wording preserved per Rule 12]: was "four empirical anchors at zero free parameters (BCS 0.00\%, BH event horizon Schwarzschild exact, BH ring-down 18/49 at 1.7\% from GR, NOAA 40-yr solar flares)". SUPERSEDED per KB leaf solar-flares-led-avalanche.md: NOAA GOES comparison is a synthesized illustrative timeline, not a live fetch; solar flare = A-034 forward prediction, not a validated anchor. Count four -> three.
\end{enumerate}

\textbf{Scope correction (2026-06-15, reconciliation to the Explicit Closure DAG below; canonical anchors: interlock register \texttt{ilk-gravmb} 2026-06-14 $G$-ruling $+$ keystone $\alpha$-verdict $+$ the verification-trace banner above).} The itemized ``all fundamental constants dynamically emerge'' framing in the paragraph below is the \textbf{parameter-reduction} claim ($\sim$26 SM parameters $\to$ 3 interlocked inputs $\{m_e, \alpha, G\}$ $+$ four axioms), read at \textbf{Class B substrate-mechanism manifestation level} --- not the claim that every value is derived from nothing. Specifically: (i) $1/\alpha = 8\pi/p_c$ is a \textbf{Class-B named geometric identification} whose exact value rests on $R \cdot r = 1/4$, which the substrate does not independently select (definitional, not an independent prediction); (ii) gravity is \textbf{mixed} --- its Achromatic-Lens $1/7$ projection \emph{form} is derived from Axioms 1 \& 4, but $G$'s \emph{value} is a calibration input via the value-fitted $\xi = 4\pi(R_H/\ell_{node})\alpha^{-2}$, with the closed-form Chain~B$'$ derivation \textbf{open} (the $G$ closure below is ``acyclic conditional on the prior closures,'' not a first-principles value-derivation); (iii) the ``zero free parameters'' verdict is precisely as strong as the Layer-8 closure conditions hold (see the Acyclicity verdict below). \textit{Body below preserved per Rule 12.}

From these initial geometric anchors and four structural rules, all fundamental constants dynamically emerge as the strict mechanical limits of the EFT:
\begin{itemize}
    \item \textbf{Geometry \& Symmetries (Parameters 1 \& 2):} Dividing the localized topological yield by the continuous macroscopic Schwinger yield dictates the emergence of the macroscopic fine-structure geometric constant ($1/\alpha = 8\pi/p_c$). The $\mathbb{Z}_3$ symmetry of the Borromean proton generates $SU(3)$ color symmetry, evaluating the Witten Effect to predict $\pm 1/3e$ and $\pm 2/3e$ fractional charges.
    \item \textbf{Electromagnetism (Axioms 1 \& 2):} Axiom 2's topo-kinematic mechanism yields the topological conversion constant ($\xi_{topo}$), demonstrating that magnetism is equivalent to kinematic convective vorticity ($\mathbf{H} = \mathbf{v} \times \mathbf{D}$); Axiom 1 supplies the wave dynamics propagating this charge.
    \item \textbf{The Electroweak Layer (Axioms 1 \& 2):} Axiom 1's LC network, evaluated via Effective Medium Theory (EMT) for a 3D amorphous central-force network with coordination $z_0 \approx 51.25$, shows that $K/G = 2$ at the unique operating point $p^* = 8\pi\alpha \approx 0.1834$, located $56.7\%$ above the rigidity threshold. The vacuum is a rigid solid, not a marginal glass. This trace-reversed geometric boundary forces the macroscopic vacuum Poisson's ratio to $\nu_{vac} = 2/7$, which evaluates the Weak Mixing Angle acoustic mass ratio ($m_W / m_Z = \sqrt{7}/3 \approx 0.8819$). The fine-structure value of $\alpha$ entering this constraint is fixed by Axiom 2.
    \item \textbf{Gravity and Cosmology (derived from Axioms 1 \& 4):} Projecting a 1D QED string tension into the 3D bulk metric via the trace-reversed tensor yields the $1/7$ isotropic projection factor for massive defects. Integrating the 1D causal chain across the 3D holographic solid angle, bounded by the cross-sectional porosity ($\alpha^2$) of the discrete graph, analytically binds macroscopic gravity ($G = \hbar c/(7\xi\,m_e^2)$) and the Asymptotic de Sitter Expansion Limit ($H_\infty$) into a single, unified mathematical identity.
    \item \textbf{The Dark Sector (Axiom 4):} The strict EFT hardware packing fraction ($p_c approx 0.1834$) limits excess thermal energy storage during lattice genesis, proving Dark Energy is a mathematically stable phantom energy state ($w \approx -1.0001$). The generative expansion of the lattice sets a fundamental continuous Unruh-Hawking drift. The exact topological derivation of the substrate mass density ($\rho_{bulk}$) and mutual inductance ($\nu_{vac}$) dictates a saturating Dielectric Saturation-plastic transition, mathematically recovering the exact empirical MOND acceleration boundary ($a_{genesis} = c H_\infty / 2\pi$), dynamically yielding flat galactic rotation curves without invoking non-baryonic particulate dark matter.
\end{itemize}

\subsection*{Explicit Closure DAG}

Earlier editions of this section asserted closure narratively without constructing the DAG. The acyclicity check below makes the dependency graph explicit, including back-edges that the Layer-8 closure introduces and the conditions under which it remains acyclic.

\paragraph{Layer 0 --- Inputs.}
\begin{itemize}
    \item $m_e$ (or equivalently $\ell_{node} = \hbar/(m_e c)$) --- one is the empirical input mass scale; the other is computed from it. \textbf{Status:} input scale.
    \item $\hbar, c, e, \mu_0, \varepsilon_0$ --- SI anchors (definitional under SI 2019).
    \item $T_{\text{CMB}} = 2.725\,\text{K}$ --- cosmological boundary condition.
    \item $\delta_{strain} \approx 2.22\times 10^{-6}$ --- \textbf{status: structure predicted (existence + sign of $\alpha$ thermal running below cold-lattice asymptote); magnitude at $T_{CMB}$ is a definitional residual} ($1-$CODATA$/\alpha_{cold}$, back-subtracted from CODATA) --- NOT a derivable thermal observable: the candidate first-principles derivation (E-mode dispersion + Bose-Einstein occupation) was \textbf{closed NEGATIVE} (FT-1, 2026-05-31: $\sim$31 OOM undershoot, generic-thermal not AVE-distinct). Same predicted/fitted pattern as Vol~6's $R$ per nucleus. See Ch.~\ref{ch:alpha_golden_torus} disclosure.
    \item Axioms 1--4 --- structural postulates.
\end{itemize}

\paragraph{Forward edges (conditional on Axioms 1--4).}
The forward DAG is constructed by inspection of the per-row formulas in Appendix~\ref{app:full_derivation_chain}; representative edges:
\begin{itemize}
    \item $\ell_{node} \leftarrow \{m_e, A1\}$;\quad $\xi_{topo} = e/\ell_{node} \leftarrow \{\ell_{node}, A2\}$;\quad $T_{EM} = m_e c^2/\ell_{node}$;\quad $V_{snap} = m_e c^2/e$.
    \item $p_c = 8\pi\alpha \leftarrow \{\alpha\}$;\quad $V_{yield} = \sqrt{\alpha}\,V_{snap} \leftarrow \{\alpha, V_{snap}, A2\}$.
    % [2026-08-02 CRIB-1 DAG-EDGE CORRECTION -- board sec 3 (Grant-ratified 2026-08-02);
    %   wave-2 backmatter reconciliation. Board finding [MEDIUM][scope-lag] at this edge.
    %   Prior edge preserved per Rule 12 (git trail), verbatim:
    %     "$\nu_{vac} = 2/7 \leftarrow \{A1$ LC mechanism, $K{=}2G$ at $\alpha\}$"
    %   The edge booked nu_vac = 2/7 as derived from A1 + K=2G with no import tag, so the
    %   forward DAG showed 2/7 reachable from Layer-0 axioms alone. K=2G is not a Layer-0
    %   input and not axiom-supplied: it is an import edge. NO VALUE CHANGE (2/7, 2/9 stand);
    %   NO VALUE REFILL. See the marked provenance note after this itemize, which also covers
    %   the companion prose "This trace-reversed geometric boundary FORCES the macroscopic
    %   vacuum Poisson's ratio to $\nu_{vac} = 2/7$" earlier in this chapter (Electroweak
    %   Layer bullet; that bullet already sits under the 2026-06-15 Scope-correction banner
    %   above it, which is silent on the K=2G import -- this note supplies that leg only).
    %   ROUTED, not adjudicated here: whether K=2G is a FOURTH import row in the register or
    %   is subsumed under G's import -- docket 2026-08-02-mr-k2g-kb-internal item (a), which
    %   names THIS site and appendix_vacuum_engineering.tex:39 as the two backmatter DAG
    %   sites awaiting that ruling. Executed under the board's ratified authority rule
    %   (sec 4: the sec 5 per-finding disposition tags govern; tagged [mechanical]).
    \item $\nu_{vac} \equiv \nu_{\text{Hill}} = 2/7 \leftarrow \{A1$ LC mechanism, $K{=}2G$ at $\alpha$ --- \textbf{$K{=}2G$ is an IMPORT edge, not a Layer-0 derivation}$\}$;\quad $\sin^2\theta_W = 2/9 \leftarrow \{\nu_{vac}\}$.
    \item $M_W \leftarrow \{m_e, \alpha, p_c, \sin^2\theta_W\}$;\quad $M_Z = (3/\sqrt{7})M_W$.
    \item $G_F \leftarrow \{M_W, \sin^2\theta_W, \alpha\}$;\quad $v_{Higgs} = 1/\sqrt{\sqrt{2}\,G_F}$;\quad $m_H = v/\sqrt{N_{K4}}$.
    \item Lepton spectrum: $m_\mu \leftarrow \{m_e, \alpha, A1$ Cosserat$\}$;\quad $m_\tau \leftarrow \{m_e, \alpha, p_c\}$.
    \item $\alpha_s = \alpha^{3/7}$;\quad quark masses $\leftarrow$ functions of $\{m_e, m_\mu, m_\tau, \alpha, \alpha_s\}$.
    \item CKM $\leftarrow \{\sin^2\theta_W, \nu_{vac}\}$;\quad PMNS $\leftarrow \{\nu_{vac}, c_1, c_3\}$.
    \item $H_\infty = 28\pi m_e^3 c G/(\hbar^2 \alpha^2)$;\quad $a_0 = c H_\infty / (2\pi)$.
\end{itemize}

\noindent\textbf{$K = 2G$ is an import edge (2026-08-02, CRIB-1; no value changes).} The $\nu_{vac}$ edge above, and the ``\emph{This trace-reversed geometric boundary \textbf{forces} the macroscopic vacuum Poisson's ratio to} $\nu_{vac} = 2/7$'' sentence in the Electroweak-Layer bullet earlier in this chapter, both read as though $2/7$ were reachable from the Layer-0 axioms alone. It is not. $K = 2G$ is \textbf{form-derived, value GR-imported}: the substrate forces the \emph{form} of the elastic response $K/G = f(\rho)$, but the value is the GR trace-reversal identity, and the corpus's two closure attempts both returned negative --- the Cauchy grade (PR~\#506) and the genuine chiral micropolar Bloch eigensolve on the ratified \emph{srs}-z3 production carrier (PR~\#508), which yields a one-parameter $\nu_{eff}(\rho)$ \emph{family} rather than a forced $K = 2G$. Read the $\nu_{vac}$ edge as \emph{acyclic conditional on that import}, the same \emph{acyclicity} qualification the $G$ closure carries below (an analogy about conditionality only --- whether $K=2G$ is a fourth import row or rides $G$\textquotesingle s import is ROUTED and explicitly NOT decided here). $2/7$ is $\nu_{\text{Hill}}$, the isotropic Voigt--Reuss--Hill average at that operating point --- an averaging choice, not a lattice-emergent bound. No number in this section changes and no replacement provenance is supplied. Canonical: \kbleaf{ave-kb/common/form-deriving-value-importing.md}; \kbleaf{ave-kb/vol1/claim-quality.md}; \kbleaf{ave-kb/common/theorem-thesaurus.md}.

By inspection, the forward graph is acyclic \emph{given} Layer-0 inputs, the axioms, and the $K = 2G$ import edge noted above: every derived quantity depends only on those and on earlier-layer derivations.

\paragraph{Back-edges (Layer-8 closure attempts).}
The framework's ``zero free parameters'' claim requires closing the Layer-0 inputs $\{m_e, \alpha, G\}$ via back-edges:
\begin{itemize}
    \item \textbf{$\alpha$ closure (Ch.~\ref{ch:alpha_golden_torus} Golden Torus):} $\alpha^{-1}_{ideal} = 4\pi^3 + \pi^2 + \pi$ at the cold-lattice asymptote, from the trefoil's three-regime decomposition. Acyclic if the Golden Torus geometry is established independently of $\alpha$'s measured value (the three regimes are individually grounded in the chapter; this can be checked separately). The thermal running $\alpha^{-1}(T) < \alpha^{-1}_{ideal}$ at $T > 0$ is a structural prediction (sign and existence). \emph{Magnitude-derivation gap:} $\delta_{strain}$ at $T_{CMB}$ is currently back-subtracted from CODATA, definitional given the engine's \texttt{DELTA\_STRAIN = 1 - (1/ALPHA)/ALPHA\_COLD\_INV}. The cold-lattice $\alpha$ closure itself is acyclic; the magnitude of the thermal correction is fitted (one scalar) until derived from $G_{vac}$ + equipartition. Same predicted/fitted pattern as Vol~6 (predicted: structure; fitted: one scalar) --- different physics, same disclosure shape.
    \item \textbf{$m_e$ closure (Layer 8 via Nyquist):} $m_e$ becomes derivable from $\ell_{node}$ if $\ell_{node}$ is fixed by Nyquist resolution of the smallest stable soliton. Acyclic if Nyquist resolution is established independently of $m_e$ (rather than through ``the smallest soliton has rest mass $m_e$'').
    \item \textbf{$G$ closure (Layer 7 via $H_\infty$ + Axiom 3):} $G = \hbar c/(7\xi\,m_e^2)$ with $\xi = 4\pi(R_H/\ell_{node})\alpha^{-2}$. Once $\alpha$ and $m_e$ are closed and $R_H$ is set by the cosmological generation thermodynamics, $G$ follows. Acyclic conditional on the prior closures.
\end{itemize}

\paragraph{Acyclicity verdict.}
The forward DAG is acyclic. Of the three Layer-8 back-edges, the $G$ closure is acyclic conditional on the other two; the $m_e$ closure depends on whether ``smallest stable soliton'' is well-defined without circular reference to $m_e$; the $\alpha$ cold-lattice closure ($\alpha^{-1}_{ideal} = 4\pi^3+\pi^2+\pi$) is acyclic by inspection. The thermal-running magnitude $\delta_{strain}$ at $T_{CMB}$ is a definitional residual (one scalar bridging cold-lattice $\alpha$ to CODATA, $1-$CODATA$/\alpha_{cold}$; same predicted/fitted pattern as Vol~6's $R$ per nucleus). The candidate first-principles derivation of $\delta_{strain}$ (E-mode dispersion + Bose-Einstein occupation, the substrate-native form of the old ``$G_{vac}$ + equipartition'' route) --- which \emph{would} have upgraded the $\alpha$ closure from ``structure predicted, magnitude fit'' to fully zero-parameter --- was \textbf{attempted and closed NEGATIVE} (FT-1, 2026-05-31): it undershoots by $\sim$31 OOM (BE occupation suppresses \emph{below} equipartition at $T_{CMB} \ll \Theta_{\text{Debye}}$) and is generic-thermal, not AVE-distinct. So the $\delta_{strain}$ magnitude stays a definitional residual; the cold-lattice $\alpha$ remains a named geometric identification with all three NAMED routes closed-negative (both golden-torus lift-routes closed Class B 2026-06-04, and the $z_0$-from-K4 rigidity-percolation route closed-NEGATIVE 2026-06-15 via the exact $z_{\text{eff}} \to 6$ Maxwell--Calladine count). %% Rule-12 2026-06-19 z0-route propagation: prior "only z_0-from-K4 open" superseded --- closed-NEGATIVE per research/2026-06-15_alpha-crystal-mc-count_result.md; alpha-value = scoped standing echo (Grant ratified 2026-06-18).

Because the forward DAG is acyclic and the back-edges are individually identifiable and conditional, the AVE framework is a mathematically structured Topological Effective Field Theory whose ``zero free parameters'' claim is precisely as strong as the Layer-8 closure conditions hold. Earlier editions overclaimed this strength; the present edition documents it.

\section*{Outstanding Rigour Gaps}

The closure conditions above resolve into a small, explicit set of pending derivations. Each is documented at its inline disclosure point in the corpus and consolidated here for reference. Closing any of these strengthens the ``structurally zero-parameter'' framing toward ``absolutely zero-parameter.''

\begin{center}\small
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{p{0.18\textwidth}p{0.30\textwidth}p{0.30\textwidth}p{0.16\textwidth}}
\toprule
\textbf{Gap} & \textbf{Status} & \textbf{What closure unlocks} & \textbf{Inline disclosure} \\
\midrule
$\delta_{strain}$ magnitude at $T_{CMB}$ (route CLOSED NEGATIVE) & Definitional residual ($\approx 2.22 \times 10^{-6}$, $1-$CODATA$/\alpha_{cold}$) back-subtracted from CODATA. Structure predicted (existence + sign of thermal running below the cold-lattice $\alpha$ asymptote); magnitude does NOT derive. & The candidate magnitude derivation (E-mode dispersion + Bose-Einstein occupation, the substrate-native form of the old ``$G_{vac}$ + equipartition'' route) that \emph{would} have upgraded the $\alpha$ closure was \textbf{attempted and closed NEGATIVE} (FT-1, 2026-05-31: $\sim$31 OOM undershoot, generic-thermal not AVE-distinct). The magnitude stays a definitional residual; only the SIGN is substrate-set. & FT-1 result \kbleaf{research/2026-05-31\_FT-1\_delta-strain-eta-epsilon\_result.md}; Ch.~\ref{ch:alpha_golden_torus} status disclosure. \\
$m_e$ closure via Nyquist independence & One of $\{m_e, \ell_{node}\}$ is the input scale; the other is computed via $\ell_{node} = \hbar/(m_e c)$. Layer~8 proposes Nyquist resolution of ``the smallest stable soliton'' fixes $\ell_{node}$ independently of $m_e$. & Removes the input-scale degree of freedom; both $\ell_{node}$ and $m_e$ become emergent. Requires demonstrating ``smallest stable soliton'' is well-defined without circular reference to $m_e$ (e.g., from Axiom~4 saturation gradient + lattice cutoff alone). & Back-edges above; full-derivation chain Layer~8. \\
Flux-tube radial profile (Gaussian ansatz) & Vol~2 Ch.~2 Layer~6 (proton mass calculation) models the flux tube as a Gaussian LC resonant loop with FWHM $= \ell_{node}$, producing the exact $M/L = 1/\sqrt{2}$ mutual coupling at $d = \ell_{node}/2$ and the threshold $\rho_{threshold} = 1 + \ell_{node}/(8\sqrt{2\ln 2}) \approx 1.1062$ via standard Gaussian arithmetic. Result internally consistent given the ansatz. Axiom~1 fixes the FWHM but not the functional form. & Removes the Gaussian as a buried assumption. Requires either (a) deriving the Gaussian profile from Axiom~4 LC dynamics + transmission-line boundary conditions, or (b) replacing it with the framework-consistent profile (sech$^2$, Bessel $J_0$, algebraic $\sqrt{1-r^2}$ Axiom-4 kernel) and re-evaluating $\rho_{threshold}$. The gap binds $\rho_{threshold}$ ONLY --- it does NOT bind $\mathcal{V}_{total} = 2$, which is the profile-independent dual-reactance count ($X_C + X_L$ sectors, Axiom~1; counted, not integrated; canonical at \texttt{\seqsplit{common/dual-reactance-storage-taxonomy.md}}) and is mass-confirmed via $m_p/m_e \approx 1836.15$ regardless of profile. (The legacy ``$\mathcal{V}_{total} = 2.0$ FEM-converged to $0.13\%$'' provenance is dropped: the only ``FEM'' script is voxel quadrature of the Gaussian-ansatz saturated overlap volume, a $\rho_{threshold}$ consistency check, not a derivation of the reactance count.) & Vol~2 Ch.~2 §Saturation Threshold. \\
$H_\infty$ closure: $G$ derivation independent of $R_H$ & The current $G$ derivation routes through $\xi = 4\pi(R_H/\ell_{node})\alpha^{-2}$ with $R_H \equiv c/H_\infty$ substituted in. Algebraically, $H_\infty = 28\pi m_e^3 c G/(\hbar^2\alpha^2)$ is the same constraint as $G = \hbar c/(7\xi m_e^2)$ rearranged --- one identity in $(G, H_\infty)$, not two independent predictions. Vol~3 Ch.~1 already discloses this as ``consistency proof, not prediction''; Vol~3 Ch.~4 §Verification has been re-aligned to the same framing. & Promotes $H_\infty$ from a geometric consistency check to a genuine downstream prediction. Requires deriving $G$ from a thermodynamic balance whose closure conditions are local (lattice tension, equipartition, generation rate per node) rather than horizon-scale; substituting that local-$G$ into the $H_\infty$ formula then yields a true prediction. & Ch.~\ref{ch:gravity_and_yield} consistency-proof paragraph; Ch.~\ref{ch:cosmology} §Verification. \\
\bottomrule
\end{tabular}
\end{center}

These are the principal outstanding rigour gaps. They are not calculational errors elsewhere in the chain --- the chain itself is acyclic and the values it produces match experiment to the precision quoted in the master prediction table. Closing any of these gaps moves the ``structurally zero-parameter'' framing closer to absolute, without invalidating the existing predictions.

\section{A-034: Universal Saturation-Kernel Empirical Anchors}
\label{sec:a034_empirical_anchors}

% [2026-08-02 COUNT CORRECTION -- wave-2 backmatter reconciliation; board finding
%   [MEDIUM][stale-value][mechanical] at this line. Prior wording preserved per Rule 12
%   (git trail), verbatim: "has been empirically matched at \textbf{four widely-separated
%   scales}:". SUPERSEDED by THIS FILE'S OWN ruled count, earlier in this chapter's Axiom-4
%   bullet: "three empirical anchors at zero free parameters (BCS 0.00\%, BH event horizon
%   Schwarzschild exact, BH ring-down $18/49$ at 1.7\% from GR); the stellar solar-flare
%   instance is a forward prediction" -- carrying the 2026-06-15 LF-03 Rule-12 comment that
%   ends "Count four -> three." (verified at HEAD two-method: `grep -n` on "four
%   widely-separated scales" and on "three empirical anchors" -> the two lines in this file;
%   KB leaf solar-flares-led-avalanche.md is the authority for the demotion). The rest of
%   this section already books three: the table's solar-flare row reads "forward
%   prediction" with the dagger footnote, and the closing paragraphs read "The 3 validated
%   anchors above (plus the solar-flare forward prediction)" and "the 3 validated empirical
%   anchors here". This line was the only surviving "four".
%   NO VALUE REFILL and no table row is added, removed or altered.
%   FLAGGED, NOT FIXED (out of this finding's scope; surfaced for the orchestrator): the
%   PRECEDING sentence's "carries the strongest direct empirical validation of any AVE axiom" (same line, separate sentence -- #853 audit R3 corrected this from "same sentence") is
%   a CRIB-4 "most direct experimental validation"-class superlative. CRIB-4 demotes that
%   class to the claim-quality grade, but this site is not in this lane's assigned CRIB-4
%   set (only 07_universal_saturation_kernel.tex:145 was assigned), so the clause is left
%   byte-untouched. Note also that two of the three surviving anchors are comparisons
%   against GR THEORY ("GR exact", "Standard GR") rather than against measurement -- the
%   same theory-vs-theory seam CRIB-4 names at the 07 site.
Beyond the internal DAG closure documented above, \textbf{Axiom~4's universal saturation kernel} $S(A) = \sqrt{1 - A^2}$ carries \textbf{three empirical anchors at zero free parameters}, each read at its own claim-quality grade (see the scope note below the table; the former ``strongest direct empirical validation of any AVE axiom'' grade is withdrawn). The same kernel --- identical functional form, no per-scale parameter retuning --- has been matched at \textbf{three widely-separated scales} (the fourth row below, the solar-flare instance, is an A-034 \emph{forward prediction}, not a validated anchor --- see the dagger note; count corrected 2026-08-02 to this chapter's own ruled count in the Axiom-4 bullet above, ``three empirical anchors at zero free parameters''):

\begin{center}
\renewcommand{\arraystretch}{1.3}
\small
\begin{tabular}{@{}l p{3.5cm} p{3.0cm} l@{}}
\toprule
\textbf{Scale} & \textbf{Phenomenon} & \textbf{Empirical Match} & \textbf{Error} \\
\midrule
Condensed-matter & BCS $B_c(T)$ kernel  & Type-I/II datasets    & \textbf{0.00\%} \\
Solar/Geophysical & Solar flare avalanche & NOAA GOES 40-yr (live fetch pending) & forward prediction\textsuperscript{$\dagger$} \\
% [2026-06-15 LF-03 -- prior cell preserved per Rule 12]: Error column was "within scatter". SUPERSEDED per KB leaf solar-flares-led-avalanche.md: the NOAA GOES comparison is a synthesized illustrative timeline, not a live fetch; the 0.46-yr FWHM is a forward prediction, empirical validation pending a live NOAA GOES fetch.
Gravitational    & BH ring-down (QNM)   & GR exact ($\omega_R M_g = 18/49$) & \textbf{1.7\%} \\
Cosmological     & Schwarzschild radius & Standard GR             & \textbf{exact} \\
\bottomrule
\end{tabular}
\end{center}

\noindent\footnotesize\textsuperscript{$\dagger$}\,The solar-flare row is an A-034 \emph{forward prediction} (0.46-yr FWHM), not a validated anchor: the NOAA GOES comparison is a synthesized illustrative timeline (LF-03, 2026-06-15), with empirical validation pending a live NOAA GOES catalog fetch. See KB leaf \texttt{solar-flares-led-avalanche.md}.\normalsize

\noindent\textbf{Anchor-grade scope --- superlative withdrawn (Rule~12, 2026-08-03; CRIB-4; prior
wording preserved in the git trail, and no number or table row in this chapter changes).}
The sentence introducing the table above previously graded Axiom~4 as carrying ``the strongest
direct empirical validation of any AVE axiom'' (canonical 2026-05-15 evening). That is a CRIB-4
``most direct experimental validation''-class superlative; it is \textbf{withdrawn, with no
replacement superlative} --- the same withdrawal already taken for the twin grade at
Backmatter Ch~\ref{app:universal_saturation_kernel}, where ``the most direct cross-scale
\emph{experimental} validation'' was withdrawn on the identical ground. Read the three anchors at
their claim-quality grades instead:
\begin{itemize}
    \item \textbf{BCS $B_c(T)$, $0.00\%$} --- a \emph{definitional identity} at per-material
    calibration: $T_c$ and $B_{c0}$ are taken as inputs per material and the \emph{temperature
    dependence} \textbf{is} the saturation kernel. Per the truth-source card this is a category
    (ii) \textbf{axiom manifestation}, \textbf{not} a curve fit and not a fitted-parameter result
    (\kbleaf{ave-kb/claim-quality.md}:171-182, \texttt{clm-3dc9qt}).
    \item \textbf{BH ring-down (QNM), $1.7\%$} --- compared against \textbf{GR exact}, i.e.\ against
    \emph{theory}, not against a measurement. It is additionally the \textbf{cold Schwarzschild}
    $a^{*} = 0$ single-point eigenvalue, which per \textbf{GRANT RULING B1} (2026-07-21) is the
    \emph{only} surviving zero-free-parameter ring-down content; the spinning-remnant LIGO-catalog
    match is retracted as a frame-mixed artifact and \textbf{no replacement number is claimed here}.
    \item \textbf{Schwarzschild radius, exact} --- compared against \textbf{Standard GR}, likewise a
    theory-vs-theory comparison, not a measurement.
\end{itemize}
Two of the three rows are therefore comparisons against GR theory rather than against experiment,
which is precisely the seam that forecloses an \emph{experimental}-grade superlative. The count in
the sentence above is this chapter's own ruled count in the Axiom-4 bullet
(``three empirical anchors at zero free parameters''); the solar-flare row remains a forward
prediction per LF-03.

\noindent\emph{Flagged, not fixed} (outside this pass's assigned CRIB-4 site, surfaced for the
orchestrator): the ``strongest form of cross-scale verification possible for an axiom'' clause in
the \textbf{Methodological significance} paragraph immediately below, and the ``most-validated
members'' clause in \textbf{Full enumeration}, are the same superlative class as the withdrawal
above and are left byte-untouched by this pass. Where they conflict with this scope note, this note
governs.

\noindent \textbf{Methodological significance.} Each empirical anchor uses \textbf{zero free parameters} beyond the framework's existing 3 inputs ($\ell_{node}$, $\alpha$, $G$ --- themselves derived in Layer~8 of the derivation chain). The kernel is not fit per-scale; it is the \emph{same} $\sqrt{1 - A^2}$ kernel applied in different physical regimes. This is the strongest form of cross-scale verification possible for an axiom: the kernel asserted abstractly in Vol~1 Ch~1 is shown empirically to be the same operator governing pairing, plasma reorganization, gravitational wave damping, and event-horizon formation.

\noindent \textbf{Full enumeration.} The 3 validated anchors above (plus the solar-flare forward prediction) are the most-validated members of a \textbf{26-instance catalog} spanning 21 orders of magnitude. Full catalog with 3-way symmetry classification (SYM / ASYM-N / ASYM-E) and measurement-hierarchy framing (single-emitter / multi-emitter bulk / phased-array PLL autoresonant): see Backmatter Ch~\ref{app:universal_saturation_kernel}.

These anchors are produced by the same code path (\texttt{src/ave/axioms/scale\_invariant.py} + \texttt{src/ave/regime\_3\_saturated/}) --- the engine literally evaluates one $S(A)$ function across all 26 instances. The \texttt{168/168 framework files} closure above (a dated static snapshot --- see the scope note under the verification block) and the 3 validated empirical anchors here (the solar-flare instance being a forward prediction pending live NOAA validation) together establish that AVE's Axiom~4 is \textbf{both internally closed AND externally calibrated} without parameter tuning.
