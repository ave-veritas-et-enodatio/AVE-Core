% 17_engine_requirements.tex
\chapter{Engine Requirements for Faithful Simulation}
\label{ch:vol9_engine_requirements}

\begin{objectivebox}
\begin{itemize}
    \item State the specification a numerical engine must satisfy to simulate the vacuum cell (Vol~9 Ch.~\ref{ch:vol9_pin_port_configuration} DOF/mode table) faithfully --- the datasheet read as a component spec for the simulator, not only for the substrate.
    \item Map each requirement to (a) the datasheet line it enforces and (b) the documented engine failure that taught it, so every requirement is empirically grounded rather than aspirational.
    \item Record the conservation canaries (Hamiltonian drift, $|\mathbf{L}|$ bound) that distinguish a conservative energize-and-lock coupling from a non-conservative pump.
    \item State the regime-reachability requirement: an engine must reach the regime the test targets (bulk near-yield for rectification); a sub-yield null is an artifact, not a falsification.
\end{itemize}
\end{objectivebox}

\section{Framing: the Datasheet as a Simulator Spec}
\label{sec:vol9_engine_framing}

The preceding chapters characterize the vacuum cell as a component: its DOFs (Ch.~\ref{ch:vol9_pin_port_configuration}), its couplings (Ch.~\ref{ch:vol9_pin_port_configuration} \S Sector-to-Sector Coupling Matrix), its absolute limits (Ch.~\ref{ch:vol9_absolute_maximum_ratings}), its small-signal and temporal characteristics (Ch.~\ref{ch:vol9_ac_electrical_characteristics}). This chapter reads the same datasheet as a specification for the \emph{simulator}: what a numerical engine must implement, per datasheet line, to render one cell faithfully.

The discipline here is empirical, not aspirational. Every requirement below is mapped to (a) the datasheet line it enforces and (b) a \emph{documented engine failure} that taught it --- a run where an engine that violated the requirement produced a wrong or null result. The substrate is natural; the engine is engineered; and the engine-failure history is the empirical record of which substrate features a simulator cannot omit. Per the substrate-native-check discipline, SM/QED defaults (Lagrangian gradient-descent, continuum-Helmholtz, energy-basin minimization, bulk-force confinement) leak in unless these requirements are stated and enforced.

\section{Engine Requirements Table}
\label{sec:vol9_engine_requirements_table}

Each row: the requirement, the datasheet line it enforces, the documented engine failure (violating engine $+$ symptom), and the discipline/skill that catches the violation.

\begin{footnotesize}
\setlength{\tabcolsep}{3pt}
\begin{longtable}{@{}p{0.04\linewidth} p{0.24\linewidth} p{0.21\linewidth} p{0.27\linewidth} p{0.14\linewidth}@{}}
\caption{Engine requirements for faithful simulation of the vacuum cell. Each requirement $\leftarrow$ a documented engine-failure lesson.}\label{tab:vol9_engine_requirements}\\
\toprule
\textbf{\#} & \textbf{Requirement} & \textbf{Datasheet line} & \textbf{Violating engine $+$ symptom} & \textbf{Discipline} \\
\midrule
\endfirsthead
\toprule
\textbf{\#} & \textbf{Requirement} & \textbf{Datasheet line} & \textbf{Violating engine $+$ symptom} & \textbf{Discipline} \\
\midrule
\endhead
1 & $c_{eff}(V)$ wave-speed modulation \textbf{per sector} in the \emph{propagation} step, not only $Z(V)$ at scatter & Ch.~\ref{ch:vol9_ac_electrical_characteristics} \S Temporal ($c_{EM}, c_{shear}, c_{bulk}$); A-027 two-engine & $Z(V)$-only-at-scatter engine: the genesis-24 saturation wall never engages ($\Gamma = -1$ TIR wall never forms; no bound state) & \kbleaf{substrate-native-check}; A-027 \\
2 & An \textbf{independent state carrier per DOF-table row}; read-only projections are observables, never state & Ch.~\ref{ch:vol9_pin_port_configuration} \S Per-port wave content ($V_{inc}$ state; $V_{ref}$ derived) & Wiring $V_{ref}$ as independent state $\Rightarrow$ double-count; $w_{pol} \equiv 0$ self-inflicted (winding pinned at zero) & \kbleaf{verify-before-cite}; observer-read-only \\
3 & Saturation / confinement / reflection rendered as \textbf{boundary conditions} ($\Gamma$, Op17-bounded), never bulk forces & Ch.~\ref{ch:vol9_pin_port_configuration} \S Boundary semantics; Ch.~\ref{ch:vol9_saturation_characteristics} kernel & Bulk-force / energy-basin confinement (the SM/QED CP10 leak): the crystal-engine / graft-v2 contrast shows a \emph{boundary}-localized rendering ($c_{eff}$ trap $\to \Gamma$-wall via $g_{front}$) confines a bound state with \emph{no} detonation ($\Gamma_{core}\to-0.24$; $\Gamma_{min}\to-0.85$). The genesis-24 detonation is the EMF-pump failure of row~4, \emph{not} bulk-force confinement. & \kbleaf{substrate-native-check} CP10 \\
4 & Cross-sector couplings as \textbf{conserved Hamiltonian pairs} (source $+$ back-reaction from one $H_{couple}$; energize-lock) & Ch.~\ref{ch:vol9_pin_port_configuration} coupling matrix (conservative Faraday/Cosserat) & The flag-gated Lagrangian-EMF (\kbleaf{k4\_cosserat\_coupling.py}:550): non-conservative \emph{amplifying} pump; $|\mathbf{L}|$ unbounded & \kbleaf{ave-conserved-vs-pumped} \\
5 & Source \textbf{symmetry class must admit the target topology} & Ch.~\ref{ch:vol9_pin_port_configuration} coupling matrix ($\kappa_{chiral}$); Ch.~\ref{ch:vol9_topological_characteristics} chirality & graft-v2: a \emph{centrosymmetric} source cannot select a chiral knot (seed symmetry forbids the target) & \kbleaf{ave-representation-capability-check} \\
6 & \textbf{float64} on conservation canaries ($H$-drift, $|\mathbf{L}|$ gates) & \S\ref{sec:vol9_engine_conservation_canaries} & float32: $H$-drift swamps the pump-vs-lock signal ($\sim10^{-9}$ canary lost) & \kbleaf{ave-conserved-vs-pumped} \\
7 & \textbf{Fixed $N$} across compared runs & cross-run comparison & Varying $N$ between compared runs $\Rightarrow$ non-comparable outputs (grid-dependent artifact) & empirical-driver discipline \\
8 & \textbf{Alias-checked} topology extractors with reliability gates ($\text{rel} > 0.1$), \textbf{and representation-capability-validated at the sampling scale} (plant-$(2,3)$-at-de-novo-scale $\to$ read-$(2,3)$ gate; minor radius $r \gtrsim 3$ cells) & Ch.~\ref{ch:vol9_topological_characteristics} extractors & (a) Aliased writhe/winding extractor: spurious $(p,q)$ readout. (b) Scale floor: a TRUE $(2,3)$ reads back as $(2,2)$/garbage at minor radius $r \approx 1.1$ cells, so the de-novo full-run $w_{pol}=0$ nulls are partly representation-limited, not physical (extractor-poloidal-misread note) & empirical-driver; \kbleaf{ave-representation-capability-check} \\
9 & Observers strictly \textbf{read-only} & Ch.~\ref{ch:vol9_pin_port_configuration} \S Per-port wave content (observer bridge) & Observer mutating state contaminates the dynamics it measures & observer-read-only \\
10 & \textbf{Regime-reachability}: the engine must reach the regime the test targets & Ch.~\ref{ch:vol9_absolute_maximum_ratings} regimes; Ch.~\ref{ch:vol9_saturation_characteristics} four-regime map & Sub-yield-linear run returns a null --- an \emph{artifact} (wrong regime), not a falsification (dark-wake Phases 1--5) & \kbleaf{ave-regime-phase-state-check} \\
\midrule
\multicolumn{5}{@{}l}{\textbf{\emph{Acceptance-suite lessons (2026-06-16/17 ground-up L0--L2 + Op/scale tier arc)}}} \\
\midrule
11 & \textbf{Chiral / optical-activity rotation must be unitary (copy-first, no NumPy view-aliasing)}; the losslessness gate must run with rotation \emph{ON} & Ch.~\ref{ch:vol9_topological_characteristics} chirality; Ch.~\ref{ch:vol9_magnetic_microrotational_characteristics} & In-place $2\times2$ rotation on NumPy views (\kbleaf{chiral\_lattice\_vector.py}:49--58, pre-fix): the first write corrupts $v_0$ before the second read $\Rightarrow$ non-orthogonal rotation $\Rightarrow$ $\sim$O(1) energy drift (Axiom-3 leak). The $\kappa=0$ free-photon gates never hit it; an energy gate \emph{with rotation ON} (A1b / T1.5) does & \kbleaf{substrate-native-check}; A1b/T1.5 \\
12 & \textbf{The medium must carry the DOF the excitation needs}: the transverse-only carrier ($2$ DOF) cannot host mass (L3 longitudinal) or charge (L4 micro-rotation); those need the full $6$-DOF Cosserat node & Ch.~\ref{ch:vol9_pin_port_configuration} DOF table; Ch.~\ref{ch:vol9_mechanical_characteristics} & The srs vector-TLM renders $2$ transverse DOF only (\texttt{(N,degree,2)}); A1a reports \texttt{carried\_dof==2} vs \texttt{axiom\_dof==6}. ``No single engine carries more than one or two'' (\kbleaf{engine-capability-map.md}:19) --- testing charge-genesis on a $2$-DOF carrier is structurally void & \kbleaf{ave-representation-capability-check}; A1a, T1.7/T1.8 \\
13 & \textbf{The $S$-exponent must be single-sourced}: a sector's $n \propto S^{p}$ (and $c \propto S^{q}$) cannot disagree between two code paths & Ch.~\ref{ch:vol9_ac_electrical_characteristics} ($c_{EM}, c_{shear}$); Ch.~\ref{ch:vol9_saturation_characteristics} kernel & \kbleaf{master\_equation\_fdtd.py}:169 returns $n=S^{0.25}$ (the :165--168 block above it is the flag-don't-fix comment) while its own \texttt{c\_eff\_squared} (:148--151) implies $n=S^{0.5}$; the two disagree. A4 verifies the internally-consistent $c_{eff}^2=c_0^2/S$ form and surfaces the exponent defect (flag-don't-fix) --- \emph{physics-review item for adjudication before any L3/L4 build that consumes $n$} & \kbleaf{ave-canonical-source}; A4 (flag) \\
14 & \textbf{Achromatic deflection ($\Gamma=0$) under a SYM bias must reproduce}: a co-scaled $\varepsilon\!\cdot\!\mu$ gradient keeps $Z=Z_0$ ($\Gamma\to0$) and bends light \emph{frequency-independently} & Ch.~\ref{ch:vol9_cosmological_characteristics} gravity FORM-bridge; Ch.~\ref{ch:vol9_ac_electrical_characteristics} & A chromatic-medium engine (frequency-dependent index) or one that loads $\varepsilon$ only ($\Gamma\neq0$) cannot render gravitational lensing: the AVE-distinct mechanism is achromatic + reflectionless (T2.2; \kbleaf{clm-k9up5c}). The deflection MAGNITUDE rides the input $A_0(x)$ --- chord-vs-echo of the magnitude left OPEN, not headlined & \kbleaf{ave-discrimination-check}; T2.2 \\
15 & \textbf{The same operator code path must run at every scale}; and \textbf{a simulation is a derivation-support, never an experiment} & Ch.~\ref{ch:vol9_cross_volume_reference} (Op catalog); \kbleaf{operators.md}:91 & A per-scale code branch breaks the framework's distinguishing scale-invariance claim: the SAME callable forces $\omega_R\!\cdot\!M=18/49$ at electron-mass AND BH-mass inputs ($30{+}$ OOM apart), $A_0$ varies, the operator does not (\kbleaf{test\_operators.py}). Per INVARIANT-S10 every acceptance test registers as a \kbleaf{sup-} node (derivation branch), and per INVARIANT-S9 never as an \kbleaf{exp-} (a simulation is not an experiment) & \kbleaf{ave-regime-phase-state-check}; INVARIANT-S9/S10 \\
\midrule
\multicolumn{5}{@{}l}{\textbf{\emph{RUNG-1 EXISTENCE lessons (2026-06-17 L3 posited-mass-cage arc, T3.3/T3.4)}}} \\
\midrule
16 & \textbf{A bound mode must be excited by a density-peak / shell BREATHING kick, not a monopole/DC kick}: the discrete eigenmode rings only under a radial-shell $\partial_t V$ kick on the wall antinode & Ch.~\ref{ch:vol9_mechanical_characteristics} bound-mode spectrum; Ch.~\ref{ch:vol9_ac_electrical_characteristics} & A monopole/DC kick on the posited Gaussian core does NOT ring --- it slowly RELAXES, and the dominant FFT component is bin~1, whose $\omega\propto1/n_\text{steps}$ is a \emph{run-length artifact}, not a physical mode (\kbleaf{test\_l3\_mass\_cage.py}:660--667, T3.4a). Sampling the wrong excitation (or the empty centroid of a shell) reads back a spurious continuum-relaxation $\omega$; with the shell kick, $\omega_\text{cutoff}\approx2.87$ is run-length-STABLE (peak/mean$\approx$456) & empirical-driver (Rule 10 density-peak sampling); T3.4a \\
17 & \textbf{A chord-vs-echo $Q$ read must REMOVE every $\alpha$-bake and measure $Q$ from cold dynamics}: ring-down envelope $+$ $-3$\,dB linewidth, never $Q_\text{TANK}$ or \kbleaf{M.ELECTRON} & Ch.~\ref{ch:vol9_ac_electrical_characteristics} $Q$-factor; Ch.~\ref{ch:vol9_breakdown_characteristics} & Reading any display computed from the baked $Q_\text{TANK}=1/\alpha$ (\kbleaf{cvr\_model.py}:72) returns $137$ \emph{by construction} (the instrument-echo-trap, \kbleaf{theorem-3-1-q-factor.md}:21): plotting more instruments does not escape the echo. With the two $\alpha$-bakes removed ($Q_\text{TANK}$ AND \kbleaf{gamma\_em\_sq} :364), the cold cage's measured $Q_\text{ringdown}\approx30.8$ --- \emph{NOT} $137$ --- empirically confirming $Q=1/\alpha$ is an instance-baked VALUE-ECHO, not a cage-emergent chord (clean chord-vs-echo NEGATIVE; corroborates the value-scoped verdict at \kbleaf{theorem-3-1-q-factor.md}:19) & \kbleaf{consistency-vs-emergence}; \kbleaf{ave-canonical-source}; T3.4b \\
18 & \textbf{Top-$K$ field-density / $\Gamma$ extractions must exclude PML cells}: filter the absorbing boundary before \texttt{argpartition} & Ch.~\ref{ch:vol9_pin_port_configuration} \S Boundary semantics ($\Gamma$-wall) & A top-$K$ $|\Gamma|$ read that includes PML cells returns the frozen-absorbing artifact, not interior physics: T3.3 reads $\Gamma_\text{bulk}$ on the PML-EXCLUDED interior ($\texttt{pml\_thickness}\le\{i,j,k\}\le N-\texttt{pml\_thickness}-1$), so $\Gamma_\text{min}(0.95)=-0.283$ (crosses the $-0.25$ OP2 gate), $\to0$ in vacuum, floors above $-1$ (S$_\text{min}$ clip) --- a genuine interior wall, not a boundary artifact (\kbleaf{test\_l3\_mass\_cage.py}:526, T3.3) & empirical-driver (Rule 10 PML exclusion); T3.3 \\
\midrule
C & \textbf{(Candidate --- characterization-in-flight)} wall sharpness / leak must be shown \textbf{NOT regularization-limited} (sweep $S_{min}$ / $A_{cap}$) before any leak $\to$ physical-constant attribution & Ch.~\ref{ch:vol9_pin_port_configuration} \S Boundary semantics ($\Gamma$-wall); Ch.~\ref{ch:vol9_absolute_maximum_ratings} & Wall sharpness $\Gamma_{min}\approx-0.85$ (carried across graft-v2/v3) is currently \emph{unattributed}: it may be set by the saturation kernel (physics) or by the $S_{min}$ floor / $A_{cap}$ cap (regularization). Characterization in flight on \kbleaf{analysis/2026-06-10-apparatus-floors} ($S_{min}$/$A_{cap}$ wall attribution $+$ $H_{bel}$ ledger floor $+$ wall-sign audit) & \kbleaf{ave-apparatus-floor-attribution} (in flight) \\
\bottomrule
\end{longtable}
\end{footnotesize}

\noindent
Requirements~1--5 are the structural ones (they decide whether the simulated object is the substrate at all); 6--9 are the numerical-hygiene ones (they decide whether a result is trustworthy); 10 is the experimental-design one (it decides whether a null means anything). A run that violates any of 1--5 is simulating a different object; a run that violates any of 6--9 cannot distinguish signal from artifact; a run that violates 10 cannot falsify anything. Requirements~11--15 are the ground-up acceptance-arc lessons (chirality losslessness, DOF-capability, single-sourced $S$-exponent, achromatic SYM deflection, one-callable-per-scale); requirements~16--18 are the RUNG-1 EXISTENCE lessons (the bound mode needs a density-peak/shell breathing kick, not a monopole; a chord-vs-echo $Q$ read must remove every $\alpha$-bake; top-$K$ $\Gamma$ extractions must exclude PML cells).

\noindent
\textbf{Candidate requirement~C --- not yet adjudicated (characterization-in-flight).} Row~C is a \emph{candidate}, not a closed requirement: before any wall-sharpness / leak number (e.g.\ $\Gamma_{min}\approx-0.85$, carried unchanged across graft-v2/v3) is attributed to a physical constant --- $\alpha$ above all --- the apparatus-floor characterization must first establish it is \textbf{not} set by a regularization knob (the $S_{min}$ saturation floor, the $A_{cap}$ amplitude cap): a number that tracks a knob is apparatus, not physics. The characterization runs on \kbleaf{analysis/2026-06-10-apparatus-floors} ($S_{min}$/$A_{cap}$ wall attribution, $H_{bel}$ ledger floor, wall-sign audit); the discipline is \kbleaf{ave-apparatus-floor-attribution}. Row~C is listed here so the gate is \emph{visible}, not asserted as met --- promote it to a numbered requirement only on that branch's verdict.

\section{Acceptance-Suite Evidence (L0--L2 + Op/scale tier)}
\label{sec:vol9_engine_acceptance_suite}

Requirements~11--15 are grounded in the 2026-06-16/17 ground-up engine-acceptance arc: a pytest regression suite (\kbleaf{src/tests/engine\_acceptance/}) built from the medium up, one excitation-layer at a time, each test a falsifiable physics claim with a frozen pre-registered pass/fail bin. The L0--L2 layers plus the Op-primitive / scale-invariance tier are GREEN, proving the engine is a \emph{valid medium}; they force \emph{zero} chords --- the chords (the $\Gamma=-1$ binding wall, the integer charge, the forced mass ratio) live at the unbuilt L3--L5 rungs. Each green test that strengthens a canonical claim is wired into the claim-DAG as a \kbleaf{sup-} derivation-support node (INVARIANT-S10 for the \kbleaf{sup-} registration; INVARIANT-S9 for the never-an-\kbleaf{exp-} bright-line; the suite map is \kbleaf{vol9/ch17-engine-requirements/engine-acceptance-suite.md}).

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\linewidth]{{T1.1_debug}.png}
\caption{L1 flagship (T1.1): a transverse photon propagates losslessly on the chiral srs grid. A localized one-way packet conserves energy to the integrator floor, never back-scatters, and translates on a single clean constant-speed diagonal at the lattice wave speed $c_{net}=c_{link}/\sqrt{3}$ ($R^2>0.99$) --- contrasted against the delocalized Bloch seed (a standing fringe) it replaced. Regenerated off the same stepper the functional test runs.}
\label{fig:vol9_acceptance_t1_1}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\linewidth]{{A1a_debug}.png}
\caption{L0-axioms DOF-capability finding (A1a, requirement~12): the srs graph is the chiral degree-3 (10,3)-a net (girth~10, enantiomorphs sign-flipped in mean writhe), but the implemented vector-TLM carries only the $2$ transverse (photon) DOF, not the full $6$ Cosserat DOF Axiom-1 specifies. The $3+3$ translational/micro-rotational grades that host mass (L3) and the winding=charge (L4) are an explicit medium-extension --- surfaced as an honest finding (flag-don't-fix), not faked.}
\label{fig:vol9_acceptance_a1a}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\linewidth]{{T2.2_debug}.png}
\caption{L2 headline (T2.2, requirement~14): a SYM gradient ($\varepsilon,\mu$ co-scaled) bends/retards a wave \emph{frequency-independently} (excess group-delay spread below $10^{-3}$ across the band) while keeping $Z=Z_0$ ($\lvert\Gamma\rvert<10^{-6}$, reflectionless) --- the AVE-distinct achromatic gravitational-lensing mechanism. The deflection magnitude is form-derived from the path integral $\int(S-1)\,dx$ but rides the input $A_0(x)$ profile, so the chord-vs-echo of the \emph{magnitude} is left OPEN, not asserted as a chord (\kbleaf{clm-k9up5c}).}
\label{fig:vol9_acceptance_t2_2}
\end{figure}

\section{RUNG-1 EXISTENCE Rung (L3 posited mass-cage: T3.3 / T3.4)}
\label{sec:vol9_engine_rung1_existence}

Requirements~16--18 are grounded in the 2026-06-17 RUNG-1 EXISTENCE arc: the suite
(\kbleaf{src/tests/engine\_acceptance/test\_l3\_mass\_cage.py}) extends past the valid-medium L0--L2 layers into the \emph{bound longitudinal-bulk} (A1 scalar) cage --- the mass precursor. It \textbf{POSITS} a saturated A1-scalar core (consistency-class; positing is legitimate --- it is NOT self-formation, which is the gated rung-2 / genesis decider) and tests what the corpus says a bound electron-cage must exhibit. \textbf{A1 SCALAR ONLY} throughout (\texttt{converter\_on=False} --- the two-3s guard, \kbleaf{master-equation.md}:20: never read charge/spin/$\mu$ off the scalar cage). Each green test wires into the claim-DAG as a \kbleaf{sup-} derivation-support node (the suite-map is \kbleaf{vol9/ch17-engine-requirements/engine-acceptance-suite.md}; the companion property$\times$bucket sheet is \kbleaf{vol2/.../electron-bound-resonator-coverage.md}, the electron = \kbleaf{BoundResonator} instance-1, \kbleaf{def-b42e9d}).

Four FORM-checks PASS --- and the one genuine chord-vs-echo question returned a clean ECHO:

\begin{itemize}
    \item \textbf{T3.3 $\Gamma=-1$ wall} ($\to$ \kbleaf{sup-1ecv2m} $\to$ \kbleaf{clm-kezk9z}, \kbleaf{clm-uatcql}) --- CONSISTENCY / FORM-chord. On the posited cage $\Gamma_\text{bulk}$ crosses the canonical OP2 engaged-gate $-0.25$ by $A=0.95$ ($\Gamma_\text{min}(0.95)=-0.283$, $\Gamma_\text{min}(0.99)=-0.454$), descends monotonically toward $-1$, and $\to0$ in vacuum; the literal $-1$ is UNREACHABLE (the S$_\text{min}$-clipped floor, by design). Read via the \textbf{$\alpha$-FREE} impedance route $Z_\text{eff}=\sqrt{S}\to0\Rightarrow\Gamma\to-1$, NOT \kbleaf{gamma\_em\_sq} (the $1-\alpha$ bake).
    \item \textbf{T3.4a mass = cutoff} ($\to$ \kbleaf{sup-xgx063} $\to$ \kbleaf{clm-ka5zdx}, \kbleaf{clm-unk0bd}) --- FORM-chord / VALUE-definitional. A DISCRETE gapped breathing eigenmode $\omega_\text{cutoff}\approx2.87$ (natural units; peak/mean$\approx$456, 23 zero-crossings) EXISTS. The mass-as-ground-state-cutoff-energy of the bound resonator is the FORM; $m_e$ the VALUE is NEVER a cage output (definitional anchor, \kbleaf{constants.py}:129; \kbleaf{electron-identification.md}:50).
    \item \textbf{T3.4b cold / $\alpha$-FREE $Q$} ($\to$ \kbleaf{sup-wuy333} $\to$ \kbleaf{clm-rtdmsn}, \emph{echo-corroboration}) --- the ONE genuine chord-vs-echo question. With both $\alpha$-bakes removed, the cold ring-down gives $Q_\text{ringdown}\approx30.8$, \textbf{NOT $137$}. Because $\alpha$ was never baked, the $\alpha$-free cold cage does not reproduce $137$ $\Rightarrow$ the corpus $Q=1/\alpha$ (\kbleaf{cvr\_model.py}:72) is an \textbf{instance-baked VALUE-ECHO}, not a cage-emergent chord. A clean chord-vs-echo NEGATIVE that \emph{corroborates} the already-canonical value-scoped verdict (\kbleaf{theorem-3-1-q-factor.md}:19, ``echo at the value level''); it strengthens no ``$Q=137$ is forced'' reading (there is none).
    \item \textbf{T3.4c zero-drive persistence} ($\to$ \kbleaf{sup-x7h9yh} $\to$ \kbleaf{clm-ka5zdx}, \kbleaf{clm-uatcql}) --- FORM. The bare posited cage holds a non-radiating standing mode with no drive (late/mid$\approx$1.31). This is PERSISTENCE of the saturation-locked standing structure (\kbleaf{ave-conserved-vs-pumped}: measured zero-drive, not pumped); it is NOT the topological-winding protection (winding sector, bucket B, not cage-testable).
\end{itemize}

\noindent
\textbf{Honest scope.} The rung POSITS the cage; it does not show it SELF-FORMING (the gated rung-2). The FORM-chords are ``GIVEN a saturated core, the corpus's reflective wall, gapped bound mode, and persistence manifest, $\alpha$-free'' --- CONSISTENCY-class, not emergence. No dimensionful electron VALUE ($m_e$, $e$, $\alpha$, $Q=137$) is a cage output.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\linewidth]{{T3.3_debug}.png}
\caption{RUNG-1 EXISTENCE (T3.3, requirement~18): the $\Gamma=-1$ wall on the posited saturated A1-scalar cage. $\Gamma_\text{bulk}$ (read on the PML-EXCLUDED interior via the $\alpha$-free $Z_\text{eff}=\sqrt{S}$ route) crosses the OP2 engaged-gate $-0.25$ by $A\approx0.95$ ($\Gamma_\text{min}(0.95)=-0.283$), deepens to $-0.454$ at $A=0.99$, and $\to0$ in vacuum; the literal $-1$ is unreachable (S$_\text{min}$ clip, by design). CONSISTENCY / FORM-chord (\kbleaf{clm-kezk9z}; \kbleaf{sup-1ecv2m}).}
\label{fig:vol9_acceptance_t3_3}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\linewidth]{{T3.4_mass_cutoff_eigenmode_debug}.png}
\caption{RUNG-1 EXISTENCE (T3.4a, requirement~16): a discrete gapped bound breathing eigenmode $\omega_\text{cutoff}\approx2.87$ (natural units) exists on the posited cage --- a sharply-peaked spectrum (peak/mean$\approx$456), not a continuum. Excited by a radial-shell breathing kick ($\partial_t V$ on the wall), NOT a monopole/DC kick (which only yields the bin-1 $1/n_\text{steps}$ relaxation artifact). FORM-chord (mass = ground-state cutoff energy); $m_e$ the VALUE is never read off the cage (\kbleaf{clm-ka5zdx}; \kbleaf{sup-xgx063}).}
\label{fig:vol9_acceptance_t3_4_mass}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\linewidth]{{T3.4_cold_Q_ringdown_linewidth_debug}.png}
\caption{RUNG-1 EXISTENCE (T3.4b, requirement~17): the cold, $\alpha$-FREE $Q$ from ring-down (envelope decay $+$ $-3$\,dB linewidth), with both $\alpha$-bakes removed. $Q_\text{ringdown}\approx30.8$, \emph{NOT} $137$ --- the clean chord-vs-echo NEGATIVE. Because $\alpha$ was never baked, the cold cage does not reproduce $137$, empirically confirming the corpus $Q=1/\alpha$ is an instance-baked VALUE-ECHO (\kbleaf{cvr\_model.py}:72), not a cage-emergent chord. Corroborates the value-scoped verdict at \kbleaf{theorem-3-1-q-factor.md}:19 (\kbleaf{clm-rtdmsn}; \kbleaf{sup-wuy333}, echo-corroboration).}
\label{fig:vol9_acceptance_t3_4_coldq}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\linewidth]{{T3.4_persistence_trace_debug}.png}
\caption{RUNG-1 EXISTENCE (T3.4c): zero-drive persistence of the bare posited cage. After the non-eigen transient sheds, the PML-excluded interior peak $|V|$ holds steady (late/mid$\approx$1.31, late\_min$\approx0.34\cdot$amp$_0$) --- a non-radiating standing mode. FORM (a conserved standing structure, measured zero-drive, not pumped); NOT the topological-winding protection (winding sector, bucket B). (\kbleaf{clm-ka5zdx}, \kbleaf{clm-uatcql}; \kbleaf{sup-x7h9yh}).}
\label{fig:vol9_acceptance_t3_4_persist}
\end{figure}

\section{Conservation Canaries}
\label{sec:vol9_engine_conservation_canaries}

Requirements~4 and~6 share a detector: the conservation canaries that distinguish a conservative \emph{energize-and-lock} coupling from a non-conservative \emph{pump}. A faithful cross-sector coupling sources one sector from another and takes the back-reaction from the same $H_{couple}$ --- energy moves between sectors but the total is conserved. A pump (the flag-gated EMF, requirement~4) adds energy that no back-reaction removes.

\begin{itemize}
    \item \textbf{Hamiltonian drift.} On a conservative run, the total $H$ is constant to integrator order; the canary is $|\Delta H / H| \sim 10^{-9}$ per step on float64 (Velocity-Verlet / leapfrog). A pump shows monotonic $H$ growth orders of magnitude above the float64 canary floor. \emph{This is why requirement~6 mandates float64}: on float32 the round-off drift ($\sim 10^{-7}$) swamps the $10^{-9}$ signal and the pump-vs-lock distinction is lost.
    \item \textbf{Total angular-momentum bound.} The microrotational sector's $|\mathbf{L}|$ must stay bounded with $d|\mathbf{L}|^2/dt \approx 0$ on a locked run. The EMF pump (\kbleaf{src/ave/topological/k4\_cosserat\_coupling.py}:550, positive-sign) drives $|\mathbf{L}|$ unbounded --- the genesis-24 detonation signature. The $|\mathbf{L}|$ gate is the direct read of requirement~4.
\end{itemize}

\noindent
\textbf{The energize-and-lock principle.} A stable bound state is built by energizing a conserved invariant and \emph{locking} it (boundary-confined, $\Gamma = -1$), not by pumping it. The genesis-arc result is that an amplifying cross-sector term grows the amplitude but is the failure mode: it never reaches a stable lock because it never conserves. An engine that passes both canaries on a closed run --- $H$-drift at the float64 floor, $|\mathbf{L}|^2$ flat --- is energizing-and-locking; one that fails either is pumping. The datasheet records the canaries so that the distinction is checkable, not assumed.
