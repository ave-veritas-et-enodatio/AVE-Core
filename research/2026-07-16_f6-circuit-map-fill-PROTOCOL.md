# F6 mode-count §5b — rigorous circuit-map fill PROTOCOL

**Date:** 2026-07-16 · **Class:** protocol (how to fill the gate — not a driver) · **Status:** ★ binding for any sibling prereg under [`2026-07-15_f6-mode-count-door_CHARTER.md`](2026-07-15_f6-mode-count-door_CHARTER.md) §5b.  
**Parents:** [`2026-07-16_f6-circuit-first-door-map_CHARTER.md`](2026-07-16_f6-circuit-first-door-map_CHARTER.md); saturation anatomy [`../manuscript/ave-kb/common/envelope-anatomy.md`](../manuscript/ave-kb/common/envelope-anatomy.md).

**What this is NOT.** Not an Arm C siphon. Not a CHANNEL-BOUNDED claim. Not permission to freeze without the pre-freeze consistency pass.

---

## 0 · Rule

Fill §5b **bottom-up from the junction**, not top-down from a door slogan.

Each of the six rows is a **triple**:

```text
claim | canonical cite (leaf:line or code:line) | engine observable / null test
```

Incomplete triple → table incomplete → **DO-NOT-MERGE / do not drive**.

---

## 1 · Fill order (do not skip)

### Step 0 — Name exactly one object

Pick **one** from the circuit-first taxonomy:

| Object | When this table is for… |
|---|---|
| Finished A1 wall | bias≠release / mirror ring / Op21-class wall consistency |
| Frontier crystallization | latent → T2 at growing surface (`node_creation`) |
| F6 occupancy chord | reading-i field door (KEEP SEPARATE from frontier) |

Two objects ⇒ **two tables**. Unification without a separator discriminator is forbidden.

### Step 1 — Ports (code identity)

| Cell | Must state |
|---|---|
| Claim | Which of the four K4 tetrahedral bond ports (`p0…p3` / `V_inc[...,p]`), which sites, which mask |
| Cite | `k4_tlm.py` bond/port layout + mask construction site |
| Observable | Port index set is stable under the run; no Cartesian 6-port fiction |

**Illegal:** “box face” unless a 1:1 map face-cell → tetrahedral bond is cited (file:line).  
**Falsifier:** mask includes sites that are not those bonds.

### Step 2 — Regime (declare, then measure)

| Label | Measurement bar (illustrative — freeze concrete tol in the prereg) |
|---|---|
| cold | \(A^2 \ll 2\alpha\), \(S\approx 1\) on claimed sites |
| knee | \(A^2 \sim 2\alpha\) |
| wall | \(S\to 0\) and/or \(\lvert\Gamma\rvert\to 1\) on claimed **ports** |
| rupture | past snap / pair-production regime (name the snap observable) |

| Cell | Must state |
|---|---|
| Claim | cold / knee / wall / rupture |
| Cite | envelope-anatomy / four-regimes / Ax-4 kernel leaf |
| Observable | scalar the engine reports (`S`, \(A^2\), bond \(\Gamma\), …) with frozen tol |

**Falsifier:** claim “wall” while the run sits at soft \(S\sim 1\) (Arm B failure mode).

### Step 3 — Port behavior (entailed, then checked)

Corpus entailment: cold ≈ matched; knee = small \(\Gamma\); wall = \(\lvert\Gamma\rvert\to 1\) mirror; rupture = snap.

| Cell | Must state |
|---|---|
| Claim | matched pass / small \(\Gamma\) / \(\lvert\Gamma\rvert\to 1\) mirror / snap |
| Cite | translation-circuit saturation rows + Op3 / master-equation branch |
| Observable | measured bond \(\Gamma\) or \(V_{ref}/V_{inc}\) on the Step-1 port set |

**Falsifier:** claimed \(\lvert\Gamma\rvert\to 1\) but measured \(\lvert\Gamma\rvert\ll 1\).

### Step 4 — Energy fate (must be entailed by Steps 2–3)

| If ports… | Energy fate may be… | Energy fate may **not** be… |
|---|---|---|
| matched / small \(\Gamma\) | in-network propagate / store | exterior T2 leave by fiat peel |
| \(\lvert\Gamma\rvert\to 1\) mirror | reflected in-network (refusal) | exterior ledger via \(V\)-scale siphon |
| new cells mint | new mode volume (frontier) | fixed-\(N\) leave without mint |
| dump-R / PML / Re(Z) | — | **forbidden** (killed classes) |

| Cell | Must state |
|---|---|
| Claim | reflected in-network / new cells / exterior ledger (only if entailed) |
| Cite | circuit-first corollary (“reflection ≠ T2 leave”) + object from Step 0 |
| Observable | energy ledger that matches the claim (in-network conservation vs mint vs exterior modes) |

**Falsifier:** wall + “energy leaves to `b[m]` by scaling \(V\)” → **table invalid**.

### Step 5 — Finished A1 protection (mechanism, not soft mask)

| Cell | Must state |
|---|---|
| Claim | how finished structure stays bias-stable using the **same** port/wall mechanism as Step 3 |
| Cite | bias≠release / envelope wall / Ax3 lossless interior |
| Observable | frozen bias/drain knives + why the door cannot move them if ports do what Step 3 says |

**Falsifier:** protection = arbitrary `CORE_R` mask while transfer is still a bulk/face siphon.

### Step 6 — Forbidden costumes (explicit non-goals)

For each closed class, one sentence: “this arm is not X because …”:

- face \(V\)-scale siphon (Arm B)
- PML / Cosserat sponge as T2
- Re(Z) absorb / dump-R / matched stub
- electron `radiation_leak`
- Arm A/B retune
- fake `node_creation`

---

## 2 · Pre-freeze consistency pass (all must pass)

1. Object ∈ {A1 wall, frontier, F6} — **exactly one**.  
2. Regime measurement matches regime label.  
3. Port \(\Gamma\) (or equivalent) matches port-behavior label.  
4. Energy fate ∈ entailment table for that port behavior.  
5. A1 protection uses the same mechanism as (3), not a parallel costume.  
6. No killed class appears in the implementation sketch.

**Fail any → do not freeze the prereg.**

---

## 3 · Worked fail — Arm B against this protocol

| Cell | Arm B answer | Rigorous fail |
|---|---|---|
| Object | implied “necessary door” / frontier-adjacent | not a single taxonomy object; WRONG-OBJECT |
| Ports | box faces | not tetrahedral bond IDs |
| Regime | soft sub-yield (unmeasured as wall) | never showed \(S\to 0\) / \(\lvert\Gamma\rvert\to 1\) |
| Port behavior | “leave” | no mirror measurement |
| Energy fate | exterior `b[m]` via \(V\)-scale | contradicts mirror entailment |
| A1 protection | soft `CORE_R` mask | not wall BC |
| Costumes | claimed not Re(Z) | **was** face \(V\)-siphon |

Receipt: [`2026-07-16_f6-arm-b-exterior-leave_result.md`](2026-07-16_f6-arm-b-exterior-leave_result.md) (BIAS-MOVED + WRONG-OBJECT).

---

## 4 · Practical sequence for the next real arm

1. **Probe only** (no door): tiny script reports \(S\), bond \(\Gamma\), in-network energy on a named port mask — fills Steps 1–3 triples.  
2. Fill energy fate from the entailment table. If the only honest fate is “reflected,” write a **wall consistency** prereg (e.g. Op21 / closed \(\Gamma\) ring) — **not** an ε→T2 CHANNEL-BOUNDED door.  
3. Freeze a mode-count door arm **only if** energy fate is still “T2 mode growth” *and* Steps 1–6 + consistency pass succeed. Today that almost certainly requires frontier / `node_creation` (map = NO) or a still-unbuilt mechanism that is not a siphon.

---

## 5 · Deliverable shape inside a sibling prereg

Every future arm prereg under the mode-count door must include a section:

```markdown
## § Circuit map (§5b) — FROZEN

Object: <A1 wall | frontier | F6>

| Question | Claim | Cite | Observable / null |
|---|---|---|---|
| Ports | … | … | … |
| Regime | … | … | … |
| Port behavior | … | … | … |
| Energy fate | … | … | … |
| A1 protection | … | … | … |
| Forbidden costumes | … | … | … |

Pre-freeze consistency: PASS (1)–(6) dated <ISO>.
```

Push that prereg **before** any driver (ave-prereg Step 3.11).

---

*Protocol only. Measure the junction until the cells entail each other; do not invent a door and backfill the table.*
