# PRE-REGISTRATION (FROZEN) — the LAST BOND under kernel collapse: premise audit, the discrete termination row, and its continuum reconciliation

**Date:** 2026-08-05
**Branch:** `research/last-bond-kernel-collapse`
**Written against `origin/main` =** `c4a546dc`
**Class:** DERIVATION prereg (research-doc + docket fragment ONLY; **mints no `clm-`/`def-`; propagates to no KB/manuscript leaf; changes no solidity; edits no falsification ledger; engine `src/ave` byte-untouched**).
**Provenance:** Grant's ruling of 2026-08-05, `_orchestration/docket-entries/2026-08-05-ruling-flag-causal-kernel-collapse.md` (on `origin/orchestration/ruling-flag-causal-0805`, PR #887 — the ruling's ONE routed premise with its KILL CONDITION is this lane's Task 1).
**SVA pilot case 5.**

**FREEZE RULE (the only rule that matters):** *no gate, tolerance, bin boundary, frozen numeric
parameter, coupling-enumeration entry, verdict wording or method element in §2–§8 may be changed
after any grep result, any algebraic result or any number produced by this lane is seen. If a
task's certification fails, this lane reports the failure for that task, adjudicates NO bin for it,
and routes to a successor with a new version number.* **UNRUN ≠ PASSED.**

---

## §0 — Standard Vacuum Analysis header (SVA v0.1-pilot + the pilot-2 numerical-conditioning row 11, adopted)

 1. SECTOR / OWNERSHIP:      Three sectors are named and kept apart. **T2-shear** owns the propagating observable (the ringdown wave and its terminal reflection) and the bond modulus `G`. **Cosserat rotation-winding** owns `G_c` (relative-rotation coupling, which is ALSO the on-site gap term `m² = 4G_c/I_ω`) and `γ` (couple-stress / curvature). **A1 dilatation** owns the DC bias profile `A(r)` that grades all of them, and — via `K = 2G` — the bulk branch. **EM/T2-transverse** is the mapped exception channel (`Γ_EM = 0`, SYM-matched, circulation-keyed `μ`) and is DECLARED OUT of the transport set whose collapse this lane audits. **Cross-wiring check run:** the "mass gap" of the origin statement is a **Cosserat `G_c`** object, NOT the A1 rest-mass store (`cosserat-mass-gap.md`:149 re-scope); this lane never uses gap language for mass and never uses A1 language for the winding sector. Inertia (`ρ`, `I_ω`) is **not** a transport coupling and is declared out of the audited set with its reason (§2.2).
 2. REGIME / PHASE-STATE:    **MODE** = small-signal AC reflection at a terminal plane on a static DC bias (a scattering problem at REAL frequency, not an eigenvalue problem). **REGIME** = sub-yield lossless-reactive on the cold side `A < 1`; the `A ≥ 1` region is Regime IV and enters ONLY as an arbitrary passive load `Z_beyond`, never as a solved domain. **PHASE-STATE** = cold lattice, Op14 ON as a static constitutive grade, `A(r) = r_sat/r`, `A = 1` exactly at `r_sat`. **DC bias point** = the gravitational grading. **A null in which the effect cannot exist under the frozen construction is an ARTIFACT-class finding, not a falsification**, and every null below is classified.
 3. CIRCUIT STATEMENT:       Before any framework word: **a lossless LC/TLM ladder of series inertances and shunt compliances, cut at a last bond whose shunt compliance is unbounded, terminated by an arbitrary passive load.** The observable is the **terminal reflection coefficient `Γ_L` at a declared plane** — a TOTAL observable (a ratio of two amplitudes at one plane), never a per-element slot. The mechanical↔electrical map is DECLARED, not assumed: `Z_shear = √(μρ) = traction/velocity`, i.e. **traction ↔ voltage, velocity ↔ current**; mass ↔ SERIES inductance, bond compliance `1/k` ↔ SHUNT capacitance (this topology is the DUAL of the mechanical layout and is derived in §3.1, not asserted). Free end (`T = 0`) is therefore the **SHORT** and `Γ = −1`; clamped end (`v = 0`) is the **OPEN** and `Γ = +1`.
 4. PLANE & PROJECTION:      **PLANE-LB (PRIMARY):** the **last-bond plane** — the inner terminal of the last cold node, i.e. the face at which the collapsed bond attaches. Every `Γ` headline of this lane is referenced to PLANE-LB and to the local characteristic impedance `Z_1` of the last intact cell. **PLANE-N0 (SECONDARY, mandatory):** the OUTER face of the last cold node, one series inertance `jωm_0` outward of PLANE-LB — reported at every configuration precisely because a plane shift changes the phase of a signed `Γ` and the sign-relativity ruling forbids a signed `Γ` travelling without its plane (`wall-taxonomy.md` §9 as shipped on `c4a546dc`, renumbered §10 by PR #886). **PROJECTION:** the collapsing bond loads the **SHUNT** branch (compliance `1/k → ∞`), which is why the same `S → 0` cutoff that gives a series-graded divergence gives `Z → 0` here; the series-vs-shunt assignment is DERIVED in §3.1 from the two field equations and is a frozen derivation, not a convention.
 5. CONSTITUTIVE PROVENANCE: `S(A) = (1−A²)^{1/2}` **DERIVED** (Ax 4, SHAPE-DERIVED-conditional per `axiom-register.md`:194). `A(r) = r_sat/r` **DERIVED-FORM / VALUE-IMPORTED** (the `7` rides GR-imported `ν_vac = 2/7`). `μ_shear = G_vac S` **DERIVED** (Op16 / Op14 chain). **`γ(A)` and `G_c(A)` grading laws: THE OBJECT OF TASK 1 — provenance UNDETERMINED at freeze and to be BINNED, not assumed.** `ρ = ρ_bulk` (RHO-A) vs `ρ_eff = ρ_bulk/S³` (RHO-B) **FORKED (FORK-3(b), OPEN)** — and this lane's central claim is that the terminal row is INDEPENDENT of that fork. `Z_beyond` (the beyond-wall load) **ARBITRARY BY CONSTRUCTION** — swept over the entire extended complex plane including `0` and `∞`, never chosen. `ℓ_node = ħ/(m_e c)` **IMPORTED-VALUE / DEFINITIONAL**. The bond-averaging rule (harmonic vs arithmetic) is **ENG-CHOICE at freeze and SWEPT**, with the substrate-native argument for the harmonic rule DERIVED in §3.2 and its failure mode reported.
 6. ENERGY LEDGER:           **No port is crossed anywhere in this lane and no loss word is used.** `Re{Z} = 0` at every element by Ax 3. The ladder is lossless, so `|Γ| = 1` **exactly** at every plane for every `|Γ_beyond| = 1`, and this is a GATE (`G-UNIT`), not an assumption. **Nothing is absorbed, dissipated, damped or lost at the wall, in the collapsed bond, or beyond it.** The statement "energy cannot transfer to the next node" is a **transport-coupling** statement (the through-path has zero series admittance), NOT a dissipation statement: the energy stays reactive on the cold side. The only port in the problem is the radiative one at infinity, outside every plane measured here.
 7. CALIBRATABILITY:         Every verdict-class output is dimensionless or a phase: `Γ` is a ratio of two amplitudes at one plane; `|Γ + 1|` is a dimensionless residual; the ρ-sensitivity is a dimensionless relative difference; the premise-audit output is a discrete bin. **`α` appears nowhere in any chain of this lane.** The one dimensional input (`ℓ_node/r_sat`) is carried as a dimensionless ratio throughout.
 8. DISCRIMINATION CLASS:    **DC-internal** — this lane derives a terminal boundary row of the substrate's own ladder; it produces no observable and no chord, and it is FENCED from producing one (§8). **Tautology filter, run at freeze:** "a zero spring transmits no force" IS a near-tautology, and the lane says so — the NON-tautological content is (i) whether canon actually grades **every** transport coupling to zero (Task 1, which can KILL the ruling), (ii) that the termination is **exactly** `ρ`-independent rather than asymptotically so, and (iii) that the harmonic (series-spring) averaging rule makes the collapse EXACT while the arithmetic rule does not — a discriminating statement about the discretization that a tautology cannot carry. **SM/GR counterfactual:** GR has no last bond and no bond modulus; the whole question is unavailable in the continuum theory, which is exactly what Task 3 reconciles. **This lane surfaces no discriminator and headlines none.**
 9. CERTIFICATION PLAN:      Gates §6 and fireability self-tests §7 are frozen here before any grep, any algebra and any number exists. **UNRUN ≠ PASSED.** Negative controls named in advance: `G-NC-SIGN` (the derived free-end sign must reproduce the echo-v2 prereg's independently-stated convention verbatim); `G-NC-ECHO` (the free-end member of echo-v2's own frozen `Γ_L` sweep must be reproduced by this lane's terminal algebra, read from the shipped JSON, not recomputed); `G-NC-ARITH` (the arithmetic averaging rule must NOT give an exact collapse — a control that the harmonic result is doing work). Determinism by two-run digest.
10. ADJUDICATION ROUTING:    **Task 1 settles the ruling's routed premise and can KILL it**: `PREM-UNIVERSAL` leaves the ruling standing on its own terms; `PREM-EXCEPTION` DEMOTES the 2026-08-05 ruling to CONDITIONAL and reopens the linear cross-channel conversion path; `PREM-UNDERDETERMINED` mints a canon question and defaults to nothing. **Task 2 settles whether the terminal row is exact-and-ρ-independent or only asymptotic.** **Task 3 settles the RELATIONSHIP between the discrete and continuum rows and adjudicates neither of them** — both stay on the record. This lane settles **nothing** about FORK-3(b), nothing about `FLAG-ECO`, nothing observational, nothing about Regime-IV interior physics, and nothing about `γ`/`G_c` VALUES (the two-band lane's `ENG-CHOICE placeholder` verdict is cited and not revisited). On every outcome the propagation target is **a research-doc result + a docket fragment only**; canonical propagation (wall-taxonomy §10, the FLAG-W leaf blocks, the FLAG-CANON `vol3/claim-quality.md`:122/:124 repair) is a **gated follow-on requiring Grant**, and this lane edits none of it.
11. ★ NUMERICAL CONDITIONING: **NAMED CANCELLATIONS, before the first expression is written.** (i) `1 − A²` at the near-wall nodes, where `ℓ_node/r_sat ≈ 6.0e-19` — **catastrophic in float64, returns exactly `0`**; computed instead from the cancellation-free `S² = x(2r_sat + x)/(r_sat + x)²` with `x = r − r_sat > 0`, the same closed form the echo-delay lanes froze. (ii) `Γ + 1 = 2Z_load/(Z_load + Z_1)` is the residual of a quantity whose two terms cancel to `−1` — it is NEVER formed by adding `1` to a computed `Γ`; it is evaluated from its own closed form. (iii) `1 + ρΓ`-class denominators do not arise (no Schur recursion in this lane; the terminal row is a single Möbius evaluation), and the `ABCD` route is DECLARED UNUSABLE here for the same reason echo-v2 found: it divides by zero at exactly the `|Γ| = 1` terminations that carry the whole result. (iv) `k_0 Z_beyond/(k_0 + jωZ_beyond)` at `Z_beyond = ∞` is evaluated as the analytic continuation `k_0/(jω)`, never as a float `inf` arithmetic. **DYNAMIC RANGE.** `ℓ_node/r_sat ~ 6e-19`; `S ∈ [1e-30, 1]` on the frozen sweep (deliberately three decades past echo-v2's `1.1e-9` window, to expose any hidden floor); `|Z_beyond|/Z_1 ∈ [1e-40, 1e+40] ∪ {0, ∞}`; `ρ_beyond/ρ_bulk ∈ [1e-30, 1e+30]`. **WORKING PRECISION.** mpmath `dps = 60` for every closed form, every gate comparison and every quantity built from `ℓ_node/r_sat` or from `S < 1e-6`. float64 is used ONLY for the sweep-orchestration bookkeeping and for cross-checking the mp results; **every float64 path has a gate that measures its own error** (`G-PREC`). **No quantity in this lane is computed in float64 without an mp re-run that measures the float64 error.**

---

## §0.1 — Substrate-native walk (`substrate-native-check`, fired BEFORE the first line of numerical code)

1. **K4 / srs connectivity, and the honest 1D reduction.** The radial path is modelled as a
   **one-dimensional cascade of node-spaced sections**, carried forward unchanged from the
   echo-delay lanes. Frozen: `the 1D radial reduction is a DISCLOSED modelling choice inherited
   from research/2026-08-04_echo-delay-regulated-sum_prereg-FROZEN.md and research/2026-08-05_echo-delay-v2-reach-through_prereg-FROZEN.md, its 3D-srs (z=3) correction is NOT computed by this lane, and no statement of this lane depends on the coordination number except through the frozen z-SENSITIVITY item below`.
2. **★ z-SENSITIVITY, stated because the ruling's premise is about BONDS and z counts bonds.**
   Frozen: `the terminal-row derivation of section 3 is stated for a node whose inward bonds ALL
   collapse together; if a last cold node retains ANY un-collapsed inward bond the derivation does
   not apply to it, and this lane reports the z-dependence of that statement explicitly rather than
   assuming the 1D cascade generalizes`.
3. **Op14 saturation enters four times and they are enumerated:** in the bond modulus
   `μ = G_vac S`; under RHO-B a second time in the inertia `ρ_eff = ρ_bulk/S³`; a third time in the
   local band top `ω_max(r) = β ω_C S^p`; a fourth time in the local characteristic impedance
   `Z(r) = ρ v = ρ_bulk c₀ S^{1−p}`. **This lane's terminal row must be shown independent of the
   second and fourth of those, which is exactly what Task 2(b) is.**
4. **The compactification is the medium's own order parameter.** `A = r_sat/r` IS the Ax-4
   saturation amplitude; `A = 1` IS the wall. No coordinate is chosen; the wall is a level set of a
   constitutive modulus, which is why it cannot be transformed away.
5. **Phase-space vs real-space (A46).** Every verdict-class observable of this lane is a
   dimensionless ratio or a phase at a declared plane. **α-CLEAN.**
6. **Boundary-not-bulk.** This lane is *entirely* a boundary computation: one bond, two nodes, one
   arbitrary load. If the physics does not live in the last bond, this lane measures nothing — and
   `G-BOND` (the collapse must be EXACT, not small) is the gate that says which.
7. **Group vs phase velocity.** Not used. The terminal row is evaluated at a plane, at a real
   frequency, from a load impedance; no group velocity enters. Declared so a successor does not
   import the echo-delay lanes' dispersion machinery into a statement that does not need it.
8. **★ NEW CHECKPOINT forced by this lane: what is a bond modulus when its two endpoints carry
   different `S`?** Springs in series compose **harmonically**, so a two-half-bond decomposition
   with one half at `S = 0` gives a bond stiffness of **exactly zero**; an arithmetic (parallel)
   average gives **half the cold value**. Frozen as an ENG-CHOICE and **SWEPT at both rules**,
   because the answer decides whether the ruling's collapse is exact or approximate — and the
   substrate-native argument for the harmonic rule (springs in series, not in parallel) is
   DERIVED in §3.2 and gated by `G-HARM`, not asserted.

---

## §0.2 — Pre-test physics check (`pre-test-physics-check`, Rule 16 — asked BEFORE the design locks)

> **Grant — your ruling says every transport coupling that rides the kernel is graded to zero at
> the last bond. Reading the canonical energy functional to enumerate them, I hit a plumber
> question I cannot answer from canon, and it is exactly where the kill condition lives.**
>
> The wall is a **strain** surface: `A = r_sat/r` reaches 1 and the shear bond modulus `G·S` goes
> to zero. That is the capacitor breaking down. But the substrate also carries a **bending**
> stiffness — the couple-stress modulus `γ`, the thing that gives the rotational sector its own
> wave speed `√(2γ/I_ω)` and, with `G_c`, the weak-force range `ℓ_c = √(γ_c/G_vac)`. In the
> canonical energy functional those two ride **different saturation arguments**:
> `cosserat_field_3d.py`:761 grades `G` and `G_c` by `S_ε² = 1 − |ε|²/ε_yield²`, while `:762`
> grades `γ` by `S_κ² = 1 − |κ|²/ω_yield²` — a **curvature** amplitude, not a strain amplitude.
> Meanwhile `:486`, in the same file, builds ONE combined `A² = |ε|²/ε_y² + |κ|²/ω_y²`.
>
> **The plumber question: at the `r_sat` surface, is the substrate bending-saturated as well as
> shear-saturated?** In bench terms — the dielectric has broken down, the capacitor is a short.
> Does the *transformer winding on the same core* also stop coupling, or does the core still carry
> flux across a surface where the dielectric is gone? Those are physically different failures, and
> the wall does not have to be both at once.
>
> **Why it decides the ruling and not just a detail.** If a static gravitational grading drives
> `|ε| → ε_yield` while leaving `|κ|` far below `ω_yield` — which is what a smooth `1/r` profile
> with a huge `r_sat/ℓ_node` ratio would do, because curvature is a **gradient** of an already
> small rotation — then `S_κ` stays near 1 at the wall, the couple-stress bond does **not**
> collapse, and there is a live transport channel across the last bond. That is exactly
> `COUPLING-EXCEPTION`: your "all transport dead = one big gap" would become "the shear transport
> is dead", the linear cross-channel conversion path reopens through the bending bond, and the
> ruling demotes to conditional.
>
> **What I am doing about it without waiting for you.** I am NOT resolving this by preference and
> I am NOT defaulting. Task 1 goes to canon and asks what grading law canon assigns — not what
> value, what LAW — for `γ` and `G_c`, with two-method verification on every absence. If canon
> states one, it binds. If canon is silent, the honest output is `PREM-UNDERDETERMINED` and a
> minted canon question with your name on it, and the ruling stands as RULED-PENDING that answer
> rather than either confirmed or killed. **I will not manufacture a grading law to save a premise.**

---

## §1 — Corpus position (`ave-prereg` grep, run at freeze, results recorded before any derivation)

This lane sits downstream of, and cites rather than recomputes:

| predecessor | what this lane takes from it | fence |
|---|---|---|
| `_orchestration/docket-entries/2026-08-05-ruling-flag-causal-kernel-collapse.md` (`origin/orchestration/ruling-flag-causal-0805`) | the four ruled points + the routed premise + the KILL CONDITION | not edited; not on this branch |
| `research/2026-08-04_coldq-axial-rhob_result.md` §2.1, §2.2, §2.3 and the CONTINUUM-VS-LATTICE CARVE at `:383` | the RHO-B Frobenius wall row, the indicial structure, the traction-exponent report, and the carve | **byte-untouched**; quoted, never rewritten |
| `research/2026-08-04_echo-delay-regulated-sum_result.md` | the certified RHO-A prompt delay law and the node-governed turning point | cited; **not recomputed** |
| `research/2026-08-05_echo-delay-v2-reach-through_result.md` | the frozen three-valued `Γ_L` sweep and its cost quantification | cited; the free-end member is **read**, not re-derived |
| `research/2026-08-05_two-band-kinematics_result.md` §1 | the `γ`/`G_c` **VALUE-PROVENANCE** verdict (`ENG-CHOICE placeholder`) | cited; **values are out of scope** — this lane asks only about the GRADING LAW |
| `manuscript/ave-kb/common/wall-taxonomy.md` §9 (as shipped on `c4a546dc`) | the sign-relativity ruling: plane, projection, computed-not-chosen | **not edited** — canonical propagation is a gated follow-on |
| `manuscript/ave-kb/common/standard-vacuum-analysis.md` | the SVA §0 header (this document's §0) | **not edited** |

**Prior-art check for the derivation itself, stated honestly at freeze:** the terminal-row algebra
of §3 is **textbook lossless-ladder theory** (a shunt element of vanishing impedance terminates a
line in a short). This lane claims **no novelty in the algebra**. What it claims to produce is
(i) the canon-side premise bin, (ii) the exactness-and-`ρ`-independence receipt for THIS ladder
under BOTH density branches, and (iii) the localization of where the continuum limit loses the
last bond. **Any temptation to headline the ladder algebra as a result is pre-emptively refused
here.**

---

## §2 — TASK 1: THE PREMISE AUDIT (the load-bearing task; the kill condition lives here)

### §2.1 — The question, stated so it cannot drift

**Not** *"what are the values of `γ` and `G_c`?"* (settled: `ENG-CHOICE placeholder`, two-band lane
§1). **The question is: does canon assign each transport coupling a grading law in `S`, and if so
which — and with which SATURATION ARGUMENT?** A coupling "rides the kernel" for the purposes of
the ruling **iff canon grades it by a factor that vanishes as the LOCAL wall amplitude reaches
yield at the last bond.** A coupling graded by a kernel with a *different argument* that does NOT
vanish at that surface **does not ride S for this purpose**, and the audit must say so.

### §2.2 — The enumerated coupling set (FROZEN — nothing may be added or removed after results)

**Operational definition of a transport coupling, frozen:** *a term in the canonical energy
functional whose energy density depends on the field at MORE THAN ONE node — equivalently, a term
containing a spatial derivative / finite difference. A term depending only on on-node field values
is an ON-SITE term and is audited separately. Inertia terms (`ρ`, `I_ω`) are coefficients of time
derivatives, not of spatial ones, and are DECLARED OUT of the transport set with that reason.*

| id | coupling | channel | canonical energy term | transport? |
|---|---|---|---|---|
| **C1** | `G` — shear bond modulus (symmetric strain) | T2-shear | `G·(ε_sym : ε_sym)` | YES |
| **C2** | `K = 2G` — bulk branch, folded as `(2/3)G(tr ε)²` | A1-bulk | `(2/3)G(tr ε)²` | YES |
| **C3** | `G_c` — Cosserat coupling (antisymmetric strain) | Cosserat | `G_c·(ε_antisym : ε_antisym)` | **MIXED** — `ε = ∂_j u_i − ε_ijk ω_k` carries both a gradient piece and a pure on-node `ω` piece; the on-node piece IS the gap `m² = 4G_c/I_ω` |
| **C4** | `γ` — couple-stress / curvature modulus | Cosserat | `γ·(κ : κ)`, `κ = ∇ω` | YES |
| **C5** | `k_op10` — Op10 quartic gradient (Faddeev–Skyrme `W₄`) | Cosserat (topological) | `k_op10·W₄(∇n̂)` | YES |
| **C6** | `k_hopf` — Hopf / Chern–Simons linking density | Cosserat (topological) | `k_hopf·(½A·B)` | YES |
| **C7** | `k_refl` — Op3 reflection density | cross-sector | `k_refl·(1/16)\|∇S\|²/S²` | YES |
| **C8** | `Z_0` — K4-TLM bond impedance (EM/T2-transverse) | EM | Op14 `Z_eff = Z_0/√S` | YES — **the declared exception channel** |
| **O1** | `ρ` (mass density) | A1 | coefficient of `u̇²` | NO — inertia, out of set |
| **O2** | `I_ω` (micro-inertia) | Cosserat | coefficient of `ω̇²` | NO — inertia, out of set |

**Frozen:** `this is the complete audited set; if the verification finds a transport coupling in
the canonical energy functional that is not in this table, that is a FREEZE ERROR of this lane, it
is reported as such in the result, the coupling is reported with its grading, and the bin is
reported as reached with a disclosed incomplete enumeration rather than silently extended`.

### §2.3 — The two-method verification protocol (MANDATED on every absence/completeness claim)

Every claim of the form *"canon does not state a grading law for X"* or *"the audited set is
complete"* is verified by **two independent methods whose file counts are reported side by side**:

- **METHOD A — `git grep`** on `origin/main` at `c4a546dc`, with a frozen pattern battery (§2.4),
  over `manuscript/`, `src/`, `research/`, `_orchestration/`.
- **METHOD B — an independent Python `re` walk** over the same file set, enumerated by
  `os.walk` rather than by a git pathspec, with the same pattern battery compiled as regexes.
  **This method exists specifically because `**` pathspec globs and shell-escaped `$…$` patterns
  have silently false-negatived in this workspace before.**

**Frozen:** `if METHOD A and METHOD B disagree on the hit set for any pattern, the DISAGREEMENT is
the reported result for that pattern, the larger hit set is used, and the discrepancy is diagnosed
in the result doc rather than reconciled silently`. **Frozen:** `both methods report their scanned
file counts, and a mismatch in scanned file count is itself reported`.

### §2.4 — The frozen pattern battery

For each coupling symbol `X ∈ {G, G_c, gamma, gamma_c, k_op10, k_hopf, k_refl, Z_0}` the battery is:
the bare symbol; the symbol adjacent to `S`; the symbol adjacent to `sqrt(1`; the symbol adjacent to
`saturat`; the symbol adjacent to `(A)`; the LaTeX forms `G_c`, `\gamma`, `G_{vac}`; and the
grading-verb forms `grade`, `graded`, `rides`, `scales as`, `\propto`. **Frozen: the battery is
fixed here and no pattern is added after any hit set is seen.**

### §2.5 — THE BINS (FROZEN; precedence `PREM-EXCEPTION` > `PREM-UNDERDETERMINED` > `PREM-UNIVERSAL`)

| bin | frozen condition | consequence, frozen |
|---|---|---|
| **PREM-UNIVERSAL** | **every** coupling in §2.2 rows C1–C7 is shown by canon to carry a grading factor that vanishes at the last bond, AND the EM exception C8 is shown to be the mapped, canonically-declared one | the ruling's premise HOLDS on canon's own terms; the ruling stands as ruled |
| **PREM-EXCEPTION** | **at least one** of C1–C7 is shown by canon NOT to vanish at the last bond | **THE KILL FIRES.** The named coupling reopens the linear cross-channel conversion path; **the 2026-08-05 ruling DEMOTES to CONDITIONAL** and this lane reports that plainly, in the headline, without softening, without a rescue attempt, and without proposing a repair |
| **PREM-UNDERDETERMINED** | canon states no grading law for at least one named coupling, and no PREM-EXCEPTION is established | **a canon question is MINTED, not a default.** The result enumerates exactly what canon is missing, per coupling, with the file:line of the closest canon gets; the ruling stands as RULED-PENDING that answer; **no grading law is invented and no engine default is promoted to canon** |

**Frozen:** `an ENGINE implementation is EVIDENCE about canon only where canon points at it. Where
the engine grades a coupling but no manuscript/KB leaf states the law, the honest bin is
PREM-UNDERDETERMINED for that coupling and the engine line is reported as the engine's choice,
NOT as canon.` **Frozen:** `the ruling's own premise sentence may not be used as evidence for
itself; a citation chain that terminates in the 2026-08-05 ruling is a CIRCLE and is reported as
one.`

### §2.6 — The precision item this lane must report whether or not it changes a bin

**Frozen:** `if the audit finds that a coupling rides a kernel whose value goes to zero, the result
must state whether the consequence at the wall is a GAP OPENING or a BAND COLLAPSE, because the
ruling's phrase "all transport dead = one big gap" is a physical claim about which. Specifically:
the Cosserat gap is m² = 4G_c/I_ω, so a G_c that rides S makes the gap CLOSE, not open; if the
whole rotational dispersion scales by the same factor the band COLLAPSES to zero width and a fixed
finite drive frequency finds no state — the same conclusion by a different mechanism. This is a
PRECISION NOTE on the ruling's wording, reported whether or not the bin changes, and it is NOT a
kill.`

---

## §3 — TASK 2: THE LAST-BOND ROW, DERIVED DISCRETELY (theorems, available before the run)

**Frozen:** `every statement of section 3 is a THEOREM available before any code exists and may NOT
be presented in the result doc as a discovery of the instrument. What the instrument adds is the
numerical certification that the frozen closed forms are what the algebra says they are, over the
frozen sweep, at the frozen precision.`

### §3.1 — The ladder, DERIVED (series/shunt assignment is not a convention here)

The 1D shear field equations, in the traction↔voltage convention declared in §0 row 3:

```
rho * dv/dt = dT/dx            (Newton)
dT/dt      = mu * dv/dx        (constitutive)
```

Against the telegrapher pair `dV/dx = -L' dI/dt`, `dI/dx = -C' dV/dt` with `V = T`, `I = v`:

```
L' = rho          (inertance, SERIES)
C' = 1/mu         (compliance, SHUNT)
Z_0 = sqrt(L'/C') = sqrt(rho*mu)          [matches the canonical Z_shear]
v_ph = 1/sqrt(L'C') = sqrt(mu/rho)        [matches the canonical c_shear]
```

Lumped on the node lattice with node spacing `ell`, cross-section `AX`, node mass
`m_n = rho_n * AX * ell` and bond stiffness `k_n = mu_n * AX / ell`:

```
spring n (between nodes n and n-1):  v_n - v_{n-1} = (j*om/k_n) * T_n   ->  SHUNT admittance j*om/k_n
mass n:                              T_{n+1} - T_n = j*om*m_n*v_n       ->  SERIES impedance j*om*m_n
```

**⇒ mass = SERIES inductance `m_n`; bond compliance `1/k_n` = SHUNT capacitance.** The circuit
topology is the DUAL of the mechanical layout. **This is derived, not chosen**, and it is what
makes the projection row of §0 a theorem: the collapsing bond loads the SHUNT branch.

Ordering inward from the observer: `… [L_1 = m_1] [C_1 = 1/k_1] [L_0 = m_0] [C_0 = 1/k_0] [Z_beyond]`,
where **bond 0 is the LAST BOND** and `Z_beyond` is the arbitrary beyond-wall load.

### §3.2 — THEOREM 1 (bond collapse). The last bond's stiffness is EXACTLY zero, not small

Decompose bond 0 into two half-bonds in series, one on each side of the wall surface: the cold half
with stiffness `2k_cold` and the beyond half with stiffness `2k_beyond`. **Springs in series
compose harmonically:**

```
k_0 = 1 / (1/(2k_cold) + 1/(2k_beyond))
```

With the kernel-riding modulus on the beyond half at `A >= 1`, canon's own clipped kernel gives
`S = sqrt(clip(1 - A^2, 0, 1)) = 0` exactly, hence `k_beyond = 0` exactly, hence

```
k_0 = 0     EXACTLY, for every finite k_cold                                 [HARMONIC rule]
```

**Control (frozen, must NOT reproduce this):** the arithmetic (parallel) rule
`k_0^arith = (k_cold + k_beyond)/2 = k_cold/2 != 0`. **Frozen:** `the harmonic rule is the
substrate-native one because two half-bonds in series carry the SAME force and add their
compliances; the arithmetic rule is the parallel-spring rule and is physically wrong for this
geometry. Both are computed; the arithmetic branch is a NEGATIVE CONTROL (G-NC-ARITH) whose job is
to FAIL to collapse, and if it collapses too then the collapse is not doing the work this lane says
it does.`

### §3.3 — THEOREM 2 (termination). The row is the free-end / stress-node SHORT, at PLANE-LB

The load seen looking inward at PLANE-LB (the node between `L_0` and `C_0`) is the shunt compliance
in parallel with everything beyond:

```
Z_C0     = 1/(j*om*C_0) = k_0/(j*om)
Z_load   = Z_C0 || Z_beyond = k_0 * Z_beyond / (k_0 + j*om*Z_beyond)
```

At `k_0 = 0` this is **`Z_load = 0` exactly**, and the limit is uniform over the whole extended
complex plane of loads:

```
Z_beyond finite, != 0 :  Z_load = 0 / (j*om*Z_beyond)      = 0
Z_beyond = 0          :  Z_load = 0                         (identically, all k_0)
Z_beyond = infinity   :  Z_load = k_0/(j*om)                = 0
```

Hence, referenced to the last intact cell's characteristic impedance `Z_1`:

```
Gamma_LB = (Z_load - Z_1)/(Z_load + Z_1) = -1     EXACTLY
```

**`T = 0` at PLANE-LB is the free-end / stress-node condition; `Gamma = -1` is its value in the
declared traction↔voltage convention (voltage-zero = short).** Equivalently, from the equation of
motion of the last cold node with `k_0 = 0`:

```
m_0 * xddot_0 = k_1 (x_1 - x_0) + k_0 (x_{-1} - x_0)  ->  m_0 * xddot_0 = k_1 (x_1 - x_0)
```

— the beyond-wall coordinate `x_{-1}` has left the equation **identically**, which is the free-end
equation of motion. **Two independent routes, same row.**

**PLANE-N0 (mandatory secondary).** Shifting the plane one series inertance outward:

```
Z_N0     = j*om*m_0 + Z_load = j*om*m_0
Gamma_N0 = (j*om*m_0 - Z_1)/(j*om*m_0 + Z_1),        |Gamma_N0| = 1 exactly
```

**The magnitude is 1 at both planes; the phase differs. This is the sign-relativity ruling's own
content and it is reported at every configuration, not buried.**

### §3.4 — THEOREM 3 (density-profile independence). EXACT, and second-order even off the limit

`Z_beyond` is built entirely from beyond-wall inertia and beyond-wall moduli. In `Z_load` it appears
ONLY inside `k_0 * Z_beyond / (k_0 + j*om*Z_beyond)`, i.e. only multiplied by `k_0`. Therefore:

```
(a) at k_0 = 0:   dZ_load/dZ_beyond = 0 identically  ->  Gamma_LB = -1 for EVERY beyond-wall
                  density profile, EVERY beyond-wall modulus, and EVERY beyond-wall termination,
                  including the free (0), clamped (infinity) and matched (Z_1) ones.
(b) off-limit:    Z_load = (k_0/(j*om)) * [1 - k_0/(j*om*Z_beyond) + O(k_0^2)]
                  ->  the beyond-wall load enters the leading term at RELATIVE order k_0,
                      i.e. the Z_beyond-dependence of Z_load is O(k_0^2) ABSOLUTE.
```

The **cold-side** density branch is separately independent: `rho` enters the row only through `Z_1`,
and `Gamma_LB = (0 - Z_1)/(0 + Z_1) = -1` for every `Z_1 != 0`. Hence:

```
RHO-A:  Z_1 = rho_bulk * c_0 * S_1^(1/2)   -> 0      ;  Gamma_LB = -1
RHO-B:  Z_1 = rho_bulk * c_0 * S_1^(-1)    -> infinity;  Gamma_LB = -1
arbitrary rho_beyond in [1e-30, 1e+30] * rho_bulk    ;  Gamma_LB = -1
```

**Frozen:** `the claim of this lane is EXACTNESS, not smallness. If the instrument measures a
non-zero |Gamma_LB + 1| at k_0 = 0 in the harmonic branch at any swept point, THEOREM 3 has failed
and the lane reports ROW-NOT-CERTIFIED.`

### §3.5 — THEOREM 4 (the approach is untouched)

`Gamma_LB` is a property of the elements at and inward of PLANE-LB. The certified delay laws of
PR #880 / #885 are integrals of `1/v(r)` and functions of `Z(r)` over cells **strictly outward** of
PLANE-LB. **No element outward of PLANE-LB is altered by Theorems 1–3**, so the approach physics —
the RHO-A prompt law, the RHO-B log law, the node-governed turning point, the achromaticity — is
unchanged. **Frozen:** `this lane CITES those results and RE-COMPUTES none of them; the only
predecessor number this lane reads is echo-v2's shipped Gamma_L = -1 sweep member, read from its
JSON, for the negative control G-NC-ECHO.`

### §3.6 — What Theorem 3 does NOT say (fence, frozen at freeze)

**Frozen:** `THEOREM 3 is a statement about the TERMINAL ROW ONLY. It does not say the density fork
is irrelevant to the observable: FORK-3(b) controls Z(r), c(r), the delay law, the optical length,
and the impedance taper of the approach, all of which the echo-delay lanes measured and none of
which this lane touches. It says exactly one thing: the fork does not select the END CONDITION.
Any restatement of this lane that drops the words "end condition" is a misquotation.`

---

## §4 — TASK 3: THE CONTINUUM RECONCILIATION (method + bins frozen; verdict NOT pre-stated)

### §4.1 — The two rows, stated side by side without preference

| | DISCRETE (this lane, §3) | CONTINUUM RHO-B (`2026-08-04_coldq-axial-rhob_result.md` §2.1) |
|---|---|---|
| object | terminal reflection at a finite last bond | branch selection at `eta = 0` |
| endpoint class | a bond with `k_0 = 0`, at FINITE node count | a **regular singular point** at INFINITE optical distance |
| the row | `Gamma_LB = -1` (free end / stress node) at PLANE-LB | `dphi/deta|_0 = 0` on `phi = eta^(-sigma) psi` — an **analyticity constraint**, not a BC |
| traction at the wall | `T_0 = k_0 * (dx) = 0 * finite = 0` **exactly** | `~ eta^(Re sigma - 1)`, **DIVERGES on BOTH branches** (measured: `-0.1459563385925332`, `-0.8540436614074667`) |
| traction-free solution | **is** the solution | *"not merely a different condition, it is an unsatisfiable one"* (that lane, §2.2) |

**This is a prima facie disagreement and this lane does not soften it.** The frozen job is to
determine its KIND.

### §4.2 — The frozen method

1. **Take the limit explicitly, not verbally.** Compute `Gamma_LB(ell_node)` and the round-trip
   optical delay `T(ell_node)` from the frozen closed forms at a geometric ladder of
   `ell_node/r_sat` values spanning the frozen range, and report both.
2. **Identify the indeterminate form.** Evaluate the wall traction two ways at the same physical
   place: (i) the continuum `T_cont = mu(r) * dpsi/dr` along each Frobenius branch, and (ii) the
   lattice `T_latt = k_0 * (x_0 - x_{-1})`. Report both and the form of their disagreement.
3. **Report the transport of the row to the observer.** The terminal row reaches an outside
   observer only through the phase factor `exp(-2 j om T(ell_node))`. Report `T(ell_node)` as
   `ell_node -> 0` and state whether `Gamma_LB` changes, whether its OBSERVABLE effect changes,
   or both.
4. **Negative control.** Repeat step 1 under RHO-A, where the optical length is FINITE, and report
   whether the same limit behaviour occurs. **If RHO-A shows the same behaviour, the proposed
   mechanism is not the optical divergence and the diagnosis is wrong.**

### §4.3 — THE BINS (FROZEN; precedence `BIN-C-CONTRADICT` > `BIN-C-DISJOINT` > `BIN-C-SHADOW`)

| bin | frozen condition |
|---|---|
| **BIN-C-SHADOW** | the continuum row is recovered as the `ell_node -> 0` limit of the discrete row, in the sense that a quantity computed on the lattice converges to the quantity the continuum row computes, **with no lattice-scale information surviving in the limit** |
| **BIN-C-DISJOINT** | the two rows are shown to be **different objects answering different questions** (a terminal impedance at a finite cut versus a branch selection at an infinitely distant endpoint), neither being the limit of the other, **AND the exact place where the continuum limit loses the last bond is named as a specific mathematical step** |
| **BIN-C-CONTRADICT** | at some finite `ell_node`, evaluated at the SAME plane for the SAME observable, the discrete row predicts a value the continuum row forbids |

**Frozen:** `BIN-C-DISJOINT requires the loss point to be NAMED and DEMONSTRATED, not asserted. A
BIN-C-DISJOINT verdict without an explicit demonstrated step is reported as UNLOCALIZED and is a
FAILURE of this task, not a result of it.` **Frozen:** `this task ADJUDICATES NEITHER ROW. Both
stay on the record with the relationship stated. This lane does not retract, demote, amend or
re-frame the RHO-B lane's derivation, and does not edit its file.`

---

## §5 — THE INSTRUMENT

**Driver:** `research/drivers/last_bond_kernel_collapse.py` → `research/drivers/last_bond_kernel_collapse_results.json`
**Number check:** `research/drivers/last_bond_kernel_collapse_number_check.py`, wired into `make verify` via its own Makefile target.
**Premise-audit verification:** the two-method scan of §2.3 is implemented inside the same driver
(METHOD B) and its METHOD A counterpart is a recorded `git grep` battery whose output is shipped in
the JSON. **Both file counts are shipped.**

**Frozen sweep (no member may be added or removed after any result is seen):**

```
S_last          in {1e-3, 1e-6, 1e-9, 1e-12, 1e-15, 1e-20, 1e-25, 1e-30}
rho branch      in {RHO-A (p=1/2), RHO-B (p=2)}                 [cold-side profile]
rho_beyond/rho_bulk in {1e-30, 1e-15, 1, 1e+15, 1e+30}
Z_beyond/Z_1    in {0, 1e-40, 1e-20, 1, 1e+20, 1e+40, INFINITY} [as the analytic continuation]
bond rule       in {HARMONIC, ARITHMETIC}                        [ARITHMETIC = negative control]
om/om_C         in {1e-25, 1e-19, 1e-13}
ell_node/r_sat  in {6.0238983090250982e-19, 1e-12, 1e-6, 1e-3}   [Task 3 limit ladder]
```

**Frozen:** `mpmath dps = 60 for every closed form; float64 used only for orchestration and for the
G-PREC cross-check; the determinism digest is computed over the full shipped result object with
_runtime_sec excluded; runtimes are written WITHOUT back-ticks and are NOT registered in the number
check (the #801 R3 lesson).`

---

## §6 — THE GATES (frozen before any number exists; UNRUN ≠ PASSED)

| gate | what it certifies | frozen tolerance / condition |
|---|---|---|
| **G-BOND** | THEOREM 1: the harmonic-rule last-bond stiffness is **exactly** zero | `k_0 == 0` exactly (mp), at every swept point |
| **G-ROW** | THEOREM 2: `Gamma_LB = -1` at PLANE-LB | `\|Gamma_LB + 1\| == 0` exactly (mp), at every swept point of the HARMONIC branch |
| **G-RHO** | THEOREM 3(a): exact independence of the beyond-wall load | `max` over the `Z_beyond` × `rho_beyond` grid of `\|Gamma_LB - Gamma_LB(Z_beyond=Z_1)\|` `== 0` exactly |
| **G-RHO2** | THEOREM 3(b): off-limit sensitivity is second order | fitted exponent of `\|dGamma/dZ_beyond\|` vs `k_0` in `[1.9, 2.1]` |
| **G-COLD** | THEOREM 3 cold-side: the row is identical on both density branches | `Gamma_LB(RHO-A) - Gamma_LB(RHO-B) == 0` exactly at every swept point |
| **G-UNIT** | Ax-3 losslessness at BOTH planes | `\| \|Gamma_LB\| - 1 \| <= 1e-50` and `\| \|Gamma_N0\| - 1 \| <= 1e-50` |
| **G-PLANE** | the plane shift is real and is reported | `\|Gamma_N0 - Gamma_LB\| >= 1e-6` at ≥ 1 swept point (**a plane shift that changes nothing would mean the plane declaration is vacuous**) |
| **G-PREC** | the float64 cross-check against mp | relative separation `<= 1e-12` on every quantity computed both ways |
| **G-COND** | the conditioning row is live | the cancellation-free `S²` form and a naive `1 - A²` float64 form are BOTH computed at `ell_node/r_sat = 6.02e-19`; **the naive one must return exactly `0.0`**, demonstrating the row caught something real |
| **G-SCAN** | the two-method scan agrees | METHOD A and METHOD B hit sets identical per pattern, and scanned file counts reported for both |
| **G-NC-SIGN** | negative control: the derived convention reproduces the independently-frozen one | the byte-exact substring `` `Γ_L = −1` in the traction↔voltage convention `` (U+2212 minus) must be PRESENT in `research/2026-08-05_echo-delay-v2-reach-through_prereg-FROZEN.md`, and this lane's derived `Gamma_LB` must equal that value. **If the predecessor's convention differs, this lane reports a CONVENTION CONFLICT and certifies nothing.** |
| **G-NC-ECHO** | negative control: this lane's derived row is a MEMBER of the predecessor's own frozen sweep, and the predecessor's own two-port makes that membership consequential | READ (never recomputed) from `research/drivers/echo_delay_v2_reach_through_results.json`: (a) the frozen sweep declaration string containing `Gamma_L in {0,-1,+1}` must be present, so `-1` is a swept member; (b) every `y8.junction_two_port` entry must carry `is_identity == true` and `W == 0`, which is the predecessor's own receipt that the depleted section provides NO screening, hence `Gamma_in = Gamma_L` at PLANE-JX |
| **G-NC-ARITH** | negative control: the arithmetic rule must NOT collapse | `k_0^arith != 0` and `\|Gamma_LB^arith + 1\| > 1e-30` at ≥ 1 swept point |
| **G-DET** | determinism | two full runs, identical digest, shipped objects byte-identical apart from `_runtime_sec` |

---

## §7 — THE FIREABILITY SELF-TESTS (each MUST fire; a gate that cannot fail is not a gate)

| self-test | frozen firing threshold |
|---|---|
| **FT-BOND** | injecting `k_beyond = 1e-300` (not exactly 0) must make `G-BOND` FAIL |
| **FT-ROW** | injecting `k_0 = 1e-300 * k_cold` must make `\|Gamma_LB + 1\| > 0`, i.e. `G-ROW` must FAIL |
| **FT-RHO** | at that injected `k_0`, the `Z_beyond` grid must produce a spread `> 0`, i.e. `G-RHO` must FAIL |
| **FT-PLANE** | at `om -> 0` the two planes must COINCIDE (`\|Gamma_N0 - Gamma_LB\| -> 0`), i.e. `G-PLANE` must fail at that limit — demonstrating the plane effect is frequency-carried and not an artifact |
| **FT-ARITH** | the arithmetic control at `S_last -> 0` must still give `\|Gamma+1\| > 1e-30`, i.e. it must fail to collapse for the RIGHT reason (`k_0 = k_cold/2 > 0`) and not because of a floor |
| **FT-SCAN** | a deliberately-absent sentinel pattern must return zero hits on BOTH methods, and a deliberately-present sentinel pattern must return the SAME non-zero hit set on both |
| **FT-COND** | the naive float64 `1 - A²` must return exactly `0.0` at the innermost ratio (if it does not, the conditioning row was mis-sized and is reported as such) |

**Frozen:** `any RUN gate FAILS, or any self-test fails to fire, or any gate is UNRUN ⇒ this lane
reports ROW-NOT-CERTIFIED for the affected task, adjudicates NO bin for it, and routes to a
successor with a new version number. Thresholds are NOT retuned after a result is seen.`

---

## §8 — FENCES (what this lane does NOT license)

1. **No canonical propagation.** wall-taxonomy §10, the FLAG-W leaf blocks, and the FLAG-CANON
   (`vol3/claim-quality.md`:122/:124) claim-quality repair are **gated follow-ons requiring Grant**
   and this lane edits none of them. Research doc + docket fragment ONLY.
2. **No `clm-`/`def-` minted; no solidity moved; no falsification-ledger edit; `src/ave` byte-untouched.**
3. **No predecessor file modified.** Every predecessor named in §1 is byte-untouched, discharged by
   an empty `git diff --stat` receipt against the freeze base.
4. **No adjudication of FORK-3(b)**, of `FLAG-ECO`, of the RHO-B lane's bins, of the polar family,
   or of anything observational.
5. **No Regime-IV physics.** The beyond-wall region is an arbitrary passive load and is never
   solved. This lane makes NO statement about what is inside the wall.
6. **No `gamma`/`G_c` VALUES.** The two-band lane's `ENG-CHOICE placeholder` verdict is cited and
   not revisited. This lane asks only about GRADING LAWS.
7. **No discriminator, no chord, no observational claim.** DC-internal by declaration (§0 row 8).
8. **No repair of a kill.** If `PREM-EXCEPTION` fires, this lane reports it and stops. It does not
   propose a fix, does not draft a replacement premise, and does not soften the wording.

---

> **Freeze receipt.** This prereg is COMMIT 1 of this lane and is pushed **ALONE** — before any
> driver code exists, before any grep of the pattern battery has been run, and before any number
> produced by this instrument exists.
