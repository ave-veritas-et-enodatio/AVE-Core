# Continuum radial-acoustic solver — STAGE 1 RESULT (instrument certification: **WITHDRAWN → REPAIRED → RE-EARNED: `A_CERTIFIED`**)

Prereg-file: research/2026-07-28_continuum-radial-solver-stage1_prereg-FROZEN.md

> **★THE CLASS WAS WITHDRAWN BEFORE IT WAS RE-EARNED — read this before the gate table.** The adversarial review of PR #801 returned one CRITICAL and three MAJOR findings. Two were the same defect: **G3 and G2 each divided two BIT-IDENTICAL computations**, so neither gate could fail.
> - **G3** (`gate_G3_ortho_reduction`) called `build_profile(cold, shell_ortho=(1.0, 1.0))` and `build_profile(cold)` — and `(1.0, 1.0)` **is** the default. The two calls were the same call; `rel = 0.0` held by construction, not by physics. Reviewer's mutation receipt: corrupting `ortho_layer` so unit gains no longer reproduce `iso_layer` — *exactly* the defect G3 names — moved the physics (`B_ortho` `0.05788` → `0.05824`) while G3's ratio stayed exactly `0.0`, and **all nine gates still passed, all four self-tests still fired, and `CLASS A` still returned.** No self-test covered G3.
> - **G2** (`gate_G2_uniform_null`) evaluated the caged arm at `s_rail = 1.0` against `build_profile(..., uniform=True)`; at zero contrast those code paths produce identical layer stacks, so the null certified that Python computes one expression twice.
>
> **The prereg's own frozen rule decides this and it is not discretionary.** §6: `A gate that cannot fail is not a gate.` §8 CLASS C: **`A gate that cannot fail voids the certification exactly as hard as a gate that fails`**. The class was therefore **withdrawn to `C_NOT_CERTIFIED_VOID` on the finding, in its own commit, before a single line of repair** — so the record cannot read as a label quietly preserved across a fix.
>
> **It was then re-earned by a RE-RUN, not restored by the fix.** G3 now compares the orthotropic path at unit gains against a genuinely independent isotropic construction (`iso_shell=True` — new code; there was previously *no* path that built the graded shell from `iso_layer`) on both source fittings, and carries a new self-test **FT-5** that mis-normalizes `ortho_layer`. G2's null arm is now `uniform_reference_profile`, a homogeneous medium built from first principles without calling `build_profile`, with FT-2 re-pointed at it. **Mutation receipts on the repaired gates:** the `ortho_layer` corruption now puts G3 at `1.9100e-04` (**FAIL**), and a transfer-kernel corruption puts G2 at `5.2800e-07` (**FAIL**) where the pre-repair comparison returned `2.2204e-16` and would still have passed. **The repaired battery returns `A_CERTIFIED`** — the underlying physics claims were true; what was missing was gates that could have said otherwise. Three further self-tests (FT-6/FT-7/FT-8) were minted so that eight of nine gates carried a breaking self-test, with the ninth (G7) left on its prereg-frozen caveat. **A second, independent re-verify audit then falsified that caveat by measurement** — G7 FAILS at `R_match/r_core = 16`, a value the prereg's own frozen G8 sweep declares admissible — so **FT-9 was minted for G7 and the caveat was dropped** (§3.1). All nine gates now carry a breaking self-test.
>
> **Nothing was relaxed to get here** (Rule 11), and **the frozen prereg is BYTE-UNTOUCHED**. **Three** frozen/merged artefacts are themselves defective — the prereg §8 CLASS-A reachability argument, the prereg §5/§7(i) claim that G7 is *"STRUCTURALLY exact"*, and the merged charter's I8 first clause — and all three are **disclosed and routed (§8.7, §8.14, §9.6), never amended in place**.

**Date:** 2026-07-28 (repaired + re-run same day, on the PR #801 adversarial review) · **Class:** INSTRUMENT-CERTIFICATION result (research-doc; **mints no `clm-`/`def-`; propagates to no KB/tex leaf; banks NO physics verdict**) · **Charter:** `research/2026-07-21_continuum-radial-solver_CHARTER.md` (#789) · **Driver:** `research/drivers/continuum_radial_solver.py` → `research/drivers/continuum_radial_solver_stage1_results.json` (deterministic, no RNG; runtime shipped in `_runtime_sec`, seconds not minutes, and machine-dependent — see §8.12).

**★Every number quoted in this document is machine-checked against the shipped JSON.** Three separate retyping defects were found by the review (F5/F13/F14), all inside sections that declared their numbers were read from the JSON. The remedy is not care, it is a check: `research/drivers/continuum_radial_solver_number_check.py` holds a registry mapping every numeric token in this document to its JSON path (or to an explicit, reasoned allow-list entry for tolerances, frozen-string constants and derived ratios), asserts the token is the correctly-rounded value at its own quoted precision, and **fails if any numeric token in this file is neither registered nor allow-listed**. Run it after any edit to this document.

**Rulings executed (Grant, 2026-07-28, verbatim `[sic]`: `"D2: disclosed, D3: follow rec, D4: do it, D5: do the rec"`).** D2 = the disclosed `β ∈ {0,1,3}` sweep; D3 = the charter's recommendation (transfer-matrix primary + analytic matched-asymptotics backstop, T1 (a)+(c)); D4 = build stage 1 now on profile-independent gates; D5 = feed the #796-measured vessel profile. **D1 (the sector-crossed `c²`) REMAINS HELD** — this build and this certification are **D1-INDEPENDENT**: no gate consumes a `c²`, and no `c²` is evaluated anywhere in the outputs.

> **★THE HEADLINE — and what it is NOT.** The instrument is **CERTIFIED (`A_CERTIFIED`), on the repaired battery and the re-run**, not on the withdrawn shipped run: all nine frozen validation gates PASS on both source fittings and both profile arms, all four frozen gate-fireability self-tests FIRE, and all five repair-added self-tests FIRE — **every one of the nine gates now carries a self-test that breaks it, and no gate is dispositioned by a caveat.** **This is an instrument statement and nothing more.** `stage 1 banks NO physics verdict; every physics-shaped number in these outputs is an INSTRUMENT-LIVENESS DEMONSTRATION and is labelled DEMONSTRATION — no verdict banked`. Two load-bearing scope findings are surfaced, not resolved: **(1)** the `n = 0` spherically-symmetric channel the charter specifies **cannot host** either of the two objects the charter's C1 and C2 name — F2's `p = 2` is a DISPLACED-source (`n = 1` dipole) statement and the structural added-mass is the `n = 1` effective-density coefficient (§7); **(2)** D5 asked for the measured profile, but #796 shipped scalar EXTREMA plus a C-V reconstruction it grades corroborative-only — **no resolved radial `ε(r)` exists**, so the profile's radial SHAPE is an engineering choice and says so in every output (§5). Neither is reframed here; both route to Grant / the auditor lane.

---

## §1 — Certification verdict (read from the shipped JSON, `certification` block)

| Field | Value |
|---|---|
| `class` | **`A_CERTIFIED`** — WITHDRAWN on the review finding, then **RE-EARNED by the repaired re-run** (the withdrawal is its own commit, ahead of every repair) |
| `all_gates_pass` | `true` |
| `gate_fireability_selftest_pass` | `true` (the frozen FT-1…FT-4 set, frozen definition unchanged) |
| `repair_selftest_pass` | `true` (the repair-added FT-5…FT-9 set — an additional NECESSARY condition, §3.1) |
| `measured_certified_band` (`k·r_core`) | `[1e-8, 1e-6, 1e-4, 1e-3, 1e-2, 1e-1, 0.3, 1.0, 3.0, 4.0]` — the full frozen band |
| `measured_G4_overlap_subband` | `[1e-8, 1e-6, 1e-4, 1e-3]` — the full frozen overlap |
| `scope_reductions` | `[]` (empty ⇒ CLASS A rather than CLASS B) |
| `determinism_digest` | `efea5059a98d0503b094737d9e7477b1d737dccba87bf9545916c6993cb25576` |
| `_runtime_sec` | shipped in the JSON; **deliberately not quoted here** — machine-dependent and excluded from the determinism digest by the frozen definition itself (§8.12) |

**Frozen:** `all of G1..G9 PASS on both source fittings AND gate_fireability_selftest_pass = True`

**★And the strengthening that is NOT in the frozen criterion, stated so it is not mistaken for one.** FT-5…FT-9 are repair-added, post-freeze. They are wired as an *additional* necessary condition: a repair-added self-test that fails to fire forces `C_NOT_CERTIFIED_VOID` by the §8 fireability rule. They can only turn a PASS into a FAIL, never the reverse, so they **strengthen the frozen criterion and never relax it** (Rule 11). `gate_fireability_selftest_pass` keeps its frozen definition byte-exactly and is reported separately from `repair_selftest_pass`. **The precedence was re-verified by mutation with FT-9 present** (§8.13): with all nine gates passing and all four *frozen* self-tests firing, making FT-9 alone fail to fire still returns `C_NOT_CERTIFIED_VOID`.

**Determinism.** **Frozen:** `two independent full driver runs produce an identical results digest (SHA-256 over the results object minus timing fields)` — RUN as two independent full driver invocations; both returned `efea5059a98d0503b094737d9e7477b1d737dccba87bf9545916c6993cb25576`, and the full result objects minus timing compared equal field-by-field. **PASS.**

**Runtime.** **Frozen:** `total certification-battery runtime <= 600 s on the reference machine; a longer run is disclosed, not silently accepted` — the measured value is shipped as `_runtime_sec` with the boolean verdict `runtime_within_budget = true`; it is seconds, not minutes. **No runtime numeral is quoted in this document, on purpose** (§8.12). **PASS.**

---

## §2 — THE NINE VALIDATION GATES (each pass/fail with its number)

Every gate below is evaluated for **both source fittings** and, where a profile arm exists, for **both the isotropic baseline and the D5-measured orthotropic vessel**; the reported number is the WORST case across all arms. **Frozen:** `every gate is evaluated for both source fittings (prescribed displacement and prescribed traction) and passes only if it passes for both`

| Gate | Frozen tolerance | Measured (worst arm) | Margin | Self-test / caveat | Verdict |
|---|---|---|---|---|---|
| **G1** Lamé exterior `∇·u = 0` static limit | `≤ 1e-10` | **`2.7003e-14`** | `3.7e3 ×` | FT-1 | ✅ PASS |
| **G1b** multi-radius agreement | `≤ 0.25` | **`6.0687e-03`** | `41 ×` | FT-1 | ✅ PASS |
| **G2** uniform-medium NULL *(REPAIRED)* | `≤ 1e-12` | **`1.8763e-13`** | `5.3 ×` | FT-2 | ✅ PASS |
| **G3** orthotropic → isotropic reduction *(REPAIRED)* | `≤ 1e-12` | **`0.0000e+00`** (exact, between two INDEPENDENT constructions) | exact | FT-5 | ✅ PASS |
| **G4** transfer-matrix ↔ matched-asymptotics | `≤ 1e-6` | **`4.8581e-07`** | `2.1 ×` | FT-4 | ✅ PASS |
| **G5** Ax3 energy balance | `≤ 1e-10` | **`1.4062e-13`** | `7.1e2 ×` | FT-3 | ✅ PASS |
| **G5b** transfer matrix is real | `≤ 1e-14` | **`0.0000e+00`** (exact) | exact | FT-3 | ✅ PASS |
| **G6** layer-refinement convergence | `≤ 1e-3` | **`1.0433e-04`** | `9.6 ×` | FT-6 | ✅ PASS |
| **G7** drive-amplitude independence | `≤ 1e-12` | **`2.9550e-13`** | **`3.4 ×` — the tightest margin in the battery relative to its achievable numerical floor** | **FT-9** (§3.1) | ✅ PASS |
| **G8** matching-radius independence | `≤ 1e-9` | **`5.0136e-11`** | `20 ×` | FT-7 | ✅ PASS |
| **G9** band conditioning | `≤ 1e12` | **`5.0202e+10`** | `20 ×` | FT-8 | ✅ PASS |

**★The margins do NOT match the frozen reachability argument, and that is disclosed rather than glossed (review F2).** The prereg §8 argues CLASS A is reachable because *"the scouted margins on every gate exceed the frozen tolerance by `≥ 4` orders of magnitude"*. **The measured run contradicts that on every gate except the two that land exactly on zero:** the largest finite margin is G1's `3.7e3 ×` (`3.6` orders) and the smallest is G4's `2.1 ×` (`0.3` orders). The frozen argument is wrong about the *size* of the headroom; it is not wrong about *reachability*, which the run demonstrates directly. The prereg is BYTE-UNTOUCHED — the defective frozen sentence is disclosed in §8.7 and routed in §9.6, not amended.

**What changed under repair, and what did not.** Every gate untouched by the repair is **bit-identical** to the withdrawn run (`G1 2.7002675694072622e-14`, `G4 4.858126515168571e-07`, `G5 1.406175503036591e-13`, `G6 1.0432960302206345e-04`, `G7 2.9550062722232135e-13`, `G8 5.0136185296992416e-11`, `G9 50202390756.2205`), verified field-by-field against the pre-repair JSON. Only **G2** moved (`0.0` → `1.8763e-13`, because it now divides two genuinely different computations) and **FT-2** moved in its last digits (`1.011991657e-04` → `1.011991659e-04`, because it was re-pointed at the repaired null). G3 still reads exactly `0.0` — but now as a measurement that could have read otherwise, which is the whole difference.

**The two gates the charter names explicitly (R5), verbatim:**

- **G1 — the Lamé exterior `∇·u = 0` static limit for a graded shell in an infinite medium (the gate #782 also carried).** **Frozen:** `lame_ratio ≡ max over r in {1.5, 2.5, 3.5} of |div u|(r) / |div u|(0.5) <= 1e-10` — measured `2.7003e-14`. Companion **Frozen:** `max|Δ div u| / mean(div u) <= 0.25 across the three exterior radii` — measured `6.0687e-03`. The exterior dilatation is computed ANALYTICALLY from each layer's modal decomposition, not by finite differencing the reconstructed field.
  - **★CORRECTION (review F10) — the `0.25` tolerance's provenance was mis-stated here, and in the direction that flattered it.** The withdrawn text read *"the #782 shell-agreement tolerance, inherited unchanged; #782's own measured value on the lattice was `0.036`"*, which reads as *#782 measured `0.036` against this tolerance*. **The truth is the opposite: #782 measured `0.6261`, `0.5853` and `0.5680` against that tolerance, on all three of its rows, and FAILED it** (`rve_aggregation_bench_results.json`, `leg2_lame_gate.*.frozen_shell_agreement_rel`, each with `frozen_shell_agreement_rel_pass_tol0p25 = false`; and #782's own result doc §7.6 states it "FAILS as written on all three rows"). The `0.036` is a **different criterion** — #782's frozen *deliverable*, the exterior/**interior** `∇·u` ratio at tolerance `0.10`, on which its Lamé PASS was re-banked.
  - **Re-justified on its actual merits, since the original justification is void.** #782's failure was diagnosed there as a genuine **near-zero-denominator pathology**: both of its exterior ratios are `~0` by the div-free result, so their *relative* difference is ill-conditioned on a lattice field. Stage 1 inherits the *number* `0.25` from #782's frozen text, but the criterion is **well-conditioned here for a structural reason and not by luck**: the exterior dilatation is evaluated analytically from each layer's modal coefficients rather than differenced from a noisy discrete field, so the three radii return a smooth `1e-14`-scale residual whose relative spread is a meaningful `6.0687e-03`. **The stage-1 PASS therefore carries no inherited evidence from #782 — #782 failed this criterion — and stands only on its own construction.**
- **G2 — the uniform-medium NULL (★REPAIRED, review F3).** **Frozen:** `at zero contrast: |rho_N - 1| <= 1e-12 AND rho_S <= 1e-12 AND |r_Z - 1| <= 1e-12` — measured worst deviation `1.8763e-13` across `k·r_core ∈ {1e-3, 0.3}` × both source fittings. **Frozen (KEEP-BOTH):** `the uniform-medium NULL is read on BOTH conventions: rho_N -> 1 and rho_S -> 0; neither convention is redefined in place (KEEP-BOTH)`.
  - **What the repair changed.** The null arm was `build_profile(..., uniform=True)` and the scatterer arm `build_profile(..., s_rail=1.0)`; at zero contrast those construct the *same layer stack*, so the gate divided a number by itself and returned exactly `0.0` **with the transfer kernel corrupted**. The null arm is now `uniform_reference_profile` — one homogeneous region `r ∈ [r_s, R_match]` at the cold moduli, built from first principles without calling `build_profile`. A uniform medium has no layers, so the single region is what the physics specifies; any subdivision would be an artefact of the caged path.
  - **What the gate now actually certifies.** That the **265-layer graded assembly at zero contrast reproduces the exact one-region analytic homogeneous solution** to `1.8763e-13` — i.e. that the layer machinery introduces no spurious scattering. That is a real statement about the instrument; the previous one was a statement about Python.
  - **Mutation receipt.** Corrupting the per-layer transfer kernel (state map `× (1+1e-9)`) puts the repaired G2 at `5.2800e-07` — **FAIL**. The pre-repair comparison under the *same* corruption returns `2.2204e-16` and would still have PASSED, because the corruption cancelled exactly between two bit-identical arms.

> **★DEFINITIONAL CONFLICT SURFACED (flag-don't-fix, not resolved by fiat).** The charter's R5(b) asks for `ρ_N → 0` in the uniform-medium null. #775's `ρ_N` is a caged/uncaged POWER RATIO whose no-scatterer value is `1` by construction (its measured band is `0.26–2.90`, and the derivation doc's "static-release `ρ_N` … plateau `~0.3`" is a ratio, not a residual). Those two statements cannot both hold of one symbol. Stage 1 does **not** redefine either in place: it freezes **both** observables (`rho_N` = the #775 ratio, `rho_S ≡ |rho_N − 1|` = the charter's residual). Which convention the charter's R5(b) intends is routed to the auditor lane / Grant; no charter text is edited here.
>
> **★"reports both everywhere" was false as shipped, and is now true (review F8).** In the withdrawn run `rho_S` appeared **only** inside `G2_uniform_null` and `FT2_null_liveness` — the two places where it is trivially `≈ 0` or a threshold — and in **no** demonstration output, so the KEEP-BOTH pair was not in fact carried through the results. The repair emits `rho_S` alongside `rho_N` at every band point of the liveness demonstration and at the matched-asymptotics limit (`liveness_demonstration.*.rho_S_by_k_r_core`, `.rho_S_matched_asymptotics_k_to_0`), which is what makes the KEEP-BOTH claim checkable rather than asserted. The substance of KEEP-BOTH was always sound; only its coverage claim was wrong.

**The remaining gates, one line each:**
- **G3 (★REPAIRED, review F1 — the CRITICAL finding).** **Frozen:** `|B_ortho(hoop=1,radial=1) - B_iso| / |B_iso| <= 1e-12` — measured `0.0000e+00` **exactly**, on **both source fittings**, and now **between two independent constructions**. The withdrawn version compared `build_profile(cold, shell_ortho=(1.0, 1.0))` with `build_profile(cold)` — the same call, because `(1.0, 1.0)` is the default — and there was **no code path anywhere in the driver that built the graded shell from `iso_layer`**, so the gate had no isotropic arm to compare against. The repair adds one (`build_profile(..., iso_shell=True)`, which assembles the shell from `iso_layer` and never touches `ortho_layer`, and refuses non-unit gains rather than ignoring them), evaluates the gate on both source fittings (the frozen source-fitting axis the old G3 silently skipped), and reports a dynamic `rho_N` diagnostic alongside the frozen static-`B` criterion (`0.0000e+00` there too).
  - **Mutation receipt.** Corrupting `ortho_layer` so that unit gains no longer reduce to `iso_layer` — the reviewer's mutation, which left the withdrawn G3 at exactly `0.0` with CLASS A standing — puts the repaired G3 at `1.9100e-04`, **FAIL**. Its shipped self-test **FT-5** carries the same mutation at `misnorm = 1e-3` and fires at `3.1829e-04`.
  - **The conclusion the withdrawn doc drew from the no-op is re-derived, not merely restated.** *"R1's representation is exact"* was previously a statement about two identical calls. It is now the output of a gate that a `0.1 %` normalization error would have broken by eight orders: the orthotropic layer at unit gains reproduces the independently-constructed isotropic layer to the last bit, so R1's orthotropic representation is an exact generalization of the isotropic one rather than an approximation of it.
- **G4 (the D3 (a)+(c) cross-check).** **Frozen:** `|rho_N_TM(k·r_core) - rho_N_MA| / rho_N_MA <= 1e-6 for every k·r_core <= 1e-3 in the frozen band` — measured worst `4.8581e-07`. The relative deviation scales as `(k·r_core)²`, the expected leading correction — **read from the JSON, not from memory (review F5; the withdrawn text carried `≈3.6e-7`/`≈3.6e-9`, which are not the shipped values):** `4.858127e-07` at `k·r_core = 1e-3`, `4.856897e-09` at `1e-4`, `2.048368e-13` at `1e-6` (displacement arm). The first two are a factor `100.0253` apart, i.e. the `(k·r_core)²` law to four digits. **Two independently-derived solvers agreeing to 13 significant figures at the band bottom is the strongest single statement in this certification.**
- **G5 (Ax3, R7).** **Frozen:** `|P_in - P_rad| / |P_in| <= 1e-10` — measured `1.4062e-13`; and **Frozen:** `max|Im T| / max|Re T| <= 1e-14` — measured `0.0` exactly. Work done by the source equals power radiated to infinity: no dissipative `Re(Z)` term is smuggled anywhere.
- **G6.** **Frozen:** `|rho_N(2·n_shell) - rho_N(n_shell)| / rho_N(n_shell) <= 1e-3 at the frozen n_shell = 256` — measured `1.0433e-04`. (Design note, disclosed: a naive `r`-uniform layer allocation converges only first-order and was `~7 %` per doubling at `n = 192` in pre-freeze scouting; the frozen allocation is **log-uniform in the grade `S`**, which recovers fast convergence. That allocation rule is an `[engineering-choice]` frozen in the prereg §5 before the run.) **That scouting claim is no longer an uncommitted assertion:** the repair ships it as **FT-6**, which runs the rejected `r`-uniform allocation at `n = 192 → 384` and measures `4.9127e-02` — the same order as the scouted figure, and `49 ×` over the G6 tolerance, so G6 is demonstrably a gate rather than a formality.
- **G7 (★the caveat is RETIRED, re-verify R1).** **Frozen:** `rho_N invariant across source amplitude 1e-6 ... 1e+6 to <= 1e-12 relative` — measured `2.9550e-13`, a margin of `3.4 ×`. **That is the tightest margin in the battery relative to its achievable numerical floor, and it is not an accident: G7 is roundoff-limited, not structurally exact.** The prereg's honesty caveat is repeated here verbatim — **Frozen:** `G7 is STRUCTURALLY exact in a linear frequency-domain solver; it certifies wiring, not physical linearity, and is reported as such` — **and its first clause is contradicted by measurement.** The residual is a floating-point floor that grows with the conditioning of the assembled system, and pushing the matching radius out lifts it through the tolerance. Measured over the prereg's **own** frozen G8 sweep `R_match/r_core ∈ {2, 4, 8, 16}` (shipped as `FT9_G7_amplitude_fireability.R_match_conditioning_sweep`):

  | `R_match/r_core` | G7 amplitude spread | vs the frozen `1e-12` |
  |---|---|---|
  | `2` | `3.3556e-14` | PASS |
  | **`4` (the frozen geometry)** | **`2.9550e-13`** | **PASS, `3.4 ×`** |
  | `8` | `3.2399e-12` | **FAIL** |
  | `16` | `1.1932e-11` | **FAIL by `11.9 ×`** |

  G7 is evaluated at the frozen instrument geometry `R_match = 4·r_core` (prereg §5), where it passes; nothing is relaxed and no criterion moves. What changes is the disposition: **a gate that fails on an input its own pre-registration calls admissible is a gate that can fail, so a self-test is constructible and one is now shipped (FT-9, §3.1).** The frozen prereg is BYTE-UNTOUCHED — the defective frozen clause is disclosed in §8.14 and routed in §9.6 alongside the other two, never amended.
- **G8 (R6-ii).** **Frozen:** `rho_N invariant across R_match/r_core in {2, 4, 8, 16} to <= 1e-9 relative` — measured `5.0136e-11`. There is no sponge to fail: **Frozen:** `the exterior beyond R_match is represented by the exact analytic outgoing solution; no sponge, no absorbing layer, no far-field truncation`.
- **G9.** **Frozen:** `cond(system matrix) <= 1e12 at every sampled k·r_core in the certified band k·r_core in [1e-8, 4]` — measured worst `5.0202e+10` (at the band bottom `k·r_core = 1e-8`, as expected). The band bottom is set by **matrix conditioning, not by a CFL floor** — the whole point of the D3 frequency-domain ruling.

---

## §3 — THE GATE-FIREABILITY SELF-TESTS (a gate that cannot fail is not a gate)

**Frozen:** `gate_fireability_selftest_pass = FT-1 AND FT-2 AND FT-3 AND FT-4 all FIRE at their frozen thresholds` — **`True`**. Every self-test is an actual run of the actual gate on an input that must break it, shipped in the same JSON. **Two species are present, and the distinction matters:** FT-1/FT-2/FT-3/FT-5/FT-6/FT-7 feed a **deliberately mis-specified** input (a broken profile, a smuggled friction, a rejected allocation rule); FT-4/FT-8/FT-9 feed an **admissible or in-family** input at the edge of, or outside, the regime the gate is certified over (out-of-regime `k`, below the declared band floor, an admissible matching radius). The second species is the stronger evidence, because it does not require the reader to accept that the mis-specification is realistic.

| # | Targets | Input fed to the gate | Firing threshold | Measured | Margin | Outcome |
|---|---|---|---|---|---|---|
| **FT-1** | G1 | exterior carries a residual power-law grade `q = 0.10`; and (separately) exterior mistakenly carries the cage's orthotropy `(1.02, 0.99)` | prereg §6 — `lame_ratio ≥ 1e-3` | `4.6477e-03` and `3.3669e-03` | `4.6 ×` / `3.4 ×` | ✅ **FIRES** |
| **FT-2** | G2 *(repaired gate)* | a tiny cage contrast `S_rail = 0.99` | prereg §6 — `rho_S ≥ 1e-5` | `1.0120e-04` | `10 ×` | ✅ **FIRES** |
| **FT-3** | G5 | an ABSORBING shell, complex modulus `Im/Re = 1e-3` (a smuggled friction — exactly what R7 forbids) | prereg §6 — imbalance `≥ 1e-2` | `2.9181e-01` | `29 ×` | ✅ **FIRES** |
| **FT-4** | G4 | the same TM↔MA comparison evaluated OUT of the backstop's regime, at `k·r_core = 3.0` | prereg §6 — rel `≥ 1e-1` | `6.2850e+00` | `63 ×` | ✅ **FIRES** |
| **FT-5** | G3 *(repaired gate)* | ★a **mis-normalized `ortho_layer`** (`misnorm = 1e-3`) — unit gains no longer reduce to `iso_layer`, exactly the defect G3 names | repair-added: rel `≥ 1e-6` | `3.1829e-04` | `318 ×` | ✅ **FIRES** |
| **FT-6** | G6 | ★the **rejected `r`-uniform layer allocation** at `n_shell = 192 → 384` (first-order convergence on this grade) | repair-added: rel `≥ 1e-2` | `4.9127e-02` | `4.9 ×` | ✅ **FIRES** |
| **FT-7** | G8 | ★an exterior that is **not homogeneous past the contour** (residual grade `q = 0.10`), so the matching radius must matter | repair-added: spread `≥ 1e-8` | `4.8706e-01` | `4.9e7 ×` | ✅ **FIRES** |
| **FT-8** | G9 | ★evaluation **below the declared band floor**, at `k·r_core = 1e-11` | repair-added: `cond ≥ 1e13` | `1.5875e+15` | `159 ×` | ✅ **FIRES** |
| **FT-9** | G7 | ★the same amplitude sweep at **`R_match/r_core = 16`** — an **ADMISSIBLE** configuration, not a mis-specification: it is a member of the prereg's own frozen G8 sweep `{2, 4, 8, 16}` | repair-added: spread `≥ 1e-11` | `1.1932e-11` | `1.2 ×` | ✅ **FIRES** |

**★FT-5…FT-9 are repair-added, post-freeze, and `frozen_at_repair` is NOT `frozen` (re-verify R6).** In this document and in the JSON, **`Frozen:`** means one thing: the quoted string byte-matches the pre-registration that was committed and pushed *before any solver code existed* — which is what `manuscript/ave-kb/tools/verify-frozen-provenance.py` checks. **`frozen_at_repair` carries a strictly weaker warrant:** the threshold was written down before *its* run, in the same commit as the test, so the pre-declaration is witnessed by git at **commit granularity only** — not by a pre-registration, and not by anything that separates "declared then measured" from "measured then declared" *within* a commit. Every FT-5…FT-9 threshold is nonetheless mechanically determined rather than chosen: `≥ 10 ×` the target gate's frozen pass tolerance, the same rule for all five. **The outcomes are reported as they came.** For FT-9 specifically, one further disclosure: the re-verify audit had already measured the `R_match = 16` configuration when FT-9 was minted, so FT-9's pre-declaration is **by-rule, not blind** — the `10 ×` rule fixes the number `1e-11` and the rule predates the measurement, but no claim of a blind pre-declaration is made for it.

- **Frozen:** `both mis-specified profiles MUST return lame_ratio >= 1e-3` — **★G1b COVERAGE IS NOW SHIPPED, NOT INFERRABLE (re-verify R5).** G1 is a compound gate: an AND of `lame_ratio ≤ 1e-10` **and** `shell_agreement ≤ 0.25`. The frozen FT-1 threshold names only the first, and as shipped FT-1 computed only the first, so a reader had to recompute the second to see whether the second conjunct was covered at all. FT-1 now emits it: the same two mis-specified exteriors return `shell_agreement` = **`2.2530`** (graded exterior) and **`2.1454`** (orthotropic exterior) against the frozen `0.25` tolerance — **both breach it, by `9.0 ×` and `8.6 ×`**. So both conjuncts of G1 are demonstrably breakable by the same mis-specification, and that is now a shipped number (`selftests.FT1_lame_fireability.shell_agreement_*`) rather than an inference. The frozen `FIRES` criterion is unchanged and still keys on `lame_ratio` alone.
- **Frozen:** `both mis-specified profiles MUST return lame_ratio >= 1e-3` — **★CORRECTION (review F12):** the withdrawn text said the mis-specified profiles land `1.7e11 ×` above the G1 *pass tolerance*. That figure is the ratio to the **measured** G1 value (`4.6477e-03 / 2.7003e-14`), not to the tolerance. The correct statement: they land `4.6e7 ×` and `3.4e7 ×` above the G1 pass tolerance of `1e-10` — the frozen FT-1 threshold `1e-3` is itself `1e7 ×` that tolerance, as the prereg states. A uniform stiffness change in the exterior does NOT break the Lamé gate (a homogeneous region is div-free at any modulus), so the mis-specification had to be a genuine INHOMOGENEITY or ANISOTROPY leaking past the cage contour — which is also the realistic profile-specification error given R1's orthotropic input.
- **Frozen:** `the contrast case MUST return rho_S >= 1e-5 at k·r_core = 1e-3` — this is the **structural-null lens**, and it is now pointed at the gate that actually ships: FT-2 was re-wired to the repaired independent null so the test and the gate share one comparison. It proves `rho_S` is a LIVE observable that a real contrast moves. **It never did — and never could — prove what the withdrawn doc claimed for it.** The withdrawn text read *"Without FT-2, G2's exact-zero pass would be indistinguishable from a disabled observable"*; in fact **with** FT-2 the exact-zero pass was still indistinguishable from a disabled observable, because FT-2 exercises the *contrast* degree of freedom while the defect was in the *null arm*. A self-test on a neighbouring input cannot certify a comparison that has no degrees of freedom. That is the transferable lesson of this repair.
- **Frozen:** `the absorbing case MUST return |P_in - P_rad|/|P_in| >= 1e-2` — a `1e-3` imaginary modulus (0.1 % loss) produces a 29 % energy imbalance, so the Ax3 gate detects friction three orders of magnitude below the level at which it would matter physically.
- **Frozen:** `the out-of-regime comparison MUST return |rho_N_TM - rho_N_MA|/rho_N_MA >= 1e-1` — measured `6.2850e+00`. **★SCOPE CORRECTED (review F4).** What FT-4 demonstrates is that the TM↔MA agreement is **regime-bounded**: it holds where the backstop's quasistatic expansion is valid and breaks where it is not. That is a real fireability statement, and it is the one claimed here. It is **not** a demonstration of code-path independence — evaluating one code path at two `k` values would also produce disagreement — and the withdrawn text's *"not two evaluations of one code path"* was reading more out of it than it contains. Note also that the frozen `k·r_core = 3.0` sits in the resonant region, where the TM `rho_N` peaks, so the measured `6.3 ×` is the disagreement at its most favourable point in the band.
  - **Code-path independence DOES hold, and is established by reading the code, not by FT-4.** `basis_matrix` (`research/drivers/continuum_radial_solver.py`, the `if omega == 0.0:` branch) returns the static Lamé power-law pair `r^s1, r^s2` when `ω = 0` and the Bessel pair `r^(−1/2)[J_ν, Y_ν]` otherwise. The matched-asymptotics backstop calls `static_solve`, which calls `stack_transfer(..., 0.0)` — the static branch — and forms `|B_caged/B_uncaged|²` from an *analytic* `k⁴` cancellation that is never evaluated numerically. The transfer-matrix solver calls `outgoing_amplitude` at `ω ≠ 0` — the Bessel branch — plus the exterior `H_ν^(1)` matching. The two solvers share the layer bookkeeping and share **no** basis evaluation, and their agreement at `4.8581e-07` is therefore a genuine cross-check.

### §3.1 — The nine-gate → self-test / caveat map (the audit the review required)

Every gate must end with a self-test that can break it. (The repair's first pass allowed the weaker alternative — an explicit disclosed caveat naming why a gate cannot be tested — and used it once, for G7; that caveat did not survive the re-verify audit, so **the weaker alternative is no longer used anywhere in this battery.** See the FLAG-1 block below the table.) The withdrawn run had four self-tests covering five gates (FT-3 covers G5 and G5b) and **no disposition at all** for G3, G6, G7, G8, G9 — which is how a dead G3 shipped. Post-repair:

| Gate | Disposition | Evidence it can fail |
|---|---|---|
| **G1 / G1b** | **FT-1** | fires at `4.6477e-03` / `3.3669e-03` on two distinct mis-specified exteriors; **G1b covered by shipped number** — the same two exteriors return `shell_agreement` `2.2530` / `2.1454` against the frozen `0.25` |
| **G2** | **FT-2** + mutation receipt | FT-2 fires at `1.0120e-04`; a corrupted transfer kernel puts the gate at `5.2800e-07`, FAIL |
| **G3** | **FT-5** + mutation receipt | FT-5 fires at `3.1829e-04`; the reviewer's `ortho_layer` corruption puts the gate at `1.9100e-04`, FAIL |
| **G4** | **FT-4** | fires at `6.2850e+00` out of regime |
| **G5 / G5b** | **FT-3** | fires at `2.9181e-01` on a `1e-3` absorbing modulus |
| **G6** | **FT-6** | fires at `4.9127e-02` on the rejected `r`-uniform allocation |
| **G7** | **FT-9** *(★the caveat is RETIRED — re-verify R1)* | fires at `1.1932e-11` at `R_match/r_core = 16`, an **admissible** configuration by the prereg's own frozen G8 sweep — so the gate is `11.9 ×` past its `1e-12` tolerance there |
| **G8** | **FT-7** | fires at `4.8706e-01` on a non-homogeneous exterior |
| **G9** | **FT-8** | fires at `1.5875e+15` below the declared band floor |

**All nine gates carry a breaking self-test. Zero gates carry a caveat, and zero are undispositioned.**

**★THE G7 CAVEAT WAS FALSIFIED BY MEASUREMENT, AND THE CORRECTION IS RECORDED HERE RATHER THAN QUIETLY DROPPED (re-verify R1, FLAG-1).** The withdrawn-then-repaired run dispositioned G7 with a caveat instead of a test. Its text, verbatim as it stood in this table and in the driver's `gate_selftest_map`:

> *"**★CAVEAT — no self-test is possible.** … The source amplitude enters only as a scale factor on the right-hand side of a linear solve, so **no admissible input can make G7 fail** — only a wiring error can, which is exactly and only what it certifies. A 'self-test' would have to inject a nonlinearity the solver does not host, i.e. test a different instrument."*

- **The load-bearing half is false, and the counterexample comes from inside this lane's own frozen document.** G7 FAILS at `R_match/r_core = 16` — a value the prereg's **own** frozen G8 criterion (§5, `rho_N invariant across R_match/r_core in {2, 4, 8, 16} to <= 1e-9 relative`) declares an admissible configuration of this instrument. Measured `1.1932e-11` against the `1e-12` tolerance, a breach of `11.9 ×`. The premise "no admissible INPUT can make it fail" is therefore not a structural fact about linear solves; it was an argument from the *form* of the equations that ignored the *conditioning* of the numerics. G7 is roundoff-limited, and its floor moves.
- **The other half IS true, and was verified rather than assumed** (out-of-tree mutations, §8.13). A genuine **wiring error** — the amplitude dropped from the reference arm, so the caged and uncaged arms are driven differently — puts G7 at `2.999999999997`, a FAIL by twelve orders that would force class C. A naive **common-mode** mutation (both arms scaled by `1+1e-9`) puts it at `5.665625e-13`, still **under** the tolerance, and correctly does **not** fire: it shifts the roundoff floor without breaking the caged/uncaged ratio. So the gate does catch the defect it claims to catch, and does not fire on a change that cancels. That much of the caveat stands.
- **Why the caveat was the wrong disposition even though its conclusion (G7 PASSES) was right.** A caveat is an argument that a gate cannot be tested; it is worth exactly what its premise is worth, and it is not checked by anything. A self-test is a measurement. This battery already mints and accepts exactly this species of test: **FT-8** evaluates G9 at `k·r_core = 1e-11`, outside the declared band but inside the instrument's own operating vocabulary. FT-9 is the same move on G7 — an in-family stress, not a corruption — so the precedent for it was already inside this lane. Constructing it was available and was not done.
- **Consequence for the audit claim.** "Zero undispositioned" now holds **by construction** (nine gates, nine breaking self-tests) rather than by an eight-plus-a-caveat count whose ninth member does not survive inspection. And the gate that most wanted a breaking test got one: G7's `3.4 ×` was already the tightest margin in the battery relative to its achievable numerical floor.

---

## §4 — HOW THE SYMBOLIC `c²` THREADS THROUGH THE OUTPUTS (D1 HELD)

**No `c²` is evaluated anywhere in stage 1.** The import is `ρ_contribution = E_trapped/c_x² · (participation)`, so the trapped-energy column depends on the held sector choice **only** through the factor `1/c_x²`. Two consequences, both shipped in `two_term_rho_report`:

**(a) The D2 sweep and the D1 choice lie on the SAME axis — which is what makes stage 1 D1-independent by construction.** `β` as defined carries the frozen symbolic label `beta = (u_trapped · P) / (rho_0 · c_x^2) — c_x SYMBOLIC, D1 HELD; no c^2 evaluated`, i.e. `β` is the PRODUCT of a magnitude and `1/c_x²`. A different sector choice does not add a degree of freedom — it relabels which member of the disclosed `β`-family is the physical one. Hence the D2 disclosed sweep is not a workaround for the held decision; it is the correct representation of it.

**(b) The LEVER LENGTH of D1 is reported as a pure dimensionless ratio, with no candidate designated.** **Frozen:** `the c^2 dependence is reported as the dimensionless candidate-swap ratio (c_i/c_j)^2 from the lattice-measured speeds, with NO candidate designated and c_EM carried as an unevaluated symbol`

| candidate swap | ratio (from I11 lattice-measured speeds) | reading |
|---|---|---|
| `c_P² → c_S²` | `(c_P/c_S)² = ` **`3.2864`** | swapping the divisor from the compression speed to the shear speed multiplies `β` by `3.2864` |
| `c_S² → c_P²` | `(c_S/c_P)² = ` **`0.30429`** | the inverse |
| `c_EM` | **UNEVALUATED SYMBOL** | no lattice-measured value is on the ledger; carried symbolically |
| designated candidate | **`null`** | D1 is HELD; stage 1 picks nothing |

**Plumber-physical reading of the lever (why this number is the useful output of a held decision).** The D2 sweep spans `β ∈ {0, 1, 3}` — a factor of 3 across the whole disclosed family. The D1 swap between the two lattice-measured candidates is a factor of `3.2864`. **So the sector choice alone moves `β` by more than the entire width of the disclosed sweep**: `β = 1` under one candidate is `β = 3.2864` under the other, which lands just above the top of the family. That is the quantitative statement of why D1 cannot be defaulted.

**★CORRECTION — THE LEVER RUNS THE OTHER WAY, AND THE CHARTER'S I8 FIRST CLAUSE IS WRONG ON ITS OWN NUMBERS (review F7).** The withdrawn text presented this lever as *confirming* the charter's I8 note. It does the opposite, and the arithmetic is not close:

- The lattice-measured speeds are `c_P = 0.51884` and `c_S = 0.28621`, so `c_P > c_S` and therefore **`c_P²` is the LARGEST inertia divisor, which gives the SMALLEST loading.** `β ∝ 1/c_x²`, so dividing by the *bigger* `c²` loads `ρ_eff` *less*. **`c_S` loads `3.2864 ×` MORE than `c_P`** — the lane's own candidate-swap ratio is exactly the proof.
- The merged charter's I8 note (`research/2026-07-21_continuum-radial-solver_CHARTER.md`, the I8 paragraph) reads verbatim: *"`c_P` (`0.519`, the compression carrier's own speed) gives the smallest inertia divisor and the largest loading; `c_S` (`0.286`) larger loading still (smaller `c²`)"*. **The first clause is simply wrong**: `0.519 > 0.286`, so `c_P` gives the *largest* divisor and the *smallest* loading. The second clause reaches the right destination — `c_S` does load more, and its parenthetical `(smaller c²)` is correct — but the comparative *"still"* inherits the first clause's inversion, so the sentence as a whole mis-orders the two candidates.
- **This is an inherited error that the withdrawn result doc relayed as agreement.** Correcting it is not a re-scoping of D1: the *magnitude* of the lever (`3.2864 ×`, wider than the whole disclosed `β` family) and the conclusion (D1 cannot be defaulted) are unchanged. What changes is **which way the choice pushes** — and that is exactly the kind of thing a held decision must not be walked with backwards.
- **The charter is NOT edited here.** This lane's fence is explicit: *no charter edit — regardless of outcome*. The I8 error is **routed** for a dated correction by the lane that owns the charter (§9.6), with the offending clause quoted verbatim so the correction can be verified against it. Flag-don't-fix.

---

## §5 — THE R2 TWO-TERM DECOMPOSITION, kept SEPARABLE (and the R4 `r_Z` family)

Shipped as `two_term_rho_report.rows`, `columns_kept_separable = true`. **Never pre-summed**; the summed column exists only under the explicit key `rho_eff_over_rho0_if_summed_DOWNSTREAM_ONLY`.

| `β` (D2 disclosed scan) | `φ_sf` (lattice) | **term (i)** structural `ρ/ρ₀` | **term (ii)** trapped-energy `ρ/ρ₀` (symbolic `c_x²`) | `r_Z` family |
|---|---|---|---|---|
| `0` | `0.489462` | `1.0` | `0.000000` | `0.543577` |
| `1` | `0.489462` | `1.0` | `0.489462` | `0.663400` |
| `3` | `0.489462` | `1.0` | `1.468386` | `0.854019` |

- **term (i) carries its own honest flag:** `structural dipole term NOT BUILT (n=1 channel absent in stage 1)`. In the `n = 0` channel at leading order the structural effective density is the volume average, which for a pure STIFFNESS grade at uniform substrate inertia is exactly `1` — the same anchor #782/#796 carry. The DYNAMIC (dipole) part of the charter's C2 term-(i) is the `n = 1` object stage 1 does not build (§7).
- **term (ii) carries its symbolic-`c²` label in the column itself** (the frozen string above), so the D1 dependence is visible in every row rather than buried inside a single summed number.
- **The `K` factor is the lattice input, not a recomputation.** **Frozen (R4):** `r_Z must NOT recompute or perturb K_eff/K_0` — the `r_Z` family uses `K_tan/K_0 = 0.2954759` read from the #796 JSON, and the instrument's own static-compliance ratio (used only inside the G2 null check) is explicitly NOT presented as `K_eff/K_0`.
- **MIXED-provenance carried as #796 requires.** **Frozen:** `any stage-1 output that carries the #796 r_Z cites it as MIXED-provenance (K derived, rho assumed at rho_eff/rho_0 = 1); the #796 rho half is UNRESOLVED and stage 1 does not repair it`. The `β = 0` row (`r_Z = 0.543577`) IS #796's structural anchor, reproduced here as an arithmetic identity — not as an independent measurement.
- Every row is labelled `DEMONSTRATION — no verdict banked`.

**★The D5 profile input, and its disclosed gap.** **Frozen:** `the D5 profile gains are computed from the shipped vessel_state_rve_results.json fields (min_kse, peak_A, k_a_RHO_STAR, k_s_KS0) at driver runtime; no vessel-state number is retyped from prose`. Read at runtime: `min_kse = 0.35299364830704594`, `peak_A = 0.13946745063352736`, `k_a = 9.77337`, `k_s = 1.0` ⇒ `radial_gain = 0.352994` (radial-soften), `hoop_gain = 2.363067` (hoop-stiffen), i.e. `ε_radial = −0.0662009`, `ε_hoop = +0.1394675`. **Frozen:** `the D5 vessel-state EXTREMA are lattice-measured; the radial SHAPE between them is an ENGINEERING CHOICE — #796 shipped no resolved radial profile, and its C-V shell reconstruction is corroborative-only by its own grading`. This is a flag, not a fix: D5 said "feed the measured profile", and what exists to feed is two measured extrema, not a profile.

---

## §6 — INSTRUMENT-LIVENESS DEMONSTRATION (NOT a verdict)

`stage 1 banks NO physics verdict; every physics-shaped number in these outputs is an INSTRUMENT-LIVENESS DEMONSTRATION and is labelled DEMONSTRATION — no verdict banked`. Reported so the reader can see the observables move; **nothing below is banked, cited downstream, or compared to a corpus claim.**

| profile arm | source fitting | `rho_N` (matched-asymptotics, `k→0`) | `rho_S` (KEEP-BOTH, `k→0`) | fitted `p` over the sub-resonant tail | `rho_N` at `k·r_core = 0.3` | at `3.0` |
|---|---|---|---|---|---|---|
| isotropic baseline | displacement | `0.413549` | `0.586451` | `3.2e-08` | `0.431136` | `3.012703` |
| isotropic baseline | traction | `0.439371` | `0.560629` | `3.2e-08` | `0.457842` | `1.244858` |
| D5 orthotropic (measured) | displacement | `0.026520` | `0.973480` | `5.1e-08` | `0.028321` | `0.232244` |
| D5 orthotropic (measured) | traction | `0.028433` | `0.971567` | `4.8e-08` | `0.030268` | `1.159893` |

**Frozen (R6-iii):** `no exponent or quasistatic quantity is read above k·r_core = 1e-3; the resonant band is reported as characterization only`.

**★SCOPE LIMIT ON THE LIVENESS MAGNITUDE — the grade SHAPE, not the rail depth, sets it (review F6; shipped as `grade_shape_scope`).** The frozen grade law is `S(t) = 1 + (S_rail − 1)·t²` with `S_rail = 1e-3` (prereg §5, `[engineering-choice]`). The quadratic shape concentrates the soft material against the outer contour: **at the frozen `p = 2`, only `5.1 %` of the grade band actually reaches `S ≤ 0.1`.** Holding the nominal rail depth fixed at `1e-3` and changing *only* the shape exponent:

| `grade_power` | fraction of the band with `S ≤ 0.1` | `rho_N` (matched-asymptotics, `k→0`, isotropic arm) |
|---|---|---|
| `0.1` | `0.6478` | `0.018165` |
| `1.0` | `0.0991` | `0.247949` |
| **`2.0` (frozen)** | **`0.0508`** | **`0.413549`** |
| `8.0` | `0.0130` | `0.748953` |
| `32.0` | `0.0033` | `0.923793` |

**Reading, and its consequence for how §6 must be read.** At one and the same nominal rail depth the liveness observable spans `0.018165` … `0.923793` — a factor of `50.9`, i.e. `1.7063` orders of magnitude — purely on an engineering choice of shape. So the baseline `rho_N ≈ 0.41` is **a property of the frozen shape, not a measurement of how well a rail-depth-`1e-3` cage shields**, and the baseline **never realizes a full pressure-release wall** (`Γ_bulk = −1`, `rho_N → 0`); the deepest shape probed still leaves `1.8 %` of the uncaged power radiating. Nothing in §6 was banked, so nothing is retracted — but any downstream lane reading a shielding magnitude off this instrument must carry the shape as a declared input.

**★And a divergence from the review, flagged rather than relayed (verify-before-cite).** The review characterized this effect as moving the liveness observable *"4–11 orders"* at fixed nominal rail depth. **This lane's own measurement does not reproduce that figure:** across `grade_power ∈ [0.1, 32]` the span is `1.7063` orders, not four to eleven. The *direction* and the *scope-limit conclusion* are confirmed and adopted; the *magnitude* is not, and is reported here as measured. Whether the review's figure came from also varying `S_rail` or the band width `W` — both of which are separately frozen — is not resolvable from this side and is routed with the rest (§9.7).

**★Why the fitted `p ≈ 0` is NOT a test of the charter's F2, and must not be read as one.** The `n = 0` **centred-source** channel is **analytically** `k`-independent in the deep-quasistatic limit: radiated power `∝ k⁴|B|²` for BOTH the caged and the uncaged arm, so the `k⁴` prefactor cancels identically in the ratio and `rho_N → |B_caged/B_uncaged|²`, a static-compliance ratio. The measured `p ~ 5e-8` is that theorem reproduced numerically to eight digits — a **channel property**, not a measurement of a physical exponent. The charter's frozen form F2 (`p = 2`) is derived in `research/2026-07-20_deep-rail-kscaling_derivation.md` §1 for a source **DISPLACED** from centre; that is an `n = 1` statement (§7). **No stage-1 output validates or refutes F2, and none is presented as doing so.**

**One consistency observation, reported not banked.** The cold medium built from the lattice-measured speeds implies `ν = 0.281313`, versus the canonical `ν_Hill = 2/7 = 0.285714` (`ave.core.constants.N_NU`) — a `−1.54 %` deviation. This is a property of the #796 shipped speeds, not of this instrument; it is recorded in `cold_medium.nu_rel_dev_vs_canon` so a downstream lane that needs the two to agree knows the size of the gap. **No claim is made about which is right.**

---

## §7 — THE C1/C2/C3 vs X1/X2/X3 FENCE: what stage 1 certifies and what it explicitly does NOT

**CERTIFIED (instrument-class only):**
- **S1** — the radial-channel machinery is correct against analytic limits: layer assembly, the static Lamé limit, the orthotropic layer (exact, not approximate), outgoing-radiation matching, and lossless-reactive bookkeeping, all to the §2 tolerances.
- **S2** — the deep-quasistatic band is REACHABLE and its floor is MEASURED: `k·r_core ∈ [1e-8, 4]` at `cond ≤ 5.02e10`, with the floor set by conditioning, **not** by a CFL limit. The lattice's `O(1)` confinement (`deep-rail-kscaling_derivation.md` §2) is cleared by **eight decades**.
- **S3** — the gates are FIREABLE: **all nine self-tests fire, at margins from `1.2 ×` to `4.9e7 ×` on their thresholds.** The weakest is **FT-9** at `1.1932e-11` against its `1e-11` threshold (`1.2 ×`); next is FT-1's orthotropic-exterior arm at `3.3669e-03` against `1e-3` (`3.4 ×`). **★CORRECTED (review F11):** the withdrawn text claimed `≥ 10 ×` on all four, which FT-1 never met on either arm (`4.6 ×` and `3.4 ×`). And S3 as originally claimed was in any case not earned — five of the nine gates had no self-test at all, and one of those (G3) was dead. **★CORRECTED AGAIN (re-verify R1):** the first repair earned it only eight-ninths — G7 was dispositioned by a caveat that measurement falsified. It is earned now, with no caveat anywhere: **nine of nine gates carry a breaking self-test** (§3.1).
- **S4** — the instrument ACCEPTS the R1 orthotropic profile and the D5-measured numbers, and produces the R2-separable outputs in the frozen format.

**NOT settled by stage 1:**

| | Statement |
|---|---|
| **C1 — the deep-quasistatic single-core exponent `p`** | **NOT delivered.** The band is reached (S2) and the observable is live, but the charter's F2 target `p = 2` is a DISPLACED-source (`n = 1` dipole) statement — see X5. The `n = 0` centred-source exponent is `0` **analytically**, so it is not the same question. |
| **C2 — the net acoustic `ρ_eff` sign with both terms present** | **NOT delivered.** Term (ii) is not evaluable while D1 is HELD (by design), and term (i)'s dynamic part is the `n = 1` object — see X5. Stage 1 delivers the SEPARABLE REPORTING FORMAT for C2, not C2's answer. |
| **C3 — `r_Z(φ)` with both `Z` factors** | **NOT delivered.** The `r_Z` family in §5 is an arithmetic composition of the lattice `K` input with the D2 sweep — its `β = 0` row is #796's structural anchor by identity. Both `Z` factors are still not independently measured anywhere in this corpus. |
| **X1 — derive `β`** | Unchanged from the charter: needs self-bound soliton dynamics. Stage 1 PROPAGATES the D2 sweep; it does not generate `β`. |
| **X2 — adjudicate the import's truth** | Unchanged; and stage-1-specific: **D1 is untouched**, so no trapped-energy magnitude is evaluated at all. |
| **X3 — ensemble aggregation (`N > 1`)** | Unchanged from the charter; not approached. |
| **X4 — any physics verdict of any class** | Explicitly NOT banked (§6). |

**★X5 — THE LOAD-BEARING SCOPE FINDING (flag-don't-fix; surfaced, not reframed).** The charter §2 specifies a **spherically-symmetric** instrument, and §4 assigns it C1 and C2. Building it exposes that the `n = 0` channel cannot host either object, and this is a property of the CHANNEL, not of the implementation:

- **C1's F2.** `research/2026-07-20_deep-rail-kscaling_derivation.md` §1 Step 1, spherical-cavity reading, verbatim: *"For a source at the exact center of a `p = 0` shell of radius `r_core`, the exterior compression is *perfectly* shielded below the fundamental cavity resonance … A source **displaced from center** by `~r_core` … leaks its leading uncancelled multipole with amplitude `∝ (k·r_core)`, hence radiated POWER `∝ (k·r_core)²`."* The `(k·r_core)²` — the frozen form F2, `p = 2` — is the DISPLACED-source result. A displaced source in a spherically-symmetric cage radiates through the **dipole (`n = 1`)** channel. A strictly spherically-symmetric instrument has no `n = 1` channel, so **F2 is not testable on the chartered object.**
- **C2's term (i).** The long-wavelength effective-DENSITY correction of an inclusion (the "soft inclusion trends DOWN, bubble-like" added-mass the charter names) is carried by the **dipole (`n = 1`)** scattering coefficient; the `n = 0` monopole coefficient carries the effective COMPRESSIBILITY. So the `n = 0` channel measures `K`-side physics, not `ρ`-side physics.

**Frozen disclosure carried by every stage-1 output:** `n=0 monopole channel only; the n=1 dipole channel (F2 displaced-source p, and the structural added-mass term of rho_eff) is NOT built in stage 1`.

**Disposition (routed, not decided here).** The `n = 1` (coupled P–S) radial channel is a tractable extension of the SAME machinery — a `4×4` transfer matrix on two potentials instead of a `2×2` on one. **★CORRECTED (review F9): it inherits NO gate certified here.** The withdrawn text said it *"inherits every gate certified here"*, which asserts gate transferability to an unbuilt channel without derivation — in a document whose own thesis is that a gate is worth exactly what its fireability demonstration is worth. Concretely: G3 is a statement about a `2×2` orthotropic layer reduction and has no `4×4` analogue until one is written; G4's matched-asymptotics backstop is derived from the `n = 0` monopole `k⁴` cancellation and does not carry to a dipole; G1's Lamé limit is the `n = 0` static pair. **Every gate would have to be re-derived and re-earned for `n = 1`, with its own fireability self-tests.** What transfers is the *pattern* — layer algebra, analytic exterior matching, the source-fitting axis, the fireability discipline — not the certification. Whether stage 2 builds it, or whether the charter's §2 object should be amended to say so explicitly, is Grant's / the auditor lane's call. **This lane does not draft the charter amendment** (the auditor lands charter-level changes), and **does not silently redefine C1 or C2 to fit what the `n = 0` channel can do.**

---

## §8 — Deviations, disclosures, and the mutual-satisfiability record

1. **Tolerance scouting, disclosed.** The §5 frozen tolerances were feasibility-scouted on an **uncommitted scratch prototype** before the freeze, per the prereg §9. Only feasibility and headroom were scouted; **no physics observable was scouted, and no tolerance was moved after any gate ran.** **Frozen:** `no adjudication criterion may be dropped or relaxed post-hoc to convert a FAIL to a PASS` — nothing has been relaxed at any point, including through the review repair (§8.8). The withdrawn run's claim that *"every gate passed on its first shipped run"* is corrected: two of those gates could not have failed, so their first-run PASS carried no information. On the repaired battery, every gate passes on its first repaired run, and every gate now has a demonstration that it could have done otherwise.
2. **The mutual-satisfiability check paid off (the #796 Protocol-E lesson applied).** #796's root cause was frozen preconditions that could not co-exist (`k·r_core ≪ 1` required, `k·r_core ≈ 2–4` realizable). The stage-1 prereg §9 ran that check BEFORE freezing and found the analogous conflict in advance: **a requirement that the TRANSFER MATRIX reach the physical constituent regime `k·r_core ~ 1e-25` is UNSATISFIABLE in double precision** (the two basis solutions differ by `~x^(−3) ~ 1e75`). It was therefore never frozen. **Frozen instead:** `below k·r_core = 1e-8 the matched-asymptotics backstop is the instrument of record; it is k-independent in that limit and is certified against the transfer matrix in the overlap band [1e-8, 1e-3]` — and the certification run confirms that overlap is real and five decades wide (G4). **This is the direct reason D3's ruling had to be (a)+(c) and not (a) alone.**
3. **Layer-allocation design note.** An `r`-uniform layer allocation converges only first-order on this grade (`~7 %` per doubling at `n = 192` in scouting); the frozen allocation is log-uniform in the grade `S`. Frozen in the prereg before the run; tagged `[engineering-choice]`.
4. **No figure shipped.** This lane produced no figure; the certification content is a gate table, which the result doc and the JSON carry directly. (Had a figure been produced it would follow the white house style via `ave.viz.style.apply`.)
5. **Wall-clock disclosure.** The lane exceeded the ~2 h wall-clock checkpoint during the driver build; disclosed rather than truncated. The certification run itself takes seconds (`_runtime_sec` in the shipped JSON; not quoted here, item 12).
6. **Fences held.** Engine `src/ave` **BYTE-UNTOUCHED** (`git diff --stat origin/main -- src/ave` empty). No `manuscript/` or KB leaf edit. No port-register, no falsification-ledger, no charter edit. Mints no `clm-`/`def-`. `make verify` green; frozen-provenance gate 0-gating; docket-key lint green.
7. **★A FROZEN CRITERION IS ITSELF DEFECTIVE — disclosed and routed, NOT amended (review F2).** The prereg §8 reachability argument states that CLASS A is reachable *"because the scouted margins on every gate exceed the frozen tolerance by `≥ 4` orders of magnitude"*. **The measured run contradicts that on every gate with a finite margin** — largest `3.7e3 ×` (G1, `3.6` orders), smallest `2.1 ×` (G4, `0.3` orders); only G3 and G5b land exactly on zero. The prereg is BYTE-UNTOUCHED: the sentence is wrong and stays wrong in the frozen file, corrected here (§2) and routed for the auditor lane's disposition (§9.6). It is a claim *about* the criteria, not a criterion, so no gate outcome depends on it — but it was absent from this section as shipped, which is the actual defect.
8. **★THE REVIEW REPAIR ITSELF — what changed, in order, and what it cost.** (a) The class was **withdrawn** to `C_NOT_CERTIFIED_VOID` in its own commit, before any repair, on the F1/F3 finding that two gates could not fail. (b) G3 and G2 were repaired (new `iso_shell` construction; new first-principles `uniform_reference_profile`), FT-5 was minted for G3, FT-2 re-pointed at the repaired G2, and FT-6/FT-7/FT-8 added so no gate is left undispositioned. (c) The battery was **re-run**, and the class re-earned as `A_CERTIFIED` on the re-run. Had the repaired G2 or G3 failed, the class would have stayed `C` and been banked as such; **no criterion was relaxed, no threshold moved, and no arm dropped** (Rule 11). Cost of the repair to the shipped numbers: **G2 only** (`0.0` → `1.8763e-13`) and **FT-2's last digits**; every other gate is bit-identical, verified field-by-field.
9. **The one hand-edit, disclosed.** The results JSON is driver-generated and never hand-edited, with exactly one exception: the withdrawal commit stamped `certification.class` to `C_NOT_CERTIFIED_VOID` so the withdrawal could be committed ahead of the repair. That stamp is self-describing in the file (`certification.class_withdrawal_stamp_provenance`) and was superseded by the re-run's regenerated JSON.
10. **A frozen axis the withdrawn G3 silently skipped.** **Frozen:** `every gate is evaluated for both source fittings (prescribed displacement and prescribed traction) and passes only if it passes for both` — the withdrawn `gate_G3_ortho_reduction` ran only the default `displacement` fitting. Not reported by the review; found while repairing it. The repaired G3 runs both. Disclosed rather than folded into the repair silently.
11. **Repair-added criteria are post-freeze and labelled as such.** FT-5…FT-9 carry `frozen_at_repair`, never `frozen`, in the JSON, so the frozen-provenance convention is not diluted: a byte-matchable `Frozen:` label in this document points only at the prereg's own text. **★And the distinction is now stated where the self-tests are PRESENTED, not only here (re-verify R6):** `frozen_at_repair` is git-witnessed at **commit granularity only** and is a strictly weaker warrant than `frozen`, which means byte-matching a pre-registration pushed before any solver code existed. See the block under the §3 self-test table.
12. **★THE RUNTIME NUMERAL IS DELIBERATELY ABSENT FROM THIS DOCUMENT (re-verify R3).** The `_runtime_sec` field is machine-dependent, and the frozen determinism definition excludes it by construction: **Frozen:** `two independent full driver runs produce an identical results digest (SHA-256 over the results object minus timing fields)`. Quoting it here and registering the quote against the JSON made `continuum_radial_solver_number_check.py` **fail on every honest re-run on every machine** — the re-verify audit re-ran the driver, got a different runtime, and the tool went MISMATCH → FAIL. A check that fails whenever someone reproduces the work is worse than no check: it trains its reader to ignore it. **Disposition: the token is DROPPED, not allow-listed.** Allow-listing would have left a runtime numeral in the doc that entered by being *typed* and is then verified against nothing — which is precisely the defect the tool exists to remove (F5/F13/F14 were three instances of it). The runtime is read from the shipped `_runtime_sec` and its budget verdict from `runtime_within_budget`; the tool now carries a `NON_REGISTRABLE` block and **refuses any future attempt to register or allow-list that field**.
13. **★MUTATION RECEIPTS FOR THE RE-VERIFY REPAIRS (out-of-tree; the mutated driver is never committed).** (a) *G7 wiring error* — the source amplitude dropped from the reference arm, so the two arms are driven differently: G7 returns `2.999999999997`, **FAIL**, which would force class C. (b) *G7 common-mode* — both arms scaled by `1+1e-9`: G7 returns `5.665625e-13`, **under** the `1e-12` tolerance, correctly **not** firing. (a) and (b) together are the verified half of the retired caveat (§3.1). (c) *FT-9 forced non-firing* — with all nine gates PASSING and all four **frozen** self-tests FIRING (`gate_fireability_selftest_pass = true`), making FT-9 alone fail to fire returns **`C_NOT_CERTIFIED_VOID`**, on the shipped precedence *"a REPAIR-ADDED self-test (FT-5..FT-9) failed to fire, so the gate it targets cannot fail"*. The strengthening survives the addition of FT-9.
14. **★A SECOND FROZEN CRITERION IS ITSELF DEFECTIVE — disclosed and routed, NOT amended (re-verify R1).** The prereg's G7 row (§5) and its R6 control (§7 (i)) both assert that drive-amplitude independence is **structurally exact**: *"in a linear frequency-domain solver this is STRUCTURALLY exact, so a PASS is a wiring check, not evidence of physical linearity"*. **Measurement contradicts the word *structurally*.** G7 is roundoff-limited: over the prereg's own frozen G8 matching-radius set `{2, 4, 8, 16}` the spread runs `3.3556e-14 → 2.9550e-13 → 3.2399e-12 → 1.1932e-11`, breaching the `1e-12` tolerance at the top two. The *conclusion* the frozen text draws — that a G7 PASS is a wiring check and not evidence of physical linearity — **stands, and is not disputed here**; what fails is the premise that no admissible input can break it. Nothing is relaxed: G7 remains evaluated at the frozen geometry `R_match = 4·r_core` (prereg §5) where it passes at `3.4 ×`, and the finding is converted into a **shipped breaking self-test (FT-9)** rather than into a weakened criterion. The prereg stays BYTE-UNTOUCHED; routed in §9.6.
15. **★A LANE TOOL WAS UNWIRED, AND IS NOW GATING (re-verify R4).** `continuum_radial_solver_number_check.py` protected this document only at the moment someone ran it by hand — there was no reference to it in the `Makefile`, in `.github/`, or in `scripts/`. It is now a prerequisite of `make verify` via a new `verify-lane-number-checks` target, so CI runs it on every PR. **GATING, not advisory, and the choice follows the Makefile's own split:** the advisory tier in this repo (`verify-anchor-content`, which "always exit 0", and `verify-docket-keys`, both invoked with a `-` prefix) is reserved for **heuristic drift detectors**, while the gating tier (`verify-md-links`, `verify-provenance-stamps`, `verify-frozen-provenance`) is for **correctness checks with a definite answer**. A number-check failure means a stated number is not the value its source ships — a definite wrong, the same class as a frozen-provenance mismatch. The tool is hermetic (stdlib only, three in-tree JSONs, one in-tree doc, no `ave` import, no network, no RNG, sub-second), so gating it costs CI nothing and cannot flake — which is exactly why item 12 above had to be fixed *first*: wiring a machine-dependent check into CI would have broken every PR.

**Ledger tags (`consistency-vs-emergence`).** `lame_ratio`, `rho_N`, `rho_S`, `r_Z`, the gate residuals and the conditioning numbers are `[derived]` dimensionless ratios, **CONSISTENCY-class** (analytic-limit reproduction). `c_P`, `c_S` and the D5 gains are `[lattice-measured]`; the radial SHAPE is `[engineering-choice]`; `ν_Hill = 2/7` is `[canon]`, read-only. `β` is a `[disclosed-scan]` with an OPEN magnitude (`clm-m5swh9`); `c_x²` is `[OPEN — HELD]` and never evaluated; the E=mc² trapped-energy inertia law is `[TAGGED IMPORT — NOT DERIVED]` (charter I5) and is never promoted to derived. `α`-CLEAN. **Frozen:** `stage-1 outputs are CONSISTENCY-class (analytic-limit reproduction) or ENGINEERING-class (numerics); no manifestation- or emergence-class claim is made`.

---


- **§8.15 — the "bubble-like" clause carried from the charter (2026-07-28 leak-audit fold, Grant [sic]: "fold").** This lane's frozen prereg §"walk checkpoint" and §C2 restated the charter's term-(i) phrase *"a soft/pressure-release inclusion trends DOWN, bubble-like."* That clause has since been scope-carved at its sources (the β-scoping §4 origin and the charter I4/C2 — dated Rule-12 notes, PR #803): the bubble mechanism is a **mass-density-contrast dipole**, which does not exist for a stiffness-graded inclusion at uniform density. **On this continuum instrument the same carve applies**: an equal-density soft inclusion has no density-contrast dipole either — term (i) is nonzero here only if the `ρ_latent` import supplies an actual density contrast, and must be **derived from the density profile**, not assumed downward. §5's R2 separable-columns output already keeps term (i) distinct, and §5's own C2 note re-grounds it as the `n = 1` dipole channel (absent in stage 1), so **no shipped number rides the carved clause**. The frozen prereg is **byte-untouched** per freeze discipline; this deviation is the disclosure.
## §9 — Consequences ROUTED (this lane surfaces; it does not land)

1. **X5 — the `n = 1` channel gap.** The charter's C1 and C2 are not reachable on the chartered `n = 0` object. Routed to Grant / the auditor lane as a charter-scope finding with the verbatim source quote (§7). No charter edit made here.
2. **The `ρ_N` convention conflict** between charter R5(b) (`ρ_N → 0`) and #775 (`ρ_N` = a ratio, `= 1` with no scatterer). Both observables frozen and shipped (KEEP-BOTH); the convention question is routed, not decided (§2).
3. **D5's missing radial profile.** #796 shipped extrema, not `ε(r)`. Whether a resolved profile is obtainable is #796's OWED-2 territory, already routed to Grant; stage 1 only records that D5's instruction is currently under-supplied by the corpus (§5).
4. **D1 remains HELD** and stage 1 has not consumed it. The §4 lever number (`(c_P/c_S)² = 3.2864`, wider than the whole D2 sweep) is offered as input to Grant's walk, not as a nudge toward any candidate — **and with its direction corrected: `c_S` loads `3.2864 ×` MORE than `c_P`, not less** (§4).
5. **The `ν` gap** (`0.281313` lattice-implied vs `2/7` canonical, `−1.54 %`) is recorded for whichever lane needs the two to agree. No claim made.
6. **★THREE FROZEN/MERGED ARTEFACTS ARE WRONG AND NEED THEIR OWN DATED CORRECTIONS — routed, not edited here.**
   - **★The prereg's G7 "STRUCTURALLY exact" claim** (§5 G7 row and §7 (i)): drive-amplitude independence is roundoff-limited, not structurally exact, and G7 breaches its own `1e-12` tolerance at `R_match/r_core ∈ {8, 16}` — values the same frozen document's G8 criterion declares admissible (§8.14). The frozen file stays byte-untouched; the auditor lane's disposition is requested. **This one has teeth beyond bookkeeping:** it is the premise that licensed dispositioning a gate by caveat instead of by test, and a caveat is unfalsifiable where a self-test is not. Any future prereg in this family should treat "this gate cannot fail" as a claim requiring a measurement, not as a disclosure.
   - **The merged charter's I8 first clause** (`research/2026-07-21_continuum-radial-solver_CHARTER.md`, I8 paragraph): *"`c_P` (`0.519`, the compression carrier's own speed) gives the smallest inertia divisor and the largest loading"*. `c_P = 0.51884 > c_S = 0.28621`, so `c_P²` is the **largest** divisor and gives the **smallest** loading; `c_S` loads `3.2864 ×` more. This lane's fence forbids a charter edit regardless of outcome, so the correction is routed to the lane that owns the charter, with the clause quoted verbatim for verification. **★It was also relayed to Grant in the full-picture briefing as an agreement between this lane's number and I8; that relay was wrong in its direction and needs correcting to him directly.**
   - **The prereg §8 reachability argument** (`≥ 4` orders of margin on every gate) is contradicted by the measured run on every gate with a finite margin (§8.7). The frozen file stays byte-untouched; the auditor lane's disposition is requested.
7. **The review's `4–11 orders` grade-shape figure** is not reproduced by this lane's own measurement (`1.7063` orders across `grade_power ∈ [0.1, 32]` at fixed `S_rail`). The scope-limit finding is adopted; the magnitude is flagged for reconciliation rather than relayed (§6).
8. **★The `verify-lane-number-checks` gate is repo-wide infrastructure minted by a research lane** (§8.15). It is currently a one-lane target. Whether other lanes' result docs should be brought under the same registry discipline — and whether the target belongs in the Makefile's gating tier long-term — is an infrastructure-lane call, not this lane's. Surfaced for disposition; the wiring itself is the minimum needed to stop this lane's own tool from being decorative.

---

> **Result-doc provenance.** The stage-1 certification RUN of the frozen prereg `research/2026-07-28_continuum-radial-solver-stage1_prereg-FROZEN.md`, under Grant's 2026-07-28 rulings (`"D2: disclosed, D3: follow rec, D4: do it, D5: do the rec"` `[sic]`) with **D1 HELD**. Driver `research/drivers/continuum_radial_solver.py`; all numbers read from the shipped `research/drivers/continuum_radial_solver_stage1_results.json` (driver-generated; hand-edited exactly once, for the disclosed withdrawal stamp, §8.9) and **machine-checked by `research/drivers/continuum_radial_solver_number_check.py`**. **CLASS WITHDRAWN → REPAIRED → RE-EARNED as `A_CERTIFIED`** (2026-07-28, on the PR #801 adversarial review). G3 and G2 each divided two bit-identical computations and therefore could not fail, which voids the certification exactly as hard as a failing gate (prereg §6/§8); the class was withdrawn to `C_NOT_CERTIFIED_VOID` **in its own commit, before any repair**. G3 was re-pointed at a new independent `iso_shell` construction and given self-test FT-5; G2's null arm was rebuilt from first principles as `uniform_reference_profile` with FT-2 re-pointed at it; FT-6/FT-7/FT-8 were added. **A second, independent RE-VERIFY audit then returned CLEAR-WITH-REPAIRS**, independently reproducing the G3/G2 repair by its own mutations and prescribing six further repairs, none of which voids the class. Its substantive finding: **G7's "no self-test is possible" caveat was false** — G7 FAILS at `R_match/r_core = 16`, a value the prereg's own frozen G8 sweep declares admissible — so **FT-9 was minted and the caveat dropped**, and all nine gates now carry a breaking self-test with zero caveats. Also repaired: the `ortho_misnorm` no-op under `iso_shell` (refused, not ignored); the number-check's self-defeating `_runtime_sec` registration (token dropped, field made non-registrable, tool wired GATING into `make verify`); FT-1 now ships G1b's `shell_agreement` coverage; and the `frozen_at_repair` vs `frozen` warrant distinction is stated where the self-tests are presented. **The re-run returns `A_CERTIFIED`** — nine gates PASS at margins of `2.1 ×` to `3.7e3 ×` (two exact), **nine** self-tests FIRE, every pre-existing gate and self-test value **bit-identical** to the pre-repair run, determinism confirmed by two independent full runs returning `efea5059a98d0503b094737d9e7477b1d737dccba87bf9545916c6993cb25576` — with mutation receipts showing the repaired gates now FAIL under the corruptions the shipped ones tolerated, and the force-C precedence re-verified with FT-9 present. No criterion was relaxed (Rule 11); the frozen prereg is BYTE-UNTOUCHED; the two defective frozen sentences and the charter's I8 error are disclosed and routed, not amended. **Banks NO physics verdict.** Eight consequences routed unreframed (§9): the `n = 0`-channel scope finding (X5, §7), the `ρ_N` convention conflict (§2), D5's missing radial profile, D1's held lever with its direction corrected, the `ν` gap, the merged charter's I8 error together with the prereg's defective reachability argument and its falsified G7 exactness claim, the unreproduced grade-shape magnitude, and the repo-wide scope of the new number-check gate. Engine byte-untouched; mints no `clm-`/`def-`; no leaf, port-register, ledger or charter edit. Companions: the frozen prereg, the merged charter **#789**, merged **#796** (`research/2026-07-22_vessel-state-rve_result.md`, the D5 profile source and the Protocol-E mutual-satisfiability lesson), merged **#782** (`research/2026-07-21_rve-aggregation-bench_result.md`, the `r_Z` discriminator and the Lamé gate), merged **#775** (`research/2026-07-20_deep-rail-kscaling_derivation.md`, F2 and the regime gap), and the docket fragment `_orchestration/docket-entries/2026-07-28-continuum-radial-solver-stage1.md`.
