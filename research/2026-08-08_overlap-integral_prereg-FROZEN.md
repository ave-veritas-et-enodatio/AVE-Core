# THE COMPRESSION-LINE OVERLAP INTEGRAL — FROZEN pre-registration

**Date:** 2026-08-08 · **Branch:** `lane/2026-08-07-overlap-integral` · **Base:** `origin/main` @ `051887c4`
**Lane brief:** [`_orchestration/2026-08-07_overlap-integral-brief.md`](../_orchestration/2026-08-07_overlap-integral-brief.md) (Grant GO 2026-08-07, ruling R29 verbatim *"let's compute?"* — `_orchestration/docket-entries/2026-08-07-rulings-r28-r30.md:25`)
**Class:** DERIVATION + lattice-derived research-driver (Tier-2 at adjudication). **Mints no `clm-`/`def-`; edits no KB leaf, register, ledger, or ruling; changes no solidity. All propagation ROUTED.** Engine `src/ave` byte-untouched (drivers import `ave.core` read-only, Rule-14 reuse of receipted lattice constructions).
**Freeze-by-push (ave-prereg Step 3.11):** this prereg commits ALONE and pushes BEFORE any driver code, derivation text, or number produced by this lane exists on this branch. The push timestamp is the freeze event.

---

## §0 — Standard Vacuum Analysis header (SVA v0.2-pilot; all 11 rows)

 1. **SECTOR / OWNERSHIP:** Source = A1 dilatation content (mass; `master-equation.md:20` banner — TWO-"3"s guard active: A1 dilatation-MASS ⊥ Cosserat (2,3) WINDING-charge; never cross-wire). Channel under test = the propagating longitudinal common mode of the bond network (the mechanical P-branch; A1-carried, gapless). Observed GW = T2 transverse shear at c — NOT touched here. **Rotational fence (brief, verbatim class): if any derivation step finds the outbound coupling routes through the Cosserat micro-rotation channel, STOP and route to Grant.** By construction the drive lives on the displacement field `u` (radial force patterns; no couple-stress applied) — the fence is declared and checked, not assumed. **Two-groups declaration (the load-bearing index bookkeeping, frozen up front):** the K4 4-port `A_1 ⊕ T_2` decomposition (`k4-port-irrep-decomposition.md:41-43`, `clm-j550uh`) acts on the PER-NODE 4-BOND AMPLITUDE space under the site group T_d; the SPATIAL angular decomposition of a shell pattern (l = 0, 1, 2, …; l=2 → E ⊕ T₂ under the crystal point group) acts on SITE POSITIONS under the space group (srs: I4₁32, point group 432). These are DIFFERENT group actions on DIFFERENT spaces. No statement licensed by `clm-j550uh` (bond-amplitude A1 preservation) transfers to spatial angular structure without an explicit derivation (gate G-CONFLATE). The corpus-grep sweep confirms the spatial l=2 E⊕T₂ decomposition appears NOWHERE in the corpus at HEAD — this lane constructs it.
 2. **REGIME / PHASE-STATE:** Far field: Regime-I cold crystalline, linear, small-signal, cold-reactive (Ax3-lossless; far-field radiation is a legal port, port-not-valve). Driver amplitudes frozen sub-yield (`A = 10⁻³` class, saturation OFF — #761 Leg-C precedent). Physical band declared: at the pulsar drive, `k·ℓ_node ≈ 3×10⁻²⁵` (#919 §4f band-corrected figure); the driver probes the SAME structure question at accessible `k·ℓ_site ≈ 0.1–0.3` and the continuum-limit extrapolation is carried by the derived correction order O((kℓ)²), declared in §8. **Member fence (kernel-collapse record, declared here):** the cross-grade aggregation rule is UNDERDETERMINED in canon (`2026-08-05_last-bond-kernel-collapse_result.md` headline; axiom-register `cross-grade aggregation underdetermined at O(α)`; successor certification PR #902 = `2026-08-05_last-bond-g-rho2-rerun_result.md`). **This lane is INSENSITIVE to that fork:** every computation lives at `A → 0`, `S_sat → 1`, where every member of the combine-rule family coincides (all kernels → identity; no wall, no grade collision). No step below consumes any aggregation-rule member.
 3. **CIRCUIT STATEMENT:** A polyphase-commutated source (the R28 walk's carve, UN-AUDITED rows consumed per the R28 audit outcome) presents a boundary condition at a port surface around the source. Two drive classes on the SAME port surface into the SAME line: (a) RADIAL-AC — net displacement current through the surface oscillating (the l=0 breathing drive; eccentric-orbit class); (b) COMMUTATION — a constant-magnitude l=2 bias pattern rotating at Ω (fixed nodes see 2Ω AC; net flux through the surface DC; circular-orbit class). Question: does the compression line (the longitudinal transmission-line branch) ACCEPT drive class (b) — i.e., is the mode-overlap integral between (b) and the propagating common mode nonzero — or does the network's structure reject it the way a common-mode choke rejects differential drive? Total-vs-slot: the pulsar comparator `Ṗ_b` reads the TOTAL source energy budget (frozen in the #919 prereg SVA row 3; inherited).
 4. **PLANE & PROJECTION:** Radiation-zone far field; plane-wave Bloch/Christoffel eigenbasis. The radiative longitudinal row has exactly ONE speed family (the P-branch, `√(10/3)·c` isotropic re-expression; direction-resolved `c_P/c_S` [100] 1.7105 / [110] 1.8528 / [111] 1.9041, `clm-bnd5rq`); the `√2·c` object is constitutive, NOT-A-WAVE (LC-1 §3 row 3a, branch receipt `origin/research/lc1-one-speed`, PR #913 OPEN — cited as branch state, not main). Near-field reactive storage (the halo, port-register P9) is a different plane and is not read as radiation. Test coordinates = the displacement-vector longitudinal/transverse partition at a measurement shell (radial vs tangential energy split — the #761 C-1 coordinate, matched to the corpus claim's channel-radiation coordinate; phase-space-coordinate check A46-class: PASS by construction, same partition as the incumbent's driver).
 5. **CONSTITUTIVE PROVENANCE:** `ρ* = k_a/k_s = 9.77337` — DERIVED-form (bisected to `ν_Hill = N_NU = 2/7` on the Born-Huang pipeline, `srs_vector_band_survey.py:151-173`; `N_NU` from `constants.py:397`), value rides the GR-imported `ν = 2/7`/`K = 2G` chain (PR #261) — IMPORT-tagged, and only the EXISTENCE of a gapless P-branch with `c_P ≠ c_S` is load-bearing here, not the ratio's value. `c_P/c_S` — lattice-computed (survey `clm-bnd5rq`, 0.8 ok-with-caveats). Angular forms `j_l'(kR)`, `8π/15`, `4π/9` — DERIVED (theorems of isotropic angular integrals; anisotropy tolerance declared §8). Matter→lattice pluck operator — ABSENT (`axiom-register.md:193`; #919 R23: NOT-DERIVABLE-on-the-amplitude). **This lane's verdict is AMPLITUDE-FREE by construction:** both drive classes carry the same unreceipted normalization, which CANCELS in every frozen observable (all dimensionless ratios); the verdict adjudicates STRUCTURE (zero vs nonzero and its order), not coupling strength.
 6. **ENERGY LEDGER:** Far-field radiation into the gapless P-branch = a PORT (boundary-crossing arrow). The drive does work on the shell nodes (work-in ledger); post-burst free evolution conserves `H` to the bounded-Verlet precedent (`|ΔH/H| ≲ 1×10⁻²`, #761 `result.md:113` + `cosserat_wave_test` T3). Static-pattern control = the no-arrow reference (stores-and-returns near field only). No loss word without its port named.
 7. **CALIBRATABILITY:** Every frozen observable is dimensionless: `R_{c/s} = E_P^{comm}/E_P^{static}`, `R_{c/a} = E_P^{comm}/E_P^{radAC}`, the spectral overlap ratio `∮|O_comm|²/∮|O_radAC|²`, the order-receipt curve `|j_2'(kR)/j_0'(kR)|²`, the eccentricity functions `f_TL(e)`, `(5/96)e²`. The unknown source normalization cancels in every one. PASS.
 8. **DISCRIMINATION CLASS:** **DC→AC coupling** (a DC medium bias configuration — the commutated pattern's magnitudes are DC — read out through the AC radiative channel at 2Ω). Liveness-proven readout: the l=0 positive control is #761 Leg-C's breathing source (`f_long = 0.982` port-open, merged) run through the SAME pipeline. Tautology filter: NOT an identity — the incumbent continuum answer (nonzero) and the walk's struck restriction (radial-AC-only ⇒ zero) are OPPOSITE fireable outcomes; the lattice adjudicates. SM counterfactual: GR's gauge structure zeroes the scalar channel entirely; continuum elasticity gives nonzero; the lattice could in principle differ from BOTH — genuinely discriminating.
 9. **CERTIFICATION PLAN:** gates frozen in §10 before any number (G-CONFLATE, G-POSCTRL, G-SPEC, G-WINDOW, G-MOMENT, G-SCALE, G-FREQ, G-XMETHOD, G-DRIFT, G-ECC, G-NUM). UNRUN ≠ PASSED; every gate is run and scored in the result.
10. **ADJUDICATION ROUTING:** The verdict (one of the three frozen bins, §2) routes to the orchestrator; #913/#919/arc-scope/#770 are HELD on it plus the R28 audit. The eccentricity-scaling statement is the bankable forward-prediction shape and routes with the verdict. NO leaf, register, ledger, or ruling edit on ANY outcome. PR opens `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; lane runs its own Tier-2 (ave-adversarial-pr-review) before anything reads CLEARED.
11. **NUMERICAL CONDITIONING:** Time-domain: velocity-Verlet at `dt = 0.2·(2/ω_max)` (power-iteration ω_max, #761 precedent); no iterated maps; energy ledger bounded. Spectral: `numpy.linalg.eigh` on 12×12 Hermitian Bloch matrices — trivially conditioned; direction sphere = Fibonacci 2000 + high-symmetry set. Bessel forms via `scipy.special.spherical_jn` against symbolically-carried closed forms. Shell-pattern conditioning gated (G-MOMENT Gram condition < 50). Named regex engines for any absence claim (GNU grep -E + Python re; the `**`-pathspec and `$..$`-escaping false-negative classes guarded).

---

## §1 — The frozen question (verbatim from the brief; the brief is the frozen source)

`_orchestration/2026-08-07_overlap-integral-brief.md:37-43`, verbatim `[sic]` (section heading at `:35`):

> For the lattice's longitudinal propagating mode (the common mode of the bond network),
> compute the source-coupling overlap for two boundary-condition classes at the port
> surface: (a) RADIAL-AC — net displacement current through the surface oscillating
> (eccentric-orbit class); (b) COMMUTATION — constant-magnitude bias pattern rotating
> (circular-orbit class; the l=2 spatial harmonic of a scalar profile, time-varying at
> 2Ω at fixed points). Does (b)'s overlap with the propagating common mode vanish by the
> network's structure (the mode functions' orthogonality / the Green function), or not?

## §2 — The frozen verdict grammar (verbatim from the brief)

`_orchestration/2026-08-07_overlap-integral-brief.md:47-55`, verbatim `[sic]` (section heading at `:45`):

> - **OVERLAP-ZERO** — (b) is projected out by named structure: circular systems are
>   silent on the compression line; the #919 floor re-scopes to radial-AC (eccentric)
>   drive only; derive the eccentric coupling order next.
> - **OVERLAP-NONZERO(n)** — (b) couples at derived order n: compute the flux and
>   compare BOTH frozen comparators (below); the scalar-tensor door is open and the
>   two-pulsar pair adjudicates.
> - **NOT-DERIVABLE** — a named piece is missing (enumerate; the likely gap is the
>   matter-to-lattice source term — if so, state exactly what a pluck derivation must
>   deliver for this integral to close).

---

## §3 — THE INCUMBENT (travels verbatim; engaged step by step, not routed around)

#761 §2, `research/2026-07-20_mechanical-commonmode-derivation_result.md:79`, verbatim `[sic]` (quoted from the SOURCE file at base HEAD; the brief carries the same text at `:16-25` with dispatch-added bold on "for a circular binary, is constant" and a closing ellipsis — content-identical, formatting delta disclosed here):

> Kept for completeness (the last source-side escape). The A1 channel is the scalar/breathing sector; the mass second moment `M_ij` has a trace `Σ M_a r_a²` that, for a circular binary, is constant. Tempting: "the A1 sees only the constant trace ⇒ no radiation." **FALSE.** A scalar/longitudinal field radiates at quadrupole order via the **traceless** second moment of the source (standard acoustic multipole radiation — the same structure that gives every earthquake its P-arrival). With `mass = A1-dilatation`, the scalar source's second moment IS `M_ij`, whose traceless part **rotates at `2Ω`** — nonzero. Reproduced by the #750/#758 drivers (`⃛Q^{TL} ≠ 0`; the traceless `xx` component varies while the trace is constant). The tracelessness rescue is dead; the surviving symmetric-coupling route reduces to the B1 grade-assignment (consistency-class, not derived) and is subsumed by the §1.3 Lagrangian argument. `[derived]`

**The frozen engagement protocol.** The incumbent's argument decomposes into steps; the lattice-native derivation (Method 1, §6) re-derives EACH on the discrete network and scores it SURVIVES / DIVERGES(named step) / NOT-DERIVABLE(named gap):

| Step | The incumbent's continuum move | The lattice-native question |
|---|---|---|
| **S1** | The far field of a localized source expands in multipoles; monopole killed by `Ṁ = 0`, dipole by momentum conservation (#761 §1.2, #919 §2 reproduced) | Does the DISCRETE far-field expansion (Bloch-mode response of the driven network) carry the same ladder with the same kills? |
| **S2** | The traceless second moment of a circular binary rotates at 2Ω — nonzero | Pure source kinematics; lattice-independent — carried, not re-derived |
| **S3** | "A scalar/longitudinal field radiates at quadrupole order via the traceless second moment" — standard acoustic multipole radiation, the IMPORTED step | Does the discrete network's propagating common mode ACCEPT the rotating l=2 boundary pattern (nonzero overlap), or does a named lattice structure — mode-function orthogonality, the discrete Green function's angular content, a point-group selection rule, topology — project it out? **THE LANE'S QUESTION.** |

**The frozen divergence-candidate register (the exact-zero hunt; every candidate adjudicated in the result, none silently dropped):**

- **D1 — port-space conflation:** "the lattice common mode is the 1-D bond-amplitude A1 irrep, a 1-D space cannot carry l=2, so the commutation pattern is orthogonal to it." (The seductive OVERLAP-ZERO argument; adjudicated by the two-groups bookkeeping of §0 row 1.)
- **D2 — sector-ownership conflation:** "traceless ⇒ T2-sector ⇒ not the A1 channel's to radiate." (The spatial-tensor-trace vs port-irrep index crossing; adjudicated against `master-equation.md:20` sector ownership: the spatial second moment of A1-scalar content is A1-sector content with l=2 ANGULAR structure.)
- **D3 — discrete-sampling zero:** the finite shell of lattice sites collapses the l=2 moment (Gram degeneracy / accidental alignment). ARTIFACT class if found (driver-gated, G-MOMENT), not physics.
- **D4 — anisotropy:** the srs Christoffel sheet `c_P(k̂)` deforms the propagating shell and mixes l-channels. Candidate for coefficient modification O(1), NOT a candidate for an exact zero (mixing adds channels; a zero requires a symmetry, and 432 ⊅ any l=2-killing constraint on a scalar shell pattern — derived in Method 1).
- **D5 — the pose question (the brief's named likely gap):** can the binary's far-field boundary condition be POSED as a port-surface force/bias pattern class at all without the ABSENT pluck operator? If the pose itself requires an unreceipted import beyond the class declaration (radial bias pattern ∝ the A1 profile's l=2 harmonic), NOT-DERIVABLE fires with the enumeration of exactly what a pluck derivation must deliver.

---

## §4 — Frozen comparators (re-retrieved from source at freeze, THIS session, before any number exists on this branch; pointers-not-values honored by quoting source + banked pointer)

| System | Source statement (verbatim, retrieved 2026-08-08 via WebFetch) | Frozen values |
|---|---|---|
| **Hulse-Taylor PSR B1913+16** | *"We find that the ratio of observed orbital period decrease due to gravitational wave damping (corrected by a kinematic term) to the general relativistic prediction, is 0.9983 pm 0.0016"* — Weisberg & Huang 2016, arXiv:1606.02744 abstract. Eccentricity NOT in the abstract; Keplerian-solution value `e_HT = 0.6171334` `[import]` (Weisberg & Huang 2016 Keplerian parameters; secondary retrieval receipt: Wikipedia Hulse–Taylor entry, retrieved 2026-08-08, "Eccentricity: 0.6171334") | `δ_HT = 0.0016` (1σ fractional); `e_HT = 0.6171` — hard radial-AC class |
| **Double pulsar PSR J0737-3039A/B** | *"two radio pulsars (A and B) that orbit each other with a period of 2.45hr in a mildly eccentric (e=0.088) binary system"* + *"We present the currently most precise test of general relativity's (GR's) quadrupolar description of gravitational waves, validating GR's prediction at a level of 1.3 × 10⁻⁴ (95% conf.)"* — Kramer et al. 2021, arXiv:2112.06795 abstract | `δ_DP = 1.3×10⁻⁴` (95% conf. fractional); `e_DP = 0.088` — almost-pure commutation class |

Cross-check: both residuals match the #919 prereg §3 frozen table exactly (`origin/research/a1-port-sourcing`, `2026-08-07_a1-port-sourcing_prereg-FROZEN.md` §3) — no drift between bank and source. The brief's `e ≈ 0.62 / e ≈ 0.09` (`:59-61`) match the retrieved values.

**Comparator role, frozen before any number:** if OVERLAP-NONZERO(n) fires, the coupling exists AT e = 0 (the commutation class is the circular limit), so BOTH pulsars are driven at their respective f_TL(e) enhancements and the eccentricity-scaling statement (§8, E-arm) must make the two-system consistency explicit: any nonzero coupling must survive the DP bound at commutation-drive level (no circularity suppression available) AND the HT bound at its enhanced drive. If OVERLAP-ZERO fires, the floor re-scopes ∝ e² (radial-AC only) and the two-system pattern INVERTS (HT binds, DP nearly escapes) — that inversion is the discriminating fingerprint between the two verdicts, stated here at freeze.

## §5 — Corpus state (ave-prereg Steps 1–2; two background corpus-grep sweeps, receipts in the lane record)

**Corpus state: PARTIAL — every structural ingredient exists; the assembled object does not.** Verified by two ave-corpus-grep sweeps at base HEAD `051887c4` (sweep A: prior-art/physics; sweep B: engine/driver inventory), first-party spot-checks by this session:

- **No prior lattice-native computation of the (b)-class overlap exists anywhere in the corpus.** The spatial l=2 decomposition under the crystal point group (E ⊕ T₂) appears in NO corpus doc (sweep A, two-method: `grep -rn "cubic harmonic|Kubic|tetrahedral harmonic|E ⊕"` — zero on-topic hits). No finite-frequency srs/K4 Green function with computed angular content exists (sweep A F8; the static Laplace G at `src/scripts/vol_1_foundations/k4_greens_function.py` + archive doc is l=0/static-only; `cosserat.py:421-461` names retarded Bethe-lattice G_ret an OPEN problem).
- **The direct precursor is an acknowledged unbuilt item:** #761's frozen C-2(b) quadrupole eigenvector projection — prereg wording `2026-07-20_mechanical-commonmode-derivation_prereg-FROZEN.md:74` verbatim: *"Compute the far-field radiated-power partition of (a) an isotropic dilatation source and (b) a rotating mass quadrupole, from the eigenvector projections `|ê_b·Ŝ|²` weighted by the `1/c_b⁵` flux scaling"* — was NOT implemented (`mechanical_commonmode_injection.py:363-365` in-code note; #761 §6.2-4 routed it as an owed follow-on). This lane delivers the lattice-native version of that owed object AND the broader overlap-vanishing adjudication.
- **The incumbent's source was a BODY-FORCE delta, not a port-surface BC** (#761 §1.2: `s = (1/ρ)∇·f ∝ M_a δ(x − x_a)`, coupling grade ASSUMED-symmetric). The port-surface BC-class formulation is brief-new (sweep A F10: no general port-surface→propagating-channel formalism exists; the only surface⇄source equivalence receipt is static/monopole, `2026-07-03_em-readout-vsector-stage1b_result.md:60`).
- **The eccentricity carve is brief-new** (sweep A F11: no corpus doc distinguishes circular vs eccentric bulk-channel emission; `grep -rn "eccentric"` returns only periastron/Gaia/Lissajous hits in other observables).
- **Reusable machinery (named engines, Rule-14):** the 12×12 rank-2 Bloch dynamical matrix `vector_bloch_D` (`src/scripts/vol_1_foundations/srs_vector_band_survey.py:100-114`); the acoustic-branch max-|ê·k̂|² identifier (`research/drivers/mechanical_commonmode_injection.py:305-314`); the finite-srs builder (8-site I4₁32 Wyckoff-8a motif, `_SRS_8A`/`_SRS_NN` from `ave.core.chiral_lattice:47-60`, cKDTree exact-NN bonds — `mechanical_commonmode_injection.py:74-92`); the rank-2 bond force `Φ_b = k_a d̂⊗d̂ + k_s(I−d̂⊗d̂)` (`:95-108`); the driven-boundary stepper pattern with rotation support (`research/drivers/envelope_sector_orbiting_lump.py:184+`, `_rotating_binary` at `:324-328` — the corpus's only rotating-source time-domain precedent, in-code expectation *"radiated wave at 2Ω (quadrupole)"*); the spectral Poincaré window (`mechanical_commonmode_injection.py:197-206`); `ρ* = 9.77337` bisected to `ν_Hill = 2/7` (`srs_vector_band_survey.py:151-173`, imported not hard-coded); the `*_number_check.py` + `--mutation-receipt` gate pattern auto-discovered by `make verify` (`Makefile:474-530`).
- **Standing-canon tension this lane's verdict feeds (flag-don't-fix; NOT edited here):** FLAG-LC1-B — `physics-lineage-map.md:63` "the A1 grade is non-radiative in free space" vs the Q1 Reading-A-live revert (#919 headline resolves it as a fossil conflation, routed).
- **Branch-state citations disclosed:** LC-1 receipts live on `origin/research/lc1-one-speed` (PR #913, OPEN); #919 receipts on `origin/research/a1-port-sourcing` (PR #919, OPEN). Both cited as branch state throughout, never as main.

**PREREG summary paragraph (Step 3):** Corpus state PARTIAL. Prior work: #761 (P-branch exists, breathing source couples at `f_long ≈ 0.98`, continuum-import color-check only for the quadrupole), #919 (order n=2 settled, amplitude NOT-DERIVABLE), LC-1 (two-eigenspeed far field), R28 walk + audit (radial-AC-only restriction STRUCK once already as a closed-path reconstruction). My prediction: **OVERLAP-NONZERO(n=2)** — declared strong prior, NOT flat: the continuum incumbent is adverse to zero, and the two-groups bookkeeping (D1) plus the Rayleigh-expansion structure of Bloch plane waves make an exact lattice zero possible ONLY via a point-group selection rule that 432 does not supply for a scalar-profile l=2 pattern. Why: the propagating longitudinal shell spans all directions k̂; a shell-restricted l=2 pattern projects onto it through `j_2'(kR)`-class radial overlaps, which have no symmetry-forced zero. Discriminating outcomes: (A, likely) OVERLAP-NONZERO(2) with the overlap matching the `j_l'` forms → the incumbent's imported step is CONFIRMED lattice-natively, the exclusion stands on the lattice's own authority, circularity provides no escape; (B) OVERLAP-ZERO with a named structure → the #761 §2 import is an IMPORT ARTIFACT, the Q1 revert chain re-opens, the floor re-scopes to eccentric drive; (C) NOT-DERIVABLE on the pose (D5). Falsifier of my framing: a derived point-group or bond-network selection rule killing the l=2 overlap exactly — that is precisely what the D-register hunts.

---

## §6 — METHOD 1 (analytical, lattice-native; the adjudicating method — stencil-lens: derivation adjudicates, engine corroborates)

Frozen step list (executed in the result doc; no step silently dropped):

- **M1-S0 (G-CONFLATE):** Write out the two-groups bookkeeping BEFORE any verdict-bearing algebra: (i) the per-node 4-port T_d action and its `A_1 ⊕ T_2` (bond-amplitude space; what `clm-j550uh` licenses); (ii) the space-group action on shell site positions; the decomposition of the l=2 spherical-harmonic quintet under the srs point group 432 (O): `l=2 → E ⊕ T₂` (dimension check 5 = 2+3); (iii) the statement that the shell site-function space of a generic port shell contains every 432 irrep (regular-representation content; Gram-rank checked numerically in Method 2). Conclusion form: whether ANY irrep argument can zero the overlap of a scalar-profile l=2 pattern with the longitudinal branch's shell restriction.
- **M1-S1 (ladder reproduction):** the driven-lattice far-field response as a Bloch-mode sum: `u(s,ω) = Σ_k χ_k(s) ⟨χ_k|f⟩ / (ω_k² − ω²)`; stationary-phase/propagating-shell extraction of the far field; reproduce the receipted monopole/dipole kills (`Ṁ = 0`, `Ẍ_cm = 0`) in the discrete expansion — the e^{ik·x_s} phase on discrete sites carries the SAME multipole ladder algebra; only the site measure and the dispersion differ from continuum. Score vs #919 §2 (must reproduce identically before any new step is trusted — the #919 G-LADDER condition inherited).
- **M1-S2 (the overlap closed form):** derive the port-shell overlap for radial-force pattern class `F(s) = F₀ Y_lm(n̂_s) n̂_s` against the longitudinal Bloch branch: continuum-shell limit `O(k) = F₀ N (−i)^{l−1} j_l'(kR) Y_lm(k̂)` (via the Rayleigh expansion + the `l·j_{l−1} − (l+1)·j_{l+1} = (2l+1)j_l'` identity), then the DISCRETE corrections: (i) finite-site sampling (Gram analysis), (ii) Bloch-eigenvector deviation from k̂ at finite k (lattice polarization pull), (iii) anisotropic shell `k(ω, k̂)`. Name the order of each correction; show whether any can produce an EXACT zero rather than a coefficient shift.
- **M1-S3 (the exact-zero hunt):** adjudicate every D-register candidate (D1–D5, §3) with a derivation or a named receipt. D5 (the pose question) is adjudicated here: state exactly what class of port-surface pattern a circular binary's far-field boundary condition occupies (the l=2 harmonic of the A1 scalar profile, rotating rigidly — carried by S2's kinematics), and whether posing it requires anything beyond the ABSENT amplitude normalization.
- **M1-S4 (the incumbent engagement table):** score S1/S2/S3 of §3 SURVIVES / DIVERGES(exact step named) / NOT-DERIVABLE(gap named). This table IS the brief's required deliverable "the exact continuum-vs-lattice divergence step if any."
- **M1-S5 (the eccentricity map):** derive the port-surface-class ↔ orbit-class map: trace channel `tr M = μ d²(t)` (AC only for e > 0; leading Fourier amplitude `∝ e` at Ω) vs traceless channel (nonzero at e = 0, rotating at 2Ω). Derive the leading-order flux decomposition `F_bulk(e)/F_bulk(0) = f_TL(e) + (5/96)·e²·[1 + O(e²)]` symbolically: the trace-channel coefficient from `⟨(⃛tr M)²⟩ = 2e²μ²a⁴Ω⁶` (O(e) Kepler expansion) against `⟨|⃛M^{TL}|²⟩_{e=0} = 32μ²a⁴Ω⁶` with the l=0 vs l=2 angular weights `4π/9` vs `8π/15`; the identification `f_TL(e) = f_PM(e) = (1 + 73e²/24 + 37e⁴/96)/(1−e²)^{7/2}` (same tensor invariant `⟨⃛M^{TL}:⃛M^{TL}⟩` as Peters–Mathews' enhancement — an identification to be VERIFIED numerically, not assumed; G-ECC). Spectral signature carried: the trace channel radiates at the orbital fundamental Ω (∝ e), the commutation channel at 2Ω.

## §7 — METHOD 2 (numerical, two arms, named engines; corroborating per the stencil lens)

**Arm 2A — spectral (the mode-overlap integral computed directly; finite-size-free; the precision arm).** Engine: `vector_bloch_D` (12×12 rank-2 srs Bloch dynamical matrix, `srs_vector_band_survey.py:100-114`, Rule-14 reuse) + the max-|ê·k̂|² longitudinal identifier (#761 driver `:305-314`). For each direction k̂ on a Fibonacci-2000 sphere (+ {100, 110, 111}): solve `c_P(k̂)` and the FINITE-k longitudinal eigenvector at `|k| = ω_d/c_P(k̂)`; evaluate the discrete overlap `O(k) = Σ_{s∈shell} [ê_L(k)·n̂_s] P(n̂_s) e^{−i k·x_s}` over the ACTUAL srs port-shell sites (from the same finite-net builder); form `∮|O|² dΩ_k̂` (unweighted primary; group-velocity/DOS-weighted variant reported as sensitivity). Patterns `P`: l=0 (`Y_00`), l=2 m=+2 (rotating-class), each unit-L2-normalized over the shell. Sweep `kR_port ∈ {1.0, 1.5, 2.2, 2.6}`. Output: the overlap ratio `ρ_spec(kR) = ∮|O_{l=2}|²/∮|O_{l=0}|²` vs the continuum closed form `|j_2'(kR)/j_0'(kR)|²`.
**Arm 2B — time-domain (independent second method; the radiation receipt).** Engine: the #761 finite-srs builder + rank-2 bond dynamics + velocity-Verlet (`mechanical_commonmode_injection.py` construction, Rule-14 reuse; driven-stepper pattern per `envelope_sector_orbiting_lump.py`). Frozen geometry: `L = 48` conventional cells (8·48³ = 884,736 sites), `a_cell = 1`, `ρ* = 9.77337` imported via the bisection function (not hard-coded), `r_port = 3.0` cells (drive shell, width 1.0), `r_meas = 12.0` (measurement shell, width 1.0), `d_face = 24`. Drive: radial FORCE patterns (body-force, matching the #761 §1.2 source class — NOT kinematic displacement clamping) with a 3-cycle Gaussian-windowed tone burst at `ω_d = c_P^{spec}·k_d`, `k_d = 0.8667 cell⁻¹` (`kR_port = 2.6`); force amplitude scaled for peak displacement response ≤ 10⁻³ (sub-yield, saturation regime OFF). Three runs, equal time-averaged mean-square force: (a) RADIAL-AC `F ∝ Y_00 n̂ cos(ω_d t)·env(t)`; (b) COMMUTATION `F ∝ Re[Y_22(n̂_s) e^{−i 2Ω t}] n̂ · env(t)` with `Ω = ω_d/2` (the pattern rotates at Ω; fixed nodes see 2Ω = ω_d); (c) STATIC CONTROL `F ∝ Re[Y_22(n̂_s)] n̂ · env_ramp-hold(t)` (same ramp, held constant — the no-arrow floor). Measurement: radial/tangential energy split at `r_meas` (the #761 C-1 partition) integrated over the analytic spectral window `[t_arr, t_reflect)` at the C-2 spectral speeds (G-WINDOW; no front-detect); FFT of the shell-mean radial velocity for the frequency receipt. Primary outputs: `E_P^{comm}`, `E_P^{radAC}`, `E_P^{static}`; `R_{c/s}`, `R_{c/a}`; the 2Ω spectral peak.
**Arm 2C — eccentricity check (pure orbit kinematics; no lattice).** Numeric Kepler-orbit average of `⟨⃛M^{TL}:⃛M^{TL}⟩(e)` and `⟨(⃛tr M)²⟩(e)` vs the symbolic §6 M1-S5 forms (G-ECC), evaluated at `e ∈ {0, 0.05, 0.088, 0.3, 0.6171}`.

**Driver ships as:** `research/drivers/overlap_integral_lattice.py` → `research/drivers/overlap_integral_lattice_results.json` + `research/drivers/overlap_integral_lattice_number_check.py` (with `--mutation-receipt`; auto-discovered by `make verify`). Driver committed BEFORE the result doc.

## §8 — Analytic expectations, WITH NUMBERS (Step 3.9; frozen before any code) + dimensional analysis (Step 3.5)

**Dimensional analysis:** every frozen observable is a dimensionless ratio of same-dimension quantities on the same lattice (energies in the identical partition/window; overlaps of identically-normalized patterns) — no dimensional primitive enters any verdict-bearing number. Power-counting for the leading forms, frozen explicitly: `j_0'(x) = −j_1(x) → −x/3` and `j_2'(x) → 2x/15` as `x → 0`, so the small-kR overlap ratio `|j_2'/j_0'|² → (2·3/15)² = 4/25 = 0.16` — the two port-surface classes couple at the SAME leading power of kR for FORCE-class drive (the (kR)^{2l} suppression of the ladder lives in the SOURCE moments across the source region — #919's order result — not in the port-shell force overlap; this distinction is stated here at freeze so the result cannot blur the two objects).

**Frozen predicted numbers (isotropic-continuum reference; closed forms carried symbolically, evaluations to 4 s.f.):**

| `kR_port` | `ρ_ref(kR) = |j_2'(kR)/j_0'(kR)|²` |
|---|---|
| 1.0 | 0.1460 |
| 1.5 | 0.1275 |
| 2.2 | 0.0862 |
| 2.6 | 0.0540 |

- **Spectral arm expectation:** `ρ_spec(kR)` within a factor **3** of `ρ_ref(kR)` at each swept point (band absorbs: srs anisotropy — direction-resolved `c_P/c_S` spread ≈ 11%, entering via shell deformation + l-mixing; finite-k eigenvector polarization pull; discrete-shell sampling), with the predicted DECREASING trend over the sweep reproduced. Discreteness correction order declared: O((k·ℓ_site)²) with `ℓ_site = √2/4 ≈ 0.354` cell → ≈ 2–9% over the sweep.
- **Time-domain expectation:** `R_{c/a} = E_P^{comm}/E_P^{radAC}` within a factor **3** of `ρ_ref(2.6) = 0.0540`; `R_{c/s} = E_P^{comm}/E_P^{static} ≫ 10`; radiated spectrum for run (b) peaked at `2Ω = ω_d` (FFT-bin resolution), content at `Ω` < 10% of the `2Ω` peak.
- **Frequency-doubling receipt:** the pattern rotates at Ω; the radiation arrives at 2Ω — the m = ±2 commutation signature (drive-frequency ≠ radiation-frequency is the clean mechanism fingerprint).
- **Post-burst ledger:** free-evolution `|ΔH/H| ≤ 2×10⁻²` (bounded-Verlet precedent ×2 margin for the larger grid).
- **Eccentricity arm (M1-S5 / 2C):** numeric orbit averages match `⟨(⃛tr M)²⟩/⟨⃛M^{TL}:⃛M^{TL}⟩_{e=0} = 2e²·[1+O(e²)]/32` within 5% at `e ≤ 0.1`; `f_TL(e)` matches `f_PM(e)` within 1% at every swept `e` (the same-invariant identification VERIFIED, not assumed); reference evaluations frozen: `f_PM(0.088) = 1.052`, `f_PM(0.6171) = 11.86`; trace-channel flux add-on `(5/96)e²` = `4.0×10⁻⁴` at `e_DP = 0.088` and (leading-order only, O(e⁴) large here) `1.98×10⁻²` at `e_HT = 0.6171`.
- **Discreteness pre-declaration (Step 3.9-3):** physical = the O((kℓ)²) dispersion/eigenvector corrections and the anisotropic shell (both derived, both bounded above); integrator artifact = post-burst drift (ledger-gated), boundary reflections (window-excluded), Gaussian-burst spectral leakage (declared: burst bandwidth `Δω/ω_d ≈ 1/(3·2π) ≈ 5%`; the G-FREQ Ω-vs-2Ω separation is 2× — 40σ of the burst bandwidth — unaffected).

## §9 — Frozen bins + fireability (Step 3.10)

- **OVERLAP-NONZERO(n=2)** fires iff ALL of: (i) `R_{c/s} ≥ 10`; (ii) G-FREQ passes (2Ω peak, Ω-content < 10%); (iii) G-SCALE passes (`ρ_spec` within ×3 of `ρ_ref` at ≥ 3 of 4 swept kR AND decreasing trend reproduced); (iv) G-XMETHOD passes (`R_{c/a}` within ×2 of `ρ_spec(2.6)`). The order label n = 2 = the coupling enters through the l = 2 channel exclusively (the pattern is l=2-pure by construction; its coupling survives; consistent with the #919 order result — this lane does NOT re-derive the source-region multipole ladder, it inherits it, M1-S1).
- **OVERLAP-ZERO** fires iff ALL of: (i) `ρ_spec < 10⁻⁶` at EVERY swept kR (machine-floor class, ≥ 4 decades below prediction); (ii) `R_{c/s} ≤ 2`; (iii) Method 1 derives the projecting structure by name (a selection rule, an orthogonality theorem, a Green-function property — not an assertion). A numerical zero WITHOUT the named derived structure is NOT-ADJUDICATED(artifact-hunt), not OVERLAP-ZERO.
- **NOT-DERIVABLE** fires iff M1-S3/D5 finds the port-surface pose itself requires an unreceipted import beyond the ABSENT amplitude normalization — deliverable then = the enumerated list of exactly what a pluck derivation must supply for the integral to close.
- **Any other outcome combination → NOT-ADJUDICATED(named artifact class enumerated)** — a diagnostic, not a verdict; no bin is force-fitted.

**Fireability declaration (honest verbs, frozen):** OVERLAP-NONZERO carries a STRONG DECLARED PRIOR (the continuum incumbent + the Rayleigh-expansion structure); it is NOT flat. The genuinely ADJUDICATED content of this lane is the exact-zero hunt (M1-S3: each D-register candidate could in principle have produced a projection-out — D1 in particular is a live corpus temptation the R28 walk already brushed) and the FIRST lattice-native construction of the coupling object. If NONZERO fires with the analytic match, the numerical arms partially DEMONSTRATE continuum-limit convergence (entailed by M1-S2 once no exact zero exists) — the result doc uses DEMONSTRATED for that leg and ADJUDICATED for the D-register + the divergence-step verdict. OVERLAP-ZERO is FIREABLE (a derived selection rule would fire it). NOT-DERIVABLE is FIREABLE (D5 is a genuine open pose question — the corpus has never posed a port-surface BC class before).

## §10 — Gates (frozen; UNRUN ≠ PASSED; every gate scored in the result)

| Gate | Criterion (frozen) |
|---|---|
| **G-CONFLATE** | The M1-S0 two-groups bookkeeping is written out (port-space vs space-group; `l=2 → E ⊕ T₂` under 432 with dimension check) BEFORE any verdict statement; no step cites `clm-j550uh` for a spatial-angular claim |
| **G-POSCTRL** | `E_P^{radAC} ≥ 10 × E_P^{static}` (the known-open l=0 channel reads through the identical pipeline — #761 Leg-C liveness inherited) |
| **G-SPEC** | Per-direction `c_P/c_S` at {100, 110, 111} reproduces the survey `[1.7105, 1.8528, 1.9041]` to < 3% (the #761 frozen direction-resolved gate, re-run on this lane's build) |
| **G-WINDOW** | The measurement window is the analytic spectral form `[t_arr, t_reflect)` at C-2 spectral speeds; NO front-detect anywhere (the #761 R2 lesson, frozen) |
| **G-MOMENT** | The discrete port-shell pattern moments: l=2 Gram matrix over {Y_00, Y_1m, Y_2m} on shell sites has condition number < 50 AND the applied pattern's own discrete l=2 moment is nonzero at > 0.9 of its continuum norm (D3 artifact guard) |
| **G-SCALE** | `ρ_spec` within ×3 of `ρ_ref` at ≥ 3 of 4 swept kR; decreasing trend reproduced |
| **G-FREQ** | Run-(b) radiated spectrum at `r_meas`: peak at `2Ω` within FFT resolution; `Ω`-content < 10% of the `2Ω` peak |
| **G-XMETHOD** | `R_{c/a}` (time domain) within ×2 of `ρ_spec(2.6)` (spectral) |
| **G-DRIFT** | Post-burst free-evolution `|ΔH/H| ≤ 2×10⁻²` |
| **G-ECC** | Numeric orbit averages: trace-channel ratio within 5% of `2e²/32·[1+O(e²)]` at `e ≤ 0.1`; `f_TL(e) = f_PM(e)` within 1% at all swept `e` |
| **G-NUM** | Every verdict-bearing numeral in the result re-derivable from the shipped JSON or a stated closed form; `overlap_integral_lattice_number_check.py` green including `--mutation-receipt`; `make verify` green in the worktree |

**Gate-floor consistency (Step 3.7b):** the only cells this prereg predicts noise-limited are the static-control energy (the floor itself — never read as a physics value, only as a denominator with the ≥10× separation requirement) and sub-percent transverse attributions (NOT read at all — the #761 §4.3 drift-floor lesson inherited; no gate consumes them).

## §11 — Fences (declared; each checked in the result)

1. **Rotational fence-reach STOP** (brief): no derivation step may route the outbound coupling through the Cosserat micro-rotation channel; the drive is displacement-sector radial force by construction; if M1 finds otherwise → STOP, route Grant.
2. **Stencil lens** (brief; structural-null discipline): Method 1 (derivation) ADJUDICATES; Method 2 (engine) CORROBORATES. A Method-2 null with a Method-1 nonzero (or vice versa) is a contradiction to surface, never to average. The Method-2 operator stencil is the receipted substrate-native rank-2 bond tensor on the srs net (NOT a Cartesian Laplacian) — the #761 sector-header discipline inherited.
3. **Kernel-member fence** (brief: "declare cold-linear kernel-insensitivity"): declared at §0 row 2 — all computations at `S_sat → 1` where every aggregation-rule member coincides; no step consumes the open combine-rule fork.
4. **Sector-ownership:** A1 owns the bias content; the spatial second moment of A1-scalar content is A1-sector with angular structure — the D2 crossing is adjudicated, not assumed either way.
5. **Regime declaration:** a null in a regime where the effect cannot exist is ARTIFACT-class (e.g., a drive above the P-branch band edge, a window excluding the arrival) — every null classified before binning.
6. **Frozen-criteria discipline:** after this push, no gate, tolerance, bin boundary, frozen parameter, or verdict wording of §§1–11 may change. Forced deviations (if any) are disclosed in a dated result-doc section with direction-of-effect stated (the #761 §4.0 pattern).

## §12 — Routing

The verdict + the eccentricity-scaling statement + the incumbent engagement table route to the orchestrator (the sourcing satellite's parent); #913/#919/arc-scope/#770 and the R28 row set are HELD on this lane plus the audit lane. The PR opens `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. This lane runs its own Tier-2 adversarial review (ave-adversarial-pr-review pattern) before any CLEARED wording. No KB leaf, register, ledger, ruling, or solidity edit on ANY outcome; FLAG-LC1-B and the port-register Q1 row are read-only inputs. The eccentricity statement is the bankable forward-prediction shape and is delivered in both verdict polarities (§4 comparator-role paragraph).

---

> **Freeze provenance.** SVA v0.2-pilot §0 filled, all 11 rows. Frozen criteria verbatim or file:line throughout; the incumbent travels verbatim (§3) with the brief's formatting delta disclosed. Comparators re-retrieved from source THIS session before any lane number exists (§4). Corpus state grounded in two background corpus-grep sweeps + first-party verification at base `051887c4`. This document commits ALONE and pushes FIRST (freeze-by-push); the driver commits second; the result last. Skill stack fired at write time: ave-prereg (Steps 1–3.11), ave-analytical-tool-selection (§1 Coupling + §6 Mode + §7 Boundary + §9 Numerical classes; tools pulled: Op3/Op17, discrete dispersion + Bloch extractor, real-vs-reactive categorical table, j_l-family radial overlaps), verify-before-cite (quote byte-checks), substrate-native-check scheduled at driver-write time.
