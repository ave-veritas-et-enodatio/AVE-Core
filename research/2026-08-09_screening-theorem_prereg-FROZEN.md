# THE SCREENING THEOREM (R34's IOU) — PRE-REGISTRATION (FROZEN)

**Date:** 2026-08-09 · **Branch:** `lane/2026-08-09-screening-theorem` · **Base:** `origin/main` @ `a510ec2b`
**Lane brief:** [`_orchestration/2026-08-08_screening-theorem-brief.md`](../_orchestration/2026-08-08_screening-theorem-brief.md) (Grant GO, R34 — [`2026-08-08-ruling-r34-sealed.md`](../_orchestration/docket-entries/2026-08-08-ruling-r34-sealed.md)).
**Class:** DERIVATION lane (analytic, with numeric two-method receipts). **Mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; edits no KB leaf, register, ledger, or ruling; changes no solidity; all propagation ROUTED.** Engine `src/ave` byte-untouched (read-only imports where a receipt needs a constant).
**This document commits ALONE and pushes FIRST (freeze-by-push), before any derivation content, driver code, or lane-produced number exists on this branch.**

---

## §0 — Standard Vacuum Analysis header (SVA v0.2-pilot; all 11 rows)

 1. **SECTOR / OWNERSHIP:** Source under test = A1 dilatation (compression) content of a compact object's interior — the "interior hum" (`master-equation.md:20` banner; TWO-"3"s guard active: A1 dilatation-MASS ⊥ Cosserat (2,3) WINDING-charge, never cross-wire). Channel under test = the longitudinal compression line (mechanical P-branch, gapless, A1-carried). Observed GW = T2 transverse shear radiating at the BANKED Peters–Mathews rate (canon's consistency-import, #919 R1) — NOT re-derived here, and any seal argument that would silence the OBSERVED shear rate proves too much (coherence gate G-SHEAR-COHERENT). The wall's mirrored family = the strain-kernel channels (G, K, G_c) per the v2 ruled text (§3 below); the Cosserat micro-rotation channel is carved OUT (member-fenced). **Rotational fence-reach STOP (brief `:41`): if any derivation step routes seal-escape or residue flux through the Cosserat micro-rotation channel, STOP and route to Grant.**
 2. **REGIME / PHASE-STATE:** Exterior graded region `r > r_sat`: Regime-I cold crystalline, sub-yield, lossless-reactive, with the static constitutive grade `A(r) = r_sat/r`, `A = 1` exactly at `r_sat` (canon form; approach-leak v2 REGIME header + `saturating-modulus-and-backreaction.md`). **INTERIOR `r < r_sat` = PAST-WALL PHASE STATE, declared here and at every point of use: the crystal-phase laws do NOT extrapolate inside (Ax1 phase-scope; brief `:39-40`). Every derivation step in this lane lives on the cold side and at the boundary limit (the last cold bond); NO step evaluates any constitutive law past the wall.** Far field: radiation is a legal Ax3 port. **Member fence (declared here, restated at every seal-consuming step):** the v2 ruled text's carve-out is stated on the per-grade (L∞-across-grades) combine member; the cross-grade combine rule is OPEN in canon (`axiom-register.md:190`, `:232`; FLAG-COMBINE-SPLIT vs `:189`). The SEAL itself rides the STRAIN-kernel collapse (G, K, G_c), which holds on BOTH members — but any step that additionally consumes the rotational carve-out inherits the fence and must say so.
 3. **CIRCUIT STATEMENT:** R34's carve of record: sealed ≠ lossless. Lossless is a property of the MEDIUM (Ax3, holds everywhere). Sealed is a property of the SOURCE COUPLING — *"the mass presents zero radiation resistance to the compression line — Re(Z_source→bulk) = 0 by structure"* (R34 `:21-22`). The candidate forcing structure: the strain-kernel transport couplings disconnect at the last bond (total reflection, exact, ρ-profile-independent); a compact object's deep-saturated interior sits behind its own wall; the interior compression hum meets that ruled mirror at the skin. The lane's circuit question, posed honestly: the wall is not a static termination — it ORBITS. The compression line therefore sees (i) interior-generated content behind a series disconnection (the seal candidate), (ii) a MOVING boundary condition (the orbiting wall as a piston — a moving Γ-mirror pumps a line; this term must be derived or bounded, not ignored: gate G-PISTON), and (iii) the exterior sub-yield graded dress (the `∇·u = 0` Lamé exterior, receipted at #919 §3.3), whose l=2 texture couples to the propagating common mode per #927's overlap result (consumed, not re-derived). The RESIDUE = (ii)+(iii)'s honest flux — the graded matching section's leakage.
 4. **PLANE & PROJECTION:** The seal's Γ statement lives at the LAST-BOND plane, exactly as the ruled text computes it (*"phase computed from the branch-derived row at the declared plane"*); wall-taxonomy §10 governs every signed Γ (plane + projection + profile declared; a quarter-wave of skin inverts short↔open; Ax3 forces only |Γ| = 1). This lane's load-bearing seal quantity is |Γ| = 1 (zero transmitted power = zero radiation resistance presented to the line) — SIGN-INDEPENDENT, so the §10 sign-relativity does not gate the seal (it gates only signed-phase statements, none of which this lane needs). The residue lives in the radiation-zone far field (the #927 plane).
 5. **CONSTITUTIVE PROVENANCE:** `r_sat = 7GM/c² = 3.5 r_s` — GR-imported /7 chain (`ave-bh-horizon-area-theorem.md:33`; owning claim `clm-law1ho`, solidity 0.55, build-band `input-only` — consumed as input only). `A(r) = r_sat/r` — canon vacuum grade (approach-leak v2 header; `saturating-modulus-and-backreaction.md`). The `∇·u = 0` deviatoric Lamé exterior — receipted (#919 §3.3, RESTORED leg). Coupling structure `O ∝ j₂'(kR)·Y_2m(k̂)`, class ratio `ρ(kR) = |j₂'(kR)/j₀'(kR)|²`, eccentricity shape `f_PM(e) + (5/96)e²` — #927 (MERGED, Tier-2'd), CONSUMED. The floor family `(2/3)(c_S/c_P)⁵` and the uncaged bracket `[0.0152, 0.0455]` — #919 (branch state, OPEN PR), anchor premise CONTESTED-BY-GRANT (R24) — cited as the incumbent structure this lane gates, never as an input to this lane's own numbers. The Branch-X exterior-texture bracket `~[3×10⁻⁴, 5×10⁻³]` — SUPERSEDED-INPUT per R34 `:40-41` (*"the prior fork-live estimate … is superseded-input only"*): this lane must DERIVE the residue, and the bracket is quoted only as the estimate being replaced. The coupling AMPLITUDE — NOT-DERIVABLE (#919 R23, inherited): wherever an absolute amplitude would enter, this lane either works in flux RATIOS anchored to the observed shear channel (anchor tagged as canon's consistency-import, R1) or reports the dependence explicitly.
 6. **ENERGY LEDGER:** A sealed source is reactive-only toward the compression line: interior content stores-and-returns at the mirror; no boundary-crossing arrow. The legal ports: (a) the T2 shear channel at the banked PM rate (canon's import — not this lane's object); (b) the compression-line residue (the object of Deliverable 2). The comparator `Ṗ_b` reads the TOTAL source energy budget (frozen at the #919 prereg SVA row 3, inherited via #927 §0 row 3) — so ANY additional radiative flux beyond the GR shear budget competes with δ_HT / δ_DP. No loss word without its port named.
 7. **CALIBRATABILITY:** The verdict-bearing number is dimensionless: `F_res ≡ F_bulk^{residue}/F_shear^{PM}` — the residue flux as a fraction of the banked shear flux — compared against `δ_DP = 1.3×10⁻⁴` (95%) and `δ_HT = 0.0016` (1σ). The unknown pluck normalization cancels in `F_res` wherever both channels are sourced by the SAME moving configuration; any step where it does not cancel is flagged in place with its dependence stated. Survival line, frozen verbatim (R34 `:41`): *"the arm survives observationally ONLY if the derived residue < 1× DP."*
 8. **DISCRIMINATION CLASS:** DC→AC coupling (a DC bias configuration — each star's static dress + static wall — read out through the AC radiative channel at Ω/2Ω). This is a SCREENING derivation gating a frozen kill, not a chord claim: no AVE-distinct positive is asserted on any outcome; the deliverable is the truth-status of the LC-1 kill's sourcing premise. Tautology filter: the three verdict bins are OPPOSITE fireable outcomes (seal holds + residue small / seal holds + residue big / seal fails); the derivation adjudicates. SM counterfactual: GR has no compression line at all — the question only exists substrate-side; the comparators are shared ground (AC), the contested content is the DC-side sourcing.
 9. **CERTIFICATION PLAN:** gate battery frozen in §7 before any derivation content exists (G-QUOTE, G-SEAL-2M, G-PISTON, G-SHEAR-COHERENT, G-DIV, G-RESIDUE-2M, G-COMP, G-NSWALL, G-MEMBER, G-FLAGW-3, G-NUM). UNRUN ≠ PASSED; every gate is run and scored in the result.
10. **ADJUDICATION ROUTING:** Verdict (one of the three frozen bins, §2) + the residue number + the FLAG-W disposition + the NS-wall answer route to the orchestrator. #913, #919, the LC-1 arc scope, and #770 are HELD on this lane (R34 dispositions). NO leaf, register, ledger, or ruling edit on ANY outcome; FLAG-W repair text, if it lands, lands as a ROUTED proposal in the result doc, not as a leaf edit. PR opens `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; the lane runs its own Tier-2 (ave-adversarial-pr-review) before anything reads CLEARED.
11. **NUMERICAL CONDITIONING:** Named engines: `sympy`/`mpmath` for closed forms; `numpy`/`scipy` quadrature for the numeric arm; `scipy.special.spherical_jn` for the consumed #927 forms; constants from `ave.core.constants` (read-only) + CODATA/IAU (G, c, M_sun) with sources named. No RNG in any verdict-bearing path. Named regex engines for absence claims (GNU grep -E + Python re; the `**`-pathspec and `$..$`-escaping false-negative classes guarded — every "0 hits / complete" claim cross-checked by a second method). Driver + gating number check with a mutation receipt, wired into `make verify`.

---

## §1 — The frozen deliverables (verbatim from the brief; the brief is the frozen source)

`_orchestration/2026-08-08_screening-theorem-brief.md:12-26`, verbatim `[sic]` (section heading at `:10`):

> 1. **THE SEAL:** from the channel-scoped kernel-collapse record (quote its ruled text
>    verbatim — v2 + the #907 correction fragment's C1/C2), derive the wall's action on
>    INTERIOR-generated compression content at the source skin: does Γ_LB = −1 apply to
>    the outbound l=2 interior hum, and with what reach (the wall-depth dependence,
>    DERIVED)? The member fence applies — declare the combine-member conditionality.
> 2. **THE RESIDUE:** the exterior sub-yield dress carries l=2 texture that couples per
>    #927 (consume its overlap result; do not re-derive). Derive the residue flux
>    honestly — the graded matching section's leakage — and state it against BOTH
>    frozen comparators (re-retrieve at freeze: HT 0.9983±0.0016; DP 1.3×10⁻⁴ — the
>    #919 prereg's sources). Survival needs residue < 1× DP; state the verdict number.
> 3. **FLAG-W RECONCILIATION:** the vent-vs-dead-end contradiction
>    (`research/2026-08-03_coldq-polar-family_result.md:170` + `:19`; three canonical
>    leaves, two answers) — enumerate the three leaves, reconcile against the seal
>    (dead-end/mirror), and land the repair or report why it cannot land. The seal
>    ruling FALLS if the vent reading survives reconciliation.

Plus the NS-wall honesty arm, `_orchestration/2026-08-08_screening-theorem-brief.md:43-46`, verbatim `[sic]`:

> NS-vs-BH honesty — the pulsar comparators are NEUTRON STARS: whether their cores
> actually sit behind an r_sat-class wall is itself a derivation obligation, not an
> assumption (if NS cores are sub-wall, the seal never engages for the comparators that
> matter — state this arm explicitly).

## §2 — The frozen verdict grammar (verbatim from the brief)

`_orchestration/2026-08-08_screening-theorem-brief.md:30-35`, verbatim `[sic]` (section heading at `:28`):

> - **SEAL-DERIVED + RESIDUE-CLEARS** — the mirror reaches the interior moment AND the
>   derived residue < 1× DP: the kill condition is un-sourced; LC-1 banks structural.
> - **SEAL-DERIVED + RESIDUE-EXCEEDS** — the mirror holds but the exterior texture
>   alone overshoots: the kill re-fires on the residue; walk-back protocol.
> - **SEAL-NOT-DERIVABLE** — the wall does not seal interior compression content (or
>   the member fence blocks it): the kill stands as derived; walk-back protocol.

**The gated kill, quoted at its frozen home** (the #913 R5 finding's authority: the ARC BRIEF's cell, not any dispatch paraphrase) — `_orchestration/2026-08-04_lorentz-compliance-arc-brief.md:44`, verbatim `[sic]` (kill-condition cell): *"An energy-carrying inter-event channel at ≠ c ⇒ arc-level kill"*; and `:59`, verbatim `[sic]`: *"LC-1 runs first and its kill condition is arc-terminating."* R34's gating disposition (`2026-08-08-ruling-r34-sealed.md:13-14`), verbatim `[sic]`: *"The frozen LC-1 kill condition is hereby **GATED on that lane's outcome**, not fired and not dismissed."*

## §3 — The seal's ruled source text (quoted verbatim at freeze; byte-checked, two engines — gate G-QUOTE)

**The v2 ruled text**, `_orchestration/docket-entries/2026-08-06-ruling-kernel-collapse-rescope-v2.md:13-29`, verbatim `[sic]`:

> **Kernel-collapse self-termination, channel-scoped (2026-08-05; member-fenced
> 2026-08-06):** at any strain-saturation wall, every transport coupling riding the strain
> kernel — G, K (both fork branches), and the u↔φ coupling G_c — disconnects at the last
> bond: total reflection, phase computed from the branch-derived row at the declared
> plane, density-profile-independent (receipts: PR #888, #890, both Tier-2-verified). The
> rotational channel's transport (γ·S_κ) is carved out: unwalled at r_sat, its own wall
> being a κ-amplitude surface. **This carve-out is stated on the per-grade
> (L∞-across-grades) combine member. The cross-grade combine rule is open in canon
> (`axiom-register.md:190`, `:232`; see FLAG-COMBINE-SPLIT for the `:189` conflict); under
> a normalized-L2-across-grades member the rotational transport would ride the shared
> kernel and collapse with it.** Shear→rotation conversion cannot occur at the wall (the
> door closes with the strain kernel) and is confined to the graded approach where G_c·S_ε
> is finite — the leak is one computable number (the approach-leak lane, computed: PR
> #903/#904). Regime IV stays fenced out for every mirrored channel. The certified
> shear-echo physics — the delay laws, BIN-DISC's fork discriminability — is unaffected;
> the print form of the echo claim is the two-branch certified-timing statement with the
> mirror scoped to strain-kernel channels.

**The #907 correction fragment's C1/C2** (`_orchestration/docket-entries/2026-08-06-ruling-kernel-collapse-rescope-v2-correction.md`), consumed as follows: **C1** — the v2 ruled text's full delta-from-v1 is three deltas, ALL declared (header stamp; the member-fence sentences; the approach-leak status-currency update) — this lane quotes the v2 form as the citable text with C1's declaration acknowledged. **C2** — the engine's saturation amplitude is coded THREE ways across two live functionals plus a separate objective; *"the member the engine actually codes"* is over-broad; the carve-out's engine receipt is **STRUCTURAL, not numerical** (γ·W_κ never rides an ε-dependent kernel in the saturated elastic sum). Consequence frozen here: **no step of this lane may cite an engine-numerical receipt for the carve-out**, and the member fence travels with every carve-out-consuming step (gate G-MEMBER).

**Member-fence conditionality, declared (Deliverable 1's requirement):** the SEAL's own basis — the strain-kernel collapse of G, K, G_c — is the part of the ruled text that holds on BOTH combine members (under normalized-L2 the rotational grade ALSO collapses; the strain channels collapse either way). The seal is therefore member-robust AT THE WALL. What is member-CONDITIONAL is the rotational channel's unwalled status (the carve-out) — relevant to this lane only as a potential BYPASS route (interior content converting to rotational transport and crossing unwalled). The ruled text itself closes that route AT the wall (*"Shear→rotation conversion cannot occur at the wall (the door closes with the strain kernel)"*) on the per-grade member; under normalized-L2 the rotational channel is itself walled, closing the route even harder. The bypass is therefore closed on BOTH members at the wall, and confined to the graded approach (the computed approach leak) — this reasoning is frozen here as the expected shape and must be re-derived, not assumed, in the result (gate G-MEMBER scores it).

## §4 — The frozen comparators (re-retrieved from source THIS session, at freeze, before any lane number exists)

| Comparator | Source sentence (re-retrieved 2026-08-09 via arXiv abstract fetch) | Frozen value |
|---|---|---|
| **Hulse-Taylor PSR B1913+16** | *"We find that the ratio of observed orbital period decrease due to gravitational wave damping (corrected by a kinematic term) to the general relativistic prediction, is 0.9983 pm 0.0016"* — Weisberg & Huang 2016 (arXiv:1606.02744, ApJ 829, 55). Masses (same abstract): pulsar `1.438 ± 0.001` M_sun, companion `1.390 ± 0.001` M_sun. | `δ_HT = 0.0016` (1σ fractional) |
| **Double pulsar PSR J0737-3039A/B** | *"validating GR's prediction at a level of 1.3 × 10⁻⁴ (95% conf.)"* — Kramer et al. 2021 (PRX 11, 041050; arXiv:2112.06795). Masses (paper body, IMPORT-tagged; not in abstract): `m_A = 1.338185` M_sun, `m_B = 1.248868` M_sun. | `δ_DP = 1.3×10⁻⁴` (95% conf. fractional bound) |

Both match the brief's frozen values (`:20`) and the #919 prereg §3 byte-for-byte on the quoted ratio sentences. Eccentricities (consumed via #927, frozen there): `e_HT = 0.6171`, `e_DP = 0.088`; #927's eccentricity consequence (*"circularity provides NO suppression"*, `F(e)/F(0) = f_PM(e) + (5/96)e²`) is consumed — the DP comparator is fully driven at `5.2%` from the circular limit, so no eccentricity escape exists in either direction.

**Rule 11 (no comparator tuning):** the comparators and the survival line are frozen above before any residue number exists; no tolerance, band, or bin boundary in this document may change after any derivation result is seen.

**NS-wall arm inputs, frozen:** `r_sat = 7GM/c²` per star from the masses above (`GM_sun/c² = 1.47662` km, IAU nominal). NS radii are an OBSERVATIONAL IMPORT, declared as such: NICER-class measurements give `R(≈1.4 M_sun) ≈ 11.4–13.7` km (95% bands; J0030+0451, J0740+6620 literature class). The arm's derivation obligation: the wall-existence condition from the canon vacuum grade `A(r) = r_sat/r` (wall exists iff the grade reaches `A = 1` outside the stellar surface, i.e. iff `R_NS < r_sat`), scored per comparator star with the radius-import bands stated — and the honest statement of what the answer rides on (the vacuum-grade formula's validity down to the surface, and the imported radii). No AVE-native NS EOS exists; none is invented here.

## §5 — Corpus state (ave-prereg Steps 1–2; two-method sweep receipts named in §7)

Prior art consumed (none re-derived): **#927** (overlap structure + eccentricity shape + comparator consequence — MERGED). **#919** (order result n=2; floor family; uncaged bracket `[0.0152, 0.0455]` = 9.5–28.5σ HT / 117–350× DP; Branch-X bracket `~[3×10⁻⁴, 5×10⁻³]` = ~2.5–40× DP, compactness-ESTIMATE provenance at its prereg §5, now SUPERSEDED-INPUT per R34; R23 amplitude NOT-DERIVABLE; R24 anchor CONTESTED — all cited as branch state, PR OPEN). **#913** (LC-1: A-COMPLIANT-AT-COMPARATOR / S2-KILL-INHERITED; the R5 finding = the kill cell's authority is the arc brief, quoted §2 above — branch state, PR OPEN). **Kernel-collapse arc** (#888/#890 receipts; #897 v1 ruling; #905 v2 ruling; #907 correction; #902 G-RHO2 re-run ROW-CERTIFIED). **Approach-leak** (#903/#904, LEAK-CERTIFIED — the graded-approach conversion template; its computed object is ε→κ conversion, NOT this lane's s→P conversion; cited as method-precedent only). **FLAG-W** (coldq-polar-family `:19`/`:170`; the three leaves = `bulk-impedance-at-saturation-boundary.md:31` [vent: `c_bulk → 0`, `Γ_bulk = −1`], `saturating-modulus-and-backreaction.md:57` [jam: `D = 1/S → ∞`, *"the modulus goes rigid"*], `engine-capability-map.md:69` [firewall: conflating stiffening/softening]). **Wall-taxonomy §10** (sign-relativity ruling 2026-08-04, Grant-ruled, incl. its *"FLAG-W dissolution"* paragraph: the two signed statements are plane/projection/profile-relative; §10.2 = the kernel-collapse block). **The vent leaf's foot declaration** (`bulk-impedance-at-saturation-boundary.md:97`: the signed value is profile-conditional, computed-not-chosen). Sweep note: the phrase "screening theorem" itself has NO prior derivation in the corpus (the R34 brief and ruling are its only homes) — this lane constructs it.

## §6 — Pre-registered expectations + discriminators (what I expect, why, what would flip it)

- **SEAL:** EXPECT SEAL-DERIVED at the last-bond plane: the strain-kernel disconnection is a property of the BOUNDARY derived on the cold side, so interior content (whatever the past-wall phase state does) has no strain-kernel transport element through the last bond — sealed without extrapolating any law inside. Member-robust per §3. **The live risk is NOT the static mirror — it is the MOVING wall (G-PISTON):** the orbiting wall is a moving boundary condition, and a moving mirror pumps a line. Discriminator: if the piston term is NOT suppressed relative to the dress term (below), the seal's reach over the radiating moment fails and the verdict heads to SEAL-DERIVED + RESIDUE-EXCEEDS or SEAL-NOT-DERIVABLE. Expected shape (to be derived, not assumed): the piston drive and the dress drive are the SAME object counted once — the moving star carries its entire exterior configuration (wall + dress) rigidly to leading quasi-static order, and the far-field compression drive is governed by the configuration's compression MOMENT, to which a `∇·u = 0` dress contributes only at mismatch order.
- **RESIDUE:** EXPECT the honest residue to come out BELOW the Branch-X estimate class, because the estimate scaled the l=2 moment by compactness (`~O(GM/Rc²)` per star ⇒ flux `~10⁻²–10⁻¹`) while the receipted dress is `∇·u = 0` — DIVERGENCE-FREE — so its DIRECT compression moment vanishes at quasi-static order and the flux enters only through (a) finite-(v/c_P) retardation mismatch and/or (b) graded-taper mode conversion (the K(A)/G(A) gradients near each wall coupling shear↔compression — the s→P analog of the computed approach leak). Both routes carry explicit small parameters; whether their product still exceeds `1.3×10⁻⁴` of the shear flux is GENUINELY OPEN — this is the verdict-bearing number. Discriminator: `F_res < 1.3×10⁻⁴` → RESIDUE-CLEARS; `≥` → RESIDUE-EXCEEDS. No intermediate bin exists; the number is stated either way.
- **FLAG-W:** EXPECT the reconciliation to LAND via wall-taxonomy §10: "vent" (`Z→0`, `Γ=−1`, pressure-release) and "jam" (`Z→∞`, `Γ=+1`, rigid) are the two SIGNS of the same `|Γ| = 1` total-reflection wall — the sign axis is plane/projection/profile-relative (Grant-ruled 2026-08-04), while the ENERGY statement (zero transmitted power = dead-end for flux) is sign-INVARIANT and is the only property the seal consumes. The "vent" reading survives reconciliation ONLY if some leaf asserts nonzero TRANSMITTED energy flux into the saturated region — expected answer: none does (`Γ_bulk = −1` at the vent leaf IS total reflection; a pressure-release boundary transmits nothing). Discriminator: if any of the three leaves (or any leaf found in the sweep) asserts through-wall energy transport on the compression line, the vent reading survives and the seal ruling FALLS (brief `:24-25`).
- **NS-WALL:** EXPECT wall-exists (marginally) for all four comparator stars: `r_sat` = 14.86 / 14.37 / 13.83 / 12.91 km (masses §4) vs imported NS radii `≈ 11.4–13.7` km — but the margins are sub-km for the lightest star (DP-B) and the answer flips within the radius-import bands. The arm reports the condition, the per-star margins, the import dependence, and the consequence honestly: if any comparator star is sub-wall (`R_NS > r_sat`), the seal never engages for that star and the verdict grammar's SEAL-DERIVED bins cannot bank against that comparator. NOTE the sub-wall case does NOT rescue the kill-clearance — it removes the seal, reverting that star toward the uncaged structure. The arm's answer feeds the verdict assembly explicitly.

## §7 — The frozen gate battery (UNRUN ≠ PASSED; every gate run and scored in the result)

| Gate | Frozen criterion |
|---|---|
| **G-QUOTE** | Every ruled/frozen quotation in the result byte-checks against its source file:line (two engines: `cmp`/`diff` on extracted spans + Python string equality); any delta enumerated C1-style or the quote fails |
| **G-SEAL-2M** | The seal statement derived by TWO independent routes agreeing: (a) the impedance/Γ route (series transport element → 0 at the last bond ⇒ `|Γ| = 1`, zero transmitted power, `Re(Z_source→bulk) = 0`); (b) the energy-flux route (the transmitted Poynting-analog flux through the wall bounded by the vanishing coupling, computed explicitly). Disagreement = SEAL-NOT-DERIVABLE |
| **G-PISTON** | The moving-boundary pumping term is explicitly derived or explicitly bounded with its small parameter named; the seal may NOT be scored without this term's disposition. A piston term that evades both derivation and bound = SEAL-NOT-DERIVABLE (honesty over convenience) |
| **G-SHEAR-COHERENT** | The seal argument, applied verbatim to the T2 shear channel (which the SAME ruled text mirrors), must NOT contradict the observed banked PM shear rate. The derivation must state WHERE the observed shear flux is sourced (exterior dress motion), and the same sourcing map must be applied to the compression channel consistently. A seal argument that silences observed GW = proves too much = FAILS |
| **G-DIV** | The `∇·u = 0` exterior receipt verified two-method: corpus grep to its receipted home(s) + an explicit re-derivation of the static graded exterior's divergence in the result (with the grade `A(r) = r_sat/r` carried, not the homogeneous-medium shortcut) |
| **G-RESIDUE-2M** | The residue computed by two methods: (a) analytic closed form (retardation-mismatch + taper-conversion channels, each with its small parameter derived); (b) an independent numeric evaluation (quadrature over the dress profile / driver script). Agreement within a declared factor (frozen: ×3, the #927 G-SCALE class) or the residue reports NOT-CERTIFIED and no CLEARS/EXCEEDS bin fires on the analytic number alone |
| **G-COMP** | Comparator arithmetic (`F_res` vs `δ_DP`, `δ_HT`; per-star `r_sat`) machine-checked by the gating number check + mutation receipt; `make verify` green |
| **G-NSWALL** | Per-star `r_sat` computed from the §4 frozen masses (constants: IAU `GM_sun/c²`, cross-checked vs `ave.core.constants` where present); wall condition `R_NS < r_sat` scored per star with the radius-import band; the flip-point radius stated per star |
| **G-MEMBER** | Every seal-consuming step carries its member-fence status inline; a grep receipt in the result shows ZERO unfenced carve-out consumptions (patterns: carve-out/unwalled/rotational-channel mentions each within N lines of a member-fence token; two regex engines) |
| **G-FLAGW-3** | All three FLAG-W leaves enumerated with verbatim quotes (byte-checked per G-QUOTE); the reconciliation scored against EACH leaf separately; the survival test (any through-wall ENERGY-transport assertion on the compression line, any leaf) run as a corpus sweep with named engines, not just against the three known leaves |
| **G-NUM** | Gating number check: every verdict-bearing numeral in the result doc (composite back-tick tokens INCLUDED — the #927 T5 lesson) machine-verified against the shipped JSON; mutation receipt targeting composite tokens; stale-literal guard; `make verify` green in the worktree at commit |

**Frozen bin arithmetic:** SEAL-DERIVED requires G-SEAL-2M PASS ∧ G-PISTON PASS ∧ G-SHEAR-COHERENT PASS ∧ G-MEMBER PASS. RESIDUE-CLEARS requires SEAL-DERIVED ∧ G-RESIDUE-2M PASS ∧ `F_res < 1.3×10⁻⁴`. RESIDUE-EXCEEDS requires SEAL-DERIVED ∧ G-RESIDUE-2M PASS ∧ `F_res ≥ 1.3×10⁻⁴`. Any seal-family gate FAIL → SEAL-NOT-DERIVABLE. G-RESIDUE-2M FAIL under SEAL-DERIVED → the residue number reports NOT-CERTIFIED and the lane verdict is SEAL-DERIVED with the residue bin UNADJUDICATED (stated plainly; no bin is inferred from an uncertified number). The FLAG-W arm can OVERRIDE: if the vent reading survives G-FLAGW-3's survival test, the verdict is SEAL-NOT-DERIVABLE regardless of the other gates (the brief's `:24-25` fall condition). The NS-wall arm does not change the verdict WORD but is reported inside it: a sub-wall comparator star is stated in the verdict sentence itself.

**No gate, tolerance, band, frozen numeric parameter, bin boundary or method element in this document may be changed after any derivation or gate result is seen. Deviations forced by instrument mechanics follow the #927 §2.0 pattern: disclosed FIRST in the result, each with direction-of-effect, frozen-literal variant reported beside the operative one.**

---

> **Freeze provenance.** SVA v0.2-pilot §0 filled, all 11 rows. Frozen criteria verbatim or file:line throughout; the kill cell quoted from the ARC BRIEF (the #913 R5 authority), not any dispatch gloss. Comparators re-retrieved from the #919 prereg's sources THIS session before any lane number exists (§4). Corpus state grounded at base `a510ec2b` with branch-state cites marked as branch-state. This document commits ALONE and pushes FIRST (freeze-by-push); any driver commits second; the result last. Skill stack fired at write time: ave-prereg (corpus inventory §5 + expectations §6), ave-worktree-paths (worktree-absolute discipline from first call), verify-before-cite (every quote byte-checked at write time; G-QUOTE re-checks at result time), ave-regime-phase-state-check (§0 row 2 past-wall declaration), ave-canonical-leaf-pull (boundary-class canon enumerated §5), ave-discrimination-check (§0 row 8 — no chord claim on any outcome). Tier-2 adversarial review at adjudication before any CLEARED wording; PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.
