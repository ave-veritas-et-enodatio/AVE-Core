# Envelope-Sector Reduction — RESULT (the frozen-bin verdict; slow dynamics of the saturable lattice; the gravitational-band coupling κ)

**Date:** 2026-07-20
**Class:** DERIVATION + lattice-derived research-driver (research-doc; **forms derived, values calibration/observation-imported and tagged; mints no `clm-`, propagates to no KB/tex leaf**). Resolves the frozen bins of the pre-registration (`research/2026-07-20_envelope-sector-reduction_prereg-FROZEN.md`).
**Provenance:** Grant-fired 2026-07-20 (`"fire the lane"` `[sic]`). The forward derivation following the #761 NONE-DERIVES carrier-level verdict (`[branch:#761 @ d17a2248]`). Frozen prereg committed + pushed ALONE first (`e27cc5e0`); analytic Legs 0/A/B in `research/2026-07-20_envelope-sector-reduction_derivation.md` (`b25fb9d5`); Leg-C driver `research/drivers/envelope_sector_orbiting_lump.py` (+ `_results.json`, white figure), `ave.core.*` read-only, engine BYTE-UNTOUCHED.
**Lane fences:** DERIVATION lane only. Engine byte-untouched; **no** `manuscript/`/`ave-kb/` leaf edits; **no** port-register edit; **no** un-revert; **no** falsification-ledger edit — regardless of outcome (held). Every `[canon]` input content-verified two-method at base HEAD `f84b622b`; `[branch @ d17a2248]` for #761; pulsar figures `[import]`.

> **★FROZEN-BIN VERDICT: BIN-1-CONDITIONAL — ENVELOPE-RADIATES on the shipped analysis (the pulsar kill is confirmed one level down), mechanism NAMED; but "BIN 2 unreachable" is CONDITIONAL on constituent-wall Γ-symmetry and ensemble aggregation — the constituent knot-core fork is OPEN.** The mass = the ENVELOPE of the A1 breather; the envelope reframe does NOT rescue Reading B at the two levels the shipped legs adjudicate (star-shell and smooth common-source). But the review found that canon puts a BULK-ONLY `Γ_bulk=−1` mirror at EVERY constituent knot core (`electron-bh-isomorphism.md:26` `[canon]`, verbatim: *"The electron's confinement is **bulk-channel** TIR (`Z_bulk → 0 ⇒ Γ_bulk = −1` at the knot core)"*) — a channel-ASYMMETRIC structure the doc's §3.1 refutations never adjudicated (they rebut a star-level shell where `Γ_shear=Γ_bulk=−1` is symmetric). So the "structurally unreachable" claim is DEMOTED: whether an ensemble of ~`10⁵⁷` bulk-only-caged constituents presents the live BIN-2 route (image-cancelled core moments) or the BIN-1 uncaged mass texture is an OPEN fork, unadjudicated both ways and potentially verdict-flipping (§3.1, §6.2 owed driver). BIN 1 remains the direction on the conservative branches (`κ_env²/κ_max² = 262×`, `114×` at the honest coupling floor; Legs A/B and the far-field-cold structure carry it independent of the fork). The frozen criteria's outputs:
>
> - **`κ_max² = δ_DP = 1.3×10⁻⁴`, `κ_max = 0.0114`** (double-pulsar binding; §1 of the prereg, recomputed from the banked q1 §2.2 numbers).
> - **Far-field structural coupling `κ_env² = A_ang(c_S/c_P)⁵ = 0.034` (central), with q1's honest O(1) coupling bracket restored (review R3): `κ_env² ∈ [0.0148, 0.1768] ⇒ 114–1360×` the bound.** The `A_ang=2/3` equal-coupling (`κ_L/κ_T=1`) is the **q1-inherited ASSUMED symmetric-coupling normalization** (the #761 prereg's own "assumed symmetric-coupling grade-assignment"; q1 §1.3 brackets `κ_L/κ_T ∈ [0.5,2]²` ⇒ `F_bulk/F_shear ∈ [0.0148, 0.1768]`), NOT a coefficient computed here from the K4/Ax4 Lagrangian. It is NOT numerically circular (κ² is a flux RATIO from a common source tensor — the mass-moment normalization cancels between P and S). Leg A analytic = the q1 value, UNSHIFTED by the envelope reframe; Leg C finite-size-free spectral color-check reproduces `0.034` at the lattice `c_P/c_S = 1.813`. `κ_env²/κ_max² = 262` central — **the compression port is open at `114–1360×` the double-pulsar bound** (`≫1` across the entire O(1)-coupling bracket; `κ_env = 0.184 = 16× κ_max` at central).
> - **Mechanism (derived, two-part).** (i) *Gaplessness defeats the adiabatic hope (Leg A / Fork F1):* the orbital-band radiation is the mass-quadrupole ponderomotive coupling of `|A|²` into the **gapless** P-branch (#761 `clm-bnd5rq` 0.8); the scale separation `ε = Ω/ω₀ ~ 10⁻²⁴` adiabatically suppresses only the *carrier* band by FREQUENCY-SCALE separation (drive `≪` carrier), **NOT by a spectral gap** — `clm-bnd5rq` (0.80) states NO internal stop-band exists in EITHER channel (both manifolds connected `0→top`), so the `ω₀`-carrier is embedded/radiatively-lossy (consistent with the corpus electron-lock FILLS-BUT-DECAYS), which only ADDS leakage; the adiabatic protection keys on the small parameter `ε`, never on a gapped carrier, and never protects the gapless acoustic channel at `2Ω`. (ii) *`K=2G` grade-locking forbids a MEDIUM-class channel asymmetry (Legs 0/B / Fork F2):* the mechanical bulk and shear channels grade together under `S(A)` (`Γ_shear=Γ_bulk=−1` at the STAR melt wall, `lattice-extreme-bh-rationality.md:37` `[canon]`) and the `c_P/c_S` RATIO is saturation-invariant (degree-0, `electron-bh-isomorphism.md:38`/PR#521 `[canon]`), so every MEDIUM-class candidate suppression is channel-symmetric ⇒ silencing compression silences the observed GW too — the CONSTITUENT bulk-only wall (`:26`) is the open exception (§3.1). (iii) *`mass=A1-dilatation` fixes the multipole order (Fork F3):* the compression monopole (`Ṁ=0`) and dipole (`Ẍ_cm=0`) are killed, so compression starts at quadrupole — SAME order as shear — with `Δℓ=0` (no `(Ω/ω_ref)^{2Δℓ}` suppression).
> - **★CONSISTENCY GATE: PASS at CONSISTENCY-IMPORT grade.** *Form derived* (the T2-shear channel is gapless and couples at quadrupole order); *rate inherited* — "at the GR rate" rides the corpus's GR-imported Peters-Mathews / HT chain (`08_gravitational_waves.tex` `[canon]`, `K=2G` PR#261), NOT re-derived within the frozen `δ` band (no within-`δ` shear-flux reproduction is derivable from an `L=20` near-zone lattice; the gate never had a *derived* suppression candidate to fire on). Within the MEDIUM-class mechanical sector, every enumerated suppression is channel-symmetric (adiabatic gap / emergent `Γ=−1` shell / `√S` softening all grade BOTH channels), so it would suppress shear too (→ BIN 3, forbidden). This forecloses the star-shell and smooth-common-source BIN-2 routes. It does NOT foreclose the constituent knot-core route: canon's constituent wall is BULK-ONLY `Γ_bulk=−1` (`electron-bh-isomorphism.md:26`), channel-ASYMMETRIC — so "BIN 2 unreachable" is CONDITIONAL on constituent-wall Γ-symmetry (see §3.1).
> - **Leg C empirical anchor: WEAK-TEXTURE invariance (re-graded down from "saturation-invariance" per review R2/F4).** An orbiting `A₀=0.5·A_yield` mass-texture radiates compression to `<0.2%` of the cold case (`f_long = 0.762` cold / `0.762` sat at `Ω=0.15`; `0.628`/`0.629` at `Ω=0.30`; `κ²` ratio `0.997`). **★Scope honesty:** the nominal deep saturation (texture peak `A/A_yield=0.487`, `S=0.874`) lives ONLY on the kinematically-CLAMPED core-core bonds (`n=504`, dynamically inert — their stiffness never feeds back into the free dynamics); the FREE field sees `S ≥ 0.987` (`≤2%` stiffness modulation on any bond touching a free site). So this null probes **weak-texture invariance only** — it has NO power against a depth-keyed suppression (`Γ=−1` engages at `S→0`; the free field never leaves `S≈0.99`) or a source-side saturated-structure suppression (excluded by the shared kinematic clamp). It confirms Leg 0's degree-0 ratio in the weak regime; it is NOT the "decisive, finite-size-INDEPENDENT" deep-saturation result the shipped headline claimed. A genuine deep-saturation test would need the free field to reach `S→0` — i.e. a self-consistent (not clamped) saturated texture whose live bonds carry the yield-wall softening, which the electron-lock arc declared INFEASIBLE on the lossless engine.
>
> **CONSEQUENCE (unchanged from #761, one level deeper, on the shipped branches): the standing Reading-A exclusion + the reverted Q1 ruling STAND on this analysis.** The envelope framing is NOT falsified (shear survives; NOT BIN 3), and no derived suppression exists at the two levels the shipped legs adjudicate — the star-level saturated shell and the smooth common uncaged source (NOT BIN 2 there). This is a bankable NEGATIVE (Rule 11) **on those branches**, but NOT the full "last structural place a suppression could live" closure the shipped headline claimed: the review surfaced an unadjudicated CONSTITUENT knot-core route (canon's bulk-only `Γ_bulk=−1` mirror, `electron-bh-isomorphism.md:26`), so the verdict is BIN-1-CONDITIONAL and the constituent-cage-ensemble question is an owed follow-on (§6.2). **Routed to Grant/auditor; NOT executed here** (READ-ONLY fence held; verdict grade demoted, bin NOT flipped — the fork is unadjudicated both ways).

---

## §0 — REGIME / SECTOR / PHASE-STATE header (fired before the integration)

**MODE.** Non-relativistic compact binary (HT B1913+16, J0737-3039A/B) as a source; contrast = observed `Ṗ_b` matched to GR shear-quadrupole `[import]`. **REGIME.** Regime-I cold-linear far field; SATURATED source (Op14 ON in the lump; `A₀=0.5·A_yield`). **PHASE-STATE.** Cold-reactive far field, saturated core. **SECTOR.** Under test = A1 bulk/compression (P-branch, `c_P/c_S≈1.71–1.90`); observed GW = T2 shear. **A46 coordinate:** longitudinal (radial, `∇·u`) vs transverse (tangential, `∇×u`) radiated-energy partition; `κ² = F_bulk/F_shear` in that channel basis — matching the corpus channel-radiation claim, not a `φ²` proxy.

---

## §1 — LEG 0 (sign check) — the compression channel grades WITH shear, ratio frozen `[canon-read + derived]`

Full derivation in `..._derivation.md §1`. Outputs: `dc_P/dA < 0` (softens, index up, converging region); `Z_bulk = ρc_P ∝ √S → 0` ⇒ `Γ_bulk → −1`, **together** with `Γ_shear → −1` (`lattice-extreme-bh-rationality.md:37` `[canon]`, verbatim *"`Γ=−1` reflection is **shear + bulk** (`G_shear→0`, `c_bulk→0`, `Γ_shear=Γ_bulk=−1`) while EM stays matched"*). The **dimensionless `c_P/c_S` ratio is FROZEN** under saturation magnitude (`electron-bh-isomorphism.md:38`/PR#521 `[canon]`: `ν`, Zener, `K/G` are *"homogeneous degree-0 in the bond stiffnesses … unshifted by saturation magnitude"*). ⇒ the compression/shear partition `κ² = A_ang(c_S/c_P)⁵` does NOT shift with saturation depth. This pre-resolves Fork F2 (mechanical channels grade-locked ⇒ symmetric) and Fork F3-ratio (saturation-invariant), and it is EMPIRICALLY confirmed by Leg C (§4, cold≈saturated).

---

## §2 — LEG A (multiple-scales) — the orbital band couples to the gapless P-branch at quadrupole order `[derived]`

Full derivation in `..._derivation.md §2`. Outputs:
- The carrier envelope obeys an **NLS-class** (parabolic/Schrödinger) equation about the cold vacuum — the carrier band sits at `ω₀ ± nΩ` (node scale), gapped, adiabatically suppressed by `ε = Ω/ω₀ ~ 10⁻²⁴`. **The envelope's own band offers no new low-frequency radiative channel.**
- The far-field radiation at `2Ω` is the **ponderomotive back-reaction** of the carrier intensity `|A|²` (= the mass-energy density, `master-equation.md:20` `[canon]`) onto the low-frequency acoustic field, sourced by the mass quadrupole `Q_ij ∝ ∫x_ix_j|A|²`. **Fork F1 resolved:** whether `|A|²` is a *source* (RHS) or a *coefficient* (the moving well the envelope rides), the far-field object is the same mass quadrupole at `2Ω`; the adiabatic suppression protects only the gapped carrier band, NOT the **gapless** P-branch (#761). *The #761 gaplessness result is exactly what defeats the envelope framing's suppression hope.*
- **Fork F3 resolved by `mass=A1-dilatation`:** compression monopole `∫∇·u ∝ M` (`Ṁ=0`, dead), dipole `∫x∇·u ∝ MX_cm` (`Ẍ_cm=0`, dead), quadrupole `∫x_ix_j∇·u = Q_ij` (`⃛Q^{TL}≠0`, radiates). Compression starts at quadrupole — SAME order as shear — `Δℓ=0`. ⇒ `κ_env² = A_ang(c_S/c_P)⁵ = (2/3)(c_S/c_P)⁵`, the q1 value, unshifted by the envelope reframe.

---

## §3 — LEG B (channel asymmetry) — SYMMETRIC on the medium sector; the gate forecloses the MEDIUM-class BIN-2 routes `[derived]`

Full derivation in `..._derivation.md §3`. Outputs: the observed GW is carrier-shear sourced by the mass quadrupole's traceless part at `2Ω` — a **gapless** acoustic branch ⇒ **radiates at the GR rate ⇒ consistency gate PASS for shear.** The derived structure is **SYMMETRIC on the medium sector**: the two mechanical channels are grade-locked (Leg 0), so every MEDIUM-class candidate suppression (adiabatic gap / emergent star-shell `Γ=−1` / `√S` softening) is channel-symmetric and CANNOT suppress compression without suppressing shear (→ BIN 3, forbidden by observation). The only medium-side asymmetric channel is EM (`Γ_EM=0`), which gravity does not use (sector-ownership). **This forecloses the MEDIUM-class BIN-2 routes (star-shell image, adiabatic gap).** It does NOT establish a universal negative: canon's CONSTITUENT knot-core wall is BULK-ONLY `Γ_bulk=−1` (`electron-bh-isomorphism.md:26`) — channel-ASYMMETRIC — so **"BIN 2 unreachable" is CONDITIONAL on constituent-wall Γ-symmetry and ensemble aggregation** (§3.1), NOT the flat "structurally BLOCKED" the shipped draft claimed.

### §3.1 — ★Adjudication of the walk's image-cancellation (Lloyd-mirror) BIN-2 candidate — PARTIAL; a constituent-level fork is left OPEN `[derived; review-corrected R1]`
The Grant-led envelope-boundary walk RECORD (`research/2026-07-20_envelope-boundary-walk_RECORD.md` §6-3, merged to `origin/main` #765 concurrent with this lane) names **image-cancellation** as the "candidate mechanism for the envelope lane's bin-2 route": a compression source against its own `Γ=−1` boundary has its long-wavelength radiative moment cancelled by its inverted image (Lloyd-mirror class), residuals suppressed by `(k·r_core)²`, while the static texture `∝ M` is untouched. Note the RECORD poses this at the CONSTITUENT/carrier level (`RECORD:46` "the mirror is made of the thing it confines"; `:54` "a fast internal CARRIER"). The shipped draft claimed to REFUTE it two ways; **the maximum-stakes review found BOTH refutations target a STAR-LEVEL shell and neither adjudicates the constituent knot-core mirror canon establishes.** Corrected below:

1. **A STAR-LEVEL saturated shell is channel-SYMMETRIC.** `lattice-extreme-bh-rationality.md:37` `[canon]` is the **BLACK-HOLE line** — `Γ_shear=Γ_bulk=−1` at the `r_sat` melt wall — NOT the electron/constituent line `:36`, which sits one line above it and reads `Γ=−1` is **bulk-channel TIR** only (`Z_bulk→0`). So this refutation's "channel-SYMMETRIC" premise is the BH melt wall; it holds for a star-level shell (IF one existed) but is FALSE at the constituent knot core (bulk-only, §-below). At the star shell, an image-cancellation would be symmetric ⇒ both cancelled (BIN 3) or both radiate (BIN 1).

2. **The actual pulsar sources are NEUTRON STARS: no STAR-LEVEL `Γ=−1` shell forms.** A star-level `Γ=−1` mirror requires a fully-saturated contour `A = A_yield` (`S=0`), which forms only at the BH saturation radius `r_sat = 7GM/c²` (`lattice-extreme-bh-rationality.md:77` `[canon]`). HT and J0737 are neutron-star binaries (`2GM/rc² ~ 0.35`, operating point `A₀ ~ 0.6–0.8 < 1`) — sub-yield, no star-level `S=0` contour. **★REVIEW CORRECTION (R1): this is NOT "decisive" and NOT "source-specific" — its "no `Γ=−1` mirror exists at the source" claim is contradicted by canon ONE paragraph above the line the draft quotes.** `electron-bh-isomorphism.md:26` `[canon]`, verbatim: *"The electron's confinement is **bulk-channel** TIR (`Z_{bulk} \to 0 \Rightarrow \Gamma_{bulk} = -1` at the knot core). The black hole's confinement at `r_{sat}` is **shear- and bulk-channel** phase transition."* Every electron/nucleon-class soliton carries its OWN `Γ_bulk=−1` mirror at its knot core, **independent of ambient star saturation** — so a sub-yield neutron star still contains ~`10⁵⁷` constituent-level bulk-only mirrors. Refutation 2 refuted the wrong level (star shell), and its "decisive" label is UNEARNED (canon-contradicting overclaim). Label stripped.

**★The honest residual fork (OPEN, potentially verdict-flipping).** The constituent knot-core mirror is BULK-ONLY (`electron-bh-isomorphism.md:26`/`:36`: electron = "bulk-channel" `Γ_bulk=−1`; BH = "shear- AND bulk-channel") — a channel-ASYMMETRIC structure, which is exactly the BIN-2 shape §3/§5 declared "structurally unreachable." At the constituent level, Lloyd cancellation would suppress each nucleon's compression moment `∝(k·r_core)²` while leaving shear at the GR rate. The fork the shipped legs never adjudicate: **does the ENSEMBLE of ~`10⁵⁷` bulk-only-caged constituents present to the `2Ω` far field (i) a sum of image-cancelled core moments (each `∝(k·r_core)²`, channel-asymmetric — the LIVE BIN-2 route), or (ii) the coarse-grained UNCAGED mass texture (radiates at the q1 partition — BIN-1 confirmed clean)?** Neither is derived; NO shipped leg tests it — Leg C drove an UN-caged, kinematically-pinned single lump (no self-consistent `Γ=−1` boundary), and the self-caged soliton was declared INFEASIBLE in the prereg fallback. The BIN-1 kill may still be independently true if aggregation washes out Compton-scale constituent caging (`k·r_core ~ 10⁻²⁵`, deep quasi-static), but that is an assertion, not a derivation. **Verdict grade demoted to BIN-1-CONDITIONAL; bin NOT flipped (the fork is unadjudicated both ways). The verdict-controlling adjudicator is the owed CONSTITUENT-CAGE-ENSEMBLE driver (§6.2).** Also flag A1⊥T2 sector-ownership: WHICH impedances collapse at a Cosserat-winding knot core (bulk-only? shear too?) is a cross-wiring question that needs adjudication, not assumption.

---

## §4 — LEG C (numerical, saturated regime) — the coefficient-coupling test

Driver: `research/drivers/envelope_sector_orbiting_lump.py` (+ `_results.json`, white figure). Engine byte-untouched (Rule-14 reuse of the #761/`srs_band_survey` rank-2 bond model + derived `ρ*=9.77337`). **FALLBACK scope (declared in prereg §3, held):** a self-bound saturated soliton is INFEASIBLE (electron-lock arc); this drives/pins a saturated dilatation texture in a dipole-free rotating BINARY, translated through the lattice — NOT envelope self-consistency. **★REVIEW SCOPE CORRECTION (R4a/F3):** the compression SOURCE is kinematically HAND-INSTALLED in BOTH the cold and the saturated cell (core sites within `r_core` overwritten to the imposed curl-free texture every step); `sat_on` ADDS a stiffness bias `S(A)` on top of the same imposed displacement. So both cells are SOURCE+COEFFICIENT, not pure-coefficient — a pure-coefficient cell (moving stiffness bias, NO imposed displacement) was never run. Leg C therefore empirically measures **saturation-invariance of an already-driven source**, NOT an adjudication of Fork F1 (source-vs-coefficient), which rests on the analytic Leg A alone.

### §4.1 — the three frozen measurements
- **Model S — BREATHING control (positive control).** A single fixed core with oscillating amplitude (the #761 source class). `f_long = 0.993` — reproduces #761's ≈98–99% longitudinal (the pipeline correctly sees a compression source). Control PASS.
- **Model C — ORBITING binary (the envelope test), cold AND saturated.** Two counter-placed dilatation textures rotating at `Ω` (quadrupole at `2Ω`, dipole-free). Measured at the `r_meas=6` shell:

  | drive `Ω` | `λ_P(2Ω)` (cells) | `f_long` cold | `f_long` sat | `κ²=F∥/F⊥` cold | `κ²` sat |
  |---|---|---|---|---|---|
  | `0.15` | `10.9` | `0.7624` | `0.7618` | `3.209` | `3.197` |
  | `0.30` | `5.4` | `0.6275` | `0.6288` | `1.684` | `1.694` |

  **★WEAK-TEXTURE invariance (re-graded from "decisive, finite-size-INDEPENDENT" per review R2/F4):** cold vs saturated agree to `<0.2%` at both `Ω` (`κ²` ratio `0.997`). **The re-grade is load-bearing:** the nominal deep saturation (texture peak `A/A_yield=0.487`, `S=0.874`) lives ONLY on the kinematically-clamped core-core bonds (`n=504`, dynamically inert — their stiffness never feeds back into the free integration); the FREE field sees `S ≥ 0.987` (`≤2%` stiffness modulation on any bond touching a free site). A same-source pair of runs differing by a `~1–2%` exterior-medium perturbation, measured on a shell that is `S≈0.99` in BOTH runs, is near-guaranteed to agree sub-percent. So this null confirms Leg 0's degree-0 ratio in the WEAK-texture regime only; it has **no power against a depth-keyed suppression** (`Γ=−1`/image engages at `S→0`, which the free field never approaches) or a source-side saturated-structure suppression (excluded by the shared kinematic clamp). It is NOT the decisive deep-saturation result the shipped headline billed.
- **Spectral C-2 (finite-size-FREE anchor; #761-parity).** From the cold Bloch `D(k)`: `c_P/c_S = 1.813` isotropic, direction-resolved `[100]1.711 / [110]1.855 / [111]1.904` — reproduces the survey (self-consistency PASS). The far-field structural partition (continuum multipole formula at the lattice speeds) `F_bulk/F_shear = (2/3)(c_S/c_P)⁵ = 0.034` — matches q1's `0.0329` and #761's `0.033`. With q1's O(1) equal-coupling bracket carried (review R3): `κ_env² ∈ [0.0148, 0.1768]`, i.e. `≫ κ_max² = 1.3×10⁻⁴` at `114–1360×` (central `262×`).

### §4.2 — finite-size honesty (declared fallback invoked)
The time-domain `r_meas=6` shell is in the **near/intermediate zone** for the orbiting quadrupole (`λ_P(2Ω) = 5.4–10.9` cells `≳ r_meas=6`), so the absolute `f_long`/`κ²` there is **compression-enriched by the moving dilatation-texture's own near field** (a pure-`∇·u` source is radially dominated in the near zone). This is confirmed by (i) the sub-quadrupole `Ω`-scaling exponents (`n_∥=0.86`, `n_⊥=1.79`; a clean far-field quadrupole would give `≈6`) and (ii) the **physical near→far trend**: as `Ω` doubles (`λ_P` shrinks below `r_meas`), `f_long` DECREASES (`0.762 → 0.628`), the transverse/shear content growing toward the far field — trending to the continuum shear-dominated `f_long = 0.033`.

**★REVIEW CORRECTION (R4b/F5) — the quarantine cites the WRONG frozen clause.** The shipped draft attributed the near-zone `κ²=3.2` quarantine to "the frozen prereg's declared finite-size fallback." But that fallback (prereg:109) fires only if **both channels are below the resolvable drift floor** — a condition that (a) did NOT occur (both channels are orders of magnitude above any floor: `κ²=3.2 ≫ κ_max²`) and (b) was UNEVALUABLE as shipped, because `|ΔH/H|` was never computed (now supplied, §4.3). The clause that ACTUALLY fires is the frozen bin-map's BIN-1 clause (prereg:109, verbatim `[sic]`): *"Model C `κ_env² > κ_max²` (finite-size correction disclosed) AND same-multipole-order as shear ⇒ supports BIN 1."* The near-zone `κ²=3.2` satisfies `> κ_max²=1.3×10⁻⁴` with the finite-size correction disclosed, and same-multipole-order is carried by the ANALYTIC Leg-A `Δℓ=0` (mass=A1 kills monopole+dipole). So the near-zone `κ²=3.2` is a POST-HOC finite-size characterization quarantined from the headline exceedance (which uses the conservative finite-size-free `0.034`); the verdict rests on the color-check, the weak-texture invariance ratio, and Legs A/B. **The numerical multipole discriminator is INCONCLUSIVE, not passed:** the `Ω`-scaling exponents `0.86` vs `1.79` differ by `0.93` against a decision spacing of `2` (`2Δℓ`), judged by a driver-MINTED `<1.0` tolerance the prereg never froze, while `κ²` is itself non-`Ω`-independent (`3.21→1.68`, factor `1.9`); the same-order conclusion is carried by Leg A, not by this flag. The `L=20`, `r_meas=6`, `dt=0.2·dt_CFL` grid numerics ran as frozen; two frozen REPORT items (drift, seed-fraction) were undelivered in the shipped run and are now supplied (§4.3, §4.4). Determinism: reruns bit-identical.

### §4.3 — the two frozen REPORT observables (supplied per review R4c/F7; prereg §3 Grid line)
The frozen prereg (`prereg-FROZEN.md:108`) declared two report items under the Grid bullet that the shipped driver never computed: the seed transverse fraction and the energy drift `|ΔH/H|`. Both are now computed deterministically by the driver (`_results.json`, per-run keys; reruns bit-identical):

| Run | seed transverse fraction (`∇×u`) | energy drift `|ΔH/H|`_window |
|---|---|---|
| Model S (breathing control) | `2.62×10⁻³²` | `0.807` |
| Model C `Ω=0.15` cold / sat | `2.62×10⁻³²` | `0.212` / `0.216` |
| Model C `Ω=0.30` cold / sat | `2.62×10⁻³²` | `0.611` / `0.612` |

- **Seed transverse fraction `= 2.6×10⁻³²`** (machine zero) — confirms the seed is curl-free by construction (`u = ∇φ` is exactly radial about each texture center), so any transverse radiation measured downstream is genuinely radiated, not seeded.
- **Energy drift `|ΔH/H|`_window `= 0.21–0.81`** — ★this is LARGE, and honestly so: the system is KINEMATICALLY DRIVEN (core sites clamped to a moving boundary), i.e. OPEN, so `H` is NOT conserved — the boundary continuously pumps work into the free field. The frozen "`≤~1%` bounded-Verlet precedent" was #761's CLOSED seeded free-evolution and does NOT transfer to a driven binary; the swing here is real boundary pumping, not numerical drift. The shipped integrator-stability check is determinism (reruns bit-identical). A clean numerical-drift diagnostic would require an undriven relaxation run (`H` conserved) or a boundary power-balance ledger — neither is shipped. **Consequence for the fallback clause:** because `|ΔH/H|` was never computed in the shipped run AND is not a resolvable-drift-floor quantity for an open system, the prereg's drift-floor fallback (prereg:109) was doubly inapplicable (§4.2 R4b).

### §4.4 — ★Leg-C prereg-vs-shipped deviations (disclosed per review R4e/F9; the frozen prereg is byte-untouched — deviations are disclosed HERE)
Standing discipline: silent prereg deviations are findings even when they do not flip the verdict. Three frozen §3 clauses changed or went undelivered in the shipped Leg C; NONE flips BIN-1-CONDITIONAL (the verdict path rests on the analytic `Δℓ=0`, the finite-size-free color-check, the design-identical cold-vs-sat matched pair, and Legs A/B — none of which depends on a dropped item), but all are disclosed:
1. **Two frozen report items undelivered in the shipped run** (drift `|ΔH/H|`, seed transverse fraction) — now supplied (§4.3). The shipped `result` said "grid ran as frozen; NO grid deviation," which is true of the grid NUMERIC params (`L`/`r_meas`/`dt` all match) but silent on these two report sub-items; corrected.
2. **The discriminator design changed from the frozen recipe.** Prereg §3 froze "Two source models on the SAME shipped srs net, SAME driven frequency, SAME multipole content," with Model C "a saturated (Op14-ON) rigid bias texture translated/oscillated on `X(t)`" (a SINGLE texture). The shipped driver runs Model S as a single fixed breathing core at `Ω=0.20` (monopole) vs Model C as a two-lump rotating BINARY at `Ω=0.15/0.30` (quadrupole) — NOT the same frequency and NOT the same multipole content, and the single-texture→binary swap (the dipole-free binary needed to source a clean `2Ω` quadrupole) is presented as design but was never flagged as a deviation. It is a defensible improvement (dipole-free quadrupole source), but it is a deviation from the frozen recipe and is disclosed as such.
3. **A driver-minted tolerance was tagged as frozen.** The `same_order` flag rode a driver-minted `abs(n_par − n_perp) < 1.0` threshold whose JSON note claimed "Frozen prereg §3," though §3 froze no numeric threshold. Corrected in the driver: the key is renamed `same_order_heuristic_lt1`, tagged `tolerance_is_driver_minted_not_frozen: true`, and the note now states the discriminator is INCONCLUSIVE and the `Δℓ=0` verdict is carried by the analytic Leg A (§4.2).

---

## §5 — FROZEN-BIN VERDICT + consistency gate + anti-seduction fence check

| Leg | Frozen outcome | Decisive step |
|---|---|---|
| **0 — sign** | compression grades WITH shear (`Γ_shear=Γ_bulk=−1`); `c_P/c_S` ratio FROZEN (degree-0) | forecloses BIN-2-via-F2; saturation does not shift the partition (canon `:37`, `:38`/PR#521) |
| **A — reduction** | orbital band = mass-quadrupole coupling into the GAPLESS P-branch; `Δℓ=0` | gaplessness defeats the adiabatic hope (F1); `mass=A1` kills monopole+dipole (F3); `κ_env²=(2/3)(c_S/c_P)⁵` |
| **B — asymmetry** | SYMMETRIC on the MEDIUM sector; shear survives at GR rate (consistency-import grade); MEDIUM-class BIN-2 routes foreclosed | every MEDIUM-class suppression is channel-symmetric ⇒ silences shear too (→ BIN 3, forbidden); constituent bulk-only `Γ` wall NOT addressed (§3.1 open fork) |
| **C — numerical** | orbiting driven texture RADIATES compression; WEAK-texture invariance cold≈sat (`<0.2%`, free-field `S≥0.987`); spectral `κ_env²=0.034 ≫ κ_max²` | weak-texture invariance (Leg-0 confirmed, weak regime); `c_P/c_S` gate PASS; near-zone `κ²` quarantined (finite-size); deep-saturation NOT probed |

**★Overall frozen-bin verdict: BIN-1-CONDITIONAL (ENVELOPE-RADIATES on the shipped analysis).** `κ_env² = 0.034 > κ_max² = 1.3×10⁻⁴` (by `114–1360×` across q1's O(1) coupling bracket; `262×` central); the compression channel is a gapless port the orbiting mass-quadrupole couples to at the q1 coupling, and no derived channel-asymmetric suppression exists **within the K=2G grade-locked MEDIUM sector**. **★"BIN 2 unreachable" is DEMOTED to CONDITIONAL** (review R1): the "structurally unreachable, NOT merely un-derived" wording is UNEARNED because canon puts a BULK-ONLY `Γ_bulk=−1` mirror at every CONSTITUENT knot core (`electron-bh-isomorphism.md:26`) — a channel-ASYMMETRIC structure of exactly the BIN-2 shape — and no shipped leg adjudicates the constituent-cage-ensemble fork (§3.1). Unreachable is CONDITIONAL on constituent-wall Γ-symmetry AND ensemble aggregation; the fork is OPEN, unadjudicated both ways, and potentially verdict-flipping. Bin NOT flipped; grade demoted.

**★Consistency gate: PASS at CONSISTENCY-IMPORT grade** (review R4d/F8). *Form derived*: the T2-shear channel is gapless and couples at quadrupole order. *Rate inherited*: "at the GR rate" rides the corpus GR-imported Peters-Mathews / HT chain (`08_gravitational_waves.tex`, `K=2G` PR#261), NOT re-derived within the frozen `δ` band (no within-`δ` shear-flux reproduction is derivable from an `L=20` near-zone lattice, and the gate never had a *derived* suppression candidate to fire on — its PASS is a BIN-2-foreclosure argument, not a survived within-`δ` test). The framing is NOT falsified (shear lives ⇒ NOT BIN 3), and the MEDIUM-class suppression that BIN 2 would need is exactly what the gate forbids (it would kill shear too) — but the constituent bulk-only route is outside this foreclosure (§3.1).

**★Anti-seduction fence check (both ways, per the frozen prereg; UPDATED by review).** (i) The walk narrative WANTED BIN 2 (rescue); the shipped legs return BIN 1 on the branches they adjudicate — the rescue direction is NOT taken. (ii) The #761 momentum WANTED BIN 1 (kill-completion); **this is exactly where the completion-bias bit: the shipped draft over-reached to "BIN 2 structurally unreachable, NOT merely un-derived," and the maximum-stakes review caught it** — canon's constituent bulk-only `Γ_bulk=−1` mirror (`electron-bh-isomorphism.md:26`/`:36`) is a channel-asymmetric BIN-2 shape the refutations never adjudicated. The verdict is therefore demoted to BIN-1-CONDITIONAL; the completion-bias is guarded by naming the open fork, not by re-asserting the kill. The verdict cites the frozen criteria's outputs (`κ_max²` §1; the far-field-structural `κ_env²=0.034` finite-size-free, with q1's O(1) bracket `114–1360×`; the weak-texture invariance ratio; the Legs-0/A/B analytic). The near-zone time-domain `κ²=3.2` — which would OVERSTATE the compression dominance — is QUARANTINED as post-hoc finite-size characterization, NOT used to headline the exceedance; the headline `114–1360×` uses the conservative finite-size-free `0.034`, not the near-zone value. Mechanism NAMED with `clm`/solidity inline (gaplessness — `clm-bnd5rq` 0.8; MEDIUM-sector grade-locking — `lattice-extreme-bh-rationality.md:37`, the BH melt-wall line, symmetric; the CONSTITUENT wall is the bulk-only `:36`/`electron-bh-isomorphism.md:26`, ASYMMETRIC and NOT foreclosed; multipole — `master-equation.md:20`); the consistency gate is independently evaluated at consistency-import grade (shear survives).

**Contradiction with the standing ruling (flag-don't-fix).** Grant's Q1 ruling was RULED-CONDITIONAL Reading-B; #761 already returned NONE-DERIVES at the carrier level, reverting it. This lane confirms the revert one level down (the envelope reframe does not rescue it). **Both surfaced with file:paths + verbatim content; neither reframed.** Grant adjudicates; the auditor lands any port-register/ledger edit. This lane touched NO KB/tex leaf (fence held).

---

## §6 — Calibration-vs-derived ledger + owed follow-ons

### §6.1 — Ledger (`consistency-vs-emergence` tags)
| Quantity | FORM | VALUE | Class |
|---|---|---|---|
| `κ_max² = δ_DP = 1.3×10⁻⁴`, `κ_max = 0.0114` | `[derived]` `=√δ` | `[import]` (Kramer 2021 `δ_DP`) | manifestation given import (the kill-line) |
| `c_P/c_S` ratio frozen under saturation (degree-0) | `[canon-read]` (`electron-bh-isomorphism.md:38`/PR#521) | `1.813` dimensionless | CONSISTENCY (`K=2G` GR-imported) |
| STAR-shell `Γ_shear=Γ_bulk=−1` (grade-locked, MEDIUM sector) | `[canon-read]` (`lattice-extreme-bh-rationality.md:37`, the BH melt-wall line) | — | the MEDIUM-sector block on BIN 2 (star-shell + adiabatic-gap routes only) |
| CONSTITUENT-wall `Γ_bulk=−1` bulk-ONLY (channel-ASYMMETRIC) | `[canon-read]` (`electron-bh-isomorphism.md:26`/`:36`, electron bulk-channel TIR) | — | ★the OPEN fork — NOT foreclosed by the MEDIUM-sector block (§3.1) |
| carrier band embedded/radiatively-lossy (NOT gapped — `clm-bnd5rq`: no internal stop-band either channel); orbital band = ponderomotive coupling into the gapless P-branch; adiabatic protection = FREQUENCY-SCALE separation `ε=Ω/ω₀~10⁻²⁴` (not a spectral gap) | `[derived]` (multiple-scales) + `[canon-read]` (#761 gaplessness `clm-bnd5rq` 0.8) | — | manifestation — Fork F1 resolved (R3b corrected) |
| compression monopole+dipole killed (`Ṁ=0`, `Ẍ_cm=0`); quadrupole radiates | `[derived]` + `[canon-read]` (`mass=A1`, `master-equation.md:20`) | `Δℓ=0` | manifestation — Fork F3 resolved |
| equal-coupling normalization `A_ang=2/3` (`κ_L/κ_T=1`) | `[assumed]` — q1-inherited symmetric-coupling grade-assignment (#761 prereg's "assumed symmetric-coupling"), NOT derived from the K4/Ax4 Lagrangian here; NOT numerically circular (flux ratio, mass-moment normalization cancels) | — | ASSUMED (consistency-class per q1:70) |
| `κ_env² = A_ang(c_S/c_P)⁵`, central `0.034`; q1 O(1) bracket `[0.0148, 0.1768]` | `[derived]`-form ×`[assumed]`-coupling = q1 value; Leg-C spectral color-check | `0.034` (bracket `114–1360×`) dimensionless | the far-field structural coupling (O(1)-bracketed) |
| consistency-gate PASS (shear at GR rate) | `[derived]`-form (gapless + quadrupole) × `[import]`-rate (Peters-Mathews/HT, `08_gravitational_waves.tex`; `K=2G` PR#261) — NOT re-derived within `δ` | — | CONSISTENCY-IMPORT grade (R4d; a BIN-2-foreclosure argument, not a within-`δ` test) |
| Leg-C WEAK-texture invariance (cold≈sat) | `[derived]` (lattice driver; ratio, finite-size-free) — free-field `S≥0.987`, deep-saturation NOT probed | `<0.2%` diff | manifestation — Leg-0 confirmed in the WEAK regime only (R2) |
| Leg-C near-zone `κ²=3.2` | `[derived]` (lattice, near-zone) | `3.2` | QUARANTINED — finite-size characterization, not a verdict input |
| pulsar exclusion (banked) | — | `[import]` (Weisberg-Huang 2016; Kramer 2021) | import (banked, #761/q1) |

No emergence-class claim headlined. The deliverable is the frozen-bin verdict (BIN-1-CONDITIONAL) + `κ_env² = 0.034` (O(1) bracket `114–1360×`) vs `κ_max² = 1.3×10⁻⁴`, riding on the derived reduction + the finite-size-free lattice measurement — with the constituent-cage-ensemble fork OPEN (§3.1, §6.2).

### §6.2 — Owed follow-ons (fenced; NOT executed here — Rule 12; slot NOT refilled)
1. **★CONSTITUENT-CAGE-ENSEMBLE DRIVER (the owed VERDICT-CONTROLLING adjudicator; routed, NOT built here).** The review demoted the verdict to BIN-1-CONDITIONAL on the constituent knot-core fork (§3.1). The owed follow-on: derive/simulate what an ENSEMBLE of ~`10⁵⁷` BULK-ONLY-caged compression cores (each with its own `Γ_bulk=−1` mirror per `electron-bh-isomorphism.md:26`) presents to the `2Ω` far field — specifically whether the aggregate is (i) a sum of image-cancelled core moments (each `∝(k·r_core)²`, channel-asymmetric ⇒ the LIVE BIN-2 route) or (ii) the coarse-grained uncaged mass texture (radiates ⇒ BIN-1 clean). This is what neither the shipped Leg C (un-caged pinned lump) nor the analytic Legs A/B (smooth common source) tested. It is the verdict-controlling adjudicator of the fork; it must also resolve the A1⊥T2 sector-ownership question (which impedances collapse at a Cosserat-winding knot core). Grant/auditor-gated; NOT built in this repair lane.
2. **NO rescue derivation for Reading B is MINTED here** — per honest closure (Rule 11) + substitution-not-retraction (Rule 12), the shipped slot is not refilled with an assertion. Any future closure gets a new version + its own verification chain, and (per the open fork) must resolve the constituent-cage-ensemble question above BEFORE it can claim "structurally unreachable."
3. **The Q1 revert + falsification-ledger promotion** were already routed by #761 (NONE-DERIVES); this lane's BIN-1-CONDITIONAL confirms the same consequence one level deeper ON THE ADJUDICATED BRANCHES, but the open constituent-cage fork means the "structurally unreachable" strengthening is NOT yet earned. *Grant-gated; auditor lands the port-register/ledger edit; this lane surfaces, does not land.*
4. **The `master-equation.md:20` sector-ownership wording tightening** (routed by #761 §6.2-2) — unchanged; this lane relied on the physical (acoustic compression) identification, which stands.
5. **NOT owed:** a finite-size-free time-domain far-field κ measurement — the spectral C-2 color-check + the analytic Leg A already give the far-field structural partition; a bigger-box time-domain far-field run is a nice-to-have, verdict-neutral (the near-zone quarantine already handles the finite-size honesty). (Distinct from item 1: the constituent-cage-ensemble driver is a DIFFERENT, verdict-controlling experiment, not a bigger box.)

---

> **Corpus-state note (2026-07-20).** Base HEAD `f84b622b`. `origin/main` advanced to `60b30b4b` during this lane: **#761 MERGED** (PR #761; the mechanical-commonmode NONE-DERIVES is now on main — cited here as `[branch @ d17a2248]`, the identical merged branch head), and the **Grant-led envelope-boundary walk RECORD landed** (`research/2026-07-20_envelope-boundary-walk_RECORD.md`, PR #765), which explicitly names THIS lane as the adjudicator of its walk candidates ("the lane's frozen-bin result wins") and routes its image-cancellation candidate here (adjudicated §3.1). My branch's merge-base is `f84b622b`; my changes are purely additive (research docs + docket append); no rebase needed for the DO-NOT-MERGE PR. The RECORD is RECORD-class canon I did not edit (fence held).
>
> **Result-doc provenance.** Fired by Grant 2026-07-20 (`"fire the lane"` `[sic]`). Frozen prereg committed + pushed ALONE first (`e27cc5e0`); analytic Legs 0/A/B (`b25fb9d5`); Leg-C driver reproduced by `research/drivers/envelope_sector_orbiting_lump.py` (+ `_results.json`, white figure), `ave.core.*` read-only, engine byte-untouched, reruns bit-identical. All `[canon]` citations content-verified two-method at base `f84b622b`; #761 `[branch @ d17a2248]`; pulsar figures `[import]`. FORMs `[derived]` by multiple-scales + multipole/conservation algebra; the far-field partition is the finite-size-free spectral color-check + the analytic q1 value; the Leg-C saturation-invariance is lattice-measured. Mints no `clm-`; propagates to no leaf; READ-ONLY on KB; port-register untouched. **Verdict (review-demoted): BIN-1-CONDITIONAL — ENVELOPE-RADIATES on the shipped analysis. `κ_env² = 0.034 > κ_max² = 1.3×10⁻⁴` (`114–1360×` across q1's O(1) coupling bracket; `262×` central); the envelope reframe confirms the #761 carrier-level kill one level down on the adjudicated branches — the compression channel is gapless (no adiabatic gap for the orbital band) and the MEDIUM-sector bulk/shear channels are `K=2G` grade-locked (no channel-asymmetric structure there; the consistency gate forecloses the MEDIUM-class BIN-2 routes at consistency-import grade). NOT "BIN 2 structurally unreachable": canon's CONSTITUENT bulk-only `Γ_bulk=−1` mirror (`electron-bh-isomorphism.md:26`) is a channel-asymmetric BIN-2 shape whose ensemble-aggregation the shipped legs never adjudicate (§3.1) — an OPEN, potentially verdict-flipping fork (owed driver, §6.2-1). A bankable NEGATIVE (Rule 11) on the adjudicated branches; grade demoted, bin NOT flipped. Consequence routed to Grant/auditor; no leaf touched.** Companions: the frozen prereg (`_prereg-FROZEN.md`), the analytic legs (`_derivation.md`), `[branch:#761 @ d17a2248]`, the q1 hardening (`research/2026-07-20_q1-pulsar-hardening.md`), the port register Q1 row, and the docket continuation (`### ENTRY 2026-07-20-envelope-sector`).

> **★REVIEW-REPAIR ADDENDUM (2026-07-20, PR #767 maximum-stakes adversarial review — this doc EDITED per Rule-12 repair-and-bank; the frozen prereg is byte-untouched).** Disposition: 11 confirmed / 0 refuted. The banked BIN-1 verdict DEMOTES to **BIN-1-CONDITIONAL**. R1 (CRITICAL): the §3.1 image-cancellation refutation adjudicated a STAR-level strawman; canon (`electron-bh-isomorphism.md:26`) puts a BULK-ONLY `Γ_bulk=−1` mirror at every CONSTITUENT knot core — a channel-asymmetric BIN-2 shape — and the constituent-cage-ensemble fork is OPEN (verdict grade demoted; bin NOT flipped; owed driver §6.2-1). R2: the Leg-C saturation-invariance is re-graded to WEAK-texture invariance (deep saturation lived only on clamped-inert bonds; free field saw `S≥0.987`). R3: q1's O(1) coupling bracket restored (`114–1360×`); the equal-coupling normalization tagged q1-inherited/assumed (not numerically circular); the "carrier gapped" wording corrected to frequency-scale separation (`clm-bnd5rq`: no internal gap either channel). R4: Leg-C honesty batch — both arms hand-install the source (§4 R4a); the fallback attribution corrected to the bin-map clause that actually fired (§4.2 R4b); the two frozen observables computed (§4.3); the gate PASS re-graded to consistency-import (§5); deviations disclosed (§4.4). R5: docket #761 merge-state corrected (append). Merged `origin/main` for the repair (union docket; no conflicts; `origin/main` advanced past `60b30b4b` — the Corpus-state note above records the ORIGINAL lane's state, historically accurate). Driver `_results.json` regenerated deterministically (existing physics keys byte-identical; two owed observables + quarantine-labeled verdict keys added). DO-NOT-MERGE.
