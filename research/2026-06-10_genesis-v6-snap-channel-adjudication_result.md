# RESULT — Genesis-v6 JOB 3: the SNAP-CHANNEL adjudication sweep → CLIP

**Date:** 2026-06-10
**Prereg (frozen, committed alone):** `research/2026-06-10_genesis-v6-transducer_prereg.md` @ `fa4420c6`
**Driver:** `src/scripts/vol_1_foundations/genesis_v6_snap_channel_adjudication.py` (serial, deterministic, seed `20260610`)
**Raw numbers:** `research/2026-06-10_genesis-v6-snap-channel-adjudication_results.json` (read FROM it — ave-driver-script-honesty)

---

## 0. VERDICT (written from the grid — Rule 11, adjudicates the LOCK only; the electron claim is closed NOT-ELECTRON)

**The v5 SNAP-LOCKED claim is CLIP.** The "pocket holds under P2 de-spin" was the CONSTRUCTION, not the
physics. Running EXACTLY the §210-skipped sweep (`Δ_heal × snap_payback_rate × K3 stop-time`): with the
CORRECTED (payable) latent the P2 pocket persists ONLY where un-snap is DISABLED, and DISSOLVES FULLY
(→ 0 %) the moment un-snap is ACHIEVABLE. The "lock" TRACKS the irreversibility knobs — it is the
accounting, NOT the snap holding the void. This CONFIRMS (does not overturn) the v5 panel demotion and
the D-PERM finding: **the MOTION (conserved circulation) is the lock; the snap is the birth-flash.**

---

## 1. THE GRID — pocket-persistence-under-P2 (forced de-spin), retain = pocket_final / pocket_built

### LEGACY accounting (the v5 construction; built pocket 5968, latent_held = 968 — the double-count, UNPAYABLE)

| Δ_heal \ payback | 0.0 | 1.0 | 5.0 |
|---|---|---|---|
| 0.00 | 100 % (unsnap 0) | **100 %** (unsnap 16) ← *the v5 operating point* | 82 % (unsnap 1072) |
| 0.02 | 100 % | 100 % | 82 % |
| 0.05 | 100 % | 100 % | 82 % |

The v5 read `5968 → 5952` lives at `payback=1.0, Δ_heal=0` → retain 100 %, only 16 un-snaps. The latent
(968) is the JOB-1 double-count (≈ 100 % shock KE) → **unpayable** → the pocket holds *regardless*. It
tracks payback only weakly (82 % at payback 5) because even an unpayable latent yields ~1000 un-snaps at
5× payback.

### CONSERVATIVE accounting (the CORRECTED, payable latent = 0.52)

| Δ_heal \ payback | 0.0 | 1.0 | 5.0 |
|---|---|---|---|
| 0.00 | **100 %** (un-snap off) | 32 % | 25 % |
| 0.02 | 100 % | **0 %** | **0 %** |
| 0.05 | 100 % | **0 %** | **0 %** |

Once the latent is payable, the P2 pocket persists ONLY at `payback = 0` (irreversibility forced); with
any `payback > 0` AND `Δ_heal > 0` it **dissolves completely** (`5256 → 0`, 31 200 un-snaps). It tracks
all three knobs: payback (100 → 25 %), Δ_heal (at payback 1: 32 % → 0 % as Δ_heal 0 → 0.02), and the
stop-time (the pocket shrinks monotonically over K3 ∈ {300, 600, 1200}).

## 2. THE DISCRIMINATOR

- **PHYSICS** would require the pocket to persist under P2 across the grid EVEN with the payable latent and
  high payback (the snap genuinely holding the void). It does NOT.
- **CLIP** is what the data show: persistence holds ONLY where un-snap is disabled (payback = 0, or the
  legacy unpayable latent) and vanishes once un-snap is achievable. The "lock" is the irreversibility
  bookkeeping (`snap_payback_rate`, `Δ_heal`), the very knobs the v5 §210 pinned-and-skipped.
- This is the discipline working: the v5 panel's demotion (SNAP-LOCKED → UNRESOLVED, construction-
  dependent) is now FORMALLY RESOLVED to **CLIP** by the sweep it named.

## 3. COROLLARY — D-PERM stands stronger

The snap does not lock the void. Under P2 forced de-spin with a physically re-enterable snap, the pocket
heals to nothing. The ONLY certified permanence is D-PERM — the conserved, ν_art-invariant **circulation**
(the v5 MOTION-LOCK: `L_bulk` drive-off ratio 0.97–0.99 all arms). The snap's certified role remains the
**birth flash** (D6, ~160 bursts 3–6 OOM above floor), demonstrated preserved under both D10 renderings
(JOB 2). v6's design premise — "v6 does NOT ask the snap to hold anything; it asks the circulation to" —
is empirically vindicated.

## 4. FLAGS (flag-don't-fix)

1. **Rule 12 (substitution-not-retraction):** the v5 SNAP-LOCKED slot's 🔴 demotion stands; this result
   converts UNRESOLVED → CLIP with its own verification chain (the swept grid). It does NOT refill the slot
   with a new positive.
2. **Corpus-state delta (auditor lands):** the v5 D8 row "SNAP-LOCKED density void (UNRESOLVED, construction-
   dependent)" is now **CLIP** (sweep-backed). D-PERM (MOTION-LOCKED) is unchanged — the lone certified lock.
