# RVE Aggregation Bench — RESULT (the frozen-bin verdict on Z_bulk,eff/Z₀: does the ensemble homogenize into a macro-cage?)

**Date:** 2026-07-21
**Class:** DERIVATION + lattice-derived research-driver (research-doc; **forms derived, values dimensionless/geometric; mints no `clm-`/`def-`; propagates to no KB/tex leaf**). Resolves the frozen bins of `research/2026-07-21_rve-aggregation-bench_prereg-FROZEN.md`.
**Provenance:** Grant-fired 2026-07-21 (`"kick off the rve aggregation bench"` `[sic]`). THE `N>1` CONSTITUTIVE aggregation half of the two-instrument routing merged **#775** (`research/2026-07-20_deep-rail-kscaling_result.md` §8.2) split the open #767/#770 fork into. Frozen prereg committed + pushed ALONE first (`0cf5f01`-class, COMMIT 1); analytic Leg 5 in `..._derivation.md`; the driver `research/drivers/rve_aggregation_bench.py` (+ `_results.json`, white figure) reuses the #770/#775 `constituent_cage_ensemble.py` primitives, `ave.core.*` read-only, engine BYTE-UNTOUCHED, **reruns BIT-IDENTICAL** (§7.0). Every `[canon]` input content-verified two-method at base HEAD `3d07ceeb`.
**Lane fences:** DERIVATION lane only. Engine byte-untouched; **no** `manuscript/`/`ave-kb/` leaf edit; **no** port-register edit; **no** un-revert; **no** falsification-ledger edit — regardless of outcome (held). Consequence ROUTED to Grant only (the radiative stage-2 is NOT run). Every scan ships its data + code path (the #770 lesson — NO prose-string conclusions).

> **★FROZEN-BIN VERDICT (RE-DERIVED at review, §7.6/§7.7): BIN 4 — REGIME / UNDETERMINED — the macro-cage-vs-matched class is constitutively MEASURED but UNDETERMINED at the `r_Z = 0.5` bin-edge.** ★This is a STRONGER state than the shipped headline ("not lattice-decidable at feasible box"): the adversarial review found two load-bearing corrections, both of which IMPROVE the bench's decisiveness. BIN-4 SURVIVES, but its BASIS changes from "collapse fails ⇒ not lattice-decidable" to "collapse PASSES on the correct variable ⇒ constitutively measured; undetermined only via the `r_Z` bin-edge straddle + Fork ρ."
>
> **Gates (honest recount, §2/§7.8).** THREE FIREABLE discriminating gates PASS (internal `rel=0.038`; STOP-gate opposite-sign; RVE-size `gap=0.014`) and the **Lamé gate PASSES on the FROZEN DELIVERABLE** (exterior/**INTERIOR** `∇·u = 0.036 → 0 ≤ 0.10`; §7.6). One "gate" (amplitude-linearity `spread ≈ 1e-15`) is a linear-by-construction IDENTITY, not a fireable gate. The frozen static-limit ramp gate **FAILED as written** (`rate_spread = 0.133 > 0.10`, JSON `pass:false`) and is carried by a disclosed POST-FREEZE CG-tolerance-independence substitute (legitimate on Ax3-lossless; §7.2). The shipped "5/6 frozen gates PASS" is honestly: **3 fireable PASS + Lamé deliverable PASS + 1 identity + 1 frozen-gate-FAILED-carried-by-substitute + the frozen φ-collapse FAILED (but the geometry-correct `f_incl` collapse PASSES; below).**
> - **Trigger (i) — the COLLAPSE — RE-AXED, does NOT fire (§7.7, finding 1/5).** The frozen collapse used `φ = (4/3π r_cage³)/s³`, which counts the COLD cage INTERIOR; the mechanically active soft phase is the SHELL. On the geometry-correct coated-inclusion fraction `f_incl = (4/3π(r_cage+cage_w)³)/s³` the HEADLINE bulk-only-cold route **COLLAPSES** (`max_rel = 0.280 ≤ 0.30`; expanded `0.269`; compressed `0.328` and symmetric `0.396` remain over-tolerance, consistent with the finite shell-overlap `f_incl > 1` above mid-scan, §7.7). **So trigger (i) does NOT fire on the correct variable** — the two routes DO homogenize onto a single effective medium. The shipped `0.515` φ-collapse fail was a mis-chosen-axis artifact, and shell-percolation is `f_incl = π/6 ≈ 0.524` (ROUTE-INDEPENDENT), vs the route-DEPENDENT interior-φ `0.09/0.13` (itself the tell that φ is not controlling).
> - **Trigger (iii) — the `r_Z` STRADDLE — the SURVIVING trigger.** BIN-4 re-derives from the frozen tree via trigger (iii): `r_Z` straddles the `0.5` MACRO-CAGE/MATCHED band edge in a verdict-controlling way — across the pre-stress class (`compressed r_Z=0.466` MACRO-side vs `cold 0.544` / `expanded 0.568` MATCHED-side; on `f_incl` the classes CROSS-CLASS-FLIP: cold/expanded = BIN-2-MATCHED vs compressed = BIN-4) AND across the φ scan (crash-min `r_Z≈0.40` vs `φ_sf r_Z=0.544`) — compounded by the KUBC-conditional matched-side reading (an UPPER bound; the true `r_Z` could be LOWER → macro-side, §7.1) and by Fork ρ (β lifts `r_Z` within `[0.39, 0.89]`).
> - **⚠ BASIS CORRECTION (2026-08-01) — routed in from merged #802. BOTH clauses of trigger (iii) above evaporate on the bound-carrying measure. ★BIN-4's LABEL STANDS; only the stated BASIS is corrected. Body above preserved per Rule 12; nothing here re-bins this doc.**
>   - **Clause 1 (the pre-stress cross-class split).** The `compressed r_Z=0.466` above is `√(R_KUBC_core)` — the CORE estimator, which the #802 lane's frozen prereg [`2026-07-28_subc-kubc-bracket_prereg-FROZEN.md`](2026-07-28_subc-kubc-bracket_prereg-FROZEN.md) §2.3 states `carries NO bound status: the Hill/Huet ordering theorem is about the whole-cell apparent modulus, and a windowed sub-region energy is not an apparent modulus of anything`. #802 reproduces this doc's own `K_eff/K_0 = 0.2176075097402979` **bit-for-bit** and reads the SAME configuration on the bound-carrying WHOLE-CELL apparent modulus at **`r_Z = 0.6098`** — **above** `0.5`. Across this doc's four `φ_sf` legs, **`2` read macro-side on the core estimator and `0` do on the bound-carrying whole-cell reading**, the core estimator sitting BELOW the whole-cell value at every leg.
>   - **Clause 2 (the φ scan).** The crash-min `r_Z≈0.40` above is `0.4048630894644663` at `bulk_only_cold` route B, `r_cage = 1.7`, `s = 4.2` — **again the CORE estimator**. #802 runs that exact configuration, reproduces the core ratio bit-for-bit, and reads it whole-cell at **`0.7106`**. Across #802's entire **21**-configuration hydrostatic set the **minimum** whole-cell KUBC `r_Z` is **`0.5850`** (`symmetric_cold_phi_sf`), with **`0`** configurations anywhere below `0.5`.
>   - **What actually survives.** The `r_Z` straddle survives — but as a **BOUNDARY-CONDITION ARTEFACT, not as a cross-class physical flip**: it is restored at **`2` of the `4`** legs (`bulk_only_compressed`, `symmetric_cold`; the other two RESOLVE-HIGH on the two-sided bracket) by #802's **SUBC LOWER** bound falling below `0.5` — the free-surface softening of the traction boundary condition — **not** by any class reading macro-side on an upper bound. The honest restatement is *"undetermined because the two admissible boundary conditions disagree"*, not *"undetermined because one pre-stress class flips to the macro side."*
>   - **★Why the LABEL is untouched.** Removing a bound-robust macro-side leg makes the `r_Z` axis **MORE** undetermined, not less — BIN-4 is the UNDETERMINED bin. Separately, the `f_incl` BIN-2-vs-BIN-4 split this doc calls *"a verdict-controlling cross-class flip"* at `:114` (§6, LEG 4 / VERDICT) is a **different object**: it is collapse-gate-driven, not `r_Z`-driven (`rve_aggregation_bench.py:831` `_bin_of` returns BIN-4 whenever the collapse gate fails, before `r_Z` is read), and it persists unchanged. BIN-4's label survives on both counts.
>   - **Provenance + standing.** Merged **#802** [`research/2026-07-28_subc-kubc-bracket_result.md`](2026-07-28_subc-kubc-bracket_result.md) §8 consequence 7 + its docket fragment [`2026-07-28-subc-kubc-bracket.md`](../_orchestration/docket-entries/2026-07-28-subc-kubc-bracket.md) findings F5/F8; every number above ships bit-exactly in `research/drivers/subc_kubc_bracket_results.json` under `F5_782_bound_robustness_crosscheck_NOT_FROZEN`. #802 edited no merged doc and minted no bin — the correction was ROUTED, and this note lands it. **This is the SECOND routed basis correction to this doc**; the first was the ρ-flags audit ([`2026-07-21-relative-offset-principle.md:6`](../_orchestration/docket-entries/2026-07-21-relative-offset-principle.md)).
>
> **★What the bench DOES establish (banked positive characterization; brackets RE-GRADED on the coated geometry, §7.7).** The bulk-only cage array produces a REAL `~5×` constitutive compression-softening — `K_eff/K_0` crashes to `≈0.16–0.19`. Graded against the GEOMETRY-CORRECT coated-inclusion pressure-release (parallel / Voigt) bound `1 − f_incl`, the measured `K_eff` sits **~AT the bound at the crash minimum** (`1 − f_incl = 0.192` vs measured `0.189` at `f_incl≈0.81`): **the composite TRACKS the coated-inclusion PARALLEL bound** — the cold matrix carries the load AROUND the coated inclusions — and does NOT reach the Reuss SERIES crash a clean macro-cage needs. **⚠ RETRACTED:** the shipped "well below the Voigt bound (`1−φ ≈ 0.8`) — the cages are NOT bypassed by the matrix" used the WRONG-fraction interior-φ bound; on the correct coated fraction the margin evaporates and the "intermediate crash" reading is re-graded to "tracks the coated-inclusion parallel/bypassing bound." (The "not-Reuss / does not reach `→0`" FLOOR reading is itself KUBC-conditional — an upper bound cannot support a floor claim, §7.1 — so it leans, if anything, toward macro-cage.)
>
> **★Both anti-seduction fences HELD (the #770 lesson NAMED — every scan ships data + code path; verdict cites ONLY frozen-criteria JSON).** MACRO-CAGE (flatters BOTH the Reading-B re-open AND Grant's walked reframe) is NOT declared — `r_Z` straddles `0.5`, and the composite tracks the PARALLEL (bypassing) bound, not the Reuss crash. MATCHED/RIGID (flatter the #761→#767→#770 kill-momentum) are NOT declared — a real `~5×` softening exists and `compressed r_Z=0.466` dips MACRO-side. **Banked in NEITHER direction.**
>
> **CONSEQUENCE (Rule 11 honest closure): the #767/#770 aggregation fork REMAINS OPEN at the constitutive level.** ⚠ The shipped "needs the larger-box / periodic-homogenization instrument #775-flagged-infeasible — now CONFIRMED at the constitutive level" routing is **RETRACTED (§7.7, finding 8):** the collapse is DECIDED at `L=16` on the correct variable (the re-axis was ZERO compute), so route-collapse failing on the wrong axis is NOT a confirmation that a periodic instrument is infeasible. The honest resolver is the (FEASIBLE, same cost-class) owed **SUBC / periodic lower-bound bracket + the Fork ρ / Fork P ontology adjudication** — NOT a larger box. BIN-4 does NOT ground a Reading-B re-open (BIN-1 needs a VALIDATED catastrophic Reuss crash) and does NOT confirm the kill (BIN-2 needs `0.5<r_Z<2` cleanly). **Routed to Grant/auditor; NO leaf touched; no rescue derivation minted (Rule 12; slot not refilled).**

---

## §0 — REGIME / SECTOR / PHASE-STATE header

**MODE.** A compact gravitating body; the object = the EFFECTIVE MEDIUM the ensemble of ~`10⁵⁷` bulk-only-caged constituent solitons homogenizes into at scales ≫ core spacing (`electron-bh-isomorphism.md:26` `[canon]`). Contrast column: the textbook bubbly-liquid (Reuss/Wood crash) vs the Voigt/parallel bound. **REGIME.** Regime-I cold-linear STATIC constitutive response — NO drive, NO lock-in, NO radiation port; cages by CONSTITUTIVE GRADING (`S(A)→0` on a `~1`-node shell), NOT a kinematic pin. **PHASE-STATE.** Cold-reactive medium (Ax3-lossless-reactive), saturated cage shells; two pre-stress phase-states (cold rail / radiation-pressurized rail via the `axiom-register.md:193` remap). **SECTOR.** Under test = A1 bulk/compression (`K_eff`) + T2 shear (`G_eff`); mass = A1 dilatation (`master-equation.md:20`); A1⊥T2, NOT cross-wired. **A46 (`phase-space-coordinate-check` PASS):** the corpus claim (the aggregation reframe, walk RECORD §6 seed 3) and the test are BOTH in real-space constitutive + impedance-plane coordinates — matched, NOT a phase-space-vs-real-space mismatch. The discriminator `Z_bulk,eff/Z_0` is the impedance-plane coordinate the corpus's short/open framing (`master-equation.md:107` `Γ_bulk=−1=SHORT`) lives in.

---

## §1 — THE DISCRIMINATOR (frozen; recomputed from the shipped JSON) `[derived]`
`r_Z ≡ Z_bulk,eff/Z_0 = √((K_eff/K_0)·(ρ_eff/ρ_0))` at the space-filling end `φ_sf`. Frozen bins (§2): MACRO-CAGE `r_Z ≤ 0.5` (short) AND `K` falling; MATCHED `0.5<r_Z<2.0`; RIGID `r_Z ≥ 2.0` (open); REGIME `r_Z` flips / collapse or STOP-gate fails. Two ρ readings: PRIMARY engine-native `ρ_eff/ρ_0=1` (acoustic inertia unchanged) ⇒ `r_Z=√(K_eff/K_0)`; COMPANION canon mass-loading `ρ_eff/ρ_0=1+β·φ`, `β∈{0,1,3}` (mass = trapped compression, `master-equation.md:20`; the ontology — is trapped-energy mass acoustic inertia? — is Fork ρ, routed to Grant, §5 prereg).

---

## §2 — LEG 0 (INSTRUMENT VALIDATION — honest gate recount; §7.8) `[derived]`
All read from `rve_aggregation_bench_results.json` `leg0_instrument_validation` (baseline `L=16`, deep rail `S_RAIL=1e-4`). **★REVIEW-REPAIR (findings 2/4/9/11):** the shipped "5/6 frozen gates PASS" over-counted. Honest tally: **3 FIREABLE discriminating gates PASS + the Lamé deliverable PASS (§4); 1 IDENTITY (amplitude, not fireable); 1 frozen rate gate FAILED-as-written carried by a post-freeze substitute; the frozen φ-collapse FAILED but the geometry-correct `f_incl` collapse PASSES for the headline (§5/§7.7).**

| Gate | Frozen output | Verdict |
|---|---|---|
| **Internal validation** (KUBC `K_0`,`G_0` vs Bloch `c_P`,`c_S`) | `(K_0+4/3·G_0)/G_0 = 3.16` vs `(c_P/c_S)² = 3.286`, `rel = 0.0385 ≤ 0.20` | **PASS (fireable)** — the static homogenization recovers the Bloch moduli (`K_0=1.654`, `G_0=0.906`) |
| **STOP-gate** (mirror validity) | rail (bulk-only) `K_eff/K_0 = 0.189, 0.296` (SOFTENS, `<1`); rigid `K_eff/K_0 = 3.1, 1.6` (STIFFENS, `>1`) | **PASS (fireable)** — OPPOSITE composite-response class confirmed (cavity-softening vs rigid-stiffening). (Control at `100×`, not the frozen `1e4×`; sign-invariant, §7.10.) |
| **Static-limit / rate** | FROZEN gate (static CG vs dynamic ramp, tol `≤0.10`) **FAILED as written**: `rate_spread = 0.133 > 0.10`, JSON `pass:false`. Reported PASS rides a POST-FREEZE CG-tolerance-independence substitute (`spread = 0.0000 ≤ 0.02`). | **FROZEN-FAIL-as-written / substitute-PASS** — the substitute is legitimate on Ax3-lossless + CG-invariance but is NOT the frozen observable (§7.2/§7.8). |
| **Amplitude linearity** | `K_eff/K_0 = 0.1904` IDENTICAL across `ε = 1e-4/1e-3/1e-2`, `spread ≈ 1e-15` | **IDENTITY (not a fireable gate)** — the bond model is exactly linear (`S(A)` is a static grade, `Φ` is `u`-independent), so `K_eff` is `ε`-invariant by algebra; a numerical-pipeline check, §7.8. |
| **RVE-size independence** | `K_eff/K_0 = 0.167 (L12, 8 cages) / 0.190 (L16, 27) / 0.188 (L20, 64)`; `gap(L16,L20) = 0.014 ≤ 0.15` | **PASS (fireable)** — size-converged (L16/L20 agree to `1.4%`); the ~5× softening is a real effective-medium value, not finite-cluster |
| **Collapse (two-route)** | frozen φ-axis `max_rel = 0.515 ≫ 0.30` (**FAIL**); geometry-correct `f_incl` axis `max_rel = 0.280 ≤ 0.30` (**PASS**, headline; §5/§7.7) | **RE-AXED** — the frozen φ-collapse fail was a mis-chosen-axis artifact (φ counts the cold interior); on the correct `f_incl` the routes homogenize ⇒ trigger (i) does NOT fire. |

---

## §3 — LEG 1 (SINGLE-CAGE CONSTITUTIVE BASELINE) `[derived]`
Single cage (`r_cage=2.4`) at box center, `L=16` (dilute — one cage in the `L/2` core cube ⇒ weak magnitude, clear SIGN). `leg1_single_cage_baseline`:

| class | `K_eff/K_0` (hydro) | `G_eff/G_0` (shear) | uniaxial |
|---|---|---|---|
| bulk-only, cold | `0.932` | `0.890` | `0.934` |
| bulk-only, **compressed** (pre-stress `ε_pre=−0.08`) | `0.885` | `0.789` | `0.866` |
| bulk-only, **expanded** (`ε_pre=+0.08`) | `0.945` | `0.922` | `0.955` |
| symmetric, cold | `0.817` | `0.667` | `0.783` |

**Reading.** The pre-stress SIGN structure is confirmed at the single-cage level (the #779 Leg-C sign): **compressed `<` cold `<` expanded** (a compressed core SOFTENS `k_s` via the remap ⇒ lower `K`; expanded STIFFENS ⇒ higher `K`); the symmetric wall softens BOTH channels most (also kills shear, `G=0.667`). Magnitude is dilute (weak for one cage in a big cell) — the aggregation (Leg 3) is where the softening accumulates. **⚠ #779 R6 currency (§7.9):** #779's R6 review-repair (landed BEFORE this result) re-scoped this scalar DC-sign axis to "the wrong question" — the ratified vessel state is DC displacement = 0 with an ANISOTROPIC hoop-stiffen/radial-soften remap, so the sign here is orientation-keyed anisotropy, not a scalar static-DC profile-member; Fork P is to be re-posed to Grant in that frame. Bin-invariant.

---

## §4 — LEG 2 (THE LAMÉ GATE — re-banked on the FROZEN DELIVERABLE; §7.6) `[derived]`
A single PRESSURIZED cage (interior pinned to a uniform radial expansion, outer boundary `u=0`, the annulus relaxed by preconditioned CG — NO transient). `leg2_lame_gate`:

| class | **FROZEN DELIVERABLE** ext/**INTERIOR** `∇·u` (tol `≤0.10`) | FROZEN rel-agree (ii) `\|Δ\|/mean` (tol `≤0.25`) | ext/wall (diag.) | Lamé PASS |
|---|---|---|---|---|
| bulk-only, cold | `0.036` — **PASS** | `0.626` — **FAILS** (near-zero denom) | `0.029` | **True** |
| bulk-only, compressed | `0.019` — **PASS** | `0.585` — **FAILS** | `0.016` | **True** |
| symmetric, cold | `0.007` — **PASS** | `0.568` — **FAILS** | `0.005` | **True** |

**★REVIEW-REPAIR (finding 0/10, false-"Frozen" provenance OWNED; §7.6).** The shipped driver BANKED the Lamé PASS on an ABSOLUTE two-shell agreement `|ext1−ext2| ≤ 0.10` that it **mislabeled "Frozen:"** (a criterion invented post-freeze — the #770-fabricated-string class), and normalized the exterior ratio by the WALL, not the interior. **The gate is re-banked here on the FROZEN DELIVERABLE** (prereg §4 Leg 2: the exterior `∇·u`/**interior** `∇·u` ratio → 0, tol `≤0.10`), which **PASSES independently** (`0.036/0.019/0.007`). The **FROZEN relative shell-agreement criterion (ii) `|Δ|/mean ≤ 0.25` FAILS as written on all three rows** (`0.626/0.585/0.568`) — a genuine NEAR-ZERO-DENOMINATOR pathology (both exterior ratios are `~0` by the div-free result, so their relative difference is ill-conditioned/meaningless). Reported honestly, not relabeled; the absolute agreement is retained only as a labeled REVIEW-REPAIR conditioning diagnostic.

**Reading (frozen consistency check, derived-before-measured).** On the re-banked frozen deliverable, the converged static exterior dilatation `∇·u → 0` (`≤0.036`, `≤0.10`) for all classes — the exterior is PURE-DEVIATORIC-dominated, the **Lamé pressurized-cavity solution `u_r=C/r²` (div-free) CONFIRMED**; the small residual is the finite-box `B·r` term. **★Retroactive adjudication of #770 Leg-1's `0.65` (on its METRIC-INDEPENDENT basis):** the static solve carries NO transient — a structural fact independent of any convergence metric — whereas #770's DYNAMIC window-average swung `0.33→1.60`; corroborated by the re-banked div-free deliverable. So the `0.65` was a TRANSIENT ARTIFACT, not a converged charge fraction. The converged answer is that a pressurized bulk-only cage does NOT leak an exterior compression monopole (a clean pressure-release cavity, exterior deviatoric).

---

## §5 — LEG 3 (THE φ SCAN — the centerpiece) + collapse + percolation `[derived]`
`N` cages on a cubic sublattice, NON-OVERLAPPING interiors (`2·r_cage<s`; caps at `φ≈π/6` touching), deep rail `S_RAIL=1e-4`, `L=16`. Two routes to `φ` (A: vary `r_cage` at `s=4.5`; B: vary `s` at `r_cage=1.7`). `leg3_phi_scan`; `K_eff/K_0`:

| `φ` | bulk-only-cold A | bulk-only-cold B | symmetric-cold A | compressed A | expanded A |
|---|---|---|---|---|---|
| `0.075` | — | `0.473` | — | — | — |
| `0.101` | `0.239` | — | `0.129` | `0.180` | `0.259` |
| `0.165` | — | `0.239` | — | — | — |
| `0.188` | `0.189` | — | `0.083` | `0.130` | `0.212` |
| `0.278` | — | `0.164` | — | — | — |
| `0.315` | `0.200` | — | `0.077` | `0.136` | `0.223` |
| `0.441` | — | `0.263` | — | — | — |
| `0.489` (`φ_sf`) | `0.296` | — | `0.149` | `0.218` | `0.322` |

`G_eff/G_0` at `φ_sf` (bulk-only cold) `= 0.671`. **Shell-percolation on the geometry-correct axis is `f_incl = π/6 ≈ 0.524` — ROUTE-INDEPENDENT** (face-connection `2(r_cage+cage_w)=s`; `reference_forms` + `percolation_f_incl_geometric_route_independent`). ⚠ The shipped route-DEPENDENT geometric `φ_perc = 0.09 (A) / 0.13 (B)` on the interior-φ axis is itself the tell that φ is not the controlling variable (finding 3); the frozen "report it from the data, do not assume it" (prereg §4 Leg 3) was delivered GEOMETRIC — a disclosed forced-deviation (§7.11), the from-data departure-fit remains owed.

**★Reading (RE-GRADED on the coated geometry, §7.7).** `K_eff/K_0` **crashes `~5×`** to a MINIMUM `≈0.16–0.19` (both routes) around the crash band, then UPTICKS toward `φ_sf` as the cold interiors fill more of the load path near touching. **The two routes DO collapse on the geometry-correct coated-inclusion fraction `f_incl`** (`collapse_f_incl.max_rel = 0.280` bulk-only-cold ≤ 0.30, `0.269` expanded; `0.328` compressed and `0.396` symmetric remain over, consistent with the finite shell-overlap `f_incl > 1` above mid-scan — §7.7), whereas on the frozen interior-φ axis they fail (`0.515`/`0.742`): the frozen φ counted only the cold cage INTERIOR, but the mechanically active soft phase is the SHELL. **⚠ RETRACTED:** the shipped "far below the Voigt bound (`1−φ ≈ 0.7–0.9`) — the cages are NOT bypassed" graded the crash against the WRONG-fraction interior-φ bound. On the geometry-correct coated-inclusion pressure-release (parallel/Voigt) bound `1 − f_incl`, the measured `K_eff` sits **~AT the bound at the crash minimum** (`1 − f_incl = 0.192` vs measured `0.189` at `f_incl ≈ 0.81`; `reference_forms_coated_f_incl`): **the composite TRACKS the coated-inclusion PARALLEL bound** — the cold matrix carries the load AROUND the coated inclusions — it is NOT the "intermediate crash well below Voigt" the shipped doc claimed, and it does NOT reach the Reuss SERIES crash (that "not-Reuss" FLOOR reading is itself KUBC-conditional, §7.1). The symmetric wall crashes deeper (`0.05–0.15`) but also kills shear (wall-class artifact, fenced). The pre-stress axis shifts the whole curve by the #779 sign (compressed lower, expanded higher; the sign R6-rescoped, §7.9). **Scope (finding 1(d)):** `f_incl > 1` for the top two route-A points (`f_incl = 1.12, 1.51`) and route-B `s ≤ 4.2` — the coated shells overlap there, so those points are outside the clean single-inclusion regime and the `1 − f_incl` bound is not defined (reported `None`).

---

## §6 — LEG 4 (VERDICT) + anti-seduction + Leg 5 analytic form `[derived]`
`leg4_verdict`. `r_Z` at `φ_sf=0.489` per class × ρ-reading:

| class | `K_eff/K_0` | `r_Z (β=0)` | `r_Z (β=1)` | `r_Z (β=3)` | bin |
|---|---|---|---|---|---|
| **bulk-only, cold (HEADLINE)** | `0.296` | **`0.544`** | `0.664` | `0.855` | BIN 4 |
| bulk-only, compressed | `0.218` | `0.466` | `0.569` | `0.733` | BIN 4 |
| bulk-only, expanded | `0.322` | `0.568` | `0.693` | `0.892` | BIN 4 |
| symmetric, cold | `0.149` | `0.386` | `0.471` | `0.606` | BIN 4 |

| Leg | Frozen outcome (RE-DERIVED at review) | Decisive step |
|---|---|---|
| **0 — validation** | 3 FIREABLE PASS (internal `rel=0.038`; STOP opposite-sign; size `gap=0.014`) + Lamé deliverable PASS; amp = IDENTITY (not fireable); frozen rate gate FAILED-as-written `0.133>0.10` carried by CG-tol substitute; **frozen φ-collapse FAIL `0.515` — but `f_incl` collapse PASS `0.280` (headline)** | honest gate recount §7.8; collapse RE-AXED §7.7 |
| **1 — single-cage** | pre-stress sign (compressed`<`cold`<`expanded); symmetric lowest | the #779 Leg-C sign (R6-rescoped §3/§7.9); dilute magnitude |
| **2 — Lamé gate** | FROZEN DELIVERABLE ext/**interior** `∇·u = 0.036 → 0 ≤ 0.10` **PASS**; frozen rel(ii) `0.626` FAILS (near-zero denom) | Lamé re-banked §7.6; #770's `0.65` = transient (metric-independent) |
| **3 — φ scan** | `K_eff/K_0` crash `~5×` to `~0.17` min; tracks the coated-inclusion PARALLEL bound `1−f_incl` (`0.192` vs `0.189`); routes **COLLAPSE on `f_incl`** (`0.280`) | crash RE-GRADED §7.7; `f_incl` is route-controlling |
| **4 — verdict** | `r_Z=0.544` at `φ_sf`; straddles `0.5` across scan (`min~0.40`) + pre-stress (compressed `0.466`; on `f_incl`: cold/exp = BIN-2 vs comp = BIN-4) | **BIN-4 via trigger (iii) the `r_Z` straddle (collapse PASSES on the correct variable)** |

**★Overall frozen-bin verdict (RE-DERIVED, §7.7): BIN 4 — REGIME/UNDETERMINED.** The instrument is validated (3 fireable gates + Lamé deliverable). Trigger (i) — the frozen φ-collapse fail (`0.515`) — does NOT fire on the geometry-correct variable: on `f_incl` the headline route COLLAPSES (`0.280 ≤ 0.30`), so `K_eff` IS constitutively measured. BIN-4 SURVIVES via trigger (iii) alone: `r_Z` straddles the `0.5` MACRO-CAGE/MATCHED edge across the pre-stress class (`compressed 0.466` macro-side; on `f_incl` cold/expanded = BIN-2-MATCHED vs compressed = BIN-4 — a verdict-controlling cross-class flip) AND across the φ scan (`min~0.40`), compounded by the KUBC-conditional matched-side (§7.1) + Fork ρ. The BASIS is thus "constitutively MEASURED; undetermined at the `r_Z` bin edge," NOT "not lattice-decidable at feasible box."

**★Anti-seduction fence check (both ways; the #770 lesson NAMED).** (i) The #761→#767→#770 kill arc WANTS a matched/rigid confirm — REFUSED: a real `~5×` `K` softening exists and `compressed r_Z ≈ 0.466` dips into macro-cage territory. (ii) The Reading-B / walk reframe WANTS a macro-cage crash — REFUSED: on the correct coated fraction the composite TRACKS the parallel (bypassing) bound (`0.189` vs `1−f_incl=0.192`), NOT the Reuss series crash (`→0`), and `r_Z` straddles `0.5`. **The honest landing is the reopened aggregation gap at the constitutive level, banked in NEITHER direction** — exactly what the fence protects. Every number here is read from `rve_aggregation_bench_results.json` (shipped) via the deterministic driver (shipped); NO prose-string conclusion.

**★Leg 5 (the analytic FORM, `..._derivation.md`).** The coated-sphere-with-soft-coating morphology crashes toward Reuss ONLY when the soft shells percolate AND cut the matrix (§2); below shell-percolation the matrix percolates (Voigt); the dilute slope is finite (matrix shear props isolated cavities, §3). The lattice reaches shell-percolation (`f_incl = π/6 ≈ 0.524`, route-independent) and confirms the departure from the dilute Voigt slope (`~5×` crash), but the measured crash tracks the coated-inclusion PARALLEL bound `1−f_incl`, i.e. it does NOT deepen to the Reuss SERIES crash on the lattice `f_incl` band — and whether it would at true nuclear `φ→1` (lattice `φ_max≈0.49<1`; carried by #770's fully-railed homogeneous `c_P→0` result + the Wood form, DECLARED un-validated scope) is exactly the KUBC-conditional residual the SUBC/periodic lower-bound bracket (§8.2) would resolve. The macro-cage-vs-matched class is thus MEASURED-but-undetermined at the bin edge — BIN 4.

---

## §7 — Disclosed deviations (the now-standard §-deviations pattern)
- **§7.0 Determinism CONFIRMED.** Two independent full driver runs are **BIT-IDENTICAL** (`diff -q` clean; `DETERMINISM: BIT-IDENTICAL`). Only RNG = `run_c2_speeds(seed=1)` + `omega_max_cold(seed=0)` (both fixed); the static CG (init `u_I=0`) and the dynamic ramp carry no RNG.
- **§7.1 KUBC bound character (Fork B, frozen honest scope; bound-DIRECTION caveat inlined, finding 7).** The kinematic uniform boundary condition gives a STIFF (upper-bound-class) modulus; the interior-`L/2`-core energy reduces but does not eliminate the boundary-layer bias. Per the frozen §3 disclosure: **only the CRASH-SIDE content is bound-robust** — the observed `~5×` crash (the true modulus, bounded above by KUBC, crashes at least as hard) and "the composite tracks the coated-inclusion parallel bound." **The "not-Reuss / does NOT reach `K_eff → 0`" FLOOR claim is NOT bound-robust:** an UPPER bound cannot support a floor, so "plateaus at the intermediate, not a Reuss crash" is KUBC-conditional and, if anything, leans toward macro-cage (the true `K_eff` could be LOWER). Likewise in trigger (ii)/(iii), the MATCHED-side legs (`cold r_Z = 0.544`, `expanded 0.568`) are `√(KUBC upper bound)` hence UPPER bounds — consistent with the true `r_Z` being uniformly `≤ 0.5` (macro-side); **only `compressed r_Z = 0.466 < 0.5` is bound-robust.** The prereg's own frozen Fork B ("a HOLD/RISE is KUBC-conditional") therefore applies INSIDE the BIN-4 straddle: the crash-side is the only bound-robust content, and the resolver is the owed SUBC/periodic LOWER-bound bracket (§8.2), NOT run here.
  - **⚠ BASIS CORRECTION (2026-08-01) — routed in from merged #802, which RAN the owed SUBC/periodic bracket this bullet defers. The sentence above — `only compressed r_Z = 0.466 < 0.5 is bound-robust` — does not hold; body preserved per Rule 12.** That `0.466` is `√(R_KUBC_core)`, the **CORE estimator**, which the #802 lane's frozen prereg [`2026-07-28_subc-kubc-bracket_prereg-FROZEN.md`](2026-07-28_subc-kubc-bracket_prereg-FROZEN.md) §2.3 states `carries NO bound status: the Hill/Huet ordering theorem is about the whole-cell apparent modulus, and a windowed sub-region energy is not an apparent modulus of anything`. #802 reproduces this doc's `K_eff/K_0 = 0.2176075097402979` **bit-for-bit** and reads the same configuration on the bound-carrying **WHOLE-CELL** apparent modulus — the quantity the Hill/Huet kinematic-uniform theorem actually bounds from above — at **`r_Z = 0.6098`, above `0.5`**. Across this doc's four `φ_sf` legs, **`0`** read macro-side on the whole-cell measure (`2` do on the core estimator), the core estimator sitting BELOW the whole-cell value at every leg — the direction that manufactures a macro-side reading out of a measure that cannot support one. **So this doc has no bound-robust macro-side `r_Z` reading**: the `0.466` leg is estimator-conditional, not bound-robust. The crash-side content of this bullet (the `~5×` crash is bound-robust; the not-Reuss FLOOR claim is not) is **unaffected** — it concerns `K_eff`, not the `r_Z` bin edge. **★BIN-4's LABEL STANDS** (removing a macro-side leg makes the `r_Z` axis MORE undetermined, not less); the straddle survives only as a **boundary-condition artefact** via #802's SUBC LOWER bound — see the fuller note at `:12`. Provenance: merged #802 §8 consequence 7 + docket [`2026-07-28-subc-kubc-bracket.md`](../_orchestration/docket-entries/2026-07-28-subc-kubc-bracket.md) F5/F8; numbers ship in `research/drivers/subc_kubc_bracket_results.json` under `F5_782_bound_robustness_crosscheck_NOT_FROZEN`. **Second routed basis correction to this doc** (first: the ρ-flags audit, [`2026-07-21-relative-offset-principle.md:6`](../_orchestration/docket-entries/2026-07-21-relative-offset-principle.md)).
- **§7.2 Static-limit / rate gate — frozen gate FAILED-as-written, carried by a POST-FREEZE substitute (corrected, findings 2/9/11).** The frozen §4 Leg-0 rate check paired a static CG solve with a ramped dynamic relaxation at tol `≤ 0.10`. Run at the feasible shallow rail (`0.03`) it **FAILED as written** — `rate_spread = 0.133 > 0.10` (JSON `dynamic_ramp_shallow_rail_0p03.pass = false`; `static_vs_dynamic_gap = 0.105 > 0.10`) — because at DEEP rail (`S_RAIL=1e-4`) the `~100×`-slower shell modes need impractically many explicit hold steps and under-settle by `13%`. The reported top-level PASS uses a **POST-FREEZE CG-tolerance-independence criterion** (`spread=0.0000 ≤ 0.02` across 4 decades) — a legitimate static-limit basis on the Ax3-lossless-reactive substrate (the linear solve is unique + tolerance-invariant ⇒ no hysteresis) but a SUBSTITUTED observable, **labeled post-freeze-derived, NOT the frozen one.** ⚠ CORRECTION: the prior "criterion NOT relaxed post-hoc" was inaccurate — the frozen criterion did FAIL as written; it was SUBSTITUTED (the observable changed from dynamic-ramp to solver-tolerance-invariance), not merely un-relaxed. The verdict-class `K_eff` numbers are static-CG and carry NONE of the `13%` residual (K2(e) resolved in the bench's favor).
- **§7.3 Overlap regime excluded (frozen grid honesty).** The discrete nearest-center shell model degrades when cage INTERIORS overlap (`2·r_cage>s`) — an early run's high-`φ` uptick past touching was that artifact; the frozen scan is restricted to NON-OVERLAPPING interiors (caps at `φ≈π/6≈0.52`, near-space-filling touching). The true nuclear `φ→1` limit is analytic (Leg 5 + #770), NOT lattice-sampled — disclosed as the load-bearing scope of the BIN-4 verdict (parallels #775's regime gap).
- **§7.4 ρ-ontology (Fork ρ) NOT decided.** `r_Z` is reported under both ρ readings; the ontology (is trapped-energy mass acoustic inertia?) is routed to Grant. The verdict (BIN-4) is invariant across `β` (all land BIN-4 via the collapse fail), so Fork ρ does not control the bin — but it DOES control where in `[0.39, 0.89]` `r_Z` sits, so it is verdict-relevant for any future collapse-passing measurement.
- **§7.5 Single-cage Leg-1 magnitude is dilute.** One cage in the `L=16` `L/2` core cube gives weak ratios (`0.82–0.95`); the SIGN structure (pre-stress + wall-class) is the deliverable, the magnitude is carried by the aggregation (Leg 3). Disclosed.
- **§7.6 ★Lamé gate false-"Frozen" provenance (finding 0/10, CRITICAL — OWNED).** The shipped driver silently substituted an ABSOLUTE two-shell agreement `|ext1−ext2| ≤ 0.10` for the frozen relative shell-agreement (ii) `|Δ|/mean ≤ 0.25`, **mislabeled it "Frozen:"** in the code comment (a criterion invented post-freeze — the #770-fabricated-string class), and normalized the exterior ratio by the WALL shell, not the interior. As frozen, criterion (ii) FAILS all three rows (`0.626/0.585/0.568` — a genuine near-zero-denominator pathology on two `~0` exterior ratios). ★REPAIR: (a) the driver comment is corrected (drops "Frozen:"; the absolute agreement is now labeled a REVIEW-REPAIR conditioning diagnostic); (b) the swap + the pathology are disclosed here; (c) **the Lamé PASS is RE-BANKED on the FROZEN DELIVERABLE** — the exterior `∇·u`/**interior** `∇·u` ratio → 0, tol `≤ 0.10` — which passes independently (`0.036/0.019/0.007`), so the div-free-exterior physics and the #770-`0.65`-transient adjudication re-bank at that grade (the latter on its metric-INDEPENDENT static-⇒-no-transient basis); (d) the frozen relative criterion (ii) is reported as fails-as-written. The Lamé `converged`/`lame_pass` flags are UNCHANGED (both `True`) — the physics was never at issue, only the false provenance.
- **§7.7 ★Packing variable re-axis + coated brackets + larger-box retraction (findings 1/5/6/8).** The frozen collapse used `φ = (4/3π r_cage³)/s³`, which counts the COLD cage INTERIOR; the mechanically active soft phase is the SHELL. ★REPAIR (KEEP-BOTH, per the standing pattern): the geometry-correct coated-inclusion fraction `f_incl = (4/3π(r_cage+cage_w)³)/s³` is added as a NEW collapse axis ALONGSIDE the legacy φ axis. On `f_incl` the HEADLINE bulk-only-cold route COLLAPSES (`max_rel = 0.280 ≤ 0.30`; expanded `0.269`; compressed `0.328` and symmetric `0.396` remain over, consistent with the `f_incl > 1` shell overlap above mid-scan). The Voigt/Reuss brackets are redone for the coated geometry (`voigt_coated_ref = 1 − f_incl`): the measured `K_eff` sits ~AT the coated-inclusion pressure-release parallel bound at the crash minimum (`0.192` vs `0.189`), so the crash characterization is re-graded from "intermediate, well below Voigt, cages NOT bypassed" (wrong-fraction) to "TRACKS the coated-inclusion parallel/bypassing bound." **The "needs the larger-box / periodic instrument #775-flagged-infeasible" routing is RETRACTED:** the re-axis was ZERO compute, the collapse is decided at `L=16` on the correct variable, and route-collapse failing on the wrong axis is not a confirmation that a periodic instrument is infeasible — the resolvers (SUBC/periodic lower-bound at `L=16`, same cost class) were never costed. See §8.2.
- **§7.8 ★Gate accounting recount (findings 2/9/11).** The shipped "5/6 frozen gates PASS / instrument VALIDATED" over-counted. Honest count: **3 FIREABLE discriminating gates PASS** (internal `0.038`, STOP-gate sign, RVE-size `0.014`) + the **Lamé deliverable PASS** (§7.6); **1 IDENTITY** (amplitude-linearity, unfireable on a linear-by-construction model — `spread ≈ 1e-15`); **1 frozen rate gate FAILED-as-written** (`0.133 > 0.10`) carried by a disclosed post-freeze CG-tolerance substitute (§7.2); the frozen φ-collapse FAILED but the geometry-correct `f_incl` collapse PASSES for the headline (§7.7). The CG-independence argument is KEPT as the honest static-limit basis, labeled post-freeze-derived.
- **§7.9 ★#779 temporal-currency refresh (finding 12).** The prereg's `[branch:#779] re-verified at ship time` tag was accurate at prereg-freeze (`08:59`, pre-R6) and the prereg is byte-frozen. But #779's R6 review-repair (`origin/research/boundary-strain-amplitude`, `09:10`, BEFORE this result's `09:44`) re-scoped the scalar DC-sign axis this bench's §3/Leg-1 relays: the canon-default vessel state is DC displacement = 0 with an ANISOTROPIC hoop-stiffen/radial-soften remap, NOT a scalar soften/stiffen keyed on a static DC sign ("the wrong question"). The pre-stress SIGN this bench reports is thus orientation-keyed anisotropy, not a scalar DC profile-member; Fork P should be re-posed to Grant in the R6 vessel-state frame. Bin-invariant (BIN-4 rests on trigger (iii), which the compressed/cold/expanded scan carries regardless). Merge #779 before/with #782 so the cited Leg-C section exists on `main` in its R6-repaired form.
- **§7.10 ★STOP-gate control magnitude (finding 9).** The rigid control ships at `k_a × 100`, not the frozen `k_a → k_a/S_RAIL` (`= 1e4×`); chosen for conditioning. Sign-invariant (rigid stiffens `K > 1` at either magnitude; "the sign is the gate"), so the STOP-gate verdict is unaffected. The stale driver docstring (`"k_a/s_rail"`, contradicting the code) is corrected. Disclosed.
- **§7.11 ★Percolation from-data + CG cap (findings 3/4/13).** (a) The frozen "report the percolation transition from the data, do not assume it" (prereg §4 Leg 3) was delivered GEOMETRIC — a disclosed forced-deviation; the route-dependence of the interior-φ value (`0.09` vs `0.13`) is itself the tell that φ is not controlling, and the route-INDEPENDENT geometric shell-percolation on the correct axis is `f_incl = π/6 ≈ 0.524`. The from-data departure-fit remains owed. (b) The CG iteration cap shipped at `4000` vs the frozen `2000` (undisclosed frozen-grid deviation); reverted to the frozen `2000` — immaterial (max iters observed anywhere = `614`, the cap never bound; reruns bit-identical).

---

## §8 — Calibration-vs-derived ledger + owed follow-ons
### §8.1 Ledger (`consistency-vs-emergence`)
| Quantity | FORM | VALUE | Class |
|---|---|---|---|
| `K_eff/K_0`, `G_eff/G_0`, `Z_eff/Z_0` | `[derived]` (lattice static homogenization) | dimensionless ratios | MANIFESTATION (lattice) |
| internal `(K+4/3G)/G` vs `(c_P/c_S)²` | `[derived]` (KUBC vs Bloch) | `3.16` vs `3.286` | CONSISTENCY (`K=2G` GR-imported) |
| `ρ*=9.77337` | `[import]` (`ν_Hill=2/7`, `ave.core.constants.N_NU`) | reused from cce, not re-hardcoded | CONSISTENCY (GR-imported `K=2G`) |
| Voigt/Reuss(Wood) reference FORMS (interior-φ legacy + **coated `f_incl`, §7.7**) | `[derived]` (textbook effective-medium) | the frozen forms the data tests; coated `1−f_incl` is the geometry-correct bound | the FORM (not fit) |
| pre-stress remap `T/ℓ~k_a·ε_pre` | `[canon]`-form (`axiom-register.md:193`) × `[import]` yield strain | sign `ε_pre`-independent (R6-rescoped as orientation-keyed, §7.9) | CONSISTENCY (remap at capped tension) |
| the `~5×` crash TRACKS the coated-inclusion parallel bound (`0.189≈1−f_incl=0.192`) + `r_Z≈0.40–0.57` straddle | `[derived]` (lattice; `f_incl` collapse PASSES `0.280`) | dimensionless | MEASURED-but-UNDETERMINED at the `r_Z` bin edge (BIN-4) |

No emergence-class claim headlined. `α`-CLEAN (the discriminator is a DIMENSIONLESS impedance ratio — the α-circularity lesson: the chord, if any, must be a dimensionless ratio). The deliverable is the BIN-4 regime verdict + the validated instrument + the banked intermediate-crash characterization.

### §8.2 Owed follow-ons (fenced; NOT executed here — Rule 12; slot NOT refilled)
1. **⚠ RETRACTED framing (§7.7, finding 8): the decisive verdict does NOT need a larger box.** The shipped "needs the larger-box / PERIODIC instrument #775-flagged-infeasible — now CONFIRMED at the constitutive level (route-collapse fails at `L=16`)" is withdrawn: the collapse is DECIDED at `L=16` on the geometry-correct `f_incl` axis (zero compute), and route-collapse failing on the wrong (interior-φ) axis is NOT a demonstration that a periodic instrument is infeasible. The FEASIBLE, same-cost-class resolvers are items 2 (SUBC/periodic lower bound at `L=16`) + the `f_incl` re-axis (already shipped) + item 3 (Fork ρ/P). NO strengthening/revert landed on this lane's basis (BIN-4 grounds neither).
2. **SUBC / periodic lower-bound cross-check (Fork B) — the decisive owed resolver, FEASIBLE at `L=16`.** The KUBC upper-bound crash-side is bound-robust; the SUBC/Reuss lower bound (a same-cost matrix-free CG at `L=16` with the free-traction boundary, OR a periodic-BC homogenization) would BRACKET the true `K_eff` between the KUBC ceiling and the SUBC floor. If the bracket at `φ_sf`/`f_incl_sf` lies wholly below `r_Z = 0.5` (macro-cage) or wholly above (matched), the class RESOLVES — this is what the "not-Reuss floor is KUBC-conditional" (§7.1) caveat turns on. Owed before any strengthening.
3. **The ρ-ontology (Fork ρ)** + the **pre-stress sign (Fork P)** are Grant-adjudicated substrate-ontology inputs (is trapped-energy mass acoustic inertia? is the electron core net-compressed?) — surfaced with file:paths + verbatim content, NOT reframed. They set where `r_Z` sits within the straddle band.
4. **The RADIATIVE consequence** (star-scale Lloyd cancellation at `k·R_star`) is stage-2 radial-solver territory — ROUTED to Grant, NOT run (the bench settles the CONSTITUTIVE half only; per the frozen scope fence, IF a future collapse-passing measurement lands MACRO-CAGE, THAT feeds the radial-solver stage-2).

---

> **Result-doc provenance.** Fired by Grant 2026-07-21 (`"kick off the rve aggregation bench"` `[sic]`). Frozen prereg committed + pushed ALONE first (COMMIT 1); analytic Leg 5 (`..._derivation.md`); driver `research/drivers/rve_aggregation_bench.py` (+ `_results.json`, white figure), reuses #770/#775 `constituent_cage_ensemble.py` primitives, `ave.core.*` read-only, engine byte-untouched, **reruns bit-identical** (§7.0). All `[canon]` citations content-verified two-method at base `3d07ceeb`: `electron-bh-isomorphism.md:26`, `master-equation.md:20/107`, `axiom-register.md:193`, `ave.core.constants.N_NU`. **Attribution:** Grant-verbatim fire + the CONSTITUTIVE reframe (walked, ratified); the KUBC static-homogenization primitive, the collapse-check-baked-in protocol (watch #7), the STOP-gate-as-composite-response-sign, the Lamé gate, and the two-ρ-reading fork are this lane's. **★Verdict (RE-DERIVED at review): BIN 4 — REGIME/UNDETERMINED — the macro-cage-vs-matched class is constitutively MEASURED but undetermined at the `r_Z = 0.5` bin edge** (a stronger state than the shipped "not lattice-decidable"). The instrument is validated on 3 fireable gates + the Lamé deliverable (§7.6/§7.8); the frozen φ-collapse fail (`0.515`) was a mis-chosen-axis artifact — on the geometry-correct `f_incl` the headline route COLLAPSES (`0.280`), so trigger (i) does NOT fire and `K_eff` IS measured; BIN-4 SURVIVES via trigger (iii), the `r_Z` straddle (`compressed 0.466` macro-side vs `cold 0.544` matched-side; KUBC-conditional matched-side + Fork ρ). A real `~5×` `K`-crash is banked, but it TRACKS the coated-inclusion parallel bound (`0.189≈1−f_incl`), NOT a Reuss crash. The "needs larger box / periodic-infeasible" routing is RETRACTED (§7.7) — the resolver is the feasible owed SUBC/periodic lower-bound bracket + Fork ρ/P. The #767/#770 aggregation fork REMAINS OPEN. Consequence routed; no leaf touched; no rescue minted. Companions: the frozen prereg, the analytic Leg 5 (`..._derivation.md`), merged **#775** (`research/2026-07-20_deep-rail-kscaling_result.md` §8.2), merged **#770** (`research/2026-07-20_constituent-cage-ensemble_result.md` §8.2), the walk RECORD (§6 seed 3), **#779** (`research/2026-07-21_boundary-strain-amplitude_result.md` §3 `[branch:#779]`), the collapse-check watch (`_orchestration/skill-candidates-watch.md` #7), the port register P9/Q1, and the docket continuation (`### ENTRY 2026-07-21-rve-aggregation-bench`).
