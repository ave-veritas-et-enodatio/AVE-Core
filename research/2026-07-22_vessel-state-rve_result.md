# Anisotropic Vessel-State RVE Bench — RESULT (walk-1 verdict run)

Prereg-file: research/2026-07-22_vessel-state-rve_prereg-FROZEN.md

**Date:** 2026-07-22
**Class:** DERIVATION + lattice-derived research-driver (research-doc; forms derived, values dimensionless/geometric; **mints no `clm-`/`def-`; propagates to no KB/tex leaf**). This is the **bench RUN** of the merged, Grant-ratified REVISION-3 frozen prereg — the "walk-1 instrument": does the GROWN vessel-state cage read HARD / SOFT / NULL / DISCORDANT / MARGINAL / UNDETERMINED?
**Driver:** `research/drivers/vessel_state_rve.py` (+ `research/drivers/vessel_state_rve_results.json`, `research/drivers/vessel_state_rve.png`). Figure: `research/drivers/vessel_state_rve_figure.py`.
**Engine:** `src/ave` **BYTE-UNTOUCHED** (imports read-only); Rule-14 reuse of the merged #782 KUBC static-homogenization scaffold (`research/drivers/rve_aggregation_bench.py` + `constituent_cage_ensemble.py`); the NEW piece is the STATE-DEPENDENT geometric-stiffness operator `k_shear,eff(u) = k_s + k_a·ε_axial(u)` (`axiom-register.md:193`) + the frozen Newton/self-consistent solve.
**Every number below is read from the shipped `vessel_state_rve_results.json` via the deterministic driver — NO prose-string conclusions (the #770 lesson NAMED).** All `[canon]` inputs content-verified at base HEAD `f050e33b`: `axiom-register.md:193` (the load-response sign rule), `electron-bh-isomorphism.md:26`, `master-equation.md:20`, `ave.core.constants.N_NU`. Deterministic (`run_c2_speeds seed=1`; no per-step RNG in the statics); total runtime `1049 s`.

> **★REVISION (2026-07-22, post-review — PR#796 adversarial-review repairs, PRE-MERGE, disclosed).** This result doc is REVISED from its first-run form to answer the PR#796 review (15 confirmed findings). The re-bank is results-repair-under-review, pre-merge, disclosed — nothing was banked to canon (the frozen prereg is byte-untouched; the driver/JSON were repaired and re-run). The load-bearing corrections: **(F2)** Protocol E was re-implemented per the frozen §4 sub-requirements (PML/window exclusion, density-peak top-K sampling, the frozen reflection-free window, a FROZEN cross-correlation arrival criterion, a REAL reactance-pair flag) and RUN at BOTH `L∈{32,48}` — and the repaired extraction is **METHOD-UNSTABLE**: it does NOT return a decidable `ρ_eff` at either box size (§6). **🔴 The first-run headline — "`ρ_eff/ρ_0 = 0.916` MEASURED ⇒ `r_Z = 0.520`", and with it every framing in which this bench measures BOTH Z factors — is RETRACTED.** The shipped `r_Z` now rides the STRUCTURAL anchor, i.e. `ρ_eff/ρ_0 ≡ 1` **assumed, not measured**. **(F6)** the "hoop-vs-radial CANCELLATION" mechanism headline is **RETRACTED** (the cheapest ablation refutes it — below). **(F11)** the `yield_saturated` NULL is re-graded from an *extrapolated* to an *OBSERVED* buckling-onset, instrument-scoped. **(F1/F4/F5/F7/F9/F10/F14)** the Leg-0 (iii) claim narrowed to fireability-in-extremis, the calibration string regenerated to computed truth, the determinism gate run as two independent FULL driver runs, the σ-knob ledgered + tested, deviations §8 completed, the F7 design-lesson recorded. **The headline BIN LABEL is unchanged (UNDETERMINED); its BASIS is not** — the verdict now has one measured Z factor and one assumed one, so it is UNDETERMINED **with the ρ half UNRESOLVED**, which is a materially weaker result than the first run claimed. No old bin is defended: the bin is re-derived from the frozen table on the repaired numbers.

**★THE CARVE (mandatory disclosure — every output carries both strings, prereg §1; both present in JSON `provenance`):**
- `grade-frame: Eulerian, imposed (not self-bound; the engine hosts no field-generated co-moving grade)`
- `source imposed (radiation-stress surrogate body-force ∝⟨A²⟩); stress state emergent through the nonlinear force balance`

**★THE S(A(u)) DECISION honoured (prereg §0):** the Op14 saturation grade `S(A)` is IMPOSED (Eulerian, static); the ONLY state-dependence in the operator is the LIVE geometric-tension term `T(u)/ℓ = k_a·ε_axial(u)`. Scope-limit disclosed: `kernel-knee physics (the Op14 S(A) saturation marginality) is NOT hosted`; the buckling/marginality physics is carried by the `T(u)` geometric term per #779.

---

## ★BOTTOM LINE

The walk-1 instrument is **NOT VOID** — `gate_fireability_selftest_pass = True`, `leg0_VOID = False`, `all_validation_pass = True`. **Scope that word carefully:** the LEG-0 gates validate the STATIC homogenization channel (the `K` side) and the ToF on a UNIFORM medium; **they do NOT validate the ToF ρ-channel on the GROWN medium, which is exactly where it fails (§6).** "Validated" here means the `K`-side verdict is trustworthy and the self-tests are not a checklist — it does not extend to the ρ-side. The grown vessel-state cage reads:

> ## HEADLINE BIN = **UNDETERMINED** (cell `(i) × L1 × Z_str`) — **with the ρ half of the verdict UNRESOLVED**
>
> `K_ratio_lift = 0.997` (band L1: `< 1.2`), amplitude gate = **outcome (i) CLEAN** (`A_sign = 0.0020`, `amp_spread± = 0.00097 / 0.00113`, converged + robust). `r_Z = 0.5436` is the **STRUCTURAL ANCHOR** `√(K_tan/K_0)` — it carries `ρ_eff/ρ_0 ≡ 1` **by assumption, not by measurement** (band Z_str: `|r_Z − 0.5| = 0.044 ≤ δ_rZ = 0.05`). Headline arm = `fixed_budget` (yield_saturated has **no stable equilibrium** — it BUCKLES before yield).
>
> **★PROTOCOL E, AS FROZEN, DOES NOT DELIVER A DECIDABLE `ρ` ON THIS INSTRUMENT.** The repaired extraction is **METHOD-UNSTABLE**: `r_Z` spans **`[0.1005, 0.5436]` at `L=32`** and **`[0.1975, 0.5436]` at `L=48`** (JSON `protocol_E.by_L.{32,48}.r_Z_method_band`), and at BOTH sizes that band spans `bins_spanned_by_band = ["Z_lo", "Z_str"]` — it crosses the SOFT/UNDETERMINED boundary. `bins_by_L_from_launch_cP = {"32": "SOFT", "48": "SOFT"}` while the structural anchor reads `Z_str`. `method_indeterminate_any_L = True`; `size_flip_L32_vs_L48 = False` (the two sizes agree with each other on the launch@`c_P` arm and disagree with the anchor). The SC-coherent arm was run at `L=32` only, so the two method bands are not like-for-like — see §6.
>
> **⇒ THE WALK-1 VERDICT IS `UNDETERMINED` WITH ITS ρ HALF UNRESOLVED — NOT "MEASURED".** The frozen §4 deliverable (Frozen: *"`r_Z = √((K_eff/K_0)·(ρ_eff/ρ_0))` is MEASURED, not assumed — the #782 discriminator with a REAL structural ρ term"*) is **NOT MET**. Of the two Z factors, ONE is measured (`K_tan/K_0 = 0.29548`) and the OTHER is not (`ρ_eff/ρ_0` set `≡ 1`). **🔴 RETRACTED: the first-run "`ρ_eff/ρ_0 = 0.916` MEASURED ⇒ `r_Z = 0.520`" headline, and every framing in which this bench measures both Z factors / supplies "a REAL structural ρ term".** On the ρ axis this bench delivers exactly what merged #782 already delivered — `r_Z(β=0) = √K_ratio` at `ρ ≡ 1` — and nothing more.

**★THE FALLBACK IS A CHOICE — graded as one, not presented as forced by the data.** On the repaired numbers two dispositions were available, and the data selects neither:

- **(a) SHIPPED — walk the frozen bin table on the STRUCTURAL anchor** (`ρ_eff/ρ_0 ≡ 1`, `r_Z = 0.5436`, `Z_str`) ⇒ **UNDETERMINED**. Cost: the ρ-side is *assumed*, and the bench's stated advance over merged #782 (a measured structural ρ) is **not delivered**.
- **(b) NOT SHIPPED — bank `SOFT` from the launch@`c_P` arm alone** (`bins_by_L_from_launch_cP` = SOFT at BOTH `L`, so it is not a size artifact). Cost: it would ride ONE unvalidated extraction as if it were a measurement — the arm whose grown `c_eff = 0.7005 > c_0 = 0.5133` is **unphysical** (a medium at `K_tan/K_0 = 0.2955` cannot outrun its own stiffer cold reference) and whose self-consistent continuation **DIVERGES** (`0.519 → 0.701 → 1.499`). SOFT is also the outcome this lane has the most incentive to bank — it is the one that would re-open the Reading-B route at the grown-vessel constitutive level (frozen §6 BIN SOFT) — which is precisely why a single unvalidated method is not allowed to carry it.

**(a) is shipped because a DISCLOSED ASSUMPTION beats an UNVALIDATED MEASUREMENT — not because the instrument decided.** Neither disposition is a physics result about the vessel; both are instrument dispositions. **The method-instability is therefore ROUTED TO GRANT as an owed resolver ALONGSIDE the SUBC bracket** (§9), not closed here.

**The physics headline (repaired).** At the small-signal-tangent regime the regime-ruling names (`.OP` to the grown equilibrium, then a second-difference tangent probe `≪` every nonlinear scale), the grown pressure-vessel stress state does **NOT** compose into a load-bearing bridge (HARD) NOR a pressure-release bottleneck (SOFT) at the aggregation level: **`K_ratio_lift = 0.997` — the grown vessel's small-signal tangent modulus is indistinguishable from its isotropic control** (`K_tan/K_0 = 0.29548` grown vs control `0.29637`). **🔴 The mechanism previously headlined — a hoop-stiffen-vs-radial-soften CANCELLATION — is REFUTED** by the cheapest ablation (§7, F6): with a core-restricted energy the "radial-soften" pattern does **not** soften the core tangent (it RAISES it `+0.43 %`); both named components STIFFEN (`cancellation_mechanism_supported = False`). **No replacement mechanism is banked** — the driver's positive attribution ("provenance/live-operator-driven") CONTRADICTS its own painted-ablation rule on the shipped numbers (GROWN ≈ PAINTED within `~1 %` ⇒ "anisotropy-driven"), so the positive attribution is withdrawn and surfaced for adjudication (§7). What this bench establishes is the NEGATIVE: the null lift is not a cancellation, and its positive mechanism is UNATTRIBUTED here. This lands the SAME BIN as merged #782's painted-isotropic bench (UNDETERMINED), now confirmed on the GROWN anisotropic vessel — **and on the ρ axis it lands on the same ANCHOR too, so the ρ-side advance over #782 is not delivered.** The impedance `r_Z = 0.5436` is the structural anchor `√K_ratio` at `ρ ≡ 1` (straddling `0.5`); the Protocol E ToF is **method-unstable on these box sizes** — the cold reference validates against the Bloch speed (rel `1.1 %` at `L=32`) but the grown cage-array is a locally-resonant scatterer, so the ToF returns an **unphysical `c_eff > c_0`** (the softer grown medium appearing FASTER than cold) and the self-consistent continuation **DIVERGES** (`0.519 → 0.701 → 1.499`); the resulting `r_Z` band crosses the SOFT/UNDETERMINED boundary at BOTH sizes, so no decidable `ρ_eff` is extractable (§6). The yield-saturated (self-bound-adjacent) arm has **no stable equilibrium on this instrument** — the vessel BUCKLES at an OBSERVED onset `p0 = 0.062` (`min k_shear,eff = −0.00278 < 0`, `peak_A = 0.2156`, `yield_reached_before_buckling = False` at all three `A_yield_scale`), before peak strain reaches `A_yield` (the energize-LOCK-adjacent negative surfaced by the pre-test-physics-check) — **instrument-scoped to this source family, this affine radiation-stress remap surrogate and this cage geometry, NOT an engine-level unreachability claim.** **Consequences routed to Grant** (§9), now TWO: (1) UNDETERMINED at the `r_Z` straddle ⇒ the owed resolver is the feasible SUBC/periodic lower-bound bracket (#782 Fork B), NOT run here — and the ToF's failure REINFORCES that a static bound, not a ToF, is what decides it; (2) **the Protocol E method-instability itself** ⇒ the frozen §4 ρ-measurement is not executable as written on a grown cage-array at `L∈{32,48}`, which is a live instrument question the SUBC bracket does not answer.

---

## §1 — LEG 0: the gate-fireability self-tests (the mandatory VOID gate — §3B)

Frozen: `gate_fireability_selftest_pass = SELFTEST-(ii) fires (ii) AND SELFTEST-(iii) fires (iii), each with the correct convergence/robustness flags` → **True.** (JSON `leg0_selftests`.)

**SELFTEST-(ii) FIRES (ii)** — a deliberately under-converged solve forces the artifact AND is diagnosed as convergence-caused. At the FROZEN loose inner CG tol `1e-3`: `amp_spread± = 649.24 / 648.96` (> 0.05, fires the artifact), `A_sign = 0.0300` (≤ 0.10, symmetric drift) AND `all_scan_legs_converged = False` (diagnosed convergence-caused); at the tight tol `1e-10`: `amp_spread± = 0.00583 / 0.00427` (≤ 0.05 both signs, removed by tightening). The dual frozen assertion holds. **No calibration latitude was needed for (ii)** — the frozen `1e-3` was shipped UNCHANGED and forced the artifact (the calibration-disclosure string is regenerated to this computed truth — F4/F15).

**SELFTEST-(iii) FIRES (iii) — fireability-in-extremis (claim narrowed, F1).** A near-buckling grown state forces sign-asymmetric squeeze-softening. Gate returns `outcome (iii)`: `amp_spread+ = 568.74` (> 0.05), `A_sign = 568.77` (> 0.10), `K_tan_minus(ε_max) = 7484.3 < K_tan_plus(ε_max) = 4.43e6` (squeeze-softening direction), with `all_scan_legs_converged = True AND residual_tightening_robust = True`. **★HONEST SCOPE (F1 narrowing):** this firing meets the frozen (iii) acceptance LITERALLY (the one-sign clause), but it demonstrates **fireability-in-extremis** — a localized stretch-side curvature divergence at a tight near-buckling core cluster — NOT a threshold-scale aggregate squeeze-softening. The demonstrated dynamic range is `0.002 ↔ 568` with nothing between: the gate is shown to be capable of firing (iii), which is what §3B requires, but the fired magnitude is a divergence, not a graded aggregate signal. The (iii) self-test VALIDATES the gate is not a checklist; it does NOT establish threshold-scale detectability of graded compressive marginality.

**★DISCLOSED calibration (design-time numeric-confirmation run, banked here per §3B, made BEFORE any verdict arm).** The frozen ≥50 %-of-shell-band near-buckling target was NOT literally met — the Gaussian core source concentrates the near-buckling bonds into a tight CORE CLUSTER (achieved fraction in the calibrated shell band = `0.0156`; `min k_shear,eff = 0.0541` at the self-test operating point `p0 = 0.028`, σ = 2.5). Per the frozen §3B latitude (*"a justified equivalent may be substituted at calibration time IF it produces the same aggregate crossing — disclosed"*) the near-buckling core cluster IS that justified equivalent: it produces the frozen SELFTEST-(iii) acceptance (converged + robust outcome (iii), `A_sign = 568.77`). **Every calibration number above is read from JSON `leg0_selftests.calibration_disclosure` + `leg0_selftests.selftest_iii`** — the line-9 no-prose-numbers rule enforced on the doc's own calibration story (F1). The first-run version of this paragraph carried alternative-band and alternative-source figures that the driver never emitted; **they are DELETED, not re-derived** — the shipped record is the achieved fraction (`0.0156`), the operating point (`p0 = 0.028`, `σ = 2.5`, band `(0.0, 3.0)`, `n_band_bonds = 1350`), and the fired metrics.

---

## §2 — LEG 0: instrument validation (§7 standard gates) — all PASS

| gate | reading | verdict |
|---|---|---|
| uniform-medium NULL | `K_tan/K_0 = 1.0000`, `ρ_N = 0`, `r_Z = 1.0000` | PASS (r_Z→1, ρ_N→0 on a uniform cold medium) |
| determinism (two full runs, RAN) | two independent full driver runs (`1049 s` / `1090 s`, separate processes, separate output paths) → byte-identical timing-stripped results, `diff -q` **CLEAN**; identical `determinism_digest = 69968227e455137f…` | PASS — the FROZEN criterion itself, not a proxy (§8.8) |
| Lamé exterior gate | exterior `∇·u`/interior `∇·u` = `0.0358` ≤ 0.10 | PASS (carried from #782 §4 Leg 2) |
| RVE-size gap | `K_eff/K_0 = 0.280 (L12) / 0.296 (L16) / 0.294 (L20)`; `gap(L16,L20) = 0.0072` ≤ 0.15 | PASS (size-converged) |
| STOP-gate class | rail `K_eff/K_0 = 0.296` (< 1, SOFTENS) vs rigid `1.583` (> 1, STIFFENS) — opposite sign | PASS |
| Bloch cross-check (§8, F14) | cold ToF vs `run_c2_speeds`: `c_P` rel `= 0.011` ✓, `c_S` rel `= 0.188` ✓ (both ≤ 0.20, advisory) | PASS (the repaired ToF recovers the Bloch speeds on a UNIFORM medium) |
| cell-walk partition | 12 rows, exhaustive + disjoint | PASS (`assert_partition`) |

Instrument cross-check against merged #782: the cold bulk-only cage at `φ_sf = 0.489` reproduces #782's `K_eff/K_0 = 0.296` (isotropic control = `0.29637`) — the instrument agrees with the merged linear bench at the cold limit.

**★The Bloch cross-check (F14) is the load-bearing sanity check on the repaired Protocol E ToF:** the cold uncaged P/S pulse ToF speeds (the repaired `_pe_pulse_xcorr`, L=20) match the Bloch `c_P`/`c_S` from `run_c2_speeds` to `1.1 % / 18.8 %`. The ToF instrument is therefore SOUND on a uniform medium — which is exactly why its FAILURE on the grown cage-array scatterer (§6) is a physical finding about the medium, not a broken instrument.

---

## §3 — the grow step + the THREE numbered STOP criteria (§1)

Frozen: `grown_equilibrium_exists = grown_CG_converged AND grown_tension_nonzero AND grown_bonds_positive`.

**Headline arm (`fixed_budget`, `p_ref = 0.040` — a DISCLOSED `[engineering-choice]` knob, grade-frame Eulerian/imposed):** a stable grown vessel state EXISTS.
- `grown_CG_converged = True` (outer converged; nonlinear residual within `1e-8`).
- `grown_tension_nonzero = True` — Frozen: `grown_tension_nonzero: max_r |T(r)| ≥ 0.05·k_a·A_yield`; `max_r |T(r)| = 0.647 ≥ 0.489` (`= 0.05·k_a·A_yield`, `k_a = 9.77337`, `A_yield = 1.0`).
- `grown_bonds_positive = True` — Frozen: `min_bond k_shear,eff(u_0) > 0`; `min_bond k_shear,eff = 0.353 > 0` (no bond crossed buckling).
- ⇒ `grown_equilibrium_exists = True`. The imposed source grew a real residual pressure-vessel state: **hoop-tension** axial strain up to `ε ≈ +0.139` (peak `A`), **radial-compression** down to `ε ≈ −0.066` (`min k_shear,eff = 0.353`).

**T-definition disclosed** (deviation §8.1): `T` is the remap term `k_a·ε_axial` (the tension CONTRIBUTION the operator sees, `= T/ℓ` in the `axiom-register.md:193` grammar), not the un-normalized axial force `k_a·ε_axial·ℓ`.

---

## §4 — the verdict arms + the cell-walk bin (§6)

Frozen: `K_ratio_lift ≡ (K_tan/K_0)_grown / (K_tan/K_0)_isotropic-control` (at matched `φ_sf = 0.489`).

- amplitude gate (hydrostatic mode → `K_eff`) = **outcome (i)**: Frozen: `amp_spread ≤ 0.05 for BOTH signs` — `amp_spread± = 0.00097 / 0.00113`, `A_sign = 0.0020`, converged + robust ⇒ a clean small-signal tangent modulus. NOT (ii), NOT (iii), NOT the (iv)-anomaly.
- `K_tan/K_0 (grown) = 0.29548`; isotropic control `= 0.29637`; **`K_ratio_lift = 0.997`** (band L1: `< 1.2`).
- `G_tan/G_0 (grown) = 0.634` (shear, corroborative).
- `r_Z = 0.5436` — the **structural anchor** `√(K_tan/K_0)` with **`ρ_eff/ρ_0 ≡ 1` ASSUMED** (the long-λ `k→0` limit for uniform point masses), **not measured** → band **Z_str** (`|r_Z − 0.5| = 0.044 ≤ δ_rZ = 0.05`; straddles 0.5). **The Protocol E ToF ρ-measurement (§6) is METHOD-UNSTABLE and is not allowed to move the bin** (the driver's `rz_decider = structural_anchor (ToF method-indeterminate/size-flip)`) — a disclosed CHOICE, graded in §BOTTOM LINE, not a measurement outcome.
- **cell-walk bin = UNDETERMINED** (walked MECHANICALLY through the frozen 12-row table: gate (i) → `(L1, Z_str)` → UNDETERMINED). Data + code path: JSON `verdict.fixed_budget_headline`; `vessel_state_rve.py::cell_walk_bin` (+ `assert_partition` proof).

The FOUR headline bins are excluded by the frozen definitions: not HARD (`lift >= 1.5` fails — lift 0.997); not SOFT (`r_Z_grown < 0.5 − δ_rZ` fails **on the anchor** — 0.5436 not < 0.45); not NULL (`r_Z_grown > 0.5 + δ_rZ` fails — 0.5436 not > 0.55); not DISCORDANT (lift < 1.5); not MARGINAL (gate (i), not (iii)).

**★The SOFT exclusion is anchor-dependent — state it plainly.** Only the HARD / NULL / DISCORDANT / MARGINAL exclusions are method-robust (they fail on the lift or the gate, which are measured). **SOFT is excluded ONLY because the ρ-side was set to the anchor**: on the launch@`c_P` extraction the same frozen table returns `SOFT` at BOTH sizes (`bins_by_L_from_launch_cP = {"32": "SOFT", "48": "SOFT"}`). The verdict therefore sits in the UNDETERMINED zone because the ANCHOR impedance straddles `0.5` while the lift is null — and the ρ measurement that was frozen to decide between UNDETERMINED and SOFT **did not return a decidable number** (§6). The `SOFT`-vs-`UNDETERMINED` fork is OPEN, not closed by this run.

---

## §5 — C-V profile (§5) — REWIRED to the live geometric term

Frozen: `K(ε_bias) profile curve + its reconstruction read: shell POSITION, WIDTH, and ASYMMETRY`.
- shell POSITION = `0.00135`; shell WIDTH = `0.006` (squeeze `0.0042` / stretch `0.0018`).
- shell ASYMMETRY = `−0.400`. Frozen: `|ASYMMETRY| >= 0.15 confirms the anisotropic vessel state`; `|ASYMMETRY| = 0.400 ≥ 0.15` ⇒ the reconstruction flags the anisotropic vessel state.
- `span_truncated = False`. `K(ε_bias=0) = 2079.2` (unbiased grown OP normalizer).
- **★HONEST READ (corroborative-only, per the frozen demoted clause).** The `K(ε_bias)` curve is **nearly FLAT** — it varies monotonically from `2079.82` (`ε_bias = −3e-3`) to `2078.59` (`ε_bias = +3e-3`), a **total variation of `0.059 %`** across the full bias span. The reconstruction (POSITION/WIDTH/ASYMMETRY) therefore reads the tiny residual monotonic slope's off-centre-ness, not a resolved depletion-edge shell feature; the `|ASYMMETRY| ≥ 0.15` flag is **corroborative-only** (the prereg's demoted clause, "meaningful ONLY on the v2 nonlinear instrument"), consistent with the headline: the grown state IS anisotropic (hoop-tension vs radial-compression), but that anisotropy barely modulates the bulk tangent (`< 0.06 %` over the bias range) — the same null lift whose CANCELLATION explanation the ablation (§7) refutes, and for which this doc banks no replacement mechanism. Data + code path: JSON `verdict.cv_profile_fixed_budget`; `vessel_state_rve.py::cv_profile`.

---

## §6 — Protocol E (the ρ_eff measurement, §4) — REPAIRED, RUN, and **METHOD-UNSTABLE: NO DECIDABLE ρ DELIVERED**

Frozen: `Protocol E measures the STRUCTURAL ρ term only; the engine hosts no trapped-energy inertia (C-load open, clm-m5swh9); NO β claim from this bench`.

> **★SECTION VERDICT (read this before the method detail).** The frozen §4 deliverable — a MEASURED `ρ_eff/ρ_0` feeding `r_Z = √((K_eff/K_0)·(ρ_eff/ρ_0))` — is **NOT DELIVERED on this instrument**. The repaired, fully-guarded extraction returns a `ρ`-dependent `r_Z` that spans `[0.1005, 0.5436]` at `L=32` and `[0.1975, 0.5436]` at `L=48`, crossing the `Z_lo`/`Z_str` boundary at BOTH sizes. **The shipped `r_Z` therefore carries `ρ_eff/ρ_0 ≡ 1` (assumed), and the first-run "`ρ_eff/ρ_0 = 0.916` MEASURED ⇒ `r_Z = 0.520`" is 🔴 RETRACTED.** This is a NEGATIVE result about the *instrument*, banked as such (Rule 11): no rescue, no re-banding, no post-hoc method selection.

**The frozen §4 sub-requirements are now implemented AS WRITTEN (the F2 repair — the shipped extraction was under-guarded):**
- **PML/window boundary-cell exclusion** — all top-K field-density extractions filter `pml_thickness ≤ {i,j,k} ≤ N − pml_thickness − 1` (here: nodes within `pml_thickness = 2.0` of any face) BEFORE any `argpartition`.
- **Density-peak (top-K |field|²) sampling** — the pulse is sampled at energy-density peaks (top-K = 48 `|field|²` nodes per monitor slab), NOT a slab-mean.
- **The FROZEN reflection-free window** — Frozen: `t_end ≤ 0.9·(L/2 − r_meas)/c_P` (source at box CENTER; `r_meas` = the far density-peak monitor); shipped windows `n_steps = 282` (`L=32`) / `423` (`L=48`). The pre-repair implementation ran a window far longer than the frozen formula allows (the overrun factor is recorded in the driver header `vessel_state_rve.py:128–135`, not in the JSON — it describes the RETRACTED implementation and is not a result number).
- **A FROZEN arrival criterion (documented)** — a two-monitor **cross-correlation transit-time** (parabolic sub-step interpolated, positive-lag constrained) between two density-peak monitors, launched as a coherent rightward compression pulse. Two coherent extractions are frozen and shipped as a method-band: (a) launch@`c_P` one-shot (the fast coherent FRONT = the prereg's "first-arrival group speed"); (b) SC-coherent (the self-consistent forward-eigenmode group speed). This REPLACES the shipped unfrozen argmax-of-slab-mean.
- **A REAL reactance-pair flag** — the C-state (compression amplitude) AND L-state (kinetic flux) are recorded at EVERY step over the frozen window; `reactance_pair_recorded = True` is now a genuine both-non-trivial check with distinct peak steps (`C_peak_step = 110`, `L_peak_step = 21` at L=32 — a genuine reactive C↔L exchange, not a one-phase snapshot), not a hardcoded `True`.
- **BOTH `L ∈ {32, 48}`** — run at both (the shipped run was L=32 only).

**★THE FINDING — the extraction is METHOD-UNSTABLE at both box sizes.**

| | cold `c_0` (Bloch rel) | grown `c_eff` | `c_0/c_eff` | `ρ_eff/ρ_0` | `r_Z` | bin |
|---|---|---|---|---|---|---|
| **L=32 launch@c_P** | `0.5133` (rel `0.011`) | `0.7005` | `0.733` | `0.159` | `0.2165` | Z_lo |
| **L=32 SC-coherent** | `0.5101` | `1.499` (**diverges** `0.52→0.70→1.50`) | `0.340` | `0.034` | `0.1005` | Z_lo |
| **L=48 launch@c_P** | `0.4372` (rel `0.157`) | `0.6543` | `0.668` | `0.132` | `0.1975` | Z_lo |
| **structural anchor** (`ρ` ASSUMED) | — | — | — | `≡ 1` | `0.5436` | Z_str |

**★THE METHOD BAND — the disclosure, per `L`, straight from the JSON (`protocol_E.by_L.<L>`):**

| `L` | `r_Z_method_band` | `bins_spanned_by_band` | `method_indeterminate` | launch@`c_P` bin | cold-ToF-vs-Bloch rel |
|---|---|---|---|---|---|
| **32** | `[0.1005, 0.5436]` | `["Z_lo", "Z_str"]` | `True` | **SOFT** | `0.0107` (PASS ≤ 0.20) |
| **48** | `[0.1975, 0.5436]` | `["Z_lo", "Z_str"]` | `True` | **SOFT** | `0.1573` (PASS ≤ 0.20) |

**⚠ The two bands are NOT like-for-like — do not read the narrower `L=48` band as convergence.** The SC-coherent arm is run at the FIRST box size only (`vessel_state_rve.py:1482`, `do_sc=(i == 0)`), a runtime choice; at `L=48` it is SKIPPED, not attempted-and-failed (`c_0_SC = c_eff_SC = r_Z_SC = NaN`, `sc_grown_history = []`). So the `L=32` band is bounded by [SC-coherent, anchor] while the `L=48` band is bounded by [launch@`c_P`, anchor] — **the `L=48` band is narrower because it contains ONE FEWER METHOD, not because the instrument stabilizes with box size.** Stated so no reader infers a size-convergence trend that was never measured.

Aggregate: `method_indeterminate_any_L = True`; `size_flip_L32_vs_L48 = False`; `bins_by_L_from_launch_cP = {"32": "SOFT", "48": "SOFT"}`; `rz_used_for_headline_bin = 0.5436`; `rz_decider = "structural_anchor (ToF method-indeterminate/size-flip)"`.

**Read that table plainly: the method band at each size straddles the SOFT/UNDETERMINED boundary, so the choice of coherent extraction — not the vessel — selects the bin.** The two sizes AGREE with each other (`size_flip = False`) and DISAGREE with the anchor; that is a method disagreement, not a size-convergence problem, and no amount of box-size refinement inside this family fixes it.

- The cold reference `c_0` is Bloch-VALIDATED (rel `1.1 %` at `L=32`) — the instrument is sound on a UNIFORM medium.
- The grown `c_eff` is **UNPHYSICAL**: `c_eff = 0.70 > c_0 = 0.51` — the SOFTER grown medium (`K_tan/K_0 = 0.2955 < 1`, expected `c_eff ≈ √0.2955·c_0 ≈ 0.28`) appears FASTER than cold — and the self-consistent iteration **DIVERGES** (`0.519 → 0.701 → 1.499`) rather than converging. The grown cage-array is a strong LOCALLY-RESONANT SCATTERER; the compression pulse does NOT propagate coherently, so the cross-correlation transit-time is not a physical group speed.
- **★THE MECHANISM — a FROZEN PRECONDITION IS VIOLATED, and it is not fixable at this cost class.** The frozen §4 measurement requires a LONG-wavelength pulse (Frozen: *"`c_eff` from the first-arrival group speed of a long-`λ` compression pulse (`k·r_core ≪ 1`)"*). On `L ∈ {32, 48}` the realizable pulse sits at `k·r_core ≈ 2–4` — **MARGINAL-λ, not `≪ 1`** (JSON `method_band_note`; driver `vessel_state_rve.py:139–142`). The frozen `L∈{32,48}` and the frozen `k·r_core ≪ 1` are **mutually unsatisfiable** on a cage-array of this `φ_sf`: the pulse must fit inside the reflection-free half-box window, which caps `λ` from above, while the cage radius fixes `r_core` from below. So the ρ-measurement was under-specified at freeze time — this is a PREREG-DESIGN miss surfaced at integrator time (Rule 10), not a coding defect in the repair.
- **⇒ `method_indeterminate = True` at both `L`. The ρ-measurement is NOT decisive and is NOT used.** The bin rests on the structural anchor `r_Z = √K_ratio = 0.5436` (`Z_str`) with `ρ ≡ 1` assumed — a **disclosed CHOICE** (graded in §BOTTOM LINE), not a measurement. **No post-hoc method selection was performed:** the launch@`c_P` arm was NOT promoted to "the" measurement despite reading SOFT at both sizes, because its own output (`c_eff > c_0`) falsifies it as a group speed.

**★DISCLOSED engineering choice (F2):** the ToF-medium is grown with a fail-fast capped solve (`outer ≤ 12`, `inner_tol = 1e-6`) — the ToF medium is a homogenized TANGENT operator, verified insensitive to the last outer/inner digits; the VERDICT `K_tan` solve keeps the frozen `1e-10`. This makes the `L=48` grow feasible without changing the grown operator. **Provenance of that verification:** it is a driver-side developer check recorded in the code header (`vessel_state_rve.py:149–153`: `L=32` capped `min_kse` identical to the full frozen `1e-10`/100-outer solve) and is **NOT emitted to the JSON** — flagged here rather than quoted as a result number, per the line-9 rule.

**★Consequences (routed to Grant, §9) — TWO, not one.** (1) The ρ-side cannot be tightened on `L∈{32,48}`, so the verdict rides the structural anchor; this REINFORCES the routed resolver — a STATIC SUBC/periodic lower-bound bracket (#782 Fork B), NOT a ToF, is what decides whether the true `r_Z` sits `Z_lo` (SOFT) or holds at the straddle. (2) **The method-instability is its own owed item:** whether a decidable `ρ_eff` is obtainable AT ALL on a grown cage-array (a bigger box at fixed `r_core` to reach `k·r_core ≪ 1`? a static/quasi-static ρ estimator instead of a ToF? accept `ρ ≡ 1` as the standing structural assumption and stop measuring it?) is a live instrument question the SUBC bracket does **not** answer. **This lane does NOT pick among those** — surfaced and routed only. Data + code path: JSON `verdict.fixed_budget_headline.protocol_E`; `vessel_state_rve.py::protocol_E`.

---

## §7 — percolation / painted-anisotropic / ABLATION / seed-independence / yield_saturated / σ-variant / anti-seduction

- **Percolation sub-check (§6, corroborative):** `hoop_percolates = False`, `largest_tense_cluster_frac = 9.2e-5` (`n_tense_bonds = 489`) — the grown tensile (hoop) bonds do NOT form a face-to-face spanning cluster ⇒ corroborates SOFT/NULL (patchy tension islands, not a closed hoop-tense shell), NOT HARD.
- **PAINTED-ANISOTROPIC provenance-ablation (§8):** the grown `k_shear,eff(u_0)` pattern painted static (u-independent) reads `K_tan/K_0 = 0.29824` vs GROWN `0.29548` — **GROWN ≈ PAINTED (`Δ ≈ 0.93 %`)**. By the frozen decision rule shipped with this arm (`GROWN≠PAINTED ⇒ provenance-real; GROWN≈PAINTED ⇒ anisotropy-driven`), this reads **anisotropy-driven, NOT growth/provenance-driven**. ⚠ This is the arm that CONTRADICTS the ablation block's positive attribution — see the surfaced contradiction below; the contradiction is left open, not resolved by preferring this arm either.
- **★ABLATION DECOMPOSITION (F6 — the "cancellation"-mechanism REFUTATION, JSON-shipped).** The retracted headline claimed the null lift is a hoop-stiffen ⊕ radial-soften CANCELLATION. That mechanism predicts `hoop_only > control AND radial_only < control` (one stiffens, one softens, cancelling to ≈ control). The cheapest ablation — paint ONLY the tensile-bond remap (radial bonds reset cold) vs ONLY the compressive-bond remap (hoop bonds reset cold), each measured core-restricted vs the isotropic control — REFUTES it:
  - `K_tan/K_0`: control `0.29637`, full-painted `0.29824`, **hoop_only `0.29696` (`+0.200 %` vs control)**, **radial_only `0.29764` (`+0.429 %` vs control)**.
  - `radial_only_softens_core_tangent = False`; `cancellation_mechanism_supported = False`. The "radial-soften" pattern does NOT soften the core tangent — it RAISES it `+0.43 %`. **BOTH named components STIFFEN.** ⇒ **🔴 the hoop-vs-radial CANCELLATION mechanism is REFUTED.** Data: JSON `verdict.ablation_decomposition_fixed_budget`; `vessel_state_rve.py::ablation_decomposition`.
  - **★WHAT SURVIVES IS THE NEGATIVE ONLY.** The refutation rests on a DIRECT measurement (`radial_only` is above control, not below), so it stands on its own. **No replacement mechanism is banked here** — see the contradiction immediately below.
- **★CONTRADICTION SURFACED (flag-don't-fix — two shipped attributions in the SAME JSON disagree; NOT silently resolved).** The driver ships a positive replacement attribution that conflicts with its own painted-ablation decision rule:
  - `verdict.ablation_decomposition_fixed_budget.attribution` (verbatim): *"cancellation REFUTED — the radial-soften pattern does NOT soften the core tangent; both named components stiffen ⇒ the null lift is **provenance/live-operator-driven**, NOT a hoop-vs-radial cancellation"* (`vessel_state_rve.py:1227–1230`).
  - `verdict.painted_anisotropic_fixed_budget.note` (verbatim): *"painted = grown k_shear,eff(u_0) pattern frozen u-independent; GROWN≠PAINTED ⇒ provenance-real; **GROWN≈PAINTED ⇒ anisotropy-driven**"* (`vessel_state_rve.py::verdict_leg`).
  - Applying the second rule to the shipped numbers: grown `0.29548` vs painted `0.29824` ⇒ `Δ ≈ 0.93 %`, i.e. **GROWN ≈ PAINTED ⇒ "anisotropy-driven", i.e. the provenance/live-operator channel is exactly what the painted ablation says is NOT carrying it.** The two strings cannot both be right.
  - **Disposition: the POSITIVE attribution is WITHDRAWN from this doc's claims pending adjudication** (it is a prose clause in the driver, not a computed flag; `cancellation_mechanism_supported = False` is the computed flag and is unaffected). The honest statement this bench supports is: **the null lift is NOT a hoop-vs-radial cancellation, and its positive mechanism is UNATTRIBUTED here.** Routed to Grant with §9 OWED-1/OWED-2; the driver's `attribution` string is left byte-unchanged so the conflict stays visible in the data rather than being papered over in prose.
- **Seed-independence sweep (`fixed_budget`, `p_0 ∈ {0.25, 0.5, 1.0}·p_ref`):** `lift = 0.999 / 0.998 / 0.997`, `r_Z(struct) = 0.5441 / 0.5439 / 0.5436`, all outcome (i). `lift` rel-spread `= 0.0020`, `r_Z` rel-spread `= 0.0010`, `stable_tol_0p15 = True` ⇒ **not a seed-controlled UNDETERMINED** (the UNDETERMINED is the `r_Z`-straddle, not a seed flip). The low-p0 arms carry weak/no tension (`max_r|T| = 0.159 / 0.320 < 0.489`) so they read near the control — consistent with lift ≈ 1.0 being source-independent.
- **yield_saturated reservoir knob (`A_yield_scale ∈ {0.9, 1.0, 1.1}`) — re-graded to the OBSERVED terminal state (F11/F3/F8).** The shipped scan stopped at an undisclosed hard-coded `p0 = 0.06` ceiling with every point STABLE, and banked yield_saturated as an *extrapolated* NULL. **That extrapolation is 🔴 RETRACTED and replaced by an OBSERVATION.** The scan is EXTENDED past the ceiling (`YIELD_SCAN_P0` runs to `0.08`) and the terminal state is now MEASURED — **identically at all three `A_yield_scale ∈ {0.9, 1.0, 1.1}`** (the knob does not move it):
  - highest STABLE point `p0 = 0.06`, `peak_A_ceiling = 0.2087`;
  - **OBSERVED terminal onset `p0 = 0.062`**: `min k_shear,eff = −0.00278` (`≤ 0` ⇒ physical buckling), `peak_A = 0.2156`, `kind = "buckled"`, `converged = False` at `outer_it = 8` with `nlres = 2.77e-07`;
  - **`yield_reached_before_buckling = False`** at all three scales; `grown_equilibrium_exists = False`; `yield_saturated_equilibrium_exists_any = False`.
  - The onset is a PHYSICAL buckle, not a solver stall: the capped-vs-uncapped confirmation at `2×` the fail-fast cap returns `min k_shear,eff = −0.00278`, `kind = "buckled"`, `classification_unchanged = True` (§8.4).
  ⇒ **YIELD IS UNREACHED — the state buckles first, at a measured onset.** **★SCOPE (F8 carve, stated as the limit it is):** this is a statement about **this instrument** — *this* imposed source family (`VERDICT_SRC_SIGMA = 1.6`, Gaussian per-core), *this* affine radiation-stress remap surrogate (`k_shear,eff = k_s + k_a·ε_axial`), *this* cage geometry (`φ_sf = 0.489`, bulk-only wall), *this* `p0` grid. It is **NOT** an engine-level claim that yield is unreachable, **NOT** a claim about the Op14 `S(A)` saturation knee (explicitly not hosted here, §0), and **NOT** a claim that no source profile could reach yield. The energize-LOCK-adjacent question is answered NEGATIVE *on this bench*, with a measured onset rather than an extrapolation. Data: JSON `verdict.yield_saturated_reservoir_knob.*.yield_scan`.
- **★σ-variant arm (F10/F14 — VERDICT_SRC_SIGMA ledgered + tested).** `VERDICT_SRC_SIGMA = 1.6` is a DISCLOSED `[engineering-choice]` per-core source width (differing from the single-core calibrated `2.5`), now on the engineering-choice ledger. A σ-variant verdict arm at `σ = 2.5`: `K_tan/K_0 = 0.29554`, `lift = 0.9972`, `r_Z(struct) = 0.5436`, gate (i) → bin `UNDETERMINED`. `verdict_relevant_flip = False` — the headline bin is σ-INSENSITIVE. Data: JSON `verdict.sigma_variant_arm`.
- **Anti-seduction fence (§6, frozen in advance):** headline `r_Z = 0.5436`; matched-ish (`0.8 ≤ r_Z ≤ 1.25`) = **False** ⇒ no candidate flag.

---

## §8 — HONEST DEVIATIONS (completed per F14)

1. **`T`-definition (grown_tension_nonzero):** `T` is taken as the remap term `k_a·ε_axial` (`= T/ℓ` in the `axiom-register.md:193` grammar), NOT the un-normalized axial force `k_a·ε_axial·ℓ`. Threshold `0.05·k_a·A_yield = 0.489` uses `k_a = 9.77337`. Stated in the driver docstring (`bond_tension_remap`).
2. **SELFTEST-(iii) shell fraction + scope:** the frozen ≥50 %-of-shell-band target was met via the §3B "justified equivalent" latitude (a near-buckling core cluster, achieved fraction `0.0156`) — disclosed in §1; the (iii) firing is scoped to fireability-in-extremis (F1), not threshold-scale aggregate detectability.
3. **★Protocol E — a FROZEN DELIVERABLE NOT MET, not merely a method note (F2).** The frozen §4 sub-requirements are implemented as written and RUN at BOTH `L ∈ {32, 48}`; the arrival criterion is a documented two-monitor cross-correlation transit-time (launch@`c_P` "first-arrival" + SC-coherent band). The ToF-medium grow uses a capped solve (`outer ≤ 12`, `inner 1e-6`; the VERDICT `K_tan` keeps `1e-10`). **The deviation is not the method — it is the OUTCOME.** The frozen deliverable — Frozen: `r_Z = √((K_eff/K_0)·(ρ_eff/ρ_0))` is MEASURED, not assumed — the #782 discriminator with a REAL structural ρ term — is **NOT DELIVERED.** The extraction is method-unstable at both sizes (`r_Z` bands `[0.1005, 0.5436]` / `[0.1975, 0.5436]`, each spanning `Z_lo`+`Z_str`), so the shipped `r_Z` carries `ρ_eff/ρ_0 ≡ 1` ASSUMED. Root cause: the frozen `L ∈ {32,48}` and the frozen `k·r_core ≪ 1` cannot both be satisfied on this cage-array — realized `k·r_core ≈ 2–4` (§6). Recorded as a PREREG-DESIGN miss surfaced at integrator time, routed to Grant (§9), **not** repaired post-hoc.
   - **The bin-affecting consequence, stated once more where a reader will look for it:** the `SOFT`-vs-`UNDETERMINED` distinction is exactly what the ρ measurement was frozen to settle, and it is unsettled. `bins_by_L_from_launch_cP` = SOFT at both sizes; the anchor says `Z_str`. The shipped bin follows the anchor **by disclosed choice** (§BOTTOM LINE).
4. **yield_saturated realization (F9):** implemented as a BOUNDED ascending `p0` scan (`YIELD_SCAN_P0` = `[0.005 … 0.08]`, extended past the ceiling) with a fail-fast cap (`outer ≤ 8`, `inner ≤ 800`) applied UNCONDITIONALLY to EVERY yield-scan solve (NOT only buckled ones — the §8.4 mis-description is corrected). A converged=False onset with bonds still positive would route to (ii)-artifact per §2; the OBSERVED onset (`p0 = 0.062`) is a physical `buckled` (`min k_shear,eff ≤ 0`). Capped-vs-uncapped confirmation at the onset: re-solving at `2×` the fail-fast cap (`outer ≤ 16`, `inner ≤ 1600`) leaves the classification UNCHANGED (`buckled`, `min k_shear,eff = −0.0028`, `classification_unchanged = True`) — more iterations do not rescue the buckled state (the full frozen `100×4000` is NOT used: a non-SPD buckled state makes the inner CG non-convergent and would grind, the pathology the fail-fast cap avoids, Rule 10). Data: JSON `yield_scan.capped_vs_uncapped`.
5. **`p_ref = 0.040` cage-array knob + `VERDICT_SRC_SIGMA = 1.6`** (F10): both DISCLOSED `[engineering-choice]` knobs (grade-frame Eulerian/imposed), **both on the §9 engineering-choice ledger**. `VERDICT_SRC_SIGMA = 1.6` differs from the single-core self-test calibration `σ = 2.5` and was previously un-ledgered and untested; the σ-variant verdict arm (§7) now runs at `σ = 2.5` and returns `K_tan/K_0 = 0.29554`, `lift = 0.9972`, `r_Z(struct) = 0.5436`, gate (i), bin UNDETERMINED ⇒ **`verdict_relevant_flip = False`** (JSON `verdict.sigma_variant_arm`). The σ knob is verdict-INSENSITIVE on this bench.
6. **Bloch cross-check (F14) — RAN, both channels PASS.** The frozen §8 internal validation (cold `K_0`/`G_0` vs Bloch `c_P`/`c_S`, rel `≤ 0.20`) — **never run in the first-run bench** — now RUNS: cold P/S ToF (repaired `_pe_pulse_xcorr`, `L=20`) vs `run_c2_speeds`. `c_P`: Bloch `0.51884` vs ToF `0.52432` ⇒ **`rel_P = 0.0105`**; `c_S`: Bloch `0.28621` vs ToF `0.34001` ⇒ **`rel_S = 0.1880`**; `pass_rel_le_0p20_P = True`, `pass_rel_le_0p20_S = True` (advisory gate). Recorded in §2. **Honest read:** `rel_S = 0.188` clears the frozen `0.20` bar by a small margin only — the shear channel is the weaker of the two, and this cross-check licenses the ToF instrument on a UNIFORM medium, nothing more (it is precisely what makes the grown-medium failure in §6 a statement about the medium rather than about the code).
7. **Grown-K_tan single-size caveat (F14):** the RVE-size gap gate (§2) ran on the COLD linear cage (`L∈{12,16,20}`: `K_eff/K_0 = 0.27990 / 0.29637 / 0.29423`, `gap_two_largest_rel = 0.0072`); the GROWN-`K_tan` lift + amplitude gate were read on the **`L=16` cage ONLY** (the verdict arm). The seed-independence + σ-variant + painted + ablation arms corroborate that the null lift is source/provenance-robust, but a grown-`K_tan` RVE-size gap (e.g. a grown `L=20` arm) is **NOT run here** — the grown lift is a SINGLE-SIZE read, and the cold size gate does not transfer to it. Disclosed, not repaired.
8. **★Determinism — the frozen two-full-runs criterion was RUN, not substituted (F5/F13).** Frozen: *"Two independent full driver runs `reruns BIT-IDENTICAL` (`diff -q` clean); only fixed-seed RNG (`run_c2_speeds(seed=1)`); the static/grown Newton solve carries no per-step RNG."*
   - **What the first run shipped (disclosed):** the `leg0_instrument_validation.determinism` gate bit-compares two IN-PROCESS `grow_vessel` calls. That is a **PROXY**, not the frozen criterion, and it is now labelled as one in the JSON note (`"IN-PROCESS PROXY (two grow calls)"`). It stays in the JSON as a cheap in-run guard; it is not what discharges the gate.
   - **What discharges the gate (RUN 2026-07-26):** the driver was executed a SECOND time, end-to-end (`--legs all`, separate process, separate `--out` path), and the two runs' results were compared with the frozen `diff -q`. **Result: `diff -q` CLEAN** — the timing-stripped payloads are byte-identical (18 222 bytes each) and the `determinism_digest` matches exactly (`69968227e455137f549586e3897486910f1fe59899ed1608fe7cb673d2aaf076`). Wall clocks differed (`1048.6 s` vs `1089.5 s`), which is why `_runtime_sec` and the digest field itself are the only keys excluded from the comparison — timing is not a result.
   - **Honest scope of that exclusion:** `diff -q` is run on the results MINUS wall-clock; a literal byte-diff of the two raw JSON files would differ in `_runtime_sec` alone. This is disclosed rather than glossed, and it also re-verifies that the SHIPPED JSON reproduces exactly — every number in this doc is reproducible, not a one-shot.
9. **Calibration-disclosure string regenerated to COMPUTED truth (F4/F15).** The first-run `leg0_selftests.calibration_disclosure` asserted, in prose, that the frozen loose tol had been *tuned* and *did not perturb the tangent by >5 %* — beside a shipped measurement of `amp_spread ≈ 649`. That string is **🔴 RETRACTED as false-to-its-own-data.** The string is now **generated by the driver from the same dict it describes** (`vessel_state_rve.py:670–683` — every figure an f-string of a `selftest_ii`/`selftest_iii` field), so it cannot drift from the JSON by construction, and it must never be hand-edited: it is regenerated by re-running the driver. Computed truth as shipped: the frozen `1e-3` was applied UNCHANGED and FIRED the artifact with **no calibration latitude used** (`loose amp_spread = 649`, `tight = 0.00583`, `loose A_sign = 0.03`, `loose all_scan_legs_converged = False`). The `%%` literal defect in the old string is gone with it.

---

## §9 — Calibration-vs-derived ledger (consistency-vs-emergence, frozen tags) + routed consequence + F7 design-lesson

`K_tan/K_0`, `G_eff/G_0`, `K_ratio_lift`, the `K(ε_bias)` profile are `[derived]` dimensionless RATIOS (lattice static homogenization + relaxed-equilibrium growth through the state-dependent operator; MANIFESTATION-class). **★TAG CORRECTION (this repair):** the frozen ledger tagged `ρ_eff/ρ_0` and `r_Z` `[derived]` on the assumption that Protocol E would measure `ρ`. It did not (§6). **As shipped, `ρ_eff/ρ_0 ≡ 1` is an `[assumption]` (the long-λ `k→0` uniform-point-mass limit), NOT `[derived]`, and the headline `r_Z = 0.5436` is therefore `[derived]` in its `K` factor and `[assumption]` in its `ρ` factor** — a MIXED-provenance number, and it must be cited as such downstream (notably by the continuum-radial-solver lane, #789 D5/R1). The `[derived]`-only tag on `r_Z` does not survive this run. The srs bond model `ρ*=9.77337` is `[import]` (`ν_Hill=2/7`, GR-imported K=2G, `ave.core.constants.N_NU`; CONSISTENCY-class). The grown `T(r)` is `[derived]` (relaxed-equilibrium output) feeding the `[canon]`-form LIVE remap `k_shear,eff(u) = k_s + T(u)/ℓ` (`axiom-register.md:193`). The source amplitudes `p_0` / `A_yield_scale` / the source width `VERDICT_SRC_SIGMA` are `[engineering-choice]` knobs (grade-frame Eulerian/imposed). `α`-CLEAN (the discriminator is a dimensionless impedance ratio). **No emergence-class claim headlined.**

**Consequences routed to Grant** (this lane surfaces + routes only; no leaf / ledger / port-register / falsification-ledger edit, regardless of outcome — prereg §9/§10). **TWO owed resolvers, not one:**

- **★OWED-1 — the `r_Z`-straddle fork (§9 Fork B/SEED).** The grown anisotropic vessel does NOT resolve bridge-vs-bottleneck: `lift = 0.997` (null) with the structural anchor `r_Z = 0.5436` straddling `0.5`. **The owed resolver is the feasible SUBC/periodic lower-bound bracket** (same cost-class, `L = 16`) that would decide whether the true `r_Z` sits `Z_lo` (macro-side / SOFT) or holds at the straddle — NOT executed here.
- **★OWED-2 — the Protocol E METHOD-INSTABILITY (NEW; surfaced by this repair, co-equal with OWED-1).** The frozen ρ-measurement is **not executable as written** on a grown cage-array at `L∈{32,48}`: the frozen long-λ precondition (`k·r_core ≪ 1`) and the frozen box sizes are mutually unsatisfiable at this `φ_sf`, the coherent extractions disagree across the `Z_lo`/`Z_str` boundary at BOTH sizes, and the grown `c_eff > c_0` is unphysical. **This is a decision Grant owns, not this lane:** is a decidable `ρ_eff` obtainable at all on a grown cage-array (larger box at fixed `r_core`? a static/quasi-static ρ estimator instead of a ToF? or is `ρ ≡ 1` adopted as the standing structural assumption and the ρ-measurement retired from this bench family?). Note that OWED-1 alone does **not** dispose of OWED-2 — a SUBC/KUBC bracket brackets `K`, not `ρ`.
- **The yield_saturated NULL** (no stable equilibrium; buckles at the OBSERVED onset `p0 = 0.062`, `min k_shear,eff = −0.00278`, `peak_A = 0.2156`, `yield_reached_before_buckling = False` at all three `A_yield_scale`) is banked as the energize-LOCK-adjacent outcome, **INSTRUMENT-SCOPED** (this source family / this remap surrogate / this cage geometry) — no rescue, no re-seed of the slot (Rule 12), no engine-level generalization.
- The trapped-energy `β` term (Fork ρ / C-load, `clm-m5swh9`) is the continuum radial-solver lane's tagged E=mc² import — NOT this bench's.

**★F7 DESIGN-LESSON (dated note 2026-07-26, carried forward — NO post-hoc re-banding here).** The frozen band placement made a clean NULL/SOFT verdict UNREACHABLE for a null-effect vessel of this outcome class: the `[canon, #782]` baseline `r_Z(β=0) ≈ 0.544` sits INSIDE the `Z_str` straddle band, so any bin OTHER than UNDETERMINED required the grown ρ-correction to shift `r_Z` OUTSIDE the straddle — which (a) is the scoped-bubble-sign direction and (b) turned out to depend on a ToF the grown cage-array defeats. **A null-lift vessel whose structural `r_Z` already sits inside the straddle can ONLY read UNDETERMINED absent a decisive ρ measurement — so the whole verdict was loaded onto the one measurement that this instrument cannot make.** That is the design fault, and it is a band-placement fault, not a data fault.

**Two requirements this hands the follow-on SUBC prereg (both must be met at freeze time, before any run):**
1. **Reachability.** Place the bands so EVERY outcome class (HARD / SOFT / NULL / UNDETERMINED) has a reachable bin — e.g. band the SUBC lower bound against the KUBC upper bound as the discriminator, rather than a single-point `r_Z` against a straddle the baseline already occupies.
2. **Executability (the NEW half, from OWED-2).** Any frozen measurement must be checked for INTERNAL SATISFIABILITY at freeze time: here, `k·r_core ≪ 1` and `L ∈ {32,48}` at `φ_sf = 0.489` are mutually unsatisfiable, and nothing in the freeze caught it. Freeze the precondition-consistency check, and freeze what the verdict falls back to when a measurement returns non-decisive — **so the fallback is pre-registered rather than chosen after seeing the numbers, as it was here.**

**This run is NOT re-banded post-hoc** — the frozen table decides on the repaired numbers, and the fallback taken here is disclosed as a post-hoc choice precisely because the prereg did not pre-register one. The lesson is recorded for the next prereg only.

> **Result-doc provenance.** The bench RUN of the merged, Grant-ratified REVISION-3 frozen prereg (`research/2026-07-22_vessel-state-rve_prereg-FROZEN.md`; ADJ-1 stencil split + ADJ-2 (iv)-demotion ratified at merge), REVISED 2026-07-22 and FINISHED 2026-07-26 to answer the PR#796 adversarial review (15 confirmed findings; repairs live in the driver/result/JSON — the frozen prereg is BYTE-UNTOUCHED, the JSON is driver-generated and never hand-edited). Driver `research/drivers/vessel_state_rve.py` reuses the #782 KUBC scaffold (Rule-14), `ave.core.*` read-only, engine byte-untouched, deterministic (shipped run `1049 s`; determinism per §8.8). LEG-0 gate-fireability PASSES (self-tests force (ii)/(iii); (iii) scoped to **fireability-in-extremis**); instrument validation all-pass (+ the Bloch cross-check, `rel_P = 0.0105` / `rel_S = 0.1880`); grown equilibrium EXISTS at `fixed_budget p_ref`; amplitude gate outcome **(i) CLEAN**. **HEADLINE BIN = UNDETERMINED — with the ρ half UNRESOLVED**: `lift = 0.997` and `r_Z = 0.5436` is the STRUCTURAL ANCHOR at `ρ_eff/ρ_0 ≡ 1` **assumed**, because the repaired Protocol E is **METHOD-UNSTABLE** (`r_Z` bands `[0.1005, 0.5436]` at `L=32` and `[0.1975, 0.5436]` at `L=48`, each spanning `Z_lo`+`Z_str`; launch@`c_P` reads SOFT at both sizes). **🔴 The first-run "MEASURED `ρ_eff/ρ_0 = 0.916` ⇒ `r_Z = 0.520`" headline and every "both Z factors measured" framing are RETRACTED**; the anchor fallback is a DISCLOSED CHOICE, and the method-instability is routed to Grant as OWED-2 alongside the SUBC bracket (OWED-1). The grown anisotropic vessel reads like its isotropic control; the CANCELLATION mechanism for that null is **REFUTED** and **no replacement mechanism is banked** (the driver's positive attribution contradicts its own painted-ablation rule — surfaced in §7, unresolved). yield_saturated buckles at the OBSERVED onset `p0 = 0.062` (energize-LOCK-adjacent NULL, **instrument-scoped**: this source family, this affine remap surrogate, this cage geometry). Consequences routed to Grant; no leaf touched; no rescue minted; no post-hoc re-banding. Companions: the frozen prereg, merged **#782** (`research/2026-07-21_rve-aggregation-bench_result.md`), merged **#779** (the anisotropic remap), merged **#788** (Protocol E arithmetic), merged **#789** (`continuum-radial-solver_CHARTER.md` — this bench feeds D5/R1 profile input), and the docket fragment (`_orchestration/docket-entries/2026-07-22-vessel-state-rve-bench.md`).
