# A1-PORT SOURCING ADJUDICATION — FROZEN pre-registration

**Date:** 2026-08-07 · **Branch:** `research/a1-port-sourcing` · **Base:** `origin/main` @ `91a910f8`
**Lane brief:** [`_orchestration/2026-08-06_a1-port-sourcing-brief.md`](../_orchestration/2026-08-06_a1-port-sourcing-brief.md) (Grant GO 2026-08-06)
**Class:** DERIVATION-ONLY (Tier-2 at adjudication). Engine `src/ave` byte-untouched; NO engine
runs planned (the brief's stencil-lens fence: *"derivation adjudicates, engine runs corroborate at
most"*). **Mints no `clm-`/`def-`; edits no KB leaf, no register, no falsification ledger, no
ruling record; changes no solidity.** Freeze-by-push: this prereg commits ALONE and pushes before
any derivation content exists on this branch (ave-prereg Step 3.11).

---

## §0 — Standard Vacuum Analysis header (SVA v0.2-pilot)

 1. **SECTOR / OWNERSHIP:** Source = A1 dilatation content (mass; `master-equation.md:20` banner —
    the TWO-"3"s guard active: A1 dilatation-MASS ⊥ Cosserat (2,3) WINDING-charge; never
    cross-wire). Channel under test = the mechanical longitudinal P-branch (A1-carried, gapless,
    LC-1). Observed GW = T2 transverse shear at c. Cosserat micro-rotation is NOT implicated by
    construction — **fence: if the derivation finds the outbound coupling runs through the
    ROTATIONAL channel (the #897-fenced frontier line — the kernel-collapse carve-out under which
    rotational transport passes Γ walls untouched, `wall-taxonomy.md:437` + `:157`), STOP and
    route to Grant before proceeding** (brief, verbatim fence). Cross-wiring check done; one
    declared distinction carried through the whole lane: the **operating-point bias field
    `A₀(r)`** (Axiom-4 kernel amplitude, the DC gravitational grading, #754) and the **dynamical
    displacement field `u`** (whose longitudinal polarization rides the P-branch) are DIFFERENT
    state variables, and every coupling claim must name which one it lives on.
 2. **REGIME / PHASE-STATE:** Far field: Regime-I cold crystalline, linear, small-signal
    (`V_GW/V_snap ~ 10⁻²⁸`, S=1), cold-reactive (Ax3-lossless; radiation is a legal port).
    Source region (declared separately per the brief): neutron-star binary — interior near-yield
    (operating point A₀ ~ 0.6–0.8, saturated), exterior static bias A₀ up to ~0.2–0.35 at the
    stellar surface falling as the receipted static profile; the DC bias point IS the
    gravitational grading. Drive = small AC modulation of the DC configuration at 2Ω
    (Ω = orbital frequency), deeply sub-carrier (ε = Ω/ω₀ ~ 10⁻²⁴).
 3. **CIRCUIT STATEMENT:** Two DC-biased varactor clouds orbit each other. Question: does the
    orbiting DC-bias configuration drive AC real power at 2Ω into the gapless longitudinal
    transmission-line branch — i.e., does a coupling element (transformer) between the DC bias
    network and the P-branch line EXIST in the receipted structure, and at what multipole order
    and coupling does it convert? Total-vs-slot: the pulsar comparator (`Ṗ_b`) reads the TOTAL
    source energy-loss budget — every port at the source, regardless of downstream conversion —
    not a per-channel slot.
 4. **PLANE & PROJECTION:** Radiation-zone far field (r ≫ c/2Ω); plane-wave Christoffel
    EIGENVALUE basis (LC-1 §3: the radiative row has exactly ONE speed, √(10/3)·c; the √2·c
    object is a constitutive re-expression, not a Bloch branch — the FLAG-A speed fork is dead on
    the radiative row). Near-zone reactive storage (the halo, port-register P9) is explicitly a
    different plane and is not read as radiation. Source-region Γ claims (constituent
    `Γ_bulk = −1` walls) carry the last-bond-plane convention (`wall-taxonomy.md` §10).
 5. **CONSTITUTIVE PROVENANCE:** `K = 2G` — IMPORTED (GR, PR#261); only K ≠ 0 is load-bearing
    (#761 §1.3) and LC-1 §2.2 makes v_L > v_T independent of the import entirely. `c_P/c_S` —
    DERIVED-form / IMPORTED-value (√(10/3) at K=2G). Angular partition `A_ang = 2/3` — DERIVED
    (isotropic-elastic P/S angular integrals; Aki–Richards-anchored, q1 §1.2/§6).
    Equal-coupling `κ_L/κ_T = 1` — **ASSUMED at HEAD** (q1-inherited; envelope §6.1 tags it
    `[assumed]`) — **this lane's primary derivation target**. Static amplitude `A = g_N/a₀` —
    RECEIPTED-correspondence (#754 `A = g_N-FORCED`, with its R1 disclosure: the g_N↔E_local leg
    is correspondence-transported, not independently traced). `mass = A1-dilatation` —
    RECEIPTED-consistency (canon banner, Grant-ratified; #761 ledger: "NOT driver-validated …
    held"). Microscopic pluck operator — ABSENT (`axiom-register.md:193`, standing open).
    Isotropy of the far-field moment coupling — MODELING CHOICE inherited from q1 (LC-1 §12
    amendment-candidate 3 noted; direction-resolved srs widens v_L/v_T to ≥ 2.13 min, which only
    strengthens any 1/c_L⁵ suppression statement made at √(10/3)).
 6. **ENERGY LEDGER:** Far-field radiation into a gapless branch = a PORT (continuum-counted;
    transfer-cost-theorem: the arrow exists iff energy crosses the system boundary into such a
    port). The halo/near-field = rim (reactive, stores-and-returns, no arrow, port-register P9 +
    DM-conflation note). This lane adjudicates WHICH ports the binary's boundary-crossing arrow
    feeds and by what receipted element. No loss word will be used without its port named.
 7. **CALIBRATABILITY:** All targets dimensionless: the multipole order n; the flux ratio
    `F_bulk/F_shear`; the comparator residual δ. The unknown microscopic mass-moment
    normalization CANCELS in the P/S flux ratio (common source tensor; envelope §6.1 "NOT
    numerically circular"). PASS.
 8. **DISCRIMINATION CLASS:** **DC→AC coupling** (a DC medium state — the mass's A1 bias — read
    out through an AC channel — the 2Ω radiative budget). Liveness-proven readout: pulsar timing
    reads the shear channel's flux to 0.16% / 1.3×10⁻⁴ (the GR-matched shear budget IS the
    positive control). Tautology filter: not an identity — GR's counterfactual is ZERO bulk flux
    (gauge-killed longitudinal), the receipted-elastic default is ~0.03: genuinely
    discriminating, direction currently AGAINST the framework. SM counterfactual run.
 9. **CERTIFICATION PLAN (gates frozen before any number):**
    - **G-ENUM** — the per-coupling provenance table is complete: every candidate source coupling
      tagged RECEIPTED-DERIVED / RECEIPTED-CONSISTENCY / ASSUMED / ABSENT with file:line receipts.
    - **G-NEGCTRL** — negative control on the enumeration grammar: the T2-shear channel pushed
      through the SAME enumeration must return its receipted GR-matched coupling (a grammar that
      cannot see the receipted shear coupling cannot adjudicate the bulk's).
    - **G-ABSENCE** — every corpus-absence claim carries two-method receipts with NAMED engines
      (GNU grep -E and ripgrep/Python-re, named per scan; the `**`-pathspec and `$..$`-escaping
      false-negative classes guarded).
    - **G-LADDER** — the multipole ladder algebra must reproduce the receipted #761/envelope
      monopole and dipole kills identically (Ṁ = 0; Ẍ_cm = 0) before any new step is trusted.
    - **G-NUM** — every load-bearing numeral in the result re-derivable from stated inputs;
      exact forms carried symbolically ((3/10)^{5/2}, 2/3). No engine runs (UNRUN ≠ PASSED does
      not arise; the stencil-lens fence stands).
10. **ADJUDICATION ROUTING:** The verdict (one of the three frozen bins, §2) routes to the
    orchestrator: LC-2..5 and the arc-scope ruling are HELD on it. FLAG-LC1-B disposition is
    verdict-keyed (§5). The constituent-cage-ensemble fork (envelope §3.1) is NOT resolved by
    this lane unless a resolution is DERIVABLE at HEAD from receipted structure; any lean is
    disclosed as a lean. Rotational fence-reach → STOP + route Grant (row 1). No leaf, register,
    ledger, or ruling edit on ANY outcome; all propagation is routed.
11. **NUMERICAL CONDITIONING:** Trivial by construction — single power laws and closed-form
    angular integrals; no iterated maps, no cancellation-sensitive subtraction, no dynamic-range
    hazard. Named regex engines per row 9 G-ABSENCE. Exclusion arithmetic in double precision
    against symbolically-carried exact forms.

---

## §1 — The frozen question (verbatim from the brief; the brief is the frozen source)

`_orchestration/2026-08-06_a1-port-sourcing-brief.md:17-28`, verbatim `[sic]`:

> Does any coupling in the receipted structure convert a gravitating source's A1 content
> into TIME-VARYING far-field bulk strain — and at what multipole order? Sharpened form
> (the static-mass argument does not cover binaries): a static mass is DC bias and cannot
> radiate; a BINARY is a time-varying A1 configuration, so the drive exists. Derive the
> lowest non-vanishing multipole order of bulk-channel emission for a binary A1
> configuration: monopole should vanish by total-mass conservation, dipole by momentum
> conservation — does the QUADRUPOLE coupling integral vanish (and if so, by what
> structure — there is no gauge to hide the trace behind in an elastic medium), or not?
> If non-vanishing: compute the flux order and compare against the pulsar-timing budget
> residual (re-retrieve the banked exclusion from source at lane time; the Hulse-Taylor
> class budget is matched by the transverse channel to ~fraction-of-a-percent — state the
> residual room honestly).

## §2 — The frozen verdict grammar (verbatim from the brief)

`_orchestration/2026-08-06_a1-port-sourcing-brief.md:32-40`, verbatim `[sic]`:

> - **RADIATIVE(n)** — a non-vanishing coupling at multipole order n, flux derived;
>   compare pulsar-timing residual; feeds Grant's arc-scope ruling as the kill-arm
>   confirmation.
> - **SOURCELESS** — every receipted coupling vanishes to the derived order, by named
>   structure (a conservation law, a coupling-integral identity), not by assumption;
>   LC-1 re-banks as structural compliance and the lineage-map line is vindicated.
> - **NOT-DERIVABLE** — canon lacks a named piece (most likely the source coupling: what
>   physically plucks the bond in matter, `axiom-register.md:193`'s standing open);
>   deliver the enumerated missing-piece list. An honest open with a name, not a shrug.

**Fireability (Step 3.10).** None of the three bins is entailed by installed structure, but the
prior is NOT flat and is declared: #761 (merged, review-grade) + the envelope result (PR #767
review, 11-confirmed/0-refuted) already hold quadrupole-order coupling at FORM grade conditional
on the sector identity, so **RADIATIVE(2) is the strong prior**. SOURCELESS remains fireable: it
fires if the enumeration finds a receipted structure the prior lanes did not adjudicate that
kills the coupling integral (e.g., the A₀-vs-u variable split severing the source from the
P-branch at derived grade, or a receipted image-cancellation). NOT-DERIVABLE remains fireable: it
fires if EVERY non-vanishing route is found to pass through an ASSUMED or ABSENT link (the pluck
operator; the untraced correspondence leg of #754) AND no receipted DC-anchor normalization
closes the amplitude. The verdict verb will be chosen per Step 3.10: ADJUDICATED where a bin
could have come out otherwise, DEMONSTRATED where it re-exhibits #761/envelope structure.

## §3 — The frozen comparator (re-retrieved from source THIS session, before any flux number)

Per the brief's Rule-11 fence, both figures re-retrieved from the source abstracts via WebFetch
on 2026-08-07 (this session), BEFORE any flux number exists on this branch:

| System | Source statement (verbatim from abstract, retrieved 2026-08-07) | Frozen residual |
|---|---|---|
| **Hulse-Taylor PSR B1913+16** | *"the ratio of observed orbital period decrease due to gravitational wave damping (corrected by a kinematic term) to the general relativistic prediction, is 0.9983 ± 0.0016"* — Weisberg & Huang 2016, "Relativistic Measurements from Timing the Binary Pulsar PSR B1913+16" (arXiv:1606.02744, ApJ 829, 55) | `δ_HT = 0.0016` (1σ fractional) |
| **Double pulsar PSR J0737-3039A/B** | *"We present the currently most precise test of general relativity's (GR's) quadrupolar description of gravitational waves, validating GR's prediction at a level of 1.3 × 10⁻⁴ (95% conf.)"* — Kramer et al. 2021, "Strong-field Gravity Tests with the Double Pulsar" (PRX 11, 041050; arXiv:2112.06795) | `δ_DP = 1.3×10⁻⁴` (95% conf. fractional bound) |

Cross-check: both figures match the banked corpus imports (`q1-pulsar-hardening.md` §2.2, ledger
entry `q1-reading-A-radiative-bulk-port`) exactly — no drift between bank and source.

**Residual room, stated honestly (the brief's ask):** the transverse channel already matches the
HT budget at the 0.16% level and the DP budget at the 1.3×10⁻⁴ level; ANY additional radiative
channel must fit inside those residuals. An extra fractional flux F is in tension at F/δ.

## §4 — Corpus state + the frozen enumeration protocol (ave-prereg Steps 1–2)

**Corpus state: PARTIAL — extensive prior work, one orthogonal open axis.** First-party reads at
HEAD (all verbatim receipts held in the lane record): #761 falsified the three candidate
non-radiativity closures (mode-absence; derived-cold-emptying; derived-source-decoupling) and
derived quadrupole order CONDITIONAL on an imposed delta-localized source; q1 hardened the
comparator and derived A_ang = 2/3; the envelope lane derived the ponderomotive |A|²→P-branch
coupling at FORM grade with the O(1) normalization ASSUMED and the constituent-cage-ensemble fork
OPEN ("unadjudicated both ways and potentially verdict-flipping"); LC-1 narrowed the radiative
row to ONE speed and showed v_L > v_T is modulus-unconditional. **This lane's question — the
PROVENANCE of the source coupling itself (receipted vs assumed vs absent) — is the axis none of
those lanes adjudicated** (#761's ledger tags the sector-identity coupling "NOT driver-validated
… held"; the Q1 exclusion's O(1) is a reading, not a derivation).

**The frozen enumeration protocol (the lane's Step-1 work plan; results do NOT feed back into
these bins):** every candidate matter→P-branch source coupling found in canon is tagged
RECEIPTED-DERIVED / RECEIPTED-CONSISTENCY / ASSUMED / ABSENT with file:line, and the multipole
ladder is run on each non-absent candidate. Candidates known at freeze (the corpus-grep sweep may
add more; additions are enumerated in the result with the same grammar):

  C-a. The sector-identity source (`mass = A1-dilatation` → delta-localized body-force divergence;
       #761 §1.2's imposed form).
  C-b. The ponderomotive texture coupling (|A|² of the carrier → acoustic field; envelope Leg A).
  C-c. The receipted STATIC exterior profile as DC anchor (#754 `A = g_N/a₀`; the
       gravity-as-refraction correspondence) promoted to time-dependence by orbital motion —
       the candidate that could DERIVE the amplitude normalization the Q1 exclusion assumed.
  C-d. Kinetic/momentum-flux stress of the moving masses (ρv_iv_j deviatoric drive).
  C-e. The microscopic pluck operator (`axiom-register.md:193`) — expected ABSENT; if every
       route dead-ends here, NOT-DERIVABLE fires with this as the named missing piece.
  C-f. The constituent-cage image-cancellation structure (envelope §3.1) — an OPEN fork, not a
       receipted coupling-killer; adjudicated here ONLY to the extent derivable from receipted
       structure at HEAD (e.g., what the receipted static exterior profile implies about DC
       transmission through the constituent wall), else inherited as OPEN.

## §5 — Analytic expectations (Step 3.9; the walked picture's numbers, frozen) + Step 3.5 dimensional analysis

**Prediction (primary, form rung):** the lowest non-vanishing multipole order is **n = 2
(quadrupole)** — monopole killed by total-mass conservation (Ṁ = 0), dipole by momentum
conservation (Ẍ_cm = 0), and the quadrupole coupling integral does NOT vanish: the tracelessness
rescue is dead (#761 §2 — a scalar/longitudinal channel radiates via the traceless second moment,
which rotates at 2Ω), and no receipted conservation law remains at n = 2. The trace has no gauge
to hide behind; expectation is that no named structure kills the integral.

**Robustness ladder (Step 3.7):** PRIMARY (gating) observable = the multipole ORDER n and the
per-coupling PROVENANCE CLASS (existence/shape rung — survives any honest knife on magnitudes).
SUPPLEMENTARY = the flux magnitude (inherits the coupling-normalization residual; reported with
per-coupling provenance, never headlined beyond its weakest link). Gate-floor consistency: the
comparator cells are far above instrument floor (δ_HT, δ_DP are published measurement precisions);
no noise-limited cell exists in this design.

**Fork-record-both (Step 3.9-2) — both branches' numbers frozen, at the single radiative speed
√(10/3)·c (LC-1 §8 narrowing 1; K=2G-imported value, only its ≥√(4/3) floor is
modulus-unconditional):**

- **Branch F (full coupling):** the source moment is the full mass second-moment (sector-identity
  or ponderomotive route at the receipted form + equal-coupling normalization).
  `F_bulk/F_shear = A_ang·(c_S/c_P)⁵ = (2/3)·(3/10)^{5/2} = 0.0329` (dimensionless;
  `(3/10)^{5/2} = 0.04930`). Against the frozen comparator: `0.0329/0.0016 = 20.5σ` (HT);
  `0.0329/1.3×10⁻⁴ = 253×` (DP). With q1's honest O(1) coupling bracket
  `A_ang·κ² ∈ [0.3, 1.0]` (speed fork now dead): `F ∈ [0.0148, 0.0493]` → `9.2–30.8σ` HT,
  `114–379×` DP. Prediction: excluded everywhere in the bracket.
- **Branch X (exterior-texture-only floor):** if the derivation finds the only RECEIPTED source
  is the exterior static-bias texture (the caged interior's coupling unreceipted pending the
  cage fork), the radiating moment is suppressed by roughly the compactness fraction of the
  A1 content sitting outside the walls. Frozen bracket expectation: amplitude suppression
  ~O(GM/Rc²) per star (NS compactness ~0.2) ⇒ flux suppression ~O(10⁻²)–O(10⁻¹) ⇒
  `F ∈ ~[3×10⁻⁴, 5×10⁻³]` ⇒ `~[0.2, 3]σ` HT but `~[2.5, 40]×` the DP bound. Prediction: even
  this floor variant stays EXCLUDED by the double pulsar (the derivation, not this bracket,
  decides where in it the receipted structure lands — and whether Branch X is even the
  receipted reading).

**Step 3.5 dimensional ingredients:** all inputs dimensionless ratios — `c_S/c_P = (3/10)^{1/2}`
(from K=2G, `constants.py` K=2G chain; IMPORTED value, DERIVED form), `A_ang = 2/3` (derived
theorem), δ_HT/δ_DP (imported, §3). No dimensional bridge is crossed anywhere in the target; the
scaling exponent 5 in `(c_S/c_P)⁵` is the standard multipole flux power (1/ρc⁵ per channel,
q1 §1.2 derivation, reproduced under G-LADDER). Sanity anchor: the identical integrals reproduce
the seismological `E_S/E_P ≈ 23.4` textbook value (q1 §6, Aki–Richards) — means-test PASS
at value level.

**Falsifier for the lane's own framing:** if the multipole ladder on the receipted couplings
yields a vanishing quadrupole by a NAMED structure this prereg did not anticipate, the
prediction above is wrong and the bin is SOURCELESS — that outcome would vindicate
`physics-lineage-map.md:63` and re-bank LC-1 as structural compliance. If every non-vanishing
route dies at an ASSUMED/ABSENT link with no receipted DC-anchor closure, the bin is
NOT-DERIVABLE and the missing-piece list is the deliverable.

## §6 — Mandatory-input receipts (verbatim, first-party at HEAD `91a910f8`)

1. **FLAG-LC1-B side A** — `manuscript/ave-kb/common/physics-lineage-map.md:63`, verbatim `[sic]`:
   *"AVE evades by sector assignment, claiming **no longitudinal photon** (the A1 grade is
   non-radiative in free space, re-engaging only inside saturation; `common/historical-precedents.md`
   Root 1 precision note; `vol1/dynamics/ch4-continuum-electrodynamics/master-equation.md:18`). The
   gapped/confined status is simultaneously the evasion and the exposure: any mechanism coupling
   the A1 bulk mode to EM detectors below saturation walks back into these bounds."*
2. **FLAG-LC1-B side B** — `manuscript/ave-kb/common/port-register.md:5` (frontmatter), verbatim
   `[sic]`: *"Q1 is a RULED row — REVERTED 2026-07-20 to Reading-A-live: the make-or-break
   mechanical common-mode derivation returned NONE-DERIVES (#761 merged @ caa51c17), firing the
   row's own clause, so the RULED-CONDITIONAL Reading-B reverts and the
   independent-radiative-port exclusion is live against the framework"*; and the Q1 row
   (`port-register.md:87`): *"the A1-dilatation rides the gapless P-branch, the binary drives it
   at quadrupole order, and the EM Gauss-kill is structurally blocked from ∇·u by the bulk
   restoring force K≠0"*. Per the challenge-the-canonical-negative rule, this lane greps the
   CONFIGS/derivations behind BOTH sides, not the conclusions (§4 protocol).
3. **#761's three falsified candidates** (`research/2026-07-20_mechanical-commonmode-derivation_result.md`
   §5 headline): **mode-absence** (FALSIFIED — the dilatation rides the gapless Bloch P-branch),
   **derived-cold-regime-emptying** (FALSIFIED — `Z_eff → Z_0` exactly cold; zero conversion),
   **derived-source-decoupling** (FALSIFIED — tracelessness rescue dead; monopole/dipole killed
   by conservation; quadrupole survives). **Orthogonality statement (frozen):** this lane's
   question is NOT among the three — #761 adjudicated whether a derived structure CLOSES the
   port; this lane adjudicates whether the coupling that OPENS it is itself derivable. #761's own
   ledger (§6.1): *"mass = A1-dilatation (the coupling grade-assignment) … consistency (NOT
   driver-validated …) — the load-bearing sector identity, held."* The quadrupole-vanishing
   route WAS killed by #761 (candidate 3) — CONDITIONAL on that held identity; the receipt is
   quoted, and the conditionality is exactly what this lane adjudicates.
4. **The pluck question** — `axiom-register.md:193`, verbatim `[sic]` (trailing clause): *"Open
   (flag for Grant, NOT landed): if the electron self-trap wall is the STATIC T2 winding (no real
   power), what physically PLUCKS the bond in matter to deliver arm-(a)'s transverse bias? — a
   substrate-ontology question surfaced, not resolved (#527 §4)."* Routing honored: if the source
   coupling is underived, the verdict routes through NOT-DERIVABLE rather than inventing one.
5. **LC-1 (PR #913, branch `research/lc1-one-speed`)** — inherited as the channel's existence/
   speed receipts + §6.4 arc-scope package; the premise this lane adjudicates is its "sourced"
   leg (LC-1 §11.1: the envelope constituent-knot-core fork "IS THE ONE THAT COULD MOVE S2").
   The spectrum is NOT re-derived here.
6. **Walk-level context (UN-AUDITED, licenses nothing):**
   `research/2026-08-06_rotation-substance-ontology_framing-note.md` §9 (the static-bias walk:
   named clocks, optical compression, nodes-fixed/waves-compress lean) and its §9 closing line —
   the V-sector scalar as *"the natural home of the static bias a passing wave reads"* — carried
   as context for the walk only, never as a receipt.

## §7 — Structural-degeneracy + liveness (Step 3.8)

No numerical run is planned, so no readout-degeneracy hazard arises. For the DERIVATION'S
decision observable (the quadrupole coupling integral): the known degeneracy classes are checked
by construction — the integral is evaluated on open-domain far-field asymptotics (no closed-sum
telescoping); parity: a quadrupole drive into a scalar/longitudinal channel is parity-allowed
(no forced zero); the G-NEGCTRL gate (§0 row 9) is the enumeration grammar's positive control.
Absence claims carry the two-method named-engine discipline (G-ABSENCE).

## §8 — What this lane does on each outcome (routing, frozen)

- **RADIATIVE(n):** report n + per-coupling provenance + flux vs the §3 frozen comparator;
  FLAG-LC1-B disposition = the lineage-map line is a FOSSIL CONFLATION (the EM-longitudinal
  Gauss-kill, true and derived, wrongly worded as covering the mechanical A1 grade) — relabel
  routed to the auditor lane, `def-9a4f07` sector-ownership adjudication routed to Grant
  (LC-1 §10 flag inherited). Kill-arm confirmation feeds Grant's arc-scope ruling.
- **SOURCELESS:** the named structure is stated with its receipt chain; LC-1 re-banks as
  structural compliance; the lineage-map line is vindicated; the Q1 revert's premise is
  surfaced for Grant re-examination (flag-don't-fix; no register edit here).
- **NOT-DERIVABLE:** the enumerated missing-piece list (expected head: the pluck operator;
  possibly the untraced #754 correspondence leg; possibly the cage-fork ensemble aggregation),
  each with two-method absence receipts. No invented coupling.
- **All outcomes:** no leaf/register/ledger/ruling edit; Tier-2 adversarial review dispatched on
  the PR before anything surfaces as CLEARED; rotational fence-reach → STOP + Grant.

---

> **Freeze provenance.** Prereg authored from first-party reads at worktree HEAD `91a910f8`
> (= `origin/main` at lane start); comparator re-retrieved from source abstracts via WebFetch
> this session BEFORE any flux number existed on this branch (§3); commits ALONE and pushes
> before any derivation content (Step 3.11 freeze-by-push — the push timestamp is the freeze
> event). SVA v0.2-pilot §0 header filled, all 11 rows. Frozen criteria travel verbatim or as
> file:line pointers per the brief; the one DISPATCH-GLOSS in this document is the §4 candidate
> list's lettering (C-a…C-f), which is this lane's organizational labeling of corpus objects
> quoted at their sources.
