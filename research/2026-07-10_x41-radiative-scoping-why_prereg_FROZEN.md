# X41 — THE "NOT YET WHY" ARC: the SVE radiative-scoping open postulate — three keys vs the round-3 exclusions [FROZEN prereg]

**Date:** 2026-07-10 · **Lane:** implementer · **Branch:** `analysis/x41-scoping-why` (off `origin/main` @ `117d9063`)
**Class:** PRE-REGISTRATION of a **derivation fork the CORPUS + AXIOMS + EXISTING DATA adjudicate** (NOT
the engine — P10 / X36 lesson, §8). No claim minted; no number retracted; the Letter (`papers/`) is
FIREWALLED OUT of this arc (zero edits regardless of outcome).
**Freeze act:** this file is FROZEN on the commit that PUSHES it. Any later change is ERRATA-BANNER-ONLY
(append-only; git is the trail). The freeze GATES the derivation doc (Step 3), which is committed AFTER
this push (git ordering = freeze proof, per the freeze-by-push rule).

---

## 0. SKILL-SELECTION PLAN (60-sec, per pre-workstream planning)

- **challenge-canonical-negative** (Step 1): the round-3 ε-DC-exclusion is a *merged* negative (#547);
  a new "why" that "explains it away" must grep the test CONFIGS, not the conclusions — the ½/¼
  over-determination tell and the reconstructed-closed-path risk are live.
- **substrate-native-check / sector-declaration** (all steps): this arc IS about a sector boundary
  (which sector's constitutive law loads); every claim declares MODE/REGIME/SECTOR and A1⊥T2 ownership.
- **phase-space-coordinate-check (A46)**: the keys are phase-space statements (transverse projection /
  propagating-mode basis / impedance plane); marks measured in matching coordinates, not lattice-Cartesian.
- **consistency-vs-emergence**: the muon/pump/copper marks ride CODATA-derived `E_c=√α·E_crit`; any
  "derived-why" verdict is tagged (identity/manifestation/consistency/emergence), no emergence headline.
- **verify-before-cite (A43 v2)**: every file:line + verbatim quote re-grepped at branch tip.
- **flag-don't-fix**: the task's mandatory "muon-zero" mark CONTRADICTS the merged #547 "muon-loads";
  surfaced with both verbatim, NOT reconciled by fiat (§4).
- **pre-test-physics-check**: the one plumber-physical question is surfaced to Grant in §6.

Retro-pass gate: before the Step-5 result commit, re-diff the applied-skill set against this plan.

---

## SECTOR HEADER (MODE / REGIME / SECTOR — extra care: this arc is a sector-boundary question)

- **MODE:** analysis + derivation-from-axioms. **NOT an engine keying-fire** (X36: the engine cannot
  adjudicate a keying choice about its own kernel — §8).
- **REGIME:** the two anchors span two regimes — (i) the Letter's registered **radiative** sector
  (deep-cold, sub-yield, weak-field `A²≈6e-7`, time-varying transverse pump+probe, on-line/propagating);
  (ii) the **atomic-static** sector (held Coulomb, strong near-nucleus field up to `A=1/√2`, DC).
- **SECTOR (the load-bearing declaration):** this arc concerns the **ε-grade = transverse-T2 permittivity
  channel** (`ε_eff=ε₀S`, the object that sets the refractive index a transverse-EM probe reads —
  `manuscript/ave-kb/CLAUDE.md`:73). It is **distinct** from the **longitudinal-A1 bond compliance**
  (`C_eff=C₀/S`), an **orthogonal reactance** (A1 ⊥ T2, `CLAUDE.md`:73, `master-equation.md:20`); and
  distinct again from the **µ / microrotational (Cosserat-B) sector** (Route C, the consistency leg §7).
  Every claim below states which sector's constitutive law it concerns.

---

## 1. THE OPEN POSTULATE (verbatim, firewalled Letter) + the walked framing

**Letter `papers/2026_birefringence_letter/main.tex`:446–454 (VERBATIM, re-grepped at branch tip):**
> "The radiative scoping is at present an *open postulate*: it states *that* the law is radiative-sector,
> not yet *why*. The dynamical scoping routes proposed to derive it — keys that would separate a
> propagating from a held field by its dynamical content, and the weaker escape that a held static field
> might load the permittivity only slightly, cut off at `ℓ_node` — have each been tested and excluded;
> static fields do not sit inside the model as a small effect, they sit outside it. The magnetic sector's
> circulation keying [Eq. (staticB), static-B transparency] is a distinct sector again and is unaffected
> by any of this."

**The walked framing (Grant + orchestrator, in-chat 2026-07-10 — the arc's ratified starting picture).**
The µ-sector's circulation keying (Route C) is the statement "the kernel saturates on the MEDIUM'S OWN
wave content and is transparent to SOURCE-SLAVED content." The E-sector why-candidates, three keys:

- **K1 — THE PROJECTION KEY.** The kernel loads on the **Helmholtz-TRANSVERSE** part of E at full
  magnitude (geometric/sector key). Longitudinal/Coulomb content is source-slaved (`∇·E=ρ/ε₀` slaves it)
  → transparent. Corpus form: the ε-kernel is a **T2-transverse** element; longitudinal content belongs
  to the **V-sector / A1** with its own accounting.
- **K2 — THE IMPEDANCE / MODE-BASIS KEY (Grant's, EE-native — the PREFERRED REGISTER).** The kernel is
  the nonlinearity OF the propagating channel; content loads **iff** it lives in the **propagating-mode
  basis** (radiation-resistance-coupled). A static E field = the **|Γ|=1 limit** — zero radiation
  resistance at `ω→0`, totally mismatched, stored **reactive** energy that **never gets on the line** →
  never meets the kernel. Ax3 closure: **`R_rad ≡ Z₀`** is the lossless vacuum's ONLY resistance — "the
  kernel lives inside the only resistor the vacuum has." **CRITICAL SUB-POINT (frozen as analytic
  expectation):** the key is **BASIS MEMBERSHIP, not net Poynting** — a cavity standing wave = two
  counter-propagating modes, fully on-line, **MUST load despite zero net flux**.
- **K3 — THE DYNAMICAL-CONTENT KEY.** Loads on time-varying fields (blind to a held static field).

**The KNOWN TRAP (killed in-chat, recorded).** The naive symmetric mapping "ε keys on `∮E·dl`" gives the
E-loading a `(kℓ_node)` suppression and kills the Letter's pump prediction by ~10¹¹. **Any surviving key
must preserve the Letter's transverse-pump FULL-MAGNITUDE loading EXACTLY** — i.e. key on the AMPLITUDE
of the transverse/on-line content, not on its spatial circulation. (K1 and K2 satisfy this by
construction: both are amplitude keys. K3's `∮E·dl`/beat member is the trap and is dead — see §2.)

---

## 2. STEP-1 INVENTORY VERDICT — the challenge-canonical-negative gate (the round-3 dead routes, verbatim)

The ε-side DC-exclusion arc ran in three rounds. Extracted VERBATIM (configs + why each died):

### The tested-and-excluded routes (verbatim configs)

- **ROUND 1 — LOCAL Poynting `E×H`** (`research/2026-07-05_em-saturation-keying-functional_RESULT.md`).
  Config: `S_E=√(1−c·𝒯)`, `𝒯=⟨E×H⟩/S_yield`. **DIED [CONSTRAINT-KILLED]:** the physical muonic atom is
  NOT transport-dead — the proton magnetic moment makes a **permanent static `E×H` circulation**
  (hidden-momentum, divergence-free but locally nonzero); the local functional engages at **1278× the
  2.3 µeV CREMA window** (−2939 µeV @ `r_cut=0.5a_µ`; +6.85 µeV @ `2a_µ` = 3×). Forward pointer: net-flux.

- **ROUND 2 — NET closed-surface flux** (`research/2026-07-05_em-keying-round2-worked-cell_RESULT.md`,
  STEP 0). Config: key on `∮_∂V S·dA = −dU_cell/dt`. **DIED [DEGENERATE — KILLED AT STEP 0]:** by
  Poynting's theorem the cycle-average net flux is **ZERO for ANY steady state — the steady pump as much
  as the atom's steady loop.** Net-flux keying would **blind the pump** and kill Table I. (This is the
  reason K2 must be BASIS-MEMBERSHIP, not net flux — §5.)

- **ROUND 2 — TIME-VARIANCE `𝒲_var=Var_t(E)/E_c²` and the temporal-gradient BEAT `𝒲_beat=⟨(∂_tE)²⟩/ω_C²`**
  (same doc). **BEAT DIED** by frequency-independence (would collapse the pump by `(ω/ω_C)²≈9e-12`, ~10¹¹
  below — the KNOWN TRAP of §1). **VARIANCE was `[SELECTED-NOT-DERIVED]`:** the cell ledger forces the
  MEAN-SQUARE (DC-included), not the variance; the DC-blindness that would make static-E transparent
  "requires a NEW ε-side Lenz-dual mechanism the corpus does NOT supply."

- **ROUND 3a — the four DC-block mechanisms M0/M1/M2/M3** (`research/2026-07-06_em-keying-round3-eps-dc-mechanism_RESULT.md`,
  merged **#547**). Verbatim configs and deaths:
  - **M0 (kernel-argument NULL):** the ε-kernel argument is `A_V=V/V_yield=|E|/E_yield`, a **static-capable
    amplitude**; leading mean deficit `⟨1−S⟩=½⟨A_V²⟩` keeps the DC baseline `a_0²` → **a held DC alone
    loads** (H1). "H2 would need an axiom-level reinterpretation."
  - **M1 (series-C DC-block):** FALSIFIED by topology (two-topology COMPUTED). The canonical
    **series-L-bond / shunt-C-node** unit passes the held DC to the varactor node; the only SERIES
    reactance is the bond inductor (the B-side Lenz element). "The ε-grade's element is the shunt
    capacitor with no such series partner."
  - **M2 (spectator mode):** FALSIFIED (two-route ledger, zero residual) — held energy fully accounted
    IN the kernel-bearing ε element; "no linear spectator capacitance to park it on."
  - **M3 (lossless slide / floppy zero-mode):** FAILS losslessly; and at LATTICE level **settled CLOSED
    (rigid)** — the K4 translational (E-coupled) sector carries shear stiffness `k_s>0`, so "no floppy
    zero-mode absorbs a held strain" (`k4-bloch-dispersion-quartic.md`:58).
  - **VERDICT:** **[DERIVED: CHARGE-KEYED]** — the ε-varactor keys on the MEAN-SQUARE, **DC-INCLUDED**;
    "the E-side worked-keying rescue (a Lenz-dual DC-blindness for the varactor) does not exist in the
    canonical structure." With a **gauge-observability RIDER**: a spatially-UNIFORM held DC bias is
    gauge-hidden (INVARIANT-S2), but a NON-uniform held field (Coulomb) "IS readable and DOES load."

- **ROUND 3b — RADIATIVE FAR-FIELD character `F`** (`research/2026-07-08_p5-radiative-far-field-keying_RESULT.md`).
  Config: key on the radiated-power fraction `F`. **DIED [RADIATIVE-KEY-REFUTED]:** loading is
  DECORRELATED from `F` — a **static E loads with `F=0`** and a **standing wave loads with net `F≈0`**;
  the only transparent config (static B) also has `F=0`. "Each channel keys on a LOCAL phase-space
  coordinate, not on the field's global radiative character."

**The two Letter-quoted routes map exactly:** "keys that would separate a propagating from a held field
by its **dynamical content**" = the K3 family (round-1 Poynting, round-2 beat+variance, round-3b
far-field) — each tested and excluded. "The **weaker escape** that a held static field might load the
permittivity only slightly, **cut off at `ℓ_node`**" = the [B-AVE] lattice-cutoff arm — excluded by the
muonic `[C-EXCLUDED]` on the **super-pitch band alone** (`p4-forward-voltage-threshold_RESULT.md`; the
`~103%`-from-sub-pitch band-split, round-3 §9) plus the high-Z X-ray transport kill.

### K1 / K2 / K3 — alive or DEAD-ON-ARRIVAL (the HARD GATE)

| key | status | why |
|---|---|---|
| **K3** dynamical-content | **DEAD ON ARRIVAL** | It IS the excluded dynamical-content family: round-1 (local Poynting killed), round-2 (net-flux killed; beat killed by freq-independence; variance selected-not-derived and re-killed by the ledger-forced mean-square), round-3b (far-field refuted). The Letter says so verbatim ("have each been tested and excluded"). No further test owed. |
| **K1** transverse projection | **ALIVE — but CONTESTED by #547** | **Distinct from every tested config:** no route decomposed E into Helmholtz longitudinal/transverse — all measured full `|E|` (M0/M2/p5), `E×H` (round-1), net flux, `Var_t`, or `F`. **Canonical basis:** A1⊥T2 orthogonal reactances (`CLAUDE.md`:73) + "a transverse-EM readout can **NEVER** pick up the A1 compliance" (`research/2026-07-07_semiconductor-cv-dip_RESULT.md`:215, #624). **Headwind:** #547 M0 sets the argument to the full `A_V=|E|` (node potential, `node-up`:104-106); K1 must overturn that to the transverse projection. |
| **K2** impedance / mode-basis | **ALIVE — but CONTESTED by #547** | **Distinct from every tested config:** net-flux was KILLED but K2 ≠ net-flux (explicitly basis-membership, §5); far-field-`F` was REFUTED but K2 ≠ `F` (a standing wave has `F=0` yet is a basis-member); M2 spectator-mode ≠ K2's R_rad-placement of the kernel. **Canonical basis:** `R_rad≡Z_0` is the vacuum's only real port (`substrate-native-terminology.md`:43). **The p5 standing-wave datum CONFIRMS the critical sub-point** (loads at `F≈0` — §5). **Headwind:** #547 places the S-kernel in the REACTANCES (`ε_eff=ε₀S`, `C_eff=C₀/S`), not in `R_rad`; K2 must relocate it. |

**HARD-GATE RULING:** **neither K1 nor K2 is dead-on-arrival** (neither is an already-excluded config).
**BUT both must OVERTURN the merged #547 `[DERIVED: CHARGE-KEYED]`** (which used the full-`|E|` argument
via the reactance). That confrontation is the Step-3 derivation. **K3 is banked DEAD.** If BOTH K1 and K2
fail the derivation, the honest arc outcome is **"the postulate stays open, now with the routes closed and
the boundary DATA-acquired (charge-keyed + scope-out)"** — a VALID result, recorded not rescued.

---

## 3. THE FROZEN BRANCHES (record-all, NO preferred outcome)

Record each surviving key as a branch with its adjudication target. No branch is favored; K2 is Grant's
**preferred register** for expressing the answer, which is NOT the same as a preferred outcome.

- **Branch-K1 [PROJECTION].** The T2-transverse permittivity keys on the transverse (solenoidal) part of
  E at full magnitude. Predicts: pump (transverse) loads full; held Coulomb (longitudinal) → the T2
  permittivity sees ZERO argument → transparent (the longitudinal load goes to the orthogonal A1
  compliance, which a transverse-EM birefringence readout cannot pick up). Near-zone (§6): the
  transverse-reactive induction field IS Helmholtz-transverse → **K1 says LOADS.**
- **Branch-K2 [IMPEDANCE / MODE-BASIS].** The kernel is the nonlinearity of the on-line (R_rad-coupled)
  channel; content loads iff it is a propagating-mode-basis member. Predicts: pump loads full; standing
  wave loads (basis-member despite `F=0`); held Coulomb = `|Γ|=1` reactive, off-line → transparent.
  Near-zone (§6): the transverse-reactive induction field is stored reactive energy, off-line → **K2 says
  NOT.**
- **Branch-NULL [POSTULATE-STAYS-OPEN].** Both K1 and K2 fail to overturn #547; the boundary is
  data-acquired (charge-keyed loads + muon scope-out). The arc closes with **three closed routes**
  (K3 dead; K1, K2 derived-against) and the postulate honestly open. This is a pre-registered valid bin.

---

## 4. THE TWO MANDATORY REPRODUCTION MARKS (with the #547 conflict flagged, NOT reconciled)

- **MARK-1 (pump, EXACT).** The Letter's registered transverse pump+probe: `A²≈6e-7`, `δn_bir=−½A²`,
  loads at FULL magnitude (no `(kℓ_node)` suppression — the KNOWN TRAP). **Every surviving key MUST
  reproduce this EXACTLY.** A key that suppresses the pump load is falsified on MARK-1 alone.
- **MARK-2 (held Coulomb / muon).** The task freezes this as **"muonic-H longitudinal → ZERO, EXACT"**
  (transparent). **⚑ FLAG-DON'T-FIX — this mark DIRECTLY CONTRADICTS merged #547.** Verbatim conflict,
  both sides recorded:
  - **Task/K1/K2 target** (walked framing): held Coulomb (longitudinal) → the T2 permittivity loads
    **ZERO** → the muon passes by DERIVED transparency.
  - **#547 canon** (`em-keying-round3-eps-dc-mechanism_RESULT.md` §9): the held Coulomb (non-uniform,
    static-in-time, spatial gradient) **LOADS** under charge-keying — the interior-excluded bracket
    integral gives **`1.52×10⁶ µeV`, overshooting the CREMA window by ~6.6×10⁵×** — reproducing #539
    `[C-EXCLUDED]`. The boundary there is **DATA-acquired (scope-out)**, NOT derived transparency.
  - **Both reduce to the same Letter-level statement** ("the Letter makes no falsified static-E
    prediction") — via *scope-out* under #547, via *transparency* under K1/K2. **The discriminator is
    MECHANISM-CLASS, not data-class:** the muon anchor alone cannot separate "loads-then-scoped-out" from
    "never-loads." **Therefore MARK-2 is reproduced by ANY branch at the data level; reproducing it as
    DERIVED ZERO (not scope-out) is exactly the claim K1/K2 must earn against #547.** Recorded as the
    load-bearing tension of the arc, surfaced not hidden.

---

## 5. THE STANDING-WAVE EXPECTATION (K2 critical sub-point) + the confirming datum

**Frozen analytic expectation (K2):** a cavity standing wave = two counter-propagating modes, fully
on-line (both are propagating-mode-basis members) → **K2 MUST predict it LOADS despite zero NET flux.** A
key that blinds the standing wave (as pure net-Poynting keying does) is NOT K2 and is falsified — this is
the exact discriminator that killed round-2 net-flux.

**Existing confirming datum (p5, already in corpus):** `p5-radiative-far-field-keying_RESULT.md` standing-
wave CONTROL: `√⟨A_V²⟩=0.299623`, ε-deficit `1−S_ε=4.750×10⁻²`, **net `F=1.17×10⁻¹⁸≈0`**, verdict
**LOAD**. So the corpus ALREADY contains the datum that a zero-net-flux standing wave loads — **consistent
with K2's basis-membership sub-point and inconsistent with net-flux keying.** (This does NOT by itself
select K2 over #547-charge-keying: both load the standing wave. It only confirms K2's internal
consistency and kills net-flux.)

---

## 6. THE K1-vs-K2 DISCRIMINATOR: the transverse-reactive near-zone + the plumber-physical question

**The discriminator (frozen).** The **transverse-reactive near-zone** — antenna induction `1/r²` terms:
Helmholtz-**transverse** + time-varying + source-attached, stored reactive energy that does not radiate:
- **K1 says LOADS** (it is Helmholtz-transverse → nonzero transverse projection).
- **K2 says NOT** (it is reactive / off-line / `|Γ|=1` → never meets the R_rad kernel).
Both K1 and K2 reproduce the two anchors identically (free wave: full load; held Coulomb: zero); **the
near-zone is where they split.**

**Existing-data touch (inventory, per Step-4c).** Expected answer: **none for the ε transverse-reactive
near-zone.** The closest existing corpus touch is the **µ-sector `(kr)²` near-zone table** (p5:
`A_I∝(kr)²` suppression of the reactive magnetic near-zone) — but that is the µ/circulation sector, not
the ε transverse-reactive near-zone, and it is consistent with BOTH keys. The ε-side discriminator is
**UNTESTED** → a **registered-probe CANDIDATE (flagged, NOT registered here):** a facility driving a
transverse-reactive near-zone E (antenna induction zone, `kr≲1`) and reading vacuum ε-loading would
split K1 from K2. Naming it is Step-4c's product; registering it is a gated follow-on.

**THE ONE PLUMBER-PHYSICAL QUESTION (pre-test-physics-check, surfaced to Grant):** *In the antenna
induction zone — the stored, non-radiating, transverse `1/r²` reactive field around a small oscillating
dipole — does the vacuum's ε-nonlinearity engage (it is transverse energy, K1) or stay silent (it is
reactive energy that never gets on the line, K2)?* This is the physical fork the whole arc reduces to,
and no existing datum answers it.

---

## 7. THE µ-SIDE CONSISTENCY LEG (the unified principle must re-derive Route C)

Any surviving E-key must, under one principle, re-derive the µ-sector's **circulation keying** (Route C,
canon: `S_B=√(1−A_I²)`, `A_I=|∮H·dℓ|/I_max`; static B → `∂_tB=0` → `A_I=0` → transparent). Frozen
expectation (both keys pass — this is a CONSISTENCY check, not a discriminator):
- **K1 unification:** "medium's own content vs source-slaved content." Static Biot-Savart B is
  source-slaved (bookkeeping) → the µ-kernel keys on the CIRCULATION (the medium's own solenoidal
  content) → static B transparent. The E-dual: longitudinal Coulomb is source-slaved (`∇·E=ρ/ε₀`) →
  transparent. **Both sectors: source-slaved → transparent** — one principle.
- **K2 unification:** a static B is the `|Γ|=1` reactive limit (zero radiation resistance) → off-line →
  transparent, identical to the static-E off-line argument. **Both sectors: reactive/off-line →
  transparent** — one principle. The p5 `(kr)²` µ near-zone suppression is consistent (reactive → off-line).

**Fail-condition (frozen):** if a surviving E-key does NOT re-derive Route C under its own principle, that
is a DIVERGENCE and is reported honestly (the key would then be E-sector-only, not unified).

---

## 8. FIREABILITY DECLARATIONS (P10 / the X36 lesson) — who adjudicates

**PLAINLY: the ENGINE CANNOT ADJUDICATE keying choices about its own kernel.** A lattice FDTD/eigsolve
takes the keying (what the kernel's argument IS) as an INPUT; it cannot output which keying is correct —
firing it "to decide K1 vs K2" would validate whichever argument was coded in (the X36 lesson). **The
adjudicators are: (A) EXISTING DATA + (B) DERIVATION-FROM-AXIOMS/CANON.** Per-branch fireability:

- **K3:** already fired and DEAD — adjudicated by existing data (muon re-kill under mean-square, #539/#547)
  + derivation (round-3 M0–M3). No re-fire owed.
- **K1:** fireable by **DERIVATION** — does the K4/T2 lattice structure make the T2-permittivity's argument
  the transverse projection (excluding the longitudinal), with the longitudinal load owned by the
  orthogonal A1 compliance? This is a structural sector-decomposition question answerable from
  axioms/canon (A1⊥T2, `CLAUDE.md`:73; the anti-cross-wire guarantee, #624:215), NOT an engine keying-fire.
  Weakly confrontable by data (standing-wave loads → consistent; muon cannot discriminate — §4).
- **K2:** fireable by **DERIVATION** — is the Axiom-4 kernel the nonlinearity of the reactance (#547) or of
  `R_rad`/the on-line channel (K2)? A structural question about where Axiom-4 sits in the translation
  circuit (`R_rad≡Z_0`, `substrate-native-terminology.md`:43; Op14 `Z_eff=Z_0/√S`, `operators.md`:54).
  Confrontable by the standing-wave datum (§5). NOT an engine keying-fire.
- **The near-zone discriminator (§6):** fireable by **NEITHER data nor (yet) derivation** cleanly — it is
  the registered-probe candidate. If the Step-3 derivation forces one key, the near-zone becomes that
  key's forward prediction; if not, it stays the named future probe.

---

## 9. SEDUCTION FLAGS (armed before any derivation)

- **⚑ The #624 A1/T2 cross-wire RHYME = RHYME, NOT EVIDENCE.** #624 (`semiconductor-cv-dip_RESULT.md`)
  found `node-up`:105/:370 cross-wires A1's form (`C₀/S`) onto T2's key (`V_yield`), and #547 used the
  same `node-up` form for its charge-keyed argument. This RHYMES with K1 (it SUGGESTS #547's `A_V=|E|`
  inherited a sector cross-wire). **It is not proof K1 is right** — the sector-ownership rhyme with the
  #624 finding is a pattern, not a derivation. Step 3 must independently check #547's actual
  sector-handling (verify-before-cite), not assume the rhyme adjudicates.
- **⚑ The forward-prediction ledger is 0-for-N and THINNING** (`forward-prediction-register.md`:69,81,252:
  the bankable AVE-distinct list is thinning; multiple candidate chords RESOLVED-TO-CONSISTENCY / SM-shared,
  not AVE-distinct). A "we found the WHY" verdict is exactly the seductive shape the discipline guards
  against; a derived-why must survive the symmetric standard (does SM/QED get the same pass for scoping a
  constitutive law by regime?).
- **⚑ The ½/¼/³⁄₂ over-determination tell** (challenge-canonical-negative): any coefficient that lands on
  `½`/`¼` is checked as the Letter's own DERIVED kernel coefficient (`δn=−½A²`, `−¼A²`) or the sqrt-kernel
  expansion `1−½A²`, NOT a fresh tell. The M3 chord-vs-tangent `½`-vs-`³⁄₂` fork is a KNOWN open
  Grant-adjudication item (#547 flags), not to be silently crowned.
- **⚑ The KNOWN TRAP** (§1): a key that reduces to `∮E·dl` circulation is the `(kℓ_node)`-suppressed dead
  route. Guarded: K1/K2 are amplitude keys by construction; any drift toward circulation-keying is a
  self-check FAIL.

---

## 10. FROZEN ADJUDICATION CRITERIA (Rule 11 — no post-hoc criterion drops)

The Step-3 derivation + Step-4 data confrontation route to exactly one bin (the substrate/axioms pick it;
criteria frozen here before the derivation):

- **[DERIVED-WHY: K1]** — the K4/T2 sector-decomposition forces the T2-permittivity argument to be the
  transverse projection, overturning #547's full-`|E|` for the longitudinal sector, WITHOUT breaking
  MARK-1 (pump) or the µ-leg (§7). Held Coulomb → derived transparency. Requires resolving the #547
  varactor-identity tangle (surfaced to Grant, not decreed).
- **[DERIVED-WHY: K2]** — the translation-circuit structure forces the Axiom-4 kernel into `R_rad`/the
  on-line channel, so a held reactive field never meets it, WITHOUT breaking MARK-1 or the standing-wave
  expectation (§5) or the µ-leg.
- **[UNDERDETERMINED — K1∧K2 with the named discriminator]** — both reproduce both anchors + the µ-leg and
  the derivation does not force one; the near-zone probe (§6) is the named discriminator. (Honest tie.)
- **[POSTULATE-STAYS-OPEN — three closed routes]** — neither K1 nor K2 overturns #547 from the axioms; the
  boundary is data-acquired (charge-keyed + muon scope-out). K3 dead; K1, K2 derived-against. VALID
  negative.

**No criterion is dropped to convert a ❌ to a ✅.** If K1/K2 fail, the arc records the NULL bin; it does
NOT refill the slot with a fourth key (Rule 12: a new key gets a new version + verification chain).

---

## 11. DISCIPLINE TAGS + PROVENANCE

- **challenge-canonical-negative:** the round-3 negative (#547) is challenged by grepping its CONFIGS
  (M0–M3, verbatim §2), not its conclusion; the ½/¼ tell + reconstructed-closed-path risk are flagged (§9).
- **substrate-adjudicates-forks:** K1/K2/NULL are pre-registered; the axioms+data resolve them, not fiat.
- **flag-don't-fix:** MARK-2 (task "muon-zero") vs #547 ("muon-loads") surfaced with both verbatim (§4);
  the #547 varactor-identity tangle carried OPEN (Grant's).
- **phase-space-coordinate-check (A46):** the keys and marks are phase-space (transverse projection /
  mode-basis / impedance-plane / `A_V` amplitude); no lattice-Cartesian↔φ² mismatch.
- **consistency-vs-emergence:** the marks ride CODATA-derived `E_c`; any derived-why is a
  MANIFESTATION/CONSISTENCY structural claim, NOT an emergence headline.
- **verify-before-cite (A43 v2):** every file:line re-grepped at branch tip `117d9063` this session
  (Letter :446-454; #547 verdict + §9; p5 standing-wave row; CLAUDE.md:73,75; substrate-native-terminology:43;
  operators.md:54; #624:215,337).
- **NO KB canonization from this lane** — research/ docs only; a Letter/KB upgrade is a gated follow-on on
  adversarial review + Grant. The Letter (`papers/`) is FIREWALLED (zero edits).
- **P10 / X36:** the engine cannot adjudicate its own kernel's keying (§8); adjudicators = data + derivation.
