# THE BIAS PROPAGATION THEOREM (R49b/R50) — FROZEN PRE-REGISTRATION

**Date:** 2026-08-10 · **Branch:** `lane/2026-08-10-bias-propagation` · **Base:** `origin/main` @ `ad376e9a`
**Lane brief:** [`_orchestration/2026-08-10_bias-propagation-brief.md`](../_orchestration/2026-08-10_bias-propagation-brief.md)
**Class:** DERIVATION. **Mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; edits no KB leaf, register, ledger, ruling, or manuscript file; changes no solidity; all propagation ROUTED.** Engine `src/ave` byte-untouched (`backreaction.py` READ-ONLY; may be run, never edited — stencil-lens fence).
**Presentation state:** `[DO-NOT-MERGE]`. Own Tier-2 before any CLEARED wording. Rule 11 (no post-hoc criterion drops) binds every bin below. Rule 12 (substitution-not-retraction) binds every later amendment.

**THIS DOCUMENT COMMITS ALONE AND PUSHES FIRST** (freeze-by-push, ave-prereg Step 3.11). No driver code, no derivation content, and no lane-produced number exists in the tree at its push. The driver commits second; the result last.

---

## §0 — Standard Vacuum Analysis header (SVA v0.2-pilot; all 11 rows)

1. **SECTOR / OWNERSHIP:** the object under derivation is **THE BIAS** `ε₁₁` — the A1 bulk/dilatation sector's operating-point scalar (R50: "the field ε₁₁'s canonical name is THE BIAS"). Its gradient's partner is **THE BOUND RESPONSE** `u₀` (R38/R50 noun; controlled-source class). **T2 shear/EM and Cosserat micro-rotation are byte-untouched**; no step routes bulk-slot content through micro-rotation (rotational fence-reach STOP re-checked at each deliverable). TWO-"3"s guard active (mass = A1 accounting; charge = Cosserat winding — never cross-wired). **THREE OBJECTS named per use throughout, never merged:** the bias `ε₁₁` (potential-class), the bound response `u₀` (field-class), the fast-layer displacement/momentum pair `(u, π)` (state-class). The bound-constitutive lane's C1 CRITICAL was exactly the crossing of this split; it is a standing gate here (G-OBJECTS).
2. **REGIME / PHASE-STATE:** MODE = constitutive-dynamics derivation (circuit-class classification + frequency response). REGIME = **crystalline cold-linear Regime-I, sub-yield, lossless-reactive**, exterior `D(A) → 1`; **past-wall and the `D(A) → ∞` wall are OUT OF SCOPE** and are never evaluated. PHASE-STATE = cold-reactive. DC bias point = the Ax5 clause-Q quiescent point (`∇·π = 0`, `θ = 0`, `ε₁₁ = 0` away from defects). Per R45 the axiom is comprehensive with phase structure inside: the **de-bonded and pre-freeze phases are NAMED-OPEN and this lane writes no law for them.**
3. **CIRCUIT STATEMENT (before any framework word):** clause G says the bias is a **node potential** on a graded conductance network — `−∇·[κD(A)∇ε₁₁] = T₀₀` is a DC load-flow solve, `κ` the sector permittivity, `D(A)` the saturation-graded conductance, `T₀₀` the injected current. The lane's question in circuit terms: **what is in series with that shunt network?** A series **R** ⇒ diffusive line; a series **L** ⇒ hyperbolic line; a **conjugate quadrature** ⇒ Schrödinger-class line; **nothing** ⇒ not a line at all, and the bias is slaved to the layer beneath it. **Total-vs-slot declared: the measurable is the TOTAL bias at a field point vs time, never a per-series-element share.**
4. **PLANE & PROJECTION:** no Γ or Z claim is made by this lane. The frequency-response reference plane is **a field point at fixed radius `r` from a source of angular drive frequency `ω`**, and the projection is the **near-zone residual against the instantaneous elliptic solve** — the same object, same plane, as the bound-constitutive lane's R8 receipt (`3.0%`, quadratic-class ω-scaling), so the two are directly comparable. Any dispersion relation reported is stated in the global (lattice/vacuum) frame with the clock named (R50/§9 vocab-cage: "frequency" without a named clock is under-specified).
5. **CONSTITUTIVE PROVENANCE:** `κ = c⁴/7G` **IMPORTED** (#261, byte-unchanged, not re-argued). `ν_vac = 2/7` **IMPORTED** (#261). `D(A) = 1/√(1−(A/A_yield)²)` **DERIVED-SHAPE / VALUE-IMPORTED** (Ax4; `A_yield` inherits `K=2G`). `ε₁₁ = 7GM/c²r` **canon profile** under the declared 4π source convention (`gordon-optical-metric.md:25`, clm-rd9cjm; R49(a)). `𝒜_g` **UNVALUED-RATIFIED-CONSTANT** (R48) — **this lane values it nowhere and consumes it only where it cancels.** The fast layer `ρü = −ρc²∇×(∇×u)` **DERIVED** (`eq_axiom_3.tex:18`, temporal-gauge-literal per the bound-constitutive item-(0) adjudication). Ax5 clauses S/G/Q **RATIFIED-AXIOMATIC, not derived** (R43/R44).
6. **ENERGY LEDGER:** rim-only. Axiom 3 forbids dissipation in the cold phase; **no "loss", "dissipated", "damped", or "Joule" may appear in any derived statement without naming a boundary-crossing port.** A diffusive verdict (RC) therefore carries a mandatory ledger obligation: exhibit where the energy goes, or show the "R" is a pure redistribution coefficient with the ledger closed inside the fast layer. **If neither can be exhibited, RC-DIFFUSIVE fails on Ax3 and must be reported as failing, not softened.**
7. **CALIBRATABILITY:** the primary targets are **dimensionless**: the circuit class (a label), the ω-scaling exponent `p` (dimensionless), the speed ratio `c_g/c` (dimensionless), and the force ratio `F_derived/F_Newton` (dimensionless). **No target requires an absolute `𝒜_g`, an absolute `B(M)`, or a unit bridge** — this is deliberate, and it is why the lane is runnable while `𝒜_g` is UNVALUED (R48/#946 C1).
8. **DISCRIMINATION CLASS:** **DC-internal** for deliverables 1 and 2 (the bias IS the DC sector; both are internal-coherence adjudications), **DC→AC coupling** for deliverable 3 (a DC medium property read out through a timing channel). **NO CHORD IS CLAIMED ON ANY OUTCOME.** SM/GR counterfactual pre-run and stated: GR carries the identical structure (a static metric potential whose gradient is the Newtonian force; a hyperbolic completion whose static limit is Poisson), so **a success here is PEER-with-GR, consistency-class — the opposite of distinct.** The tautology filter is armed and pre-fired on deliverable 2 at §4 below.
9. **CERTIFICATION PLAN:** all gates and bins frozen here, before any number exists. **UNRUN ≠ PASSED.** Negative controls named at §5 (three analytic Green's functions with known `p`). Liveness pair required in both directions (the instrument must see the class where it exists AND its absence where it doesn't).
10. **ADJUDICATION ROUTING:** §6. Every outcome's propagation is stated in advance, including the lane's fence on itself. **The substrate adjudicates; nothing here is decided by fiat, and the seductive-structural outcome is ROUTED, never asserted (brief fence).**
11. **NUMERICAL CONDITIONING:** the near-zone residual is a small difference of large quasi-static fields — the named cancellation. Working precision: float64 with a `mpmath` 50-digit arm on the analytic legs; the residual extraction reports its own floor, and **any `p` fitted on residuals within 10× of that floor is reported UNRESOLVED, not fitted** (Step 3.7(b) gate-floor consistency). Fit windows frozen at §5.

---

## §1 — The derivation target, stated precisely (ave-prereg Step 1)

**Derive the circuit class and frequency response of the bias sector's own dynamics** — the dynamical completion of clause G's elliptic abstraction `−∇·[κD(A)∇ε₁₁] = T₀₀` — **from the fast layer alone (no new hardware)**, and **derive the coupling law of a trapped resonance to the bias gradient**; then state the observable settling signature and its magnitude for the two pulsar comparators.

This discharges Axiom 5's named-open entry (the bias propagation debt, R49(b) as renamed by R50) and adjudicates the walk-level mechanism seeds of `research/2026-08-06_rotation-substance-ontology_framing-note.md` §10, which are **⚑ UN-AUDITED INPUT, NOT RECEIPTS**.

## §1.5 — The physical picture, in plumber terms (ave-prereg Step 1.5)

- **What the bias is.** Every node is an LC tank. A mass defect loads the tanks around it — the bias `ε₁₁` is *how hard each tank is loaded*, measured toward the yield wall (`A = (2/7)ε₁₁ = r_s/r` in the gravitational dialect; `A = 1` at the horizon, Ax4). Loaded tanks tick slower. The bias is the DC operating point; signals ride on top of it.
- **What the bias is NOT.** It is not the bound response `u₀` (that is the displacement pattern, the field), and per R48's walk note it must be a **distinct object from mechanical strain `∂u`** — the `ℓ_node²` hypothesis's 57-order miss is the evidence for that distinctness.
- **The question, physically.** Push the mass sideways. The far tanks must find out they are loaded differently. **Down what wire does that news travel, and how long does it take at each frequency?** Clause G, as written, answers "instantly, everywhere" — because a static load-flow solve has no clock in it. That is an abstraction, not a claim; the theorem's job is the dynamical completion.
- **The two-layer picture (the walk's own answer to the causality worry).** The bias is not a second medium stacked on the first; it is the *operating point of the first*. Nothing outruns `c` because everything IS the fast layer underneath — the power-rail analogy. If that is right, the bias has **no independent line at all**, and its delay is the fast layer's delay.
- **The coupling, physically.** A trapped resonance sitting in a loaded region ticks slow; its energy `ħω` measured at infinity is lower where the bias is deeper; energy downhill is a force. That is ponderomotive-class attraction — and it must come out to `GM/r²`, not near it.
- **The geometry.** Longitudinal *configuration* (curl-free `u₀ = −𝒜_g∇ε₁₁`), DC *dynamics*. **A longitudinal configuration is not a longitudinal wave** — the slot holds books. Whether the theorem re-introduces a longitudinal *wave* is the single most dangerous thing this lane can do, and §6-F1 arms the falsifier for it.

## §2 — Corpus state (ave-prereg Step 2; grep-verified at base `ad376e9a`)

**Corpus state: PARTIAL — substantial prior work, one directly-superseded prior lane.**

| Prior work | file:line | Bearing |
|---|---|---|
| Ax5 clause G (the elliptic abstraction being completed) | `manuscript/common_equations/eq_axiom_5.tex` @ `origin/docs/2026-08-10-r43-execution` (post-doc-lane-pass form; the ratified text travels verbatim per the brief) | **THE TARGET.** Also its "forbids" clause — see F1. |
| The bound-constitutive lane (the four-tuple, BC-SRC, R7/R8/R9 receipts) | `research/2026-08-10_bound-constitutive_result.md` | Fast layer's derived structure; **R8 already measured a quadratic-class ω-scaling of the near-zone residual on the `(u,π)` sector at 3.0%** — the pre-existing datum this lane's deliverable 1 must reconcile with, on the GRADE. |
| The bound-response carve (R38) | `research/2026-08-09_bound-response_result.md` | `NOT-DERIVABLE` re-cut; the four-item worklist whose item (i)/(ii) this lane's parent axiom discharged. |
| **The envelope-sector reduction lane (2026-07-20)** | `research/2026-07-20_envelope-sector-reduction_result.md`, `_derivation.md §2` | **DIRECTLY ON DELIVERABLE 1 — AND ITS SUBSTRATE IS SUPERSEDED.** Leg A ran a multiple-scales reduction and returned an **NLS-class (parabolic/Schrödinger) envelope equation about the cold vacuum**, with far-field radiation as the ponderomotive back-reaction of `\|A\|²` onto **the gapless P-branch**. The P-branch is exactly the structure the 2026-08 lanes established has **no preimage in the receipted dynamics** (bound-response §1 (a2)/(a3); bound-constitutive T1/R1/R3/R5). **The prior lane's carrier-envelope algebra is live input; its P-branch coupling leg is VACATED-BY-DRIFT (the cite drifted past the state it argued about) and is re-derived here, not inherited.** |
| The canon bias profile + 4π convention | `gordon-optical-metric.md:25,:28,:33` (clm-rd9cjm) | Deliverable 2's declared input, byte-quoted. |
| **The local clock is SLOPE-1** | `vol3/gravity/ch01-gravity-yield/temporal-spatial-lattice-decomposition.md`, W2 walk-back | **Deliverable 2's load-bearing canon:** `n_temporal = 1 + (2/7)ε₁₁` is the *bulk/propagation* index (slope 2), and is **explicitly distinguished** from the *local clock rate / gravitational redshift*, a **slope-1** quantity: `√g₀₀ ≈ 1 − GM/c²r`, `z = (n_temporal−1)/2`. |
| The Ax4 kernel and its `c_shear` exponent | `manuscript/common_equations/eq_axiom_4.tex` | `S(A) = √(1−A²)`, `D = 1/√(1−A²)`, `c_eff = c₀√S = c₀(1−A²)^{1/4}`; the **sign/ontology selector is flagged PENDING Grant** in the axiom text itself. **This is a SECOND, competing clock law — see §4 CLOCK-S.** |
| The pulsar comparators | `port-register.md:87,:93`; `research/2026-07-20_envelope-sector-reduction_result.md` §HEADLINE | `κ_max² = δ_DP = 1.3×10⁻⁴`, `κ_max = 0.0114` (double pulsar); Reading-A exclusion **9–110σ (Hulse-Taylor) / 100–1400× (double pulsar)**; envelope lane `κ_env² = 0.034` central, bracket `[0.0148, 0.1768]` ⇒ `114–1360×`. |
| `𝒜_g` is UNVALUED | `research/2026-08-10_ag-derivation_result.md` (#946) | Forces every target of this lane to be `𝒜_g`-free. Verified at §4. |

**What is genuinely open and is this lane's to close:** the dynamical completion itself. No corpus leaf writes a time-dependent law for `ε₁₁`. The elliptic solve in `src/ave/gravity/backreaction.py` is statics by design (`a1-port-sourcing:325-328`: *"STATICS design choice, not a derived constraint theorem"*), now axiom-licensed by clause G but still clock-free.

---

## §3 — ANALYTIC EXPECTATIONS, with numbers, frozen (ave-prereg Step 3.9; fork-record-BOTH is the default shape)

### §3.1 Deliverable 1 — the envelope reduction: four bins, each with its frozen dispersion relation

The brief freezes a **three-way** classification. Per the KEEP-BOTH discriminator pattern this prereg **preserves all three verbatim and adds a fourth residual bin rather than redefining any** — because the brief's own two-layer reduction ("the bias network is not a second medium — it is the fast layer's envelope") describes an outcome that is not one of the three. **The naming of bin 4 is ROUTED to Grant; the physics is adjudicated by receipts either way.**

| Bin | Frozen dispersion / response | Frozen near-zone ω-scaling exponent `p` | What it must exhibit to be awarded |
|---|---|---|---|
| **RC-DIFFUSIVE** | `(κ/𝒟)∂_tε₁₁ − ∇·[κD∇ε₁₁] = T₀₀`; `ω = −i𝒟k²`; response `∝ √(iω)` | **`p = 0.5`** | **The Ax3 ledger, closed** — where the energy goes, or that "R" is pure redistribution with the ledger closed in the fast layer. *Row 6 obligation; unmet ⇒ this bin FAILS, it does not soften.* |
| **LC-HYPERBOLIC** | `(κ/c_g²)∂²_tε₁₁ − ∇·[κD∇ε₁₁] = T₀₀`; `ω = c_g k/√D`; retarded, phase `e^{iωr/c_g}` | **`p = 2.0`** | **The bias inertia's hardware origin** — a fast-layer object supplying inertia density `𝓘 = κ/c_g²`. For `c_g = c` that is `𝓘 = 1.9237×10²⁶ kg/m` (§3.3). |
| **DISPERSIVE-UNITARY** | Schrödinger/NLS class: `iΛ∂_tψ = −∇·[κD∇ψ] + g\|ψ\|²ψ`; Bogoliubov `ω² = c_B²k² + (k²/2m*)²` | **`p = 1.0`** | **A carrier and a conjugate quadrature** — the bias must be one quadrature of a two-component object, and the carrier must exist in the cold phase (clause Q). |
| **NONE-OF-THE-THREE (residual; must be NAMED)** — the anticipated `SLAVED` shape | no independent line; `ε₁₁` an algebraic functional of the fast-layer state; delay inherited from the fast layer's own transport | **`p` inherits the fast layer's** — R8 measured **quadratic-class** on `(u,π)`, so **`p = 2.0` is the prediction here too** | **The slaving map, exhibited** — the explicit functional `ε₁₁[u, π]`, and a statement of why it is `𝒜_g`-consistent and `∂u`-distinct (R48). |

> **★THE DEGENERACY, DECLARED BEFORE THE RUN (Step 3.8(b) structural self-check).** **LC-HYPERBOLIC-at-`c` and SLAVED predict the SAME `p = 2.0`.** The ω-scaling exponent therefore **cannot** separate them, and any result doc that claims it did is wrong. The frozen separator is **not** `p`: it is **the far-zone pole test** — does the completed law admit a finite-ω longitudinal eigenmode carrying a `1/r` term (LC-hyperbolic: YES, a real radiative pole) or not (SLAVED: NO, the bound-constitutive R1/R3/R5 machine-zero result survives)? **The pole test is the deliverable-1 discriminator; `p` is the RC/dispersive-unitary discriminator only.** Both are run.

### §3.2 Deliverable 2 — the coupling law: TWO clock branches, both frozen with numbers

The brief's frozen check, verbatim: *"F = −ħ(∂ω/∂ε)∇ε against g = GM/r² on the canon bias profile — REPRODUCES-WITH-ZERO-NEW-CONSTANTS / REPRODUCES-WITH-IMPORT(name it) / FAILS."* Canon contains **two** candidate clock laws. Both are frozen; the cheapest separator (the leading-order power of `ε₁₁`) is run.

Common inputs: `ε₁₁ = 7GM/c²r`; `A ≡ (2/7)ε₁₁ = 2GM/c²r = r_s/r` (Ax4 gravitational dialect, `A = 1` at `r_s` — verified consistent at §3.3); trapped-resonance mass `m = ħω₀/c²` (Planck–Einstein, canon's own resonator mass identity); energy at infinity `E = ħω_∞`.

- **CLOCK-1 (slope-1 local clock; `temporal-spatial-lattice-decomposition.md` W2):** `ω_∞ = ω₀√(1−A) ≈ ω₀(1 − ε₁₁/7)`. Then `∂ω/∂ε₁₁\|₀ = −ω₀/7` and
  `F = −∇E = (ħω₀/7)∇ε₁₁ = (ħω₀/7)(−7GM/c²)r̂/r² = −(ħω₀/c²)(GM/r²)r̂ = −m·GM/r²·r̂`.
  **FROZEN PREDICTION: `F_derived/F_Newton = 1` EXACTLY, and `ħ` cancels — zero new constants, `𝒜_g`-free.**
- **CLOCK-S (Ax4 kernel route; `eq_axiom_4.tex` `c_eff = c₀√S = c₀(1−A²)^{1/4}`, resonance frequency riding `c_shear`):** `ω_∞ = ω₀(1−A²)^{1/4} ≈ ω₀(1 − A²/4)`. Then `∂ω/∂ε₁₁\|₀ = 0` and the leading force is `F = −(ħω₀/2)(r_s²/r³)r̂`.
  **FROZEN PREDICTION: `F_derived/F_Newton = A = r_s/r`. At the solar surface (`r_s☉ = 2954.008 m`, `R☉ = 6.957×10⁸ m`): `A = 4.2461×10⁻⁶` — CLOCK-S under-predicts Newtonian gravity by `2.3551×10⁵×` and gives the wrong radial power (`1/r³`, not `1/r²`).**

> **★ENTAILED-BRANCH DECLARATION (ave-prereg Step 3.10 — the honesty keystone of this lane).** CLOCK-1's arithmetic is **ENTAILED, not fireable**: `ε₁₁ = 7GM/c²r` was *constructed* (`gordon-optical-metric.md:25-33`) so that the `/7` chain reproduces the Newtonian potential, and the slope-1 clock IS gravitational redshift. `F = −∇(mc²√g₀₀) = −m∇Φ` is a **standard static-metric identity that GR carries unchanged**. **Therefore: if CLOCK-1 fires, the honest verb is DEMONSTRATED, not DERIVED, and the honest class is CONSISTENCY/PEER-WITH-GR — the "redshift–lensing–attraction unification" is an ECHO at the coefficient level and MUST be reported as one.** The tautology filter (SVA row 8) is hereby pre-fired on it. **What IS fireable, and is the real content of deliverable 2:** (a) **which** clock canon's own leaves force — CLOCK-1 and CLOCK-S disagree by `2.4×10⁵` and in radial power, so this is a genuine, decidable, verdict-bearing internal-coherence fork; (b) counter-arm **C7** — whether a trapped resonance's total energy tracks the local clock **linearly** with no further bias-dependence (mode-volume, `D(A)`-graded confinement, or `c_eff`-graded size). If C7's assumption fails, CLOCK-1's exactness fails with it, and that failure is fireable. **Unification honesty, pre-stated:** even on CLOCK-1, redshift/lensing/attraction consume **three different projection coefficients** off one field (`1/7` clock, `2/7` transverse index, `9/7` matter-wave) — the unification is at the FIELD level, **not** at the coefficient level, and the coefficients are the GR-imported `/7` chain.

### §3.3 Dimensional analysis at canonical primitives (ave-prereg Step 3.5)

Ingredients from `src/ave/core/constants.py` (canonical; imported via `from ave.core.constants import ...`, never hard-coded — `ave-canonical-source`): `C_0 = 299792458.0`, `G = 6.6743e-11`, `RHO_BULK = 7909692.740007466`, `HBAR = 1.054571817e-34`, `L_NODE = 3.8615926772428334e-13`, `M_E = 9.1093837015e-31`, `XI_MACHIAN = 8.154833696927648e+43`.

- `κ = c⁴/7G = **1.728937×10⁴³ N**`. Units check: `∇·[κ∇ε₁₁]` = `N/m² = Pa` = `[T₀₀]` ✓.
- **Bias inertia required for `c_g = c`:** `𝓘 = κ/c² = **1.923701×10²⁶ kg/m**`. Units: `𝓘(∂²_tε₁₁)` = `kg·m⁻¹·s⁻² = Pa` ✓.
- **In substrate primitives:** `𝓘/(ρ_bulk·ℓ_node²) = **1.630967×10⁴⁴** = **2 × ξ_Machian** (ratio to `XI_MACHIAN`: `1.99999999987`).`
  > **⚑ TAUTOLOGY-FILTER PRE-FIRE (this is why it is written here and not saved for the result):** this is a **DEFINITIONAL IDENTITY, not a discovery.** `RHO_BULK` is defined as `ξ_topo²μ₀/(p_c ℓ_node²)`, which evaluates to `m_e/(2ℓ_node³)`; `ξ_Machian = c²ℓ_node/(7Gm_e)` from `T_max,g = ξT_EM = c⁴/7G` with `T_EM = m_ec²/ℓ_node`. Substituting reduces `𝓘 = 2ξρ_bulkℓ_node²` to `𝓘 = c²/7G` — the same statement. **The lane will verify the identity symbolically and report it as an identity. Any result wording that presents the factor 2 as a derived structural coincidence is a FAIL of this gate (G-TAUTOLOGY).** Its *informative* content, which the lane may state: the bias line's required inertia is **not** the substrate's bulk inertia — it is `ξ_Machian`-enhanced by `~10⁴⁴`, and **exhibiting a fast-layer object with that inertia density is exactly the LC-hyperbolic bin's admission price.**
- **Dialect consistency check (frozen, must pass):** `A ≡ (2/7)ε₁₁` must give `A = 1` at `r = r_s = 2GM/c²`. Check: `(2/7)(7GM/c²r_s) = 2GM/c²r_s = 1` ✓ — this reconciles Ax4's *"BH event horizon ε₁₁(r) = 1 matches Schwarzschild r_s = 2GM/c² exactly"* with the `ε₁₁ = 7GM/c²r` profile, and **the reconciliation is that Ax4's `A` and the profile's `ε₁₁` differ by the factor `ν_vac = 2/7`.** If the lane cannot verify this against canon's own usage two ways, it reports a NAMED INCONSISTENCY (flag-don't-fix) rather than choosing.
- **`𝒜_g`-freedom, verified:** deliverable 1's targets are `(class, p, c_g/c)`; deliverable 2's is `F/F_Newton`; deliverable 3's is `Δt` and `c_g/c`. `𝒜_g` appears only in `u₀ = −𝒜_g∇ε₁₁`, which enters **no** target. **If any derived target is found to require `𝒜_g`, the lane STOPS and reports `BLOCKED-ON-UNVALUED-CONSTANT` rather than adopting the C2 bracket** (which #946 established is self-inconsistent: solar dress strain `2.3×10¹⁰`, ten OOM past yield).

### §3.4 Deliverable 3 — the settling signature

Frozen form of the statement: **the fractional bias-speed deviation `η ≡ c_g/c − 1`**, the induced timing observable, and its bound from the two comparators re-retrieved at freeze (§2 table): `κ_max² = δ_DP = 1.3×10⁻⁴` (double pulsar); Reading-A exclusion `9–110σ` (Hulse-Taylor) / `100–1400×` (double pulsar).

- **If bin = SLAVED or bin = LC-HYPERBOLIC with `c_g = c` and no pole:** frozen prediction **`η = 0`, no anomalous settling term, and the observable is a NULL that is PEER-with-GR — reported as a null, with no chord and no bench claim.**
- **If bin = LC-HYPERBOLIC with `c_g ≠ c`, or any bin admitting a finite-ω longitudinal pole:** the lane computes the radiated compression fraction on the completed law and compares to `κ_max² = 1.3×10⁻⁴`. **Frozen falsifier F1 fires (§6).**
- **R30 design input (the bench candidate's, per the brief):** whatever lands, the lane states the **lag** observable — a delay/dispersion measurement, explicitly **not** a wave receiver — and its magnitude, or states that the magnitude is UNCONSTRAINED-BY-RETRIEVAL. **The lane invents no comparator it cannot cite at file:line or to a published bound already banked in the corpus.**

### §3.5 Observable robustness ladder (ave-prereg Step 3.7(a))

Declared before freezing, primary rung first: **existence/class (PRIMARY, gating) → sign/shape (the pole test, `p`) → ratio (`c_g/c`, `F/F_Newton`) → magnitude (`𝓘`, `η`, timing residuals: SUPPLEMENTARY, reported).** If the magnitudes prove knob-ridden or `𝒜_g`-blocked, **the class + pole answer still stands as the lane's deliverable** and the lane reports the demotion explicitly rather than discovering it mid-arc.

---

## §4 — FROZEN VERDICT GRAMMAR (no aggregation; per-deliverable; Rule 11)

- **D1 (circuit class):** one of `RC-DIFFUSIVE` / `LC-HYPERBOLIC(c_g stated)` / `DISPERSIVE-UNITARY` / `NONE-OF-THE-THREE(named)`, each **only** if its admission price (§3.1 column 4) is paid with a receipt; otherwise `NOT-DERIVABLE(missing structure enumerated)`. The dispersion relation / frequency response ships explicitly **or the bin is not awarded.**
- **D2 (coupling law):** `REPRODUCES-WITH-ZERO-NEW-CONSTANTS` / `REPRODUCES-WITH-IMPORT(named)` / `FAILS` — **awarded per clock branch**, with the entailed/fireable split of §3.2 stated in the same sentence as the verdict. A bare "REPRODUCES" without the DEMONSTRATED-not-DERIVED qualifier is a gate failure (G-ECHO).
- **D3 (settling signature):** `SIGNATURE(observable + magnitude)` / `NULL-PEER-WITH-GR` / `UNCONSTRAINED-BY-RETRIEVAL`.
- **Arc-level:** `F1` (below) is the only kill.

## §5 — Certification: gates, controls, liveness (frozen; UNRUN ≠ PASSED)

**Positive controls (Step 3.8(a)) — the SAME extraction pipeline must read all three correctly before any `p` is banked:** (i) retarded wave Green's function `e^{iωr/c}/4πr` ⇒ must report `p = 2.0 ± 0.2`; (ii) diffusion kernel ⇒ `p = 0.5 ± 0.15`; (iii) free-Schrödinger kernel ⇒ `p = 1.0 ± 0.2`. **No `p` verdict is bookable until all three read correctly. A pipeline that reads only its expected answer is not an instrument.**
**Negative control:** the instantaneous (elliptic, clock-free) solve must report **no** resolvable `p` (residual at the §0-row-11 floor). This is the "absence where it doesn't exist" half of the liveness pair.

| Gate | Frozen criterion |
|---|---|
| **G-OBJECTS** | The bias `ε₁₁` / bound response `u₀` / fast-layer `(u,π)` split is maintained at every step; each pinned statement names its object. *(The bound-constitutive C1 CRITICAL was exactly this crossing.)* |
| **G-PRICE** | No D1 bin is awarded without its §3.1 admission price paid by a receipt, not by plausibility. |
| **G-POLE** | The far-zone pole test is RUN and reported, separately from `p`, with the LC-vs-SLAVED degeneracy of §3.1 stated. |
| **G-LIVE** | All three positive controls green + the negative control green, before any lane `p` is reported. |
| **G-AX3** | No loss/dissipation word in any derived statement without a named boundary-crossing port (row 6). |
| **G-ECHO** | D2's verdict carries the DEMONSTRATED-not-DERIVED / PEER-with-GR qualifier; `ave-discrimination-check` run before any framing sentence. |
| **G-TAUTOLOGY** | The `𝓘 = 2ξρℓ²` relation is reported as a definitional identity, symbolically verified, never as a structural coincidence. |
| **G-AGFREE** | No target consumes a value for `𝒜_g`; verified by symbolic cancellation, not by inspection. |
| **G-SECTOR** | Zero Cosserat/T2 consumption; TWO-"3"s guard; rotational fence-reach STOP checked per deliverable. |
| **G-BANKED** | Banked negatives are not relitigated: #261 (`K=2G` imported, both gates) and the bound-constitutive T1/R1/R3/R5 pole-absence receipts are inputs, not targets. Any contact carries a challenge-receipt. |
| **G-VACATED** | The 2026-07-20 envelope lane's P-branch legs are re-derived, never inherited; every use states VACATED-BY-DRIFT with the superseding receipt. |
| **G-ENGINE** | `git diff --stat ad376e9a..HEAD -- src/` is EMPTY at every commit. |
| **G-QUOTE** | Every ruled/frozen quote byte-checks, two engines, with a committed quote gate + `--mutation-receipt`. |
| **G-NUM** | Every numeral machine-verified; two full driver runs byte-identical; `make verify` green in the worktree. |

## §6 — Adjudication routing + falsifiers (frozen; what propagates on which outcome)

- **★F1 (ARC-LEVEL KILL, the dangerous direction).** Axiom 5's own text forbids *"any new longitudinal stiffness or wave (it adds no kinetic or potential term on the flat direction, so the pole-absence results survive it untouched)."* **If this lane's completion introduces a finite-ω longitudinal pole — a propagating bias wave with a radiative `1/r` term — then either Axiom 5's forbids-clause is violated by its own dynamical completion, or the pulsar bound bites the new channel.** On that outcome the lane reports `AXIOM-5-INTERNAL-CONTRADICTION` or computes the radiated fraction against `κ_max² = 1.3×10⁻⁴` and reports the exclusion — **and STOPS to Grant. It does not choose a rescue.**
- **F2 (framing falsifier).** If the reduction requires a **carrier that the cold phase does not have** (clause Q sets `ε₁₁ = 0`, and canon's *"only spatial gradients of A … are physically observable"* rule is the only thing that could rescue a nonzero uniform carrier), then DISPERSIVE-UNITARY is **inapplicable, not merely unselected** — and the lane says so rather than importing a carrier to keep the bin alive.
- **F3 (my framing is wrong).** If `p` comes out at none of `{0.5, 1.0, 2.0}` within the frozen bands on a green instrument, the three-bin axis itself is wrong; report `NONE-OF-THE-THREE` with the measured `p` and route the axis to Grant.
- **Routing on each outcome:** D1 → the Ax5 named-open entry's discharge text (**PROPOSED, never landed by this lane**); the `port-register.md:87` Q1 row's live-exclusion status (**FLAGGED, never edited** — its P-branch premise is the VACATED structure of G-VACATED); the R40 re-derivation queue. D2 → the `/7` triad's coherence (**flagged, not repaired**), and the Ax4 `c_shear` sign/ontology selector already marked PENDING Grant. D3 → the R30 bench candidate's design input.
- **The lane's fence on itself:** this lane **licenses no KB edit, no register edit, no solidity move, no ledger edit, and no un-revert, on any outcome.** Everything is routed to Grant/orchestrator. The §10 walk seeds remain UN-AUDITED input; nothing here promotes them by being cited.

---

> **Freeze provenance.** SVA v0.2-pilot §0 filled, all 11 rows. Frozen criteria travel verbatim or as file:line throughout; the brief's deliverables, fences, and the C9 check are quoted verbatim from the tracked brief at base `ad376e9a`; the Ax5 clause text is quoted from `eq_axiom_5.tex` at `origin/docs/2026-08-10-r43-execution` (the post-doc-lane-pass form the brief names). Skill stack fired at write time: `ave-prereg` (Steps 1–3.11 incl. entailed-branch §3.2, robustness ladder §3.5, liveness §5, freeze-by-push), `ave-worktree-paths` (worktree-absolute from the first call), `verify-before-cite`, `ave-canonical-source` (canonical constants imported, never hard-coded), `ave-discrimination-check` (§0 row 8 + §3.2 pre-fire — no chord on any outcome), `ave-regime-phase-state-check` (§0 row 2), `consistency-vs-emergence` (D2's classification routed to the result's ledger). **This document commits ALONE and pushes FIRST; the driver commits second; the result last.** Tier-2 adversarial review before any CLEARED wording. PR `[DO-NOT-MERGE]`.
