# PREREG — Annihilation / Evaporation: the REVERSE REACTION of the condensation ontology

**Date (frozen):** 2026-06-11
**Branch:** `analysis/2026-06-11-annihilation-evaporation` (worktree `/tmp/ave-annih`, off `origin/analysis/2026-06-10-genesis-v6-transducer` @ `7484dd0b` — the merged-main-synced v6 tip; do NOT push/merge — the orchestrator PRs at the end only, base `main`, review-gated).
**Engine lineage (subclassed, inherited physics unchanged BY DEFAULT):** `UnifiedGenesisEngine` (`src/ave/core/unified_genesis_engine.py`) ← `CrystalGraftV4` ← V3 ← V2 ← `CrystalEngine`. Every annihilation-phase addition is behind a NEW parameter that DEFAULTS to the v6 byte-identical path (the inherited keeper suite — incl. `src/tests/test_unified_transducer_v6.py` — must stay green).
**Governing discipline:** `ave-apparatus-floor-attribution v1.1` (ORDERED BINS — floor-check gates every positive; PROBE-CAPABILITY — every discriminating probe validated on a known-different reference with a keeper unit test; every new knob inventoried + swept). **§210-COMPLIANCE GATE: the run phase executes every sweep this prereg mandates, or states the deviation explicitly BEFORE running and re-bins. A positive whose governing knob was unswept is CLIP by this prereg's own law.**
**Skills fired at design time (recorded):** `pre-test-physics-check` (Trigger 8 dispatch-time ONTOLOGY gut-check — §2, the one plumber-physical question that GATES the Phase-2 run), `substrate-native-check` (CP1/CP2/CP9/CP10 walked §1), `ave-regime-phase-state-check` (the MODE/REGIME/PHASE-STATE of an un-binding RELEASE — §1.5; a null where the rupture regime is never reached is a wrong-regime ARTIFACT, not a falsification), `ave-conserved-vs-pumped` (the relative translation is ENERGIZED + COASTED — not a continuously-pumped closing force; the per-object circulation L is a conserved invariant whose net SUM is the load-bearing handedness channel — §5), `consistency-vs-emergence` (the objects + the channel ledger are class-tagged — §0.2; no CODATA target; this is a substrate-internal dynamics test, NOT an "AVE reproduces e⁺e⁻→γγ" emergence claim), `ave-prereg` (corpus-grep + the Step-3.5 dimensional subsection §10), `phase-space-coordinate-check` (the latent RELEASE is read in BULK volumetric-strain / `p(ρ̄)` coordinates, NOT lattice-Cartesian — §1/§9), `ave-driver-script-honesty` (every Phase-2 number must come FROM the evolved field / dumped JSON), `verify-before-cite` (every v6 / engine anchor grep-confirmed this session — §0), `flag-don't-fix` (the ontology gate §2 + the gross-vs-field discipline §9 are SURFACED, not silently resolved).

---

## 0. INHERITED VERDICTS (from the adjudicated v6 record — recorded verbatim-class; every anchor grep/JSON-confirmed this session)

### 0.1 What the v6 object actually IS (the load-bearing inheritance — this is what we are annihilating)

- **NOT-ELECTRON STANDS (the v5 panel ruling, un-reopened by v6).** Verify-before-cite, `research/2026-06-10_genesis-v6-transducer_result.md:20,90`: *"NOT-ELECTRON (the v5 panel) was NOT reopened and is NOT overturned — the electron claim stays closed."* This prereg does NOT reopen the electron claim either. The object under test is the v6 **converged dilatation mass**, NOT a corpus electron.
- **The v6 MAIN object = a converged DILATATION mass (T1 PASS), the only spec-sheet test that passed.** `..._result.md:24`: `E_V^cons` **11.70→12.91** (drift **0.86 %**), `H_total^cons` drift **0.83 %** → CONVERGED; N-robust (12.86–12.88), stop-time-robust (K3: 12.87–13.00). The recipe (the MAIN, `..._prereg.md:154-163`): **seed + self-limited snap + chiral drive + transducer ON + ω-recipient wired**, `seed_bulk(frac=0.85)`, `vent_mode="absorbed"` ⊕ `meissner_harden=0.05` ⊕ `snap_accounting="conservative"`, `drive_chiral_photon(helicity=±1)`, `chi_exch=0.02`, `omega_sector_on=True`, `buckle_on=True`, `H_total` in `bulk_energy_conserved` (CP2), `N=48`, `n_build=3200`, `n_persist=1200`, seed `20260610`.
- **The "handedness" of a v6 object is a HELICITY-ODD AM/charge ledger — NOT a quantized (2,3) winding.** `..._result.md:16`: `w_pol` is **0 across the ENTIRE χ̃ ladder AND ω-recipient ladder** — the poloidal "3" never forms. The handedness lives only in the AM/charge channel (`L_transferred`, `H_bel`, sign-flipped RH↔LH). **And per the v6 final-verdict panel that net field handedness is TINY and lock-limited:** the headline `∓0.539` was the by-construction GROSS accumulator; the genuine NET field effect is `ΔL_ω(MAIN−OFF) = −3.605e-5` (`..._result.md:84`), and the `H_bel` charge half is BUCKLE-attributable, not transducer (`:86`). **This is the load-bearing inheritance for §2's ontology question.**

### 0.2 The channel ledger — the FORWARD (genesis) reaction whose REVERSE we test (Grant-ratified 2026-06-10)

The condensation ontology's channel statement, verify-before-cite `research/2026-06-10_matter-as-vapor-locked-pump_framing.md:280` (§11.5.3, Grant-ratified):

> **the genesis event in channel language:** transverse in → chiral conversion at the phase front → **longitudinal latent stored (the new mass)** + longitudinal vent to the seed (ledger excess) + transverse remainder radiated.

- **Class-tag (`consistency-vs-emergence`):** the channel taxonomy (shear=matter-clock / transverse=EM / **longitudinal=the medium's STATE / order parameter**) is **consistency-class** (`..._framing.md:251`, §11.1 L2/L3, on `origin/main`). The genesis-direction "latent heat `= mₑc²`" stays **HYPOTHESIS-class** (`:259`, §11.2). **This prereg inherits those tags unchanged and adds no number.** It tests a substrate-internal DYNAMICS (does a built object un-bind?), not a CODATA match.
- **The REVERSE reaction (this prereg):** if genesis is *transverse→longitudinal-stored*, then annihilation/evaporation is *longitudinal-stored→released*: the trapped longitudinal latent store un-binds and EXITS — a **longitudinal burst** (the FLASH detector = the longitudinal-burst detector, `..._framing.md:278`, reading the exact-EOS bulk ledger `p(ρ̄)=ρ₀c₀²[ρ̄−½ln(1−ρ̄²)]`, `src/ave/core/cavitation_flow.py:166`, grep-confirmed) **AND** a transverse remainder radiated. This is exactly the directive's ANNIHILATE bin.

### 0.3 The discipline inheritances (carried forward, not re-litigated)

- **Gross-vs-field (the v6 DEMOTION lesson, HARD constraint here):** the v6 headline over-credited a GROSS per-step accumulator (`∓0.539`) ~4 OOM above the NET field effect (`−3.6e-5`). **Every headline number in the Phase-2 run is a NET (MAIN − control) FIELD quantity** (the evolved-field functional / EOS-ledger scalar), never a gross per-step deposit tally (§9).
- **Conserved-vs-pumped (the AM is energize+lock):** the per-object circulation `L` is a conserved invariant; you do NOT pump it. The relative TRANSLATION is energized once (an initial closing velocity from a finite KE reservoir) and then COASTS — there is NO continuously-applied closing force (a secular pump would both violate the discipline and inject spurious momentum/energy that contaminates the ledger). The net SUM `L_RH + L_LH` is the load-bearing handedness channel (§5).
- **Rule 11/12, flag-don't-fix, lane discipline:** the bins (§7) are frozen pre-run; a clip-valued positive is APPARATUS; a clean negative with a named mechanism is the discipline at full strength; the auditor lands any manual/manuscript entry — this prereg SURFACES the empirical design only.

---

## 1. SUBSTRATE-NATIVE CHECKPOINTS WALKED (before the first line of Phase-2 numerical code)

- **CP1 (dynamical, no minimization):** unchanged from v6 — FDTD leapfrog (V/w/ω) + RK2 (ρ̄/u). The two objects are EVOLVED toward each other and collide under the substrate's own wave dynamics; there is NO energy-basin descent, NO "find the merged ground state," NO gradient flow. Annihilation, if it happens, is a DYNAMICAL un-binding under the evolved field, not the minimizer finding a lower well.
- **CP2 / conservation-by-channel — THE LOAD-BEARING CHECK.** The conserved invariant is the c_eff²-weighted functional `bulk_energy_conserved` (`crystal_engine.py:380`), NOT the naive `bulk_energy` (the v6 D11 lesson — the naive functional over-reports the saturated-core breather). **Every T1-mass and ledger number is read in `bulk_energy_conserved`.** The annihilation ledger must close across THREE channels (§5): longitudinal/bulk (the latent store), transverse/EM (the shear radiation), rotational/AM (the two circulations).
- **CP9 (dynamical integration, not algebraic):** the burst, the radiated remainder, and the residual trapped mass are all read from EVOLVED-field ledger scalars (`bulk_pressure_integral` `unified_genesis_engine.py:1055`; the `E_latent_*`/`E_vent_*`/`E_diss_snap` ledger `:191-193,957-958`; `bulk_energy_conserved`), NOT from an algebraic "annihilation cross-section" formula. No heuristic stands in for the dynamics (substrate-native-check CP9).
- **CP10 (boundary, not bulk):** the approach is energized as an INITIAL CONDITION (a drift velocity imprinted on each object's `V_prev`, §4), NOT as a bulk attractive force added to the EOM (which would be the singular-at-contact bulk rendering that v5/v6 repeatedly detonated). The saturation/Γ=−1 walls of the two cores stay BOUNDARY conditions; the collision is two boundaries meeting, not a bulk potential pulling them in.

### 1.5 REGIME / PHASE-STATE DECLARATION (`ave-regime-phase-state-check` — the load-bearing structural-possibility check)

> **MODE.** The latent RELEASE lives in the **mechanical-BULK** (volumetric, K) mode — the longitudinal V-sector / order-parameter channel (§0.2); the radiated remainder lives in the **EM-transverse** mode (the w shear sector). The un-binding itself is a **bulk** event.
> **REGIME.** The two objects are built **deep-saturated** (`frac=0.85` ⇒ A≈0.85·V_yield, the v6 operating point) — each core sits at its **Γ=−1 saturation front** (near-yield→ruptured). Annihilation = an IRREVERSIBLE un-binding RELEASE; structurally it is a **YIELD/RUPTURED** event (the reverse of pair-production), `r → r₃ = 1.0`. It CANNOT live in the linear regime.
> **PHASE-STATE.** Loaded (two saturated masses in a cold-lattice background) → approaching → the **overlap region goes pre-geodesic/ruptured at contact**.

**Structural-possibility check (the load-bearing step).** The release is a RATE-ASYMMETRIC / irreversible event (a burst, ∮-like) → it exists **only** in the bulk mode at near-yield/ruptured, and is **structurally absent** in the linear regime. **Consequence frozen into the design:** the approach must drive the two cores to **overlap-rupture** (combined volumetric strain in the overlap exceeds `r₃`) for the release to be able to exist at all. A gentle approach where the cores co-orbit or bounce WITHOUT the overlap reaching rupture returns a guaranteed no-burst null — **that null is a wrong-regime ARTIFACT, not a falsification of annihilation, and is labelled as such (`ave-regime-phase-state-check` Step 4).** Therefore the sweeps (§8) MUST include head-on, zero-impact-parameter, sufficient-closing-speed configs that demonstrably reach overlap-rupture (a per-run rupture witness `tr_min` crossing `r₃` / `ρ̄` approaching `RHO_CAV = −0.618`, `unified_genesis_engine.py:64`), and every "no-annihilate" verdict is reported WITH whether rupture was reached.

---

## 2. THE ONTOLOGY GATE — the one plumber-physical question for Grant BEFORE the Phase-2 run (`pre-test-physics-check` Trigger 8)

**Per Rule 16 strengthening (ask BEFORE design / dispatch, not after 30+ commits) and `pre-test-physics-check` Trigger 8 (dispatch-time ONTOLOGY gut-check before an expensive run whose framing rests on a load-bearing noun):** the prereg DESIGN is frozen below per the directive, but the EXPENSIVE Phase-2 RUN is GATED on one ontology answer — exactly as v6's Phase-3 matrix was gated behind the Phase-2 smoke.

> **The question (one sentence):** *Given NOT-ELECTRON stands and `w_pol ≡ 0` (no quantized winding formed), is the "opposite handedness" of two v6 objects a REAL charge-conjugate (the opposite circulation/AM ledgers genuinely cancel, freeing both longitudinal latent stores to evaporate — a true reverse-genesis), or is it only the TINY lock-limited buckle/transducer AM remainder (`ΔL_ω ≈ −3.6e-5`, the v6 DEMOTION) decorating two scalar dilatation blobs that — having no real opposite charge — simply MERGE regardless of handedness?*

**Why this gates the run (the wrong-noun risk).** If the handedness is real (reading A), opposite-handed → ANNIHILATE is a meaningful discriminator against the same-handed control. If it is only the lock-limited remainder (reading B), then at the dilatation-merge level opposite-handed ≡ same-handed and **the run returns MERGE by construction** — uninformative for the annihilation question, the same structural way `w_pol ≡ 0` was preordained in v6. The cost of asking is one turn; the cost of a wrong noun is the whole Phase-2 matrix plus a kill + re-dispatch.

**Corpus-search done (don't ask the already-answered).** The corpus has the FORWARD direction (genesis: `..._framing.md:280`) and the conservation-by-channel argument that makes the same-handed control physical (§5). It does NOT settle whether the v6 object's tiny AM remainder is a sufficient charge for a reverse reaction — that is a genuinely OPEN ontology question (no annihilation/evaporation driver exists in the tracked corpus; grep this session: zero `annihilat*` driver hits under `src/scripts/`). **Surfaced to Grant; the Phase-2 run does NOT start until this collapses.**

**Flag-don't-fix:** I do NOT pre-resolve this by assuming reading A. The design freezes BOTH readings into the bins — reading B's prediction (handedness-blind MERGE) is an explicit, non-failing outcome (§7), and the same-handed control is precisely the discriminator that distinguishes A from B empirically even if Grant's answer is "run it and see."

---

## 3. FLOORS (ORDERED BINS — floor-check FIRST, gates EVERY positive; `ave-apparatus-floor-attribution v1.1`)

All floors are RE-MEASURED at the annihilation config (the directive: a floor carried over from a different config is invalid). The known-null for the destructive floors is **two well-separated static masses, NO approach energized** (the objects exist but never meet) — this isolates the apparatus/numeric scatter from any real collision signal.

- **F0a interior-only (CP7).** PML+sponge excluded (`pml_thickness ≤ {i,j,k} ≤ N−pml_thickness−1`); the residual-trapped-mass read is sampled at energy-density PEAKS (top-K `|field|²`), NOT centroid (a shell's centroid is the empty middle). The "final trapped energy → background" verdict is an interior read above this floor.
- **F0c reactance-pair completeness (A-Rule-10).** C-state (V_inc / ω) AND L-state (Φ_link / ω_dot) recorded EVERY step over the whole encounter window for BOTH cores. A single-phase snapshot cannot distinguish "the core un-bound" from "the core's LC oscillator caught at a trough" — so a lock-vs-unbind verdict with incomplete reactance pairs is UNRESOLVED by construction.
- **F-BURST (the longitudinal-burst floor — the ANNIHILATE primary).** `LongitudinalBurstDetector.calibrate_floor(engine, steps)` (`src/ave/core/longitudinal_burst_detector.py:54`) on the two-static-masses known-null = the max per-step `|Δ bulk_pressure_integral|` acoustic scatter. **GATE: an ANNIHILATE longitudinal burst must clear F-BURST × `threshold_mult` (default 3.0)** via `detector.scan()` (`:67`). A "burst" below the floor is UNRESOLVED, never a positive. (Re-uses the v6 D6 birth-flash detector unchanged — `verify-before-cite`: it read 561 bursts 40–70× above floor at genesis, `..._result.md:52`.)
- **F-RADIATE (the transverse-remainder floor).** The transverse (w-sector) energy reaching the PML in the two-static-masses known-null (free dispersion/leak). **GATE: an ANNIHILATE transverse remainder must exceed F-RADIATE** — the directive requires BOTH the longitudinal burst AND the transverse radiate.
- **F-CLOSE (the conservation canary / pump pre-gate — the D11 inheritance).** The known-null `H_total^cons` drift over the encounter window (free PML/numeric one-way sink). **GATE: the full ledger (§5) closes to within F-CLOSE — NO positive (energy-CREATING) excursion.** A dissipative residual (a one-way PML/snap sink) is physical and ALLOWED; only NET creation fails. If the MAIN build pumps above F-CLOSE, the encounter does NOT run (honest block — the v6 D11 condition, re-applied).
- **F-TRANSLATE (the motion-lock-under-transport floor — itself a valuable result, and a KNOWN-POSITIVE).** From the single-object + empty-target control (§6): a translating mass must ARRIVE with its T1 mass intact — the trapped `bulk_energy_conserved` must not leak in transit above this floor. **GATE: a verdict on the two-body encounter is only admissible if F-TRANSLATE PASSES** (each object survives transport). If transport itself leaks the mass, any "annihilation" is contaminated by transport-leak → UNRESOLVED. This control doubles as the discriminator's known-positive: a clean translate proves the apparatus can carry a mass without destroying it, so a destroyed mass in MAIN is the collision, not the transport.
- **F-PROBE (PROBE-CAPABILITY keepers — the m-even lesson).** Each discriminating probe is validated on a known-different reference with a keeper unit test (§9): (i) the handedness probe must SEPARATE the two cores' opposite circulation sense on the freshly-built seeds BEFORE any dynamics; (ii) the burst detector must fire on a known single-cell snap and stay silent on the no-approach null; (iii) the residual-mass probe must read the FULL T1 mass on an isolated single object and ≈background on an empty box.

**ORDERING RULE (HARD CONSTRAINT, Rule 11).** For every observable the FLOOR-CHECK is evaluated FIRST and GATES the positive. A signal below its calibrated floor → UNRESOLVED (cannot be a positive). A positive that TRACKS a §8 swept knob (approach speed / impact parameter / N) → CLIP, named by the knob. The frozen verdict bins (§7) are reached ONLY after FLOOR-0 + F-TRANSLATE pass.

---

## 4. THE BUILD RECIPE — two opposite-handed v6 objects + the transport mechanism (the NEW knob)

### 4.1 Building the two objects (the MAIN v6 recipe, run to T1 convergence, TWICE)

Each object is built by the FROZEN v6 MAIN recipe (§0.1) run to T1 convergence, at a chosen center:

- **Object A (RH):** `seed_bulk(center=c_A, frac=0.85, ...)` + `drive_chiral_photon(helicity=+1, center=c_A)` + transducer ON (`chi_exch=0.02`) + `omega_sector_on`/`buckle_on`, snap = absorbed ⊕ meissner 0.05 ⊕ conservative, evolved `n_build=3200` to the converged `E_V^cons ≈ 12.9` dilatation mass.
- **Object B (LH):** identical recipe with `helicity=−1, center=c_B` — the v6 C-LH arm gives the sign-reversed AM/charge ledger (`L_transferred +0.539` vs `−0.539`, `..._result.md:35`). This is the "opposite circulation handedness."

The engine's `seed_bulk(center, ...)` and `drive_chiral_photon(center=...)` already accept independent centers (grep-confirmed: `crystal_engine.py:287`, `unified_genesis_engine.py:602`). **Build-validity gate:** BOTH objects must independently pass the v6 T1 floor (`E_V^cons` converged, drift < F0e, N-robust) BEFORE any approach is energized; a non-converged object voids the encounter (you cannot annihilate a transient).

### 4.2 The transport mechanism — the NEW capability + the NEW knob (`v_approach`), inventoried + swept

**Honest implementation flag (`ave-apparatus-floor-attribution`: every new knob inventoried).** The base v6 engine has NO transport seeding — `seed_bulk` sets `V_prev = self.V.copy()` ⇒ a STATIONARY start (`∂_t V = 0`, `crystal_engine.py:303`, grep-confirmed). "Energize a relative translation" is therefore a NEW Phase-2 capability that must be added behind a new parameter (defaulting OFF to preserve the v6 byte-identical path). The substrate-native rendering (CP10 — initial condition, not bulk force):

> a translating dilatation blob is `V(x,t) = A·env(x − v·t)`; imprint it by setting `V_prev` to the back-shifted field `A·env(x − v·dt)` (the exact analog of how `seed_photon` imprints group velocity by phase-stepping `w_prev`, `crystal_engine.py:323-326`, grep-confirmed). This gives each core a rigid drift velocity `v_approach` toward the other as an INITIAL CONDITION; the cores then COAST (momentum conserved — `ave-conserved-vs-pumped`: energized once, NOT pumped continuously).

- **NEW knob `v_approach`** (the relative closing speed) — the directive's mandated **approach-speed sweep** (§8). CLIP telltale: the verdict tracks `v_approach` (e.g. ANNIHILATE only at one speed) ⇒ CLIP, named.
- **NEW knob `impact_parameter`** `b` (the transverse offset between `c_A`, `c_B`) — the directive's mandated **impact-parameter sweep** (§8). `b=0` is head-on (the rupture-reaching config the regime check §1.5 requires).
- The transport addition gets its own keeper unit test (F-PROBE): a single translating object conserves its `bulk_energy_conserved` and its momentum to the F-TRANSLATE floor (the motion-lock-under-transport known-positive).

---

## 5. CONSERVATION-BY-CHANNEL LEDGER (the annihilation ledger must close across all three channels)

`ave-conserved-vs-pumped` + `ave-representation-capability-check` (C): the ledger is split by CHANNEL, each conserved/tracked separately. **Before** the encounter: `E_before = E_V^cons(A) + E_V^cons(B) + KE_approach` (the two converged T1 masses + the energized closing kinetic energy — `KE_approach` is part of the ledger, not free). **After:**

| channel | observable (evolved field / EOS ledger) | conservation statement |
|---|---|---|
| **Longitudinal / bulk** (the latent store) | `bulk_pressure_integral` step-change → `detector.total_burst_energy()` (`longitudinal_burst_detector.py:82`); `E_latent_held`+`E_latent_restored`+`E_vent_radiated` (`unified_genesis_engine.py:191-192,958`) | the trapped longitudinal latent RELEASED = the burst energy; reverse of the §0.2 genesis store |
| **Transverse / EM** (the radiated remainder) | the w-sector energy crossing into the PML over the window | the transverse remainder that RADIATES away |
| **Rotational / AM** (the handedness channel) | `angular_momentum_bulk` (`:1017`) + `angular_momentum_omega_axial` (`:574`), NET sum over both cores | `L_total` CONSERVED through the encounter (Noether) |

- **Energy closes:** `E_before = E_burst(long.) + E_radiate(transv.) + E_residual(trapped, interior) + E_PML(absorbed)`, to within **F-CLOSE**. NO net creation.
- **The AM channel IS why the same-handed control is the physical contrast (the load-bearing conservation argument):** for the **opposite-handed** pair `L_total = L_RH + L_LH ≈ 0` ⇒ annihilation is **AM-ALLOWED** (the two circulations cancel; nothing conserved forces a residual; both cores CAN fully unbind and evaporate). For the **same-handed** pair `L_total = 2L ≠ 0` ⇒ full evaporation is **AM-FORBIDDEN** (the net AM must be carried by *something*) ⇒ the masses MERGE into one spinning object (carrying the `2L`) or BOUNCE (AM in the orbital). **This is the substrate reason the directive's canon predicts "same-handed → NO annihilation (repulsion/merge class)."** `L` is a conserved invariant (energize+lock, NOT pumped) — the ledger does not let the same-handed pair evaporate.
- **The translation is energized, not pumped (`ave-conserved-vs-pumped`):** `KE_approach` is a finite, one-time energization; there is NO continuously-applied closing force. A secular closing drive would (a) violate the discipline and (b) pump unbounded energy/momentum into the ledger, making F-CLOSE meaningless. Recorded class: **the relative translation is energized+coasted; the per-object L is conserved; the net L_total is the handedness channel.**

---

## 6. THE ARM MATRIX (FROZEN — Rule 11)

| arm | object A | object B | handedness | approach | purpose / FROZEN expectation |
|---|---|---|---|---|---|
| **MAIN** | RH (h=+1) @ c_A | LH (h=−1) @ c_B | **OPPOSITE** (`L_total ≈ 0`) | energized, head-on | the annihilation thesis — opposite circulations cancel ⇒ AM-ALLOWED evaporation; the candidate ANNIHILATE |
| **C-same-handed** (control) | RH (h=+1) @ c_A | RH (h=+1) @ c_B | **SAME** (`L_total = 2L ≠ 0`) | energized, head-on | the directive's canon control — NO annihilation (AM-forbidden; repulsion/MERGE class); the handedness KNOWN-NULL for the burst |
| **C-translate** (control) | RH (h=+1) @ c_A | **NONE** (empty target) | — | energized toward empty box | the motion-lock-under-transport KNOWN-POSITIVE (F-TRANSLATE) — a mass must just TRANSLATE intact; itself valuable |
| **C-no-approach** (known-null) | RH @ c_A | LH @ c_B | opposite | **NONE** (static, well-separated) | the floors' known-null — re-measures F-BURST, F-RADIATE, F-CLOSE, F0e at the build config WITHOUT a collision |
| **C-static-overlap** (probe control) | RH @ c | LH @ c (co-located, no drift) | opposite | none (placed overlapping) | distinguishes a GEOMETRIC overlap burst (placement artifact) from a DYNAMICAL collision burst — a burst here that matches MAIN ⇒ the "annihilation" is placement geometry, not the energized encounter |

**The load-bearing contrast:** MAIN − C-same-handed (the handedness-selective annihilation) AND MAIN − C-translate (the collision-vs-transport isolation). **FLAG (flag-don't-fix):** if C-same-handed ALSO produces a longitudinal burst above F-BURST, the burst is HANDEDNESS-BLIND ⇒ GEOMETRIC (two saturated cores rupture on overlap regardless of handedness) — surfaced before binning, NOT silently attributed to the opposite-handed case. This is the direct analog of the v6 T5 GEOMETRIC false-positive discipline (`..._result.md:49`).

---

## 7. THE FROZEN BINS — ORDERED, FLOOR-CHECKS FIRST (Rule 11; no post-hoc criterion drop)

**Evaluation order (HARD CONSTRAINT — floors gate every positive):**

1. **FLOOR-0 + F-TRANSLATE gate (evaluated FIRST).** If any FLOOR-0 floor (§3) fails for an observable, OR F-TRANSLATE fails (transport leaks the mass), OR the reactance pair (F0c) is incomplete, OR the MAIN ledger pumps above F-CLOSE → **UNRESOLVED** for that observable (it cannot be a positive). The regime witness (§1.5) is recorded: a no-burst result where overlap-rupture was NEVER reached is labelled **UNRESOLVED (wrong-regime artifact)**, not a negative.
2. **The §8 knob-tracking gate.** A candidate positive that TRACKS a swept knob (`v_approach` / `b` / `N`) in its VERDICT (not merely its magnitude) → **CLIP**, named by the knob.
3. **Only then the FROZEN verdict bins** (mutually exclusive; every number a NET MAIN−control FIELD quantity, §9):

| bin (FROZEN, ordered) | criteria (ALL must hold, after floors pass) | class |
|---|---|---|
| **ANNIHILATE** | (a) the longitudinal burst FIRES above F-BURST×`threshold_mult` **AND** (b) the transverse remainder radiates above F-RADIATE **AND** (c) the final interior trapped energy → background (≤ F0a mass floor — NO residual mass survives) **AND** (d) the full §5 ledger CLOSES to F-CLOSE across all three channels **AND** (e) it is HANDEDNESS-SELECTIVE: present in MAIN, ABSENT (burst ≤ F-BURST) in C-same-handed AND in C-static-overlap **AND** (f) overlap-rupture WAS reached (regime witness) | emergence-class (a de-novo reverse reaction) |
| **MERGE** | the two objects coalesce into ONE surviving object; report its converged `E_V^cons` (T1) and net `L`; trapped energy does NOT go to background (a residual mass persists above F0a); no above-floor burst-evaporation. **This is the FROZEN expectation for C-same-handed (AM-forbidden evaporation), and the reading-B outcome for MAIN (§2).** | manifestation |
| **BOUNCE** | the two objects approach, repel, and SEPARATE with both T1 masses surviving (interior mass ≈ 2× build value); the lock holds through the encounter; AM goes to orbital | manifestation |
| **PASS-THROUGH** | the two objects interpenetrate and EMERGE with both T1 masses intact (solitonic transparency); no burst, no merge, no residual loss above floor | manifestation |
| **UNRESOLVED** | floor-gated (above) OR knob-CLIP OR a mixed/ambiguous outcome not cleanly in one bin OR a wrong-regime null | — |

**HARD CONSTRAINT (Rule 11 honest closure).** A positive at a clip value is APPARATUS. Do NOT debug toward ANNIHILATE; do NOT drop a criterion (a/b/c/d/e/f) post-hoc to convert MERGE→ANNIHILATE. If MAIN returns MERGE handedness-blind (= reading B, §2), that is a CLEAN result that ANSWERS the ontology question NO at this architecture (the same discipline the v6 `w_pol≡0` recorded) — record it, name the mechanism (no real opposite charge ⇒ no annihilation channel), close the branch (Rule 11), do NOT refill the slot with a rescue (Rule 12). A v7-class mechanism (e.g. a real winding-carrying object) is a NEW hypothesis with its own prereg.

---

## 8. THE MANDATED-SWEEP LIST (§210-COMPLIANCE GATE — every knob the bins depend on, EXPLICITLY ENUMERATED; FROZEN)

**The run MUST execute every sweep below, OR state the deviation explicitly BEFORE running and re-bin. A positive whose governing knob was unswept is CLIP by this prereg's own law.** The THREE directive-mandated sweeps + the inherited CLIP-suspects:

| # | knob | swept grid | which bin it gates | CLIP telltale |
|---|---|---|---|---|
| 1 | **approach speed** `v_approach` | {slow, …, fast} bracketing sub-rupture → over-rupture (incl. one that demonstrably reaches overlap-rupture; one too gentle to — the regime bracket §1.5) | ANNIHILATE vs BOUNCE/PASS-THROUGH; the rupture-regime gate | the VERDICT tracks `v_approach` (e.g. ANNIHILATE only at one speed) — CLIP; only the burst MAGNITUDE may scale |
| 2 | **impact parameter** `b` | {0 (head-on), …, > core radius (grazing)} | ANNIHILATE vs PASS-THROUGH/BOUNCE; rupture requires `b≈0` overlap | the verdict tracks `b` smoothly with no physical threshold — a head-on requirement is physical and REPORTED, not hidden |
| 3 | **`N` resolution** | {40, 48 (primary), 56} | ALL bins (the standing K2 CLIP suspect — an under-resolved core ruptures spuriously) | the verdict or the burst onset tracks N — the headline MUST be N-robust (the v6 K2 lesson, `..._result.md:53`) |

**ALSO mandated (§210 "every knob the bins depend on" — inherited CLIP-suspects, swept):**

| knob | swept grid | gates | CLIP telltale |
|---|---|---|---|
| **`chi_exch`** (the transducer coupling that sets the handedness magnitude) | {0, 0.005, 0.02 (default), 0.08} | the handedness-selectivity (criterion 7.e) | ANNIHILATE present at `chi_exch=0` (no handedness wired) ⇒ the selectivity is spurious |
| **`frac`** (build saturation depth) | {0.60, 0.85 (default), 0.95} | the regime gate — a burst only at shallow `frac` is a sub-saturation artifact | an ANNIHILATE seen ONLY at one `frac` |
| **`meissner_harden`** | {0.0, 0.05 (default), 0.10} | the build hygiene / deflagration containment carried from v6 | the surviving/residual mass tracks the increment |
| **`threshold_mult`** (burst detector) | {2.0, 3.0 (default), 5.0} | the F-BURST gate cosmetics | the ANNIHILATE verdict tracks the detector threshold (only the burst COUNT may) |
| **K3 stop-time** `n_persist` | {600, 1200, 2400} | the "evaporated → background" convergence (the v6 stop-time lesson) | "trapped → background" tracks when the run STOPS (the residual re-condenses or never finishes evaporating) |

**§210 DEVIATION POLICY (the v5/v6 lesson, structural):** the run executes EVERY sweep above, or states the deviation explicitly BEFORE running and re-bins. (Anticipated honest deviation, stated now: if the CFL `dt` makes a discrete "approach speed" degenerate with the dispersion floor — the v6 per-bounce degeneracy precedent, `..._prereg.md:232` — the headline becomes the cumulative outcome at fixed total closing displacement, with `v_approach` swept and the deviation logged, not papered over.)

---

## 9. GROSS-VS-FIELD RULE + PROBE-CAPABILITY KEEPERS

### 9.1 Gross-vs-field (HARD constraint — the v6 DEMOTION lesson, structural)

**Every headline number is a NET (MAIN − control) FIELD quantity — the evolved-field functional or EOS-ledger scalar — NEVER a gross per-step accumulator tally.** The v6 record over-credited a gross deposit accumulator (`∓0.539`) ~4 OOM above the net field effect (`−3.6e-5`, `..._result.md:84`); this prereg forecloses the repeat:

- **Burst energy** = `Δ(detector.total_burst_energy)` MAIN − C-same-handed (the handedness-selective net), read from `bulk_pressure_integral` step-changes (a FIELD EOS scalar), not from a running deposit counter.
- **Radiated remainder** = the net transverse PML flux MAIN − C-no-approach.
- **Residual trapped mass** = the interior `bulk_energy_conserved` MAIN − the F-TRANSLATE single-object baseline (the net loss attributable to the collision, not the transport).
- **AM cancellation** = the net `L_total` from the FIELD (`angular_momentum_bulk` + `angular_momentum_omega_axial`), not a gross per-step transducer-deposit tally.

Any quantity that is a by-construction bookkeeping identity (a per-step accumulator whose value is fixed by the wiring) is reported as such and is NOT the headline. The headline is the FIELD.

### 9.2 Probe-capability keepers (every discriminating probe validated on a known-different reference; encoded as keeper unit tests)

- **K-HANDED:** the handedness probe (net circulation sense) reads OPPOSITE sign on the two freshly-built RH/LH seeds and ≈0 on an achiral build — BEFORE any dynamics. A probe that cannot separate the two cores' handedness on the known seed DISQUALIFIES the handedness-selectivity verdict (CLIP). (The v6 m-even / F-PROBE lesson, `..._prereg.md:109`.)
- **K-BURST:** the `LongitudinalBurstDetector` FIRES on a known single-cell snap (the v6 D6 known-positive) and stays SILENT (≤ F-BURST) on the C-no-approach null. A detector that fires on the no-approach null is reading apparatus scatter (DISQUALIFIED).
- **K-MASS:** the residual-trapped-mass probe reads the FULL converged `E_V^cons ≈ 12.9` on an isolated single object and ≈ background on an empty box — so "trapped → background" in MAIN is a real evaporation, not a probe that always reads ≈0.
- **K-TRANSPORT:** a single translating object conserves `bulk_energy_conserved` and momentum to F-TRANSLATE — the transport addition does not itself leak the mass (the F-TRANSLATE known-positive, §3).

Encoded alongside the inherited v6 keepers in a new `src/tests/test_annihilation_evaporation.py` (Phase-2 deliverable; the prereg names them, the run builds them).

---

## 10. STEP-3.5 — EXPLICIT DIMENSIONAL ANALYSIS (mandatory; `ave-prereg` v1.1)

All quantities in ENGINE-NATURAL units (the substrate primitives `V_yield`, `c0`, `dx`, `dt` — NO CODATA, NO SI substitution; `consistency-vs-emergence`: this is a substrate-internal dynamics test, Class — substrate-mechanism, NOT an emergence claim against `mₑc²` or any external target). The expected outcomes carry three dimensionless ratios, each evaluated at the canonical primitives, each pre-freezing a leading-order prediction:

**(D1) The rupture-reachability ratio `R_rupt = 2·frac` vs the yield boundary 1.0 (the load-bearing structural prediction).**
Each core is built to dilatation `A ≈ frac·V_yield`. At impact parameter `b=0` (head-on) the two cores' dilatation fields superpose in the overlap; to leading (linear-superposition) order the overlap dilatation is `A_overlap ≈ 2·frac·V_yield`. Overlap-rupture (the §1.5 regime requirement) needs `A_overlap ≥ V_yield`, i.e. `R_rupt = 2·frac ≥ 1` ⇒ **`frac ≥ 0.5`**. Evaluated at the sweep grid:
- `frac=0.85` (default): `R_rupt = 1.70 > 1` ⇒ **rupture-reachable by superposition alone** (head-on).
- `frac=0.60`: `R_rupt = 1.20 > 1` ⇒ reachable.
- `frac=0.30`: `R_rupt = 0.60 < 1` ⇒ **NOT reachable by superposition** ⇒ a no-burst null here is a REGIME artifact (§1.5), not a falsification.
**Frozen leading-order prediction:** the rupture threshold sits near `frac* ≈ 0.5`; the `frac` sweep {0.60, 0.85, 0.95} brackets it from above and the `frac=0.30` point sits below — so a burst that appears only above `frac*` is PHYSICS (the rupture turning on), and a burst at `frac=0.30` would be a spurious sub-yield read. **Nonlinear caveat (stated, not assumed away):** the bulk EOS is self-steepening (`c_bulk²=c₀²(1+ρ̄/(1−ρ̄²))`, → 0 at `RHO_CAV=−1/φ≈−0.618`, `unified_genesis_engine.py:64`); compressive self-steepening can lower the EFFECTIVE threshold below the linear `2·frac=1` estimate, so the sweep brackets `frac*` rather than asserting it at exactly 0.5.

**(D2) The closing Mach number `M_app = v_approach / c_bulk` (sets self-steepening / whether the encounter ruptures dynamically vs just by static superposition).**
The closing speed enters dimensionlessly only as a fraction of the bulk sound speed (engine-natural `c0`). `M_app ≪ 1` is the "slow approach" the directive specifies (the cores meet quasi-statically; rupture, if any, is by superposition D1). `M_app ≳ 1` would add shock self-steepening (a second rupture route, but also a larger `KE_approach` contaminant — D3). **Frozen:** the headline runs at `M_app ≪ 1` (slow), with `v_approach` swept up to test whether a dynamic (shock) route opens a burst that the quasi-static superposition does not.

**(D3) The approach-KE contamination ratio `η_KE = KE_approach / (2·E_V^cons)` (the slow-approach DISCIPLINE — the burst must be the LATENT release, not the dumped approach KE).**
`KE_approach = ½·M_eff·v_approach²` where `M_eff` is the effective inertial mass of a core (its added-mass; engine-natural). For the longitudinal burst to MEAN "latent store released" and not "we dumped the closing KE into the bulk," the energized approach KE must be small vs the trapped latent store: `η_KE = KE_approach/(2·E_V^cons) ≪ 1` with `E_V^cons ≈ 12.9` (the v6 converged value, engine-natural, `..._result.md:24`). **Frozen requirement:** the headline ANNIHILATE config sits at `η_KE ≪ 1` (e.g. ≲ 0.1), enforced by choosing `v_approach` small; the energy ledger (§5) reports `KE_approach` explicitly so the burst's provenance (latent vs dumped-KE) is auditable. A burst whose energy `≈ KE_approach` is the dumped-KE artifact (UNRESOLVED), not the latent release; a burst `≫ KE_approach` drawn from the trapped store is the real evaporation. **This is the dimensional content of the directive's word "slow."**

**Power-counting summary (the three pre-frozen exponents):** burst energy ∝ (trapped latent store) at leading order, INDEPENDENT of `v_approach` in the slow limit (`η_KE→0`) — so the ANNIHILATE burst must NOT scale with `v_approach` (if it does, it is the dumped-KE artifact, D3); the rupture ONSET is a threshold in `frac` near `2·frac=1` (D1), not a smooth function; and `M_app` gates only the dynamic (shock) route, off in the slow headline (D2).

---

## 11. SCALE / BUDGET (the §8 N³ law governs; N frozen at 48 primary)

Two converged objects + the encounter. Per the v6 cost law (`..._prereg.md:264`, `~9.4e-8·N³ s/step`): at N=48, ~10.4 ms/step. A build is `n_build=3200` per object (×2) + the encounter `n_persist` window. Per full MAIN encounter (2 builds + ~1200-step encounter) ≈ 2×33 + 13 ≈ **~80 s** at N=48. The matrix: 5 arms (§6) × the §8 sweeps (`v_approach` ~4, `b` ~4, `N` 3, `chi_exch` 4, `frac` 3, `meissner` 3, `threshold_mult` 3 cosmetic, stop-time 3) ≈ **~40–60 build-equivalents**. At N=48 via `genesis_parallel_runner` (ProcessPool, 5× effective, `serial==parallel` determinism confirmed in v6): ~40×80/5 ≈ **~11 min wall**; N=56 ≈ ~18 min — within a single-session budget. **FROZEN: N=48 primary** (the v6-validated resolution; the K2 sweep {40,48,56} brackets the headline so any N-tracking is caught CLIP). **EXCLUDED (honesty):** f32 dtype — physically forbidden (the conservation canaries operate at 1e-3, the ledger-close gate needs finer; f32 desyncs F-CLOSE).

---

## 12. CORPUS STATE + DISCIPLINE

- **OPEN.** This freeze does NOT promote any claim. NOT-ELECTRON (the v5/v6 panel) STANDS; the objects under test are converged DILATATION masses, not electrons (§0.1). The annihilation/evaporation question is NEW (no prior driver in the tracked corpus). **The Phase-2 RUN is GATED on the §2 ontology answer from Grant** (Rule 16: ask before the expensive dispatch).
- **Rule 11:** the bins (§7) are frozen; floor-checks gate every positive; a clip-valued positive is APPARATUS; a clean MERGE/no-annihilate with a named mechanism (no real opposite charge ⇒ no annihilation channel — reading B, §2) is the discipline at full strength, NOT a failure to debug away. No post-hoc criterion drop, no debugging toward ANNIHILATE.
- **Rule 12:** if the run falsifies the annihilation hypothesis, it retracts via 🔴 header (body preserved); the slot is NOT refilled with a new unverified mechanism — a winding-carrying-object reverse reaction would be a NEW hypothesis with its own version + verification chain.
- **A44 (missing-axiom-vs-engine-bug):** a handedness-blind MERGE diagnoses an engine coupling-family limit (the object carries no real opposite charge — the v6 `w_pol≡0` lineage), NOT a missing axiom. Do NOT draft an Ax-5 candidate.
- **Lane discipline:** the auditor lands any manual/manuscript entry; this prereg SURFACES the empirical design only. Do NOT draft the auditor's manual; do NOT draft methodology pivots without corpus + Grant (Rule 16).

---

*Disciplines fired this session, retroactive pass: `pre-test-physics-check` (Trigger 8 ontology gate §2, surfaced to Grant, gates the run); `substrate-native-check` (CP1/CP2/CP9/CP10 §1); `ave-regime-phase-state-check` (MODE/REGIME/PHASE-STATE §1.5, the wrong-regime-null guard); `ave-conserved-vs-pumped` (translation energized+coasted, L conserved §4.2/§5); `consistency-vs-emergence` (substrate-internal dynamics, engine-natural units, no CODATA target §0.2/§10); `ave-prereg` (Step-3.5 dimensional §10); `phase-space-coordinate-check` (latent release read in bulk `p(ρ̄)` coordinates §1/§9); `ave-apparatus-floor-attribution v1.1` (ORDERED floors §3, every knob swept §8, probe keepers §9.2); `ave-driver-script-honesty` (every Phase-2 number FROM the field); `verify-before-cite` (every v6/engine anchor grep/JSON-confirmed); `flag-don't-fix` (the ontology gate §2, the gross-vs-field rule §9, the geometric-burst flag §6 — all surfaced, none silently resolved).*

