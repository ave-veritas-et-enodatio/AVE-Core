# SUBC/KUBC Bracket — RESULT (OWED-1: `K` bounded on BOTH sides, not one)

Prereg-file: research/2026-07-28_subc-kubc-bracket_prereg-FROZEN.md

**Date:** 2026-07-28
**Class:** DERIVATION + lattice-derived research-driver (research-doc; forms derived, values dimensionless/geometric; **mints no `clm-`/`def-`; propagates to no KB/tex leaf**). This is the bench RUN of the frozen prereg committed and pushed ALONE at `1288e288`.
**Driver:** `research/drivers/subc_kubc_bracket.py` (+ `research/drivers/subc_kubc_bracket_results.json`, `research/drivers/subc_kubc_bracket.png`).
**Engine:** `src/ave` **BYTE-UNTOUCHED** (imports read-only). Rule-14 reuse of the merged #782 KUBC scaffold (`research/drivers/rve_aggregation_bench.py`) and the merged #796 state-dependent grow (`research/drivers/vessel_state_rve.py`). The NEW piece is the SUBC (uniform-traction, pure-Neumann) primitive: the discrete traction load set, the Hill-lemma macroscopic-stress identification, the active-node restriction and the translation-only null-space projection.
**Every number below is read from the shipped `subc_kubc_bracket_results.json` via the deterministic driver — NO prose-string conclusions (the #770 lesson NAMED).** Deterministic; `determinism_digest = 37b5fc81eb537902…`. **Every numeric token in this document is machine-checked against that JSON** by `research/drivers/subc_kubc_bracket_number_check.py` (§7 D-14) — a number cannot enter this doc by being typed, only by being registered against its source. Wall-clock is machine-dependent, is excluded from the frozen determinism digest by the driver's own `determinism_digest()`, and is therefore **deliberately not quoted anywhere in this document** (it lives in the JSON at `_runtime_sec`).

**★WHICH MODULUS THIS `K` IS — stated once here and repeated at every headline.** The frozen §1.2 fixes both ends of the hydrostatic mode to the pure dilatational response (`E = ε·I` ⇒ `dev E = 0`; `Σ = σ·I` ⇒ `dev Σ = 0`), so the bracketed modulus is the **BULK modulus `K`** — **NOT** any longitudinal modulus that a normal-incidence impedance `Z = ρc_P` carries, and **NOT** Young's `E`. The `T2` companion mode is the pure deviatoric shear constant, which on this lattice is specifically **`C44`** (the `xy` engineering shear). Supplementary longitudinal axes are shipped ADDITIVELY in §6 — they are **not** the frozen deliverable and amend nothing.

> **★ANISOTROPY DISCLOSURE, added by the PR #802 adversarial review (finding F2) and stated before any longitudinal number is read.** This medium is **CUBIC, not isotropic.** Measured Zener anisotropy of the cold uncaged reference at `L = 16`: `A = C44/C′ = 1.330402` (SUBC) and `1.605316` (KUBC) — far from the isotropic `A = 1`. **A cubic medium therefore has no single "longitudinal modulus": it is direction-dependent.** Along `[100]` the longitudinal modulus is `C11 = K + 4C′/3` with `C′ = (C11−C12)/2`; along `[111]` it is `K + 4C44/3`. The axis this driver previously shipped and labelled *"the P-wave modulus `M = K + 4G/3` (`= C11` for an isotropic average)"* is the **`[111]`** one, and that label is **WITHDRAWN**: on the cold uncaged reference it overstates the true `C11` by **`18.9 %`** under KUBC (`M_[111] = 2.861883` vs `C11 = 2.406553`) and by **`7.9 %`** under SUBC (`1.891127` vs `1.752290`). `C′` is now MEASURED directly through the same machinery (§6.5). **No downstream reader may consume `M_*_abs` as `C11`.** No isotropy check existed anywhere in the frozen prereg, the driver, or the first version of this result doc — that absence is the finding.

**★SCOPE, verbatim from the frozen §7 and required at every headline:** `SCOPE: this bench brackets K_eff ONLY. Every r_Z interval it reports is a K-BRACKET AROUND AN ASSUMED ρ_eff/ρ_0 ≡ 1 — the ρ half is ASSUMED, not measured, not bracketed. This lane does NOT resolve walk-1's ρ half; that is OWED-2 (research/2026-07-22_vessel-state-rve_result.md §9), a separate lane, and OWED-1 does not dispose of it.`

---

## ★BOTTOM LINE

**The bracket was built, run, and reproduces the merged corpus bit-exactly. The frozen GATE STRUCTURE then returned VOID on two independent counts — and both are defects in the frozen criteria, not in the extraction.** Under the frozen §4B rule (`gate_fireability_selftest_pass = selftest_G1_fires AND selftest_G2b_fires AND selftest_partition_pass. If ANY fails to force its target, the correctness gates are a checklist not gates ⇒ the bench is VOID before any bracket is read; route to Grant.`) the correct disposition is: **the bracket is COMPUTED AND SHIPPED IN FULL, but is NOT BANKED as physics, and the two criterion defects are ROUTED.** Nothing is relabelled to convert a ❌ into a ✅.

> ### BENCH STATUS = **VOID AS FROZEN** — on two counts, both criterion-side
>
> **COUNT 1 — `selftest_G2b_fires = False`.** The frozen §4B self-test cannot fire as written. Ships both normalizations, as the frozen latitude requires.
> **COUNT 2 — frozen §4 G1 ratio-ordering violations on `8` of the `42` ratio-bearing measurements** (`19 %`; see §4 for why the honest denominator is `42` and not the `48` walked rows), every one of them in the `T2` shear companion mode, every one of them RATIO-ONLY: `absolute_theorem_grade_ordering_holds_everywhere = True` across all `48` walked rows. Under the strict reading of "VOID for that configuration" this voids 8 configurations **including both headline arms**.

**And here is what the instrument nevertheless established, labelled as computed-not-banked:**

> **THE BRACKET (BULK `K`; `ρ_eff/ρ_0 ≡ 1` ASSUMED, not measured).** For the #782 headline wall `bulk_only_cold` at `φ_sf` (`L=16`, `r_cage = 2.2`, `s = 4.5`, deep rail `1e-4`):
> `K_eff/K_0 ∈ [0.26340, 0.41421]` (PRIMARY, width `0.15081`, `w_rel = 0.44514`) ⇒ `r_Z ∈ [0.51322, 0.64359]`
> `K_eff/K_0 ∈ [0.21209, 0.51442]` (CONSERVATIVE / theorem-grade) ⇒ `r_Z ∈ [0.46053, 0.71723]`
>
> For the **#796 grown arm on its frozen tangent operator**:
> `K_eff/K_0 ∈ [0.26367, 0.41429]` (PRIMARY, width `0.15062`) ⇒ `r_Z ∈ [0.51348, 0.64365]`
> `K_eff/K_0 ∈ [0.21231, 0.51451]` (CONSERVATIVE) ⇒ `r_Z ∈ [0.46077, 0.71729]`
>
> **★THE VERDICT-RELEVANT SPLIT, and it is the whole result:** against the #782 band edge `T1: r_Z = 0.5`, the **PRIMARY** bracket **RESOLVES-HIGH** at both headline arms (`0.51322 > 0.5`, `0.51348 > 0.5`) — but the **CONSERVATIVE (theorem-grade)** bracket **STRADDLES** it (`0.46053 ≤ 0.5 ≤ 0.71723`). **The two bracket definitions disagree about whether the corpus's `r_Z ≈ 0.54` reading is bound-robust, and the one that disagrees is the rigorous one.** Both were frozen in advance precisely so this could not be chosen after the fact.

**The banked corpus numbers sit INSIDE their brackets.** #782's `K_eff/K_0 = 0.296` (core convention) reproduces at `0.29636822324939766` and lies inside `[0.26340, 0.41421]`; #796's painted/frozen-tangent `0.2982369862639104` reproduces to `0.2982369859937546` and lies inside `[0.26367, 0.41429]`; #796's grown live-operator `K_tan/K_0 = 0.29548` also lies inside. `core_estimator_inside_primary_bracket = True` at both headline arms. **No banked number is contradicted by this bench.**

> ### ★AND THE CONSEQUENCE THAT CUTS THE OTHER WAY — stated here, at the top, with numbers
>
> **#782's SOLE bound-robust macro-side reading does NOT survive on the bound-carrying measure.** Merged #782 `research/2026-07-21_rve-aggregation-bench_result.md:124` states, verbatim: `only compressed r_Z = 0.466 < 0.5 is bound-robust.` `:12` carries that same compressed leg as the MACRO-side leg of the BIN-4 `r_Z` straddle. **That `0.466` is `√(R_KUBC_core)` — the CORE-energy estimator, which the frozen prereg §2.3 states carries NO bound status under EITHER boundary condition.** This lane reproduces it **bit-for-bit** (`K_here_core = 0.2176075097402979` = #782's own shipped `leg4_verdict.by_class.bulk_only_compressed.K_eff_over_K0_sf`, `reproduces_782_core_bitwise = true`) and then measures the same configuration on the WHOLE-CELL apparent modulus — the measure the Hill/Huet kinematic-uniform theorem actually bounds from above:
>
> | #782 leg (at `φ_sf`) | `r_Z` CORE estimator (#782's number; **no bound status**) | `r_Z` whole-cell KUBC **apparent modulus** (theorem-grade on the ABSOLUTE; this RATIO is the §2.1 PRIMARY, **not** bound-ordered) | `r_Z` **theorem-grade** `[R_lo, R_hi]` (§2.2 CONSERVATIVE — the bound-ordered pair) | macro-side (`< 0.5`) on the whole-cell reading? |
> |---|---|---|---|---|
> | `bulk_only_compressed` | `0.4664842009546496` | `0.6098352173422836` | `[0.38080390724320373, 0.67960726963222229]` | **NO** |
> | `symmetric_cold` | `0.38583455489392426` | `0.5850392860613116` | `[0.29686852685722953, 0.65197440311905175]` | **NO** |
> | `bulk_only_cold` | `0.5443971190678709` | `0.6435934144284258` | `[0.46053254733292703, 0.71722778661286535]` | NO |
> | `bulk_only_expanded` | `0.5676919873253737` | `0.6556895088638865` | `[0.48627161605611224, 0.7307078111813432]` | NO |
>
> **★COLUMN LABEL CORRECTED, and a theorem-grade column ADDED (RE-VERIFY finding F4).** The first version headed column 3 *"the actual upper bound"*. That applies the bound-ordering word to a **ratio** — and this doc's own §4 correctly diagnoses exactly that conflation in frozen G1: the frozen §2.1 says the same-instrument ratio `is NOT theorem-grade on the RATIO, because the uncaged reference is itself boundary-conditioned`. What is theorem-grade here is the **ABSOLUTE** whole-cell KUBC modulus; the *ratio* built from it is the §2.1 PRIMARY. The column is relabelled accordingly, and the §2.2 CONSERVATIVE (theorem-grade, bound-ordered) `[R_lo, R_hi]` pair is added per leg — **one divide from already-shipped absolutes, no re-run, no new solve.**
>
> **The direction of the finding is unaffected, and the theorem-grade column makes it sharper, not weaker.** `2` of `4` legs read macro-side on the core estimator; `0` of `4` do on the whole-cell reading. The core estimator sits BELOW the whole-cell KUBC reading at **every** leg — the direction that manufactures a macro-side reading out of a measure that cannot support one. **So `compressed r_Z = 0.466 < 0.5` is not bound-robust; it is estimator-conditional.** And on the **theorem-grade** pair the compressed leg reads `[0.38080390724320373, 0.67960726963222229]` — it **STRADDLES** `0.5`, exactly as the PRIMARY two-sided bracket `[0.4243721849890805, 0.6098352173422836]` does. **All four legs STRADDLE on the theorem-grade pair; not one RESOLVES-LOW.** JSON: `F5_782_bound_robustness_crosscheck_NOT_FROZEN`.
>
> **Does "#782 BIN-4 stands unchanged" survive this? Re-examined honestly: the BIN survives; its stated BASIS does not, and the basis correction is stated rather than absorbed.** BIN-4 is *UNDETERMINED*. Removing a bound-robust macro-side leg makes the `r_Z` axis **more** undetermined, not less, so nothing here can promote BIN-4 to a decided bin — and this lane mints no bin and edits no merged doc. **But #782 §7.1's basis sentence is now known to be wrong as written**, and so is the `r_Z` MACRO-side/MATCHED-side split that #782 `:12` grounds trigger (iii) on — `compressed r_Z=0.466` MACRO-side **vs** `cold 0.544` / `expanded 0.568` MATCHED-side. **That split does not exist on the bound-carrying measure**: there, all four legs' upper bounds sit above `0.5`, so no leg reads MACRO-side and there is nothing for the other two to split away from; the two-sided brackets STRADDLE or RESOLVE-HIGH, never RESOLVE-LOW. **The honest statement is therefore: BIN-4's label stands, #782's stated basis for it weakens, and this is the second basis correction routed to #782** (the first is the Fork-ρ relative-offset walk's consequence flag, `_orchestration/docket-entries/2026-07-21-relative-offset-principle.md:6`). ROUTED to Grant / the auditor lane — see §8 consequence 7. It is **not** softened here: it was computed by this lane, it cuts against the lane's own status-quo-preserving headline, and it is stated in the same breath as the headline it undercuts.
>
> **★AND WHAT IS *NOT* TOUCHED — a MIS-CITATION in the first version of this doc, corrected (RE-VERIFY finding F3).** The first version attributed the phrase *"verdict-controlling cross-class flip"* to **#782 §5**. **It is not there.** §5 is LEG 3 (`:76` through the §6 header at `:96`) and contains **zero** occurrences of `cross-class`, in either case. The quoted phrase occurs exactly once in the whole document, at **`:114`**, which is **§6 (LEG 4 / VERDICT)**; the only other site is `:12`'s `CROSS-CLASS-FLIP`, and **that one names the same `f_incl` object** (`on f_incl the classes CROSS-CLASS-FLIP: cold/expanded = BIN-2-MATCHED vs compressed = BIN-4`), which is the point: **every cross-class phrase in #782 attaches to the `f_incl` collapse object, never to the `r_Z` one this lane re-measures.** And at `:114` it denotes a **DIFFERENT OBJECT** from the one this lane re-measures: the `f_incl` **BIN-2-vs-BIN-4** split. **That split is COLLAPSE-GATE-driven, not `r_Z`-driven, and this lane does not touch it.** `research/drivers/rve_aggregation_bench.py:831` `_bin_of` returns `BIN4_REGIME_UNDETERMINED` whenever `collapse_ok` is `False` — **before `r_Z` is consulted at all** — so `bulk_only_compressed`, which FAILS the `f_incl` collapse gate (`0.3280883375040938` against the `0.30` threshold ⇒ `collapses = False`), reads BIN-4 **for any `r_Z` whatever**, while `bulk_only_cold` (`0.28045314594331805`) and `bulk_only_expanded` (`0.2687184264458418`) pass it and read BIN-2. **Changing the measure `r_Z` is read on leaves that flip exactly where it was: the `f_incl` BIN-2/BIN-4 flip PERSISTS UNCHANGED on the bound-carrying measure.** What evaporates is `:12`'s `r_Z` macro/matched clause, and only that. JSON: `F5_782_bound_robustness_crosscheck_NOT_FROZEN.f_incl_cross_class_flip_NOT_TOUCHED_F3`, read from #782's own artefact at run time rather than retyped.

**What this does NOT do.** It does not measure `ρ`. It does not resolve OWED-2. It does not tighten, move or re-band any standing verdict — under the frozen §5.5 the STRADDLE disposition is explicit: `If a bracket STRADDLES its threshold: the corresponding corpus verdict is recorded as BOUND-CONDITIONAL and is NOT tightened in either direction. The standing #782 BIN-4 and #796 UNDETERMINED labels STAND UNCHANGED. No new bin is minted, no side is picked, no rescue is derived (Rule 11/12).` That is the disposition taken **on the bin label**; the *basis* correction above is stated separately and routed, because a disposition that leaves a label alone is not licence to leave a known-wrong basis unstated.

---

## §1 — Instrument validation: the SUBC primitive is sound

| check | reading | verdict |
|---|---|---|
| active set / connectivity | `L∈{12,16,20}`: exactly `1` degree-0 node each; degree>0 subgraph = `1` connected component at all three sizes | PASS (the frozen null space is the 3 translations of a single component) |
| Born-model rotation (frozen §0 walk item 7) | `E(uniform translation) = 0.0` exactly; `E(rigid rotation, 1e-3) = 1.984e-3 > 0`; `born_model_confirmed_rotations_cost_energy = True` | CONFIRMED as an instrument fact — rotations are NOT null modes, so translation-only projection is correct |
| Hill-lemma stress deficit | `Σ̄/σ = 0.872309 (L12) / 0.904749 (L16) / 0.924044 (L20)` | the expected shell-mean-depth deficit; it CANCELS in every ratio |
| G4 SUBC convergence | worst relative residual `9.99e-10 ≤ 1e-9` over `48` solves; worst iteration count `6474` vs the `60000` cap | **PASS** |
| G5 work identity | worst `|U − ½f·u|/U = 8.63e-16 ≤ 1e-8` | **PASS** (convergence-EQUIVALENT, labelled) |
| G8 load-amplitude invariance | `R_SUBC(σ) vs R_SUBC(10σ)` rel `1.05e-15 ≤ 1e-10` | **PASS** (IDENTITY, labelled — not a discriminating gate) |
| G2a uniform-medium null | both ratios `1.0` to `< 1e-12`, both modes | holds (IDENTITY, labelled — never counted as a gate) |
| G3 determinism | two independent full runs, separate processes, separate `--out`; `diff -q` on the timing-stripped payloads **CLEAN** (byte-identical); `determinism_digest` identical `37b5fc81eb537902ea1d4e137b2a41cdc897fef52c8a55e60efa1b74e3db26f2` | **PASS** — the frozen criterion itself, RUN, not a proxy (re-run again after the PR #802 RE-VERIFY repairs, §7 D-16) |

Frozen: `G4: every SUBC pure-Neumann solve reaches relative residual ≤ 1e-9 within the 60000-iteration cap; the residual and iteration count of every solve are shipped in the JSON.` → PASS.
Frozen: `G3: two independent full driver runs, in separate processes writing separate output paths, produce byte-identical timing-stripped results (diff -q CLEAN) and an identical determinism digest.` → PASS.
Frozen: `G5: for every SUBC solve, |U_SUBC − ½·f·u| / U_SUBC ≤ 1e-8.` → PASS.
Frozen: `G8: R_SUBC is invariant under σ → 10σ to within 1e-10.` → PASS.

### §1.1 — G6, the reproduction cross-check: BIT-EXACT

Frozen: `G6: the driver's own KUBC re-computations must reproduce the merged numbers within 2e-3 relative` → **PASS**, and two of the three at machine zero.

| merged quantity | recomputed | merged target | rel |
|---|---|---|---|
| #782 `bulk_only_cold` core-energy ratio at `φ_sf` | `0.2963682232` | `0.296` | `1.24e-03` |
| #796 isotropic control | `0.2963682232` | `0.2963682232` | `0.00e+00` |
| #796 painted-anisotropic | `0.2982369863` | `0.2982369863` | `0.00e+00` |

**This is the load-bearing sanity check:** our reconstruction of the shipped #782/#796 arms IS the shipped arm, to the last printed digit. Everything that follows is therefore a statement about boundary conditions, not about a re-implementation drifting from the corpus.

### §1.2 — G7, the STOP-gate mirror and the size trend

Frozen: `G7a (mirror validity, carried from #782): under BOTH boundary conditions the deep-rail bulk-only cage array must SOFTEN (ratio < 1) while the RIGID control must STIFFEN (ratio > 1). A wrong-sign mirror under EITHER boundary condition stops the lane.` → **PASS.** `bulk_only` at `φ_sf`: `R_SUBC = 0.26340 < 1` and `R_KUBC = 0.41421 < 1`. `rigid`: `R_SUBC = 1.84885 > 1` and `R_KUBC = 20.91818 > 1`. The mirror holds under BOTH boundary conditions — the sign is the gate, and it does not turn on the boundary condition.

Frozen: `G7b (size trend): the PRIMARY bracket width w(L) at φ_sf must be non-increasing across L ∈ {12,16,20} within a 0.02 absolute slack` → **PASS** as written: `w = 0.22153 (L12) → 0.15081 (L16) → 0.09937 (L20)`, monotonically decreasing.

**★HONEST CAVEAT ON G7b, disclosed rather than banked (see §6 deviation D-4).** The three size points do NOT hold the composite fixed: at a fixed intensive label `φ = 0.48946` the REALIZED cage fraction is `0.2200 / 0.3082 / 0.3705` (box basis) and `0.2186 / 0.3079 / 0.3713` (bond basis) at `L = 12/16/20`, because the number of cages that fit inside the standoff grows `8 → 27 → 64`. So `w(L)` is a trend across three DIFFERENT realized composites, not a clean size-convergence trend. The gate passes as written; the inference "the bracket is converging with box size" is **NOT** licensed by it and is not made.

---

## §2 — ★THE BRACKET (frozen §5.1), per configuration

All at `L = 16` unless stated; `bw = 1.5`; deep rail `S_RAIL = 1e-4`; `cage_w = 1.0`; KUBC `ε = 1e-3`; SUBC `σ = 1`. **Modulus = BULK `K` (hydro rows) / SHEAR `G` (shear rows).** `ρ_eff/ρ_0 ≡ 1` ASSUMED throughout.

| configuration (mode) | PRIMARY `[SUBC, KUBC]` | width | `g_0` | CONSERVATIVE `[R_lo, R_hi]` | banked CORE | `T1` PRIMARY / CONSERVATIVE |
|---|---|---|---|---|---|---|
| `bulk_only_cold_rc1.3` (hydro) | `[0.34824, 0.56040]` | `0.2122` | `1.2419` | `[0.28040, 0.69597]` | `0.23907` | RESOLVES-HIGH / RESOLVES-HIGH |
| `bulk_only_cold_rc1.6` (hydro) | `[0.26889, 0.48343]` | `0.2145` | `1.2419` | `[0.21652, 0.60037]` | `0.18948` | RESOLVES-HIGH / STRADDLES |
| `bulk_only_cold_rc1.9` (hydro) | `[0.23983, 0.42898]` | `0.1892` | `1.2419` | `[0.19311, 0.53276]` | `0.19972` | STRADDLES / STRADDLES |
| **`bulk_only_cold_phi_sf` (hydro)** | **`[0.26340, 0.41421]`** | `0.1508` | `1.2419` | **`[0.21209, 0.51442]`** | `0.29637` | **RESOLVES-HIGH / STRADDLES** |
| `bulk_only_cold_phi_sf` (shear) | `[0.69233, 0.60774]` | `−0.0846` | `2.1600` | `[0.32052, 1.31274]` | `0.67119` | ★G1 RATIO INVERSION |
| `symmetric_cold_rc1.3` (hydro) | `[0.23145, 0.50562]` | `0.2742` | `1.2419` | — | `0.12874` | STRADDLES / STRADDLES |
| `symmetric_cold_rc1.6` (hydro) | `[0.14984, 0.42955]` | `0.2797` | `1.2419` | — | `0.08331` | STRADDLES / STRADDLES |
| `symmetric_cold_rc1.9` (hydro) | `[0.10939, 0.36323]` | `0.2538` | `1.2419` | — | `0.07686` | STRADDLES / STRADDLES |
| `symmetric_cold_phi_sf` (hydro) | `[0.10945, 0.34227]` | `0.2328` | `1.2419` | — | `0.14887` | STRADDLES / STRADDLES |
| `bulk_only_compressed_phi_sf` (hydro) | `[0.18009, 0.37190]` | `0.1918` | `1.2419` | — | `0.21761` | STRADDLES / STRADDLES |
| `bulk_only_expanded_phi_sf` (hydro) | `[0.29366, 0.42993]` | `0.1363` | `1.2419` | — | `0.32227` | RESOLVES-HIGH / STRADDLES |
| `rigid_phi_sf` (hydro) | `[1.84885, 20.91818]` | `19.069` | `1.2419` | — | `1.58271` | RESOLVES-HIGH / RESOLVES-HIGH |
| `routeB_…_s3.6` (hydro) | `[0.37170, 0.59742]` | `0.2257` | `1.2419` | — | `0.20390` | RESOLVES-HIGH / RESOLVES-HIGH |
| `routeB_…_s4.2` (hydro) | `[0.27764, 0.50501]` | `0.2274` | `1.2419` | — | `0.16391` | RESOLVES-HIGH / STRADDLES |
| `routeB_…_s5.0` (hydro) | `[0.26089, 0.41606]` | `0.1552` | `1.2419` | — | `0.23922` | RESOLVES-HIGH / STRADDLES |
| `routeB_…_s6.5` (hydro) | `[0.58429, 0.72413]` | `0.1398` | `1.2419` | — | `0.47279` | RESOLVES-HIGH / RESOLVES-HIGH |
| **`grown_frozen_tangent` (hydro)** | **`[0.26367, 0.41429]`** | `0.1506` | `1.2419` | **`[0.21231, 0.51451]`** | `0.29824` | **RESOLVES-HIGH / STRADDLES** |
| `grown_frozen_tangent` (shear) | `[0.66805, 0.59587]` | `−0.0722` | `2.1600` | `[0.30928, 1.28710]` | `0.63239` | ★G1 RATIO INVERSION |
| `painted_anisotropic` (hydro) | `[0.26367, 0.41429]` | `0.1506` | `1.2419` | — | `0.29824` | RESOLVES-HIGH / STRADDLES |
| `isotropic_control` (hydro) | `[0.26340, 0.41421]` | `0.1508` | `1.2419` | — | `0.29637` | RESOLVES-HIGH / STRADDLES |
| `uniform_medium_null` (both) | `[1.00000, 1.00000]` | `0.0000` | — | — | `1.00000` | IDENTITY (§4 G2a) |
| `bulk_only_cold_phi_sf_L12` (hydro) | `[0.30027, 0.52180]` | `0.2215` | `1.3452` | — | `0.27990` | RESOLVES-HIGH / STRADDLES |
| `bulk_only_cold_phi_sf_L20` (hydro) | `[0.24927, 0.34864]` | `0.0994` | `1.1845` | — | `0.29423` | STRADDLES / STRADDLES |

Full per-row data (both modes, all 48 measurements, every CG residual and iteration count, every `r_Z` interval, both VOID scopings and the no-VOID-overlay class): JSON `reads` and `configurations`; code path `subc_kubc_bracket.py::read_config` / `::bracket_from_pair`.

**The `painted_anisotropic` row is byte-identical to `grown_frozen_tangent` by construction** — on the #796 carve they are the SAME operator, and running both is the G6 faithfulness cross-check, not two physics arms (frozen §3 B.8).

### §2.1 — Anti-seduction fence, applied

Frozen: `a wide bracket is a statement about the INSTRUMENT, not about the medium: the result doc may NOT convert bracket width into physical significance in either direction, and must report the uncaged gap g_0 alongside every width so the reader can see how much of it is finite-size boundary layer`. `g_0` is in every row above. The headline width `0.1508` sits on an uncaged gap of `1.2419`; **no physical significance is claimed for the width in either direction.**

---

## §3 — ★COUNT 1: `selftest_G2b_fires = False` — the frozen self-test cannot fire as written

Frozen: `SELFTEST-G2b: recompute the uncaged g_0 using the NOMINAL applied σ instead of the Hill-lemma Σ̄ read from the shipped load set, and assert G2b REPORTS A VIOLATION (g_0 < 1 or non-monotone in L).`
Frozen acceptance: `selftest_G2b_fires = True`. **COMPUTED: `False`.**

The frozen latitude's SHIPS-clause is discharged — both normalizations are in the JSON unconditionally:

| `L` | `Σ̄/σ` | `g_0` (Hill-normalized, correct) | `g_0` (NOMINAL-σ, deliberately broken) | `1/(Σ̄/σ)²` |
|---|---|---|---|---|
| 12 | `0.872309` | `1.3451548` | `1.0235586` | `1.3141942` |
| 16 | `0.904749` | `1.2419126` | `1.0165925` | `1.2216425` |
| 20 | `0.924044` | `1.1844537` | `1.0113541` | `1.1711562` |

The mis-normalized `g_0` is `≥ 1` at every `L` **and** still non-increasing in `L`. **Neither clause of G2b fires**, so the deliberately-broken extraction is not caught.

**★THE MECHANISM — and it is a real instrument finding, not an excuse.** `g_0(L)` and `1/(Σ̄/σ)²` agree to `2.4 % / 1.7 % / 1.1 %` and are converging. **The uncaged KUBC/SUBC gap and the Hill-stress deficit are the SAME finite boundary layer measured two ways.** Substituting the nominal `σ` therefore multiplies `g_0` by very nearly its own inverse, landing at `1.01–1.02` — just barely on the PASSING side. The frozen §4B design anticipated the magnitude (*"the same order as the true gap"*) but drew the opposite conclusion from it: same-order **and same-sign** means the perturbation cancels the signal rather than probing it. **This is a frozen-criterion design defect, surfaced at integrator time (Rule 10), and it is NOT repaired here** (repairing a frozen self-test post-hoc is exactly the move the §4B rule exists to prevent).

**★THE FROZEN TEXT IS AMBIGUOUS ABOUT WHAT HAPPENS NEXT, and this lane does not resolve it.** The frozen latitude reads: `if the nominal-σ mis-normalization does not push g_0 below 1 at every L, the self-test is accepted on the MONOTONICITY clause alone provided the driver SHIPS the computed g_0(L) under both normalizations so the reader can see which clause fired`. Two readings:

- **(a) "judged on the monotonicity clause alone"** — monotonicity is not violated either ⇒ the self-test does **not** fire ⇒ `gate_fireability_selftest_pass = False` ⇒ VOID.
- **(b) "accepted, provided both normalizations are shipped"** — both are shipped ⇒ accepted ⇒ not VOID.

Reading (a) is the one this result carries, **because it is the reading that voids the bench** and therefore cannot be a rescue. The adjudication is Grant's / the auditor's, not this lane's. Both booleans are shipped: JSON `selftest_G2b.selftest_G2b_fires_STRICT` and `.selftest_G2b_fires_LATITUDE_SHIPS_CLAUSE`.

**The other two self-tests DO fire.**
- Frozen: `SELFTEST-G1: recompute R_SUBC with the ratio taken in the KUBC direction (U_SUBC^arm/U_SUBC^uncaged instead of U_SUBC^uncaged/U_SUBC^arm) on the bulk_only_cold φ_sf configuration, and assert G1 REPORTS A VIOLATION.` → **FIRES.** Correct direction `R_SUBC = 0.26340` (ordering OK); inverted `3.79654` (ordering VIOLATED, as required). `selftest_G1_fires = True`.
- Frozen: `SELFTEST-PARTITION: walk a synthetic grid of (bracket_lo, bracket_hi, threshold) tuples through the SAME classifier the verdict uses; assert every tuple lands in EXACTLY one of RESOLVES-LOW / RESOLVES-HIGH / STRADDLES / VOID, that each of the three non-VOID classes is returned by at least one tuple, and that no tuple is unclassified.` → **PASSES.** `32` tuples, `0` unclassified, all four classes returned. `selftest_partition_pass = True`.

---

## §4 — ★COUNT 2: the G1 ratio-ordering violations — `8` of `42` ratio-bearing rows, ALL shear, ALL ratio-only

Frozen: `G1: for EVERY bracketed configuration and BOTH modes, R_SUBC ≤ R_KUBC must hold on the WHOLE-CELL pair, with a numerical slack of 1e-6 relative. A violation means the SUBC extraction is WRONG — the bench is VOID for that configuration and the violation is reported as an instrument failure, NEVER as a physical finding that the lower bound exceeds the upper bound.`

**Computed: `8` violations.** Every one is in the `T2` shear companion mode:
`bulk_only_cold_phi_sf::shear`, `bulk_only_cold_phi_sf_L12::shear`, `bulk_only_cold_phi_sf_L20::shear`, `bulk_only_expanded_phi_sf::shear`, `grown_frozen_tangent::shear`, `isotropic_control::shear`, `painted_anisotropic::shear`, `routeB_bulk_only_cold_s5.0::shear`.

**★HONEST DENOMINATOR, corrected by the PR #802 review (finding F3).** The G1 walk covers `48` rows, but those are two populations. `42` are real `(configuration, mode)` measurements with a genuine `R_SUBC` / `R_KUBC` pair — these **can** fail the ratio clause. The other `6` are the uncaged-reference rows at `L ∈ {12,16,20}` × both modes, which are written with `R_SUBC = R_KUBC = 1.0` and `ratio_order_ok = True` **by construction** and therefore **cannot** fail it; they exercise only G1's ABSOLUTE clause. **Quoting "`8` of `48`" (`17 %`) silently inflates the denominator with rows that cannot fail. The honest ratio-clause figures are `8` of `42` = `19 %`, and on the hydrostatic side `0` of `21`, not "24 of 24".** JSON `gate_G1_VOID_ordering.honest_ratio_clause_denominators`; the `48`-row figure remains correct — and is retained above — for the ABSOLUTE clause, which every row does exercise.

| G1 clause | denominator | rows that can fail | violations | rate |
|---|---|---|---|---|
| ABSOLUTE (theorem-grade) | `48` walked rows | all `48` | `0` | `0` |
| RATIO (PRIMARY, not theorem-grade) | `42` ratio-bearing measurements | `42` (`21` hydro + `21` shear) | `8` | `19 %` |
| RATIO — hydrostatic `A1` sector only | `21` | `21` | `0` | `0` |
| RATIO — `T2` shear companion only | `21` | `21` | `8` | `38 %` |

**The hydrostatic (A1) sector — the headline sector — is clean on both clauses: `0` ratio-clause violations of `21` ratio-bearing hydro rows.**

**★AND THE VIOLATIONS DO NOT SUPPORT THE INFERENCE THE FROZEN TEXT DRAWS FROM THEM (flag-don't-fix).** G1's second clause is `G1 also requires the ABSOLUTE ordering K_SUBC ≤ K_KUBC and G_SUBC ≤ G_KUBC on every configuration INCLUDING the uncaged reference.` That clause — the theorem-grade one — **holds on all 48 measurements**, including every violating row: JSON `gate_G1_VOID_ordering.absolute_theorem_grade_ordering_holds_everywhere = True`, and `violations_that_are_RATIO_ONLY_with_absolute_ordering_intact` contains all 8. At `bulk_only_cold_phi_sf` shear the absolutes are `G_SUBC = 0.290283 ≤ G_KUBC = 0.550409`. **So the SUBC extraction is NOT wrong at those rows, and the frozen inference "a violation means the SUBC extraction is WRONG" is not supported by the data.**

**The mechanism is algebraic and the prereg already contains it.** `R_KUBC/R_SUBC = g_0^arm / g_0^uncaged`, so the PRIMARY ratio pair is bound-ordered **only when the arm's own KUBC/SUBC gap exceeds the reference's**. In the shear channel the UNCAGED gap is huge (`g_0 = 2.1600` at `L=16`, vs `1.2419` in bulk) because the under-coordinated free surface is far floppier in shear; the caged arm's gap is smaller (`0.550409/0.290283 = 1.896 < 2.160`), so the ratio inverts. The frozen §2.1 states this outright: `the PRIMARY same-instrument bracket cancels the finite-size boundary-layer bias to leading order (numerator and denominator share the box and the boundary condition) but is NOT theorem-grade on the RATIO, because the uncaged reference is itself boundary-conditioned`. **G1 demands a theorem-grade ordering from a quantity the same prereg declares not theorem-grade.** This is the SAME mutual-satisfiability defect the prereg's §6 audit caught for the core estimator and moved G1 off — the audit moved the gate from the core pair to the whole-cell pair but did not notice the whole-cell RATIO carries the identical exposure whenever the reference's own gap is large. **The gate outcome is reported AS FROZEN (VOID). The criterion is ROUTED, not relabelled.**

**★VOID SCOPING — also ambiguous, also shipped both ways, also resolved conservatively.** Frozen G1 says the bench is VOID "for that configuration"; §5.3 says `G1 violated for that configuration`. Under the **STRICT** reading a shear-mode inversion voids that configuration's hydrostatic read too — voiding 8 configurations **including both headline arms**. Under the per-`(configuration, mode)` reading only the 8 shear measurements void. **This result carries the STRICT reading**, again because it is the one that voids more and so cannot be a rescue. Both are shipped: JSON `gate_G1_VOID_ordering.void_configs_STRICT` and `.void_config_modes`, and every read row carries `VOID_strict_per_configuration`, `VOID_per_configuration_and_mode`, and `classes_no_void_overlay` so the VOID overlay is separable from the threshold relation.

Per frozen §5.5 item 4: `If a configuration VOIDs: its bracket is NOT reported as physics; the standing merged verdict for that configuration is left exactly as merged, and the extraction failure is reported as an instrument finding with its residuals.` — **honoured: nothing above is banked, and no standing verdict is moved.**

### §4.1 — ★THE THIRD FROZEN AMBIGUITY, and the VOID applied consistently (PR #802 review, findings F6 / F7)

**★A THIRD frozen ambiguity exists and was not surfaced in the first version of this doc.** Frozen §5.5 names its dispositions by OUTCOME CLASS — `If a bracket STRADDLES its threshold` (item 1), `If a bracket RESOLVES-HIGH at T1 on a headline arm` (item 3) — but **never says which of the two frozen bracket definitions the words "a bracket" denote.** §2.1 (PRIMARY, same-instrument, explicitly *not* theorem-grade on the ratio) and §2.2 (CONSERVATIVE, theorem-grade) are both frozen, and on this data **they disagree at many rows**. Where the PRIMARY bracket RESOLVES and the CONSERVATIVE one STRADDLES, **item 1's antecedent and item 3's antecedent are satisfied simultaneously on the same configuration, and the frozen text does not adjudicate between them.** Measured: `n_BOTH_antecedents_satisfied = 14` rows, listed at JSON `verdict.VOID_and_ambiguity_consistency_audit.BOTH_antecedents_satisfied_rows` — including **both headline arms** (`bulk_only_cold_phi_sf::hydro`, `grown_frozen_tangent::hydro`). This joins the two already-disclosed ambiguities (the G2b latitude sentence, §3; the G1 VOID scope, above) as a **third criterion-side defect. It is surfaced, not resolved: adjudication is Grant's / the auditor's.**

**★AND THE VOID IS NOW APPLIED CONSISTENTLY.** The first version of this doc took item 1's STRADDLE disposition on arms it had itself declared VOID, while refusing item 3 partly on VOID grounds — an asymmetry. Stated plainly, and this is the resolution carried: **the only frozen §5.5 disposition that survives a VOID is item 4 — nothing moves.** Items 1, 2 and 3 all presuppose a READABLE bracket, so on a VOIDed row none of them is available; the fact that item 1 and item 4 happen to agree on the outcome (nothing moves) does not make item 1 *available* on a VOIDed row. **So: on the `16` VOIDed rows the operative disposition is item 4, and on the rest it is item 1 — and both say NOTHING MOVES, which is what this lane reports.** The asymmetry is therefore corrected without changing any outcome, and the corrected reasoning is shipped at JSON `verdict.VOID_and_ambiguity_consistency_audit.ASYMMETRY_SURFACED` so it cannot drift from the prose.

**★AND THE DOC/JSON RECONCILIATION on frozen §5.5 item 3 (finding F6).** §8 consequence 6 says item 3 is NOT invoked. That is **true of the headline arms**, and true of them on **both** bracket definitions — JSON `verdict.frozen_5p5_item3_invocation_audit.item3_invoked_at_a_HEADLINE_arm_on_PRIMARY` and `..._on_CONSERVATIVE` are **both empty**. It is **not** true as a statement about the whole artefact: the per-row disposition string is generated from the §2.1 PRIMARY class, so it emits the item-3 sentence on **`20`** of the `42` rows on PRIMARY and would on **`13`** on CONSERVATIVE — and `2` of the `20` are the `uniform_medium_null` identity rows, whose bracket is `[1.0, 1.0]` by construction and is not a physics disposition at all. **An auditor querying the JSON would find item-3 strings; the doc previously implied they did not exist. Both counts are now shipped and both statements are made, so the doc and the artefact cannot drift apart.** No row's disposition is changed by this: every one of those `20` rows is either VOID (item 4 governs, nothing moves) or non-headline (frozen item 3 is conditioned on `a headline arm`, so it does not fire), and **no standing caveat is discharged anywhere in this lane.**

---

## §5 — ★THE READ, had the gates not voided (frozen §5.4) — reported as CONDITIONAL, not banked

Frozen: `HEADLINE: the PRIMARY r_Z bracket of configuration 1 (bulk_only_cold at φ_sf) against T1 (r_Z = 0.5), and the PRIMARY r_Z bracket of configuration 7 (the #796 grown frozen-tangent arm) against T1 and T2 — each reported with its outcome class, its width, and its CONSERVATIVE counterpart.`

| arm | `r_Z` PRIMARY | vs `T1 = 0.50` | `r_Z` CONSERVATIVE | vs `T1 = 0.50` | vs `T2 = 0.45` | vs `T2 = 0.55` |
|---|---|---|---|---|---|---|
| `bulk_only_cold` @ `φ_sf` | `[0.51322, 0.64359]` | **RESOLVES-HIGH** | `[0.46053, 0.71723]` | **STRADDLES** | RESOLVES-HIGH | STRADDLES |
| #796 `grown_frozen_tangent` | `[0.51348, 0.64365]` | **RESOLVES-HIGH** | `[0.46077, 0.71729]` | **STRADDLES** | RESOLVES-HIGH | STRADDLES |

**All numbers `[derived]` in their `K` factor and `[assumption]` in their `ρ` factor — MIXED-provenance. `SCOPE: this bench brackets K_eff ONLY.`**

**What this would have meant, stated conditionally.** On the PRIMARY (same-instrument) definition, the low side of both headline brackets sits **above** the `r_Z = 0.5` macro-cage edge, so the `Z_lo`/SOFT reading — the one #796 §4 flagged as excluded only by the anchor choice, and the one that would re-open the Reading-B route — would be **excluded on both boundary conditions**, i.e. the exclusion would not be an artefact of clamping. **On the CONSERVATIVE (theorem-grade) definition it would not be excluded**: the rigorous bracket still contains `0.5`. Frozen §2.2: `the CONSERVATIVE bracket is theorem-grade and always contains the PRIMARY bracket; it is wider by exactly the uncaged gap g_0 on each side`. **The rigorous answer is therefore that the corpus's `r_Z ≈ 0.54` remains BOUND-CONDITIONAL, and the frozen §5.5 STRADDLE disposition applies: standing labels UNCHANGED.**

**T3 (`R = 1.0`, the soften/stiffen sign) is the one threshold that resolves under BOTH definitions and both boundary conditions**: every deep-rail cage arm is RESOLVES-LOW (softens) and the `rigid` control is RESOLVES-HIGH (stiffens). That is the §5.3 Layer-2 forced-reachability discharge, realized. `outcome_classes_returned_with_VOID_overlay_removed = ["RESOLVES-HIGH", "RESOLVES-LOW", "STRADDLES"]` — all three non-VOID classes were returned on the physical set, and VOID was returned by SELFTEST-G1. **No class was a dead letter.**

**T4 lift (frozen §5.2), applied per-boundary-condition to the grown-vs-control pair — NARROWED to what was measured (PR #802 review, finding F9).** Measured: `lift_under_SUBC = 1.00102`, `lift_under_KUBC = 1.00019`, and on the core convention `0.99521` (SUBC) / `1.00631` (KUBC). All four variants sit within `0.0063` of unity (`worst_departure_from_unity`).

**★Two honest corrections to the sentence this doc previously carried.** (i) **This is not #796's banked `K_ratio_lift` read back.** It is `(grown_frozen_tangent ratio) / (isotropic_control ratio)` recomputed *in this driver* under each boundary condition — a different construction on differently-reconstructed arms. Naming #796's banked lift while measuring this quantity conflates the two. (ii) **The frozen T4 band test cannot fire here.** The bands are `L1 < 1.2` / `L2 = 1.2–1.5` / `L3 ≥ 1.5`; every variant is `L1` **by arithmetic** at a departure of `0.63 %` from unity, so `band_flips_across_boundary_condition = False` is not a discriminating statement and is not read as one. **What IS supported, and all that is claimed: the grown-vs-control ratio is `≈ 1` under BOTH boundary conditions on BOTH energy conventions — i.e. the near-null lift this bench family measures is not an artefact of clamping the outer skin.** Reported as a per-BC pair, not a bracket — a ratio of two ratios is not bound-ordered (JSON `verdict.T4_lift_bands_per_boundary_condition.note` / `.WHAT_WAS_MEASURED`).

---

## §6 — SUPPLEMENTARY axes shipped ADDITIVELY (NOT frozen, NOT the deliverable, nothing amended)

### §6.1 — The `[111]` longitudinal modulus `K + 4·C44/3` (the modulus-identity question, ROUTED)

> **★LABEL WITHDRAWN (PR #802 review, finding F2).** This axis was previously shipped as *"the P-wave / VRH modulus `M = K + 4G/3` (`= C11` for an isotropic average)"*. **That label is WRONG on this medium and is withdrawn.** The `G` this driver measures is the `xy` engineering shear constant **`C44`**, and the medium is **cubic-anisotropic** (§6.5). On a cubic medium `K + 4C44/3` is the longitudinal modulus along the body diagonal **`[111]`**; the `[100]` longitudinal modulus is `C11 = K + 4C′/3` with `C′ = (C11−C12)/2`, and the two differ by `(4/3)(C44 − C′)`. **`M_SUBC_abs` / `M_KUBC_abs` below are the `[111]` quantity and must NOT be consumed as `C11`.** The true `C11` is measured and shipped in §6.5.

The frozen deliverable is the BULK-`K` bracket. But a normal-incidence impedance is `Z = ρc_P = √(ρ·M_L)` with a LONGITUDINAL modulus `M_L`, and `√(ρK)` is the correct impedance **only for a medium with no shear response** — which this composite is not (`C44_eff/C44_0 ≈ 0.61–0.67` at `φ_sf`). Because both `K` and `C44` are bracketed by the same pair of boundary conditions and `K + 4C44/3` is monotone increasing in both, `M_SUBC ≤ M* ≤ M_KUBC` is a legitimate absolute bracket on the `[111]` longitudinal modulus.

**★AND BOTH RATIO BRACKETS ARE NOW SHIPPED (PR #802 review, findings F1 / F8).** The first version of this axis shipped only the PRIMARY ratio — the definition frozen §2.1 itself declares `NOT theorem-grade on the RATIO, because the uncaged reference is itself boundary-conditioned` — with no CONSERVATIVE counterpart, even though all four absolutes needed to build one were already shipped. **That caveat lived only in a driver comment; it is lifted into the prose here, and the theorem-grade column is added** (it is one divide from values already shipped: `[R_SUBC/g_0, R_KUBC·g_0]`, the frozen §2.2 construction applied to this axis — **no re-run, no new solve**). Shipped for every configuration that ran both modes:

| arm | `M_[111]` absolute `[SUBC, KUBC]` | `R_M` **PRIMARY** | `r_Z(M)` **PRIMARY** | `R_M` **CONSERVATIVE** | `r_Z(M)` **CONSERVATIVE** | `g_0(M)` |
|---|---|---|---|---|---|---|
| `bulk_only_cold` @ `φ_sf` | `[0.7379, 1.4191]` | `[0.39020, 0.49587]` | `[0.62466, 0.70418]` | `[0.25784, 0.75041]` | `[0.50778, 0.86626]` | `1.5133` |
| `symmetric_cold` @ `φ_sf` | `[0.2897, 1.1408]` | `[0.15319, 0.39860]` | `[0.39140, 0.63135]` | `[0.10123, 0.60321]` | `[0.31817, 0.77667]` | `1.5133` |
| `bulk_only_compressed` @ `φ_sf` | `[0.4928, 1.2536]` | `[0.26056, 0.43803]` | `[0.51045, 0.66184]` | `[0.17218, 0.66289]` | `[0.41494, 0.81418]` | `1.5133` |
| #796 `grown_frozen_tangent` | `[0.7247, 1.4049]` | `[0.38321, 0.49091]` | `[0.61904, 0.70065]` | `[0.25322, 0.74290]` | `[0.50321, 0.86192]` | `1.5133` |

**★THE ROUTED CONSEQUENCE, RESTATED THRESHOLD-BY-THRESHOLD — and it is STRONGER on the theorem-grade bracket, not weaker.** The first version said only "the axis choice moves the bin," which is a PRIMARY-only statement. Honestly, per threshold, on the **CONSERVATIVE (theorem-grade)** bracket at the two headline arms:

- **At `T1 = 0.50` the bin MOVES.** Bulk-`K` CONSERVATIVE **STRADDLES** (`[0.46053, 0.71723]` headline, `[0.46077, 0.71729]` grown) while `M_[111]` CONSERVATIVE **RESOLVES-HIGH** (`[0.50778, 0.86626]`, `[0.50321, 0.86192]` — both low ends above `0.5`). **So on the rigorous definition, the axis choice is the difference between "undetermined" and "resolved on the matched side" at the bin edge that actually controls #782's BIN-4.** That is a stronger form of the routed question than the PRIMARY-only version: it is no longer "a non-theorem-grade reading is axis-sensitive," it is "the *theorem-grade* reading is axis-sensitive."
- **At `T2 = 0.55` the bin does NOT move.** Both axes STRADDLE on CONSERVATIVE (`K`: `[0.46053, 0.71723]`; `M_[111]`: `[0.50778, 0.86626]`). The `Z_hi` clearance the first version reported at `T2` is a PRIMARY-only effect and does **not** survive to the theorem-grade bracket.
- On PRIMARY, for completeness: both axes RESOLVE-HIGH at `T1` at both headline arms; at `T2 = 0.55` the `K` axis STRADDLES while `M_[111]` RESOLVES-HIGH.

Machine-readable at JSON `configurations[*].SUPPLEMENTARY_M_axis_NOT_FROZEN.threshold_classes` (all three thresholds × both definitions). **WHICH modulus the corpus discriminator `r_Z` should ride is a DEFINITION question this lane does not own and does not settle.** It is surfaced with data and ROUTED to Grant. The frozen bulk-`K` axis remains the deliverable; nothing here amends the frozen prereg (§9 note 2).

### §6.2 — The pinned-shell confound (KUBC side only)

`rve_aggregation_bench.cubic_cage_centers` documents a `margin = 3.0` standoff *"so cages never touch the KUBC boundary shell"*, but the cage SHELL has radius `r_cage + cage_w`, so at larger `r_cage` the shell reaches into the `bw = 1.5` Dirichlet layer. Measured fraction of cage-shell NODES that are pinned under KUBC:

- route A (`s = 4.5`, `r_cage = 1.3/1.6/1.9/2.2`): `9.4 % / 19.4 % / 34.3 % / **51.3 %**`
- route B (`r_cage = 1.7`, `s = 3.6/4.2/5.0/6.5`): `0.0 % / 12.7 % / 40.9 % / 0.0 %`
- size scan at `φ_sf`: `53.3 % (L12) / 51.3 % (L16) / 48.5 % (L20)`

**At the headline geometry more than half the soft shell is clamped affine and cannot open, which stiffens the KUBC side.** Under SUBC no displacement is prescribed anywhere, so `pinned_fraction_SUBC = 0.0` by construction. **This asymmetry is exactly what a two-sided bracket is positioned to expose**, and it is a live confound for any KUBC-side number in this bench family — including the merged ones. It is reported **RAW and never subtracted** (frozen §0 pre-test check: RAW, no subtraction), and **no claim is made here about how much of the KUBC/SUBC gap it accounts for** — that attribution is routed, not asserted. It is also strongly route-dependent, so it cannot be treated as a common-mode offset that cancels in a scan.

### §6.3 — Realized vs intensive phase fractions (the labelling question, settled for THIS lane's configs)

The intensive label `φ = (4/3)πr³/s³` is a per-array-cell quantity; the finite bench cell realizes less because the cage cluster stands off the outer faces. Measured, for every configuration (JSON `configurations[*].fractions`):

| configuration | `n_cages` | `φ` intensive | `φ` realized (box) | `φ` realized (bond) | `f_incl` intensive |
|---|---|---|---|---|---|
| `bulk_only_cold_rc1.3` | 27 | `0.1010` | `0.0636` | `0.0612` | `0.5593` |
| `bulk_only_cold_rc1.6` | 27 | `0.1883` | `0.1186` | `0.1123` | `0.8079` |
| `bulk_only_cold_rc1.9` | 27 | `0.3153` | `0.1986` | `0.1973` | `1.1211` |
| `bulk_only_cold_phi_sf` | 27 | `0.4895` | `0.3082` | `0.3079` | `1.5063` |
| `routeB_…_s3.6` | 27 | `0.4411` | `0.1422` | `0.1400` | `1.7671` |
| `routeB_…_s4.2` | 27 | `0.2778` | `0.1422` | `0.1392` | `1.1128` |
| `routeB_…_s5.0` | 27 | `0.1646` | `0.1422` | `0.1395` | `0.6596` |
| `routeB_…_s6.5` | 8 | `0.0749` | `0.0421` | `0.0413` | `0.3002` |
| `…_phi_sf_L12` | 8 | `0.4895` | `0.2200` | `0.2186` | `1.5063` |
| `…_phi_sf_L20` | 64 | `0.4895` | `0.3705` | `0.3713` | `1.5063` |

**Two facts follow and are stated without adjudicating anything.** (i) The three `L∈{12,16,20}` size points carry the SAME intensive label `0.4895` but THREE DIFFERENT realized fractions (`0.220 / 0.308 / 0.371`) — so any size gate on this family compares three different composites (see §1.2 caveat, and note this bears on the merged #782/#796 RVE-size gate, which is the auditor's to assess, not this lane's to re-open). (ii) Route B holds realized `φ ≈ 0.142` almost constant across `s = 3.6/4.2/5.0` while the intensive label sweeps `0.441 → 0.165` — the two routes are not scanning the same variable in realized terms. **Nothing in this lane's bracket depends on either convention:** every frozen configuration is fixed by the TUPLE `(L, r_cage, s, cage_w, wall_class, s_rail)`, never by a `φ` label.

### §6.4 — The fully-SUBC-grown companion (frozen §3 D — explicitly NOT a bracket)

Frozen: `A fully-SUBC-grown arm, if run, is reported as a labelled companion and is explicitly NOT part of any bracket, because its microstructure co-varies with the boundary condition`.
Frozen budget: `the fully-SUBC-grown companion is capped at 40 outer iterations and 20 minutes wall-clock; on exceeding either it is reported as NOT-RUN with the reason, and no verdict depends on it` → **RAN and CONVERGED well inside budget: `5` outer iterations, and on wall-clock by a wide margin (the wall-clock figure is machine-dependent and therefore not quoted here — JSON `companion_fully_SUBC_grown.wall_clock_s`; it is excluded from the frozen determinism digest for the same reason).** Self-consistency decay `0.6891 → 0.02404 → 3.592e-4 → 8.939e-6 → 1.371e-7`.

Grown under a traction-free boundary, the vessel reaches `min k_shear,eff = 0.29044 > 0` (no bond buckles), `peak_A = 0.07260`, `max|T| = 0.68910`. Probed under traction on its own frozen tangent: `R_SUBC = 0.26189`, `R_KUBC = 0.41276`, core `0.29820`. **These sit within `~0.6 %` of the KUBC-grown frozen-tangent arm** — i.e. the growth boundary condition barely moves the resulting tangent modulus on this instrument. **That is a COMPANION observation only and enters no bound-robustness claim** (frozen §8 Fork TANGENT); it is reported because it is cheap and informative, not because it brackets anything.

### §6.5 — ★THE MEDIUM IS CUBIC, NOT ISOTROPIC: `C′`, the true `C11`, and the Zener anisotropy (PR #802 review, finding F2)

**No isotropy check existed in the frozen prereg, in the driver, or in the first version of this result doc.** The `[111]`/`C11` mislabel of §6.1 survived precisely because nobody measured whether the assumption behind it holds. It does not. The repair MEASURES the missing constant rather than assuming it away.

**How `C′` is measured — through this lane's own machinery, no new physics.** A third probe mode `tetra` is added with `E = ε·diag(1,−1,0)` (KUBC) and `Σ = σ·diag(1,−1,0)` (SUBC), run through the SAME `subc_solve` / `kubc_solve` primitives, the SAME operator, the SAME tolerances as the two frozen modes. For a cubic medium that mode has strain energy density `2C′ε²` and complementary energy density `σ²/(2C′)`, so both extractions return `C′ = (C11−C12)/2` directly. **It is deliberately NOT in `BOTH_MODES` and NOT in any `by_mode` block, so it enters NO frozen gate, NO frozen read and NO frozen count** — it is carried in a separately-labelled `SUPPLEMENTARY_anisotropy_NOT_FROZEN` block. Additive, never substitutive.

> **★THE AXIS-ALIGNMENT EVIDENCE, REPLACED — the first version's was a NON SEQUITUR (RE-VERIFY finding F1).** The first version of this section offered the realized SUBC macroscopic stress `diag(+0.9047486144890591, −0.9047486144890545, 0)` as evidence that *"the cubic axes are the lattice axes."* **It is not evidence of that.** That `Σ̄` is `hill_stress(traction_load(...))` — built from the load set and the node positions, and it **never sees `Phi`**. It is bit-identical for the cold uncaged medium, for the caged arm, for the `rigid` control at `100×` the shell stiffness, and for `uniform_medium_null` with no cages at all; a medium whose elastic axes were rotated `45` degrees would return exactly the same tensor. **It is a property of the LOAD SET, not of the MEDIUM, and it is WITHDRAWN as evidence.** The conclusion it was offered for is nonetheless TRUE, and is here MEASURED two ways that do see `Phi` (JSON `SUPPLEMENTARY_anisotropy_NOT_FROZEN.ISOTROPY_CHECK`; this also discharges the *"no direct `C11` probe"* half of §7 D-8):
>
> **(a) A DIRECT UNIAXIAL `C11` PROBE.** KUBC with `E = ε·diag(1,0,0)` gives `C11 = 2U/(V ε²)` with **no assembly step at all**. On the cold uncaged reference at `L = 16` that reads `2.4065530792934391` against the assembled `K + 4C′/3 = 2.4065530790628289` — **rel `9.582601379065636e-11`**; on the `bulk_only_cold` `φ_sf` arm, `1.2109017632998478` against `1.2109017633494381` — **rel `−4.0953240798558e-11`**. **This is a CUBIC IDENTITY that holds only if `[100]` is an elastic axis of the medium**, and it is medium-sensitive: had the cubic axes been rotated `45` degrees about `z`, the same two extractions would read `2.7480507557864744` and `2.8618833146943565` — a `4.142301908659762e-02` relative disagreement, some **nine orders** above the measured one.
>
> **(b) THE KUBC REACTION-STRESS STRUCTURE.** Unlike the SUBC load set, the reaction field `r = K u` **is** a functional of `Phi`, so its tensor structure *is* a property of the medium. Under the tetragonal strain the medium returns a **purely tetragonal** stress — `|Σ₃₃|/|Σ₁₁| = 1.4546606129234564e-08`, `max|offdiag|/max|diag| = 7.691164892224701e-04` (uncaged) and `1.4802740995621563e-04` (arm). Under the uniaxial strain it returns **`Σ₂₂ = Σ₃₃`** — `1.2782237879433649` vs `1.2782237945402415`, rel `5.1609715472077028e-09` — which is exactly the cubic `C12 = C13` signature, and `Σ₁₁/ε` independently recovers the same `C11` the energy route gives (rel `4.3949173189616886e-09`), so the two extractions cross-check each other. **Honest limit, stated rather than glossed:** the off-diagonal test bounds a misalignment `θ` about `z` to `|sin 4θ| ≤ 2.5412080825437736e-03`, i.e. `θ ≤ 0.036400163675767545` degrees — but it is **blind at exactly `θ = 45` degrees**, where `sin 4θ` vanishes too. That branch is the one **(a)** excludes. **Neither measurement alone is the evidence; the two together are.**

**The cold uncaged reference medium, both boundary conditions, all three box sizes:**

| `L` | `C44` `[SUBC, KUBC]` | `C′` `[SUBC, KUBC]` | **Zener `A = C44/C′`** `[SUBC, KUBC]` | `M_[111] = K + 4C44/3` `[SUBC, KUBC]` | **true `C11 = K + 4C′/3`** `[SUBC, KUBC]` |
|---|---|---|---|---|---|
| 12 | `[0.408064, 1.019449]` | `[0.312143, 0.625135]` | **`[1.307300, 1.630766]`** | `[1.780225, 3.022065]` | `[1.652330, 2.496313]` |
| 16 | `[0.419281, 0.905662]` | `[0.315154, 0.564165]` | **`[1.330402, 1.605316]`** | `[1.891127, 2.861883]` | `[1.752290, 2.406553]` |
| 20 | `[0.426097, 0.828878]` | `[0.316846, 0.523053]` | **`[1.344806, 1.584690]`** | `[1.960543, 2.754420]` | `[1.814875, 2.346654]` |

**`A ≠ 1` under BOTH boundary conditions at every box size — the medium is strongly cubic.** At `L = 16` the shipped `M_[111]` **overstates the true `C11` by `18.9 %` under KUBC and `7.9 %` under SUBC.**

**And the caged arms are cubic too** (`SUPPLEMENTARY_anisotropy_NOT_FROZEN` on every configuration). At the two headline arms:

| arm | `C′` `[SUBC, KUBC]` | Zener `A` `[SUBC, KUBC]` | `M_[111]` `[SUBC, KUBC]` | true `C11` `[SUBC, KUBC]` | `M_[111]` overstates `C11` by |
|---|---|---|---|---|---|
| `bulk_only_cold` @ `φ_sf` | `[0.26297, 0.39424]` | `[1.1039, 1.3961]` | `[0.73791, 1.41912]` | `[0.70149, 1.21090]` | `5.2 %` SUBC / `17.2 %` KUBC |
| #796 `grown_frozen_tangent` | `[0.25311, 0.38466]` | `[1.1066, 1.4029]` | `[0.72469, 1.40492]` | `[0.68871, 1.19826]` | `5.2 %` SUBC / `17.2 %` KUBC |

`C11 = K + 4C′/3` is monotone increasing in BOTH `K` and `C′`, each bracketed in the same direction by the same boundary-condition pair, so `C11_SUBC ≤ C11* ≤ C11_KUBC` is a legitimate ABSOLUTE bracket, exactly as for the `[111]` axis. Ratio brackets, both definitions, at the headline arms: `r_Z(C11)` PRIMARY `[0.63271, 0.70934]` / CONSERVATIVE `[0.53990, 0.83129]` (`bulk_only_cold`); PRIMARY `[0.62693, 0.70563]` / CONSERVATIVE `[0.53496, 0.82694]` (grown).

> ### ★ROUTED TO GRANT — this SHARPENS the modulus-identity question rather than answering it
>
> **A cubic medium has a DIRECTION-DEPENDENT longitudinal modulus. Protocol E launches its pulse along `[100]`.** Therefore the comparator for a Protocol-E time-of-flight is **`C11 = K + 4C′/3`** — **not** the `K + 4C44/3` axis this driver previously mislabelled "`C11` for an isotropic average", and **not** a VRH isotropic average of the two. This is a *third* live option in the already-routed modulus question (`K` vs `[111]` vs `[100]`), and the three do not agree: at the headline arm the CONSERVATIVE `r_Z` brackets are `K`: `[0.46053, 0.71723]` (STRADDLES `T1`), `M_[111]`: `[0.50778, 0.86626]` (RESOLVES-HIGH), `C11_[100]`: `[0.53990, 0.83129]` (RESOLVES-HIGH). **This lane does not answer it and does not pick.** JSON `configurations[*].SUPPLEMENTARY_anisotropy_NOT_FROZEN.ROUTED_TO_GRANT`.
> **★BENCH-FRAME CAVEAT (2026-07-28 leak-audit fold — Grant [sic]: "fold").** `[100]` is the **bench's** launch direction because the simulation grid made it convenient — an **engineering choice**, not a derived one. The **physical** discriminator (a compression wave meeting a star-scale composite) has its propagation direction set by **source geometry relative to the cosmically-fixed lattice axes** — generically off-axis, with mode conversion at oblique incidence. So the three-way question below is the **bench-consistency** form; the physical-scenario form needs the direction-resolved Christoffel treatment or a **derived source-direction average** — explicitly **NOT** a VRH/polycrystal average: the vacuum is a **single crystal**; there are no grains to average over, and "average over grain orientations" and "average over propagation directions of a fixed crystal" are different objects.
>
> **★NEW FRONTIER FLAG (routed, not run): the anisotropy observable.** The measured cubic anisotropy (Zener `A = 1.605` KUBC / `1.330` SUBC at lattice scale) implies a **direction-dependent long-wave P-speed** in the mechanical/gravitational band of a single-crystal vacuum. Whether that direction dependence survives to the long-wavelength limit of the P-branch, at what magnitude, and whether it is already constrained observationally (gravitational-band propagation anisotropy vs the cosmically-fixed axes) is a **new, unexamined question** this measurement surfaces. Routed to the frontier tracker (item 21); no claim made here.

---

## §7 — HONEST DEVIATIONS

1. **★D-1 — the lane died mid-run TWICE and was RESUMED both times; the frozen prereg is byte-untouched across both.** (a) The original lane process was killed by an API error immediately after the frozen prereg was committed and pushed ALONE (`1288e288`), at the point recorded as *"Now the driver."* A 437-line driver skeleton survived UNCOMMITTED in the worktree (the SUBC primitive, the three ratio definitions, the classifier, SELFTEST-PARTITION, the geometry helpers); it was read, kept, and completed. (b) The **PR #802 adversarial-review repair lane** was likewise killed by an API error, mid-repair, at the point recorded as *"Now the MODULUS_IDENTITY block and the anisotropy machinery"* (i.e. inside finding F2). Uncommitted work again survived in the worktree: driver changes and a regenerated JSON. **That surviving work was AUDITED, not trusted** — the resumed lane re-ran the driver from the surviving source and compared the regenerated JSON leaf-by-leaf against the surviving one; the surviving work was a complete, self-consistent, deterministic run of the surviving source and was KEPT rather than reverted, and the repair was completed on top of it. **★THE FIGURE THIS DEVIATION PREVIOUSLY QUOTED IS WITHDRAWN AS UNAUDITABLE (RE-VERIFY finding F5).** It read *"`8587` of `8587` numeric leaves bit-identical, `0` removed, `0` changed."* **No committed state of this lane's JSON yields `8587` under any leaf convention**, the counting convention was never stated, and the comparison's right-hand side was an uncommitted working-tree file that no longer exists — so a reader cannot re-derive the number from anything in the repository. It is withdrawn rather than restated at a new value, because a leaf count of a moving artefact is not the evidence that matters. **The evidence is restated as the two checks an auditor can actually re-run:** (i) **two independent full driver runs**, separate processes, separate `--out`, `diff -q` CLEAN on the timing-stripped payloads and identical `determinism_digest` (frozen G3, §1) — which is what "the surviving run was deterministic" means operationally; and (ii) the **`0e8d4e67` → HEAD leaf diff**, reported with its exact scope in D-16. **The frozen prereg was BYTE-UNTOUCHED across BOTH interruptions and remains so** — md5 `9e467d603d5745c1e05094506234e5d2`, verified against the `1288e288` blob before and after. No verdict number existed before the freeze; none was carried across either interruption.
2. **★D-2 — SELFTEST-G2b does not fire (§3).** A frozen-criterion design defect: the mis-normalization the self-test uses is the same finite boundary layer as the gap it is meant to break, so it cancels rather than probes. Both normalizations shipped as the frozen latitude requires. The latitude sentence is ambiguous; the VOIDing reading is carried; the adjudication is ROUTED. **Not repaired.**
3. **★D-3 — frozen G1 fires on 8 non-errors (§4).** All shear-mode, all ratio-only, absolute theorem-grade ordering intact on 48/48. G1 demands a bound-ordering from the PRIMARY ratio, which frozen §2.1 itself declares not theorem-grade. Reported AS FROZEN (VOID); criterion ROUTED. **Not repaired, not relabelled.** Both VOID scopings shipped; the stricter one carried.
4. **★D-4 — G7b passes but its inference does not transfer.** The size scan changes the realized composite as it changes `L` (§6.3), so the monotone width decrease is not clean size-convergence evidence. Disclosed; the "bracket is converging" inference is NOT made.
5. **★D-5 — THE SHIPPED LOAD SET DOES NOT SATISFY A FROZEN DESIGN PROPERTY** (re-characterized by the PR #802 review, finding F4 — the first version called this "a prereg pilot number does not reproduce," which understated it). Frozen §1.1 does not merely report a pilot measurement; it asserts a **property of the load-set construction it freezes**: *"the constructed set is self-equilibrated AND moment-free to machine precision"* (`|Σ_i (x_i−x_c)×f_i| = 3.2e-14` at `L=12`). **The shipped construction does not have that property.** Measured net moment: `4.238e-3` at `L=12`, `4.283e-3` at `L=16`, `4.309e-3` at `L=20` — about `2.6e-6` of the `σ·A·span` scale, but **eleven orders above machine precision.** So the correct statement is *"the shipped load set is self-equilibrated but is NOT moment-free to machine precision, contrary to the frozen §1.1 assertion"* — a design-property deviation, not a failed reproduction of an incidental number. Net FORCE **is** machine-zero as frozen (`|Σf| = 4.5e-12`). The residual moment is **harmless and does not touch any result**: (i) it is reference-point-independent because the net force vanishes, (ii) rigid rotations are NOT null modes of the Born bond model (§1), so a moment-carrying load is admissible and the pure-Neumann operator is invertible on the complement of the translations, and (iii) Hill's lemma symmetrizes, so the antisymmetric part never enters `Σ̄`. Every solve converged to `≤ 1e-9` with the work identity exact to `8.6e-16`. **Flagged, not fixed** — I could not reproduce the pilot's `3.2e-14`, I do not know what the pilot computed differently, and the prereg is frozen and is not amended. **This is a defect in a frozen design property and is ROUTED with the other criterion-side defects.**
6. **★D-6 — supplementary axes added post-freeze, ADDITIVELY.** The longitudinal-modulus axis (§6.1), the pinned-shell fractions (§6.2), the realized-fraction table (§6.3) and — added at review repair — the anisotropy block (§6.5) and the F5 cross-check are **NOT** frozen criteria and are labelled as such everywhere they appear. They add data; they replace, reweight and amend nothing. To supply the longitudinal axis, every bracketed configuration was run in BOTH modes rather than the frozen §3 per-configuration mode list (which called for `hydro` only on most arms) — a SUPERSET of the frozen configuration set, never a substitution. The runtime cost of the supplementary work is real but machine-dependent and is therefore not quoted (§7 D-13).
7. **★D-7 — a driver bug found and fixed before shipping (Rule 10).** The companion's self-consistency test initially compared the pre-solve operator against the previous pre-solve operator, which returns `0` on the first pass and declared convergence at `u = 0` after one outer iteration. Fixed to compare the operator re-evaluated at the NEW iterate against the one the solve used; the companion now converges in 5 outer iterations with clean decay (§6.4). The bug affected the companion only — which is explicitly not part of any bracket — and no bracket number changed.
8. **★D-8 — what was NOT run.** No periodic-BC arm (it is not a bound and is not what OWED-1 asks for; frozen §9 owed item 2). No surface-corrected SUBC estimator (frozen Fork SURFACE; routed). No `ρ` measurement of any kind (§7 scope fence; OWED-2 stands). **★The "no direct `C11` probe" half of this caveat is DISCHARGED at the RE-VERIFY repair (§6.5 (a), finding F1):** a direct uniaxial KUBC probe (`E = ε·diag(1,0,0)` ⇒ `C11 = 2U/(V ε²)`) now runs on every configuration and agrees with the assembled `K + 4C′/3` to `~1e-10`. **What is still NOT run is the SUBC side of that probe** — the uniaxial dual would need its own complementary-energy identification, and an axis-alignment statement is a property of the medium, not of the boundary condition, so it is not needed for what the probe is used for. The `[111]` `M` axis of §6.1 remains an assembly from the bracketed `K` and `C44`, which is rigorous for `M_[111]` and is not a direct measurement of it.
9. **★D-9 — an externally-reported claim NOT relied on; ITS "REFUTED" STATUS IS ORCHESTRATOR-RELAYED, NOT VERIFIED HERE** (provenance corrected by the PR #802 review, finding F11). A concurrent un-reviewed scoping lane reported a "3.19× KUBC over-stiffening" from a PBC comparison. **The first version of this doc asserted flatly that the claim "is REFUTED" — carrying no anchor.** Correcting the provenance honestly: that refutation was **relayed to this lane by the orchestrator** as an in-flight report (root-caused as an artefact of a rotation-contaminated elastic-constant reconstruction); **this lane did not run it, did not verify it, and could not locate it in any pushed branch at write time.** It is therefore recorded here as an **orchestrator-relayed input with that provenance**, not as an established corpus fact, and **the reader should treat "REFUTED" as unverified-by-this-lane until it is anchored to a landed artefact.** The load-bearing point is unchanged and does not depend on which way the refutation goes: **nothing in this lane's design, execution or reading depends on the 3.19× claim in either direction** — it is recorded only so the non-dependence is explicit. This lane does not adjudicate PBC-vs-KUBC — that is a different lane's and the auditor's question (frozen §9 owed item 2).
10. **★D-10 — A DEAD CITE INSIDE THE FROZEN PREREG, disclosed and routed, NOT amended** (PR #802 review, finding F10). The frozen prereg's own provenance block forward-cites `_orchestration/docket-entries/2026-07-28-subc-kubc-bracket-prereg.md`. **That file never existed:** the lane process died immediately after the prereg was committed and pushed alone at `1288e288` (D-1), before the companion fragment was written, so from `1288e288` until this repair the frozen prereg contained a citation to a non-existent path. It was invisible to the markdown link gate because it sits inside an inline code span. **The prereg is FROZEN and is NOT amended** — the md5 is `9e467d603d5745c1e05094506234e5d2`, byte-identical to the blob at `1288e288`, verified before and after this repair. Instead the **missing fragment is now supplied**, dated honestly to its actual writing date (2026-07-28, at repair time) with an explicit statement that it was NOT written at freeze time, so nothing is backdated. The defect in the frozen text stands as a disclosed defect and is ROUTED with the others.
11. **★D-11 — THE FIRST VERSION'S DISPOSITION ACCOUNTING WAS INCONSISTENT AND INCOMPLETE** (PR #802 review, findings F6 / F7; repaired in §4.1). Three separate items: (a) a **third frozen ambiguity** — frozen §5.5 never says which of the two frozen bracket definitions "a bracket" denotes, so items 1 and 3 have their antecedents satisfied simultaneously on `14` rows including both headline arms — was never surfaced; (b) the **VOID was applied asymmetrically**, taking item 1's STRADDLE disposition on VOIDed arms while refusing item 3 partly on VOID grounds; (c) the doc's "item 3 is NOT invoked" was **true of the headline arms but false of the artefact**, where the PRIMARY-basis disposition string emits the item-3 sentence on `20` of `42` rows. All three are now stated, the VOID rule is applied consistently (only item 4 survives a VOID), and both counts are shipped in the JSON. **No row's disposition changes and no standing caveat is discharged anywhere in this lane** — the repair is to the accounting and the honesty of the statement, not to any outcome.
12. **★D-12 — SUPPLEMENTARY AXES CORRECTED AND EXTENDED POST-REVIEW, still ADDITIVELY.** The `K + 4G/3` axis was shipped with the label *"P-wave modulus (`= C11` for an isotropic average)"*, which is **wrong on this cubic medium**; the label is withdrawn, the axis is relabelled `[111]`, `C′` and the true `C11` are newly MEASURED via a supplementary `tetra` mode, an anisotropy disclosure is added, and the axis's missing CONSERVATIVE (theorem-grade) ratio bracket is added from already-shipped absolutes (§6.1, §6.5). The `tetra` mode enters **no** frozen gate, read or count. **No frozen criterion, no frozen configuration and no frozen number is changed by any of this** — the frozen bulk-`K` bracket, every gate outcome, the VOID status and every frozen read are bit-identical to the pre-repair run (verified leaf-by-leaf: `0` changed numeric leaves outside the newly-added blocks).
13. **★D-13 — the numbers in this doc are now MACHINE-CHECKED, not carefully typed.** `research/drivers/subc_kubc_bracket_number_check.py` scans every inline-code numeric token in this document and requires each to be either the correctly-rounded value of a **named leaf of a shipped JSON** (this lane's, merged #782's, merged #796's), or **hand-PINNED** to a named path, or explicitly allow-listed with a reason. **A precision floor is enforced:** tokens below `3` significant digits are REFUSED auto-registration, because at 1–2 significant digits a match against the enumerated rounded forms (`31164` then, `69120` now) is coincidence, not provenance — those must be hand-pinned (and are still value-verified) or allow-listed as constants. **★AND SO ARE BARE INTEGERS, from the RE-VERIFY round on** — the precision floor was designed against FLOAT coincidence and works there, but it does not cover INTEGER-vs-INTEGER collision, and this bench ships thousands of integer leaves (CG iteration counts, node and bond counts). The checker's own second run caught this **in itself**: the doc's D-16 leaf-diff count `1108` auto-matched `configurations>>5>>by_mode>>hydro>>solver>>subc_iters`, an unrelated CG iteration count, and was reported as verified provenance. **A count is not a measurement**, so a bare integer must now be ALLOW-listed with a reason or PINNED to a named path, never matched by value. That is the check getting stricter, not laxer, and it is recorded rather than quietly patched (D-15). **Wall-clock is NOT registrable and NOT allow-listed:** it is machine-dependent, the driver's `determinism_digest()` excludes it, and registering it would make the checker fail on every honest re-run on a different machine — so the runtime numeral is **removed from this document** rather than laundered through an allow-list, and the checker's self-check refuses any future attempt to name it as a source. That self-check is deliberately STRUCTURAL (does any registration *name* a machine-dependent path?) rather than value-based, because a value-based version would itself vary by machine. Counts are in §8. **★COVERAGE EXTENDED at the RE-VERIFY repair (finding F7).** The checker's `DOC` was this result doc **alone**, so this lane's docket fragments were outside it — and a reader of the fragment BODY was getting numerals the appended REPAIR NOTE had already superseded (the pre-D-14 net moment, the pre-repair determinism digest, `8 of 48`, `24 of 24`, the withdrawn *"is REFUTED"* and *"P-wave `M = K + 4G/3`"* labels, and wall-clock numerals D-13's own policy had removed from the result doc). Nothing there was live-false — every one is superseded by the appended note — but a body-only reader would not know that. `DOC` is now `DOCS`, a **list**: this result doc **plus both lane docket fragments**, all scanned under the same rules, so the same standard that keeps a number out of the result doc keeps it out of the fragments. The fragment bodies are corrected in place to match (git carries the prior text; the REPAIR NOTE carries the narrative). **Wiring into `make verify` is deliberately deferred:** the equivalent gate target `verify-lane-number-checks` is introduced by a concurrent branch, and adding a second definition here would create a Makefile merge conflict; whichever branch lands second should add this script to that target's recipe. Disclosed so the omission is not mistaken for an oversight.
14. **★D-14 — FOUR RETYPED NUMBERS WERE FOUND IN THE SHIPPED DOC AND CORRECTED, by the D-13 checker on its first run.** Every one was in a hand-typed table cell that this document's own header claims is *"read from the shipped JSON."* None changes any verdict, class, gate outcome or routed consequence — they are all in the fifth decimal place of a supplementary column or the fourth significant figure of a diagnostic — but all four are corrections to numbers that were wrong as shipped, and they are recorded rather than quietly fixed:
    - §2 `bulk_only_cold_rc1.3` CONSERVATIVE upper: shipped `0.69588`, JSON `0.69597`.
    - §2 `bulk_only_cold_rc1.6` CONSERVATIVE bracket: shipped `[0.21651, 0.60034]`, JSON `[0.21652, 0.60037]`.
    - §2 `bulk_only_cold_rc1.9` CONSERVATIVE upper: shipped `0.53275`, JSON `0.53276`.
    - §7 D-5 net moment at `L=12`: shipped `4.284e-3`, JSON `4.238e-3` (a digit transposition).
    **★The lesson is the point of D-13:** these survived a full adversarial review, because a reviewer checks the numbers that carry the argument and these carried none. A machine checks all of them. **The absence of such a check is why F5 and F2 were possible at all** — both are, at bottom, "the prose is not what the artefact says."
15. **★D-15 — THE PR #802 RE-VERIFY REPAIRS (eight findings; two GATING on downstream use).** The re-verify audit reproduced both of this lane's MAJORs independently from primitives — F5's numbers exactly, F2's numbers exactly plus a cubic-identity probe this lane had not run — and returned CLEAR-WITH-REPAIRS. Each repair, named where it lands:
    - **★F3 (GATING) — a MIS-CITATION that also mis-identified the object** (§BOTTOM LINE, §8 consequence 7). The phrase *"verdict-controlling cross-class flip"* was attributed to #782 §5; it is at `:114`, which is §6, and there it denotes the **collapse-gate-driven `f_incl` BIN split**, which **survives the change of measure unchanged**. Retargeted to the `:12` `r_Z` object it actually describes; the `f_incl` flip is separately qualified as NOT touched.
    - **★F1 (GATING) — the axis-alignment evidence was a NON SEQUITUR** (§6.5). The SUBC `Σ̄` never sees `Phi`. Replaced by a direct uniaxial `C11` probe and a KUBC reaction-stress structure check, both of which do. **The conclusion was TRUE; the evidence was not evidence.**
    - **★F8 — this lane's own F5 was UNDERSTATED, in #782's favour** (§8 consequence 7). `:12` grounds trigger (iii) on two clauses; only one was addressed. The φ-scan clause is added, and the surviving straddle is named for what it is — a **boundary-condition artefact**, not a cross-class flip.
    - **F4 — a bound-ordering word applied to a ratio the frozen §2.1 says is not bound-ordered** (§BOTTOM LINE table) — the same conflation §4 correctly diagnoses in G1. Column and JSON key relabelled; the theorem-grade pair ADDED per leg.
    - **F5 — D-1b's leaf count was unauditable** (D-1). Withdrawn, evidence restated as re-runnable checks.
    - **F6 — an unanchored citation** ("the ρ-flags audit"). Anchored to `_orchestration/docket-entries/2026-07-21-relative-offset-principle.md:6` at both sites. Same defect class as the F11 repaired in the previous round.
    - **F7 — the number-check covered the result doc only** (D-13). Extended to the lane's docket fragments; their bodies corrected to match.
    - **the margin note** — the routed modulus consequence moved onto `C11_[100]`, where the `T1` clearance is `7.98 %` at the headline arm and stable across the box-size scan, rather than `M_[111]`, where it is `1.56 %` at that arm and `0.642 %` on the grown one.

    ★**AND ONE DEFECT THIS REPAIR FOUND IN ITS OWN SHIPPED ARTEFACT, recorded rather than quietly fixed.** The JSON string `F5_782_bound_robustness_crosscheck_NOT_FROZEN.WHAT_THIS_DOES_NOT_DO` asserted that the `two_sided_class_vs_T1` column *"STRADDLES at every leg on the two-sided bracket."* **The column it cites contradicts it:** it STRADDLES at `2` legs (`bulk_only_compressed`, `symmetric_cold`) and RESOLVES-HIGH at `2` (`bulk_only_cold`, `bulk_only_expanded`). Corrected in place with the correction stated inside the string, and the count is now shipped as its own leaf (`n_legs_two_sided_STRADDLE`) so the prose and the column cannot drift apart again. This is the same failure mode as D-14, one level up: a *sentence* was not what the artefact says. **No frozen criterion, frozen configuration, frozen gate outcome or frozen number changes anywhere in this round.**
16. **★D-16 — WHAT THE RE-VERIFY RE-RUN CHANGED IN THE ARTEFACT, leaf by leaf (this is the auditable half of D-1b's withdrawn figure).** The driver was re-run in full — twice, in separate processes writing separate `--out`, for frozen G3 — after the F1/F3/F4/F8 changes. Two comparisons, both by JSON path, both excluding the machine-dependent runtime leaves:
    - **against `0e8d4e67`, the FIRST JSON this lane ever shipped** (before *any* review repair): **`0` numeric leaves CHANGED in value, `0` REMOVED.** `3673` leaves then, `6253` now — every difference is an ADDITION.
    - **against `1b007cb2`, the pre-RE-VERIFY HEAD**: **`0` numeric leaves CHANGED in value**; `1108` added; **`5` REMOVED, and all `5` are the F4 RENAME**, not a deletion — `F5_….rows[*].r_Z_here_WHOLE_CELL_KUBC_the_actual_UPPER_bound` → `…_apparent_modulus` and `n_macro_side_BOUND_ROBUST_on_the_bound_carrying_WHOLE_CELL_KUBC` → `n_macro_side_on_the_WHOLE_CELL_KUBC_reading`. **Same values, corrected labels** — which is the whole content of F4.

    **So no frozen criterion, frozen configuration, frozen gate outcome, VOID status, headline bracket or frozen read moved by so much as a bit in this round.** The `determinism_digest` DID change (`7bcebdb2d83092dd…` → `37b5fc81eb537902…`) — as it must, since the digest covers the added blocks and the renamed keys. A changed digest across an additive repair is expected; a changed *value* would not be, and there are none.

---

## §8 — Calibration-vs-derived ledger (`consistency-vs-emergence`, frozen tags) + routed consequence

`R_SUBC`, `R_KUBC`, `R_KUBC_core`, `R_SUBC_core`, `R_lo`, `R_hi`, `g_0`, the bracket widths and the `G_eff` counterparts are `[derived]` dimensionless RATIOS (lattice static homogenization under two boundary conditions) — **CONSISTENCY-class, not emergence**: they test whether an already-banked lattice number is boundary-condition-robust, which is an internal-consistency question, and **no new physical constant is produced**. `ρ_eff/ρ_0 ≡ 1` is `[assumption]`, so every `r_Z` interval is **MIXED-provenance** — `[derived]` in `K`, `[assumption]` in `ρ` — and must be cited as such downstream. The srs bond model `ρ* = 9.77337` is `[import]` (`ν_Hill = 2/7`, GR-imported `K = 2G`, `ave.core.constants.N_NU`). The probe amplitudes (`ε = 1e-3`, `σ = 1`) and solver tolerances are `[engineering-choice]`, disclosed, and shown to cancel (G8) or be converged (G4). The Hill/Huet apparent-modulus ordering is a `[derived]` textbook theorem — the FORM the extraction is built on, not a fit. **`α`-CLEAN** (no `α`, no `Q_TANK`; every value is a dimensionless ratio). **No emergence-class claim headlined. No `clm-`/`def-` minted. No KB/tex leaf, port-register, falsification-ledger or matrix edit — regardless of outcome.**

**Consequences ROUTED to Grant / the auditor lane (this lane surfaces and routes only):**

1. **★The frozen-criterion defects (§3, §4, §4.1, §7 D-5)** — `SELFTEST-G2b` cannot fire as written; `G1` fires on `8` non-errors; the shipped load set does not satisfy frozen §1.1's moment-free design property; and **THREE** frozen sentences are ambiguous, not two: the G2b latitude, the G1 VOID scope, and (newly surfaced, §4.1) **which of the two frozen bracket definitions §5.5's word "a bracket" denotes** — on `14` rows including both headline arms, items 1 and 3 fire together and the frozen text does not adjudicate. Grant/the auditor own whether the bench is VOID, whether G1's ordering clause should be restated on the CONSERVATIVE pair where it IS a genuine theorem, and how all three ambiguities resolve. **This lane does not amend the frozen prereg and does not pick.**
2. **★The modulus-identity question (§6.1) — now SHARPENED into a THREE-way question by the anisotropy measurement (§6.5), not a two-way one.** The corpus `r_Z` is built on bulk `K`. A normal-incidence impedance carries a LONGITUDINAL modulus — but **the medium is CUBIC (Zener `A = 1.605316` KUBC / `1.330402` SUBC on the cold reference), so "the longitudinal modulus" is DIRECTION-DEPENDENT and is not a single number.** ★**Protocol E launches along `[100]`, so a Protocol-E time-of-flight comparator is `C11 = K + 4C′/3` — NOT the `K + 4C44/3` axis this lane previously mislabelled `C11`, and NOT a VRH isotropic average.** On the theorem-grade CONSERVATIVE bracket at the headline arm the three candidates disagree at the controlling bin edge: `K` `[0.46053, 0.71723]` **STRADDLES** `T1 = 0.5`, `M_[111]` `[0.50778, 0.86626]` **RESOLVES-HIGH**, `C11_[100]` `[0.53990, 0.83129]` **RESOLVES-HIGH**. ★**The routed consequence is stated on `C11_[100]`, because that is where the margin is real.** `C11_[100]` clears `T1` by `7.98 %` of the threshold at the `L = 16` headline arm and holds that clearance across the whole box-size scan (`8.11 %` at `L = 12`, `8.65 %` at `L = 20`); `M_[111]` clears it by only `1.56 %` at `L = 16`, `0.702 %` at `L = 12`, and `0.642 %` on the #796 grown arm — margins too thin to route a consequence on, and they move by a factor of `4` across the scan. So the `[100]` axis is both the physically-indicated comparator (Protocol E launches along `[100]`) and the only one whose RESOLVES-HIGH is stable. **A definition question, surfaced with data, sharpened rather than answered, and NOT settled here.**
3. **★The pinned-shell confound (§6.2)** — `51.3 %` of the soft shell is clamped affine under KUBC at the headline geometry, `0 %` under SUBC, strongly route-dependent. A live confound on the KUBC side of every number in this bench family. Reported raw; attribution routed.
4. **★The realized-vs-intensive `φ` labelling (§6.3)** — the size scan compares three different realized composites at one intensive label. Bears on the merged RVE-size gate; **the auditor's to assess, not this lane's to re-open.**
5. **OWED-2 stands entirely untouched.** A `K`-bracket does not dispose of the `ρ` question. Said again so it cannot be quietly counted as closed.
6. **The standing verdicts are NOT moved.** #782 BIN-4 and #796 UNDETERMINED stand exactly as merged. Frozen §5.5 item 3 (`If a bracket RESOLVES-HIGH at T1 on a headline arm: the matched-side reading becomes BOUND-ROBUST`) is **NOT invoked at any headline arm on either bracket definition** (JSON `verdict.frozen_5p5_item3_invocation_audit`, both headline lists empty). **The artefact does contain item-3 disposition strings on `20` of `42` non-headline / VOIDed / identity rows** — reconciled in §4.1 so doc and JSON cannot drift; none of them discharges any standing caveat, because item 3 is conditioned on a headline arm and item 4 governs every VOIDed row. On the `16` VOIDed rows only item 4 is available; elsewhere item 1 applies; **both say nothing moves, which is what is reported.**
7. **★#782'S STATED BASIS FOR BIN-4 IS CORRECTED — the consequence that cuts AGAINST the status quo (§BOTTOM LINE, JSON `F5_782_bound_robustness_crosscheck_NOT_FROZEN`).** #782 `:12` grounds trigger (iii) — the surviving trigger — on **TWO** clauses, and **BOTH** evaporate on the bound-carrying measure. The first version of this consequence addressed only the first (RE-VERIFY finding F8).

    - **Clause 1 — the pre-stress cross-class split.** Merged #782 §7.1 states `only compressed r_Z = 0.466 < 0.5 is bound-robust`, and `:12` carries it as `compressed r_Z=0.466` MACRO-side vs `cold 0.544` / `expanded 0.568` MATCHED-side. **That `0.466` is `√(R_KUBC_core)`, the core estimator, which frozen §2.3 says carries NO bound status.** This lane reproduces it bit-for-bit and measures the same configuration on the whole-cell modulus: **`0.6098352173422836` — above `0.5`.** Across #782's four `φ_sf` legs, `2` read macro-side on the core estimator and **`0` do on the whole-cell reading**, the core estimator sitting below it at every leg; on the theorem-grade pair all four STRADDLE.
    - **★Clause 2 — the φ scan** (`:12` verbatim: `AND across the φ scan (crash-min r_Z≈0.40 vs φ_sf r_Z=0.544)`). **#782's crash-min is `0.40486308946446631`**, at `bulk_only_cold` route B `r_cage = 1.7`, `s = 4.2` — **again the CORE estimator**. This lane runs that exact configuration (`routeB_bulk_only_cold_s4.2`), reproduces its core ratio **bit-for-bit**, and reads it on the whole-cell measure at **`0.71063915021141977`**. And across this lane's **entire `21`-configuration hydrostatic set** the **minimum** whole-cell KUBC `r_Z` is **`0.58503928606131161`** (`symmetric_cold_phi_sf`), with **`0`** configurations anywhere below `0.5`. **So the φ-scan clause supplies no macro-side crash-min either.**

    **⇒ BOTH legs of trigger (iii) evaporate on the bound-carrying measure, not one.** ★**And here is the distinction that is the actual substance of what BIN-4's basis becomes.** The `r_Z` straddle **survives** — but as a **BOUNDARY-CONDITION ARTEFACT, not as a cross-class physical flip.** It is restored at **`2` of the `4`** legs (`bulk_only_compressed`, `symmetric_cold`; the other two RESOLVE-HIGH on the two-sided bracket), and restored specifically by this lane's SUBC **LOWER** bound falling below `0.5` — i.e. by the free-surface softening of the traction boundary condition, **not** by any class reading macro-side on an upper bound. The honest restatement of the basis is therefore *"undetermined because the two admissible boundary conditions disagree"*, **not** *"undetermined because one pre-stress class flips to the macro side."*

    **What is NOT touched (RE-VERIFY finding F3).** #782 `:114` (§6, LEG 4 / VERDICT — **not** §5, which is LEG 3 and contains no such phrase) uses the words *"a verdict-controlling cross-class flip"* for a **different object**: the `f_incl` BIN-2-vs-BIN-4 split. That split is **collapse-gate-driven, not `r_Z`-driven** — `rve_aggregation_bench.py:831` `_bin_of` returns BIN-4 whenever the collapse gate fails, before `r_Z` is read — and **it persists unchanged here.** BIN-4's *label* survives on both counts. **This lane mints no bin, edits no merged doc, and does not re-bin #782** (frozen lane fence: `no edit to the merged #782 or #796 result docs regardless of outcome`). The **basis correction is ROUTED** — the second such correction to #782, after the Fork-ρ relative-offset walk's consequence flag (`_orchestration/docket-entries/2026-07-21-relative-offset-principle.md:6`).
8. **Number-check counts (§7 D-13 / D-14 / D-15), reported so an auditor can re-run rather than trust** — `python3 research/drivers/subc_kubc_bracket_number_check.py`, now scanning **three** documents (this result doc plus both lane docket fragments, RE-VERIFY finding F7): **`471` distinct numeric tokens** → **`367` auto-registered-and-verified** against a named shipped-JSON leaf at `≥ 3` significant digits, **`19` hand-PINNED-and-verified**, **`79` allow-listed constants** (frozen thresholds, frozen grid points, counts, Miller indices, line cites, this checker's own self-reported counts, the D-16 leaf-diff counts with their convention stated, the D-14 values quoted *as wrong*, the D-1b figure quoted *as withdrawn*, and four values quoted VERBATIM from the frozen prereg precisely because the shipped run does NOT reproduce them), **`6` sha/digest-shaped**, and **`0` UNACCOUNTED**. **PASS.** Earlier rounds: on its first run it found `4` retyped numbers (D-14); on its second it found a **false-provenance path in itself** (D-15) — a bare-integer count auto-matching an unrelated CG iteration count — now closed by refusing to auto-register bare integers at all.

> **Result-doc provenance.** The bench RUN of the frozen prereg `research/2026-07-28_subc-kubc-bracket_prereg-FROZEN.md` (committed and pushed ALONE at `1288e288`, **BYTE-UNTOUCHED** since — md5 `9e467d603d5745c1e05094506234e5d2`, verified against the `1288e288` blob before and after the PR #802 adversarial-review repairs, again before and after the PR #802 RE-VERIFY repairs, and across the mid-lane interruption disclosed in §7 D-1). Driver `research/drivers/subc_kubc_bracket.py`; JSON `research/drivers/subc_kubc_bracket_results.json`; white figure `research/drivers/subc_kubc_bracket.png`; number-check `research/drivers/subc_kubc_bracket_number_check.py`. Engine `src/ave` byte-untouched; Rule-14 reuse of merged #782 and merged #796, **neither of which this lane edits, regardless of outcome**; deterministic (G3 two-full-runs, separate processes and output paths, `diff -q` CLEAN, digest `37b5fc81eb537902…`; wall-clock deliberately not quoted — machine-dependent, §7 D-13). **G6 reproduces the merged corpus BIT-EXACTLY** (#796 control and painted at `rel = 0`), so the instrument is the shipped instrument. **BENCH STATUS = VOID AS FROZEN on two counts, both criterion-side, both ROUTED**; the bracket is computed and shipped in full but **NOT BANKED**, and no standing verdict is moved. **★SCOPE, repeated: this brackets `K` ONLY — every `r_Z` interval is a `K`-bracket around an ASSUMED `ρ ≡ 1`, the modulus is the BULK `K` and not any longitudinal modulus, and OWED-2 is NOT discharged.** **★ANISOTROPY, repeated: the medium is CUBIC — `M_*_abs` is the `[111]` longitudinal modulus, NOT `C11`; the `[100]` `C11` is measured separately in §6.5.** **★AND, repeated because it cuts against this lane's own status-quo headline: #782's sole bound-robust macro-side reading does NOT survive on the bound-carrying measure (§8 consequence 7).** Companions: the frozen prereg, merged **#782** (`research/2026-07-21_rve-aggregation-bench_result.md`), merged **#796** (`research/2026-07-22_vessel-state-rve_result.md`), and the docket fragments (`_orchestration/docket-entries/2026-07-28-subc-kubc-bracket.md`, `_orchestration/docket-entries/2026-07-28-subc-kubc-bracket-prereg.md`).
