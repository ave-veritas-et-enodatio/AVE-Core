# Ringdown-Systematics Organizers — FROZEN pre-registration (beyond-Kerr DEVIATION organizers from the BH soft-mode transition)

**Date:** 2026-07-20
**Lane:** RINGDOWN-SYSTEMATICS (`research/ringdown-systematics`). **FORWARD-PREDICTION class** (chord-surface work).
**Class:** DERIVATION + numeric-check research-doc. **Forms derived from the corpus soft-mode BH picture; every VALUE either substrate-derived from the canonical chain (ν_vac=2/7, the ringdown eigenvalue, the C_44 collapse) OR `[import]`-tagged (GR Kerr QNM reference values, current LIGO/Virgo constraint levels). Mints no `clm-`; propagates to no KB/tex leaf (canonical-propagation is a gated follow-on).** This is COMMIT 1 — the pre-registration ALONE, frozen and pushed before the derivation doc or the checks script (the #761 frozen-first discipline).
**Provenance:** Grant-fired 2026-07-20 (verbatim `[sic]`: `"word"`, ratifying the frontier queue with this candidate first). Registered candidate: `research/2026-07-20_vacuum-metallurgy_kz-relic-and-instruments.md` D4a (★CANDIDATE FORWARD CONTENT — mode-ratio locking + critical-slowing, surfaced not derived). Frontier-queue item #2 (`_orchestration/2026-07-20_pending-rulings-and-frontier-queue.md` §2, "queued next, orchestrator strong rec, #762 D4").
**Lane fences:** DERIVATION lane only. Engine `src/ave` **BYTE-UNTOUCHED** (the checks script imports nothing from `ave`; it is `math`-only and lives under `research/`). **No** `manuscript/` or `manuscript/ave-kb/` `.tex`/`.md` leaf edits; **no** matrix (`divergence-test-substrate-map.md`) edit; **no** KB propagation — regardless of outcome. Every `[canon]` input content-verified two-method (grep + direct read) at base HEAD `d3203d37` (verify-before-cite). GR Kerr reference + current-constraint figures `[import]`-tagged.

> **★ THE KERR-WORDING FENCE (rides from #762 D4a review; FROZEN, load-bearing).** GR quasinormal modes are **(M, a)-determined** — that is the no-hair theorem, and it is not in dispute here. Every claim in this lane is scoped as an organizer for **beyond-Kerr DEVIATION analyses** (the δf / δτ parametrized-deviation frameworks a LIGO/Virgo ringdown test already runs), **never** as a replacement for the Kerr spectrum. The deliverable is: *IF the ringdown deviates from Kerr, the substrate soft-mode picture predicts the deviation has THIS structure* (locked ratios, arrested critical slowing, rank-1). A null deviation (pure Kerr) is fully consistent with the substrate at the current sensitivity — the organizers only bite where a deviation is resolved. No claim of the form "AVE's ringdown replaces Kerr" is licensed by this document.

> **FREEZE STATEMENT.** This document freezes: (i) the REGIME/SECTOR/PHASE-STATE header + substrate walk (§0); (ii) the two organizers as dimensionless falsifiable statements with their FROZEN numbers + the rank-1 unification (§1); (iii) the four-bin classification + the per-organizer bin criteria (§2); (iv) the data-confrontation protocol + the anti-retrofit fence (§3); (v) the internal linear-ℓ vs √(ℓ(ℓ+1)) FORK guard and the UNDETERMINED handling (§4). Nothing below is a result — the derivation and the numeric checks land in COMMIT 2. Any verdict may cite ONLY the frozen criteria's outputs.

---

## §0 — REGIME / SECTOR / PHASE-STATE header + substrate-native walk (fired BEFORE any ringdown algebra)

**MODE.** A post-merger black-hole remnant **ringing down** — the fundamental (and sub-dominant) resonance of the newly-formed saturation cavity at `r_sat = 7GM/c² = 3.5 r_s`. Contrast column: the observed LIGO/Virgo ringdown modes (frequency ω_R, damping ω_I / decay time τ) and the beyond-Kerr deviation constraints δf / δτ.

**REGIME.** The far-field GW is **Regime I** (deeply linear, lossless-reactive transverse shear wave). The *cavity boundary* the mode rings off is a **Regime III↔IV soft-mode transition**: at `r_sat` the shear modulus `G_shear → 0` (topology melts), quantified on the srs Cauchy tensor as the `C_44` collapse `0.17661 → 0.02536 → 4×10⁻⁵` (`research/2026-07-04_saturated-elastic-tensor_result.md` §4 `[canon]`, PR#521). The ringdown is **shear-channel spectroscopy of a soft-mode structural transition**.

**PHASE-STATE.** **Cold-reactive far field** (Ax3 lossless-reactive; a radiating shear mode is a legal Ax3 port, not a valve). **Saturated soft boundary** (Op14 ON at `r_sat`; `G_shear → 0`, `Γ_shear = −1` perfect shear reflector; `Γ_EM = 0` so the horizon is black to light — the two-channel instrument, `electron-bh-isomorphism.md:24-47` `[canon]`).

**SUBSTRATE-NATIVE WALK (soft-mode toolkit, node-up).** A displacive/structural transition's textbook signature is a **soft mode** — a vibrational mode whose restoring modulus collapses to zero at the transition. The BH interior **is** this: the shear restoring force collapses at `r_sat`. Two facts of the collapse are load-bearing and drive the two organizers:
- **(A) The dimensionless RATIOS freeze.** The Born-Huang map `(k_a, k_s) ↦ C_ij` is homogeneous degree-1, so ν, Zener, K/G are homogeneous **degree-0** — unshifted by the saturation magnitude. Near yield the lattice is *"a soft region with locked proportions"* (`saturated-elastic-tensor_result.md` §4 + `electron-bh-isomorphism.md:38` `[canon]`). ⇒ **mode-RATIO locking** (ORG-1).
- **(B) The ABSOLUTE scale collapses.** The absolute moduli scale by the overall saturation factor `S`, so the shear-wave speed `c_shear = c₀√S ∝ √C_44` softens toward zero — the soft-mode ω-scaling — **but** the rigid `ν_vac=2/7` Cosserat skeleton fraction never softens (its share of the restoring force is `[canon]` the `x_sat(a*) → 2 + 5/3` floor, `ave-merger-ringdown-eigenvalue.md` v2 resultbox). ⇒ **ARRESTED critical slowing** (ORG-2).

**Consensus-knife (both ways).** GR posits Kerr QNMs as the (M,a) spectrum with no deeper structure; beyond-Kerr *phenomenology* (pyRing / ParSpec / δf-δτ) parametrizes deviations with **free per-mode** coefficients. The substrate soft-mode picture is a COMPETING, MORE-RESTRICTIVE hypothesis for the deviation structure (fewer free parameters → more falsifiable). The knife cuts both ways: the substrate side gets no pass for "the ratios lock" unless the lock is a genuine dimensionless prediction that a deviation analysis can reject; and the GR side is credited that Kerr-consistency is itself the strongly-favored current result.

---

## §1 — THE TWO ORGANIZERS (frozen dimensionless falsifiable statements)

### ORG-1 — MODE-RATIO LOCKING

**Derivation skeleton (full chain in COMMIT 2).** The AVE ringdown eigenvalue is `ω_R(ℓ) M_g = ℓ (1+ν_vac) / x_sat(a*)` (`ave-merger-ringdown-eigenvalue.md` resultbox `[canon]`). The factors `(1+ν_vac)` (frozen elastic ratio) and `x_sat(a*)` (cavity radius) are **common to all multipoles** — they carry no ℓ. Therefore the multipole frequency RATIO is set by the frozen ratios alone:

> **FROZEN ORG-1.** `ω_R(ℓ') / ω_R(ℓ) = ℓ'/ℓ` — **EXACTLY, and INDEPENDENT of spin a\*** (`x_sat` cancels). Damping locks too: `Q_ℓ = ℓ` (`qnm-quality-factor.md` `[canon]`) ⇒ `Q_{ℓ'}/Q_ℓ = ℓ'/ℓ`.

**FROZEN numbers (linear-ℓ reading, the corpus form `ω_R = ℓ·c/r_eff`, leaf line 16):**
- `ω_R(3)/ω_R(2) = 3/2 = 1.5000`
- `ω_R(4)/ω_R(2) = 4/2 = 2.0000`
- `ω_R(4)/ω_R(3) = 4/3 = 1.3333`

**FROZEN deviation signature (vs GR Schwarzschild, the a\*=0 anchor where both sides are verified):** Kerr `ω(3,0)/ω(2,0) = 1.604` `[import — Leaver]`, so AVE (linear-ℓ) sits at **−6.5%** below Kerr on the ℓ=3/ℓ=2 ratio. The **robust, fork-independent** ORG-1 statement (survives the §4 fork): the AVE multipole ratio is **BELOW the Kerr ratio AND spin-independent (frozen)**, whereas Kerr's ratio is a\*-dependent.

**Scope honesty (frozen).** The corpus derives + validates `ω_R ∝ ℓ` only at ℓ=2. Extending to ℓ≥3 is a forward reading of the corpus's own membrane-mode form (a corollary, tagged as such). The **overtone** ladder (n≥1) is **NOT** derived in the corpus — so overtone-ratio locking is FORM-ONLY (§2). This matters for the data confrontation: the best-constrained current test (GW150914 220/221 overtone) probes exactly the axis AVE does not predict; AVE's derived organizer is the multipole (higher-mode) axis.

### ORG-2 — ARRESTED CRITICAL SLOWING (ω ∝ √C_44, floored by the rigid skeleton)

**Derivation skeleton (full chain in COMMIT 2).** The soft-mode absolute scale rides `ω_R ∝ c_shear ∝ √S ∝ √C_44` (the mode softens as the modulus collapses). As `a* → extremal`, the prograde reflecting boundary `r_ph⁺` moves inward toward the yield wall where `C_44` is more collapsed — **but** the rigid `ν_vac=2/7` skeleton floors the cavity: `x_sat(a*) = 2 + 5·r_ph⁺(a*)/(3M) → 11/3` at extremal (never → 2, the pure photon sphere). So the soft mode does **not** fully soften — critical slowing is **arrested** by the frozen-ratio skeleton.

> **FROZEN ORG-2 (form + sign, robust).** Near-extremal, `ω_R` does NOT track the Kerr geometric softening — it is held above it by the skeleton floor. The AVE `ω_R(a*)` sits **above** Kerr for `a* ≳ 0.8`, growing with spin. **Extremal floor:** `ω_R M_g → 2(9/7)/(11/3) = 54/77 = 0.7013` (ℓ=2) — a hard ceiling the soft mode never softens below.

**FROZEN numbers (v2-EXTRAPOLATION — see the caveat):** the v2 spin curve gives spin-excess over the in-repo Berti (2,2,0) Kerr table of **+5.5% at a\*=0.90** and **+9.2% at a\*=0.95**.

**★ v2-EXTRAPOLATION CAVEAT (frozen, load-bearing honesty).** These excess magnitudes are the **v2 formula extrapolated into `a* ≥ 0.90`**, which the corpus itself flags as the **Option-B regime** (full spheroidal cavity, `ligo-ringdown-driver-design.md` §9 `[canon]`: "divergence onset a\* ≥ 0.90"). The **SIGN and the FLOOR (form)** are robust (they follow structurally from the rigid-skeleton fraction). The **exact excess magnitude is Option-B-revisable** — it is a v2 extrapolation, not a validated number. Frozen bin treats ORG-2's FORM+SIGN as derived-and-testable and its MAGNITUDE as extrapolation-pending-Option-B.

**Not-a-discriminator (consensus-knife, frozen).** Mass scaling `ω_R ∝ 1/M` is IDENTICAL in AVE and Kerr — mass is NOT where any deviation lives. Recorded so the confrontation does not miscredit a mass-scaling "match" as a discriminator.

### RANK-1 UNIFICATION (the two organizers are one deformation)

> **FROZEN RANK-1.** `ω_R(ℓ, a*) M_g = [ ℓ(1+ν_vac) ] × [ 1/x_sat(a*) ]` — frozen ratios (ORG-1) times a single overall scale `1/x_sat(a*)` common to every mode (ORG-2). ⇒ the AVE beyond-Kerr deviation across the whole multipole spectrum is **RANK-1** (single-parameter). **FALSIFIER:** a spectroscopy analysis resolving statistically-INDEPENDENT per-mode deviations (δf_220, δf_330 uncorrelated, not a common scale) rules out the soft-mode organizer.

---

## §2 — FOUR-BIN CLASSIFICATION + per-organizer bin criteria (frozen)

Every organizer lands in exactly one bin:

- **ORGANIZER-DERIVED-AND-TESTABLE** — a dimensionless statement follows from the canonical chain AND a current-or-future deviation analysis can reject it, with a stated sensitivity.
- **ALREADY-EXCLUDED** — current data already rejects the frozen statement at ≥2σ. (A bankable NEGATIVE — Rule 11 — reported as such, no retrofit.)
- **FORM-ONLY-NO-NUMBER** — the organizer is a real structural statement but the corpus supplies no derived number (e.g. the overtone ladder AVE never derived).
- **UNDETERMINED** — the frozen chain does not decide the statement (e.g. the §4 fork leaves the exact value open, though the robust statement may still land elsewhere).

**Frozen per-organizer criteria:**
| Organizer | Lands DERIVED-AND-TESTABLE iff | Lands ALREADY-EXCLUDED iff |
|---|---|---|
| ORG-1 multipole-ratio locking | the ℓ'/ℓ ratio + spin-independence is a dimensionless statement AND a higher-mode measurement of ω_330/ω_220 (or ω_440/ω_220) exists or is forecast at a sensitivity finer than the AVE−Kerr gap (≈6–12%) | a measured ω_330/ω_220 excludes BOTH forks (1.41 and 1.50) at ≥2σ, i.e. is pinned to the Kerr 1.60 to better than ~6% |
| ORG-1 overtone-ratio | — (never; no corpus number) → **FORM-ONLY** | n/a |
| ORG-2 arrested critical slowing (form+sign) | the floor/excess SIGN follows from the skeleton AND a near-extremal (a\*≳0.9) detection or a next-gen sensitivity forecast exists | a resolved a\*≳0.9 ringdown shows ω_220 AT or BELOW Kerr (no positive excess) at ≥2σ |
| ORG-2 excess MAGNITUDE | — (Option-B-revisable) → carried as extrapolation, not a frozen testable number | n/a (not frozen as a number) |
| RANK-1 unification | multi-mode spectroscopy can test deviation-correlation across modes | a multi-mode fit resolves independent per-mode deviations inconsistent with a common scale at ≥2σ |

---

## §3 — DATA-CONFRONTATION protocol + anti-retrofit fence (frozen)

**Protocol (frozen).** Confront each organizer against current published QNM/no-hair constraints, `[import]`-tagged, two-method where the number is load-bearing:
1. **GW150914 220/221 overtone** (Isi+2019 no-hair test) — the overtone axis. Expected: probes the axis AVE does NOT predict (ORG-1 overtone = FORM-ONLY); records the *sensitivity* (~10% postinspiral, ~20% first-overtone frequency `[import]`).
2. **Higher-mode / subdominant-mode events** (GW190521 and other loud/high-mass events with a resolved ℓ=3 content) — the multipole axis ORG-1 predicts. Expected: current ω_330/ω_220 precision (~tens of %) coarser than the 6–12% AVE−Kerr gap → CONSISTENT-UNTESTED.
3. **Population no-hair / ringdown deviation** (GWTC-3 TGR δf_220, δτ_220) — the overall-scale axis ORG-2 shares. Expected: population δf_220 at the few-%-to-~10% level, all events a\*<0.85 → ORG-2's near-extremal excess UNATTESTED → CONSISTENT-UNTESTED.

**★ ANTI-RETROFIT FENCE (frozen, Rule 11).** If a confrontation shows an organizer is **already excluded** by current data, that is a **bankable NEGATIVE** — it is recorded as ALREADY-EXCLUDED, the branch closes, and the mechanism is named. **No** re-reading of the organizer to slip under the bound; **no** post-hoc dropping of the dimensionless statement. The organizers were frozen (this doc) BEFORE the confrontation; the confrontation may only bin them, never redefine them. A "consistent" outcome is NOT a confirmation of AVE — it is "not yet falsified at current sensitivity"; the banked v2 ω_R/τ match is CONSISTENCY-class (`lattice-extreme-bh-rationality.md` §4 `[canon]`) and this lane does not upgrade it.

---

## §4 — INTERNAL FORK guard + UNDETERMINED handling (frozen)

**The linear-ℓ vs √(ℓ(ℓ+1)) fork.** The corpus writes the membrane mode as `ω_R = ℓ·c/r_eff` (LINEAR-ℓ) → ORG-1 ratio 1.500. The physical default for a mode on a 2-sphere would be `ω ∝ √(ℓ(ℓ+1))` → ratio 1.414. **This fork is OPEN** (the corpus asserts linear-ℓ; the spherical form is the standard-physics default). Frozen handling:
- The **exact** ORG-1 number (1.500 vs 1.414) is **UNDETERMINED** pending a substrate derivation of the surface-mode dispersion (flagged, not resolved — flag-don't-fix; surfaced to Grant/auditor).
- The **robust** ORG-1 statement — *ratio < Kerr AND spin-independent* — holds for **BOTH** forks (1.41 and 1.50 both < 1.60), so ORG-1's falsifiable content survives the fork and lands DERIVED-AND-TESTABLE on the robust statement; only the point-value is UNDETERMINED.

**UNDETERMINED discipline.** Where a frozen chain does not decide, the outcome is UNDETERMINED and is reported as such — never quietly filled with the more convenient fork. The point-value fork is the one UNDETERMINED slot; it does not poison the robust statement or ORG-2.

---

*Standing caveat: this is a frozen pre-registration. No result is asserted here. COMMIT 2 lands the derivation doc + the `math`-only checks script (`research/2026-07-20_ringdown-systematics_checks.py`) + the content-keyed docket entry; the verdict there may cite only the criteria frozen above. Kerr-wording fence rides every claim. Engine byte-untouched; no KB/matrix edit; mints no `clm-`.*
