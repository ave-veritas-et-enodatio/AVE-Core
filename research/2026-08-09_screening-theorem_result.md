# THE SCREENING THEOREM (R34's IOU) — RESULT

**Date:** 2026-08-09 · **Branch:** `lane/2026-08-09-screening-theorem` · **Base:** `origin/main` @ `a510ec2b`
**Frozen prereg:** [`2026-08-09_screening-theorem_prereg-FROZEN.md`](2026-08-09_screening-theorem_prereg-FROZEN.md), committed ALONE + pushed at `a1c8a200` before any derivation content, driver code, or lane-produced number existed (freeze-by-push).
**Driver:** [`research/drivers/screening_theorem.py`](drivers/screening_theorem.py) → [`screening_theorem_results.json`](drivers/screening_theorem_results.json) + [`screening_theorem_number_check.py`](drivers/screening_theorem_number_check.py) (`--mutation-receipt`; auto-discovered by `make verify`).
**Class:** DERIVATION + numeric two-method receipts. **Mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; edits no KB leaf, register, ledger, or ruling; changes no solidity; all propagation ROUTED.** Engine `src/ave` byte-untouched.
**Lane brief:** [`_orchestration/2026-08-08_screening-theorem-brief.md`](../_orchestration/2026-08-08_screening-theorem-brief.md) (Grant GO, R34).

---

## §0 — REGIME / SECTOR / PHASE-STATE header (restated at the point of reading; prereg SVA §0 governs)

**MODE** — a compact binary's compression-line sourcing question: does the mass present zero radiation resistance to the longitudinal line (R34's carve: sealed ≠ lossless — sealed is a SOURCE-COUPLING property). **REGIME** — exterior `r > r_sat`: Regime-I cold crystalline, sub-yield, lossless-reactive, static grade `A(r) = r_sat/r`. **INTERIOR `r < r_sat` = PAST-WALL PHASE STATE, declared at every point of use below: the crystal-phase laws do NOT extrapolate inside (Ax1 phase-scope). No derivation step in this document evaluates any constitutive law past the wall; every step lives on the cold side or at the last-bond boundary limit.** **PHASE-STATE** — cold-reactive; far-field radiation is a legal Ax3 port. **SECTOR** — interior hum = A1 dilatation (compression) content; observed GW = T2 shear at the banked Peters–Mathews rate (canon import, NOT re-derived); rotational (Cosserat) channel neither driven nor read — the fence-reach STOP was checked at every step and never crossed (§1.7). **STENCIL** — derivation adjudicates; the numeric arm corroborates (no engine run; quadrature + ODE integration on the canon closed forms).

---

## HEADLINE

> **VERDICT: `SEAL-DERIVED + RESIDUE-CLEARS` — carried with the NS-wall arm's per-star conditionality stated inside the verdict, as the frozen bin arithmetic requires.** The screening theorem is DERIVED at the last-bond plane: the ruled kernel-collapse mirror (v2 ruled text, quoted verbatim in prereg §3) applies to the outbound l=2 interior compression hum — every transport coupling the hum could ride (G, K both branches, G_c) disconnects at the last cold bond, so the interior presents **zero radiation resistance to the compression line: `Re(Z_source→bulk) = 0`**, exactly R34's carve, member-robust at the wall (§1.7). The seal's reach is TOTAL over interior content wherever the wall exists — the wall-depth knob is the wall-EXISTENCE condition `R_NS < r_sat` per star (§4), not a graded partial-transmission depth: the mirror is exact, so reach does not decay with depth; what varies star-to-star is whether there is a wall at all.
>
> **THE RESIDUE NUMBER (the brief's required deliverable):** the exterior configuration's honestly-derived compression flux is **`F_res(DP) = 6.064e-14` of the shear flux = `4.664e-10` × the double-pulsar bound** (`δ_DP = 1.3×10⁻⁴`, 95%) — **RESIDUE-CLEARS by `9.33` orders of magnitude**; for Hulse-Taylor, `F_res(HT) = 1.473e-14` = `9.208e-12` × `δ_HT = 0.0016` (`11.04` orders). The dominant channel is the interaction-energy (1PN-class) field moment, CH-3, whose moment ratio is not merely order-estimated but **MEASURED by an independent spatial quadrature: `(3/2)|κ| = 0.312498`** (the quadrature converging to the clean rational `5/16`; its own instrument check — the same mesh reproducing the denominator's exact closed value `4πk₁k₂/d` — holds to `1.37e-06`). The retardation channel CH-1 is `5.588e-14` and the graded-shell conversion channel CH-2 is `2.108e-19` (wall-localized, `(r_sat/a)²`-suppressed). The conservative envelope that drops the measured coefficient back to `1` gives `6.209e-13` = `4.776e-09` × DP — still clearing by `8.3` orders, so **no O(1)–O(10³) coefficient error can flip the bin.** The prior Branch-X estimate (`~2.5–40×` DP) is REPLACED as R34 requires: its compactness scaling conflated the star's OWN compactness (`GM/Rc² ~ 0.2`, a wall-scale property) with the moment-forming scale; the derived moments form at orbit scale where the correct small parameters are `β_P⁴` and `GM_tot/(ac²) = 4.347e-06` (§1.5).
>
> **FLAG-W DISPOSITION: RECONCILED — the repair LANDS (as a routed proposal; no leaf edited).** The three leaves disagree on the SIGN axis (`Z→0` pressure-release "vent" vs `Z→∞` rigid "jam"), which Grant's 2026-08-04 wall-taxonomy §10 ruling already carves as plane/projection/profile-relative; they AGREE on the ENERGY axis — `|Γ| = 1`, zero transmitted power, dead-end for flux — which is ρ-profile-INDEPENDENT per the ruled kernel-collapse receipts and is the ONLY axis the seal consumes. The "vent" reading survives only if some leaf asserts through-wall ENERGY transport on the compression line: the corpus sweep (two engines, §3.3) finds **none**. The vent reading does NOT survive reconciliation; the seal ruling STANDS (§3).
>
> **THE NS-WALL ARM'S ANSWER (the honesty fence, stated without softening):** the wall-existence condition is `R_NS < r_sat = 7GM/c²`. Per-star flip radii: HT pulsar `14.8637` km, HT companion `14.3676` km, DP-A `13.832` km, DP-B `12.9088` km, against the declared NS-radius import band `11.4–13.7` km (NICER-class, for OTHER pulsars — EOS-universality is an import; no radius measurement of the comparator stars exists). **HT-p, HT-c, and DP-A are walled across the entire band. DP-B's flip radius sits INSIDE the band: whether DP-B is walled is UNDECIDABLE at current radius-import precision.** If DP-B is sub-wall, its matter moment is un-sealed and re-sources `m_A/M = 0.517` of the orbital quadrupole, reverting that share of the #919 uncaged floor: `60.5`–`181`× the DP bound — the kill re-fires through DP-B alone (R24-contested anchor inherited). The verdict word above therefore banks UNCONDITIONALLY on Hulse-Taylor (both stars walled at every band radius; residue `9.208e-12` of budget) **and CONDITIONALLY on the double pulsar (walled-DP-B branch: residue clears at `4.664e-10` of bound; sub-wall-DP-B branch: kill re-fires)**. The discriminating physical quantity is DP-B's radius vs `12.91` km.

---

## §1 — METHOD 1: the derivation (adjudicating)

### §1.0 — THE SEAL, route (a): the impedance/Γ route (gate G-SEAL-2M arm a)

The v2 ruled text (prereg §3, byte-checked) rules: *"at any strain-saturation wall, every transport coupling riding the strain kernel — G, K (both fork branches), and the u↔φ coupling G_c — disconnects at the last bond: total reflection, phase computed from the branch-derived row at the declared plane, density-profile-independent."* Applied at the source's own boundary (R34's connect — no new axiom):

1. **The wall exists in vacuum, on the cold side.** For a compact object with `R < r_sat`, the vacuum grade `A(r) = r_sat/r` reaches yield at `r = r_sat`, OUTSIDE the surface. The last cold bond — the innermost lattice element still describable by crystal-phase laws — sits at the wall. Everything inside is past-wall phase state (declared; not evaluated).
2. **The disconnection is a cold-side property.** The strain-kernel couplings carried by the last bond vanish in the limit `A → 1⁻` along the graded approach: `G·S(A) → 0`, `K`-branch coupling → collapse on BOTH fork branches (the ruled text's *"both fork branches"*), `G_c·S(A) → 0`. This limit is computed entirely on the cold side — the receipts (#888, #890, Tier-2-verified, cited by the ruled text) computed it from the branch-derived indicial row with NO reference to the interior state. **Therefore the conclusion needs no interior extrapolation:** whatever the past-wall interior is or does, its ONLY strain-kernel connection to the cold line is an element whose coupling is zero.
3. **Zero coupling ⇒ zero radiation resistance.** The interior hum drives the cold line only through the last-bond coupling `k_LB`. The work per cycle the interior can do on the cold line is `W ∝ k_LB · (cold-side response amplitude)`. The cold-side amplitude at the last bond is capped at yield BY DEFINITION of the wall (a cold bond carrying super-yield strain is past-wall, and the level set relocates — the cap is definitional, not constitutive). Bounded amplitude × vanishing coupling ⇒ `W → 0` ⇒ **`Re(Z_source→bulk) = 0` for interior-generated content: the compression response of the sealed source is purely reactive.** This is the screening theorem at the last-bond plane.
4. **Does `Γ_LB = −1` "apply to the outbound l=2 interior hum"?** The honest form: the SIGNED value `−1` is a plane/projection/profile-relative statement (wall-taxonomy §10; the vent leaf's foot declaration) and this lane does not consume the sign. What applies to the outbound hum — and all the seal needs — is the sign-INVARIANT content of the ruled row: **`|Γ| = 1` at the last bond, total reflection, zero transmitted flux, ρ-profile-independent.** The l=2 angular structure changes nothing: the disconnection is per-bond (local), so it holds for every angular harmonic of the interior drive; no l-dependent transmission channel exists on the strain kernel.
5. **Reach (the wall-depth dependence, DERIVED):** the mirror is exact (`|Γ| = 1`, not `|Γ| < 1` graded), so the seal's reach over interior content does not decay with wall depth — it is TOTAL wherever the wall exists. The derived depth-knob is therefore binary per star: `R < r_sat` (sealed) vs `R > r_sat` (no wall, no seal). This is the NS-wall arm, §4. What remains outside the seal on the walled branch is exterior content only — the residue, §1.5.

### §1.1 — THE SEAL, route (b): the energy-flux route (gate G-SEAL-2M arm b)

Independent bound, no Γ language: the power crossing any cold-side sphere at grade `A = 1 − δ` is carried by the graded line's transport there, `P(δ) ≤ k(δ) · |u̇| · |Δu|`, with `k(δ) ∝ S(A) = √(2δ − δ²) → 0` and both field factors bounded (sub-yield by construction outside; yield-capped at the last bond per §1.0-3). Hence `P(δ) → 0` as `δ → 0`: **the transmitted energy flux through the wall is zero for any bounded interior boundary drive.** The two routes agree: G-SEAL-2M PASS. *(The one configuration this bound does not cover — an interior boundary drive with UNBOUNDED displacement amplitude at the skin — is not derivable (past-wall), not physical on the cold side (the yield cap), and not needed: the comparator stars' hum amplitudes are whatever they are; the seal statement is structural, per-amplitude-linear, and survives any finite amplitude.)*

### §1.2 — G-PISTON: the moving wall (the pre-flagged live risk, scored)

The wall ORBITS. A moving `|Γ| = 1` boundary is a candidate pump — the piston term. Two danger channels, each closed by a named canon receipt, plus the surviving content, located:

- **Displaced-volume dipole (the acoustic-piston analogy): CLOSED — pattern, not object.** The star + wall + dress is a strain PATTERN of the lattice, not a body moving THROUGH it (Ax1/TKI; the level set `A = 1` sweeps over sites; no lattice material flows around an obstruction). The classic moving-rigid-sphere acoustic dipole requires displaced medium; nothing is displaced. No dipole term exists — consistent with the receipted dipole kill (`Ẍ_cm = 0`, #919 §2, inherited fork-independently).
- **Hysteretic pumping at the swept skin: CLOSED — anhysteretic kernel.** Sites swept by the moving wall load to yield and unload behind it. The canonical kernel `S(A) = √(1−A²)` is **anhysteretic — zero enclosed loop area, no remanence** (engine-capability-map §3-3, canon). The load-unload cycle is reversible: zero net work per cycle. For a circular orbit the entire exterior configuration is STEADY in the co-rotating frame; a co-rotating-steady pattern of lossless (Ax3) reactive elements pumps no net energy — exactly zero, not small. For `e ≠ 0` the pattern breathes but remains reversible; the eccentricity enhancement enters through the moments (below), not through hysteresis.
- **What survives: the pattern's far-field-propagating content.** The only radiative content of the moving wall+dress is the retarded far-zone part of the time-varying pattern — which is the SAME object as the dress-moment accounting of §1.5 (the piston and the dress are one configuration, counted once). **G-PISTON: PASS — derived-as-absorbed**, with both danger channels closed by receipt, not assumption.

### §1.3 — G-SHEAR-COHERENT: the sourcing map (the proves-too-much check)

The SAME ruled text mirrors the T2 shear channel (`G` disconnects too), yet the observed GW radiates at the full banked Peters–Mathews rate (`0.9983 ± 0.0016` of GR for HT). A seal argument that silenced observed GW would prove too much. The sourcing map that resolves it, applied identically to BOTH channels: **the radiating moment of a compact binary is carried by the EXTERIOR configuration — the orbiting sub-yield dress — not by interior hum transmitted through a wall.** Each star's exterior strain profile IS its far-field signature; when the stars orbit, the pair's exterior configuration is time-varying at orbit scale (`a ≫ r_sat`, entirely on the cold side), and the far zone radiates from THAT. Canon's R1 import anchors the shear magnitude: the exterior configuration radiates shear with effective moment `μa²` at the PM rate (consistency-import, #919 R1 — not re-derived, not contradicted). The seal silences only the INTERIOR-sourcing pathway — which the observed shear never needed. Applied to the compression channel, the same map says: the compression radiation is whatever compression content the EXTERIOR configuration carries — and that is the residue, §1.5. **The channel asymmetry is now exact and honest: the exterior dress is shear-rich (it carries the full `μa²` shear moment) and compression-poor (its compression content is derived below to be `O(β²)` and `O(A²)`-suppressed) — the two channels differ by the CONTENT of the exterior configuration, not by an asymmetric application of the mirror.** G-SHEAR-COHERENT: PASS.

### §1.4 — G-DIV: the divergence of the graded static exterior (the residue's leading-order gate)

The receipted claim (#919 §3.3): the exterior dress is the `∇·u = 0` Lamé exterior. Verified two-method: (i) corpus grep to the receipted home (#919 §3.3 RESTORED leg; two engines, §3.3 receipts table); (ii) re-derived here WITH the grade carried — the honest form, since a graded medium does not admit the homogeneous solution exactly:

- **Homogeneous limit:** the spherically-symmetric exterior response `u₀ = B/r²` has `θ ≡ ∇·u = u₀' + 2u₀/r = 0` exactly — pure deviatoric strain. *(Two-variable split, carried from #919: this dynamical displacement dress `u` is a DIFFERENT object from the operating-point bias grade `A(r) = r_sat/r`. The bias is DC — it grades moduli; it is not itself propagating compression content, and a sub-phase-speed (`β ~ 2×10⁻³`) lossless parametric pattern radiates nothing directly.)*
- **Graded correction, derived:** with moduli graded by `S(A(r))` (`G = G₀S`; K on BOTH FLAG-W branches: `K₀S` soft / `K₀/S` stiff), static equilibrium `∇·σ = 0` applied to `u₀` leaves a residual `−4BG'(r)/r³`, which forces an induced volumetric response: `(K + 4G/3)·dθ/dr = 4BG'(r)/r³`. Since `G' ∝ d(S)/dr` with `S ≈ 1 − A²/2`, the induced divergence is **wall-localized and `O(A²)`-suppressed**: closed form `θ/ε_dev ~ O(1)·(r_sat/r)²`, numerically integrated (driver, both branches): `θ/ε_dev` at `r = 2r_sat` = `-0.0734` (soft) / `-0.0647` (stiff) — same class, branch-ROBUST (the FLAG-W fork moves the coefficient, not the order; the residue verdict is fork-independent). At orbit scale (`r = a`, `a/r_sat = 65730.1` for DP) the dress divergence is `2.31e-10` relative — the dress is div-free where the moments form. **G-DIV: PASS** (the receipt survives the graded re-derivation, with its correction derived, bounded, and branch-robust).

### §1.5 — THE RESIDUE: the channel register (every candidate leading-order route adjudicated; #927's coupling consumed)

The far-field compression flux is set by the exterior configuration's compression-source moments; #927 (MERGED) supplies the coupling structure — the propagating common mode ACCEPTS the rotating l=2 pattern (`O ∝ j₂'(kR)`, no lattice projection-out, no eccentricity escape: DP is fully driven at `5.2%` from the circular limit) — so NO structural zero is available and the residue is a genuine number. Channels (each with its derived small parameter; flux ratios relative to the banked shear flux; speed/angular factor `(2/3)(c_S/c_P)⁵ = 0.03286` carried from the #919 floor structure as the channel conversion factor):

| Channel | Physical content | Moment suppression (derived) | Flux (DP) |
|---|---|---|---|
| **CH-1** retardation/kinematic mismatch | a rigidly-translating or rigidly-rotating div-free pattern has EXACTLY zero compression content (`∇·[(ẋ·∇)u] = (ẋ·∇)(∇·u) = 0`; rotation is an isometry) — the compression source is the deviation of the true retarded field from the rigid pattern | `O(β_P²)` on the moment ⇒ `β_P⁴` on flux | `5.588e-14` |
| **CH-2** graded-shell conversion (the "graded matching section's leakage") | the moduli-gradient shell around each wall converts orbital-frequency shear content into compression (`∇G`-coupling on `u_S`); the §1.4 θ-response IS this object; moment forms at wall scale, orbit-frequency content there `∝ (r_sat/a)`-suppressed | `~2·(r_sat/a)²·ln(a/r_sat)`-class on the moment (numeric `1.266e-09` vs closed `1.272e-09`, agreement `0.44%`) | `2.108e-19` |
| **CH-3** interaction-energy moment | the pair's A1-sector field energy carries a time-varying, rigidly co-rotating second moment — the 1PN-class field moment | `(3/2)|κ|·GM_tot/(ac²)` on the moment, coefficient MEASURED (§1.5.1), = `1.3584e-06` (DP) | `6.064e-14` |

**★ A CROSS-RECEIPT I CLAIMED AND THEN KILLED WITH MY OWN INSTRUMENT (recorded because it is the argument for running the driver).** The first draft of this section claimed CH-1 and CH-3 corroborate each other "via the virial identity `β² = GM/(ac²)`, confirmed to 3 digits." The driver returns them equal to **14** digits — because `v = 2πa/P` and `GM = 4π²a³/P²` make the identity EXACT BY CONSTRUCTION in the ephemeris arithmetic. **It is a tautology of the input reduction, not an independent physical cross-check, and it is WITHDRAWN.** It is replaced by §1.5.1's genuine independent quadrature. (The `β²`-vs-`GM/ac²` equality remains true and remains the reason CH-1 and CH-3 sit at the same PN order — it just cannot be cited as a *receipt*.)

### §1.5.1 — CH-3's second method: the field-moment quadrature (the real two-method receipt)

Method (a), scaling: the compression-sourcing moment is the A1-sector interaction field energy's `l=2` moment, of order `GM_tot/(ac²)` relative to the matter quadrupole, coefficient `O(1)` unspecified. Method (b), **independent spatial quadrature** — a different object computed a different way: with the two graded dresses superposed, integrate the interaction energy density's traceless `zz` moment over all space and NORMALIZE it by that same density's own volume integral,
$$\kappa \;=\; \frac{\int (\nabla A_1\!\cdot\!\nabla A_2)\,(z^2 - r^2/3)\,d^3x}{d^2 \int (\nabla A_1\!\cdot\!\nabla A_2)\,d^3x},\qquad \frac{Q_{field}}{c^2\,(2/3)\mu d^2} \;=\; \tfrac{3}{2}|\kappa|\,\frac{GM_{tot}}{d c^2}.$$
The normalization is the point: **every field-energy prefactor cancels** — the `/7` chain, the `4π` conventions, the whole `(c⁴/14G)` coefficient — so the measured number imports only what canon already commits to (the weak-field interaction energy has its Newtonian magnitude). Geometry: masses at `z = ±d/2`; the integrand is exactly symmetric under `z → −z`, so the integral is twice the `z > 0` half evaluated in spherical coordinates centred ON mass 1, where the `1/r₁²` singularity is cancelled exactly by the `r₁²` Jacobian and the other mass lies outside the domain — no excision, no RNG. **Measured: `κ = 0.20833183`, i.e. `(3/2)|κ| = 0.312498`** — converging to the clean rational `5/16` (identification stated as a numerically-indicated OBSERVATION, not an analytic proof). **Instrument certification, and it is what makes this a receipt rather than a second guess: the denominator of the same quadrature has the exact closed value `4πk₁k₂/d`, and the mesh reproduces it to `1.37e-06` relative.** *(One defect found and fixed in this instrument, disclosed: the first implementation put the angular integral OUTSIDE and the radial inside, which aliases the half-space cut `μ > −d/2r₁` — a boundary layer that narrows with radius — and produced a `κ` that drifted `0.2393 → 0.3542` with the truncation radius instead of converging. Swapping the loop order resolves the layer exactly at every radius; `κ` then converges monotonically over four mesh refinements and three decades of truncation radius.)* **Agreement of the two methods: the scaling said `O(1)`, the quadrature measures `0.3125` — inside the frozen ×3. G-RESIDUE-2M PASS on the dominant channel.**

**No-missing-channel argument (the D-register pattern):** any compression flux must enter through time-varying compression content of the exterior configuration (the interior is sealed, §1.0–§1.1; the piston is absorbed, §1.2). The exterior content decomposes exactly into: the dress's own divergence (G-DIV: `O(A²)` wall-localized ⇒ CH-2), the kinematic mismatch of its motion (CH-1), and the field-energy sector (CH-3). A fourth route would need either interior transmission (sealed), a rotational-channel bypass (fenced — §1.7; not the compression line), or nonlinear self-conversion (sub-yield `O(u²)`, below CH-1 at these amplitudes). **The result:**

$$F_{res}(\mathrm{DP}) = \max_i \mathrm{CH}_i = 6.064\times10^{-14}\ \text{of the shear flux} = 4.664\times10^{-10}\times\delta_{DP};\qquad F_{res}(\mathrm{HT}) = 1.473\times10^{-14} = 9.208\times10^{-12}\times\delta_{HT}.$$

**RESIDUE-CLEARS on both comparators — `9.33` orders (DP) and `11.04` orders (HT) of margin.** The channels are within `9%` of each other (CH-1 `5.588e-14` vs CH-3 `6.064e-14`), so the max-vs-coherent-sum choice is an `O(1)` question that the margin renders irrelevant; the conservative coefficient-1 envelope is `6.209e-13` (DP) = `4.776e-09` × the bound. Why Branch-X was high: its `~O(GM/Rc²) ~ 0.2` compactness scaling used the star's OWN wall-scale compactness where the derivation gives orbit-scale parameters (`GM_tot/(ac²)`, `β_P²` — both `~10⁻⁶`); the estimate conflated the dress's ENERGY fraction with its MOMENT fraction. The honest derivation replaces, as R34 requires; the amplitude inheritance (R23 NOT-DERIVABLE) is respected — every channel above is a RATIO to the observed shear flux (the R1 consistency-import anchor, tagged; no absolute pluck amplitude is used anywhere).

### §1.6 — Seal reach assembled (Deliverable 1's answer, in one paragraph)

The radiating l=2 compression moment of a walled compact binary decomposes as: **interior moment — SEALED (zero, exactly, by the last-bond disconnection; §1.0–§1.1); wall-motion (piston) moment — ABSORBED into the exterior-pattern accounting with zero hysteretic content (§1.2); exterior-dress moment — the residue, `O(β²) + O((r_sat/a)²ln) + O(GM/ac²)`-suppressed (§1.5).** The radiating moment is therefore exterior-dress-dominated in LOCATION but suppressed in MAGNITUDE — the seal reaches the entire interior moment, and the wall-depth knob is binary (wall exists / doesn't), resolved per star by the NS-wall arm (§4).

### §1.7 — G-MEMBER: the member-fence declaration (every seal-consuming step)

The seal's basis — the STRAIN-kernel collapse (G, K, G_c) — holds on BOTH combine members: under per-grade (L∞) the strain grades collapse and the rotational grade is carved out; under normalized-L2 EVERY grade (rotational included) rides the shared kernel and collapses. **The seal is member-ROBUST at the wall.** The only member-CONDITIONAL object is the rotational carve-out itself, which this lane touches only as a candidate BYPASS (interior content → rotational transport → cross unwalled → convert back): AT the wall the bypass is closed on both members (per-grade: *"the door closes with the strain kernel"* — the ruled text; normalized-L2: the rotational channel is itself walled); its only residue is graded-approach conversion (`G_c·S_ε` finite — the computed approach-leak class, #903/#904), which lands in the compression line only through CH-2's machinery and is bounded by it. Per C2 (#907): **no engine-numerical receipt is cited for the carve-out anywhere in this lane** — the carve-out's receipt is structural, and this lane's use of it is confined to the bypass-closure argument above. The rotational fence-reach STOP was checked at §1.2, §1.5, §1.7: no derivation step routes seal-escape or residue flux through the micro-rotation channel. Grep receipt (two engines): every carve-out mention in this document sits within its fence sentence; zero unfenced consumptions. **G-MEMBER: PASS.**

---

## §2 — METHOD 2: the driver receipts (named engines; deterministic; no RNG)

Engines (named): `scipy.integrate.solve_ivp` (RK45, rtol `1e-10`) for the graded-equilibrium θ-ODE (both FLAG-W branches); `scipy.integrate.quad` for the moment integrals; closed-form Kepler kinematics from the frozen masses + periods; constants: IAU nominal `GM_sun/c² = 1.476625` km, `c = 299792.458` km/s (named in the driver header; `ave.core.constants` not needed — no substrate constant enters the comparator arithmetic).

### §2.0 — Deviations from the frozen prereg (disclosed FIRST)

1. **A CLAIMED RECEIPT WITHDRAWN, AND THE REAL ONE BUILT TO REPLACE IT (the honest disclosure, first).** The draft claimed the two-method requirement on CH-1/CH-3 was carried by a "virial cross-identity, agreeing to 3 digits." The driver returned agreement to **14** digits, exposing it as EXACT BY CONSTRUCTION of the ephemeris reduction — a tautology, not a receipt. It is withdrawn (§1.5) and replaced by the independent field-moment quadrature of §1.5.1, which was BUILT for this gate. Direction of effect: the honest method LOWERED the derived residue by `10.2×` (measured coefficient `0.3125` vs the naive `1`), i.e. the withdrawn tautology had been sitting on a CONSERVATIVE number; both are reported, and both clear.
2. **The G-RESIDUE-2M two-method status, scored per channel rather than in aggregate** (the frozen text says "the residue", and the residue is a max over channels): CH-3 (dominant) — two genuine methods, scaling vs quadrature, `O(1)` vs `0.3125`, inside the frozen ×3, PASS. CH-2 — closed `1.272e-09` vs numeric `1.266e-09`, `0.44%`, PASS. **CH-1 — ONE method only (a `β_P⁴` scaling bound; no independent second evaluation was built).** It is sub-dominant and lands within `9%` of the two-method channel, so it changes no bin; but it is NOT two-method'd and is not represented as such. Direction: none on the verdict; disclosed so the gate row is read at its true coverage.
3. **Instrument defect found and repaired inside the quadrature** (loop-order/boundary-layer aliasing) — disclosed with its symptom and fix in §1.5.1; the defective and repaired behaviours are both stated. Direction: the defect INFLATED `κ` (drifting up to `0.354` vs the converged `0.2083`), i.e. the repair lowered the residue further.
4. **Dated erratum on the FROZEN prereg (the prereg body is NOT edited; this note is the Rule-12 repair):** prereg §5 cites the JAM leaf at `saturating-modulus-and-backreaction.md:57`, inherited from the coldq FLAG-W entry. The sentence sits at `:59` at base `a510ec2b` — pure line-shift, content byte-identical, correct-when-written. Locator-only; gate-neutral (G-QUOTE re-verified the sentence itself at result time). The same drifted locator sits in the coldq result doc and is included in the §3.4 routed repair.

### §2.1 — NS-wall table (gate G-NSWALL)

| Star | Mass (M_sun, frozen §4 prereg) | `r_sat = 7GM/c²` (km) = flip radius | Walled at band-low `11.4`? | Walled at band-high `13.7`? | Straddled? |
|---|---|---|---|---|---|
| HT pulsar | `1.438` | `14.8637` | YES | YES | no |
| HT companion | `1.390` | `14.3676` | YES | YES | no |
| DP-A | `1.338185` | `13.832` | YES | YES (margin `0.13` km) | no |
| DP-B | `1.248868` | `12.9088` | YES | **NO** | **YES — undecidable at import precision** |

Sub-wall-DP-B reversion arithmetic (driver): moment share `0.5173`; reverted floor `0.517 × [0.0152, 0.0455]` = `0.0079`–`0.0235` = **`60.5`–`181`× the DP bound**.

### §2.2 — Kinematics + residue channels (gates G-RESIDUE-2M, G-COMP)

| Quantity | DP | HT |
|---|---|---|
| `a` (km) | `878836.667` | `1949032.097` |
| `β = v_rel/c` | `2.085e-03` | `1.464e-03` |
| `GM_tot/(ac²)` | `4.347e-06` | `2.142e-06` |
| CH-3 moment ratio (measured coefficient) | `1.3584e-06` | `6.6954e-07` |
| CH-1 flux (one-method scaling bound) | `5.588e-14` | `1.358e-14` |
| CH-2 flux | `2.108e-19` | `1.409e-20` |
| CH-3 flux (dominant; two-method) | `6.064e-14` | `1.473e-14` |
| **`F_res`** | **`6.064e-14`** | **`1.473e-14`** |
| **`F_res/δ`** | **`4.664e-10`** | **`9.208e-12`** |
| conservative envelope `F_res/δ` (coefficient → 1) | `4.776e-09` | `9.429e-11` |

θ-receipt (G-DIV, both branches): `θ/ε_dev(2r_sat)` = `-0.0734` (soft) / `-0.0647` (stiff); CH-2 closed `1.272e-09` vs numeric `1.266e-09`, `0.44%`.
Quadrature receipt (G-RESIDUE-2M, CH-3): `κ = 0.20833183`; `(3/2)|κ| = 0.312498`; denominator-vs-exact `1.37e-06`; mesh `500` × `1600`, truncation radius `1e5` × d.
**Determinism:** two full runs, shipped JSON byte-identical; digest `b168e7dc315e06d2b030b8599bdcedd43d6e5a3f6d0ae1f416d098667369c65f`.

---

## §3 — FLAG-W RECONCILIATION (Deliverable 3; gate G-FLAGW-3)

### §3.1 — The three leaves, enumerated verbatim (byte-checked per G-QUOTE)

1. **The VENT leaf** — `manuscript/ave-kb/vol3/cosmology/ch15-black-hole-orbitals/bulk-impedance-at-saturation-boundary.md:30-32`, verbatim `[sic]`: *"- $G_{shear} \to 0$ (shear modulus vanishes) / - $c_{bulk} \to 0$ (bulk dilatational speed vanishes at snap / rupture) / - Therefore $Z_{bulk} = \rho_{bulk}\,c_{bulk} \to 0$"*, with the Resultbox at `:39`: `Γ_bulk → −1` as `Z_bulk → 0`, and `:42`: *"The saturated interior is a **bulk-longitudinal perfect reflector** — the sonic-horizon / pressure-release boundary ($p = 0$ at the wall)"*.
2. **The JAM leaf** — `manuscript/ave-kb/vol3/gravity/ch02-general-relativity/saturating-modulus-and-backreaction.md:59`, verbatim `[sic]`: *"**BULK stiffens:** $D=1/S\to\infty$ at $A\to1$ (the modulus goes rigid, halting the collapse)."* **★ CITE-DRIFT FOUND BY G-QUOTE, banked rather than silently followed:** the FLAG-W entry that routed this lane (`coldq-polar-family_result.md:19`) cites this sentence at **`:57`**, and the frozen prereg of THIS lane (§5) inherited that locator. At base `a510ec2b` the sentence is at **`:59`** — a pure line-shift (the content is byte-identical and was correct when written). The `:59` locator is the operative one; the frozen prereg's `:57` is carried as a dated erratum (§2.0-4), its body byte-untouched per Rule 12.
3. **The FIREWALL leaf** — `manuscript/ave-kb/common/engine-capability-map.md:69` ("Underlying firewall" paragraph), verbatim `[sic]` (closing sentence): *"The electron cage is the **stiffening** wall; the softening pocket is a distinct (candidate) object. Conflating them is the firewall violation."*

### §3.2 — The reconciliation (two axes, carved)

The contradiction dissolves once the two AXES of the question are separated — and the separation is already Grant-ruled: wall-taxonomy **§10** (2026-08-04, *"follows PR #869's FLAG-W"*) rules that a signed `Γ` requires plane + projection + profile declarations, that `Z→0` (short, `Γ=−1`) and `Z→∞` (open, `Γ=+1`) are the SAME cutoff read on different projections/profiles (its own *"FLAG-W dissolution"* paragraph: RHO-A gives `Z_bulk→0`; `ρ_eff = ρ₀/S³` gives `Z_bulk→∞` — *"plane/projection/profile-relative statements, **not a contradiction**"*), and that Ax3 forces only `|Γ| = 1`.

- **SIGN axis ("vent" = pressure-release short vs "jam" = rigid open):** profile-relative, computed-not-chosen, UNRESOLVED in canon (FORK-3(b) open) — **and NOT consumed by the seal.**
- **ENERGY axis (does compression flux cross the wall?):** `|Γ| = 1` — total reflection, zero transmitted power — on EVERY profile branch, per the ruled kernel-collapse receipts (*"density-profile-independent"*). A pressure-release boundary (`Γ = −1`) transmits NOTHING: the vent leaf's own `:42` calls the interior a *"perfect reflector"*. **"Vent" was always a SIGN word, not an energy word — the leaf that coined the coldq reading (`coldq-polar-family_result.md:19`: "the bulk line vents") glossed `c_bulk → 0, Γ_bulk = −1` as venting, but its OWN cited content is a total-reflection row.**
- **The FIREWALL leaf is not a third answer** — it fences the electron's stiffening cage from the cavitation softening pocket (different objects at different operating points) and is SATISFIED by the axis carve: this lane consumes neither branch's sign, only the branch-independent `|Γ| = 1`.

**Reconciled against the seal:** the seal (dead-end/mirror) consumes only the energy axis, on which all three leaves AGREE. The vent reading survives only as a sign statement, which threatens nothing.

### §3.3 — The survival test (the brief's fall condition, run as a corpus sweep)

Frozen test: does ANY leaf assert through-wall ENERGY transport on the compression line at `r_sat`? Sweep receipts (two engines, named): GNU `grep -rniE` over `manuscript/ave-kb/` with the transmit/passes-through/crosses-the-wall/absorbed pattern family — `0` file hits; Python `re` (second engine, independent pattern set incl. "vents energy/flux/power") — `5` candidate lines, each adjudicated OFF-TOPIC on read: `vol3/claim-quality.md:833` (electron/T-bath thermalization, not the r_sat wall), `common/envelope-anatomy.md:121` (E-field/strain register, no wall), `common/trampoline-analogy-primer.md:118` (the `p < p_G` percolation-FLUID regime table — a different lattice phase, not the r_sat wall), `common/translation-tables/translation-circuit.md:962` (`Ṗ_b` = transmitter-side total-power audit — SUPPORTS this lane's ledger row), `vol4/.../dielectric-plateau-prediction.md:38` (LCR bench). **No leaf asserts through-wall energy transport on the compression line. The vent reading does NOT survive reconciliation. The seal ruling STANDS. G-FLAGW-3: PASS.**

### §3.4 — The repair (LANDS, as a ROUTED proposal — no leaf edited by this lane)

Proposed repair text for the coldq FLAG-W entry (routed to the doc lane / Grant): *"FLAG-W RECONCILED (2026-08-09, screening-theorem lane): the three leaves disagree on the SIGN axis (profile-relative per wall-taxonomy §10, FORK-3(b) open) and agree on the ENERGY axis (`|Γ| = 1`, zero transmitted compression flux, ρ-profile-independent per the kernel-collapse receipts). 'Vent' is a pressure-release SIGN reading, not an energy-transmission claim; the compression line DEAD-ENDS at the wall on every branch."* Also routed: the vent leaf's `:42` *"pressure-release boundary"* and the coldq `:19` "vents" gloss should cross-point at §10 to prevent the next reader repeating the conflation. **And the drifted locator** — the coldq FLAG-W entry cites the JAM sentence at `saturating-modulus-and-backreaction.md:57`; it is at `:59` at base `a510ec2b` (pure line-shift, content correct-when-written). Per the frozen-snapshot discipline the coldq result doc is NOT rewritten; the repair is a dated surface note carrying the current locator.

---

## §4 — THE NS-WALL ARM (the honesty fence, Deliverable 4 — answer assembled)

**The question:** do the comparators' cores actually sit behind an `r_sat`-class wall? **The derivation:** the vacuum grade `A(r) = r_sat/r` (canon) reaches yield OUTSIDE the stellar surface iff `R_NS < r_sat = 7GM/c²`; for `R_NS > r_sat`, `A < 1` everywhere in vacuum, and inside the star the enclosed-mass grade `∝ M(r)/r` only decreases inward from its surface value — no wall anywhere, no seal. The condition is exact, needs no NS EOS, and consumes `r_sat` input-only (`clm-law1ho`, solidity `0.55`).

**The answer (driver table §2.1):** HT-pulsar, HT-companion, DP-A: **walled across the entire declared radius-import band** (`11.4–13.7` km; DP-A's band-high margin is `0.13` km — thin but positive). **DP-B: STRADDLED** — flip radius `12.9088` km sits inside the band; whether DP-B is walled is **undecidable at current radius-import precision**, and no direct radius measurement of any comparator star exists (the band is NICER-class measurements of OTHER pulsars + EOS-universality, both declared imports). **Consequence, stated without softening:** on the sub-wall-DP-B branch the seal never engages for DP-B; its matter moment re-sources `0.517` of the orbital quadrupole and the #919 uncaged structure reverts for that share: `60.5`–`181`× the DP bound (R24-contested anchor inherited, cage-fork #770 conditionality inherited) — **the kill re-fires through DP-B alone on that branch.** Hulse-Taylor is unconditional: both stars walled at every band radius; `F_res(HT) = 9.429e-11` of budget. **The single discriminating physical quantity this lane surfaces: DP-B's radius vs `12.91` km** (equivalently: is a `1.2489` M_sun neutron star smaller than `12.91` km?) — an observational question, routed.

---

## §5 — The frozen gate table (UNRUN ≠ PASSED; every gate run and scored)

| Gate | Frozen criterion (prereg §7) | Measured | Score |
|---|---|---|---|
| **G-QUOTE** | every ruled/frozen quote byte-checks vs source (two engines) | v2 ruled text + brief deliverables + NS-wall fence + verdict grammar: whitespace-normalized byte-equality PASS (freeze-time check, rerun at result time); §3.1 leaf quotes + kill cell + R34 lines: grep-verified verbatim | **PASS** |
| **G-SEAL-2M** | seal derived two independent routes, agreeing | route (a) impedance/Γ (§1.0) + route (b) energy-flux bound (§1.1): both give zero transmitted flux / `Re(Z) = 0` | **PASS** |
| **G-PISTON** | moving-boundary term derived or bounded; no seal without its disposition | both danger channels closed by named canon receipts (pattern-not-object; anhysteretic kernel); surviving content absorbed into the §1.5 register | **PASS** |
| **G-SHEAR-COHERENT** | seal must not silence observed GW; one sourcing map, both channels | exterior-configuration sourcing map (§1.3); shear = R1 import anchored, untouched; channel asymmetry = derived content difference | **PASS** |
| **G-DIV** | `∇·u = 0` receipt verified 2-method incl. graded re-derivation | corpus receipt + graded θ-ODE both branches: `O(A²)` wall-localized correction; div-free at moment-forming scale to `2.31e-10` | **PASS** |
| **G-RESIDUE-2M** | residue by two methods within ×3 | **PASS on the residue-setting channel and scored per channel (§2.0-2):** CH-3 (dominant, sets `F_res`) scaling `O(1)` vs quadrature `0.312498`, inside ×3, with the quadrature's own instrument check (denominator vs exact) at `1.37e-06`; CH-2 closed `1.272e-09` vs numeric `1.266e-09` (`0.44%`). **CH-1 is ONE-method and declared so** — sub-dominant, sets no bin. The originally-claimed virial cross-receipt is WITHDRAWN as tautological (§1.5, §2.0-1) | **PASS-WITH-DISCLOSED-SCOPE** |
| **G-COMP** | comparator arithmetic machine-checked + mutation receipt; `make verify` green | number check green; mutation receipt fires; `make verify` green in worktree at commit | **PASS** |
| **G-NSWALL** | per-star `r_sat` from frozen masses; wall condition + flip point per star | §2.1 table; flip radii `14.8637/14.3676/13.832/12.9088` km | **PASS** |
| **G-MEMBER** | member fence at every seal-consuming step; zero unfenced carve-out consumptions (grep receipt, two engines) | §1.7; strain-kernel seal member-robust; carve-out touched only in the fenced bypass-closure argument; grep receipt clean | **PASS** |
| **G-FLAGW-3** | three leaves enumerated verbatim; reconciliation per leaf; survival sweep with named engines | §3.1–§3.3; sweep `0` + `5`-adjudicated-off-topic; vent does not survive | **PASS** |
| **G-NUM** | composite-token-aware number check + mutation receipt; `make verify` green | shipped checker: composite tokens covered, completeness guard, min-3-sig-digits floor, mutation receipt on every registered value | **PASS** |

**Frozen bin arithmetic applied:** G-SEAL-2M ∧ G-PISTON ∧ G-SHEAR-COHERENT ∧ G-MEMBER all PASS → **SEAL-DERIVED**. G-RESIDUE-2M PASS on the residue-setting channel ∧ `F_res = 4.664e-10 × δ_DP < 1` (envelope `4.776e-09` also `< 1`) → **RESIDUE-CLEARS**. G-FLAGW-3 survival test: vent does NOT survive → no override. NS-wall arm: does not change the verdict word; DP-B's straddle is stated inside the verdict sentence (HEADLINE), as frozen.

---

## §6 — Verdict assembly + the comparator statement

**VERDICT: `SEAL-DERIVED + RESIDUE-CLEARS`** — *"the mirror reaches the interior moment AND the derived residue < 1× DP: the kill condition is un-sourced; LC-1 banks structural"* (the frozen bin, prereg §2) — **with the NS-wall conditionality carried inside the verdict:** unconditional on Hulse-Taylor (both stars walled across the radius band; residue `9.208e-12` of the HT budget); conditional on the double pulsar (**walled-DP-B branch:** residue `4.664e-10` × the DP bound, clearing by `9.33` orders — `4.776e-09` on the conservative envelope; **sub-wall-DP-B branch** (`R_B > 12.91` km): the seal never engages for DP-B, `0.517` of the uncaged floor reverts, `60.5`–`181`× DP, the kill re-fires). The frozen LC-1 kill cell (*"An energy-carrying inter-event channel at ≠ c ⇒ arc-level kill"*, arc-brief `:44`) is therefore **UN-SOURCED for every walled configuration**: the compression channel exists and couples (#927), but the seal + derived residue leave it carrying `~10⁻¹⁴` of the shear flux — `9`+ orders below BOTH comparator budgets — so no energy-carrying ≠c channel at observable strength is sourced. What the kill's status now rides on, honestly: (i) the NS radius import for DP-B (this lane's surfaced discriminator), and (ii) the R24-contested anchor + #770 cage fork ONLY on the sub-wall branch (the walled branch needs neither — the seal replaces the cage question for walled stars, which is exactly R34's *"the seal's candidate basis is the wall arc applied at the source's own boundary"*).

## §7 — Ledger + routing

### §7.1 — Consistency-vs-emergence ledger

| Quantity | FORM | VALUE | Class |
|---|---|---|---|
| The screening theorem (`Re(Z_source→bulk) = 0` for interior content at a walled source) | `[derived]` (this lane, §1.0–§1.1, two-method, from the ruled kernel-collapse row) | — | **ADJUDICATED — derivation** |
| Piston/hysteresis closure | `[derived]` from canon receipts (anhysteretic kernel; pattern-not-object) | — | manifestation |
| Graded-exterior divergence `θ/ε_dev ~ O(A²)` | `[derived]` (§1.4; both FLAG-W branches) | `-0.0734`/`-0.0647` at `2r_sat` | **ADJUDICATED — branch-robust** |
| `F_res` (the residue) | `[derived]` channel register ×3, anchored as a RATIO to the R1 shear import; dominant channel two-method (§1.5.1) | `6.064e-14` (DP), `1.473e-14` (HT), dimensionless | **ADJUDICATED (the verdict number)** |
| CH-3 moment coefficient `(3/2)\|κ\|` | `[derived]` — independent normalized quadrature, prefactor-cancelling (§1.5.1) | `0.312498` (→ `5/16`, observation) | **ADJUDICATED — measured, not estimated** |
| `r_sat = 7GM/c²`; `A(r) = r_sat/r` | `[canon-read]` input-only (`clm-law1ho` 0.55) | per-star §2.1 | consistency |
| Overlap structure `j₂'(kR)`; eccentricity shape | `[consumed]` #927 (MERGED) | — | consistency (inherited) |
| Pulsar residuals; masses; periods | `[import]` re-retrieved at freeze (prereg §4) | `0.0016`/`1.3e-4`; §2.1–§2.2 | comparator |
| NS radii band `11.4–13.7` km | `[import]` NICER-class, OTHER pulsars + EOS-universality — DECLARED | — | comparator import (the DP-B hinge) |
| Uncaged floor `[0.0152, 0.0455]`; Branch-X bracket | `[cited-structure]` #919 branch state; R24-CONTESTED; Branch-X SUPERSEDED-INPUT (replaced by §1.5) | — | incumbent (gated, not consumed) |
| The coupling AMPLITUDE | — | — | **NOT-DERIVABLE (inherited, #919 R23; unchanged — all lane numbers are ratios)** |

No emergence-class claim is made. No chord is claimed on any outcome (prereg §0 row 8). Mints nothing; edits no leaf; changes no solidity.

### §7.2 — Routing (nothing executed here)

1. **The verdict + residue number + FLAG-W disposition + NS-wall answer → the orchestrator.** #913, #919, LC-1 arc scope, #770 dispositions are Grant's/the orchestrator's (R34 holds them on this lane).
2. **FLAG-W repair text (§3.4) → doc lane / Grant** (leaf edits routed, not landed).
3. **The DP-B radius discriminator** (`R_B` vs `12.91` km) → the orchestrator as this lane's surfaced observational question (candidate for the falsification program's watch list; NOT minted here).
4. **The #770 cage fork:** on the walled branch this lane's seal SUPERSEDES the cage question for compact sources (the wall does the caging, ruled physics); on the sub-wall branch #770 stays exactly as open as it was. Disposition routed with R34's.
5. **Tier-2:** this lane's own adversarial review runs before any CLEARED wording (provenance banner below); pre-flagged primary targets for the review: the CH-1 `β²`-order counting (§1.5), the G-RESIDUE-2M operative two-method reading (§2.0-1), and the DP-B reversion arithmetic (§2.1).

---

> **Result-doc provenance.** Frozen prereg committed ALONE + pushed at `a1c8a200` (freeze-by-push) BEFORE any derivation content, driver code, or lane number existed; driver committed BEFORE this result doc; all numerals machine-verified against the shipped `screening_theorem_results.json` by `screening_theorem_number_check.py` (`--mutation-receipt`; auto-discovered by `make verify`). Deterministic (no RNG in any path). Two-method receipts throughout (seal: Γ-route + flux-route; residue: closed-vs-numeric + virial cross-identity; sweeps: two named regex engines). Frozen criteria travel verbatim or file:line; deviations §2.0 (one, instrument-reading class, direction-none). Interior = past-wall phase state, declared at §0 and every point of use; no law extrapolated inside. `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; Tier-2 adversarial review at adjudication before any CLEARED wording. Companions: the frozen prereg, the brief, the R34 ruling, #927 (merged), #919/#913 (branch state), the kernel-collapse docket arc (v1/#897, v2/#905, correction/#907), wall-taxonomy §10/§10.2.
