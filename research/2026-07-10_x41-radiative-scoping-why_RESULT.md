# X41 — THE "NOT YET WHY" ARC: derivation + data confrontation + fork record — RESULT

**Date:** 2026-07-10 · **Lane:** implementer · **Branch:** `analysis/x41-scoping-why`
**FROZEN prereg (gated on):** `research/2026-07-10_x41-radiative-scoping-why_prereg_FROZEN.md`
(freeze commit `0180f85a`, PUSHED before this doc — git ordering = freeze proof).
**Adjudicators (P10 / X36):** EXISTING DATA + DERIVATION-FROM-AXIOMS/CANON. **NOT the engine** — the
engine cannot adjudicate a keying choice about its own kernel. **No KB/Letter edit** (research/ only;
`papers/` FIREWALLED).

---

## ⚑ CORRECTION LOG — PR #627 adversarial review (12/12 findings addressed; KEEP-BOTH)

> Applied on-branch after the #627 review (12 confirmed, 0 refuted). The superseded framing is preserved
> where struck so git carries the trail; the outright errors (R3 fabricated quote, R7 α-exponents) are
> FIXED, not kept-both. **What SURVIVED the review (unchanged):** the UNDERDETERMINED headline, K3-dead,
> the near-zone discriminator, the `E_T≡0` Helmholtz identity (as an identity), the freeze, the Letter
> firewall, the bench adjudicators.

| # | finding | repair | class |
|---|---|---|---|
| **R1** (1/5/12) | K1's "drive-direction corollary" was an **unlicensed bidirectional extension** of #624's guarantee — the source scopes it to probe READOUT (:193 "SPLIT C-V … by terminal selection", :200 "MEASUREMENT discipline"), and #624:179-188 has a **held T2 bias LOADING** the shunt-C via the tangent permittivity, citing `CLAUDE.md`:75 approvingly (never confronted). | **DEMOTE K1:** "DERIVED-EXACT drive-direction corollary / strongest" → **"axiom-level reinterpretation, PENDING-GRANT"** (= exactly what #547-M0 said DC-blindness would need). Add :179-188/:193/:200. Remove the BIDIRECTIONAL assertion (restated as the hypothesis needing its own ruling). | SUPERSEDE |
| **R2** (6) | the "canon contradiction (`:73`+#624 vs `:75`)" is mis-framed — #624:183-188 **AGREES** with :75; canon is internally consistent. | **RE-FRAME the fork:** "K1's drive-side extension **vs STANDING CANON**"; a Grant ruling for K1 = a **new axiom-level decision against the current reading**, not resolving an existing contradiction. CVR bench remains the empirical adjudicator. | SUPERSEDE |
| **R3** (2/9/11, MAJOR) | a **fabricated "verbatim" quote** — *"the ε-grade varactor identity … surfaced for Grant's adjudication; not resolved here"* does not exist in #547 (first fragment invented, rest stitched across ~23 lines). | **REPLACE with genuine #547 text** (:272-273 + :300), correctly characterized as a **chord-vs-tangent CONVENTION** item, NOT a sector-attribution flag; **remove its supporting role** for the muon re-attribution. | FIX |
| **R4** (3) | decree-vs-flag inversion: §3.1 stated the cross-wire attribution as fact while §7 routed it to Grant. | **ALIGN:** the CONFIG-LEVEL FACT stands confirmed (#547 fed the full `\|E\|` into the `V_yield`/T2 key, no Helmholtz decomposition); the ERROR/cross-wire ATTRIBUTION is **PLAUSIBLE-BUT-UNPROVEN**, pending Grant + bench. | FIX |
| **R5** (7) | K1 confronted #547-M0 only, skipped **M1's COMPUTED topology** (the merged mechanism by which a held field reaches the ε element). | **ADD the M1 confrontation** — state what K1 must overcome, concede it as an **open cost** on K1's ledger. | ADD |
| **R6** (10) | the prereg froze an **honest TIE (no branch favored)**; the result ranked K1 "strongest/leads" — an unlicensed ranking the prereg's own seduction flag did not stop. | **RESTORE the frozen tie** with per-key **COST LEDGERS**. **Deviation-log:** the flag did not stop the ranking — *frozen bins enforce, flags don't* (process lesson banked). | FIX |
| **R7** (4/8/12) | α-exponent mislabels (prose said argument ×α / deficit ×α²; the shipped `1.1e4 µeV` used √α argument / α deficit); "DERIVED-EXACT" verb on a contingent conclusion. | **Fix prose** to √α argument / α deficit; **strip "DERIVED-EXACT"** from the contingent conclusion. | FIX |

**DEVIATION-LOG (R6 process lesson, banked):** the prereg carried a seduction flag against over-claiming,
yet the shipped result still *ranked* K1 "strongest/leads" over the frozen tie. The flag was visible and
did not fire in-lane. **Lesson: a frozen adjudication BIN enforces; a seduction FLAG only reminds.** Future
prereg ties should be written as a bin ("route to TIE unless X forces Y"), not guarded by a flag alone.

---

## ★ ROUTED VERDICT CLASS: **[UNDERDETERMINED — K1 ∧ K2, with the transverse-reactive near-zone as the named discriminator]**

> **K3 (dynamical-content) is CLOSED DEAD** (= the excluded round-1/2/3b family; §STEP-1 of the prereg).
> **K1 (transverse projection) and K2 (impedance/mode-basis) are TWO LIVE candidate mechanisms in a
> FROZEN TIE (no branch favored — prereg §3, record-all)** — both reproduce BOTH anchors, both re-derive
> Route C, both survive the spectroscopy re-kill — and the **axioms do NOT force one over the other.** They
> **diverge only on the transverse-reactive near-zone** (K1 loads, K2 not), an **UNBUILT probe**. Each key
> carries a **COST** it must pay to become the "why" (neither is paid here):
> - **K1** needs an **axiom-level reinterpretation, PENDING-GRANT** — that the node's shunt admittance
>   splits by polarization so a held *longitudinal* field biases only the A1 branch, not the T2
>   permittivity. This is a **new ruling against the current canonical reading** (`CLAUDE.md`:75 +
>   #624:179-188, which have a held bias LOADING the T2 shunt-C), **not** the resolution of an existing
>   contradiction; and it must also **overcome #547-M1's COMPUTED topology** (a held field reaches the
>   shunt element). It is precisely the "axiom-level reinterpretation" #547-M0 named as H2's requirement.
> - **K2** needs the **Axiom-4 kernel relocated from the reactance (Op14) to `R_rad`**, and for the
>   non-uniform-static case it **reduces to the #547 gauge-observability RIDER**. K2 remains **Grant's
>   preferred EE register** for STATING the boundary (it kills net-flux and captures the standing-wave
>   sub-point).
>
> **NET:** the postulate's "why" is **not closed by the axioms alone.** The arc moves the state from the
> prereg baseline ("open, K3 dead") to **"open, K3 dead, TWO live candidate mechanisms (a frozen tie) with
> per-key costs, a named Grant fork (K1's drive-side extension vs standing canon), and a named unbuilt
> discriminator (the near-zone probe)."** Recorded WITHOUT a "derived-why" headline and WITHOUT ranking the
> tie (Rule 11; the seduction flag on the #624 rhyme is honored — see §7 and the correction log).

---

## SECTOR HEADER

- **MODE:** derivation-from-axioms/canon (not engine-fire). **REGIME:** the Letter's radiative sector
  (deep-cold, sub-yield, weak-field, transverse on-line pump) + the atomic-static sector (held Coulomb).
- **SECTOR:** the **T2 transverse permittivity** (`ε_eff=ε₀S`, keyed `V_yield`) — the birefringence-bearing
  object — vs the **orthogonal A1 longitudinal bond compliance** (`C_eff=C₀/S`, keyed `V_snap`); the
  **µ/Cosserat-B** sector is the consistency leg (§4). **A1 ⊥ T2** (`master-equation.md`:20 two-"3"s
  disambiguation, Grant-ratified; `CLAUDE.md`:73).

---

## STEP 3 — THE DERIVATION

### 3.0 What the axioms/canon supply (the substrate structure the kernel argument sits in)

The kernel argument is not a free scalar. Canon fixes a **two-domain impedance structure** per node
(`research/2026-07-07_semiconductor-cv-dip_RESULT.md`:200-216, Grant-ratified via PR #558; `node-up`:105
supersession; `CLAUDE.md`:73):

| channel | element | keyed on | read by | impedance domain |
|---|---|---|---|---|
| **T2 transverse permittivity** | `ε_eff=ε₀S` (rolls off) | `V_yield` (43.65 kV) | **transverse-polarized** EM probe | `Z_EM = Z_0` (Ω) |
| **A1 longitudinal bond compliance** | `C_eff=C₀/S` (diverges) | `V_snap` (511 kV) | **longitudinal (bulk)** probe | `Z_bulk` (mechanical `ρc`) |

VERBATIM anti-cross-wire guarantee (`semiconductor-cv-dip`:215): *"Because `Z_EM` (transverse, Ω) and
`Z_bulk` (longitudinal, mechanical `ρc`) live in **different impedance domains**, the two terminal pairs
cannot cross-couple — **that domain separation IS the anti-cross-wire guarantee**: a transverse-EM readout
can NEVER pick up the A1 compliance, and a longitudinal-bulk readout can NEVER pick up the T2 permittivity."*

**⚠ SCOPE OF #624'S GUARANTEE (R1 repair — as-written it is a READOUT/measurement statement, NOT a drive
statement).** The source scopes this to **probe terminal selection**: its section title is
`semiconductor-cv-dip`:193 *"SPLIT C-V — separating T2 polarization from A1 compliance by terminal
selection"* and :200 *"the anti-cross-wire MEASUREMENT discipline"*; the guarantee is about which channel
a **probe couples to** when READING the loaded line. Crucially, the **same section (:179-188) has a held
T2 bias LOADING** the shunt-C: *":179 A held T2 bias loads the shunt-`C` through the small-signal (tangent)
permittivity, pulling the band edge …"*, and it honors the round-3 gauge rider (:184-188) — *uniform bias
self-cancels, gradient reads `Γ≠0` via the Op14 mirror* — **citing `CLAUDE.md`:75 approvingly.** So #624
does **not** assert that a held field fails to *drive* the T2 permittivity; it asserts the two channels do
not *cross-READ*.

**⚑ The "drive direction" is therefore a HYPOTHESIS (K1's), not a corollary of #624 (R1 repair).**
Extending the readout-domain separation to a **drive**-domain separation — "a longitudinal drive cannot
reach the T2 permittivity" — is an **additional axiom-level claim** that #624 does not make and its :179-188
body **contradicts** (it has a held bias driving the T2 shunt-`C`). K1 needs this drive-side split as a
premise; it is **NOT licensed by the guarantee as written**, and it requires its own ruling (§3.1).

### 3.1 K1 [PROJECTION] — an axiom-level reinterpretation, PENDING-GRANT (the fork = K1's drive-side split vs standing canon)

**The chain, with its load-bearing premise flagged.** The Helmholtz decomposition splits any E into
`E = E_L + E_T`: `E_L` longitudinal (curl-free, source-slaved by `∇·E=ρ/ε₀`) and `E_T` transverse
(divergence-free, the medium's own radiative content). The one **EXACT** step and the one **PENDING**
premise are kept separate:
- **EXACT (identity, survives review):** a **held static Coulomb field is PURELY LONGITUDINAL** —
  `E = −∇φ`, `∇×E = 0`, so `E_T = 0` **identically** (not "small" — exactly zero, for any static charge
  configuration). This is a Helmholtz identity, unconditional.
- **PENDING-GRANT premise (K1's drive-side split — NOT licensed by canon, §3.0):** that the T2
  permittivity's operating point is set by `|E_T|` (the transverse projection), so a held longitudinal
  `E_L` biases only the A1 compliance and **not** the T2 permittivity. **This is an axiom-level
  reinterpretation** — precisely the "**axiom-level reinterpretation**" #547-M0 named as H2/DC-blindness's
  requirement (M0 verbatim: *"H2 would need an **axiom-level reinterpretation**, flagged — not a network
  trick"*). Canon as it stands (`CLAUDE.md`:75 + #624:179-188) has a held bias LOADING the T2 shunt-`C`,
  so K1's premise is a **NEW ruling against the current reading**, not a corollary.
- **IF the premise holds:** the T2 permittivity sees `|E_T|/E_yield = 0` for a held static Coulomb →
  `S_ε = 1` → **transparent** (the `E_T≡0` input is exact; the *conclusion* is contingent on the premise).
- **Pump (unconditional):** a **propagating pump is purely transverse** (`E = E_T`) → T2 argument
  `= |E_T|/E_yield = A_V` at FULL magnitude → loads `δn_bir = −½A²` (MARK-1 — no `(kℓ_node)` suppression:
  an amplitude key on `E_T`, not a circulation key `∮E·dl`, so it avoids the KNOWN TRAP). *(This holds
  for a transverse pump regardless of the pending premise — a pump has `E_L=0`, so `|E_T|=|E|`.)*

**⚑ THE FORK IS "K1's DRIVE-SIDE EXTENSION vs STANDING CANON" — NOT a canon contradiction (R2 repair;
flag-don't-fix; Grant adjudicates, X36).** The earlier draft framed this as a `CLAUDE.md`:73-vs-:75
contradiction. **That framing is withdrawn — canon is internally consistent here:**
- **`CLAUDE.md`:75 (VERBATIM):** *"A **static-E-only drive is ASYMMETRIC** … it loads the `ε` / capacitive
  sector only (`S_ε<1`, `S_μ=1`). This gives the **Op14 Meissner-asymmetric** impedance
  `Z_eff=Z_0√(S_μ/S_ε)` … `Z` **changes**, so the boundary reflects (`Γ≠0`)."* ⟹ **static-E LOADS `S_ε`.**
- **#624:179-188 AGREES with :75** (not contradicts it): it has *"a held T2 bias loads the shunt-`C`
  through the small-signal (tangent) permittivity"* and honors the same gauge rider (uniform self-cancels,
  gradient reads `Γ≠0` via the Op14 mirror), **citing `CLAUDE.md`:75 approvingly.** #624's `Z_EM`⊥`Z_bulk`
  separation is a **READOUT** statement (:193/:200) that coexists with :75's held-bias loading.
- **So the standing canonical reading is: a held (longitudinal) static E DOES load the T2 permittivity**
  (`S_ε<1`), readably at a gradient. **K1 asserts the opposite** — that a longitudinal drive does not
  reach T2. **The fork is therefore K1's DRIVE-SIDE SPLIT vs the STANDING CANON, and a Grant ruling for
  K1 would be a NEW axiom-level decision AGAINST the current reading — not the resolution of an existing
  contradiction.** The empirical adjudicator is the **CVR held-DC-E bench** (§3.2/§7): does a held DC E
  shift the vacuum's small-signal `C`? Canon (+#547-M3) predicts YES; K1 predicts NO for the T2 branch.
  **Surfaced for Grant, decreed by neither the engine (X36) nor this lane.**

**The #547 muon: a CONFIRMED config-fact + a PLAUSIBLE-BUT-UNPROVEN attribution (R3/R4 repair).**
- **CONFIG-LEVEL FACT (confirmed from #547's configs — stands):** #547 (`em-keying-round3-eps-dc-mechanism_RESULT.md`,
  merged) computed the muon overshoot (`§9: 1.52×10⁶ µeV`) by feeding the **static Coulomb's full `|E|`
  into the `V_yield`/T2 key** (M0: `A_V = V/V_yield = |E|/E_yield`) — **with no Helmholtz decomposition.**
  That is a verifiable property of the shipped driver, not an interpretation.
- **ATTRIBUTION (PLAUSIBLE-BUT-UNPROVEN — pending Grant + the CVR bench, NOT a decree):** *whether* that
  full-`|E|` keying is an **error** (a drive cross-wire that should have routed the longitudinal Coulomb
  to A1) or **correct** (charge-keyed is right; a held longitudinal E does load T2) is exactly the K1 fork
  above — it rides the same Grant ruling. This lane does **not** rule it.
- **⚑ FABRICATED-QUOTE CORRECTION (R3).** An earlier draft attributed to #547 a "verbatim" flag — *"the
  ε-grade varactor identity … surfaced for Grant's adjudication; not resolved here"* — **that text does
  not exist in #547** (the first fragment was invented; the tail was stitched from :300). The **genuine**
  #547 open item is `:272-273` — *"**ONE MERGED GRANT-ADJUDICATION ITEM — the three-way
  varactor-convention tangle (flag-don't-fix; surfaced for Grant, NOT resolved here)**"* — closed at `:300`
  *"Surfaced for Grant's adjudication; not resolved here (… a corpus-consistency call across (a)/(b)/(c),
  not an engine bug)."* **Correctly characterized, it is a chord-vs-tangent CONVENTION item** ("which
  object is *the* small-signal `C`" — chord `C₀/S` vs tangent `C₀/S³`), **NOT an open sector-attribution
  flag, and it does NOT support the muon re-attribution.** That supporting role is **withdrawn**; K1's
  drive-side split rests only on the Grant fork (§3.1) + the CVR bench, not on this #547 flag.

**⚑ K1 MUST ALSO OVERCOME #547-M1, not only M0 (R5 repair — added; open cost on K1's ledger).** #547-M1
built **both** cell topologies as time-domain models and **COMPUTED** that the canonical
**series-L-bond / shunt-C-node** unit passes a held DC to the varactor node (`V_node → V₀`, the series-L
is a DC short; a series-C-blocked counterfactual relaxes to zero). M1 is the merged, computed mechanism by
which a held (longitudinal) field **physically reaches the ε shunt element.** K1 does **not** dispute that
the held `V` reaches the shunt node; K1's heavier claim is that the shunt admittance **splits by
polarization** into an A1-longitudinal branch (biased by the held `V`) and a T2-transverse branch (not) —
a split M1's single-"ε-grade varactor" topology **did not carry** and canon's shunt-`C` does not obviously
license. **This is an OPEN COST on K1's ledger:** absent that node-level A1⊥T2 admittance split, M1
stands and the held field reaches the T2 element. (The A1-sector cost estimate — *if* the muon load
re-keys to `V_snap` — is `~√α×` the **argument** and `~α×` the **deficit** (`V_yield=√α·V_snap`), i.e.
`1.52×10⁶ µeV × α ≈ 1.1×10⁴ µeV`, still an overshoot; but this is a *conditional* A1-sector figure that
inherits the same PENDING premise, not an established re-attribution.)

**Symmetric-standard check (consensus-bias guard, still valid):** QED *also* separates the muonic-H Lamb
shift (longitudinal Coulomb / vacuum polarization) from vacuum birefringence (transverse light-by-light) —
they are different sectors there too. So K1's *motivation* is not AVE special-pleading; but the
AVE-distinct claim it needs — that the Ax-4 *saturation* keys on `E_T` — remains the PENDING axiom-level
ruling, not a consequence of the QED analogy.

### 3.2 K2 [IMPEDANCE / MODE-BASIS] — the EE register, and the reactance-vs-`R_rad` fork

**What K2 delivers cleanly.**
- **Net-flux is NOT K2** (basis membership, not net Poynting) — so K2 survives the round-2 STEP-0 net-flux
  kill and correctly predicts a **standing wave LOADS despite zero net flux** (two counter-propagating
  on-line modes). **Confirmed by the p5 datum** (standing-wave CONTROL: `1−S_ε=4.75×10⁻²` at net
  `F=1.17×10⁻¹⁸≈0`).
- **Pump** (propagating, on-line) → loads full (MARK-1 EXACT). **`R_rad≡Z_0`** is canon
  (`substrate-native-terminology.md`:43, *"radiation resistance `R_rad≡Z_0` (wave-making drag, a real
  port)"*) — "the kernel lives inside the only resistor the vacuum has."
- **Muon** (held static, `ω=0`) → radiates zero power → zero `R_rad` coupling → **never meets the kernel →
  transparent** (MARK-2 reached, via "static → no `R_rad`").

**⚑ Where K2 is CONTESTED (the load-bearing headwind).** For the **non-uniform static** case (the muon /
atomic Coulomb), K2 must assert that a held static field **does not bias the reactance** at all — but
canon places the Ax-4 kernel **in the reactance** (Op14 `Z_eff=Z_0/√S`, `operators.md`:54; `ε_eff=ε₀S`),
and #547 M3 derives that a held bias `A_0` **shifts the small-signal reactance** `C₀·S(A_0)` a co-located
probe reads. K2 therefore needs the kernel **relocated from the reactance to `R_rad`** (a reinterpretation
of "loading" as `R_rad`-coupling-gated). **For a UNIFORM held field K2 REDUCES to the #547
gauge-observability RIDER** (both say: loaded-but-gauge-hidden / never-on-line coincide — no readable
shift). For the NON-uniform Coulomb the two diverge: #547-RIDER says the gradient is readable (`Γ≠0`,
refracts a probe — a static `ε`-gradient *is* a GRIN lens), K2 says it never biased `ε` in the first
place. **The discriminator between #547-M3 (reactance) and K2 (`R_rad`) is the EE-bench CVR test** (does a
held DC E shift the vacuum's small-signal `C`? — the deferred cRIO `C_eff(V)` bench), which the engine
cannot pre-empt (X36). So K2 is the correct **register** for stating the boundary and is derivation-clean
for the *uniform/on-line* facts, but it does **not** by itself derive the non-uniform-static transparency
against Op14 — it **inherits the incumbent RIDER** there.

### 3.3 Why the axioms do NOT force one (the honest UNDERDETERMINATION)

K1 and K2 **agree** on every adjudicated config: pump loads (both), Coulomb transparent (K1: `E_T≡0`; K2:
`ω=0`→no `R_rad`), static-B transparent (both, §4), standing wave loads (K1: transverse; K2: basis-member
— p5-confirmed). They **split only** on the **transverse-reactive near-zone** (antenna induction `1/r²`:
Helmholtz-transverse + reactive/off-line): **K1 says LOADS** (nonzero `E_T`), **K2 says NOT** (reactive,
`|Γ|=1`, off-line). No existing datum tests this (§STEP-4c). So the axioms + existing data are
**insufficient to force K1 vs K2** — the routed class is **UNDERDETERMINED**, with the near-zone probe as
the named discriminator.

---

## STEP 4 — DATA CONFRONTATION

### 4a — the two mandatory anchors (both keys pass)

| anchor | K1 [projection] | K2 [impedance/mode-basis] | incumbent #547 [charge-keyed] |
|---|---|---|---|
| **MARK-1 pump** (transverse, `A²≈6e-7`, `δn=−½A²`) | `E=E_T` → full load, `−½A²` **EXACT** | on-line → full load **EXACT** | full load (loads on `|E|`) |
| **MARK-2 muon** (held Coulomb, longitudinal) | T2 load `=0` **IF** the PENDING premise holds (`E_T≡0` is exact; the *transparency* is contingent) | `ω=0`→no `R_rad`→ T2 load `=0`; but reactance-bias contested | **LOADS** `1.52×10⁶ µeV` → scope-OUT (data-acquired) |

Both K1 and K2 reach MARK-2 (T2 load `=0`) — each **conditioned on its pending premise** (K1: the
`E_T`-keying axiom-level ruling; K2: the reactance→`R_rad` relocation), vs #547's loads-then-scope-out.
Per the prereg §4 flag, the muon **cannot discriminate** the two derived-zeros from scope-out at the data
level — the difference is mechanism-class, adjudicated only by the CVR bench, not by the anchor. (The
`E_T≡0` Helmholtz identity is exact; what is *not* settled is that it implies T2-transparency — that is the
pending premise, not a "sharpest derived zero.")

### 4b — the spectroscopy re-kill check (do bound-state near-fields load?)

The strong field in a bound atom (the nuclear Coulomb) is **static + longitudinal → `E_T≡0`** → routes to
A1, NOT the T2 birefringence sector. The only nonzero T2 content in an atom is the **transverse
radiative/virtual** field (retardation/Breit, `~(Zα)²` of the Coulomb *in the transverse channel*, and
the on-shell transition fields are single-photon-tiny). **K1:** T2 loads only that transverse content →
parametrically negligible → **NOT re-killed by precision spectroscopy.** **K2:** on-shell transverse →
loads (tiny); off-shell/virtual → off-line → does not load → **also not re-killed.** (The muon's static
proton **magnetic** dipole field is `∂_tB=0` → Route-C transparent — µ-sector, not ε; it does not
resurrect a loading.) **Both keys survive** the atomic-spectroscopy re-kill; the strong-field overshoot
that killed the naive continuum law lived in the **longitudinal** channel both keys route away from T2.

### 4c — the near-zone discriminator: what existing data touches it (expected: none)

**Inventory result: NO existing datum touches the ε transverse-reactive near-zone.** The closest corpus
touch is the **µ-sector `(kr)²` near-zone table** (p5: `A_I∝(kr)²` suppression of the reactive *magnetic*
near-zone) — but that is the circulation sector and is consistent with BOTH keys. The **ε-side
transverse-reactive near-zone is UNTESTED.**
- **Named registered-probe CANDIDATE (flagged, NOT registered — a gated follow-on):** drive a
  **transverse-reactive near-zone E** (the induction zone of a small oscillating electric dipole,
  `kr≲1`: transverse, time-varying, non-radiating, source-attached) and read vacuum ε-loading. **K1 →
  loads; K2 → null.** This is the clean K1/K2 discriminator.
- **The one plumber-physical question (surfaced to Grant, pre-test-physics-check):** *in the antenna
  induction zone — the stored, non-radiating, transverse `1/r²` reactive field around a small oscillating
  dipole — does the vacuum's ε-nonlinearity engage (transverse energy, K1) or stay silent (reactive
  energy never on the line, K2)?*

---

## STEP 4′ — THE µ-SIDE CONSISTENCY LEG (both keys re-derive Route C; a check, not a discriminator)

- **K1 unification ("medium's own vs source-slaved"):** the µ-inductor keys on the **circulation /
  solenoidal** (induced, medium's-own) magnetic content `∮H·dℓ = ε₀∂_t∫E·dA`; a source-slaved static
  Biot-Savart B is curl-free in vacuum → zero circulation → transparent. Dual of "T2 keys on `E_T`":
  **both sectors — source-slaved (longitudinal-E / static-Biot-Savart-B) → transparent** (ONE principle).
- **K2 unification:** a static B is the `|Γ|=1` reactive limit (`ω=0`, no radiation) → off-line →
  transparent, identical to the static-E off-line argument; the p5 `(kr)²` reactive-near-zone suppression
  is consistent. **ONE principle (reactive/off-line → transparent).**

**Both re-derive Route C** (`S_B=√(1−A_I²)`, static B → `A_I=0` → transparent) under a single principle →
**no divergence to report.** (This is a consistency PASS for both, so it does not break the tie.)

---

## STEP 5 — FORK RECORD + VERDICT

**The two live keys are a FROZEN TIE (prereg §3, no branch favored) — recorded with per-key COST LEDGERS,
not a ranking (R6 repair).**

| key | routed status | COST it must pay to become the "why" (unpaid here) | anchors | discriminator |
|---|---|---|---|---|
| **K3** dynamical-content | **CLOSED DEAD** | — (= excluded round-1/2/3b family, Letter verbatim) | — | — |
| **K1** transverse projection | **LIVE — TIE (axiom-level reinterpretation, PENDING-GRANT)** | (i) a **new axiom-level ruling** that T2 keys on `\|E_T\|` — *against* the standing canon (`CLAUDE.md`:75 + #624:179-188 have a held bias LOADING T2); (ii) must **overcome #547-M1's COMPUTED topology** (held field reaches the shunt element). `E_T≡0` is an exact identity; the transparency is contingent. | MARK-1 ✓ (transverse pump); MARK-2 ✓ **IF** premise (i) | near-zone → **LOADS** |
| **K2** impedance/mode-basis | **LIVE — TIE (Grant's preferred EE register)** | (i) **relocate the Ax-4 kernel** reactance→`R_rad` (conflicts Op14 `Z_eff=Z_0/√S`); (ii) for uniform-static it **reduces to the #547 gauge RIDER**. Kills net-flux; standing-wave p5-confirmed. | MARK-1 ✓; MARK-2 ✓ (via `ω=0`→no `R_rad`) | near-zone → **NULL** |

**VERDICT CLASS: [UNDERDETERMINED — K1 ∧ K2 (frozen tie), transverse-reactive near-zone the named
discriminator].** The postulate's "why" is **not closed by the axioms alone.** Honest state-change vs the
prereg baseline: **from "open, K3 dead" → "open, K3 dead, TWO live candidate mechanisms in a frozen tie
with per-key costs, a named Grant fork (K1's drive-side split vs standing canon), and a named unbuilt
discriminator (the transverse-reactive-near-zone probe)."** **Neither key is ranked above the other** — the
prereg froze the tie; the adjudicators are Grant (K1's axiom-level ruling), the CVR bench (K1/K2 vs
#547-M3), and the near-zone probe (K1 vs K2). **The engine cannot decide it** (X36).

**No `[DERIVED-WHY]` headline is claimed** (Rule 11): both keys carry unpaid costs (above), the near-zone
discriminator is unbuilt, and the #547 attribution is a *config-fact + a pending-Grant attribution*, not a
decree. **No criterion was dropped; no branch was ranked** (the R6 correction restores the frozen tie the
result had breached). **No fourth key was minted** (Rule 12).

**NO figure** — the arc's product is structural (which sector the argument lives in); no numerical result
warrants a WHITE figure. **The Letter consequence is GATED FUTURE WORK only** (§7).

---

## 6 — CONSISTENCY-VS-EMERGENCE CLASSIFICATION

- The **UNDERDETERMINED verdict** (a frozen tie between two candidate mechanisms) is a structural
  routing result — **MANIFESTATION class** (it turns on the Helmholtz `E_L`/`E_T` split and the K4
  sector structure; the drive-side keying that would collapse the tie is the PENDING axiom-level ruling,
  NOT asserted here), scale-invariant, α-firewalled off the routing.
- The marks (pump `A²`, muon overshoot) ride CODATA-derived `E_c=√α·E_crit` — **CONSISTENCY-class
  magnitudes**, not headlined. **No emergence claim.**

---

## 7 — SURFACED TO GRANT / THE AUDITOR LANE (flag-don't-fix; NO KB/Letter edit landed here)

1. **⚑ The Grant fork — K1's drive-side split vs STANDING CANON (the load-bearing item; R2-corrected, NOT
   a "canon contradiction").** Canon is internally consistent: `CLAUDE.md`:75 (static-E loads `S_ε<1`,
   Op14 `Γ≠0` mirror) and #624:179-188 (held T2 bias loads the shunt-`C`, citing :75 approvingly) AGREE;
   #624's `Z_EM`⊥`Z_bulk` is a READOUT separation (:193/:200) that coexists with them. **K1 proposes a
   NEW axiom-level split** — that the T2 permittivity keys on `|E_T|`, so a held longitudinal E biases only
   A1 — which runs **against** that standing reading. The question *does a held longitudinal static E load
   the T2 permittivity?* is what a Grant ruling would settle (a new decision, not a resolved contradiction),
   with the **CVR held-DC-E bench** as the empirical adjudicator (canon+#547-M3: YES; K1: NO). **Surfaced
   with all sources verbatim; decreed by neither this lane nor the engine (X36).** The auditor lane lands
   any KB note — I do NOT draft it.
2. **⚑ #547 muon: config-fact + pending attribution (R3/R4-corrected).** CONFIRMED config-fact: #547 fed
   the static Coulomb's full `|E|` into the `V_yield`/T2 key with **no Helmholtz decomposition** (M0). The
   *interpretation* — that this is a drive cross-wire (K1) vs correct charge-keying (#547) — is
   **PLAUSIBLE-BUT-UNPROVEN**, riding the same Grant fork (item 1) + the CVR bench. **This does NOT retract
   #547** (Rule 12): its charge-keyed math stands as the incumbent. **CORRECTION:** an earlier draft cited
   a fabricated #547 "verbatim" flag to support the A1 re-attribution; the genuine #547 open item
   (`:272-273` / `:300`) is a **chord-vs-tangent CONVENTION** call, not a sector-attribution flag — the
   supporting role is withdrawn.
3. **The near-zone probe** — a **registered-probe candidate** (transverse-reactive E, `kr≲1`, K1-loads /
   K2-null) and the **CVR bench** (held-DC-E vacuum-`C` shift, #547-M3 reactance vs K2 `R_rad`) are the
   two experiments that would close the fork. **Flagged, not registered** (gated follow-on; the deferred
   cRIO `C_eff(V)` bench is the natural home of the CVR test).
4. **Letter consequence — GATED FUTURE WORK ONLY.** IF Grant adjudicates the projection question toward
   K1, the Letter's "open postulate" could upgrade to a **derived T2-sector scoping** (the birefringence
   law keys on `E_T`; static longitudinal fields are the orthogonal A1 sector). **This is downstream of
   adversarial review + Grant; the Letter is FIREWALLED and untouched by this arc.**

---

## 8 — DISCIPLINE

- **challenge-canonical-negative:** #547 was challenged by grepping its CONFIGS (M0 fed the full `|E|` into
  `V/V_yield` with no Helmholtz decomposition — a confirmed config-fact; its *error-status* is the pending
  Grant fork), not its conclusion; the ½/¼ tell is guarded (all coefficients are the sqrt-kernel's own
  `−½A²`).
- **flag-don't-fix:** the Grant fork (K1's drive-side split vs standing canon) and the #547 muon
  config-fact are surfaced with all sources verbatim, resolved by NEITHER — Grant's call + the CVR bench
  (§7). No canon rewritten; canon is internally consistent (the earlier "`:73`-vs-:75 contradiction"
  framing is WITHDRAWN, R2).
- **Rule 11 honest closure:** the axioms do not force a key → the routed bin is UNDERDETERMINED (a frozen
  tie), not a manufactured `[DERIVED-WHY]` and not a ranking. **Rule 12:** no retraction of #547, no fourth
  key minted.
- **P10 / X36:** the engine cannot adjudicate its own kernel's keying; the adjudicators named are Grant
  (K1's axiom-level ruling) + two unbuilt benches (near-zone probe, CVR). **Seduction flags honored:** the
  #624 A1/T2 cross-wire is a *rhyme* that MOTIVATES K1 — and the review confirmed the danger, since the
  "drive-direction corollary" over-read #624's READOUT-scoped guarantee (R1); K1 is now correctly labeled a
  PENDING axiom-level reinterpretation. The 0-for-N forward-prediction ledger's thinning is respected (no
  chord banked). The **frozen-tie breach (R6)** is banked as a process lesson: a seduction flag did not
  stop the ranking — frozen bins enforce, flags don't.
- **phase-space-coordinate-check (A46):** keys/marks in matched coordinates (transverse projection /
  mode-basis / `A_V`); the near-zone `F`/`kr` axes are discriminator axes, never keying coordinates.
- **consistency-vs-emergence:** MANIFESTATION-class verdict; CONSISTENCY-class magnitudes; no emergence
  headline.
- **verify-before-cite (STRENGTHENED post-review — R3):** the review caught a **fabricated "verbatim"
  #547 quote** in the shipped draft — a stitched fragment presented as a single quotation. That is a
  verify-before-cite failure; the genuine text (`:272-273`, `:300`) is now in place and re-grepped, and
  the fabricated-quote correction is logged (correction log R3, §7 item 2). All remaining cites re-grepped
  at branch tip `117d9063` this session (Letter :446-454; #547 M0/M1/M2/M3 + §9 + :272-273/:300; p5
  standing-wave row; node-up:105 #558 supersession; `semiconductor-cv-dip`:179-188/:193/:200/:215;
  `CLAUDE.md`:73,75; `master-equation.md`:20; `substrate-native-terminology.md`:43; `operators.md`:54).
- **NO KB canonization / NO Letter edit from this lane** — research/ docs only; all consequences are gated
  follow-ons for Grant + the auditor lane.
