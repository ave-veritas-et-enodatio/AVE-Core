# F6 thermal-floor arm — revival-vs-floor — RESULT

**Date:** 2026-07-19 · **Re-banked post-review:** 2026-07-19 (adversarial review wf_932a21ad; 12 findings, 1 CRITICAL) · **Class:** result (the fire; honest closure, Rule 11 / Rule 12 — the STRONG pre-registered prediction is excluded ~5σ and the reactive-floor mechanism is structurally inexpressible; a mild partial is unconstrained; see §0 POST-FIRE CORRECTIONS).
**Prereg (FROZEN):** [`2026-07-19_f6-thermal-floor-arm_prereg_FROZEN.md`](2026-07-19_f6-thermal-floor-arm_prereg_FROZEN.md) — §0–§7 frozen-by-push `2026-07-19T22:31:10Z` (API committedDate), **BEFORE any arm driver code existed** (freeze margin ≈ **5.5 min**, push→first-arm-code).
**Driver:** `src/scripts/vol_1_foundations/f6_thermal_floor_arm.py` · **Data:** `2026-07-19_f6-thermal-floor-arm_result.json` · **Test:** `src/tests/test_f6_thermal_floor_arm.py`
**Instrument:** `src/ave/thermal/f6_bath_meter.py` (`LatticeBathCoupler` — **BYTE-UNTOUCHED**; floor = config-only). **Certificate consumed:** FLOOR-METER-VALID-BAND[0,5] (STAGE-1). **Plant:** standalone-K4, κ=0.030 MILD.

> ## 🔴 POST-FIRE CORRECTIONS — adversarial review wf_932a21ad (15 agents; 12 findings, 1 CRITICAL) · Rule-12 supersessions
>
> **The harshest review of the F6 program. The verdict SURVIVES only in reduced, honest form.** The frozen prereg §0–§7 body is UNTOUCHED; every correction below is a disclosure/relabel (Rule-11 — no gate retune except the one prescribed FB4 hardening in the floor-battery §Dp, disclosed). What changed:
>
> - **★R-1 (CRITICAL — a FABRICATED prereg quote, RETRACTED).** §6 (orig.) attributed to prereg §4 the verbatim phrase *"banked as a disclosed non-gating cross-check."* **That phrase EXISTS NOWHERE in the prereg** — verified by `grep` of `2026-07-19_f6-thermal-floor-arm_prereg_FROZEN.md` (0 hits; the terms `ens_avg_first` / `eaf` / `ensemble-average-first` are absent from the prereg entirely; the only "non-gating" string is §7's FD leg, an unrelated section). The `ens_avg_first` (**eaf**) coherent-revival metric was **INVENTED AT FIRE TIME — it is POST-HOC, not pre-registered.** Every "eaf" use in this doc is relabelled **POST-HOC (disclosed)**. The eaf keeps evidential value *as a disclosed post-hoc read* (it reproduces bit-for-bit; the review's own jackknife used it) — but it is **never pre-registered**, and no headline may lean on it as a frozen observable.
> - **R-2** the single-headline "NO-SUPPRESSION / empirical falsification" is **demoted**; the verdict re-forms to the three honest statements below. "Empirical falsification" is **not** the headline; the honest weight rests on statement (a) STRONG-form exclusion + statement (b) STRUCTURAL inexpressibility.
> - **R-3** the `floor/E0` column was inflated **~2.55×** (banked `e0=2.14143`; `e_floor/E0` at ρ=5 is **0.917**, not 2.33) — corrected in §2. The FB3 row "c>1 at ρ=5" contradicted the banked `c_range01_all=True` — corrected (the c>1 pump breach lives only at ρ=30 on the frozen-literal ladder).
> - **R-4** the cold-control "= banked #726 bit-for-bit" claim is **FALSE at value level** (arm ρ=0 revival `0.899` ≠ #726 `R_cum[10]=0.932`; different observables per the #726 R-1 history). Relabelled to what it actually establishes: **seeding is a no-op** (a ρ=0-seeded vs ρ=0-unseeded self-comparison — cannot fail), NOT cross-PR value reproduction.
> - **R-5** validity gate 5 (DETUNED-VALID) FAILED. Under the **frozen-literal** prereg reading (§4 L73) a gate-5 failure ⇒ **SUPPRESSION-NOT-TRACKING-ρ** (observable-cannot-separate); the shipped tree let NO-SUPPRESSION fire first. **Both readings now banked** (§D-post both-ways). Under R-2's re-form they converge (the frozen observable was degenerate; the post-hoc eaf carries the evidence).
> - **R-6** the categorical *"the floor and the signal never interact"* / *"cannot carry phase information … anyway"* are **FALSE** (the lattice ran `nonlinear=True` with op3) — corrected to the quantified honest form (§3): they interact **negligibly at MILD A≈0.10**; the flat eaf **despite** the raw amount-channel jitter is the measurement.
> - **R-7** small: walk clamp-moot bound (§4b), FB3 form-agreement relabelled checklist-not-gate, FB4 SEM/mean hardening + COV_CHAOS relabel (floor-battery §Dp), finding-4 both-ways sharpening.
>
> ## ★ VERDICT — three honest statements (the re-formed re-bank; frozen §4 tree returned NO-SUPPRESSION, banked as-is per Rule-11, disclosed DEGENERATE below)
>
> **(a) ★The STRONG walked hypothesis is EXCLUDED at ~5σ.** The frozen hypothesis (prereg §0/§4): *"revivals DIE as the floor rises past the signal."* The **POST-HOC** coherent revival (ensemble-average-first, jitter-cancelled over the 6 frozen seeds) at ρ=5 is **`0.923 ± 0.167`** (jackknife, N=6) — **5.5σ above zero**: at a 5× floor the coherent revival is *fully alive*, it does not die. It is flat across the whole certified band `ρ ∈ [0, 5]` (`0.899 → 0.903 → 0.908 → 0.915 → 0.920 → 0.923`, +2.7%). *(The weaker "≥halving" strong-form is excluded only at ~2.8σ — marginal, disclosed; a `die`-to-zero is the ~5σ claim.)* **Grant's ride-on-top alternative is corroborated at the STRONG-form level.**
>
> **(b) ★The reactive-floor arrow mechanism is STRUCTURALLY INEXPRESSIBLE on a non-interacting bath — identity-class corroboration (the #721-W2 shape).** The bath is a set of **INDEPENDENT harmonic oscillators** (Caldeira–Leggett; free rotation is per-mode, linear, no mode–mode coupling). "A static random floor sets the local arrow by dephasing a coherent revival" **requires** the floor to scramble the signal's phase, which needs an **interacting/mode-coupled/dissipative** channel — absent by construction in the Ax3 lossless-reactive regime. So the arrow-from-a-STATIC-REACTIVE-floor mechanism is **inexpressible on this junction — the same structural reason the #721 conservation was an ALGEBRAIC IDENTITY, not an empirical survival** (the #721-W2 relabel: "structurally guaranteed, not empirically discovered"). This is **identity-class corroboration**, promoted to the headline; the earlier "empirical falsification" framing is demoted. It does **not** falsify arrow-from-thermalization in an *interacting* bath (untested).
>
> **★ B2-SHARPENING (dated additive note, 2026-07-20; ratified-by-merge #742; KEEP-BOTH — the (b) sentence above is preserved verbatim, NOT rewritten).** The (b) mechanism sentence names **one** barrier — the bath's **independent-oscillator / no-mode–mode-coupling** structure (call it **B1**). The #742 interacting-bath derivation (`research/2026-07-19_arrow-candidate-interacting-bath_SPEC.md` §2.2) isolates a **SECOND, SEPARABLE barrier B2**: the meter's coupling is a **scalar collective-coordinate port + a global, spatially-uniform, phase-blind amplitude rescale** (`f6_bath_meter.py:198` κ·g degeneracy), so **even a fully interacting (Kerr-coupled) bath cannot imprint its phases on the signal through it** — B2 is a property of the **METER's PORT**, and it persists **regardless of the bath's internal coupling** (Caldeira–Leggett OR Kerr). *Precision (the #742 review note, §8):* this doc's own **:100** ("that channel carries no phase") **already engages** the phase-blindness — but bundles it into B1's *linear-regime / independent-oscillator* story; the genuinely NEW content #742 adds is the **SEPARABILITY** (B2 is a scalar-PORT property, not a bath-independence property, so lifting B1 alone does not suffice). No verdict changes — the two docs agree the arrow is inexpressible on the certified instrument; this note records that the mechanism sentence names one of *two* barriers. Surfaced, not adjudicated (implementer surfaces).
>
> **(c) ★A PARTIAL suppression ≤~30% at ρ=5 is UNCONSTRAINED — no statistical power.** With jackknife σ≈`0.167` on N=6 seeds, the 2σ-detectable eaf drop is **~37% of the cold revival** — so a partial suppression of **≤~30%** at ρ=5 **cannot be excluded**. Stated plainly: the STRONG form is dead, but the arm has no power to constrain a mild partial suppression. Not hidden, not spun as "no effect."
>
> **★The frozen §4 tree returned NO-SUPPRESSION — banked AS-IS (Rule-11), disclosed DEGENERATE (finding 6).** The frozen observable `S(ρ) = max(R̄_rev(primary) − R̄_rev(detuned), 0)` is **clipped to 0 across the entire interior** (`S = 0.024, 0, 0, 0, 0, 0.172` at ρ = 0…5): the detuned artifact residual exceeds the primary at every interior ρ, so the tree's `ratio5 = S5/S0 = 7.07 ≥ 0.80` verdict is **decided by the artifact residuals at the two endpoints**, not by an interior curve. The NO-SUPPRESSION output is byte-faithful (`self_check.match=True`) and is banked unchanged, but its verdict is an **artifact-direction accident**, not the honest evidence. The honest weight rests on (a)+(b); the frozen tree is a degenerate corroborator, disclosed.
>
> **NOT banked:** any FLOOR-ARROW / arrow-of-time-from-noise-floor result; nothing at emergence-class. **THE ARROW QUESTION STAYS OPEN** — the reactive-floor mechanism is closed *structurally* (b); interacting-bath thermalization and the X40-class *click* (boundary/topological minting) are the **open** arrow candidates (SPEC only). The depletion-rate rung (`Γ=3Hρ_latent`), the growth/node-genesis picture (re-homed), and the #721/#724/§D certificates are untouched. Meter module + K4 engine **byte-untouched** (floor = config-only).

---

## 1 · Sector header (result-time restatement)

- **Sector:** R7 thermal / entropy-sink (F6 ε→T2 candidate). **NOT** A1 mass, **NOT** Cosserat (2,3) winding/charge. **Regime:** Regime I sub-yield, `A_max≈0.10` MILD, κ=0.030; driven-then-source-off; closed cavity (pml=0). **Plant:** standalone-K4, conservation identity-enforced and audited green (`max drift = 2.3e-14 ≪ 1e-6`, no clamp).
- **The ruling under test (Grant verbatim, [sic]):** "my gut says its couples through a static noise floor" / "so wffectively constant" / "the noise floor woild set the arrow of time right?" — **tested**: the STRONG form (revivals DIE) is excluded at ~5σ (a), and the reactive-floor arrow mechanism is **structurally inexpressible** on the non-interacting bath (b); a mild ≤~30% partial is unconstrained (c).
- **Consistency-vs-emergence:** the STRONG FLOOR-ARROW **FORM** (revival dying with ρ) was **not observed** (excluded ~5σ) and the mechanism is **structurally inexpressible** (identity-class, the #721-W2 shape) — a **CONSISTENCY-class** result (STRONG-form negative + structural corroboration). Nothing at emergence-class. The frozen-tree NO-SUPPRESSION output is disclosed degenerate (§0); a partial suppression is unconstrained (§0c), so this is **not** a clean "no effect" claim.

---

## 2 · The fire (every §3 grid value run; revival-vs-ρ table)

Primary comb `Δω=0.050, M=15`; sparse `Δω=0.080, M=10`; detuned band `ω_min=1.18, q-frac=2.1e-3` (FROZEN `_place_detuned_band`). 6-seed ensemble; window `11·T_rec`.

🔴 **R-3 (floor/E0 corrected).** The prior `floor/E0` column {0.14, 0.47, 0.93, 1.40, 2.33} was inflated **~2.55×** (a fire-time transcription corruption). Banked values (`e_floor_expected/e0`, `e0=2.14143`) are below; the per-mode ρ framing (ρ = 5 ⇒ floor 5× **signal-per-mode**) is unchanged. The `eaf` column is **POST-HOC** (R-1), not a pre-registered observable.

| ρ | floor/E0 (banked) | **coherent revival (eaf, POST-HOC)** | raw `R̄_rev` (§4, artifact) | ±SEM | detuned `R̄_rev` (artifact) | detuned plateau (gated) | primary plateau | drift |
|---|---|---|---|---|---|---|---|---|
| 0.0 | 0.000 | **0.899** | 0.899 | 0.000 | 0.875 | 0.0096 ✅ | 0.193 | 7.7e-15 |
| 0.3 | 0.055 | **0.903** | 0.942 | 0.036 | 1.497 | 0.030 | 0.194 | 2.3e-14 |
| 1.0 | 0.18 | **0.908** | 1.032 | 0.084 | 1.455 | 0.053 | 0.224 | 1.8e-14 |
| 2.0 | 0.37 | **0.915** | 1.148 | 0.117 | 1.418 | 0.076 | 0.252 | 2.0e-14 |
| 3.0 | 0.55 | **0.920** | 1.255 | 0.137 | 1.366 | 0.098 | 0.275 | 1.6e-14 |
| 5.0 | 0.92 | **0.923** | 1.389 | 0.166 | 1.218 | 0.135 | 0.311 | 1.5e-14 |

**The load-bearing read is the coherent revival (eaf, POST-HOC): FLAT `~0.90` across ρ ∈ [0,5]** — at ρ=5 it is `0.923 ± 0.167` (jackknife, N=6), 5.5σ above zero (statement a). The raw `R̄_rev` column jitters and inflates (`0.90 → 1.39`, seed CoV ≈0.29 at ρ=5) — the floor DOES exchange energy (amount channel, §3); the coherent eaf stays flat *despite* that jitter, which is the measurement. The detuned column is the artifact-laden frozen §4 ingredient (disclosed); the `S(ρ)` built from it is degenerate (§0, finding 6).

| criterion (frozen §4) | threshold | measured | verdict |
|---|---|---|---|
| CONSERVATION (all cells) | `< 1e-6` | `2.3e-14` | ✅ |
| CLAMP-NEVER (all cells) | none clamp | none | ✅ (the band [0,5] holds) |
| COLD-CONTROL (ρ=0 seed no-op) 🔴R-4 | `diff = 0.0` | `0.0` | ✅ **seeding is a no-op** — a ρ=0-seeded vs ρ=0-unseeded self-comparison (cannot fail). It does **NOT** reproduce #726 bit-for-bit: arm ρ=0 revival `0.899` ≠ #726 `R_cum[10]=0.932` (different observables, #726 R-1 history). |
| DETUNED transfer-gated (ρ=0) | plateau `< 1e-2` | `0.0096` | ✅ (resonance-gating alive) |
| **gate 5** DETUNED-VALID (`R_rev(det) < 0.5·primary`) 🔴R-5 | pass | `0.875` (artifact) | ❌ **FAILED.** Frozen §4 L73: gate-5 fail ⇒ **SUPPRESSION-NOT-TRACKING-ρ** (observable-cannot-separate). The shipped tree let NO-SUPPRESSION fire first (precedence step 2 short-circuits before gate 5). **Both readings banked** (§0 R-5). The failure is the disclosed detuned-normalization blowup, not a physical resonance-gating loss. |
| `S(5)/S(0) ≥ 0.80` ⇒ NO-SUPPRESSION 🔴finding 6 | 0.80 | **7.07** | ⚠ **DEGENERATE.** `S(ρ)` clips to 0 across the whole interior (`0.024, 0, 0, 0, 0, 0.172`); `ratio5=7.07` is decided by the two-endpoint artifact residuals, not an interior curve. Banked as-is (Rule-11); the tree's verdict is an artifact-direction accident (§0). |
| coherent revival dies with ρ (eaf, POST-HOC) | eaf(5) → 0 | **`0.923 ± 0.167`** (5.5σ above 0) | ❌ STRONG "revivals die" EXCLUDED ~5σ (statement a) |

Frozen §4 tree → **NO-SUPPRESSION** (byte-faithful; `self_check.match=True`) — banked as-is per Rule-11, disclosed degenerate (§0). The honest verdict is the three statements (§0), not the tree label.

---

## 3 · The mechanism (Rule 11 — one root explains every cell)

**One root: the lossless-reactive bath has NO mode–mode coupling, so a static floor cannot DEPHASE a coherent revival.** Each bath oscillator free-rotates independently (`OscillatorBath.free_rotate` is diagonal in the mode basis). 🔴 **R-6 (finding 8 — categorical falsehoods corrected).** The floor and signal DO interact: the lattice ran `nonlinear=True` with `op3_bond_reflection=True`, and they share the collar + the amount-channel back-reaction (κ=0.03). The earlier "the floor and the signal never interact" / "cannot carry phase information … anyway" is **wrong as stated**. The honest quantified form: at MILD `A≈0.10` the floor–signal exchange is a **weak AMOUNT-channel** transfer that jitters the raw per-seed `R̄_rev` substantially (seed CoV ≈0.29, ensemble-mean `R̄_rev` inflates `0.90→1.39` over ρ) — the floor is emphatically *not* a spectator. What it does **negligibly** here is DEPHASE the coherent re-phasing: with no mode-coupling in the linear rotation, the shared amount-channel moves energy without imprinting the floor's random phases onto the signal's recurrence. When the excess ledger is ensemble-averaged, the amount jitter cancels (√6) and the coherent revival is exposed **flat at ~0.90 despite ~29% raw amount-jitter** — that flatness-despite-jitter is the measurement of "no phase-dephasing."

**Why Grant's picture fails HERE (regime-scoped, honest — the STRUCTURAL statement b).** "A noise floor sets the arrow" is a **thermalization** intuition — it requires the floor to *scramble* the signal's phase, which needs **interaction that carries phase** (mode coupling, nonlinearity, or dissipation). The Ax3 **lossless-reactive** regime this meter operates in is **linear and phase-non-mixing** by construction (the honest no-valve rail): the amount-channel exchanges energy but not phase. The arrow-from-a-STATIC-REACTIVE-floor mechanism is therefore **structurally inexpressible on this junction — the same reason the #721 conservation was an ALGEBRAIC IDENTITY** (the #721-W2 relabel; identity-class corroboration, not empirical falsification). This does **not** falsify arrow-from-thermalization in an *interacting* bath, nor the X40-class *click*; both are the open arrow candidates (§4).

**Regime read.** Not numerical (drift `2e-14`, no clamp). Not a dead coupling (the floor exchanges — raw amount-jitter grows with ρ, §2 + FD leg §5). The STRONG form is excluded ~5σ by the POST-HOC eaf (a) and the mechanism is structurally inexpressible (b); the frozen §4 tree's own NO-SUPPRESSION is a degenerate corroborator (§0, finding 6), and a partial ≤~30% is unconstrained (c). A **mechanism-named STRONG-form negative**, not a clean full-range "no effect."

---

## 4 · Disposition (Rule 12 — retract, do NOT refill)

- **Retract** the frozen **STRONG FLOOR-ARROW prediction** ("revivals DIE as the floor rises past the signal"). It is **excluded at ~5σ** (statement a); the reactive-floor arrow mechanism is **structurally inexpressible** on the non-interacting bath (statement b, identity-class). **Branch closed negative at the STRONG-form + structural level.** *Caveat (statement c):* a mild ≤~30% partial suppression at ρ=5 is **unconstrained** — the branch is closed against the walked mechanism, not against every conceivable weak partial. **THE ARROW QUESTION stays OPEN** (the reactive-floor route is closed structurally; the open candidates are below).
- **Do NOT refill the slot** (Rule 12 — retract, do not refill). The routed follow-ons are **SPEC only** (each earns its own charter + verification chain if pursued):
  1. **★The dephasing arrow needs an INTERACTING bath (SPEC; the mechanism this fire named).** A floor that dephases a coherent revival requires a **phase-carrying interaction** — mode–mode coupling / nonlinearity / genuine dissipation — absent in the lossless-reactive Caldeira–Leggett comb. This is a **meter/physics change** (breaks the #721 identity ⇒ full W/X/floor-battery re-cert) — SPEC, NOT built. Honest next home for "does thermalization set the local arrow?"
  2. **★The X40-class *click* (boundary/topological minting)** — the alternative one-way-at-an-event arrow (route-to-the-click, per the #726 kappa-sweep NO-ARROW branch and the registers-walk X40 thread). SPEC, not built; the second open arrow candidate.
  3. **The observable needs an interaction-robust revival metric** — the frozen §4 `S(ρ)` is degenerate (interior clips to 0, §0 finding 6); a future interacting-bath arm should **pre-register** the ensemble-average-first coherent-revival metric (the POST-HOC eaf here, which was well-behaved) as the gating observable — this time frozen, not invented at fire time.
- **Re-homed / MOOT (unchanged):** the growth/node-genesis picture stays at the cosmological rate rung; the DOS-balance A/B fork stays MOOT (bath-emptiness, not head-count).
- **Untouched:** depletion-rate rung; #721/#724/§D certificates; meter module + K4 engine (byte-untouched; floor = config-only).

---

## 4b · Reconciliation with the noise-floor-arrow walk-record (#731, on main; verify-before-cite)

The orchestrator's walk-record `research/2026-07-19_noise-floor-arrow-walk_RECORD.md` (merged to main via PR #731 while this lane was in flight; a WALK-RECORD, *"the arm adjudicates; NOTHING here is new canon"*) is the framing this arm was built to test. Reconciled (flag-don't-fix — surfaced, not silently smoothed):

- **✅ The arm fired the walk-record's OWN designated falsifier — at the STRONG-form level.** Walk §2: *"★ The arm's frozen classes include **NO-SUPPRESSION** (ride-on-top) as the **live falsifier**... if a populated floor does not suppress coherent returns, the 'dephasing-against-a-random-floor sets the arrow' mechanism is **dead**, and honest closure (Rule 11) applies."* The walk's central hypothesis (walk §1c-ii: *"THE LOCAL ARROW = coherent returns dephasing into the pre-occupied random background"*) is **dead at the STRONG form**: the coherent return does not die (excluded ~5σ, §0a) and the reactive-floor mechanism is structurally inexpressible (§0b). *(Honesty caveat: the frozen §4 tree's NO-SUPPRESSION label is degenerate, §0 finding 6, and a ≤~30% partial is unconstrained, §0c — so the walk's mechanism is dead, but not every weak partial is excluded.)* No framing conflict: the walk set the arm as adjudicator and named this outcome as the falsifier; the arm lands there.
- **✅ The mechanism is the SHARP form of the walk's own §1e precision note.** Walk §1e (PRECISION NOTE): *"In the **linear** engine, per-mode energies do not literally shift frequency... literal spectral downconversion requires the **nonlinear** channel... Conflating the two would overclaim a literal frequency shift the linear engine does not produce."* This arm makes that caveat decisive: in the lossless-reactive (linear, independent-oscillator) bath the coherent revival is **not dephased** — the floor and signal exchange energy via the amount channel (they *do* interact, `nonlinear=True`+op3, §3 R-6) but that channel carries **no phase**, so the "cross-mode phase decorrelation" the walk hoped would carry the arrow does **not** touch the coherent signal's re-phasing. Dephasing-that-sets-the-arrow needs the phase-carrying interacting/nonlinear channel the walk's §1e already flagged as the only literal one.
- **⚠ PARTIAL CONFLICT surfaced — walk §1c-i "the clamp becomes moot" is comb-dependent (STAGE-1 §D-post Dp-1).** Walk §1c-i: *"a pre-occupied floor changes the endpoint: the cavity relaxes **to the floor**, not to zero. The over-extraction **clamp** then becomes **moot**."* STAGE-1 found this is **only true on partial-transfer combs**: on the arm's `Δω=0.050` comb the floor DOES keep `E_lat` alive (clamp moot — the arm ran clamp-free across [0,5], consistent with the walk); but on the **near-/full-discharge** combs the floor's per-step jitter drives `E_lat→0` and fires the clamp **earlier** (the #727 wall from the floor side) — clamp **NOT** moot there. 🔴 **R-7 (finding 12 — bound corrected):** clamp-not-moot appears **already at `Δω=0.030`, ρ≥3** (STAGE-1 boundary comb: ρ=3 breaks, 2/6 seeds clamp), not only at `Δω≤0.020` as previously stated. The walk's clamp-moot intuition holds where the arm operates (`Δω=0.050`, [0,5]) and is falsified at `Δω≤0.030` for ρ≥3; the boundary is the FLOOR-METER-VALID-BAND. Both file paths surfaced for adjudication; not silently reconciled.
- **Owed to the auditor lane (implementer surfaces, auditor lands):** the walk-record's §2 falsifier is **fired at the STRONG-form level** (a: revival does not die, ~5σ; b: structural inexpressibility) — the bare "NO-SUPPRESSION" tree label is degenerate (§0 finding 6) and should not be the headline; the §8c "owed placement" pointer (walk KB-pointer section) should record the arm's adjudication (STRONG-form + structural; partial unconstrained) once #728 lands §8c on main; the walk §1c-i clamp-moot claim needs the comb-dependent qualifier (`Δω≤0.030`, ρ≥3). This lane does **not** edit the walk-record (a different lane owns it) nor draft the auditor's §8c entry.

---

## 5 · The FD leg (SECONDARY — non-gating; §7; NO claim minted)

The floor-injected fluctuation (ensemble seed-spread of `R_rev`, a fluctuation proxy) vs the relaxation rate (`t_fp / T_rec`, the excess-transfer 1/e time). **FD ratio = fluctuation / relaxation** per ρ (exploratory data routed to the ℏ-as-FD open; **excluded from the §4 verdict**):

| ρ | 0.0 | 0.3 | 1.0 | 2.0 | 3.0 | 5.0 |
|---|---|---|---|---|---|---|
| FD ratio | 0.000 | 0.051 | 0.088 | 0.092 | 0.107 | 0.129 |

The fluctuation proxy grows with ρ (more floor ⇒ more jitter) while the relaxation time is roughly ρ-stable, so the FD ratio rises smoothly with the floor level. **No fluctuation-dissipation theorem is asserted;** banked for the routed open only.

---

## 6 · Independent re-derivation + gates + provenance

- **Independent re-derivation from the raw banked JSON (the #726 F9 lesson — NOT the driver's own booleans):** `src/tests/test_f6_thermal_floor_arm.py` loads `…_result.json` and, using **only** the raw per-ρ ensemble fields, independently re-derives: (i) the POST-HOC coherent revival (eaf) is flat (`max − min ≤ 0.05`, non-decreasing) across ρ ∈ [0,5]; (ii) `S(5)/S(0) ≥ 0.80` ⇒ the frozen tree returns NO-SUPPRESSION (disclosed degenerate, §0 finding 6); (iii) every cell conserves (`< 1e-6`) with no clamp; (iv) ρ=0 seed no-op reproduces the ρ=0 unseeded revival bit-for-bit (a self-comparison — establishes seeding is a no-op, **not** #726 value reproduction, R-4); (v) the detuned transfer is gated (`plateau(ρ=0) < 1e-2`) while its `R_rev` is the disclosed artifact. It **also** runs a **live** ρ=0 and ρ=5 primary cell and asserts the coherent revival does not drop. The independent re-derivation reproduces the STRONG-form negative (revival flat, does not die).
- **Diagnostics provenance:** every number is driver-computed and banked in `…_result.json` — the ensemble `R̄_rev`/SEM per ρ/comb, the **POST-HOC** `ens_avg_first` coherent revival, `S(ρ)` primary + sparse, the detuned reads, per-cell drift/clamp, the cold-control (seed-no-op) diff, the **Rule-10 reactance pair** (bath C-state AND L-state across the window at ρ∈{0,1,5} on the primary comb), and the FD-leg rows. Nothing prose-only.
- **Gates:** `ruff check` clean; `make verify` green; the classifier self-check `match=True` (a precedence guard — the genuinely independent check is the test's raw-JSON + live re-derivation).
- 🔴 **Prereg deviations (DISCLOSED) — R-1 CRITICAL RETRACTION.** The frozen §4 `S(ρ)` observable is **partly artifactual off-plateau** and, worse, **degenerate** (interior clips to 0; §0 finding 6) — a fire-time finding disclosed, not silently fixed. **The `ens_avg_first` (eaf) coherent-revival metric was NOT pre-registered.** The retracted prior text claimed it was *"pre-registered as a non-gating cross-check (§4 'banked as a disclosed non-gating cross-check')"* — **that attribution is FABRICATED: the quoted phrase exists nowhere in the prereg, and the eaf metric is absent from the prereg entirely** (verified by grep; the only "non-gating" string in the prereg is §7's unrelated FD leg). The eaf was **INVENTED AT FIRE TIME — it is POST-HOC**. It retains evidential value as a disclosed post-hoc read (it reproduces bit-for-bit; the review's jackknife used it), but it is never a frozen observable and no headline leans on it as pre-registered. No frozen threshold was retuned (Rule 11); the one gate change (floor-battery FB4 SEM/mean hardening, §Dp) is a disclosed tightening.
- **Freeze margin (real):** prereg frozen-by-push `2026-07-19T22:31:10Z`, arm driver first written `~22:36:38Z` (≈5.5 min); the revival-vs-ρ prediction was UNMEASURED at freeze.

---

*Honest closure (Rule 11 / Rule 12), re-formed post-review (wf_932a21ad): the frozen **STRONG FLOOR-ARROW** prediction — "revivals DIE as the floor rises past the signal" — is **(a) excluded at ~5σ** (the POST-HOC ensemble-average-first coherent revival at ρ=5 is `0.923 ± 0.167`, jackknife N=6, 5.5σ above zero; flat `0.899 → 0.923` across the band), and **(b) the reactive-floor arrow mechanism is structurally inexpressible** on the non-interacting lossless-reactive bath — identity-class corroboration, the #721-W2 shape (an algebraic identity, not an empirical falsification). **(c) A mild ≤~30% partial suppression at ρ=5 is unconstrained** (no statistical power at N=6). The frozen §4 `S(ρ)` tree returned NO-SUPPRESSION and is banked as-is (Rule-11) but disclosed **degenerate** (interior clips to 0; verdict decided by artifact residuals) — the honest weight rests on (a)+(b), not the tree label. **Grant's ride-on-top alternative is corroborated at the STRONG-form level; the reactive-floor branch is closed structurally.** **THE ARROW QUESTION STAYS OPEN** — interacting-bath thermalization and the X40-class *click* are the open candidates (SPEC only, not built). The R-1 fabricated prereg attribution is retracted (§6). Nothing banked at emergence-class; the meter + engine are byte-untouched (floor = config-only).*
