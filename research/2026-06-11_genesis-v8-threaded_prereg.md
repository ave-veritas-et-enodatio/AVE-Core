# PREREG — Genesis-v8: THREAD THE BUBBLE (D16) + SPARE THE FEEDSTOCK (D17) + POLYPHASE CONDUCTION (D15) — the TRAVELING-WAVE winding test

**Date (frozen):** 2026-06-11
**Branch:** `analysis/2026-06-11-genesis-v8-threaded` (worktree `/tmp/ave-v8`, off `origin/analysis/2026-06-10-genesis-v6-transducer` @ `7484dd0b`; do NOT push/merge — the orchestrator PRs at the end only). Live `/tmp/ave-*` worktrees (annih, s11denovo, bubblephys + the matrix/response scouts) are NOT touched.
**Engine lineage (subclassed, inherited physics UNCHANGED by default):** `UnifiedGenesisEngine` (`src/ave/core/unified_genesis_engine.py`) ← `CrystalGraftV4` ← V3 ← V2 ← `CrystalEngine`. Every v8 engine addition is behind a NEW parameter that DEFAULTS to the v6/v7 byte-identical path (the inherited keeper ladder `src/tests/test_unified_transducer_v6.py` + `test_unified_snap_machine.py` + `test_unified_snap_machine_v6.py` must stay green — the D-INHERIT regression gate). The v8 keeper file is NEW: `src/tests/test_unified_threaded_v8.py`.
**Governing discipline:** `ave-apparatus-floor-attribution v1.1` — ORDERED BINS (floors §4 evaluated FIRST, gate every positive); PROBE-CAPABILITY keepers (§9, every discriminating probe validated on a known-different reference + a known-positive plant-at-scale INSIDE the geometry); every new knob inventoried + swept (§5). **§210-COMPLIANCE GATE: the run phase executes every sweep this prereg mandates, or states the deviation explicitly BEFORE running and re-bins. A positive whose governing knob was unswept is CLIP by this prereg's own law (the v7 demotion-(c) lesson: the mandated `ν_art` sweep was NEVER RUN — now structural, §5 row binding).**
**THE GATE-AS-EXECUTABLE-ASSERTION RULE (the v7 demotion-(h) lesson, now STRUCTURAL — §8):** EVERY gate in this prereg has an EXECUTABLE ASSERTION in the driver. A docstring gate is an automatic CLIP on whatever it gates (v7's single headline discriminator, F-NETFIELD, lived only in the `analyze()` docstring `genesis_v7_quadrature_run.py:388`, was never computed, and let the run report SURVIVES on a substituted criterion while the frozen gate FAILED). §8 maps every §4 floor + §6 bin to its driver assertion.
**THE GROSS-VS-FIELD RULE (the v6/v7 panel lesson, STRUCTURAL — §12):** a deposit/accumulator is never a measurement. Every transducer/threading-effect claim in v8 is the NET FIELD quantity (MAIN − OFF), measured ON the field, above its floor. The buckle contaminates RH−LH (v6 DEMOTION-2), so the clean increment contrast is **MAIN − OFF at the SAME handedness**.
**Skills fired at design time:** `substrate-native-check` (CP1/CP2/CP9/CP10 walked §1 — the polyphase deposit + the channel-aware snap rendering are per-cell BOUNDARY operations, NOT bulk EOM terms); `pre-test-physics-check` (the ONE plumber-physical question surfaced to Grant — §2, the axial→poloidal AM-routing-through-threaded-flux gut-check, carried forward + sharpened from the v7 §2.4/§8 UNRESOLVED flag, asked BEFORE freezing per Rule 16 strengthening); `phase-space-coordinate-check` (A46 — the winding is read in PHASE-SPACE: `w_pol` = the LC quadrature `arg((ω·d̂)+i(π_ω·d̂))` around the minor circle ψ; the deposit + the read torus are derived in MATCHING coordinates §3; interior contours r≥3 cells, plant-at-scale INSIDE the threaded geometry — the v7 A46 SPHERE-has-no-major-radius obstruction is the precise thing D16 sets out to FIX); `consistency-vs-emergence` (the plant-at-scale reads are CONSISTENCY/probe-capability; the de-novo assembled `w_pol≠0` is the EMERGENCE claim; T1 `E_V^cons≈12.9` is inherited MANIFESTATION — tagged §6/§11); `ave-representation-capability-check v1.1` incl. (C) conservation-by-channel + carrier/engine DOF-capability (the deposit must carry a TRAVELING quadrature — the standing pattern is DOF-incapable of a sustained winding; §3.2); `ave-conserved-vs-pumped` (the deposit DEPLETES the photon 1:1 and lands in a passive sink — never pumps standing-V §2/§3.2; the D17 feedstock-sparing must NOT reopen the v5 deflagration pump — the CASCADE keeper §4 F-CASCADE); `ave-driver-script-honesty` (every number in the result phase comes FROM the evolved field/JSON; the accumulator is logged, NEVER the headline §12); `verify-before-cite` (every v5/v6/v7 + engine anchor below grep/line/JSON-confirmed THIS session — §0); `flag-don't-fix` (the §2 AM-routing question and the v7-inherited demotions are SURFACED, not silently resolved); `ave-prereg v1.1` (the Step-3.5 dimensional subsection §3.5 is mandatory — the threading-threshold and the traveling-sustain claims are magnitude claims).

---

## 0. INHERITED VERDICTS (verbatim-class; verify-before-cite — every number re-confirmed against the v5/v6/v7 result docs + run JSON + engine source THIS session)

- **NOT-ELECTRON (the v5 panel) STANDS.** v8 does NOT reopen the electron claim. T1 is INHERITED (D-INHERIT), not re-claimed. v8 tests ONE new structural hypothesis the v7 closure named: that the winding fails because (1) there is no TORUS to wind around (the pocket is a genus-0 SPHERE) and (2) the deposited winding is a STANDING pattern that de-coheres. v8 threads a torus (D16), spares the feedstock that carves it (D17), and drives a TRAVELING winding (D15).
- **v6 = PARTIAL (`research/2026-06-10_genesis-v6-transducer_result.md` §1).** T1 (primary) CONVERGED: `E_V^cons` **11.70→12.91**, drift **0.86 %**, N-robust (12.86–12.88), stop-time-robust — **the v8 D-INHERIT / T1 regression baseline (the ~12.9-class mass that MUST survive every v8 arm).** The transducer SOLVED helicity→charge/AM (`L_transferred_omega = ∓0.539`, odd-fraction **1.000**, ∝χ̃, transducer-OFF structural-null, `E_absorbed ≥ 0`) but `w_pol ≡ 0` across the whole χ̃ {0,9e-4,0.005,0.02,0.08} and ω-recipient {0,0.5,1.0} ladders — the poloidal "3" never formed. D-PERM = MOTION-LOCKED PHYSICS (ν_art-invariant, deficit −0.0365); snap-channel = CLIP.
- **v7 = DEPOSIT-DRAINED-AGAIN (`research/2026-06-10_genesis-v7-quadrature_result.md` §11 🔴 addendum @ re-bin).** The quadrature-deposit + lock-survival arc closed DRAINED on the prereg's own frozen gap-to-accumulator gate (ISO 3.34 OOM, FULL 4.03 OOM — never cleared the ≲1-OOM survival threshold). Four load-bearing carry-forwards into v8:
  1. **CLOSURE-1 (no topological lock-in):** the deposited integer winding DE-COHERES **`3 → 1` within one LC quarter-period (~907 steps)** even with the deposit continuously live (`result §4` K3 series: `w_pol` = 0,2,1,1,1,1,1,1,1 at steps 0..3200). The deposit was a STANDING poloidal pattern; the isolated LC tank has no quantizer to protect it. The read TRACKS `q_dep` (reads back the DEPOSITED q ∈ {2,3,4}) — **consistency-class, NOT an emergent `(2,3)` attractor.** **v8's D15 answer: drive a TRAVELING wave, not a standing one.**
  2. **CLOSURE-2 (column + buckle dominance):** in the full assembly the deposit's poloidal content (`C_pol` MAIN `+4.1e-5`) is sub-dominant by ~2 OOM to the energized rotation column + the buckle's own ω (`C_pol` OFF `−4.3e-3`) → `w_pol ≡ 0` (the v6 outcome reproduced). **v8's D17 answer: stop ERASING the circulation feedstock at the wall (`u_adv[cm]=0.0`, `unified_genesis_engine.py:396`) so the winding has a live carrier inside the channel.**
  3. **A46 obstruction (the read coordinate):** the genesis pocket at the 3200-step stop is a **near-core SPHERE** (`ρ ∈ [0.71, 2.55]`, filled center, `pocket_cells = 0`) with **NO field-defined major radius** — so `pol_R` is a CHOICE and the default `pol_R ∝ N` read N-COLLAPSES (~7 OOM over N=28→56). **v8's D16 answer: thread the flux to OPEN a hole — a genus-1 torus with a real, field-derived major radius (the channel locus).**
  4. **PROCESS demotions (binding on the v8 driver design):** (h) a frozen gate that exists only as a DOCSTRING is not a gate → §8 makes every gate executable; (c) the mandated `ν_art` sweep was NEVER RUN (`nu_art` pinned at 5e-4) → §5 makes it binding; (d) the implemented deposit DEVIATED from the frozen definition — the rigid δL was still carried and a SEPARATE energy-budget-sourced decorative ripple was added, so the D13 hypothesis (route the photon's OWN δL into the winding) was **never tested** → v8's D15 is **D13-FAITHFUL keeper-pinned** (the deposited winding's amplitude IS the extracted photon δL, asserted by `K-DELTA-L-FAITHFUL`, §9).
- **Rule 12 (no slot refilled):** v8 is a NEW hypothesis with its OWN verification chain (this prereg + the run result + a NEW version number). It does NOT refill the v6 DEMOTED-PARTIAL or v7 DRAINED-AGAIN slots; the v5 NOT-ELECTRON / SNAP-LOCKED 🔴 demotions stand UNCHANGED.

---

## 0.1 THE V8 DESIGN (Grant-adjudicated 2026-06-10/11; recorded verbatim-class; FROZEN, Rule 11 — the design INTENT does not change post-run)

### D16 — THREAD THE BUBBLE (the superconducting-bubble step)
Build the v6 MAIN bubble (the T1-converging recipe §7). **The rotation column's axial flux must THREAD the shell.** The type-II expectation: the snap shell — a Meissner-class condensate (the snapped void EXPELS the medium, `rho_bar` held at floor, `unified_genesis_engine.py:386-396`) — cannot remain genus-0 around threading axial flux; Meissner-class expulsion concentrates the flux into a NORMAL (un-snapped) channel THROUGH the pocket = the hole = **ball → torus.**

**THE TOPOLOGY GATE (executable, REAL topology code — NOT an assertion):** measure the pocket's GENUS — a connected un-snapped channel through the shell along the axis, detected from the **`snap_mask` connectivity** (`scipy.ndimage.label`; the channel = a connected un-snapped tube spanning the shell's axial extent AND transversely encircled by snapped shell cells; the shell = the largest connected `snap_mask` component). Driver assertion `assert_topology()` (§8). **Bins:** THREADED (a connected un-snapped axial channel encircled by the shell) / NO-PENETRATION (the shell stays genus-0; the Meissner expectation FALSIFIED at this config — honest, sweep the drive M and shell thickness §5 BEFORE concluding) / SHELL-NEVER-FORMS (no connected snapped shell exists — the floor case).

### D17 — SPARE THE FEEDSTOCK (the look-inside formation-suppression finding)
The inherited snap ZEROES `u_adv` in snapped cells (`unified_genesis_engine.py:396` `self.u_adv[cm] = 0.0`) — **erasing the circulation feedstock at the wall** (the carrier the channel needs to wind, and the carrier the threading flux needs to carve the hole — D16 and D17 are coupled). Render alternatives, SWEPT as a mandated axis (§5):
- **(a) WALL-NORMAL-ONLY:** zero only the wall-normal component of `u_adv` (project out `û_n = ∇A/|∇A|`), PRESERVE the tangential circulation (the snap still kills the in/out shock but keeps the swirl).
- **(b) CHANNEL-CELLS-LIVE:** no zeroing in cells flagged CHANNEL by the D16 topology gate (the threaded channel stays live — the conducting tube the directive demands).
- **(c) INHERITED (the control):** `u_adv[cm]=0.0` exactly as v6 (the byte-identical path).

**THE CASCADE KEEPER (executable):** none of the renderings may reintroduce the v5 DEFLAGRATION — `E_V` bounded (< 10× F-EV) on the v5 cascade config, asserted by `assert_cascade_bounded()` (§8; F-CASCADE §4). ave-conserved-vs-pumped: sparing the feedstock must NOT reopen the runaway energy creation the D10 self-limiting snap closed.

### D15 — POLYPHASE CONDUCTION (Grant: "the superconducting tube must conduct")
`N_phase ≥ 2` injection sites around the threaded channel's POLOIDAL contour, phased in QUADRATURE, driving a **TRAVELING phase wave** — the rotating-field STATOR (the plumber-physical core: a single-phase winding makes a PULSATING/standing field that cannot start a motor; ≥2 phases in quadrature make a ROTATING/traveling field — v7 used one phase, it decohered; v8 uses polyphase, it should sustain). **The photon's helicity sets the phase SEQUENCE** (helicity-odd by construction — RH ⇒ +ψ travel, LH ⇒ −ψ travel; achiral ⇒ no sequence ⇒ structural null). **D13-FAITHFUL (keeper-pinned, the v7 demotion-(d) fix):** the extracted photon spin δL IS what is routed into the traveling deposit — the v7 deviation (a decorative energy-budget ripple separate from δL) must NOT recur (`K-DELTA-L-FAITHFUL`, §9). Conservation 1:1 (the AM ledger closes); boundary-local (CP10 — on the channel-wall poloidal contour only, a post-substep BC, no bulk EOM term).

### D18 — INSTRUMENTATION
- **Interior contour reads** (BETWEEN the core and the channel wall — r≥3 cells, plant-at-scale INSIDE the threaded geometry per the look-inside hygiene; external settled-average reads are the WRONG instrument for this object).
- **Phase-locked snapshot reads at the RESOLVED internal period** (resolve the oscillator FIRST — the look-inside N=32 lesson: FFT the channel-wall ω-tank L-state to get `T_travel`, THEN phase-lock the `w_pol` reads to it; a random-phase snapshot cannot distinguish a standing wave caught at peak from a traveling winding — the reactance-pair completeness rule at the traveling frequency).
- **The extractor known-positive planted INSIDE the threaded geometry at the run's own scale** (the field-derived torus from the D16 channel locus, r≥3, BEFORE any de-novo read).

---

## 1. SUBSTRATE-NATIVE CHECKPOINTS WALKED (before the first line of v8 numerical code)

- **CP1 (dynamical, no minimization):** UNCHANGED — FDTD leapfrog (V/w/ω) + RK2 (ρ̄/u) + the inherited helicity-conserving lock substep. No energy-basin descent. The v8 polyphase deposit (D15) and the channel-aware snap rendering (D17) are per-step BCs, not relaxations toward a minimum. The topology gate (D16) is a pure DIAGNOSTIC on `snap_mask` — it changes no dynamics; the channel-cell carve emerges from the inherited snap + the spared feedstock, NOT an algebraic insert (CP9).
- **CP2 / representation-capability (C) conservation-by-channel — the load-bearing check.** The ω-sector LC reactance pair is `(ω, π_ω≡∂_tω)`. The poloidal "3" is a winding of the LC phase `arg(Z)`, `Z=(ω·d̂)+i(π_ω·d̂)`, around ψ — a property of the (C-state, L-state) QUADRATURE. **A STANDING pattern (ω and π_ω IN PHASE, oscillating together) has `arg(Z)` non-winding ⇒ w_pol=0 capability — this is the v7 failure mode and the carrier/engine DOF-capability check: a standing deposit is DOF-INCAPABLE of a sustained winding.** The v8 D15 deposit imprints a genuine TRAVELING quadrature (ω ∝ cos(qψ−Ωt), π_ω ∝ the advancing quadrature partner) on the channel-wall poloidal contour, so `arg(Z)` winds AND sustains. The energy ledger is TRACKED (passive lossy mirror `E_absorb ≥ 0`), never assumed conservative.
- **CP9 (dynamical integration, not algebraic):** ρ̄ integrated by continuity; the bulk deficit + the snap shell EMERGE. The THREADED CHANNEL must EMERGE from the threading flux + the spared feedstock acting on the inherited snap (the type-II expulsion) — it is NOT algebraically carved (a hand-carved channel would VOID D16). Whether the traveling deposit self-organizes into a quantized de-novo `(2,3)` is the open WINDING-TAKES question — it must EMERGE from the LC dynamics on the threaded torus, not be read-back from the plant (the consistency-vs-emergence split, §6).
- **CP10 (boundary, not bulk) — THE constraint that detonated every v5 bulk-coupling architecture.** The D15 polyphase deposit acts ONLY on the channel-wall poloidal contour (interior-masked, derived from the D16 channel locus), a post-substep BC, NOT a term added to any field's acceleration/EOM — no bulk trilinear potential (the indefinite-Hamiltonian pump that detonated `photon_deplete=True` cannot recur). The D17 renderings are per-cell snap-state operations (the same boundary state machine the inherited snap is), NOT bulk forces. The D16 gate is read-only.

---

## 2. THE ONE PLUMBER-PHYSICAL QUESTION (pre-test-physics-check; SURFACED to Grant BEFORE freezing, Rule 16 strengthening — flag-don't-fix; carried forward + SHARPENED from the v7 §2.4/§8 UNRESOLVED AM-conservation flag)

> **A polyphase rotating-field stator wrapped on the threaded channel — driven by the photon's AXIAL spin δL — sustains a TRAVELING POLOIDAL wave (winding in ψ). The photon supplies net AXIAL (toroidal-direction) angular momentum; a poloidal traveling wave carries net POLOIDAL momentum. The threading flux (D16) through the torus hole is the geometric LINK between the two (toroidal flux × poloidal circulation = the linking number = helicity). The question: is routing the photon's axial δL into a poloidal traveling wave a PHYSICAL linking-number transfer enabled by the threading (the flux through the hole IS what lets axial AM become poloidal winding — and the linking number IS the `(2,3)`), or does enforcing BOTH "route the photon's OWN δL (D13-FAITHFUL)" AND "a poloidal traveling wave" over-constrain the same way v7's §2.4 did (axial δL vs zero-net-axial-AM poloidal mode)?**
>
> The honest tension: in v7 the resolution was to keep the rigid axial component carrying ALL the δL (lock-drained) and add a SEPARATE poloidal ripple — which broke D13 fidelity (demotion-d) and read consistency-class. v8's claim is that **the threading geometry removes the over-constraint**: with a hole threaded, the axial flux and the poloidal circulation are LINKED (a force-free / Beltrami-class A∥B state — the very state `_lock_relax` relaxes toward, `crystal_graft_v4.py:203`), so the photon's axial AM and the poloidal winding are not independent channels fighting over δL — they are ONE linked object whose linking number the photon's helicity SETS.
>
> **The design does NOT pre-resolve it — it brackets it as the `N_phase` sweep + the traveling-vs-standing discriminator + the threading gate, and lets the empirics + Grant adjudicate:**
> - If WINDING-TAKES fires ONLY when THREADED (genus-1) AND polyphase-traveling (N_phase≥2), with the winding helicity-odd and persisting P1, the linking-number picture is supported (the threading IS the enabling link).
> - If the winding fires THREADED-NO-WINDING (the hole opens but the phase won't circulate), the linking is geometric-only and the routing is still blocked — name the missing closure (A44, engine coupling-family — NOT an axiom).
> - If it fires even UN-threaded or even STANDING, the read is a floor-grazer / geometric artifact (the v6/v7 false-positive class) — CLIP.
>
> **Surfaced for Grant's adjudication (the framing source of truth); NOT self-resolved.** The WINDING-TAKES bin (§6) is ROBUST to the answer — it requires de-novo w_pol≠0, helicity-odd, threaded-and-traveling-gated, OFF/standing/achiral-absent, knob-robust, P1-persistent, REGARDLESS of the linking interpretation.

---

## 3. THE DEPOSIT + TOPOLOGY DERIVATION PLAN (phase-space-coordinate-check A46: derived in the SAME coordinate `w_pol` is read in; interior contours r≥3, plant-at-scale INSIDE the geometry)

### 3.1 The coordinate the winding is READ in (the target — verify-before-cite, `src/ave/utils/fast_winding_extractor.py`)
The poloidal "3" is the winding of `arg(Z)`, `Z=(ω·d̂)+i·(π_ω·d̂)`, around the MINOR circle ψ = arctan2(z, ρ−R) at major radius R, minor radius r; `d̂` = the principal transverse axis from the ω covariance; amplitude-weighted, modal-voted over `n_walks=12` toroidal starts, reliability gate `w_pol_rel > 0.1`, ≥16 valid contour samples, r-radius ≥ 3 cells (F0b). **THE v8 FIX OF THE A46 OBSTRUCTION:** v7's pocket was a SPHERE with no field-defined R (so `pol_R` was a choice that N-collapsed). D16 OPENS a hole → the threaded channel locus (from `snap_mask` connectivity) DEFINES R = the channel's transverse radius and the minor circle = the shell wall around it. **The read torus is now FIELD-DERIVED, not a choice** (`derive_read_torus_from_channel()`, §8) — the central improvement that makes the de-novo read decidable rather than coordinate-limited. Robustness is still swept (`pol_R`, `pol_r` §5 "contour radii").

### 3.2 The traveling-wave template (representation-capability (C): a TRAVELING quadrature, NOT standing, NOT rigid, NOT random — the carrier/engine DOF-capability lens)
The canonical (p,q) ω-knot the extractor reads as (w_tor=p, w_pol=q) winds the (C,L) pair `Θ=q·ψ` around the minor circle (`crystal_graft_v4.py:296-324 seed_omega_known_2_3`). **The v7 deposit imprinted this `q·ψ` structure as a STANDING snapshot — read q at t=0, then de-cohered (the C-state and L-state were not maintained in advancing quadrature).** The v8 D15 deposit is a TRAVELING wave around ψ, supplied by `N_phase` injection sites in quadrature:
```
ê_pol(r)         = the poloidal unit director on the channel-wall contour (tangent to the minor circle, in the d̂-plane)
ψ_loc(r)         = the poloidal angle about the FIELD-DERIVED channel axis (D16 locus)
δπ_ω(r, t) = A_dep · g_chan(r) · ê_pol(r) · cos( q·ψ_loc(r) − s_h·Ω·t )      # TRAVELING: phase advances with t, sequence set by handedness s_h=±1
```
realized as `N_phase` sites at `ψ_k = 2πk/N_phase`, each injected with phase offset `s_h·Ω·t` so the superposition is the traveling wave (the rotating-stator construction; `N_phase ≥ 2` is the minimum for ANY traveling wave, `N_phase ≥ 2q` to imprint a q-fold winding cleanly — the spatial-Nyquist constraint §3.5). `A_dep` is fixed by the 1:1 AM-ledger closure to the extracted photon δL (D13-FAITHFUL — `A_dep ∝ δL`, NOT an independent source; `K-DELTA-L-FAITHFUL` §9). `g_chan` = the channel-wall window (the D16 locus, interior-masked — CP10). **Why this should sustain where v7 decohered:** the traveling wave continuously re-imprints the advancing quadrature at rate Ω, faster than the LC dephasing unwinds it (§3.5(2)); the standing control (N_phase=1) reproduces v7 and must read w_pol=0 (the discriminator).

### 3.3 The topology gate (D16 — REAL code on `snap_mask`, NOT an assertion; the executable plan)
`snap_mask` (`unified_genesis_engine.py:183`, set at `:347`, the void-shell cells) is the substrate. Plan for `measure_genus()` / `assert_topology()` (§8):
1. **Shell:** the largest connected component of `snap_mask` (`scipy.ndimage.label`, 6-connectivity). SHELL-NEVER-FORMS if its cell count < F-SHELL (§4).
2. **Channel candidate:** the un-snapped interior cells (`~snap_mask & interior_mask`) forming a connected component that (i) SPANS the shell's axial extent `[z_min, z_max]` along n̂ AND (ii) is TRANSVERSELY ENCIRCLED by shell cells (in each z-plane within the shell extent, the candidate is enclosed by `snap_mask` in the ρ direction). A connected un-snapped tube through the shell = the THREADED channel (genus-1).
3. **Genus / Euler check (the topology number):** the Euler characteristic of the shell component (`χ = β0 − β1 + β2`) via the 3D cell-complex count or the cross-sectional ring test (a z-plane slice through a threaded torus shell is an ANNULUS — `snap_mask` ring enclosing an un-snapped hole enclosing nothing; a sphere-shell slice is a DISK or filled). Bins THREADED / NO-PENETRATION from this number. The CHANNEL cells (for D17(b)) are exactly the candidate-component cells.

### 3.5 EXPLICIT DIMENSIONAL ANALYSIS (ave-prereg v1.1 Step-3.5 — MANDATORY: the threading-threshold §D16 and the traveling-sustain §D15 are magnitude claims)

**Dimensional ingredients (canonical, probed live from the assembled engine this session; verify-before-cite against the v6 driver `genesis_v6_transducer_run.py` + the v7 prereg §3.5 table):**

| primitive | symbol | canonical value | role |
|---|---|---|---|
| node spacing | `dx`=ℓ_node | **1.0** | lattice unit |
| CFL timestep | `dt` | **1.732e-3** (=0.30·dx/(c_eff·√3)) | integrator step |
| shear/ω speed | `c_ω` | **0.5477** (=√(3/10)) | micro-rotation |
| ω-tank mass-gap | `ω_0`=`omega_gap` | **1.0** | LC restoring frequency |
| lock fraction | `η`=`lock_eta` | **0.08** (swept {0,0.05,0.08,0.12}) | per-step rigid-rotation removal |
| drive amplitude | `M` | **1.8** (`M_MAIN`, swept §5) | the rotation-column / threading-flux source |
| wall shell center | `R_II`=`wall_center` | **0.866** (=√3/2, the Γ=−1 front) | shell/channel locus (A-units) |
| seed sigma | `σ` | **4.0–5.0** | sets the pocket scale (~real-space ρ≤2.5) |
| extraction fraction | `χ̃`=`chi_exch` | **0.02** (swept {0,9e-4,…,0.08}) | per-step wall spin-extraction |

**(1) The threaded channel must clear the read floor (the geometry is decidable):** the v6/v7 pocket sits at real-space `ρ ∈ [0.71, 2.55]` (N-independent, set by σ). A threaded channel through it has transverse radius `r_chan` set by where the axial flux exceeds the snap shell's local restoring (the type-II penetration depth analog). For the read torus to clear r ≥ 3 cells, the minor circle `r ≈ R_II/dx · N`-scaled must span ≥3 cells AND the channel must be ≥1–2 cells wide. **At N=48 the shell is ~6–8 cells across the pocket diameter; a 2–3-cell channel + a 3-cell minor circle is marginal — so N is a MANDATED sweep {40,48,56} (§5), and the geometry-decidability is itself binned (SHELL-NEVER-FORMS / r-floor-VOID).** (Falsifier: if no N in {40,48,56} yields a channel + a minor circle both clearing their floors, the object is under-resolved and the winding read is VOID — an honest UNRESOLVED, not a negative.)

**(2) The traveling wave must out-run the LC dephasing (it sustains where v7 decohered):** v7's STANDING deposit decohered `3→1` in **~907 steps** (= one LC quarter-period `(π/2)/(ω_0·dt) = (π/2)/1.732e-3 ≈ 907`). A TRAVELING wave re-imprints the advancing quadrature at the stator frequency Ω. **For sustain, the re-imprint period `T_travel = 2π/Ω` must be ⟪ the dephasing time ~907 steps** ⇒ `Ω·dt ≫ ω_0·dt·(1/q)`-class. With Ω anchored to the ω-tank natural rate `Ω ≈ ω_0` (the resonant stator — the deposit drives at the LC frequency it lives in), `T_travel = 2π/(ω_0·dt) ≈ 3628 steps` is TOO SLOW (slower than dephasing) — so **the stator must be driven at Ω = q·ω_0 or faster (sub-907-step re-imprint), OR the per-step deposit must dominate the per-step dephasing locally.** This is the load-bearing magnitude claim: `Ω` (equivalently the per-step phase advance `Ω·dt` and the deposit amplitude `A_dep` vs the dephasing rate) is a MANDATED structural knob, swept, and the SUSTAIN is measured directly (the w_pol time-series under continuous drive — does it hold q, or decohere like v7?). **Frozen prediction: a traveling deposit with `T_travel ≲ 450 steps` (≲ ½ quarter-period) sustains w_pol=q through the build; one with `T_travel ≳ 907` decoheres like v7's standing pattern** — and that crossover IS the traveling-vs-standing physics (a sweep, §5 `Omega_stator`).

**(3) The threading threshold (D16 type-II expulsion):** the shell expels flux when the threading axial AM density exceeds the snap restoring. The rotation-column axial flux scales with the drive `M` and the ω-axial AM `L_ω,axial` (v6: the column dominated, `core_sense_u = 20.0`). The snap restoring scales with the void depth `(rho_cav − rho_bar)` and the Meissner hardening. **Frozen prediction: below a threshold drive `M_c`, NO-PENETRATION (the column threads but the shell holds genus-0); above `M_c`, THREADED (the column carves a channel).** `M` and `meissner_harden` (shell thickness/hardness) are MANDATED sweeps (§5) — the NO-PENETRATION bin is HONEST only after both are swept (the directive). (Power-counting: `M_c ~ √(snap_restoring/column_inertia)`; the v6 `M=1.8` is the single tested point — v8 brackets it {1.2, 1.8, 2.5, 3.5}.)

**(4) Above the floor, decidable:** the F-EXCHANGE structural floor is ~5.3e-18 (v6); any de-novo w_pol content rides ~12 OOM above it, so the WINDING question is NOT floor-limited — it is decided by the topology gate + the traveling-sustain + the helicity-oddness, NOT by magnitude (gross-vs-field §12: the headline is the de-novo w_pol read + the threaded/traveling gating, NEVER the accumulator).

---

## 4. FLOORS (ORDERED BINS — floor-check FIRST, gates every positive; ave-apparatus-floor-attribution v1.1; RE-MEASURED at the v8 run config by their own known-null run BEFORE any binning — a floor carried over from a different config is invalid. EVERY floor has an executable driver assertion §8.)

- **F-T1 (the D-INHERIT / T1 REGRESSION gate — evaluated FIRST OF ALL, IN EVERY ARM).** The inherited converged mass `E_V^cons ≈ 12.9`, drift < 2 %-class, N-robust, stop-time-robust (v6 baseline 11.70→12.91, drift 0.86 %). **GATE: every v8 arm — MAIN and EVERY control — must keep T1 converged; an arm that breaks T1 is binned T1-BROKEN (§6), and its topology/winding/persistence reads are VOID (a winding on a detonating object carries no weight — the v5/v6/v7 caveat). T1-BROKEN is a BIN, not a tweak target — do NOT tune the D17 rendering or N_phase to rescue T1 (Rule 11).** Driver: `assert_t1_regression()`.
- **F-SHELL (the topology pre-floor — gates D16).** The snap shell must FORM: a connected `snap_mask` component of ≥ F-SHELL cells (re-measured on the MAIN build at the snap onset). **GATE: no shell ⇒ SHELL-NEVER-FORMS (§6); the topology gate is VOID (nothing to thread).** Driver: `assert_shell_forms()`.
- **F-GENUS (the topology gate itself — D16, REAL `snap_mask` connectivity code, NOT an assertion).** The genus/channel measure of §3.3. **GATE: THREADED requires a connected un-snapped axial channel encircled by the shell, robust across the N sweep (a one-N channel that vanishes at N±1 is under-resolved → r-floor-VOID, UNRESOLVED, not THREADED).** Driver: `assert_topology()` returning {THREADED, NO-PENETRATION, SHELL-NEVER-FORMS}.
- **F-WPOL (the extractor floor + known-positive INSIDE the geometry — gates every WINDING-TAKES read).** (a) structural: `w_pol_rel > 0.1` AND r-radius ≥ 3 cells AND ≥16 valid contour samples, on the FIELD-DERIVED read torus (§3.1). (b) KNOWN-POSITIVE plant-at-scale INSIDE the threaded geometry (§9 `K-PLANT-IN-CHANNEL`): the extractor returns `w_pol = q_dep` on the v8 traveling deposit planted at the run's OWN scale into the threaded channel BEFORE the de-novo read; a de-novo read on an extractor not shown known-positive at scale inside the geometry is UNRESOLVED. Driver: `assert_wpol_floor()` + `assert_plant_in_channel()`.
- **F-TRAVEL (the TRAVELING-vs-STANDING discriminator floor — the heart of v8; PROBE-CAPABILITY keeper).** The extractor's `w_pol` must DISTINGUISH a planted TRAVELING quadrature from a planted STANDING pattern: a STANDING plant (ω, π_ω IN PHASE) reads `w_pol = 0`; a TRAVELING plant (ω, π_ω in advancing quadrature, `cos(qψ−Ωt)`) reads `w_pol = q`. **GATE: a probe that reads w_pol≠0 on the standing plant is reading amplitude geometry, not a winding — DISQUALIFIED, the WINDING-TAKES verdict is CLIP.** Driver: `assert_travel_vs_standing()` (`K-TRAVEL-VS-STANDING` §9). This is the executable form of the directive's keeper: "a planted STANDING pattern must read w_pol=0, a planted TRAVELING pattern must read its integer."
- **F-NETFIELD (the GROSS-VS-FIELD floor — gates every threading/deposit-effect read; structural §12).** Net field = MAIN − OFF at the SAME handedness (the D9-increment isolation; the buckle contaminates RH−LH — v6 DEMOTION-2). **GATE: every threading/winding effect is the net MAIN−OFF field quantity, above F-NETFIELD; the accumulator is logged, NEVER the headline.** Driver: `assert_net_field()` (computed, asserted — NOT a docstring; the v7 demotion-h fix).
- **F-CASCADE (the D17 deflagration containment — the conserved-vs-pumped keeper).** `E_V` bounded < 10× F-EV on the v5 cascade config, for EVERY D17 rendering (a, b, c). **GATE: a D17 rendering that reopens the v5 deflagration is DISQUALIFIED (the feedstock-sparing must not pump).** Driver: `assert_cascade_bounded()`.
- **F-EXCHANGE / F-ACHIRAL (the transducer structural nulls).** `chi_exch=0` deposit ≡ structural zero (v6: 5.3e-18); `helicity=0` (achiral) traveling deposit ≡ 0 (no phase sequence). Every positive gated ≥ 100× F-EXCHANGE; re-confirmed, not re-measured. Driver: `assert_structural_nulls()`.
- **F-CLOSE (the D11 pump pre-gate, inherited).** The MAIN-config drive-off `H_total^cons` must show NO positive excursion above F-CLOSE (re-measured at the v8 config); else the genesis arm does NOT run (honest block). Driver: `assert_pump_closed()`.

---

## 5. THE MANDATED-SWEEP LIST (§210-COMPLIANCE GATE — every knob the bins depend on, EXPLICITLY ENUMERATED; FROZEN. The run executes EVERY sweep, OR states the deviation explicitly BEFORE running and re-bins. A positive whose governing knob was unswept is CLIP by this prereg's own law — the v7 demotion-(c) lesson, now STRUCTURAL: `ν_art` row 8 is BINDING and will be ASSERTED present in the swept axes by the driver.)

**The v8-specific sweeps (the new physics — the directive-named axes: N_phase, the D17 rendering axis, drive M, shell thickness, χ̃, the contour radii):**

| # | knob | swept grid | which bin it gates | CLIP / falsifier telltale |
|---|---|---|---|---|
| 1 | **`N_phase`** (polyphase count; the traveling-vs-standing axis) | {**1 (STANDING control = v7 reproduction)**, 2, 3, 4, 6} | WINDING-TAKES vs THREADED-NO-WINDING; the whole v8 thesis | a w_pol≠0 at N_phase=1 (standing) is a floor-grazer/geometric, NOT the traveling winding (must be ABSENT at N_phase=1, PRESENT at N_phase≥2 and ≥2·q_dep) |
| 2 | **D17 rendering** `snap_u_mode` ∈ {(a) wall-normal-only, (b) channel-cells-live, (c) inherited} | all three | THREADED-NO-WINDING (does the spared feedstock let the winding carry?) + F-CASCADE | the winding tracks the rendering AND a rendering reopens the cascade (F-CASCADE fail ⇒ that rendering DISQUALIFIED) |
| 3 | **drive `M`** (rotation-column / threading-flux strength) | {1.2, 1.8 (v6 default), 2.5, 3.5} | THREADED vs NO-PENETRATION (the §3.5(3) threading threshold `M_c`) | NO-PENETRATION is honest ONLY after this sweep (the Meissner expectation falsified at a config, not globally) |
| 4 | **shell thickness** `meissner_harden` (the type-II hardness) | {0.0, 0.02, 0.05 (default), 0.10} | THREADED vs NO-PENETRATION + F-CASCADE | the threading tracks shell hardness (expected — it is the type-II control; reported) |
| 5 | **`Omega_stator`** (traveling re-imprint frequency; the §3.5(2) sustain) | {q·ω_0, 2q·ω_0, ω_0 (= ~907-step, the decohere control)} | WINDING-TAKES (sustain) vs THREADED-NO-WINDING (decohere) | the winding sustains only at fast Ω and decoheres at Ω=ω_0 like v7 (the traveling-sustain physics — a sweep, not a tuning) |
| 6 | **`chi_exch`** (χ̃ extraction) | {0, 9e-4 (κ̃-anchored), 0.005, 0.02 (default), 0.08} | the coupling magnitude; net-field ∝ structure | the VERDICT (sign/oddness/null/winding) tracks χ̃ — it must NOT; only the MAGNITUDE may scale (§210 invariance) |
| 7 | **contour radii** `pol_R`, `pol_r` (the read torus, field-derived but robustness-swept) | `pol_R` ∈ {channel-derived, ±1 cell}; `pol_r` ∈ {3, 4, 5} | F-WPOL read robustness (the A46 carry-forward) | the w_pol read tracks `pol_R/pol_r` (must be invariant once field-derived — the v7 N-collapse must NOT recur on the threaded torus) |
| 8 | **`ν_art`** artificial viscosity (BINDING — the v7 demotion-c fix) | {1e-4, 5e-4, 1e-3, 2e-3, 5e-3} (50× span) | D-PERM / the P1 motion-lock / the non-lock-sink level | the winding/persistence tracks ν_art ⇒ apparatus; plateau as ν_art→0 ⇒ physics. **DRIVER ASSERTS this axis is PRESENT in the swept keys (the v7 docstring-gate / pinned-knob failure must not recur).** |
| 9 | **`q_dep`** (target poloidal order) | {2, 3 (default), 4} | WINDING-TAKES (de-novo must track DESIGNED q ⇒ reading the deposit; an emergent (2,3) independent of q_dep ⇒ emergence) | de-novo w_pol does not track q_dep AND is not a fixed (2,3) ⇒ floor-grazer |
| 10 | **`N` resolution** | {40, 48 (primary), 56} | F-SHELL / F-GENUS / F-WPOL (the under-resolved channel/torus, §3.5(1)) | the topology or w_pol verdict tracks N (under-resolved) — the headline must be N-robust within the floor |
| 11 | **K3 stop-time** `n_build`/`n_persist` | build {2400, 3200, 4000}; persist {300, 600, 1200} | T1 convergence + the traveling-sustain time-series + snap onset | the "converged" mass or the w_pol read tracks when the run STOPS |
| 12 | **`lock_eta`** (η) | {0, 0.05, 0.08 (default), 0.12} | T3 spin η-invariance (carried to §11 IF WINDING-TAKES) | a surviving winding tracks η ⇒ it leaks net-L into the drain (a sink) |
| 13 | **`omega_recipient_frac`** (f_ω) | {0, 0.5, 1.0} | the ω carrier the winding lives in | w_pol≡0 across f_ω ⇒ residual robust to ω-wiring (v6/v7 carry) |
| 14 | **K4 seed `frac`** (saturation depth) | {0.30, 0.60, 0.85 (default), 0.95} | the regime gate (a winding only at shallow frac = sub-saturation artifact) | w_pol≠0 ONLY at shallow frac |

**§210 DEVIATION POLICY (the v5/v6/v7 lesson, structural):** the per-bounce metric is degenerate at the CFL dt (the v6/v7 honest deviation — continuous spin-drain, not ballistic bounces); the v8 headline is the de-novo w_pol on the threaded torus + the topology gate + the traveling-sustain time-series, NOT a per-bounce count. Any further deviation is stated BEFORE the run and re-binned. **The `ν_art` sweep (row 8) is NON-NEGOTIABLE — its omission is exactly the v7 demotion-(c) that re-binned that result; the driver asserts its presence.**

---

## 6. THE FROZEN BINS (ORDERED, floors first; Rule 11 — no post-hoc criterion drop, no debugging toward a rescue, no dropping an adjudication criterion to convert ❌→✅; consistency-vs-emergence TAGGED)

**The floor gate (evaluated FIRST, before any positive bin): F-T1 (regression, every arm) → F-SHELL → F-GENUS (topology) → F-WPOL (extractor floor + known-positive-in-channel) → F-TRAVEL (traveling-vs-standing discriminator) → F-NETFIELD (gross-vs-field) → F-CASCADE → F-EXCHANGE / F-ACHIRAL → F-CLOSE.** A signal below its own calibrated floor → UNRESOLVED. A positive that TRACKS a §5 knob (other than the designed control axes N_phase / q_dep / magnitude) → CLIP, named by the knob.

- **SHELL-NEVER-FORMS** (floor): the snap shell does not form (F-SHELL fail) — there is nothing to thread. Honest floor result; report the snap-onset/config and close (a different snap recipe is a new prereg).
- **NO-PENETRATION**: the shell forms (genus-0) but the threading flux does NOT open a channel — the Meissner/type-II expectation FALSIFIED at the swept configs. **HONEST ONLY after the drive-M (§5 row 3) and shell-thickness (row 4) sweeps** (the directive: sweep before concluding). Name it: the type-II expulsion did not carve a normal channel in this substrate — a clean negative on the topology hypothesis (A44, engine geometry — NOT a missing axiom).
- **THREADED-NO-WINDING**: the geometry OPENS (genus-1 channel, F-GENUS THREADED) but the phase does NOT circulate (w_pol stays 0 / floor-grazing de novo despite the polyphase traveling drive). **LOCALIZE the missing closure** (candidate: the traveling wave decoheres faster than it imprints — the §3.5(2) sustain failed; or the channel carrier is still swamped by the column despite D17; or the LC tank still has no quantizer) — name it (A44 refined; do NOT auto-draft an Ax-5).
- **WINDING-TAKES** (the EMERGENCE claim) iff ALL: **de-novo `w_pol ≠ 0`** (the de-novo read on the assembled threaded object, NOT a planted seed) AND above the extractor floor (F-WPOL: rel > 0.1, r ≥ 3, known-positive-in-channel-calibrated at scale) AND **THREADED-GATED** (present only when F-GENUS = THREADED; absent in C-no-thread) AND **TRAVELING-GATED** (present at N_phase≥2; ABSENT at N_phase=1 standing — the v7 reproduction control; F-TRAVEL keeper passed) AND **helicity-odd** (w_pol sign or the chiral-twin signature reverses RH↔LH; ABSENT/symmetric in C-achiral and C-transducer-OFF) AND **knob-robust** (tracks q_dep as designed, sign/oddness invariant across χ̃ / contour-radii / N / frac / ν_art) AND **persists P1** (drive-off, L-conserved — the winding survives the persistence window). → run the FULL spec sheet on the product (§11). **Consistency-vs-emergence:** the plant-at-scale reads (F-WPOL(b), F-TRAVEL) are CONSISTENCY/probe-capability (the extractor reads back a designed pattern — REQUIRED to trust the instrument, NOT the claim); the DE-NOVO assembled w_pol is the EMERGENCE claim (a `(2,3)` the object made itself, ideally fixed independent of q_dep).
- **T1-BROKEN** iff: any arm's deposit/rendering destabilizes the inherited converged mass (F-T1 fails) → an HONEST regression finding (the D17 feedstock-sparing or the D15 deposit is not passive in the full assembly despite boundary-locality + E_absorb≥0 — surface the conservation leak; do NOT tune to rescue T1). Its topology/winding reads are VOID. T1-BROKEN is a BIN.
- **UNRESOLVED**: anything else (above a floor but not separable into physics-vs-knob; a probe failing its known-positive/known-null keeper; a one-N channel that vanishes at N±1; floors not cleared; the traveling-sustain indeterminate).

---

## 7. THE ARM MATRIX (FROZEN — Rule 11; the controls the directive names; evaluated under BUILD then PERSISTENCE P1 on any positive)

| arm | thread (D16) | D17 feedstock | N_phase (D15) | drive chirality | transducer | purpose / frozen expectation |
|---|---|---|---|---|---|---|
| **MAIN (RH)** | ON (M≥M_c) | spared (a or b) | ≥2 (traveling) | chiral RH (h=+1) | ON | the v8 build claim — threaded torus + traveling polyphase winding |
| **C-no-thread** | **OFF** (M<M_c / D16 off) | spared | ≥2 | chiral RH | ON | **the v6 reproduction** — bubble stays genus-0; the KNOWN-NULL for the threading thesis (winding must NOT take without a torus) |
| **C-standing-phase** | ON | spared | **1 (STANDING)** | chiral RH | ON | **the v7 reproduction** — the traveling-vs-standing discriminator LIVE: must give `w_pol=0` (a single-phase pulsating field cannot wind) |
| **C-achiral** | ON | spared | ≥2 | achiral (h=0) | ON | NULL — no phase sequence ⇒ no traveling wave ⇒ `w_pol=0`; the m-even known-null (the twin must be symmetric here) |
| **C-transducer-OFF** | ON | spared | ≥2 | chiral RH | **OFF** | NULL — the D9-increment isolation (the clean GROSS-VS-FIELD contrast is MAIN − this, same handedness; v6 DEMOTION-2) |
| **C-LH** | ON | spared | ≥2 | chiral LH (h=−1) | ON | SIGN-REVERSAL — the winding sign / chiral-twin must FLIP vs MAIN (helicity-odd) |
| **C-no-spare** | ON | **inherited (c)** | ≥2 | chiral RH | ON | the D17 control — feedstock erased (`u_adv[cm]=0.0`); expect the v7 swamp (winding does not carry) — isolates the feedstock-sparing as the enabling move |

**PERSISTENCE P1** (drive-off, L-conserved, dissipation-minimized + the ν_art sweep §5 row 8) is run on ANY positive arm: the winding must SURVIVE drive-off (the electron's circulation never stops) — a winding that vanishes when the stator stops is driven-not-locked (apparatus). **The load-bearing v8 contrasts:** MAIN vs C-no-thread (threading), MAIN vs C-standing-phase (traveling), MAIN vs C-no-spare (feedstock), MAIN vs C-transducer-OFF (the D9 increment). **FLAG (flag-don't-fix):** if C-transducer-OFF or C-achiral is NOT handedness-null in the winding channel, the contrast is contaminated (the inherited buckle leak — v6 DEMOTION-2) — surfaced before binning, not silently fixed.

---

## 8. THE GATES-AS-EXECUTABLE-ASSERTIONS PLAN (the v7 demotion-(h) STRUCTURAL fix — EVERY gate is a computed driver assertion, NOT a docstring; a docstring gate is an automatic CLIP on whatever it gates)

**Every §4 floor and §6 bin maps to a named driver function that COMPUTES the quantity from the field/JSON and ASSERTS/BINS it. The result phase reports each assertion's PASS/FAIL/BIN with the field number. Planned in `src/scripts/vol_1_foundations/genesis_v8_threaded_run.py`:**

| gate (§4/§6) | driver assertion | what it computes (from the field — ave-driver-script-honesty) |
|---|---|---|
| F-T1 (every arm) | `assert_t1_regression(arm)` | `E_V^cons` late-window drift < 2 %; bins T1-BROKEN if detonates/still-rising |
| F-SHELL | `assert_shell_forms()` | largest connected `snap_mask` component cell count ≥ F-SHELL |
| F-GENUS (D16 topology) | `assert_topology()` | `scipy.ndimage.label` on `snap_mask`; channel-encirclement + Euler/ring test → {THREADED, NO-PENETRATION, SHELL-NEVER-FORMS} |
| (read torus) | `derive_read_torus_from_channel()` | R, r of the read torus FROM the D16 channel locus (field-derived — the A46 fix) |
| F-WPOL (a) | `assert_wpol_floor()` | `w_pol_rel > 0.1`, r ≥ 3, ≥16 contour samples on the field-derived torus |
| F-WPOL (b) | `assert_plant_in_channel()` | extractor returns `w_pol=q_dep` on the deposit planted INSIDE the threaded channel at scale |
| F-TRAVEL (heart) | `assert_travel_vs_standing()` | standing plant → `w_pol=0`; traveling plant → `w_pol=q` (the discriminator keeper) |
| F-NETFIELD (gross-vs-field) | `assert_net_field()` | net = MAIN − OFF (same handedness); asserts ≥ floor; the accumulator is logged, NEVER headline (the v7 fix: COMPUTED, not docstring) |
| F-CASCADE (D17) | `assert_cascade_bounded()` | `E_V` < 10× F-EV on the v5 cascade config, for EACH D17 rendering |
| F-EXCHANGE/F-ACHIRAL | `assert_structural_nulls()` | `chi_exch=0` and `helicity=0` deposits ≡ structural zero |
| F-CLOSE (pump) | `assert_pump_closed()` | drive-off `H_total^cons` no positive excursion above F-CLOSE |
| §5 row 8 binding | `assert_nu_art_swept()` | the `nu_art` axis IS present in the JSON `sweeps` keys (the v7 demotion-c fix) |
| WINDING-TAKES | `bin_winding(...)` | the conjunction of §6 (de-novo + threaded-gated + traveling-gated + helicity-odd + knob-robust + P1) |

**No gate is satisfied by a docstring or a comment. Each `assert_*` runs in the result phase and its PASS/FAIL is in the JSON.** (Verify-before-cite: the v7 failure was `grep -i netfield` over the driver returning ONLY the docstring line — the v8 driver's `grep` for each gate name above MUST return an executable call site, asserted by a meta-test in `test_unified_threaded_v8.py`.)

---

## 9. APPARATUS / PROBE-CAPABILITY KEEPERS (ave-apparatus-floor-attribution v1.1 — every discriminating probe validated on a known-different reference + a known-positive; encoded as `src/tests/test_unified_threaded_v8.py`, the NEW v8 keeper file; run + pass BEFORE the matrix)

| keeper | known-reference | assertion |
|---|---|---|
| **K-OFF byte-identical (D-INHERIT)** | all v8 knobs (`thread_on`, `snap_u_mode=inherited`, `n_phase=1`-default-OFF, `traveling_deposit=False`) OFF | the v8 engine reproduces the v6/v7 path bit-for-bit (`test_unified_transducer_v6.py` + the snap ladder stay green); no v8 knob perturbs the inherited dynamics when off |
| **K-TRAVEL-VS-STANDING (the HEART of v8 — F-TRAVEL)** | a planted STANDING quadrature vs a planted TRAVELING quadrature, both `q_dep`, at scale | the STANDING plant reads `w_pol = 0`; the TRAVELING plant reads `w_pol = q_dep`, rel > 0.1 — the extractor measures a TRUE winding, not amplitude geometry (else WINDING-TAKES is CLIP). The directive's keeper, executable. |
| **K-PLANT-IN-CHANNEL (the de-novo gate, F-WPOL known-positive INSIDE the geometry)** | the v8 traveling deposit planted ONCE into the threaded channel at run scale | `extract_2_3` returns `w_pol = q_dep` on the field-derived torus — the deposit IS winding-capable in the read coordinate inside the actual geometry (the look-inside hygiene; external reads disqualified) |
| **K-TOPOLOGY (the D16 gate keeper)** | a hand-built genus-0 sphere-shell `snap_mask` AND a hand-built genus-1 torus-shell `snap_mask` | `assert_topology()` returns NO-PENETRATION on the sphere and THREADED on the torus — the topology code distinguishes ball from torus (else F-GENUS is CLIP). REAL topology, validated on knowns. |
| **K-DELTA-L-FAITHFUL (D13-FAITHFUL, the v7 demotion-(d) fix)** | the run's own ledger | the traveling deposit's amplitude `A_dep` is set BY the extracted photon spin δL (`A_dep ∝ δL`, NOT an independent energy-budget source); `S_photon_removed ≡ L_transferred` to 1e-9 — the photon's OWN spin is what's routed (the v7 decorative-ripple deviation cannot recur) |
| **K-HELICITY-ODD (m-even keeper)** | a fresh RH vs LH vs achiral traveling deposit, 200 steps | the phase SEQUENCE reverses RH↔LH and is structurally absent achiral (the helicity-odd travel direction is from the field, not dialed) |
| **K-AM-LEDGER (conservation-by-channel)** | the run's own ledger | `S_photon_removed ≡ L_transferred` to 1e-9; `E_absorbed ≥ 0` (passive); H_total not increased (the D11 discipline on the v8 deposit) |
| **K-CASCADE-EACH-RENDER (F-CASCADE)** | the v5 cascade config under each D17 rendering (a, b, c) | `E_V` bounded < 10× F-EV for EVERY rendering — the feedstock-sparing does not reopen the deflagration pump (ave-conserved-vs-pumped) |
| **K-NU-ART-PRESENT (the §5 row-8 / v7 demotion-c meta-keeper)** | the driver's JSON `sweeps` keys | the `nu_art` axis IS present and populated (the mandated sweep is executed, not pinned — the gate-as-executable discipline applied to §210 itself) |

**A probe that fails its known-positive/known-null keeper DISQUALIFIES the corresponding verdict (CLIP), per the m-even lesson.** The keepers run + pass BEFORE the matrix.

---

## 10. FAIL-FAST ASSERTIONS (D12 class — cheap, early; run BEFORE the full matrix; do not burn the full run on a dead coupling)

1. **D12(i) — handedness alive:** after the traveling deposit is enabled (MAIN, N_phase≥2), RH-drive and LH-drive must NOT be byte-identical within **200 steps** in the ω/π_ω channel — else the deposit is DEAD: ABORT, report (TRANSDUCER-DEAD class).
2. **D12(ii) — standing null:** the N_phase=1 (C-standing-phase) arm must read `w_pol=0` de novo within the resolved window (the traveling-vs-standing discriminator at the matrix level; a standing arm that winds is a floor-grazer — F-TRAVEL already gates the probe, this gates the ARM).
3. **D12(iii) — achiral / transducer-OFF null:** `helicity=0` and `transducer_on=False` traveling deposits ≡ 0 (the structural known-nulls; the buckle background is isolated by MAIN−OFF, §12).
4. **D12(iv) — RH≠LH winding sign within 200 steps:** if a w_pol forms, its handedness signature must NOT be byte-identical RH vs LH (a helicity-EVEN "winding" is geometric — the v6/v7 false-positive lesson).
5. **D12(v) — topology gate sanity:** `assert_topology()` returns SHELL-NEVER-FORMS before snap onset and a definite {THREADED, NO-PENETRATION} after (the K-TOPOLOGY keeper passed at scale).
6. **D12(vi) — T1 fail-fast:** if `E_V^cons` > 2× the v6 12.9 baseline by the 200-step gate in any arm, flag T1-BROKEN early.

---

## 11. THE FULL-SPEC-SHEET CONDITIONAL (IF WINDING-TAKES — the complete electron acceptance test; FROZEN, Rule 11)

GATED: runs ONLY if §6 returns WINDING-TAKES. It does NOT run for SHELL-NEVER-FORMS / NO-PENETRATION / THREADED-NO-WINDING / T1-BROKEN / UNRESOLVED (those close the v8 branch with the named mechanism — Rule 11). If WINDING-TAKES, run the FULL spec sheet on the product (consistency-vs-emergence tagged):

- **T1 mass (RE-AFFIRMED by D-INHERIT; MANIFESTATION).** `E_V^cons → ~12.9`-class converged, drift < 2 %, N- and stop-time-robust — the inherited baseline; the spec sheet adds the winding the v6/v7 object lacked.
- **T2 charge (EMERGENCE de-novo vs CONSISTENCY read-back).** the de-novo `(w_tor, w_pol)` integer `(2,3)` (or `(3,2)`), rel > 0.1, r ≥ 3, threaded-and-traveling-gated; sign = handedness, FLIPS RH↔LH; the buckle SEPARATED via OFF arms (charge tracks the DEPOSIT, scales with N_phase/χ̃, vanishes at transducer-OFF — NOT invariant like the v6 `H_bel` DEMOTION-2 contaminant). **Emergence requires the `(2,3)` be FIXED independent of q_dep (else it is the consistency read-back of the deposited q — the v7 failure).** Phase-space Park-along-contours read, A46.
- **T3 spin (EMERGENCE vs CONSISTENCY; STATE WHAT THE LOCK DOES).** the locked `L_ω` η-invariance (the v6 T3 CLIP: `L_ω` tracked `lock_eta` 5.63→0.0099). IF the v8 winding is the lock-surviving traveling quadrature, state explicitly whether the spin is now in the η-invariant poloidal mode. Reactance pair (C-state ω, L-state π_ω) every step (F0c) + CP5 local-clock `A²_local`.
- **T5 chiral twin — SHARPENED.** the counter-rotating partner / pocket-split asymmetry PRESENT in MAIN & C-LH, FLIPS RH↔LH, ABSENT/symmetric in C-achiral (the m-even keeper); separated into the charge-ledger sign-flip vs a genuine SPATIAL vorticity twin (the v6 split: chiral in charge, geometric in vorticity — does the v8 threaded-traveling winding finally give a SPATIAL twin?). The directive's "sharpened chiral-twin T5."
- **T6 de Broglie (CONSISTENCY; weight-bearing ONLY if T1 passes).** translate the locked winding state at ≥2 momenta; `λ ∝ 1/p` (exponent −1) within fit-floor.

**SPEC-SHEET verdict bins (inherited from v6/v7):** ELECTRON-CLASS (T1 + ≥4 of T2–T6 at floor, no clip-valued positive) / PARTIAL (a named residual survives) / NOT-ELECTRON (T1 fails — impossible under D-INHERIT unless the deposit broke T1 = T1-BROKEN) / VOID (a forbidden seeder, a non-null OFF, a winding below F-WPOL, an OFF not byte-identical across handedness). **A positive at a clip value is APPARATUS; a clean negative with a named mechanism is the discipline at full strength.**

---

## 12. THE GROSS-VS-FIELD RULE — STRUCTURAL (the v6/v7 panel lesson, binding on every v8 threading/deposit claim)

**A deposit ACCUMULATOR is never a measurement.** The engine logs `L_transferred_omega` (the by-construction per-step deposit tally) — BOOKKEEPING, used ONLY to compute the gap-to-net-field, NEVER a headline. **Every v8 threading/winding effect is the NET FIELD quantity (MAIN − OFF at the SAME handedness), measured ON the field, above its floor (F-NETFIELD), via an EXECUTABLE driver assertion `assert_net_field()` (NOT a docstring — the v7 demotion-h fix).**

- **The v6/v7 numbers carried (verify-before-cite §0):** v6/v7 net field ω-axial deposit = **−3.6e-5** (rigid, lock-drained); the accumulator ∓0.539 was bookkeeping. v7's frozen survival gate was never computed → the run reported SURVIVES on a substituted criterion while the gate FAILED (3.34–4.03 OOM DRAINED).
- **v8's headline (the de-novo winding test):** the de-novo `w_pol` read on the FIELD-DERIVED threaded torus (A46-matched coordinate — the threaded hole gives a real R, the v7 SPHERE obstruction removed), MAIN−OFF, threaded-and-traveling-gated. NOT a lattice-Cartesian L_ω,axial (the WRONG coordinate for a poloidal-winding claim — the v6/v7 mode).
- **The headline sentence the result MUST be able to write:** "the de-novo poloidal winding is `w_pol = X` (MAIN−OFF, threaded torus, N_phase=Y traveling, helicity-odd, persists P1), ABSENT at N_phase=1 and un-threaded" — the FIELD winding + its threaded/traveling gating, NEVER the accumulator.

---

## 13. CORPUS STATE + ADJUDICATION / LANE DISCIPLINE

- **OPEN.** v8 does NOT promote any candidate-claim and does NOT reopen NOT-ELECTRON (the v5 panel). It tests ONE structural hypothesis (thread a torus + spare the feedstock + drive a traveling winding) the v7 closures named. T1 is inherited (D-INHERIT), not re-claimed.
- **Rule 11 (honest closure):** the bins (§6) are frozen pre-run; floor-checks gate every positive; a clip-valued positive is APPARATUS; NO-PENETRATION / THREADED-NO-WINDING / T1-BROKEN with a NAMED mechanism is the discipline working, not a failure. No post-hoc criterion drop, no debugging toward a rescue, no tuning the D17 rendering / N_phase / drive M to save a verdict.
- **Rule 12 (substitution-not-retraction):** v8 is this hypothesis's OWN chain (this prereg + result + a NEW version number). It does NOT refill the v6 DEMOTED-PARTIAL or v7 DRAINED-AGAIN slots; the v5 NOT-ELECTRON / SNAP-LOCKED 🔴 demotions stand UNCHANGED. A v8 WINDING-TAKES (should it survive) is a NEW positive with its own tracked entry.
- **A44 (missing-axiom vs engine-bug):** the diagnosis is engine geometry / coupling-family / closure (the topology, the feedstock, the traveling drive), NOT a missing axiom — **no Ax-5 candidate drafted** (lane discipline; the engine-violates-Ax-3-vs-missing-axiom distinction holds).
- **Lane discipline:** this prereg + the run result SURFACE the empirical finding only (incl. the §2 AM-routing plumber-physical flag for Grant). The auditor lands any manual / manuscript / `COLLABORATION_NOTES` entry; this doc does NOT draft those, does NOT draft the auditor's manual, does NOT draft Ax-5 candidates. The §2 framing question is for Grant (the framing source of truth), not self-resolved.

---

## 14. SCALE / BUDGET (the N³ cost law governs; N frozen at 48 primary)

Inheriting the v6/v7 cost model (verified live, v6 §7.9 / v7 §12): `~9.4e-8·N³ s/step`; at N=48 ≈ 10.4 ms/step, ≈46 s per full 4400-step arm, ~20 MB/engine. `genesis_parallel_runner.py` (ProcessPool, ~5× effective, `serial==parallel` bit-identical) fans the matrix; `fast_winding_extractor.py` (25.8×, float64-mandatory) accelerates the de-novo + plant-in-channel w_pol reads; `scipy.ndimage.label` (the topology gate) is O(N³), negligible.

The v8 mandated matrix: the core threading/traveling arms (7 controls × {build, P1}) + the directive-named sweeps (N_phase 5, D17-rendering 3, drive-M 4, shell-thickness 4, Omega_stator 3, χ̃ 5, contour-radii ~5) + the binding regression/attribution sweeps (ν_art 5, q_dep 3, N 3, K3 9, lock_eta 4, f_ω 3, frac 4) ≈ **~80 build-equivalents**. At N=48: ~80 × 46 s ≈ 3680 s serial / 5× ≈ **~12 min wall**; at N=56 ≈ 20 min — within a single-session budget.

**FROZEN: N=48 primary** (the disciplined "largest N that keeps the inherited N=40/48 keepers within one resolution step"); the N sweep {40, 48, 56} brackets the headline so any N-tracking (incl. the §3.5(1) channel-resolution concern) is binned CLIP/UNRESOLVED. **EXCLUDED (honesty):** f32 dtype — physically forbidden (the conservation canaries operate at 1e-3, the winding gate needs 1e-12; f32 desyncs the ledger). The BINDING constraint is NOT budget — it is the hard floor-recalibration + keeper-revalidation rule (§4, §9) + the topology-gate validation on knowns (K-TOPOLOGY), which N=48 keeps cheap.

---

*Prereg committed ALONE (this commit) before any v8 engine line or run artifact. The deposit/topology derivation §3 is a PLAN; the engine changes (the D15 polyphase `_transducer_step` traveling branch, the D17 `snap_u_mode` renderings of `unified_genesis_engine.py:396`, the D16 `measure_genus()` diagnostic) + the §8 executable gates + the §9 keepers are built + smoke-gated in SEPARATE Phase-2 commits. Skills fired recorded in the front matter. verify-before-cite: every v5/v6/v7 + engine anchor (`unified_genesis_engine.py:183,347,353-364,386-396,452-547,602,928`; `crystal_graft_v4.py:138,186-226,296-324`; the v6/v7 result docs + run JSON) grep/line/JSON-confirmed THIS session.*
