# THE BIAS PROPAGATION THEOREM (R49b/R50) — RESULT

**Date:** 2026-08-10 · **Branch:** `lane/2026-08-10-bias-propagation` · **Base:** `origin/main` @ `ad376e9a`
**Frozen prereg:** [`2026-08-10_bias-propagation_prereg-FROZEN.md`](2026-08-10_bias-propagation_prereg-FROZEN.md), committed ALONE + pushed at `d4bee683` (freeze-by-push) before any driver code, derivation content, or lane-produced number existed. Byte-untouched since.
**Driver:** [`research/drivers/bias_propagation_lane.py`](drivers/bias_propagation_lane.py) → `bias_propagation_lane_results.json` + [`bias_propagation_number_check.py`](drivers/bias_propagation_number_check.py) and [`bias_propagation_quotes_number_check.py`](drivers/bias_propagation_quotes_number_check.py) (both `--mutation-receipt`; both auto-discovered by the `make verify` umbrella). **95 number checks green** (receipt fires 4 detectors) + **29 quotes two-engine byte-verified** (receipt fires); two full driver runs byte-identical.
**Derivation companion:** [`2026-08-10_bias-propagation_derivation.md`](2026-08-10_bias-propagation_derivation.md) — the widened lemma's proof, the corpus `√S` collision, the GR force-object distinction, the pole-test scope law.
**⚑ REVIEW-CYCLE REPAIR PASS, 2026-08-11.** This document was amended after an orchestrator verify. Repairs are marked in place and dated; **the FROZEN prereg is byte-untouched.** The material changes: the `S^p` lemma is **widened** to every `C¹` function of the kernel (§2.2); a **resolution proposal that shipped as fact is withdrawn** and replaced by the named corpus contradiction it was papering over (§2.2a); the pole test is **scoped to `c_g ≲ O(c)`** at every site (§1.3); the GR comparison object is **relabelled** from force-at-infinity to static-observer proper force (§2.1); `BLOCKED-ON-D1` is **declared off-grammar** (§3); G-QUOTE is **re-scored against its frozen criterion** on a committed gate (§4); one stale numeral is corrected (§2.1).
**⚑ DELTA RE-VERIFY MICRO-FIX PASS, same day.** An independent delta re-verify confirmed those repairs and found two residual defects, both fixed here: the F3 two-arms disclosure **stated the wrong reason** — the arms did not share a central, and the gap is exactly the contested `κ²` (§1.3, and `derivation.md` §4 (16)); and §1.4's FORK-2 span was **quoted without its verb** — restored byte-exact and now registered in the quote gate. Scoping fixes alongside: G-NUM's "machine-held" claim is scoped to the 30 registered pairs (§4), G-QUOTE's auto-discovery is scoped to the script (§4), and one mis-mapped reconciliation registry row was repaired in the checker.
**Class:** DERIVATION. **Mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; edits no KB leaf, register, ledger, ruling, or manuscript file; changes no solidity; all propagation ROUTED.** Engine `src/ave` byte-untouched (`git diff --stat ad376e9a..HEAD -- src/` empty); `backreaction.py` read-only.
**Lane brief:** [`_orchestration/2026-08-10_bias-propagation-brief.md`](../_orchestration/2026-08-10_bias-propagation-brief.md)
**Presentation state:** `[DO-NOT-MERGE]`. **Nothing in this document reads CLEARED.**

---

## HEADLINE

> **🔴 THE AXIOM-5 DEBT IS NOT DISCHARGED. D1 = `NOT-DERIVABLE(missing structure enumerated)`.**
>
> Three independent routes (variational completion, multiple-scales envelope reduction, slaving map) each produced a candidate completion of clause G, and each awarded a `NONE-OF-THE-THREE(SLAVED…)` bin. **All three were refuted at this lane's own Tier-2, by the same defect, found independently by six verifiers:** the law each route derived has a source→field response kernel that is **instantaneous at infinite range at every frequency**. The brief's frozen deliverable-1 object — *"the delay's frequency structure IS the deliverable"* — **comes out identically zero on every route.** A completion whose delay is zero at every `ω` and every `r` is not a completion of the elliptic abstraction; it re-installs it. The bins are WITHDRAWN and the honest verdict is the prereg's own fallback.
>
> **This is a bankable negative (Rule 11), and it agrees with the axiom's own text.** `eq_axiom_5.tex` at the doc-lane branch tip (`7adbc187`) already carries the debt as named-open entry **(c1)**, verbatim: *"Clause G's elliptic law is the **static abstraction of underived finite-speed bias dynamics**: an elliptic solve is instantaneous by construction, and the axiom does not write what replaces it when the source moves. **The $(u,\pi)$ no-signalling theorem does NOT cover the bias read** … the finite propagation speed of the bias is **owed, not held**."* The lane confirms that framing from three directions and **enumerates what is missing** (§1.4) — which is the deliverable that remains.
>
> **D2 = `REPRODUCES-WITH-IMPORT(κ = c⁴/7G and ν_vac = 2/7 — both GR-imported per #261)`, on CLOCK-1; `FAILS` on CLOCK-S.** The prereg's ENTAILED-BRANCH declaration is **CONFIRMED and strengthened**: `F/F_Newton = (1 − r_s/r)^{−1/2}` is byte-identical to GR's *exact* **static-observer PROPER force** `m·a = (GMm/r²)(1−r_s/r)^{−1/2}` (equivalently `−∂_r E_∞`) — **not** the force at infinity, which is `GMm/r²` exactly (Wald §6.3). So what is imported is not merely the Newtonian limit but **the whole `g₀₀` function in Schwarzschild coordinates** (sympy: difference identically zero). **The spatial-metric sector — PPN `γ` — is untouched by this construction and is neither imported nor tested here.** **The honest verb is DEMONSTRATED, not DERIVED; the class is CONSISTENCY / PEER-with-GR; the redshift–lensing–attraction "unification" is an ECHO at the coefficient level.** `ħ` cancels and the target is `𝒜_g`-independent, so the frozen grammar's *zero-new-constants* clause is also literally true — and the IMPORT clause is the honest one to award.
>
> **The one genuinely fireable result of the lane (D2's real content): the kernel-clock lemma, at its true width.** For **any** clock law `W = f(S)` on Axiom 4's kernel `S = √(1−A²)` with `f` differentiable and `f′(1)` **finite**, `dW/dA|₀ = 0` identically (because `dS/dA|₀ = 0` — the kernel is quadratic in `A` at the origin), so `F = −ħω₀f′(1)r_s²/r³` — **no `C¹` clock built on the Axiom-4 kernel can produce a `1/r²` force at all.** `W = S^p` is the special case `f′(1) = p`. The **escape condition is `dW/dA|₀ ≠ 0`** — the clock must be leading-order LINEAR in `A` — and the surviving realization in canon is the **slope-1 lapse** `√(1−A)`. Keying on the observable gradient `|∇ε₁₁|` instead does **not** escape: it returns `1/r³` for every coupling. Since canon has Newtonian gravity, **canon cannot be running a `C¹`-function-of-the-kernel gravitational clock.** This kills the CLOCK-S *family* — and a strictly larger class than this document originally named — and it is an adjudication the run could genuinely have lost. **⚑ It also NAMES A LIVE CONTRADICTION IN CANON (§2.2a): `saturating-modulus-and-backreaction.md:128` runs `ω_local = ω√S` on the kernel `S`, while `temporal-spatial-lattice-decomposition.md:28` writes `√g₀₀ = √S ≈ 1 − GM/rc²`, which requires `S = 1 − r_s/r`. Two functions, one symbol, same `E = ħω` construction — and the gravity KB's Stage-4 peel target (`:174-176`) rests on the reading the lemma weak-field-falsifies. ROUTED; this lane picks no winner.**
>
> **D3 = `BLOCKED-ON-D1` — ⚑ an OFF-GRAMMAR label, disclosed as such (§3).** The frozen D3 grammar admits exactly three labels and this is none of them; the lane reports off-grammar with the reason rather than dropping a criterion to fit (Rule 11).
>
> **★F1 (arc-level kill) does NOT fire on the delivered verdict**, and its first horn is **SETTLED-NEGATIVE by the axiom's own text**, not by this lane's preference. The pole test delivered the lane's sharpest constraint, **stated with the scope the first version of this document omitted: for `c_g ≲ O(c)`, a pole-bearing completion is excluded by the banked pulsar bound of `10²–10³×` (`port-register.md:93`)** — a rescaling to this lane's `c_g = c` branches gives `10³–10⁴×`, but that step is the lane's own and its digits are not banked. **The exclusion is suppressed by `(c/c_g)^{2ℓ+1}`, so it does NOT reach a superluminal completion: at this lane's own forced `c_g = 1.2771×10²²c` the ratio is `~10⁻¹⁰⁷` and the pulsars are silent** (§1.3). Whatever eventually completes clause G, **it cannot be a radiating line at `c_g ≲ O(c)`** — that door is closed by data, and only that door.
>
> **Two of this lane's own findings were PHANTOMS and are WITHDRAWN pre-presentation, never routed:** a "4π source-convention seam" — **withdrawn as PHANTOM-BY-ATTRIBUTION**: the *quote* it rested on was candidate BC-SRC prose attributed to the ratified axiom file, so the finding is void as a finding. *(The underlying rendering difference is nevertheless real and is disclosed, dated, at §2.0; it enters no target.)* — and an "Axiom-5 text ambiguity" (built on a **stale blob** — a superseded commit of `eq_axiom_5.tex`). Both were caught by this lane's Tier-2 and re-verified by the orchestrator against the branch tip. **Neither reaches Grant as a finding.** They are recorded here as instrument failures because the corpus's standing discipline says the near-miss is the receipt. **Both were CITATION failures — which is why this pass commits a two-engine quote gate (§4, G-QUOTE) that pins its axiom-text rev instead of naming a branch.**

---

## §0 — REGIME / SECTOR / PHASE-STATE (prereg SVA §0 governs; restated at the point of reading)

**MODE** — constitutive-dynamics derivation (circuit-class classification + frequency response) + an internal-coherence adjudication of the node clock. **REGIME** — crystalline cold-linear Regime-I, sub-yield, lossless-reactive, exterior `D(A) → 1`; **past-wall and the `D(A) → ∞` wall are OUT OF SCOPE and were evaluated nowhere.** **PHASE-STATE** — cold-reactive; the de-bonded and pre-freeze phases are NAMED-OPEN (R45) and this lane writes no law for them. **SECTOR** — the A1 bulk/dilatation slot; T2 shear/EM and Cosserat micro-rotation byte-untouched. The rotational fence-reach STOP fired once and held: micro-rotation inertia `I_ω` was enumerated as a candidate for the bias inertia `𝓘` and **REFUSED** — routing bulk-slot content through the micro-rotation channel is the cross-wiring G-SECTOR forbids. TWO-"3"s guard active. **THREE OBJECTS** named at every use: the bias `ε₁₁` (potential-class), the bound response `u₀` (field-class), the fast layer `(u, π)` (state-class). **STENCIL** — no run of `src/ave` corroborates anything here; the numeric arm is the purpose-built driver plus sympy/mpmath algebra.

---

## §1 — DELIVERABLE 1: THE ENVELOPE REDUCTION → `NOT-DERIVABLE`

### §1.1 What the three routes actually derived, and why all three were withdrawn

Each route enumerated the candidate time-terms that can complete clause G's static law, in the prereg's own circuit taxonomy (series **L** ⇒ LC-hyperbolic, series **R** ⇒ RC-diffusive, conjugate quadrature ⇒ Schrödinger-class, nothing ⇒ slaved). A structural fact fell out immediately and survived Tier-2:

> **The static law is BLIND to the time term.** Every member of the family returns the canon elliptic law under `∂_t → 0`. The completion is therefore a one-function family that the *statics cannot select*; only the fast layer can. **This is the precise, receipted statement of what "the elliptic abstraction" abstracts away** — and it is the lane's most durable structural output.

What the routes then built, and what Tier-2 did to it:

| Route | Awarded | Refuted because (verifier, re-derived) |
|---|---|---|
| **R1** variational | `SLAVED-WITH-LOCAL-RESTORING` | Its own EL equation is `∂²ₓ[ρ𝒜_g²ε̈ + κε] = −T₀₀`, i.e. an **elliptic spatial operator times a temporal ODE** — the textbook signature of infinite propagation speed. A step source produces `ε₁₁(r,t) = (1−cos ω_b t)·P(r)` at **every radius simultaneously**: onset delay zero at all `r`. The route reported only the homogeneous branch (`dω/dk = 0 ⇒ nothing transports`) and read the absent `e^{ikr}` as safety; the same kernel says the *driven* response is in-phase with the source at every `ω`. |
| **R2** multiple-scales | `SLAVED-VIA-CLAUSE-G` | The `p = 2.0` leg is refuted by the route's own slaving map (the map's kernel contains the entire transverse `c`-sector, whose divergence vanishes identically). `p` was reported ANALYTIC and **not bookable** — G-LIVE had not run in that route. |
| **R3** slaving map | `SLAVED` | **The map is not preserved by the dynamics it claims to be slaved to.** Clause G is a **vector** relation; holding it at all times imposes 3 constraints on a fast layer whose longitudinal sector obeys `ρü = f_L` with `f_L` matter-supported. For a **moving** source — the exact case the debt is about — `ü` is nonzero exactly where the EOM demands zero: over-determined, no solution. The route's clean answer came from projecting the 3-DOF vector constraint onto its 1-DOF divergence, **where the contradiction cancels exactly.** |

**Common defect, stated once:** all three delivered a law with **zero delay at every `ω` and every `r`**. Two of them additionally imported the `O((ωr/c)²)` near-zone structure from the bound-constitutive lane's **R8 receipt — which was measured on `(u,π)`, a declared different object** — to keep a peer-with-GR story alive. That import is exactly the C1 object-crossing the bound-constitutive lane's own Tier-2 was built to catch, reproduced here by three independent routes. It is refused.

> **⚑ WARN, carried not repaired — the SLAVED bin's frozen `p = 2.0` is an INHERITANCE, and the band it inherits is post-hoc.** Prereg §3.1's bin-4 row sets `p = 2.0` by inheriting R8's *"quadratic-class"* characterisation of the `(u,π)` near-zone residual. **R8's measured exponent is `2.765`, not `2.0`** (§3, overclaim (i)); the tolerance that lets `2.765` read as "quadratic-class" was set after the measurement, not before it. **No verdict in this document rests on that inheritance:** every D1 bin is withdrawn, the frozen discriminator is the **pole test, not `p`** (§1.3, the degeneracy declared pre-run), and this lane's own analytic `p = 1.993` is reported as a *refutation* of the "reproduces R8" claim rather than as a confirmation of anything. Recorded so the inherited number is not later mistaken for a receipt.

### §1.2 The per-bin dispositions that DO stand

- **LC-HYPERBOLIC — NOT AWARDED, admission price NOT PAID.** The bin requires a bare inertia density `𝓘 = κ/c_g²`; for `c_g = c`, `𝓘 = 1.923701×10²⁶ kg/m`. The fast layer's complete inertia inventory was enumerated: the only substrate-native candidate is `ρ_bulk·ℓ_node² = 1.179485×10⁻¹⁸ kg/m`, **short by exactly `1.630967×10⁴⁴ = 2ξ_Machian`**. Paying with `κ/c²` itself is circular (`κ` is GR-imported and per the bound-constitutive C5 has no receipted home in the written action). Forcing the node-scale inertia into the slot gives `c_g = √(2ξ)·c = 1.2771×10²² c` — the LC-1 frozen-cell kill shape. `I_ω` refused (sector fence).
- **RC-DIFFUSIVE — NOT AWARDED.** No series `R` exists on the line. The Ax3 ledger obligation is therefore **moot, not softened**: there is no dissipative term to close a ledger for. (Had one existed, SVA row 6 says the bin fails; it does not soften.)
- **DISPERSIVE-UNITARY — `INAPPLICABLE`; ★falsifier F2 FIRED.** An envelope/modulation theory needs a carrier for the envelope to modulate. Clause Q sets `ε₁₁ = 0`, `∇·π = 0`, `θ = 0` in the sourceless cold phase. Canon's *"only spatial gradients of `A` … are physically observable"* rule was tested as the one available rescue (a nonzero uniform cold carrier whose absolute level is unobservable) and does **not** license one here. **The bin is inapplicable, not merely unselected** — and per the prereg the lane says so rather than importing a carrier to keep it alive. **This retires the 2026-07-20 envelope lane's NLS-class reading of the cold vacuum at this rung.**

### §1.3 ★G-POLE — the frozen degeneracy is broken, and the result is a real constraint

The prereg declared, before any run, that `p` **cannot** separate LC-at-`c` from SLAVED (both predict `p = 2.0`) and froze the **far-zone pole test** as the actual discriminator. It worked exactly as designed:

| Bin | Pole structure | `1/r` radiative term | F1 |
|---|---|---|---|
| RC-DIFFUSIVE | `ω = −i𝒟k²`, purely imaginary (relaxation) | none; far field exponentially screened | does not fire |
| **LC-HYPERBOLIC** | `ω = ±c_g k`, **real** | **present, undamped** | **FIRES** |
| **DISPERSIVE-UNITARY** | real `k = √(Λω/κ)`; Bogoliubov arm gapless acoustic | **present** | FIRES — unless F2 voids the bin first (it does) |
| SLAVED | `ω² ≡ 0`; pole at the origin only | none (quasi-static tail, `P(r) → 0`) | does not fire |

**The constraint this buys, which survives the withdrawal of every D1 bin — stated with its VALIDITY SCOPE, which the first version of this section omitted:**

- **BANKED INPUT (not this lane's arithmetic).** `port-register.md:93` carries the Reading-A exclusion: *"excluded at 9–110σ Hulse-Taylor / 100–1400× the double-pulsar bound"* — i.e. **10²–10³×**, computed for a Reading-A **quadrupole** (`port-register.md:89`: *"monopole + dipole killed by conservation, but the **quadrupole radiates**"*) at a radiative speed of order `c`.
- **THE LANE'S OWN STEP, marked as such.** Rescaling that banked bound to this lane's `c_g = c` pole-bearing branches moves it to **10³–10⁴×** (central `≈8974×`). **That rescaling is this lane's, not the corpus's**, and it is the step the confirmed MAJOR sits on. **The digits — `8974`, and the `10³–10⁴×` bracket — are NOT banked**; only the order is reported, and the headline now carries the banked `10²–10³×` figure rather than the unbanked rescaled one.
- **★VALIDITY SCOPE (the repair): the test bounds only `c_g ≲ O(c)`.** Multipole-`ℓ` radiated power carries `(v/c_g)^{2ℓ+1}`, so the exclusion of a pole-bearing completion is suppressed by **`(c/c_g)^{2ℓ+1}`**. At the quadrupole `ℓ = 2` that is a **fifth power**. **At this lane's own forced `c_g = √(2ξ_Machian)·c = 1.2771×10²² c`** (§1.2's admission-price arithmetic) the suppression is `(c/c_g)⁵ ≈ 2.9×10⁻¹¹¹`, and the exclusion ratio collapses to **`~10⁻¹⁰⁷` — roughly 107 orders BELOW the comparator instead of ~4 above.** **A SUPERLUMINAL pole-bearing completion is therefore NOT excluded by pulsar timing.** *(Driver leg L6; the suppression is recomputed independently in the number check. The review's arithmetic quotes `~1.5×10⁻¹⁰⁷` and this driver's returns `~2.6×10⁻¹⁰⁷`. **★Corrected 2026-08-11 — the earlier text gave the wrong reason for that gap.** The two arms did **not** run on the same central, and the difference is not an unexplained residue: it is exactly the `κ²` central. The exclusion ratio is a radiated compression fraction `κ²` over the observational cap `κ_max² = 1.3×10⁻⁴` (§3 item b), so this driver's `8974×` **implies** `κ² = 8974 × 1.3×10⁻⁴ = 1.1666`, while the review's arm ran `κ² = 0.666` — and `1.1666 / 0.666 = 1.7517` is precisely the ratio of the two exclusion figures, `2.6416×10⁻¹⁰⁷ / 1.5081×10⁻¹⁰⁷`. **`κ²` is the O(1)-coupling coefficient the one not-refuted MAJOR contests** (§5): the gap between the arms IS that open coefficient and nothing else. Hence only the ORDER is banked, neither leading digit is, and this lane picks neither `κ²`. Reconciled in the number check.)*
- **⚑ The exclusion NUMBER carries a confirmed MAJOR finding and is reported as ORDER-OF-MAGNITUDE ONLY** (the route's Lagrangian normalisation was set by a check structurally blind to the coefficient; the coupling bracket was transferred from the envelope lane as a declared deviation). **The ORDER survives; the digits do not, and are not banked.**

> **Physical statement, at the honest grade AND in scope:** whatever completes clause G, **it cannot be a radiating longitudinal line with `c_g ≲ O(c)`.** That door is closed by pulsar timing, not by preference, and it closes independently of which bin the completion lands in — **but only inside that speed scope.** The lane's own LC-hyperbolic admission-price arithmetic drives `c_g` to `10²²c`, where the `(c/c_g)^{2ℓ+1}` suppression makes the pulsars silent. **A superluminal pole is not excluded here; it is excluded — if at all — by the LC-1 frozen-cell kill, which is a different test on a different criterion.**

### §1.4 ★THE ENUMERATED MISSING STRUCTURE (the deliverable that remains)

Per the frozen grammar, `NOT-DERIVABLE` ships its enumeration. Three items, each derived, none supplied:

1. **The channel by which any `c`-limited fast-layer transport reaches `ε₁₁`.** The slaving map's kernel contains the entire transverse `c`-sector (`∇·(transverse) ≡ 0` identically, and cold-linear Regime-I decouples the Helmholtz sectors exactly), while the only sector the map does see has characteristic speed **exactly zero**. **No derived channel exists.** This is the sharpest form of the debt.
2. **The matter-coupling law `f`** — simultaneously the only remaining delay candidate *and* the compatibility condition needed for clause G to survive a moving source.
3. **Whether clause G is a STATIC LIMIT ONLY.** The vector relation `u₀ = −𝒜_g∇ε₁₁`, held at all times, over-determines the fast layer for a moving source (§1.1, R3 row). Either a curl-sector mediation channel breaks `u₀`'s curl-freeness during transients — in which case **clause G is the static limit only and no slaving map exists off-static** — or there is a nonzero exterior source, which `eq_axiom_5.tex` forbids (*"A sourceless bound response"*). **Both horns are consequential and neither is this lane's to rule.**

> **⚑ Consequence carried, flag-don't-fix:** item 3 bears on the bound-constitutive lane's §2.6 item 3 — the corpus's only standing causality leg for the bias (*"clause G makes the grade's GRADIENT local in the dress … so every observable grade reading is a local functional of the causal `(u,π)` fields"*), which is what carries that lane's FORK-2 verdict — *"the observable-grade branch lands CAUSAL-CONDITIONAL-ON-BC-SRC"* (`bound-constitutive_result.md:151`, now byte-checked in this lane's quote gate; the earlier rendering of this span dropped its verb and was repaired 2026-08-11). **If clause G is static-limit-only, that leg does not carry.** ROUTED to Grant/orchestrator; **this lane edits nothing and rules nothing on it.**

---

## §2 — DELIVERABLE 2: THE COUPLING LAW → `REPRODUCES-WITH-IMPORT` (CLOCK-1) / `FAILS` (CLOCK-S)

### §2.0 ⚑ DATED SURFACE-NOTE (2026-08-11) — the clause-G source convention the prereg froze

**On the RESULT doc; the FROZEN prereg is byte-untouched and is not amended.**

The prereg renders clause G as `−∇·[κD(A)∇ε₁₁] = T₀₀` (§0 row 3, §1). **That rendering predates `a00eda3c`'s R49(a) landing.** The ratified axiom text now carries the 4π source convention explicitly — `eq_axiom_5.tex` @ `7adbc187` reads `-\nabla\cdot\!\left[\kappa\, D(A)\,\nabla\varepsilon_{11}\right] = 4\pi\,T_{00}`, matching `gordon-optical-metric.md:25`'s `4\pi Mc^{2}\delta^{3}(r)`, which is the convention under which `ε₁₁ = 7GM/c²r` is the solution the lane consumes. **The ratified form is `= 4πT₀₀`.**

**Nothing moves.** The prefactor enters **no target of this lane**: deliverable 2 consumes the *solved profile* `ε₁₁ = 7GM/c²r` (already the 4π-convention solution), and deliverables 1 and 3 turn on the *time* structure of the law, on which a source prefactor is silent. The note is here so the prereg's rendering is not mistaken for a competing convention, and so the phantom's withdrawal is correctly attributed: the withdrawn "4π seam" finding died because its **quote was fabricated by attribution**, not because the rendering difference was imaginary. **Withdrawal class: PHANTOM-BY-ATTRIBUTION.**

### §2.1 Both branches, sympy-exact, two engines

Common frozen inputs: `ε₁₁ = 7GM/c²r` (`gordon-optical-metric.md:33`, clm-rd9cjm); `A ≡ (2/7)ε₁₁ = 2GM/c²r = r_s/r`; `m = ħω₀/c²`; `E = ħω_∞`.

- **CLOCK-1** (canon's slope-1 local clock, `temporal-spatial-lattice-decomposition.md` W2 walk-back): `ω_∞ = ω₀√(1−A)`, giving

  > `F/F_Newton = (1 − r_s/r)^{−1/2}` — **exactly GR's static-observer PROPER force `m·a = (GMm/r²)(1−r_s/r)^{−1/2}`, equivalently `−∂_r E_∞` in Schwarzschild coordinate `r`** (`sympy`: `ratio − 1/√(1−2GM/c²r) ≡ 0`). `G⁰` term = 1 exactly; numeric at the solar surface `1.000002123`. **`ħ` cancels; the target is `𝒜_g`-independent (G-AGFREE; §4 states the basis precisely).**
  >
  > **⚑ NAME THE OBJECT (relabel, 2026-08-11 review).** The derived quantity is **NOT** the force at infinity. The force applied at infinity through an ideal string is `F_∞ = √(1−r_s/r)·m·a = GMm/r²` **exactly, with no correction at any order** (Wald §6.3) — the two differ by the redshift factor, which is precisely the coordinate-`r`-vs-proper-length conversion `dl = dr/√(1−r_s/r)`. Both are now emitted and machine-checked by the driver (`force_AT_INFINITY_ratio_expression = 1`). The earlier "static force-at-infinity" wording named the wrong one of the two GR quantities; the algebra is unchanged, the label is corrected.

- **CLOCK-S** (Ax4 kernel route, `c_eff = c₀√S = c₀(1−A²)^{1/4}`): `F/F_Newton = A = 4.246265×10⁻⁶` at the solar surface — short of Newton by **`2.355×10⁵`** — and the **wrong radial power** (`1/r³`, not `1/r²`). Branches separate by **5.37 OOM**.

  > **⚑ NUMERAL CORRECTION (2026-08-11 review, F9).** The frozen prereg §3.2 quotes `A = 4.2461×10⁻⁶` (`r_s☉ = 2954.008 m`); this document previously repeated `4.246094×10⁻⁶` and called both frozen §3.2 numbers *"reproduced exactly"*. They are not: the prereg's numerals were computed **before** the driver's canonical-source repair, when solar mass was hard-coded. With `M_SUN` imported from `ave.core.constants` the driver returns `r_s☉ = 2954.1266 m` and `A = 4.246265×10⁻⁶` — a `4.0×10⁻⁵` relative shift, and the under-prediction factor moves `2.3551×10⁵ → 2.35501×10⁵`. **The prereg is FROZEN and byte-untouched**; this is the result-side disclosure of the repair, not a criterion change. The number check now **tests** the live numeral (and asserts the stale one no longer matches, so the disclosure cannot rot into decoration). No verdict moves: the branch separation is 5.37 OOM either way.

### §2.2 ★The fireable content: the kernel-clock lemma, WIDENED (the clock adjudication)

The prereg pre-declared CLOCK-1's arithmetic ENTAILED and pre-fired the tautology filter on it. Tier-2 **confirmed** that and strengthened it: the agreement is not leading-order but *exact* **on the `g₀₀` function in Schwarzschild coordinates**, so even the residual there is GR-imported, and **zero residue of new content survives**. The genuinely fireable question was therefore never "does CLOCK-1 reproduce Newton" — it was **which clock canon forces**, and that adjudication produced a structural lemma. **It is stated here at its true width; the `S^p` form this document previously shipped is a proper subset.**

> **★THE LEMMA (widened, 2026-08-11 review).** Let `S(A) = √(1−A²)` be Axiom 4's kernel and let the clock law be **any** `W = f(S)` with `f` differentiable at `S = 1` and `f′(1)` **finite**. Then
>
> `dW/dA|₀ = f′(1)·dS/dA|₀ = 0` **identically**, because `dS/dA|₀ = 0` — the kernel is *quadratic* in `A` at the origin (`S = 1 − A²/2 + O(A⁴)`).
>
> The surviving leading term is `W ≈ f(1) − ½f′(1)A²`, so with `A = r_s/r` and `E = ħω₀W`:
>
> **`F = −∂_r E = −ħω₀ f′(1) r_s²/r³` — a `1/r³` force, for every admissible `f`.**
>
> **`W = S^p` is the special case `f′(1) = p`**, returning the previously-shipped `F = −pħω₀ r_s²/r³`. The family that dies is not the one-parameter power family; it is **the whole `C¹`-functions-of-the-kernel class.**
>
> **ESCAPE CONDITION, stated positively:** a clock survives iff **`dW/dA|₀ ≠ 0`** — i.e. iff it is **leading-order LINEAR in `A`**. No `C¹` function of `S` supplies that (`S` has no linear term in `A`), so an escape requires `f′(1) → ∞`, a non-`C¹` branch point at `S = 1`. Canon's surviving realization is exactly that: the **slope-1 lapse** `W = √(1−A)`, with `dW/dA|₀ = −½`, which is the coefficient Newtonian gravity requires.
>
> **⚑ NAMED NON-ESCAPE — the gradient-observable rescue does not work either.** The one canon rule that could plausibly re-key the clock (*"only spatial gradients of `A` across the substrate are physically observable"*, KB `CLAUDE.md`) suggests keying the clock on `|∇ε₁₁|` rather than on `A`. It fails by the same arithmetic: `|∇ε₁₁| = 7GM/c²r²`, so a clock linear in it gives `E = ħω₀(1 − λ·7GM/c²r²)` and **`F = −2λħω₀(7GM/c²)/r³` — `1/r³` again, for every coupling `λ`.** Because the radial power is `λ`-independent, no choice of coupling strength recovers `1/r²`. (Driver leg L5; radial powers recomputed in plain `math` from two-radius samples.)

This kills the CLOCK-S **family** — and now a strictly larger class than the one previously named — not merely the branch, and the run could genuinely have come out otherwise: it is an ADJUDICATION, not a demonstration.

#### §2.2a ⚑ WHAT THIS COLLIDES WITH IN CANON — a named contradiction, ROUTED, not resolved

The previous version of this section closed by asserting a resolution: *"the kernel governs the constitutive modulus, and the clock is the slope-1 lapse."* **That was a resolution PROPOSAL shipped as fact, and it is withdrawn as a statement of corpus state.** The corpus does not say that. It says two incompatible things with **one symbol**, and this lane's job is to name the contradiction, not to pick the winner:

| # | file:line | verbatim | what it makes `S` |
|---|---|---|---|
| 1 | `manuscript/ave-kb/vol3/gravity/ch02-general-relativity/saturating-modulus-and-backreaction.md:128` | *"deeper in the well the compliance reduces ($S\downarrow$, bulk stiffens), the local clock $\omega_{\text{local}}=\omega\sqrt{S}$ down-regulates; since $E=\hbar\omega$ and $m=E/c^2$, matter in the well weighs less (the mass defect)"* | **the Axiom-4 kernel**, `S = √(1−A²)` — i.e. `W = S^{1/2}`, an `f′(1) = ½` member of the class the lemma above weak-field-falsifies |
| 2 | `manuscript/ave-kb/vol3/gravity/ch01-gravity-yield/temporal-spatial-lattice-decomposition.md:28` | *"The genuine local clock rate / gravitational redshift is a slope-1 quantity: $\sqrt{g_{00}} = \sqrt{S} \approx 1 - GM/rc^2$, so $z = GM/rc^2$ (the $c_{shear}$ clock — what a clock sitting at $r$ ticks)"* | **the metric lapse-squared**, forcing `S = g₀₀ = 1 − r_s/r` — the ESCAPING linear clock |

**These are two different functions of `r` wearing one symbol.** `√(1−A²)` and `1−A` agree only at `A = 0` and `A = 1`; they differ at first order in `A`, which is exactly the order gravity lives at. Site 1 and site 2 are, moreover, **the same construction** — `E = ħω`, clock riding the well, energy gradient is the force. So the collision is not a notational nicety: **read with site 1's `S`, the construction produces `1/r³` and no Newtonian gravity; read with site 2's `S`, it produces `1/r²` exactly.**

**⚑ And the collision is LOAD-BEARING downstream.** `saturating-modulus-and-backreaction.md:174-176` states the Stage-4 target as: *"it is the saturation of the frequency down-regulation in the strong field, where $S\to0$ at the yield shell, $\omega_{\text{local}}\to0$ (the clock freezes) and AVE's $\sqrt{S}$ can peel from GR's $\sqrt{1-r_s/r}$"* — a sentence that only *has* content on **site 1's reading** (it names `√S` and `√(1−r_s/r)` as two different things that can peel apart). **If site 2's reading is the right one, `√S` IS `√(1−r_s/r)` and there is nothing to peel.** So the Stage-4 peel target — the strong-field chord carried in the gravity KB — **rests on the reading this lemma weak-field-falsifies.**

**This lane rules nothing here.** It does not choose the reading, does not edit either file, does not touch the Stage-4 target, and does **not** claim the peel is dead: a two-`S` resolution (kernel for the modulus, lapse for the clock) is *available* and would preserve both — but it is **not written in the corpus**, and inventing it is precisely the move this section previously made and now retracts. **Routed to the auditor lane as ONE item, bundled with §2.4's `ε₁₁`/`A` collision (§6 item 3).**

#### §2.2b ⚑ REFINEMENT of the walk's C9 mechanism — the FORM survives, the KEYING dies

`research/2026-08-06_rotation-substance-ontology_framing-note.md:177-186` states the walk's coupling mechanism as *"**saturation** → clock down-regulation → ponderomotive drift"*: *"a trapped resonance saturates its nodes → the local resonant frequency down-regulates → a second resonance's energy E = ħω(local) is lower toward the bias → the energy gradient is the attractive force."* The lemma cuts that sentence in exactly one place:

- **The mechanism FORM survives, intact.** Clock gradient → energy gradient → ponderomotive-class attraction is the surviving chain, and on the slope-1 lapse it delivers `F/F_Newton = 1` exactly. Nothing in the lemma touches the *shape* of the argument.
- **The walk's SATURATION-KEYING dies.** "Saturates its nodes" keys the clock to the Axiom-4 saturation kernel `S(A)` — an `f(S)` law — and **every `C¹` such law is weak-field-falsified above.** The `A²`-quadratic onset that saturation-keying implies is exactly what removes the `1/r²` term.
- **The surviving realization is the slope-1 lapse**, `ω_∞ = ω₀√(1−A)`, which is *linear-onset in the bias* rather than saturation-onset. That is a different physical statement about what the node clock reads, and it is the one the arithmetic forces.

So framing-note C9's own frozen check (*"the unification is an ECHO until the force MAGNITUDE reproduces the canon chain"*) is answered **YES on the lapse keying, NO on the saturation keying** — with the ECHO verdict of §2.3 standing on the surviving branch. **The walk seeds remain UN-AUDITED INPUT; this refines what the lane adjudicated, and promotes nothing.**

### §2.3 The unification claim, adjudicated

The walk's claim was *"redshift, lensing, and attraction from ONE mechanism — the node clock riding the bias."* **Verdict: TRUE AT THE FIELD LEVEL, ECHO AT THE COEFFICIENT LEVEL.** One field `ε₁₁` does feed all three, but the three observables consume **three different projection coefficients** — `1/7` (local clock/redshift), `2/7` (transverse index/lensing), `9/7` (matter-wave parallax) — and those coefficients are the GR-imported `/7` chain (#261, untouched). The unification does not reduce three constants to one; it re-expresses three imported projections of one imported profile. **No chord. PEER-with-GR** — and, per the symmetric standard: GR carries exactly this structure (one metric potential, several observables, different weights) and is not criticised for it. The criticism here is not that AVE echoes GR; it is only that the walk's wording implied coefficient-level unification that the algebra does not deliver.

### §2.4 Counter-arms C7 / C8, and two ROUTED inconsistencies

- **C7** (*"assumes the trapped resonance's total energy tracks the local clock linearly — underived"*) — **remains LIVE and is not discharged by this lane.** `E = ħω_∞` with no further bias dependence is an *assumption*; mode-volume change, `D(A)`-graded confinement, and `c_eff`-graded resonator size are candidate additional dependences that were enumerated but not closed. **CLOCK-1's exactness is conditional on C7.**
- **C8** (regime declaration owed) — **discharged**: the bias is static and the resonance's clock is the fast scale; the slowly-varying-bias condition holds by many orders for every case evaluated.
- **★ROUTED FOR VERIFICATION (flag-don't-fix; NOT asserted, NOT resolved here) — restated precisely.** The dialect reconciliation `A = (2/7)ε₁₁` passes **algebraically** (driver L1: `A = 1` at `r = r_s`, exact), but the axiom file uses **two symbols for one quantity, and drops the conversion factor between them**:

  | file:line | verbatim | the quantity it names |
  |---|---|---|
  | `eq_axiom_4.tex:10` | *"the same $S(A)$ function governs strain expressed as … $r_s/r$ (gravitational metric strain)"* | **`A`**, the normalized strain, in its gravitational dialect: `A = r_s/r` |
  | `eq_axiom_4.tex:24` | *"BH event horizon $\varepsilon_{11}(r) = 1$ matches Schwarzschild $r_s = 2GM/c^2$ **exactly**"* | **the same quantity**, written `ε₁₁` |
  | `eq_axiom_4.tex:56` | *"Near a massive defect, $\varepsilon_{11} \to 1$"* (→ `G_shear → 0` → horizon) | **the same quantity**, written `ε₁₁` again |

  **The missing factor is `ν_vac = 2/7`.** `:10` names the gravitational strain correctly as `r_s/r`; `:24` and `:56` write `ε₁₁` for that same object. But canon's `ε₁₁` is the **profile** `ε₁₁ = 7GM/c²r = (7/2)(r_s/r)`, so `ε₁₁ = 1` sits at `r = 7GM/c² = 3.5 r_s`, **not** at `r_s`. Read literally, `:24` and `:56` place the same surface at two different radii. The reconciliation `A = (2/7)ε₁₁` is exactly what makes `:10` and `:24` agree — **and it is not written in the axiom file.** *(Orchestrator-verified by direct read; the surrounding argument that surfaced it drew its own MAJOR correction at Tier-2, so the **finding** is routed and the **analysis around it** is not banked.)*

  **⚑ This is the SAME class of defect as §2.2a's `√S` collision — one symbol, two functions, in a single-source-of-truth file — and the two are bundled as ONE routable auditor item (§6 item 3).** Related: `eq_axiom_4.tex`'s own footnote already flags the `c_shear` **sign/ontology selector as PENDING Grant**; this lane's widened kernel-clock lemma is a new input to that pending ruling, and §2.2a is the second.

---

## §3 — DELIVERABLE 3: THE SETTLING SIGNATURE → `BLOCKED-ON-D1` (⚑ OFF-GRAMMAR)

> **★OFF-GRAMMAR DECLARATION (2026-08-11 repair, F8d). `BLOCKED-ON-D1` is NOT one of D3's frozen labels.** Prereg §4 freezes exactly three: *"`SIGNATURE(observable + magnitude)` / `NULL-PEER-WITH-GR` / `UNCONSTRAINED-BY-RETRIEVAL`"*. The lane returns none of them, and says so rather than bending one to fit:
>
> - **`NULL-PEER-WITH-GR` is not awardable** — its receipt is the clause-G slaving map, which §1 found is not derived and may not exist off-static. Awarding it would be awarding a bin whose admission price is unpaid, which is the exact failure G-PRICE exists to stop.
> - **`UNCONSTRAINED-BY-RETRIEVAL` is false on its face** — both comparators *were* retrieved and are banked (prereg §2; `port-register.md:87,:93`).
> - **`SIGNATURE(observable + magnitude)` is not awardable** — D1 produced no delay, so there is no magnitude to ship.
>
> **The frozen grammar has no bin for "the null's premise is underived."** Unlike D1's grammar, which carries an explicit `NOT-DERIVABLE` fallback, D3's does not. Per Rule 11 the lane does **not** drop or widen a criterion post-hoc to manufacture a fit; it reports **off-grammar, with the reason**, and routes the grammar gap itself (§6). *Reason for the gap, stated once: D3's three labels were written assuming D1 would land a bin; the prereg did not anticipate that D3's conditional branches could be blocked upstream rather than resolved.*

The frozen §3.4 partition has exactly **two** conditional branches. The route shipped **three**, and the extra one — *"`η ≠ 0` with no pole ⇒ SIGNATURE + UNCONSTRAINED-BY-RETRIEVAL"* — **is literally the prereg's own first disjunct wearing the opposite routing**: `:108` fires F1 on `c_g ≠ c` **alone**, carrying no pole qualifier. The invented branch converted an arc-level kill into a comfortable "no comparator bounds this" null. **Caught at Tier-2 (Rule 11), struck before presentation. The verdict ships as two branches.**

- **(a) `bin = SLAVED or LC-HYPERBOLIC with c_g = c and no pole` → NULL-PEER-WITH-GR, but `BLOCKED-ON-D1`.** The null inherits the fast layer's cancellation **through the clause-G slaving map** — which §1 just found is not derived and may not exist off-static. Per `eq_axiom_5.tex` (c1), *"the finite propagation speed of the bias is owed, not held."* **The null is therefore conditional, not receipted**, and the lane does not bank it.
- **(b) Any `η ≠ 0` → ★F1 FIRES.** Reported per the frozen routing: the radiated compression fraction against `κ_max² = 1.3×10⁻⁴` (§1.3, order-of-magnitude grade). **STOP to Grant. No rescue chosen, none constructed.**

**What survives as physics, at supplementary grade (robustness ladder §3.5 — form-end rungs only):**

- **The aberration cancels identically at every order in `v/c_g`.** The naive "finite speed ⇒ lag angle ⇒ orbital decay" argument is **wrong here for the same reason it is wrong in GR and EM** (this is why Laplace's classic speed-of-gravity bound is invalid). The lane navigated the trap rather than falling into it, and says plainly that the cancellation is **the same theorem GR and EM carry** — peer, not distinct. The real observable is a `γ_g`-renormalisation of the effective `GM`, not a lag.
- **Two overclaims struck at Tier-2.** (i) The verdict's *"verified: it reproduces the R8 receipt's 3.0% analytically"* is **FALSE at the receipt's second data point** (analytic `0.1121` vs measured `0.2031`; analytic `p = 1.993` vs R8's `2.765`) — the route held the falsifying number and reported only the agreeing one. **Struck; R8 is not independent confirmation of anything here.** (ii) The R30 bench figures were computed for a modulated **monopole**, which clause S forbids and the route's own earlier section had already killed; the honest near-zone lag scaling is `∝ 𝒬_ℓ ω^{2ℓ+1} r^ℓ / c^{2ℓ+1}`, **`r`-invariant only at `ℓ = 0`**. **The "clock beats gravimeter by ~10⁹" claim is STRUCK.**
- **R30 design input, at the honest grade:** the lane hands R30 the *scaling law* above and the statement that a **lag** experiment on the bias sector has no `ℓ = 0` channel available to it. It hands R30 **no sensitivity target**, because D1 did not produce a delay to target. Requirements-derived-first is not satisfiable until D1 lands.

---

## §4 — GATE TABLE (frozen §5; UNRUN ≠ PASSED)

| Gate | Score | Basis |
|---|---|---|
| **G-LIVE** | **PASS** | All three frozen positive controls read correctly on the SAME pipeline: `p = 2.0000 / 0.5000 / 1.0000` vs frozen `2.0 / 0.5 / 1.0`. Negative control (clock-free elliptic solve) **UNRESOLVABLE** as required. Declared-vs-recomputed reconciled. |
| **G-TAUTOLOGY** | **PASS** | `𝓘 = 2ξ_Machian ρ_bulk ℓ_node²` proven a **definitional identity** symbolically (residual exactly 0); both premises checked (`ρ_bulk = m_e/2ℓ³` to `6.5e-11`; `ξ = c²ℓ/7Gm_e` to `1.2e-16`). The `6.5e-11` residual is traced to `constants.py` carrying two numerical routes to α — an artefact of the file, not of physics. Reported as an identity throughout. |
| **G-AGFREE** | **PASS** | No target consumes a value for `𝒜_g`. **Basis stated precisely (2026-08-11 repair):** `𝒜_g` is introduced as a live sympy symbol and carried through the one relation canon gives it (`u₀ = −𝒜_g∇ε₁₁`); the deliverable-2 target is then shown **`𝒜_g`-INDEPENDENT on the expression tree** — `𝒜_g ∉ free_symbols(target)` **and** `∂target/∂𝒜_g ≡ 0` — not by string inspection of a printed form. **Honest wording: this is an INDEPENDENCE proof, not the cancellation of a term that was ever present** — `𝒜_g` never enters `E = ħω_∞(A)` at all. Detector liveness is shown the other half of the pair: on a declared **counterfactual** control in which the energy is allowed to depend on `u₀`, the same check **does** see `𝒜_g`. |
| **G-POLE** | **PASS** | Run and reported separately from `p`, with the LC-vs-SLAVED degeneracy stated and broken (§1.3). **Scoped 2026-08-11:** the test's validity condition is `c_g ≲ O(c)`; the `(c/c_g)^{2ℓ+1}` suppression is stated at every site, and the banked-vs-lane-rescaled exclusion ranges are separated. |
| **G-PRICE** | **PASS (by refusal)** | No bin was awarded without its price. Every D1 bin ended NOT-AWARDED / INAPPLICABLE / withdrawn. |
| **G-AX3** | **PASS** | No loss word in any derived statement; the diffusive bin's ledger obligation is reported moot, not waived. The `\|H\| = 1` amplitude receipt is the port test, run. |
| **G-ENGINE** | **PASS** | `git diff --stat ad376e9a..HEAD -- src/` empty at every commit. |
| **G-NUM** | **PASS** | **95 checks green** (was 32, then 90; this micro-pass added the `|∇ε₁₁|`-boolean reconciliation and the four F3-arms checks); mutation receipt fires **4** detectors; two runs byte-identical; `make verify` green (EFT gate `MATHEMATICALLY PURE`). **Declared-boolean coverage:** all **30** declared booleans the checker consumes are paired with a recomputation from the JSON's own inputs, and a structural gate FAILS if any pairing goes missing. **★Scope of that claim, stated precisely (2026-08-11 delta re-verify):** what is **machine-held** is the **30 registered declared↔recomputed pairs** — remove any one reconciliation and the gate FAILS (probed live). What is **not** machine-held is **registry completeness**: an unregistered declared-boolean consumption injected into the checker passes silently, so "every consumed boolean is registered" was **hand-verified** by the 2026-08-11 delta re-verify and is **narrated, not gated**. Several declared **non-boolean** fields are likewise consumed against frozen targets without recomputation. **The `--mutation-receipt` driver run now writes to a temp path**, so a receipt run can no longer dirty the tracked JSON. |
| **G-VACATED** | **PASS** | The 2026-07-20 envelope lane's P-branch legs were re-derived, never inherited; its NLS-class cold-vacuum reading is retired at this rung by F2. |
| **G-SECTOR** | **PASS** | Zero Cosserat/T2 consumption; `I_ω` explicitly enumerated and REFUSED at the fence. |
| **G-BANKED** | **PASS** | #261 and the bound-constitutive pole-absence receipts consumed as inputs; no relitigation. |
| **G-OBJECTS** | **⚑ FAIL-THEN-REPAIRED** | All three D1 routes crossed the bias/bound-response/`(u,π)` split — two by importing R8's `(u,π)` frequency structure onto the bias, one by projecting a vector constraint onto its divergence. **Caught at Tier-2, bins withdrawn.** The gate did its job; the routes did not pass it. |
| **G-QUOTE** | **PASS** (re-scored 2026-08-11 against the FROZEN criterion) | **Frozen criterion, verbatim (prereg §5):** *"Every ruled/frozen quote byte-checks, two engines, with a committed quote gate + `--mutation-receipt`."* The gate the criterion names is now **committed**: [`bias_propagation_quotes_number_check.py`](drivers/bias_propagation_quotes_number_check.py) — **29 quotes, two engines each** (subprocess `grep -F` + Python `str.find`), `--mutation-receipt` fires. **★Scoped 2026-08-11:** the **29 quote registrations are hand-curated**; what `make verify` auto-discovers is the **script**, via the umbrella glob. So byte-exactness of each registered span is machine-held, while **completeness of the registry is curation, not a gate** — the 29th row was added this pass after the delta re-verify caught a quoted span (§1.4's FORK-2 consequence) rendered without its verb. **6 of the 29 are retrieved at a PINNED rev** (`git show 7adbc187:…`) rather than through a branch name, which is the structural fix for the stale-blob failure. Previously scored `FAIL-THEN-REPAIRED`, which was a **criterion substitution** — a narrative about incidents rather than the machine check the prereg froze. **The incidents stand on the record** (one quote fabricated by attribution, one stale-blob cite; both caught at Tier-2, both findings WITHDRAWN, no phantom reaches Grant) and are reported at §5 — but they are the Tier-2's receipt, not this gate's score. |
| **G-ECHO** | **PASS** | D2's verdict carries the DEMONSTRATED-not-DERIVED / PEER-with-GR qualifier in the same sentence as the award. |

---

## §5 — TIER-2 DISPOSITION

Run **before** presentation: 6 derivation routes × 3 adversarial lenses (physics-knife / frozen-criteria / seductive-narrative), 24 agents, per-finding refutation with default-to-refuted. **18 verdicts: 9 CRITICAL, 8 MAJOR refuted, 1 MAJOR not-refuted.** Every D1 bin award was withdrawn; D3's branch set was collapsed from three to two; D2's two bin awards **survived** (both verifiers that attacked them confirmed the bins and refuted only the surrounding headline wording, which is repaired above).

**★THE 1 NOT-REFUTED MAJOR, named (2026-08-11):** it is the **exclusion-arithmetic** finding against §1.3's radiated-fraction number — the objection that the route's Lagrangian normalisation was fixed by a check structurally blind to the coefficient, and that the O(1)-coupling bracket was transferred from the envelope lane as a declared deviation. **It was not refuted, and it stands.** Its standing disposition: §1.3's exclusion figure is demoted to **ORDER-OF-MAGNITUDE ONLY**, its digits are **not banked**, and §6 item 4 (`port-register.md:87` Q1 row) is routed **as a question with no recommendation** for the same reason. **This review pass adds a second, independent limitation to the same number** — the `(c/c_g)^{2ℓ+1}` validity scope (§1.3) — which narrows where the constraint can be read at all, and does not depend on the coefficient question either way.

**⚑ RECORD SCOPE (F10 disposition, stated rather than manufactured).** The lane's Tier-2 **per-finding** records were not committed by the (now-closed) route lane; only the aggregate above survives in the tree. **This document is therefore the sole record of the Tier-2 dispositions**, and this pass deliberately does **not** reconstruct a per-finding table from the summary — post-hoc reconstruction would manufacture provenance of exactly the kind this lane's two phantoms were made of. What the pass *does* commit is a companion carrying the **new** derivation content it produced, with equations and machine checks: [`2026-08-10_bias-propagation_derivation.md`](2026-08-10_bias-propagation_derivation.md).

**The Tier-2 earned its cost three times over:** it caught (1) a fabricated-by-attribution quote, (2) a stale-blob citation manufacturing a phantom axiom ambiguity, (3) an invented verdict branch that routed an arc-level kill into a benign null, (4) a cherry-picked one-of-two data points reported as "verified", (5) a bench requirement computed for a source the axioms forbid, and (6) the common instantaneous-kernel defect that inverted all three D1 stories. **Items (1)–(2) would have put two false findings in front of Grant.**

---

## §6 — ROUTING (zero corpus edits; everything below is ROUTED, nothing executed)

1. **The Ax5 (c1) named-open entry stays OPEN.** This lane does not discharge it. **Proposed** for the entry, never landed here: the enumerated missing structure of §1.4 and the pole-exclusion constraint of §1.3 (a completion cannot be a radiating line).
2. **The clause-G static-limit question (§1.4 item 3) → Grant/orchestrator.** It bears on the bound-constitutive lane's §2.6 item 3 causality leg and on FORK 2's disposition. **Flagged, not ruled, not edited.**
3. **★ONE ROUTABLE AUDITOR ITEM: two symbol collisions in the gravity-clock chain, both LOAD-BEARING, neither ruled here.** *(Re-routed 2026-08-11. This item previously read as "a new input to the `c_shear` selector," which understated it: the lemma does not merely inform a pending ruling, it puts two canon sites in direct contradiction. The `c_shear` selector remains a downstream consumer, not the destination.)*

   - **(3a) `√S` — the CLOCK collision (§2.2a).** `saturating-modulus-and-backreaction.md:128` runs the local clock as `ω_local = ω√S` on Axiom 4's kernel `S = √(1−A²)`; `temporal-spatial-lattice-decomposition.md:28` writes `√g₀₀ = √S ≈ 1 − GM/rc²`, which **requires** `S = 1 − r_s/r`. **Same symbol, same `E = ħω` construction, two different functions of `r`.** The widened lemma weak-field-falsifies the first reading (`1/r³`, no Newtonian gravity); the second is the escaping slope-1 lapse. **Consequence to flag with the item: `saturating-modulus-and-backreaction.md:174-176`'s Stage-4 strong-field peel target — AVE's `√S` peeling from GR's `√(1−r_s/r)` — has content ONLY on the falsified reading.** A two-`S` resolution is available but is **not written in the corpus**; this lane proposes none.
   - **(3b) `ε₁₁` vs `A` — the STRAIN collision (§2.4).** `eq_axiom_4.tex:10` names the gravitational strain `r_s/r` (that is `A`); `:24` and `:56` write `ε₁₁` for the same quantity. **The missing factor is `ν_vac = 2/7`** — on canon's profile `ε₁₁ = 7GM/c²r`, the `ε₁₁ = 1` surface sits at `3.5 r_s`, not `r_s`. The conversion `A = (2/7)ε₁₁` reconciles them and is not stated in the file.

   **Bundled as ONE item** because they are the same defect class (one symbol, two objects, in single-source-of-truth files) on the same chain, and because a fix to either that ignores the other will re-open it. **Downstream consumer, carried with the item, not as its headline:** `eq_axiom_4.tex`'s own footnote flags the `c_shear` sign/ontology selector as **PENDING Grant**; both collisions are inputs to that ruling. **This lane edits nothing, rules nothing, and proposes no resolution text.**
4. **`port-register.md:87` Q1 row — NOT EDITED, and deliberately not adjudicated here.** Whether its LIVE pulsar exclusion is VACATED-BY-DRIFT (its gapless-P-branch premise was removed from the receipted dynamics by the 2026-08 lanes) is a real question this lane surfaced but did **not** settle: the route that examined it drew a confirmed MAJOR on its exclusion arithmetic. **Routed as a question, with no recommendation.**
5. **D3/R30:** no bench sensitivity target is deliverable until D1 lands. The `𝒬_ℓ ω^{2ℓ+1} r^ℓ/c^{2ℓ+1}` scaling and the no-`ℓ=0`-channel statement are the only design inputs banked.
6. **The lane's fence on itself:** no KB edit, no register edit, no solidity move, no ledger edit, no un-revert, on any outcome. The §10 walk seeds remain UN-AUDITED input and are not promoted by being cited here.
7. **The D3 GRAMMAR GAP itself → prereg-methodology, not physics.** D3's frozen label set has no bin for "the branch is blocked upstream" (§3). The lane returns off-grammar rather than widening the set post-hoc. **Whether future preregs should carry an explicit `BLOCKED-ON(<deliverable>)` label alongside the three is a methodology question, routed, not decided here** — and it is a KEEP-BOTH-shaped question (add a label, never redefine one).
8. **The pole test's `(c/c_g)^{2ℓ+1}` validity scope (§1.3) → carried with any downstream use of the exclusion.** Any later citation of "a pole-bearing completion is excluded by the pulsars" that omits `c_g ≲ O(c)` is citing a claim this lane does not make. Recorded here so the scope travels with the constraint.

---

> **Result-doc provenance.** Frozen prereg committed ALONE + pushed at `d4bee683` before any driver code existed — byte-untouched since; driver committed second (`e6177c94`); this result last. Two engines wherever a number carries a verdict (sympy exact + mpmath 50-digit, with the number check re-verifying in plain `math`). Liveness proven in both directions before any `p` was reported. Canonical-source repair disclosed: the driver's first cut hard-coded solar mass and the repo's own EFT gate caught it; `M_SUN` is now imported and the solar radius is declared ENG-CHOICE at its use site (it scales an exhibit, never a verdict). **The numerals that repair moved are corrected and dated at §2.1 (F9); the prereg's pre-repair figures are FROZEN and left alone.** Tier-2 run before presentation (§5); every confirmed finding is repaired in place or its award withdrawn above. **Review-cycle repair pass 2026-08-11** (see the header note), then a **delta re-verify micro-fix pass** the same day (the F3 disclosure's stated reason corrected to the arithmetic fact; the FORK-2 span's verb restored and the span registered; the "machine-held" and "auto-discovered" claims scoped; one mis-mapped registry row repaired): all counts re-taken after the edits — 95 number checks, 30 registered declared-boolean reconciliations, 29 two-engine quotes, 4 mutation detectors. **Nothing in this document reads CLEARED.** `[DO-NOT-MERGE]`.
