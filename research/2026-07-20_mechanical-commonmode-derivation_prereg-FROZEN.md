# Mechanical Common-Mode Derivation — FROZEN BINS + three-leg adjudication criteria + Leg-C frozen observable (pre-registration)

**Date:** 2026-07-20
**Class:** PRE-REGISTRATION (frozen bins + per-leg adjudication criteria + Leg-C frozen observable/tolerance/grid; committed ALONE, BEFORE any derivation or driver run, per the frozen-criterion-only process rule that is ACTIVE for this lane family). This doc mints no `clm-`, edits no KB/tex/engine leaf, and states the criteria the companion result (`research/2026-07-20_mechanical-commonmode-derivation_result.md`) is adjudicated against.
**Provenance:** Grant-fired 2026-07-20 (verbatim `[sic]`: `"fire 1 and 2, then 3 once those land"` — this is item 1, THE make-or-break derivation the gravitational sector's survival rests on). Inherits the re-banked #758 state (`research/2026-07-20_b-mechanism-derivation_result.md`): the candidate `T_d` structure's **bare** scatter PRESERVES the A1 common mode (`+1`, `clm-j550uh` 0.85); the A1→T2 emptying is `clm-9kd2t3` (0.42, **do-not-build**, "asserted rather than shown", saturation-gated, EM-`∇·E`-scoped). **Nothing closes the port at derived grade going into this lane** — the job is to derive the closure or establish NONE-DERIVES.
**Lane fences:** DERIVATION + research-driver lane only. **READ-ONLY on KB** (`manuscript/`, `manuscript/ave-kb/` untouched — including the port-register Q1 row, whose consequence is ROUTED not edited). No engine edits (the Leg-C driver imports `ave.core.*` read-only and touches no engine primitive). Every `[canon]` input content-verified two-method at HEAD (`git rev-parse HEAD` recorded in the result doc; verify-before-cite).

---

## §0 — The question, stated precisely (per the re-banked #758)

The pulsar exclusion (BANKED — falsification ledger; Reading-A extra flux `F_bulk/F_shear ≈ 0.03–0.12` excluded at **9–110σ** Hulse-Taylor / **100–1400×** the double-pulsar bound; `research/2026-07-20_q1-pulsar-hardening.md` §2–§3) kills any O(1)-coupled far-field **bulk (A1/longitudinal/dilatation) radiation** from binaries. Grant's **Reading-B** ruling (`port-register.md` Q1 row, **RULED-CONDITIONAL**) — *no far-field radiative port for gravitating sources; the halo is its complete story* — survives ONLY if a closure is **DERIVED**. Per the Q1 row's own clause: *"if NONE-DERIVES this ruling REVERTS and the banked Reading-A exclusion goes live."*

**The target (one sentence, inherited from the #753 §6 sharpened obligation):** find the structure the vacuum **has** and a rock **lacks** — a generic isotropic elastic solid RADIATES its P/bulk channel (Aki-Richards `E_S/E_P ≈ 23.4`, every earthquake), so the vacuum's bulk sector must be provably **NON-GENERIC** at **DERIVED, COLD-REGIME** grade — or the banked exclusion goes live against the framework.

---

## §1 — FROZEN BINS (the prove-or-disprove outcome space)

The lane resolves to exactly one of three bins. **These are frozen before any derivation or driver run.** Every mechanism statement in the result carries its `clm-`/solidity inline (the mechanism-headlines-with-solidity process rule).

- **PORT-CLOSED-DERIVED** — a closing mechanism is DERIVED at cold-regime grade, with the mechanism NAMED as exactly one (or more) of:
  - *mode-absence* — the A1-dilatation sector has NO independent propagating (gapless, DC-reaching) Bloch branch a gravitating source can launch far-field; OR
  - *derived-cold-regime-emptying* — a nonzero A1→T2 conversion is DERIVED to operate in the cold-linear (`S_sat → 1`) far field (NOT saturation-gated), driving the radiative A1 content to zero; OR
  - *derived-source-decoupling* — the gravitational source's projection onto the A1/dilatation radiative port is DERIVED to vanish (a theorem, not the assumed symmetric-coupling grade-assignment).
  A trace earns this bin only if EVERY load-bearing step is `[derived]` or `[canon-read]` with no `[assumed]`/unforced load-bearing step, AND it operates in the **cold** Regime-I far field where the GW question lives (a saturation-gated closure does NOT qualify — that is the exact #758 R1(c) breach).

- **NONE-DERIVES** — all three legs fail to produce a cold-regime derived closing mechanism ⇒ Reading A is forced ⇒ **the banked pulsar exclusion goes LIVE against the framework and the Reading-B ruling REVERTS per its own clause.** This is a legitimate, bankable outcome (Rule 11 honest closure). **Anti-seduction fence (survival WANTS closed):** NONE-DERIVES must be genuinely reachable — if the lattice has a propagating compression/dilatation branch (Leg A / Leg C) AND the source drives it (Leg A source-trace) AND no DERIVED cold constraint kills it (Leg B), NONE-DERIVES wins and is banked plainly. **Reverse-seduction fence (the make-or-break lane could be seduced by a clean kill):** a kill must also be genuinely derived — apply the consensus knife both ways (does GR/SM close this port by a *derived* constraint, and does AVE have the analog?).

- **UNDETERMINED** — a candidate cold-regime closing mechanism exists but its applicability rests on an **unforced choice** the corpus carries unresolved (e.g. which of two co-carried dynamical models — K4-TLM bond-scatter (Model α) vs srs Cauchy site-elasticity (Model β) — governs the gravitational A1 coupling), AND that choice actually **controls the verdict** (one branch closes the cold port, the other opens it). Per the discipline, an unforced verdict-controlling choice ⇒ STOP at UNDETERMINED with the fork stated precisely; do NOT pick a side by fiat. **Discriminator vs NONE-DERIVES:** if BOTH branches of the unforced fork leave the cold port OPEN (neither branch closes it at derived grade), the fork does NOT control the verdict and the bin is NONE-DERIVES, not UNDETERMINED.

**Adjudication ordering rule (frozen):** any load-bearing `[assumed]`/unforced step demotes a candidate closure to UNDETERMINED (fork stated) or, if the choice's other branch is the radiating one AND the fork does not control the verdict, contributes toward NONE-DERIVES.

---

## §2 — The three legs (independent routes) + their frozen adjudication criteria

Each leg tags every step `[derived]` / `[canon-read]` / `[assumed]`. Regime/sector headers fired before any structural algebra. Sector-ownership discipline (A1 ⊥ T2, no cross-wiring). Consensus knife BOTH ways (QED/GR "longitudinal is always gauge-killed" reflex AND the AVE-native "the vacuum is a real compressible elastic solid that radiates P-waves" reflex are both held; the decider must be an AVE-native structural fact).

### LEG A — MODE EXISTENCE (★the sharpest, per #758 B4.4)
**Question:** `srs-band-structure.md:117` flags the A1-scalar bulk-sound (`√2·c` "port speed") as *"NOT a Bloch branch … not lattice-computed"* — a constitutive re-expression, not a propagating mode. Derive the ACTUAL mode content: of the survey's 3 acoustic branches (vector/site-displacement problem), which are longitudinal vs transverse, and is there a genuine **SCALAR-A1-dilatation propagating branch** at all? If the A1-dilatation sector has NO independent Bloch branch, a gravitating source cannot launch it far-field REGARDLESS of emptying — **mode-absence closes the port.** But then the VECTOR longitudinal branch (which IS Bloch, `c_P/c_S ≈ 1.71–1.90`) must be adjudicated: does the gravitational source couple to it (exclusion re-arms against THAT branch), or is that branch's longitudinal content the T2-shear-sector's own with the gravitational coupling entering only through the metric/shear channel? **Trace the source-coupling operator explicitly** (the #750 multipole source term → which lattice DOF it drives).
**PASSES to PORT-CLOSED-DERIVED (mode-absence) iff:** the adjudicated band survey + the polarization/dilatation content of its acoustic branches show NO gapless propagating branch carrying `∇·u ≠ 0` that a mass source couples to.
**FALSIFIED (mode-absence does NOT close) iff:** a gapless propagating branch carrying nonzero dilatation (`∇·u ≠ 0`) exists AND the traced gravitational source drives it.

### LEG B — COLD-REGIME EMPTYING
**Question:** derive the cold-linear (`S_sat → 1`) limit of the Op3 A1→T2 mechanism properly (the `clm-9kd2t3` leaf §4, `k4-port-irrep-decomposition.md:101-113`). Does ANY conversion survive at cold bias (e.g. a nonzero cold-limit impedance mismatch from the graded index around a mass — a STATIC galactic gradient means `z_local` varies spatially even cold — does the spatial gradient itself scatter A1→T2?), or is the cold limit exactly zero conversion?
**PASSES to PORT-CLOSED-DERIVED (derived-cold-emptying) iff:** a nonzero A1→T2 conversion is DERIVED to operate at `S_sat → 1` (cold), from a corpus-grounded mechanism, at a rate sufficient to empty the radiative A1 content over the propagation.
**FALSIFIED (this route eliminated) iff:** the cold limit of the Op3 emptying is exactly zero conversion (the impedance mismatch `Z_eff = Z_0/√S_sat → Z_0` as `S_sat → 1`, no bond-level scatter), and the static spatial gradient's A1→T2 scatter is either zero or negligible.

### LEG C — NUMERICAL INJECTION (lattice-derived empirical leg; criteria FROZEN below, BEFORE running)
**Question:** on the ACTUAL shipped srs lattice (read-only engine import), does an injected point breathing (isotropic dilatation) source radiate longitudinal far-field content, or is longitudinal content absent/evanescent?
**FROZEN OBSERVABLE, TOLERANCE, GRID (below §3).**
**PASSES to NONE-DERIVES lean (port OPEN on the lattice) iff:** the lattice radiates longitudinal content at the continuum-predicted partition (within tolerance).
**PASSES to PORT-CLOSED-DERIVED lean (mode-absence confirmed empirically) iff:** longitudinal content is absent/evanescent (below tolerance) despite the breathing source.

---

## §3 — LEG-C FROZEN CRITERIA (observable + tolerance + grid + finite-size honesty)

**Substrate-native + phase-space-coordinate check (A46).** The corpus claim is a *channel-radiation* claim (does the A1 dilatation branch carry a far-field port?). The matching test coordinate is the **longitudinal (`∇·u`, radial-displacement) vs transverse (`∇×u`, tangential-displacement) partition of the radiated field in the same displacement-vector basis the vector band survey uses** — NOT a scalar-field `φ²` proxy the corpus never claimed. `consistency-vs-emergence`: the lattice band eigenstructure is a MANIFESTATION (theorem of the srs net's `D(k)`); every VALUE reported is dimensionless (energy fractions, speed ratios) or `[import]`/`[canon]`-tagged.

**Engine substrate (read-only):** the driver imports `ave.core.chiral_lattice` (`_SRS_8A`, `_SRS_NN`, `build_srs_net` geometry) and `ave.core.constants` read-only; it builds the finite real-space srs net + the rank-2 bond-tensor mass-spring dynamics + the time-stepper IN THE DRIVER (Rule-14 reuse of the survey's `Φ_b = k_a d̂⊗d̂ + k_s(I − d̂⊗d̂)` bond model at the DERIVED bond ratio `ρ*` from `ν_Hill = 2/7`, imported not hard-coded). Engine byte-untouched.

**Two lattice-derived measurements (both frozen):**

- **C-1 (PRIMARY, time-domain "radiation at range"):** a finite BCC srs supercell of `L³` conventional cells (8 sites/cell), free (fixed-boundary) edges. Seed an isotropic breathing pulse at the center site cluster: initial displacement `u(x) = A · r̂ · g(|x − x₀|)` with `g` a narrow radial Gaussian shell (a pure outward dilatation, `∇·u ≠ 0`, `∇×u = 0` by construction), zero initial velocity. Velocity-Verlet integrate the vector EOM `ρ ü_s = −Σ_bonds Φ_b (u_s − u_t)`. At a measurement shell `r_meas` (several cells, well inside the boundary), decompose each site's displacement into **radial (longitudinal)** `u_∥ = (u·r̂)r̂` and **tangential (transverse)** `u_⊥ = u − u_∥`, and form the shell-integrated, Poincaré-windowed energy fractions.
  - **OBSERVABLE:** `f_long ≡ ⟨E_∥⟩ / ⟨E_∥ + E_⊥⟩` at `r_meas`, time-averaged over the **Poincaré window** `[t_arrival, t_reflect)` — after the pulse front reaches `r_meas` (`t_arrival ≈ r_meas/c_P`) and before the earliest boundary reflection returns to `r_meas` (`t_reflect ≈ (2·L_edge − r_meas)/c_P`). Report the window explicitly.
  - **CONTINUUM PREDICTION (frozen reference):** an isotropic breathing (monopole dilatation) source in a continuum elastic solid radiates a **PURE longitudinal (P) far field** ⇒ `f_long → 1`. Mode-absence ⇒ no outgoing longitudinal ⇒ `f_long → 0` (energy stays reactive/evanescent near source, or converts entirely to trapped transverse).
  - **FROZEN TOLERANCE (three-way):**
    - `f_long > 0.5` at `r_meas` in the window ⇒ **longitudinal radiation PRESENT → port OPEN on the lattice → NONE-DERIVES leans.**
    - `f_long < 0.1` ⇒ **longitudinal absent/evanescent → mode-absence confirmed empirically → PORT-CLOSED-DERIVED (mode-absence) leans.**
    - `0.1 ≤ f_long ≤ 0.5` ⇒ ambiguous; report as INCONCLUSIVE on C-1 and defer to C-2 + the analytic legs.

- **C-2 (CROSS-CHECK, finite-size-free spectral partition):** from the SAME lattice's Bloch dynamical matrix `D(k)`, compute the acoustic-branch eigenvectors `ê_b(k)` in the long-wave (`k → 0`) limit along a direction grid; identify the **longitudinal branch** (max `|ê·k̂|²`) and its speed `c_P` and the transverse branches `c_S`. Compute the **far-field radiated-power partition** of (a) an isotropic dilatation source and (b) a rotating mass quadrupole, from the eigenvector projections `|ê_b·Ŝ|²` weighted by the `1/c_b⁵` flux scaling, and compare to the continuum `A_ang·(c_S/c_P)⁵`. This has NO finite-size artifact (it is the exact lattice far-field density-of-modes coupling) and is the robust anchor for C-1's window read.
  - **GATE (frozen):** C-2 must reproduce the vector-survey `c_P/c_S ≈ 1.71–1.90` (direction-resolved) to `< 3%` (a lattice-derivation self-consistency gate, same class as the survey's G4); a breathing source's longitudinal fraction must be `≈ 1` in the `k → 0` limit if a longitudinal branch exists (the spectral analog of C-1's `f_long`).

**GRID (frozen):** C-1 supercell `L = 16` conventional cells (`8·16³ = 32768` sites), `r_meas = 6` cells, `A = 10⁻³` (deep-linear, sub-yield — cold regime, saturation OFF), `dt = 0.2 · dt_CFL`, integrate to `t_reflect`. C-2 direction grid: `[100],[110],[111]` + 24 random directions, `k·ℓ = 10⁻⁴` (long-wave). Bond ratio `ρ*` DERIVED from `ν_Hill = 2/7` (imported), `k_s = 1`, `ρ = 1`, `c_S = 1` (natural units; all verdicts dimensionless).

**0D/finite-size honesty (declared BEFORE running):** (i) the finite supercell has a Poincaré recurrence / boundary-reflection horizon — C-1's read is valid ONLY inside the window `[t_arrival, t_reflect)`; results are reported with the window bounds and the reflection-return time stated. (ii) A finite lattice cannot resolve a *truly* DC (`k → 0`) far field — the breathing pulse has a finite spectral width; the read is a finite-`k` proxy for the continuum limit, cross-checked by C-2's `k → 0` spectral partition. (iii) fixed (Dirichlet) boundaries reflect; no absorbing layer is used — the window discipline (read before reflection) is the mitigation, and C-2 (finite-size-free) is the anchor. (iv) the breathing seed is `∇×u = 0` by construction, so any transverse content at `r_meas` is *generated by the lattice's mode-coupling*, not seeded — `f_long < 1` with `f_long` still `> 0.5` is a real (lattice-anisotropy) P→S conversion signal, not a seeding artifact.

---

## §4 — Discipline ledger (frozen)

- **Substrate-native + EE-first** language; regime/sector headers; **sector-ownership** (A1 ⊥ T2, no cross-wiring — mass = A1-dilatation and T2-shear/winding must not be cross-wired).
- **Consensus-bias both ways:** the "longitudinal is gauge-killed" route is the QED/GR reflex (known SM/QED leakage risk); the "real compressible elastic medium radiates P-waves" route is the AVE-native reflex. Both held; the DECIDER must be an AVE-native structural fact.
- **FORM/VALUE tags** on every quantity; `consistency-vs-emergence` class on every result; the K4 scatter / band eigenstructure is MANIFESTATION (theorem), every dimensionful VALUE is `[import]`/`[canon]`.
- **Flag-don't-fix:** contradictions (K4-TLM-A1-preserved/emptied vs srs-Cauchy-P-branch-propagates; the elastic-Lagrangian-bulk-term vs EM-Gauss-kill sector question; any conflict with the Grant RULED-CONDITIONAL Reading-B) are surfaced with both file:paths + verbatim content, NOT resolved by reframing one to match the other. The port-register Q1 consequence is ROUTED, never edited (READ-ONLY on KB).
- **Rule 12 (substitution-not-retraction):** this lane mints no `clm-`; if a candidate closure is falsified the slot is NOT refilled with an assertion — the named follow-on carries its own version + verification chain.

---

> **Pre-registration provenance.** Fired by Grant 2026-07-20 (`"fire 1 and 2, then 3 once those land"` `[sic]`, item 1). Bins + three-leg criteria + Leg-C frozen observable/tolerance/grid committed ALONE, BEFORE deriving or running (freeze-by-push, real margin). Mints no `clm-`; edits no leaf; READ-ONLY on KB. Companion (post-derivation): `research/2026-07-20_mechanical-commonmode-derivation_result.md` + the Leg-C driver `research/drivers/mechanical_commonmode_injection.py` (+ `_results.json`). Docket continuation: ENTRY 37.
