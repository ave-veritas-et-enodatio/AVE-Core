# FROZEN BRANCH-SIGNATURE MAP — the astrophysical adjudicator sweep (D2)

**★ FREEZE-BY-PUSH ARTIFACT.** This map is committed AND pushed **before any retrieval output is read.** Git ordering is the freeze proof (the freeze-analog of a prereg freeze). Every signature below is frozen **verbatim-or-tightened from the handoff brief; NEVER loosened post-retrieval.** A signature relaxed after seeing the data is a freeze violation and a review-CRITICAL finding.

**Status.** Not canon. Not a ruling. This satellite **recommends**; **Grant rules**; nothing is canonized in-lane (no KB / Letter / manuscript edit — `research/` + `_orchestration/` only). Every downstream verdict is `PENDING-GRANT`.

**Provenance.** Handoff brief `_orchestration/2026-07-11_astro-adjudicator-sweep-handoff.md` (branch `docs/keying-walk-and-astro-sweep-handoff`). Seeded from the D1 keying-register walk `research/2026-07-11_keying-register-walk_framing.md` §4/§5/§6 and the collapse-target registry `research/2026-07-10_collapse-target-registry.md` (rows T3 `:46`/`:190`, T4 `:47`/`:243`).

**Freeze rule (binding).** Each adjudicator is a frozen branch-signature map: (1) what each branch predicts (the observable signature), and (2) what observed state counts AGAINST each branch (the demotion condition). **P10 fireability** is binding: an adjudicator none of whose outcomes could demote any branch is decorative and is reworked or dropped before freeze — each block below carries an explicit fireability line confirming ≥1 outcome demotes ≥1 branch. **KEEP-BOTH:** the T4 third branch (tide) rides alongside the frozen internal/total pair; the G fork keeps live/fossil/attractor together. No branch redefined-in-place.

---

## A1 — RAR ENVIRONMENTAL DEPENDENCE → decides T4 keying (internal / total / TIDE)

**SECTOR / REGIME / PHASE-STATE.** gravity-bulk (Axiom-4 `η_eff` saturation); weak-field embedded-galaxy regime; cold saturated-tail read of the radial-acceleration relation.

**Decides.** Registry T4 (`research/2026-07-10_collapse-target-registry.md:47`, "needs Grant keying decision (internal vs total field)") + the D1 §4 third branch (external TIDE).

**Signatures (frozen):**
- **internal-only** (`A` keys on the source's `g_N`) → **zero environmental dependence** of the RAR.
- **total-field** (`A` keys on internal + external `g_ext`) → **deviations track `g_ext`** (systems in strong external fields pushed toward the Newtonian branch).
- **tide** (`A` keys on `∇g_ext`) → **deviations track external TIDAL stress**, not `g_ext` itself.

**Demotion conditions:** a confirmed environmental RAR deviation that tracks `g_ext` **but not tide** demotes internal-only and tide, favors total-field; a deviation that tracks **tide but not `g_ext`** demotes internal-only and total-field, favors tide; a confirmed **null** (no environmental dependence at either axis) demotes both total-field and tide, favors internal-only. **The axes separate in data:** high-`g_ext`-low-tide systems vs high-tide systems must be compared *separately*.

**P10 fireability:** FIRES — each of the three data outcomes above demotes at least one branch (a `g_ext`-tracking deviation kills internal-only + tide; a tide-tracking deviation kills internal-only + total-field; a null kills total-field + tide).

**Retrieval anchors to verify:** SPARC RAR scatter (`~0.11 dex` claimed); Chae et al. EFE-in-RAR claims **and their contested status** (report the dispute, do not adjudicate the dispute).

---

## A2 — WIDE BINARIES vs CRATER-II-CLASS → the tide-branch discriminator

**SECTOR / REGIME / PHASE-STATE.** gravity-bulk (Axiom-4 `η_eff` saturation); weak-field regime; the discriminating split is high-`g_ext`/low-tide (wide binaries) vs low-`g_ext`/high-tide (Crater-II-class).

**Decides.** The D1 §4 tide branch vs standard-EFE vs internal-only, on systems where `g_ext` and tide **separate**.

**Signatures (frozen):**
- **standard-EFE** (total-field, MOND-as-modified-gravity) → **suppression in BOTH** wide binaries and Crater-II-class satellites.
- **tide** → **NO suppression in wide binaries** (strong `g_ext ≈ 1.8–2.0 a₀`, negligible tide → isolated-MOND-like behavior); **suppression allowed in Crater II** (strong MW tide).
- **internal-only** → **no suppression in EITHER** (Crater II then needs another mechanism to explain any observed suppression).

**Demotion conditions:** confirmed wide-binary suppression demotes tide and internal-only (favors standard-EFE); confirmed wide-binary *isolated-MOND* behavior + Crater-II suppression demotes standard-EFE and internal-only (favors tide); no suppression anywhere demotes standard-EFE and tide (favors internal-only, leaving Crater II as a separate puzzle).

**P10 fireability:** FIRES — each of the three outcomes demotes at least two branches; the discriminator is specifically the wide-binary row where tide and total-field diverge.

**Retrieval anchors to verify:** the **Chae-vs-Banik wide-binary dispute** — report the current dispute state as **contested, never a picked winner** (Chae 2023 / Pittordis–Sutherland-class analyses on opposite sides). Crater II velocity-dispersion measurements. (Reference arithmetic, D1: `g_ext ≈ 2.1×10⁻¹⁰ = 1.96 a₀` against AVE-derived `a₀ = 1.07×10⁻¹⁰`, `= 1.75 a₀` against empirical `1.2×10⁻¹⁰` — order-`a₀` either way; `research/2026-07-10_collapse-target-registry.md` T4 auditor arithmetic.)

---

## A3 — HIGH-Z ROTATION CURVES / a₀(z) → decides the WHEN axis (live vs attractor keying)

**SECTOR / REGIME / PHASE-STATE.** gravity-bulk / cosmological boundary; the Machian termination register read at cosmic time `z ~ 1–2`; the live-vs-attractor WHEN axis.

**Decides.** The D1 §5 G-provenance WHEN axis — live-keyed (`a₀` tracks `H(z)`) vs attractor-keyed (`a₀ = cH_∞/2π`, redshift-constant).

**Signatures (frozen):**
- **live** (`a₀ ∝ H(z)`) → a **larger MOND scale at `z ~ 1–2`** (where `H` was `~2–3×` today's).
- **attractor** (`a₀ = cH_∞/2π`, the ∞ subscript, `manuscript/ave-kb/vol3/cosmology/ch05-dark-sector/derived-mond-acceleration-scale.md:15`) → **redshift-constant `a₀`.**

**Demotion conditions:** a confirmed `a₀(z)` **rising with `H(z)`** demotes attractor; a confirmed **redshift-constant `a₀`** out to `z ~ 1–2` demotes live. (Either result is also a constraint on the fossil branch, which predicts a per-node formation-era value — but the fossil branch leans dead on the storage-medium argument already; see D1 §5.)

**P10 fireability:** FIRES — a rising `a₀(z)` demotes attractor; a flat `a₀(z)` demotes live. Both outcomes are branch-lethal.

**Retrieval anchors to verify:** Genzel-class declining-rotation-curve claims at `z ~ 1–2` **and their contested reanalyses** (report both sides).

---

## A4 — RINGDOWN ECHOES + ABSORPTION → decides T3 (Γ=−1 loss-character)

**SECTOR / REGIME / PHASE-STATE.** cross-scale `Γ=−1` TIR/saturation boundary; a black hole is the substrate's rupture boundary at astrophysical scale — the at/above-yield regime (regime-distinct from the sub-yield cold vacuum).

**Decides.** Registry T3 (`research/2026-07-10_collapse-target-registry.md:46`, "needs Grant loss-character ruling") — branch (i) lossless-reflective short vs branch (ii) dissipative fuse/matched at the rupture boundary.

**Signatures (frozen):**
- **branch (i) — lossless-reflective short** (the *same* `Γ=−1` object as the electron cage) → **post-ring-down ECHOES at detectable amplitude** + **sub-unity horizon absorption.**
- **branch (ii) — dissipative fuse/matched** → **no echoes**, **horizon-grade (near-unity) absorption.** Regime-legal: above-yield dissipation does not violate Axiom-3's sub-yield losslessness (`manuscript/ave-kb/vol4/claim-quality.md:1168`, "linear below rupture, dissipative at and above rupture").

**Demotion conditions:** a **confirmed echo detection** at credible significance demotes branch (ii); a **confirmed absorption null / horizon-grade absorption** with no echoes demotes branch (i). The D1 lean is branch (ii) (echo searches contested-but-mostly-null + horizon-grade absorption) — the sweep tests whether the current data actually support that lean or leave it open.

**P10 fireability:** FIRES — a credible echo detection demotes branch (ii); a horizon-grade absorption null with no echoes demotes branch (i). Opposite, branch-lethal outcomes.

**Retrieval anchors to verify:** Abedi/Afshordi-class echo claims **vs their null reanalyses** (report both sides + significance levels); QNM / inspiral-merger-ringdown (IMR) consistency levels.

---

## A5 — GW170817 ONE-c → books the channel-speed identity (already passed)

**SECTOR / REGIME / PHASE-STATE.** shear-channel + EM-transverse channel co-propagation; sub-rupture linear regime; a consistency book (no live fork), not an open adjudication.

**Decides.** Formal booking of the channel-speed identity — `c_gw = c_light` to `~10⁻¹⁵` reads in AVE as the shear and EM channels riding **ONE substrate `c`.** Already passed at the physics level; this adjudicator **books the bound and states what it excludes.**

**Signature / what it excludes:** the tight bound **excludes channel-split propagation modifications** (a separate shear-channel `c` differing from the EM `c` at the `10⁻¹⁵` level). There is no live AVE branch predicting a split, so this is a **consistency book, not a fork** — but it is a real constraint on any future channel-split proposal.

**P10 fireability:** N/A-as-fork by design (no live AVE branch to demote); the fireability is asymmetric — a *failure* to verify the bound, or a bound looser than claimed, would weaken the "one substrate `c`" consistency book. Booked, not forked.

**Retrieval anchors to verify:** the GW170817 + GRB 170817A arrival-coincidence bound (`|c_gw − c|/c ≲ 10⁻¹⁵`). **NB — the corpus flags this as an external bound requiring verification** (`research/2026-06-11_chiral-vacuum-reactor-framing.md:393`); the sweep is where that verification lands. **Cheap lane** (retrieval-only; no contested adjudication).

---

## A6 — Ġ BOUNDS vs MACHIAN-G READINGS → constrains the live/fossil/attractor fork

**SECTOR / REGIME / PHASE-STATE.** gravity-bulk / cosmological boundary; the Machian termination register on the `Ġ` (time-derivative) side; books empirical bounds only.

**Decides.** The D1 §5 G-provenance fork (live / fossil / attractor), on the `Ġ` side.

**Books (frozen):**
- **LLR** `|Ġ/G| ≲ 1.5×10⁻¹³/yr` (+ pulsar-timing and BBN bounds as cross-checks);
- the **`~500×` exclusion of naive-live** G (a naive live rate `~ H₀ ≈ 7×10⁻¹¹/yr` is `~470×` above the LLR bound);
- the **non-discrimination of the Sciama coincidence under flatness** (it holds at every epoch, so "it holds now" favors no branch).

**Demotion conditions:** the bounds **already demote naive-live**; they do **not** by themselves demote flatness-protected-live or attractor (both satisfy the bound). This adjudicator's job is to **book the bounds cleanly and state which branches survive them**, not to resolve the fork.

**P10 fireability:** FIRES — the bound demotes naive-live directly (`~470×` over LLR). It does NOT demote flatness-protected-live or attractor; that non-demotion is itself the booked result, not a decorative pass.

**★ SCOPE FENCE (binding):** the **flatness-`Ġ` self-cancellation derivation** — does the canon Machian termination form self-cancel to `Ġ = 0` under flatness, or leave an era-transition residual at the LLR bound — is a **THEORY item, explicitly OUT of this satellite's scope** (named derivation debt, D1 §5). This satellite books the empirical bounds only.

**Retrieval anchors to verify:** LLR `Ġ/G` limits (Hofmann/Müller-class); pulsar-timing `Ġ` bounds; BBN `Ġ` constraints.

---

## LINEAGE-MAP AMENDMENT CANDIDATES (flagged at freeze; NOT landed)

Recorded here so novelty honesty is frozen alongside the branch maps — flags, not edits (the lineage map is not touched in-lane):
- **Milgrom modified-inertia MOND** — the tide branch's possible lineage (D1 §4 kill-test 3); the tide branch may be that class in lattice clothing.
- **Brans–Dicke / Dirac large-numbers hypothesis** — the live-G branch's lineage (D1 §5 prior-art node); both died on exactly these `Ġ` bounds.
