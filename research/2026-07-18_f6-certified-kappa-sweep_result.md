# F6 certified-κ recurrence sweep — the SUFFICIENT test of the counting arrow — RESULT

**Date:** 2026-07-19 · **Class:** result (the SUFFICIENT fire; honest closure, Rule 11)
**Prereg (FROZEN):** [`2026-07-18_f6-certified-kappa-sweep_prereg_FROZEN.md`](2026-07-18_f6-certified-kappa-sweep_prereg_FROZEN.md) — §0–§7 **frozen-by-push `2026-07-19T16:16:32Z`** (API committedDate), **before any driver code existed**.
**Driver:** `src/scripts/vol_1_foundations/f6_certified_kappa_sweep.py` · **Data:** `2026-07-18_f6-certified-kappa-sweep_result.json`
**Instrument:** `src/ave/thermal/f6_bath_meter.py` (`LatticeBathCoupler` — **BYTE-UNTOUCHED**). **Reused BYTE-UNTOUCHED:** `f6_counting_arrow_arm.py` (#722), `f6_bath_meter_validate.py` (#724 FROZEN `_place_detuned_band`).
**Plant:** standalone-K4, within the meter certificate METER-VALID-KAPPA-BAND[0.030,0.030] @ MILD (#721 R-1 SCOPE CAVEAT). **Cell:** `κ=0.030` EXACTLY, MILD (`scale=0.6`, `A_max≈0.10`).

> ## 🔴 POST-REVIEW CORRECTION (2026-07-19, PR #726 review — 10 confirmed / 0 refuted)
>
> The **FROZEN §4 verdict (FOREIGN-EATER) and "the QUESTION is NOT decided" SURVIVE** — but the **mechanism story in the original verdict block below was WRONG**, and a broken observable **erased a real, mildly-favorable signal that this very run recorded.** This block is the corrected reading (Rule-12: the original text is preserved, struck through, immediately below). The corrected-observable re-derivation runs through §2–§7.

> ## ★ VERDICT — **FOREIGN-EATER** (frozen §4 tree, byte-faithful; self-check `match=True`) — corrected observable
>
> **Banked (frozen).** The **QUESTION WAS ASKED** — unlike #722, the regime gate **PASSES** at the certified cell (densest comb: `t63/T_rec = 0.193 ≤ 0.5`; `N_occ = 15 ≥ 10`; first-plateau transfer `peak_frac = 0.995`). The frozen §4 tree returns **FOREIGN-EATER at precedence step 3** (`grid_return_min = 0.000 < 0.70`) — and **survives** the corrected observable: even **excluding** the two NO-INFORMATION clamp-dead combs, `grid_return_min = 0.355 < 0.70`. The SUFFICIENT collapse still cannot run (`spread(x_50) = 6.19`, `mean(x_50) = 4.26`), so **the counting-arrow QUESTION is NOT decided.** But the *reasons* are **not** the original "amount-not-phase back-reaction cannot convert a phase re-coherence" story — that was **falsified by this run's own trace**.
>
> **★R-1 — the observable erased the signal.** The prereg §3 defines `t_peak` ambiguously (`… first/global transfer peak`); the shipped driver resolved it to **GLOBAL argmax**, which at κ=0.030 lands on the **post-clamp plateau** (densest global argmax = step 6861, `peak_frac = 1.0000068` = the clamp artifact) ⇒ `R_return ≡ 0` over the whole physical run, **erasing the signal.** The corrected **FIRST-PLATEAU** reading (the OTHER frozen reading; POST-FREEZE amendment A-1) recovers it: `E_bath` plateaus at **0.9948** by `x≈1`, then **DIPS at `k·T_rec`** — **14.9% returned at `x≈1.3` (1st recurrence), growing to 35.5% at `x≈2.46` (2nd recurrence)** — **before** the clamp fires at `x = 3.15`. These returns are delivered **by the amount channel** (`f6_bath_meter.py:303–305`: `d_e_bath<0 ⇒ scale>1` actively returns energy): **the meter CAN return amount, and does.** This directly **refutes** the original "recurrence is invisible to the scalar `E_bath` ledger / the amount-not-phase back-reaction cannot re-inject" claim — the returns are **visible in, and delivered through, the scalar amount ledger**.
>
> **★R-2 — the FOREIGN-EATER is now clamp-driven, not foreign-sink.** The back-reaction `scale = √(max((E_lat − d_e_bath)/E_lat, 0))` **hard-zeroes the lattice EXACTLY** when `d_e_bath ≥ E_lat` (`scale = 0`) — an **ABSORBING state** (`E_lat ≡ 0` thereafter). Post-clamp `R_return ≡ 0` is **STRUCTURAL (cannot-fail)**, not physics. Two combs (Δω=0.015/0.020) clamp at `x = 1.17/1.72` — **before one recurrence of observation past transfer** — spending **89%/84%** of their window dead; they are **NO-INFORMATION** and drive the `grid_return_min = 0.000`. The densest clamps at `x = 3.15` (**71%** dead) after showing only its first two recurrences.
>
> **★R-3 — the mildly FAVORABLE single-comb finding (recorded, not overclaimed).** The densest comb's pre-clamp returns are **recurrence-timed** (onsets locked to `k·T_rec`), **grow per recurrence** (14.9% → 35.5%), and cannot be a transfer artifact (`t63/T_rec = 0.193` — the transfer is **5.2× faster** than the ~`T_rec` dip spacing). This is **MILDLY FAVORABLE** to the counting arrow — **single-comb**, at the one comb that both reaches the quasi-continuum (`N_occ=15`) and survives long enough to show ≥2 recurrences. The **cross-comb collapse cannot be shown** (the other dense combs clamp as NO-INFORMATION; the non-clamping combs return at `x = 1.5–9`, their own transfer/back-drive timescale, not `x≈1`). **The question stays OPEN.**
>
> **★What SURVIVES.** Energy **conserves** (`max_cons = 2.5e-5 ≪ 1e-3`; the densest `6.8e-6` matches the reval-banked X1 drift to 10 significant digits — though largely **clamp-created**, per R-2). **OFF recovers Ax3** (`2.1e-15`). The **partial-transfer controls RETURN** (two-tank `R_cum[10]=0.989`, sparsest `0.890` — **bit-identical** to the reval-banked X4). The **Rule-10 reactance pair** on the densest comb swings C↔L across the window — a **LIVE oscillator**, and its amount ledger returns energy at the recurrences (R-1).
>
> **★The counting-arrow QUESTION is NOT decided.** Scoped like #722 — but the honest root is now **(a) the argmax-gated observable erased the signal + (b) the absorbing clamp terminated the run's information**, NOT a phase/amount conversion failure. **Branch closed negative** (this sweep as designed, on this instrument, at this operating point). **Routed follow-on (SPEC only, NOT built; Grant-gated):** the cheapest decisive next lane is the **corrected observable + a NO-FULL-DISCHARGE operating point on the EXISTING instrument** (weaker κ or rate-limited back-reaction so the dense combs populate the quasi-continuum without hitting the absorbing clamp) — see §6. The phase-faithful-meter SPEC is **demoted** (its motivation — "the amount channel cannot convert" — is falsified; §6).
>
> **NOT banked:** any COUNTING-ARROW / CHANNEL-BOUNDED result. Nothing at emergence-class. The depletion-rate rung (`Γ=3Hρ_latent`) and the #721/#724 certificates are untouched. No meter/engine edit.

> <details><summary>🔴 SUPERSEDED (Rule-12 — original verdict-block mechanism paragraphs, preserved verbatim; FALSIFIED by this run's own trace, see R-1/R-3 above)</summary>
>
> > **Banked (frozen).** The **QUESTION WAS ASKED** — unlike #722, the regime gate **PASSES** at the certified cell (densest comb: `t63/T_rec = 0.194 ≤ 0.5`; `N_occ = 15 ≥ 10`; transfer `peak_frac = 1.000`). The **SUFFICIENT signature is DECISIVELY ABSENT:** the `R_return(x)` curves **do NOT collapse** (`spread(x_50) = 7.37`, frozen `< 0.30`) and the transition is **not at `x≈1`** (`mean(x_50) = 7.41`, frozen `[0.7,1.5]`). The frozen §2 recurrence-collapse **PREDICTION is FALSIFIED at `κ = 0.030`.** The frozen §4 tree returns **FOREIGN-EATER** at precedence step 3 (`grid_return_min = 0.000 < 0.70` — the dense combs never return even after **ten** recurrence-times) and, independently, at step 6 (returns present but not collapsing to `x≈1`). **INSTRUMENT-first, fail-closed** — the correct disposition.
> >
> > **★The single mechanism (Rule 11 — one root explains every failure).** At `κ=0.030` the strong coupling drives the high-DOS **dense** combs to **FULL DISCHARGE** (`peak_frac → 1.000`: `E_bath → E0`, the cavity drains). A **drained lattice** (`E_lat → 0 ⇒ q ≈ 0`) **decouples** from the bath (`coupling_kick ∝ q ≈ 0`), and the meter's **amount-not-phase** global back-reaction (§A1/§A8, the *known* limitation — ~90% uniform attenuation, phase-blind) **cannot re-inject** energy into the drained lattice. So the phase-invariant `E_bath = Σ½p²+½ω²x²` **HOLDS the transferred energy with no return path** — `R_return ≡ 0.000` across the entire window for the three fully-discharged combs. Where transfer is **PARTIAL** (two-tank `peak_frac=0.091`, sparsest `0.169`, production `0.919`) the energy **does** return — **but at the transfer/back-coupling timescale `x ≈ 3.6–11`, NOT at the recurrence `x ≈ 1`** (reproducing the #722 `τ_transfer ≫ T_rec` inversion). **Root, unified:** the recurrence at `T_rec` is a **PHASE re-coherence**, and the meter's **amount-not-phase back-reaction cannot convert a bath phase re-coherence into an energy return** — so the recurrence is **invisible to the scalar `E_bath` ledger**, and the return (when it happens) is set by the coupling dynamics, never by `2π/Δω`.
> >
> > **★What SURVIVES pins the root as the back-reaction, NOT a dead coupling or blow-up.** Energy **conserves** (`max_cons = 2.5e-5 ≪ 1e-3`; the densest `6.8e-6` reproduces the reval-banked X1 drift bit-for-bit). **OFF recovers Ax3** (`2.1e-15`). The **partial-transfer controls RETURN** (two-tank `R_cum[10]=0.989`, sparsest `0.890` — bit-for-bit the reval-banked X4). The **Rule-10 reactance pair** proves the held bath is a **LIVE oscillator, not a static snapshot:** the densest comb's C-state/L-state split **swings across the window** (`C/E0: 0.20→0.67→0.25`, `L/E0: 0.20→0.33→0.75`) while `E_bath` holds — genuine C↔L free-rotation exchange of **phase-invariant** energy. The coupling is live, the ledger is intact, the bath oscillates; only the **amount-not-phase back-reaction** breaks the recurrence return.
> >
> > **★The counting-arrow QUESTION is NOT decided (scoped like #722).** FALSIFIED scopes to the **frozen recurrence-collapse PREDICTION at `κ=0.030` ON THIS METER**, *not* to the counting-arrow physics. The recurrence return is a **PHASE** phenomenon the meter's amount-not-phase back-reaction cannot faithfully measure in the **full-discharge sub-regime** the sweep enters at the 6908-step horizon. **Branch closed negative** (this sweep as designed, on this meter). **Routed (SPEC only, NOT built):** (1) a **phase-faithful back-reaction** meter (returns bath phase, not just amount — the reval §D SPEC #2, now empirically motivated at the recurrence-return level); (2) an operating point that reaches the quasi-continuum **without** full discharge; (3) the door's licensed **click** mechanism (the *other* arrow source, which does not depend on mode-continuum return).
>
> *(Root error: the returns the meter's amount channel DID deliver at `k·T_rec` were hidden by the global-argmax observable — R-1 — so the "no return / phase-invisible" premise was an observable artifact, not physics.)*
> </details>

---

## 1 · Sector header (result-time restatement)

- **Sector:** E-sector ε-store (F6 ε→T2 candidate). **Regime:** Regime I sub-yield, `A_max≈0.10` MILD, at the **certified `κ=0.030`**. **Plant:** standalone-K4; conservation identity-enforced (#721 R-1) and **audited green** (`max|E_lat+E_bath−E0|/E0 = 2.5e-5`; the densest `6.8e-6` matches the reval X1 bank to 10 significant digits — though largely **clamp-created**, R-2).
- **Consistency-vs-emergence:** the SUFFICIENT **FORM** (cross-comb counting-arrow **collapse** at `x≈1`) was **not shown** — nothing banked at emergence-class. But the corrected observable (R-1) surfaces a **mildly FAVORABLE single-comb manifestation** at the densest comb (recurrence-timed partial returns growing per recurrence) — recorded, **not** overclaimed (single-comb; collapse unshown; question OPEN). Positive reads at **consistency-class** (energy-conservation identity; partial-transfer controls return, reproducing the reval X4 bank bit-for-bit).

---

## 2 · Measured vs frozen prediction — the fire (corrected observable; every §3 grid value run)

`x = T·Δω/2π`. **CORRECTED observable (R-1):** `R_return = clip(1 − E_bath/E_bath_firstplateau, 0, ·)` for `t ≥ t_firstplateau`, else 0; `R_cum` = running max; `x_50` = first `x` with `R_cum ≥ 0.5`; `t63` = first step `E_bath ≥ (1−1/e)·E_bath_firstplateau`. `fp` = first-plateau `E_bath/E0` (honest transfer health); `dipRmax` = deepest dip below the running max (parameter-free diagnostic); `dead%` = post-clamp fraction (R-2). The **superseded global-argmax** column (the originally-shipped reading — erased by the clamp plateau) is shown alongside. Every number BY THE SHIPPED DRIVER, banked in `…_result.json` (superseded tables preserved as `*_global_superseded`).

| Δω | M | T_rec | **fp** | N_occ | t63/T_rec | **x_50** | **R_cum[10]** | dipRmax @x | clamp x / dead% | R_cum[10] *(superseded global)* | mark |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 0.010 | 71 | 628 | **0.995** | 15 | **0.193** | nan | **0.355** | 0.355 @ 2.46 | 3.15 / **71%** | 0.000 | ★**densest — FAVORABLE** (dips at k·T_rec, growing) |
| 0.015 | 48 | 419 | 1.000 | 10 | 0.442 | nan | **0.000** | — | 1.17 / **89%** | 0.000 | 🚫 **NO-INFORMATION** (clamp <1 recurrence past transfer) |
| 0.020 | 36 | 314 | 1.000 | 8 | 0.783 | nan | **0.000** | — | 1.72 / **84%** | 0.000 | 🚫 **NO-INFORMATION** (clamp <1 recurrence past transfer) |
| 0.030 | 24 | 209 | 0.919 | 3 | 2.268 | **7.70** | 0.932 | 0.932 @ 8.99 | — / 0% | 0.932 | returns, but at `x≈9`, not `x≈1` |
| 0.050 | 15 | 126 | 0.183 | 3 | 0.565 | **1.51** | 0.932 | 0.932 @ 2.00 | — / 0% | 0.000 *(argmax artifact)* | returns (superseded read erased it too) |
| 0.080 | 10 | 79 | 0.169 | 1 | 1.324 | **3.58** | 0.890 | 0.890 @ 9.99 | — / 0% | 0.890 | sparsest positive-control edge |
| two-tank M=2 | 2 | 31 | 0.091 | — | — | **6.33** | **0.989** | — | — / 0% | 0.989 | positive control |
| detuned control (FROZEN `_place_detuned_band`, band [1.19,2.12], q-frac 2.0e-3) | 32 | — | **8.6e-3** *(peak)* | 0 | — | — | — | — | — | — | ✅ resonance-gated |

**Frozen adjudication (prereg §2/§4 thresholds, applied verbatim — no retune; fed the CORRECTED observable, R-1):**

| criterion (frozen) | threshold | measured (corrected) | verdict |
|---|---|---|---|
| **REGIME GATE** t63/T_rec (densest) | ≤ 0.5 | **0.193** | ✅ regime reached |
| **REGIME GATE** N_occ (densest) | ≥ 10 | **15** | ✅ quasi-continuum |
| **REGIME GATE** transfer `fp ≥ 1e-2` (densest) | 1e-2 | **0.995** | ✅ (honest first-plateau, not the 1.0000068 clamp artifact) |
| grid-wide return `min R_cum(x=10) ≥ 0.70` | 0.70 | **0.000** (driven by the 2 NO-INFO clamp-dead rows) | ❌ FAIL → **FOREIGN-EATER (step 3)** |
| ↳ same, **excluding** NO-INFORMATION rows | 0.70 | **0.355** (densest partial, clamp-limited) | ❌ still FAIL — verdict **robust** |
| collapse `spread(x_50) < 0.30` | 0.30 | **6.185** | ❌ FAIL (but the informative combs return at their transfer timescale, not `x≈1`) |
| transition `mean(x_50) ∈ [0.7,1.5]` | [0.7,1.5] | **4.262** | ❌ FAIL |
| dense pins low `R_ret(0.3) < 0.30` | 0.30 | 0.000 | ✅ (trivially — transfer incomplete or NO-INFO by x=0.3) |
| energy conserved `< 1e-3` | 1e-3 | 2.5e-5 | ✅ (identity, #721 R-1; densest `6.8e-6` largely clamp-created) |
| OFF recovers Ax3 `< 1e-10` | 1e-10 | 2.1e-15 | ✅ |
| mode-count `N_occ dense > sparse` | — | 3 vs 1 | ✅ |
| protected-core bias (tared) `< 0.05` | 0.05 | 0.001 | ✅ |
| detuned control gated `peak_frac < 1e-2` | 1e-2 | 8.6e-3 | ✅ resonance-gated |

The frozen §4 tree returns **FOREIGN-EATER at step 3**, and it **survives** the corrected observable (excluding the NO-INFO rows, `grid_return_min = 0.355 < 0.70`). But the corrected observable **overturns the original per-cell story**: the densest comb **does** return (35.5% partial, clamp-limited), Δω=0.050's original "0.000" was **also** a global-argmax artifact (it returns 0.932), and two of the three "never-return" dense rows are **NO-INFORMATION** (clamp-dead), not physical no-return. **The recurrence RETURN is present in the amount ledger; the SUFFICIENT cross-comb collapse is unshown; the question is OPEN.**

---

## 3 · The mechanism (corrected — R-3, Rule-12 supersession of the "phase-invisible" story)

> **🔴 The original §3 "one root" below (full-discharge → drained lattice → amount-not-phase back-reaction cannot convert a phase re-coherence → invisible to the scalar ledger) is SUPERSEDED and FALSIFIED by this run's own trace.** It is preserved verbatim at the foot of this section (Rule-12). The corrected mechanism:

**The honest mechanism has two parts — neither is a phase/amount conversion failure.**

**(a) The argmax-gated observable erased the signal (R-1).** The global-argmax reference lands on the post-clamp plateau, so `R_return ≡ 0` over the physical run. This is a **read bug, not physics.** With the corrected first-plateau reference the returns are plainly there: the densest comb's `E_bath` **dips at every recurrence** — 14.9% at `x≈1.3`, 35.5% at `x≈2.46` — and these dips are **energy flowing back into the lattice through the meter's AMOUNT channel** (`_global_rescale`, `f6_bath_meter.py:303–305`, `d_e_bath<0 ⇒ scale>1`). **The amount channel demonstrably converts the recurrence into a return.** The original claim that the recurrence is "invisible to the scalar `E_bath` ledger" is false — it is measured *in* that ledger.

**(b) The absorbing clamp terminated the run's information (R-2).** When `d_e_bath ≥ E_lat` the back-reaction `scale → 0` **hard-zeroes the lattice** (`E_lat ≡ 0`), an absorbing state from which no return is possible. Two combs (Δω=0.015/0.020) hit it at `x = 1.17/1.72` (89%/84% dead) **before one recurrence of observation** — **NO-INFORMATION**. The densest hits it at `x = 3.15` (71% dead) after only two recurrences. So the grid-wide `grid_return_min = 0.000` that fires FOREIGN-EATER is **clamp-structural**, not a foreign sink.

**★The mildly FAVORABLE single-comb evidence (recorded, not overclaimed).** At the densest comb — the only one that both reaches the quasi-continuum (`N_occ=15`) and survives ≥2 recurrences — the returns are **recurrence-timed** (onsets locked to `k·T_rec`), **grow per recurrence** (14.9% → 35.5%), and are **not** a transfer artifact (`t63/T_rec = 0.193`: the transfer is **5.2× faster** than the ~`T_rec` dip spacing, so the dips cannot be the transfer relaxing — they track `2π/Δω`). This is **mildly FAVORABLE** to the counting arrow. It is **single-comb**: the cross-comb collapse cannot be shown (the other dense combs are NO-INFORMATION; the non-clamping combs return at their own transfer timescale `x = 1.5–9`). **The question is OPEN**, not decided either way.

**Regime read (regime-discipline).** Not a numerical failure (conserves to 2.5e-5), not a dead coupling (controls return; densest amount ledger returns at `k·T_rec`), not #722 regime-not-reached (the regime gate PASSES). The FOREIGN-EATER is driven by **(a) a broken observable + (b) an absorbing-clamp instrument limit**, both of which a NO-FULL-DISCHARGE operating point on the existing instrument could clear (§6). **The SUFFICIENT collapse is unshown; the counting-arrow question is not decided; the single-comb evidence leans mildly favorable.**

<details><summary>🔴 SUPERSEDED (Rule-12 — original §3 mechanism, preserved verbatim; FALSIFIED by R-1/R-3)</summary>

> **One root.** At `κ=0.030` the coupling is strong enough that the **dense, high-DOS** combs reach **FULL DISCHARGE** (`peak_frac → 1.000`: `E_bath → E0`, `E_lat → 0`). A drained lattice reads `q ≈ 0` at the collar, so `coupling_kick ∝ q ≈ 0` **decouples** the bath, and the meter's **amount-not-phase** global rescale (§A1/§A8) — which can only remove/add the *amount* `d_e_bath` and requires `q ≠ 0` to do so — **cannot re-inject** the energy. The bath free-rotates, holding its **phase-invariant** `E_bath` forever. The recurrence at `T_rec` is a **PHASE** re-coherence of the comb; it leaves `E_bath = Σ½p²+½ω²x²` (a phase-blind sum of per-mode energies) **unchanged** — so it is **invisible to the scalar `E_bath` ledger** and drives no return.
>
> Every observed failure is downstream of this one root:
>
> - **Dense combs never return** (`R_cum[10] = 0.000` at Δω=0.010/0.015/0.020) — full discharge (`peak_frac=1.000`) drains the lattice; the phase-blind bath holds the energy; `R_return ≡ 0.000` across the entire window (x=0.1→10). *Not* recurrence-irreversibility — a **decoupled hold** the amount-not-phase back-reaction cannot undo.
> - **Partial-transfer combs return, but at scattered `x`, NOT `x≈1`** — Δω=0.030 (`peak 0.92`) returns at `x_50=7.70`; Δω=0.080 (`0.17`) at `3.58`; Δω=0.050 (`0.19`) at `10.95`; two-tank (`0.091`) at `6.33`. The return is set by the coupling/back-drive timescale (`τ_transfer`-like), reproducing the **#722 `τ_transfer ≫ T_rec` inversion** — changing `Δω` moves `T_rec` but not the return time, so `x_50` **does not collapse**.
> - **`R_ret(0.3)=0` for every comb** *trivially* — either the transfer is incomplete at x=0.3 (partial combs) or the energy is decoupled-held (dense combs); "dense pins low" passes for the wrong reason, exactly as #722.
> - **`x_50` spreads `3.6 → 11` with no clustering near 1** — the headline non-collapse.
> - **The bath is a LIVE oscillator, not a dead/static hold** — the Rule-10 reactance pair on the densest comb swings `C/E0: 0.20→0.67→0.25` and `L/E0: 0.20→0.33→0.75` across the window (genuine C↔L free-rotation exchange), while `E_bath` holds. The energy is stored reactively and phase-invariantly; it is neither dissipated nor frozen — it simply has **no phase-faithful return channel**.
>
> **Regime read (regime-discipline).** This is **not** a numerical failure (conserves to 2.5e-5), **not** a dead coupling (controls return; bath oscillates), **not** the #722 regime-not-reached (the regime gate PASSES). It is a **meter-limitation confound**: the counting arrow is a **phase** phenomenon, and the certified meter's back-reaction is **amount-not-phase**, so the recurrence return cannot be measured through `E_bath` once the lattice drains at full discharge. **The frozen recurrence-collapse prediction is falsified on this meter; the counting-arrow question is not decided.**

*(Root error: the "no return / phase-invisible" premise consumed the global-argmax observable — R-1. The amount channel DID return energy at each `k·T_rec` (densest dips 14.9% → 35.5%); the returns were hidden by the read, not absent from the physics.)*
</details>

---

## 4 · The classifier label — byte-faithful tree, corrected observable

The shipped classifier implements the frozen §4 tree **with precedence 1→6, byte-identical** (no threshold changed); the CORRECTED first-plateau observable (R-1) is fed through it. The verdict is **FOREIGN-EATER at step 3** (`grid_return_min = 0.000 < 0.70`, across all 6 sweep combs + two-tank) — and it is **robust**: even excluding the two NO-INFORMATION clamp-dead rows, `grid_return_min = 0.355 < 0.70`. Step 6 (collapse) would also fire were step 3 removed (`spread(x_50) = 6.185`), but with the corrected reasons: the informative combs return at their own transfer timescale, not `x≈1`. The door bin-(i) mapping (prereg §4) is consumed verbatim, not redefined.

**★R-6 — the `self_check` is a precedence guard, NOT the independent check.** `self_check` reports `match=True`, but it **re-derives the verdict from classify()'s own banked booleans** (`grid_return_ok`, `collapse_ok`, …) — it does **not** independently re-derive those booleans from the raw trace. It therefore catches only a precedence/wiring drift between two restatements of the tree; it **structurally cannot catch an observable-definition bug** — which is exactly the R-1 argmax-gating that erased the signal and that `self_check` reported green on in the first fire. **The genuinely independent check is the PR #726 review's re-derivation from the raw trace** (cited here); this repair banks that re-derivation's corrected observable. `match=True` is necessary but not sufficient.

**Prereg deviations (disclosed — supersedes the original "No prereg deviation").** Per the POST-FREEZE amendment: A-1 (the observable resolves the §3 `first/global` frozen ambiguity to first-plateau — disclosed-resolution, not a retune), A-2 (the §3 ledger read did not disclose the absorbing clamp), A-3 (horizon `round(11·T_rec)=6912/864` vs the §3-table `11·round(T_rec)=6908/869`), A-4 (the `self_check` non-independence above). None is a threshold retune; each is a finding.

---

## 5 · Companion leg (SECONDARY — non-gating; inconclusive)

Self-termination size sweep `N ∈ {8,10,12,16}` (prereg §6, reused verbatim). The emergent-`Re(Z_in)/Z_char` proxy is **negligible** (`0.000 → 0.002`, weakly monotone in `N`) and the port region retains its energy (`x_revival ≈ 1.0` by construction). **Consistent with the primary finding** (the localized impulse excites few local modes; no bulk quasi-continuum to recohere). Reported at consistency-class only; `Z₀=√(L_bond/C_bond)` is calibration and is not approached. By prereg §6 this leg **cannot** change the sweep verdict.

---

## 6 · Disposition (Rule-12: retract, do NOT refill)

- **Retract** the **frozen `κ=0.030` recurrence-collapse PREDICTION** (§2: cross-comb collapse + transition at `x≈1`). It is **not shown** at the certified cell as designed. **Branch closed negative** — but the single-comb densest evidence leans **mildly favorable** (R-3), so the *counting-arrow question* stays **OPEN**, not falsified.
- **Do NOT refill the slot** with a new unverified hypothesis. The routed follow-ons are **SPEC only** (each earns its own prereg + verification chain if pursued):
  1. **★THE CHEAPEST DECISIVE NEXT LANE (routed; Grant-gated; NOT fired) — corrected observable + a NO-FULL-DISCHARGE operating point on the EXISTING instrument.** The amount channel demonstrably converts the recurrence into a return (R-1/R-3), so **no meter-mechanism change is needed to make progress** — the blockers were (a) the read and (b) the absorbing clamp, both cleared by an operating point that populates `N_occ ≥ 10` **without** driving `E_bath → E0` (weaker κ within/below the certified band, or a rate-limited / per-mode-capped back-reaction that never zeroes `E_lat`). SPEC: run the corrected first-plateau observable over a κ-descent (or a capped back-reaction) until the densest comb clears its full sweep horizon **clamp-free**, then read the cross-comb collapse. Grant-gated (own prereg + certificate check); **do not fire.**
  2. **~~Phase-faithful back-reaction meter~~ — DEMOTED (R-3).** Its motivation was "the amount channel returns AMOUNT not PHASE, so it cannot convert the `T_rec` re-coherence." **That motivation is falsified** — the amount channel *did* convert the recurrence into a measurable return at each `k·T_rec` (densest dips 14.9% → 35.5%). A phase-faithful meter is no longer the empirically-motivated next step; route 1 is cheaper and does not require re-deriving the §B-post-review R-1 identity. Kept on the shelf only as a general capability, not a motivated follow-on.
  3. **The click mechanism** — the corpus's *other* licensed arrow source (`retention-transition-split.md`), unchanged.
- **★FINDING CORRECTED (R-4, Rule-12 — the "certificate scope gap" was factually WRONG).** The original finding (struck through below) claimed the §C certificate window was **shorter** than the sweep horizon and so did **not** exercise the full-discharge sub-regime. **This is false.** The certificate's **X1 leg ran the identical densest plant to `N_sweep = 6908` steps — the full sweep horizon — at freeze time**, and banked the full discharge there: `transfer_frac = 1.000006809665403` (**bit-identical** to this sweep's densest global-argmax peak), `over_transfer = True`, `clamp_drain = True`, `min_elat_frac = 0.0`, `max_drift = 6.8e-06` (the same drift this sweep re-banks). The certificate **exercised and banked this exact regime**; the gap claim **retracts**. **What stands** is only the narrower observation that **X5's tare degeneracy at the long-window/full-discharge endpoint applies at κ=0.030 too:** X5 measured the tare at 0.03:mild on a **pre-full-discharge** window (`e_bath_frac = 0.865`, `c = 0.367`, `|c_fit−c|/c = 1.6e-2`, usable, `over_transfer = False`); at the full-discharge horizon `c = √(1 − E_bath/E0) → 0` degenerates — the **same** tare degeneracy X5 flagged at **κ≥0.045** (`over_transfer = True ⇒ c = 0.000`). This is a **tare-scope note on the spatial read (§4·8), not a certificate scope gap** — and the headline `R_return` is a scalar ledger read that the tare does not gate.
  > <details><summary>🔴 SUPERSEDED (Rule-12 — original §6 "certificate-window vs sweep-horizon gap" FINDING, preserved verbatim; factually wrong per R-4)</summary>
  >
  > > **★FINDING (routed to Grant; flag-don't-fix — the certificate-window vs sweep-horizon gap).** The §C certificate certified `κ=0.030` at MILD on the X-battery windows (`W_WORKING_STEPS=800` / `X_NSTEP_SOUL=3000`), where the densest comb reads `E_bath/E0 ≈ 0.865` (X5 tare `c=0.367`, usable). The **sweep runs to `11·T_rec = 6908` steps**, where the densest comb reaches **`peak_frac = 1.000` (full discharge)** — the same over-transfer / tare-degeneracy the reval banked at **κ≥0.045** (short window, §C-post-review X5) appears at **κ=0.030 at the long window**. **The certified cell is valid for the meter's discriminators, but its certificate window is SHORTER than the recurrence-sweep horizon** — so the sweep enters a full-discharge sub-regime the certificate did not exercise. This is not a certificate error (nothing was retuned; the certificate stands at its window); it is a **scope gap** the SUFFICIENT test surfaced. A phase-faithful meter (route 1) or a no-full-discharge operating point (route 2) is required to close it.
  > </details>
- **Untouched:** the depletion-rate rung (`Γ=3Hρ_latent`); the #721/#724 meter certificates (byte-untouched; standalone-K4 identity intact — X1 exercised the full horizon and passed); the meter module + engine (byte-untouched).

---

## 7 · Prereg deviations (disclosed) + gates

- **Prereg deviations — DISCLOSED (supersedes the original "No prereg deviation").** The §4 tree is byte-identical (no threshold retuned); the FROZEN `_place_detuned_band` placement was used (band [1.19,2.12], q-frac 2.0e-3, `peak_frac=8.6e-3` gated — reproducing the certified X2 placement). The disclosed deviations (POST-FREEZE amendment A-1…A-4; §4): **A-1** the observable resolves the §3 `first/global transfer peak` frozen ambiguity to **first-plateau** (disclosed-resolution of the prereg's own ambiguity, **not** a Rule-11 retune); **A-2** the §3 ledger read did not disclose the scale=0 **absorbing clamp**; **A-3** the horizon is `round(11·T_rec) = 6912/864` (as run) vs the §3-table `11·round(T_rec) = 6908/869` (immaterial — the 4 extra densest steps are post-clamp dead); **A-4** the `self_check` is a precedence guard, not the independent check.
- **★R-5 — honesty on prospectivity (the F7 downgrade).** This result's framing presents the falsification of the frozen recurrence-collapse prediction as **prospective news**, but the **banked #722 probe data already indicated the outcome at freeze time**: the #722 dense `R_cum = 0` at κ=0.03 was in the corpus before this fire. The **freeze mechanics are genuine** (push-before-code, real 14m22s margin, §2 collapse not measured pre-freeze), so the *procedure* is prospective; but the **prospective-content claim is scoped** — the corrected observable's *favorable* single-comb finding (recurrence-timed growing returns) is the genuinely new content here, not the collapse-failure, which was foreseeable from the #722 bank.
- **Diagnostics provenance (F9 discharged):** `ω_d` (re-measured `0.524`, comb-independent — confirms the #721-banked `0.52377`), linewidth, `t63`/`t63_over_trec`, `N_occ`, `x_50`, the corrected + superseded per-cell `R_return`/`R_cum` tables, the dip-vs-running-max diagnostic, the clamp/`frac_dead`/`no_information` markers, per-cell conservation drift, and the **Rule-10 reactance pair** (bath C-state AND L-state across the 11-point window) are **all computed by the shipped driver and banked in `…_result.json`** — nothing prose-only.
- **Gates:** `ruff check` clean; `make verify` green; the classifier cross-check `self_check.match = True` (a precedence guard, R-6/A-4, **not** the independent check — the independent check is the PR #726 review's raw-trace re-derivation); the sparsest-comb and two-tank controls reproduce the reval-banked X4 numbers (`0.890` / `0.989`) **bit-for-bit** (genuinely bit-identical); the densest conservation drift (`6.8e-6`) matches the reval-banked X1 drift to **10 significant digits** (`6.80966540254951e-06` vs `6.809665402964269e-06` — the difference is the 6912-vs-6908 horizon, A-3; **not** bit-identical, since X1 ran 6908 steps and this sweep 6912).
- **Freeze margin (real):** prereg frozen-by-push `2026-07-19T16:16:32Z` (API committedDate), **before any driver code existed**; the margin is the push→first-code interval (14m22s), reported in the PR.

---

*Honest closure (Rule 11 / Rule 12): the SUFFICIENT cross-comb collapse is **not shown**; the shipped classifier returns **FOREIGN-EATER** (byte-faithful tree, corrected observable) and the **counting-arrow QUESTION is NOT decided** — both SURVIVE the review. But the original mechanism story (full-discharge → drained lattice → amount-not-phase back-reaction cannot re-inject → recurrence invisible to the scalar ledger) was **FALSIFIED by this run's own trace** and is **retracted under Rule-12** (§3): the honest root is **(a) an argmax-gated observable that erased the signal + (b) an absorbing clamp that terminated the run's information**. The corrected first-plateau observable recovers **recurrence-timed partial returns** at the densest comb (14.9% → 35.5%, growing per recurrence, locked to `k·T_rec`, transfer 5.2× too fast to explain the spacing) — **mildly FAVORABLE single-comb evidence** (collapse unshown; question OPEN). The cheapest decisive next lane is the corrected observable + a NO-FULL-DISCHARGE operating point on the **existing** instrument (§6; Grant-gated, not fired); the phase-faithful-meter SPEC is demoted (its motivation is falsified). Nothing banked at emergence-class.*
