# Mechanical Common-Mode Derivation — does the K4 common-mode rejection close the gravitational bulk port at COLD/DERIVED grade (result)

**Date:** 2026-07-20
**Class:** DERIVATION + lattice-derived research-driver (research-doc; **forms derived, values calibration/observation-imported and tagged; mints no `clm-`, propagates to no KB/tex leaf**). Resolves the frozen bins of the companion pre-registration (`research/2026-07-20_mechanical-commonmode-derivation_prereg-FROZEN.md`) — THE make-or-break derivation the gravitational sector's survival rests on (#758 R2).
**Provenance:** Grant-fired 2026-07-20 (verbatim `[sic]`: `"fire 1 and 2, then 3 once those land"` — item 1). Executes the three independent legs (A mode-existence / B cold-regime-emptying / C numerical-injection) frozen in the prereg. Inherits the re-banked #758 state.
**Lane fences:** DERIVATION lane only. **READ-ONLY on KB** — no `manuscript/` / `manuscript/ave-kb/` leaf edits, including NO edit to the port-register Q1 row (its consequence is ROUTED, §5). No engine edits (the Leg-C driver imports `ave.core.chiral_lattice` + `ave.core.constants` read-only; the finite net, the rank-2 bond dynamics, and the stepper are built in the driver). Every `[canon]` input content-verified two-method (`sed` + `grep`) at base HEAD `3fdf88a4` (verify-before-cite). The prereg was frozen + pushed ALONE first (freeze-by-push, commit `db124539`); the Leg-C driver committed before this result (`8ec0743a`).

> **HEADLINE (stated plainly; the mechanism-headlines-with-solidity rule active — every closing candidate carries its `clm-`/solidity inline).** The frozen-bin verdict is **NONE-DERIVES**. **No cold-regime derived closing mechanism exists, and the elastic-solid structure of the AVE vacuum actively FORCES the opposite** (a radiating longitudinal/dilatation wave). All three named closure routes fail at COLD/derived grade:
>
> - **mode-absence — FALSIFIED (Leg A + Leg C).** The A1-dilatation is carried by a genuine gapless propagating Bloch branch — the vector-survey **P-branch** (`clm-bnd5rq`, **solidity 0.8, ok-with-caveats** — the vector-band values ride on the GR-imported `ν = 2/7` / `K = 2G` (PR#261), an echo input; gates PASS; `c_P/c_S ≈ 1.71–1.90`, lattice-computed) — reproduced by the Leg-C driver (`c_P/c_S = 1.82`, per-dir `[1.711, 1.855, 1.904]` exact, frozen direction-resolved `<3%` gate PASS). The `srs-band-structure.md:117` "`√2·c` NOT a Bloch branch" flag is about the CONSTITUTIVE `V_LONG` re-expression, NOT the dilatation-carrier: the P-branch IS Bloch and DOES carry `∇·u ≠ 0`. Empirically, a point **breathing (pure-dilatation) source radiates ≈ 98% LONGITUDINAL far-field content** on the shipped srs net (Leg-C `f_long = 0.982` in the reflection-free spectral window; `0.994` at the pulse peak; `0.9988` in the finite-size-free C-2 spectral anchor). Mode-absence is falsified analytically AND empirically.
> - **derived-cold-regime-emptying — FALSIFIED (Leg B).** The only emptying mechanism (`clm-9kd2t3`, 0.42, **do-not-build**) is `Z_eff = Z_0/√S_sat` (`k4-port-irrep-decomposition.md:107` `[canon]`), which **→ `Z_0` exactly as `S_sat → 1`** — zero bond-level mismatch, zero conversion, in exactly the cold-linear far field where the GW question lives. The candidate static-gravitational-gradient scatter is **Z-preserving** (`Γ = 0`, `master-equation.md:113` `[canon]`) ⇒ refracts, does not mode-convert; and even a nonzero transit-conversion cannot rescue the **source** energy budget the pulsar measures. Cold emptying is exactly zero.
> - **derived-source-decoupling — FALSIFIED (Leg A source-trace + §2 tracelessness).** The gravitational source IS an A1-dilatation source (`mass = A1 dilatation`, `master-equation.md:20` `[canon]`: "`mₑc²` = trapped acoustic **compression** energy"); its quadrupole moment is the rotating mass second-moment (`⃛Q^{TL} ≠ 0`, the tracelessness rescue is dead, #758 §2 / #750 §3.3). No derived theorem makes the A1 radiative projection vanish.
>
> **The make-or-break `∇·u` derivation (#758 §3.4(b), R2) is not merely un-derived — it is STRUCTURALLY BLOCKED.** The EM Gauss-kill rests on a **curl-only** potential energy (`eq_axiom_3.tex`: `L_node = ½ε₀|∂_t A|² − (1/2μ₀)|∇×A|²`) that gives the EM longitudinal `∇·A` NO restoring force (gauge/non-dynamical). The **mechanical** dilatation `∇·u` has a genuine **bulk restoring force** `½K(∇·u)²` with `K ≠ 0` (`cosserat-mass-gap.md:30` elastic `W`, bulk-`K` term at `:122`; `K = 2G` is a GR-imported value per `srs-band-structure.md:116` / PR #261, but only `K ≠ 0` is load-bearing; the mass energy is *stored* in this compression per `master-equation.md:20`) ⇒ it is a **propagating radiative wave**, not a gauge mode. Carrying the EM kill to `∇·u` would require deleting the bulk term — which destroys `K = 2G`, the P-wave, and the solid-crystal identity of Axiom 1. **AVE's commitment to a real elastic-medium vacuum is what forces Reading A.**
>
> **CONSEQUENCE (per the port-register Q1 row's own clause).** Reading B (Grant's RULED-CONDITIONAL, `port-register.md:87`) **REVERTS**, and the **banked Reading-A pulsar exclusion goes LIVE** (falsification ledger; extra flux `F_bulk/F_shear ≈ 0.03–0.12` excluded at **9–110σ** Hulse-Taylor / **100–1400×** the double-pulsar bound). The Leg-C driver reproduces the excluded-flux magnitude as a **continuum-import color-check**: rotating-quadrupole `F_bulk/F_shear = 0.033` = the continuum multipole formula re-evaluated at the lattice `c_P/c_S` (matches the q1-hardening `0.0329`; a co-monotone re-expression of the C-2 speed-ratio match, no independent evidential weight — §4.2). **This is a clean, bankable NEGATIVE (Rule 11 honest closure)** — the consequence is ROUTED to Grant/auditor, NOT edited here (READ-ONLY on KB). The ruling to REVERT is Grant's; this lane delivers the derivation the ruling was conditioned on and the derivation says NONE-DERIVES.

---

## §0 — REGIME / SECTOR / PHASE-STATE header (fired before any structural algebra)

**MODE.** A non-relativistic compact binary (Hulse-Taylor B1913+16, `v/c ~ 10⁻³`; double pulsar J0737-3039A/B, `v/c ~ 2×10⁻³`) as a **source** driving the deep vacuum's A1/bulk sector. Contrast column: the observed orbital decay `Ṗ_b`, matched to the GR shear-quadrupole (#753 §2).

**REGIME.** **Regime I** — deeply linear far field (`V_GW/V_snap ~ 10⁻²⁸`); the lattice "responds linearly, lossless propagation at `c`." Saturation (Op14) does NOT enter the propagation. This is load-bearing: the only corpus emptying mechanism (`clm-9kd2t3`) is `S_sat`-gated and vanishes as `S_sat → 1`, i.e. ABSENT in this cold-linear far field. A closure that needs saturation does not qualify for PORT-CLOSED-DERIVED (the #758 R1(c) fence).

**PHASE-STATE.** **Cold-reactive** (Ax3-lossless-reactive; `eq_axiom_3.tex` `[canon]`). Far-field radiation is a legal Ax3 loss channel (port-not-valve), so a radiating bulk channel does NOT violate Ax3 — that is why this is a live question, not an Ax3-forbidden one.

**SECTOR.** The observed GW = **T2 transverse shear** at `c`. The channel under test = **A1 bulk dilatation** (the mass sector). **Sector-ownership discipline (do NOT cross-wire):** A1 owns compression/mass/dilatation; T2 owns shear/GW/charge-winding. The binary's masses ARE the A1-dilatation content (`master-equation.md:20` `[canon]`, "A1 dilatation-MASS … `mₑc²` = trapped acoustic compression energy"), so the source is genuinely an A1 source — the derivation must take the A1 far-field seriously, not dismiss it by sector fiat.

**SUBSTRATE-NATIVE + PHASE-SPACE-COORDINATE CHECK (A46).** The corpus claim is a *channel-radiation* claim (does the A1 branch carry a far-field port?). The matching test coordinate is the **longitudinal (`∇·u`, radial) vs transverse (`∇×u`, tangential) displacement partition** — the same displacement-vector basis the vector band survey uses, NOT a scalar `φ²` proxy. `consistency-vs-emergence`: the band eigenstructure is a **MANIFESTATION** (theorem of the srs `D(k)`); every reported VALUE is dimensionless (energy fractions, speed ratios) or `[import]`/`[canon]`-tagged.

**CONSENSUS-KNIFE, BOTH WAYS (fired up front, and it DECIDES this lane).** (i) The QED/GR reflex — "the longitudinal is always gauge/constrained" — would hand Reading B the win by default and is a known SM/QED leakage risk. (ii) The AVE-native vacuum-engineer reflex — "the vacuum is a real compressible elastic medium, so it radiates P-waves like a rock" — would hand Reading A the win by default. **The decider is which one AVE's own structure realizes.** GR closes the port by a genuine *gauge* structure (diffeomorphism invariance ⇒ the scalar/longitudinal metric parts are constrained, non-radiative). AVE's vacuum is NOT a gauge field — it is a real elastic **crystal** with physical node displacements `u` and `K = 2G ≠ 0`. A physical displacement with a bulk restoring force is a radiating wave, full stop. **AVE does not inherit GR's gauge kill; it has an elastic structure instead — and the elastic structure radiates.** The reverse-seduction fence (a make-or-break lane could be seduced by a clean kill) is satisfied: the kill is genuinely derived (the P-branch exists, the source drives it, no cold constraint kills it) AND the consensus knife confirms GR closes by a mechanism AVE structurally lacks.

---

## §1 — LEG A: MODE EXISTENCE + source-coupling trace (★the sharpest)

### §1.1 — The A1-dilatation IS carried by a genuine propagating Bloch branch (the vector P-branch) `[canon-read + derived]`

The prereg's Leg-A question: is there a genuine SCALAR-A1-dilatation propagating branch at all, given `srs-band-structure.md:117` flags the `√2·c` bulk-sound as "NOT a Bloch branch"? **The flag is about the wrong object.** The corpus carries THREE distinct "longitudinal" objects, and only one is the dilatation-radiation carrier:

- **The `√2·c` A1-scalar bulk-sound** `V_LONG = √(2G/ρ)` (`srs-band-structure.md:117` `[canon]`): *"the pure-dilatation A1 port mode that DROPS the `4G/3` shear term — a scalar-sector object imported from `constants.py:770`, **NOT a Bloch branch** of this translational problem."* This is a **constitutive/impedance** re-expression (the `Z_bulk` reflection speed), NOT a propagating far-field mode. The flag is correct — but this object is not what a gravitating source launches.
- **The `√(10/3)·c` isotropic P-wave** (`c_P/c_S ≈ 1.71–1.90`, direction-resolved, `srs-band-structure.md:120-122` `[canon]`): the **full compressional wave** (`+4G/3` shear stiffening), which **IS a lattice-computed gapless Bloch branch** reaching DC. This is the dilatation-carrier: a plane compression shears the medium, and `∇·u ≠ 0` for the P-wave.
- **The `1·c` transverse S-branch** (`c_S = c₀`): the shear/transverse acoustic branch (`∇·u = 0`).

**The decisive fact (`[derived]`):** the A1-dilatation `θ = ∇·u` is the **longitudinal polarization of the vector displacement field** — it is nonzero exactly for the P-branch and zero for the S-branches. It is NOT a separate scalar DOF; it is a projection of the same 3-vector `u` the vector survey solves. So the object that carries the gravitational A1-dilatation into the far field is the **P-branch**, which the adjudicated survey (`clm-bnd5rq`, **solidity 0.8, ok-with-caveats** — vector-band values ride on the GR-imported `ν = 2/7`/`K = 2G` echo input; gates PASS #604/#607) shows is a **gapless propagating Bloch branch reaching DC** with NO stop-band. **Mode-absence is FALSIFIED: the dilatation-carrier is a genuine propagating Bloch mode.** The `#758 B4.4` nuance (the `√2` object is not Bloch) is real but does not rescue mode-absence — it flags the wrong object; the dilatation rides the P-branch, which IS Bloch. `[canon-read; derived]`

### §1.2 — The source-coupling operator: gravity drives the dilatation channel `[derived]`

Tracing the #750 multipole source term to the lattice DOF it drives (the prereg's explicit Leg-A ask): the A1/bulk field equation is (taking the divergence of the linear elastodynamic equation, #750 §2.1 `[canon-read]`):
$$\partial_t^2\theta - c_L^2\nabla^2\theta = \tfrac{1}{\rho}\nabla\!\cdot\!\mathbf{f},\qquad \theta = \nabla\!\cdot\!\mathbf u,\ \ c_L^2 = (K+\tfrac43 G)/\rho.$$
The binary imposes `s = (1/ρ)∇·f ∝ M_a δ(x − x_a)` (because `mass = A1-dilatation`, `master-equation.md:20`). Its **quadrupole source moment is the rotating mass second-moment** `Q^{(s)}_{ij} ∝ Σ_a M_a x_{a,i}x_{a,j}`, whose traceless part rotates at `2Ω` (`⃛Q^{TL} ≠ 0`; the tracelessness rescue is dead — #758 §2, #750 §3.3, reproduced here §2). So the source **drives the P-branch dilatation channel at quadrupole order**, with no conservation law left to kill it (monopole killed by `Ṁ = 0`, dipole by momentum conservation). The gravitational coupling enters the dilatation channel, NOT only the shear channel. `[derived]`

**The prereg's alternative (is the P-branch's longitudinal content "the T2-shear-sector's own"?) is FALSIFIED.** The P-branch's longitudinal content IS `∇·u ≠ 0` (a compression), which by sector-ownership (A1 owns compression/dilatation) IS the A1 sector, NOT T2 (which owns shear, `∇·u = 0`). And the mass is the A1-dilatation. So the gravitational coupling is genuinely to the A1/dilatation P-branch content. Source-decoupling has no derived support. `[derived]`

### §1.3 — ★The Lagrangian-structure argument: the EM Gauss-kill is STRUCTURALLY BLOCKED from `∇·u` `[derived]`

This is the decisive analytic finding — it converts the #758 §3.4(b) make-or-break from "un-derived" to "structurally blocked," which is the difference between UNDETERMINED and NONE-DERIVES.

**The EM longitudinal kill (Model α / `∇·E` / Gauss) rests on a curl-only Lagrangian.** The Ax3 EM/potential Lagrangian (`eq_axiom_3.tex` `[canon]`) is `L_node = ½ε₀|∂_t A|² − (1/2μ₀)|∇×A|²`. The potential energy depends **only on `∇×A`** (the transverse/curl part). The longitudinal part `A_∥` (with `∇×A_∥ = 0`) therefore has kinetic energy `½ε₀|∂_t A_∥|²` but **ZERO potential energy** ⇒ its EOM is `ε₀ ∂²_t A_∥ = 0` ⇒ **no restoring force ⇒ non-propagating / gauge**. This IS the substrate-native Gauss law (`∇·E = 0`), and it is genuinely DERIVED for the EM sector. The bare K4 4-port scatter `S = ½·𝟙 − I` merely *preserves* the A1 bond-common-mode (`+1`, `clm-j550uh` 0.85, `k4-port-irrep-decomposition.md:85` `[canon]`); the *emptying* that would kill it is `clm-9kd2t3` (0.42, **do-not-build**, `S_sat`-gated, EM-`∇·E`-scoped) — and per Leg B it vanishes cold. So even within the EM sector the closing step is do-not-build; but the deeper point is the SECTOR.

**The mechanical dilatation (`∇·u`, the gravitational mass sector) has a curl-carrying restoring force — a DIFFERENT Lagrangian.** The Cosserat elastic Lagrangian (`cosserat-mass-gap.md:30` `[canon]`) is `L = ½ρ|u̇|² + ½I_ω|ω̇|² − W(u,ω)`, where `W(u,ω)` is the Cosserat energy density (`:32`). For the isotropic solid `W` includes the **bulk term** `½K(∇·u)²` with `K = 2G ≠ 0` (`cosserat-mass-gap.md:122` `[canon]`, the A1/bulk-`K` dilatational speed row: "bulk `K` (`K = 2G` magic angle)"). **Provenance tag (finding-4 fix):** `K = 2G` is a **GR-IMPORTED re-expression** (`srs-band-structure.md:116` `[canon]`: "a **K=2G RE-EXPRESSION** (GR-imported, PR #261), **NOT lattice-emergent**"), NOT a native K4-crystal property — but only **`K ≠ 0`** (any nonzero bulk modulus) is load-bearing for the structural block below; the specific value `2G` is not. The longitudinal displacement `u_∥` (dilatation `∇·u ≠ 0`) thus has kinetic `½ρ|u̇_∥|²` **AND potential `½K(∇·u)²`** ⇒ its EOM is `ρ ∂²_t u_∥ = K∇(∇·u_∥) + …` ⇒ a **propagating wave at `c_L`** (the P-wave) ⇒ **radiates**. This is not incidental: `master-equation.md:20` `[canon]` states the rest-mass energy IS "trapped acoustic **compression** energy" — the mass is *stored in* the bulk-compression restoring force. A mode that stores energy in compression HAS a restoring force and IS a wave.

**The two sectors have structurally different Lagrangians:**

| Sector | Longitudinal potential energy | Restoring force on the longitudinal | Result |
|---|---|---|---|
| **EM `∇·A`** (Model α, `eq_axiom_3.tex`) | **curl-only** `−(1/2μ₀)|∇×A|²` ⇒ **none** for `A_∥` | **ZERO** | gauge/non-dynamical ⇒ Gauss-kill ✓ |
| **Mechanical `∇·u`** (Model β, `cosserat-mass-gap.md:30` + bulk-`K` `:122`) | `½K(∇·u)²`, `K ≠ 0` (`K = 2G` GR-imported, `srs:116`) | **`K∇(∇·u)` ≠ 0** | propagating P-wave ⇒ RADIATES |

**Therefore the §3.4(b) make-or-break (carry the K4 `T_d` common-mode rejection from `∇·E` to `∇·u`) cannot be done:** the mechanical dilatation has a genuine bulk restoring force the EM longitudinal lacks. To kill it the same way, one must DELETE the `½K(∇·u)²` term — which sets `K = 0`, destroying `K = 2G`, the P-wave, the macroscopic-moduli edifice, and the solid-crystal identity of Axiom 1 (a `K = 0` vacuum is a fluid, not the chiral Laves K4 crystal). **The closure is not un-derived; it is structurally incompatible with the rest of the corpus.** `[derived]`

**NOTE (sector-ownership WORDING — routed for a Grant-level tightening; the physics identification is settled).** `master-equation.md:20` `[canon]` is the Grant-ratified 🔴 TWO-"3"s disambiguation banner (PR#260): it names the A1 dilatation-mass "the **Heaviside-excised longitudinal compression scalar** (the A1 breather; `mₑc²` = trapped **acoustic compression** energy)." These two phrases are the **NAME and the PHYSICAL CONTENT of ONE ratified object**, NOT two dynamically-incompatible readings. The "Heaviside-excised longitudinal compression scalar" is the **physical V-sector scalar** (`def-9a4f07` `[canon]`: "the real V-sector scalar grade … **physical, NOT Gauss-deleted** … must **never** be framed in QED-vector terms"); the gauge reading — "the longitudinal is always constrained/non-dynamical" — is exactly the QED-leakage the corpus **forecloses** (§0 consensus-knife; the longitudinal scalar is real). So the mechanical dilatation does **NOT** inherit an EM gauge-kill: the word "**acoustic**" pins it to a compression mode with a `K ≠ 0` restoring force ⇒ it radiates. The routed item is narrow and wording-only: the canon line's **dual phrasing** ("Heaviside-excised … scalar" alongside "acoustic compression") *invited* a gauge misreading — **this review closes the misreading** (the object is the physical acoustic scalar, radiates), and flags that the **sector-ownership wording on that canon line deserves a Grant-level tightening** so it cannot be re-misread. **Surfaced with both verbatim phrases from the same `[canon]` line; not reframed. Grant/auditor sector-ownership WORDING ruling (wording-only; the identification stands).**

---

## §2 — LEG A companion: the tracelessness rescue is dead (source-side) `[derived]`

Kept for completeness (the last source-side escape). The A1 channel is the scalar/breathing sector; the mass second moment `M_ij` has a trace `Σ M_a r_a²` that, for a circular binary, is constant. Tempting: "the A1 sees only the constant trace ⇒ no radiation." **FALSE.** A scalar/longitudinal field radiates at quadrupole order via the **traceless** second moment of the source (standard acoustic multipole radiation — the same structure that gives every earthquake its P-arrival). With `mass = A1-dilatation`, the scalar source's second moment IS `M_ij`, whose traceless part **rotates at `2Ω`** — nonzero. Reproduced by the #750/#758 drivers (`⃛Q^{TL} ≠ 0`; the traceless `xx` component varies while the trace is constant). The tracelessness rescue is dead; the surviving symmetric-coupling route reduces to the B1 grade-assignment (consistency-class, not derived) and is subsumed by the §1.3 Lagrangian argument. `[derived]`

---

## §3 — LEG B: COLD-REGIME EMPTYING — exactly zero conversion `[canon-read + derived]`

**The Op3 emptying vanishes cold.** The only corpus A1→T2 emptying is the Op3 bond-level impedance mismatch (`clm-9kd2t3`, 0.42, do-not-build): `Z_eff = Z_0/√S_sat` (`k4-port-irrep-decomposition.md:107` `[canon]`), `S_sat` = the Axiom-4 saturation factor `S(A) = √(1 − (A/A_yield)²)`. In the cold-linear far field, `A → 0` ⇒ `S_sat → 1` ⇒ **`Z_eff → Z_0` exactly** ⇒ **zero bond-level mismatch ⇒ zero A1→T2 conversion.** The bare scatter `S = ½·𝟙 − I` takes over, which PRESERVES the A1 (`+1`, `k4-port-irrep-decomposition.md:85` `[canon]`: "the bare scattering **preserves the `A_1` mode exactly**"). **The derived cold limit of the Op3 emptying is exactly zero.** The empirical `A1 → 0` anchor (`phasor_discovery.py`, `:89-97`) was a **saturation-engaged bound (2,3) soliton** (amplitude 0.5) — NOT the cold far field. `[canon-read + derived]`

**The static-gradient scatter (the prereg's cold sub-route) is Z-preserving ⇒ does not mode-convert.** The prereg asked: a static galactic gradient means `z_local` varies spatially even cold — does the spatial gradient itself scatter A1→T2? **No.** The gravitational index gradient is a **SYMMETRIC (`ε,μ` co-scaled) modulation** that **preserves `Z_0`** (`master-equation.md:113` `[canon]`: "strains … `ε_eff` and `μ_eff` fields symmetrically … preserving `Z_0`"). An impedance-matched (`Γ = 0`) gradient **REFRACTS but does not reflect/mode-convert** — there is no impedance mismatch to drive an A1→T2 conversion. It is also **adiabatic** (weak, `2GM/rc² ~ 10⁻⁶`, smooth over the GW wavelength) ⇒ WKB modes propagate independently, no conversion. `[derived]`

**And even a nonzero transit-conversion cannot rescue the pulsar budget `[derived]`.** The static-gradient scatter (if any) is a **PROPAGATION** effect (A1 launched at the source, converts en route), NOT a **SOURCE** effect. Pulsar timing measures `Ṗ_b`, the **energy-loss rate at the binary** — it counts every channel the source radiates into, regardless of downstream conversion. Energy that leaves the binary in the A1 channel shows up in `Ṗ_b` even if it converts to T2 a kiloparsec later. So transit-conversion is irrelevant to the exclusion. **Leg B eliminates the derived-cold-emptying route: cold conversion is exactly zero, and any transit-conversion is both suppressed (Z-preserving/adiabatic) and budget-irrelevant.** `[derived]`

---

## §4 — LEG C: NUMERICAL INJECTION — the lattice radiates ≈ 98% longitudinal (port OPEN on the lattice)

Driver: `research/drivers/mechanical_commonmode_injection.py` (+ `_results.json`, figure). Engine byte-untouched (imports `ave.core.chiral_lattice`/`constants` read-only; Rule-14 reuse of the survey bond model + the DERIVED `ρ* = 9.77337` from `ν_Hill = 2/7`, imported not hard-coded). **Frozen criteria (prereg §3): the frozen OBSERVABLE (`f_long`), TOLERANCE (`>0.5`), seed (`A = 10⁻³`, `∇×u = 0`), `r_meas = 6`, `dt = 0.2·dt_CFL` ran as frozen; three elements DEVIATED and are disclosed in §4.0.**

### §4.0 — Deviations from the frozen prereg (disclosed; all verdict-neutral) `[#761 review]`
The frozen-criterion-only discipline requires silent deviations to be surfaced even when forced or conservative. Three exist; none changes the frozen bin (all reads `≫ 0.5`), and the review re-ran both grids first-party.

1. **GRID: frozen `L = 16` → shipped `L = 20`.** Prereg §3 (`:77`) froze `L = 16` (`32768` sites); the shipped run used `L = 20` (`64000` sites). The change is **forced by window mechanics** (at `L = 16` the reflection-free window `[11.55, 19.24)` closes just *before* the lattice-converted S front at `t_S = 21.03`; `L = 20` moves the boundary out so the window captures the full P+S content) and **conservative** (a larger box admits more transverse S content ⇒ can only *lower* `f_long` toward the threshold, not away). **Both grids are now banked first-party** (§4.1): L=16 `f_long = 0.9730`, L=20 `f_long = 0.9816` — both `≫ 0.5`, and the finite-size-FREE C-2 spectral anchor (`0.9988`) is L-independent.
2. **C-2 GATE: frozen direction-resolved `<3%` → shipped isotropic-mean band.** Prereg §3 (`:75`) froze "reproduce the vector-survey `c_P/c_S ≈ 1.71–1.90` (direction-resolved) to `< 3%`"; the first machine gate checked only the isotropic mean in a wider `[1.60, 1.95]` band (a weaker proxy that cannot fire on the failure mode the frozen gate targets). **The frozen direction-resolved gate is now implemented and run** (`#761 R3`): per-direction `|meas − survey|/survey` vs the survey `[1.7105, 1.8528, 1.9041]` = `[0.00, 0.13%, 0.00]`, **all `< 3%` → PASS**. Both gates are kept (KEEP-BOTH); the direction-resolved one is the operative frozen criterion.
3. **WINDOW MECHANICS: frozen analytic formula → first-pass 5%-of-peak front-detect → SPECTRAL analytic (this review).** Prereg §3 (`:67`) froze `t_arrival ≈ r_meas/c_P`, `t_reflect ≈ (2·L_edge − r_meas)/c_P`. The first-pass driver instead detected the front from a 5%-of-peak shell-energy crossing; that crossing fires on the seed's **near-field precursor ramp** (measured "front speed" `0.949` cells/time ≈ **1.8×** the physical acoustic `c_P ≈ 0.52` — unphysical). An **out-of-band first-pass correction** (recorded here for the first time, per audit-trail-in-git) moved the reported `f_long` from a first-pass **`0.855`** to **`0.9905`** by adjusting that front-detect; both cleared the frozen `>0.5` bin (the change never controlled the verdict, and moved the number in the results-favorable direction). **This review supersedes BOTH** with the frozen analytic definitions evaluated at the C-2 **spectral** speeds (§4.1): corrected `f_long = 0.9816` (L=20) / `0.9730` (L=16). The superseded first-pass numbers (`0.855`, `0.9905`) are preserved here, not operative.

*(Also disclosed: the full pytest suite was SIGTERM'd during the original run; only `make verify` was asserted green, which reproduces. This review re-ran `make verify` green and the driver deterministically.)*

### §4.1 — C-1 (time-domain, radiation at range, SPECTRAL-windowed) — the frozen primary
Finite BCC srs supercell, rank-2 bond dynamics `ρü_s = −Σ_b Φ_b(u_s − u_t)`, cold seed `A = 10⁻³` (sub-yield, saturation OFF). Seed = a pure-dilatation breathing pulse `u = ∇[exp(−r²/2σ²)]` (curl-free by construction: measured **seed transverse-energy fraction `= 2.15×10⁻³²`**). At `r_meas = 6` cells the site displacements are split into **radial (longitudinal)** and **tangential (transverse)** parts and energy-integrated over the Poincaré window. **Run at BOTH grids** — the frozen `L = 16` (`32768` sites) and the shipped `L = 20` (`64000` sites, `93600` bonds) — with the corrected spectral window (deviations disclosed in §4.0).

- **★ Poincaré window from the SPECTRAL speeds (REVIEW-CORRECTED, #761 R2 — see §4.0).** The window is `[t_P_arrival, t_reflect)` with `t_P = r_meas/c_P`, `t_reflect = (2·L/2 − r_meas)/c_P`, `t_S = r_meas/c_S`, all at the **C-2 spectral** speeds (isotropic `c_P ≈ 0.520`, `c_S ≈ 0.285` cells/time):
  - **L = 20 (shipped/operative):** window `[11.55, 26.94)` — **captures both** the P front (`t_P = 11.55`) and the lattice-converted S front (`t_S = 21.03 < t_reflect`) before any reflection returns.
  - **L = 16 (frozen grid):** window `[11.55, 19.24)` — captures the P front + the shell-energy peak; the S front (`t_S = 21.03`) falls just **outside** the shorter reflection-free window (the frozen box is smaller — this is precisely why the shipped run enlarged to L=20, §4.0).

  Energy drift `|ΔH/H| = 9.7×10⁻³` at both grids (bounded Verlet, matches the corpus `cosserat_wave_test` ~1% precedent).
  > **Superseded (NOT deleted — #761 R2).** The first-pass window `[6.32, 12.6)` was built from a 5%-of-peak shell-energy crossing that fired on the Gaussian seed's **near-field precursor ramp**, reporting a "front speed" of `0.949` cells/time — ~**1.8×** the maximum acoustic speed (spectral `c_P ≈ 0.52`), which is unphysical (nothing outruns the `k → 0` sound speed on an acoustic branch). That window closed *before* the shell-energy peak (`t = 13.1`), and its "captures the S front" and "post-window fall = boundary-reflection artifact" labels were **false at the true speeds** (the true S front is at `t ≈ 21`, the true reflection return at `t ≈ 27`). The `0.949` crossing is retained only as a reported artifact diagnostic (`cP_precursor_diag`), never for the window.
- **★ OBSERVABLE (frozen): `f_long = E_∥/(E_∥+E_⊥)`** (energy-weighted over the corrected window):
  - **L = 20:** `f_long = 0.9816` (window) / `0.9936` at the shell-energy peak (`t = 13.2`) / `0.8175` over the FULL trace *including* post-boundary reflections.
  - **L = 16 (frozen grid):** `f_long = 0.9730` (window) / `0.9927` at peak / `0.7216` full-trace.

  The result is **window-independent**: every read — reflection-free window, at-peak, and full-trace-with-reflections — is `≫ 0.5`. *(Superseded first-pass artifact-window read: `0.9905`; preserved for the record, not operative.)*
- **Frozen tolerance:** `f_long > 0.5` ⇒ **longitudinal radiation PRESENT → PORT OPEN on the lattice → NONE-DERIVES leans.** Got `0.982` (L=20) / `0.973` (L=16, frozen grid) ≫ `0.5` — **verdict class UNCHANGED at both grids** and under every window read.

### §4.2 — C-2 (spectral, finite-size-FREE anchor) — the robust cross-check
From the SAME lattice's Bloch `D(k)`, long-wave (`k·ℓ = 10⁻⁴`) acoustic eigenvectors; identify the longitudinal branch (`max|ê·k̂|²`); compute the far-field partition from `|ê_b·Ŝ|²` × `1/c_b⁵`.
- **Gate (frozen, direction-resolved `<3%`): `c_P/c_S = 1.8209` isotropic**, per-direction `[100] 1.711, [110] 1.855, [111] 1.904` — **reproduces the vector survey `[100]1.7105 / [110]1.8528 / [111]1.9041`** to per-direction relative errors `[0.00, 0.13%, 0.00]` (the **frozen direction-resolved `<3%` gate PASSES**, §4.0-2; self-consistency PASS, the lattice-derivation is faithful).
- **Breathing-source longitudinal fraction `= 0.9988`** (essentially pure P — a longitudinal Bloch branch exists and a dilatation source couples to it almost purely). This is a **genuine lattice eigenvector projection** (`⟨|ê_L·k̂|²⟩` over the direction sphere) and the finite-size-free confirmation of C-1's `f_long`.
- **Rotating mass quadrupole `F_bulk/F_shear = 0.0333` — a CONTINUUM-IMPORT color-check, NOT an independent lattice partition (`#761 R4`).** This is the continuum multipole formula `A_ang·(c_S/c_P)⁵` (with `A_ang = 2/3` the isotropic P/S angular integral and `1/c⁵` the flux scaling — **both continuum imports**, q1 §1.2) **re-evaluated at the lattice-measured `c_P/c_S = 1.8209`**. It matches the q1-hardening `0.0329` (the identical formula at the VRH ratio `√(10/3) = 1.8257`) simply because the lattice ratio landed within `0.3%` of `√(10/3)` — the two differ only by `(1.8257/1.8209)⁵ ≈ 0.987`. It is therefore a **co-monotone re-expression of the C-2 `c_P/c_S` agreement already reported above; it adds NO independent evidential weight** beyond that speed-ratio match. The prereg's frozen C-2(b) `|ê_b·Ŝ|²` **quadrupole** eigenvector projection was **NOT implemented** (only the (a) breathing projection is a genuine lattice computation); it is routed as an owed follow-on (§6.2). The port-open verdict rests on C-1 `f_long` and the C-2 breathing projection, NOT on this color-check.

### §4.3 — Leg-C verdict + finite-size honesty
Both measurements agree decisively: **the shipped srs lattice radiates ≈ 98–99% longitudinal far-field content from a breathing source** (`f_long = 0.982` C-1 window / `0.994` at the pulse peak / `0.9988` C-2 spectral); **mode-absence is empirically FALSIFIED; the port is OPEN on the lattice itself.** Finite-size honesty (declared in the prereg, held): (i) the C-1 read is valid inside the reflection-free **spectral** window (§4.1) — and the port-open conclusion is additionally **window-independent** (it holds at-peak `0.994` and over the full trace-with-reflections `0.82`, all `≫ 0.5`); (ii) a finite lattice cannot resolve a truly-DC far field — the finite-`k` C-1 pulse is cross-checked by C-2's `k → 0` spectral partition (`0.9988`), and they agree; (iii) fixed (Dirichlet) boundaries reflect — window discipline is the mitigation, C-2 is the finite-size-free anchor; (iv) the seed is `∇×u = 0` by construction (seed transverse fraction `2.15×10⁻³²`), so the residual transverse content is **not a seeding artifact** — but it is **`≤ ~1–2%`, at/near the integrator ledger-drift floor** (`|ΔH/H| = 9.7×10⁻³`; the at-peak transverse `~0.6%` sits *below* drift) and is **NOT attributed to a resolved physical P→S conversion signal** at this dt/CFL: the integrator-free, finite-size-free C-2 spectral anchor puts the true far-field transverse at only `~0.12%`, an order of magnitude below the time-domain residual. `f_long ≈ 0.98–0.99` (port OPEN) is robust and `≫ 0.5`; the sub-percent transverse attribution would need a tighter dt/CFL (drift `≪` residual) before it could be cited as a measured lattice property.

---

## §5 — FROZEN-BIN VERDICT + decisive step per leg + consequence

| Leg | Frozen outcome | Decisive step |
|---|---|---|
| **A — mode existence + source-coupling** | **mode-absence FALSIFIED; source-decoupling FALSIFIED; make-or-break `∇·u` STRUCTURALLY BLOCKED** | The dilatation-carrier is the **P-branch** (Bloch, gapless, `c_P/c_S ≈ 1.8`; the `√2` "not-Bloch" flag is the wrong object). The source drives it (`mass = A1-dilatation`, quadrupole rotates). The EM Gauss-kill (**curl-only** Ax3 Lagrangian ⇒ no `∇·A` restoring force) does NOT transfer to `∇·u`, which has a **bulk restoring force `K ≠ 0`** (`cosserat-mass-gap.md:30` Lagrangian + bulk-`K` `:122`; `K = 2G` GR-imported per `srs:116`/PR#261, only `K ≠ 0` load-bearing; mass = "trapped acoustic compression energy") ⇒ radiating P-wave. Deleting the bulk term to force the kill sets `K = 0` ⇒ destroys the P-wave/the crystal. |
| **B — cold-regime emptying** | **derived-cold-emptying FALSIFIED (exactly zero)** | `Z_eff = Z_0/√S_sat → Z_0` as `S_sat → 1` (`k4-port-irrep-decomposition.md:107`) ⇒ zero cold conversion; bare scatter PRESERVES A1 (`:85`). The static gradient is Z-preserving (`Γ=0`, `master-equation.md:113`) ⇒ refracts not converts; and transit-conversion is budget-irrelevant to `Ṗ_b`. |
| **C — numerical injection** | **PORT OPEN on the lattice (mode-absence falsified empirically)** | Breathing source ⇒ `f_long = 0.982` (C-1 window, L=20) / `0.973` (L=16 frozen grid) / `0.994` at-peak / `0.9988` (C-2 spectral) — all `≫ 0.5`; `c_P/c_S = 1.82` reproduces the survey (direction-resolved `<3%` PASS); rotating-quadrupole `F_bulk/F_shear = 0.033` (continuum-import color-check, §4.2). |

**Overall frozen-bin verdict: NONE-DERIVES.** All three named closure routes (mode-absence / derived-cold-regime-emptying / derived-source-decoupling) FAIL at cold/derived grade, and the make-or-break `∇·u` derivation is structurally blocked by the elastic-solid Lagrangian.

**UNDETERMINED is NOT the verdict (the discriminator, per the prereg).** The re-banked #758 landed UNDETERMINED by framing the **Model α (K4-TLM bond-scatter) / Model β (srs Cauchy site-elasticity)** choice as the verdict-controlling unforced fork. It does NOT control the verdict — and the reason is **not** the earlier phrasing that "both branches happen to leave the cold port open" (that leg equivocated: §1.3 classifies Model α's EM-`∇·A` longitudinal as gauge-killed = *closed*, so "Model α leaves the port open" contradicted this doc's own table). The **sound** reason is upstream of the fork: **the object the fork disputes — the gravitational mass channel — is unavoidably a mechanical acoustic compression `∇·u`, which radiates regardless of which naming wins.**

**The mass IS mechanical acoustic compression (the fork-foreclosing fact).** `master-equation.md:20` `[canon]` (the Grant-ratified 🔴 TWO-"3"s banner, PR#260) names the A1 dilatation-mass "the **Heaviside-excised longitudinal compression scalar** (the A1 breather; `mₑc²` = trapped **acoustic compression** energy)" — the two phrases are the **NAME and the PHYSICAL CONTENT of ONE object**, not two readings (the gauge reading is corpus-foreclosed, §1.3 NOTE / §0 consensus-knife). The word "**acoustic**" pins the mass to mechanical sound (`∇·u`), NOT the EM potential `∇·A`. A mechanical compression **source** unavoidably drives the mechanical `∇·u` channel, which is the **longitudinal polarization of the vector displacement field** (§1.1, "NOT a separate scalar DOF"), rides the **gapless lattice-computed P-branch** (`srs-band-structure.md:120` direction-resolved `c_P/c_S` [100]1.71/[110]1.85/[111]1.90, "lattice-computed"; the VRH ratio `√(10/3)` re-expression at `:116`; `clm-bnd5rq`, **0.8, ok-with-caveats**), and radiates against the `K ≠ 0` (`K = 2G`) restoring force (Legs A + C). **You cannot relocate the mass into EM-`∇·A` because it is *acoustic*.**

**Neither fork branch closes the mechanical `∇·u` port at derived grade:**
- **Route (b) [carry the EM Gauss-kill to `∇·u`] is STRUCTURALLY BLOCKED** (§1.3): the mechanical dilatation has a bulk restoring force `½K(∇·u)²`, `K ≠ 0`, that the curl-only EM Lagrangian lacks — killing it deletes the P-wave and the crystal.
- **Route (a) [gravity is the α / EM-`∇·A` gauge sector; β does not own gravity] is FORECLOSED** by the acoustic identification above — the mass cannot be relocated out of the mechanical `∇·u` channel.
- **Model α's bond-level A1-preservation is SILENT on the port, not a closure.** Model α (K4-TLM bond-scatter) is the EM-`∇·E` sector (§1.3), not the mechanical `∇·u` gravitational sector. Its unitary bond-level A1-preservation (`k4-port-irrep-decomposition.md:105` `[canon]`: "`A_1` would propagate forever, `T_2` would reflect forever, no energy loss") is **normal-mode persistence / no-dissipation — NOT a closed-port statement and NOT a radiation statement**; it is simply *silent* on far-field coupling. **Do NOT cite the `√2·c` row (`srs-band-structure.md:117`'s not-a-Bloch constitutive `V_LONG` object) as the carrier — the far-field carrier is the lattice-computed P-branch.**

Per the prereg's frozen UNDETERMINED-vs-NONE-DERIVES discriminator (`prereg §1`, verbatim: *"if BOTH branches of the unforced fork leave the cold port OPEN (neither branch closes it at derived grade), the fork does NOT control the verdict and the bin is NONE-DERIVES, not UNDETERMINED"*): neither branch closes the mechanical `∇·u` port at derived grade — route (b) is structurally blocked, route (a) is foreclosed by the acoustic identification — so **the fork does NOT control the verdict** and the bin is **NONE-DERIVES**. The anti-seduction fence is satisfied both ways: the lattice HAS the propagating compression branch (Legs A, C), the source DRIVES it (Leg A), no DERIVED cold constraint KILLS it (Leg B; the make-or-break is structurally blocked, §1.3); and the reverse-seduction fence is satisfied (GR closes by a gauge structure AVE structurally lacks — the kill is genuinely derived, not manufactured).

**CONSEQUENCE STATEMENT (Reading B REVERTS; routed, not edited).** Per the port-register Q1 row's own clause (`port-register.md:87` `[canon]`, verbatim: *"if NONE-DERIVES this ruling REVERTS and the banked Reading-A exclusion (falsification ledger; 9–110σ pulsar) goes live"*): **the RULED-CONDITIONAL Reading-B ruling REVERTS, and the banked Reading-A pulsar exclusion goes LIVE against the framework.** This is a clean, bankable NEGATIVE (Rule 11 honest closure). **The ruling to REVERT is Grant's; this lane delivers the derivation the ruling was conditioned on, and the derivation is NONE-DERIVES.** The consequence is ROUTED to Grant/the auditor lane, NOT executed here (READ-ONLY on KB fence — no edit to `port-register.md`, `08_gravitational_waves.tex`, or any leaf).

**Contradiction with the standing ruling (flag-don't-fix).** Grant's RULED-CONDITIONAL states Reading B = standing physics ("no far-field radiative port for gravitating sources; the halo is its complete story"). This derivation finds the port DERIVED-OPEN (NONE-DERIVES). **Both are surfaced with file:paths + verbatim content; neither is reframed to match the other.** The ruling was explicitly conditioned on this derivation; the derivation returns the condition-failing branch. Grant adjudicates.

---

## §6 — Calibration-vs-derived ledger + owed follow-ons

### §6.1 — Ledger (`consistency-vs-emergence` tags)

| Quantity | FORM | VALUE | Class |
|---|---|---|---|
| P-branch is a gapless propagating Bloch branch carrying `∇·u ≠ 0` | `[canon-read]` (`clm-bnd5rq`, **0.8, ok-with-caveats** — `ν=2/7`/`K=2G` GR-import echo) + `[derived]` (dilatation = longitudinal polarization) | `c_P/c_S ≈ 1.8`, dimensionless | consistency/characterization — **mode-absence FALSIFIED** |
| EM Gauss-kill is curl-only-Lagrangian ⇒ no `∇·A` restoring force | `[canon-read]` (`eq_axiom_3.tex`) | — | manifestation (EM sector) — does NOT transfer to `∇·u` |
| mechanical `∇·u` has bulk restoring force `K ≠ 0` ⇒ radiating P-wave (`K = 2G` value GR-imported) | `[canon-read]` (`cosserat-mass-gap.md:30` + bulk-`K` `:122`, `master-equation.md:20`) + `[derived]`; `K = 2G` `[import]` (`srs:116`, PR#261 — only `K ≠ 0` load-bearing) | — | **the structural block on the make-or-break** |
| Op3 cold limit `Z_eff → Z_0` (zero conversion at `S_sat → 1`) | `[canon-read]` (`clm-9kd2t3` `:107`) + `[derived]` | — | **derived-cold-emptying FALSIFIED** |
| static gravitational gradient is Z-preserving (`Γ=0`) ⇒ no mode-conversion | `[canon-read]` (`master-equation.md:113`) + `[derived]` | — | consistency (subsumes the static-gradient sub-route) |
| Leg-C breathing-source `f_long` | `[derived]` (lattice driver; genuine time-domain + eigenvector projection) | `0.982` (C-1 window) / `0.994` at-peak / `0.9988` (C-2), dimensionless | **manifestation — PORT OPEN empirically** |
| Leg-C rotating-quadrupole `F_bulk/F_shear` | `[import]` continuum multipole formula re-evaluated at the lattice `c_P/c_S` (co-monotone with C-2 gate; C-2(b) projection NOT implemented) | `0.033` | **import color-check — NOT an independent lattice partition** (§4.2, §6.2-4) |
| pulsar exclusion `9–110σ` / `100–1400×` | — | `[import]` (Weisberg-Huang 2016; Kramer 2021) | import (banked) |
| `mass = A1-dilatation` (the coupling grade-assignment) | `[canon-read]` | — | consistency (NOT driver-validated; `cosserat-mass-gap.md:151`) — the load-bearing sector identity, held |

No emergence-class claim is headlined. The deliverable is the frozen-bin verdict (NONE-DERIVES) + the Leg-C measured partition (`f_long ≈ 0.98`, port open); both ride on the derived Lagrangian-structure block + the lattice measurement, not on a hidden calibration.

### §6.2 — Owed follow-ons (fenced; NOT executed here — Rule 12; slot NOT refilled with an assertion)

1. **★The Q1 REVERT ruling** — Grant's sector-ownership adjudication: does the elastic-solid `∇·u` radiate (Reading A, this derivation) or is it gauge-constrained (Reading B, requires deleting the bulk term)? This lane returns **NONE-DERIVES**; per the Q1 clause the RULED-CONDITIONAL Reading-B reverts and the banked exclusion goes live. *Grant-gated physics ruling; then the auditor lands the `port-register.md` Q1 revert + the falsification-ledger promotion.* (The auditor lands the manual entry; this lane surfaces the empirical finding — lane discipline.)
2. **The `master-equation.md:20` sector-ownership WORDING tightening (§1.3 NOTE)** — the Grant-ratified TWO-"3"s banner names the A1 dilatation-mass "the Heaviside-excised longitudinal compression scalar (the A1 breather; `mₑc²` = trapped acoustic compression energy)." This is **ONE** ratified object (the physical V-sector acoustic scalar, `def-9a4f07`), **NOT** two incompatible readings; the gauge reading is corpus-foreclosed and **this review closed it** (the acoustic identification forecloses the fork, §5). The narrow routed item is **wording-only**: the canon line's dual phrasing invited a gauge misreading — a Grant-level wording tightening so it cannot be re-misread. *Grant/auditor sector-ownership WORDING ruling (the physics identification stands; no verdict dependence).*
3. **The `08_gravitational_waves.tex` warningbox + FLAG-A speed-label reconciliation** — resolves WITH the ruling (auditor lane; not edited here). The warningbox's bulk+shear double-count is exactly the port this derivation finds open.
4. **The frozen C-2(b) `|ê_b·Ŝ|²` quadrupole eigenvector projection (`#761 R4`)** — the prereg §3 froze an independent lattice quadrupole projection to COMPARE against the continuum `A_ang·(c_S/c_P)⁵`; only the (a) breathing projection was implemented, and the (b) quadrupole line is the continuum formula re-evaluated at the lattice speed ratio (a co-monotone color-check, §4.2). Routed rather than implemented in-review because a faithful quadrupole projection still requires the continuum angular-emission weight (`A_ang` is a continuum import), so it adds no evidential weight beyond the already-banked C-2 `c_P/c_S` match and risks a new unverified computation in a make-or-break lane. *Owed follow-on; verdict-neutral (the color-check is not a verdict gate).*
5. **NOT owed:** a rescue derivation for Reading B. Per honest closure (Rule 11) and substitution-not-retraction (Rule 12), the NONE-DERIVES branch is closed; the slot is not refilled with an unverified rescue. Any future closure attempt gets a new version + its own verification chain and must overcome the §1.3 structural block (i.e. explain how `∇·u` is gauge-constrained WITHOUT deleting the bulk modulus `K ≠ 0`).

**None of items 1–5 are executed here.** This lane delivers the make-or-break derivation the #758 R2 named and returns **NONE-DERIVES**, leaving the REVERT ruling to Grant.

---

> **Derivation-doc provenance.** Fired by Grant 2026-07-20 (`"fire 1 and 2, then 3 once those land"` `[sic]`, item 1). Frozen bins + three-leg criteria + Leg-C observable pre-registered ALONE before deriving (`_prereg-FROZEN.md`, committed `db124539`, pushed with real margin); Leg-C driver committed before this result (`8ec0743a`). All `[canon]` citations content-verified two-method (`sed` + `grep`) at base HEAD `3fdf88a4` (verify-before-cite). FORMs `[derived]` by the Ax3-vs-Cosserat Lagrangian-structure argument + standard elastodynamic multipole algebra; the lattice partition is reproduced by `research/drivers/mechanical_commonmode_injection.py` (+ `_results.json`, figure), which imports `ave.core.*` read-only and touches no engine primitive. Mints no `clm-`; propagates to no leaf; READ-ONLY on KB; owed follow-ons fenced to §6.2. **Verdict: NONE-DERIVES — mode-absence FALSIFIED (P-branch is Bloch; Leg-C `f_long ≈ 0.98` port open, window-independent, both grids), derived-cold-emptying FALSIFIED (`Z_eff → Z_0` cold; Z-preserving gradient), derived-source-decoupling FALSIFIED (tracelessness dead; source drives the dilatation P-branch), and the make-or-break `∇·u` closure STRUCTURALLY BLOCKED by the bulk restoring force `K ≠ 0` (`K = 2G`, GR-imported value) the EM curl-only Lagrangian lacks. Consequence: the RULED-CONDITIONAL Reading-B REVERTS per its own clause and the banked 9–110σ pulsar exclusion goes live — a clean bankable negative, routed to Grant.** Companion: the pre-reg (`_prereg-FROZEN.md`), the re-banked #758 (`research/2026-07-20_b-mechanism-derivation_result.md`), the Q1 hardening (`research/2026-07-20_q1-pulsar-hardening.md`), the scalar-GW derivation (`research/2026-07-20_scalar-gw-bulk-channel_derivation.md`), the port register (`manuscript/ave-kb/common/port-register.md`, Q1 row), and the docket continuation (ENTRY 37).
