# Fork-W resolution candidate — sector-resolved kernel keying (swing rails k_a; circulation keys the rotational side)

**Date:** 2026-07-20
**Class:** DERIVATION (research-doc). Resolves the *residual* Fork-W left OPEN by merged **#770**
(`research/2026-07-20_constituent-cage-ensemble_result.md` §3, §7 — "★Wall-class / asymmetry paradox (Fork W): INVERTED — verdict-controlling again … the physical MODE/GEOMETRY mechanism by which a Cosserat-winding knot core grades `k_a` alone … routed to Grant; NOT picked"). Forms `[derived]`; every engine line + canon cite verified two-method at base HEAD `d3203d37`. **Canonizes nothing; mints no `clm-`; propagates to no KB/tex leaf; engine byte-untouched.** Routed for Grant ratification.
**Provenance / attribution.** Direction Grant-ratified 2026-07-20 (walk `[sic]`: *"yup that sounds promising!"*) — ratifies DIRECTION, not content. The keying formulation (ε keys the extension/swing coordinate, μ keys the circulation/slew coordinate, opposite signs) is **ORCHESTRATOR WALK-LEVEL**; this note tests it against the implemented keying and reports whether it stands. **Verdict: SUPPORTS the walked direction with two named refinements + one flag-don't-fix (§5).**

---

## §0 — REGIME / SECTOR / PHASE-STATE header

**MODE.** Static/constitutive — the question is *which stiffnesses a knot core grades*, not a dynamical run. **REGIME.** Near-yield saturated core (the A1 breather at `A ≈ A_yield`) embedded in a cold-linear exterior. **PHASE-STATE.** Op14 saturation active on the load-bearing coordinate; cold on the rest. **SECTOR.** Under test = A1 bulk/compression (`Z_bulk`, `k_a`, `K`) vs T2 shear (`Z_shear`, `k_s`, `G`) vs the Cosserat couple-stress (`μ`, `γ_c`); mass=A1, charge/spin=(2,3) Cosserat winding — A1 ⊥ T2, NOT cross-wired (`master-equation.md:20` `[canon]`). **A46 coordinate discipline:** the whole note is in the deformation-coordinate basis (axial swing / transverse swing / circulation), NOT lattice-Cartesian.

---

## §1 — The three layers of keying (axiom vs engine vs driver) `[canon-read + engine-read]`

Fork-W ("how a knot core grades `k_a` alone") is a *keying* question. So the first move is to read what each layer of the corpus actually commits the saturation kernel to — **which coordinate, and which sign**. The answer is that the AXIOM under-specifies the coordinate, the ENGINE resolves it per-sector with **opposite signs on orthogonal coordinates**, and the recent DRIVERS neither key on a swing coordinate nor derive the `k_a`-only grade — they *impose* it. That gap is exactly Fork-W.

| Layer | Kernel commitment | Argument coordinate | Direction | Cite (verified two-method @ `d3203d37`) |
|---|---|---|---|---|
| **AXIOM 4** | one universal quarter-arc `S(A)=√(1−(A/A_yield)²)` on "the substrate's bulk response to **local strain A** normalized to the bandwidth limit `A_yield`" | **generic scalar "local strain A" — coordinate UNSPECIFIED at the axiom text**; softening only | SOFTENS (`S→0`) | `axiom-register.md:186` (canonical-statement); `eq_axiom_4.tex` `eq:axiom4_saturation` |
| ↳ AXIOM 4 residual (buckling) | the √ *form* is the **axial projection of a fixed-arc-length K4 bond bow** — *"axial A1 dilatation load → transverse T2 bow response; A²+S²=arc\*²"* | the load-response is **coordinate-resolved**: an **axial** load, a **transverse** response | — | `axiom-register.md:189, 193` (residual_content + LOAD-RESPONSE SIGN RULE `[SIGN-RULE-DERIVED]`) |
| **ENGINE ε-kernel** | `ε_eff = ε_base·S(A)`, `A = V_local/v_yield`, `V_local = |E_component|·dx` | **EXTENSION / SWING** — the per-Cartesian-**component** local voltage (bond-parallel stretch amplitude) | SOFTENS | `fdtd_3d_jax.py:110-113` (`_compute_local_epsilon_kernel`); canonical source `scale_invariant.epsilon_eff` `:164`; applied per-component `fdtd_3d.py:361` |
| **ENGINE μ-kernel (Route-C)** | `μ_eff = μ_base/S(A_I)`, `A_I = curl_h·ℓ_node/I_max` | **CIRCULATION / SLEW** — the per-component discrete curl of H (internal circulating current) | **STIFFENS** (relativistic inductor; static-B ⇒ `curl_h≡0` ⇒ `A_I=0` ⇒ transparent) | `fdtd_3d.py:329-343` (numpy stepper `_compute_local_mu`), `:516-519` (energy readout), `fdtd_3d_jax.py:76-78` (JAX twin) |
| **ENGINE μ-kernel (matter/Meissner)** | `μ_eff = μ_base·S(|B|)` | `|B|` field amplitude (per-component magnitude) | SOFTENS | `scale_invariant.mu_eff` `:198` — the sector-agnostic kernel, **unchanged** by Route-C (used by `superconductor.meissner_mu_eff`, Yang-Mills) |
| **DRIVER #767/#770** | `Φ_b = k_a·(d̂⊗d̂) + k_s·(I−d̂⊗d̂)`; `bulk_only` rails `k_a→S_RAIL·k_a`, `k_s` FULL | **SPATIAL SHELL MEMBERSHIP** — a static rail dip by position, **NOT a swing coordinate**; the `k_a`-only grade is IMPOSED | SOFTENS `k_a` (imposed) | `constituent_cage_ensemble.py:87-93` (`bond_tensors`), `:125-167` (`cage_stiffness`, class `bulk_only` at `:136/:164`) |

**Three findings from the table.**

1. **The axiom under-determines the coordinate.** Axiom 4's canonical statement (`axiom-register.md:186`) commits to a **softening √-kernel on a single scalar "local strain A"** — it names neither the coordinate nor a sector-resolved sign. The SHAPE-DERIVED residual (`:189`) and the LOAD-RESPONSE SIGN RULE (`:193`, `[SIGN-RULE-DERIVED]`) are *sharper than the axiom text*: they already read the kernel as a **coordinate-resolved load-response bifurcation** (axial A1 load → transverse T2 bow), i.e. the corpus's own derivation of the √ form *already keys on the axial-extension coordinate*. So "the kernel grades everything together" is a property of a *scalar* `A`, not of the axiom.

2. **The engine keys per-sector on ORTHOGONAL coordinates with OPPOSITE signs** — exactly the walked claim. The ε-kernel softens on the **extension/swing** coordinate (per-Cartesian-component `V_local = |E|·dx`); the Route-C μ-kernel *stiffens* on the **circulation/slew** coordinate (`A_I = curl_h·ℓ_node/I_max`). The signs are reciprocal (`S` vs `1/S`) because the μ-side is the **relativistic inductor** dual (`translation-circuit.md:207`, `L_eff = L_0/√(1−(I/I_max)²)`), and its argument `A_I = Ė/(E_c·ω₀)` is the **slew-rate rating dual to the ε-kernel output-swing rating** (`translation-circuit.md:214`; ⚑ *algebra ✓ but the "μ-kernel-as-a-second-rating" FRAMING is #595/#603-UNMERGED, NOT canon — carried as framing-status*). The static-B transparency (`A_I=0` for zero circulation) is the birefringence FORK-1 resolution (`research/2026-06-25_vca-mu-circulation-observable-derivation.md`; Route-C).

3. **The driver assumes the answer Fork-W asks for.** `constituent_cage_ensemble.py` grades `k_a` by **spatial shell membership** (`cage_stiffness`, `:159` `rail = 1 − (1−s_rail)·w`), and the `bulk_only` class hand-selects "`k_a → S_RAIL·k_a`, `k_s` kept full" (`:164`, comment *"compression component grades; shear kept full"*). There is **no swing coordinate and no per-component keying** in the driver — the `k_a`-only grade is a **surrogate**, exactly as #770 states (`result §3`: *"a surrogate `k_a`-only rail is what expresses the wall here, not a derived saturation mode"*). #770 proved the surrogate is *constitutively realizable* (§2 below); it explicitly left **the mechanism** — *why* a knot core presents that grade — OPEN. This note supplies that mechanism from layer 2 (the engine's per-component keying) via the TKI bond map.

---

## §2 — The TKI transfer: swing rails k_a; where k_s falls; the −0.236 leak `[derived]`

**The bond map (TKI / Axiom 2, `translation-circuit.md:35, 104` `[canon]`).** The six per-node DOF split into **three translational → capacitive storage (E-sector, ε)** and **three microrotational → inductive flywheel (B-sector, μ)**; **bond twisting (microrotational gradient) = Cosserat couple-stress = mutual-inductance gradient `γ_c`** (`:104`). This is the dictionary that transfers the engine's ε/μ keying (§1 layer 2) onto the mechanical stiffnesses. ⚑ **Status:** the translational→ε / microrotational→μ split is verbatim Axiom 1 (INVARIANT-S2, canon); the *formal* transform node `def-tk1xfm` is **PROPOSED (PR #265), "identity-by-translation, NOT a derivation"** — carried at that ceiling, so §2's transfer is a **translation of the engine's keying, not an independent derivation of the mechanics** (the derivation content lives in the engine's per-component keying + the #770 `(I−d̂⊗d̂)du=0` algebra, both canon/derivation-grade).

**Step 1 — the extension coordinate rails `k_a` → `K` → `Z_bulk` → `Γ_bulk = −1`.** The corpus already carries the load-bearing chain, and it is A1-longitudinal, not T2-transverse:

> `master-equation.md:90` `[canon]`, verbatim: *"the **longitudinal-A1 bond compliance** `C_eff = C_0/S → ∞` (the stretch-reactance softening at yield). Per the Q1 = (B) sector split (INVARIANT-S2, Grant-ratified 2026-06-15) this `C_0/S` is a DISTINCT sector from the transverse permittivity — orthogonal reactances (A1 ⊥ T2), not one object; the LCR-measured transverse capacitance `C_diel = ε_eff·A/d ∝ S` rolls off, it does not diverge."*

So as the **axial swing** `A → A_yield`, the A1 stretch-compliance `C_eff = C_0/S → ∞` ⇒ the axial elastance `1/C_eff → 0` ⇒ the axial bond goes soft, `k_a → 0` ⇒ the bulk modulus `K ∝ k_a → 0` ⇒ `Z_bulk = √(ρK) → 0` ⇒ **`Γ_bulk = −1`** — the electron mass-cage (`master-equation.md:106` #260 banner `[canon]`: *"The electron mass-cage is the A1 longitudinal dilatation (`Z_bulk→0 ⇒ Γ_bulk=−1`)"*; `electron-bh-isomorphism.md:26` `[canon]`: *"bulk-channel TIR (`Z_bulk → 0 ⇒ Γ_bulk = −1` at the knot core)"*). This is the ε-kernel of §1 read in the **A1-longitudinal stretch-reactance** channel — see §5 for why that reading, not the T2-transverse-permittivity reading, is the correct one (the flag-don't-fix).

**Step 2 — the ε-kernel keys PER-COMPONENT, so a *uniform* A1 dilatation rails `k_a` ALONE (localized-core grade: ASSERTED — see the caveat below).** The engine's ε-kernel takes `E_component` and softens on that component's own `|E_component|·dx` (§1: applied per-Cartesian-component, `fdtd_3d.py:361`). Transferred to the bond frame: the **axial** component (`E ∥ d̂`) keys `k_a`; the **transverse** components (`E ⊥ d̂`) key `k_s`. A **uniform** A1 dilatation drives every bond into **pure axial extension** — `du_b = ε·d_b ∥ d̂_b`, the affine kinematics #770 used — so it excites **only the axial swing** and the per-component kernel softens **only `k_a`**. **★DERIVATION-STATUS CAVEAT (review R2, findings 1/4) — this is derivation-grade ONLY in the affine (uniform-dilatation) limit.** A knot core is a **localized** breather `u = f(r)r̂`, whose strain is `ε_rr = f′`, `ε_θθ = ε_φφ = f/r`, with a **deviatoric part `∝ (f′ − f/r)` that vanishes ONLY for `f ∝ r`** (the affine case, `k=0`). A localized mode (`k ≠ 0`) is *not* affine: its exterior/boundary field (`f ~ A/r²`, the pressurized-cavity form) has **zero dilatation** (`∇·u = 0`) and **pure deviatoric strain** — the bonds at the saturation shell see a transverse swing *of the same order as* the axial swing, exactly where the kernel is near yield. So a localized A1 breather **may rail `k_s` too**, via the transverse-swing channel a *uniform* dilatation leaves cold. Compounding this, the axiom-register line the note cites for support carries a **countervailing mechanism**: `axiom-register.md:193` `[SIGN-RULE-DERIVED]`, verbatim *"an **axial end-load** buckles the strut ⟹ **COMPRESSION** (`T<0` …) ⟹ `k_{shear\,eff}` **SHRINKS ⟹ UNCAPPED** track"* — i.e. the pre-stress remap `k_{shear,eff} = k_s + T/ℓ` **softens effective shear under axial compression**; the same line's Open flag (*"what physically PLUCKS the bond in matter"*) admits its applicability to a static-winding wall is unresolved. **So the `k_a`-alone grade is DERIVED for uniform dilatation but ASSERTED for the localized core.** The deciding computation — the **transverse-vs-axial swing amplitude at the core boundary** for the actual srs breather envelope, versus yield — is **OWED** (§4(a)) and is the single computation Fork-W now hinges on. (#770's *imposed*-rail ladder, which clamps `k_s` full by spatial shell membership, cannot evidence what a *driven* dilatation mode excites — §4(c) falsifier 2.)

**Step 3 — where `k_s` falls (the one place the clean story could leak).** `k_s` is the bond **transverse stiffness** — resistance to bond-perpendicular *translation* of the endpoints (the `(I−d̂⊗d̂)` projector, `constituent_cage_ensemble.py:88`). Per Axiom 1's DOF assignment (`translation-circuit.md:35`: **translational → E/ε**), `k_s` is a **translational (ε/force-class) stiffness**, keyed on the **transverse swing** — it is **NOT** the Cosserat microrotation (`γ_c`, the μ/inductive/circulation-keyed "twist" channel of `:104`). So:

- `k_s` and `k_a` are **both on the ε/extension side**, keyed on **orthogonal swing components** (transverse vs axial). Under a **uniform** A1 dilatation, `k_s` stays cold by **MODE-ORTHOGONALITY** (the affine dilatation excites no transverse swing), *not* by living on the inductive side — **but this is the affine limit only; for a localized breather `k_s`-coldness is ASSERTED pending the core-boundary strain-amplitude computation (§2 Step 2 caveat), because the localized mode's boundary strain is deviatoric-dominated**.
- The genuinely circulation-keyed "rotational side" (which *stiffens*, and is untouched by any pure translation of the endpoints) is the **Cosserat couple-stress `γ_c`** — the μ-kernel channel of §1 layer 2. It is a **distinct third channel**, not `k_s`.

**Step 4 — the shear response against the measured −0.236 (the FORM is derived; the VALUE −0.236 is IMPORTED).** Two facts fix the shear behavior — one derivation-grade FORM, one measured VALUE:

- **`K` is 100 % `k_a`-sourced** (derivation-grade, #770 `result §6`/`derivation §3 Step 2`): under uniform dilatation `(I−d̂⊗d̂)du = 0`, so `k_s` stores **exactly zero dilatation energy**; `K_eff ≤ K_affine ∝ k_a → 0` (relaxation only lowers it). ⇒ railing `k_a` alone gives a **PERFECT bulk short**, `Γ_bulk → −1` as `S_RAIL → 0` (the measured ladder `−0.512 → −0.9997`).
- **`G` is a COOPERATIVE `k_a`–`k_s` effect, NOT an additive `k_a`/`k_s` decomposition** (measured, #770 ladder): with `k_s` kept **full** (unrailed), railing `k_a` alone still drops the shear speed to `c_S = 0.177 = 62 % of cold` — a **partial shear FLOOR survives the rail precisely because `k_s` is unrailed**. But this floor is **not** a `61 %/39 %` sourcing split of `G`: the complementary ablation (`k_s → 0`, `k_a` full) gives `c_S ≈ 0` on the sub-isostatic `z=3` srs net (central-force-only shear is floppy), so `k_a` alone sources ≈0 % of `G`, and additivity fails badly (`c_S²(cold) ≈ 0.082` vs the survivor-sum `c_S²(k_a→0)+c_S²(k_s→0) ≈ 0.031`). The rail-away shear rigidity is the **cooperative `k_a`–`k_s` cross-term**, not a `k_a`-sourced fraction. What is real and reproduced is the **`k_a`-rail FLOOR RATIO** `c_S,railed/c_S,cold ≈ 0.62`; the earlier `(0.62)² ≈ 0.39` "held fraction" and "≈61 % borrowed" are ill-posed on a cooperative net — read them as *the floor ratio*, not a decomposition.

The measured wall shear reflection is the impedance mismatch of that floor:
`Γ_shear = (Z_railed − Z_cold)/(Z_railed + Z_cold) = (0.618 − 1)/(0.618 + 1) = −0.236`. **This is a VALUE-IMPORT, not a derivation or an independent match to #770.** The `Γ` used here is an **identity restatement of the one measured `c_S` ratio through the driver's own definition** (`constituent_cage_ensemble.py:608` `gamma(cr,cc)=(cr−cc)/(cr+cc)`, `Z=ρ·c`, `ρ` common) — the same Möbius map #770 already applied to the same Bloch speeds (`:618` `gamma_shear`). So `−0.236` and the "62 % of cold" are **one measured number related by a definition**; there is no world where recomputing #770's own `Γ_shear` fails to agree with #770. `−0.236` is therefore **self-consistent with** the banked floor, **NOT "derived" and NOT "matching #770 exactly."** What IS derivation-grade is only the **FORM**: *a partial shear floor exists (`Γ_shear` floors above `−1`) because `k_s` is unrailed* — `k_s` is cold, so the shear mirror is partial (`|Γ|² = 0.056` ⇒ ≈94 % power transmission), not a full short. **This is another instance of the framework's FORM-deriving / VALUE-importing signature** (cf. G, α, K=2G, Axiom-4's `arc*`): the FORM is forced; the VALUE `−0.236` is imported from the measured `c_S` ratio it claims to match. A genuinely fail-able version — an *a-priori* srs `k_a`-response of `G` from `RHO_STAR/K_S = 9.77337/1.0` compared to the measured floor — is **owed** (§4(a)); note it is ill-posed as a "decomposition" for the non-additivity reason above and must be framed as the *`k_a`-rail floor response*, not a sourcing fraction.

---

## §3 — One keying rule, two walls: electron (mode-specific) vs BH melt (ambient) `[derived + canon-read]`

The same per-component extension-keyed kernel (§2) produces **both** canonical walls, as its two drive limits:

| | **Electron wall** (`electron-bh-isomorphism.md:26`, `lattice-extreme-bh-rationality.md:36`) | **BH melt wall** (`electron-bh-isomorphism.md:36`, `lattice-extreme-bh-rationality.md:37`) |
|---|---|---|
| Drive | **MODE-SPECIFIC** — a localized A1 dilatation breather (one coordinate: axial swing) | **AMBIENT** — a smooth phase-transition strain; an isotropic scalar bias on every coordinate (`translation-circuit.md:115`, common-mode DC bias) |
| Coordinates railed | axial swing only ⇒ `k_a` only | all coordinates equally ⇒ `k_a`, `k_s`, whole tensor by a **common factor `S`** |
| Moduli | `K → 0` (100 % `k_a`); `G` drops to a **partial floor** (`c_S` → ≈62 % of cold — a cooperative `k_a`–`k_s` response, NOT a `k_a`/`k_s` split) | `K, G → 0` **together**; **ratio frozen** (degree-0 grade-lock, `electron-bh-isomorphism.md:38`/PR#521 `[canon]`: `ν, Zener, K/G` *"homogeneous degree-0 in the bond stiffnesses … unshifted by saturation magnitude"*) |
| Wall | `Γ_bulk = −1`, `Γ_shear = −0.236` (**channel-ASYMMETRIC**) | `Γ_shear = Γ_bulk = −1`, `Γ_EM = 0` (**channel-SYMMETRIC**) |
| Interior | topology preserved → **CONSTRUCTIVE** | topology melts → **DESTRUCTIVE** |

**The unifying statement:** the degree-0 grade-lock (`K/G` frozen, symmetric) is a theorem about a **scalar** saturation factor — it holds *iff* every bond stiffness is scaled by the **same** `S`. That is the **ambient** limit: a smooth common-mode strain biases all coordinates equally (`S` factors out of `K` and `G`, the ratio is degree-0). A **mode-specific** drive breaks the common-scaling premise: the A1 breather rails one coordinate, so `S` does **not** factor out — `K/G` is **not** frozen, it drops (`K → 0`, `G` to a partial floor). The channel-asymmetric electron wall is therefore not a violation of the grade-lock; it is the grade-lock's premise (scalar `S`) failing because the drive is a vector, not a scalar.

**Reconciliation with #767 Leg 0 (the scalar-A grade-lock, MEDIUM-class — scoped, not contradicted).** #767 (`research/2026-07-20_envelope-sector-reduction_result.md:101` `[canon-read]`) banks Leg 0 as: *"compression grades WITH shear (`Γ_shear=Γ_bulk=−1`); `c_P/c_S` ratio FROZEN (degree-0)"* — and reads it as foreclosing **MEDIUM-class** channel asymmetry (`:12` (ii): *"`K=2G` grade-locking forbids a MEDIUM-class channel asymmetry … every MEDIUM-class candidate suppression is channel-symmetric"*). But #767 **explicitly scopes** this to the smooth/ambient medium and names the constituent knot-core wall as the un-foreclosed exception (`:13`, `:106` `[canon-read]`, verbatim: *"the CONSTITUENT bulk-only wall (`:26`) is the open exception … 'BIN 2 unreachable' is CONDITIONAL on constituent-wall Γ-symmetry"*). This note supplies **why** the exception exists and is not a contradiction: #767 Leg 0 is the **ambient (scalar-`S`) limit** of the keying rule; the electron knot-core wall is the **mode-specific (vector-drive) limit** of the *same* rule. Both are the one per-component extension-keyed kernel; they differ only in whether the drive is a scalar bias (all coordinates) or an A1 mode (one coordinate). #767 Leg 0 stands, MEDIUM-class and correct in its scope; this note fills the mode-specific limit it left open.

---

## §4 — Consequences, the ratification sentence, and falsifiers `[routing]`

**(a) The #770 deep-rail lane upgrades from surrogate to derived-model — IF this note stands review + Grant ratifies.** #770 proved the `bulk_only` rail (`k_a → S_RAIL·k_a`, `k_s` full) *constitutively realizes* the canon wall (`Γ_bulk → −1`, `c_S` finite), but flagged it a **surrogate** (`result §3`: *"a surrogate `k_a`-only rail … not a derived saturation mode"*). This note derives the surrogate **for a uniform dilatation** (affine limit): the driver's imposed "`k_a` only" is the behavior the engine's per-component extension-keyed kernel produces when the drive is a pure *affine* A1 breather — **but for the LOCALIZED core the `k_a`-alone grade is ASSERTED pending the boundary-strain computation (§2 Step 2 caveat)**, so this upgrade is conditional on that computation, not just on Grant ratification. The measured `Γ_shear = −0.236` is the impedance signature of that partial `G` floor (§2 Step 4). **`−0.236` is a VALUE-IMPORT** — a self-consistent restatement of the measured `c_S` ratio through the driver's own `Γ` definition (`constituent_cage_ensemble.py:608/:618`), NOT an independently-derived match to #770; only the FORM (*a partial shear floor because `k_s` is unrailed*) is derivation-grade. The deep-rail frozen-first program (`#770 result §8.2` owed follow-on) can read its `k_a`-only wall as a **model rather than a hand-set surrogate**, and — to convert `−0.236` from a self-consistent restatement into a *fail-able* prediction — should compute the **`k_a`-rail floor response of `G`** *a priori* from the srs geometry + `RHO_STAR/K_S = 9.77337/1.0`. (This is NOT a "decompose `G` into `k_a`/`k_s` fractions" diagnostic: `G` is non-additive on the sub-isostatic `z=3` net — the complementary `k_s → 0` ablation gives `G ≈ 0` — so a sourcing decomposition is ill-posed; the well-posed quantity is the floor ratio under the `k_a` rail.)

**(b) The GRANT-RATIFICATION ITEM (SPLIT by the review into a ratifiable part and a held part).** The mechanical-sector keying is an axiom-level commitment. Framed for ratify, with the review's re-scoping explicit:

> **[RATIFIABLE — value-free keying commitment.]** Axiom 4's saturation argument `A` is the **per-deformation-coordinate SWING (extension) amplitude** — the kernel grades each stiffness component on its own swing coordinate (axial swing keys `k_a`; transverse swing keys `k_s`), and the Cosserat couple-stiffness is keyed on the orthogonal **circulation** coordinate (the relativistic-inductor `1/S` dual) — so saturation is **MODE-RESOLVED**, not a single scalar over all channels.
>
> **[NOT-YET-RATIFIABLE — CONDITIONAL on the owed boundary-strain computation.]** The consequence *"a pure A1 dilatation breather therefore rails `k_a` ALONE (`K→0`, `Γ_bulk=−1`), the shear channel surviving as a partial floor"* holds **only if** the *localized* breather's core-boundary deviatoric strain (`ε_rr = f′`, `ε_θθ = f/r`, deviatoric `∝ (f′ − f/r)`, non-zero for any localized `f ≠ r`) stays **sub-yield**, so that `k_s` is not softened by the pre-stress remap (`axiom-register.md:193`, `k_{shear,eff} = k_s + T/ℓ`). Mode-orthogonality is derived only for **UNIFORM** dilatation (`f ∝ r`), not for the localized core — so this clause **must NOT be ratified until the core-boundary strain-amplitude computation (§4(a)) lands.**

The **value-free** part is a **content** commitment (which coordinate the kernel argument is), not a count change: Axiom 4 stays SHAPE-DERIVED (conditional), count 4 — the residual yield-anchor `arc*` is untouched. It sharpens the LOAD-RESPONSE SIGN RULE (`axiom-register.md:193`) from "axial-vs-transverse load sets the sign" to "the kernel argument itself is the per-coordinate swing." The **held** part — the localized-core `k_a`-alone grade — the review demoted to ASSERTED (mode-orthogonality is affine-only); it carries **no** ratification weight until the boundary-strain follow-on resolves whether the localized breather's transverse boundary swing reaches yield.

**(c) Falsifiers (what would kill the keying picture).**
1. **A non-extension mechanical keying site.** An engine or axiom site that keys mechanical saturation on a coordinate that is **not** a per-component swing/extension amplitude — e.g. a *scalar field-energy density* or a *global strain invariant* applied uniformly to all stiffness components — would refute the per-coordinate reading and re-collapse the electron wall into the symmetric grade-lock. (Checked: `scale_invariant.mu_eff:198` keys on `|B|` **per-component amplitude** and softens — consistent, per-component, NOT a scalar-over-all-channels; the Route-C μ-kernel keys on per-component `curl_h`. No scalar-over-all-channels mechanical site found at `d3203d37`.)
2. **A measured wall shear-collapse inconsistent with the partial-floor account.** If a deep-rail driver that grades **only the axial swing coordinate** produced `Γ_shear → −1` (full shear collapse), that would refute the partial-floor-only account — it would mean the axial drive *is* railing `k_s`, i.e. an A1 breather excites transverse bond swing (which, per §2 Step 2, a *localized* breather's deviatoric boundary strain plausibly does). The prediction is a **`Γ_shear` floor above `−1`** (the partial shear floor), saturating as `S_RAIL → 0`. **⚠ CONSISTENCY, NOT A TEST:** the #770 ladder (`−0.219 → −0.236 → −0.236`, saturated) is consistent with the floor but does **not** probe it — that driver clamps `k_s` **full by spatial shell membership** (`constituent_cage_ensemble.py:164`, *"compression component grades; shear kept full"*), so it *cannot* exhibit `k_s` railing under **any** mechanism; the saturation is guaranteed by the clamp, not by a driven mode. A firing test needs a driver that grades a **localized A1 breather** and lets the core-boundary strain modulate `k_s` freely via the pre-stress remap (#770 §8.2's owed deep-rail frozen-first program), not the `k_s`-clamped surrogate.
3. **A μ-site keying the rotational/twist stiffness on the EXTENSION coordinate.** If the Cosserat couple-stiffness (`γ_c`) were found keyed on the axial-extension coordinate and *softening* with `k_a`, the orthogonal-coordinate split would fail. The Route-C μ-kernel keying on **circulation** (and *stiffening*) is the standing evidence it does not (`fdtd_3d.py:329-343`).

---

## §5 — Verdict: SUPPORTS the walk (two refinements + one flag) `[adjudication]`

**The derivation SUPPORTS the walked candidate's direction and headline** — *the corpus's sector kernels key on different coordinates with opposite signs; the swing/extension coordinate rails `k_a`, and the circulation coordinate keys the rotational side, so a swing-railed A1 core presents `Γ_bulk → −1` with the rotational/shear channel alive.* The engine's implemented keying (§1 layer 2) is exactly this, and the TKI bond map (§2) transfers it to the mechanical `k_a`-alone grade Fork-W asked for. **Two refinements and one flag ride the support:**

**Refinement 1 — `k_s` is RECLASSIFIED (do not conflate it with the inductive twist channel).** The walk's "bond twisting = couple-stress/inductive = the rotational side stays alive" is correct **for the Cosserat microrotation `γ_c`** (a genuine third, circulation-keyed, stiffening channel) — but `k_s` (the rank-2 bond **transverse translational** stiffness) is **NOT** that channel. `k_s` is a translational (ε/extension-class) stiffness on the **same side as `k_a`**, kept cold under a *uniform* A1 breather by **mode-orthogonality** (the affine dilatation excites no transverse swing), not by ε-vs-μ membership — **though for the localized core `k_s`-coldness is ASSERTED, not derived** (the localized breather's boundary strain is deviatoric-dominated; §2 Step 2 caveat). The clean two-way "`k_a` capacitive / `k_s` inductive" split is *wrong*; the correct picture is **three channels** (axial-swing `k_a`, transverse-swing `k_s`, circulation `γ_c`), and the A1 mode selects the axial one *in the affine limit*.

**Refinement 2 — "shear stays alive" is QUANTITATIVELY PARTIAL (`≈94 %`, not `100 %`); the −0.236 is VALUE-IMPORTED, not derived.** Railing the axial swing drops `c_S` to ≈`62 %` of cold — a partial shear **floor** that survives *because `k_s` is unrailed* (a cooperative `k_a`–`k_s` response, **NOT** a `61 %/39 %` sourcing split: the complementary `k_s → 0` ablation gives `G ≈ 0` on the sub-isostatic `z=3` net). The wall is a **perfect bulk short but a partial shear mirror** (`Γ_shear = −0.236`, ≈94 % transmission). But `−0.236` is a **self-consistent restatement of that one measured `c_S` ratio through the driver's own `Γ` definition** — **NOT "derived," and NOT an independent "match to #770"** (recomputing #770's own `Γ_shear` from #770's own speeds cannot fail to agree with it). The derivation-grade content is the **FORM** (a shear floor exists because `k_s` is unrailed); the VALUE `−0.236` is imported — the framework's **FORM-deriving / VALUE-importing signature** (cf. G, α, K=2G, `arc*`).

**★FLAG-DON'T-FIX — the walk's "ε-side softens → `Z→0`, the short" needs the A1⊥T2 disambiguation (routed to Grant; NOT silently fixed).** Read naively against `translation-circuit.md:119` `[canon]` — *"`ε_eff → 0` (electric branch): `Z → ∞`, **open-circuit**, `Γ → +1`; the `Γ=−1` short is the **magnetic** branch (`μ_eff → 0`)"* — the walk's "`ε→0 = Z→0 = short`" looks **inverted** (for the transverse EM impedance `Z_EM = √(μ/ε)`, `ε → 0` is an OPEN, not a short). The disambiguation is the corpus's own **A1 ⊥ T2 sector split** (`master-equation.md:90` `[canon]`, Q1=(B) Grant-ratified 2026-06-15): the electron short is the **A1-longitudinal stretch-reactance** `C_eff = C_0/S → ∞` (`k_a` soft ⇒ `Z_bulk = √(ρK) → 0`), a **different impedance** from the **T2-transverse permittivity** `C_diel ∝ S → 0` (`Z_EM → ∞`, open). The two must not be conflated:
- **Correct reading of the walk (A1-longitudinal):** the extension-swing softens the A1 stretch-compliance `C_eff = C_0/S → ∞` ⇒ `k_a → 0` ⇒ **`Z_bulk → 0` ⇒ `Γ_bulk = −1` (short)**. ✓ This is the electron mass-cage (`master-equation.md:106` #260 banner).
- **Wrong reading (T2-transverse):** the transverse permittivity `ε_eff = ε·S → 0` ⇒ `Z_EM → ∞` (open, `Γ → +1`, the dielectric `τ_yield` branch). ✗ Not the wall.

The walk's "ε-side" must be read as the **A1-longitudinal stretch-reactance**, not the T2-transverse permittivity. This is not a contradiction in the corpus — the A1⊥T2 split is exactly the tool that resolves it — but the walk's loose "ε-side" wording could mislead, so it is **surfaced with verbatim cites for Grant to ratify the reading**, per flag-don't-fix. (Note also that `translation-circuit.md:119` assigns the `Γ=−1` wall to the *magnetic* branch via `clm-lv3uw1`; `master-equation.md:106` #260 re-scopes that magnetic-vs-electric fork to a **chirality/spin SIGN-selector, DEGENERATE on the mass observables, NOT the cage mechanism** — the cage is the A1 bulk channel. So the current-canon mass-cage is A1-bulk, consistent with this note; the `:119` magnetic-branch reading is the sign-selector, a separate axis.)

---

> **Note provenance.** Direction Grant-ratified 2026-07-20 (`[sic]` *"yup that sounds promising!"* — DIRECTION only). The keying formulation is ORCHESTRATOR WALK-LEVEL; this note tested it. All engine lines (`fdtd_3d.py:329-343/:516-519`, `fdtd_3d_jax.py:76-78/:110-113`, `scale_invariant.py:164/:198`, `constituent_cage_ensemble.py:87-93/:125-167`) and canon cites (`axiom-register.md:186/:189/:193`, `master-equation.md:20/:90/:106`, `electron-bh-isomorphism.md:26/:36/:38`, `lattice-extreme-bh-rationality.md:36/:37`, `translation-circuit.md:35/:104/:115/:119/:207/:214`, `envelope-sector-reduction_result.md:12/:13/:101/:106`) verified two-method at base HEAD `d3203d37`. **Verdict: SUPPORTS the walked direction with two named refinements (k_s reclassification; −0.236 = derived `k_a`-in-`G` bleed) + one flag-don't-fix (the A1⊥T2 "ε-side" disambiguation).** Canonizes nothing; mints no `clm-`; engine byte-untouched; no KB/tex leaf edited. Companions: merged #770 (`_result.md §3/§7`, `_derivation.md §2`), merged #767 (`_result.md §3.1`), the docket continuation (`### ENTRY 2026-07-20-forkw-kernel-keying`). Routed for Grant ratification of the §4(b) sentence.
