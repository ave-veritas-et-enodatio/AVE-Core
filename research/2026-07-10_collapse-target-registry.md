# Collapse-Target Registry — corpus-wide sweep (task #33, X35 main body)

**Date:** 2026-07-10 · **Branch:** `analysis/collapse-target-registry` · **PR:** [DO-NOT-MERGE][REVIEW: pending-orchestrator]

> **Ledger status update (2026-07-14).** The "0-for-7 hopeful-interior-mechanism ledger" cited throughout this
> 2026-07-10 sweep doc was **booked to 0-for-9** by the 2026-07-13 D3 "classify and book" ruling (increment 8 = X43
> ringdown-port, frozen-form miss; increment 9 = C13b Bullet-Cluster, forward-prediction miss) — canonical at
> [`program-arc-map.md:404`](../manuscript/ave-kb/common/program-arc-map.md). The inline "0-for-7" figures below are
> the sweep-date snapshot; each is pointed forward to the current **0-for-9** value (KEEP-BOTH). The RHYME-tier
> seduction-guard logic is unchanged — the ledger only grew, so a convergence this pretty is *more* to be distrusted.

**CLASS: candidate-generation registry. NOT physics claims, NOT executed collapses.** This doc is the ranked
output of the corpus-wide collapse-target sweep that follows the X35 operator-typing opener
([`2026-07-10_operator-typing-pass_result.md`](2026-07-10_operator-typing-pass_result.md)). It hunts three
validated patterns from the breakthrough methods note
([`_orchestration/2026-07-09_breakthrough-patterns-methods-note.md`](../_orchestration/2026-07-09_breakthrough-patterns-methods-note.md)):

- **P1 — identity collapse (MERGE):** two separately-maintained corpus structures that are ONE bench object seen
  from two angles. The collapse is compression that then *generates* predictions.
- **P6 — homonym split (SPLIT):** one word / glyph carrying two distinct objects — the corpus's biggest historical
  confusion source. Resolution shape = ownership table / def-node.
- **P8 — named-object archaeology:** a canonical name / constant whose standard-physics namesake carries structure
  not yet cashed in.

**Method.** Seven read-only sweep lanes (KB common registers; vol1+2; vol3+6; vol4+9; vol5+claim-registers;
recent `research/`; a dedicated P8-by-object lane), each in the pinned worktree at `origin/main 7fedf5c3`, each
required to (a) name the **shared bench mechanism** — not a resemblance — for every **P1/P8** candidate (a clean **P6
SPLIT** legitimately has *no* shared mechanism: two unrelated objects sharing one word is exactly its polarity), (b)
verify every load-bearing receipt with **two independent grep methods** (LaTeX-aware, per `feedback_grep_completeness`),
(c) pre-name a **kill-test** — fireable for P1/P8; for a *pure-terminology* P6 the kill-test may legitimately be n/a
(the sole such case is T16, flagged in situ), (d) estimate cost, and (e) run a seduction self-check against the **0-for-7 hopeful-
interior-mechanism ledger** (the sweep-date value; **now booked 0-for-9** per the 2026-07-13 D3 ruling, [`program-arc-map.md:404`](../manuscript/ave-kb/common/program-arc-map.md)). The
orchestrator then deduplicated across lanes, re-verified the headline + the two concrete numeric/arithmetic
catches personally, and applied a **romance-knife pre-filter**: any candidate whose "collapse" is a rhyme rather
than a shared mechanism is demoted to the RHYME tier with its kill-test, not ranked among the fire-ready targets.

**This registry does NOT execute any collapse.** The core planning session adjudicates which targets fire, in what
order, and which need a Grant physics ruling first. Several targets are explicitly `flag-don't-fix` — they surface
a contradiction or a keying decision that is Grant's to make, not the sweep's.

**Ranking axis.** `(clarification value × confidence × fire-readiness) ÷ cost`, with forward-prediction unlocks
weighted up (the program's measured edge is the bench/beam, not interior mechanism-hunting) and rhymes weighted to
zero. Cost key: **S** = one register row / def-node mint / single-leaf annotation · **M** = multi-leaf KEEP-BOTH
propagation · **L** = new derivation arc or engine work.

---

## Tier summary (ranked)

| # | Target | Pattern | Action | Cost | Fire-readiness |
|---|---|---|---|---|---|
| **T1** | Quantization homonym — a TRIAD (mode-count ⊥ winding ⊥ Nyquist-sampling) | P6 | SPLIT + def-node | S | **fire-ready** (mint staged by the corpus itself) |
| **T2** | The "empty slew catalog" is un-cross-linked, not empty: hysteresis §1 IS the rate-keyed family | P1 | MERGE / cross-link | M | fire-ready; "slew" *label* A4-gated |
| **T3** | Γ = −1 homonym — lossless CONFINEMENT vs dissipative RUPTURE (a NEW axis; KEEP-BOTH with cq:1612) | P6 | SPLIT + ownership table (branch (ii)); language-retraction (branch (i)) | M | needs Grant loss-character ruling |
| **T4** | MOND `a₀` namesake carries the External-Field Effect — absent from the whole KB | P8 | forward-prediction opener | M | needs Grant keying decision (internal vs total field) |
| **T5** | The `θ` power-factor axis and the EM loss-tangent `tan δ` are one phasor (`tan δ = cot θ`) — kills the "all-orthogonal" over-claim; the `δ_AVE` duty axis stays distinct | P1 | de-orthogonalize (θ↔tan δ only) | S | fire-ready (narrowed) |
| **T6** | boundary-observable `M`: mass→inductance `L` collides with the ratified `X_L` = spin ruling | P6 | SPLIT / sector disambiguation | S–M | flag-don't-fix (latent contradiction) |
| **T7** | "Cauchy relation" mislabel on `K=2G` — the genuine Cauchy relation forces `ν=1/4`, not `2/7` | P8/P6 | SPLIT / name-correction | S | fire-ready; reinforces a standing negative |
| **T8** | Four-bin content taxonomy is 3/4 a relabel of `lattice-model-register` Axis-A | P1 | MERGE-into-register (deflation) | M | fire-ready for 3 bins; bin-3 gated on X41 |
| **T9** | Complex Poynting `Im(S)=2ω(⟨W_m⟩−⟨W_e⟩)` = undeployed formal home for the off-line / "no-Re(Z)" register | P8 | derivation surface | M | seduction-adjacent; first output could be a kill |
| **T10** | `Q`-glyph carries ≥4 electron-scale objects with no central register | P6 | SPLIT / ownership row | S | fire-ready (janitorial) |
| **T11** | A4 fork to carry + `I_max` quoted at **124.4 A vs 116 A** under one identity | fork + cite-catch | fork-record + reconcile | S | the I_max catch is fire-ready now |
| **T12** | `u₀*` glyph — SYM operating-point vs T-breaking bias (deflates the §b triple-convergence) | P6 | SPLIT / def-node | S | fire-ready as a *deflation* |
| **T13** | vol5 protein-folding subtree + divergence-map B4 STALE against the N13 impedance-folding NEGATIVE | honesty-lag | reconcile (cross-repo) | M | needs the AVE-Protein receipt + Grant scope ruling |
| **T14** | "strain" the word — `δ_strain` (α-echo residual) vs `ε₁₁` (saturation amplitude), both dimensionless | P6 | SPLIT / register row | S | fire-ready (narrow: one un-guarded collision) |
| **T15** | `S₁₁` INVARIANT-N4: declared a homonym; substrate suggests ONE reflection object | P6→P1 | adjudicate (touches an invariant) | S | needs Grant + the out-of-repo `eq:s11_energy` |
| **T16** | "register" now carries ≥4 senses; ledger-index ⊥ impedance-content-bin | P6 | SPLIT / hygiene note | S | fire-ready (low value) |
| **T17** | Ferrite / Polder gyrotropic-μ-tensor = undeployed mechanism for the circulator's non-reciprocity | P8 | inventory (gated) | M | INVENTORY-ONLY; the transfer IS the PENDING-GRANT fork |
| **T18** | Kron's Schrödinger-network + rotating-machine branch — only the Maxwell-1944 branch is deployed | P8 | prior-art home | M–L | gated (linear-vs-saturating scope cap) |
| **T19** | Conjugate-impedance-match term fragmented across two vol5 translation tables | P6 | drift reconcile | S | fire-ready (low value) |

**RHYME tier (seduction-flagged — do NOT fire without the kill-test passing; cited against the 0-for-7 ledger — now 0-for-9, `program-arc-map.md:404`):**
R1 Manley–Rowe power-partition · R2 `Q`-namesake radiative linewidth (gated on T9) · R3 AC-Josephson `f=2eV/h`
DC→AC discriminator · R4 Kolmogorov row "slew-mis-shelf" · R5 the §b `u₀*` triple-convergence *as a convergence* · R6 the multi-stage cascade-filter ontology (🔴 KILLED 2026-07-13, PR #668) · R7 PFC / power-factor ordering (🔴 KILLED with R6).

**Anti-target caution (a collapse that must NOT fire):** band-curvature effective mass `m*=ℏ²(d²E/dk²)⁻¹` — see
the ANTI-TARGET CAUTION section; minting it would validate an A1⊥T2 cross-wire the corpus has already deleted.

---

## T1 — Quantization homonym is a TRIAD (mode-count ⊥ winding ⊥ Nyquist-sampling)

**Pattern:** P6 homonym SPLIT + def-node mint. **Sector:** A1-dilatation embedding/cavity (mode-count) ⊥ T2/Cosserat
micro-rotation winding ⊥ Ax1 lattice-sampling floor (Nyquist); cold-linear (Regime I) for the atomic modes.

**The three objects under one word "quantized" (each with a distinct failure mode — the operational proof they are
different objects):**

1. **Mode-count integers** — cavity-deformable; embedding register. The atomic `n, l, m`. The vol2 leaf literally
   writes them as "winding" — the smoking-gun overload:
   - `manuscript/ave-kb/vol2/quantum-orbitals/ch07-quantum-mechanics/de-broglie-standing-wave.md:181` "**(e) Standing
     wave condition (mode number $n$).**", `:223` a table column header labels the mode-counts "**Winding #**", `:236`
     "current ring soliton … with **winding number $n$**." (grep + rg agree, verbatim.)
   - Failure mode: **ionization destroys them** ("ionization kills the mode, not the knot",
     [`2026-07-10_impedance-register-walks_framing.md:62`](2026-07-10_impedance-register-walks_framing.md)).
2. **Winding integers** — topologically protected; graph register. Charge, the `(2,3)`, `Link(∂Ω,F)∈ℤ`.
   - `manuscript/ave-kb/vol2/particle-physics/ch01-topological-matter/hollow-vortex-binding.md:96` "the winding
     integer $n$ (the $(2,3)$: $w_{tor}=2$, $|\text{Link}|=1$)"; def-node `def-3638f2` at
     `manuscript/ave-kb/common/vocabulary-register.md:226`.
   - Failure mode: **invariant under any smooth deformation** (survives ionization).
3. **Nyquist / lattice-sampling quantization** — substrate resolution floor keyed on `ℓ_node`; **NOT in the framing
   note's pair, and load-bearing in the α keystone.**
   - `manuscript/ave-kb/vol1/ch8-alpha-golden-torus.md:94` "Ax 1 Nyquist quantizes the minimum substrate-lattice-
     resolvable tube diameter at $d = 1\,\ell_{node}$"; used as the forced `Λ_line = π·d = π` step in the
     `α⁻¹ = 4π³+π²+π` decomposition at `manuscript/ave-kb/vol4/circuit-theory/ch1-vacuum-circuit-analysis/op21-multi-mode-mode-counting.md:130,169`.
   - Failure mode: **moves iff `ℓ_node` (= c/ω₀) changes** — invariant under both cavity-wall deformation and smooth
     deformation. A third, distinct invariance group.

**Candidate SPLIT — the shared bench object:** all three are integer eigenvalue-labels *of the same bound electron*,
read by three different machineries (Op6 phase-closure of the graded-impedance cavity / boundary linking integer of
the Cosserat sector / lattice Nyquist floor). Three distinct invariance groups = three objects; the corpus's own
"different failure modes" criterion is the discriminator, not a resemblance.

**What it clarifies:** removes the corpus's self-named "biggest historical confusion source." Concretely, it stops a
reader parsing "current ring soliton with winding number $n$" (`:236`) as topologically protected (it is cavity-
deformable), and stops the α-keystone's `Nyquist-quantized d=1` from being mis-read as a protected FORM (it is a
Class-B sampling floor) — which would mis-rate the keystone's solidity. The freshest exemplar
([`impedance-register-walks_framing.md:55-68`](2026-07-10_impedance-register-walks_framing.md)) already staged the
*pair* for the vocabulary register ("not minted here"); this target supplies the vol2 receipt AND the missed **third**
sense.

**Site-count clarification value (two methods, and they diverge — the documented grep-false-negative failure mode):**
`grep -rin 'quantiz'` → 386 research + 113 KB lines; `rg -in '\bquanti[sz]'` → 489 combined. Of ~489, ~413 carry a
same-line disambiguator, ~76 are bare "quantized/quantization" — the ambiguous-load subset the def-node resolves.
Collocation: charge/topological/flux ≈130 (leg 2); mode/energy/BC ≈40 (leg 1); Nyquist ≈16 (leg 3).

**Kill-test:** the mode↔winding boundary is *already* killed-and-banked (#417 detuning-knob + #626 tethered-pivot: the
dynamical mode tracks the knob, the `(2,3)` winding does not). To refute the *third* leg's distinctness: exhibit a case
where the Nyquist floor `d=1 ℓ_node` co-varies with cavity-wall deformation (collapses 3→1) or is topologically
protected (collapses 3→2). Neither is in-corpus.

**Cost:** **S** — one def-node triad in `vocabulary-register.md`, cross-linked to `def-3638f2` (winding) + `def-kn0t01`
(phase-vs-real knot); optionally a one-leaf relabel of `de-broglie-standing-wave.md:181/223/236` (mode-count, not
"winding"). **Confidence:** high. **Seduction check:** low — the split's legs have materially different, corpus-
operationalized failure modes; the only risk is over-splitting, guarded by leg-3 being load-bearing in a *specific*
derivation.

**Lane provenance:** vol1+2 C1 (mode/winding receipts) ⊕ research C1 (the third Nyquist sense).

---

## T2 — The "empty slew catalog" is un-cross-linked, not empty (hysteresis §1 is the rate-keyed family)

**Pattern:** P1 identity MERGE (with a P8 flavor — the "relativistic inductor / Lenz back-EMF" namesake already carries
the rate-cap structure). **Sector:** μ / T2-inductive (`X_L`); near-yield / saturation-crossing (Level-2 memristive) —
a sub-yield reading is a wrong-regime null.

**Two corpus structures, one bench object:**

- **The kernel-catalog RATING-TYPE section calls the slew family un-catalogued/"empty."**
  `manuscript/ave-kb/common/universal-saturation-kernel-catalog.md:151-152` "The one likely slew instance ('relativistic
  mass') currently hides among the swing rows; the slew-instance catalog is otherwise **empty**"; X35 result doc
  [`2026-07-10_operator-typing-pass_result.md:157`](2026-07-10_operator-typing-pass_result.md) "the slew catalog is
  *empty*" (G2). *(Orchestrator re-verified: the catalog author already knows the relativistic inductor is the slew
  instance — so the accurate finding is narrower than "empty is false"; see below.)*
- **`substrate-hysteresis-index.md §1` has already assembled a SIX-member rate-keyed family on the same object.**
  `manuscript/ave-kb/common/substrate-hysteresis-index.md:47` "any time the dynamics are governed by a **rate**
  ($d\omega/dt$, $dI/dt$) rather than an instantaneous amplitude"; `:53` `$L_{eff}(I)=L_0/\sqrt{1-(I/I_{max})^2}\to\infty$
  at $I_{max}$ — … physically **collapses the slew rate** $dI/dt\to0$ … the magnetic-sector projection of the same
  Axiom-4 kernel" (`clm-p5cf3t`). Members: relativistic inductor (`clm-p5cf3t`), dark-wake BEMF (`clm-exjfai`),
  Newtonian-inertia-as-Lenz (`clm-jwyy6l`), Op14 local-clock (`clm-1eg13f`), Op14 cross-sector-trading reactance-pair
  (`clm-p2tp9i`), geodynamo (`clm-wd5rs0`).
- **The two registers do not cross-link *as the slew catalog*.** `grep -rniE "hysteresis|lenz|bemf|memristiv"` in the
  kernel catalog and `operators.md` → **zero hits** (orchestrator-verified). `clm-p5cf3t` IS co-cited by both registers
  and the hysteresis index — the *claim* is shared, but neither the kernel-catalog RATING-TYPE block nor the X35 G2
  "empty" line points at §1 as the already-built rate-keyed catalog.

**Candidate MERGE — the shared bench mechanism (precisely scoped):** one diverging reactance `L_eff(I)=L₀/√(1−(I/I_max)²)`
at its `I_max` cap. Hitting the cap drives `dI/dt→0` — *that one event* is **both** "the slew-rate rating is reached"
(X35 slew-μ cell) **and** "the configuration latches/freezes/remembers" (hysteresis §1 Lenz-freeze). X35/G2's job —
"build the empty slew catalog / find the rate-keyed siblings of the 26 swing rows" — is *substantially the job §1 already
did*. The honest finding is therefore **not** "'empty' is false" (the catalog already flags the relativistic inductor
as the one known slew instance); it is that **§1 has assembled the rate-keyed FAMILY the RATING-TYPE section still calls
un-catalogued, with no cross-link** — a MERGE that populates the swing/slew amplifier's "second column" for free.

**What it clarifies:** (1) reconciles "un-catalogued/empty" (kernel-catalog + G2) with "already a 5-entry index" (§1);
(2) identifies the memory/hysteresis/LOOP-GAP program as the **slew-side** of the swing/slew amplifier the corpus already
calls "the two ratings of one amplifier" ([`lattice-model-register.md`](../manuscript/ave-kb/common/lattice-model-register.md));
(3) puts the open **A4 keying question** (below) in one place instead of two.

**Kill-test (bounds the MERGE):** X35's **A4 is OPEN** — `L_eff(I)` keys on `I = ξ_topo·v` (a state *amplitude*), while
the ratified Axis-2 defines slew as keyed on the *rate/conjugate* (`operators.md:137`). If A4 resolves that `L_eff(I)`
is amplitude-keyed (= **swing-μ**), then §1 is the μ-sector dynamic-impedance/memory family, **not literally** "the slew
catalog," and the MERGE weakens to "same object, wrong column." Either way the missing cross-link and the "un-catalogued"
tension stand; only the *label* "slew" is A4-contingent. Second kill: any §1 member shown to be pure amplitude-triggered
with no rate-blocking (e.g. test geodynamo `clm-wd5rs0`) exits the family.

**Cost:** **M** — cross-reference rows in `substrate-hysteresis-index.md §1` ↔ `universal-saturation-kernel-catalog.md`
RATING-TYPE ↔ `operators.md §3.5`, plus a reconciliation line on the X35 result-doc G2 (KEEP-BOTH). Building the *full*
rate-keyed sibling of all 26 swing rows is a separate **L**. **Confidence:** high on the un-cross-linked tension; medium
on the "slew" label (A4-gated). **Seduction check:** low — this compresses *existing derived canon* (all `clm-`-backed),
not a new interior mechanism; the risk is over-stating the label ahead of A4, flagged not asserted.

**Lane provenance:** KB-common C1 (orchestrator-re-verified + scope-corrected).

---

## T3 — Γ = −1 homonym: lossless CONFINEMENT vs dissipative RUPTURE (a NEW axis, KEEP-BOTH)

**Pattern:** P6 homonym SPLIT + ownership table. **Sector:** T2 self-trap / A1 mass-cage (Object A) vs at/above-yield
breakdown (Object B); the split axis is **loss-character** (lossless-reactive vs dissipative), *distinct* from the two
Γ overloads already flagged at `vol4/claim-quality.md:1612` — this is KEEP-BOTH, a new axis alongside the existing one.

**One glyph "Γ = −1", two loss-opposite objects:**

- **Object A — the lossless reactive confinement short** (cold side of yield; `Z_core→0`, `Γ→−1`; `Q_intrinsic→∞`):
  `manuscript/ave-kb/vol4/circuit-theory/ch1-vacuum-circuit-analysis/cvr-reflection-smith.md:30` "$Z_{core}\to0$,
  $\Gamma\to-1$ — **the short-circuit TIR wall**"; `theorem-3-1-q-factor.md:147` "the **intrinsic** electron mode is
  **lossless** … intrinsic $Q\to\infty$."
- **Object B — the dissipative breakdown rupture** (at/above yield; energy *released*):
  `manuscript/ave-kb/vol4/falsification/ch11-experimental-bench-falsification/vacuum-impedance-mirror.md:127` "the
  inductive capacity … physically shatters ($\Gamma=-1$) … absolute dielectric breakdown, completely dropping its
  topological grip … causes … the Muon … to decay (the 'Leaky Cavity' mechanism)"; `project-torsion-05.md:19` "absolute
  impedance rupture ($\Gamma=-1$), producing exactly $0.0\,\text{mN}$ of backward reaction force"; `project-zener-04.md:14`;
  `metric-levitation-limit.md:16`.
- **The corpus draws the loss boundary but does not tag the glyph with it:** `vol4/claim-quality.md:1168` "linear below
  rupture, **dissipative at and above rupture**"; `common/substrate-hysteresis-index.md:82,84` (sub-yield lossless-reactive
  vs above-yield genuinely-dissipative Level-2 memristive). And `vol4/claim-quality.md:1612` already flags **two other**
  Γ's on a *different* axis (`Γ_spinor` stability wall vs `|Γ_EM|²=1−α` radiative-leak) — "**pending human physics
  ruling**" (orchestrator-verified: genuinely a different axis, so this target is additive KEEP-BOTH, not a duplicate).

**Candidate SPLIT — the shared mechanism + the tension:** both objects are the *same Axiom-4 saturation event at V_yield*
driving `Z_core→0 ⇒ Γ→−1` — but A is the reactive standing self-trap (lossless, `Q→∞`, energy retained) and B is the
driven transient past yield (dissipative, energy released). A perfect short `|Γ|=1` is *lossless by construction*, so
labeling a **dissipative** breakdown "Γ = −1" is internally strained: TORSION-05's "Γ=−1 → 0 mN backward reaction" rests
on *perfect reflection* (which returns a full 180°-inverted reaction, not zero), while the muon "leaky cavity" rests on
energy *escaping* (`|Γ|<1`). The homonym may be masking a sign/loss error in the thrust and decay bench predictions — a
load-bearing bench-design question.

**What it clarifies:** a single ownership table stops the falsification benches (ZENER-04, TORSION-05, metric-levitation,
the SPICE muon leaky-cavity) from importing lossless-reflection reasoning into a dissipative event and vice-versa.

**Kill-test:** Grant rules whether the at-yield rupture is (i) lossless-reflective — then it is genuinely the *same*
Γ=−1 object as the electron cage, and the muon/0-mN "energy escapes / no reaction" language is the error to retract; or
(ii) dissipative — then `|Γ|<1`, the benches mislabel it "Γ=−1", and the split is real. Either branch refutes the current
undifferentiated usage; the substrate-hysteresis Level-1/Level-2 boundary already predicts (ii).

**Cost:** **M** — def-node/ownership table (S) + KEEP-BOTH regime tags across ~5 falsification leaves that currently write
bare "Γ=−1 impedance rupture." **The booked action is branch-contingent (per the kill-test): SPLIT + ownership table under
branch (ii) (dissipative — the glyph really is mislabeled), but a LANGUAGE RETRACTION (of the muon/0-mN "energy escapes /
no reaction" wording) under branch (i) (lossless-reflective — the glyph is one object, only the prose is wrong).**
**Confidence:** high the glyph is undifferentiated across loss-opposite objects; medium on
which kill-test branch fires (Grant's ruling). **Seduction check:** the pull is "declare it a physics error"; resisted per
flag-don't-fix — surface the homonym + the loss-character tension with receipts, do not assert the benches are wrong. The
pre-existing `cq:1612` flag (different axis, same glyph) de-risks this as a real corpus concern.

**Lane provenance:** vol4+9 F1.

---

## T4 — MOND `a₀` namesake carries the External-Field Effect (a forward-prediction opener)

**Pattern:** P8 named-object archaeology → forward-prediction unlock. **Sector:** macroscopic gravity/dark-matter; carrier
is the **μ-sector inductive drag `η_eff`** (NOT A1 mass, NOT ε-sector DE); regime axis Regime I (`g_N ≪ a₀`) vs Regime IV
(`g_N ≫ a₀`) is load-bearing.

**Two objects:**

- **The corpus MOND kernel is purely LOCAL-algebraic in `g_N`:** `manuscript/ave-kb/vol3/cosmology/ch05-dark-sector/effective-galactic-acceleration-mond.md:15`
  `g_{eff} = g_N + \sqrt{g_N a_0}\,\sqrt{1 - g_N/a_0}`; regime map `r = g_N/a_0`
  (`asymptotic-limits.md:10-11,25`). The argument is `g_N` alone.
- **The "MOND / a₀" namesake carries the External-Field Effect (EFE)** — the sharp MOND-vs-particle-DM discriminator —
  **absent from the entire KB.** Absence receipts (two methods): `grep -rwil "external field effect" manuscript/ave-kb`
  → **0 files** (the token "EFE" that appears elsewhere means Einstein Field Equation — itself a mild homonym); `grep
  -rwinE "poisson|nabla|field equation"` across `manuscript/ave-kb/vol3/cosmology/ch05-dark-sector/` → **0** (and `rg -in
  'poisson|\nabla|field equation'` on the same dir → **0**). Both commands actually fire: the `-E` alternation returns 41
  hits on a control `acceleration|mond` in that dir, so the zero is a real absence — **not** the literal-pipe vacuity of the
  earlier un-`-E`'d form, which matched the whole string `poisson|\nabla|…` verbatim and returned 0 against *any* corpus.
  No modified-Poisson field PDE anywhere; only the algebraic `g_eff(g_N)`.

**Candidate collapse (substrate-walk, not a menu):** standard MOND's EFE states that the interpolation keys on the **total
local field** (internal + external), so a system in an external field `g_ext ≳ a₀` is driven toward the Newtonian branch
even when its internal `g_int ≪ a₀`. The AVE mechanism is **Axiom-4 saturation of the local lattice's `η_eff`**. **IF the
kernel `S(A)=√(1−A²)` keys on the total local amplitude** (internal + external strain — the unadjudicated hinge, since an
external field is *also* local field), **THEN an external-field pre-saturation of `η_eff` follows structurally** — i.e. the
EFE, sitting un-named inside the `a₀` object. *(This is the un-cashed structure; the internal-vs-total keying is the
unadjudicated hinge — flag-don't-fix, Grant's call.)*

**What it unlocks:** a live, currently-tested AVE-distinct forward prediction the corpus states nowhere — whether AVE-MOND
is modified-*inertia* (internal `g_N` only → **no EFE**) or substrate-saturation (total local field → **EFE present**).
Auditor arithmetic: `a₀ = cH_∞/2π ≈ 1.07×10⁻¹⁰ m/s²`; the Galactic external field at the solar neighborhood is
`g_ext ≈ 2.1×10⁻¹⁰` — **≈ 2.0 a₀ against the AVE-derived a₀ = 1.07×10⁻¹⁰** (`2.1/1.07 = 1.96`), or **≈ 1.8 against the
empirical a₀ = 1.2×10⁻¹⁰** (`2.1/1.2 = 1.75`) — *order-a₀* either way, so the EFE is an order-1 effect on real embedded
dwarfs. Kill-test regime is
populated (Crater II dispersion; wide-binary tests, Chae 2023 / Pittordis–Sutherland).

**Kill-test:** derive whether the Ax4 saturation amplitude `A` for the galactic `η_eff` keys on the **internal source's**
`g_N` (external-field-blind → AVE predicts **no EFE**, falsified by any confirmed EFE detection) or the **total local**
field (→ AVE predicts an EFE ~ standard MOND, falsified by a confirmed EFE *null* in a strong-external-field dwarf). Either
way the currently-underdetermined keying becomes a sharp fork.

**Cost:** **M** — one discriminator leaf + a `predictions.yaml` row; the physics hinges on one existing kernel-argument
decision, not new machinery. **Confidence:** medium-high the EFE is genuinely un-cashed (0-hit absence, two methods).
**Seduction check:** the seductive move is to *assert* "Ax4 → EFE, matches MOND, another win" — flagged not asserted; the
keying is the unadjudicated hinge, belongs to Grant/derivation. **This is the single target that opens a NEW forward
prediction rather than cleaning up — ranked high for that reason, per the program's stated edge (bench/beam over interior
mechanism).**

**Lane provenance:** vol3+6 F1.

---

## T5 — `δ_AVE` loss-tangent axis and `θ` power-factor axis are one dissipation phasor (`tan δ = cot θ`)

**Pattern:** P1 identity MERGE (de-orthogonalize) + a P6 homonym on "δ". **Sector:** sector-agnostic Axiom-4 kernel
dissipation; regime = at/across saturation only (sub-yield is lossless, `θ=90°`, `δ_AVE=0` by Axiom 3).

**Two "orthogonal" axes inside one leaf `temporal-saturation-regime-classifier.md`:**

- `δ_AVE` asserted to BE the substrate loss tangent: `:21` "Definition: substrate-native loss tangent"; `:32`
  "$\tan\delta = \sigma/(\omega\varepsilon)$ — ratio of dissipative to reactive current."
- The power-domain `θ` axis, asserted **orthogonal** to `δ_AVE`: `:17` "the phase angle between substrate voltage and
  current at the LC tank"; `:331` "**All three axes are orthogonal classifiers.**" Yet `:42-44` map `δ_AVE` 1:1 to `θ`
  (Lossless↔θ=90°, Lossy↔θ→0°) — a functional dependence, not independence.

**Candidate collapse — the shared mechanism (two claims, one fireable, one gated).** The *fireable* claim is a genuine
phasor identity: the **EM loss tangent** `tan δ = σ/(ωε)` (`:32`, a per-cycle dissipative/reactive-current ratio, a real
impedance-phasor quantity) and the **power-factor angle** `θ` are complementary angles of the same LC-tank phasor
`Z = R + jX` — `tan δ = R/X = cot θ`. So the leaf's power-domain `θ` axis and its EM-`tan δ` reading are **not orthogonal**;
that single de-orthogonalization is the shared mechanism and it kills the `:331` "all three axes are orthogonal" over-claim.
**The gated (NOT fireable) claim:** `δ_AVE = t_sat/t_period` is a **duty-at-saturation ratio**, NOT `tan δ` — a system can
spend a *small* duty fraction at *high* instantaneous loss, so "`δ_AVE` = the time-average of the `θ`-dissipation" is **not**
an identity. That θ↔`δ_AVE` tie is exactly the **taxonomic-not-derivational analogue the leaf itself flags at `:310`**
("does NOT yet derive their numerical values from `S(A)`"), not a phasor identity — it must not be written into canon as one.
This is the 0-for-7 (now 0-for-9, `program-arc-map.md:404`) shape guarded against (a real identity `tan δ = cot θ` must not be *stretched* onto the `δ_AVE` duty
ratio); the fireable residue is the `θ`↔`tan δ` de-orthogonalization only.

**What it clarifies:** removes the "three fully-orthogonal classifiers" over-claim (`:331`) on its real leg — the power-domain
`θ` axis and the EM-`tan δ` reading are the same phasor read two ways, so a reader must not treat them as carrying independent
information. The `δ_AVE` (temporal duty) axis stays a **distinct** axis (the `:310` taxonomic bridge, not collapsed into `θ`).

**Kill-test:** exhibit two systems with the **same** `δ_AVE` but genuinely non-degenerate `θ`-trajectories that put them
in **different classifier bins** — i.e. a `θ`-signature `δ_AVE` cannot see. If the Cyclic-vs-Lossy bin can differ at fixed
`δ_AVE`, orthogonality survives. (Expected to fail: the leaf's own `:42-44` table shows the bins co-move.)

**Cost:** **S** — one-leaf annotation: correct `:331` by adding the `tan δ = cot θ` identity tying the `θ`-axis to the EM
`tan δ` reading (the fireable de-orthogonalization); **do NOT** write "`tan δ = cot θ` ties `θ` to `δ_AVE`" (that stretches
the identity onto the `δ_AVE` duty ratio — keep `δ_AVE`↔`θ` as the `:310` taxonomic bridge). **Confidence:** high on the
`tan δ = cot θ` phasor identity and the `:331` de-orthogonalization; the θ↔`δ_AVE` merge is explicitly *not* asserted.
**Seduction check:** the trap here is exactly the one the review caught — a genuine trig identity worn by a rhyme; the
fireable claim is narrowed to the `θ`↔`tan δ` leg only. Caveat: the `θ` definition's home leaf is `orbital-friction-paradox.md`
(out of the common lane's scope) — the implementer should grep-confirm the `θ`-owner leaf before landing.

**Lane provenance:** KB-common C2.

---

## T6 — boundary-observable `M`: mass→inductance `L` collides with the ratified `X_L` = spin ruling

**Pattern:** P6 homonym on "inductance L" / A1⊥T2 sector cross-wire — SPLIT (flag-don't-fix). **Sector:** A1 (mass /
dilatation, `Z_bulk`) vs T2 / `X_L` (spin / microrotation); the crosswire is A1↔inductive — a direct instance of the
"A1⊥T2 cross-wiring watch."

**Two structures assigning "inductance" to opposite sectors:**

- **`boundary-observables-m-q-j.md:19` (`clm-ze4clw`):** `$\mathcal{M}$` (mass) → "**EE projection = inductance $L$**";
  `:21` `$\mathcal{J}$` (spin) → EE projection = magnetic moment.
- **The 2026-06-20 Grant-ratified dual-reactance ruling:** `dual-reactance-storage-taxonomy.md:221` "🔴 **$X_L$ = the
  FLYWHEEL (spin/frequency-regulation) sector, NOT the rest-mass store** … the **rest mass is the A1 longitudinal
  DILATATION** (the $X_C$-side / bulk-dilatation depression, $Z_{bulk}\to0$)"; corroborated by `def-portmp`
  (`vocabulary-register.md:423`) "**MASS (A1) $\perp$ CHARGE (T2)** … never one phasor," with an explicit DOMAIN guard
  against wiring EM/inductive params onto the mechanical sector.

**Candidate SPLIT — the object at stake:** the EE circuit-element identity of the electron's rest energy. The reconciling
structure is a **P6 homonym on "inductance"**: (a) the **TKI translation-image** `L = ξ_topo⁻²·m` (mass's kinematic image;
"identity-by-translation, NOT a derivation" per `def-tk1xfm`) and the Lenz inertial-resistance reading (`clm-jwyy6l`), vs
(b) the **physical reactance sector** `X_L` = microrotational-B = spin flywheel. `boundary-observables:19` uses the bare
word "inductance L" without saying *which* — and, read against the ruling, appears to wire rest-mass into the `X_L` (spin)
sector, the exact A1⊥T2 cross-wire the ruling was minted to forbid.

**What it clarifies:** removes a latent contradiction where a canonical leaf (`clm-ze4clw`) and a Grant-ratified ruling
assign "inductance" to opposite sectors; prevents a reader concluding "mass and spin are both inductance."

**Kill-test:** if `boundary-observables:19`'s "inductance L" is *explicitly* the TKI translation-image (a dictionary entry)
and stated **distinct** from the physical `X_L` sector, it is a documented homonym, not a contradiction — no fix. The leaf
currently carries no "TKI"/"translation"/"X_L" qualifier on line 19 (grep-clean), so the disambiguation is owed. Conversely,
if the rest-energy store is shown genuinely inductive (not the A1 `Z_bulk` depression), then `dual-reactance:221` is stale —
the flag surfaces the conflict for Grant, does not pick the winner.

**Cost:** **S** single-leaf disambiguation; **M** if propagated to every "mass = inductance" site (`newtonian-inertia-as-lenz`,
`relativistic-inductor`, the TKI dictionary). **Confidence:** medium (the cross is real and grep-clean; a homonym-resolution
plausibly exists but is not stated at the M-row). **Seduction check:** contradiction-surfacing flag, low seduction; the
discipline risk is over-reading a homonym as a contradiction, guarded by the kill-test.

**Lane provenance:** KB-common C3.

---

## T7 — "Cauchy relation" mislabel on `K=2G` (the genuine Cauchy relation forces `ν=1/4`, not `2/7`)

**Pattern:** P8 named-object archaeology with a P6 "Cauchy" homonym underneath — SPLIT / name-correction. **Sector:** A1-bulk
(`K`, dilatation) vs T2-shear (`G`); cold lattice (the trace-reversal operating point `u₀*`).

**Two labels on the one isotropic elastic-constant ratio `λ/μ`:**

- **`K=2G` labeled "Cauchy relation":** `manuscript/ave-kb/vol1/claim-quality.md:644` "the **Cauchy relation** for the
  isotropic lattice"; `:662` already flags it OPEN — "it is stated as the isotropic-lattice Cauchy relation but the
  substrate is explicitly non-Cauchy/micropolar … **STAYS OPEN (2026-07-04, PR #508)**" (orchestrator-verified).
- **The SAME `K=2G` correctly labeled the GR-imported trace-reversal value:** `vol1/ch8-alpha-golden-torus.md:13` "the
  **K=2G elastic ratio (ν_vac=2/7) is GR-imported, not crystalline-forced**"; `vol2/claim-quality.md:120` "the vacuum
  Poisson ratio $2/7$ is the GR-imported trace-reversal value."

**Candidate SPLIT — the shared object + the arithmetic (auditor-owned):** both labels describe the ONE elastic ratio, so
this is a genuine bench-object collision, not a resemblance. The *genuine* classical **Cauchy relation** (central-force
lattice, every site a center of symmetry, `c₁₂=c₄₄`) reduces in the isotropic limit to `λ=μ`, giving `ν=1/4` and `K=5G/3`.
AVE's `K=2G` instead requires `λ=4μ/3`, giving `ν=2/7` and `K=2G`. So `K=2G` is **not** the Cauchy relation — the Cauchy
namesake forces a *different* point (`ν=1/4`, `K=5G/3`). Verified un-noted: `grep -rniE "nu=1/4|lambda=mu|central.force→1/4"`
across vol1 returns the amorphous-EMT `z₀≈51.25` material but **no** site tying the classical Cauchy relation to `ν=1/4`.

**What it clarifies:** removes a false impression that classical central-force elasticity *forces* `K=2G`, and thereby
**strengthens the corpus's own standing conclusion** that `K=2G` is GR-imported (PR #261/#506/#508). The one classical-
elasticity route that could have forced the ratio (the Cauchy relation) forces a *different* value, so naming AVE's `ν=2/7`
lock "the Cauchy relation" both mis-cashes the namesake and papers over the very import the echo-register already flags.
Note `:662` already flags the label as *micropolar*-wrong; the **net-new** content here is the `ν=1/4` arithmetic + def-noding
"Cauchy" as a 3-way homonym (Cauchy elastic solid / "Cauchy relation K=2G" mislabel / true central-force `λ=μ⇒ν=1/4`).

**Kill-test:** if a defensible convention exists under which "Cauchy relation" denotes `λ=4μ/3` / `K=2G` (rather than
central-force `λ=μ`), the mislabel dissolves. None known — the Cauchy relations *are* the central-force elastic identities.
Second kill: if the substrate sat at `ν=1/4` and `2/7` were a typo — but `2/7` is corpus-wide and load-bearing (feeds the
Weinberg angle `vol2/claim-quality.md:120`, PMNS), so no.

**Cost:** **S** — one register/def-node row (the 3-way "Cauchy" homonym + the `ν=1/4`-vs-`2/7` separation) + a one-line
correction on `claim-quality.md:644` (it is the GR trace-reversal lock, already correctly named elsewhere). No derivation
work; values unchanged. **Confidence:** medium-high (verbatim two-method receipts; textbook arithmetic). **Seduction check:**
does not create a chord — a name-hygiene compression that *reinforces* an existing negative, the low-seduction direction.

**Lane provenance:** vol1+2 C2.

---

## T8 — The four-bin content taxonomy is 3/4 a relabel of `lattice-model-register` Axis-A (deflation)

**Pattern:** P1 identity MERGE-into-existing-register (a *deflation* — the pretty 4-fold symmetry is 3/4 already canon).
**Sector:** the bins are defined by kernel-coupling; sector-agnostic taxonomy.

**Two structures:**

- **The four-bin taxonomy** ([`2026-07-10_impedance-register-walks_framing.md:15-20`](2026-07-10_impedance-register-walks_framing.md),
  FRAMING/seduction-flagged): Traveling (on-line/TL) · Standing (Op6 eigencavity `clm-gdd70j`) · Reactive-static (off-line)
  · Winding (graph/counting/kernel-blind).
- **The canonical `lattice-model-register.md` 2×2** (`:22-26`): Axis-A **K4 graph** column owns "winding classes, charge =
  Link ∈ ℤ", "frequency-blind AND amplitude-blind"; the **srs-embedding** column owns "the dispersion ω(k) … band edge."

**Bin-by-bin map:** bin 4 Winding ≡ Axis-A K4-graph column (same object); bins 1+2 (traveling + standing) ≡ Axis-A srs-
embedding column (owns ω(k), band, Op6 cavity — same object); bin 3 (reactive-static / off-line / DC boundary-value) has
**no column** in the register — genuinely new, and its novelty *is* the X41 K1-vs-K2 open fork (`:19,24`, PENDING-GRANT,
merged #627).

**Candidate MERGE — the shared mechanism:** every bin is defined by *one axis* — its coupling to the Axiom-4 saturation
kernel (loads / loads-at-full-amplitude / transparent / blind). The four-bin table is the least-common-refinement of the
existing 2-way carves (`lattice-model-register` graph-vs-embedding; the AC/DC / FPB charge-vs-photon carve; the Op6
traveling→standing trap).

**What it clarifies:** prevents minting a **fifth standing register** redundant with `lattice-model-register`. Recommend the
taxonomy land as a *third axis* (kernel-coupling: loads/full/transparent/blind) annotated onto `lattice-model-register.md` —
the precedent the X35 pass itself cites (`temporal-saturation-regime-classifier` added an orthogonal axis without rewrite).
Only bin 3 needs new derivation, gated on X41.

**Kill-test:** refuted if bin 3 (off-line/reactive-static) turns out to also live in one of the existing two columns (i.e.
the off-line dress is just the embedding column's DC limit, not a distinct register) — *precisely* the K1-vs-K2 fork (X41).
So "really four bins or three-plus-a-fork" is already the corpus's open gate; **do not pre-resolve it.**

**Cost:** **M** — annotate `lattice-model-register` with the kernel-coupling axis, KEEP-BOTH with the framing note, bin-3 row
stays gated. **Confidence:** high on bins 1/2/4 being the existing columns (verbatim column-ownership match). **Seduction
check:** the pretty "4-bin" symmetry is itself the seduction; this finding *deflates* it (3 of 4 are old), the safe direction;
add-an-axis, not redefine-in-place.

**Lane provenance:** research C2 (⊕ KB-common rejected-Lead-1, same conclusion).

---

## T9 — Complex Poynting `Im(S)=2ω(⟨W_m⟩−⟨W_e⟩)` = undeployed formal home for the off-line register

**Pattern:** P8 (also a derivation surface for a standing framing). **Sector:** cross-sector field identity (EM T2 line +
A1/V reactive store); sub-yield reactive/lossless (Ax3). **Seduction-adjacent — see self-check.**

**Objects:** the corpus deploys the **lumped scalar** reactive power `Q = VI sin θ` (`orbital-friction-paradox.md:17`;
electron-orbital `Q_reactive = m_e c²·α` at `θ=90°`, `P_real=0`) and the real radiation-resistance port `R_rad ≡ Z_0`
(`substrate-native-terminology.md:43`). The **field-form complex Poynting theorem** `S = ½ E×H*`, `Re(∮S·dA)=P_diss+P_rad`,
`Im(∮S·dA)=2ω(⟨W_m⟩−⟨W_e⟩)` (reactive flux = magnetic-minus-electric stored-energy imbalance, vanishing at resonance) is
**undeployed** — absence receipts (three patterns): `rg -i "complex Poynting"` → 0; `rg -iE "E ×H\*|reactive Poynting|complex
power"` → 0; `rg -iE "Re\{S\}|Im\{S\}|Poynting theorem"` → 0 field-form hits (the only Poynting+complex files use a *real*
"Poynting-like flux" in the engine test + the shear-sector lumped dark-wake doc).

**What it clarifies (triple):** (1) a **formal home** for the 2026-07-10 off-line register — the theorem makes
`Re(∮S·dA)=0 ⟺ no net real power ⟺ "no Re(Z)"` a *derivation* instead of the walked analogy in
[`impedance-register-walks_framing.md:80`](2026-07-10_impedance-register-walks_framing.md); (2) it **supplies the K1-vs-K2
discriminator variable** — "loads vs transparent" is exactly `Re(∮S·dA)≠0 vs =0`; (3) it **reconciles a live internal
tension** — the electron tank is called both "radiation-resistance-limited `Q=1/α`" (`parametric-coupling-kernel.md:239`)
and "`P_real=0`, rings forever" (orbital-friction), which under Ax3 hold together only if the "radiation resistance" is the
reactive matched-coupling port — the Re/Im split the theorem provides.

**Kill-test (the sector gate — this could kill the off-line framing, not confirm it):** if the substrate's reactive store
cannot be written as a field bilinear `E×H*` because the confining channel is A1-longitudinal (V-sector scalar, no transverse
H to pair with E), the magnetic half `⟨W_m⟩` is sector-empty and the theorem degenerates — the off-line register is A1-only
and the `E×H*` form does not transfer (it would need an A1-native "compression×velocity" reactive-flux analog). **Verify the
reactive store lives in a channel with both an E-like and H-like conjugate before importing `E×H*`.**

**Cost:** **M.** **Confidence:** high it is undeployed; medium-high the mechanism transfers. **Seduction check:** the framing
note is *so* pretty (four converging walks) that formalizing it feels like confirmation — it is not; the theorem's first
output could be the sector-gate *kill*. A rhyme until something pays to kill it and fails.

**Lane provenance:** P8-archaeology C1.

---

## T10 — `Q`-glyph carries ≥4 electron-scale objects with no central register

**Pattern:** P6 homonym SPLIT / ownership row. **Sector:** electron LC tank (T2/V), cold, sub-yield reactive.

**Objects (all glyph "Q", all electron-scale, distinct values):** loaded/radiative `Q=1/α=137.036` (α-baked ECHO,
`theorem-3-1-q-factor.md:15,147`) · intrinsic/stability `Q→∞` (`:147`) · α-free cold-cage ring-down `Q_ringdown≈30.8`
(`vacuum-varactor-scatter-operator.md:185`; `vol9/claim-quality.md:507`) · structural radiative-Q floor `Z_RADIATION≈29.98`
(kept "band-consistent, NOT identical," guarded by pinned anti-coincidence test
`test_graded_vacuum_network_isolation.py:141-146` — `test_anti_coincidence_Q_is_not_Z_radiation`; the stale `:119` was
inherited from canon's own cite at `vacuum-varactor-scatter-operator.md:188` (`:119-124`, likewise stale — flag for the
next KB hygiene pass, not fixed here))
· per-mode count `Q=ℓ` (`op21-multi-mode-mode-counting.md:10`). No `def-Q` exists (`grep '^## '` on `vocabulary-register.md`
→ 0). Adjacent risk: `parametric-coupling-kernel.md:239` reuses `Q_atomic=α⁻¹≈137` as an input while `:213` uses
`Q_apparatus~10³–10⁹`; the α-echo status of the former does not travel with the symbol.

**Shared bench mechanism:** the *same electron LC tank's* quality factor read at different ports/conditions — not
interchangeable numbers (137 ≠ 30.8 ≠ ∞ ≠ 29.98). **Clarifies:** one `def-Q` ownership row lets a reader tell which "Q" a
downstream cite means. **Kill-test:** if a single derivation shows {137, 30.8, ∞, 29.98} are one object under stated
conditions (pure regime labels, not distinct physics), the homonym collapses to one calibration axis and only a footnote is
warranted — but the T3.4b banked NEGATIVE (30.8 ≠ 137, α never baked; loaded-Q derivability adjudicated CIRCULAR/OPEN,
`theorem-3-1-q-factor.md:156`) says they are genuinely distinct today. **Cost:** **S.** **Confidence:** medium-high (no central
register; objects distinct). **Seduction check:** low — janitorial; discounted *because* the corpus already firefights it
leaf-by-leaf, hence ranked here not higher.

**Lane provenance:** vol4+9 F2.

---

## T11 — A4 fork to carry + the `I_max` = 124.4 A vs 116 A cite-catch

**Pattern:** standing fork the registry should CARRY (per `feedback_substrate_adjudicates_forks`) + a concrete verify-before-
cite catch. **Sector:** μ/L slew-side; particle-tier vs constitutive-tier.

**The fork (OPEN, Grant-flagged):** is the relativistic inductor `L_eff(I)=L_0/√(1−(I/I_max)²)` (`relativistic-inductor.md:15`,
`clm-p5cf3t`) literally the FPB slew kernel `S_B(A_I)=√(1−A_I²)`
([`operator-typing-pass_result.md:112`](2026-07-10_operator-typing-pass_result.md), A4 "**Grant's physical ruling — still
OPEN**"), or its dual? Engine evidence of unsettledness: `cosserat_master_equation_fdtd.py:19-20,144,238` keys `L_eff` on **V**
(amplitude) yet applies to **V̇** (rate) — mixed keying in use.

**The cite-catch (orchestrator-re-verified, fire-ready NOW regardless of A4):** `I_max` is quoted at **two values under one
identity**. `relativistic-inductor.md:15`: `I_max = ξ_topo·c ≈ 124.4 A`. FPB note
[`2026-07-09_highE-carrier-fpb-corner_walked-framing.md:48`](2026-07-09_highE-carrier-fpb-corner_walked-framing.md):
`I_max = E_c·ℓ_node/Z₀ ≈ 116 A`, echoed in the X35 population table (`operator-typing-pass_result.md:135`) and the kernel
catalog (`universal-saturation-kernel-catalog.md:170`). A ~7% mismatch under one "slew-μ / relativistic-inductor" label, via
two *different* defining formulas, no SHA-pin (an A47 cross-tree-citation gap).

**What it clarifies:** whether G1 mints one "Op23-class" slew-μ operator or a swing/slew pair; and reconciles the numerical
`I_max` (one kernel → one SHA-pinned value; dual pair → two caps that the labels must distinguish). **Kill-test:** the
displacement-current identity `I=C·Ė` makes `I/I_max ≡ A_I` only if the keying is on the rate; the engine's V-keying (not V̇)
is the discriminator — a driver keying `L_eff` on V̇ vs the V-keyed engine separates "one kernel" from "dual pair." **Cost:**
**S** to carry the fork + note the `I_max` discrepancy; **L** if A4 is resolved by derivation. **Confidence:** high (fork
Grant-flagged; mismatch orchestrator-verified). **Seduction check:** the "as above/so below, same cell two tiers" reading is
the seductive one; the `I_max` 124.4-vs-116 mismatch is the object-level knife that keeps it honest.

**Lane provenance:** research C4 (⊕ orchestrator numeric re-verification).

---

## T12 — `u₀*` glyph: SYM operating-point vs T-breaking bias (deflates the §b triple-convergence)

**Pattern:** P6 homonym SPLIT on a symbol — reported as the **deflation** of the mission's candidate #3 (the §b triple-
convergence), NOT an upgrade. **Sector:** the two senses sit on opposite sides of the SYM/ASYM fence; cold-lattice.

**Objects (one glyph, two senses):** **Sense A** — substrate magic-angle SYM-class operating point `u₀*≈0.187`,
`K(u₀*)=2G(u₀*)`, `ν=2/7` (`vol1/ch8-alpha-golden-torus.md:206`; SYM-class per `2026-07-09_fast-sector-settling…:155`).
**Sense B** — frozen T-breaking / even-χ² DC bias the parity meter reads (`universal-saturation-kernel-catalog.md:216`;
`claim-quality.md:888` "whether the frozen bias $u_0^*$ enters the transverse channel … PENDING-GRANT"). Tension already
in-corpus: `vol3/cosmology/ch04-generative-cosmology/op14-cosmic-horizon-profile.md:91` "the over-bracing $u_0^*$ at K4
magic-angle IS the static-E-field component" — while `manuscript/ave-kb/CLAUDE.md:75` classes a static-E-only drive
**ASYMMETRIC**. So the same `u₀*` is called SYM-class *and* identified with a static-E over-brace that CLAUDE.md classes
ASYM-N(ε).

**Why this deflates (does not upgrade) the §b rhyme:** the §b "triple-convergence" (parity-meter READS / matched-vertex
REQUIRES / nucleation WRITES one `u₀*`) draws part of its prettiness from all three threads wearing the *same symbol* — a
symbol the corpus already overloads across a T-symmetric SYM operating point and a T-breaking bias. Whether those are one
physical object is exactly the A1↔T2 D-II calibration bridge (PENDING-GRANT), which §b itself names as kill-test #1. **What
it clarifies:** a `u₀*` def-node (Sense A = SYM magic-angle ≈0.187, back-fit; Sense B = the parity-meter frozen bias, PENDING
the bridge) stops the two senses cross-authenticating. `u₀*` is currently un-registered (`rg -n 'u₀|u_0\*|u_0\^'
vocabulary-register.md` → **0 relevant hits**; the only `u_0` substring matches are at `:423,:681`, both inside
`\mu_0`/`\mu_{eff}` EM-constitutive text — not `u₀*` entries — so the earlier bare `rg 'u_0' → 0` was a false receipt).
**Kill-test:** the D-II bridge showing the frozen bias enters the T2 channel *and* equals the magic-angle value would
collapse B→A; until run, treat as two. **Cost:** **S.** **Confidence:** high the glyph is overloaded. **Seduction check:** this
IS the deflationary lens the 0-for-7 ledger (now 0-for-9, `program-arc-map.md:404`) demands — do not upgrade the §b convergence (see RHYME tier R5).

**Lane provenance:** research C3.

---

## T13 — vol5 protein-folding subtree + divergence-map B4 are STALE against the N13 impedance-folding NEGATIVE

**Pattern:** honesty-lag reconciliation (task-authorized reportable class; the sweep's `flag-don't-fix` member). **Sector:**
EM/graded-impedance (Ax3 Minimum-Reflection); Regime I linear-lossless at chain scale — NOT an A1-mass or T2-charge claim.

**The unreconciled conflict:** `common/program-arc-map.md:370` records N13 "Protein impedance-folding (impedance carries the
fold)" as **NEGATIVE (all EE-reflection channels dead)** — with its own "Core walk-back STAGED, B4 row may be STALE
`[PARTIAL-RECEIPT]`" caveat. Meanwhile the entire `vol5/protein-folding-engine/` subtree + `divergence-test-substrate-map.md`
B4 (`:111,113` "per-residue `Z_topo` … zero-parameter, no Levinthal random search") + `solver-toolchain.md:478-480` assert
that same mechanism as **live**, none carrying a 🔴 Rule-12 / staleness header. The reconciliation ledger itself flags the
risk twice (`research/2026-06-07_vol0-kb-reconciliation-ledger.md:108,153`). **This aligns with the standing memory record**
(`project_ave_protein_impedance_folding_negative`: README/B4 row STALE, Core walk-back STAGED).

**Auditor limit (cross-tree discipline):** the N13 kill-details are **not in this checkout** — the ledger only *references*
the AVE-Protein lane; N13 is self-tagged `[PARTIAL-RECEIPT]`. The conflict **exists and is unreconciled at 7fedf5c3**; the
sweep does **not** assert the negative is correct (unverifiable here). **What it clarifies:** resolves whether Vol 5's
flagship result (Levinthal resolved by impedance matching) is a live prediction or a retracted route — currently a reader
hits "NEGATIVE, all channels dead" in one leaf and "zero-parameter live discriminator" in another. **Kill-test:** produce the
AVE-Protein walk-back receipt showing N13 kills only one specific `S₁₁` channel (not the `Z_topo`-regime B4 prediction), OR
show an existing 🔴 header in vol5 missed by the sweep — either collapses this to "already reconciled / N13 over-stated."
**Cost:** **M** (propagate Rule-12 headers once the receipt lands, OR demote N13's "NEGATIVE" to match its `[PARTIAL-RECEIPT]`
status). **Confidence:** high the conflict exists; cannot assess which side is right (cross-repo). **Seduction check:** resisted
"big walk-back = big find"; the honest object is the *unreconciled conflict the corpus flags itself*, not a verdict the sweep
can source. **Grant one-sentence ruling owed:** is N13 a full falsification of the vol5 `|S₁₁|²`-folding mechanism, or does it
kill only a narrower AVE-Protein channel?

**Lane provenance:** vol5 F1.

---

## T14 — "strain" the word: `δ_strain` (α-echo residual) vs `ε₁₁` (saturation amplitude), both dimensionless

**Pattern:** P6 homonym SPLIT (mostly modifier-managed; one un-guarded dimensionless collision). **Sector:** `δ_strain` is
ε-sector (dielectric thermal-running residual); `ε₁₁` is A1-dilatation gravitational saturation amplitude. Cross-sector, same
word, both dimensionless.

**Objects:** `δ_strain = 1 − CODATA/α_cold ≈ 2.225×10⁻⁶` (`vol3/cosmology/ch05-dark-sector/delta-strain-cosmic-tcc.md:90`,
"fractional vacuum strain coefficient") vs `ε₁₁ = 7GM/c²r` "radial strain" = the Ax4 amplitude `A`
(`vol3/gravity/ch01-gravity-yield/temporal-spatial-lattice-decomposition.md:14`; `vol3/claim-quality.md:59`). The **δ-glyph**
node `def-de17a0` guards `δ_strain` only against the tan-δ / loss-angle family — it does **not** guard it against the
amplitude-`A`/`ε₁₁` family (both dimensionless; `ε₁₁` can be ~10⁻⁶ at large `r`). q-g22 (`vol4/…/q-g22-strain-convention.md`)
resolves the amplitude-`A` sense into `A_geom`/`A_field` but is silent on `δ_strain`. (vol6 adds further modifier-managed
senses: "vacuum strain" = `n(r)−1` = M-integrand; "topological strain" = knot tension.)

**Shared word, distinct objects:** at least four bench objects share "strain"; the sharp un-registered collision is
`δ_strain ↔ ε₁₁`. **Clarifies:** the one dimensionless "strain-vs-strain" pair a reader can conflate (an α-echo residual with
a gravitational saturation amplitude). **Kill-test:** if every load-bearing site already carries a disambiguating modifier
(`radial`/`transverse`/`phase`/`δ`/`vacuum`) such that no bare-"strain" site is sense-ambiguous, the homonym is cosmetic —
grep shows modifiers cover most sites (hence medium, not high). **Cost:** **S** (extend the δ-glyph row to name the amplitude-`A`
sibling, or a one-line `strain`-word ownership node → q-g22 + `def-de17a0` + M-integrand). **Confidence:** medium. **Seduction
check:** resisted "105+89 instances ⇒ big problem"; modifiers manage most, the only genuinely un-guarded collision is the flag.

**Lane provenance:** vol3+6 F2.

---

## T15 — `S₁₁` INVARIANT-N4: declared a homonym, but the substrate suggests ONE reflection object

**Pattern:** P6-declared-split that is substantively a P1 identity — MERGE the glyph, rename the objective. **Touches a
canonical invariant (INVARIANT-N4) — adjudication, not a silent mint.** **Sector:** EM reflection (Ax3); not A1/T2.

**Objects:** INVARIANT-N4 (`CONVENTIONS.md:361`; `vol5/claim-quality.md:217,221`, `clm-a3rby3`, solidity 1.00) declares `S₁₁`
a homonym — "reflection coefficient in vol 4/vol 7; folding objective function in vol 5 … Do NOT carry over the magnitude
bounds, units, or interpretation." But three vol5 folding leaves use the *EE standing-wave reflection coefficient* meaning:
`solver-toolchain.md:478` "Standing Wave Reflection ($S_{11}$)"; `levinthal-mechanical-resolution.md:19` "minimizes the
backbone `|S_{11}(ω_0)|²` — the standing-wave reflection at the amide-V frequency"; `red-light-therapy.md:30` "`|S_{11}|` …
measured directly via microwave network analysis." And the axiom register itself: `CLAUDE.md:72` Axiom-3 signature "`S₁₁`
minimization (EE-projection form)" — i.e. vol5's `S₁₁` minimization IS the EE-projection of min-`|Γ|²`, the same object.

**Candidate collapse:** in vol5, `S₁₁` is the backbone transmission-line's standing-wave reflection coefficient (identical to
vol4/7); the "folding objective function" is the derived functional `min_geom |S₁₁(ω₀)|²` (Ax3 applied to the residue cascade),
**not** a redefinition of the glyph. N4 mis-locates the homonym at the symbol level; the real distinction is system + frequency
(`ω₀`=amide-V), not meaning. **Clarifies:** `clm-a3rby3`'s "Do NOT carry over … interpretation" reads *backwards* — the vol5
usage requires the standard dimensionless `|S₁₁|≤1` reflection interpretation to be meaningful. **Kill-test:** the private
`eq:s11_energy` (AVE-Protein, `vol5/index.md:30`) may define a genuinely *dimensioned* free-energy functional distinct from
`|S₁₁|²`; if so, the split is partly real and only the *label* "`S₁₁` objective function" is loose. **Not visible from this
checkout — flag, don't resolve.** **Cost:** **S** (def-node `S₁₁` as the single reflection object + rename the vol5 objective
to e.g. `𝓕_fold=min|S₁₁|²`; OR soften N4/`clm-a3rby3` to "same object, different system + ω₀"). **Confidence:** high the in-repo
`S₁₁`=reflection coefficient; medium the homonym-binding is fully spurious (out-of-repo eq unverifiable). **Seduction check:**
the pull is "homonym → secretly identity" (a program favorite); guarded by pinning to the Axiom-3 register's own words and
hedging on the private eq. **Because this contradicts a solidity-1.00 canonical invariant, it needs Grant + the eq before any
edit.**

**Lane provenance:** vol5 F2.

---

## T16 — "register" now carries ≥4 senses (ledger-index ⊥ impedance-content-bin)

**Pattern:** P6 homonym SPLIT / hygiene. **Sector:** terminological. **Objects (four senses):** (1) tracked-index/ledger
(vocabulary-/axiom-/interlock-/model-register — the spine node-type, dominant); (2) verb "to register / pre-register / a
meter registers a signal"; (3) impedance/content-domain bin ("which **register** it lives in", "**graph register**",
`2026-07-10_impedance-register-walks_framing.md:13,20`, fresh); (4) linguistic/communication register (ee/qft register).
**Collision:** senses 1 and 3 co-occur in one doc — titled "impedance-**register** walks" (sense 3) while citing
`vocabulary-register` (sense 1) throughout. **Shared mechanism:** none — a clean SPLIT (a ledger-index and an impedance-content-
bin are unrelated objects sharing a word). **Clarifies:** a register-of-registers note so "off-line register" is never parsed
as a tracked index. **Kill-test:** n/a (terminological — this is a *pure-terminology* P6 clean SPLIT with no fireable
kill-test). **T16 is the sole ranked target without a fireable kill-test; that is precisely why the romance-knife's "18 of
19 name a genuine shared mechanism or honestly-flagged fork" count is 18, not 19 — T16 is the named exception, not an
arithmetic slip.** **Cost:** **S.** **Confidence:** high it is a homonym; **moderate value**
— most uses are self-disambiguating `X-register` compounds. **Seduction check:** low; hygiene, flagged because the mission asked
for the count. **Lane provenance:** research C5.

---

## T17 — Ferrite / Polder gyrotropic-μ-tensor = undeployed mechanism for the circulator's non-reciprocity (INVENTORY-ONLY)

**Pattern:** P8 + a P1-merge flag (Larmor spin-flywheel ⊕ circulator T-breaking → one DC-biased precessing-medium object).
**Sector:** μ-sector (T2/Cosserat micro-rotation), cold. **INVENTORY-ONLY per task scope — the transfer question IS the
PENDING-GRANT fork; not resolved here.**

**Objects:** `def-ch1crc` **chiral circulator** = the bipartite non-reciprocal coupling drawn as a circulator element
(`vocabulary-register.md:700`), with EM S-parameter magnitude **explicitly open** (`vol4/claim-quality.md:717` "EM face
magnitude is separately pending … still awaits the chiral-crystal EM engine"; #620 C2, `program-arc-map.md:283` "sole surviving
escape is non-reciprocity … requiring a T-breaking bias — PENDING-GRANT"). The **ferrite-circulator namesake** gets its
non-reciprocity from the **Polder permeability tensor** — a DC magnetic bias → Larmor precession → off-diagonal imaginary
`κ(ω)=ωωₘ/(ω₀²−ω²)`; `S_ij≠S_ji` follows from `κ≠0`, the bias is the T-breaker. **The gyrotropic-μ *mechanism* is undeployed**
— absence receipts (two patterns, corrected per the #631 review): `grep -rin "polder" manuscript/ave-kb` → **0**;
`grep -rinE "off-diagonal (mu|permeab)" manuscript/ave-kb` → **0**. The terms `gyromagnetic` / `larmor` DO return hits
(~9 corpus-wide), but every one is the **classical g=2 gyromagnetic-*ratio* / spin-flywheel reading** (e.g.
`electron-identification.md:92` "$g = 2$ (gyromagnetic ratio, leading value)", `substrate-perspective-electron.md:237`,
`larmor-derivation.md:12`), **none a Polder gyrotropic-μ-tensor mechanism** — so the *mechanism* is genuinely absent while the
*ratio* is deployed. (The earlier draft mis-placed `gyromagnetic` inside the "→ 0" disjunction; `polder` and
`off-diagonal (mu|permeab)` are the true zero-hit terms.)

**Clarifies:** closes the named open gap `clm-gvn4r1` (compute the chiral-circulator non-reciprocity magnitude) — the ferrite
namesake tells you *what a T-breaking bias physically is* and *what magnitude it yields*, and the corpus owns the two halves
(a Larmor-precessing spin flywheel; a circulator needing a bias) without wiring them (the P1-merge tell). **Kill-test:** the
ferrite mechanism *requires an externally applied DC B-bias*; the vacuum has only **P-breaking** srs I4₁32 chirality, not an
intrinsic T-breaking magnetic bias (`translation-circuit.md:179`). If no `u₀*`/Ω_freeze bias couples to the μ-sector as a
Larmor-precession bias, the Polder mechanism does **not** transfer and the circulator escape stays reciprocal-dead. **This is
precisely the PENDING-GRANT fork.** **Cost:** **M.** **Confidence:** high the mechanism is undeployed; the transfer IS the open
fork. **Seduction check:** "circulator → ferrite → Polder" is a clean vocabulary chain; the danger is treating the namesake
match as evidence the vacuum supplies the bias — it does not; the bias is the unadjudicated variable. **Lane provenance:**
P8-archaeology C2.

---

## T18 — Kron's Schrödinger-network + rotating-machine branch (only the Maxwell-1944 branch is deployed)

**Pattern:** P8 (undeployed structure named in the lineage capsule — the "sitting in the index" tell). **Sector:** methodological
(network formalism); targets are cross-sector. **Objects:** the corpus deploys **only** Kron's 1944 Maxwell equivalent circuit
(`vol4/future-geometries/ch13-future-geometries/cem-methods-survey.md:105-107`, lineage "Kron → TLM → K4-TLM"). The capsule
`physics-lineage-map.md:359` names the undeployed branches verbatim: "any linear physical system — **rotating machine**, elastic
solid, Maxwell field, **Schrödinger wavefunction** — is exactly representable as an electrical network … the Schrödinger
equation node-by-node 1945 … **diakoptics**." Kron 1945 is even cited in `historical-precedents.md:55` — as "method-lineage"
only. Undeployed (two methods): `rg -i kron | grep -iE "rotat|moving|machine|diakopt|schrodinger"` → hits only in the lineage
capsule/bibliography, **none in an engine module or derivation**.

**Clarifies (two homes):** (1) the **atom-as-port eigencavity** (task #11, owed) — Kron's 1945 Schrödinger-network is a
ready-made node-by-node LC formalism whose eigenmodes ARE the spectrum (prior art first, per `physics-lineage-map.md:30`); (2)
the **moving-frame back-reaction** (#86) — Kron's rotating-machine speed-EMF networks are the undeployed formalism for the
DE-tracks-matter / two-way-coupling frontier. **Kill-test:** Kron's network represents the *linear* Schrödinger equation; the
AVE bound state is a *saturating* (Γ=−1, nonlinear) cavity requiring the Master-Equation-FDTD engine precisely because "a linear
lossless network cannot trap" (`two-engine-architecture-a027.md:24`). If Kron's network is inherently linear, it supplies only
the *un-trapped* spectrum — a scope cap, not a home; the rotating-machine branch has higher seduction risk (moving-frame ≠
speed-EMF automatically). **Cost:** **M–L** (formalism import, no new engine). **Confidence:** medium (Schrödinger-network);
low (rotating-machine). **Seduction check:** Kron is explicitly tagged "methodological rather than a physics precedent"
(`historical-precedents.md:10`) — the value is *concrete prior art for an owed derivation*, not a new claim. **Lane provenance:**
P8-archaeology C4.

---

## T19 — Conjugate-impedance-match term fragmented across two vol5 translation tables

**Pattern:** P6 homonym at the translation layer + P1 identity of the resolution (drift-grade). **Sector:** EM impedance (Ax3-
lossless). **Objects:** `vol5/common/translation-protein.md:28` maps "Salt bridge → Conjugate impedance match, `Re(Z_i·Z_j*)>0`";
`translation-protein-solver.md:20` maps "**Hydrophobic core** → Conjugate impedance match, `Re(Z_iZ_j*)`" and `:21` gives the
salt bridge a *different* mapping ("Reactive LC resonance, `+jX×−jX`"). Solver ground truth (one term for both):
`solver-toolchain.md:477` "**Conjugate Matching (`Y_shunt`):** … Salt Bridges, Hydrophobic pairs … `Y_shunt ∝ Re(Z_iZ_j*)/d_ij²`."
**Shared mechanism:** the `dc_analysis()` conjugate-matching shunt admittance is a single solver term applied to any touching
residue pair with correlated impedance; the two vol5 tables each attach its label to a *different* one of {salt bridge,
hydrophobic core}. **Clarifies:** removes a reader's genuine ambiguity — is `Re(Z_iZ_j*)` the salt-bridge or hydrophobic term?
**Kill-test:** if the production `dc_analysis()` implements *two distinct* terms (a `+jX/−jX` LC-resonance term for salt bridges
AND a separate `Re(Z_iZ_j*)` term for hydrophobic pairs), the tables are correctly distinguishing them and only the label string
is the (minor) homonym — not verifiable here. **Cost:** **S.** **Confidence:** high the assignment is inconsistent across tables;
medium that one term collapses both (the solver-toolchain's "hydrophobic pairs have *opposite* reactances" is physically odd —
two hydrophobics should be similar — so the lump-row may itself be loose). **Seduction check:** did not upgrade a table drift
into a deep identity; drift-grade, ranked last. **Lane provenance:** vol5 F3.

---

## RHYME tier — seduction-flagged; do NOT fire without the kill-test passing (0-for-7 ledger; now 0-for-9)

These are recorded so the core planning session sees them and does not re-discover them as "new." Each is a resemblance whose
shared *mechanism* is unproven; the miss-ledger (`program-arc-map.md:404`) stands at ~~**0-for-7**~~ **0-for-9** (booked 2026-07-13, D3 ruling) on exactly this shape.

- **R1 — Manley–Rowe power-partition (P8).** The lossless-reactance photon-number-flux sum rules are named as a *description*
  only (`translation-circuit.md:252` "Gate (a) PASSED as descriptions") but not cashed as a *constraint* on the two-tone FWM
  sideband powers. Because AVE is Ax3-lossless, Manley–Rowe is *exactly* applicable and *could* convert the numerically-measured
  `P_diff/P_FWM ≈ 3.4×10⁻¹¹` + A⁶ χ³ law into a closed-form partition. **BUT both the vol4 and P8 lanes flag it seduction-HIGH
  and rank it last:** the parity theorem (`clm-invmtr`) already forbids the difference tone by symmetry, so Manley–Rowe may just
  re-derive the same nulls, and it sits at an `interface_scoped__BULK_OPEN` hard-clamped drive (may not be a clean pump-idler
  triad). **Kill-test:** apply the sum rule to the parametric-coupling-kernel ports — if it reproduces `4π/N²` with zero new
  knobs it is a genuine constraint (chord-eligible); if it merely re-labels the already-derived balance, it is a rhyme, drop it.
  **Do NOT schedule ahead of any fire-ready target. Cost L.**
- **R2 — `Q`-namesake radiative linewidth `Δω/ω = α` (P8, GATED ON T9).** The Q-factor namesake carries `Δω/ω=1/Q` and ringdown
  `τ=Q/ω₀`; for `Q=1/α` that would be a fractional linewidth `α≈7.3×10⁻³` and ~137-cycle ringdown as *forward electron
  observables*. **But the tank is reactive (`P_real=0`, "rings forever") — `τ=Q/ω₀` and `Δω/ω=1/Q` are *dissipative-Q* relations
  and do not transfer to a reactive tank; only the tiny `R_rad` port gives a real linewidth.** So this is gated on T9 (the
  complex-Poynting Re/Im split decides which Q-namesake relations are real vs reactive-only). **Vocabulary-not-mechanism trap;
  cost S; present after T9 or not at all.**
- **R3 — AC-Josephson `f = 2eV/h` DC→AC discriminator (P8, adjacent to the banked fluxoid map).** Phase-slip + Shapiro-lock are
  deployed; the frequency-voltage relation itself is absent (`rg -iE "ac.?josephson|2eV/h"` → 0). It lands on the AC/DC carve
  (discriminating tests live only in DC→AC coupling) — a DC bias across the srs Γ=−1/3 weak-link emitting AC at `f=2eV/h` would
  be a native discriminator. **But it needs a pinned charge-2e tunneling carrier; the integer→flux-VALUE conversion is the
  imported α-echo (`axiom-register.md:230`), so `f=2eV/h` has no calibrated coefficient — form-only, and it risks re-opening the
  banked fluxoid/superconductor map under a Josephson hat.** **Kill-test:** is there a pinned tunneling charge at the vacuum
  weak-link? If not, form-only. **Cost M; check it is not the banked map renamed.**
- **R4 — Kolmogorov turbulence row "slew-mis-shelf" (registry).** The catalog shelves Kolmogorov on `A=k/k_node` (amplitude-like),
  but standard turbulent dissipation is strain-*rate* limited and the corpus carries that framing (`full-derivation-chain.md:1029`
  "when shear *rate* exceeds a yield"). So it *may* be a slew instance wearing a swing label — the one latent second mis-shelf
  beyond G2's relativistic-mass. **Low confidence — needs physics not re-derived here. Kill-test:** does the Kolmogorov cutoff key
  on `∂u/∂x` (slew) or `k/k_node` (swing)? **Cost S to flag `⚠ slew-candidate, PENDING`; L to confirm.**
- **R5 — the §b `u₀*` triple-convergence AS A CONVERGENCE (framing).** Three threads (parity-meter READS / matched-vertex REQUIRES
  / nucleation WRITES one `u₀*`) is **corpus-labeled a rhyme** (`impedance-register-walks_framing.md:30`, SEDUCTION-FLAGGED, 0-for-7
  cited — that ledger now stands 0-for-9, `program-arc-map.md:404`). Recorded here ONLY as the pre-flagged rhyme with its pre-named kill-tests (D-II calibration, X39 fork-record task #37,
  ring-quantization transient task #38). **Do NOT upgrade.** The *fireable* residue of this thread is T12 (the `u₀*` glyph
  deflation) — a def-node that stops the three threads cross-authenticating, which is the opposite move to upgrading the
  convergence.
- **R6 — the multi-stage CASCADE-FILTER ontology AS A CONVERGENCE (framing; §5 `research/2026-07-13_registers-walk_framing.md:120-134`). 🔴 KILLED 2026-07-13 — kill-test FIRED, NOT pending.** The **12th convergence** of the 2026-07-10/13 arc (so-numbered at `registers-walk_framing.md:132`): the Q-ladder read as a cascade filter's stability spectrum (electron = lowest-order terminal pole, universe envelope = highest-order stage; the §4 A/B depletion mechanisms = the two coupling classes of the cascade; internal mechanical stress draws the stage boundaries). **Pre-registered kill-shape** (frozen `research/2026-07-13_t1-atom-q-cascade-gate_prereg_FROZEN.md:43,201`): the cascade is *"the homogeneous vacuum line relabeled — a vocabulary echo."* The **T1 atom-Q cascade gate** (PR #668, `research/2026-07-13_t1-atom-q-cascade-gate_RESULT.md:28-42`) **FIRED it — bin (ii) NO-DISTINCT-VALUE:** the atom rung produces no distinct intermediate loss-Q. The **wall** loss-Q is the electron-intrinsic endpoint `Q_wall → ∞` (α-free); the observed `~10⁷` is the *transverse-EM radiative-port* α-echo (`Q_rad = 4α⁻³` exact, a different sector), not a cascade cutoff. The verified ENDS survive (electron terminal pole `Q = α⁻¹`; black-hole `Q = ℓ` — one Op21 `Γ=−1` saturation-boundary mode-count read cross-scale); the intermediate LADDER between them is not distinct physics. **Ratified Grant 2026-07-13.** KEEP-BOTH: the §5 ontology stays a framing record and the Q-ladder now carries honest per-rung provenance (`research/2026-07-11_keying-register-walk_framing.md`, atom-rung relabel); it is *not* upgraded to a ruling. **Do NOT re-discover as new.**
- **R7 — the PFC / power-factor ORDERING between endpoints AS A CONVERGENCE (framing). 🔴 KILLED WITH R6 2026-07-13.** The **13th convergence** (Grant-assigned 2026-07-13; not pre-numbered in-corpus): the complex-power ordering read as a *ladder* between endpoints (photon `PF → 1` / soliton `PF → 0`). **It rode the SAME T1 gate (PR #668) and dies with the cascade ladder (R6):** absent a distinct intermediate rung there is no ordered PFC ladder between the endpoints. **Survives as dictionary / peer content:** the two-endpoint complex-power *mapping* itself (photon `PF → 1`, soliton `PF → 0`) is retained — it is the `θ` power-factor / `tan δ` phasor content already carried by **T5** (`tan δ = cot θ`); only the LADDER (an ordered sequence of distinct intermediate power-factor stages) is killed. **Ratified Grant 2026-07-13.** KEEP-BOTH; **do NOT re-discover as new.**

---

## ANTI-TARGET CAUTION — a collapse that must NOT fire

**Band-curvature effective mass `m* = ℏ²(d²E/dk²)⁻¹` (srs band canon, #609).** The Brillouin/band-structure namesake carries
group velocity (`dE/dk`, **deployed and tested** — `cosserat-mass-gap.md:95`, "gapped group velocity, 34% error") and effective
mass from band curvature (`d²E/dk²`). The curvature `m*` is undeployed **and rejected in the vol-4 engineering register**:
`manuscript/vol_4_engineering/chapters/19_silicon_design_engine.tex:52` lists it among "arbitrary variables like effective mass
($m^*$) and thermal mobilities ($\mu$)", and the doping-engine figure strikes it through
(`manuscript/vol_4_engineering/figures/doping_engine_map.svg:102`, `text-decoration="line-through"` on "Effective Mass (m*)").
That rejection lives in the **superseded vol-4 engineering register**, however; the **KB — the truth-source — treats `m*` as
peer-with-SM** (`vol6/period-3/silicon/topological-area.md:17` parameterizes $V_{bi}$ "by effective mass ($m^*$) and mobility
($\mu$)"; `:18` grants it "peer with standard semiconductor physics rather than a distinct empirical prediction … does not
supersede the thermal treatment"), and that softer KB reading governs. **Mass is exclusively A1-dilatation** (`constants.py:72`;
`master-equation.md:20`). Minting "effective
mass from band curvature" would cross-wire T2/embedding curvature into A1 mass — the exact A1⊥T2 violation the corpus has already
deleted. **The undeployed remainder is `d²E/dk²` as sector-neutral wave-packet dispersion/spreading, NOT as "effective mass."**
Flagged so an implementer does not read the P8 archaeology as license to mint a deleted cross-wire.

**Report-only (do not resolve): the far-field-bath W1 sites.** The "external bath coupled ONLY through boundary-impedance-mismatch"
object recurs under several names — `vol3/condensed-matter/ch11-thermodynamics/nyquist-noise-fdt.md:46` ("boundary-impedance
thermalization"), `transmon-decoherence.md:12` ("thermal noise enters exclusively through the lead boundaries"),
`thermal-softening-skyrme.md:12`, `vol3/cosmology/ch04-generative-cosmology/dark-energy-latent-heat-definition.md:85` ("T2
reservoir"), and the cosmological instance under adjudication (`program-arc-map` OF12(b), the "uniform Z₀ far-field bath" question,
**PENDING-GRANT**). Whether these are one scale-invariant object or scale-instances is exactly the register-mint W1 would settle —
**but that is Grant's W1 call; the sweep only reports that four+ sites carry the identical mechanism-phrasing.**

---

## CONSIDERED-AND-REJECTED (already-collapsed, anti-targets, or resemblance-only) — audit trail

- **Anti-targets confirmed already def-registered/ruled** (grep-verified against `vocabulary-register.md`): swing/slew 2×2 (X35);
  the three "3"s (PR#220); `C_eff`-vs-`ε_eff` A1⊥T2 (Ax4); `c_EM`-vs-`c_shear` (`clm-8nkvwy`); `ξ_topo`-vs-`ξ` (`def-095760`);
  `δ`-glyph (`def-de17a0`); electron=0₁-unknot / trefoil=phase-space (`def-kn0t01`, `def-3638f2`); wall-branch fork = degenerate
  sign-selector (PR#260); chiral circulator vs gyrator (`def-ch1crc`/`def-0pt1ac`, disambiguation done); "port" overload
  (`def-portmp`, open-ambiguity registered).
- **de Broglie wave = boosted Compton clock** (P1 MERGE) — already stated (`translation-circuit.md:644`); only a register cross-link
  (Compton `def-90f843` ↔ carrier `def-a9eef5`) arguably owed (S, low generativity).
- **Op6 eigencavity = potential-well** — already explicitly collapsed ("this IS Op6", `radial-eigenvalue-solver.md:160`); the
  atom-as-port spectrum is owed to task #11, nothing to collapse until that gate runs.
- **temporal `δ_AVE` axis vs X35 swing/slew RATING axis** — genuinely orthogonal (duty-at-saturation vs which-variable-caps); NOT a
  merge. (The real nearby find is T2: slew ↔ hysteresis §1, a different pairing.)
- **`Q_ringdown≈30.8` ≟ `Z_RADIATION≈29.98`** — corpus explicitly rules "band-consistent, NOT identical," pinned anti-coincidence
  test; anti-P1.
- **`Z_0^EM` vs `Z_0^mechanical`** — one object under the `ξ_topo` VCA isomorphism, already-collapsed-by-design.
- **`a₀ ↔ ρ_Λ ↔ ν_slew` "all one object via H_∞ / 2π"** — RHYME; shared *input* `H_∞`, not shared object; corpus already walked back
  the cross-scale claim (`dm-mechanism-unification.md:117`).
- **"self-lock" vs "install-tautology"** — no convergence; "self-lock" is the genesis-autoresonance object (TESTED-NEGATIVE
  2026-06-14), "install-tautology" has zero unrelated corpus hits.
- **"settled fast channel" vs "boundary condition"** — asserted IDENTICAL by design (`fast-sector-settling…:9`); a P1 the corpus
  already made.
- **Op8/Op9 steric-wall settling** — already X35 AMBIGUOUS-A5 ("thin, candidate"); RHYME until A5 closes.
- **membrane `T_c≈278.3 K` ≡ water +4°C density max** — already cashed (`vol5/claim-quality.md:164` "interpretively the same
  lattice-yield event").
- **H-bond Op4 (vol5) vs vol2 Op4 potential well** — already collapsed (`operators.md:44`, one operator, H-bond = middle regime).
- **mass↔inductance `L=m/ξ²` (vol5 vs circuit table)** — consistent, no drift (`translation-circuit.md:22` matches
  `mass-to-inductance.md`).
- **/7 family (1/7 projection, `7ξ_M`, `T_max=c⁴/7G`, `ν=2/7`)** — already one trace-reversed-bulk-projection object (#90/91/92).
- **skin depth → surface impedance** — x29 skin-depth banked (survived review); reactive `Z_s` subsumed by T9 (complex Poynting).
- **transmission-line loaded-line/Bragg/stub/Smith for X37 bond-bore** — reciprocal-stub-match is theorem-dead (#620); the bore fork
  went to X38 S₁₁-selection, not periodic-loading.
- **diakoptics (Kron tearing)** — solver-decomposition primitive, no physics payload beyond T18's Schrödinger-network home.
- **clm-rg7cls ≈ clm-x5z09x (five-scale regime double-maintain)** — self-flagged (`vol5/claim-quality.md:631`); partial scope
  difference; housekeeping near-dup, not a clarification-generating collapse.

---

## Provenance + verification note

Seven read-only `ave-auditor` sweep lanes ran against the pinned worktree at `origin/main 7fedf5c3` (HEAD after merge #630);
none touched the main checkout or any tracked file. Every load-bearing receipt in the tier-1/2 targets carries a two-method
grep confirmation from its lane; the orchestrator independently re-verified the T2 headline (the "empty slew catalog" scope
correction), the T11 `I_max` 124.4-vs-116 numeric mismatch, the T7 "Cauchy relation" `:644`/`:662` receipts, and the T3
`cq:1612`-is-a-different-axis KEEP-BOTH check before landing. Cross-repo items (T13 N13 negative) are explicitly marked
unverifiable from this checkout (the receipt lives in the AVE-Protein lane) — the sweep asserts the *unreconciled conflict at
7fedf5c3*, not the verdict. This registry executes nothing; the core planning session adjudicates which targets fire.

**Adversarial review (PR #631, two lenses — receipt-verification + romance-knife — each with per-finding adversarial
verify).** All priority headline receipts (T1, T2, T3, T4, T7, T11) verified verbatim at correct lines; **no fabricated
quotes, no paraphrase-in-quotes, no wrong-line-that-changes-meaning, no ranking flips.** The romance-knife confirmed 18 of 19
fire-ready/gated targets name a genuine shared bench mechanism or an honestly-flagged fork, and that the entire RHYME-tier
pre-filter (R1–R5) and the band-curvature anti-target caution hold (none wrongly demoted or promoted). **Two MINOR
EVIDENCE-VOID findings repaired** (conclusions survived; only the demonstration was imprecise): (1) **T17** — the absence
pattern mis-placed `gyromagnetic` in the "→ 0" disjunction (that term returns ~9 hits, all the classical g=2 gyromagnetic-
*ratio* reading); corrected so the true zero-hit terms are `polder` and `off-diagonal (mu|permeab)`, with the ratio-hits
moved to the spin-flywheel caveat that already accounts for them — the "Polder *mechanism* undeployed" conclusion is unchanged.
(2) **T5** — the fireable claim was narrowed to the `θ`↔EM-`tan δ` de-orthogonalization (`tan δ = cot θ`, a real phasor
identity that kills the `:331` all-orthogonal over-claim); the `θ`↔`δ_AVE` tie is corrected to the taxonomic-not-derivational
analogue the leaf flags at `:310` (`δ_AVE` is a duty ratio, not `tan δ`), NOT written into canon as a phasor identity.

---

## Post-review repairs (2026-07-10, orchestrator-dispatched)

All 8 second-pass adversarial-review findings were **EVIDENCE-VOID** class: broken or imprecise receipts wrapped around
independently-true conclusions. Every conclusion survived; only the demonstration was repaired (receipt re-sourced, or command
made fireable). Each replacement receipt below was **run and verified in the pinned worktree before it was written into the
body** — the whole point of the repair pass is receipts that actually fire.

| # | Finding | Severity | Fix |
|---|---------|----------|-----|
| 1 | Anti-target caution mis-cited `topological-area.md:17` as calling `m*` an "arbitrary variable" and an SM-crutch rejection; the KB leaf is NEUTRAL (`:17`) and grants `m*` **peer** status (`:18`). | **MAJOR** | Re-sourced the rejection receipt to the *superseded vol-4 engineering register* (`19_silicon_design_engine.tex:52` + `doping_engine_map.svg:102` `line-through`); demoted the strength claim — the KB is the truth-source and its peer-with-SM reading governs. Operative core (never mint band-curvature `m*` as A1 mass; `d²E/dk²` stays sector-neutral dispersion; A1⊥T2 at `master-equation.md:20`) unchanged. |
| 2 | T4 absence receipt `grep -rwin "poisson\|\nabla\|field equation"` lacked `-E` → literal pipes → matches nothing against ANY corpus (vacuous). | MINOR | Replaced with `grep -rwinE …` + `rg -in …` cross-check, both verified to fire (control `acceleration\|mond` → 41 hits; the true absence-terms → 0). |
| 3 | T12 `rg 'u_0' vocabulary-register.md → 0` was false (2 hits at `:423,:681`, both `\mu_0` substring matches). | MINOR | Restated with `rg 'u₀\|u_0\*\|u_0\^'` → 0 relevant; named the two `\mu_0` substrings explicitly. |
| 4 | T4 "≈ 1.8 a₀" keyed to the empirical a₀=1.2×10⁻¹⁰ while the same sentence quoted the AVE-derived a₀=1.07×10⁻¹⁰. | MINOR | Tagged both denominators (≈ 2.0 a₀ AVE-derived / ≈ 1.8 a₀ empirical) with explicit ratios. |
| 5 | T4 mechanism asserted the unadjudicated Ax4 total-local keying in the indicative. | MINOR | Rewritten subjunctive (IF Ax4 keys on total-local amplitude, THEN external-field pre-saturation follows); parenthetical + kill-test kept. |
| 6 | T3 tier row + Cost line booked "SPLIT + ownership table" unconditionally; kill-test branch (i) (lossless-reflective) makes it a LANGUAGE RETRACTION instead. | MINOR | Annotated both the tier-row Resolution cell and the Cost line with the branch-(i)/branch-(ii) split. |
| 7 | T16 shipped "Shared mechanism: none" + "Kill-test: n/a" while the method required both for every candidate. | MINOR | Scoped method requirements (a)/(c) to P1/P8 (a clean P6 SPLIT legitimately has no shared mechanism — that is its polarity); flagged T16 as the sole ranked target with no fireable kill-test, explaining the 18-of-19 count by name not arithmetic. |
| 8 | (a) T2 said "FIVE-member" hysteresis §1 (it has SIX rows — omitted `op14-cross-sector-trading` / `clm-p2tp9i`); (b) T10 cited the anti-coincidence test at `:119` (now `:141-146`). | MINOR (2 nits) | (a) corrected to SIX-member, added `clm-p2tp9i`; (b) corrected to `test_graded_vacuum_network_isolation.py:141-146` (`test_anti_coincidence_Q_is_not_Z_radiation`), flagged canon's own stale `:119-124` cite at `vacuum-varactor-scatter-operator.md:188` for the next KB hygiene pass. |

**KEEP-BOTH — superseded MAJOR wording (finding #1, preserved for the audit trail; the body was corrected in place):**

> The curvature `m*` is **not merely undeployed — it is actively rejected as an SM crutch**: `vol6/period-3/silicon/topological-area.md:17` calls `m*` an "arbitrary variable"; the doping-engine figure renders "Effective Mass (m*)" struck-through.

Retained here (not in the body) because the branch is unmerged: in-place body correction **plus** logged supersession is the
right pattern (per the KEEP-BOTH discriminator convention), so the old wording stays visible even though the corrected body no
longer carries it.
