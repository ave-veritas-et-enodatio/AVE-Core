# Constituent-Cage-Ensemble — analytic legs (Leg 0 convention-freeze/sign-check + Leg 6 homogenization + the asymmetry paradox)

**Date:** 2026-07-20
**Class:** DERIVATION (analytic companion to the prereg `research/2026-07-20_constituent-cage-ensemble_prereg-FROZEN.md` and the driver `research/drivers/constituent_cage_ensemble.py`). Forms `[derived]`; canon inputs `[canon-read]` two-method at base HEAD `5287ef32`. Mints no `clm-`; propagates to no leaf.
**Scope:** Legs 0 and 6 are ANALYTIC (no verdict weight of their own beyond orienting/extrapolating); Legs 1–5 are the lattice-derived empirical legs (in the driver + result). This doc carries the `N→10⁵⁷` extrapolation (Leg 6) and states the asymmetry paradox (Fork W).

---

## §1 — LEG 0: convention freeze + sign check `[canon-read + derived]`

**The mechanical↔electrical impedance analogy (frozen).** stress↔voltage, velocity↔current ⇒ `Z = stress/velocity = ρc`. A reflection coefficient in the STRESS variable is `Γ_stress = (Z_L − Z_0)/(Z_L + Z_0)`.

**The rail shell = pressure-release (SHORT).** A cage is a shell where the local bond stiffness grades toward the rail (`S(A)=√(1−(A/A_yield)²)→0` as `A→A_yield`). The bulk impedance `Z_bulk = ρ c_P ∝ √(K) → 0` as the compression stiffness `K→0`. By the convention freeze and canon `master-equation.md:107` `[canon]` (cite currency-bumped `:105→:107` post-#769, Finding 12; content byte-unchanged) (`Z→0 ⇒ Γ→−1` "short-circuit"): **`Z_bulk→0 ⇒ Γ_bulk = −1 = SHORT = pressure-release (free surface)`**. A compression source against its own rail shell has an INVERTED image (Lloyd-mirror class) ⇒ its far-field compression MOMENT cancels (the candidate BIN-2 mechanism).

**The clamped shell = rigid (OPEN) — the control.** A kinematically-pinned shell (`u=0`) is a rigid wall: velocity→0 ⇒ `Z→∞ ⇒ Γ_stress = +1 = OPEN`. Its image is SAME-sign ⇒ far-field compression DOUBLES. (Displacement-variable dual: rail `Γ_disp=+1` = free-surface antinode; clamped `Γ_disp=−1` = rigid node. The driver's Leg-3 pulse measures `Γ_disp`; the STOP-gate reads the opposite-sign requirement between clamped and rail — CONFIRMED, both P and S.)

**Sign check (deliverables, confirmed by the Leg-3 impedance S-matrix).** `dc_P/dA<0` (saturation softens compression, index up); `dZ_bulk/dA<0`; `Z_bulk→0 ⇒ Γ_bulk→−1`. The railed-medium Bloch speeds (driver Leg 3): symmetric rail `c_P,c_S` both `∝√S_RAIL→0` with the ratio `c_P/c_S` FROZEN at `1.813` (degree-0 grade-lock, `electron-bh-isomorphism.md:38`/PR#521 `[canon]`); bulk-only rail (`k_a→rail`, `k_s` full) gives `Γ_bulk≫|Γ_shear|` (channel-asymmetric); `c_P` is finite ONLY at the shallow shipped `S_RAIL=0.03` — 🔴 **review-repair (Findings 0/5): `c_P → 0 ∝ √k_a` and `Γ_bulk → −1` as `S_RAIL→0` (`k_s` stores ZERO dilatation energy), with `c_S` finite. The "`c_P` stays FINITE = load-bearing fact for Leg 6" claim is INVERTED — see §3 Step 2.**

**★FLAG-DON'T-FIX (routed corpus tension — checked, NOT present).** Verify-before-cite at base HEAD `5287ef32`: `translation-circuit.md:119` reads `Γ=−1 saturation TIR boundary → **Short-circuit (Z→0)**`, CONSISTENT with the convention freeze; the "open-circuit" in that row is the OPPOSITE (electric `τ_yield`, `Γ=+1`) branch. The orchestrator-flagged contradiction (`Γ=−1 TIR → Open-circuit`) does NOT exist; no contradiction routed to the vocabulary register.

---

## §2 — The asymmetry paradox (Fork W), stated precisely with both canon cites `[canon-read]`

The BULK-ONLY constituent wall is a CANON ASSERTION whose grade-mechanism is UN-DERIVED. Two canon lines are in tension for a constitutive realization:

1. **The derived grade-lock (SYMMETRIC).** `electron-bh-isomorphism.md:38`/PR#521 `[canon]`: `ν`, Zener, `K/G` are *"homogeneous degree-0 in the bond stiffnesses … unshifted by saturation magnitude"* — the saturation kernel `S(A)` grades bulk AND shear TOGETHER (the absolute moduli collapse by the common factor `S`, the ratio frozen). `lattice-extreme-bh-rationality.md:37` `[canon]`: at the BH melt wall, *"`Γ_shear=Γ_bulk=−1` (`G_shear→0`, `c_bulk→0`)"* — CHANNEL-SYMMETRIC.

2. **The canon-asserted constituent wall (BULK-ONLY).** `electron-bh-isomorphism.md:26`/`:36` `[canon]`, verbatim: *"The electron's confinement is **bulk-channel** TIR (`Z_bulk→0 ⇒ Γ_bulk=−1` at the knot core)"* — CHANNEL-ASYMMETRIC (shear un-melted at the knot core; the electron interior preserves topology).

**The paradox:** there is NO derived mechanism producing a bulk-ONLY collapse via `S(A)` grading — `S(A)` grades everything together (line 1). The bulk-only wall (line 2) is asserted, its `(k_a, k_s)` route un-derived. The driver's `bulk_only` leg SURROGATES it (rail only `k_a`, keep `k_s`).

🔴 **Resolution [REVIEW-REPAIR, Findings 0/5 — the original clause "never a perfect `Γ_bulk=−1/Γ_shear=0`, because the srs channels SHARE `k_s`" is INVERTED, Rule 12].** The substrate does NOT refuse the channel-asymmetric wall: railing `k_a` alone drives `Γ_bulk → −1` (`−0.512→−0.9997` as `S_RAIL 0.03→0`) with `c_S` FINITE (`Γ_shear` saturates `−0.236`, NOT `−1`), because `k_s` stores ZERO dilatation energy. So **the canon bulk-only wall (line 2) IS constitutively realizable in the rank-2 bond model at the impedance level** — the asymmetry paradox resolves TOWARD canon. What REMAINS un-derived is the physical MODE/GEOMETRY mechanism by which a Cosserat-winding knot core grades `k_a` alone (the `S(A)` kernel of line 1 still grades both channels together — a surrogate `k_a`-only rail is what expresses the wall here, not a derived saturation mode). ~~and the substrate then adjudicates whether a channel-asymmetric wall is even constitutively realizable (Leg 3: partially — `Γ_bulk≫|Γ_shear|` but never a perfect `Γ_bulk=−1/Γ_shear=0`, because the srs channels SHARE `k_s`).~~ **This is the A1⊥T2 sector-ownership question — which impedances collapse at a Cosserat-winding knot core — routed to Grant; NOT picked here.** Per the frozen bins, because the COMPRESSION outcome FLIPS between rail depths (shallow radiates, deep closes `c_P`) in a verdict-controlling way, the paradox now DOES control the verdict (BIN-3/Fork-W) ⇒ REOPENED.

---

## §3 — LEG 6: analytic homogenization — the `N→10⁵⁷` extrapolation `[derived]`

**Setup.** A real gravitating source (neutron star) is ~`10⁵⁷` constituent solitons at NUCLEAR density — the cages are NEAR-SPACE-FILLING, NOT dilute isolated Lloyd mirrors (`φ→0`). **Packing arithmetic (added per Finding 4; explicit assumption tagged):** at saturation `n_sat = 0.16 fm⁻³` the inter-nucleon spacing is `n^(−1/3) ≈ 1.84 fm`, so with a core radius `r_core ≈ 0.84–1.0 fm` the packing fraction is `φ = (4π/3) r_core³ n ≈ 0.40–0.67` — `O(1)`, near-space-filling (higher at the several-× saturation densities of NS interiors). ★**ASSUMPTION (un-derived, tagged):** this identifies the saturated cage radius `r_cage` with the full nucleon extent (`~fm`); no cited canon fixes the core scale (`electron-bh-isomorphism.md:26` says "at the knot core" only qualitatively). IF `r_core ≪ fm` then `φ ≪ 1` and the dilute limit is physical — but this is now secondary (Findings 0/5 show the space-filling limit does not save BIN-1 either). The relevant limit is the effective-medium of a DENSELY-loaded cage network, and the `k·r_core→0` deep-quasistatic limit (`k·r_core~10⁻²⁵` in the real system; `~O(1)` on the lattice).

**Step 1 — which effective-medium limit governs (dilute Lloyd vs space-filling).** The walk's image-cancellation (Lloyd) mechanism is a DILUTE-limit statement: an ISOLATED source against its own `Γ_bulk=−1` shell has its far-field compression moment cancelled by its inverted image, residual `∝(k·r_core)²`. This is the `φ→0` limit. But constituents are `φ→1` (space-filling). In the space-filling limit the object is NOT a sum of isolated Lloyd mirrors; it is a homogenized medium whose EFFECTIVE constants set the radiation. **Which limit is physical decides the fork.** For `φ→1`, homogenization (Step 2), not isolated Lloyd, governs.

**Step 2 — [CONCLUSION-WRONG — REVIEW-REPAIR 2026-07-20, PR#770, Findings 0/5 CRITICAL].**

🔴 **The self-labeled "load-bearing result" below the strike is INVERTED (Rule 12, body preserved).** The claim was "`K_eff` is propped FINITE by `k_s` ⇒ `c_P,eff` stays finite as `S_RAIL→0`, `Γ_bulk` saturates `−0.5…−0.8` NOT `−1`." **The lane's OWN `run_c2_speeds`, continued one decade past its own scan, says the OPPOSITE:** bulk-only rail (`k_a→S_RAIL·k_a`, `k_s` full) gives `c_P = 0.167 / 0.063 / 0.012 / 0.0012 / 0.00007` and `Γ_bulk = −0.512 / −0.784 / −0.956 / −0.996 / −0.9997` at `S_RAIL = 0.03 / 0.003 / 1e-4 / 1e-6 / 0` — `c_P → 0 ∝ √k_a` with NO floor, `Γ_bulk → −1` — while `c_S` stays FINITE at `0.177` (`Γ_shear` saturates `−0.236`). **The algebra:** under uniform dilatation `ε`, every bond strain `du_b = ε·d_b` is PARALLEL to `d̂_b`, so the transverse projector annihilates it — `(I−d̂⊗d̂)du = 0` — and the shear springs `k_s` store EXACTLY ZERO dilatation energy. `k_s` does NOT prop up the bulk modulus `K`; `K_eff ≤ K_affine ∝ k_a → 0` (sublattice relaxation only lowers it). Corroborating tell already in the shipped data: railed `c_P/c_S = 0.769 < √(4/3) = 1.155` ⇒ `K` collapsing below `G`, the OPPOSITE of "`K` propped finite." ⇒ **a space-filling bulk-only-caged medium does NOT keep `c_P,eff` finite; it closes the compression channel (`c_P,eff→0`) with shear alive** — realizing the canon channel-asymmetric bulk-only wall (`electron-bh-isomorphism.md:26`). The "0.17 finite / saturates NOT `−1`" read was a scan-truncation artifact (ONE rail depth, `S_RAIL=0.03`; "saturates" asserted, never scanned).

~~A space-filling BULK-ONLY rail = the srs medium with `k_a→S_RAIL·k_a`, `k_s` FULL, everywhere. Its effective bulk modulus `K_eff` is NOT zero: on the srs net the bulk modulus has contributions from BOTH the central `k_a` AND the transverse `k_s` (diagonal bonds' transverse stiffness contributes to compression resistance). Railing only `k_a` leaves `K_eff` FINITE — **propped up by the shear springs `k_s`.** Lattice validation (driver Leg 3 impedance S-matrix, space-filling railed Bloch speeds): bulk-only rail gives `c_P,eff = 0.17` (finite; only `~3×` below cold `0.52`) even at `S_RAIL=0.03`, and stays finite as `S_RAIL→0` (`Γ_bulk` saturates near `−0.5…−0.8`, NOT `−1`). By contrast the SYMMETRIC rail gives `c_P,eff = √S_RAIL·c_P,cold → 0`. ⇒ **A space-filling bulk-only-caged medium still PROPAGATES compression** (`c_P,eff` finite); only the symmetric rail freezes it.~~ [RETRACTED — false; `k_s` stores zero dilatation energy, `c_P→0` as `S_RAIL→0`.]

**Step 3 — [REVIEW-REPAIR: the BIN-1 conclusion does NOT follow — Findings 0/5].**

🔴 **The step below the strike is retracted (Rule 12, body preserved): its premise "`c_P,eff` is finite (Step 2)" is false.** The correct picture: a space-filling bulk-only rail closes the compression channel (`c_P,eff → 0` as `S_RAIL→0`) with shear alive — precisely the channel-asymmetric structure the image-cancelled-sum route (BIN 2) needs. So the trilemma "BIN 2 needs `c_P,eff→0` ⇒ SYMMETRIC rail ⇒ kills shear ⇒ BIN 3 forbidden" is BROKEN: `c_P,eff→0` is ALSO reached by the deep BULK-ONLY rail with `c_S` finite. You CAN open a compression-only stop; the shear springs do NOT prop up the bulk modulus. Whether the ensemble then radiates or cancels is rail-depth-conditional (Fork-W verdict-controlling) — REOPENED, NOT a clean BIN-1.

~~The mass energy density `ρ(x) = |A(x)|²` (`master-equation.md:20` `[canon]`, `mₑc²`=trapped compression energy) is unchanged by caging (the cages TRAP the energy; they do not remove it). An orbiting ensemble has a rotating mass quadrupole `Q_ij ∝ ∫x_ix_j ρ` at `2Ω`. Because `c_P,eff` is finite (Step 2), the compression channel is a gapless propagating branch the quadrupole couples to — the SAME structural coupling #767 found at the smooth-source level, now surviving homogenization. **The far field radiates the COARSE-GRAINED mass texture at the q1 partition ⇒ BIN 1.** The image-cancelled-sum route (BIN 2, i) requires `c_P,eff→0` (no compression channel), which needs the SYMMETRIC rail — and that also freezes `c_S,eff→0` (kills the observed GW) ⇒ BIN 3, forbidden by observation. **This is the constituent-level restatement of #767's `K=2G` grade-lock: you cannot open a compression-only stop, because the shear springs prop up the bulk modulus.**~~ [RETRACTED — the bulk-only rail DOES open a compression-only stop; see Step 2 review-repair.]

**Step 4 — what the lattice validates, what it CANNOT establish (honest scope) [REVIEW-REPAIR — Findings 0/2/5].**

🔴 **Corrected reading (Rule 12, original preserved below the strike):**
- **Validated by the lattice:** (Leg 3, review-repair ladder) the bulk-only railed medium drives `Γ_bulk → −1` with `c_P → 0` and `c_S` FINITE as `S_RAIL→0` — a channel-asymmetric compression stop IS realizable (the canon bulk-only wall). (Leg 5, review-repair rail ladder) `ρ_N` rises `N2→N8` ONLY at the shallow shipped `S_RAIL=0.03`; at deeper (more physical) rail it PLATEAUS `~0.3` and goes flat-to-FALLING `N4→N8` — the coarse-grained-texture emergence is rail-depth-conditional, NOT robust. (Leg 1/2) a single bulk-only cage gives a PARTIAL compression seal at the scanned depths (the frozen BINARY charged-line discriminator survives; the `0.65` figure is unconverged — result §2, Finding 3).
- **NOT establishable from an `L=20` lattice:** the isolated deep-quasistatic (`k·r_core→0`) single-core Lloyd seal (lattice `k·r_core~O(1)`), AND the deep-rail (`S_RAIL→0`) frozen homogenization the review-repair only characterized post-hoc. **Both residuals are LIVE**: at deep rail the space-filling bulk-only medium closes `c_P` (BIN-2 shape), so the "space-filling argument saves BIN-1" claim is false; the compression outcome depends on the un-derived wall depth ⇒ Fork-W verdict-controlling ⇒ REOPENED. The decisive adjudicator is the deep-rail frozen-first program (result §8.2).

~~- **Validated by the lattice:** (Leg 3) the bulk-only railed medium has finite `c_P,eff`, propped by `k_s`; the symmetric rail freezes both; the channels SHARE `k_s` (no clean bulk-only wall). (Leg 5) `ρ_N` (caged/uncaged compression) RISES toward 1 as cages pack (`N=1→8`), ROBUST across rail depth (`S_RAIL 0.03→0.003`) — the finite-`N` manifestation of the coarse-grained texture emerging. (Leg 1/2) a single bulk-only cage gives only a PARTIAL compression seal (`~25–75%` leak), deepening with rail but never full, and shear stays entangled.~~
~~- **NOT establishable from an `L=20` lattice:** the isolated (`φ→0`) single-core deep-quasistatic (`k·r_core→0`) Lloyd seal — the lattice is at `k·r_core~O(1)`. **But this is the DILUTE limit, which Step 1 argues is NOT the physical (space-filling) limit for constituents.** The load-bearing space-filling argument (Steps 2–3, finite `c_P,eff`) is `k·r_core`-INDEPENDENT (it is a statement about the effective bulk modulus, validated by the railed Bloch speeds), so the BIN-1 conclusion does NOT rely on reaching `k·r_core→0`. The honest residual: IF the physical constituent packing were dilute (`φ≪1`) AND the wall were a perfect `Γ_bulk=−1/Γ_shear=0` (which the substrate refuses, Leg 3), the deep-quasistatic Lloyd seal COULD suppress — but neither antecedent holds (constituents are space-filling; the clean bulk-only wall is not constitutively realizable).~~ [RETRACTED — the clean bulk-only wall IS realizable at deep rail; both residuals are live.]

**Leg-6 verdict contribution [RE-BANKED — Findings 0/2/5]:** 🔴 the original claim (the homogenization carries the extrapolation to BIN-1 via a finite-`c_P,eff`/`k_s`-propped argument = `K=2G` grade-lock) is CONCLUSION-WRONG. The review-repair `S_RAIL` ladder shows the space-filling bulk-only medium CLOSES `c_P` (`→0 ∝ √k_a`, `Γ_bulk→−1`) with `c_S` finite, and `ρ_N` does NOT rise robustly across rail depth (plateaus `~0.3`, falling `N4→N8`). So the homogenization does NOT carry the extrapolation to BIN-1; the compression outcome is rail-depth-conditional and Fork-W is verdict-controlling ⇒ the fork is REOPENED. The decisive adjudicator is the deep-rail frozen-first program (result §8.2). ~~the homogenization carries the extrapolation to BIN 1 via the finite-`c_P,eff` (space-filling, `k_s`-propped) argument — the `K=2G` grade-lock at the constituent level — validated by the lattice railed-medium speeds and the `ρ_N`-rising aggregation trend, with the dilute-Lloyd (`φ→0`, `k·r_core→0`) route dismissed as the non-physical limit for space-filling constituents.~~ [RETRACTED.]
