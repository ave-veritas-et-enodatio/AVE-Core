# Electron-Existence Keystone — Chord/Echo Discrimination Pre-Reg

**2026-06-16 · banked BEFORE the Stage-1.5 two-grid build · gates what a 𝓜/𝓠/𝓙 PASS is worth**
**Source:** workflow `wenqg7x94` (6 agents; 2/3 refute lenses confirm, conf 0.86/0.90; the 3rd's reasoning corroborates).

## Verdict
The 𝓜/𝓠/𝓙 **magnitude** test is **ECHO**. The **only chord axis is EMERGENCE** of the (2,3)/Γ=−1 FORM from a precursor.

## Per-observable (grep-confirmed @ origin/main)
- **𝓜 = m_e — ECHO (definitional).** ℓ_node ≡ ℏ/(m_e c) is a calibration INPUT (one of {m_e, α, G}); "M=m_e out" is a consistency check, NOT a derivation (`electron-identification.md:50,64`; `full-derivation-chain.md:42`). Zero AVE-distinct value content.
- **𝓠 = e — PARTIAL chord.** Bare |Q|=1 is topological-generic (true of ANY single closed-loop soliton); the AVE-distinct content is the lattice-PINNED quantization + chiral sign + Z₃ fractional ladder (Burgers vector on K4, I4₁32 — `axiom-definitions.md:31`), NOT |Q|=1 itself.
- **𝓙 = ℏ/2 — STRONGEST chord, MIXED.** Spin-½ via K4→A4→2T⊂SU(2) + Cosserat micro-rotation (Axiom 1) + Finkelstein-Misner — BUT the SU(2)/Pauli LANGUAGE + the F-M THEOREM are IMPORTED scaffolding; pure T=A_4 does NOT yield ½ without the A↔B sublattice swap (`k4-rotation-group.md:123,136`).

## The α = 𝓜+𝓙+𝓠 decomposition it rests on — ECHO
Class-B named identification, explicitly NOT Class-2 (`ch8-alpha-golden-torus.md:135,152`). The additive orthogonality is **asserted-not-derived**; the **Λ_vol ≡ 4π·Λ_surf collinearity** is confirmed (the (R,r,d)→Λ map has only 2-D image, `ch8:148` / `claim-quality.md:97`); the 1+2+3 codim ordering has **NO substrate Hilbert-space realization** (T_d gives A₁⊕T₂ = 1+3, not 1+2+3 — `ch8:150`). The boundary-observable reading **RE-LABELS the same formula** (`boundary-observables-m-q-j.md:70` "the dimensional reading *of that same formula*"). Consistent with the already-ratified α-echo-at-value.

## What a PASS means — the discriminator (ave-discrimination-check)
- **Magnitude-only PASS (read m_e, e, ℏ/2 off a region) = ECHO.** Consistent with BOTH AVE + SM; m_e is an input; the α-value is already echo. **The current genesis suite (`electron_genesis_finish.py`) measures EXACTLY this — a PLANTED ansatz (B2 planted-persistence), NOT emergent formation** (`2026-06-08_electron-genesis-finish-adjudication.md:11`).
- **CHORD = the ALPHA-FREE EMERGENCE lane.** Does the (2,3) winding + Γ=−1 wall + finite **α-free Q** SELF-GENERATE from generic/precursor IC, **WITHOUT planting** (CP8) **AND WITHOUT inserting α** (inserting α converts it to a calibration model — `2026-06-07_electron-genesis-observer-bridge-prereg.md:30`)? **This discriminating lane was NEVER EXECUTED** — "no current engine simultaneously carries the bound-state dynamics AND the bond-phasor/reactance observer" (`:117`). The Stage-1.5 two-grid build is exactly the engine that would close that gap.

## Pre-registered success criterion for the Stage-1.5+ build
The multi-week two-grid-reconciliation build is **worth its cost ONLY IF scoped to the α-free emergence lane:**
1. The (2,3) winding + Γ=−1 confinement **self-FORM from generic IC** (no planted (2,3)). — CP8
2. **WITHOUT α inserted anywhere** (α-free) — else it is a calibration model, not emergence.
3. **Measure the α-free Q** (does ~137 emerge WITHOUT being told). — the form-chord readout.
4. The **Z→0 longitudinal confinement** (the lane's Stage-1.5 gate) is **necessary but NOT sufficient** — it gates trap-FORMATION, not emergence.

**Reading m_e / e / ℏ/2 off the region is NOT the success criterion** — m_e is an input, |Q|=1 is generic, and the magnitudes re-derive the echo.

## Meta-frame (why the build is still worth it)
This is the **FORM-deriving / VALUE-importing meta-finding at the keystone**: the electron's **FORM self-generating** (the (2,3)/Γ=−1 emerges) is the **chord** — the deepest FORM-chord (the foundational object self-assembles); its **VALUE** (m_e, α=137) is **echo** (input + already-adjudicated). The build advances the framework **iff it tests FORM-emergence, not value-readout.**

## Carries into the Stage-1.5 brief (Rule-11 prereg-freeze)
- Success = α-free winding-emergence + Z→0, NOT magnitudes.
- R10 (anhysteretic↔loop / remanence) is the SEPARATE retention wall (does it STAY) — parked.
- Cross-refs: `wenqg7x94` audit; the 2026-06-07 emergence-bridge prereg; `boundary-observables-m-q-j.md`; the α-keystone-echo ruling.
