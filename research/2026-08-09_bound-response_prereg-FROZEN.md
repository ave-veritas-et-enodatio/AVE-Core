# THE BOUND-RESPONSE CARVE (R38) — PRE-REGISTRATION (FROZEN)

**Date:** 2026-08-09 · **Branch:** `lane/2026-08-09-bound-response` · **Base:** `origin/main` @ `9230f434`
**Lane brief:** [`_orchestration/2026-08-09_bound-response-brief.md`](../_orchestration/2026-08-09_bound-response-brief.md) (Grant GO, R38 — [`2026-08-09-ruling-r38-bound-response.md`](../_orchestration/docket-entries/2026-08-09-ruling-r38-bound-response.md)).
**Class:** DERIVATION + CONSUMER-AUDIT lane (analytic; audit receipts by two named engines; small algebra driver). **Mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; edits no KB leaf, register, ledger, or ruling; changes no solidity; all propagation ROUTED.** Engine `src/ave` byte-untouched (read-only imports where a receipt needs a constant).
**This document commits ALONE and pushes FIRST (freeze-by-push), before any derivation content, audit output, driver code, or lane-produced number exists on this branch.**

---

## §0 — Standard Vacuum Analysis header (SVA v0.2-pilot; all 11 rows)

 1. **SECTOR / OWNERSHIP:** Object under test = the **A1 bulk/dilatation SLOT** of the graded-vacuum four-channel map (`port-register.md` §1 channel 3) — NOT the A1 irrep itself (the K4 `V_4-port = A1 ⊕ T2` decomposition is canon, `clm-j550uh`/`clm-9kd2t3`, untouched), and NOT the T2 shear/EM channels (photon + observed GW, untouched at their banked rates), and NOT the Cosserat micro-rotation channel (gapped, untouched; the rotational fence-reach STOP from the brief `:57` is active — if any step routes bulk-slot content through the micro-rotation channel, STOP and route to Grant). TWO-"3"s guard active (A1 dilatation-MASS ⊥ Cosserat (2,3) WINDING-charge; `master-equation.md:20`). The carve under test assigns the A1 slot **controlled-source class** (R38 term of record: *bound response field* — "no autonomy, no independent flux, no port; bound to the sectors that drive it"). Sector-ownership consequence declared up front: **mass = A1 stays the accounting** (how much back-reaction is sourced); the response field's **driver inventory** = the matter sector's dilatation charge (the enclosed-flux monopoles receipted at #930 §1.5.0) + the T2/Cosserat configurations that load it — enumerated in the result, not assumed.
 2. **REGIME / PHASE-STATE:** The carve is a **COLD-LINEAR constitutive statement** (Regime-I, sub-yield, lossless-reactive). PAST-WALL states (`r < r_sat`, electron interior, Regime-IV rupture) are OUT OF SCOPE by Ax1 phase-scope: no claim in this lane evaluates any constitutive law past a wall, and the carve asserts nothing about what the saturated phase does. Wall-boundary statements (Γ rows, the ruled kernel-collapse mirror) are COLD-SIDE objects and are consumed as banked. DC bias grade `A(r) = r_sat/r` = the operating point (canon); the two-variable split is load-bearing and declared: the DC bias/grade field and the dynamical displacement dress are DIFFERENT objects (#919 §3.3 / #930 §1.4 carried this split; this lane keeps it).
 3. **CIRCUIT STATEMENT:** In circuit terms, BEFORE any framework word: the question is whether the vacuum network's **common-mode (A1) node variable is a STATE variable or a DEPENDENT variable**. A state variable has its own L-C pair (independent flux + charge; can oscillate; presents a port; `Re(Z) > 0` reachable). A dependent variable is fixed by the sources and the differential sectors' configuration (a controlled source: no independent flux, no port, `Re(Z) ≡ 0`; stores/loads reactively; its energy bookkeeps in the driving branches). Canon's own half-statement, generalized under test (R38): `port-register.md:35` — the halo "stores/loads, added-mass … **NOT a port** (a port *drains*; the near-field *stores-and-returns*)" → the bulk is NEVER a port. Total-vs-slot: the comparator (`Ṗ_b`) reads the TOTAL source energy budget (inherited SVA row 3 of the #919 prereg via #927/#930); the carve's prediction re-partitions that total to shear-only.
 4. **PLANE & PROJECTION:** No signed Γ is consumed anywhere in this lane. Wall rows enter only through their sign-invariant `|Γ| = 1` / zero-transmission content (the ruled kernel-collapse text, wall-taxonomy §10.2), exactly as #930 consumed them. The consumer audit READS signed-Γ sites (vent/jam leaves) as provenance exhibits, never as inputs. The kill-chain re-run's plane: the far-field radiation zone (the #927/#919 plane) for flux statements; the last-bond plane for seal statements (banked, #930).
 5. **CONSTITUTIVE PROVENANCE (each law tagged):** `K = 2G` — **IMPORTED** (GR trace-reversal; PR #261 two-gate NEGATIVE, Grant-ratified: "Imported, both legs. End of line."; `program-arc-map.md:114-119`; `form-deriving-value-importing.md:87`). `G_vac = ρc²` — **MATCHING CONDITION at value level** (LC-1(a), branch `research/lc1-one-speed` @ `be04ea03`, result §1.1: "G is assigned the value that makes √(G/ρ) = c true"; cited as branch state). The elastic W's bulk term `½K(∇·u)²` — rides the K import (`cosserat-mass-gap.md:30` hosts the W; #761 result `:14` names the term). The axiom-level dynamics — **CANON**: `eq_axiom_3.tex` `L_node = ½ε₀|∂_t A_n|² − (1/2μ₀)|∇×A_n|²`, **curl-only potential**, "the standard Maxwell Lagrangian … recovered as the substrate's effective action in the linear regime". `ν_vac = 2/7` — **IMPORTED-DOWNSTREAM** of K=2G (`one-seventh-impedance-projection.md:18`: "takes ν=2/7 as input … GR-imported via K=2G"). The seismic partition `(2/3)(c_S/c_P)⁵` — **IMPORTED** (Aki-Richards class; `q1-pulsar-hardening.md:215` "[import], external anchor"). `r_sat = 7GM/c²` — GR-imported /7 chain (`clm-law1ho`, 0.55, input-only). The carve itself — **WALK-LEVEL premise under test** (R38), to be derived or refuted here.
 6. **ENERGY LEDGER:** Under the carve the bulk slot is rim-only (within-system reactive; stores-and-returns). The legal ports are unchanged: T2 far-field radiation (EM + GW at their banked rates), detector terminations, Regime-IV rupture (out of scope). The carve's testable ledger consequence: **zero boundary-crossing arrow on the compression line, for every source and every frequency, in the cold-linear regime** — not because a wall seals it (#930's seal, banked, interior content only) but because the channel has no port to cross. No loss word is used without its port named. Mass-energy accounting: the bound field's energy density is real and stores in the configuration (reactive), exactly as the halo row already books it (P9).
 7. **CALIBRATABILITY:** The verdict is STRUCTURAL (a classification + a re-derivation + a re-read of banked kills), not a new measured number. Where numbers appear they are (a) dimensionless ratios already banked (comparator residuals `δ_HT = 0.0016`, `δ_DP = 1.3×10⁻⁴`, frozen at the #930 prereg §4 by same-day source re-retrieval at base `a510ec2b`, re-quoted not re-fetched; the #919 floor family, quoted as SUPERSEDED-INPUT record), or (b) algebraic identities re-verified by the driver (ν(K=2G) = 2/7; c_P/c_S at K=2G = √(10/3); at K=0 = √(4/3); the LC-1 receipt that superluminality is G-driven, not K-created). The carve's own flux prediction is an identity: `F_bulk ≡ 0` on the compression line (no channel), hence trivially `< δ_DP` — the comparator's role is to adjudicate the FORK (compressible-import reading vs bound-response reading), and it already has: the compressible reading is the one carrying the LIVE 9–110σ exclusion.
 8. **DISCRIMINATION CLASS:** DC-internal constitutive claim (the ontology of the A1 slot) read out through an existing DC→AC channel (the pulsar `Ṗ_b` budget — liveness-proven, the banked comparison). **No chord is claimed on any outcome**: shear-only quadrupole radiation at the Peters–Mathews rate is exactly GR's prediction (peer-with-SM; consistency class). The carve's value is INTERNAL-COHERENCE + kill-status truth-content, per the truth-over-chords register. SM counterfactual run: GR also has a constrained scalar sector (the longitudinal metric parts are constraint-fixed, not radiative — `q1-pulsar-hardening.md:130` states the parallel); the carve makes AVE's bulk slot GR-CLASS on this axis, which is the OPPOSITE of distinct — stated plainly. Tautology filter: the carve is NOT a restatement of the halo row; it generalizes it from one configuration (the halo) to the channel's class, and the generalization is exactly what #761/#919/#930 pressure-tested from the other side.
 9. **CERTIFICATION PLAN:** gate battery frozen in §7 before any derivation/audit content exists (G-QUOTE, G-CARVE-2M, G-CONSUMER-2M, G-STATIC, G-KILL-3, G-PROV-4, G-SCALAR-REAL, G-SHEAR-UNTOUCHED, G-RECON, G-ENGINE-FLAG, G-NUM). UNRUN ≠ PASSED; every gate run and scored in the result. Negative control named in advance (G-CONSUMER-2M): the sweep must POSITIVELY find the known propagating-claim sites (`master-equation.md:26`, `port-register.md:49` radiative column, `cosserat-mass-gap.md:132`, `gw-propagation-lossless.md:48`) — a sweep that misses the known-positive set is a dead instrument, and no absence claim banks from it.
10. **ADJUDICATION ROUTING:** Verdict (one of the three frozen bins, §2) + the consumer-audit table + the kill-chain readings route to the orchestrator. The #930 walk-back's manuscript demotion sweep HOLDS until this lane returns (R38: "one sweep, the complete arc, not two") — this lane's result FEEDS that sweep; it executes none of it. #913/#919/arc-scope/#770 stay HELD (R24/R34 dispositions; nothing here dispatches them). The constitutive revision, if DERIVED, is proposed as a **versioned supersede for Grant's ratification** (R38's grammar) — no leaf, register, ledger, or ruling edit on ANY outcome. Engine implication FLAGGED only (§7 G-ENGINE-FLAG). PR opens `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; the lane runs its own Tier-2 (ave-adversarial-pr-review) before anything reads CLEARED.
11. **NUMERICAL CONDITIONING:** Named engines: `sympy`/plain-Python arithmetic for the algebra receipts; GNU `grep -E` + Python `re` (both named per invocation, patterns printed in the result) for every sweep/absence claim — the `**`-pathspec and `$..$`-escaping false-negative classes guarded; every "0 hits / complete" claim cross-checked by the second engine; the known-positive negative-control set (row 9) gates instrument liveness. No RNG anywhere. Driver + gating number check (`--mutation-receipt`) wired into `make verify`, run in THIS worktree (worktree-aware PYTHONPATH discipline per repo CLAUDE.md).

---

## §1 — The frozen deliverables (verbatim from the brief; the brief is the frozen source)

`_orchestration/2026-08-09_bound-response-brief.md:15-37`, verbatim `[sic]` (section heading at `:13`):

> 1. **THE CONSUMER AUDIT:** every canon site consuming the bulk sector AS A
>    PROPAGATING WAVE (the √(10/3)c radiative rows, the √2c port rows, Regime-IV
>    mechanical readings, V_snap's mechanical vs electrical reading, the seismological
>    partition rows) — enumerate with verbatim quotes; classify each under the carve:
>    SURVIVES-AS-RESPONSE / DIES-WITH-THE-PHANTOM / NEEDS-RE-DERIVATION. Two-method
>    receipts, named engines.
> 2. **THE STATIC SECTOR RE-DERIVATION:** the gravity sector's banked successes
>    (the dress, gravity-as-refraction, the 7GM couplings, perihelion/light-bending)
>    re-derived under the constraint (∇·u slaved exterior; the dress as the native
>    incompressible solution). The carve FAILS here if any banked success consumed a
>    propagating bulk mode — name it if so.
> 3. **THE KILL-CHAIN RE-RUN:** LC-1's longitudinal branch, #919/#930's residue, and
>    the pulsar comparison under the carve. Expected: the branch is a constraint (no
>    energy-carrying channel — the frozen LC-1 kill condition un-fires), the residue
>    has no port, and the prediction is shear-only radiation = the observed Ṗ_b.
>    State each explicitly against its frozen wording.
> 4. **THE PROVENANCE CASE:** show the phantom's paper trail — K = 2G GR-imported
>    (#261), the seismic partition imported (Aki-Richards, valid for compressible
>    rock), FLAG-CEFF's four forms, FLAG-W's contradictions — the carve must EXPLAIN
>    the corpus's bulk-sector incoherence, not just delete it.
> 5. **THE ENGINE IMPLICATION (flagged, not executed):** what the constraint does to
>    the coded functional (∇·u as multiplier; the A1 accounting re-homed to the
>    response field; which tests/gates would move). Architecture note only.

## §2 — The frozen verdict grammar (verbatim from the brief) + the banked kill chain at its frozen homes

`_orchestration/2026-08-09_bound-response-brief.md:41-48`, verbatim `[sic]` (section heading at `:39`):

> - **BOUND-RESPONSE-DERIVED** — the carve is consistent with every banked success,
>   explains the incoherence, and un-fires the kill: the constitutive revision is
>   proposed as a versioned supersede (Grant ratifies; the demotion sweep then writes
>   the COMPLETE arc).
> - **BOUND-RESPONSE-INCONSISTENT(site)** — a banked success genuinely consumed the
>   propagating bulk mode: name it verbatim; the carve dies there and the walk-back
>   proceeds without it.
> - **NOT-DERIVABLE** — the adjudicating structure is missing; enumerate.

**The banked kill chain, quoted at its frozen homes (fence: #761/#919/#927/#930 are BANKED — not relitigated; this lane changes the PREMISE they consumed and shows receipts):**

- **LC-1 kill cell** — `_orchestration/2026-08-04_lorentz-compliance-arc-brief.md:44`, verbatim `[sic]`: *"An energy-carrying inter-event channel at ≠ c ⇒ arc-level kill"*; `:59`: *"LC-1 runs first and its kill condition is arc-terminating."* (The #913 R5 authority: the ARC BRIEF's cell, never the dispatch's "a detector reads" gloss.)
- **#761 (merged `caa51c17`)** — `research/2026-07-20_mechanical-commonmode-derivation_result.md:8`, verbatim `[sic]`: *"The frozen-bin verdict is **NONE-DERIVES**. **No cold-regime derived closing mechanism exists, and the elastic-solid structure of the AVE vacuum actively FORCES the opposite** (a radiating longitudinal/dilatation wave)."* And `:14`: *"The EM Gauss-kill rests on a **curl-only** potential energy (`eq_axiom_3.tex`: `L_node = ½ε₀|∂_t A|² − (1/2μ₀)|∇×A|²`) that gives the EM longitudinal `∇·A` NO restoring force (gauge/non-dynamical). The **mechanical** dilatation `∇·u` has a genuine **bulk restoring force** `½K(∇·u)²` with `K ≠ 0` (…`K = 2G` is a GR-imported value per `srs-band-structure.md:116` / PR #261, but only `K ≠ 0` is load-bearing…) ⇒ it is a **propagating radiative wave**, not a gauge mode. Carrying the EM kill to `∇·u` would require deleting the bulk term — which destroys `K = 2G`, the P-wave, and the solid-crystal identity of Axiom 1."*
- **#919 (branch `research/a1-port-sourcing` @ `ba1d872d`, OPEN, HELD per R24)** — the floor: result `:220` `F_bulk/F_shear ≥ (2/3)(c_S/c_P)⁵`; the uncaged bracket `[0.0152, 0.0455]` = 9.5–28.5σ HT / 117–350× DP (result `:74-77`); verdict `RADIATIVE(2)-on-the-order / NOT-DERIVABLE-on-the-amplitude` (R23). Anchor premise CONTESTED-BY-GRANT (R24, `_orchestration/docket-entries/2026-08-07-rulings-r23-r27.md`: *"They seem different. A GW is a saturation wave…"*).
- **#930 (merged `bd9ee715`)** — `research/2026-08-09_screening-theorem_result.md:19`: verdict `SEAL-DERIVED + RESIDUE-EXCEEDS`; CH-0 = the enclosed-charge quadrupole at `F_res = κ_env² = (2/3)(c_S/c_P)⁵ = 0.032863 = 252.8× δ_DP / 20.5σ_HT`; §6: *"the sealed arm does NOT rescue it"* — the kill's live status reverted to the #919 two-branch family.
- **The premise this lane changes** — R38 (`_orchestration/docket-entries/2026-08-09-ruling-r38-bound-response.md`): *"the substance has no bulk degree of freedom; the apparent compression sector is the BACK-REACTION"*; *"What dies: the free-running P-branch — an artifact of the coded compressibility (K = 2G, GR-imported per #261, minted the phantom channel)."* Grant's carve verbatim: *"The material of the vacuum doesn't the back reaction does?"* — Grant's GO: *"Let's maybe use a different term than 'slaved' but let's derive."*

---

## §3 — The derivation's frozen shape (two independent routes; gate G-CARVE-2M)

**The question, stated honestly.** "derive whether the receipted structure admits — or forces — the bulk slot as a **bound response field**" (brief `:3-4`). Three findable outcomes are live: the structure FORCES the carve (the strongest), the structure ADMITS the carve while also admitting the compressible-import reading (in which case the banked pulsar comparison is the discriminating measurement between the two admissible readings — substrate-adjudicates-forks, and the exclusion already ran), or the structure cannot adjudicate (NOT-DERIVABLE). The result states which was reached and does not upgrade ADMITS+SELECTED to FORCES.

**Route (a) — the axiom/provenance route (substrate-native, EE-first).** Walk the receipted derivation chain for the translational sector's dynamics and locate every element that makes the longitudinal projection DYNAMICAL rather than BOUND:
  1. The canon axiom dynamics (`eq_axiom_3.tex`) is **curl-only**: `L_node = ½ε₀|∂_t A_n|² − (1/2μ₀)|∇×A_n|²`. A curl-free (longitudinal) configuration has NO potential term — no restoring force, no oscillation, no wave; with sources it is the Gauss-constraint sector (bound to charge). This is #761's own step, banked.
  2. The ONLY structure that ever gave `∇·u` a restoring force is the mechanical elastic `W` (`cosserat-mass-gap.md:30`) with its bulk term `½K(∇·u)²` — and canon's adjudicated position (`vocabulary-register.md:870`) is that the u-vs-A difference is *"**constitutive**, not kinematic"*: same Helmholtz split, the difference IS the K-term.
  3. The K-term's provenance is CLOSED: #261, two gates, Grant-ratified — *"Imported, both legs. End of line."* NOT crystalline, NOT constitutively forced, GR trace-reversal. And LC-1(a): even `G_vac = ρc²` is a matching condition at value level. The srs "lattice-computed P-branch" (`clm-bnd5rq`) is computed FROM the imported moduli (`src/scripts/vol_1_foundations/srs_vector_band_survey.py` consumes `K_bulk`, `G_Hill` as inputs) — it is the import's echo, not an independent derivation.
  4. Therefore the derivation question reduces to: **does ANY receipt, independent of the K-import chain, force the longitudinal projection of the translational sector to be a state variable?** The result answers by exhaustive walk of the candidates (the elastic-W form itself; the A1 bond-compliance `C_eff = C_0/S` reactance; ρ_bulk's inertia reading; the R24-contested one-displacement-field identity; the "solid-crystal identity of Axiom 1" objection from #761 `:14`). Expected shape (to be derived, not assumed): none forces it — Ax1 commits to node topology + 6 DOF whose translational sector's derived dynamics IS the curl-only axiom form; "crystal" commits to discrete topology and impedance structure, not to ball-and-spring volumetric inertia.
  5. If (4) holds, the receipted structure natively gives the bulk slot the SAME status as the EM longitudinal: **real, physical, Gauss-undeleted, source-bound, portless** — the controlled-source class. That is the carve, derived at admits-level or better.

**Route (b) — the constrained-continuum route (consistency of the carve against every banked success).** Impose the carve as the constitutive statement (the material carries no volumetric DOF; `∇·u` is determined by the source constraint — the elliptic/Poisson class; equivalently: pressure/A1-amplitude as the constraint's multiplier field) and re-derive: the static exterior (expect: `u = B/r²` — the unique spherically-symmetric divergence-free radial exterior with enclosed flux = the source's dilatation charge — survives NATIVELY, as R38 states); the wave sector (expect: transverse-only spectrum at `c`; NO longitudinal branch at ANY speed — removed, not slowed, not gapped: the LC-1 receipt that `v_L ≥ √(4/3)c` even at `K = 0` is the proof that no VALUE of K un-fires the kill; only the constraint class does); the radiation sector (expect: shear-only quadrupole at the banked PM rate; a moving dilatation charge carries its bound longitudinal near-field convectively and radiates nothing on a channel that does not exist). Route (b) FAILS — and the carve with it — if any banked success requires the deleted structure (gate G-STATIC's fall condition, brief `:24-25` verbatim in §1).

**Agreement requirement (G-CARVE-2M):** route (a)'s conclusion (what the receipts force/admit) and route (b)'s conclusion (what the carve reproduces/breaks) must agree on the verdict bin. Disagreement = NOT-DERIVABLE, stated with the disagreement enumerated.

## §4 — Corpus state (ave-prereg Steps 1–2; grounded at base `9230f434` + named branch states)

**Prior art ANTICIPATING the carve (the carve is not new structure — it is canon's own Candidate B1, un-derived):** `research/2026-07-20_q1-pulsar-hardening.md:130` — *"If the A1/bulk sector is similarly **constrained** — slaved to the instantaneous matter distribution by an *elliptic* (Poisson-like, near-zone) equation rather than a *hyperbolic* (retarded, radiative) one — it has no independent far-field radiative DOF"*; `:217` — *"**B1 (constraint/gauge slaving)** and **B4 (gapped radiative bulk sector)** are precisely the non-generic structure class required … find the AVE-specific structure that makes the vacuum's bulk sector different from a rock's."* `port-register.md:35` (P9 halo NOT-A-PORT) + `:91` (Reading B: *"the corpus then **owes a mechanism** for why a mode that propagates freely at √(10/3)c in its linear passband does not radiate from a strong quadrupolar source"* — the carve's answer: there is no passband because there is no DOF). Source-slaved dress bin: `research/2026-07-10_impedance-register-walks_framing.md:19` (*"the dress rides the source, radiates nothing (no Re(Z))"*). LC-1 row 3a: the `√2·c` object adjudicated **NOT-A-WAVE** (*"constitutive, not a Bloch branch … the speed fork is dead"*, LC-1 result §3/§8, branch state); `research/2026-08-03_coldq-polar-family_prereg-FROZEN.md:262`: the `√2·c` *"enters … ONLY through the reflection statement at the wall and NEVER as a propagation speed in the wave operator."*

**Counter-evidence (the sites where canon asserts the propagating bulk DOF — the consumer audit's hardest rows, enumerated up front, not discovered later):** `master-equation.md:26` + `vocabulary-register.md:870` (`def-l0ngdu`, SOLID, Grant-ratified 2026-07-20): *"∇·u propagates; ∇·A is gauge"* — premised on the K-backed restoring force. `vocabulary-register.md:594` (★SHARPENED 2026-08-07): *"a polarization of a physical displacement field that carries a bulk restoring force K≠0 cannot be [constrained] — it is a propagating P-branch wave."* `the-abandoned-interior.md:22` (MYTH-GUARD): AVE *"ADDS a medium with a genuine longitudinal DOF."* `gauge-boson-masses.md:38` (Grant ruling 2026-08-03): *"The vacuum at K = 2G is definitively compressible … no finite K is incompressible"* — NOTE the carve does not dispute this arithmetic: the carve is not a finite-K statement and not a K→∞ limit; it removes the K-reservoir as a constitutive fact rather than tuning its value (the kinematic-constraint class, not the stiff-limit class). `port-register.md:49/:57/:87` (channel-3 radiative column; Q1 Reading-A LIVE). `cosserat-mass-gap.md:132` (the "FOURTH speed"). `gw-propagation-lossless.md:48` (Reading-A admixture). Each of these post-dates or rides the K-import; NONE independently derives the longitudinal reservoir — that asymmetry is route (a)'s object.

**The R24 opening:** the one-displacement-field identity (`def-9a4f07`) that underwrites "GW-shear and A1-bulk are two polarizations of one field" is **CONTESTED-BY-GRANT, NOT RATIFIED** (R24 docket) — the carve's two-object reading (dynamical curl sector; bound divergence sector) is a candidate RESOLUTION of R24's tension, and the result must state what it offers the pending GW-formation walk without pre-empting it.

**Sweep note:** the phrases "bound response" / "controlled-source class" have NO corpus home before R38 (two-engine grep, agent-verified; re-verified at result time per G-CONSUMER-2M); "Lagrange multiplier" as a reading of `∇·u` likewise absent. The carve's derivation is genuinely new work; its INGREDIENTS are all receipted.

## §5 — The physical picture (ave-prereg Step 1.5; mechanical/EE terms, before any equation)

- **The network is floating.** The vacuum LC network has no external ground reference ("a medium with no outside", SVA §1-4). Its differential/curl sectors (T2, Cosserat) carry derived L-C dynamics (two-band k·p, #884: photon-at-c). Its common-mode/A1 slot is the sector a common-mode choke would see — and the receipted axiom dynamics gives that slot **no restoring element**: charge injected there does not oscillate; it biases. The A1 slot is the network's DC/bias sector: solved elliptically (operating-point solve), not propagated.
- **A star is a standing current injection into the bias network.** Its dress `u = B/r²` is the bias distribution the injection forces — the unique div-free radial exterior — with the enclosed flux `∮u·n̂ = 4πB` as the injection's magnitude = the mass accounting (A1). Nothing waves; the dress is the back-reaction, and it rides the source (receipted bin).
- **Two orbiting stars carry their bias fields with them.** The bound field re-arranges convectively (near-zone, quasi-static, reactive — added-mass class, the P9 halo object at binary scale). Energy leaves the system ONLY through the sectors that have ports: the T2 shear quadrupole at the banked PM rate. The "compression radiation" of #919/#930 was the imported elastic model's answer, computed with a P-wave Green function the substrate never derived.
- **What is saturating/circulating:** nothing in this lane — cold-linear throughout; walls enter only as banked boundary receipts. **Scaling:** dress `∝ 1/r²` (bound, near-field); shear radiation `∝ 1/r` (port). **Discrete onset:** none claimed (no new wall, no new knee).
- **The plumber's one-liner:** the corpus has been treating the vacuum's DC bus as if it were a transmission line, because the imported rock-physics constants said every displacement field comes with a pressure wave. The bus is real, the bias is real, the energy in the bias is real — but a bus with no return path carries no signal, and nothing you bolt onto it will make it ring.

---

## §6 — Pre-registered expectations + discriminators (Steps 3, 3.9, 3.10)

**Consumer-audit bins, DEFINED at freeze (deliverable 1's classification criteria — frozen so no post-hoc bin can be minted):**
- **SURVIVES-AS-RESPONSE** — the site's claim, re-read with the bulk slot as bound response, is true as stated or with pure re-labeling; its physical ROLE (reflection, reactive storage, accounting, boundary condition) is unchanged.
- **DIES-WITH-THE-PHANTOM** — the site's claim REQUIRES the independent propagating bulk DOF (a far-field bulk wave, a bulk radiative port, a P-wave Green function, a bulk propagation speed as a physical transit speed); under the carve the claim is void, and its voiding IS the carve's content.
- **NEEDS-RE-DERIVATION** — the site has surviving physical content but its stated mechanism/formula consumes the phantom (e.g. `Z_bulk = ρc_bulk` as a formula; a K-backed stability argument); a re-derivation under the carve is OWED and ROUTED, not performed as a leaf edit.

**Expected classifications (expectations, NOT criteria; the audit adjudicates site-by-site with verbatim quotes):** the √(10/3)c RADIATIVE rows (port-register ch-3 radiative column + FLAG-A's radiative half, `cosserat-mass-gap.md:132`, `srs-band-structure.md:116`, `constants.py:778`, `gw-propagation-lossless.md:48`, LC-1 row 3b, the Q1 Reading-A row, `master-equation.md:26`'s "propagates" sentence, `vocabulary-register.md:594/:870` propagating halves, the seismological partition rows) → DIES-WITH-THE-PHANTOM or NEEDS-RE-DERIVATION. The √2c PORT rows (V_LONG as near-field/reflection object, P9 halo, the coldq `:262` operator-exclusion, LC-1 3a NOT-A-WAVE) → SURVIVES-AS-RESPONSE (they already carry the bound reading), with `Z_bulk = ρc_bulk` NEEDS-RE-DERIVATION at formula level. Regime-IV/wall rows (`Γ_bulk` at the wall, vent/jam leaves, kernel-collapse mirror) → SURVIVES as sign-invariant boundary receipts (banked #930 consumption), with signed/speed LABELS re-scoped. V_snap's A1-compliance reading (INVARIANT-S2 sector split; `vol4/claim-quality.md:102`) → SURVIVES-AS-RESPONSE (a reactance of the bound sector, not a wave's); the ligo-GW channel-label flag stays FLAGGED (not adjudicated here). `mass = A1` accounting (`master-equation.md:20`) → SURVIVES-AS-RESPONSE (R38: accounting re-homed). `the-abandoned-interior.md:22` "genuine longitudinal DOF" → NEEDS-RE-DERIVATION (real sector ≠ independent DOF; the MYTH-GUARD's anti-Heaviside point SURVIVES — see G-SCALAR-REAL). Electron-sector sites ("trapped acoustic compression energy"; `crystal_engine.py` docstring) → NEEDS-RE-DERIVATION at label level with past-wall scope declared (the carve is cold-linear; the saturated interior is out of its scope).

**Static sector (deliverable 2) expectations:** every banked success survives, because each consumes only (i) the elliptic/static strain-bias field (which IS the bound response — FORM unchanged), (ii) `ν_vac = 2/7`-class VALUES whose GR-imported status is ALREADY banked (#261, `one-seventh-impedance-projection.md:18`) and is neither improved nor worsened by the carve (imports before, imports after — the carve deletes the DYNAMIC reservoir, not the static response structure or its imported coefficients), and (iii) transverse/EM propagation through the static grade (untouched). Named jeopardy sites checked explicitly: the /7 projection algebra (does any step need the K-reservoir to be DYNAMICAL? expect NO); `cauchy-implosion-resolution.md:15` (K=2G as the stability guarantor — expect NEEDS-RE-DERIVATION: the carve supplies stability kinematically, no compression DOF to implode; the leaf's JOB survives, its mechanism is the import's); #930 §1.4's graded θ-correction (consumed K on both FLAG-W branches — expect: re-scoped to a response-field profile statement; it was lane-derivation, not banked canon). **Fall condition (frozen, brief verbatim §1): "The carve FAILS here if any banked success consumed a propagating bulk mode — name it if so."**

**Kill-chain re-run (deliverable 3) expectations, each against its frozen wording:**
- **LC-1 cell** (*"An energy-carrying inter-event channel at ≠ c ⇒ arc-level kill"*): under the carve there is NO energy-carrying longitudinal channel at ANY speed — the branch is removed from the spectrum (constraint class), not moved in speed. The bound field's elliptic bookkeeping carries no energy inter-event (`Re(Z) ≡ 0`, no port — the #930 seal statement generalized channel-wide). Expected reading: the kill's predicate is UNSATISFIABLE on the compression line → **un-fires**. Honesty clause: the instantaneous-constraint bookkeeping and the one-speed principle must be reconciled EXPLICITLY (the GR-parallel: constraint sectors are not signal channels), not waved past.
- **#919/#930 residue:** CH-0's enclosed-charge quadrupole survives as a CHARGE (the dress structure is real); its FLUX dies with the P Green function (the partition `(2/3)(c_S/c_P)⁵` has no referent when `c_P` is not a propagation speed). Expected: `F_bulk ≡ 0`; the #919 floor family and #930's `252.8× δ_DP` re-read as the imported reading's self-exclusion — the strongest receipt FOR the carve, not against it.
- **Pulsar comparison:** shear-only radiation at the banked PM rate = the observed `Ṗ_b` (HT `0.9983 ± 0.0016`; DP within `1.3×10⁻⁴`) — consistency-class, GR-shared, NO chord claimed. The comparison's role: it is the measurement that ADJUDICATES the fork between the two admissible readings, and it selects the carve.

**Provenance case (deliverable 4) expectations:** each incoherence exhibit is EXPLAINED by the phantom mechanism — a slot with no independent physical referent, when modeled as if it had one, yields (a) a modulus that can only be imported (#261: both native routes returned NOT-FORCED); (b) a wave speed that fractures into irreconcilable forms (FLAG-A's √2c-vs-√(10/3)c; the FLAG-CEFF family's bulk members — honesty clause: FLAG-CEFF's c_shear-form disagreements are NOT bulk-sector and are NOT claimed as explained); (c) a wall sign that is convention-relative with no fact of the matter to converge on (FLAG-W, dissolved by §10 as plane/projection/profile-relative — the carve explains WHY only relativized statements were derivable); (d) a radiation partition that had to be imported from rock physics (no native P/S partition exists to derive). Plus the two-symptom exhibit: `cauchy-implosion-resolution.md` bought "incompressible and thermodynamically stable" WITH the same import that mints a compressible P-branch elsewhere — one import, contradictory duties.

**Entailed-branch check (Step 3.10) — every bin FIREABLE:** BOUND-RESPONSE-INCONSISTENT is fireable at (at minimum): any static-success step found to consume the propagating mode (G-STATIC); any canonical duty of the longitudinal scalar that the bound reading cannot host (G-SCALAR-REAL — electron confinement re-engagement at saturation, halo added-mass, mass-energy accounting); route (a)/(b) disagreement (G-CARVE-2M). NOT-DERIVABLE is fireable at: the route-(a) walk finding a receipted, K-import-independent forcing of the longitudinal reservoir that can neither be confirmed nor dismissed on receipts (e.g. the R24-gated identity turning out to be load-bearing in a direction only the GW-formation walk can settle). BOUND-RESPONSE-DERIVED requires ALL of: G-CARVE-2M ∧ G-CONSUMER-2M ∧ G-STATIC ∧ G-KILL-3 ∧ G-PROV-4 ∧ G-SCALAR-REAL ∧ G-SHEAR-UNTOUCHED ∧ G-RECON pass — none of which is entailed by the setup: the audit can find a genuine consumer, the static walk can find a genuine consumption, the scalar-duties check can find an un-hostable duty. The one ENTAILED element, declared as such: IF the carve holds, `F_bulk ≡ 0 < δ_DP` is arithmetic, not a finding — the honest verb there is DEMONSTRATED, and the comparator's adjudicating role is between READINGS (frozen above), not a new numerical test of the carve.

**Constraint-falsifier targets (named, visible, ½/¼-knife armed):** the banked comparators (`δ_HT`, `δ_DP`) — the carve must not be tuned toward them (it has no knobs: `F_bulk ≡ 0` identically or the carve is wrong); the banked PM shear rate (consistency-import, untouched — any step that ADJUSTS shear to absorb the deleted bulk flux is a Rule-11 violation and fails G-SHEAR-UNTOUCHED); the static /7 coefficients (unchanged imports — any step that "improves" them under the carve is flagged as suspicious, not banked).

## §7 — The frozen gate battery (UNRUN ≠ PASSED; every gate run and scored in the result)

| Gate | Frozen criterion |
|---|---|
| **G-QUOTE** | Every ruled/frozen quotation in the result byte-checks against its source file:line (two engines: extracted-span comparison + Python string equality); branch-state quotes byte-check against the NAMED commit (`be04ea03` LC-1, `ba1d872d` #919); any delta enumerated or the quote fails |
| **G-CARVE-2M** | The carve adjudicated by BOTH §3 routes independently — (a) the axiom/provenance walk (every candidate forcing of the longitudinal reservoir enumerated and dispositioned with receipts), (b) the constrained-continuum re-derivation (static dress + wave spectrum + radiation sector) — and the two routes AGREE on the verdict bin. The FORCES-vs-ADMITS distinction stated explicitly in the verdict sentence. Disagreement = NOT-DERIVABLE |
| **G-CONSUMER-2M** | The consumer enumeration run by TWO named engines (GNU `grep -E` + Python `re`, patterns printed) over `manuscript/` + `research/` + `src/ave/` + `_orchestration/`; the known-positive control set (§0 row 9) found by BOTH engines or the sweep is dead and no absence claim banks; every enumerated site classified into a §6 frozen bin with a verbatim quote; sites the engines disagree on adjudicated by read |
| **G-STATIC** | Each named banked success (the dress; gravity-as-refraction; the /7 couplings incl. `r_sat = 7GM/c²`; perihelion; light-bending/double-deflection) re-derived or re-read under the constraint with its consumption inventory stated (static-elliptic field / imported value / transverse propagation / ANYTHING ELSE); the brief's fall condition applied verbatim: any banked success consuming a propagating bulk mode ⇒ BOUND-RESPONSE-INCONSISTENT(site), named verbatim |
| **G-KILL-3** | All three kill-chain items (LC-1 cell; #919/#930 residue; pulsar comparison) re-read against their frozen wordings QUOTED IN FULL at their frozen homes (§2), each with an explicit "under the carve this reads:" statement; no frozen bin of any banked lane is re-scored (their verdicts stand on their premises — the premise-change is this lane's content); the LC-1 honesty clause (instantaneous-constraint vs one-speed reconciliation) explicitly derived, not asserted |
| **G-PROV-4** | All four incoherence exhibits (K=2G import; seismic-partition import; FLAG-CEFF bulk members; FLAG-W) EXPLAINED with an explicit mechanism sentence each ("under the carve, this incoherence is expected because …"), plus the honesty clause: what the carve does NOT explain (FLAG-CEFF's shear-form members) stated plainly. An exhibit merely DELETED without its incoherence explained = G-PROV-4 FAIL |
| **G-SCALAR-REAL** | The carve must NOT reconstruct the transverse-only-photon/Heaviside-excision move: the longitudinal scalar's canonical duties (real, physical, Gauss-undeleted; the halo's added-mass; mass = A1 accounting; the saturation-re-engagement statement at `master-equation.md:16` under its past-wall scope) each shown HOSTED by the bound response field, with the distinction "physical-and-bound ≠ deleted" derived in the result. Any duty the bound reading cannot host ⇒ BOUND-RESPONSE-INCONSISTENT(site) |
| **G-SHEAR-UNTOUCHED** | The T2 sectors (photon at c; observed GW at the banked PM rate) byte-untouched by the carve: no step re-derives, re-scales, or re-anchors the shear channel; the shear-only prediction cites the banked R1 consistency-import as-is. Any shear-side adjustment = FAIL |
| **G-RECON** | Challenge-a-canonical-negative receipts: for EACH of #761/#919/#927/#930, the result states (i) the banked verdict UNCHANGED on its own premises, (ii) the specific premise the carve changes, (iii) the receipt that premise was an import (file:line), (iv) what the banked derivation becomes under the carve (diagnosis, not error). Any step that re-argues a banked derivation's internal logic = FAIL. The ½/¼ over-determination tell checked: if the carve "explains" MORE than the bulk-sector incoherence it claims (e.g. starts absorbing unrelated negatives), flag as seductive-narrative risk in the result |
| **G-ENGINE-FLAG** | Deliverable 5 is an architecture NOTE: named files/lines of the coded compressibility (`crystal_engine.py` bulk branch; `constants.py` V_LONG/K-block; `observable_battery.py:539` λ; solver sites), the constraint-form each would take (∇·u as multiplier/projection; A1 accounting re-homed), and which tests/gates would move — with ZERO code edits (verified: `git diff --stat src/` empty of engine changes) |
| **G-NUM** | Gating number check: every verdict-bearing numeral in the result (composite tokens included) machine-verified against the driver JSON; mutation receipt fires; `make verify` green in THIS worktree at commit; the driver's algebra receipts include: ν(K=2G) = 2/7, c_P/c_S(K=2G) = √(10/3), c_P/c_S(K=0) = √(4/3), the #919 partition arithmetic reproduced as SUPERSEDED-INPUT record, and the comparator arithmetic |

**Frozen bin arithmetic:** **BOUND-RESPONSE-DERIVED** requires G-CARVE-2M ∧ G-CONSUMER-2M ∧ G-STATIC ∧ G-KILL-3 ∧ G-PROV-4 ∧ G-SCALAR-REAL ∧ G-SHEAR-UNTOUCHED ∧ G-RECON all PASS (G-QUOTE, G-ENGINE-FLAG, G-NUM are integrity gates: any FAIL blocks the verdict word until repaired and disclosed). **BOUND-RESPONSE-INCONSISTENT(site)** fires when G-STATIC's fall condition or G-SCALAR-REAL's hosting check or G-CONSUMER-2M finds a banked success genuinely consuming the propagating bulk mode — the site named verbatim in the verdict sentence; the carve dies there regardless of every other gate. **NOT-DERIVABLE** fires on G-CARVE-2M route disagreement or on the route-(a) walk bottoming out in R24-gated structure that receipts cannot settle — with the missing adjudicating structure enumerated. Verdict sentences state ADMITS vs FORCES explicitly. No gate, tolerance, bin boundary, classification-bin definition, or frozen numeric parameter in this document may change after any derivation, audit, or gate result is seen. Deviations forced by instrument mechanics follow the #927 §2.0 pattern: disclosed FIRST in the result, each with direction-of-effect.

---

> **Freeze provenance.** SVA v0.2-pilot §0 filled, all 11 rows. Frozen criteria verbatim or file:line throughout; the kill cell quoted from the ARC BRIEF (`:44`, the #913 R5 authority), never the dispatch gloss; branch-state citations carry their commits (`be04ea03`, `ba1d872d`) and their HELD status (R24). Comparators frozen by same-day re-retrieval provenance (#930 prereg §4 at `a510ec2b`, quoted not re-fetched). Corpus state grounded at base `9230f434` by an 8-agent two-method context pull + orchestrator byte-checks of every freeze-bound quote. This document commits ALONE and pushes FIRST (freeze-by-push); the driver commits second; the result last. Skill stack fired at write time: ave-prereg (Steps 1–3.11 incl. the entailed-branch check §6 and freeze-by-push), ave-worktree-paths (worktree-absolute from first call), verify-before-cite (every freeze-bound quote byte-checked at write time; G-QUOTE re-checks at result time), ave-canonical-leaf-pull (propagation-speed canon enumerated §4), ave-ee-first-mapping (§5 circuit statement first), ave-discrimination-check (§0 row 8 — no chord on any outcome; GR-parallel stated), ave-regime-phase-state-check (§0 row 2 past-wall scope), consistency-vs-emergence (classifications routed to the result's ledger). Tier-2 adversarial review at adjudication before any CLEARED wording; PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.
