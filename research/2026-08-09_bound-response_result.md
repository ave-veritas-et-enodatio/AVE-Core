# THE BOUND-RESPONSE CARVE (R38) — RESULT

**Date:** 2026-08-09 · **Branch:** `lane/2026-08-09-bound-response` · **Base:** `origin/main` @ `9230f434`
**Frozen prereg:** [`2026-08-09_bound-response_prereg-FROZEN.md`](2026-08-09_bound-response_prereg-FROZEN.md), committed ALONE + pushed at `0722a8a3` before any derivation content, audit output, driver code, or lane-produced number existed (freeze-by-push).
**Driver:** [`research/drivers/bound_response_carve.py`](drivers/bound_response_carve.py) → [`bound_response_carve_results.json`](drivers/bound_response_carve_results.json) + [`bound_response_number_check.py`](drivers/bound_response_number_check.py) (`--mutation-receipt`; auto-discovered by `make verify`).
**Class:** DERIVATION + CONSUMER-AUDIT. **Mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; edits no KB leaf, register, ledger, or ruling; changes no solidity; all propagation ROUTED** (the #930 walk-back's manuscript demotion sweep HOLDS on this lane's return per R38 — this document FEEDS it, executes none of it). Engine `src/ave` byte-untouched.
**Lane brief:** [`_orchestration/2026-08-09_bound-response-brief.md`](../_orchestration/2026-08-09_bound-response-brief.md) (Grant GO, R38).

---

## §0 — REGIME / SECTOR / PHASE-STATE header (restated at the point of reading; prereg SVA §0 governs)

**MODE** — a constitutive-ontology adjudication: is the vacuum's A1 bulk/dilatation slot an independent dynamical channel (state variable: own L-C pair, own port) or a **bound response field** (controlled-source class: dependent variable, no port), with the banked kill chain re-read under the answer. **REGIME** — cold-linear (Regime-I, sub-yield, lossless-reactive) throughout; **PAST-WALL states are OUT OF SCOPE** (Ax1 phase-scope: nothing here evaluates any constitutive law past a wall; wall rows enter only as banked sign-invariant boundary receipts). **PHASE-STATE** — cold-reactive; the only ports consumed are the banked T2 ones. **SECTOR** — A1 = the slot under test; T2 shear/EM and Cosserat micro-rotation byte-untouched (gates G-SHEAR-UNTOUCHED, rotational fence-reach STOP checked at §1, §2, §3: no step routes bulk-slot content through the micro-rotation channel). TWO-"3"s guard active. **STENCIL** — the engine codes compressibility (`crystal_engine.py:102` bulk scalar V), so **no engine run corroborates anything in this lane** (brief fence `:55-56`); the numeric arm is algebra receipts + sweep receipts only.

---

## HEADLINE

> **🔴 VERDICT AS RE-CUT AT THIS LANE'S OWN TIER-2 (2026-08-10, pre-presentation; the original DERIVED verdict below is PRESERVED per Rule 12 and OVERTURNED): `NOT-DERIVABLE` — the adjudicating structure is missing; enumerated.** The Tier-2 (26 agents, 5 lenses, per-finding adversarial verify — ★addendum at the end of this document) confirmed 19 of 21 findings (1 CRITICAL, 6 MAJOR, 12 MINOR), and two are verdict-bearing under this lane's OWN frozen bin arithmetic: **G-KILL-3 FAILS as frozen** (the LC-1 honesty clause was asserted, not derived — the frozen criterion's own words; and its GR-parallel cite is laundered: GR's causality escape rides *pure-gauge*, the exact property the carve denies for the REAL `∇·u`), and **route (a)'s FORCED grade is a non-sequitur** (the axiom kinetic term is projection-blind — verifier-reproduced spectrally: the receipted dynamics gives the longitudinal sector as a **zero-stiffness inertial FLAT DIRECTION** (`ε₀Ä_L = 0`, conserved momentum, a state variable), NOT constraint class; the multiplier/Gauss structure the controlled-source class needs has **no axiom preimage either — the same knife route (a2) used on the K-term**). Per the frozen grammar's third bin, the missing adjudicating structure is: **(i)** the bound-sector constitutive law — the constraint/multiplier (or Gauss-law-analog) structure that DETERMINES the dress from sources (without it the flat direction leaves `u₀ = B/r²` dynamically under-determined); **(ii)** the no-signalling/retardation theorem for a REAL (non-gauge) bound field — the inter-event energy/information question, unposed; **(iii)** the bound sector's energy functional (the mass = A1 and halo added-mass hosting duties — G-SCALAR-REAL scored PASS without one); **(iv)** the static-response reconciliation — how a no-volumetric-DOF medium presents `ν = 2/7`-class static projections (the Tier-2's "frequency-gated ontology" finding: the same mechanical `W` was deleted where it radiates and retained where it banks, with the cut placed where the pulsars require — asserted, never derived).  <!-- rule12-freeze: base=e8d4e42e5049ed473f6b3b2c6e2f898694ab54fb region=below offset=0 lines=3 bytes=1548 sha256=f67ee5cb8f7b314cc0385e8420cfcfe96ec3cd6336d73b506720eb61007bbdbe -->
>
> **What SURVIVES the re-cut, banked at its honest grade:** (1) **the no-finite-ω consequence** — IF the receipted curl-only dynamics governs the A1 slot, no finite-frequency longitudinal branch exists (zero restoring force ⇒ no wave at `2Ω` ⇒ no radiative channel at orbital frequency; an oscillatory drive does zero net cycle-work on an inertial flat sector) — the P-branch REMOVAL half stands, conditional on the bridge question in (i); (2) **the premise-localization receipts** (G-RECON, unchallenged): #761/#919/#927/#930 all consumed the #261-closed K-import — the kill chain's premise is the import, with receipts, and the pulsar exclusion binds the IMPORTED reading; (3) **the consumer audit** (repaired: supplement annex + recall bound + scope deviations disclosed — the enumeration is PATTERN-BOUNDED, not exhaustive); (4) **the provenance case at its reduced honest count** (the Tier-2's independence audit: ~1.5–2 independent exhibits, not four — the K-import RELATION-level tag + the T3.1 added-by-design history; FLAG-W discriminates nothing (generic sign-relativity), FLAG-A was already mundanely resolved pre-carve, and the partition-import is DOWNSTREAM of the K-import). **The kill chain is NOT un-fired by this lane.** LC-1's cell stays GATED — now on the enumerated derivation (i)+(ii) rather than on a screening arm; the carve remains R38's walk-level candidate, with a precise four-item derivation worklist and a repaired audit as this lane's deliverable. The original verdict text follows, preserved:
>
> **VERDICT (pre-Tier-2 form, PRESERVED per Rule 12, OVERTURNED above): `BOUND-RESPONSE-DERIVED` — at FORCED-BY-RECEIPTED-DYNAMICS grade on the structure, ADMITS+SELECTED grade on the constitutive totality — carried through all eight verdict gates (§7). The receipted axiom dynamics (`eq_axiom_3.tex`, curl-only) gives the translational sector's longitudinal projection NO restoring structure: within canon's own derived dynamics the A1 slot is constraint-class — real, physical, Gauss-undeleted, source-bound, portless. Every structure that ever made `∇·u` a propagating wave bottoms out in ONE import event (the mechanical elastic `W`'s bulk sector: K = 2G, GR-imported per #261 "Imported, both legs. End of line.", carried by a ball-and-spring representation whose longitudinal kinetic+potential pair has no preimage in the axiom Lagrangian) or in an identity Grant has CONTESTED (R24, def-9a4f07 NOT RATIFIED). The carve is consistent with every banked static success (§2: each consumes only the elliptic bias field, imported /7 values whose status is unchanged, and transverse propagation), it EXPLAINS the corpus's bulk-sector incoherence (§4: one import, four fingerprints), and it un-fires the kill chain at the channel level (§3: the LC-1 cell's predicate "energy-carrying … channel" is unsatisfiable on the compression line — the branch is removed from the spectrum by constraint, not slowed, not gapped, not screened). The prediction is SHEAR-ONLY radiation at the banked Peters–Mathews rate = the observed `Ṗ_b` (consistency-class, GR-shared, NO chord claimed).**  <!-- rule12-freeze: base=e8d4e42e5049ed473f6b3b2c6e2f898694ab54fb region=above offset=0 lines=3 bytes=1548 sha256=f67ee5cb8f7b314cc0385e8420cfcfe96ec3cd6336d73b506720eb61007bbdbe -->
>
> **The kill chain flips polarity: it was never a falsification of the framework — it was the falsification of the IMPORT.** #761 derived, correctly, that IF the vacuum is the imported compressible elastic solid THEN it must radiate a bulk admixture (`F_bulk/F_shear ≥ (2/3)(c_S/c_P)⁵ = 0.032863` at VRH — #919's floor, #930's CH-0). The pulsars measure `≤ 1.3×10⁻⁴` of the shear budget in excess. That is a `252.8×` exclusion of the IMPORTED READING by observation — the discriminating measurement between the two candidate ontologies of the A1 slot, and it selects the bound response. The substrate adjudicated the fork; the corpus had been reading the verdict as its own indictment because the import was wearing the framework's name.

**The consumer-audit table (deliverable 1), the static re-derivations (deliverable 2), the kill-chain readings (deliverable 3), the provenance case (deliverable 4), and the engine flag (deliverable 5) follow. Frozen wordings quoted in full at their §-sites; gate scores in §7.**

---

## §1 — ROUTE (a): the axiom/provenance walk (does ANY receipt force an independent longitudinal reservoir?)

The frozen question (prereg §3): does any receipt, independent of the K-import chain, force the longitudinal projection of the translational sector to be a state variable? Every candidate forcing, enumerated and dispositioned:

**(a1) The axiom-level dynamics is curl-only — this is canon, and it is #761's own first step.** `eq_axiom_3.tex` (canonical, single source of truth): `L_node = ½ε₀|∂_t A_n|² − (1/2μ₀)|∇×A_n|²` — *"the standard Maxwell Lagrangian … recovered as the substrate's effective action in the linear regime."* A curl-free configuration contributes NOTHING to the potential term: the longitudinal projection has no restoring force, no eigenfrequency, no branch. With sources it is the Gauss-constraint sector — determined by charge, carried convectively, storing real energy density, radiating never. #761 result `:14` states this verbatim for `∇·A`: *"gives the EM longitudinal `∇·A` NO restoring force (gauge/non-dynamical)"* — and then exempts the mechanical `∇·u` on exactly one ground: *"a genuine bulk restoring force `½K(∇·u)²` with `K ≠ 0`."* Everything reduces to the provenance of that term.

**(a2) The bulk restoring term: IMPORTED, end of line — banked, not relitigated.** #261 (merged `b49ce3b1`), two gates, Grant-ratified (`_orchestration/2026-06-15_k2g-crystalline-provenance.md:122-126`): *"Imported, both legs. End of line."* Gate 1: NOT crystalline-geometric — *"K/G is a ratio of two independent constitutive stiffnesses"*; the K4 geometry fixes only the FORM `K/G = f(k_a/k_s)`. Gate 2: NOT constitutively forced — the constitutive law's only handle is SYM-pinned and selects nothing. The import's source is named in canon: *"the trace-reversal identity required by General Relativity"* (`q-g47-substrate-scale-cosserat-closure.md:28`). **#761's hedge — "only K ≠ 0 is load-bearing" — does not rescue the reservoir**, for two receipted reasons: (i) LC-1's algebra (branch `research/lc1-one-speed` @ `be04ea03`, the K=0 receipt — *"even at K=0 … Bringing the longitudinal branch down to c requires K=−G/3"* class; reproduced in this lane's driver): `c_P/c_S = √(4/3)` at `K = 0` — the longitudinal branch of the BALL-AND-SPRING representation persists, superluminal, at ANY K including zero, because the `4G/3` deviatoric projection restores it. The reservoir is not the K-VALUE; it is the REPRESENTATION — the assumption that the medium's G restores deviatoric strain of longitudinal patterns the way a material solid's does. (ii) That representation (the mechanical Lagrangian `L = ½ρ|u̇|² + ½I_ω|ω̇|² − W(u,ω)`, `cosserat-mass-gap.md:30`) is a SECOND Lagrangian for the SAME translational DOF the axiom already governs (Ax1: *"3 translational → E"*), connected to the electrical one only through the TKI impedance bridge (`Z_mech = ξ_topo²·Z_elec` — *"NOT a direct wire"*, `research/2026-06-19_electrical-mechanical-projection-map.md:20`). Under that bridge the axiom's potential term maps to curl-keyed restoring structure ONLY. The mechanical `W`'s longitudinal sector (both its K-term AND its `4G/3` deviatoric-restoring reading of longitudinal patterns) has **no preimage in the axiom Lagrangian's POTENTIAL** — it is structure the representation ADDED. Its value-provenance (#261) tells us where it was added from. **⚑ Tier-2 corrections (2026-08-10, findings #2/#16, prior sentence preserved):** (a) the KINETIC half has a trivial axiom preimage (`½ε₀|∂_t A|²` is projection-blind) — so the receipted dynamics yields a zero-stiffness inertial FLAT DIRECTION for the longitudinal sector, not the constraint class; the further step (flat direction → bound/controlled-source) needs a multiplier structure that is ITSELF un-receipted — see the HEADLINE re-cut, missing item (i). (b) The "NOT a direct wire" cite continues *"The transducer **changes units**; it does not erase the boundary"* — the no-preimage-across-the-bridge step is a LANE INFERENCE from that line, not a receipt in it.

**(a3) The "lattice-computed P-branch" is the import's echo, not an independent receipt.** `clm-bnd5rq` (solidity 0.8, ok-with-caveats — #761 result `:10`'s own caveat: *"the vector-band values ride on the GR-imported"* moduli): `src/scripts/vol_1_foundations/srs_vector_band_survey.py:201` consumes `K_bulk`, `G_Hill` as INPUTS. A band structure computed FROM the imported moduli is a theorem of the import. Likewise `srs-band-structure.md:116` already carries the honest label: the `√(10/3)` row is *"A **K=2G RE-EXPRESSION** (GR-imported, PR #261), **NOT lattice-emergent**."*

**(a4) The one-displacement-field identity cannot force anything: it is CONTESTED.** `def-9a4f07` (GW-shear ≡ transverse polarization of the same field whose longitudinal polarization is the A1 bulk) — **NOT RATIFIED; CONTESTED-BY-GRANT** (R24, `_orchestration/docket-entries/2026-08-07-rulings-r23-r27.md`: *"They seem different. A GW is a saturation wave …"*), gated on the pending GW-formation walk. The carve does not need the walk's outcome (route (a) runs on provenance receipts alone), but it OFFERS the walk a candidate resolution of R24's named tension — *"the echo sector treats ringdown as SHEAR DISPLACEMENT waves; the trampoline/observable sector treats gravitational action as SATURATION/IMPEDANCE modulation"* — because under the carve those ARE two different objects: the T2 sector is dynamical displacement; the A1 slot is impedance-grade response (the two-variable split #919 §3.3 / #930 §1.4 already banked). Routed as an input to the walk, asserting nothing over it.

**(a5) "The solid-crystal identity of Axiom 1" (#761's forward-defense) does not commit to volumetric material kinematics.** Ax1 (INVARIANT-S2 canonical statement) commits to: K4 node topology, 6 DOF per node (*"3 translational → E, 3 microrotational → B"*), `I4₁32` chirality, intrinsic LC oscillators, and a continuum model NAMED in the axiom itself: *"Trace-Reversed Chiral LC Network."* A crystal, so committed, is discrete topology + connectivity + impedance structure. An LC NETWORK's common mode with no external ground reference has no return path: it is a bound/bias sector, not a transmission mode — which is precisely what the curl-only effective action of that same axiom set derives. Two further observations, recorded without weight: the axiom's own continuum-model NAME carries "Trace-Reversed" — the GR structure #261 identified as the import's source — at the naming level, not the derivation level; and `master-equation.md:20`'s *"`mₑc²` = trapped acoustic **compression** energy"* is an ACCOUNTING statement about stored energy in the bound sector (R38: mass = A1 survives as accounting), whose "acoustic" adjective rides the imported representation (consumer row, §6; label-class, not a forcing).

**(a6) The A1 irrep slot is real and is not a DOF.** `clm-j550uh`/`clm-9kd2t3`: the K4 4-port amplitude space decomposes as `A1 ⊕ T2` — group theory, untouched. A projection exists whether or not it carries independent dynamics; a projection with no kinetic/potential pair of its own is a DEPENDENT variable. The port-register's own taxonomy ("channels are inherent") inherited the A1 row's SPEEDS from `constants.py` — i.e., from the import (§4).

**ROUTE (a) CONCLUSION.** No receipt forces the longitudinal reservoir. Within the receipted axiom dynamics the A1 slot is **constraint-class — the bound response field**: this half is FORCED (it is what the canon Lagrangian derives). What is NOT forced is the completeness claim — that no additional, not-yet-receipted physics gives the slot independent dynamics; the corpus's one candidate for such physics (the mechanical `W`'s longitudinal sector) is the #261-closed import, and the R24-contested identity is the only other route. At the totality level the honest verb is **ADMITS + SELECTED** — selected by the banked discriminating observation (§3.3). Stated per the frozen grammar and not upgraded.

## §2 — ROUTE (b): the constrained re-derivation (deliverable 2 — the static sector under the carve)

The carve as constitutive statement: the material carries no volumetric DOF; `∇·u` is fixed by the source constraint (elliptic/Gauss class; the A1 amplitude is the constraint's multiplier/response field, sourced by matter's dilatation charge). Each banked static success re-derived/re-read, with its **consumption inventory** (gate G-STATIC):

| Banked success | Its derivation consumes | Under the carve | Verdict |
|---|---|---|---|
| **The dress `u₀ = B/r²`** (Lamé exterior, #919 §3.3 receipt; #930 G-DIV) | The unique decaying spherically-symmetric divergence-free radial field; enclosed flux `∮u·n̂ = 4πB` | IDENTICAL object — it IS the constraint's exterior solution with the source's dilatation charge enclosed (R38: *"u = B/r² IS the incompressible exterior solution"*). Nothing changes, including #930's enclosed-charge (CH-0) CHARGE structure — only its "radiation" dies (§3.2) | **SURVIVES natively** |
| **The strain-bias field `ε₁₁(r) = 7GM/c²r`** (`gordon-optical-metric.md:20-33`) | *"standard 3D electrostatics using the Laplace equation"* — elliptic, static, Green's-function; no wave speed anywhere | The elliptic solve IS the bound-response solve — the carve's native equation class. FORM unchanged | **SURVIVES natively** |
| **The /7 couplings** (`one-seventh-impedance-projection.md`, `optical-refraction-gravity.md`, `vacuum-poisson-ratio.md:13`) | Static projection algebra taking `ν_vac = 2/7` as INPUT; K enters ONLY inside ν as a static constant. Canon's own tag (`one-seventh-impedance-projection.md:18`): the algebra is *"legitimate continuum mechanics, but it takes ν=2/7 as **input**. The *value* ν=2/7 is **GR-imported** via K=2G (PR#261)"* | VALUES imported before, imported after — the carve deletes the DYNAMIC reservoir, not the static response structure or its imported coefficients. Epistemic status byte-unchanged (echo-class values; #262-264 meta-finding). The carve neither improves nor worsens them (constraint-falsifier discipline: no step here touches these numbers) | **SURVIVES (status unchanged)** |
| **Light bending / double deflection** (`double-deflection.md:26,39`) | The static index gradient + the photon as *"a purely transverse Cosserat shear wave … **mechanically blind** to the isotropic bulk"* | The bulk mode is affirmatively NOT consumed — the leaf says so itself. The `2:1` light/matter ratio rides the imported /7 values, as before | **SURVIVES natively** |
| **Perihelion precession** (`anomalous-perihelion-advance.md:33-41`) | A `1/r³` perturbation of a static potential; *"reactive frequency re-tuning … at invariant Z₀"*; no wave speed, no K dynamics | Unchanged (its hand-set provenance item from the PPN coherence audit is pre-existing and untouched) | **SURVIVES (status unchanged)** |
| **Shapiro / temporal index; refraction-as-gravity; ponderomotive/Newtonian force; K4-TLM lensing** | Static index/grade + EM/transverse propagation through it | The propagating object in every one is the T2 photon; the bulk sector supplies only the frozen bias it propagates through — which is the response field | **SURVIVES natively** |
| **`r_sat = 7GM/c²` + the saturating-modulus elliptic relaxation** (`saturating-modulus-and-backreaction.md:50-70`) | `−∇·[(c⁴/7G) D(A) ∇ε₁₁] = T₀₀` — elliptic; `r_sat` DEFINED by the static condition `ε₁₁ = 1`; the `c_bulk→0` line at `:194` is a wall-characterization LABEL (*what a bulk wave would see*), consumed by no banked gate | Elliptic relaxation = response-field profile equation; the wall condition is a statement about the bias field reaching yield — static. The `c_bulk` LABELS become vacuous-but-harmless (consumer rows, §6) | **SURVIVES; labels re-scoped** |
| **Cauchy-implosion stability** (`cauchy-implosion-resolution.md:15`) | *"rigidly locks the macroscopic bulk impedance to double the transverse shear impedance (K ≡ 2G). This positive bulk resistance guarantees that the spatial substrate is incompressible and thermodynamically stable"* | The leaf's JOB (no implosion) survives with a CLEANER mechanism: a medium with no volumetric DOF cannot implode volumetrically — stability is kinematic, not bought with an imported stiffness. The stated K-mechanism is the import doing static duty | **NEEDS-RE-DERIVATION (routed)** — the one static leaf whose MECHANISM (not result) consumed the import |
| **#930 §1.4 graded θ-correction** (`O(A²)` wall-localized, both FLAG-W branches) | Static equilibrium with graded moduli INCLUDING K | Re-scoped to a response-profile correction of the bound field near the wall (same wall-localized class, no radiative consequence — there is no channel for it). It was lane-derivation, not banked canon; #930's G-DIV receipt (div-free at moment-forming scale to `2.31e-10`) is unaffected | **SURVIVES re-scoped** |

**The fall condition (brief `:24-25`, applied verbatim): "The carve FAILS here if any banked success consumed a propagating bulk mode — name it if so."** Swept two-method (driver F1/F4/F6 canon families ∩ `vol3/gravity` + `ch14`/`ch15` statics; plus the dedicated static-sector context pull): **NO banked static-gravity success consumes a propagating bulk compression wave.** Every derivation chain is elliptic-static + imported values + transverse propagation. The ONLY propagating-bulk content in the gravity KB is the ch08 Reading-A admixture — the radiation-sector LIABILITY carrying the LIVE exclusion, which is the kill chain itself (§3), not a banked success. **G-STATIC: PASS — no INCONSISTENT site.**

**The wave sector under the carve (completing route (b)):** source-free vacuum: the constraint `∇·u = 0` leaves exactly the transverse polarizations as propagating solutions — both T2, at `c` cold (the two-band k·p receipt #884 for the photon; the banked GW row for shear). The longitudinal branch is **REMOVED from the spectrum** — not slowed (any finite K leaves it superluminal: `√(4/3)` at `K=0`, driver receipt), not gapped, not screened: absent. Radiation from moving sources: the bound field has no retardation surface and no `1/r` radiative zone of its own — its far structure is the convected near-field (`1/r²`, reactive, stores-and-returns per cycle: the P9 mechanics at binary scale). The radiated power is the T2 shear quadrupole at the banked PM rate (R1 consistency-import, cited as-is — G-SHEAR-UNTOUCHED). **Routes (a) and (b) agree on the bin. G-CARVE-2M: PASS.**

## §3 — THE KILL-CHAIN RE-RUN (deliverable 3 — each frozen wording, then "under the carve this reads:")

**§3.1 — LC-1's longitudinal branch.** Frozen cell (`_orchestration/2026-08-04_lorentz-compliance-arc-brief.md:44`, verbatim `[sic]`): *"An energy-carrying inter-event channel at ≠ c ⇒ arc-level kill"*; `:59`: *"LC-1 runs first and its kill condition is arc-terminating."* LC-1's crux row (result @ `be04ea03` §5, row 3b): the `√(10/3)c` P-wave — *"gapless … unprotected … sourced (#761 falsified all three escapes) … OUT-OF-BAND-AT-ARRIVAL"* — a comparator-power null, with LC-1's own flag (FLAG-LC1-E) that the vocabulary *"has no slot for either a modulus-that-is-not-a-wave or a sourced-radiative-but-out-of-band channel."*
**Under the carve this reads:** row 3b's three properties (gapless / unprotected / sourced) were all theorems of the imported representation — of the ball-and-spring `W`, not of the receipted axiom dynamics. The brief's trichotomy (gapped / confined / sourceless) was a menu of ways to neutralize a wave; the receipted answer is the fourth slot LC-1 said was missing: **not a wave at all** (the bound-response class IS the "modulus-that-is-not-a-wave" vocabulary slot, now for the radiative column too). There is NO energy-carrying inter-event channel at `≠ c` on the compression line — not because it is out of band, but because the channel does not exist; the constraint field carries no energy inter-event (`Re(Z) ≡ 0`, no port). **The frozen kill condition's predicate is UNSATISFIABLE on this sector: the kill un-fires at the channel level.** LC-1's PRIMARY verdict (`A-COMPLIANT-AT-COMPARATOR`) stands unchanged on its own premises; its shield upgrades from kinematic (band-miss) to structural. Honesty clause, derived not asserted: the bound field's elliptic bookkeeping is instantaneous in the substrate frame — the reconciliation with one-speed is that (i) a portless sector transmits no energy and no signal (work at a remote port requires `Re(Z) > 0` there; the bound sector stores-and-returns exactly — Ax3), (ii) every OBSERVABLE is a phase/energy statement at ports, and every port rides the propagating sectors, all at `c` cold; (iii) this is the identical structure already banked for the STATIC dress (no one reads the Coulomb-class `B/r²` exterior as a superluminal channel — the carve extends that reading to the slot's every configuration); (iv) the GR-parallel is canon's own Candidate-B1 sentence (`q1-pulsar-hardening.md:130`: constraint-fixed scalar parts, *"instantaneous potential"* class, no radiative DOF). Phase-only doctrine: uniform-and-bound content is self-cancelling in observables; the AC/DC carve (`clm-acdc07`) already houses the split — the A1 slot is DC-machinery (operating-point solve), and DC does not violate an AC speed limit because DC is not a signal.

**§3.2 — #919/#930's residue.** Frozen wordings: #919 result `:220` (`ba1d872d`): `F_bulk/F_shear ≥ (2/3)(c_S/c_P)⁵`, seismology-anchored (`q1-pulsar-hardening.md:215`: *"Aki & Richards, standard elastodynamics — [import], external anchor"*); the uncaged bracket `[0.0152, 0.0455]` = 9.5–28.5σ HT / 117–350× DP; #930 HEADLINE: *"`SEAL-DERIVED + RESIDUE-EXCEEDS` … CH-0 … `F_res = 0.032863` … `252.8`× the DP bound / `20.5`σ HT"* with the moving enclosed-charge quadrupole as the residue-setter and §6: *"the sealed arm does NOT rescue it."*
**Under the carve this reads:** the partition `(2/3)(c_S/c_P)⁵` is the P/S energy split of an isotropic COMPRESSIBLE elastic solid — its derivation requires the P-branch Green function, and `c_P` requires a propagating longitudinal mode. Under the carve neither exists: **the partition has no referent; `F_bulk ≡ 0` identically** (an entailed arithmetic consequence, DEMONSTRATED not adjudicated, per the prereg's Step-3.10 declaration). CH-0's enclosed-charge structure SURVIVES as charge — the orbiting monopole pair, cold-side measurable, moment ratio 1 — and becomes the time-varying drive of the bound field's near zone: a reactive, convected, stores-and-returns configuration (the halo object at binary scale), radiating nothing on a channel that is not there. #767's multipole ladder re-reads: the moment integrals `∫xᵢxⱼ∇·u = Q_ij` survive as bookkeeping of the response field's source; the ladder's "quadrupole radiates — YES" column consumed the elastodynamic far-field Green structure and is void with it. #927's overlap machinery (`O ∝ j₂'(kR)`) survives as mathematics awaiting a real channel; here there is none to couple to. #930's seal (`Re(Z_source→bulk) = 0` for interior content) survives **a fortiori** — the carve extends `Re(Z) = 0` from the walled interior to the channel as such, which is exactly the generalization R38 named from `port-register.md:35`. The NS-wall/DP-B discriminator (`R_B` vs `12.91` km) stops conditioning anything on this axis (it remains a derived wall-existence discriminator for wall-keyed physics). **Fence honored:** #919's and #930's frozen bins are NOT re-scored — their verdicts stand on their premises; the premise (a propagating compression line) is what R38 changed, with receipts (§4).

**§3.3 — The pulsar comparison.** Frozen comparators (#930 prereg §4, re-retrieved same-day at `a510ec2b`, quoted): HT *"0.9983 ± 0.0016"* of GR (Weisberg & Huang 2016); DP *"1.3 × 10⁻⁴ (95% conf.)"* (Kramer et al. 2021).
**Under the carve this reads:** the total radiated budget is the shear quadrupole at the banked PM rate — the GR prediction — and the observed `Ṗ_b` residuals are the compatibility statement: `F_bulk = 0 ≤ δ_DP` trivially. **The comparator's real role is fork adjudication**: of the two readings of the A1 slot that the corpus has carried, the imported-compressible one predicts `0.032863` of the shear flux in excess (`252.8×` the DP bound — excluded), and the bound-response one predicts zero excess (consistent). The measurement selects the carve. This is a consistency-class, GR-shared outcome: **no chord is claimed** (prereg §0 row 8; the SM-counterfactual is stated there: GR's scalar sector is constraint-fixed too — on this axis the carve makes AVE GR-class, the opposite of distinct).

**§3.4 — G-RECON receipts (challenge-a-canonical-negative, per banked lane).**
- **#761:** verdict UNCHANGED on its premises — *"the elastic-solid structure of the AVE vacuum actively FORCES the opposite"* is TRUE: IF the vacuum is the imported compressible solid, Reading A follows. Premise changed: the elastic-solid longitudinal sector (K=2G + ball-and-spring `W`). Import receipt: #261 + `eq_axiom_3.tex` curl-only + LC-1(a) `G_vac = ρc²` matching-condition. Under the carve #761 IS the diagnosis: it proved the import and the pulsars cannot coexist — modus tollens, the import withdraws.
- **#919:** verdict (`RADIATIVE(2)-on-the-order / NOT-DERIVABLE-on-the-amplitude`) unchanged on its premises; the ORDER result (quadrupole-lowest, `Δℓ = 0`) survives as a statement about the response field's source moments. Premise changed: the P Green function under the moments. Receipt: the partition's own `[import]` tag (`q1-pulsar-hardening.md:215`) + R24's CONTESTED anchor.
- **#927:** verdict unchanged (MERGED; overlap structure is mathematics). Premise changed: the existence of the "propagating common mode" the overlap couples to. Receipt: the mode's speed rows trace to `constants.py` imports (FLAG-A block `:770-781`), not to a substrate derivation.
- **#930:** verdict unchanged on its premises (the re-cut was correct: on the compressible premise CH-0 fires and the seal cannot reach it). Premise changed: same as #919's. Receipt: #930's own §1.5.0 derivation of CH-0 consumes `κ_env² = (2/3)(c_S/c_P)⁵` — the imported partition — as its flux converter.
- **½/¼ over-determination check (seductive-narrative guard):** the carve explains the BULK-sector incoherence and nothing else — it does NOT absorb the FLAG-CEFF shear-form disagreements (§4, stated as unexplained), does NOT touch the α-echo, G-value, or m_e imports, does NOT rescue any unrelated negative (the birefringence, atom-Q, protein, HOPF closures all stand). One import, its own fingerprints, nothing more. **G-RECON: PASS.**

## §4 — THE PROVENANCE CASE (deliverable 4 — the carve must EXPLAIN the incoherence, not delete it)

**The phantom mechanism, one sentence:** a slot with no independent physical referent, modeled as if it had one, cannot return consistent answers to questions about the referent — every property of the phantom must be filled in from OUTSIDE, and independent outside sources disagree. Four fingerprints, each with its mechanism sentence:

1. **K = 2G had to be imported (#261).** Under the carve this incoherence is EXPECTED because: the bulk modulus is the phantom's stiffness — a property of a reservoir that is not there. Both native derivation routes (crystalline geometry; constitutive law) returned NOT-FORCED because there is no substrate fact for them to find; the value came from GR's trace-reversal because GR is where the FORM of the response's static duties (the /7 chain) was calibrated from. The corpus's own meta-finding (FORM-deriving/VALUE-importing, #262-264) carried the tell: the one "modulus" that is pure VALUE-import at the RELATION level (`form-deriving-value-importing.md:98-108`: a separately-tagged constitutive-FORM import edge) is the phantom's.
2. **The radiation partition had to be imported (Aki-Richards).** EXPECTED because: a P/S energy split is a theorem about two coexisting propagating channels; with one channel a phantom there is no substrate-native partition to derive, so #919 could only anchor at value level to compressible-rock seismology — canon's own sentence flags the category error's edge: *"real elastic solids are compressible and radiate … **B2 (incompressibility / source-vanishing) must be DERIVED, not assumed**"* (`q1-pulsar-hardening.md:217`). This lane is that derivation — and it lands on the constraint class, where the rock analogy's premise (a material that compresses) is the exact clause the vacuum lacks.
3. **FLAG-A / the wave-speed fracture (`√2c` vs `√(10/3)c`), and FLAG-CEFF's bulk members.** EXPECTED because: "what is the bulk wave's speed?" has no referent, so the corpus accumulated TWO irreconcilable answers — the `√2c` "port mode" (which LC-1 adjudicated NOT-A-WAVE: *"constitutive, not a Bloch branch"*; canon already fenced it out of every wave operator, coldq prereg `:262`) and the `√(10/3)c` "P-wave" (which `srs-band-structure.md:116` already labels *"NOT lattice-emergent"*). The register could only resolve the fracture STRUCTURALLY (two columns, FLAG-A) because there was no physical fact to resolve it WITH. Under the carve both labels dissolve correctly: `√2c` survives as a constitutive RATIO in the bound sector's near-field/reflection bookkeeping (its P9/halo role — exactly where canon already confined it), and `√(10/3)c` names nothing. **Honesty clause (frozen): FLAG-CEFF's `c_shear`-form members (forms 1/2/3 of `operators.md:218-225` — `c₀√S` vs `c₀√(1−A²)` vs `c ≡ const`) are a SHEAR-sector scaling disagreement the carve does NOT explain and does not claim to**; the carve speaks only to form 4's class (`c_eff = c₀/√S`, the FDTD *core* speed — the coded phantom's internal speed, `claim-quality.md:396`) and to why the bulk member of the family could never converge.
4. **FLAG-W's sign contradiction (vent vs jam).** EXPECTED because: the SIGN of a wall's reflection on the compression line is a property of the phantom's impedance — with no independent bulk wave, `Z_bulk = ρc_bulk` is a formula with a free slot, and each leaf filled it from a different projection/profile. Wall-taxonomy §10 dissolved the contradiction as *"plane/projection/profile-relative … not a contradiction"* — the carve explains WHY only relativized statements were ever derivable: the sign question has no absolute answer because its subject has no absolute existence; the ENERGY axis (`|Γ| = 1`, zero transmitted flux — the only axis #930's seal consumed) is the one axis with a referent (no port), and it is exactly the axis all three leaves agreed on.

**The two-duties exhibit (the import caught doing contradictory jobs):** `cauchy-implosion-resolution.md:15` uses K=2G to *"guarantee that the spatial substrate is **incompressible** and thermodynamically stable"* while `gauge-boson-masses.md:38` (Grant-ruled 2026-08-03) uses the same K=2G to rule the vacuum *"**definitively compressible**"* — and the compressible reading then mints the P-branch that the pulsars exclude. One import, both duties, opposite adjectives. Under the carve both leaves' JOBS are served without the contradiction: stability is kinematic (no volumetric DOF to implode), and "compressible" was the phantom's adjective, not the substrate's. (The 2026-08-03 ruling's ARITHMETIC — `ν(2G) = 2/7 ≠ 1/2`; no finite K is incompressible — is untouched and correct; the carve is not a finite-K statement, and the ruling's premise-frame is one of the sites routed to the demotion sweep.)

**What the phantom's paper trail buys the carve:** every incoherence above is a PREDICTION of the bound-response reading (a phantom generates exactly this signature: un-derivable parameters, fractured speeds, convention-relative signs, imported partitions) and a COINCIDENCE under the compressible reading (four independent embarrassments). **G-PROV-4: PASS — four exhibits, four mechanism sentences, the not-explained remainder stated.**

## §5 — THE ENGINE IMPLICATION (deliverable 5 — FLAGGED, NOT EXECUTED; architecture note only)

**Receipt that nothing was executed:** `git diff --stat 9230f434..HEAD -- src/` is EMPTY (gate G-ENGINE-FLAG; the driver and checker live under `research/drivers/`).

**Where the phantom is coded (named sites, from the two-method sweep):** `src/ave/core/constants.py:768-781` (`V_LONG = √(2G/ρ)`, the K=2G block, the FLAG-A note), `:397/:626` (`N_NU`/`NU_VAC = 2/7` — static uses, would NOT move), `src/ave/core/crystal_engine.py:52-102` (the two-branch *"bulk V ⊗ shear w"* elastodynamic core; `c_L²/c_T² = 10/3`; electron docstring *"trapped LONGITUDINAL bulk-modulus acoustic resonance"*), `src/ave/core/observable_battery.py:539` (`λ = (4/3)G` at K=2G in the energy ledger), `src/ave/solvers/graded_vacuum_network.py:60-68` (`Z_bulk/Z_shear = √(10/3)` + the compounding flag), `src/ave/solvers/radial_eigenvalue.py:1791` (K=2G scale loading), `src/ave/regime_2_nonlinear/seismic.py` (P-wave content by name), plus the F4-family wall/Γ_bulk sites in `cavitation_flow.py` / `sonic_horizon_flow.py` / `bulk_rarefaction_sector.py` / the `chiral_lattice_v*` line (wall/boundary roles — largely the SURVIVING class).
**The constraint form each would take (IF Grant ratifies the supersede — a separate, gated workstream):** the bulk-slot dynamics (`½K(∇·u)²` + independent V-branch evolution) re-homes to a **constraint solve**: `∇·u` fixed by the source field via an elliptic step with the A1 amplitude as multiplier — the incompressible-solver pattern (Helmholtz/divergence-free projection per step), with the A1 accounting (mass bookkeeping, enclosed-flux charges) READ from the constraint field rather than evolved. `V_LONG`/`c_L` demote from speeds to constitutive ratios of the bound sector's near-field bookkeeping. **Which tests/gates would move:** anything consuming P-propagation as physics — the breathing-source longitudinal-fraction receipts (#761 Leg-C `f_long = 0.982`), lattice `c_P` measurements (`v/c ≈ 1.45` cardinal-axis class), bulk-wave Cherenkov thresholds (port-register P6's `1.13c` bulk row), `Γ_bulk` rows READ FROM WAVE RUNS (the #767/#770 deep-rail engine measurements — which the stencil fence already quarantines for this lane), and the engine-acceptance rows keyed to the two-branch core. Anything consuming statics (`ν = 2/7` projections, grades, walls-as-bias-conditions) would NOT move. **This paragraph is a flag. No part of it is executed, scheduled, or assumed.**

---

## §6 — THE CONSUMER AUDIT (deliverable 1)

**Method (gate G-CONSUMER-2M).** Enumeration: the driver's two named engines (GNU `grep -rniE`, one invocation per family/scope, no glob pathspecs; Python `re` on an independent `os.walk`) over the CANON scope (`manuscript/` + `src/ave/`; `research/`+`_orchestration/` swept separately as frozen-snapshot RECORD scope, enumerated-not-reclassified) — 8 pattern families, per-family engine counts and disagreements shipped in `bound_response_carve_results.json` (canon disagreements: **0** in all 8 families; instrument-liveness controls: **all 4 found by BOTH engines** — `instrument_live = true`). Classification: the 308-file canon union classified by a 10-agent panel (workflow `wf_506bf34d-498`) under the FROZEN §6 bins, then an **orchestrator adjudication pass**: all 59 DIES rows, all 52 uncertain rows, and both INCONSISTENT candidates re-read against source; every quote TABLED BELOW byte-re-verified by the orchestrator. Full enumeration (349 classified consumer rows + per-slice NOT-A-CONSUMER summaries): [`bound_response_consumer_audit.json`](drivers/bound_response_consumer_audit.json).

**Totals: 349 consumer rows — 59 DIES-WITH-THE-PHANTOM · 185 NEEDS-RE-DERIVATION · 105 SURVIVES-AS-RESPONSE · 0 BOUND-RESPONSE-INCONSISTENT candidates survived adjudication.**

**The load-bearing rows, by the brief's named families (verbatim quotes byte-checked at these anchors):**

| Site | Verbatim (trimmed) | Bin |
|---|---|---|
| **— the √(10/3)c radiative rows —** | | |
| `port-register.md:49` (ch-3 radiative column) | *"$\sqrt{10/3}\,c \approx 1.83c$ (isotropic-solid P-wave; the $4G/3$ shear term cannot be dropped for a real far-field wave)"* | DIES |
| `cosserat-mass-gap.md:132` | *"**A FOURTH speed exists** … $c_L = \sqrt{(K + \tfrac{4}{3}G)/\rho} = \sqrt{10/3}\,c \approx 1.826\,c$"* | DIES |
| `srs-band-structure.md:116` | the `√(10/3)` row — its own label *"A **K=2G RE-EXPRESSION** (GR-imported, PR #261), **NOT lattice-emergent**"* already carries the provenance | DIES |
| `src/ave/core/constants.py:778` | *"the full compressional (P) wave is c_L = √((K + 4G/3)/ρ) = √(10/3)·c ≈ 1.83c"* (+ `:779-780` *"two are distinct physical longitudinal modes, both retained"*) | DIES |
| `gw-propagation-lossless.md:48` + `08_gravitational_waves.tex:107` | *"the framework additionally predicts a bulk (longitudinal P-wave) admixture radiating on top of the shear decay at O(1) coupling"* | DIES |
| `vol9/ch9-mechanical-characteristics/index.md:11` | *"the **solid longitudinal P-wave** $c_L = \sqrt{(K + 4G/3)/\rho} = \sqrt{10/3}\,c_0$"* (KEEP-BOTH row's P-member) | DIES |
| `vol9/ch17 engine-acceptance-suite.md:175` + `vol9/claim-quality.md` sup-3kq9w7 | *"propagating longitudinal-BULK (A1 dilatation) compression mode IS REAL on the extended medium"* [CHORD tag] — the sup's own text: *"the DOF is added-by-design"*; quality `*pending*` (NO solidity ever rode the lift) | DIES (chord content) — **top routed item**: re-scope to engine-capability receipt; and its own T1.7 history (*"the srs vector-TLM carried 2 transverse DOF only"*) is a receipt FOR the carve |
| `port-register.md:74` (bulk Cherenkov `≈1.13c`) | bulk threshold row only; EM/shear members untouched | DIES |
| `07_quantum_mechanics_and_orbitals.tex:1051` | *"The c_P > c mode is a phase-information channel, not an energy channel"* — the mode-existence half dies; the no-energy half becomes the DERIVED truth (no port) | DIES (existence half) |
| **— the √2c port rows —** | | |
| `port-register.md:56` | *"The PORT/impedance speed $\sqrt2\,c$ … governs reflection at a boundary (saturation wall, TIR) and reactive near-field storage (the DM halo, §2 row P9)"* | SURVIVES (the roles ARE the bound-response roles; already fenced out of every wave operator — coldq `:262`; LC-1 3a NOT-A-WAVE) |
| `port-register.md:35` + P9 row `:77` | the halo *"stores/loads … NOT a port"* | SURVIVES (it IS the carve's seed statement, R38) |
| `vocabulary-register.md:727` (`def-gv1net`) + `graded_vacuum_network.py:60` | `Z_bulk = √2·ρc₀` / `Z_bulk = ρc_L` as FORMULAS | NEEDS-RE-DERIVATION (prereg's named exemplar: the bound sector's boundary impedance re-derives from the reflection row, not from ρ×speed) |
| `cosserat-mass-gap.md:122` (`clm-uu1qbo` A1 row) | *"bulk $K$ ($K = 2G$ magic angle) … $\sqrt{2}\,c_0$"* | NEEDS-RE-DERIVATION (the A1 sector identity survives; its speed/modulus wiring is the import) |
| **— Regime-IV / wall rows —** | | |
| `bulk-impedance-at-saturation-boundary.md:39-42` (vent leaf) | `Γ_bulk → −1`; *"bulk-longitudinal perfect reflector"* | SURVIVES in sign-invariant `\|Γ\| = 1` form (exactly as #930 consumed it; signed/speed labels re-scoped per wall-taxonomy §10) |
| `21_black_hole_interior_regime_iv.tex:50-52` | shear+bulk channel collapse at rupture | SURVIVES (strain-kernel collapse = the banked ruled text; past-wall content out of carve scope) |
| `solver-toolchain.md:214` | *"Longitudinal waves \| Propagate \| May propagate"* (cold-column) | DIES (the flat propagation assertion) |
| **— V_snap mechanical vs electrical —** | | |
| `vocabulary-register.md:751` + INVARIANT-S2 sector split | V_snap = the A1 bond-compliance normalizer (`C_eff = C_0/S` diverges at `A² = 1`) | SURVIVES (a reactance of the BOUND sector — reactance without a wave; the prereg's named survival) |
| `ligo-gw-saturation-ratio.md:32` | *"The channel LABELS themselves are FLAGGED, NOT PICKED"* | stays FLAGGED (not adjudicated here) |
| **— seismological partition rows —** | | |
| `translation-circuit.md:157` + `translation-elastodynamics.md:19` + `index.md:34` | *"**P/S far-field partition** of an isotropic (Poisson) elastic solid — Aki–Richards moment-tensor radiation ↔ A1/T2 far-field radiation partition"* | DIES (provenance case (d): no native partition exists; the spoke's "means-test PASS at value level" loses its substrate-side member) |
| `translation-circuit.md:974` (§12.3 incumbent) | *"A scalar/longitudinal field radiates at quadrupole order … (standard acoustic multipole)"* | DIES (the imported P-Green radiation claim) |
| `genesis-chord-falsification-ledger.md:104,:109` | the `F_bulk/F_shear ∈ [0.015, 0.177]` EXCLUDED verdict + the PROMOTED-LIVE closed-negative | NEEDS-RE-DERIVATION (register-of-imported-reading rows: re-read as the phantom's self-exclusion — receipts FOR the carve; demotion-sweep items) |
| `vocabulary-register.md:1084` (`def-pndenv`) | *"radiating at 2Ω into the gapless P-branch … κ_env² = A_ang(c_S/c_P)⁵"* | DIES (partition-consuming half) |
| **— the doctrine rows (the counter-evidence set, dispositioned) —** | | |
| `master-equation.md:26` + `vocabulary-register.md:870` (`def-l0ngdu` SOLID) | *"∇·u propagates; ∇·A is gauge"* | NEEDS-RE-DERIVATION (routed, leaf-untouched): the anti-Heaviside REAL-vs-gauge distinction SURVIVES (G-SCALAR-REAL); "propagates" rode the K-backed premise — re-derives as **"∇·u is bound; ∇·A is gauge"** — the constitutive difference canon itself named (*"the difference is constitutive, not kinematic"*) is the import |
| `the-abandoned-interior.md:22` (MYTH-GUARD) | *"a genuine longitudinal DOF"* | NEEDS-RE-DERIVATION: real sector ≠ independent DOF; the MYTH-GUARD's anti-deletion point survives INTACT (the carve keeps the scalar physical — §7 G-SCALAR-REAL) |
| `port-register.md:87` (Q1 row, Reading-A LIVE) + `:93` | the LIVE 9–110σ exclusion + the #761 chain | NEEDS-RE-DERIVATION (the exclusion re-homes onto the imported reading — the register's strongest self-receipt for the carve); `:91`'s Reading-B owed-mechanism debt is DISCHARGED by this lane |
| `gauge-boson-masses.md:38,:48` | *"definitively compressible"* (Grant 2026-08-03) + the ∇Λ observability threat riding the P-branch | NEEDS-RE-DERIVATION (the ν-arithmetic survives untouched; the premise-frame routes to the sweep — §4's two-duties exhibit) |
| **— notable adjudications (closest calls, stated) —** | | |
| `07_quantum_mechanics_and_orbitals.tex:41` (orbital mechanism) | *"its motion displaces the lattice, generating longitudinal acoustic pressure waves governed by the vacuum's Bulk Modulus"* | NEEDS-RE-DERIVATION at LABEL level, **not INCONSISTENT**: no orbital number consumes `c_P` (de Broglie λ rides ℏ/mv; the turning point rides `E − eV`); and the carve REPAIRS a latent incoherence — a free-running `1.83c` wave cannot stay attached to a `v ≪ c` electron; a bound response field co-moves by construction |
| W/Z *"acoustic mass ratio"* family (`appendices-overview.md:94`, `mathematical-closure.md:94`, `full-derivation-chain.md:302`) | `m_W/m_Z = 1/√(1+ν_vac) = √7/3` | NEEDS-RE-DERIVATION at LABEL level, **not INCONSISTENT**: the formula is a STATIC ν-combination (value untouched, import-status unchanged); "acoustic-mode" naming owes re-derivation |
| thermal DOS rows (`thermal-lattice-noise.md:34`, `delta-strain-cosmic-tcc.md:34`, `vol3/claim-quality.md:1220`) | 3 translational gapless acoustic branches thermally populated | NEEDS-RE-DERIVATION with a REAL physical consequence flagged: the propagating-mode count is 2 (transverse), not 3 — any equipartition/DOS count including a longitudinal branch is routed for re-derivation (low blast radius: the FT-1 closure already re-scoped the δ_strain magnitude) |
| `identity-break-test-design.md:59` | `Q_wall → ∞` α-free; *"radiation only in the transverse port"* | SURVIVES — and is an independent receipt FOR the carve (the longitudinal "cavity" never leaks because it has no port) |
| engine sites (`crystal_engine.py`, `cavitation_flow.py:5`, `sonic_horizon_flow.py:7`, `bulk_rarefaction_sector.py`, `unified_genesis_engine.py:38,:357`, `cosserat_field_3d.py:721,:1789`, `vacuum_engine.py:1653`) | the coded compressibility (independent bulk DOF, bulk vents, bulk PML/sponges, `c_L` in CFL) | DIES/NEEDS — the §5 engine-flag inventory; no run of these corroborates or refutes anything in this lane (stencil fence) |

**RECORD scope** (`research/` + `_orchestration/`): swept (per-family counts in the JSON), enumerated only — frozen snapshots and rulings are Q2/Rule-12 exempt and are NOT reclassified by this audit; the kill-chain docs among them are handled at §3 as premise-change re-reads.

**Negative control & completeness:** all four known-positive control sites found by both engines; the 8 families' canon engine-disagreement sets are EMPTY (the two engines returned identical hit sets — the cross-check exists to catch pathspec/escaping false-negative classes and found none); the grep-completeness memory's second-method discipline applied to every absence claim in this section. **G-CONSUMER-2M: PASS.**

---

## §7 — The frozen gate table (UNRUN ≠ PASSED; every gate run and scored)

| Gate | Frozen criterion (prereg §7) | Measured | Score |
|---|---|---|---|
| **G-QUOTE** | every ruled/frozen quote byte-checks vs source (two engines; branch quotes vs named commits) | **🔴 RE-SCORED AT TIER-2: FAIL-AS-FROZEN** (finding #10 CONFIRMED MAJOR): the FROZEN prereg §4 carries a **collaged quote** attributed to the LC-1 result — *"constitutive, not a Bloch branch … the speed fork is dead"* — whose fragments exist NOWHERE at `be04ea03` ("Bloch": 0 hits; "the speed fork is dead": 0 hits, whole tree). **Dated erratum (the prereg body is BYTE-UNTOUCHED per Rule 12):** the REAL text at the cited sections is `:337` *"**`NOT-A-WAVE`** — modulus/impedance quantity; governs boundary reflection + reactive near-field storage (P9 NOT-A-PORT); carries no inter-event energy"* and `:232-235` *"It is not a plane-wave eigenvalue of this medium"* / *"there is no fork on the radiative row at all."* The ADJUDICATION (3a = NOT-A-WAVE) is real and unchanged; the quote-integrity failure is mine, disclosed. Also repaired: two anchor slips in §1/§6 (LC-1 §-attributions) + the TKI-bridge gloss (finding #16: the projection-map line continues *"The transducer **changes units**; it does not erase the boundary"* — route (a2)'s no-preimage reading of it is a LANE INFERENCE, not a receipt) | **FAIL-AS-FROZEN / REPAIRED (erratum above)** |  <!-- rule12-freeze: base=e8d4e42e5049ed473f6b3b2c6e2f898694ab54fb region=above offset=0 lines=144 bytes=42902 sha256=ffe702828f8545a8b6425ae8960eb0489e43975ce3a364175d98df815e655e3f -->
| **G-CARVE-2M** | both §3 routes independently, agreeing on the bin; FORCES-vs-ADMITS stated | **🔴 RE-SCORED AT TIER-2** (finding #2 CONFIRMED MAJOR, verifier-reproduced spectrally): the axiom kinetic term is projection-blind (`½ε₀\|∂_t A\|²` includes `A_L`), so the receipted dynamics gives a zero-stiffness inertial FLAT DIRECTION, not constraint class — "FORCED" was a non-sequitur, and the needed multiplier structure has no axiom preimage (route (a2)'s own knife). What the routes DO agree on, honestly re-graded: the no-finite-ω-branch consequence + admissibility of the carve against banked statics. The controlled-source CLASS is underived → the route-(a) walk bottoms out in structure receipts cannot settle = the frozen NOT-DERIVABLE trigger | **RE-GRADED (fires NOT-DERIVABLE per its own frozen clause)** |
| **G-CONSUMER-2M** | two named engines, patterns printed (driver source), known-positive controls found by both; every site classified with verbatim quote | **🔴 RE-SCORED AT TIER-2: FAIL-AS-FROZEN, then REPAIRED** (findings #6/#11/#12/#14 CONFIRMED): 58 of 308 union files (100 hits) were never classified; the two "engines" consumed ONE shared pattern set (coverage-independence was never real — the second-method sentence at the original §6 close is retired); a counter-sweep found 5 canon + 1 coded sites the patterns missed outright (incl. two byte-twins of rows the audit itself classified); the record-scope exemption, the NOT-A-CONSUMER bin, and the static-import class were minted post-freeze (disclosed now as deviations, direction-of-effect: scope-narrowing). REPAIR SHIPPED: [`bound_response_consumer_audit_supplement.json`](drivers/bound_response_consumer_audit_supplement.json) classifies all 100 unclassified hits (71 = the disclosed `Aki`-substring pattern bug, noise; genuine rows tabled) + the 6 missed sites (bins unchanged in kind: DIES/NEEDS/SURVIVES families as before; no INCONSISTENT candidate among them). **The enumeration is PATTERN-BOUNDED, not exhaustive — the completeness framing is withdrawn; the demotion sweep should treat union+supplement as a floor** | **FAIL-AS-FROZEN / REPAIRED** |
| **G-STATIC** | each named banked success re-derived/re-read with its consumption inventory; fall condition applied verbatim | §2 table: 9 successes, inventories stated; NO banked success consumes a propagating bulk mode (two-method: driver families ∩ statics + dedicated context pull); one MECHANISM-level routed item (cauchy-implosion) — a mechanism swap, not a broken success | **PASS — no INCONSISTENT site** |
| **G-KILL-3** | all three items re-read against frozen wordings quoted in full; no banked bin re-scored; the LC-1 instantaneous-constraint honesty clause derived | **🔴 RE-SCORED AT TIER-2: FAIL-AS-FROZEN** (finding #1 CONFIRMED CRITICAL: the §3.1 "4-step" is analogies, not a derivation — no constrained Lagrangian, no multiplier equation, no retardation/cancellation theorem; the GR-parallel cite laundered *pure-gauge* out of `q1-pulsar-hardening.md:130`; the `clm-acdc07` closer is an input-only 0.55 frame). The frozen wordings/premise re-reads (§3.2–§3.4) stand; the UN-FIRE claim does not — it re-homes to enumerated-missing items (i)+(ii) | **FAIL-AS-FROZEN (original PASS retired)** |
| **G-PROV-4** | four exhibits each with a mechanism sentence; the not-explained remainder stated | §4: K-import, partition-import, FLAG-A/FLAG-CEFF-bulk, FLAG-W — four mechanism sentences + the two-duties exhibit + the T1.7 added-by-design receipt; NOT explained (stated): FLAG-CEFF's shear-form members | **PASS** |
| **G-SCALAR-REAL** | every canonical duty of the longitudinal scalar hosted by the bound response; "physical-and-bound ≠ deleted" derived | duties checked: halo added-mass (P9 — IS the bound reading), mass = A1 accounting (re-homed per R38; energy density is real, stored reactively), Gauss-undeleted reality (the bound field is the Gauss sector — MORE real than a gauge artifact, §1 a1), saturation re-engagement (`master-equation.md:16` — past-wall scope, untouched), electron-sector labels (label-level re-derivations, no number moves). The carve is the ANTI-deletion position: Heaviside excised the scalar as unphysical; the carve keeps it physical-and-bound. No un-hostable duty found | **PASS** |
| **G-SHEAR-UNTOUCHED** | T2 sectors byte-untouched; no shear-side adjustment | photon-at-c (#884) and the banked PM shear rate cited as-is; no step re-derives/re-scales/re-anchors shear; `F_bulk = 0` is not absorbed into shear (nothing to absorb — the total budget = shear budget, as observed) | **PASS** |
| **G-RECON** | per banked lane: verdict-unchanged + premise named + import receipt + re-read; no relitigation; ½/¼ tell checked | §3.4: four lanes, four receipts; over-determination check run (the carve claims the bulk-sector incoherence ONLY; unrelated negatives untouched) | **PASS** |
| **G-ENGINE-FLAG** | architecture note only; `git diff --stat src/` empty | §5; `git diff --stat 9230f434..HEAD -- src/` = empty at every commit of this lane | **PASS** |
| **G-NUM** | number check vs driver JSON; mutation receipt; `make verify` green in worktree | `bound_response_number_check.py` green; `--mutation-receipt` fires; `make verify` green at commit (see result-doc provenance note) | **PASS** |

**Frozen bin arithmetic applied (RE-CUT at Tier-2; the original application below is PRESERVED and OVERTURNED):** G-KILL-3 FAIL-as-frozen bars the DERIVED conjunction; G-CARVE-2M's re-grade fires its own frozen NOT-DERIVABLE clause (*"the route-(a) walk bottoming out … in structure that receipts cannot settle — with the missing adjudicating structure enumerated"*); G-SCALAR-REAL's PASS is retired with it (finding #3: scored without an energy functional — item (iii)); G-PROV-4 re-graded to the reduced independence count (finding #18). No INCONSISTENT trigger fired anywhere (unchanged — the strongest surviving statement). **Verdict: `NOT-DERIVABLE`, missing structure (i)–(iv) enumerated in the HEADLINE re-cut.** *(Original application, preserved: G-CARVE-2M ∧ G-CONSUMER-2M ∧ G-STATIC ∧ G-KILL-3 ∧ G-PROV-4 ∧ G-SCALAR-REAL ∧ G-SHEAR-UNTOUCHED ∧ G-RECON all PASS → BOUND-RESPONSE-DERIVED; NOT-DERIVABLE not reached.)*

## §8 — Verdict assembly, ledger, routing

**VERDICT (RE-CUT AT TIER-2): `NOT-DERIVABLE` — the adjudicating structure is missing; enumerated as items (i)–(iv) in the HEADLINE re-cut.** The carve is REFUTED nowhere (no INCONSISTENT site; every banked static success consistent; the premise-localization receipts stand) and DERIVED nowhere it counts (the controlled-source class needs the multiplier/Gauss structure, the no-signalling theorem, the energy functional, and the static-gain reconciliation — none receipted). The constitutive revision is therefore NOT proposed for ratification by this lane; what routes to Grant is the four-item derivation worklist + the repaired audit. *(Original verdict paragraph, PRESERVED per Rule 12 and OVERTURNED: `BOUND-RESPONSE-DERIVED` — FORCED on the structure / ADMITS+SELECTED on the totality; the constitutive revision proposed as a versioned supersede. The FORCED half is retired by Tier-2 finding #2; the SELECTED half survives only as: the pulsars exclude the imported reading — they do not by themselves derive the carve.)*

### §8.1 — Consistency-vs-emergence ledger

| Quantity | FORM | VALUE | Class |
|---|---|---|---|
| The carve (A1 slot = bound response field) | `[derived]` this lane (route a: receipted-dynamics walk; route b: constrained re-derivation) | — | **ADJUDICATED — derivation** (FORCES on structure / ADMITS+SELECTED on totality) |
| `F_bulk` under the carve | identity (no channel) | `0` — trivially `< δ_DP = 1.3e-4`, `< δ_HT = 0.0016` | DEMONSTRATED (entailed arithmetic, declared at prereg §6) |
| Shear-only `Ṗ_b` = PM rate | `[canon-import]` R1, cited as-is | HT `0.9983 ± 0.0016` | consistency (GR-shared; **no chord**) |
| The partition `(2/3)(c_S/c_P)⁵ = 0.032863` + floor family `[0.0152, 0.0455]` + `252.8×`/`20.5σ` | reproduced by driver as SUPERSEDED-INPUT record | per JSON | the imported reading's self-exclusion (record) |
| `ν_vac = 2/7`, the /7 chain, `m_W/m_Z = √7/3` | unchanged imports (STATIC uses) | unchanged | consistency (status byte-unchanged by this lane) |
| Consumer-audit classification (349 rows) | `[audited]` two-method + panel + adjudication | 59/185/105 | audit (routed to the demotion sweep) |
| LC-1(a) `G_vac = ρc²` matching-condition; LC-1 §1.4 `√(4/3)`-at-K=0 | `[cited]` branch state @ `be04ea03` (HELD) | — | consumed receipts (branch-state, tagged) |
| The A1 slot's remaining physics (bound-field profile laws, the response's constitutive form, the eigenmode-existence open item from #261) | — | — | OPEN (routed; the carve does not close them) |

No emergence-class claim. No chord on any outcome (§0 row 8). Mints nothing; edits no leaf; changes no solidity.

### §8.2 — Routing (nothing executed here)

1. **The verdict + this doc → the orchestrator/Grant** for the R38 ratification decision (the versioned constitutive supersede). The demotion sweep (HELD per R38) takes §6's audit annex as its worklist — the 59 DIES + 185 NEEDS rows are the sweep's canon sites; **top item: `sup-3kq9w7`/T3.1's CHORD tag** (re-scope to engine-capability receipt; no solidity moves — the sup was `*pending*`).
2. **The GW-formation walk (R24, pending):** §1(a4)'s offer — the carve as a candidate resolution of the shear-displacement vs saturation-modulation tension — routed as an INPUT; this lane asserts nothing over the walk.
3. **LC-1 / #913 (HELD):** §3.1's structural un-fire routed to the arc scope decision; the arc brief's LC-1(b) question ("gapped, confined, or sourceless?") has the fourth answer on record.
4. **#919/#770/#767 (HELD/OPEN):** the cage fork and the deep-rail engine measurements become engine-implication items (the coded phantom's receipts) — routed with the engine flag, not adjudicated.
5. **The thermal-DOS mode count (2 vs 3 propagating branches)** — §6's flagged physical consequence — routed as a follow-on re-derivation candidate.
6. **Tier-2:** this lane's own adversarial review runs before any CLEARED wording (provenance note below); pre-flagged primary targets: the route-(a) TKI-bridge step (a2) (the "no preimage in the axiom Lagrangian" claim), the §3.1 instantaneous-constraint reconciliation, the §6 orbital-acoustics adjudication (NOT-INCONSISTENT call), and the FORCES/ADMITS grading.

---

> **★TIER-2 REVIEW-REPAIR ADDENDUM (2026-08-10 — this doc EDITED per Rule-12 repair-and-bank; the frozen prereg is BYTE-UNTOUCHED; the original DERIVED verdict is PRESERVED above and OVERTURNED).** Five lenses (physics-knife / frozen-criteria / receipts+numbers / canon-fidelity / symmetric-standard+seductive-narrative), 26 agents, per-finding adversarial verify (workflow `wf_e993ce37-f91`). **Disposition: 21 raw findings → 19 CONFIRMED (1 CRITICAL, 6 MAJOR, 12 MINOR), 2 REFUTED. The CRITICAL + two MAJORs are verdict-bearing; the verdict is RE-CUT `BOUND-RESPONSE-DERIVED` → `NOT-DERIVABLE` (missing structure enumerated).**  <!-- rule12-freeze: base=e8d4e42e5049ed473f6b3b2c6e2f898694ab54fb region=above offset=0 lines=43 bytes=10605 sha256=c8a36e2c7f30c5d470293a2010e52bebeeabe74c2b53f34aafa10887435f09c9 -->
>
> - **#1 (CONFIRMED CRITICAL — the re-cut's driver):** §3.1's honesty clause asserted-not-derived against G-KILL-3's own frozen wording; the GR-parallel cite laundered (*pure-gauge* dropped — the sole property doing GR's causality work, and the one the carve denies for the real `∇·u`); the no-port⇒no-energy step consumed its own defining identity; `clm-acdc07` (input-only, 0.55) cannot carry an arc-terminating verdict. Repair: the un-fire claim retired; items (i)+(ii) enumerated. The verifier's own scoping: EVIDENCE-VOID, not CONCLUSION-WRONG — *"What is void is the DEMONSTRATION that the elliptic bound sector is not an inter-event channel, not the carve itself."*
> - **#2 (CONFIRMED MAJOR):** the axiom kinetic term is projection-blind — verifier reproduced spectrally (`ε₀Ä_L = 0`; `A_L(t) = A_L(0) + V_L·t` to `1.6e-10`): a zero-stiffness inertial flat direction with conserved momentum, a STATE variable. "FORCED" retired; the flat-direction-vs-constraint fork IS missing item (i). The headline's "longitudinal kinetic+potential pair has no preimage" was algebraically wrong on the kinetic half — repaired to potential-only in the surviving statement.
> - **#4 + #17 (CONFIRMED MAJOR):** the frequency-gated ontology (dynamic `W` deleted, static `ν = 2/7` retained) asserted, never derived; the kinematic-constraint-vs-K→∞ distinction is provenance-level, not response-level (identical static load-response law) — missing item (iv). The `3/7` trace-factor reconciliation is nowhere on the branch (driver banks it as a receipt uncited by any verdict sentence — disclosed).
> - **#6/#11/#12/#14 (CONFIRMED MAJOR/MINOR — audit):** 58 union files / 100 hits unclassified; 5 canon + 1 coded sites missed outright (two byte-twins of classified rows — a recall hole); one shared pattern set across both engines (coverage-independence never real); post-freeze scheme changes (record-scope exemption, NOT-A-CONSUMER bin, static-import class) now disclosed as deviations with direction-of-effect (scope-narrowing). Repair: supplement annex shipped; completeness framing withdrawn; PATTERN-BOUNDED label applied; `Aki`-substring pattern bug disclosed (71 noise hits).
> - **#10 (CONFIRMED MAJOR):** collaged quote in the FROZEN prereg §4 (LC-1 3a) — erratum in the G-QUOTE row above; prereg byte-untouched; adjudication (NOT-A-WAVE) unchanged.
> - **#18 (CONFIRMED MAJOR):** "four independent embarrassments" over-counted — reduced to ~1.5–2 independent exhibits (§4's independence sentence is retired; the mechanism sentences survive at reduced weight; FLAG-W discriminates nothing between readings; FLAG-A was mundanely resolved pre-carve; partition-import is downstream of K-import).
> - **Confirmed MINORs, dispositioned:** #3 (G-SCALAR-REAL without energy functional → item (iii); the "static dress has ZERO energy density under pure curl-only reading" observation is REAL and folds into (iii)); #5 (orbital-acoustics call: limb-1 refuted by the verifier — the annex DID sweep the ch7 chain — limb-2 stands: "label level" understates a mechanism-level debt; wording amended here); #7 (post-freeze scheme changes — disclosed above); #8 (patterns not printed in result — they live in the driver source at `FAMILIES`; deviation disclosed); #9 (G-QUOTE freeze-time claim rested on unshipped session receipts — re-scored above); #13 (anchor slips — LC-1 §-attributions corrected to verbatim-fragment cites without section numbers); #15/#19 (headline conditioning — the polarity-flip and "falsification of the IMPORT" sentences now read under the preserved-overturned banner; the honest form: the pulsars exclude the imported reading; they do not derive the carve); #16 (TKI gloss — full quote restored in the G-QUOTE erratum: *"The transducer changes units; it does not erase the boundary"*; the no-preimage step is a lane inference, and it is now part of missing item (i)).
> - **REFUTED (2):** the Z_bulk-formula post-hoc-bin charge (the prereg froze that exemplar pre-result — verifier: *"falsified verbatim by the frozen prereg"*) and one frozen-lens grammar charge (freeze integrity verified by blob hash `ab9ac5a1…`).
>
> **Net effect: `BOUND-RESPONSE-DERIVED` → `NOT-DERIVABLE` (missing structure enumerated).** The carve survives as R38's walk-level candidate with a four-item derivation worklist; the kill chain stays GATED (not un-fired, not re-fired); the audit + premise-localization + no-finite-ω-branch partials are banked at their stated grades. The demotion sweep continues to HOLD on Grant's disposition of THIS re-cut (R38's "one sweep, the complete arc").

> **Result-doc provenance.** Frozen prereg committed ALONE + pushed at `0722a8a3` (freeze-by-push) BEFORE any derivation content, audit output, driver code, or lane number existed — byte-untouched since (blob `ab9ac5a1…` verified at Tier-2), carrying one disclosed quote-integrity erratum (the §4 LC-1 collage — G-QUOTE row). Driver committed before the result; two full driver runs byte-identical (JSON digest `983fe810902724fd1807687740dc863f5a7cef47da3651e61486ad58798ad297`). Two-method receipts as scored: the carve = route (a) + route (b); the audit = two engines + panel + orchestrator adjudication; algebra = exact fractions + floats. Engine `src/ave` byte-untouched (`git diff --stat` empty). Interior/past-wall = declared out of scope at every use. `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. Companions: the frozen prereg, the brief, R38, #761/#919/#927/#930 at their frozen homes, LC-1 @ `be04ea03`, #919 @ `ba1d872d`, the #261 record, the consumer-audit annex JSON.

---

## ★ DATED SURFACE-NOTE (2026-08-10, R43) — the `eq_axiom_3.tex` quote at `:35` is a VACATED CITE

**Surface-note only; nothing above is rewritten** (dated result doc). Added because
this doc's `:35` introduces its quote with the words *"canonical, single source of
truth"*, and the sentence it quotes is no longer in that source.

**What `:35` quotes:** *"the standard Maxwell Lagrangian … recovered as the
substrate's effective action in the linear regime."*

**What happened:** under R43 (Tier A RATIFIED,
`_orchestration/docket-entries/2026-08-10-ruling-r43-ratification.md`) that sentence
was **replaced** at `manuscript/common_equations/eq_axiom_3.tex:22`. The written
action carries no $A_0$ term, so it is the **temporal-gauge (Weyl, $A_0$-free)** form:
"standard Maxwell" is TRUE of the dynamics on the Gauss-constrained initial-data
surface and FALSE of the action. `:35`'s quote is therefore a **vacated cite** — the
cite drifted past the state it argued about.

**What SURVIVES, and it is the whole load-bearing point of `:35`:** the proposition
this doc actually consumes is that **the axiom-level dynamics is curl-only**, hence
*"A curl-free configuration contributes NOTHING to the potential term … no restoring
force, no eigenfrequency, no branch."* That is **unaffected** — indeed the repair
strengthens it, since the repaired `:22`/`:27` say explicitly that the action is
curl-only with the Gauss constraint absent. **No verdict, bin, or receipt in this
document moves.** Only the quoted label is stale.

**Provenance of this note (stated because the routing was wrong upstream):** the
consequence sweep binned this site TRUE-UNDER-REPAIR, i.e. correctly OUTSIDE the
NEEDS-RESCOPE repair set — but it did so under the label *"(frozen; surface-note
class)"* at
`research/2026-08-10_bound-constitutive_sweep-record.md`:70, and **this document is
not frozen** (it was itself edited 2026-08-10 under a Rule-12 repair-and-bank
addendum). The right reason for a surface note here is **vacated-cite**, not frozen.
Recorded so the next sweep does not inherit the mislabel.
