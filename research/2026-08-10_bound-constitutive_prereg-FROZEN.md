# THE BOUND-SECTOR CONSTITUTIVE LAW (R41) — PRE-REGISTRATION (FROZEN)

**Date:** 2026-08-10 · **Branch:** `lane/2026-08-10-bound-constitutive` · **Base:** `origin/main` @ `6c291196`
**Lane brief:** [`_orchestration/2026-08-10_bound-sector-constitutive-brief.md`](../_orchestration/2026-08-10_bound-sector-constitutive-brief.md) (Grant GO, R41 — [`2026-08-10-rulings-r40-r42.md`](../_orchestration/docket-entries/2026-08-10-rulings-r40-r42.md)).
**Class:** DERIVATION lane (analytic; algebra + purpose-built toy-lattice receipts; two named engines). **Mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; edits no KB leaf, register, ledger, or ruling; changes no solidity; all propagation ROUTED.** The item-(0) axiom-text repair and any new-axiom candidate are **PROPOSALS (Grant-ratification objects), not edits and not lane verdicts.** Engine `src/ave` byte-untouched (read-only imports where a receipt needs a constant).
**This document commits ALONE and pushes FIRST (freeze-by-push), before any derivation content, driver code, or lane-produced number exists on this branch.**

---

## §0 — Standard Vacuum Analysis header (SVA v0.2-pilot; all 11 rows)

 1. **SECTOR / OWNERSHIP:** Object under derivation = the **missing constitutive structure of the A1 bulk/dilatation slot** — the #935-enumerated items (i)–(iv) — plus, upstream, **the axiom text itself** (item 0: `eq_axiom_3.tex:22/:27`). NOT the T2 shear/EM channels (byte-untouched at their banked rates), NOT the Cosserat micro-rotation channel (gapped, untouched; **rotational fence-reach STOP active** — if any step routes bulk-slot content through the micro-rotation channel, STOP and route to Grant). TWO-"3"s guard active (A1 dilatation-MASS ⊥ Cosserat (2,3) WINDING; `master-equation.md:20`). **The two-variable split is DECLARED load-bearing and kept:** the displacement dress `u₀ = B/r²` (div-free exterior, enclosed flux `∮u·n̂ = 4πB`; `research/2026-08-09_screening-theorem_result.md:63,:68`) and the operating-point bias grade `ε₁₁ = A(r) = r_sat/r = 7GM/c²r` (the Ax4 amplitude; `vol3/claim-quality.md:1254`) are **TWO OBJECTS**; every derivation step below states WHICH object it pins. Sector-ownership consequence: mass = A1 stays the accounting (PR#260); charge = Cosserat winding stays Ax2's (never cross-wired).
 2. **REGIME / PHASE-STATE:** Cold-linear (Regime-I, sub-yield, lossless-reactive) constitutive derivation throughout. **PAST-WALL states are OUT OF SCOPE** (Ax1 R7 phase-scope, axiom-register `:151`): no law derived here is evaluated past a wall. DC bias grade `A(r)` = the operating point (canon). MODE: constitutive-structure derivation + axiom-text adjudication — no new empirical comparison is run; banked comparators enter only as frozen context, never as inputs to any derivation step (see row 8 + G-CUT-DERIVED).
 3. **CIRCUIT STATEMENT (before any framework word):** A floating LC network's common-mode rail has no return path. This lane derives, in order: **(0)** what the network's own written action actually is (the schematic-vs-label audit); **(i)** what pins the rail's bias distribution to the sources — the incidence/KCL structure plus the constitutive assignment that makes it non-vacuous (TH-5: incidence alone is "ontologically silent" — the content is WHAT deposits divergence, not that it telescopes); **(ii)** why a rail with no return path carries no signal at any frequency — derived as a property of the network equations (locality + characteristic structure), not asserted from the no-port vocabulary; **(iii)** where the rail's bias energy physically books (the bias-network energy ledger); **(iv)** why the DC load-response of the rail is finite and /7-structured while AC transmission on it is absent — the bias-tee question: the cut between "banks statically" and "does not radiate" must fall out of the network equations, not out of where the pulsars need it.
 4. **PLANE & PROJECTION:** No signed Γ is consumed anywhere. Wall rows enter only as sign-invariant `|Γ| = 1` boundary receipts. Flux statements live on the far-field radiation-zone plane; the **near-zone (1/r², reactive) vs far-zone (1/r, radiative) split is declared load-bearing for item (iv)** and is a candidate DERIVED home for the cut (frozen as an expectation in §6, not a conclusion).
 5. **CONSTITUTIVE PROVENANCE (each law tagged):** `K = 2G` — **IMPORTED** (#261 two-gate NEGATIVE, Grant-ratified *"Imported, both legs. End of line."*; gates quoted §4). `G_vac = ρc²` — **MATCHING CONDITION at value level** (LC-1 §1.1 @ `be04ea03`). The axiom dynamics — **CANON, curl-only** (`eq_axiom_3.tex:18`), with the **:22/:27 labels UNDER ADJUDICATION in this lane (item 0)**. `ν_vac = 2/7` — **IMPORTED-DOWNSTREAM** of K=2G (`one-seventh-impedance-projection.md:18`). The canon elliptic gravity solve `−∇·[(c⁴/7G)D(A)∇ε₁₁] = T₀₀` — **FORM in canon, STATICS DESIGN CHOICE not derived theorem** (`research/2026-08-07_a1-port-sourcing_result.md:325-328`; `src/ave/gravity/backreaction.py:12-17` Komar-ruled). The srs DEC operator set — **CANON, CI-certified** (`div = −∂₁` exact adjoint, `∂₁∂₂ = 0` combinatorial; #512, `engine-capability-map.md:319`). The electrical↔mechanical variable maps — **RATIFIED-BUT-COLLIDING** (u↔A per def-uatk1s `:885`; u↔E per def-tk1xfm `:438`; x↔q per the six-row ξ dictionary `translation-circuit.md:17-26`; A↔momentum-class per `dielectric-lagrangian.md:40`): declared as the lane's recorded FORK (§6). Ax4 kernel — **SHAPE-DERIVED (conditional)** (`eq_axiom_4.tex:50`); its A ≡ ε₁₁/ε_yield identification in the gravity sector is canon (`saturating-modulus-and-backreaction.md:50-52`).
 6. **ENERGY LEDGER:** The lane DERIVES the bound sector's ledger (item iii) — until it lands, no energy claim is consumed from the overturned #935 clause. Frozen crux it must resolve: **the static dress scores ZERO energy in both receipted storage terms** (curl-only potential = 0 for curl-free config; kinetic = 0 for static config; #935 Tier-2 finding #3, `bound-response_result.md:220`; `dielectric-lagrangian.md:12-14`), yet mass = A1 and the halo P9 row require real, positive, stores-and-returns energy (Ax3-lossless, `eq_axiom_3.tex:24`). Legal ports unchanged (T2 radiation, detector terminations, Regime-IV out of scope).
 7. **CALIBRATABILITY:** Verdicts are STRUCTURAL (derivations close or they don't). Numbers appearing: (a) banked dimensionless ratios reproduced as frozen-context records (ν(K=2G) = 2/7; trace factor 3/7; v_L/v_T(K=0) = √(4/3) = 1.1547005383792515; v_L/v_T(K=2G) = √(10/3) = 1.8257418583505538 — LC-1 receipts); (b) toy-lattice receipts in lattice units (front speeds, flux ratios, conservation defects) with grid-order tolerances declared in §6. **No new dimensional magnitude is minted** (Step 3.5 scope statement: no scaling-law-magnitude expectation is part of any verdict; dimensional analysis is therefore satisfied by the lattice-unit declaration + the banked-ratio table).
 8. **DISCRIMINATION CLASS:** DC-internal constitutive derivation. **No chord is claimed on any outcome.** SM counterfactual, stated now: Maxwell has exactly this structure (Gauss constraint conserved by evolution; retarded causality) and GR's scalar sector is constraint-fixed — success on (i)+(ii) makes AVE's bound sector **Maxwell/GR-CLASS, peer not distinct**. The lane's value is truth-content + the LC-1 gate adjudication, per the truth-over-chords register. Constraint-falsifier targets, named and knife-armed: the banked pulsar comparators (`δ_HT = 0.0016`, `δ_DP = 1.3×10⁻⁴`) are NOT consumed by any derivation step (G-CUT-DERIVED checks the dependency list); the /7 VALUES must come out byte-unchanged (any "improvement" is flagged suspicious, not banked); the PM shear rate untouched (G-SHEAR-UNTOUCHED).
 9. **CERTIFICATION PLAN:** Gate battery frozen in §7 before any derivation content exists. UNRUN ≠ PASSED; every gate run and scored in the result. **Negative/positive controls named in advance (Step 3.8):** (a) the SAME derivation machinery run on textbook Maxwell must reproduce the known-good case (Gauss conservation; retarded fields; Coulomb energy); (b) the SAME toy-lattice checkers run on the K=2G-LOADED form must POSITIVELY DETECT the superluminal P-front at ≈1.83c and the nonzero far-zone bulk flux — a checker that cannot see the phantom on the imported form certifies nothing about its absence on the receipted form; (c) all divergence/flux observables are LOCAL (finite-sphere flux vs radius) — the global-sum telescoping zero (`em-readout-vsector-stage1_result.md:17`) is a banked structural-degeneracy and is designed out.
10. **ADJUDICATION ROUTING:** The item-(0) adjudicated repair proposal + any DERIVED-VIA-NEW-AXIOM candidate route to **Grant for ratification** — the lane executes NO axiom edit on any outcome. The four-tuple verdict + this doc route to the orchestrator. **LC-1's frozen cell re-adjudicates ONLY on (i)+(ii) both DERIVED-class** (brief, verbatim §2) — this lane STATES status against LC-1's wording and dispatches nothing. The R40 demotion sweep is unaffected except as routed input. #913/#919/arc-scope stay HELD. PR opens `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; the lane runs its own Tier-2 (ave-adversarial-pr-review class) before anything reads CLEARED.
11. **NUMERICAL CONDITIONING:** Named engines: `sympy` exact symbolic algebra + independent plain-Python float arithmetic (every verdict-bearing identity computed by BOTH); purpose-built toy-lattice drivers under `research/drivers/` implementing the RECEIPTED dynamics directly — **no run of `src/ave` corroborates anything in this lane** (stencil fence: the engine codes K; brief `:44`). Sweeps: GNU `grep -rniE` + Python `re` on an independent walk, patterns printed, known-positive controls gating instrument liveness (the `**`-pathspec and `$..$`-escaping false-negative classes guarded). RNG: none, except fixed-seed random fields for operator-identity checks (seed printed in the JSON). Driver + gating number check (`*_number_check.py`, `--mutation-receipt`) auto-discovered by the `make verify` umbrella (zero Makefile edits — the 2026-08-06 infra); run in THIS worktree with worktree PYTHONPATH discipline.

---

## §1 — The frozen deliverables (verbatim from the brief; the brief is the frozen source)

`_orchestration/2026-08-10_bound-sector-constitutive-brief.md:14-31`, verbatim `[sic]` (heading at `:12`):

> 1. **(i) THE CONSTITUTIVE LAW:** derive — from the axioms, the K4/srs structure, or
>    an honestly-TAGGED new axiom proposal (state which!) — the structure that
>    determines the bound field u₀ = B/r² from its sources: the multiplier/Gauss-analog
>    the controlled-source class requires. The flat direction leaves the dress
>    under-determined; the law must pin it. If the ONLY route is a new axiom, deliver
>    the minimal candidate with its full consequence audit (what else it forces) —
>    that is a Grant-ratification object, not a lane verdict.
> 2. **(ii) THE NO-SIGNALLING THEOREM:** for a REAL, non-gauge bound field (G-SCALAR-REAL
>    binds — no gauge escape), derive that the bound sector transports no
>    energy/information superluminally: the retardation/constraint theorem #935 found
>    missing. LC-1's cell un-fires ONLY on this theorem; state its status against
>    LC-1's frozen wording explicitly.
> 3. **(iii) THE ENERGY FUNCTIONAL:** the bound sector's energy bookkeeping (mass = A1
>    accounting hosted; the halo/near-field row) — consistent with (i).
> 4. **(iv) THE ν = 2/7 RECONCILIATION:** derive the static-response/frequency-gating
>    the Tier-2 bias finding demands — WHY the mechanical response banks statically
>    (the /7 couplings) yet does not radiate; the cut's placement must be derived, not
>    sit where the pulsars require. This is the lane's honesty keystone.

And the dated addendum `:49-61`, verbatim `[sic]` (opening lines):

> **Item (0), UPSTREAM of all four deliverables — the axiom-text contradiction.**
> `eq_axiom_3.tex:22` asserts the written form is "the standard Maxwell Lagrangian" —
> it is not (no A₀/scalar-potential term, hence no Gauss constraint exists in it);
> `:27` asserts U(1) gauge symmetry follows as a Noether consequence — the written form
> is not gauge-invariant (verified: |∂_t A|² is not invariant under A → A + ∇λ). This
> generates the fork the whole arc exposed: under the "standard Maxwell" READING, A_L
> is pure gauge and def-9a4f07/G-SCALAR-REAL is contradicted in the EM sector; under
> the LITERAL reading, A_L is a physical flat direction and the axiom's own labels are
> wrong. The corpus cannot straddle both. The lane's FIRST deliverable is the
> adjudicated repair proposal for this axiom text (which reading is the physics; what
> the corrected text says; the full consequence audit) — a Grant-ratification object.
> Items (i)-(iv) then build on whichever reading is ratified.

**Conditionality declaration (frozen):** Grant is not in-session; item (0) is delivered as a RECOMMENDED reading + repair + consequence audit, and items (i)–(iv) run under the recommended reading with an explicit conditionality banner; wherever the other reading would change an item's verdict, the result says so at that item.

## §2 — The frozen verdict grammar (verbatim from the brief) + the banked inputs at their frozen homes

`_orchestration/2026-08-10_bound-sector-constitutive-brief.md:35-37`, verbatim `[sic]` (heading at `:33`):

> Per-deliverable: DERIVED / DERIVED-VIA-NEW-AXIOM(candidate) / NOT-DERIVABLE(named
> missing piece). The lane verdict is the four-tuple; no aggregation. LC-1's cell
> re-adjudicates only on (i)+(ii) both DERIVED-class.

**Frozen sub-clause (entailed by the grammar, declared now):** if any route DERIVES the NEGATION of item (ii) — an actual superluminal energy/information channel through the bound sector under the ratifiable reading — the lane reports `NOT-DERIVABLE(obstruction: <named>)` for (ii), states the derived obstruction as a POSITIVE finding routed to Grant with its LC-1 consequence named, and upgrades nothing else. No massaging into an adjacent bin.

**The banked inputs (consumed as banked, never relitigated — gate G-BANKED):**

- **LC-1 kill cell** — `_orchestration/2026-08-04_lorentz-compliance-arc-brief.md:44`, verbatim `[sic]`: *"An energy-carrying inter-event channel at ≠ c ⇒ arc-level kill"*; `:59`: *"LC-1 runs first and its kill condition is arc-terminating."* (The #913 R5 authority: the ARC BRIEF's cell, never the dispatch's gloss.)
- **The #935 knives** (`research/2026-08-09_bound-response_result.md`, merged `229a5023`): the HEADLINE re-cut `:19` — the receipted dynamics gives the longitudinal sector as a *"zero-stiffness inertial FLAT DIRECTION (`ε₀Ä_L = 0`, conserved momentum, a state variable), NOT constraint class; the multiplier/Gauss structure the controlled-source class needs has no axiom preimage either"*; finding #1 `:214` (the G-KILL-3 knives: no defining-identity circularity, no laundered cites — *pure-gauge* is GR's causality escape and the bound field is REAL); finding #2 `:215` (the flat direction, verifier-reproduced: `A_L(t) = A_L(0) + V_L·t` to `1.6e-10`); finding #3 `:220` (the zero-energy-density crux); findings #4/#17 `:216` (the frequency-gated ontology asserted-never-derived; the 3/7 trace receipt uncited). **What SURVIVES at its banked grade** `:21`: the no-finite-ω consequence (conditional on item i), the premise-localization receipts (G-RECON), the PATTERN-BOUNDED consumer audit, the reduced provenance count.
- **#261** (K=2G GR-imported): Gate 1 — z=4 sub-isostatic, *"K/G is a one-parameter family in ρ=k_a/k_s"*, K=2G needs tuned ρ* *"none geometrically forced"* (`_orchestration/2026-06-15_k2g-crystalline-provenance.md:61-67`); Gate 2 — *"ρ=k_a/k_s∝L_eff/C_eff=Z_eff²"*, on SYM *"ρ=Z₀² INVARIANT for all S"*, *"K=2G NOT constitutively forced ⟹ … GR-imported, END OF LINE"* (`:85-99`); Grant `:124-126`. Re-confirmed z=3: ν=2/7 only at externally-supplied ρ*≈9.7734 (#506, `vacuum-poisson-ratio.md:18-28`).
- **The four-lock no-go** (charge sector, MERGED): `the-sourced-charge-no-go-cascade.md:111-119` — `∂_t(∇·(ε_eff E)) = −∇·J_coupling ≡ 0` for axiom-native coupling (DERIVATION GRADE); `:117-119` *"set by initial data, not emergent … whatever was seeded"*; `:129-138` `[NO-AXIOM-NATIVE-TERM]` at net-monopole grade; Lock 3 maximum principle `:94-97`. **Sector fence:** those locks live on the EM/Cosserat charge sector (lane Y/Z carve); this lane's A1-sector derivation must NOT reconstruct the closed sourced-static-monopole path and must state explicitly, per step, why its structure is not that path.
- **TH-5** (`theorem-thesaurus.md:147-152`): KCL/Fredholm/self-equilibration are *"one statement of linear algebra wearing three names … it constrains what may be imposed, and derives nothing."* Any (i)-derivation leaning on incidence must carry its constitutive content separately or fail honestly.
- **R24** (`2026-08-07-rulings-r23-r27.md:16-28`): def-9a4f07's one-displacement-field identity **CONTESTED-BY-GRANT**, gated on the GW-formation walk. This lane asserts nothing over that walk.
- **Keystone energize-LOCK** (memory-banked negative): electron-genesis-from-free-precursor LEANS-FALSIFIED — nothing here re-opens formation-route claims; "defect deposits divergence at genesis" is used ONLY as constraint-surface bookkeeping (what the constraint's initial data IS), never as a formation-dynamics claim.

## §3 — The derivations' frozen shape (two independent routes per item)

**Item (0) — the axiom-text adjudication.** Route (α): constraint-analysis of the written form as a mathematical object — what symmetries the written action HAS (exact check: δL under A → A+∇λ(x) vs λ(x,t)), what its EOM and conserved structures ARE, and what each candidate label ("standard Maxwell", "U(1) Noether") would require. Route (β): corpus-coherence audit — which reading the canon load-bearingly consumes (def-l0ngdu/def-uatk1s "∇·A is gauge" vs the derivation-grade temporal-gauge dynamics of the no-go cascade + #935's flat direction; vol2 ch05's Helmholtz-shift U(1) with single-valued Λ; MYTH-GUARD). The repair proposal must: (a) make every sentence of the repaired text true of the written form; (b) preserve or explicitly re-scope every downstream consumer (two-method sweep of "standard Maxwell" + U(1)/Noether sites, incl. `backmatter/02_full_derivation_chain.tex:163-164` drift and the axiom-register `:174`-vs-`:231` equivalence contradiction); (c) state which reading is recommended and why the corpus cannot straddle (the brief's charge). Agreement of (α)+(β) on the recommended reading = adjudicated; disagreement = the fork is presented to Grant un-recommended (frozen honest outcome).

**Item (i) — the constitutive law.** Route (a): the symmetry/constraint route — identify the residual symmetry of the receipted action and its Noether/momentum-map structure; derive what the dynamics CONSERVES (the candidate Gauss function), then derive what PINS its value: the constitutive source identification (what a matter defect deposits in the A1 slot; the Ax4 identification A ≡ ε₁₁/ε_yield and the T₀₀-sourced canon FORM as the candidate preimage) + the quiescent-substrate initial condition + uniqueness of the decaying exterior. Route (b): the network/DEC route — the same law derived on the discrete srs complex from the CI-certified incidence operators + the bond-variable constitutive assignment (nodes-store-V/struts-carry-I, clm-q39qct class), with TH-5's knife applied (the incidence identity is bookkeeping; the derivation's CONTENT is the constitutive assignment and it must be named as canon-or-new). Both routes must state, per the row-1 two-object split, **which object is pinned** (the dress u₀, the grade ε₁₁, or a derived relation between them) — if the derivation pins the grade and the dress rides it (or vice versa), that is stated, not blurred. **The brief's knife (frozen verbatim, `:41-43`):** *"a derivation that quietly reconstructs the flat-direction-as-constraint assertion dies at its own gate."* If neither route closes from receipts: the minimal new-axiom candidate, with EVERY clause shown load-bearing by ablation + the full consequence audit (what else it forces, what it forbids, which banked results it touches).

**Item (ii) — the no-signalling theorem.** The theorem's frozen legs: **(T1)** the receipted evolution is LOCAL with a characteristic structure whose maximum speed is c and whose longitudinal characteristic speed is ZERO (not small — absent; the LC-1 K=0 receipt marks the trap: `v_L ≥ √(4/3)c` for every K ≥ 0 in the IMPORTED W — the receipted form must be shown to lack the deviatoric-restoring term that creates that floor, not merely to have K=0); **(T2)** the (i)-constraint is conserved by that evolution given the source sector's own conservation law (named: which conservation, whose theorem); **(T3)** a domain-of-dependence / energy-estimate argument: no energy or information crosses outside the c-cone — including the REAL-field observability question faced head-on: the bound field couples to observables through Ax4 (the grade modulates n(r)); the theorem must cover the gravimeter scenario (a local reading of the bias field when a distant source moves) and either derive its retardation or fail; **(T4)** the result stated against LC-1's frozen cell verbatim, with the honest scope sentence (what the theorem does and does not establish for the cell's predicate). Two methods: the analytic proof + the toy-lattice front-speed/flux receipts (with both §0-row-9 controls). The #935 finding-#1 knives are self-applied in the result: no step may consume the no-port vocabulary as a premise (that is the conclusion), and every GR/EM parallel must carry its disanalogy (they escape via gauge; this field is real).

**Item (iii) — the energy functional.** Route (a): Legendre/Hamiltonian structure of the receipted action + the (i)-constraint — where the bound sector's energy sits (candidate: the momentum-sector/Coulomb-class term and/or the Ax4 cross-coupling term — the T2-sector stored-energy shift due to the A1 bias, canon's own "envelope = the energy-density / operating-point bias pattern (what gravitates)", `vocabulary-register.md:1039`). Route (b): duty-hosting audit — mass = A1 accounting, halo P9 added-mass (stores-and-returns, d'Alembert zero-steady-drag walk receipts), Ax3-losslessness, positivity, integrability (Coulomb-form finiteness outside a core), convection with the source. **The finding-#3 crux must be resolved explicitly** (where the static dress's energy lives given both bare storage terms score zero), and the functional must be CONSISTENT with (i) (same constraint, same multiplier structure — no second bookkeeping).

**Item (iv) — the ν = 2/7 reconciliation.** Route (a): the response-function derivation — the constrained sector's transfer function from source to static observable, derived from (i)+(iii): why the response is finite, ω-independent-elliptic in the near zone, with NO radiative pole at any ω; where the cut physically sits (candidate: zone structure — bound 1/r² at every ω, no 1/r channel — NOT a frequency gate; frozen as expectation). Route (b): the static-stiffness route — what plays the role of the static effective bulk response in the constrained theory (candidate: the constraint/multiplier structure + the A1 bond-compliance `C_eff = C_0/S` reactance, INVARIANT-S2 sector split), and whether the /7 FORM survives with its VALUE imported exactly as before. **Anti-reconstruction fences (frozen):** no step claims K=2G is geometrically forced (Gate 1's closed config) or constitutively forced (Gate 2's closed config); the ν=2/7 VALUE's import status is byte-unchanged on every outcome; the srs-band-structure seam (static import GENERATING the P-branch, `srs-band-structure.md:146`) is addressed as a consumer, not re-derived. **The pulsar-independence receipt:** the result lists item (iv)'s complete input dependency set and shows the pulsar comparison is not in it — the cut's placement cites structure only. Reading-B's owed mechanism (`port-register.md:91`) is answered or the item fails.

## §4 — Corpus state (ave-prereg Steps 1–2; 8-agent two-method context pull, grounded at base `6c291196`)

**Prior art FOR the derivations (structural ingredients, none a closed solution):** the DEC/incidence conservation theorem on the charge sector (derivation grade, the no-go's engine — the TEMPLATE and the knife); `backreaction.py`'s T₀₀-sourced elliptic solve (the un-derived working model of (i)'s FORM); Candidate B1's obligation statement (`q1-pulsar-hardening.md:130-131` — the "pure-gauge" words are present in canon; nothing here launders them); the NOT-A-WAVE vocabulary slot (LC-1 row 3a); the AC/DC carve (`clm-acdc07`, input-only 0.55 — context, never verdict-bearing); the thread no-signalling theorem (`phase-locked-topological-thread.md:185` — statistical-marginal class, a DIFFERENT object, cited as form-precedent only); the Kirchhoff-network-method leaf (clm-q39qct, 0.63 input-only); Ax4's canon identification A ≡ ε₁₁/ε_yield with D(A) = 1/S(A) in the gravity solve.
**Counter-evidence / hardest sites (enumerated up front):** `master-equation.md:26` + def-l0ngdu (*"∇·u propagates; ∇·A is gauge"* — SOLID, premised on the K-backed W; the #935 audit routed it NEEDS-RE-DERIVATION); `a1-port-sourcing_result.md:325-328` (elliptic slaving = *"a STATICS design choice, not a derived constraint theorem"* — the standing counter-site this lane must answer WITH a derived theorem or fail); clm-9kd2t3 (0.42, *"Does NOT derive Gauss's law from the four axioms"* — the prior near-miss, do-not-build); the vol2 `07_quantum…tex:1047-1053` no-signalling-by-citation site (a CONSUMER of item (ii), not prior art); the production TLM engine conserves power not node charge (no `q_n` variable exists; Gauss enforcement is FORBIDDEN-INSERTION per Stage-1 `:108`).
**The variable-class collision (live, unflagged in the register):** u↔A (def-uatk1s SOLID) and u↔E (def-tk1xfm SOLID) and x↔q (six-row dictionary) and A↔momentum-class (`dielectric-lagrangian.md:40`, `isomorphism.py:88-104`) are ALL ratified; `def-uatk1s:892` cites the u↔E row as support for the u↔A node. **No corpus site derives ε₁₁ from ∂u; the dress and the grade are kept as two variables** (two-pattern-family grep, absence-grade). This collision is upstream of which variable a Gauss-analog pins — recorded as the lane's FORK (§6).
**Sweep note (two-engine, at base):** "bound-sector constitutive law" / "no-signalling theorem for a real bound field" / "domain of dependence" have no corpus home; "temporal gauge"/"Weyl gauge" have ZERO corpus hits — canon nowhere names its own gauge structure or discusses A₀'s absence. The derivations are genuinely new work; their INGREDIENTS are all receipted above.

## §5 — The physical picture (mechanical/EE terms, before any equation)

- **The schematic was mislabeled, not miswired.** The written axiom action is a two-element schematic (C on node rate, L on curl). The label says "standard Maxwell," but standard Maxwell's schematic has a third element — the multiplier rail (A₀) whose only job is to enforce KCL on initial data. Item (0) decides: fix the label (the schematic is the temporal-gauge chassis, constraint owed separately) or fix the schematic (a physical flat rail, labels wrong). The corpus's own usage runs on the first reading's dynamics and the second reading's vocabulary — that straddle is the bug.
- **A bias rail is pinned by what charged it, not by a spring.** The dress around a star is the bias distribution its formation deposited; the network's incidence structure conserves the deposit (KCL telescopes), and the constitutive question is only what a defect DEPOSITS — the injection inventory, not a new force law. Nothing about the rail rings: its restoring element is absent, so its "spring constant" questions (the #261 phantoms) were never well-posed.
- **News travels on the lines that have lines.** When the star moves, the bias pattern re-settles behind a wavefront carried by the sectors with real characteristics (T2, at c). The rail's own characteristic speed is zero — a signal ON the rail goes nowhere; the rail's far pattern is updated BY the propagating sectors. That is the theorem shape: locality + one real cone + a conserved constraint = no message beats the cone.
- **The bias energy is real and lives in the loaded cells.** A biased varactor line stores energy even at DC — in the shifted operating point of every cell it grades (canon: the envelope IS what gravitates). That is where the dress's energy books when both bare terms read zero.
- **The bias-tee: DC banks, AC is blocked, and the cut is the tee's topology.** The static /7 response is the operating-point solve (elliptic, finite, value-imported as before); there is no AC path on the compression line at ANY frequency — the cut between them is the near-zone/far-zone (1/r² vs 1/r) structure of a portless rail, not a frequency gate placed where the pulsars need silence. **Scaling:** dress ∝ 1/r² (bound); shear radiation ∝ 1/r (port). **Discrete onset:** none claimed (no new wall, no new knee).

## §6 — Pre-registered expectations, forks, discriminators (Steps 3, 3.5–3.10)

**PREREG summary block:**
- Corpus state: OPEN with named structural ingredients (no closed solution anywhere; #935's enumeration confirmed at base).
- Prediction: (0) adjudicates to a single recommended reading with a repair making both labels true-as-written (expected shape: the written form is the A₀-less/temporal-gauge system; the true residual symmetry is time-independent `A → A + ∇λ(x)`; the Gauss function is its generator; full time-dependent U(1) fails on the written form by the exact remainder `ε₀(∂_t𝐀)·(∇∂_tλ)`). (i) closes DERIVED-or-VIA-NEW-AXIOM with the conservation leg derived (Noether/DEC, both routes) and the honest boundary at the SOURCE-IDENTIFICATION clause (what a defect deposits) — expected: that clause is where either the Ax4 preimage closes it or the minimal new axiom is named. (ii) closes DERIVED conditional on (i)'s bin (the three legs are provable for the receipted curl-only form; the gravimeter scenario is the live risk — fork below). (iii) closes DERIVED with the energy in the momentum-sector/coupling terms (finding-#3 crux resolved). (iv) closes DERIVED at FORM level with the cut = zone structure and /7 values byte-unchanged.
- Why: the Maxwell control case has exactly this derivation chain (residual symmetry → Gauss generator → constraint conservation → retarded causality → Coulomb energy), and every ingredient has a receipted substrate home (§4); the risk concentrates where the substrate DIFFERS from Maxwell (real field, Ax4 observability of the grade, the u↔A/u↔E collision).
- Falsifier of the framing: if the toy-lattice receipts show the receipted curl-only form's longitudinal sector does NOT have zero characteristic speed (a hidden restoring term), or the Maxwell control fails to reproduce textbook results through the same machinery, the lane's framing is wrong and stops for re-derivation, not patching.

**Recorded FORK 1 (fork-record-both; the variable-class collision):** which variable the Gauss-analog pins. Branch u↔A-class (dress = configuration): the constraint pins the MOMENTUM'S divergence (`∇·π`), the static dress's pinning then requires the additional Ax4/coupling structure, and the residual-gauge reading makes the static curl-free CONFIGURATION shift-soft — predicted consequence: the dress-vs-grade split is FORCED (the observable pinned object is the grade; the dress is bookkeeping on the soft direction). Branch u↔E-class (dress = momentum/field-strength analog): the dress ITSELF is the Gauss-pinned object (Coulomb-form `B/r²` directly), predicted consequence: the dress is gauge-invariant and real, and the grade couples downstream. The result adjudicates by which branch the RECEIPTED structures (six-row dictionary, Ax1's ε₀-on-translational, the canon solve) actually force, quoting each; if receipts split, the fork goes to Grant un-recommended with both consequence sets.
**Recorded FORK 2 (item ii, the gravimeter scenario):** either the theorem covers Ax4-mediated observability of the grade (retardation derived: local grade readings update only inside the cone) — expected branch — or the elliptic update of an Ax4-OBSERVABLE grade is genuinely superluminal, which fires the §2 frozen sub-clause (obstruction reported, LC-1 consequence named). Both branches are fireable; neither is entailed (the toy-lattice front receipts decide, and the Maxwell control shows the machinery CAN produce the retarded branch).

**Analytic expectations with numbers (Step 3.9; toy-lattice tolerances):** δL ≡ 0 exactly (sympy) under time-independent ∇λ; the time-dependent remainder = `ε₀(∂_t𝐀)·(∇∂_tλ)` exactly (sympy). Vacuum conservation `∂_t(∇·Π) = 0` to float roundoff (≤1e-12 rel) on the toy; sourced continuity defect ≤ O(Δt²) (declared per-run). Flat direction: `Ä_L = 0`; `A_L(t)` linear to ≤1e-10 (matching #935's verifier receipt class). Front speeds: receipted-form transverse front = c within grid dispersion (declared ≤3% at the run resolution, measured on the Maxwell control first); longitudinal front on the receipted form = ABSENT (energy outside the c-cone ≤ the control's floor, declared per-run); K=2G-loaded control's longitudinal front measured ≥1.5c (must DETECT ≈1.8257c — liveness). Coulomb-class energy: `∫(B/r²)²·4πr²dr = 4πB²(1/r_in − 1/r_out)` exact (sympy) — finite, positive. Response: near-zone driven amplitude tracks the elliptic solve with corrections O((ωr/c)²) (measured ratio at two ω); far-zone time-averaged longitudinal flux on the receipted form ≤ floor while the K-loaded control's is nonzero (≥10× floor). Banked-ratio records: 2/7, 3/7, √(4/3), √(10/3) exact fractions.

**Entailed-branch check (Step 3.10):** For (0): both readings are genuinely recommendable (route β's corpus evidence is split — §4) → the adjudication is FIREABLE both ways; the route-(α) symmetry facts alone are ENTAILED (the algebra is what it is) and are labeled DEMONSTRATED, not adjudicated. For (i): DERIVED fireable (the Ax4/coupling preimage may close the source clause), VIA-NEW-AXIOM fireable (TH-5 + no-preimage may leave the clause open), NOT-DERIVABLE fireable (the FORK-1 collision may be R24-gated) — none entailed. For (ii): DERIVED and the §2 obstruction sub-clause both fireable (FORK 2); the vacuum-sector legs (T1/T2) are largely entailed by the receipted algebra and are labeled DEMONSTRATED where so. For (iii): DERIVED vs NOT-DERIVABLE fireable (the crux may resist a receipted resolution). For (iv): DERIVED at FORM vs NOT-DERIVABLE fireable (the static-stiffness route may bottom out in the imported value in a way that leaves the FORM underived). **The one wholly-entailed element, declared:** IF (i)+(ii) close, the LC-1 cell's re-adjudication eligibility is arithmetic on the brief's sentence — the honest verb there is TRIGGERED, not derived.

**Robustness ladder (Step 3.7):** primary (gating) rung = STRUCTURE (does each derivation close, and on which clause does it stop) — the four-tuple is a structure verdict. Magnitude receipts are supplementary instrument checks; no magnitude is verdict-bearing except as a named gate control. Gate-floor consistency: every toy-lattice gate carries its declared floor/tolerance from this section; any cell the driver cannot resolve at the declared resolution is OUT-OF-SCOPE for that gate, disclosed, never a verdict cell.

## §7 — The frozen gate battery (UNRUN ≠ PASSED; every gate run and scored in the result)

| Gate | Frozen criterion |
|---|---|
| **G-QUOTE** | Every ruled/frozen quotation byte-checks against source file:line by TWO engines (extracted-span + Python string equality); branch-state quotes carry their commits; any delta enumerated or the quote fails |
| **G-AX0-2M** | Item (0): routes (α)+(β) run independently; the symmetry algebra machine-verified (driver); the repair text drafted with every sentence true of the written form; the consequence audit enumerates every "standard Maxwell"/U(1)-Noether consumer site (two-method sweep, counts pinned measure-then-edit) + the register `:174`/`:231` contradiction + the def-collision, each with a disposition; (α)+(β) agreement → recommended reading; disagreement → fork presented un-recommended |
| **G-LAW-2M** | Item (i): both §3 routes run; every candidate axiom preimage for the SOURCE clause enumerated + dispositioned with receipts; TH-5 knife applied in-text (the incidence identity's contribution labeled bookkeeping); the four-lock sector fence stated per step; the brief's flat-direction-assertion knife applied to the lane's own §; pinned-object declared per FORK 1; if new-axiom: minimality-by-ablation + full consequence audit; routes agree on the bin or NOT-DERIVABLE |
| **G-NOSIG-3** | Item (ii): legs T1/T2/T3 each DERIVED (not asserted), T4 stated against the LC-1 cell verbatim; the gravimeter scenario adjudicated (FORK 2); the finding-#1 knives self-applied (no no-port premise, every gauge-parallel carries its disanalogy); both §0-row-9 controls run and passed or the gate fails regardless of the analytic text |
| **G-ENERGY-HOST** | Item (iii): functional derived by route (a), consistent with (i) (same constraint structure — checked explicitly); finding-#3 crux resolved in-text; duties hosted (mass=A1, halo P9, Ax3-lossless, positivity, integrability, convection) each with a receipt; any un-hostable duty ⇒ NOT-DERIVABLE(named) |
| **G-CUT-DERIVED** | Item (iv): the cut's placement derived with its complete input-dependency list printed; the pulsar comparison ABSENT from that list (checked by the list itself + a sweep of the item's §); neither #261 gate's closed configuration re-argued (anti-reconstruction para mandatory); /7 values byte-unchanged; `port-register.md:91`'s owed mechanism answered explicitly |
| **G-SECTOR** | Sector-ownership: no step routes bulk-slot content through the Cosserat channel (rotational fence-reach STOP — checked at each item); TWO-3s guard; charge stays Ax2's winding; the two-object split (dress/grade) maintained at every use |
| **G-SHEAR-UNTOUCHED** | T2 sectors byte-untouched: no step re-derives, re-scales, or re-anchors the photon or the banked GW shear rate |
| **G-BANKED** | The #935 knives, #261, the four-lock no-go, R24, keystone-LOCK consumed as banked; no relitigation; any contact with a closed negative carries a challenge-a-canonical-negative receipt (what config was closed; why this step is not that config) |
| **G-ENGINE-FLAG** | `git diff --stat <base>..HEAD -- src/` EMPTY at every commit; engine implications (if any) flagged as architecture notes only |
| **G-NUM** | Every verdict-bearing numeral machine-verified against the driver JSON; both engines agree per identity; `*_number_check.py --mutation-receipt` fires; `make verify` green in THIS worktree at commit; fixed seeds printed |

**Frozen bin arithmetic:** each item's verdict rides its own substantive gate (G-AX0-2M, G-LAW-2M, G-NOSIG-3, G-ENERGY-HOST, G-CUT-DERIVED): PASS with routes-agree → `DERIVED` (or `DERIVED-VIA-NEW-AXIOM(candidate)` where the closing clause is the candidate, minimality shown); route-disagreement or an un-dispositioned missing clause → `NOT-DERIVABLE(named missing piece)`. **No aggregation** — the lane verdict is the item-(0) adjudication + the four-tuple, stated separately. G-QUOTE, G-SECTOR, G-SHEAR-UNTOUCHED, G-BANKED, G-ENGINE-FLAG, G-NUM are integrity gates: any FAIL blocks all verdict wording until repaired and disclosed (Rule 12: repairs banked, originals preserved). No gate, tolerance, bin, fork branch, or frozen parameter in this document may change after any derivation or receipt is seen; instrument-forced deviations follow the disclosed-first pattern with direction-of-effect stated.

---

> **Freeze provenance.** SVA v0.2-pilot §0 filled, all 11 rows. Frozen criteria verbatim or file:line throughout; the LC-1 cell quoted from the ARC BRIEF (`:44`, the #913 R5 authority), never a dispatch gloss; the brief's deliverables, verdict grammar, addendum, and knife quoted verbatim from the tracked brief at base. Corpus state grounded at base `6c291196` by an 8-agent two-method context pull with orchestrator byte-checks of every freeze-bound quote (LC-1 `:44`/`:59` and the brief's grammar re-verified by direct `sed` at write time). Skill stack fired at write time: ave-prereg (Steps 1–3.11 incl. entailed-branch, robustness-ladder, liveness, freeze-by-push), ave-worktree-paths (worktree-absolute from first call), verify-before-cite, ave-analytical-tool-selection (§6 Mode / §7 Boundary / §8 Network consulted), ave-canonical-leaf-pull (framework-design canon enumerated before the new-axiom candidate), ave-ee-first-mapping (§5 circuit statement first), ave-discrimination-check (§0 row 8 — no chord on any outcome), ave-regime-phase-state-check (§0 row 2), consistency-vs-emergence (classifications routed to the result's ledger). This document commits ALONE and pushes FIRST; the driver commits second; the result last. Tier-2 adversarial review before any CLEARED wording; PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`.
