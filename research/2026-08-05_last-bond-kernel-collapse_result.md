# The LAST BOND under kernel collapse — RESULT: the discrete free-end row is EXACT and beyond-wall-independent; the ruling's premise is **NOT** established because canon's own cross-grade aggregation rule is UNDERDETERMINED, and the plane the wave actually meets is one node-mass outward

**Date:** 2026-08-05
Prereg-file: research/2026-08-05_last-bond-kernel-collapse_prereg-FROZEN.md
**Prereg-commit:** `bf77d07a` (frozen and pushed **ALONE**, before any driver code existed, before any grep of the frozen pattern battery had been run, and before any number produced by this instrument existed)
**Driver:** [`research/drivers/last_bond_kernel_collapse.py`](drivers/last_bond_kernel_collapse.py) → [`research/drivers/last_bond_kernel_collapse_results.json`](drivers/last_bond_kernel_collapse_results.json) (driver committed at `8a27b1c9`, **before** any result existed)
**Number check:** [`research/drivers/last_bond_kernel_collapse_number_check.py`](drivers/last_bond_kernel_collapse_number_check.py) — gating via `make verify`, with a mutation receipt
**Class:** DERIVATION result (research-doc; **mints no `clm-`/`def-`; propagates to no KB/manuscript leaf; changes no solidity; edits no falsification ledger**). Engine `src/ave` byte-untouched and never imported.
**Provenance:** Grant's ruling of 2026-08-05, `_orchestration/docket-entries/2026-08-05-ruling-flag-causal-kernel-collapse.md` (on `origin/orchestration/ruling-flag-causal-0805`, PR #887) — this lane is the ruling's ONE routed premise, with its KILL CONDITION. Written against `origin/main` = `c4a546dc`.
**SVA pilot case 5.** The per-row fill experience is logged in §7.

---

## REGIME HEADER (mandatory, restated at the point of reading)

**MODE** — small-signal AC reflection at a terminal plane on a static DC bias; a scattering problem at REAL frequency, **not** an eigenvalue problem. **REGIME** — sub-yield lossless-reactive on the cold side `A < 1`; the `A ≥ 1` region is Regime IV and enters ONLY as an arbitrary passive load, never as a solved domain. **PHASE-STATE** — cold lattice, Op14 ON as a static constitutive grade, `A(r) = r_sat/r`, `A = 1` exactly at `r_sat`. **A null in which the effect cannot exist under the frozen construction is an ARTIFACT-class finding, not a falsification**, and every null below is classified.

---

## HEADLINE

> **★ CERTIFICATION, STATED FIRST AND WITHOUT SOFTENING.**
> **TASK 1 is `SCAN-NOT-CERTIFIED` and adjudicates NO premise bin.** `G-SCAN` FAILS and `FT-SCAN` does not fire, both on defects this lane built into its own frozen scan design. **The per-coupling grading table below is therefore a NOT-ADJUDICATED DIAGNOSTIC**, and it is reported in full because a successor should inherit a measurement rather than a silence.
> **TASK 2 is `ROW-NOT-CERTIFIED`.** `G-RHO2` FAILS on an injection point this lane sized wrong at freeze. **Every other Task-2 gate passes, and four of them pass at EXACTLY ZERO.**
> **TASK 3 is CERTIFIED and `BIN-C-DISJOINT` IS ADJUDICATED**, with the loss point named and demonstrated as the freeze requires.
>
> **★ THE THREE LOAD-BEARING THEOREMS ARE MEASURED EXACT, NOT SMALL.** Over `3360` swept rows — `1680` on the harmonic branch — the last-bond stiffness is `0.0`, the residual `|Γ_LB + 1|` is `0.0`, the spread over the entire beyond-wall grid (`48` groups spanning `1e-30` to `1e+30` in beyond-wall density and the whole extended complex plane of loads including `0` and `INFINITY`) is `0.0`, and the separation between the two density branches is `0.0`. **Not a tolerance. Zero.** The zero spring does not merely dominate the beyond-wall load; it removes it from the algebra.
>
> **★ AND THE PREMISE IS NOT ESTABLISHED — because canon's OWN cross-grade aggregation rule is the thing that would decide it, and canon says that rule is UNDERDETERMINED.** `research/2026-07-02_axiom4-combine-rule_result.md`:79, verbatim: *"**per-yield-normalized; cross-grade aggregation underdetermined at O(α)."***, carried into `manuscript/ave-kb/common/axiom-register.md`:190 verbatim as *"the combine is per-yield-normalized (single-radius-L2 falsified), cross-grade aggregation underdetermined at $O(\alpha)$"*. Under the **normalized-L2-across-grades** member every grade rides ONE kernel and the ruling's premise holds. Under the **L∞-across-grades** member — *"the wall is whichever grade reaches `S→0` first"* (`:55`), which is **the member the engine actually codes** (`:110`) — the `r_sat` wall is set by the STRAIN grade, the CURVATURE grade's `S_κ` is untouched there, and the couple-stress bond `γ·S_κ` does **not** collapse. **That is `COUPLING-EXCEPTION` in everything but the bin: on one live member of canon's own open fork the rotational transport survives the wall and the linear conversion channel reopens.** This lane does not choose the member, does not invent a grading law, and does not promote the engine's coded choice to canon.
>
> **★ AND CANON'S `r_sat` CHANNEL TABLE HAS THREE ROWS WHERE THE PORT REGISTER HAS FOUR CHANNELS.** `bulk-impedance-at-saturation-boundary.md`:48–54 tabulates EM (`Γ_EM = 0`), shear (`Γ_shear = -1`) and bulk (`Γ_bulk = -1`). **Two-method absence receipt: the strings "Cosserat", "couple-stress" and "micro-rotation" occur ZERO times in that entire file, on `grep -c` and on an independent Python `re` count alike.** `port-register.md`:47–50 carries FOUR canonical channels, the fourth being *"Cosserat micro-rotation / wryness (couple-stress; the (2,3) winding)"*. **Canon has never stated what the `r_sat` wall does to channel 4.** That absence and the underdetermined combine rule are the same gap seen from two sides.
>
> **★ AND THE SIGN OF THE ROW DEPENDS ON THE PLANE BY THE FULL SHORT↔OPEN INVERSION — MEASURED.** `G-PLANE` measures a maximum `|Γ_N0 − Γ_LB|` of `2.00000000000000000000000000000`: shifting the reference plane outward by ONE node mass carries `Γ` from `-1` to `+1`. The magnitude is `1` at both planes (`G-UNIT` = `0.0` at both — Axiom 3, nothing crosses a port), so the **mirror** conclusion is plane-invariant and the **sign** is not. The crossover is a closed form, `S_cross = √(ω/ω_C)` = `3.16227766016837933199889354443e-10` at the frozen middle rung; at the PHYSICAL last cell (`S_last` = `0.00000000109762455411903921151585733431`) the ratio `ω m_0/Z_1` is `0.0830027291879898136165708582241`, so **the inversion does NOT fire at physical parameters** — it sits `3.47099360674291596844875758216` decades-of-`S` away and only the deliberately-over-extended sweep reaches it. **Reported because the ruling's mirror-family sentence names a sign, and a sign without its plane is not a claim.**
>
> **★ AND THE CONTINUUM DOES NOT DISAGREE — IT NEVER SEES THE BOND.** `BIN-C-DISJOINT`, with two demonstrated loss points: the wall traction is the indeterminate form `μ(r)·∂ψ/∂r → 0·∞`, which the continuum resolves along a Frobenius branch to divergence (exponents `-0.1459563385925332` and `-0.8540436614074667`, both negative, READ from the axial lane) and the lattice resolves as *exactly zero* times a bounded finite difference; and the round-trip optical length to the wall diverges logarithmically as `ell_node → 0` at the measured rate `1.15137588677240333934710329731` per decade against the derived `ln(10)/2` = `1.15129254649702284200899572734`. **The negative control is decisive: on RHO-A the same span moves by a factor of `1.00499579253354892345842477337` over nine decades — the optical length is FINITE and the mechanism is RHO-B-specific.** `Γ_LB = -1` at every rung of the ladder, unchanged in the limit; what diverges is the delay through which it reaches anybody.

---

## §1 — THE GATE TABLES (measured against frozen; nothing dropped, widened or re-defined)

**Frozen:** `after any grep result, any algebraic result or any number produced by this lane is seen. If a` no gate, tolerance, bin boundary, frozen numeric parameter, coupling-enumeration entry, verdict wording or method element of §2–§8 may be changed.

**No frozen criterion was dropped, widened, or re-defined.**

### §1.1 The gates

| gate | task | frozen | measured | verdict |
|---|---|---|---|---|
| **G-BOND** ★ | 2 | `k_0 == 0` exactly (mp) at every swept HARMONIC point | max `\|k_0\|` = `0.0` | **PASS** |
| **G-ROW** ★ | 2 | `\|Γ_LB + 1\| == 0` exactly at every swept HARMONIC point | max = `0.0` over `1680` points | **PASS** |
| **G-RHO** ★ | 2 | spread over the `Z_beyond × ρ_beyond` grid `== 0` exactly | max spread = `0.0` over `48` groups | **PASS** |
| **G-RHO2** ✗ | 2 | fitted exponent of `\|dΓ/dZ_beyond\|` vs `k_0` in `[1.9, 2.1]` | `0.00370115115631918737071374823881` | **FAIL** |
| **G-COLD** ★ | 2 | `Γ_LB(RHO-A) − Γ_LB(RHO-B) == 0` exactly | max sep = `0.0` | **PASS** |
| **G-UNIT** | 2 | `\|\|Γ\|−1\| <= 1e-50` at BOTH planes | `0.0` (PLANE-LB) / `0.0` (PLANE-N0) | **PASS** |
| **G-PLANE** ★ | 2 | `\|Γ_N0 − Γ_LB\| >= 1e-6` at ≥ 1 swept point | `2.00000000000000000000000000000` | **PASS** |
| **G-PREC** | 3 | float64-vs-mp relative separation `<= 1e-12` | `1.05470693719389027251354899102e-16` | **PASS** |
| **G-COND** ★ | all | the naive float64 `1−A²` must return exactly `0.0` | naive = `0.0`; cancellation-free `S²` = `1.20477966180501963891137947488e-18` | **PASS** |
| **G-SCAN** ✗ | 1 | METHOD A and METHOD B hit sets identical per pattern | `4` of `50` patterns disagree; file counts `3921` / `3921` | **FAIL** |
| **G-NC-SIGN** | 2 | the byte-exact free-end convention substring present in the echo-v2 prereg | present; this lane's `Γ_LB` = `-1` matches | **PASS** |
| **G-NC-ECHO** | 2 | `-1` is a swept member AND the predecessor's junction two-port is the identity | sweep declaration present; all `4` two-port entries `is_identity` with `W = 0` | **PASS** |
| **G-NC-ARITH** | 2 | the arithmetic rule must NOT collapse | max `k_0` = `830027291879898.135415708582244`; max `\|Γ+1\|` = `2.00000000000000000000000000000` | **PASS** |
| **G-DET** | all | two full runs, identical digest, byte-identical apart from `_runtime_sec` | digest `778f6ef9f0819a6f` twice; diff empty apart from `_runtime_sec` | **PASS** |

### §1.2 The fireability self-tests (each MUST fire; a gate that cannot fail is not a gate)

| self-test | frozen firing condition | measured | fired? |
|---|---|---|---|
| **FT-BOND** | injecting `k_beyond = 1e-300` must make `G-BOND` fail | `k_0` = `2.00000000000000000000000000000e-300` ≠ 0 | **FIRES** |
| **FT-ROW** | injecting `k_0 = 1e-300·k_cold` must make `\|Γ+1\| > 0` | `6.32455532033675866399778708887e-286` | **FIRES** |
| **FT-RHO** | at that `k_0` the `Z_beyond` grid must spread | `6.32455532033675866399778708887e-286` | **FIRES** |
| **FT-PLANE** | at `ω → 0` the two planes must COINCIDE | `6.32455532033675866399778708887e-196` at `ω/ω_C = 1e-200` | **FIRES** |
| **FT-ARITH** | the arithmetic control must fail to collapse for the RIGHT reason | `k_0` = `830027291.879898135415708582244` = `k_cold/2` exactly; `\|Γ+1\|` ≈ `1` | **FIRES** |
| **FT-COND** | the naive float64 `1−A²` must return exactly `0.0` | `0.0` | **FIRES** |
| **FT-SCAN** ✗ | absent sentinel → zero hits on BOTH methods; present sentinel → same non-zero set | present sentinel agrees exactly (`118` / `118`); **absent sentinel returns 1 hit on both** | **DOES NOT FIRE** |

### §1.3 The three failures, diagnosed exactly — every one is this lane's own freeze error

**`G-RHO2` — a mis-sized injection point, and the diagnosis is arithmetic.** The gate injects `k_0 ∈ {1e-10, 1e-12, 1e-14} × k_cold`. The crossover into the small-`k_0` regime — where `Z_load ≈ k_0/(jω)` and the beyond-wall dependence is genuinely second order — sits at `k_0 ≈ ω|Z_beyond|`. At the gate's own operating point that threshold is about five parts in a million, while the three frozen injections land between roughly two parts in a hundred thousand and two parts in ten — four to five orders of magnitude too coarse. **All three sit on the WRONG side of the crossover**, in the plateau where `Z_load ≈ Z_beyond` and the sensitivity exponent is genuinely `0`. The instrument measured the plateau correctly; the freeze pointed it at the wrong place. **The tolerance is not moved and the injections are not re-sited.** Repair, named and routed: inject `k_0 = ε·ω·Z_1`, not `ε·k_cold`. **`G-RHO2` gates Theorem 3(b) — the OFF-LIMIT sensitivity — and gates nothing else; Theorem 3(a), the exact independence AT the limit, is `G-RHO` and it passes at exactly zero.**

**`G-SCAN` — two causes, and the second one is a real methodological catch that a single-method scan would have hidden.** With the driver tracked, the two methods scan the identical `3921` files and agree on `46` of `50` patterns. The `4` that disagree are `C3_Gc::ADJ_S`, `C5_op10::ADJ_S`, `C6_hopf::ADJ_S`, `C7_refl::ADJ_S`, and in every case **METHOD A finds hits METHOD B misses** — never the reverse. **The cause is `\b`.** `git grep -P` runs PCRE in ASCII `\w` mode, so a Unicode subscript is a NON-word character and `S\b` matches in `S₁₁`; Python `re` on `str` uses Unicode `\w`, `'₁'.isalnum()` is `True`, so there is no word boundary and `S\b` does **not** match. Confirmed directly: the same pattern on the same line returns `False` under Python's default and `True` under `re.ASCII`. **The AVE corpus uses Unicode subscripts heavily (`S₁₁`, `ε₁₁`), so any two-method verification built on `\b` will silently diverge — this is the `grep`-completeness false-negative class, caught in the direction nobody expects (the hand-rolled scanner under-reporting relative to `git grep`).** Per the freeze, the DISAGREEMENT is the reported result, **the larger hit set (the union) is used** everywhere below, and nothing is reconciled silently.

**`FT-SCAN` — a self-referential sentinel, which is a freeze design flaw and not a corpus fact.** The "deliberately-absent" sentinel string is *defined in the driver*, and the driver lives inside `research/`, which is one of the four scanned directories. Once the driver is tracked the sentinel is in the corpus and cannot be absent — both methods return exactly `1` hit, they AGREE, and the frozen condition (zero on both) can never be met. **The same self-reference means the scan is TREE-STATE-DEPENDENT**: this lane's own result JSON, number-check and result doc all land in `research/` and all contain battery patterns. **Exact reproduction recipe, stated because the shipped digest depends on it: check out `8a27b1c9` — the driver commit, at which none of this lane's other artifacts exist — and run the driver.** That is the state in which both determinism runs were taken. Repair, named and routed: restrict METHOD B to `git ls-files` output and site the sentinels outside the scanned tree.

**Frozen consequence, applied:** `any RUN gate FAILS, or any self-test fails to fire, or any gate is UNRUN ⇒ this lane reports ROW-NOT-CERTIFIED for the affected task, adjudicates NO bin for it, and routes to a successor with a new version number.` **TASK 1 → `SCAN-NOT-CERTIFIED`, NO premise bin adjudicated. TASK 2 → `ROW-NOT-CERTIFIED`. TASK 3's gates are `G-PREC` (PASS) and its inputs are Theorems 1 and 2, gated by `G-BOND` and `G-ROW` (both PASS at exactly zero); it uses Theorem 3(b) nowhere, so `BIN-C` IS adjudicated and the dependency is stated here rather than assumed.**

### §1.4 Scope split — RUN, N/A, UNRUN

| task | gates RUN | N/A by construction | UNRUN by omission |
|---|---|---|---|
| TASK 1 | G-SCAN, FT-SCAN | — | **none** |
| TASK 2 | G-BOND, G-ROW, G-RHO, G-RHO2, G-COLD, G-UNIT, G-PLANE, G-NC-SIGN, G-NC-ECHO, G-NC-ARITH, FT-BOND, FT-ROW, FT-RHO, FT-PLANE, FT-ARITH | — | **none** |
| TASK 3 | G-PREC, G-COND, FT-COND | — | **none** |
| all | G-DET | — | **none** |

**Frozen:** `UNRUN ≠ PASSED`. **No gate in this lane is UNRUN by omission.**

---

## §2 — TASK 1: THE PREMISE AUDIT — a NOT-ADJUDICATED DIAGNOSTIC, reported in full

> **⚑ READ THIS ROW FIRST.** `TASK 1` is `SCAN-NOT-CERTIFIED` (§1.3). **No premise bin is
> adjudicated.** Everything in §2 is a DIAGNOSTIC shipped so a successor inherits a measurement
> rather than a silence, exactly as the axial and echo-delay lanes shipped theirs. **No sentence
> of §2 may be quoted as a bin outcome.** Every cite below was verified two-method at ship tip;
> where the two methods disagreed the union was used and the disagreement is in §1.3.

### §2.1 — THE PER-COUPLING GRADING TABLE

**Operational definition of a transport coupling, frozen at §2.2 of the prereg:** *a term in the
canonical energy functional whose energy density depends on the field at MORE THAN ONE node —
equivalently, a term containing a spatial derivative / finite difference.*

| id | coupling | what CANON states about its GRADING LAW | what the ENGINE codes | rides a kernel that vanishes at the `r_sat` last bond? |
|---|---|---|---|---|
| **C1** | `G` — shear bond modulus | **STATED.** `μ = G_vac·S` is carried in the frozen preregs of three certified/near-certified lanes and its consequence is a canon leaf: `bulk-impedance-at-saturation-boundary.md`:51 verbatim *"$G_{shear} \to 0$ (shear modulus vanishes)"*, with `Γ_shear = -1` in the :48–54 channel table; Op16 `c_shear = c_0·√S` at `operators.md`:56 | `cosserat_field_3d.py`:767 — `W_cauchy * G` rides `S_eps_sq` | **YES** |
| **C2** | `K = 2G` — bulk branch | **FORKED, UN-ADJUDICATED.** `coldq-polar-family_prereg-FROZEN.md`:312 froze *both* `BRANCH-SOFT sets K = 2*mu = 2*G_vac*S` and `BRANCH-STIFF sets K = 2*G_vac/S`, *"neither is preferred and both are run"*. The polar result at `:81` states the STIFF consequence verbatim: *"the bulk row becomes the incompressibility `Δ(r_sat) = 0` forced by `K → ∞`"* — and `:83` records that the indicial analysis that would settle it *"is part of the UNRUN set … and `BIN-PF-WALLSING` was therefore never evaluated"* | folded into `G` as `(2/3)G(tr ε)²`, `:767` — rides `S_eps_sq` | **SOFT: yes. STIFF: NO — it DIVERGES.** Canon has not chosen |
| **C3** | `G_c` — Cosserat coupling | **NO GRADING LAW STATED.** Canon gives the gap `m² = 4G_c/I_ω` (`cosserat-mass-gap.md`:11,:59) and the channel-4 row (`port-register.md`:50) but states no `G_c(A)`. VALUES are separately `ENG-CHOICE placeholder` (`two-band-kinematics_result.md`:53) | `cosserat_field_3d.py`:767 — `W_micropolar * G_c` rides `S_eps_sq`, the **strain** kernel | **UNDETERMINED BY CANON** — the engine's choice is the engine's |
| **C4** ★ | `γ` — couple-stress / curvature | **NO GRADING LAW STATED, AND THE RULE THAT WOULD DECIDE IT IS CANONICALLY UNDERDETERMINED.** See §2.2 — this is the load-bearing row | `cosserat_field_3d.py`:768 — `W_kappa * gamma` rides `S_kappa_sq`, a **CURVATURE** kernel with a DIFFERENT argument (`:762` `S_kappa_sq = clip(1 − kappa_sq/omega_yield**2)`) from C1/C3's (`:761` `S_eps_sq = clip(1 − eps_sq/epsilon_yield**2)`) | **UNDETERMINED — and on one live member of canon's own fork, NO** |
| **C5** | `k_op10` — Op10 quartic gradient | **NO GRADING LAW STATED.** The nearest canon is the Axis-C kernel-coupling taxonomy, `lattice-model-register.md`:72, which bins the winding/topological content as ***"blind** (frequency- AND amplitude-blind)"* — a statement about the integer charge, not explicitly about the coupling constant | `cosserat_field_3d.py`:769 — **OUTSIDE the saturation kernel entirely** | **UNDETERMINED BY CANON; engine says NO (ungraded)** |
| **C6** | `k_hopf` — Hopf / Chern–Simons | **NO GRADING LAW STATED.** Same Axis-C row is the nearest canon | `cosserat_field_3d.py`:771 — **OUTSIDE the kernel** | **UNDETERMINED BY CANON; engine says NO (ungraded)** |
| **C7** | `k_refl` — Op3 reflection density | **NO CANON LEAF PLACES IT IN THE ENERGY FUNCTIONAL.** The register it rides IS canonical — Op14 `Z_eff = Z_0/√S` (`operators.md`:54) forces `Γ² = (1/16)\|∇S\|²/S²` — but that is a DERIVED bookkeeping density, not an independent bond stiffness | `cosserat_field_3d.py`:770 — outside the kernel, and the density itself **DIVERGES** as `1/S²` (engine-regularized at `1e-6`) | **UNDETERMINED BY CANON; and it is not an independent transport coupling — double-counting it would be an error, so no exception is claimed on it** |
| **C8** | `Z_0` — EM/T2-transverse bond | **STATED, and it IS the declared exception.** `bulk-impedance-at-saturation-boundary.md`:48–54: *"$Z_{EM} \equiv Z_0$ … $\sqrt{\mu/\varepsilon}$ invariant under SYM scaling … $\Gamma_{EM} = 0$"*; the `μ`-grade is circulation-keyed so a static field sees no wall at all (`wall-taxonomy.md` §4) | Op14 register | **NO — by canon, and this is the MAPPED exception the ruling already names** |
| **O1/O2** | `ρ`, `I_ω` | inertia; coefficients of TIME derivatives | — | **OUT OF SET by the frozen definition, with that reason** |

**Freeze-completeness receipt.** The audited set was frozen before the scan. The scan found no
transport term in the canonical energy functional outside the set: `cosserat_field_3d.py`:727 (bare)
and `:766`–`:772` (saturated) enumerate exactly `G`, `G_c`, `gamma`, `k_op10`, `k_refl`, `k_hopf`,
which is C1–C2 (folded), C3, C4, C5, C7, C6. **No freeze error of enumeration is reported.**

### §2.2 — ★ THE LOAD-BEARING ROW, AND WHY IT IS NOT A BIN

**Canon states the kernel's SHAPE and its per-grade NORMALIZATION, and explicitly does NOT state
how grades AGGREGATE.** Verbatim, `research/2026-07-02_axiom4-combine-rule_result.md`:79:

> **"per-yield-normalized; cross-grade aggregation underdetermined at O(α)."**

carried into the axiom register itself, `manuscript/ave-kb/common/axiom-register.md`:190 verbatim:

> *"the combine is per-yield-normalized (single-radius-L2 falsified), cross-grade aggregation underdetermined at $O(\alpha)$"*

The two members, both quoted from `:108`–`:111` of the same result:

- **normalized-L2 ACROSS grades** — one combined amplitude, one kernel. *Every* grade collapses
  together at one surface. **The ruling's premise HOLDS.**
- **L∞ ACROSS grades** — *"separate `S_mu`, `S_eps` kernels — first grade to `S→0`"*, and at `:55`,
  *"The wall is whichever grade reaches `S→0` first"*. **The wall is grade-specific.** The `r_sat`
  wall is a STRAIN surface (`A(r) = r_sat/r` is an A1 dilatation), and the curvature amplitude
  `|κ| = |∇ω|` of a STATIC gravitational grading carries no micro-rotation at all — so `S_κ` is
  untouched at `r_sat`, the couple-stress bond modulus `γ·S_κ` stands at its full cold value, and
  **the rotational transport channel is LIVE across the last bond.**

The same result records, at `:110`–`:111`, that L∞-across-grades is *"the engine's coded choice,
NOT forced by the electron"* — and `cosserat_field_3d.py`:761–762 is exactly that coded choice,
while `:486` in the same file builds the OTHER member (`A_sq = eps_sq/epsilon_yield² + kappa_sq/omega_yield²`,
one combined amplitude) for the reflection density. **The engine carries both readings, in one file.**

**Stated plainly, as the freeze requires and without softening: on one live member of canon's own
open fork, a transport coupling does not ride the kernel that vanishes at the last bond, the linear
cross-channel conversion path reopens through the couple-stress bond, and the 2026-08-05 ruling
would DEMOTE to conditional. On the other member it stands as ruled. Canon does not choose, this
lane does not choose for it, and no grading law is invented to save either side.**

**What canon is missing, enumerated exactly (this is the minted question, not a default):**

1. **The cross-grade aggregation rule** — L∞ vs normalized-L2 — for the substrate's constitutive
   grades. Already open in canon since 2026-07-02; this lane adds that the `r_sat` wall's
   channel completeness DEPENDS on it, which the combine-rule lane did not know.
2. **A grading law for `γ(A)`** — none stated anywhere. The nearest canon gets is
   `q-g47-substrate-scale-cosserat-closure.md`:13, that at the operating point `u_0*` *"the
   substrate's Cosserat constitutive tensor reaches the substrate-scale saturation condition
   $S(A^*) = 0$"* — a statement about ONE operating point, not a grading law, and not about `r_sat`.
3. **A grading law for `G_c(A)`** — none stated.
4. **The `r_sat` behaviour of channel 4.** `bulk-impedance-at-saturation-boundary.md` tabulates
   three channels; `port-register.md`:47–50 carries four. **Two-method absence receipt: "Cosserat",
   "couple-stress" and "micro-rotation" each occur ZERO times in the whole of
   `bulk-impedance-at-saturation-boundary.md`, on `grep -c` and on an independent Python `re`
   count.**
5. **The `K(A)` fork** (C2) — `BRANCH-SOFT` vs `BRANCH-STIFF`, un-adjudicated, with the settling
   indicial analysis in the polar lane's own UNRUN set.

**A freeze imprecision of this lane, disclosed rather than stretched.** The frozen bins read
`PREM-EXCEPTION` = *"shown by canon NOT to vanish"* and `PREM-UNDERDETERMINED` = *"canon states no
grading law"*. **Neither wording anticipated the case that actually obtains — canon stating TWO
laws and declining to choose.** The direction is unambiguously toward `PREM-UNDERDETERMINED`
(nothing is *shown*), and the frozen consequence of that bin — mint the canon question, default to
nothing, leave the ruling RULED-PENDING — is the correct handling under either reading. **But the
bin is not adjudicated, because Task 1 is `SCAN-NOT-CERTIFIED`, and the bin wording is repaired by
a successor, not here.**

### §2.3 — THE PRECISION NOTE THE FREEZE REQUIRES WHETHER OR NOT A BIN MOVES

**Frozen:** `must state whether the consequence at the wall is a GAP OPENING or a BAND COLLAPSE, because the`

It is a **BAND COLLAPSE, not a gap opening.** The Cosserat gap is `m² = 4G_c/I_ω`
(`cosserat-mass-gap.md`:59). If `G_c` rides a kernel that vanishes, the gap **CLOSES**. If the
curvature stiffness `γ` rides the same factor, the whole rotational dispersion
`ω² = (2γ/I_ω)k² + 4G_c/I_ω` scales by it and **every** rotational frequency — band bottom and band
top together — is driven to zero. A fixed finite drive frequency then finds no state because it is
**above the local band top**, not because it is below a gap. **The ruling's conclusion (stateless,
no transport, unitary return) is unaffected; its phrase "all transport dead = one big gap" names
the wrong mechanism for the right outcome, and this is a wording precision note, NOT a kill.**
It is also the same mechanism the echo-delay lanes already carry as the local band top
`ω_max(r) = β·ω_C·S(r)^p`.

---

## §3 — TASK 2: THE DISCRETE ROW — theorems restated, then what the instrument measured

**Frozen:** `every statement of section 3 is a THEOREM available before any code exists and may NOT` **They are restated as derivations. What the instrument added is §3.3 onward.**

### §3.1 — The derivation, restated (not re-derived, not presented as a measurement)

The series/shunt assignment is DERIVED from the two field equations, not chosen:

```
rho dv/dt = dT/dx ,  dT/dt = mu dv/dx     with  V = T (traction), I = v (velocity)
  =>  L' = rho   (inertance, SERIES)      =>  mass  = SERIES inductance m_n
  =>  C' = 1/mu  (compliance, SHUNT)      =>  bond  = SHUNT capacitance 1/k_n
  =>  Z_0 = sqrt(L'/C') = sqrt(rho mu)    [the canonical Z_shear]
```

so the ladder, inward, is `… [L_1 = m_1] [C_1 = 1/k_1] [L_0 = m_0] [C_0 = 1/k_0] [Z_beyond]`, and
the collapsing bond loads the **SHUNT** branch. Two half-bonds in series across the wall surface
compose **harmonically**, and the beyond half carries canon's clipped kernel `S = sqrt(clip(1−A², 0, 1)) = 0`:

```
k_0 = 2 k_cold k_beyond / (k_cold + k_beyond)  =  0   EXACTLY, for every finite k_cold
Z_load = (k_0/(j om)) || Z_beyond = k_0 Z_beyond/(k_0 + j om Z_beyond)  =  0  EXACTLY
Gamma_LB = (Z_load − Z_1)/(Z_load + Z_1) = −1   EXACTLY
```

and the same row falls out of the equation of motion with no circuit language at all: with `k_0 = 0`,
`m_0 ẍ_0 = k_1(x_1 − x_0) + k_0(x_{−1} − x_0)` loses the beyond-wall coordinate **identically** and
becomes the free-end equation. `T = 0` at PLANE-LB is the stress-node condition; `Γ = −1` is its
value in the declared traction↔voltage convention. **Two independent routes, one row.**

### §3.2 — Why the collapse is EXACT and not merely deep

**Because springs in series compose harmonically and a harmonic mean with a zero member is exactly
zero.** This is the whole non-tautological content of Theorem 1, and the frozen negative control is
what makes it legible: the ARITHMETIC (parallel-spring) rule, which is the physically wrong rule for
this geometry, gives `k_0 = k_cold/2` — measured at `830027291.879898135415708582244`, exactly
`k_cold/2` — and with it the beyond-wall load takes **complete** control of the row: `G-NC-ARITH`
measures a maximum `|Γ + 1|` of `2.00000000000000000000000000000`, i.e. under the wrong averaging
rule the termination ranges over the whole unit circle from short to open as `Z_beyond` is swept.
**The control does its job: without the harmonic collapse there is no row at all, only a restatement
of whatever is beyond the wall.**

### §3.3 — ★ THE ρ-INDEPENDENCE RECEIPT

| what | frozen | measured |
|---|---|---|
| last-bond stiffness, harmonic rule | `k_0 == 0` exactly | `0.0` at every one of the `1680` harmonic points |
| the row | `\|Γ_LB + 1\| == 0` exactly | `0.0` at every one of the `1680` harmonic points |
| **beyond-wall independence** | spread over `Z_beyond × ρ_beyond` `== 0` exactly | **`0.0`** over `48` groups |
| **cold-side branch independence** | `Γ_LB(RHO-A) − Γ_LB(RHO-B) == 0` exactly | **`0.0`** |
| Ax-3 losslessness, both planes | `\|\|Γ\|−1\| <= 1e-50` | `0.0` and `0.0` |

**The grid this holds over, in full:** beyond-wall density `ρ_beyond/ρ_bulk ∈ {1e-30, 1e-15, 1, 1e+15, 1e+30}`;
beyond-wall load `Z_beyond/Z_1 ∈ {0, 1e-40, 1e-20, 1, 1e+20, 1e+40, INFINITY}` — i.e. **including the
free (`0`), the clamped (`INFINITY`) and the matched (`1`) terminations, the exact three readings
`FLAG-CAUSAL` forked over**; cold-side profile `RHO-A` (`p = 0.5`, `Z_1 → 0`) and `RHO-B` (`p = 2`,
`Z_1 → ∞`); `S_last` from `1e-3` down to `1e-30`, three decades past the echo-v2 window by design.

**The reason it is exact rather than asymptotic, stated once:** `Z_beyond` enters the load ONLY
inside `k_0 Z_beyond/(k_0 + j ω Z_beyond)`, i.e. only multiplied by `k_0`. At `k_0 = 0` the
derivative `∂Z_load/∂Z_beyond = k_0²/(k_0 + jωZ_beyond)²` is identically zero. **The zero spring
does not out-weigh the beyond-wall region; it disconnects it.** `FT-RHO` demonstrates the
instrument can see the alternative: at an injected `k_0 = 1e-300·k_cold` the same grid immediately
spreads by `6.32455532033675866399778708887e-286`.

### §3.4 — ★ AND THE PLANE, WHICH IS THE PART A SIGNED Γ CANNOT TRAVEL WITHOUT

`Γ_LB = −1` sits at the **inner** terminal of the last cold node. The wave arriving down the last
intact cell terminates one series inertance **outward** of that, at PLANE-N0, where

```
Z_N0 = j om m_0 + Z_load = j om m_0 ,     Gamma_N0 = (j om m_0 − Z_1)/(j om m_0 + Z_1)
```

— magnitude exactly `1` (`G-UNIT`), phase set by `ω m_0 / Z_1`. In the frozen units that ratio is
`(ω/ω_C)/S²` on the RHO-B branch, so the two planes read the SAME sign while `S² > ω/ω_C` and
OPPOSITE signs below it:

```
S_cross = sqrt(om/om_C) = 3.16227766016837933199889354443e-10     (frozen middle rung)
S_last  (physical, ell_node/r_sat = 6.0238983090250982e-19)      = 0.00000000109762455411903921151585733431
om m_0 / Z_1 at those values                                     = 0.0830027291879898136165708582241
S_last / S_cross                                                 = 3.47099360674291596844875758216
```

**Read plainly. At physical parameters the inversion does NOT fire** — the node-mass reactance is
about eight per cent of the line impedance, so PLANE-N0 reads a short with a small phase and the
free-end label survives the plane shift by a factor of about three and a half in `S`. **But the
inversion exists and this instrument reached it**: `G-PLANE` measures `2.00000000000000000000000000000`,
the maximum the unit circle allows, in the deliberately-over-extended part of the sweep where the
RHO-B node mass `ρ_bulk ell/S³` has outgrown `Z_1 = 1/S`. **Under RHO-A it never fires anywhere on
the sweep**, because there the node mass does not grow at all. `FT-PLANE` confirms the effect is
frequency-carried, not an artifact: at `ω/ω_C = 1e-200` the two planes coincide to
`6.32455532033675866399778708887e-196`.

**What this does and does not do to the ruling.** It does **not** touch the mirror conclusion: `|Γ| = 1`
at BOTH planes, exactly, so nothing is transmitted and no port is crossed at either. It **does** mean
the ruling's *"free end (stress node — the canonical short in the impedance convention)"* is a
**PLANE-LB** statement, and that under RHO-B deep enough into the skin the same wall reads as a
clamped end one node outward. **That is the sign-relativity ruling's own content, measured at the
sub-node scale for the first time, and it is surfaced rather than folded away.**

### §3.5 — Theorem 4: the approach is untouched, and this lane recomputed none of it

`Γ_LB` is a property of the elements at and inward of PLANE-LB. The certified RHO-A prompt delay,
the RHO-B log law, the node-governed turning point and the achromaticity are integrals over cells
strictly OUTWARD of that plane; Theorems 1–3 alter no such cell. **This lane cites
`research/2026-08-04_echo-delay-regulated-sum_result.md` and
`research/2026-08-05_echo-delay-v2-reach-through_result.md` and recomputes nothing from either.**
The only predecessor numbers READ are echo-v2's frozen `Γ_L` sweep declaration and its
`junction_two_port` identity receipt, both consumed by `G-NC-ECHO` (§4.2).

---

## §4 — TASK 3: THE CONTINUUM RECONCILIATION — `BIN-C-DISJOINT`, ADJUDICATED

**Frozen precedence:** `BIN-C-CONTRADICT` > `BIN-C-DISJOINT` > `BIN-C-SHADOW`.
**Frozen:** `BIN-C-DISJOINT requires the loss point to be NAMED and DEMONSTRATED, not asserted. A`

### §4.1 — The verdict

**`BIN-C-CONTRADICT` does NOT fire.** There is no plane and no observable at which the discrete row
predicts a value the continuum row forbids, because — as §4.3 demonstrates — the two rows are never
evaluated on the same object.

**`BIN-C-SHADOW` does NOT fire.** The continuum row is not the `ell_node → 0` limit of the discrete
row: the discrete row is *constant* in that limit (`Γ_LB = −1` at every rung), while the continuum
row is an analyticity constraint on a branch exponent, which is not the limit of a constant.

**`BIN-C-DISJOINT` FIRES, with two loss points named and demonstrated.**

### §4.2 — LOSS POINT 1: the traction is an indeterminate form and the two limits resolve it oppositely

The wall traction is `T = μ(r)·∂ψ/∂r`, and at `r → r_sat` this is `0·∞`.

- **The continuum** evaluates it along a solution branch: `T ~ η^{Re σ − 1}` with the axial lane's
  measured exponents `-0.1459563385925332` (`σ₊`) and `-0.8540436614074667` (`σ₋`) — both negative,
  **so the traction diverges on both branches** and, in that lane's own words, a traction-free wall
  condition *"is not merely a different condition, it is an unsatisfiable one"*. **Those two numbers
  are READ from `research/2026-08-04_coldq-axial-rhob_result.md` §2.2 and are NOT recomputed here.**
- **The lattice** evaluates it as a product: `T_latt = k_0 · (x_0 − x_{−1})` with `k_0 = 0` EXACTLY
  (Theorem 1, `G-BOND` = `0.0`) and the finite difference bounded by construction. **The product is
  exactly zero.**

**The step where the continuum loses the last bond is therefore explicit: `∂ψ/∂r` at the wall is not
the limit of the lattice's last-bond difference quotient, because the lattice never takes that
quotient — its last bond has no stiffness with which to convert a displacement difference into a
traction.** The continuum takes a limit of a ratio; the lattice takes a product with an exact zero.
`0·∞` is not commutative between those two procedures, and that non-commutation IS the loss point.

### §4.3 — LOSS POINT 2: the row survives the limit but its transport to any observer does not

`Γ_LB = −1` is `ell_node`-independent: the ladder rung below reports it unchanged at every rung.
What is NOT `ell_node`-independent is the delay through which it reaches an observer, `exp(−2jωT)`:

| `ell_node/r_sat` | `S_last` (from the cancellation-free `S²`) | RHO-B round-trip optical length (`r_sat/c_0`) | RHO-A round-trip (control) | `Γ_LB` |
|---|---|---|---|---|
| `1e-3` | `0.0446878506420480353805937212568` | `8.50079004591562371799002486417` | `2.52450674679608492105838669591` | `-1` |
| `1e-6` | `0.00141421250171393973397294133247` | `15.4100439498559847221716022637` | `2.53704775037165751343703472091` | `-1` |
| `1e-12` | `0.00000141421356237203438862990991939` | `29.2255560078188838262378842157` | `2.53711865875262268475491205922` | `-1` |
| `6.0238983090250982e-19` | `0.00000000109762455411903921151585733431` | `43.5479170493955256194828841603` | `2.53711866099496463908574175566` | `-1` |

**The RHO-B growth is exactly logarithmic and the rate is derived, not fitted.** From the exact
antiderivative `F(x) = x + (1/2)·ln(x/(x+2))` of `1/S²` on the cancellation-free
`S² = x(2r_sat+x)/(r_sat+x)²`, the one-way length grows by `ln(10)/2` per decade of `ell_node`:
measured `1.15137588677240333934710329731` against `1.15129254649702284200899572734`, relative
separation `0.0000723884434360948500593824509422`.

**★ The frozen negative control is what makes this a mechanism and not a coincidence.** On RHO-A the
identical span of `ell_node` moves the optical length from `2.52450674679608492105838669591` to
`2.53711866099496463908574175566` — a factor of `1.00499579253354892345842477337` over **nine
decades**, i.e. the optical length is FINITE and essentially `ell_node`-blind. **The divergence is
RHO-B-specific, exactly as the frozen §4.2 step-4 control required. Had RHO-A shown the same
behaviour, this diagnosis would have been wrong and the lane would have said so.**

**So: the continuum RHO-B problem correctly imposes a purely-ingoing branch (`ROW-IN`) — not because
the termination is different, but because at infinite optical length nothing returns in finite time,
so a terminal impedance is not a well-posed object there. The lattice cuts the line at a finite
number of cells, the terminal impedance exists, and it is a short. Both rows are right about their
own object.**

### §4.4 — What this does NOT do

**Frozen:** `this task ADJUDICATES NEITHER ROW. Both` **Honoured: `research/2026-08-04_coldq-axial-rhob_result.md` is byte-untouched** (empty-diff receipt, §6). Its `⚑ CONTINUUM-VS-LATTICE CARVE` at `:383` already states the framing this lane's numbers now instantiate; **this result is that carve's arithmetic, not a revision of it.**

---

## §5 — CONSEQUENCES DOCUMENTED (no claims minted, nothing propagated)

### §5.1 — The `Γ_L` fork read back onto echo-v2's sweep — CITED, NOT RECOMPUTED

Echo-v2 swept `Γ_L ∈ {0, −1, +1}` and quantified the cost of the unresolved fork rather than
resolving it. **This lane's derived row selects the `−1` member.** Two receipts, both READ from the
predecessor's shipped JSON by `G-NC-ECHO`:

1. The frozen sweep declaration is present, so `−1` is a member of the predecessor's own swept set —
   **the two lanes agree on the convention** (`G-NC-SIGN` byte-checks the echo-v2 prereg's verbatim
   *"`Γ_L = −1` in the traction↔voltage convention"*).
2. **All `4` of echo-v2's `junction_two_port` entries carry `is_identity` with `W = 0`.** That is the
   predecessor's own measurement that the depleted section provides **no screening at all** — the
   two-port is the identity exactly — so at PLANE-JX the observable `Γ_in` **is** `Γ_L`.

**Consequence, stated as a consequence and not as a new claim:** on the free-end member, echo-v2's
`FLAG-CAUSAL` three-way spread of `0.9921666107469662` collapses onto its `Γ_L = −1` limb, with no
numerical cushion between the fork and the observable. **This lane recomputes none of echo-v2's
numbers and mints nothing; the selection is the consequence of §3, and whether it propagates is
Grant's.**

### §5.2 — The EM-channel exception

The ruling's point 5 names the EM channel as the mapped exception, and canon agrees at the leaf:
`bulk-impedance-at-saturation-boundary.md`:48–54 gives `Z_EM ≡ Z_0 = sqrt(μ/ε)` **invariant under
SYM scaling**, hence `Γ_EM = 0` — transparent, not reflecting — with that leaf's own conclusion that
*"BH-echo yes/no is therefore a channel question."* The `μ`-grade is **circulation-keyed**, so a
field that would saturate the rotational grade if it were circulating does nothing when it is static
(`wall-taxonomy.md` §4, Route C). **Nothing in this lane touches that row: the EM channel passes the
wall by matching, not by a bond that survives, and it is the one exception canon has actually
stated.** Contrast with §2.2: the Cosserat channel's behaviour at `r_sat` is the exception canon has
**not** stated, and that is the whole of Task 1's finding.

### §5.3 — Cross-reference to the electron-ontology E1 closure (feeds; adjudicates nothing)

`research/2026-08-04_electron-ontology-walk_framing-note.md`:91 routes **E1** as *"the |Γ|=1
self-closure condition stated and derived in the certified-circuit language (post RHO-B;
plane/projection per wall-taxonomy §9)."* **This lane's §3 is that derivation pointed inward:** the
same kernel collapse that terminates the ringdown ladder outward-in is what closes a self-trapped
soliton's own ladder, and this lane supplies E1 with (i) `|Γ| = 1` measured exactly at both planes,
(ii) the exact beyond-wall independence, and (iii) **the plane caveat E1 will need — the sign is
plane-relative and inverts one node-mass outward once `S² < ω/ω_C`.** That third item is the useful
one and it is a caution, not a result about the electron. **This lane adjudicates nothing about the
electron, mints nothing, and E1 remains unscoped.**

---

## §6 — FLAGS, FENCES AND RECEIPTS

### §6.1 — Flags raised (surfaced, not repaired)

**⚑ FLAG-COMBINE — the ruling's premise is degenerate with canon's OWN open cross-grade
aggregation fork.** Full statement and cites at §2.2. **Routed to Grant. This lane proposes no
resolution and invents no grading law.**

**⚑ FLAG-CH4 — canon's `r_sat` channel table enumerates three of the four canonical channels.**
`bulk-impedance-at-saturation-boundary.md`:48–54 vs `port-register.md`:47–50; two-method absence
receipt at §2.2 item 4. **Surfaced, edited nowhere.**

**⚑ FLAG-KFORK — the bulk modulus grading is a live un-adjudicated fork with a DIVERGING member.**
`coldq-polar-family_prereg-FROZEN.md`:312 (`BRANCH-SOFT K = 2 G_vac S` vs `BRANCH-STIFF K = 2 G_vac/S`),
`coldq-polar-family_result.md`:81 (`K → ∞` on STIFF), `:83` (the settling indicial analysis is in
that lane's UNRUN set). **On the STIFF member the bulk bond does not collapse at the wall.**
Surfaced; not adjudicated; the polar lane owns it.

**⚑ FLAG-A2DEF — the engine carries TWO definitions of the saturation amplitude in ONE file.**
`cosserat_field_3d.py`:761–762 build separate `S_eps_sq` and `S_kappa_sq` (the L∞-across-grades
member); `:486` (and `:411`) build one combined `A_sq = eps_sq/epsilon_yield² + kappa_sq/omega_yield²`
(the normalized-L2 member). **Both are in the same module and they are the two members of
FLAG-COMBINE.** Reported as an internal inconsistency of the engine relative to itself; **not
repaired here — `src/ave` is byte-untouched and was never imported.**

**⚑ FLAG-PLANE-N0 — the free-end sign is plane-relative and inverts one node-mass outward.**
Measured, §3.4. The MAGNITUDE (`|Γ| = 1`) is plane-invariant; the SIGN is not. **Consequential for
the ruling's point-3 wording and for the doc-lane Q2/Q3 inheritance, both of which are gated
follow-ons.**

**⚑ FLAG-SCANSELF — this lane's frozen scan design is self-referential and tree-state-dependent.**
§1.3. Repair named; routed to a successor with a new version number.

**⚑ FLAG-UNIWB — `\b` means different things to `git grep -P` and to Python `re`, and the AVE corpus
uses Unicode subscripts.** §1.3. **This is a general instrument hazard for any two-method
verification in this repo, not a fact about this lane's physics.** Surfaced for the discipline lane.

### §6.2 — Fences honoured

- **No canonical propagation.** wall-taxonomy §10, the FLAG-W leaf blocks and the FLAG-CANON
  `vol3/claim-quality.md`:122/:124 repair are **untouched**. Research doc + docket fragment only.
- **No `clm-`/`def-` minted; no solidity moved; no falsification-ledger edit.**
- **Engine fence.** `src/ave` byte-untouched; the driver does not import `ave` at all.
- **Predecessor fence.** Every predecessor named in prereg §1 is byte-untouched, discharged by an
  empty `git diff --stat` against the freeze base (§6.3).
- **No adjudication** of FORK-3(b), `FLAG-ECO`, the RHO-B lane's bins, the polar family, or anything
  observational. **No Regime-IV physics**: the beyond-wall region is an arbitrary passive load and
  was never solved. **No `γ`/`G_c` VALUES.** **No discriminator, no chord.**
- **No repair of a kill.** `PREM-EXCEPTION` did not fire as a bin; the direction that points at it is
  reported at full strength in the headline and §2.2, with no fix proposed and no wording softened.

### §6.3 — Reproduction and determinism receipts

- **Determinism.** Two full runs, digest `778f6ef9f0819a6f` twice, shipped objects byte-identical
  apart from `_runtime_sec`. Runtimes are written without back-ticks and are NOT registered
  (machine-dependent; the #801 R3 lesson).
- **Exact reproduction recipe** (load-bearing because of FLAG-SCANSELF): check out `8a27b1c9`, the
  driver commit, at which none of this lane's other artifacts exist, and run the driver. That is the
  tree state in which both determinism runs were taken.
- **Number check.** `research/drivers/last_bond_kernel_collapse_number_check.py`, wired into
  `make verify` via its own Makefile target, with a **mutation receipt**: the target invokes the
  checker a second time with `--mutation-receipt`, which perturbs one registered numeral in a copy
  of this doc and asserts the checker FAILS — so the gate proves it can fail on every invocation.
  Three registered values are **DERIVED** in the checker from registered JSON inputs by a stated
  formula (`ω m_0/Z_1 = (ω/ω_C)/S_last²`, `S_cross = √(ω/ω_C)`, and their ratio) rather than read.
- **Makefile conflict class, disclosed unchanged.** This lane touched the shared `.PHONY` line and
  the shared `verify:` prerequisite line, plus an appended target block. **Those two shared lines
  are a REAL two-line union-conflict class with any concurrent lane, not an append-only merge.** The
  umbrella-glob proposal that would replace the per-lane targets is **pending**, so a per-lane target
  is still the shipping form and one was added.

---

## §7 — SVA PILOT NOTES (pilot case 5)

| row | filled? | what it was worth on this lane |
|---|---|---|
| **1 · Sector / ownership** | FILLED | **High, and it located the finding.** Forcing "which channel owns `γ`" is what turned the audit from "does the modulus go to zero" into "which SATURATION ARGUMENT does it ride" — the question whose answer is FLAG-COMBINE. Without row 1 this lane would have read `S_eps_sq` and `S_kappa_sq` as two spellings of one kernel. |
| **2 · Regime / phase-state** | FILLED | Moderate. Kept Regime IV out of the domain as an arbitrary load rather than a solved region, which is what made THEOREM 3's *"for every `Z_beyond` including `0` and `∞`"* formulation available at all. |
| **3 · Circuit statement** | FILLED | **HIGH, and it decided the algebra.** Stating the object as *a ladder whose last shunt compliance is unbounded* before any framework word is what produced the series/shunt DERIVATION (§3.1) instead of an assumed topology — and the series/shunt assignment is what makes "the collapsing bond loads the SHUNT branch" a theorem rather than a convention. |
| **4 · Plane & projection** | FILLED | **HIGHEST value on this lane, and it produced a result nobody asked for.** The mandatory PLANE-N0 secondary is the only reason FLAG-PLANE-N0 exists. A lane that reported only PLANE-LB would have shipped *"the wall is a free end, `Γ = −1`"* and been silently wrong about the plane a wave actually meets. **This row alone justified the header.** |
| **5 · Constitutive provenance** | FILLED | **High.** Tagging `γ(A)`/`G_c(A)` as *"THE OBJECT OF TASK 1 — provenance UNDETERMINED at freeze and to be BINNED, not assumed"* is what stopped the lane importing the engine's coded choice as canon on first read. |
| **6 · Energy ledger** | FILLED | **High and it generated a gate.** *"No port is crossed; `Re{Z} = 0` at every element by Ax 3"* is what turned `\|Γ\| = 1` into `G-UNIT` rather than an assumption — and `G-UNIT` at both planes is what keeps FLAG-PLANE-N0 from being over-read as a physics change. |
| **7 · Calibratability** | FILLED | Low-moderate. Everything here is already a ratio or a phase; the row's value was confirming `α` appears nowhere. |
| **8 · Discrimination class** | FILLED | **High, and it cost the lane its most quotable sentence before the result existed.** The frozen tautology filter says out loud that *"a zero spring transmits no force" IS a near-tautology* and names the three non-tautological items. **Writing that at freeze is what stopped the headline being "the wall is a free end, derived".** |
| **9 · Certification plan** | FILLED | **High — and it is why this doc reports three failures instead of a clean sweep.** All three negative controls were named in advance and all three passed; the three failures are all in gates the lane sized wrong, and UNRUN ≠ PASSED kept every one of them visible. |
| **10 · Adjudication routing** | FILLED | **High.** Pre-stating that `PREM-EXCEPTION` would DEMOTE the ruling — *"report plainly, do not soften"* — before knowing the answer is what makes §2.2's headline sentence writable now. |
| **11 · ★ Numerical conditioning** | FILLED | **HIGH, and it caught something real for the third pilot running.** `G-COND` measures the naive float64 `1−A²` returning **exactly `0.0`** at `ell_node/r_sat = 6.0238983090250982e-19` while the cancellation-free form returns `1.20477966180501963891137947488e-18`. The row also forced `Γ+1` to be evaluated from `2Z_load/(Z_load+Z_1)`; had it been formed by adding `1` to a computed `Γ`, THEOREM 2's exact-zero result would have been a float-noise result instead. **Row 11 is the difference between "exact" and "small" in this lane's headline.** |

**Two amendments this lane proposes to the SVA, from its own failures, offered as feedback:**

1. **A row-9 sub-clause: an instrument whose scan includes its own output directory is
   self-referential and must exclude its own artifacts by construction.** FLAG-SCANSELF cost this
   lane a certification. The generalization is *"a gate whose input set contains the gate's own
   output is not a gate"*, which is the row-9 *"a gate that cannot fail is not a gate"* principle
   pointed at the input side rather than the threshold side.
2. **A row-11 sub-clause: name the REGEX ENGINE, not just the precision.** FLAG-UNIWB is a
   conditioning failure in the same sense as a float64 cancellation — two implementations of "the
   same" comparison silently disagreeing on a corpus that uses Unicode subscripts. Row 11 currently
   covers arithmetic conditioning only.

---

> **Result provenance.** Resolves the frozen gates and bins of
> `research/2026-08-05_last-bond-kernel-collapse_prereg-FROZEN.md` (commit `bf77d07a`, COMMIT 1 of
> this lane, pushed **ALONE** before any driver code existed and before any number produced by this
> instrument existed). The driver landed as COMMIT 2 (`8a27b1c9`), before any result existed. All
> numbers above are read from the shipped
> `research/drivers/last_bond_kernel_collapse_results.json` and are machine-verified against it by
> `research/drivers/last_bond_kernel_collapse_number_check.py`, wired into `make verify` with a
> mutation receipt. **Mints no `clm-`/`def-`; propagates to no leaf; engine byte-untouched and never
> imported; falsification ledger untouched.** Companion: the docket fragment
> `_orchestration/docket-entries/2026-08-05-last-bond-kernel-collapse.md`.
