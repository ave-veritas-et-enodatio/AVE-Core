# THE 𝒜_g DERIVATION (R46 DERIVE-FIRST) — RESULT

**Date:** 2026-08-10 · **Branch:** `lane/2026-08-10-ag-derivation` · **Base:** `origin/main` @ `8424995f`
**Frozen prereg:** [`2026-08-10_ag-derivation_prereg-FROZEN.md`](2026-08-10_ag-derivation_prereg-FROZEN.md), committed ALONE + pushed at `ee728d89` (freeze-by-push) before any driver code, consumer arithmetic, or 𝒜_g number existed. Byte-untouched since (verified at the Tier-2: `git diff ee728d89..HEAD --` on it is empty).
**Driver:** [`research/drivers/ag_derivation_lane.py`](drivers/ag_derivation_lane.py) → [`ag_derivation_lane_results.json`](drivers/ag_derivation_lane_results.json) + [`ag_derivation_lane_number_check.py`](drivers/ag_derivation_lane_number_check.py) (`--mutation-receipt` fires; auto-discovered by the `make verify` umbrella). Post-Tier-2 instrument: two full runs byte-identical (sha256 `f8a31e9e…`, printed digest = file digest); 34 checks green. *(First-cut instrument: `de439794…`, 24 checks — preserved in `bbe57bc8`/`cf576d3f`.)*
**Class:** DERIVATION-ADJUDICATION. **Mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`; edits no KB leaf, register, ledger, or ruling; changes no solidity; all propagation ROUTED.** Engine `src/ave` byte-untouched (`git diff --stat 8424995f..HEAD -- src/` empty; backreaction.py consumed READ-ONLY + run, per the brief's stencil-lens fence).
**Lane brief:** [`_orchestration/2026-08-10_ag-derivation-brief.md`](../_orchestration/2026-08-10_ag-derivation-brief.md) (R46).

---

## HEADLINE

> **🔴 VERDICT AS RE-CUT AT THIS LANE'S OWN TIER-2 (2026-08-10, pre-presentation; the original verdict below is PRESERVED per Rule 12 and OVERTURNED).** The six-lens adversarial panel (★addendum §9) REFUTED the first-cut `OVER-DETERMINATION-FAILS`: **the 4π source convention the first cut declared absent is DECLARED CANON at the profile's own derivation home** — [`gordon-optical-metric.md:25`](../manuscript/ave-kb/vol3/gravity/ch03-macroscopic-relativity/gordon-optical-metric.md) (`kind: leaf`, `claims: [clm-rd9cjm]` — the exact claim-id the ch02 implication line cites): `−(c⁴/7G)∇²ε₁₁(r) = 4πMc²δ³(r)`, with the Green's function named at `:28` (*"Convolving this source with the 3D Laplacian Green's function (−1/4πr)"*) and the profile `ε₁₁ = 7GM/c²r` derived at `:33`; byte-equivalent published twin at `03_macroscopic_relativity.tex:38-46`; both present since 2026-05-16 (`8f16bf20`). Under the canon-declared convention **f_chain = 7 = f_profile exactly (sympy; `consumers_agree_under_canon_convention: true`) — the consumers AGREE and BC-SRC's internal falsifier did NOT fire.**
>
> **The first-cut absence claim was an instrument artifact:** all three C3c sweep patterns were structurally incapable of matching LaTeX `4\pi` (the backslash defeats `4\s?[πp]`), all required a literal `T00` token canon's declaration never uses, the sweep had no known-positive liveness control, the claim-id trail of clm-rd9cjm was never walked, and the doc's "two engines" attestation was false for this sweep (single-engine). The exact failure class of the banked grep-completeness and follow-the-claim-id-trail disciplines — instrument repaired (liveness control + symbol-agnostic patterns + genuine second engine + claim-id-trail enumeration; the repaired sweep finds the declaration: `declaration_found` lists both sites).
>
> **RE-SCORED VERDICT under the SAME frozen §4 grammar (no new criteria minted), applied mechanically with the repaired instrument: `CONSISTENT-UNVALUED`.** Rule 1: the reconciliation is canon-declared and exhibited → `f_chain = 7`. Rule 3: dead on both clauses (`f = 7`; no VALUED pair exists to disagree). Rule 2: `DERIVED(c)` blocked (C1 UNVALUED). Rule 4: the everything-else bin, with sub-cases: **C1 UNVALUED** (no absolute `B(M)` anchor in canon; upgrade = an absolute dress-amplitude normalization), **C2 VALUED-BRACKET** `𝒜_g ≈ 3.7e32–1.2e33 m²` ⇒ `c = 𝒜_g/ℓ_node² ≈ 2.5e57–7.9e57` — NOT at ℓ_node² scale per rule 5, and self-inconsistent with the Regime-I declaration (solar-surface dress strain `2.3e10` vs yield ~1), **C3 COHERENT at f = 7** (values nothing standalone). Per the brief's own sentence this bin fires the interlock/count adjudication; per R46: *"If NOT: 𝒜_g enters the interlock register with the count adjudication (3→4 or new edge class) as a Grant ruling."* **The frozen hypothesis `𝒜_g = c·ℓ_node²` (c a pure number) is NOT-SUPPORTED on every receipted route: no consumer values 𝒜_g at ℓ_node² scale, and the only valued route is 57 OOM away.**
>
> *(Original first-cut verdict, PRESERVED verbatim and OVERTURNED:)* **🔴 VERDICT (per the frozen §4 grammar, mechanically applied): `OVER-DETERMINATION-FAILS` — the receipted consumers DISAGREE on 𝒜_g by exactly 4π. BC-SRC's internal falsifier has fired; the lane STOPPED at the frozen rule and routes to Grant immediately. This is the axiom's own test port doing its job.** *(Its three-branch convention fork for Grant is VACATED: canon already sits at branch (a) — the fork asked Grant to rule what canon had ruled. The first cut's branch-(b) pricing was additionally wrong — `κ = c⁴/7G` IS `T_max,g`, the Machian stress boundary, load-bearing at 8+ independent sites tied to the ξ chain, not "bookkeeping"; and the first cut's branch-(c) elimination was post-STOP derivation outside the frozen scope — both preserved in `cf576d3f`, both retracted as adjudications.)*

---

## §0 — REGIME / SECTOR / PHASE-STATE (prereg SVA §0 governs; restated)

**MODE** — over-determination adjudication of one constant (𝒜_g). **SECTOR** — A1 bulk/dilatation slot; dress `u₀` and grade `ε₁₁` are TWO OBJECTS, named per use; T2/Cosserat untouched. **REGIME** — all consumers read crystalline cold-linear Regime-I, exterior D→1; the C3 lattice legs measured `max_A ≤ 1.1e-2` (linear ✓). **STENCIL** — `backreaction.py` is a CONSUMER, not an adjudicator; no engine file edited.

**Instrument deviations (all six, direction-of-effect stated; #1/#4 re-cut and #5/#6 ADDED at the Tier-2):**
1. *(RE-CUT per Tier-2 finding 3′.)* The first cut demoted the parity-confounded point-source profile fit and substituted the discrete flux identity as "the load-bearing chain receipt," claiming direction-of-effect STRENGTHENS. The panel's κ-rescale probe showed the flux identity is **normalization-blind** (ratio 1.0 under any rescale, including a deliberate 4π error) — a solve-residual check, not a chain-coefficient receipt; the substitution replaced a biased-but-informative estimator with an information-free one. Honest state: the **smooth-blob masked fit is the only native GF-amplitude measurement** (`b/b_bare-GF = 0.951`, R² `0.881`, window band `0.74–0.99` across shifts) — sufficient to separate 1× from 16×, NOT a tight bare-GF confirmation; the ×16 point-source inflation = 1/(class fraction) with the class count **16** settled three ways (difference-lattice index `|det| = 16`; `connected_components(L) = 16` at N ≡ 0 mod 4 — halves to 8 at N ≡ 2 mod 4, run caveat; FFT lattice-GF `b·4π = 15.75–15.85` vs 16 predicted). The point-source fit is a MASKED fit (drops the 15/16 zero classes), not "site-averaged" as the first cut wrote. Direction-of-effect of THIS re-cut: weakens the lattice side to its honest content; the SI-chain conclusion now rides the sympy algebra + the canon-declared convention, where it belongs.
2. Point leg ran via `relax_finite_core_strain` (the Stage-1 entry the two-way loop wraps), blob via the full two-way `solve_backreaction` (komar, `max_A = 1.07e-2`) — covering the frozen "point + blob". Direction-of-effect: none (the two entries share the operator; the blob leg exercises the production path). The point leg's `converged: false` is DISCLOSED: the flag keys on shell-radius stationarity requiring a saturated shell (`A ≥ 0.99`), structurally unreachable at `max_A ~ 1e-3`; the underlying interior solve is a direct `spsolve` (residual machine-zero — the flux receipts).
3. GNU grep absent on this host; BSD `grep -rniE` + Python `re` where two engines are claimed (inherited disclosure). Direction-of-effect: none observed where both ran; **the first cut falsely attested "two engines" for the C3c sweep (single-engine as committed)** — repaired: the C3c sweep now runs both engines with per-pattern agreement banked.
4. The C1 sweep excludes the lane's own artifacts BY CONSTRUCTION (SVA row-9 pilot-5 G-SCAN). Direction-of-effect: none — every excluded hit was lane-own text (descriptor corrected at the Tier-2: of the two excluded `4πB` prereg hits, one is the clause-S quote (`:36`), the other is the prereg's own sweep-pattern list (`:47`)).
5. *(ADDED at the Tier-2.)* The C2 solar-surface strain exhibit (`2.3e10`) was NOT in the frozen §3-C2 method — an un-preregistered added analysis, disclosed here. Direction-of-effect: strengthens the negative on the hypothesis (it makes C2's bracket self-inconsistent); routed as exhibit, adjudicates nothing.
6. *(ADDED at the Tier-2.)* With deviation #1's substitution, the first cut delivered `f = 7/(4π)` from sympy alone — the frozen G-C3 wording "f measured with fit receipts" was not met by any lattice measurement of f. Disclosed; the re-cut's f-statement is explicitly algebra + canon-declared convention, with the blob fit as the lattice-side amplitude cross-check at its honest precision.

---

## §1 — C1: the dress normalization → UNVALUED

Clause G applied to the canon profile reproduces the internal relation exactly: `u₀ = −𝒜_g∇(7GM/c²r) = 𝒜_g·(7GM/c²)·r̂/r²` ⇒ `B_pred = 7·𝒜_g·GM/c²` (sympy, `bridge_matches_internal_relation: true`) — a coherence receipt, pinning nothing by itself (prereg row-8 tautology filter).

**Absolute-anchor sweep (two engines, presence/absence agreement on all 6 patterns; lane-own artifacts excluded by construction; the zero-counts now ASSERTED in the number check, not merely engine-agreed):** the `4πB` family (12 hits) = the S-clause statement + its quotes, symbolic dress-structure statements (`u₀ = B/r²`, `∮u·n̂ = 4πB`), one unrelated Larmor caveat, and the Coulomb-energy integral — all PROPORTIONAL or symbolic. The `B = [0-9]` family (267 grep-line hits; 182 after noise screening) = Boltzmann `k_B`, galactic `(l,b)` coordinates, binding energies `E_B`, boiling points `T_b`, orbital radii `R_b`, static-B tesla values — none a dress amplitude. **Zero absolute dimensional `B(M)` sites exist in canon**, consistent with the going-in receipts (`screening-theorem :19` `qₐ ∝ mₐ` proportional-only; `a1-port-sourcing :387` *"NO receipted DC-anchor normalization closes the amplitude"*). Tier-2 caveat carried: the C1 patterns share the C3c patterns' LaTeX-blindness class, and a corroborating LaTeX-aware sweep (`4\s*\\,?\s*\\pi\s*\\,?\s*B`) also returned zero — C1's bin survives, un-derisked only against exotic spellings. **C1 output: UNVALUED** (frozen §3-C1 rule).

## §2 — C2: the halo added-mass row → VALUED-BRACKET, 57 OOM off ℓ_node² scale, and self-inconsistent

Exact convected-dress added mass (sympy; independent finite-difference + quadrature cross-check `1.05e-12` rel; independently re-derived by two Tier-2 lenses):

> `m_add = ρ_bulk · (8π/3) · B²/r_c³` (inner cutoff r_c; cutoff-dominated — `𝒜_g ∝ r_c^{3/2}` — exhibited)

With `B = 7·𝒜_g·GM/c²` and the P9 row's only magnitude anchor (the halo IS the phenomenological DM mass: `m_add/M ∈ [1,10]` declared at freeze), at the ENG-CHOICE galactic parameters (`M_b = 6e10 M_⊙`, `r_c = 10 kpc`, `ρ_bulk = 7.91e6 kg/m³` canonical; the χ=1 inversion closes exactly, `m_add/M = 1.00` — Tier-2 re-derived):

> **`𝒜_g(required) = 3.71e32 m²` (χ=1) … `1.17e33 m²` (χ=10)** ⇒ **`c = 𝒜_g/ℓ_node² = 2.5e57 … 7.9e57`.**

**Self-consistency exhibit (deviation #5; routed):** at the χ=1 value the dress strain `~2B/r³` is `1.6e-14` at galactic r_c (fine) but **`2.3e10` at a solar surface — ~10 OOM past the yield strain** — the halo-required 𝒜_g would put every star's near dress past-wall, contradicting the crystalline cold-linear regime every consumer (and the P9 row itself) declares. **C2 output: VALUED-BRACKET, NOT at ℓ_node² scale, and physically self-inconsistent under the row's own regime declaration.** *(Bin-vocabulary note for the record: the frozen §3-C2 cutoff clause arguably names this UNVALUED-BRACKET — cutoff-dominated with `r_c` an ENG-CHOICE; either binning leaves the verdict unchanged, and the vocabulary question is routed, not ruled.)* No DM mechanism is adjudicated; the row is read, not touched.

## §3 — C3: the κ chain → COHERENT at f = 7 under the canon-declared convention

**The canon-declared convention (the Tier-2's falsifying evidence, now the receipted premise):** `gordon-optical-metric.md:25` (clm-rd9cjm) declares `−(c⁴/7G)∇²ε₁₁(r) = 4πMc²δ³(r)`; `:28` names the Green's function `−1/4πr`; `:33` derives `ε₁₁ = 7GM/c²r`. Sympy: exact, zero residual; `f_chain = 7` ⇒ **f_chain = f_profile = 7; the consumers AGREE** (`consumers_agree_under_canon_convention: true`). The independent GR-route cross-check (panel): `ε₁₁ = −7Φ/c²` with `∇²Φ = 4πGρ` gives `−(c⁴/7G)∇²ε₁₁ = 4π·T₀₀` — the 4π belongs on the source side, exactly as canon writes it.

**Preserved arithmetic (first cut, still true as arithmetic):** under a PLAIN-density reading of the same operator, `f = 7/(4π) = 0.55704`; the two READINGS differ by exactly 4π. That reading is NOT canon's — it is what five downstream restatement sites *appear* to say when read without their upstream (the survivor finding, §7.2).

**Lattice receipts, at their honest scope (deviation #1 re-cut):** negative control first (7-pt Laplacian: fitted `b/[M/(4π)] = 1.0011/0.9980`, R² `0.999/0.990`); the native operator's divergence-form residual identities are exact (point `1.0` machine; blob `1.0000034`) — residual checks only, normalization-blind by construction; the **blob masked fit `0.951` (R² `0.881`, window band `0.74–0.99`)** is the sole native GF-amplitude measurement — consistent with the bare-GF normalization at its precision and cleanly separating 1× from 16×; the point-source masked fit `15.5–15.7×` = the 16-class sublattice diagnostic (`|det| = 16`; `connected_components = 16` at N ≡ 0 mod 4; interior zero-fraction `0.93752 ≈ 15/16`), corroborating the corpus's banked exposure note on this stencil (`master-equation.md:112`). Clause S defines `B` by TOTAL enclosed flux (`∮u·n̂ = 4πB`), which is insensitive to the sublattice anisotropy — the ×16 does not infect `B`.

**C3 output: COHERENT at f = 7.** Standalone it values nothing (the chain pins 𝒜_g only jointly with an absolute `B` — §3-C3's frozen sub-case rule); its over-determination content is the coefficient agreement, now receipted.

## §4 — Verdict assembly (mechanical, frozen §4, repaired instrument)

| Consumer | Output | Bin |
|---|---|---|
| C1 dress normalization | coherence only; no absolute anchor exists | UNVALUED |
| C2 halo added-mass | `𝒜_g ∈ [3.7e32, 1.2e33] m²`; `c ∈ [2.5e57, 7.9e57]`; self-inconsistent (solar strain `2.3e10`) | VALUED-BRACKET, not ℓ_node²-scale |
| C3 κ chain | `f_chain = 7 = f_profile` under the canon-declared convention | COHERENT (agrees; values nothing standalone) |

Rule 1 (reconciliation exhibited) → rule 3 dead (both clauses) → rule 2 blocks DERIVED(c) (C1 UNVALUED; and no route lands at pure-number × ℓ_node²) → rule 4: **CONSISTENT-UNVALUED**, sub-cases as §1–§3. *(First-cut §4 assembly preserved in `cf576d3f`; its rule-3 firing rode the falsified absence receipt — the rule's antecedent "after explicit convention reconciliation" was never satisfied by the blind instrument.)*

## §5 — Consistency-vs-emergence ledger

| Quantity | FORM | VALUE | Class |
|---|---|---|---|
| C3 coefficient agreement (f = 7 = 7) | derived (sympy + canon-declared convention, clm-rd9cjm) | — | consistency (coherence receipt) |
| C1 UNVALUED | sweep + per-site adjudication + asserted zero-counts | — | consistency (absence receipt, Tier-2-hardened) |
| C2 bracket + exhibits | exact integral, derived; χ=1 inversion closes exactly | anchors ride ENG-CHOICE galactic params + imported G | exhibit (not a mechanism claim; no DM content minted) |
| 16-class sublattice diagnostic | measured + derived (index/components/FFT, three methods) | — | instrument receipt (quantifies the banked `master-equation.md:112` exposure note) |
| First-cut 4π "seam" | RETRACTED as canon-vs-canon; survives as downstream notation drift (§7.2) | — | routed repair inventory |

**No chord on any outcome** (prereg row 8): internal bookkeeping throughout; the GR-counterfactual now reads honestly — AVE's canonical derivation home writes its 4π exactly as Newton–Poisson does; AVE and GR sit in the SAME declared-convention state.

## §6 — Frozen gate table (re-scored at the Tier-2; first-cut scores preserved in `cf576d3f`)

| Gate | Frozen criterion | Tier-2 re-score |
|---|---|---|
| **G-QUOTE** | every ruled/frozen quote byte-checks, two engines | **FAIL-AS-FROZEN → repaired**: two cite defects found (`master-equation.md:113`→**`:112`** — the quoted phrase is verbatim, the line number was off-by-one, inherited from a corpus cite; `screening-theorem :68`→**`:19`** for the `qₐ ∝ mₐ` token). All quotes re-verified at the corrected lines |
| **G-C1** | two-engine sweep; per-site adjudication; §3-C1 binning | **PASS-WITH-REPAIRS**: counts corrected (12 + 182; the first-cut §6 row carried stale 14 + 184); absence now ASSERTED in the number check (was engine-agreement-only, vacuous per the receipts lens) |
| **G-C2** | exact sympy + numeric ≤1e-8; cutoff exhibited; inversion vs declared bracket | **PASS** (two Tier-2 lenses re-derived both routes; χ=1 closes exactly) |
| **G-C3** | control BEFORE native; point + blob; convention factors exhibited; f measured with fit receipts (R² ≥ 0.99 point-source class) at two exterior radii | **FAIL-AS-FROZEN → repaired**: the convention-reconciliation arm ran on a structurally blind instrument (no liveness control; LaTeX-blind patterns; claim-id trail unwalked; single-engine attested as two) — repaired sweep FINDS the declaration; the flux-substitution left f unmeasured on the lattice (deviation #6) — the point-source-class fits DO meet the frozen R² wording (0.996/0.995), the blob fit's R² `0.881` is outside the first cut's own gate-cell gloss ("fits R² ≥ 0.99" — a paraphrase that dropped the frozen "point-source class" scope) and is now stated at its honest precision |
| **G-VERDICT** | §4 mapping mechanical; on DISAGREE the lane STOPS | **re-scored**: the first-cut firing rode an unmet antecedent (reconciliation never performed by the blind instrument); with the repaired instrument the mapping lands **CONSISTENT-UNVALUED** mechanically; the first-cut STOP itself was executed faithfully (panel: order-of-operations airtight; rule 3 frozen pre-number; not manufactured) — except the branch-(c) elimination, which was post-STOP derivation (retracted as adjudication, preserved as receipt with the (9/7)-projection caveat: `vol3/claim-quality.md:45` contests the temporal-only deflection chain the first cut cited as a clean observational pin) |
| **G-ENGINE-FLAG** | `git diff --stat 8424995f..HEAD -- src/` empty | **PASS** (verified independently by two lenses) |
| **G-NUM** | two runs byte-identical; number-check green; mutation receipt fires; `make verify` green | **PASS (re-run)**: `f8a31e9e…` ×2; 34 checks green incl. the new absence/coherence/R²/converged assertions; mutation fires; verify at commit |

## §7 — Routing (final, re-cut)

1. **Verdict → CONSISTENT-UNVALUED ⇒ per R46 the interlock/count adjudication FIRES:** 𝒜_g enters the interlock register with the count adjudication (3→4 or new edge class) **as a Grant ruling** (R47 item 3 unblocks on that ruling; the register execution is the DOC LANE's, never this lane's edits). Grant sub-question routed with it: C2 is simultaneously the only VALUED consumer and self-inconsistent under its own row's regime declaration — does a self-inconsistent bracket count as *"𝒜_g is real, one value"* for the interlock entry, or does the entry carry UNVALUED with the bracket as exhibit?
2. **The survivor finding (downstream notation drift, flag-don't-fix; repair inventory for the doc lane):** five-plus sites restate the elliptic law with a bare `= T₀₀` where their canonical upstream (clm-rd9cjm) writes `= 4πMc²δ³`: `saturating-modulus-and-backreaction.md:42,:50` (whose `:43-44` cite also names a file — `gravitational-refractive-index-gradient.md` — that carries neither formula; repair the cite to the gordon leaf / claim-id), `vol3/claim-quality.md:1254`, `gw_propagation.py:364,:368,:456` (docstrings), `backreaction.py:12` (docstring), `2026-06-29_grqed-stage1-gr-extension_result.md` sites (frozen doc — dated surface-note, never a rewrite). **Plus the R43-ratified clause-G text itself** (`2026-08-10_bound-constitutive_result.md:120` writes `= T₀₀`): Grant question — typo-or-redefinition in ratified language (one symbol, two spellings in canon).
3. **Engine units-bridge gap (routed, distinct from the notation drift):** `backreaction.py` runs dimensionless-lattice with ratio/shape gates (κ-scale-invariant by its own declaration) and contains no 4π in its source normalization; nothing bridges its lattice `T₀₀` to the canon 4π-carrying SI source. Latent, never exercised by any certified gate; flagged for the engine lane.
4. **Dimensional-label question (panel find, routed):** `κ = c⁴/7G ≈ 1.73e43` *(corrected 2026-08-11 at Tier-2 review — the first print carried `c⁴/G = 1.21e44`, a 7× slip; machine check: `c⁴/(7G) = 1.7289e43` from `ave.core.constants`; the numeral propagated nowhere — `git grep -F "1.21e44"` on `origin/main` returns nothing — and the item's units argument is unaffected)* carries units of FORCE (as `−κ∇²ε = T₀₀ [J/m³]` with dimensionless ε requires), yet `backreaction.py:94` labels it `[Pa]` and canon calls it the *"Machian stress boundary"* — is `T_max,g` a max-force boundary (GR's `c⁴/4G` class) with a label slip, or is a per-area object intended upstream?
5. **Minor cite repairs (doc lane):** `master-equation` line-drift (`:113`→`:112`, same drift in `b-mechanism-derivation_result.md:98`); `02_general_relativity_and_gravity.tex:200` + `double-deflection.md:42` attribute the profile to an equation that carries `G = ħc/(7ξm_e²)` instead; `closure-roadmap.md:82` cite-range drift.
6. **Tier-2: RUN (six lenses, ★§9)**; presentation state `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. The R46 derive-first gate is DISCHARGED by this lane's adjudication (hypothesis not-supported; bin CONSISTENT-UNVALUED); no re-run is owed unless Grant's interlock ruling changes the consumer set.

---

## §9 — ★TIER-2 REVIEW-REPAIR ADDENDUM (2026-08-10 — this doc RE-CUT per Rule-12 repair-and-bank; the frozen prereg is BYTE-UNTOUCHED; the first-cut doc is preserved at `cf576d3f`)

Six read-only adversarial lenses, launched pre-presentation against the first-cut verdict: two reconcilers (KB chain / tex+eq files), algebra knife, frozen-criteria fidelity, symmetric-standard + seductive-narrative, receipts-and-numbers. **Disposition: the verdict-bearing finding was REFUTED by four lenses independently (both reconcilers, symmetric-standard lens A, receipts) and the algebra knife broke its lattice receipt; the frozen-criteria lens CONFIRMED the firing was procedurally faithful (order-of-operations airtight; rule 3 frozen 25 minutes before any number; not manufactured) — the instrument, not the procedure, was the defect.**

- **The falsifying evidence** (four independent routes to it): `gordon-optical-metric.md:25/:28/:33` + `03_macroscopic_relativity.tex:38-46` (+ `2026-07-11_C13b_bullet_cluster_result.md:81` quoting it) declare the 4π source and derive the profile. The first cut's "zero canon sites" was a structural regex false-negative (LaTeX-blind `[πp]`; `T00`-token requirement; ordering assumption) with no liveness control, single-engine despite the doc's attestation, and the clm-rd9cjm claim-id trail unwalked — the reconciling leaf was even inside the arc's own previous-day citation set (`2026-08-09_bound-response_result.md:56` cites `gordon-optical-metric.md:20-33`).
- **Algebra knife (finding 3′, the panel's own catch on the repair):** the substituted flux-identity receipt is normalization-blind — κ-rescale probe passes it under a deliberate 4π error; the 7-pt negative control, run on the flux receipt, kills it. Repairs: receipt reclassified residual-only; blob masked fit restored as the sole native amplitude measurement at honest precision; deviation #6 added. The 16-class count settled three ways (the committed driver comment said 8 — corrected; N ≡ 2 mod 4 halving caveat banked).
- **Frozen-criteria lens:** verbatim-fidelity PASS on every load-bearing span (whitespace-normalized); freeze-by-push receipts airtight; DRIFT-1 (branch-(c) elimination = post-STOP derivation) and DRIFT-2 ("implicitly adopted" = un-receipted intent attribution) — both retracted as adjudications above; deviations list completed (#5, #6, direction-of-effect on #2/#3, #4 descriptor).
- **Symmetric-standard lens:** LENS A — the firing was asymmetric-unfair (AVE's canonical home writes its 4π exactly as Newton–Poisson writes its; same declared-convention state); LENS B — the κ-is-imported deflation is itself REFUTED (`c⁴/7G` = `T_max,g`, load-bearing 8+ sites, ξ-chain-tied; branch (b) was never a one-line repair); the (9/7)-projection caveat attached to the deflection-pin sentence.
- **Receipts lens:** every driver-sourced numeral in the first cut verified against the JSON; determinism independently reproduced (third run byte-identical); the number-check coverage gaps (unasserted C1 absence; ungated blob R²; undisclosed `converged: false`; printed-vs-file sha) — all repaired in the post-Tier-2 instrument (34 checks).
- **Timeline disclosure:** the instrument repair was applied mid-panel (after the two reconcilers reported); the algebra/receipts/symmetric lenses observed the uncommitted working-tree repair and pinned their audits to the committed HEAD `cf576d3f` — their findings were derived independently of the repair, and each flagged the concurrent-write event per the mid-flight rule. Sequencing honored: no doc re-cut was committed until all six lenses reported.

**Net effect: `OVER-DETERMINATION-FAILS` → RETRACTED (instrument artifact); re-scored under the SAME frozen grammar → `CONSISTENT-UNVALUED`; the axiom's falsifier did NOT fire; the survivors are the §7 routed repairs (notation-drift inventory, ratified-text spelling question, engine units-bridge, dimensional label, cite repairs) and the interlock/count adjudication now owed to Grant per R46. Nothing in this document reads CLEARED.**

---

> **Result-doc provenance.** Frozen prereg committed ALONE + pushed at `ee728d89` before any derivation content; byte-untouched. First-cut driver + receipts + doc at `bbe57bc8`/`cf576d3f` (the doc's first version rode into `bbe57bc8` with the driver — stale staging from two hook-blocked commit attempts, disclosed); post-Tier-2 instrument + this re-cut committed after all six lenses reported. Two-method receipts as scored: bridge/chain algebra = sympy exact + independent float (+ two Tier-2 lenses re-derived); C2 integral = sympy exact + finite-difference/quadrature (`1.05e-12`) (+ two lenses re-derived, two routes each); C3 = negative-control-first lattice runs with residual identities + the blob masked fit at honest precision + the canon-declared convention (clm-rd9cjm); sweeps = BSD grep + Python `re` with liveness controls and asserted zero-counts. Engine `src/ave` byte-untouched. Constants via `ave.core.constants` only. Past-wall out of scope (the C2 solar exhibit REPORTS a past-wall contradiction; it does not derive there). `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`. Companions: the frozen prereg, the brief, R43/R44/R45–R47 docket entries, `2026-08-10_bound-constitutive_result.md` (§2.6 BC-SRC), the port register, `gordon-optical-metric.md` (clm-rd9cjm), `saturating-modulus-and-backreaction.md`, `backreaction.py` at HEAD.

---

## §10 — DATED DISPOSITION NOTE (2026-08-11, post-Tier-2) — reconciliation to landed rulings R48/R49/R50

**What this is.** An independent Tier-2 review of PR #946 at tip `d08d482d` returned **CLEARED-after-disposition-note**, conditional on two items riding the PR before merge, neither requiring a re-run: (i) this note, and (ii) the `§7:104` κ numeral correction (both landed 2026-08-11). The lane record above predates rulings **R48/R49** and **R50**, all three of which landed on `origin/main` *after* this branch was cut — so their files are **not present in this tree** and are cited here as `origin/main` pointers, not intra-repo links: `_orchestration/docket-entries/2026-08-10-rulings-r48-r49.md`, `_orchestration/docket-entries/2026-08-10-ruling-r50-vocab.md`, with the execution landed at `manuscript/common_equations/eq_axiom_5.tex:64` (`-\nabla\cdot[\kappa D(A)\nabla\varepsilon_{11}] = 4\pi T_{00}`) and `:84` (`UNVALUED-RATIFIED-CONSTANT` / *"The calibration count stays 3"*). Per A47 v11b a record that predates a ruling is not falsified by it, and per Rule 12 + the vacated-cite pattern **frozen text gets a dated surface-note, never a rewrite** — so nothing above §10 is rewritten. **No bin moves: the verdict stays `CONSISTENT-UNVALUED`.**

### D1–D8 — the ruling-reconciliation sites

| # | Site (this doc) | Landed ruling | Disposition |
|---|---|---|---|
| **D1** | `:101` §7 item 1 — *"𝒜_g enters the interlock register with the count adjudication (3→4 or new edge class) as a Grant ruling"* | **R48** | **ANSWERED** — R48 ruled: **new edge class `UNVALUED-RATIFIED-CONSTANT`; the calibration count STAYS 3** (moves only when a *sound* consumer produces a value). Executed at `eq_axiom_5.tex:84` |
| **D2** | `:101` §7 item 1, trailing sub-question — *"does a self-inconsistent bracket count as "𝒜_g is real, one value" for the interlock entry, or does the entry carry UNVALUED with the bracket as exhibit?"* | **R48** (`rulings-r48-r49.md:10-12`) | **ANSWERED — the second branch**: the entry is UNVALUED; the C2 bracket (3.7e32–1.2e33 m²) is carried **as an EXHIBIT** of that consumer's own inconsistency |
| **D3** | `:102` §7 item 2, final clause — *"Grant question — typo-or-redefinition in ratified language (one symbol, two spellings in canon)"* | **R49(a)** | **ANSWERED — closed, not open**: **TYPO-CLASS**, corrected by a **dated fragment (R31-style)**; the ratification never exhibited the convention question |
| **D4** | `:102` §7 item 2, the five-plus `= T₀₀` drift inventory | **R49(a)** | **CONSUMED — DISCHARGED-BY-R49(a)**: the ruling executed *from* this §7 inventory (*"the five-plus downstream drift sites repaired from the #946 §7 inventory"*); the Tier-2 re-verified **7 of the named sites** rather than trusting them |
| **D5** | `:101` §7 item 1 — *"R47 item 3 unblocks on that ruling"* | **R48 landed** | **ANSWERED** — R47 item 3 is unblocked |
| **D6** | vocabulary — **"dress"** at `:17, :25, :37, :41, :45, :53, :69` | **R50** | **RETIRED-VOCAB** — *"'dress' is RETIRED … The canonical noun … is **the bound response**"* (back-reaction = the mechanism gloss). Read every "dress" above as the bound response; frozen record NOT rewritten |
| **D7** | vocabulary — **"grade"** at `:17, :25` | **R50** | **RETIRED-VOCAB** — *"The field ε₁₁'s canonical name is **THE BIAS**; 'grade' demotes to informal use"* |
| **D8** | vocabulary — **"halo"** at `:43, :49, :53, :70` | **R50** | **RETIRED-VOCAB** — *"'halo' survives only as the astronomical referent"*; where the physics is meant (`:43`, `:70`) the native term is the **near-field store / added-mass**. `:49` (*"the halo IS the phenomenological DM mass"*) names the astronomical referent and is arguably already compliant |

*(Quoting convention for the table above, declared per WARN 7c below: the quoted spans are **markup-normalized** — `**` emphasis markers stripped, multi-line source lines joined, elisions marked `…`. Each traces to the cited file:line under `git grep -F` on the normalized text.)*

**Nil-returns (grep-confirmed at the Tier-2, re-confirmed 2026-08-11 on this file):** the result doc contains **zero** occurrences of `λ`, of `ξ_g`, and of *"retardation"* — no R46-retired symbol and no R49(b)-retired word to reconcile. And **nowhere does this doc state the 4π question as OPEN**: `:19` marks the first cut's three-branch convention fork **VACATED**, and `:57` states the canon-declared convention as the receipted premise.

### The numeral correction (Tier-2 §7a, FAIL-class, landed)

`§7:104` shipped `κ = c⁴/7G ≈ 1.21e44`, which is `c⁴/G` — a **7× slip**. Corrected in place 2026-08-11 to `≈ 1.73e43` with a dated inline parenthetical (machine check from `ave.core.constants`: `c⁴/(7G) = 1.7289365204831522e+43` vs `c⁴/G = 1.2102555643382063e+44`). **Containment verified on two engines** (`git grep -n -F` and a Python `str.find` pass over all 5165 tracked paths): `1.21e44` occurs **nowhere on `origin/main`**; it is in no JSON field, asserted by none of the 34 checks, and R48/R49 and `eq_axiom_5.tex` carry no κ numeral. The routed item's **units argument is unaffected** — `backreaction.py:94` does carry the cited `[Pa]` label.

### WARN carries (non-blocking; carried, not fixed)

**WARN 5b — the C2 exhibit's threshold object (carried + ROUTED TO GRANT).** The exhibit at `:53` compares `2B/r³` — a gradient of the **bound response** (`∂u`) — against *"the yield strain"*, but canon defines yield on the **bias**: [`saturating-modulus-and-backreaction.md:51`](../manuscript/ave-kb/vol3/gravity/ch02-general-relativity/saturating-modulus-and-backreaction.md) reads `A = ε₁₁/ε_yield (ε_yield = 1)`. This lane's own `§0:25` freezes the two as TWO OBJECTS, and R48's walk note of record asserts the same distinctness (*"clause G is non-circular only if the grade ε₁₁ is a DISTINCT object from mechanical strain (∂u)"*) — so the exhibit borrows one object's threshold for the other. The *"yield ~ 1"* number appears in no JSON field and no gate. **The exhibit's conclusion survives** on the `∂u ≫ 1`-is-unphysical reading: a displacement gradient of `2.3×10¹⁰` is unphysical for any elastic medium with or without a named yield constant, and the verdict rests on the **57-OOM miss**, not on the exhibit. **ROUTED TO GRANT:** should the exhibit's threshold be stated as *"∂u ≫ 1, unphysical for any elastic displacement gradient"* rather than *"past the ε₁₁ yield"* — same conclusion, no cross-wire? This also touches R48's recorded phrasing (*"self-refuting regime: solar dress strain ten orders past yield"*). R48's operative content (new edge class, count stays 3) is **unaffected** either way. Per flag-don't-fix: surfaced, not resolved.

**WARN 7b — narrated numerals with no shipped source (carried; tagged, none verdict-bearing).** Every numeral in this doc was run against a flattened dump of the shipped JSON; beyond ordinary 2-sig-fig rounding (all of which reconcile), these have **no JSON field and no gate** and are hereby tagged **panel-derived-not-shipped**: the blob-fit *"window band `0.74–0.99` across shifts"* (`:28`, `:61`; the JSON has a single window, 5–10); *"FFT lattice-GF `b·4π = 15.75–15.85`"* (`:28`); *"`|det| = 16`"*, *"`connected_components(L) = 16`"*, *"halves to 8 at N ≡ 2 mod 4"* (`:28`); *"182 after noise screening"* (`:41`, `:92`) — hand-screened, no screening code shipped; and the corroborating LaTeX-aware C1 sweep quoted at `:41` **is not in the driver** (`run_c1`'s pattern list at `ag_derivation_lane.py:54-61` is unchanged from the first cut and remains LaTeX-blind). **The Tier-2 independently re-ran that un-instrumented C1 claim** with a Python-`re` LaTeX-aware sweep over `manuscript/ research/ src/ _orchestration/` (lane artifacts included): `4\s*\\,?\s*\\pi\s*\\,?\s*B` → **0 hits**; `B\s*=\s*[0-9].{0,40}(m\^?3|m³)` → **0 hits** — **C1's UNVALUED bin is independently corroborated**. Two further **prose-overstates** tags: `C3_convention_sweep.hit_counts["Green"] = 400` is a **truncation cap, not a count** (`hits[p] = sorted(...)[:400]` at `ag_derivation_lane.py:404`; the Python engine reports 616) — presence-only gate, non-load-bearing, but a shipped integer that does not mean what it says; and the driver docstring's *"a KNOWN-POSITIVE control line the pattern set MUST match"* (`ag_derivation_lane.py:378`) is satisfied by **1 of 4** patterns — the JSON exhibits this honestly (`liveness_known_positive_matched` is `false` for the `4π(G|T00|rho)` and `Green` patterns), the prose overstates it.

**WARN 7c — quoted spans are markup-normalized, not byte-exact (carried + ROUTED TO GRANT).** `prereg-FROZEN.md:80` freezes G-QUOTE as *"Every ruled/frozen quote above byte-checks against its cited file:line, two engines"*, and §9 reports *"verbatim-fidelity PASS on every load-bearing span (whitespace-normalized)"* — but four load-bearing spans resolve only under transformations beyond whitespace: `:13` (LaTeX→unicode transliteration of `gordon-optical-metric.md:28`), `prereg:50` (`$=$`→`=` plus `**`-stripping of `port-register.md:35`), `prereg:39` (`**`-stripping plus line-join of `2026-08-10-ruling-r43-ratification.md:39-40`), and `prereg:46` (partial transliteration of `vol3/claim-quality.md:1257`). **Every one traces to real, correctly-attributed source content**, each confirmed by the Tier-2 on two engines (`git grep -F` + Python `str.find`) — this is a disclosure gap in the §6 G-QUOTE re-score, not a substance defect (its two named line-number corrections, `master-equation.md:113`→`:112` and `screening-theorem :68`→`:19`, both re-verified correct). **ROUTED TO GRANT** as a convention question for **future preregs**: does G-QUOTE's frozen word *"byte-checks"* admit markup-normalization (LaTeX→unicode, `**`-stripping, `$=$`→`=`, line-joins), or is that a defect class the Tier-2 should have listed? The frozen prereg is **byte-untouched** either way (blob `3266b52cc5f871952c7583e93d7389eb7dd40731`).

### What this note does NOT do

It rewrites no frozen text, mints no `clm-`/`def-`/`exp-`/`sup-`/`ilk-`, edits no KB leaf, register, ledger or ruling, changes no bin or solidity, and re-derives nothing — **no lane re-run is owed**: no ruling changed a consumer, and the driver still reproduces bit-for-bit at this tip (re-run twice at this edit purely as a determinism receipt; its input files are untouched). Receipts re-taken at the Tier-2 and again at this edit are unchanged: `make verify` green, `make test` green, driver sha256 `f8a31e9e…` on two consecutive runs, 34 checks green with the mutation receipt firing, `git diff --stat 8424995f..HEAD -- src/` empty. The two questions above (5b threshold-object, 7c quoting convention) are **ROUTED TO GRANT, open**.
