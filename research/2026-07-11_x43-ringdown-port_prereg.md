# PRE-REGISTRATION — X43: the ringdown-port decay-rate FORM Γ(ω)

**★ FREEZE-BY-PUSH (P9) — BINDING.** This pre-registration is its own commit, **pushed to `origin/analysis/x43-ringdown-port` before any derivation content or code exists.** Git ordering IS the freeze proof: the push of this file precedes the first line of the derivation. A bin loosened, a comparator relaxed, or a decision rule rewritten after a derivation result is seen is a freeze violation and a review-CRITICAL finding.

**Arc:** the program's **first forward-form derivation attempt of the testing pivot** — the first time the turn tries to *derive a forward observable's FORM from the substrate* rather than adjudicate an existing claim. **Object:** the exponent `n` in `Γ ∝ ω^n` for an above-band overtone draining through the weak-leakage channel into the parity-gated neutrino drain. **FORM ONLY** — the absolute coupling (the `G_F`-analog VALUE) stays imported per the FORM/VALUE law; the exponent is the whole prize. Nothing here is canon; the satellite derives a FORM and reports it against the frozen bins below; Grant rules.

**Sources (verbatim-faithful, all on `origin/main`):** brief `_orchestration/2026-07-11_x43-ringdown-port-handoff.md`; primary framing `research/2026-07-11_ringdown-nyquist-pi-register_framing.md` §1; Appendix-A0 owner `research/2026-07-11_keying-register-walk_framing.md` §4; miss-ledger `research/2026-07-10_impedance-register-walks_framing.md`.

---

## SECTOR HEADER (declared before any standard-physics vocabulary)

- **MODE.** Spectral content vs the lattice band — **NOT** translation through it. The picture rides the *spectral* "too fast" (a muon **at rest** whose internal clock `ω_int = m c²/ℏ` sits above the band top); the *translational* Cherenkov/wake sense is explicitly NOT the picture (`ringdown-nyquist-pi-register_framing.md:15-17`).
- **SECTOR carrying the drain.** The **longitudinal / V-sector parity-gated channel** — the mode-gated neutrino load (the left-handed-only propagating mode of the chiral LC bandgap), NOT the transverse EM channel. Drain and leakage-port are keyed on the **same Cosserat couple-stress modulus `γ_c`**: the chiral dispersion `ω² = c²k² ∓ γ_c k` gates neutrino handedness (`chiral-screening.md:11`) and the leakage-inductance length is `l_c = √(γ_c/G_vac)` (`gauge-boson-masses.md:39`). One modulus, both roles — a *pointer*, not a discharged step.
- **REGIME / PHASE-STATE.** Cold lattice, single excitation (one soliton ringing, not a driven many-body bath). **Above-band evanescent, sub-yield** (`A < 1` per node, Axiom-3 lossless bulk — the drain is a boundary/port coupling, not bulk dissipation). Regime discipline: a "null" measured inside a regime where the drain cannot exist is an ARTIFACT, not a falsification.

---

## MISSION

Derive the decay-rate **FORM** `Γ(ω)` and report the exponent `n` in `Γ ∝ ω^n` against the frozen bins. `Γ` per cycle = (leak rate through the nearly-closed port) / (stored energy). The port coupling = (evanescent overlap of the above-band mode with the leakage channel) × (multipole order of the port) × (parity-gated V-sector drain-mode count at ω). The exponent must **EMERGE** from mode-/multipole-counting on the actual srs lattice; the absolute coupling is imported and tagged.

---

## ★ BINDING #1 — CORPUS SWEEP RESULT (re-run + extension of the plan-time sweep)

**VERDICT: PROCEED — no `Γ(ω)` decay-rate FORM exists anywhere in the corpus.** A 5-modality blind fan-out (formula-pattern, mechanism, drain-channel, mode-count/multipole, cross-repo+concurrent) + independent adversarial re-verification (6 agents, 112 tool-uses, `origin/main` + all 48 unpushed local-main commits + 9 sibling repos + incubators + the Applied-Vacuum-Engineering archive). The plan-time verdict is **CONFIRMED**: the only textual `Γ ∝ ω⁵ (Γ ∝ m⁵)` occurs in the framing note itself (the excluded pre-reg source, `ringdown-nyquist-pi-register_framing.md:27/30/32`), which explicitly states *"the port calculation is underived … not computed to give ω⁵"* (§1f:36) and *"No AVE-distinct number is claimed here — only the shape"* (:32). No **derived-or-asserted** AVE `Γ(ω)` rate form exists → HALT does NOT trigger; the derivation is licensed.

**Confirmed non-forms (the corpus's existing muon/lepton-decay content, none a rate exponent — cite as the canonical mechanism leaves, NOT as a rate FORM):**
- `vol4/simulation/ch14-leaky-cavity-particle-decay/theory.md:53` — the canonical "leaky cavity" decay leaf: half-life from **RC-discharge time constant / cavity Q-factor**. A lifetime, no `ω^n`.
- `vol3/gravity/ch01-gravity-yield/leaky-cavity-decay.md:26` and `vol4/falsification/ch11-experimental-bench-falsification/vacuum-impedance-mirror.md:127` — the **`Γ = −1` Zener-shatter mechanism NAME** ("mechanical origin of heavy particle lifetimes and weak decay"); `Γ` here is a boundary reflection coefficient, not a decay rate. **(These two leaves ARE Tension A below.)**
- `ch05-electroweak-mechanics/gauge-boson-masses.md:39` (`l_c = √(γ_c/G_vac)` = a LENGTH, Yukawa spatial evanescence), `:59` (`ω ∝ √(k/m)` = a MASS form); `ch03-neutrino-sector/chiral-screening.md:11` (`ω² = c²k² ∓ γ_c k` = a real-frequency DISPERSION). **The substrate leaves the derivation builds on carry NO inherited rate form → any exponent produced is genuinely new (nothing to install from them).**
- `LIVING_REFERENCE.md:396` — *"quantitative lifetime comes from the Fermi formula with AVE-derived G_F"* — the `Γ ∝ m⁵` form is REFERENCED as the **imported SM formula** (confirms the FORM/VALUE law: absolute coupling imported).
- Decoys correctly binned OUT: Kerr-QNM `ω_I` (`app-f:20`), `κ_quality(ρ_def)` detector quenching (`closure-roadmap:190`), QED Euler-Heisenberg `σ_EH ∝ (ω/m_ec²)^6` (`intermodulation-distortion:80`), impedance reflection `Γ ∝ (Z_p−Z_n)/(Z_p+Z_n)` (`02_full_derivation_chain.tex:1332`).

**★ EXTENSION — the direct methodological precedent (was NOT in the plan-time 3-item sweep):** `research/2026-07-09_superband-carrier-fork_result.md` (task #29, χ³/four-wave band-edge). This is the corpus's **closest prior attempt at an above-band leak/coupling power-law exponent**: a candidate coupling law (`p ≈ 8.29`) was **RETRACTED** as a ramp turn-on transient and the whole coupling-law leg **DROPPED as structurally unmeasurable by a single-tone driver** (verdict: **NULL-mobility banked**; two-tone = a queued FORK-A). Method differs (X43 makes `n` EMERGE from mode/multipole counting, not a driver measurement), but **the null is a live warning that the no-law bin is reachable** — an above-band leak may not present a clean power law at all. Cited as anti-install related-work; the associated `superband_carrier_fork.py:329` `κ = arccosh(ω²/2−1)` is a **spatial** skin constant (a decoy for a temporal rate), noted so the derivation does not mis-bin it.

---

## ★ TWO FLAG-DON'T-FIX TENSIONS — SURFACED, NOT RESOLVED (reconcile with Grant/corpus in-lane)

### Tension A — the Γ=−1 Zener-shatter drain vs the nearly-closed evanescent port
**Two** corpus leaves book muon decay as the **"Leaky Cavity" mechanism = a Zener-avalanche `Γ = −1` dielectric shatter**: `vacuum-impedance-mirror.md:127`, verbatim, *"If a macroscopic volume is statically pushed past `V_yield` using a rapid impulse, the inductive capacity of the LC network physically shatters (`Γ = −1`) … absolute dielectric breakdown, completely dropping its topological grip on matter. This is the exact mechanism that causes heavy particles (like the Muon) to decay"*; and `vol3/gravity/ch01-gravity-yield/leaky-cavity-decay.md:26` (`Γ = −1` topological mirror shatters at yield → *"mechanical origin of heavy particle lifetimes and weak decay"*). A third leaf, `ch14-leaky-cavity-particle-decay/theory.md:53`, instead derives the lifetime from an **RC-discharge time constant / cavity Q** — a nearly-closed-port-flavoured reading, closer to the framing note.

The framing note's Q arithmetic **contradicts a bare shatter as the RATE-setter**: `Q_μ = ω_μ·τ_μ ≈ 3.5×10¹⁷`, `Q_τ ≈ 7.8×10¹¹`. A bare `Γ = −1` mismatch at order-unity coupling dumps its energy in `~1/ω ≈ 10⁻²³ s` — 40+ OOM faster than the muon lives. So the RATE cannot be a bare shatter; it must be a **nearly-closed port** (tiny coupling leaking a huge stored energy slowly).

**Candidate reconciliation (FLAGGED, not asserted):** the two describe *different parts* of one process — the **nearly-closed port sets the slow per-unit-time hazard `Γ(ω)`** (governs the exponential law and `Q ~ 10¹⁷`), while the **`Γ = −1` shatter, if real, is the CHARACTER of the terminal jump** (the topological-grip-drop that *happens when* the low-probability hazard fires), NOT the rate-determining step. Compatible under this reading. **Competing reading:** the leaf's Zener line describes a *macroscopic volume externally impulsed past `V_yield`* — a bulk-breakdown scenario that may simply **not apply** to a muon at rest whose clock is *internally* above-band; the leaf is already heavily retired-headline-hedged. **Load-bearing consequence held for the derivation:** the RATE derivation uses the nearly-closed-port hazard (Q arithmetic forces it); the shatter, if real, does not enter the RATE *scaling*. **Grant adjudicates: terminal-jump-character (compatible) vs superseded/inapplicable (walk-back). Not resolved in-lane.**

### Tension B — the naive `2ω_C` band edge vs the canonical srs band tops
The framing note reasons with a **naive 1D edge** `ω_max = 2c/ℓ_node = 2 ω_C` (`ringdown-nyquist-pi-register_framing.md:17`). The **canonical srs band structure** is richer: **scalar top `π√3 ω_C ≈ 5.44 ω_C`**, **vector/Cosserat top BRACKET `[5.441, 17.011] ω_C`**, NO internal gap in either channel (`srs-band-structure.md:35,86`).

**Consequences carried (all re-verified):**
- The **"103× above band edge"** figure (`206.77/2`) is a **naive-edge artifact**. Against the canonical tops the muon is `206.77/17.011 ≈ 12×` (vector) to `206.77/5.44 ≈ 38×` (scalar) above. The **above-band premise SURVIVES** under every reading — but the number is band-model-dependent, so **"103×" is NOT headlined**.
- The **electron-stability anchor SURVIVES** (electron at `ω_C` is below every band top → in-band).
- The corpus explicitly carries the **"above-band mobile breather"** (freq > the true top, in the semi-infinite gap) as a live carrier (`srs-band-structure.md:136-140`) — the muon overtone is corpus-grounded, not invented.
- **The drain-mode count MUST use the canonical srs DOS in the CORRECT channel** — the parity-gated V-sector = the **vector/Cosserat channel**, which has **3 acoustic branches (linear near k=0) → Debye DOS `ρ(ω) ∝ ω²` below its band top** (`srs-vector-band-survey_result.md:141,165`), NOT a naive 1D `ω²` continuum stand-in. Whether the drain sees the scalar or vector channel, and whether the parent's above-band frequency (`>>` the drain top) permits single-quantum emission at all, is **load-bearing for the exponent (trap 1)**.

---

## ★ FROZEN BINS (P9)

| Derivation result | Bin | Classification |
|---|---|---|
| **`ω⁵`** | banks the ringdown form (Sargent-matching) | **FORM-derivation, forward-prediction class IFF** the anti-install audit shows **no Sargent-adjacent input anywhere in the chain** — the exponent EMERGES from mode/multipole counting, every step auditable. Any Sargent/SM-phase-space value inserted → drops to **consistency-class** (installed answer). |
| **`ω³`** | dipole-class port | The bullet/overtone picture AS STATED **dies**; banked negative (miss-ledger candidate). |
| **`ω¹`** (or other integer) | bare-mismatch / monopole class | Picture **dies**; banked negative. |
| **no well-defined power law** | wrong model | The leakage-port circuit is the wrong model; **HALT and surface, do not force** a power law onto a non-power-law result. |

---

## ★ P10 — GENUINELY FIREABLE BINS (binding) + THE HONEST POWER BOOKKEEPING

**The exponent decomposes (single-quantum multipole emission, standard radiation theory):**
`Γ_Eℓ ∝ ω^(2ℓ+1)`, decomposing as `n = ?_multipole-matrix-element (= 2ℓ−1) + ?_drain-DOS (= 2 for a 3D acoustic/Debye channel)`, where **`ℓ` is the lowest radiating multipole order the parity-gated port's lattice symmetry ALLOWS** and the **`+2` is the honest srs vector-channel acoustic DOS `ρ ∝ ω²`, NOT an SM three-body phase-space factor** (trap 1). Additional powers from the evanescent overlap `?_overlap` are counted separately and must not smuggle a Sargent shape.

**Each summand is independently derived and auditable; none may be a Sargent/SM-phase-space value in disguise.**

**What fires each bin — the decision rule (states the mapping, NOT the answer; `ℓ` is what the derivation determines):**
- **`ω⁵`** fires iff the port's leading allowed multipole is **quadrupole-class (`ℓ = 2`)** — i.e. the chiral/parity-selecting port **FORBIDS the dipole (`ℓ = 1`)** and the quadrupole is the leading survivor — AND the drain-DOS contributes exactly `+2` from an **honest count of srs vector-channel acoustic modes at ω** (NOT from importing three-body phase space), AND the overlap contributes `0`. `n = (2·2−1) + 2 = 5`.
- **`ω³`** fires iff the leading allowed multipole is **dipole (`ℓ = 1`)** — the lowest radiating multipole the lattice symmetry permits — with the honest `+2` acoustic DOS. `n = (2·1−1) + 2 = 3`. **If the symmetry allows the dipole, the honest answer is `ω³` and the picture dies — that is a clean negative; take it.**
- **`ω¹`** fires iff the coupling is a **bare above-band mismatch / monopole (`ℓ = 0`)** with no evanescent-port multipole suppression. `n = (2·0−1) + 2 = 1`.
- **no-law** fires iff `Γ` is **not a clean power of ω** over the muon→tau range: e.g. (a) the above-band parent (`ω_μ ≈ 12–38×` the drain band top) forbids single-quantum emission and the drain proceeds by **evanescent-tail coupling → exponential suppression**, not a power; (b) a **band-edge / van-Hove / band-structure-resonant feature** dominates; or (c) a **multi-quantum drain** whose quantum-number `N` is not lattice-forced (in which case reproducing `ω⁵` via an `N`-body count is trap-1 install, not emergence).

**Anti-install commitment (P10 void-check):** nothing in this setup installs `ω⁵`. The `ω³`/`ω¹`/no-law bins are reachable by an honest derivation — `ω³` fires if the parity-gating does not forbid the dipole (the expected-value outcome per the 0-for-7 ledger), and no-law fires if the above-band evanescence gives exponential rather than power-law suppression. The bins are **not decorative**.

---

## FROZEN COMPARATORS + BOUNDARY ANCHORS

**Sargent empirical check (the FORM target the mechanism must reproduce, or the picture dies honestly):**
- `(m_τ/m_μ)⁵ = 16.817⁵ ≈ 1.345×10⁶`;
- measured `Γ(τ→μνν)/Γ(μ→eνν) = (τ_μ/τ_τ)·BR(τ→μνν) ≈ 7.57×10⁶ × 0.174 ≈ 1.32×10⁶`; **agreement ~2%.** The exponent is the discriminator.

**★ BOUNDARY ANCHORS the model MUST respect (a model violating either is dead on arrival):**
1. **Electron stability (in-band → lossless).** Electron at `ω_C = c/ℓ_node` sits below **every** band top (naive `2ω_C`, canonical scalar `5.44ω_C`, canonical vector `17.011ω_C`) → in-band → a propagating lattice eigenmode with no above-band leakage port → `Γ(in-band) = 0` (or the port exponentially closed). **A model predicting electron decay is disqualified.**
2. **Absolute-scale marks:** `Q_μ ≈ 3.5×10¹⁷`, `Q_τ ≈ 7.8×10¹¹`. **Form-first honesty:** the *exponent* is the target and must EMERGE (FORM result); the *absolute* Q may legitimately require the imported `G_F`-analog coupling (a VALUE-import, identity/manifestation, NOT a chord). **Tag which is which** in the result.

---

## ★ TWO NAMED TRAPS (the review's anti-install lens checks these explicitly)

- **TRAP 1 — no SM three-body phase-space import.** In the SM, `m⁵ = coupling² × THREE-BODY final-state phase space` — two of the five powers are *final-state counting*. The lattice derivation must **count drain modes honestly** from the canonical srs vector-channel DOS (`ρ ∝ ω²` acoustic) and must **NOT** import `ρ(E) ∝ E⁵` or any Sargent-shaped final-state factor. Importing it installs the answer and demotes the result out of the forward-prediction class regardless of the number.
- **TRAP 2 — the multipole order `ℓ` is DERIVED, not asserted.** `ω³` = dipole; `ω⁵` = quadrupole. If the lattice symmetry (srs `I4₁32` chirality / `Δc_crit = 3` parity gating of the ν channel, `chiral-screening.md:11-20`) **FORCES** the port's multipole order — e.g. a chiral parity-selecting port forbids the dipole and the quadrupole is the leading survivor — **that forcing is the load-bearing step and MUST be derived.** "The port is quadrupole because we need `ω⁵`" is a disqualifying install. Chain: *lattice symmetry of the parity-gated port → lowest allowed radiating multipole → exponent.* If the symmetry allows the dipole, the honest answer is `ω³` and the picture dies.

---

## APPENDIX A0 — inherited-frozen criterion (severable, run FIRST, kill-on-failure expected)

The tide-branch **dimensional-`L` kill-test** (owner: `keying-register-walk_framing.md` §4 kill-test #1, already committed to `origin/main` — its criterion is **inherited-frozen**, not re-minted here). The §4 tide-EFE branch keys the Axiom-4 argument on the external **tide `∇g_ext`**; `∇g/a₀` is dimension-ful, so a tide argument needs `(∇g · L)/a₀` — a **forced length `L`**.

**Frozen binary criterion (from the source):**
- **no canon-chain-FORCED `L`** → the tide branch **dies at birth** → report dead to the docket (`collapse-target-registry.md` T4 / rulings docket).
- **a canon-chain-FORCED `L`** → write the tide-kernel FORM and flag it to the **astro-sweep A2 lane** (`2026-07-11_astro-adjudicator-sweep-handoff.md`).

**Method (frozen):** enumerate every candidate length in the canon chain (`ℓ_node`, weak `l_c = √(γ_c/G_vac)`, `ELL_C = √6 ℓ_node`, Bohr `a₀ = ℓ_node/α`, horizon `R_H = c/H_∞`, any Machian/de-Sitter length); for each, test canon-chain FORCING (does the chain force it, or is it picked by taste?) AND, for any forced `L`, whether it yields a dimensionally-consistent tide kernel with sensible galactic-scale phenomenology (a forced-but-phenomenologically-dead `L` does not save the branch). Horizon-class candidates are **checked against the Machian structure, not picked by taste** (source directive).

---

## DISCIPLINE STACK (embedded; carried into the result)

- **Freeze-by-push (P9):** this file pushed before any derivation content. Git ordering = freeze proof.
- **P10 fireable bins:** stated per bin above; anti-install void-check done.
- **P11 sabotage where code appears:** any driver must prove its gates FIRE by disabling the physics (null the port coupling → `Γ→0`; null the drain channel → no leak). A gate passing with the physics disabled is a checklist, not a gate. A null in a regime where the drain cannot exist is an ARTIFACT.
- **Substrate-native-first + sector header** (declared above) before any standard-physics word.
- **Consistency-vs-emergence tag in the result:** the exponent is **emergence-class** iff it emerged from mode/multipole counting with no Sargent/CODATA input; **consistency-class** if any target value was substituted. The absolute Q, if it needs the imported `G_F`-analog, is a VALUE-import.
- **ave-canonical-source for constants:** `m_e`, `m_μ`, `ℓ_node`, `ω_C` from `ave.core.constants` (`M_E`, `M_MUON`, `L_NODE`, `OMEGA_C`); `m_τ` is an external CODATA comparator (no canonical constant); the weak `l_c` and `γ_c` enter as FORM only (absolute coupling imported). **Disambiguation held:** `ELL_C = √6 ℓ_node ≈ 9.46×10⁻¹³ m` (lattice) ≠ weak `l_c = √(γ_c/G_vac) ≈ 10⁻¹⁸ m` (`constants.py:329-336`); Bohr `a₀` ≠ MOND `a₀`.
- **pre-test-physics-check — ALREADY GRANT-WALKED:** the plumber ontology is settled (spectral, not translational relaxation through a nearly-closed port). No new plumber question owed before design; if the derivation surfaces one, surface to Grant before concluding.
- **flag-don't-fix:** the two tensions above are surfaced, not resolved.
- **miss-ledger caveat:** this ringdown convergence is the **6th convergence-shaped move** of the register arc; the hopeful-interior-mechanism ledger stands at **0-for-7** (`impedance-register-walks_framing.md:7`). Expected-value outcome is a clean negative; take it if it comes (Rule 11 honest closure).
- **pure-corpus; self-isolated worktree; branch `analysis/x43-ringdown-port`; incremental commits; PR `[DO-NOT-MERGE][REVIEW: pending-orchestrator]`; adversarial review before CLEARED; only Grant merges.**
