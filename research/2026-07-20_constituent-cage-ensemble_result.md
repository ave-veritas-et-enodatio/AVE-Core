# Constituent-Cage-Ensemble — RESULT (the frozen-bin verdict for the open #767 fork)

**Date:** 2026-07-20
**Class:** DERIVATION + lattice-derived research-driver (research-doc; **forms derived, values calibration/observation-imported and tagged; mints no `clm-`, propagates to no KB/tex leaf**). Resolves the frozen bins of `research/2026-07-20_constituent-cage-ensemble_prereg-FROZEN.md`.
**Provenance:** Grant-fired 2026-07-20 (`"lets finish the ensemble walk and fire it"` `[sic]`). THE verdict-controlling adjudicator of the fork left OPEN by merged **#767** (`research/2026-07-20_envelope-sector-reduction_result.md` §3.1/§6.2-1, BIN-1-CONDITIONAL). Frozen prereg committed + pushed ALONE first (`b7a6ce46`); analytic Legs 0/6 + the asymmetry paradox in `..._derivation.md`; the six-leg driver `research/drivers/constituent_cage_ensemble.py` (+ `_results.json`, white figure), `ave.core.*` read-only, engine BYTE-UNTOUCHED.
**Lane fences:** DERIVATION lane only. Engine byte-untouched; **no** `manuscript/`/`ave-kb/` leaf edits; **no** port-register edit; **no** un-revert; **no** falsification-ledger edit — regardless of outcome (held). Every `[canon]` input content-verified two-method at base HEAD `5287ef32`.

> **★FROZEN-BIN VERDICT RE-BANKED (2026-07-20, PR#770 maximum-stakes review-repair — SUPERSEDES the original BIN-1-CLEAN box below, Rule 12): WALL-CLASS / RAIL-DEPTH-CONDITIONAL → REOPENED.** The shipped shallow-rail (`S_RAIL=0.03`, un-frozen) measurements land BIN-1-direction, but the load-bearing Leg-6 exclusion of BIN-2 is refuted (CONCLUSION-WRONG): the canon bulk-only wall IS realizable (review live-fire: `Γ_bulk→−1` with shear finite), and at deeper rail the review's own scan shows `ρ_N ≈ 0.3` and falling — the BIN-2 route is LIVE again at deep rail. The fork #767 left open REMAINS OPEN; the decisive adjudicator = the DEEP-RAIL frozen-first program (registered as the owed follow-on — §8.2 here, discharging #767 §6.2-1: frozen `S_RAIL` ladder to `1e-6`, converged Leg-1, deep-rail N-scaling, shear-gate diagnosis).
>
> **★Review-repair `S_RAIL` ladder (COMPUTED from the shipped `run_c2_speeds` + `leg5_ensemble_scaling`; this REPLACES the fabricated `ROBUST … pressure-tested` prose string — Findings 0/2/5/6/9; `review_repair_rail_depth_scan` in the reran JSON).**
>
> | `S_RAIL` | bulk-only `c_P` | `Γ_bulk` | bulk-only `c_S` | `Γ_shear` | bulk-only `ρ_N` (N=1→8) |
> |---|---|---|---|---|---|
> | `0.03` (shipped, **un-frozen**) | `0.167` | `−0.512` | `0.218` | `−0.136` | `0.485→0.523→0.572→0.583` (rising) |
> | `0.003` | `0.063` | `−0.784` | `0.183` | `−0.219` | `0.243→0.280→0.306→0.305` (plateau; **N4→N8 falls**) |
> | `1e-4` | `0.012` | `−0.956` | `0.177` | `−0.236` | `0.216→0.255→0.277→0.273` (plateau; **N4→N8 falls**) |
> | `1e-6` | `0.0012` | `−0.996` | `0.177` | `−0.236` | — |
> | `0` (canon wall) | `0.00007` | `−0.9997` | `0.177` (**FINITE**) | `−0.236` | — |
>
> ⇒ **`Γ_bulk → −1` with `c_S` FINITE (0.177 = 62% of cold) IS the canon bulk-only wall** (`electron-bh-isomorphism.md:26`, `Γ_bulk=−1`, shear un-melted) — constitutively REALIZABLE in the rank-2 bond model. The originally-shipped "`c_P` stays finite / `Γ_bulk` saturates `−0.5…−0.8` NOT `−1`" (derivation §3 Step 2) is INVERTED: the scan stopped one decade short at `S_RAIL=0.003`. And `ρ_N` does NOT rise toward 1 across rail depth — it plateaus `~0.3` (flat-to-falling `N4→N8`) at deeper rail. **Consequence:** the "compression can't be stopped without killing shear ⇒ BIN-2 forbidden" chain is DEAD; the "`K=2G` grade-lock at the constituent level" headline does NOT survive; the compression outcome depends on the un-derived wall depth ⇒ Fork-W is verdict-controlling again ⇒ REOPENED (do NOT flip to BIN-2 — the deep-rail data is review-produced, not frozen-criteria output). See §7.5 (disclosed deviations) for the full finding-by-finding disposition.

---

🔴 **SUPERSEDED (2026-07-20, PR#770 maximum-stakes adversarial review). The original `BIN-1-CLEAN` verdict box below is RETAINED VERBATIM per Rule 12 (preserve the body; do not delete or silently rewrite). Its load-bearing Leg-6 leg is CONCLUSION-WRONG (see the re-banked verdict + review-repair ladder above, and §7.5 disclosed deviations). Read the re-banked `WALL-CLASS/RAIL-DEPTH-CONDITIONAL → REOPENED` verdict as CURRENT; the box below is the falsified original, preserved for the audit trail.**

> **★FROZEN-BIN VERDICT [ORIGINAL — SUPERSEDED]: BIN 1 — ENSEMBLE-RADIATES (BIN-1-CLEAN). The constituent-cage fork CLOSES to route (ii): an ensemble of ~`10⁵⁷` bulk-only-caged constituent compression cores presents to the `2Ω` far field the COARSE-GRAINED uncaged mass texture, which RADIATES at the q1 partition — the #767 pulsar kill is confirmed one level deeper, at the constituent level. The image-cancelled-sum route (i) is NOT taken.** Mechanism NAMED. The frozen criteria's outputs:
>
> - **`κ_max² = δ_DP = 1.3×10⁻⁴`** (double-pulsar binding; §1, recomputed from banked q1 §2.2). Uncaged far-field baseline `κ_env² = 0.034` (#767; reproduced by this lane's spectral color-check).
> - **Leg 5 (verdict-controlling N-scaling): the bulk-only-caged ensemble RADIATES compression, `∝N`, tracking the uncaged texture.** `ρ_N` (caged/uncaged far-field compression) = `0.485 → 0.523 → 0.572 → 0.583` for `N = 1,2,4,8` — **RISING toward 1** (the coarse-grained texture emerges as cages pack). Scaling exponents `p_caged = 1.45 ≈ p_uncaged = 1.37` (caged tracks uncaged `∝N`, NOT sub-linear). **ROBUST across rail depth** (`S_RAIL 0.03→0.003` all RISING; single-core seal deepens `Γ_bulk −0.51→−0.78` but the aggregation erodes it regardless). `κ_ensemble²(N=8, bulk-only) = ρ_N·κ_env² ≈ 0.58×0.034 ≈ 0.020 ≫ κ_max²` (by `~150×`). SHEAR survives (`0.60×` uncaged; radiates ⇒ NOT BIN 3).
> - **Leg 3 (wall S-matrix): the bulk-only rail is channel-ASYMMETRIC but does NOT freeze compression.** Impedance S-matrix from railed Bloch speeds: symmetric `Γ_bulk = Γ_shear = −0.71` (channel-SYMMETRIC, BH melt wall, `c_P/c_S` FROZEN at `1.813` degree-0); bulk-only `Γ_bulk = −0.51`, `Γ_shear = −0.14` (channel-ASYMMETRIC `3.8×`) — but the bulk-only railed medium keeps **`c_P` FINITE (`0.17`, propped by `k_s`)**. ★STOP-gate: the CLAMPED control shows `Γ_disp = −1` (rigid/doubling), OPPOSITE sign to the rail cages' `+1` (pressure-release/cancellation) — image-doubling CONFIRMED (both P and S) ⇒ mirror realization VALID ⇒ lane proceeded.
> - **Leg 6 (homogenization, the `N→10⁵⁷` extrapolation): the `K=2G` grade-lock at the constituent level.** A space-filling (`φ→1`, nuclear-density) bulk-only-caged medium has FINITE `c_P,eff` because the srs bulk modulus is propped up by the shear springs `k_s` — you CANNOT open a compression-only stop; the coarse-grained mass quadrupole radiates into the finite-`c_P,eff` channel ⇒ BIN 1. The image-cancelled route (BIN 2) needs `c_P,eff→0`, which requires the SYMMETRIC rail — which also freezes `c_S,eff→0` (kills the observed GW) ⇒ BIN 3, forbidden by observation. This is #767's `K=2G` block, now surviving one level down.
> - **Leg 1 (charged-line) / Leg 2 (seal): a single bulk-only cage is only a PARTIAL compression seal.** Exterior DC `∇·u` (caged/uncaged) `= 0.65` (bulk-only) / `0.22` (symmetric); single-cage compression seal `~25%` (bulk-only) / `~85%` (symmetric). The bulk-only-caged core RETAINS most of its exterior compression charge (charged-line, BIN-1 shape); the empty cage sources nothing (Leg-2 control PASS).
> - **★CONSISTENCY GATE: PASS (both channels radiate ⇒ NOT BIN 3).** Under the LIVE bulk-only route, shear survives (`0.60×` uncaged) AND compression radiates (`κ²~0.020`); both are gapless propagating channels the quadrupole couples to. The SYMMETRIC wall suppresses compression more (`ρ_N~0.10`) but ALSO kills shear — EXPECTED wall-class data (the BH melt wall), FENCED from being read as a BIN-2 rescue or a BIN-3 flip.
>
> **★Honest residual (anti-seduction, the kill arc WANTED this verdict).** The lattice is at `k·r_core~O(1)`; the deep-quasistatic (`k·r_core→10⁻²⁵`) ISOLATED single-core Lloyd seal is NOT simulated. The BIN-1 verdict rests on the SPACE-FILLING homogenization (finite `c_P,eff`, `k_s`-propped — Leg 6), which is `k·r_core`-INDEPENDENT and lattice-validated (Leg 3 railed speeds) + the `ρ_N`-rising aggregation trend (Leg 5, robust). The dilute-Lloyd deep-quasistatic route is dismissed as the NON-PHYSICAL limit for space-filling constituents — an ARGUMENT (Leg 6 Step 1), not a lattice measurement. So this is BIN-1-CLEAN with a NAMED residual (the dilute deep-quasistatic single-core seal), NOT an absolute "no suppression can exist" — it is stronger than #767's BIN-1-CONDITIONAL (which had the whole constituent fork open) but does not headline beyond the frozen bins.
>
> **CONSEQUENCE (one level deeper than #767, on the adjudicated grounds): the standing Reading-A exclusion + the reverted Q1 ruling STAND; the constituent-cage route does NOT ground a Reading-B re-open.** A bankable NEGATIVE (Rule 11) for the BIN-2 route: the bulk-only-caged ensemble radiates compression above the kill line, and the `K=2G` grade-lock forbids the compression-only stop the BIN-2 route needed. **Routed to Grant/auditor; NOT executed here** (READ-ONLY fence held; no leaf touched). The Fork-W asymmetry paradox (which impedances collapse at a Cosserat-winding knot core) is surfaced, NOT picked.

---

## §0 — REGIME / SECTOR / PHASE-STATE header

**MODE.** Compact binary source (HT, J0737), but the object is the ENSEMBLE of ~`10⁵⁷` constituent solitons, each canonically carrying a bulk-only `Γ_bulk=−1` knot-core cage (`electron-bh-isomorphism.md:26` `[canon]`). **REGIME.** Regime-I cold-linear far field; caged sources realized by CONSTITUTIVE GRADING (no kinematic pin on the source path). **PHASE-STATE.** Cold-reactive far field, saturated cage shells (rail `S_RAIL=0.03`, `A≈A_yield`). **SECTOR.** Under test = A1 bulk/compression; observed GW = T2 shear; charge/spin = `(2,3)` Cosserat winding (`master-equation.md:20` `[canon]`) — A1⊥T2, NOT cross-wired. **A46:** longitudinal (`∇·u`, radial) vs transverse (`∇×u`, tangential) partition; `κ² = F_bulk/F_shear`.

---

## §1 — LEG 0 (convention freeze + sign check) `[canon-read + derived]`
Full in `..._derivation.md §1`. Impedance analogy stress↔voltage, velocity↔current; `Z_bulk→0 ⇒ Γ_bulk=−1 = SHORT = pressure-release` (`master-equation.md:107` `[canon]`; cite currency-bumped `:105→:107` post-#769 merge, Finding 12 — content byte-unchanged) ⇒ rail cage = inverted image (Lloyd cancellation candidate); clamped = `Γ=+1 = OPEN = rigid` ⇒ same-sign image (doubling). Sign check confirmed by Leg-3 impedance S-matrix (`dc_P/dA<0`; symmetric `c_P/c_S` frozen degree-0; bulk-only `c_P` finite ONLY at the shallow shipped `S_RAIL=0.03` — it drives to `0` (`Γ_bulk→−1`) at deep rail, review-repair §6/§7, Findings 0/5). **★Flag-don't-fix (verify-before-cite, base `5287ef32`):** the orchestrator-flagged `translation-circuit.md` contradiction (`Γ=−1 TIR → Open-circuit`) is NOT present — `:119` reads `Γ=−1 → SHORT`, consistent; the "open-circuit" there is the opposite (electric `τ_yield`, `Γ=+1`) branch. No contradiction routed.

---

## §2 — LEG 1 (charged-line) + LEG 2 (seal) `[derived]`
Single cage at box center, interior energized by a curl-free dilatation seed (initial displacement, zero velocity — FREE dynamics, NO pin), free-evolve, measure exterior `∇·u` at the far shell over the reflection-free window.
- **Leg 1 charged-line (exterior DC `∇·u`, caged/uncaged):** bulk-only `0.65`, symmetric `0.22`. 🔴 **Review-repair (Finding 3, Rule 12):** the 2-digit `0.65` is UNCONVERGED as a static charge fraction — the window-averaged ratio swings `0.328` (first half) → `1.600` (second half) of the frozen reflection-free window (a `~5×` transient-passage swing, not a relaxed DC tail), and is protocol-fragile in `r_meas` (`0.97` at `r_meas=5`). What survives every probe is the FROZEN BINARY discriminator only: exterior `∇·u` does NOT → 0 (min caged/uncaged ratio `0.33` at `S_RAIL=0.03`) ⇒ the line IS CHARGED (BIN-1 shape at the scanned depth). ~~The bulk-only-caged core RETAINS ~65% of its exterior compression charge `V`.~~ [RETRACTED as a converged charge-fraction — use the binary discriminator.] (`i = V·dC/dt`: `V≠0` ⇒ moving it pumps.)
- **Leg 2 seal (shell compression energy, caged/uncaged):** bulk-only single-cage seal `~25%`, symmetric `~85%`. The EMPTY cage sources NOTHING (control PASS — the cage itself is not a source; only the energized interior is). The bulk-only cage is a PARTIAL per-core seal (consistent with Leg-3 `Γ_bulk=−0.51`); the symmetric cage seals more (but also kills shear).
- **Scope:** `k·r_core~O(1)` on the lattice (NOT the deep-quasistatic `10⁻²⁵` of the real system; Leg 6 carries that extrapolation). A single clamped cage is a full enclosure that traps everything (exterior→0); image-DOUBLING shows only in the Leg-3 half-space geometry, not a full enclosure.

---

## §3 — LEG 3 (wall S-matrix, three classes + the clamped STOP-gate) `[derived]`
**Impedance S-matrix (clean, from railed-medium Bloch speeds — the primary channel characterization):**

| wall class | `Γ_bulk` | `Γ_shear` | `c_P/c_S` (railed) | reading |
|---|---|---|---|---|
| **symmetric** (both `k_a,k_s`→rail) | `−0.705` | `−0.705` | `1.813` (FROZEN, degree-0) | channel-SYMMETRIC (BH melt wall, `lattice-extreme:37` `[canon]`); `c_P→0` |
| **bulk-only** (`k_a`→rail, `k_s` full) | `−0.512` | `−0.136` | `0.769` (shifted) | channel-ASYMMETRIC `3.8×` (electron surrogate, `electron-bh-iso:26` `[canon]`); `c_P` finite AT `S_RAIL=0.03` ONLY — **`Γ_bulk→−1`, `c_P→0`, `c_S` finite at deep rail** (Findings 0/5; `k_s` does NOT prop `K` — it stores 0 dilatation energy) |

**★Review-repair rail-depth ladder (this table's bulk-only row scanned to `S→0`; Findings 0/5 CRITICAL):** `S_RAIL = 0.03 / 0.003 / 1e-4 / 1e-6 / 0` ⇒ bulk-only `Γ_bulk = −0.512 / −0.784 / −0.956 / −0.996 / −0.9997`, `c_P = 0.167 / 0.063 / 0.012 / 0.0012 / 0.00007 → 0 ∝ √k_a`, while `c_S = 0.218 / 0.183 / 0.177 / 0.177 / 0.177` stays FINITE and `Γ_shear` saturates `−0.236` (94% shear transmission). The single shipped depth (`0.03`) is a `3×` impedance drop (`Γ=−0.51`), NOT the canon wall; the canon `Γ_bulk=−1` wall (`:26`) IS the `S→0` limit, realizable with shear alive.

**★STOP-gate (pulse-reflection SIGN):** rail cages → `Γ_disp` SIGN `+1` (free surface ⇔ `Γ_stress=−1` pressure-release ⇒ compression moment CANCELS); the CLAMPED control → `−1` (rigid ⇔ `Γ_stress=+1` ⇒ compression moment DOUBLES). **OPPOSITE sign CONFIRMED (both P and S) ⇒ the mirror realization is VALID ⇒ lane proceeded.** (Pulse magnitudes are wavelength-artifact contaminated by the finite wall thickness; the impedance S-matrix is the clean magnitude.)

**★Fork-W finding [INVERTED by review-repair — Findings 0/5].**

🔴 **The original "a perfect `Γ_bulk=−1/Γ_shear=0` wall is NOT cleanly realizable" finding (below the strike) is INVERTED (Rule 12, body preserved).** Railing `k_a` alone DOES drive `Γ_bulk → −1` (`−0.512→−0.9997` as `S_RAIL 0.03→0`) with `c_S` FINITE (`0.177`) and `Γ_shear` saturating at `−0.236` (NOT growing to `−1`; the shipped `−0.14→−0.22` was two shallow points, not the limit). So the **canon bulk-only electron wall (`:26`, `Γ_bulk=−1`, shear un-melted) IS constitutively realizable** in the rank-2 bond model at `S→0`: the asymmetry paradox resolves TOWARD canon at the constitutive/impedance level. `k_s` does NOT prop up `K` (under uniform dilatation the shear projector `(I−d̂⊗d̂)du = 0` — zero dilatation energy). What REMAINS open is the physical MODE/GEOMETRY mechanism that a Cosserat-winding knot core uses to grade `k_a` alone (the derived `S(A)` kernel still grades both channels together — the un-derived A1⊥T2 sector-ownership question). **Routed to Grant; NOT picked; verdict-controlling (§7) ⇒ REOPENED.**

~~The srs channels SHARE `k_s`: a PERFECT `Γ_bulk=−1/Γ_shear=0` wall is NOT cleanly realizable — railing `k_a` alone leaves `Γ_bulk` saturating near `−0.5…−0.8` (never `−1`, because `k_s` props up `K`) while `Γ_shear` grows too (`−0.14→−0.22` as rail deepens). The canon bulk-only electron wall (`:26`) is only PARTIALLY constitutively realizable.~~ [RETRACTED — realizable at deep rail; see above.]

---

## §4 — LEG 4 (moving single cage) `[derived; declared LINEAR-translation fallback]`
Single cage, interior energized (seed, FREE dynamics, NO pin), the CONSTITUTIVE GRADE translated linearly (moving-`C(x,t)`, bond tensors recomputed each step); measure the far-field partition + whether the moving cage CARRIES the interior energy. **Declared fallback (prereg §3):** orbital motion of a single cage is infeasible for a clean far-field on `L=20` — linear translation is the scoped fallback. Results: bulk-only far-field `f_long = 0.747` (compression-dominated, MATCHES the static Leg-5 `~0.77` — motion adds no new suppression), symmetric `f_long = 0.623`; **carry fraction `≈ 2.37` (bulk-only) / `2.33` (symmetric) — the cage does NOT cleanly transport the interior energy** (`carry≈1` would be clean transport; `>1` = the interior disperses/radiates rather than being dragged). This is the moving-mass infeasibility (electron-lock arc) made concrete: a moving constitutive grade does not coherently carry the trapped compression energy on the lossless lattice. Consistent with the static Leg-1/5 picture; the homogenization (Leg 6) carries the moving-ensemble extrapolation.

---

## §5 — LEG 5 (N-scaling — the verdict-controlling measurement) `[derived]`
`N = 1,2,4,8` energized caged cores (dipole-free two-lobe ensemble), seeded (FREE dynamics, NO pin), net far-field compression at a shell enclosing all `N`.

| `N` | `ρ_N` bulk-only (caged/uncaged compression) | `ρ_N` symmetric |
|---|---|---|
| 1 | `0.485` | `0.077` |
| 2 | `0.523` | `0.104` |
| 4 | `0.572` | `0.107` |
| 8 | `0.583` | `0.104` |

**Discriminator (frozen): `ρ_N → 1` = coarse-grained texture (BIN-1) vs bounded `<1`/falling = per-core cage survives (BIN-2).** At the **shipped shallow depth `S_RAIL=0.03` (un-frozen)** bulk-only `ρ_N` rises (`0.485→0.523→0.572→0.583`), `p_caged = 1.45 ≈ p_uncaged = 1.37`, `κ_ensemble²(N=8, bulk-only) ≈ 0.020 ≫ κ_max² = 1.3×10⁻⁴`.

🔴 **REVIEW-REPAIR (2026-07-20, PR#770; Findings 2/6/9 — the original "RISING toward 1 / ROBUST across rail depth / all RISING, pressure-tested" claim below the strike is RETRACTED, body preserved per Rule 12).** The "ROBUST across rail depth" clause was a HARD-CODED prose string in the driver's JSON note (`constituent_cage_ensemble.py:783`) written unconditionally while the run evaluated ONLY `S_RAIL=0.03` — a reconcile-don't-declare failure (no scan was ever run or shipped). The COMPUTED `S_RAIL` ladder (`review_repair_rail_depth_scan`, this repair): bulk-only `ρ_N` does NOT rise toward 1 across depth — it PLATEAUS `~0.3` and goes flat-to-**FALLING** `N4→N8` at `S_RAIL=0.003` (`0.243→0.280→0.306→0.305`) and `1e-4` (`0.216→0.255→0.277→0.273`). So the "RISING toward 1" asymptote is a shallow-rail, single-depth read; the deeper (more physical, near-yield) rail forces the per-core-cancellation-survives (BIN-2-shape) plateau the frozen discriminator (`prereg §5`) calls "`ρ_N` roughly constant `<1`". The kill-line magnitude claim is unchanged (`κ_ensemble² ≫ κ_max²` at every measured depth, `78–152×`), but it is a magnitude fact, NOT the asymptote.

~~bulk-only `ρ_N` **RISES** (`0.485→0.583`) toward the uncaged texture ⇒ BIN-1; the cages wash out as they pack … **ROBUST across rail depth** (`S_RAIL 0.03/0.01/0.003` all RISING; deeper rail lowers the prefactor `ρ_N(N=1): 0.485→0.243` but the RISING trend persists).~~ [RETRACTED per the review-repair above — the "all RISING / persists" read is false at deeper rail.]

SHEAR is SUPPRESSED to `0.60×` uncaged at `N=8` (`0.23×` at `N=1`) for bulk-only — NOT surviving "at the uncaged rate" (Finding 7; the frozen consistency gate wording is not met — see §7). `κ_ensemble²(N=8, bulk-only) ≈ 0.020` is a COMPOSITE `ρ_N·κ_env²`, NOT the frozen "measured-directly" `F_bulk/F_shear` (the direct far-shell `shell_kappa2 = 3.31`, `kr~O(1)` seed-dominated — Finding 11). The symmetric wall's flat `ρ_N~0.10` ALSO kills shear (wall-class artifact, fenced).

---

## §6 — LEG 6 (homogenization — the `N→10⁵⁷` extrapolation) `[derived]`
Full in `..._derivation.md §3`.

🔴 **CONCLUSION-WRONG (2026-07-20, PR#770; Findings 0/5 CRITICAL — the Leg-6 homogenization is INVERTED; the argument below the strike is RETRACTED, body preserved per Rule 12).** The load-bearing claim was "a space-filling bulk-only-caged medium has FINITE `c_P,eff` (propped by `k_s`) ⇒ you CANNOT open a compression-only stop ⇒ BIN-2 forbidden." **The lane's OWN `run_c2_speeds`, run one decade past its own scan, says the opposite:** railing only `k_a` (`k_s` full) drives `c_P → 0 ∝ √k_a` with NO floor — `Γ_bulk = −0.512 → −0.784 → −0.956 → −0.996 → −0.9997` as `S_RAIL = 0.03 → 0.003 → 1e-4 → 1e-6 → 0` — while `c_S` stays FINITE at `0.177` (62% of cold, `Γ_shear` saturates `−0.236`). The algebra: under uniform dilatation every bond strain `du_b = ε·d_b ∥ d̂_b`, so `(I−d̂⊗d̂)du = 0` and the shear springs `k_s` store EXACTLY ZERO dilatation energy — `k_s` does NOT prop up the bulk modulus `K`; `K_eff ∝ k_a → 0`. So a **channel-asymmetric compression-only stop IS constitutively realizable** (`c_P→0`, `c_S` finite) — this is precisely the canon bulk-only electron wall (`electron-bh-isomorphism.md:26`, `Γ_bulk=−1`, shear un-melted), realizable in the rank-2 bond model in the `S→0` limit. The "compression can't be stopped without killing shear ⇒ BIN-2 forbidden" chain is DEAD; the "`K=2G` grade-lock at the constituent level" headline does NOT survive; the compression outcome is rail-depth-conditional ⇒ Fork-W is verdict-controlling ⇒ REOPENED (§7). The "0.17 finite / saturates `−0.5…−0.8` NOT `−1`" read was a scan-truncation artifact — the driver evaluated ONE rail depth (`S_RAIL=0.03`, module constant); "saturates" was asserted, never scanned.

~~Constituents are SPACE-FILLING (`φ→1`), NOT dilute Lloyd mirrors (`φ→0`). A space-filling bulk-only-caged medium has FINITE `c_P,eff` (lattice-validated Leg 3: `0.17`, propped by `k_s`) ⇒ the coarse-grained mass quadrupole radiates ⇒ BIN 1. Freezing compression (`c_P,eff→0`) needs the symmetric rail, which also freezes shear (kills the GW) ⇒ BIN 3, forbidden. **This is #767's `K=2G` grade-lock surviving one level down.** Honest scope: the dilute deep-quasistatic (`k·r_core→0`) single-core Lloyd seal is not simulated (lattice `k·r_core~O(1)`) — but that is the non-physical (dilute) limit; the space-filling argument (finite `c_P,eff`) is `k·r_core`-independent and lattice-validated.~~ [RETRACTED — the finite-`c_P,eff`/`k_s`-props-`K` premise is false; see the review-repair above. The space-filling regime scoping (`φ→1` at nuclear density; packing arithmetic `φ≈0.4–0.7` at saturation — derivation §3 Step 1, Finding 4) is unaffected, but it no longer excludes BIN-2, because the space-filling bulk-only rail ALSO closes `c_P` in the deep limit.]

---

## §7 — FROZEN-BIN VERDICT + consistency gate + anti-seduction + asymmetry paradox

| Leg | Frozen outcome | Decisive step |
|---|---|---|
| **0 — convention/sign** | rail = pressure-release (`Γ_bulk=−1`); clamped = rigid (`Γ=+1`) | STOP-gate opposite-sign CONFIRMED ⇒ mirror valid |
| **1 — charged-line** | bulk-only core RETAINS ~65% exterior compression charge | charged-line (BIN-1 shape); `k·r_core~O(1)` scope |
| **2 — seal** | empty cage sources nothing; bulk-only single-cage seal ~25% | partial per-core seal (consistent with `Γ_bulk=−0.51`) |
| **3 — wall S-matrix** | symmetric channel-symmetric; bulk-only asymmetric, `c_P` finite ONLY at shallow `S_RAIL=0.03` (`c_P→0`, `Γ_bulk→−1` at deep rail — Finding 0) | STOP-gate PASS; **Fork-W: a channel-asymmetric bulk-only wall IS realizable at deep rail (`c_S` finite)** |
| **4 — moving cage** | far-field `f_long=0.75` matches static; carry `2.37` (no clean transport) | moving-mass infeasibility made concrete; consistent w/ static |
| **5 — N-scaling** | `ρ_N` rises `N2→N8` ONLY at `S_RAIL=0.03`; at deeper rail PLATEAUS `~0.3`, flat-to-falling `N4→N8` (Findings 2/6/9) | rail-depth-conditional; the "RISING toward 1 / ROBUST across rail depth" read RETRACTED |
| **6 — homogenization** | ~~space-filling `c_P,eff` FINITE (`k_s`-propped)~~ **CONCLUSION-WRONG**: `c_P→0 ∝ √k_a` with `c_S` finite ⇒ compression-only stop IS realizable (Findings 0/5) | ~~`K=2G` grade-lock~~; the "BIN-2 needs `c_P→0`=kills shear=forbidden" chain is DEAD ⇒ REOPENED |

🔴 **RE-BANKED (2026-07-20, PR#770; the original BIN-1-CLEAN verdict below the strike is CONCLUSION-WRONG on its load-bearing Leg-6 leg — retained per Rule 12).**

**★Overall frozen-bin verdict [RE-BANKED]: WALL-CLASS / RAIL-DEPTH-CONDITIONAL → REOPENED.** The shipped shallow-rail (`S_RAIL=0.03`, un-frozen) measurements land BIN-1-direction (`ρ_N` rises `N2→N8`, `κ_ensemble² ≫ κ_max²`), but the load-bearing Leg-6 exclusion of BIN-2 is refuted: (a) the bulk-only cage DOES close compression in the deep homogenized limit (`c_P→0 ∝ √k_a`, `k_s` stores ZERO dilatation energy) with `c_S` finite — the canon channel-asymmetric wall IS realizable; (b) `ρ_N` does NOT rise robustly across rail depth — it plateaus `~0.3` (falling `N4→N8`) at deeper rail. The fork #767 left open REMAINS OPEN; the decisive adjudicator is the deep-rail frozen-first program (§8.2). Do NOT flip to BIN-2 — the deep-rail data is review-produced, not frozen-criteria output.

~~**★Overall frozen-bin verdict: BIN 1 — ENSEMBLE-RADIATES (BIN-1-CLEAN).** The constituent-cage fork closes to route (ii): the coarse-grained uncaged mass texture radiates at the q1 partition (`κ_ensemble²≈0.020 ≫ κ_max²`, `~150×`). The image-cancelled-sum route (i) is NOT taken: (a) the bulk-only cage does not seal compression in the space-filling homogenized limit (`k_s` props `K` ⇒ `c_P,eff` finite), (b) aggregation ERODES even the partial per-core seal (`ρ_N` rising, robust across rail depth), (c) both channels radiate above the kill line.~~ [RETRACTED — (a) is CONCLUSION-WRONG (Findings 0/5), (b) is rail-depth-conditional (Findings 2/6/9); only the shallow-depth magnitude survives.]

**★Consistency gate: RE-GRADED PASS → INAPPLICABLE/FAIL-AS-WORDED (Finding 7).** The frozen gate (`prereg §2`) requires shear to "survive at the uncaged rate"; the bulk-only data SUPPRESS shear to `0.60×` (`N=8`) / `0.23×` (`N=1`) — the frozen wording is NOT met, so "PASS" over-read it. On the (now-reopened) verdict path the gate is moot ("both channels radiate" cannot fire when `ρ_N≈0.58`), i.e. it was a checklist not a live gate. What DOES survive on independent legs: the symmetric-vs-bulk-only shear contrast (`0.027–0.119` vs `0.226–0.599`, `5–8×`) supports the wall-class FENCE.

**★Wall-class / asymmetry paradox (Fork W): INVERTED — verdict-controlling again.** The substrate does NOT refuse the canon wall: railing `k_a` alone (the bulk-only surrogate for `electron-bh-isomorphism.md:26`) drives `Γ_bulk → −1` with `c_S` FINITE as `S_RAIL→0` — the channel-asymmetric bulk-only wall IS constitutively realizable in the rank-2 bond model (the "`k_s` props up `K`, no clean bulk-only wall" framing was a scan-truncation artifact — one rail depth evaluated). So the asymmetry paradox resolves TOWARD canon at the constitutive/impedance level: the rank-2 bond model CAN express the wall. What REMAINS open is the physical MODE/GEOMETRY mechanism — WHICH `(k_a,k_s)` grading a Cosserat-winding knot core actually presents (the un-derived A1⊥T2 sector-ownership question; the derived `S(A)` kernel still grades both channels together — derivation §2). Because the compression outcome FLIPS with rail depth in a verdict-controlling way, this is the frozen BIN-3/Fork-W condition ⇒ **routed to Grant; NOT picked; the verdict REOPENS.**

**★Anti-seduction fence check (both ways).** (i) Grant's ensemble walk WANTED CANCELS (BIN-2); the shallow-rail shipped legs do NOT cancel — but the deep-rail bulk-only wall DOES close `c_P` with shear alive, so BIN-2 is LIVE again, not excluded. (ii) The #761→#767 kill arc WANTED RADIATES (BIN-1); the original doc banked BIN-1-CLEAN on the falsified Leg-6 premise — exactly the completion-bias the fence warned of. The review-repair holds the doc to its OWN `run_c2_speeds` at its OWN stated `S→0` limit; the honest verdict is the reopened conditional, NOT a clean bank in either direction.

**Contradiction with the standing ruling (flag-don't-fix).** The Q1 ruling reverted to Reading-A (#761/#767). This lane's ORIGINAL claim — that the revert is "confirmed one level deeper (the constituent-cage route does not rescue Reading B)" — does NOT survive review: the constituent-cage BIN-2 route is LIVE again at deep rail, so this lane neither confirms nor refutes the revert; it REOPENS the constituent-level fork. Both the standing ruling and this reopening are surfaced with file:paths + verbatim content; neither reframed. Grant adjudicates; the auditor lands any port-register/ledger edit. This lane touched NO KB/tex leaf (fence held).

---

## §7.5 — Disclosed deviations + review-repair provenance (PR#770 maximum-stakes review)

This section discloses the review-repair (Rule-12 substitution, reconcile-don't-declare) applied 2026-07-20. The frozen prereg (`_prereg-FROZEN.md`) is BYTE-UNTOUCHED; the engine `src/ave` is BYTE-UNTOUCHED; no KB/tex/port-register/ledger leaf was touched.

**★Review-repair rerun of the shipped artifact (Findings 1/6/9 — the reconcile-don't-declare failure, named plainly).** The originally-shipped driver JSON (`constituent_cage_ensemble_results.json` at `fa59998a`) carried the string `"ROBUST across rail depth (S_RAIL 0.03→0.003 all RISING, pressure-tested)"` and `"shear_ratio≈1"` HARD-CODED into `l5['scaling']['note']` (`constituent_cage_ensemble.py:783`), written unconditionally while the run evaluated ONLY the module constant `S_RAIL=0.03` — no scan loop existed, no scan data shipped, and the JSON's own `shear_ratio` values (`0.226…0.599`) contradict `"≈1"`. That is a self-declared field no computation backed. The rail depth `S_RAIL=0.03` was NEVER frozen (grep of the prereg: `S_RAIL/0.03` appears only at the unrelated `κ_env²=0.034` line), yet it controls the extrapolated conclusion. **Repair:** the fabricated string is REMOVED from the driver's JSON-writing path; a `review_repair_rail_depth_scan` key is added that COMPUTES the `S_RAIL` ladder from the shipped `run_c2_speeds` + `leg5_ensemble_scaling`; the driver was re-run (deterministic). The reran JSON supersedes the original — provenance stamped in `provenance.review_repair`. All originally-shipped legs are bit-identical (`s_rail` defaults to `0.03`); only the fabricated note, the verdict-editorializing trend label, and the added scan differ. Git preserves the original `fa59998a` JSON.

**★Finding 8 (omitted frozen deliverables).** Prereg `:106` froze "report the seed transverse fraction" and "Energy-drift `|ΔH/H|` reported as the driven-window swing"; the shipped driver computes neither (`hamiltonian()` at `:226` has zero call sites). Verdict-invariant (reruns bit-identical; the Fork-A BIN-4 guard's PRIMARY branch, non-monotone, was evaluable and did not fire), but the two frozen report-items were silently omitted — disclosed here.

**★Finding 10 (frozen-grid deviation, `r_meas`).** Leg 5 uses `r_meas=7.5` (driver `:454`) vs the frozen `r_meas=6` (prereg `:106`); the declared fallback was to REDUCE `N`, not enlarge the shell. Physically forced (the `±3.5` lobes with `r_cage=1.6` cores straddle an `r_meas=6` shell) and verdict-invariant (`ρ_N` is a caged/uncaged ratio at identical `r_meas` in both arms), but undeclared in the shipped doc — disclosed here.

**★Finding 11 (composite vs frozen "measured-directly" observable).** The headline `κ_ensemble²(N=8) ≈ 0.020` is a COMPOSITE `ρ_N·κ_env²` (`0.58×0.034`), NOT the frozen `κ_ensemble² = F_bulk/F_shear` "measured directly" (prereg `:43`/`:101`). The direct far-shell ratio is in the JSON (`leg5…bulk_only.8.shell_kappa2 = 3.31`; uncaged `3.40`) but never surfaced in the doc; it is `kr~O(1)` seed-dominated (`shell_f_long=0.768`, seed curl-free), which is the rationale for preferring the seed-cancelling `ρ_N`. The `≫κ_max²` verdict is invariant either way (`152×` composite; `25464×` direct) — disclosed as a deviation from the frozen "measured-directly" label.

**★Finding 12 (temporal currency).** The branch cited `master-equation.md:105`; #769 merged mid-lane (`~66 s` before the branch's final push) inserted a 2-line sector-dynamics tag, drifting the cited short-circuit line to `:107` on current main. Content byte-unchanged. The result + derivation cites are currency-bumped to `:107`; the FROZEN prereg (`:33/:86/:122`) still reads `:105` and is byte-untouched — its staleness is covered by this note (the prereg pins base HEAD `5287ef32` where `:105` is correct).

**★Finding 4 (packing arithmetic).** The `φ→1` space-filling premise (derivation `§3`) ships with no packing arithmetic. The honest arithmetic (`n_sat=0.16 fm⁻³` ⇒ spacing `1.84 fm`, `r_core=0.84–1.0 fm` ⇒ `φ≈0.40–0.67`) SUPPORTS near-space-filling at saturation, but rests on an unstated `r_cage ≈ nucleon-radius` identification no cited canon fixes — added as an explicit assumption tag in the derivation (`§3 Step 1`). Note this is now secondary: the space-filling limit no longer saves BIN-1 either (Findings 0/5).

---

## §8 — Calibration-vs-derived ledger + owed follow-ons

### §8.1 — Ledger (`consistency-vs-emergence` tags)
| Quantity | FORM | VALUE | Class |
|---|---|---|---|
| `κ_max² = 1.3×10⁻⁴` | `[derived]` `=δ_DP` | `[import]` (Kramer 2021) | manifestation given import (kill-line) |
| symmetric `c_P/c_S` frozen (degree-0) | `[canon-read]` (`electron-bh-iso:38`/PR521) | `1.813` | CONSISTENCY (`K=2G` GR-imported) |
| STOP-gate image-doubling opposite-sign | `[derived]` (impedance analogy + pulse sign) | — | the mirror-realization validity gate |
| ~~bulk-only channel-asymmetry + finite `c_P,eff`~~ **c_P→0 with `c_S` finite at deep rail** | `[derived]` (railed Bloch speeds, full `S_RAIL` ladder — this repair) | `Γ_bulk: −0.51→−0.9997` (`S_RAIL 0.03→0`); `c_P=0.17→0`, `c_S=0.177` finite | ~~load-bearing Leg-6 fact~~ **INVERTED (Findings 0/5): canon bulk-only wall realizable** |
| `ρ_N` rises `N2→N8` at `S_RAIL=0.03` ONLY; plateaus `~0.3` (falling `N4→N8`) at deep rail | `[derived]` (lattice N-scaling; the COMPUTED rail ladder, not "robust") | dimensionless | manifestation — rail-depth-CONDITIONAL (Findings 2/6/9) |
| `κ_ensemble²(N=8,bulk-only) ≈ 0.020` | `[derived]`-form COMPOSITE `ρ_N·κ_env²=0.034` (NOT the frozen direct `F_bulk/F_shear`; direct far-shell `shell_kappa2=3.31` — Finding 11) | `~150× κ_max²` (magnitude survives) | the ensemble far-field coupling (composite) |
| constituent bulk-only wall `(k_a,k_s)` route | `[assumed]`-surrogate — canon-asserted (`:26`); **impedance-realizable at deep rail (this repair)**; MODE/geometry mechanism un-derived | `Γ_bulk→−1`, `c_S` finite | ★Fork-W OPEN + verdict-controlling (routed to Grant) |
| pulsar exclusion (banked) | — | `[import]` (Weisberg-Huang; Kramer) | import (banked, #761/#767/q1) |

No emergence-class claim headlined. The deliverable [RE-BANKED] is the WALL-CLASS/RAIL-DEPTH-CONDITIONAL → REOPENED verdict: shallow-rail measurements land BIN-1-direction, but the Leg-6 exclusion of BIN-2 is refuted and the deep-rail data reopens the #767 fork. ~~the frozen-bin verdict (BIN 1) + `ρ_N` rising + `κ_ensemble²≈0.020 ≫ κ_max²`, riding on the space-filling homogenization~~ [the space-filling-homogenization leg is CONCLUSION-WRONG — the shallow-depth magnitude survives, the BIN-1-CLEAN closure does not].

### §8.2 — Owed follow-ons (fenced; NOT executed here — Rule 12)
1. **The #767 "structurally unreachable" strengthening is NOT earned** — the review-repair refuted the Leg-6 exclusion, so the constituent-cage fork does NOT close to BIN-1; the #767 §6.2-1 owed-driver is discharged as a REOPENING (the constituent-level fork is live), not a strengthening. The auditor lands no port-register strengthening on this basis; **this lane surfaces, does not land.** Grant-gated.
2. **★THE DECISIVE ADJUDICATOR = the DEEP-RAIL FROZEN-FIRST PROGRAM (owed follow-on).** A new prereg-frozen driver: (a) a frozen `S_RAIL` ladder to `1e-6` (the shipped `run_c2_speeds`/`leg5` ladder banked in this repair is REVIEW-PRODUCED, not frozen-criteria output); (b) a CONVERGED Leg-1 (the shipped `0.65` is unconverged — window halves swing `0.33→1.60`, Finding 3); (c) deep-rail N-scaling (does the `ρ_N~0.3` plateau hold or fall further as `N` grows?); (d) a shear-gate diagnosis (why `Γ_shear` saturates `−0.236` not `0`). NO rescue derivation for Reading B is minted (Rule 11/12; slot not refilled) — the reopening is a stated fork, not a new hypothesis.
3. **The Q1 revert + falsification-ledger promotion** were routed by #761/#767; this lane does NOT confirm the same consequence one level deeper (the constituent BIN-2 route is live again). Grant-gated; auditor lands.

---

> **Result-doc provenance.** Fired by Grant 2026-07-20 (`"lets finish the ensemble walk and fire it"` `[sic]`). Frozen prereg committed + pushed ALONE first (`b7a6ce46`); driver `research/drivers/constituent_cage_ensemble.py` (+ `_results.json`, white figure), `ave.core.*` read-only, engine byte-untouched, reruns bit-identical. All `[canon]` citations content-verified two-method at base `5287ef32`. **Attribution:** Grant-verbatim walk quote `[sic]`; the charged-line/graded-line formulation, wall-scan design, clamped-boundary control, and asymmetry-paradox naming are ORCHESTRATOR WALK-LEVEL candidates (this lane's derivation tested them). **★Verdict [RE-BANKED 2026-07-20, PR#770 maximum-stakes review-repair]: WALL-CLASS / RAIL-DEPTH-CONDITIONAL → REOPENED** — the shipped shallow-rail legs land BIN-1-direction, but the load-bearing Leg-6 exclusion of BIN-2 is refuted (the canon bulk-only wall IS realizable; `ρ_N` plateaus at deep rail), the #767 fork REMAINS OPEN, and the decisive adjudicator is the deep-rail frozen-first program (§8.2). The original `BIN-1-CLEAN` claim is preserved verbatim under its Rule-12 supersession banner (§0). Consequence routed to Grant/auditor; no leaf touched. Companions: the frozen prereg, the analytic legs (`..._derivation.md`), merged #767 (§3.1/§6.2-1), the walk RECORD (§6-3), q1 §2.2, the port register P9/Q1, and the docket continuation (`### ENTRY 2026-07-20-constituent-cage-ensemble`).
