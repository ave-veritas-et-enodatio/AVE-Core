# Constituent-Cage-Ensemble Adjudicator — FROZEN pre-registration (the open #767 fork: image-cancelled sum vs coarse-grained texture)

**Date:** 2026-07-20
**Class:** DERIVATION + lattice-derived research-driver (research-doc; **forms derived, values calibration/observation-imported and tagged; mints no `clm-`, propagates to no KB/tex leaf**). This is COMMIT 1 — the pre-registration ALONE, frozen and pushed before any derivation or driver code (the #761/#767 frozen-first discipline).
**Provenance:** Grant-fired 2026-07-20 (verbatim `[sic]`: `"lets finish the ensemble walk and fire it"`). THE verdict-controlling adjudicator of the fork left OPEN by merged **#767** (`research/2026-07-20_envelope-sector-reduction_result.md` §3.1 "the honest residual fork", §6.2-1 "owed follow-on", verdict **BIN-1-CONDITIONAL**). #767 confirmed the #761 carrier-level pulsar kill one level down (ENVELOPE-RADIATES) on the two branches it adjudicated (star-level saturated shell + smooth common uncaged source), but its maximum-stakes review demoted the verdict to CONDITIONAL: canon puts a **BULK-ONLY `Γ_bulk=−1` mirror at every constituent knot core** (`electron-bh-isomorphism.md:26`/`:36` `[canon]`), a channel-ASYMMETRIC structure of exactly the BIN-2 shape, whose ENSEMBLE aggregation no shipped leg tested. This lane resolves that fork.
**THE fork (verbatim from #767 §3.1):** does an ENSEMBLE of ~`10⁵⁷` BULK-ONLY-caged constituent compression cores present to the `2Ω` far field **(i)** a sum of image-cancelled core moments (each `∝(k·r_core)²`, channel-ASYMMETRIC — the LIVE BIN-2 route; grounds for re-opening the reverted Reading-B ruling), or **(ii)** the coarse-grained UNCAGED mass texture (radiates at the q1 partition — BIN-1 confirmed clean at every level)?
**Lane fences:** DERIVATION lane only. Engine `src/ave` **BYTE-UNTOUCHED** (imports read-only; all cage/ensemble dynamics live in the driver under `research/drivers/`). **No** `manuscript/` or `manuscript/ave-kb/` `.tex`/`.md` leaf edits; **no** port-register edit; **no** un-revert; **no** falsification-ledger edit — regardless of outcome. Every `[canon]` input content-verified two-method at base HEAD `5287ef32` (verify-before-cite). #767/#761 content cited from merged `origin/main`. Pulsar figures `[import]`-tagged.

> **FREEZE STATEMENT.** This document freezes: (i) the KILL-LINE `κ_max` recomputed from the banked q1 §2.2 numbers (§1); (ii) the four verdict BINS + the CONSISTENCY GATE + the ANTI-SEDUCTION FENCE (both ways) + the CLAMPED-BOUNDARY FENCE + the CONVENTION FREEZE (§2); (iii) the per-LEG frozen discriminators (§3, feasibility-assessed); (iv) the UNDETERMINED fork guard (§4). Nothing below §4 is a result — the derivation and driver land in later commits. **The verdict may cite ONLY the frozen criteria's outputs.**

---

## §0 — REGIME / SECTOR / PHASE-STATE header + substrate-native walk + CONVENTION FREEZE (fired before any cage code)

**MODE.** A non-relativistic compact binary (Hulse-Taylor B1913+16, `v/c~10⁻³`; double pulsar J0737-3039A/B, `v/c~2×10⁻³`) as a **source**. But the object under test is one level deeper than #767: each star is not a smooth mass-texture but an ENSEMBLE of ~`10⁵⁷` constituent solitons (nucleons/electrons), **each carrying its own `Γ_bulk=−1` bulk-only cage at its knot core** (`electron-bh-isomorphism.md:26` `[canon]`). Contrast column: the observed `Ṗ_b` matched to the GR shear-quadrupole `[import]`.

**REGIME.** Regime-I cold-linear far field (`V_GW/V_snap~10⁻²⁵`); **caged sources** — the cage is a real emergent boundary, realized here as a self-maintaining constitutive grade (a shell of bonds driven toward the rail), NOT a cold breathing monopole, NOT the #767 kinematically-pinned lump.

**PHASE-STATE.** Cold-reactive far field (Ax3-lossless-reactive), saturated cage shells (the `S(A)→0` rail at the cage contour). Far-field radiation is a legal Ax3 loss channel (port-not-valve).

**SECTOR.** Observed GW = **T2 transverse shear** at `c`. Channel under test = **A1 bulk/compression** (mass sector), radiative P-branch (`c_P/c_S≈1.71–1.90`). **Sector-ownership discipline (do NOT cross-wire):** A1 owns compression/mass/dilatation; T2 owns shear/GW; the `(2,3)` Cosserat winding owns charge/spin (`master-equation.md:20` `[canon]`). ★The A1⊥T2 question this lane must NOT assume away: WHICH impedances collapse at a Cosserat-winding knot core — bulk-only (canon `:26`) or shear too? — is itself part of the fork (§4, Fork W).

**A46 PHASE-SPACE-COORDINATE CHECK.** The corpus claim is a *channel-radiation* claim (does an ensemble of caged masses open a far-field COMPRESSION port?). The matching test coordinate is the **longitudinal (`∇·u`, radial) vs transverse (`∇×u`, tangential) displacement-energy partition of the RADIATED field**, and the static exterior dilatation `∇·u(r)` outside a cage — the same channel basis #761/#767 use, NOT a scalar `φ²` proxy, NOT lattice-Cartesian. `κ² ≡ F_bulk/F_shear` in that channel basis (§1). A46-clean.

**SUBSTRATE-NATIVE WALK (8-checkpoint, fired before scaffolding any cage primitive).**
1. **K4 connectivity.** Rule-14 reuse of the #767 / `srs_band_survey` rank-2 bond model on the chiral srs-z3 net (`ave.core.chiral_lattice._SRS_8A/_NN`; `Φ_b = k_a d̂⊗d̂ + k_s(I−d̂⊗d̂)`; derived `ρ*=9.77337` from `ν_Hill=2/7`). No new stencil; NOT a Cartesian Laplacian.
2. **Cosserat / channel basis.** Partition radiated + static fields into radial (longitudinal, P — compression/A1) AND tangential (transverse, S — shear/T2). S-branch survival is the consistency gate.
3. **Op14 saturation — the cage.** The cage is a shell where `S(A)=√(1−(A/A_yield)²)→0` grades the local bond stiffness toward the rail (`eq_axiom_4.tex:7` `[canon]`). Realized as a **static/moving constitutive grade on the bond tensors**, NOT a kinematic pin (the #767 lesson; see the clamped-boundary fence §2).
4. **Phase-space vs real-space (A46).** Observable = channel-basis dilatation/energy partition (above); NOT a `φ²` proxy.
5. **Checkpoint 8 (emergence/hosting) — the FALLBACK constraint.** A self-bound saturated soliton is INFEASIBLE on the lossless engine (electron-lock arc; #767 §0-ckpt5). So this lane does NOT plant a finished self-consistent soliton. The honest fallback: realize the cage by **constitutive grading of bond stiffnesses** (a self-maintaining rail shell), energize/move the caged interior by FREE dynamics (initial conditions + a moving grade), measure the exterior. Scoped as the constitutive-cage-coupling test, NOT envelope self-consistency (declared, §3).
6. **Checkpoint 10 (boundary-not-bulk).** The cage is a bounded operating-point BIAS on the coefficients (a graded shell, one node thick per the walk RECORD's emergent boundary), NOT a bulk force that detonates.

**★CONVENTION FREEZE (Leg 0 — pinned in the prereg; the mechanical↔electrical analogy).** The impedance analogy: **stress↔voltage, velocity↔current**, so `Z = stress/velocity = ρc`. Under it: `Z_bulk→0 ⇒ Γ_bulk=−1 = SHORT = pressure-release (free surface)` — canon `master-equation.md:105` `[canon]` (`Z=√(μ_eff/ε₀)→0` and `Γ→−1` "short-circuit"). A caged core (a `Z_bulk→0` rail shell) presents a **pressure-release / free-surface** boundary to the exterior compression channel ⇒ its image is INVERTED (Lloyd-mirror class) ⇒ far-field compression moment CANCELS. The OPPOSITE sign (the control): a **kinematically-clamped** boundary (`u=0`, a rigid wall) is an OPEN circuit (`Z→∞`, `Γ=+1`) ⇒ its image is SAME-sign ⇒ far-field compression DOUBLES.

**★FLAG-DON'T-FIX (the routed corpus tension — VERIFIED at base HEAD, two-method).** The orchestrator brief flagged an expected corpus contradiction: that `translation-circuit.md`'s mapping row reads *"Γ=−1 saturation TIR boundary → Open-circuit boundary"* (either the mobility-analogy dual or an error). **This lane checked it two-method at base HEAD `5287ef32` and the expected contradiction is NOT present** — the current `manuscript/ave-kb/common/translation-tables/translation-circuit.md:119` reads verbatim: *"**$\Gamma = -1$ saturation TIR boundary** | **Short-circuit ($Z\to0$) / Total reflection** … NOT the dielectric-yield boundary: $\tau_{yield}$ is the **electric** branch ($\varepsilon_{eff}\to0$, $Z\to\infty$, **open-circuit**, $\Gamma\to+1$)"*. That row maps `Γ=−1 → SHORT-circuit`, CONSISTENT with the convention freeze; the "open-circuit" in it is explicitly attributed to the OPPOSITE (electric `τ_yield`, `Γ=+1`) branch. So **no contradiction flag is routed to the vocabulary register** (there is nothing to reconcile; the corpus already agrees with the convention freeze). Disclosed as a deviation from the brief per flag-don't-fix + verify-before-cite; the auditor may confirm. (A DIFFERENT, already-tagged object — the geodynamo magnetopause `Γ=+1` = open-circuit = pressure-balance register, `geodynamo-vca-back-emf.md:18` — is not this row and is not in scope.)

**PRE-TEST PHYSICS CHECK (Rule 16; one plumber-physical question surfaced to Grant BEFORE the framing locks).** *Grant — plumber-physically: when a chunk of matter (an ensemble of ~`10⁵⁷` caged compression cores) is accelerated as a whole, does the surrounding vacuum's COMPRESSION channel see (a) each core's mass as a sealed pressure-release cavity (a moving bubble — displaces fluid incompressibly, added-mass reactive near-field, NO far-field compression at leading order = the P9 DM-halo object), so the far-field compression moment is the sum of image-cancelled cores (BIN-2); or (b) the coarse-grained mass texture between and across the cores (the inter-core medium compresses as the whole chunk accelerates), which no individual cage seals, so it radiates at the q1 partition (BIN-1)?* This is the fork the ensemble driver derives; the ontology is surfaced now, not after the run.

---

## §1 — THE KILL-LINE `κ_max` (recomputed from the banked q1 §2.2 numbers)

**Effective-coupling definition (frozen).** `κ` ≡ the effective far-field amplitude coupling of the orbiting ENSEMBLE's net mass moment into the **bulk/compression (P) channel**, normalized so the extra fractional orbital-energy-loss the pulsar over-determines is `ΔṖ_b/Ṗ_b = F_bulk/F_shear ≡ κ²`. (An amplitude coupling appears squared in a radiated-power ratio ⇒ `κ²` = the channel-basis flux ratio the driver measures directly — A46-matching.)

**The over-determined-`Ṗ_b` bound (q1 §2.1 `[canon]`).** The masses are pinned INDEPENDENTLY by the conservative post-Keplerian parameters; the radiative `Ṗ_b` is an over-determined check; any EXTRA radiative fraction is bounded by the fractional agreement `δ`: `κ² = F_bulk/F_shear ≤ δ`.

**Frozen numbers (`[import]`, WebFetch-verified in q1 §2.2; discharged 2026-07-20):**

| Bound | `δ` (frac.) | `κ_max² = δ` | `κ_max = √δ` | Source `[import]` |
|---|---|---|---|---|
| **Double pulsar J0737 (BINDING)** | `1.3×10⁻⁴` | **`1.3×10⁻⁴`** | **`0.0114`** | Kramer et al. 2021, PRX 11, 041050 (arXiv:2112.06795) |
| Hulse-Taylor B1913+16 (cross-check) | `1.6×10⁻³` | `1.6×10⁻³` | `0.0400` | Weisberg & Huang 2016, ApJ 829, 55 (arXiv:1606.02744) |

$$\boxed{\ \kappa_{\max}^2 = \delta_{\rm DP} = 1.3\times10^{-4},\qquad \kappa_{\max} = 0.0114\ \ \text{(double-pulsar, binding)}\ }$$

**The BIN-1 (uncaged) baseline this lane must move off of, or confirm.** #767 established the UNCAGED / coarse-grained far-field structural coupling `κ_env² = A_ang(c_S/c_P)⁵ = 0.034` (central; q1 O(1)-bracket `[0.0148, 0.1768]` ⇒ `114–1360×`; `262×` central). **The ensemble question is quantitative:** does caging the constituents drive the ensemble's net `κ_ensemble²` **down toward `κ_max² = 1.3×10⁻⁴`** (image-cancelled ⇒ BIN-2 candidate — it must clear the same intentionally-hard bar #767 quoted: `≥16–30×` amplitude / `≥250–900×` flux suppression below the uncaged default), or does it leave `κ_ensemble² ≈ κ_env² = 0.034` (coarse-grained texture ⇒ BIN-1, kill confirmed at the constituent level too)? `κ_ensemble²` is measured at the far shell (§3 Legs 4–5) and compared to BOTH `κ_max²` and the uncaged `κ_env²`.

---

## §2 — THE FROZEN BINS + consistency gate + anti-seduction fence + clamped-boundary fence

**BIN 1 — ENSEMBLE-RADIATES (BIN-1-CLEAN; the constituent fork CLOSES).** The caged ensemble's net far-field compression moment scales `∝N` at the q1 partition (`κ_ensemble² ≈ κ_env² ≫ κ_max²`) under **BOTH** wall classes ⇒ aggregation washes out per-core caging; the coarse-grained mass texture radiates; the #767 kill is confirmed at every level. *Consequence: the standing Reading-A exclusion + the reverted Q1 ruling STAND; the "structurally unreachable" strengthening #767's headline over-reached toward is now EARNED at the constituent level. Routed, not executed.*

**BIN 2 — ENSEMBLE-CANCELS (BIN-2-LIVE; grounds for a Reading-B re-open).** The caged ensemble's net far-field compression moment is **cancelled / sub-linear in N** (per-cage image cancellation survives aggregation, residual `∝(k·r_core)²`) under **at least the BULK-ONLY wall class**, AND the SHEAR channel stays at FULL coupling (consistency gate) ⇒ a DERIVED channel-asymmetric suppression structure EXISTS ⇒ grounds for a Reading-B re-open. **CONSEQUENCE ROUTED TO GRANT ONLY** — no port-register/ledger edit, no un-revert. This lane executes NOTHING; it surfaces the derived mechanism with `clm`/solidity inline and the driver's frozen outputs.

**BIN 3 — WALL-CLASS-CONTROLLED (the asymmetry paradox).** The COMPRESSION outcome (radiates vs cancels) FLIPS between the symmetric (full-rail, BH-class) and bulk-only (electron-class surrogate) wall classes in a verdict-controlling way ⇒ the verdict defers to the UN-DERIVED knot-core asymmetry mechanism. **State the paradox precisely with both canon cites** (grade-locked kernel `Γ_shear=Γ_bulk=−1` symmetric, `lattice-extreme-bh-rationality.md:37` `[canon]`, vs canon's bulk-only electron wall `Γ_bulk=−1` only, `electron-bh-isomorphism.md:26`/`:36` `[canon]`) and **route to Grant; do NOT pick a side.** *(Fenced from the consistency gate: the symmetric wall additionally killing SHEAR is EXPECTED wall-class data, NOT a flip — see the gate. BIN 3 fires only on a COMPRESSION-outcome flip.)*

**BIN 4 — UNDETERMINED.** An unforced verdict-controlling choice (which cage realization, which aggregation ansatz, source-vs-coefficient at the ensemble level, which `Γ` a knot core presents to each channel) not forced by the substrate ⇒ STOP, state the fork precisely (§4), do NOT pick by fiat.

**★THE CONSISTENCY GATE (frozen).** The observed SHEAR channel MUST stay at full coupling in every configuration that is read as a BIN-2 suppression. Operationally: in the BULK-ONLY (live BIN-2) wall class, the shear (transverse) far-field flux must survive at the uncaged rate (within the resolvable window), while compression is cancelled — that channel-asymmetry IS the BIN-2 signature. **★The symmetric (full-rail) wall is EXPECTED to kill BOTH channels** (`Γ_shear=Γ_bulk=−1`, canon `:37`) — that is DATA ABOUT THE WALL CLASS, **fenced from being read as a BIN-3 framing-falsification or a rescue.** A configuration that kills shear is flagged as a wall-class artifact, not a verdict input, UNLESS the compression outcome itself flips (then BIN 3).

**★ANTI-SEDUCTION FENCE, BOTH WAYS (frozen).** (i) Grant's ensemble walk WANTS CANCELS (BIN-2 — it rescues the framework via the P9/added-mass picture; the seductive direction). (ii) The completed #761→#767 kill arc WANTS RADIATES (BIN-1 — kill-completion at every level; the completion-bias direction). This lane is inside BOTH blast radii. **The verdict section may cite ONLY the frozen criteria's outputs** (§1 `κ_max`; the far-field `κ_ensemble²` and its N-scaling; the Leg-1 static exterior `∇·u`; the Leg-3 clamped-control sign; the consistency-gate shear survival). Every other measurement is POST-HOC CHARACTERIZATION, labeled and quarantined. The mechanism is NAMED with `clm`/solidity inline whichever way it lands.

**★THE CLAMPED-BOUNDARY FENCE (the #767 lesson, now a control).** **NO kinematic pinning anywhere on the SOURCE path.** Cages are realized by **CONSTITUTIVE GRADING ONLY** (bond stiffnesses → rail), free dynamics everywhere on the source/ensemble. The driver MUST include a deliberate **CLAMPED-CAGE CONTROL** (`u=0` pinned shell — the ONLY place a kinematic pin is allowed, and it is a NEGATIVE control, not a source) that demonstrates image-**DOUBLING** (the open/short sign demonstration: rail shell `Γ_bulk=−1` = pressure-release = cancellation vs clamped shell `Γ=+1` = rigid = doubling). **If the clamped control does NOT show the opposite sign to the rail cage, the mirror realization is BROKEN and the lane STOPS** (BIN 4, report the broken realization).

---

## §3 — FROZEN LEG CRITERIA (feasibility-assessed; refined and frozen before running)

**Cage realizations (frozen; the three wall classes + the control).** All are per-bond stiffness modifications of the Rule-14 rank-2 bond tensor `Φ_b = k_a d̂⊗d̂ + k_s(I−d̂⊗d̂)` on a shell of thickness `~1` node at radius `r_cage` around each core (the walk RECORD's "boundary layer 1 node thick" emergent contour). No kinematic pin on the source path.
- **SYMMETRIC (full-rail, BH-class):** on the shell, `Φ_b → s·Φ_b` with the rail factor `s = S(A_cage) → 0` scaling BOTH `k_a` and `k_s`. Canon: `Γ_shear=Γ_bulk=−1` (`lattice-extreme-bh-rationality.md:37` `[canon]`). Expected to kill BOTH channels (consistency-gate wall-class data).
- **BULK-ONLY (electron-class SURROGATE — the LIVE BIN-2 route):** on the shell, scale ONLY the central/compression component `k_a → s·k_a` (`s→0`) and KEEP `k_s` at full. **Declared explicitly: this SURROGATES the un-derived knot-core asymmetry mechanism** (canon `electron-bh-isomorphism.md:26`: electron confinement is "bulk-channel TIR", `Z_bulk→0 ⇒ Γ_bulk=−1`, shear NOT melted). The derived `S(A)` kernel grades BOTH channels (degree-0 ratio frozen, `:38`/PR#521) — there is NO derived mechanism producing a bulk-ONLY collapse; this leg hand-installs it as a surrogate and the asymmetry paradox (§4 Fork W, BIN 3) records the tension.
- **CLAMPED CONTROL (negative control ONLY):** the shell nodes are kinematically pinned `u=0` (rigid/open, `Z→∞`, `Γ=+1`). Used ONLY to demonstrate image-DOUBLING (opposite sign to the rail cage). NOT a source realization.

### Leg 0 — CONVENTION FREEZE + sign check (no verdict weight; orients Legs 1–5)
Frozen in §0 CONVENTION FREEZE. Deliverable (derivation doc): `dc_P/dA<0` (softens); `Z_bulk=ρc_P∝√S→0 ⇒ Γ_bulk→−1 = SHORT = pressure-release`; the rail shell is a pressure-release (free-surface) boundary to compression ⇒ inverted image ⇒ Lloyd cancellation candidate; the clamped shell is rigid/open ⇒ same-sign image ⇒ doubling. `[canon-read]`: `master-equation.md:105`, `lattice-extreme-bh-rationality.md:36-37`, `electron-bh-isomorphism.md:26,38`.

### Leg 1 — THE CHARGED-LINE TEST (first, cheapest, sharpest)
Construct a caged compression lump (rail-graded shell, interior energized by a curl-free dilatation seed as an INITIAL condition — free dynamics, no pin), let the exterior FREE-evolve, and measure the **static (DC / time-averaged over the reflection-free window) exterior dilatation `∇·u(r)` OUTSIDE the shell** (lattice bond-projected divergence `θ_i = Σ_{j~i}(u_j−u_i)·d̂_ij` at exterior nodes; report the exterior radial/tangential shell partition too). **Frozen discriminator:** exterior static `∇·u → 0` (graded/UNCHARGED line — a moving texture cannot drive the compression channel at leading order; the electrical analog `i = d(CV)/dt` with the compression "voltage" `V = ` exterior static `∇·u = 0`) **vs** `∝` interior energy (CHARGED line — the caged mass carries a static exterior compression tail; a moving texture pumps). Report the exterior `∇·u` for the bulk-only and symmetric cages, normalized to the UNCAGED lump's exterior `∇·u`. This is the source-vs-coefficient fork's direct lattice measurement, one level below #767's Leg C.

### Leg 2 — THE SEAL TEST
Far-field independence of interior content: **ENERGIZED vs EMPTY cage, identical exterior observables ⇒ the wall seals.** Same shell/geometry as Leg 1; one run with the interior seeded (energized), one with the interior cold (empty). **Frozen discriminator:** exterior `∇·u` and shell partition identical (within the reflection-free window) ⇒ the cage seals the interior from the exterior compression channel; any leakage quantified vs the `(k·r_core)²` expectation (report the leak fraction and the effective `k·r_core`).

### Leg 3 — WALL S-MATRIX, THREE CLASSES (incl. the STOP-gate clamped control)
Planar-slab geometry (a graded/pinned slab of bonds at `x=x_wall`): send an incident **P (curl-free)** pulse and an incident **S (divergence-free)** pulse toward the wall; measure the reflected `Γ_bulk` and `Γ_shear` (amplitude AND SIGN) for each of (i) SYMMETRIC full-rail, (ii) BULK-ONLY rail, (iii) CLAMPED control. Also measure the Lloyd far-field moment of a compression source placed adjacent to each wall (cancellation vs doubling). **Frozen requirements:** the BULK-ONLY surrogate must show `Γ_bulk ≈ −1` AND `Γ_shear ≈ 0`; the SYMMETRIC wall `Γ_bulk ≈ Γ_shear ≈ −1`; **★the CLAMPED control must show `Γ ≈ +1` (opposite sign to the rail cage's `−1`) and far-field DOUBLING — if it does not, the mirror realization is broken and the lane STOPS (BIN 4).**

### Leg 4 — MOVING SINGLE CAGE (FEASIBILITY-ASSESSED)
Translate/orbit ONE caged lump by moving the constitutive grading along the path (**time-dependent bond tensors — the moving-`C(x,t)` graded-line drive; NO pinned DOFs on the source**); measure the far-field compression/shear partition `κ² = F_bulk/F_shear` at the `r_meas` shell and the effective compression-moment class (volume/footprint-scaled vs interior-energy/mass-scaled); both wall classes; **spectral-speed windows only** (the #761 R2 lesson — never energy-crossing front-detects; window `[t_P,t_reflect)` from the cold Bloch `c_P,c_S`). Note the **P9 connection:** the near-field slosh IS the halo object; the RADIATIVE fraction at `kr~1` is the measured quantity (a moving pressure-release cavity should present an added-mass reactive near-field with suppressed far-field compression, per the P9 DM-halo reading — but that is the hypothesis under test, not assumed). **Feasibility (assessed):** the #767 `L=20` grid class is the baseline; a moving constitutive grade recomputes the bond tensors each step (same cost class as #767's `sat_on`). **If orbital motion of a single cage is infeasible on the box, linear translation + oscillation is the declared fallback** (scope: samples the moving-compression coupling, not a closed orbit; stated in the result).

### Leg 5 — SMALL-N ENSEMBLE SCALING (FEASIBILITY-ASSESSED — the verdict-controlling measurement)
`N = 1, 2, 4, 8` caged lumps arranged as a dipole-free NON-axisymmetric ensemble (two lobes at `±R`, `N/2` cores per lobe) orbiting/rotating as a RIGID ensemble at `Ω` (quadrupole at `2Ω`); measure the **net far-field compression moment / flux `F_bulk(N)`** at the `r_meas` shell. **Frozen discriminator:** `F_bulk(N) ∝ N` — tracks the UNCAGED ensemble's `∝N` growth (texture-class, uncancelled ⇒ BIN 1) **vs** sub-linear / geometric (per-core image cancellation survives aggregation ⇒ BIN 2). Report BOTH the absolute scaling exponent `p` (`F_bulk ∝ N^p`; `p≈1` = BIN-1, `p<1` sub-linear = BIN-2) AND the **caged/uncaged suppression ratio `ρ_N = F_bulk^caged(N)/F_bulk^uncaged(N)`** (removes the fixed lobe geometry: `ρ_N` roughly constant `<1` = per-core cancellation survives = BIN-2; `ρ_N → 1` as `N` grows = coarse-grained texture dominates = BIN-1), and `κ_ensemble²(N) = F_bulk/F_shear` vs `κ_max²` and the uncaged `κ_env²`. **Both wall classes if compute allows; the BULK-ONLY class is the PRIORITY** (it is the live BIN-2 route). **Feasibility (assessed):** `N=8` (two lobes of 4) on `L=20` requires the ensemble + `r_meas` shell to fit inside the reflection-free box; if the `N=8` ring does not fit, the declared fallback is `N∈{1,2,4}` with the extrapolation scope stated (never silently reduce). Rigid rotation is the priority; if a full orbit is infeasible, a rigid linear translation/oscillation of the ensemble is the declared fallback.

### Leg 6 — ANALYTIC HOMOGENIZATION (derivation doc; carries the `N→10⁵⁷` extrapolation)
The effective-medium derivation (sub-wavelength stub-loaded network: renormalized bulk/shear constants + effective compression source density for a medium loaded with `Γ_bulk=−1` cage inclusions). **Frozen deliverable:** state EXPLICITLY which lattice legs validate which analytic steps (e.g. Leg 3 validates the single-inclusion `Γ`; Leg 5 validates the small-`N` aggregation law the homogenization extrapolates), and **what the extrapolation to `N~10⁵⁷` CANNOT establish** from an `L=20` lattice (honest scope: `k·r_core~10⁻²⁵` in the real system vs `O(0.1–1)` on the lattice — the deep quasi-static / long-wavelength limit is reached analytically, not simulated).

**Grid FROZEN (Leg-C parity):** `L=20` supercell baseline (`64000` sites), `r_meas=6` cells (Legs 1–5, unless the `N=8` fit forces the declared fallback), `dt=0.2·dt_CFL`, seed/texture `∇×u=0` by construction (report the seed transverse fraction), reflection-free spectral Poincaré window from the cold Bloch `c_P,c_S`. **Determinism (frozen):** fixed seed; reruns bit-identical. Energy-drift `|ΔH/H|` reported as the driven-window swing with the OPEN-system caveat (the #767 R4c lesson — a moving constitutive grade pumps work; determinism is the shipped stability check).

---

## §4 — THE UNDETERMINED FORK GUARD (state precisely; do NOT pick by fiat)

**Fork A — aggregation (the verdict-controlling one).** Does the per-core image cancellation (Leg 3 single-inclusion `Γ_bulk=−1`) SURVIVE aggregation into a coarse-grained ensemble (Leg 5 `N`-scaling), or does an un-cancelled coarse-grained compression texture emerge between/across the cores that no individual cage seals? **Frozen resolution rule:** Leg 5's `F_bulk(N)` scaling exponent `p` + the caged/uncaged ratio `ρ_N` decide it empirically; if the small-`N` lattice trend is monotone and clean it forces a bin; if the trend is non-monotone or within the drift floor, that is BIN 4 (state the fork; the homogenization Leg 6 carries the extrapolation with declared scope).

**Fork W — which `Γ` a knot core presents to each channel (the ASYMMETRY PARADOX — BIN 3 if it controls the verdict).** The derived `S(A)` kernel grades bulk AND shear TOGETHER (grade-locked, degree-0 ratio frozen — `electron-bh-isomorphism.md:38`/PR#521; `Γ_shear=Γ_bulk=−1` at the melt wall, `lattice-extreme-bh-rationality.md:37`) ⇒ SYMMETRIC wall. But canon ASSERTS the electron/constituent wall is BULK-ONLY (`Γ_bulk=−1`, shear un-melted — `electron-bh-isomorphism.md:26`/`:36`) ⇒ channel-ASYMMETRIC. **There is NO derived mechanism in the corpus for a bulk-only collapse via saturation grading** — the bulk-only wall is a canon assertion whose grade-mechanism is un-derived; the BULK-ONLY leg SURROGATES it. **Frozen resolution rule:** if the COMPRESSION outcome (radiate vs cancel) is the SAME under both wall classes, the paradox does not control the verdict (report it, proceed on the shared outcome). If the compression outcome FLIPS between wall classes, that is BIN 3 — state the paradox with both canon cites and route to Grant; do NOT pick which wall is physical (that is the un-derived A1⊥T2 sector-ownership question — which impedances collapse at a Cosserat-winding knot core).

**Fork C — source vs coefficient at the ensemble level.** Does an orbiting caged mass sit on the RHS of the exterior compression wave equation (a moving compression source ⇒ radiates ⇒ BIN 1) or in its COEFFICIENTS (a moving pressure-release cavity = added-mass reactive near-field, the P9 halo ⇒ suppressed ⇒ BIN 2)? **Frozen resolution rule:** Leg 1 (static exterior `∇·u`) + Leg 4 (moving single cage far-field) decide it; the Leg-1 charged-vs-uncharged discriminator is the leading-order test (`V=0 ⇒ i=V·dC/dt=0`); if both orderings are self-consistent and give different bins, BIN 4.

---

## §5 — Calibration-vs-derived ledger (tags frozen) + owed follow-ons fence

**Ledger tags (`consistency-vs-emergence`, frozen).** `κ_max` is `[derived]` from `[import]` pulsar `δ` (MANIFESTATION given the imports). The cage `Γ_bulk=−1 = pressure-release` and the image-inversion/doubling signs are `[derived]` (theorem of the `Z_bulk→0` boundary + the mechanical impedance analogy) `×` `[canon-read]` (`master-equation.md:105`; `electron-bh-isomorphism.md:26`). The bulk-only wall's channel-asymmetry is `[assumed]`-surrogate (un-derived; canon-asserted `:26`). The `c_P/c_S` speeds are CONSISTENCY-class (`K=2G` GR-imported, PR#261). Leg-5 `F_bulk(N)` scaling + `ρ_N` are `[derived]` (lattice-measured, dimensionless). **No emergence-class claim headlined.** The deliverable is the frozen-bin verdict + the ensemble `κ_ensemble²(N)`/`ρ_N` vs `κ_max²` and the uncaged `κ_env²`, riding on the derived cage physics + lattice measurement, not a hidden calibration.

**Owed follow-ons (fenced; NOT executed here — Rule 12; slot NOT refilled with an assertion).**
1. If BIN 2: the derived channel-asymmetric suppression mechanism becomes a NEW derivation with its own version + verification chain; the re-open of the reverted Q1 ruling is Grant's alone; the auditor lands any port-register/ledger edit. This lane executes none — it surfaces the mechanism + driver outputs and routes to Grant.
2. If BIN 1: the standing Reading-A exclusion + reverted ruling stand; #767's "structurally unreachable" strengthening becomes EARNED at the constituent level; the auditor may land the strengthening — this lane surfaces, does not land.
3. If BIN 3: state the asymmetry paradox with both canon cites; route to Grant; no side picked; the A1⊥T2 knot-core sector-ownership question is flagged for adjudication.
4. If BIN 4: the fork is stated; corpus + Grant consulted before any methodology pivot (Rule 16); no fiat pick.
5. The `master-equation.md:20` sector-ownership wording tightening (routed by #761/#767) — unchanged; this lane relies on the physical (acoustic compression) identification, which stands.

---

> **Pre-registration provenance.** Fired by Grant 2026-07-20 (`"lets finish the ensemble walk and fire it"` `[sic]`). This is COMMIT 1 — the prereg ALONE, frozen and pushed before any derivation or driver (the #761/#767 frozen-first discipline). All `[canon]` citations content-verified two-method at base HEAD `5287ef32` (verify-before-cite); #767/#761 content from merged `origin/main`; pulsar figures `[import]`, WebFetch-verified in q1 §2.2. Kill-line `κ_max=0.0114` (`κ_max²=1.3×10⁻⁴`, double-pulsar binding) recomputed from the banked q1 §2.2 numbers by this lane's driver, not chat. **Attribution:** Grant-verbatim walk quote `[sic]`: *"lets finish the ensemble walk and fire it"*; the charged-line / graded-line formulation, the wall-scan design, the clamped-boundary control, and the asymmetry-paradox naming are **ORCHESTRATOR WALK-LEVEL candidates** (labeled; this lane's derivation may refute them). Mints no `clm-`; propagates to no leaf; engine byte-untouched; port-register/ledger untouched regardless of outcome. Companions: merged **#767** (`research/2026-07-20_envelope-sector-reduction_result.md` §3.1/§6.2-1), the walk RECORD (`research/2026-07-20_envelope-boundary-walk_RECORD.md` §6-3), the q1 hardening (`research/2026-07-20_q1-pulsar-hardening.md` §2.2), the port register (`manuscript/ave-kb/common/port-register.md` P9/Q1), and the docket continuation (`### ENTRY 2026-07-20-constituent-cage-ensemble`).
